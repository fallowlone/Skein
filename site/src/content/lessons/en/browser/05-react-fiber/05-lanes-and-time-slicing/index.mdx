---
concepts:
- lanes
- priority-scheduling
- time-slicing
- useTransition
- useDeferredValue
- MessageChannel
- INP
deepensInto:
- 06-bailout-and-memoisation
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 01-reconciler-overview
- 03-render-and-commit-phases
slug: 05-lanes-and-time-slicing
sources:
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useDeferredValue
- https://github.com/facebook/react/blob/main/packages/react-reconciler/src/ReactFiberLane.js
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: React 18 assigns every update a lane (priority bucket). Time-slicing breaks
  render work into 5 ms slices. useTransition and useDeferredValue put expensive updates
  in the transition lane so keystrokes can interrupt them.
title: Priority lanes, time-slicing, and useTransition
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Pulse from "~/components/diagram/Pulse.astro";

<Hook>
You wrap a slow list filter in `startTransition`. Typing stays instant, the list catches up when React has spare time, and a spinner signals the wait. Without it, every keystroke triggers a 50 ms blocking render. The mechanism that makes this work is React's priority lane system.
</Hook>

<Crux>Lanes assign priority buckets; time-slicing yields every 5 ms; transitions put work in an interruptible lane so input stays fast.</Crux>

<Explanation>

**Priority lanes.** React 18 assigns every update a *lane* — a priority bucket. The lane model (a bitmask of 31 lanes) lets React work on high-priority updates first and defer low-priority ones. The important tiers:

- **Sync / discrete:** A click, a keypress. React renders it immediately and blocking, because the user is waiting on that exact interaction.
- **Default:** Ordinary state updates with no transition wrapper.
- **Transition:** Set via `useTransition` or `useDeferredValue`. Used for updates the user is not directly waiting on — filtering a big list, navigating to a new view. These can be interrupted by anything more urgent.
- **Idle:** The lowest. Background work the app can do whenever the main thread is free.

When a keypress arrives mid-transition-render, React abandons the in-progress transition render, processes the keypress at high priority, and restarts the transition afterward.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Lane priority tiers</div>
  <div class="flex flex-col gap-1 text-xs">
    <div class="flex items-center gap-2">
      <span class="w-28 text-right font-semibold text-red-700 dark:text-red-400">sync / click</span>
      <span class="flex-1 rounded bg-red-100 dark:bg-red-950/30 px-2 py-1">Renders immediately, blocking — user is waiting</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-28 text-right font-semibold text-orange-700 dark:text-orange-400">default</span>
      <span class="flex-1 rounded bg-orange-100 dark:bg-orange-950/30 px-2 py-1">Normal setState — scheduled soon, not blocking</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-28 text-right font-semibold text-blue-700 dark:text-blue-400">transition</span>
      <span class="flex-1 rounded bg-blue-100 dark:bg-blue-950/30 px-2 py-1">startTransition / useDeferredValue — interruptible, time-sliced</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-28 text-right font-semibold text-gray-500">idle</span>
      <span class="flex-1 rounded bg-gray-100 dark:bg-gray-800/50 px-2 py-1">Lowest — runs only when nothing else is pending</span>
    </div>
  </div>
</div>

**Time-slicing: how the render phase yields.** A long synchronous render blocks input — the exact failure mode the event-loop piece described. React's scheduler breaks the render phase into slices. After about 5 ms of reconciliation work, the scheduler checks `shouldYield()`; if true, it stops, posts a continuation task via `MessageChannel` (the cross-browser way to schedule a task with no clamping), and lets the browser run input handlers and rendering. The continuation task resumes the render from the stored fiber pointer.

A 50 ms render becomes ten 5 ms slices interleaved with the browser's own work, and input stays responsive throughout. This only applies to *concurrent* renders — transitions and other non-urgent updates. A synchronous-priority update (discrete input) still renders in one blocking pass, because making the user wait for a yield would defeat the purpose.

<FlowDiagram
  label="A concurrent render is split into roughly five-millisecond units; after each unit React yields to the browser so input and painting can run, then resumes the next unit. A higher-priority keystroke interrupts the slices entirely and is handled first."
  hue="var(--d-frontend)"
  nodes={[
    { id: "slice1", label: "Render unit", sub: "~5 ms", col: 0, row: 0 },
    { id: "yield", label: "Yield", sub: "input + paint", col: 1, row: 0 },
    { id: "slice2", label: "Render unit", sub: "~5 ms", col: 2, row: 0 },
    { id: "key", label: "Keystroke", sub: "higher-priority lane", col: 1, row: 1 },
  ]}
  edges={[
    { from: "slice1", to: "yield", label: "shouldYield()" },
    { from: "yield", to: "slice2", label: "resume" },
    { from: "key", to: "yield", label: "interrupts" },
  ]}
  caption="At each yield point React checks shouldYield(); if the ~5 ms slice is up it posts a continuation via MessageChannel and lets the browser run. A keystroke sits in a higher-priority lane, so it interrupts the time-sliced transition: React abandons the in-progress render, handles the input, then restarts."
/>

At each <Pulse>yield</Pulse> point the browser gets a turn — that pause is what keeps typing instant during a long render.

**The lane bitmask.** Why does it matter that lanes are bits rather than strings or numbers? Because the performance of React's scheduler depends on these operations being single CPU instructions — and understanding this tells you why automatic batching works the way it does. A lane is one bit in a 31-bit integer. Representing priority as a bitmask lets React do set operations in single CPU instructions: "are there any high-priority lanes pending?" is a mask-and-test; "render all lanes at or above this priority" is a bitmask comparison. Updates in compatible lanes are *batched* — multiple `setState` calls in one event handler (or, since React 18, across promises and timeouts via automatic batching) collapse into a single render. This is why you cannot read the new state synchronously right after `setState`: the render that applies it has not happened yet.

**useTransition and useDeferredValue.** These are the two hooks that put work into the transition lane. `useTransition` gives you a `startTransition` function — state updates inside it are marked low-priority and interruptible, plus an `isPending` flag for showing a spinner. The canonical use: a search input where the input value itself updates synchronously (so typing feels instant) but the expensive filtered-list update is wrapped in `startTransition` (so it yields to further keystrokes).

`useDeferredValue` is the same idea from the consuming side: it gives you a copy of a value that "lags behind" — React updates it at transition priority, so a component reading the deferred value re-renders only when React has spare time. Use `useTransition` when you own the state update; `useDeferredValue` when the value comes from props or context you do not control.

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="05-react-fiber"
  lang="en"
  title="Keep an expensive filtered list from blocking the input"
  steps={{
    solved: (
      <pre><code>{`function Search({ allItems }) {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();
  const [results, setResults] = useState(allItems);

  function onChange(e) {
    setQuery(e.target.value);              // urgent: input stays instant
    startTransition(() => {                // low priority, interruptible
      setResults(filter(allItems, e.target.value));
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      {isPending && <Spinner />}
      <List items={results} />
    </>
  );
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Fill in the two missing pieces: the hook that creates the transition, and the wrapper that marks the expensive update as low-priority.</p>
      ),
      blanks: [
        { id: "hook", expected: /useTransition/i, placeholder: "hook for the transition" },
        { id: "wrap", expected: /startTransition/i, placeholder: "wrapper for the expensive update" },
      ],
    },
    blank: {
      prompt: (
        <p>An input filters a large list. Every keystroke runs an expensive <code>filter</code> and the input lags behind the user's typing. Rewrite the change handler so the input stays instant while the list update can be interrupted by the next keystroke. What is the structural change?</p>
      ),
      reveal: (
        <p>Split the one state update into two priorities. The <code>query</code> update that drives the input stays a plain <code>setState</code> — synchronous lane, so the input reflects each keystroke immediately. The expensive <code>setResults(filter(...))</code> goes inside <code>startTransition</code> — transition lane, interruptible and time-sliced. Now when the next keystroke arrives mid-filter, React abandons the in-progress list render, applies the keystroke at high priority, and restarts the filter. The input never lags; the list catches up when there is spare time. <code>isPending</code> drives a spinner so the lag is visible feedback, not a freeze.</p>
      ),
    },
  }}
/>

<MetaphorComplete
  id="mid-metaphor-lanes"
  pieceSlug="05-react-fiber"
  lang="en"
  setup="React 18 assigns every update a priority bucket so it can render urgent work first and defer the rest. A keypress goes in the synchronous bucket; a big list filter wrapped in startTransition goes in a low, interruptible bucket. What does React call these priority buckets?"
  accepted={["lanes", "lane", "lane model", "priority lanes", "the lanes"]}
  canonical="lanes"
  explanation="Lanes are React 18's priority model — internally a 31-bit bitmask, each bit a lane. Discrete input lands in a synchronous lane and renders immediately and blocking; transitions land in a transition lane that any more-urgent lane can interrupt; idle work lands in the lowest lanes. The lane bitmask lets React batch compatible updates and pick which set to work on next, which is how it decides what to render now versus later."
/>

<Quiz
  id="mid-quiz-lanes-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="You wrap an expensive list update in `startTransition`. A keystroke arrives while that render is in progress. What does React do?"
  choices={[
    { label: "Abandons the in-progress transition render, processes the keystroke at high priority, then restarts the transition render", correct: true },
    { label: "Finishes the transition render first, then handles the keystroke", misconception: "That would make the keystroke wait — the opposite of what transitions are for. React interrupts the transition for the urgent input." },
    { label: "Runs both renders in parallel on two threads", misconception: "React's reconciler runs on the single main thread. There is no parallel rendering — there is interruption and resumption." },
    { label: "Drops the keystroke", misconception: "The keystroke is never dropped. It is processed at high priority, ahead of the interruptible transition work." },
  ]}
/>

<Quiz
  id="mid-quiz-lanes-2"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Which kind of update does React render in one blocking pass, with no time-slicing?"
  choices={[
    { label: "A discrete input update — a click or keypress — because the user is waiting on that exact interaction", correct: true },
    { label: "Any update inside startTransition", misconception: "Transition updates are exactly the ones that ARE time-sliced and interruptible. Discrete input is the opposite." },
    { label: "All updates — React never time-slices", misconception: "React 18 time-slices transition and other non-urgent updates. Discrete input is the exception that renders synchronously." },
    { label: "Updates triggered by useDeferredValue", misconception: "useDeferredValue updates run at transition priority — interruptible and time-sliced, not blocking." },
  ]}
/>

<NumberDrill
  id="mid-drill-lanes"
  pieceSlug="05-react-fiber"
  lang="en"
  question="React's scheduler does about 5 ms of render work per slice. The frame budget at 60 fps is 16.67 ms. Roughly how many React render slices can fit in one frame alongside ~6 ms of browser overhead?"
  expected={2}
  tolerance={1}
  unit="slices"
  explanation="16.67 ms minus ~6 ms browser overhead leaves ~10 ms for everything else. At ~5 ms per slice that is about 2 slices per frame. Time-slicing means a 50 ms render is spread across roughly 5 frames — each frame still paints, input still flows — instead of one 50 ms long task that drops 3 frames and spikes INP."
/>

</Explanation>

<KeyTakeaway>Lanes assign priority: discrete input is sync and blocking; transitions are interruptible. The scheduler slices renders into ~5 ms chunks via MessageChannel, keeping input responsive. useTransition marks expensive state updates as transition-priority; useDeferredValue does the same from the consumer side.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-lanes-and-time-slicing-retrieval"
  lang="en"
  questions={[
    { q: "Why does a click event render synchronously and blocking, while a startTransition update does not?", a: "A click is discrete user input — the user expects the result immediately and is staring at the screen waiting. Making it yield would feel broken. A startTransition update is work the user is not directly waiting on (a list filter, a navigation). React can interrupt and restart it without the user noticing, so it runs at transition priority — interruptible and time-sliced." },
    { q: "How does React yield to the browser between render slices?", a: "Via MessageChannel. After ~5 ms of work the scheduler posts a continuation task on a MessageChannel port. The browser can run input handlers and rendering tasks before picking up that continuation. setTimeout would also work but is clamped to 4 ms minimum, making it less precise." },
    { q: "What is the difference between useTransition and useDeferredValue?", a: "Both put work in the transition lane. useTransition gives you a startTransition function you call when dispatching an update you own, plus an isPending flag. useDeferredValue gives you a lagged copy of a value you receive from props or context — useful when you do not own the state update. They solve the same problem from different sides of the data flow." },
  ]}
/>

<Recap lang="en">
React 18's lane system assigns each update a priority bucket. Discrete input (clicks, keystrokes) renders in the sync lane — one blocking pass, no yield. Ordinary state updates use the default lane. Work wrapped in `startTransition` or read via `useDeferredValue` runs in the transition lane — interruptible and time-sliced. The scheduler breaks transition renders into ~5 ms slices, posts a continuation via `MessageChannel` between slices, and resumes from the stored fiber pointer. This is why a 50 ms list render does not drop frames: it becomes ten 5 ms slices interleaved with browser repaints and input handling. When a keystroke arrives mid-transition, React abandons the in-progress render, handles the keystroke at sync priority, and restarts the transition from scratch. Now when you see a sluggish filter or a janky navigation, you know the question to ask: is the expensive update wrapped in a transition lane, or is it competing with input in the sync lane?
</Recap>
