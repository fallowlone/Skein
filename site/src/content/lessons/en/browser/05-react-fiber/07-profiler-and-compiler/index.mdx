---
concepts:
- react-devtools-profiler
- loaf
- react-compiler
- production-observability
- TradeoffMatrix
- DesignPrompt
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: senior
order: 7
prereqs:
- 06-bailout-and-memoisation
- 05-lanes-and-time-slicing
slug: 07-profiler-and-compiler
sources:
- https://react.dev/learn/react-developer-tools
- https://react.dev/blog/2024/10/21/react-compiler
- https://web.dev/articles/inp
- https://web.dev/articles/loaf
spiral:
- statefulness
status: ready
summary: The React DevTools Profiler exposes why each fiber re-rendered and how long
  it took. The React Compiler automates memoisation. Pairing Profiler with LoAF entries
  closes the loop from user-reported lag to the exact line of code.
title: React Profiler, the Compiler, and production observability
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import Term from "~/components/prose/Term.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";

<Hook>
Users report lag. The app feels fine in the office. The Profiler shows a 38 ms commit triggered by a keystroke in a search box — and 240 ProductCards re-rendering with reason "parent rendered, props unchanged." That is the bug signature. Without the Profiler, you are guessing; with it, the fix is mechanical.
</Hook>

<Crux>Profiler names each re-render cause and duration; Compiler automates memoisation; LoAF links production lag to specific React commits.</Crux>

<Explanation>

**Reading the React Profiler.** The React DevTools Profiler records a commit-by-commit timeline. For each commit it shows which fibers rendered, why each rendered ("props changed", "hook changed", "parent rendered", "context changed"), and how long each took. The senior workflow: record an interaction, find the commit that is too long, sort fibers by self-time, and read the "why did this render" reason.

- **"Parent rendered" on unchanged props** → missing bailout. Wrap in `React.memo`, stabilise the offending prop reference.
- **"Context changed" across many components** → unstable context value. Memoise the provider's value with `useMemo`.
- **"Hook changed"** → a `useState` or `useReducer` value that changed. Check if the computation producing it is memoised.

<DebugLog
  id="sr-debug-1"
  pieceSlug="05-react-fiber"
  lang="en"
  label="React DevTools Profiler — commit detail"
  output={`Commit #47  —  render: 38.2 ms  (1 of 1 committed)

  <App>                rendered  — 0.4 ms  — "hook changed"
    <Header>           rendered  — 0.2 ms  — "parent rendered"
    <Sidebar>          rendered  — 0.3 ms  — "parent rendered"
    <ProductGrid>      rendered  — 36.9 ms — "parent rendered"
      <ProductCard> x240  rendered  — 0.15 ms each — "parent rendered"

  Why did <App> render?  -> useState (searchTerm) changed
  Note: <Header>, <Sidebar>, <ProductGrid> props unchanged`}
  outputLang="log"
  question="Typing in the search box triggers a 38 ms commit. Header, Sidebar, and all 240 ProductCards re-render with reason 'parent rendered', and the Profiler says their props did not change. What is the fix?"
  hint="The reason is 'parent rendered', not 'props changed'. What stops a child from re-rendering when its parent does but its props are equal?"
  answer="Every child re-renders because <App> re-rendered and nothing lets them bail out. 'Parent rendered' plus 'props unchanged' is the exact signature of a missing bailout. The fix is layered. First, wrap Header, Sidebar, and ProductCard in React.memo so they bail out when their props are referentially equal — that immediately spares Header and Sidebar, whose props genuinely never change. Second, make ProductCard's props referentially stable: if App passes inline objects or arrow functions (onClick={() => ...}, style={{...}}), memoise them with useCallback/useMemo, or the React.memo on ProductCard will never fire because a fresh literal is never Object.is-equal. Third — the structural fix — move searchTerm state down: if only the search box and the grid need it, lifting it out of <App> stops the entire tree re-rendering on every keystroke. Fourth, for 240 cards, virtualize so only the visible ~20 mount. And wrap the grid update in startTransition so even a heavy render yields to keystrokes. Verify in the Profiler: after the fix the commit should show only ProductGrid and the changed cards rendering, well under 16 ms."
/>

**Production observability: LoAF + Profiler.** The Profiler gives you local reproduction; <Term k="loaf" lang="en">Long Animation Frames</Term> (LoAF) give you production attribution. When a LoAF entry attributes a slow frame to a React bundle, the Profiler reproduces it locally and names the exact fibers. The two together close the loop from "users report lag" to "this component re-renders 200× because of an inline style object on line 44."

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Profiler → fix workflow</div>
  <div class="flex flex-col gap-2 text-xs">
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">1. Record</span> an interaction in the Profiler
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">2. Find</span> the longest commit — sort by total render time
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">3. Read</span> the "why did this render" reason for slow fibers
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">4. Fix</span>: "parent rendered" + stable props → React.memo; "context changed" → memoise provider value; state down → collocate state
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">5. Verify</span> — the fixed commit should show only genuinely-changed fibers
    </div>
  </div>
</div>

**React Compiler and the future of memoisation.** For a decade, keeping the bailout firing meant scattering `useMemo`, `useCallback`, and `React.memo` by hand — tedious, error-prone, and itself not free (each memo cell costs memory and a comparison). The React Compiler (formerly "React Forget", stable-track as of React 19) is a build-time tool that analyses component code and inserts the memoisation automatically and correctly, at finer granularity than a human would. It understands which values depend on which inputs and memoises exactly those, so the bailout fires without any hand-written hooks.

The senior takeaway: the *mechanism* — referential stability gating the bailout — is not going away; it is the foundation the compiler builds on. Understanding why a stable reference lets a fiber skip work is what lets you read the compiler's output, debug it when it bails out conservatively, and reason about performance whether the memoisation is hand-written or compiler-generated.

<Inset kind="why" lang="en">
**Why the virtual DOM is not just "overhead."** "Virtual DOM" is the marketing name for the element tree React diffs. It is worth being precise: the virtual DOM is not faster than direct DOM manipulation — a hand-written, optimal sequence of DOM calls always beats React. What the virtual DOM buys is a *programming model*: you describe the UI as a pure function of state and never write imperative DOM code, and React makes the result fast *enough* by diffing. The cost is real — building and diffing the element tree is CPU work that a compiler-based framework (Svelte) or a fine-grained reactive one (SolidJS) avoids. React's bet is that the developer-experience win of "UI = f(state)" is worth the reconciliation overhead, and that time-slicing keeps that overhead from ever blocking the user.
</Inset>

<TradeoffMatrix
  id="card-fiber-cost"
  pieceSlug="05-react-fiber"
  lang="en"
  prompt="A search box filters a 5 000-row table. Typing is janky. Pick the primary fix."
  options={[
    { name: "Virtualize the table + wrap the filtered update in startTransition", summary: "Caps mounted nodes; yields to keystrokes.", correct: true, justification: "Virtualization makes commit cheap — only ~20 visible rows mount regardless of dataset size, so the uninterruptible commit phase stays tiny. startTransition puts the filtered re-render in the transition lane, so it time-slices and any keystroke interrupts it. Together they fix both halves: small commit, interruptible render. This is the canonical fix." },
    { name: "Wrap every row component in React.memo", summary: "Helps, but does not cap commit cost.", justification: "React.memo lets unchanged rows bail out of the render phase — useful — but all 5 000 rows are still mounted, so the commit phase and the DOM itself stay huge. Memo without virtualization treats a symptom; the 5 000-node tree is the disease." },
    { name: "Debounce the search input by 300 ms", summary: "Reduces frequency, not cost.", justification: "Debounce makes the expensive render happen less often, which masks the jank, but each render is still a 5 000-row blocking update. When it does fire it still drops frames. Acceptable as a complement, never as the primary fix." },
    { name: "Move the table render into a web worker", summary: "Impossible — workers have no DOM.", justification: "A worker cannot render React DOM. It could pre-filter the 5 000-row dataset off-thread and hand back a small result, which helps if the filter computation is the bottleneck — but the rendering itself must stay on the main thread." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="05-react-fiber"
  lang="en"
  prompt="Design the rendering strategy for a real-time trading dashboard: a 2 000-row price grid updating ~10× per second, plus interactive filters and a detail panel. It must keep INP under 200 ms and never drop the input box's responsiveness."
  constraints={[
    "Price ticks arrive ~10 times per second from a WebSocket.",
    "Only visible rows need to be correct; the grid scrolls.",
    "Filter and sort controls must feel instant.",
    "The commit phase must stay short — no long uninterruptible blocks.",
    "External price store must not tear during concurrent renders.",
    "INP target: under 200 ms p75.",
  ]}
  canonical="Virtualize the grid so only the ~30 visible rows plus overscan are ever mounted — this caps the commit phase regardless of the 2 000-row dataset. Subscribe to the WebSocket price store through useSyncExternalStore so concurrent renders cannot tear: every component in a commit sees one consistent snapshot. Batch incoming ticks: instead of a setState per tick (10/s × many symbols), accumulate ticks and flush once per animation frame via a single store update, collapsing the work into one render per frame. Route the price-grid update through startTransition (or rely on useDeferredValue for the grid's price values) so it renders at transition priority — interruptible, time-sliced, never blocking the filter input. Keep filter/sort state local and synchronous so those controls render in the sync lane and feel instant. Wrap each row in React.memo and keep its props referentially stable (memoised handlers, no inline objects) so unchanged rows bail out. Key every row by its stable symbol id, never the index, so reorders from sorting preserve per-row state correctly. Verify: in the React Profiler, a tick flush should re-render only the visible rows whose price changed, commit under 8 ms; in DevTools Performance, no long tasks during a 30 s session of live ticks plus continuous typing in the filter."
  keyPoints={[
    "Virtualize to cap the commit phase regardless of dataset size.",
    "useSyncExternalStore for the price store — prevents tearing under concurrent rendering.",
    "Batch WebSocket ticks to one store flush per frame, not one setState per tick.",
    "Price grid renders at transition priority; filter input stays in the sync lane.",
    "Stable symbol-id keys + React.memo + stable props so unchanged rows bail out.",
  ]}
/>

<Quiz
  id="sr-quiz-compiler-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="The React Compiler inserts memoisation automatically. Does understanding useMemo and useCallback still matter?"
  choices={[
    { label: "Yes — the mechanism (referential stability gating the bailout) is what the compiler builds on. You need it to read the compiler's output, debug conservative bailouts, and reason about performance.", correct: true },
    { label: "No — the compiler handles it, so developers no longer need to understand memoisation", misconception: "The compiler can bail out conservatively, generate unexpected renders, or be disabled on complex patterns. Without understanding the mechanism, you cannot debug or override it." },
    { label: "Only for class components — function components are fully automated", misconception: "The compiler targets function components. Understanding bailout is still essential for debugging compiler output and for codebases not yet using the compiler." },
    { label: "Only if you are using React 17 or earlier", misconception: "The compiler is a React 19+ feature, but the mechanism it automates is fundamental to all versions of React." },
  ]}
/>

<p class="text-ink-2 my-6">A fiber is where component state lives between renders — the `useState` values, the refs, the hook list all hang off the fiber, not the component function. Render after render, the fiber persists and carries that state forward; destroy the fiber (unmount, type change, key change) and the state is gone. The reconciler is, at bottom, a machine for deciding which fibers survive and which are rebuilt — the same statefulness question, of what persists across a transition and what is re-derived, that runs through every layer of the stack. <SpiralCue thread="statefulness" lang="en" /></p>

</Explanation>

<KeyTakeaway>The Profiler shows each commit's duration and why each fiber re-rendered — "parent rendered + unchanged props" is the bailout-missing signature. The React Compiler automates memoisation but relies on the same bailout mechanism. Pair Profiler with LoAF to close the production lag loop.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-profiler-and-compiler-retrieval"
  lang="en"
  questions={[
    { q: "A Profiler trace shows 240 ProductCards re-rendering with reason 'parent rendered, props unchanged'. What does that tell you and what do you do?", a: "'Parent rendered + props unchanged' is the signature of a missing bailout. React.memo is either absent or being defeated by an unstable prop reference. Wrap ProductCard in React.memo. Then check if any prop is an inline object or arrow function — memoise those with useMemo/useCallback. Finally check if the parent can collocate state closer to only the components that need it, reducing how much of the tree re-renders." },
    { q: "What is a Long Animation Frame (LoAF) and how does it help diagnose React performance problems?", a: "A LoAF is a browser performance entry for frames that took longer than 50 ms (more lenient than Long Task's 50 ms threshold but covering the full frame including rendering). When a LoAF entry attributes the slow frame to React bundle code, you use the Profiler to reproduce it locally and find the specific commit and fibers. LoAF gives you production attribution; the Profiler gives you local detail." },
    { q: "What is the key insight about the React Compiler that a senior engineer needs to hold onto?", a: "The compiler automates memoisation but does not change the mechanism. Referential stability gating the bailout is still how React avoids unnecessary re-renders — the compiler just generates the useMemo and useCallback calls that a human would write. You need to understand the mechanism to debug the compiler when it bails out conservatively, to reason about performance in codebases not using the compiler, and to validate compiler output." },
  ]}
/>

<Recap lang="en">
The React DevTools Profiler records each commit with per-fiber render reasons and durations. "Parent rendered, props unchanged" is the mechanical signature of a missing bailout: add `React.memo` and stabilise unstable prop references with `useMemo`/`useCallback`. "Context changed" across many components points to an unstable context value. In production, Long Animation Frames (LoAF) entries attribute slow frames to React code — pair them with the Profiler to name the exact commit and fibers. The React Compiler (stable-track in React 19) inserts memoisation automatically at build time, eliminating the manual scatter of `useMemo`/`useCallback`. But the underlying mechanism — referential stability gating the bailout — remains: the compiler builds on it, and you must understand it to debug the compiler's conservative decisions and reason about performance across any React codebase.
</Recap>
