---
concepts:
- fiber-object
- workInProgress-tree
- current-tree
- alternate-pointer
- interruptible-rendering
deepensInto:
- 06-bailout-and-memoisation
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-reconciler-overview
slug: 02-fiber-data-structure
sources:
- https://github.com/acdlite/react-fiber-architecture
- https://github.com/facebook/react/blob/main/packages/react-reconciler/src/ReactFiberLane.js
- https://react.dev/learn/preserving-and-resetting-state
spiral:
- statefulness
status: ready
summary: A fiber is a plain JS object — one per component — holding state, hooks,
  and tree pointers. The linked-list structure is what makes rendering interruptible,
  and the two-tree double buffer is what makes interruption free.
title: The fiber object and the double-buffer tree
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
React cannot pause a recursive function — the call stack is a system resource it does not own. The fiber architecture solved this by replacing the call stack with a data structure React controls: a linked tree of objects it can stop and resume at any node.
</Hook>

<Crux>One fiber per component; linked-list shape lets React pause after any node. Double buffering keeps the screen never half-updated.</Crux>

<Explanation>

**What a fiber actually is.** A fiber is a plain JavaScript object — one per component instance, one per DOM element in the tree. Each fiber holds: the component `type`, its `props`, its `key`, its current `state` and hooks, pointers to its `child`, its next `sibling`, and its `return` (parent), plus an `alternate` pointer to its counterpart in the other tree. The fibers form a linked tree, not an array — and that linked structure is the whole point.

A recursive tree walk lives on the call stack and cannot be paused; a linked-list walk holds its position in a plain variable, so React can stop after any fiber, store the pointer, and resume from exactly there later. Fiber is the data structure that made interruptible rendering possible. The name predates React 18 — the architecture landed in React 16 (2017) — but only React 18 turned on the concurrent features that exploit it.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm overflow-x-auto">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Fiber object fields</div>
  <table class="w-full text-xs">
    <thead>
      <tr class="border-b border-border">
        <th class="text-left py-1 pr-4 text-ink-2">Field</th>
        <th class="text-left py-1 text-ink-2">What it holds</th>
      </tr>
    </thead>
    <tbody class="text-ink">
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">type</td><td class="py-1">The component function or DOM tag</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">props</td><td class="py-1">Current props passed to this node</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">key</td><td class="py-1">The reconciliation key</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">memoizedState</td><td class="py-1">Linked list of hook records (useState, useEffect …)</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">child / sibling / return</td><td class="py-1">Tree navigation pointers (not a call stack)</td></tr>
      <tr><td class="py-1 pr-4 font-semibold">alternate</td><td class="py-1">Pointer to the twin fiber in the other tree</td></tr>
    </tbody>
  </table>
</div>

<DiagramFrame
  label="A parent fiber and its two child fibers, linked by three pointers: child points from the parent to its first child, sibling chains the first child to the next, and return points from each child back up to its parent."
  hue="var(--d-frontend)"
  caption="No call stack: the whole tree is reachable through three plain pointers. child reaches the first child, sibling chains siblings left to right, return walks back up to the parent. React holds its position in one variable, so it can stop after any fiber and resume from exactly there."
>
  <svg viewBox="0 0 460 280" width="460" height="280" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    <defs>
      <marker id="fiber-arrow-en" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
    </defs>
    {/* child: parent -> first child */}
    <path d="M195 72 L120 168" fill="none" stroke="var(--muted)" stroke-width="1.6" marker-end="url(#fiber-arrow-en)" />
    <text x="138" y="116" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">child</text>
    {/* sibling: first child -> second child */}
    <path d="M186 196 L268 196" fill="none" stroke="var(--muted)" stroke-width="1.6" marker-end="url(#fiber-arrow-en)" />
    <text x="200" y="188" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">sibling</text>
    {/* return: first child -> parent */}
    <path d="M138 168 L210 76" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" marker-end="url(#fiber-arrow-en)" />
    <text x="150" y="146" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">return</text>
    {/* return: second child -> parent */}
    <path d="M312 168 L242 76" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" marker-end="url(#fiber-arrow-en)" />
    <text x="300" y="146" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">return</text>
    {/* parent fiber */}
    <rect x="150" y="24" width="160" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="230" y="44" text-anchor="middle" font-size="13" fill="var(--ink)">Parent fiber</text>
    <text x="230" y="60" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;List&gt;</text>
    {/* child 1 fiber */}
    <rect x="40" y="172" width="146" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="113" y="192" text-anchor="middle" font-size="13" fill="var(--ink)">Child fiber 1</text>
    <text x="113" y="208" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;Item&gt;</text>
    {/* child 2 fiber */}
    <rect x="268" y="172" width="146" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="341" y="192" text-anchor="middle" font-size="13" fill="var(--ink)">Child fiber 2</text>
    <text x="341" y="208" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;Item&gt;</text>
  </svg>
</DiagramFrame>

**The two trees: current and workInProgress.** React always holds two fiber trees. The *current* tree is the one whose state matches what is painted on screen right now. When an update starts, React builds a *workInProgress* tree — cloning fibers from current as it goes, applying new props and state. Each fiber's `alternate` pointer links it to its twin in the other tree, so cloning is cheap: unchanged subtrees can be reused by reference.

When the workInProgress tree is complete and committed, the two trees swap: workInProgress becomes current, and the old current tree is kept around to become the next workInProgress. This is double buffering. It guarantees the on-screen tree is never observed in a half-updated state — and it is why a render that gets interrupted and thrown away costs nothing permanent: the discarded workInProgress tree simply never commits.

<Inset kind="why" lang="en">
**Why fiber, not recursion?** Before React 16, reconciliation was an ordinary recursive function. Recursion lives in the JS engine's call stack, which cannot be paused and resumed — a function either runs to completion or does not run at all. This meant the entire render for a large tree was one indivisible synchronous task. Fiber replaced the recursion with an explicit linked-list walk: the "call stack" became a data structure in the heap that React owns and can stop, save, and resume. The name refers to the "fiber" concept from systems programming — a user-space lightweight thread — because a fiber tree is essentially a hand-implemented suspendable call stack on top of plain JS.
</Inset>

<Quiz
  id="mid-quiz-fiber-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Why is reconciliation with a linked fiber tree interruptible, while a recursive approach is not?"
  choices={[
    { label: "A linked-list walk holds its position in a variable React controls; a recursive call stack is owned by the JS engine and cannot be suspended mid-walk", correct: true },
    { label: "Linked lists are faster than recursion for large trees", misconception: "Speed is not the point. The key property is pausability: the JS engine owns the call stack; React owns the fiber cursor." },
    { label: "React disables recursion in React 18", misconception: "React 18 still uses recursion for some internal paths. The fiber architecture is the structural change that enables interruption." },
    { label: "The alternate pointer lets React skip tree nodes", misconception: "The alternate pointer links twins across the two trees for cloning and bailout — it is not a skip mechanism." },
  ]}
/>

<Quiz
  id="mid-quiz-fiber-2"
  pieceSlug="05-react-fiber"
  lang="en"
  question="A render is halfway done when a higher-priority update arrives. React throws away the in-progress workInProgress tree and starts over. Why does this cost nothing permanent?"
  choices={[
    { label: "The workInProgress tree never committed — the current tree on screen is untouched, so discarding workInProgress reverts nothing visible", correct: true },
    { label: "React checkpoints the workInProgress tree to localStorage before discarding", misconception: "There is no checkpoint mechanism. The cost is zero because nothing committed — the current tree on screen is what it always was." },
    { label: "The alternate pointer lets React replay mutations cheaply", misconception: "The alternate pointer is for cloning, not replay. Discarding is cheap because no DOM mutations occurred yet." },
    { label: "React saves the half-built tree to re-use its nodes later", misconception: "Discarded trees are not directly re-used. React rebuilds from current on the next render. Cloning from current is already cheap via alternate pointers." },
  ]}
/>

<DragOrder
  id="mid-drag-fiber-1"
  pieceSlug="05-react-fiber"
  lang="en"
  prompt="Order the steps of one React render cycle, from state change to tree swap."
  items={[
    { id: "schedule", label: "Update arrives — React schedules work on the appropriate lane" },
    { id: "clone", label: "Fiber walk begins — clone fibers from current into workInProgress" },
    { id: "run", label: "Run component functions, reconcile new element tree" },
    { id: "commit", label: "Commit: apply DOM mutations from workInProgress" },
    { id: "swap", label: "Swap: workInProgress becomes current; old current kept for next cycle" },
  ]}
  correctOrder={["schedule", "clone", "run", "commit", "swap"]}
/>

</Explanation>

<KeyTakeaway>A fiber is a JS object with child/sibling/return pointers forming a linked tree React controls — that is why renders are pauseable. Double buffering (current + workInProgress) ensures the on-screen tree is never half-updated, and thrown-away renders cost nothing.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-fiber-data-structure-retrieval"
  lang="en"
  questions={[
    { q: "What fields on a fiber object enable the linked-tree walk?", a: "child (first child fiber), sibling (next sibling in parent), and return (parent fiber). Together they form a linked tree React navigates without a call stack." },
    { q: "What does the alternate pointer on a fiber point to?", a: "Its twin in the other tree. The current fiber's alternate points to its workInProgress counterpart, and vice versa. This lets React clone cheaply — unchanged subtrees are reused by reference." },
    { q: "Why can a discarded workInProgress tree be thrown away at zero visible cost?", a: "Because the workInProgress tree never committed. The current tree, which matches what is on screen, is untouched. Discarding workInProgress simply means no DOM mutations happened — nothing to undo." },
  ]}
/>

<Recap lang="en">
A fiber is a plain JS object holding a component's type, props, key, state, and hooks, plus child/sibling/return pointers that form a linked tree. Unlike a recursive call stack, this linked tree can be stopped after any node by saving a cursor variable, which is the structural basis for interruptible rendering. React always keeps two of these trees: the current tree (matching the screen) and the workInProgress tree (the update being built). The alternate pointer on each fiber links it to its twin across the two trees, making cloning cheap. When a render commits, the trees swap; when a render is abandoned, the workInProgress tree is discarded at zero cost because it never touched the DOM. Now when you encounter a component that loses state unexpectedly, or a render that seems to start over for no reason, you can trace it to this model: something unmounted the fiber, or a higher-priority update discarded the in-progress tree.
</Recap>
