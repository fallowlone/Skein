---
slug: 04-reconciliation-and-keys
lang: en
track: browser
unit: 05-react-fiber
order: 4
title: "Reconciliation: diffing heuristics and the key trap"
summary: "React reduces O(n³) tree diffing to O(n) with two heuristics — same type keeps state, different type destroys it. Keys identify list items across renders: index keys silently corrupt uncontrolled state on reorder."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs: ["02-fiber-data-structure", "03-render-and-commit-phases"]
deepensInto: ["06-bailout-and-memoisation"]
spiral: ["statefulness"]
concepts: ["reconciliation", "diffing-heuristics", "key-prop", "list-reconciliation", "state-corruption", "controlled-vs-uncontrolled"]
sources:
  - https://react.dev/learn/preserving-and-resetting-state
  - https://react.dev/learn/rendering-lists
  - https://github.com/acdlite/react-fiber-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
A checkbox checked on row 3 stays checked after you delete row 1 — now appearing on what used to be row 4. No React warning. No console error. One line change — `key={index}` to `key={item.id}` — and the bug disappears. Keys are not hints. They are the mechanism React uses to pair fibers with items across renders, and getting them wrong corrupts state silently.
</Hook>

<Crux>Reconciliation is O(n) via two heuristics: different element type resets state; keys bind fibers to item identity, not position.</Crux>

<Explanation>

**Reconciliation: the diffing rules.** Reconciliation compares the new element tree against the current fiber tree to decide what to keep, update, or replace. A full tree diff is O(n³) in the general case; React makes it O(n) with two heuristics.

**Heuristic 1 — same type, same fiber.** If a `<div>` stays a `<div>`, or `<ComponentA>` stays `<ComponentA>`, React updates the existing fiber in place — patching its props, running reconciliation for its children. The fiber keeps its state.

**Heuristic 2 — different type, full replacement.** If a `<div>` becomes a `<span>`, or `<ComponentA>` becomes `<ComponentB>`, React tears down the whole subtree (unmounting it, destroying its state) and builds the new one fresh. It does not try to diff across types. This is why conditionally rendering two different components in the same slot resets all state — even if the new component looks similar.

**Keys identify list children across renders.** Within a list, React matches old and new children by their `key`, not their position. A stable key tells React "this is the same logical item, even if it moved." This is the entire reason keys exist. React builds a map from `key` to old fiber, then walks the new children matching each to its old fiber by key. The matched fiber carries forward everything stateful: `useState` values, `useRef` contents, uncontrolled DOM state (an input's typed text, a checkbox's checked-ness, scroll position, focus), and the actual DOM node.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">key=&#123;index&#125; vs key=&#123;id&#125; — what happens on delete</div>
  <div class="grid grid-cols-2 gap-4 text-xs">
    <div>
      <div class="mb-1 font-semibold text-red-700 dark:text-red-400">key=&#123;index&#125; (broken)</div>
      <div class="rounded bg-red-50 dark:bg-red-950/30 border border-red-200 dark:border-red-800 p-2">
        <div>Before: key=0 → item A ✓checked</div>
        <div>Before: key=1 → item B</div>
        <div>Before: key=2 → item C</div>
        <div class="border-t border-red-200 dark:border-red-800 mt-1 pt-1">Delete item A</div>
        <div>After: key=0 → item B (but fiber from item A — ✓checked!)</div>
        <div>After: key=1 → item C</div>
      </div>
    </div>
    <div>
      <div class="mb-1 font-semibold text-green-700 dark:text-green-400">key=&#123;item.id&#125; (correct)</div>
      <div class="rounded bg-green-50 dark:bg-green-950/30 border border-green-200 dark:border-green-800 p-2">
        <div>Before: key="a" → item A ✓checked</div>
        <div>Before: key="b" → item B</div>
        <div>Before: key="c" → item C</div>
        <div class="border-t border-green-200 dark:border-green-800 mt-1 pt-1">Delete item A</div>
        <div>After: key="b" → item B (correct fiber, no state)</div>
        <div>After: key="c" → item C</div>
      </div>
    </div>
  </div>
</div>

**Why `key={index}` is a trap.** With `key={index}`, the key is the position, so when the list reorders or an item is inserted at the front, every item's key shifts. React then thinks item-at-index-0 is "the same item" even though the data moved — it keeps the old fiber's state and DOM but feeds it the new item's props. The visible bug: a checkbox checked on row 3 stays checked after you delete row 1, now attached to what used to be row 4.

Controlled props update correctly (they are passed fresh each render); uncontrolled state does not (it lives on the fiber). `key={index}` is safe only for a list that is static and never reorders; for anything dynamic it silently corrupts state.

<Inset kind="edgecase" lang="en">
`Math.random()` as a key is worse than `key={index}`. It assigns a new key every render, so React always sees a "different" item at every position — it unmounts and remounts every list item on every update. You get correct state (because the fiber is always new) but you pay for an unmount + mount of every item on every keystroke. `key={index}` is wrong for dynamic lists; `key={Math.random()}` is wrong for all lists.
</Inset>

<Misconception id="mc-key-index">
**Myth:** `key={index}` is fine — keys just need to be unique.

**Reality:** Keys identify items *across renders*. An index is a position, not an identity. When the list reorders, inserts, or deletes, every index shifts — React keeps each fiber's state but pairs it with the wrong item. Use a stable id.
</Misconception>

<Quiz
  id="mid-quiz-keys-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="A list of items renders with `key={index}`. You delete the first item. A checkbox that was checked on the old second item is now checked on the wrong row. Why?"
  choices={[
    { label: "With index keys, deleting item 0 shifts every key down one — React matches fibers by key, so it keeps each fiber's state but pairs it with the next item's data", correct: true },
    { label: "React lost the checkbox state during the delete", misconception: "State was not lost — it was preserved on the wrong fiber. The fibers stayed; the data they were paired with shifted." },
    { label: "Checkboxes always reset when a list changes", misconception: "Checkboxes keep their state fine with stable keys. The bug is specifically the index-as-key mismatch." },
    { label: "The commit phase applied mutations out of order", misconception: "Commit order is correct. The mismatch happened in reconciliation, where index keys made React pair fibers with the wrong items." },
  ]}
/>

<Quiz
  id="mid-quiz-keys-2"
  pieceSlug="05-react-fiber"
  lang="en"
  question="You render `condition ? <ComponentA /> : <ComponentB />` in the same slot. When condition flips, what happens to ComponentA's state?"
  choices={[
    { label: "It is destroyed — different element type triggers full subtree replacement, unmounting ComponentA and mounting ComponentB fresh", correct: true },
    { label: "ComponentA's state is transferred to ComponentB since they occupy the same slot", misconception: "React does not transfer state across type boundaries. A type change is always a full teardown and rebuild." },
    { label: "React merges the two components' states", misconception: "There is no state merging. Different type = full replacement = ComponentA unmounts and loses all state." },
    { label: "The state is preserved in a ref and passed to ComponentB", misconception: "Nothing is automatically preserved across a type change. If you need to persist state across a type change, you must lift it out manually." },
  ]}
/>

<Quiz
  id="mid-quiz-keys-3"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Why does controlled state (value, checked passed as props) look correct with `key={index}` while uncontrolled state (typed text, scroll position) does not?"
  choices={[
    { label: "Controlled props are re-passed fresh every render, so they always reflect the new item's data regardless of which fiber they land on. Uncontrolled state lives on the fiber and is not re-passed — so it follows the wrong fiber.", correct: true },
    { label: "Controlled state is stored in a global variable, not on the fiber", misconception: "Controlled state is React state or props — it lives in the component tree. The distinction is about how the DOM field is populated, not storage location." },
    { label: "The browser treats controlled and uncontrolled inputs differently", misconception: "The browser has no special handling for React's controlled/uncontrolled distinction. The difference is entirely in how React populates the DOM node on each render." },
    { label: "Uncontrolled state is stored in localStorage", misconception: "Uncontrolled DOM state (a checkbox's checked-ness, an input's typed text) is stored on the DOM node itself, which is attached to the fiber. The fiber moves with the wrong data; the DOM state moves with it." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconciliation is O(n) via two heuristics: same type updates the fiber in place (state preserved); different type tears down the subtree (state destroyed). Keys bind fibers to item identity — index keys corrupt uncontrolled state on any reorder, insert, or delete.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-reconciliation-and-keys-retrieval"
  lang="en"
  questions={[
    { q: "Explain, at the fiber level, why key={index} corrupts state when a list reorders.", a: "Reconciliation matches new list children to old fibers by key. With index keys, the key is position. When the list reorders, the data at index 2 changes but React still matches the new index-2 item to the old key-2 fiber. That fiber carries forward all stateful data — useState values, refs, uncontrolled DOM state, the DOM node itself. Controlled props are re-passed fresh and look correct; uncontrolled state stays with the wrong item." },
    { q: "What does React do when a component type changes in the same slot?", a: "It tears down the entire subtree — unmounts the old component (destroying all its state and refs), then mounts the new one fresh. React never tries to diff across type boundaries." },
    { q: "When is key={index} actually safe?", a: "Only for a list that is purely display-only, never reorders, never has items inserted or deleted, and contains no uncontrolled stateful inputs. Any dynamic list — filterable, sortable, or editable — needs stable identity keys." },
  ]}
/>

<Recap lang="en">
Reconciliation reduces O(n³) tree diffing to O(n) with two heuristics: same element type means update the fiber in place (state preserved); different type means tear down the subtree and rebuild fresh (state destroyed). For lists, React uses `key` to match new children to old fibers by identity, not position. A fiber carries everything stateful — `useState` values, refs, uncontrolled DOM state — so pairing the wrong fiber to the wrong item corrupts that state silently. `key={index}` is the trap: index keys are positional, so any reorder, insert, or delete shifts which fiber an item gets. Use a stable identity key (database id, slug) whenever the list is dynamic.
</Recap>
