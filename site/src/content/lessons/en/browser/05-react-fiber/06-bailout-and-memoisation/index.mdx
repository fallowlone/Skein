---
concepts:
- bailout
- react-memo
- useMemo
- useCallback
- hooks-on-fiber
- rules-of-hooks
- useSyncExternalStore
- tearing
deepensInto:
- 07-profiler-and-compiler
estMin: 16
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- 02-fiber-data-structure
- 04-reconciliation-and-keys
- 05-lanes-and-time-slicing
slug: 06-bailout-and-memoisation
sources:
- https://react.dev/reference/react/memo
- https://react.dev/reference/react/useSyncExternalStore
- https://github.com/reactwg/react-18/discussions/86
- https://react.dev/learn/preserving-and-resetting-state
spiral:
- statefulness
status: ready
summary: React bails out of re-running a component if props and state are unchanged
  by reference. Referential stability (useMemo, useCallback, React.memo) is what enables
  the bailout. useSyncExternalStore prevents concurrent-render tearing.
title: Bailout, memoisation, and tearing
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A component wrapped in `React.memo` still re-renders on every parent render. Nothing changed. The memo is there. Open the DevTools Profiler and the reason says "props changed" — but the prop is an object whose contents are identical. The problem is not the memo. The problem is an inline object literal passed as a prop: a fresh reference every render, and `Object.is` always reports a change.
</Hook>

<Crux>Bailout fires when Object.is finds props and state unchanged. Hooks are position-keyed on fiber; wrong call order corrupts all that follow.</Crux>

<Explanation>

**Bailout: how React skips work.** Not every component in a re-rendering subtree actually re-runs. When React reaches a fiber during render, it checks: did props change (by `Object.is` on each prop)? did state change? is there a pending context update this fiber consumes? If none of these is true, React *bails out* — it clones the fiber from the alternate, reuses the entire existing child subtree by reference, and does not call the component function at all.

This is why referential stability matters so much: a child wrapped in `React.memo` bails out only if its props are referentially equal. Pass it an inline `{}` or an arrow function as a prop and the bailout fails every render, because a fresh literal is never `Object.is`-equal to the last one. `useMemo` and `useCallback` exist to preserve those references so the bailout can fire.

The mental model: re-rendering a parent does not automatically re-render children — it *offers* them the chance to bail out, and stable references are what let them take it.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Bailout decision per fiber</div>
  <div class="flex flex-col gap-1 text-xs">
    <div class="flex items-start gap-2">
      <span class="w-2 h-2 rounded-full bg-green-500 mt-1 flex-shrink-0"></span>
      <span><strong>Props equal (Object.is) AND state unchanged AND no context update</strong> → bail out: clone fiber, reuse child subtree, skip component call</span>
    </div>
    <div class="flex items-start gap-2">
      <span class="w-2 h-2 rounded-full bg-red-500 mt-1 flex-shrink-0"></span>
      <span><strong>Any prop differs by reference OR state changed OR context updated</strong> → re-run component function, diff new element tree</span>
    </div>
    <div class="flex items-start gap-2">
      <span class="w-2 h-2 rounded-full bg-orange-400 mt-1 flex-shrink-0"></span>
      <span><strong>Common trap:</strong> inline <code>&#123;&#125;</code> or <code>{`() => {}`}</code> as prop → new reference every render → bailout never fires even with React.memo</span>
    </div>
  </div>
</div>

<FlowDiagram
  label="When React reaches a fiber during render it tests whether props and state are unchanged by reference; if so it bails out and skips re-rendering the whole subtree, otherwise it re-runs the component function and diffs the new tree."
  hue="var(--d-frontend)"
  nodes={[
    { id: "check", label: "Reach fiber", sub: "props & state unchanged?", col: 1, row: 0 },
    { id: "bailout", label: "Bail out", sub: "skip subtree re-render", col: 0, row: 1 },
    { id: "rerender", label: "Re-render", sub: "run fn, diff children", col: 2, row: 1 },
  ]}
  edges={[
    { from: "check", to: "bailout", label: "Object.is equal" },
    { from: "check", to: "rerender", label: "changed" },
  ]}
  caption="The test is Object.is per prop, plus state and consumed context. Unchanged → clone the fiber from its alternate, reuse the entire child subtree by reference, never call the component. Changed → re-run the function and reconcile. A fresh inline object or arrow function is never Object.is-equal, so it forces the right branch every render — defeating React.memo."
/>

**State is stored on the fiber, not the component.** A function component has no instance — it is just a function React calls. So where does `useState` keep its value across renders? On the fiber. Each fiber holds a linked list of hook records, one per `useState`/`useReducer`/`useEffect` call, in call order. React advances a cursor through that list as your component calls hooks.

This is the deep reason for the Rules of Hooks: hooks must be called unconditionally and in the same order every render, because React identifies each hook purely by its *position* in the call sequence, not by any name. Put a `useState` behind an `if` and on the render where the condition flips, every subsequent hook reads the wrong record. It also explains state preservation: state survives a re-render because the fiber survives; state is destroyed when the fiber is destroyed — which happens on unmount, on a type change, or on a key change.

<Inset kind="why" lang="en">
**Why not name hooks?** Hook records could be stored by name instead of position, eliminating the ordering requirement. But names would need to be unique per component, turning every hook call into a string-keyed lookup instead of a cursor advance. Position-based lookup is O(1) per hook and requires no extra runtime. The tradeoff: mandatory call order in exchange for minimal overhead per render.
</Inset>

**Tearing and useSyncExternalStore.** Interruptible rendering introduces a hazard called *tearing*: if an external store changes between two time-slices, the first slice's components saw the old value and the later slice's components see the new one, so the committed UI is internally inconsistent — "torn". React's own state cannot tear because React controls when it changes; external stores can, because React does not.

`useSyncExternalStore` fixes this: it gives React a `subscribe` function and a `getSnapshot` function. React reads the snapshot, and if it detects the snapshot changed during the concurrent render, it restarts the render, guaranteeing every component in one commit observed the same store value. Any state library integrating with React 18 (Redux, Zustand, Jotai) routes through `useSyncExternalStore` for this reason.

<TraceScenario
  id="mid-trace-context"
  pieceSlug="05-react-fiber"
  lang="en"
  scenario="A dashboard re-renders slowly on every keystroke in an unrelated search box. The React Profiler shows almost every component on the page re-rendering, even ones whose props did not change. Where is the problem?"
  steps={[
    { prompt: "A context provider near the top of the tree has a value that is a fresh object literal every render — every consumer re-renders because the context value is a new reference", reveal: "Correct. `<Ctx.Provider value={{ user, theme }}>` creates a new object every parent render. Context consumers re-render whenever the provider's value changes by reference — and a fresh literal always differs. Every keystroke updates some state high up, the provider re-runs, the value object is new, and the whole subtree of consumers re-renders. Fix: memoise the context value with useMemo so its reference is stable when its contents are." },
    { prompt: "The search box is missing a debounce", reveal: "Debounce would reduce how often this happens, but it does not explain why unrelated components re-render at all. The root cause is the unstable context value; debounce only masks the frequency." },
    { prompt: "React's diffing algorithm is O(n³)", reveal: "React's diff is O(n) thanks to the type and key heuristics. The mass re-render is not the diff being slow — it is too many components being marked dirty by the context change." },
    { prompt: "The list needs virtualization", reveal: "Virtualization helps if a long list is the cost. But here the Profiler shows unrelated components re-rendering — that is a context-value identity problem, not a list-length problem." },
  ]}
/>

<Quiz
  id="sr-quiz-memo-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="A component wrapped in `React.memo` still re-renders every time its parent does. Its props look unchanged. What is the most likely cause?"
  choices={[
    { label: "A prop is an inline object, array, or arrow function — a fresh reference each render, so the memo's Object.is comparison always reports a change", correct: true },
    { label: "React.memo only works on class components", misconception: "React.memo wraps function components specifically. It works fine here — it is being defeated by an unstable prop reference." },
    { label: "React.memo is disabled in development", misconception: "React.memo is active in development. The re-render is real and caused by referential inequality of a prop." },
    { label: "The component uses hooks, which disables memo", misconception: "Hooks do not disable memo. memo gates the render-phase entry; hooks run normally once a render proceeds." },
  ]}
/>

<Quiz
  id="sr-quiz-setState-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Why can you not read the updated state value synchronously on the line right after calling `setState`?"
  choices={[
    { label: "setState schedules an update in a lane; the render that produces the new state has not run yet — the current function still closes over the old value", correct: true },
    { label: "setState is asynchronous because it makes a network request", misconception: "setState makes no network request. It schedules a render; the delay is the scheduled render, not I/O." },
    { label: "React intentionally hides the new value for security", misconception: "There is no security mechanism. The new value simply does not exist yet — the render that computes it is scheduled, not done." },
    { label: "The new value is on a different thread", misconception: "React runs on the single main thread. The value is not elsewhere — it has not been computed yet." },
  ]}
/>

<RFCQuiz
  id="sr-rfc-tearing"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Which React API exists specifically to prevent 'tearing' — components in one commit observing different values of an external store during a concurrent render?"
  choices={[
    { rfc: "useSyncExternalStore", title: "Lets React detect an external store change mid-render and restart, so one commit is consistent", correct: true },
    { rfc: "useState", title: "Manages React-internal state; tearing is about external stores" },
    { rfc: "useMemo", title: "Caches a computed value; unrelated to external-store consistency" },
    { rfc: "useEffect", title: "Runs side effects after commit; does not guarantee mid-render store consistency" },
  ]}
/>

</Explanation>

<KeyTakeaway>Bailout fires when Object.is finds all props and state unchanged. Stable references (useMemo, useCallback) enable it; inline objects defeat it. Hooks are position-keyed on the fiber — call order must never change. useSyncExternalStore prevents tearing from external store mutations between render slices.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-bailout-and-memoisation-retrieval"
  lang="en"
  questions={[
    { q: "A component is wrapped in React.memo but re-renders on every parent render. Walk through diagnosing and fixing it.", a: "React.memo gates the render phase: the component runs only if its props are not referentially equal to last render, compared with Object.is per prop. If it re-renders anyway, a prop is getting a fresh reference each time. Diagnose in the React DevTools Profiler: the render reason will say 'props changed', and hovering shows which prop. The usual culprits: an inline object (style={{color:'red'}}), an inline array, or an arrow function (onClick={() => f()}) created in the parent's JSX — each is a new reference every render. The fix: wrap callbacks in useCallback, wrap derived objects/arrays in useMemo, or hoist truly constant values out of the component." },
    { q: "Why must hooks be called in the same order every render?", a: "React stores hook state on the fiber as a linked list of records, identified purely by call position. If a hook is skipped (e.g., behind an if statement), the cursor slips and every subsequent hook reads the wrong record. There is no name-based lookup — position is the only identity. This is why the Rules of Hooks exist." },
    { q: "What is tearing, and how does useSyncExternalStore prevent it?", a: "Tearing is a committed UI where some components rendered with an old external store value and others with a new one — internally inconsistent. It happens because interruptible renders run in slices and an external store can change between slices. useSyncExternalStore gives React a getSnapshot function. If the snapshot changes between slices, React detects it and restarts the render, so every component in one commit observed the same store value." },
  ]}
/>

<Recap lang="en">
React bails out of re-running a component when `Object.is` finds all its props and state unchanged — it clones the fiber and skips the function call entirely. `React.memo` makes the bailout conditional on props; `useMemo` and `useCallback` preserve the stable references that let the bailout fire. Hooks are stored as a positional linked list on the fiber, which is why call order must be unconditional — skipping a hook misaligns the cursor for every subsequent one. State lives on the fiber, not the component function, which is why state persists across re-renders and disappears on unmount, type change, or key change. In concurrent mode, external stores can cause tearing — components in one commit seeing different values — because a store can mutate between render slices. `useSyncExternalStore` detects this and restarts the render, which is why every serious React state library routes through it.
</Recap>
