---
concepts:
- react-profiler
- bailout-memoisation
- time-slicing
- virtualization
- tearing-prevention
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://react.dev/learn/react-developer-tools
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useSyncExternalStore
status: ready
summary: "Hands-on project — build a janky live data grid, diagnose the re-render storm in the Profiler, and apply the fiber fix ladder until INP holds under 200 ms, proving each step with before/after numbers."
title: 'React fiber: tame a re-render storm'
track: browser
unit: 05-react-fiber
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about bailouts and lanes is not the same as pulling a real UI out of a re-render storm. Build a live, interactive data grid, drive it into jank, then apply the unit's fix ladder — memoise, collocate, time-slice, virtualise, de-tear — until the numbers come back, with Profiler evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: instrument with the React Profiler, read the re-render reason for each fiber, fix at the right level (referential stability, then state collocation, then priority, then virtualisation), defend against tearing, and verify the fix with before/after INP and commit-time numbers.
</Goal>

<ProjectBrief
  id="05-react-fiber-project"
  lessonSlug="05-react-fiber"
  lang="en"
  objective="Take a deliberately janky live data grid (your own or the spec below) and bring its keystroke INP under 200 ms and its commit time under one frame — without rewriting it in another framework — proving each step with React Profiler and Performance-panel measurements."
  requirements={[
    "Build a React 18+ grid of at least 2,000 rows fed by a simulated live store that ticks ~10 times/second (a setInterval mutating an external object is fine). Add a search input that filters the grid and a sort control that reorders it. This is the reproduction harness.",
    "Seed the jank deliberately: hold the search-term state at the top of the tree, pass an inline arrow handler and an inline style object to each row, key rows by array index, and read the live store with a plain module variable (not useSyncExternalStore). Confirm typing in the search box is visibly laggy.",
    "Instrument with the React DevTools Profiler: record a session of typing plus live ticks, find the longest commit, sort fibers by self-time, and capture the 'why did this render' reason for the slow fibers. Capture baseline keystroke INP from the Performance panel (or the web-vitals INP attribution).",
    "Fix referential stability: wrap rows in React.memo and remove the inline arrow/style props so the per-row props are Object.is-stable (useCallback for handlers, hoist or useMemo the style). Re-profile and show rows now bail out when their data is unchanged.",
    "Fix identity and state placement: key rows by a stable id so sorting preserves per-row uncontrolled state, and collocate the search-term state so a keystroke no longer re-renders the whole tree. Document the before/after re-render count.",
    "Fix priority and commit size: wrap the filtered/sorted grid update in startTransition (or useDeferredValue) so it yields to keystrokes, and virtualise the grid so only the ~30 visible rows mount — capping the uninterruptible commit phase regardless of dataset size.",
    "Fix tearing: route the live store through useSyncExternalStore and batch incoming ticks to one store flush per animation frame, so concurrent renders see one consistent snapshot and the grid does not re-render once per tick per symbol.",
  ]}
  acceptance={[
    "A before/after table: keystroke INP (p75), longest commit time, and re-render count per keystroke — all measured under the same live-tick load, not estimated.",
    "Profiler traces before and after: the baseline shows a wide 'parent rendered, props unchanged' storm; the fixed trace shows only the genuinely-changed visible rows rendering, with commit under ~8 ms.",
    "Keystroke INP holds under 200 ms p75 during sustained typing while the store ticks ~10/second, and no Long Animation Frame is attributed to the grid during a 30-second session.",
    "A one-paragraph write-up naming which lever fixed each symptom (referential stability vs state collocation vs priority vs virtualisation vs tearing) and why that lever ranked where it did.",
  ]}
  stretch={[
    "Turn on the React Compiler and re-profile: confirm it inserts the memoisation you wrote by hand, note any component where it bails out conservatively, and explain why understanding the bailout mechanism let you read its output.",
    "Add a deliberate render-phase side effect (an analytics call in a component body), run it under StrictMode, and show the double-count; then move it to useEffect and show the count corrected — documenting why render must be pure.",
    "Add a useLayoutEffect that measures a row with getBoundingClientRect and adjusts a style, then show in the Profiler how it lengthens the commit (forced reflow); compare against doing the same work in useEffect after paint.",
    "Write a one-page on-call runbook: the Profiler triage steps, the 'parent rendered + unchanged props' signature, the fix ladder in order, and a verification checklist tying each fix to a measurable Profiler/INP delta.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real React performance incident: instrument with the Profiler first, read the re-render reason per fiber, then fix at the right level — referential stability before state collocation before priority before virtualisation — defend against tearing with useSyncExternalStore, and verify with before/after INP and commit numbers under identical load. Doing it once on a toy grid makes the production version muscle memory, and makes the React Compiler's output something you can read rather than trust blindly.
</Recap>
