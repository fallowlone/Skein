---
slug: 01-reconciler-overview
lang: en
track: browser
unit: 05-react-fiber
order: 1
title: "What the reconciler does: render vs commit"
summary: "React splits every update into an interruptible render phase that computes the diff and an atomic commit phase that applies DOM mutations — the gap between them is where both performance wins and bugs live."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto: ["02-fiber-data-structure", "03-render-and-commit-phases"]
spiral: ["statefulness"]
concepts: ["reconciler", "render-phase", "commit-phase", "double-buffering", "time-slicing"]
sources:
  - https://react.dev/learn/render-and-commit
  - https://github.com/acdlite/react-fiber-architecture
  - https://web.dev/articles/inp
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
You call `setState`. Somewhere between that call and a pixel changing on screen, React decided which components to re-run, compared two trees, worked out the minimum set of DOM mutations, and applied them — possibly pausing halfway to let a keystroke through. The machine that does this is the fiber reconciler.
</Hook>

<Crux>A tree of components becomes DOM mutations: render computes the diff (interruptible), commit applies it (atomic).</Crux>

<Explanation>

**What the reconciler does.** When your data changes, React figures out the smallest set of page changes that match the new data, and applies only those. The DOM is slow to mutate — touching it a thousand times is far slower than ten. React's job is to compute the ten real changes instead of blindly redoing a thousand.

**The blueprint metaphor.** A naive builder would demolish the house and rebuild it whenever the blueprint changes. React is a smart builder: it keeps the *current* blueprint on the wall, draws the *new* one on a second sheet, lays them side by side, and marks only the differences. Then it sends the crew one work order with exactly those changes. Drawing the second blueprint is the render phase; handing the order over is the commit phase.

**Why two phases.** React splits every update into two phases. The **render phase** walks the component tree, runs your function components, and builds a new tree of *fiber* nodes — this phase is pure, produces no visible change, and since React 18 can be paused, resumed, or thrown away. The **commit phase** takes the finished new tree and applies the DOM mutations in one synchronous, uninterruptible burst. Everything good and everything dangerous about React performance lives in the gap between "interruptible render" and "atomic commit."

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">setState → screen</div>
  <div class="flex flex-col gap-2">
    <div class="rounded bg-amber-50 dark:bg-amber-950/30 border border-amber-200 dark:border-amber-800 px-3 py-2">
      <span class="font-semibold text-amber-800 dark:text-amber-300">RENDER PHASE</span>
      <span class="ml-2 text-ink-2">Re-run components · build fiber tree · diff</span>
      <span class="ml-2 text-xs text-green-700 dark:text-green-400">← interruptible</span>
    </div>
    <div class="rounded bg-blue-50 dark:bg-blue-950/30 border border-blue-200 dark:border-blue-800 px-3 py-2">
      <span class="font-semibold text-blue-800 dark:text-blue-300">COMMIT PHASE</span>
      <span class="ml-2 text-ink-2">Apply DOM mutations · run effects · paint</span>
      <span class="ml-2 text-xs text-red-700 dark:text-red-400">← atomic, never paused</span>
    </div>
  </div>
</div>

<PersonaTag id="bea" lang="en" /> types in a search box that filters a long list. <PersonaTag id="sven" lang="en" /> narrates: "Each keystroke is a state change. React re-runs the components and diffs the new fiber tree against the current one. The list of 800 rows changed — but React finds only the 12 rows that differ and mutates just those 12 DOM nodes. The other 788 are untouched. That is why the box feels instant."

<DragOrder
  id="jr-drag-1"
  pieceSlug="05-react-fiber"
  lang="en"
  prompt="A setState call travels through React to the screen. Drag the steps into order."
  items={[
    { id: "setstate", label: "setState is called — React schedules an update" },
    { id: "render", label: "Render phase: re-run components, build the new fiber tree" },
    { id: "diff", label: "Reconcile: diff the new tree against the current tree" },
    { id: "commit", label: "Commit phase: apply the DOM mutations in one burst" },
    { id: "paint", label: "Browser paints the changed pixels" },
  ]}
  correctOrder={["setstate", "render", "diff", "commit", "paint"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Why does React diff two trees instead of just rewriting the whole DOM?"
  choices={[
    { label: "Mutating the DOM is expensive — diffing finds the minimal set of changes so only what actually changed is touched", correct: true },
    { label: "Diffing is required by the JavaScript language", misconception: "Nothing in JS requires it. It is React's design choice, made precisely because blind DOM rewriting is slow." },
    { label: "The browser cannot rewrite the whole DOM at once", misconception: "The browser can rewrite the whole DOM — it is just slow. React diffs to avoid that slowness, not because it is impossible." },
    { label: "Diffing makes the JavaScript bundle smaller", misconception: "The reconciler is code that adds to the bundle. Its payoff is runtime speed, not bundle size." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Which phase can React pause partway through?"
  choices={[
    { label: "The render phase — building and diffing the tree produces no visible change, so it is safe to interrupt", correct: true },
    { label: "The commit phase", misconception: "Commit applies DOM mutations and must be atomic — pausing it would leave the page in a half-updated, inconsistent state." },
    { label: "Both phases equally", misconception: "Only render is interruptible. Commit is always one synchronous, uninterruptible burst." },
    { label: "Neither — React always runs start to finish", misconception: "Since React 18, the render phase is interruptible. That is the core of concurrent rendering." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="05-react-fiber"
  lang="en"
  setup="In the builder metaphor, React keeps the current blueprint pinned to the wall and draws the new one on a second sheet, so it can compare them. Holding two versions of the tree at once — one shown, one being built — is called what?"
  accepted={["double buffering", "double-buffering", "double buffer", "two trees", "current and workInProgress"]}
  canonical="double buffering"
  explanation="Double buffering means React keeps two fiber trees: the 'current' tree that matches what is on screen, and the 'workInProgress' tree it is building for the next update. It works on the workInProgress copy without disturbing the current one, so a half-built update is never visible. When the workInProgress tree is finished and committed, the two swap roles — the new one becomes current. This is the same technique games use to avoid showing a half-drawn frame."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="React's scheduler does render work in small chunks, then yields to the browser. Roughly how many milliseconds is one chunk before React checks whether it should yield?"
  expected={5}
  tolerance={1}
  unit="ms"
  explanation="React works in slices of about 5 ms, then asks 'should I yield?' If the browser has input or rendering to do, React pauses and resumes later. 5 ms is small enough that even a few of them back-to-back fit inside one 16.67 ms frame alongside the browser's own work — so input stays responsive even during a large render."
/>

<NumbersCard
  id="card-fiber-numbers"
  title="React reconciler numbers"
  stats={[
    { label: "Time-slice before shouldYield()", value: "~5 ms" },
    { label: "Reconciliation complexity", value: "O(n) (heuristic)" },
    { label: "Naive tree-diff complexity", value: "O(n³)" },
    { label: "Lane bitmask width", value: "31 lanes" },
    { label: "Commit phase", value: "Atomic, not sliceable" },
    { label: "Bailout comparison", value: "Object.is per prop" },
  ]}
/>

</Explanation>

<KeyTakeaway>Render is interruptible and pure; commit is atomic and synchronous. React keeps two trees at once so a half-built update is never visible. Every 5 ms the scheduler asks whether to yield to the browser.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-reconciler-overview-retrieval"
  lang="en"
  questions={[
    { q: "Why can the render phase be interrupted but not the commit phase?", a: "Render produces no visible change — it only builds a tree in memory. If interrupted, nothing on screen changes, and React can resume or discard safely. Commit applies DOM mutations; pausing mid-commit would leave the page in a half-updated, inconsistent state visible to the user." },
    { q: "What is double buffering in React?", a: "React keeps two fiber trees: the 'current' tree matching what is on screen, and the 'workInProgress' tree it is building for the next update. It modifies workInProgress without touching current, so the on-screen tree is never in a partial state. On commit, the two trees swap roles." },
    { q: "Roughly how long is one React render slice before it checks shouldYield()?", a: "About 5 ms. After that slice the scheduler checks if the browser has urgent work (input, rendering) and pauses if so, resuming via a MessageChannel task." },
  ]}
/>

<Recap lang="en">
React's reconciler splits every update into a render phase (interruptible, pure, builds the new fiber tree in memory) and a commit phase (atomic, synchronous, applies DOM mutations). The render phase can be paused and resumed because it produces no visible change; the commit phase cannot because it mutates the DOM and must leave the page in a consistent state. React uses double buffering — two trees at once — so the in-progress update is never visible. The scheduler slices render work into ~5 ms chunks and yields to the browser between them, keeping the page responsive even during a large re-render.
</Recap>
