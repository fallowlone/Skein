---
concepts:
- render-phase-purity
- commit-sub-steps
- before-mutation
- useLayoutEffect
- useEffect
- StrictMode
deepensInto:
- 06-bailout-and-memoisation
estMin: 13
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 01-reconciler-overview
- 02-fiber-data-structure
slug: 03-render-and-commit-phases
sources:
- https://react.dev/learn/render-and-commit
- https://react.dev/reference/react/useLayoutEffect
- https://react.dev/learn/keeping-components-pure
spiral:
- statefulness
status: ready
summary: Render must be pure because React 18 may replay it; commit is synchronous,
  atomic, and has three sub-steps — before-mutation, mutation, and layout — each with
  specific APIs and failure modes.
title: Render phase purity and commit phase sub-steps
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A component that writes to a global counter on every render works fine with React 17. With React 18 in StrictMode, that counter doubles. The difference is not a bug — it is React telling you the render phase can replay your function, and side effects there will run more than once.
</Hook>

<Crux>Render must be pure — React 18 may replay it. Commit is atomic: before-mutation, mutation, layout; useLayoutEffect fires before paint.</Crux>

<Explanation>

**Render phase: interruptible, must be pure.** The render phase walks the workInProgress tree, calls each function component, and runs reconciliation. Because React 18 can pause this phase, resume it, or discard it and start over, your component function may be called more than once for a single visible update — and a paused render may sit half-finished while the user interacts. This is why a render must be *pure*: no side effects, no mutating external variables, no DOM reads or writes, no network calls in the function body. A side effect in render runs an unpredictable number of times. React's StrictMode deliberately double-invokes components in development precisely to surface impure renders early. The disciplined place for side effects is `useEffect` (after commit) or an event handler (outside render entirely).

**Commit phase: atomic, synchronous, three sub-steps.** Once the workInProgress tree is fully built, commit runs in one uninterruptible block. It has three sub-steps:

1. **Before-mutation:** React reads any pre-mutation state. This is where `getSnapshotBeforeUpdate` and the cleanup snapshot for `useLayoutEffect` happen.
2. **Mutation:** React applies every DOM change — insertions, deletions, attribute updates, text changes — driven by the effect flags it tagged onto fibers during render.
3. **Layout:** React runs `useLayoutEffect` callbacks and updates refs, synchronously, before the browser paints.

Because commit is synchronous and uninterruptible, a huge tree with thousands of mutations produces one long task — commit is the part of React you cannot time-slice, so the way to keep it short is to keep the number of mutations small (virtualisation, memoisation).

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Commit phase timeline</div>
  <div class="flex flex-col gap-1 text-xs">
    <div class="flex items-center gap-2">
      <span class="w-32 text-right text-ink-2">before-mutation</span>
      <span class="flex-1 rounded bg-purple-100 dark:bg-purple-900/30 px-2 py-1">getSnapshotBeforeUpdate · useLayoutEffect cleanup snapshot</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right text-ink-2">mutation</span>
      <span class="flex-1 rounded bg-orange-100 dark:bg-orange-900/30 px-2 py-1">DOM insertions · deletions · attribute updates · text changes</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right text-ink-2">layout</span>
      <span class="flex-1 rounded bg-blue-100 dark:bg-blue-900/30 px-2 py-1">useLayoutEffect · ref updates → before browser paints</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right text-ink-2">after paint</span>
      <span class="flex-1 rounded bg-green-100 dark:bg-green-900/30 px-2 py-1">useEffect (passive) → separate async task</span>
    </div>
  </div>
</div>

<FlowDiagram
  label="The interruptible render phase precedes the commit phase, whose three sub-steps always run in this fixed order: before-mutation, then mutation, then layout."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "render", label: "Render", sub: "interruptible" },
    { id: "before", label: "Before-mutation", sub: "read snapshots" },
    { id: "mutation", label: "Mutation", sub: "apply DOM" },
    { id: "layout", label: "Layout", sub: "useLayoutEffect" },
  ]}
  edges={[
    { from: "render", to: "before" },
    { from: "before", to: "mutation" },
    { from: "mutation", to: "layout" },
  ]}
  caption="Render can pause; the three commit sub-steps cannot. Before-mutation reads pre-mutation state (getSnapshotBeforeUpdate); mutation applies every DOM change; layout runs useLayoutEffect and updates refs synchronously, before the browser paints. Passive useEffect fires later, after paint."
/>

**useLayoutEffect vs useEffect.** The two differ in *when* they run relative to paint. `useLayoutEffect` fires synchronously in the commit phase's layout step — after DOM mutation, before the browser paints. `useEffect` (a "passive effect") fires asynchronously *after* paint, on a later task. The rule: anything that must run before the user sees the frame — measuring a just-mutated DOM node and adjusting layout to prevent a visible flash — belongs in `useLayoutEffect`. Everything else — data fetching, subscriptions, logging — belongs in `useEffect`, because doing it in `useLayoutEffect` delays paint. Overusing `useLayoutEffect` is a real INP regression: every layout effect is synchronous commit-phase work that pushes the paint later.

<Inset kind="mistake" lang="en">
A `useLayoutEffect` that reads `getBoundingClientRect` then writes a style triggers a synchronous layout inside commit — a forced reflow. The browser must recompute layout mid-commit to answer the `getBoundingClientRect` call, then React writes a style, then the browser must recompute again before painting. This is the commit-phase equivalent of a layout thrash, and it is invisible in normal DevTools unless you look at commit timing.
</Inset>

<Quiz
  id="mid-quiz-rpc-1"
  pieceSlug="05-react-fiber"
  lang="en"
  question="Why must a function component's body be pure (no side effects)?"
  choices={[
    { label: "React 18 can pause, resume, or discard a render — the component may run more than once per visible update, so side effects would run an unpredictable number of times", correct: true },
    { label: "Pure functions run faster", misconception: "Purity is not primarily about speed. It is about correctness under interruptible rendering — an impure render misbehaves when React re-runs it." },
    { label: "JavaScript requires component functions to be pure", misconception: "JS imposes no such rule. It is React's contract, required because the reconciler may call the function multiple times." },
    { label: "Side effects break the bundler", misconception: "The bundler does not care. The constraint is runtime: a render that may be replayed must not cause observable effects each time." },
  ]}
/>

<DragOrder
  id="mid-drag-rpc-1"
  pieceSlug="05-react-fiber"
  lang="en"
  prompt="Order the sub-steps of the commit phase, from the moment the workInProgress tree is finished."
  items={[
    { id: "before", label: "Before-mutation: read pre-mutation state (getSnapshotBeforeUpdate)" },
    { id: "mutation", label: "Mutation: apply DOM insertions, deletions, updates" },
    { id: "swap", label: "Swap: workInProgress tree becomes the current tree" },
    { id: "layout", label: "Layout: run useLayoutEffect, update refs (before paint)" },
    { id: "paint", label: "Browser paints; passive effects (useEffect) run after" },
  ]}
  correctOrder={["before", "mutation", "swap", "layout", "paint"]}
/>

<Quiz
  id="mid-quiz-rpc-2"
  pieceSlug="05-react-fiber"
  lang="en"
  question="You need to measure a DOM node's size right after React updates it and before the user sees the frame. Which API do you use?"
  choices={[
    { label: "useLayoutEffect — it runs synchronously after DOM mutation, before the browser paints, so the measurement is accurate and no flash occurs", correct: true },
    { label: "useEffect — it runs after every render so it is always up to date", misconception: "useEffect runs after paint. If you measure there and then adjust layout, the user sees the old size for one frame (a visible flash) before the correction." },
    { label: "A ref callback on the element", misconception: "A ref callback fires when the DOM node is attached, which happens during the mutation step — but it does not block paint by itself. Measurement + adjustment still needs useLayoutEffect." },
    { label: "componentDidMount in a class component", misconception: "componentDidMount is the class-component equivalent of useLayoutEffect, so it would work — but the question is about the correct hook-based API." },
  ]}
/>

</Explanation>

<KeyTakeaway>Render must be pure — React 18 may replay it. Commit is atomic: before-mutation reads snapshots, mutation applies DOM changes, layout runs useLayoutEffect synchronously before paint. useEffect is passive — it fires after paint on a separate task.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-render-and-commit-phases-retrieval"
  lang="en"
  questions={[
    { q: "Why does React StrictMode double-invoke component functions in development?", a: "To surface impure renders early. If a component has side effects in its body, the double invocation makes those effects fire twice — which exposes bugs that would otherwise appear only intermittently in production when React's concurrent mode replays renders." },
    { q: "List the three sub-steps of the commit phase in order.", a: "1. Before-mutation: reads pre-mutation state (getSnapshotBeforeUpdate, useLayoutEffect cleanup snapshots). 2. Mutation: applies all DOM changes flagged on fibers. 3. Layout: runs useLayoutEffect callbacks and updates refs synchronously, before the browser paints." },
    { q: "What is the failure mode of overusing useLayoutEffect?", a: "Every useLayoutEffect is synchronous commit-phase work that delays the browser paint. Expensive or poorly written layout effects lengthen the commit task, increasing INP. A layout effect that reads getBoundingClientRect then writes a style also triggers a forced synchronous layout inside commit." },
  ]}
/>

<Recap lang="en">
The render phase calls your component functions and may do so multiple times per visible update in React 18's concurrent mode — so the function body must be pure. Any side effect there (writing to a global, mutating a ref, fetching data) runs an unpredictable number of times. The commit phase is the opposite: one synchronous, uninterruptible block with three ordered steps. Before-mutation reads snapshots. Mutation applies every DOM change React tagged during render. Layout runs `useLayoutEffect` and updates refs before the browser paints. Passive `useEffect` hooks are separate and fire after paint. The commit phase cannot be time-sliced, so keeping its mutation count small is the primary lever for commit performance. Now when you see a double side-effect in StrictMode or a measurement flash before paint, you know exactly which phase to blame and which hook to reach for.
</Recap>
