---
concepts:
- man-in-the-middle
- arp-spoofing
- tls-stripping
- interception
- network-trust
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-mitm-and-interception
sources:
- https://attack.mitre.org/tactics/TA0008/
- https://owasp.org/www-project-web-security-testing-guide/
spiral: []
status: ready
summary: Interception only works when the attacker sits on the traffic path. ARP spoofing and TLS stripping put them there; authenticated TLS plus integrity collapses that position into a useless, detected link.
title: MITM and interception
track: security-offensive
unit: 03-network-and-infra-attacks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Everything here is lab-and-authorization-only. Putting yourself between two parties on a network you don't own — sniffing, redirecting, or stripping someone else's traffic — is interception, and in most jurisdictions that's a crime regardless of intent. This lesson teaches the *mechanism* so you can break it as a defender: stand it up in an isolated lab or a CTF range, against hosts you control and have written scope for, and never on production or a network you're a guest on. The whole point is that once you understand *why* interception works, you understand exactly which property a defender adds to make it fail.
</Hook>

By the end of this lesson you'll know what an attacker must achieve to intercept traffic, the two classic ways they get there, and the single cryptographic property that turns a perfect on-path position into a useless one.

<Crux>Interception needs an attacker on the path; authenticated TLS plus integrity turns that position into a useless, detected link.</Crux>

<Explanation>

## What "man-in-the-middle" actually requires

A man-in-the-middle (MITM) attack is one where the attacker sits *between* two communicating parties, relaying — and potentially reading or altering — every message, while each side believes it is talking directly to the other. The MITRE ATT&CK framework files this under Lateral Movement and the broader "Adversary-in-the-Middle" technique, because it's almost never the first foothold — it's something you do *after* you already have a position on the network.

That framing is the key insight a defender needs: **interception is not magic, it's a position.** To read or tamper with a conversation, the attacker's machine must physically or logically be on the path the packets travel. They do not get to reach into a TCP stream from across the internet. They have to *become a hop*. Everything an attacker does — ARP spoofing, rogue DHCP, evil-twin Wi-Fi, DNS spoofing, BGP hijack — is just a different way to answer the same question: *how do I get the victim's packets to flow through me?* And everything a defender does is the inverse: assume the path is hostile and make a hostile path not matter.

Once an attacker holds that position, two outcomes are possible, and they're very different in severity:

- **Passive interception (eavesdropping):** the attacker copies traffic as it passes. Confidentiality is lost. With plaintext protocols (HTTP, plain SMTP, unencrypted database wire protocols) this hands them credentials and data outright.
- **Active interception (tampering):** the attacker modifies traffic in flight — injecting, dropping, or rewriting messages. Now integrity is lost too: they can alter a bank transfer, inject a payload into an HTTP response, or strip a security upgrade.

## How the attacker gets on the path: ARP spoofing

On a typical switched LAN, the most common way onto the path is **ARP spoofing** (also called ARP poisoning). ARP — the Address Resolution Protocol — is how a host on a local network maps an IP address to a MAC address so it can actually frame the packet for delivery. The fatal design property: ARP has **no authentication**. When a host asks "who has 192.168.1.1?", any machine can answer, and hosts cache whatever reply arrives — even unsolicited ones (gratuitous ARP).

So the attacker, already on the LAN, sends forged ARP replies: it tells the victim "the gateway's MAC is *mine*," and tells the gateway "the victim's MAC is *mine*." Both caches now point at the attacker. Traffic that should go victim → gateway now goes victim → attacker → gateway, and back. The attacker forwards packets so the connection still works (otherwise the victim notices the outage), but every byte now passes through them. They've become a hop without touching a single cable.

<FlowDiagram
  label="The ARP spoofing on-path attack: the attacker, already on the LAN, sends forged ARP replies that poison the victim's and gateway's caches so each maps the other's IP to the attacker's MAC; traffic is then relayed victim to attacker to gateway, where it can be read or tampered with; authenticated TLS is what makes the captured bytes useless."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "lan", label: "Attacker on LAN", sub: "same segment" },
    { id: "arp", label: "Forged ARP reply", sub: "no auth" },
    { id: "cache", label: "Poisoned cache", sub: "IP→wrong MAC" },
    { id: "relay", label: "Traffic via attacker", sub: "on path" },
    { id: "tls", label: "Authenticated TLS", sub: "bytes useless" },
  ]}
  edges={[
    { from: "lan", to: "arp" },
    { from: "arp", to: "cache", label: "spoof" },
    { from: "cache", to: "relay", label: "reroute" },
    { from: "relay", to: "tls", label: "the fix" },
  ]}
  caption="ARP spoofing in one path: ARP's lack of authentication lets forged replies redirect the victim's traffic through the attacker. Becoming a hop is the whole attack — authenticated, integrity-protected TLS is what turns the captured stream into noise the attacker can't read or alter undetected."
/>

## Why TLS breaks it — and how TLS stripping tries to dodge that

Here is the property that matters. Properly used **TLS gives you three things at once**: confidentiality (the attacker sees ciphertext, not plaintext), integrity (any tampered byte fails the record's authentication tag and the connection aborts), and — most importantly for MITM — **authentication of the server**. The client validates the server's certificate against a trusted CA chain and checks that the name matches. An on-path attacker can copy the ciphertext all day, but they cannot present a *valid, trusted* certificate for `bank.example` they don't have the private key for. If they try to terminate TLS themselves and re-originate it, the client gets a certificate that doesn't chain to a trusted root — and throws a warning. So a correct TLS deployment converts the attacker's perfect on-path position into a useless one: they hold the wire but not the keys.

That is exactly why the interesting attack isn't breaking TLS — it's **avoiding** it. **TLS stripping** (the classic "sslstrip" technique) is an active MITM that keeps the victim on plaintext HTTP. The user types `bank.example` and the browser first makes an HTTP request; the real site would respond with a redirect to `https://`. The on-path attacker intercepts that redirect, talks HTTPS to the real server itself, and serves the victim plain HTTP — rewriting `https://` links to `http://` as they pass. The victim's browser shows a working page with no certificate error, because there's no TLS on the victim's side to error on. No certificate was forged; the encryption was simply never allowed to start.

<Inset kind="why" lang="en">
Why does TLS stripping fail against a modern site? Because the defense moved the trust decision *off the network path*. **HSTS** (HTTP Strict Transport Security) tells a browser, via a header it remembers, "for this domain, never speak plain HTTP again — upgrade to HTTPS before sending anything." Once HSTS is cached (or, better, the domain is on the browser's preloaded HSTS list shipped in the binary), the very first request is HTTPS, so there is no plaintext redirect for the attacker to intercept. The attacker's whole technique depended on that one cleartext hop existing. HSTS deletes it.
</Inset>

## How a defender reasons about the whole class

The senior move is to stop thinking "can someone get on my path?" — on a shared LAN, hostile Wi-Fi, or a compromised router, assume yes — and start thinking "**what does on-path actually buy them?**" If every sensitive channel is authenticated TLS with integrity, the answer is: ciphertext they can't read, and tampering that aborts the connection instead of succeeding silently. That's defense that doesn't depend on trusting the network, which is the only kind that survives a hostile path. The network-layer hardening (dynamic ARP inspection on switches, 802.1X, DHCP snooping) raises the cost of getting on the path, but it's the *second* line — useful, yet you never want your confidentiality and integrity to rest on the LAN staying clean.

<TradeoffMatrix
  id="01-mitm-interception-tradeoff-1"
  lessonSlug="01-mitm-and-interception"
  lang="en"
  prompt="In an authorized internal pentest you've achieved an ARP-spoofing on-path position between a workstation and the gateway. The target app is served over HTTPS with HSTS preloaded. Which conclusion should your report lead with?"
  options={[
    { name: "On-path interception alone yields only ciphertext here; the confidentiality/integrity risk is mitigated by authenticated TLS + HSTS — escalation would require a separate trust failure (rogue CA, client cert-pin bypass, endpoint compromise)", summary: "Position ≠ plaintext.", correct: true, justification: "This is the correct senior read. Becoming a hop is necessary but not sufficient: TLS authenticates the server and protects confidentiality and integrity, and HSTS preload removes the cleartext hop that stripping needs. So the on-path position is real but its impact is bounded to metadata (timing, SNI, traffic volume) unless the attacker can defeat the trust anchor itself. The finding is the residual exposure, not a full compromise." },
    { name: "Full compromise — once you're on the path you can read and modify all traffic, TLS included", summary: "On-path = game over.", justification: "This is the exact misconception the lesson targets. On-path lets you copy and reroute packets, but TLS encryption, integrity tags, and certificate authentication mean you get ciphertext and any tampering aborts the connection. Position is not plaintext." },
    { name: "Just present your own TLS certificate for the domain and decrypt everything", summary: "Forge the cert.", justification: "You can't present a valid, trusted certificate for a domain whose private key you don't hold; the client validates the chain and the name and will throw an error. This only works if the trust store is already compromised or the client doesn't validate — a separate failure, not a property of being on-path." },
    { name: "TLS-strip the connection to downgrade it to HTTP and read the credentials", summary: "Downgrade to HTTP.", justification: "Stripping depends on an initial cleartext HTTP hop to intercept. With HSTS preloaded, the browser's first request is already HTTPS, so there is no plaintext redirect to hijack. Against a preloaded domain this technique is dead." },
  ]}
/>

<Quiz
  id="01-mitm-interception-quiz-1"
  lessonSlug="01-mitm-and-interception"
  lang="en"
  question="ARP spoofing works because of which property of ARP?"
  choices={[
    { label: "ARP has no authentication, so any host can answer 'who has this IP' — including with unsolicited replies that get cached", correct: true },
    { label: "ARP traffic is encrypted, so the switch can't inspect it", misconception: "ARP is plaintext, not encrypted. The flaw is the opposite of secrecy — it's the total absence of authentication on replies, so forged answers are trusted." },
    { label: "ARP requires a password the attacker can brute-force", misconception: "There is no authentication step in ARP at all, password or otherwise. Hosts simply cache whatever reply arrives, which is precisely why forged replies work." },
    { label: "ARP only works across the internet, so remote attackers can poison it", misconception: "ARP is a link-local protocol confined to the local segment. That's why the attacker must already be on the same LAN to spoof it — it's not remotely reachable." },
  ]}
/>

<Quiz
  id="01-mitm-interception-quiz-2"
  lessonSlug="01-mitm-and-interception"
  lang="en"
  question="Why does an on-path attacker fail to read a correctly deployed TLS session, even with a perfect interception position?"
  choices={[
    { label: "TLS authenticates the server's certificate and encrypts + integrity-protects the data, so the attacker gets unreadable ciphertext and can't forge a trusted cert", correct: true },
    { label: "TLS detects the attacker's MAC address and refuses to connect", misconception: "TLS operates above the link layer and knows nothing about MAC addresses. Its protection comes from cryptography — encryption, integrity tags, and certificate authentication — not from spotting the attacker's hardware." },
    { label: "Being on-path is impossible once TLS is enabled", misconception: "TLS doesn't stop anyone from getting on the path; ARP spoofing still works. What TLS does is make the captured bytes ciphertext and make tampering abort — the position becomes useless, not unreachable." },
    { label: "The switch encrypts ARP once TLS is turned on", misconception: "TLS is an application/transport-layer protocol and has no effect on ARP or the switch. ARP stays unauthenticated; TLS just makes the intercepted traffic worthless." },
  ]}
/>

<DragOrder
  id="01-mitm-interception-drag-1"
  lessonSlug="01-mitm-and-interception"
  lang="en"
  prompt="Order the stages of an ARP-spoofing-then-strip interception attempt, from getting on the path to where a hardened defense breaks it:"
  items={[
    { id: "a", label: "Attacker is already on the same LAN segment as the victim" },
    { id: "b", label: "Forged ARP replies poison the victim's and gateway's caches" },
    { id: "c", label: "Victim's traffic is rerouted through the attacker (on-path)" },
    { id: "d", label: "Attacker tries TLS stripping to keep the victim on plaintext HTTP" },
    { id: "e", label: "HSTS preload forces HTTPS first — no cleartext hop to strip; attack fails" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Interception is a *position*, not a magic power: the attacker must become a hop (ARP spoofing, evil twin, rogue DHCP). Authenticated TLS + integrity makes that position worthless — ciphertext they can't read, tampering that aborts — and HSTS kills the cleartext hop stripping depends on. Defend by assuming the path is hostile, not by trusting the LAN.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-mitm-interception-retrieval"
  lang="en"
  questions={[
    { q: "Explain why a man-in-the-middle attack is described as 'a position, not a power,' and walk through how ARP spoofing achieves that position.", a: "A MITM attack is described as a position because the attacker fundamentally cannot reach into a conversation between two parties from anywhere on the internet — they must become a hop on the actual path the packets travel. MITRE ATT&CK files this under Lateral Movement / Adversary-in-the-Middle precisely because it's something done after you already have a network foothold, not a remote magic trick. ARP spoofing is the classic way onto a switched LAN: ARP maps IP addresses to MAC addresses and has no authentication, so any host can answer 'who has this IP' and hosts cache whatever reply arrives, including unsolicited (gratuitous) ones. The attacker, already on the segment, sends forged ARP replies telling the victim that the gateway's MAC is the attacker's, and telling the gateway that the victim's MAC is the attacker's. Both caches now point at the attacker, so traffic flows victim → attacker → gateway and back; the attacker forwards packets so the connection keeps working while reading or tampering with every byte. Becoming a hop without touching a cable is the entire attack — which is why the defender's job is to make holding that hop not matter." },
    { q: "Why does correctly deployed TLS defeat interception, and what is TLS stripping trying to dodge — and how does HSTS shut it down?", a: "Properly deployed TLS gives confidentiality (the attacker sees only ciphertext), integrity (any tampered byte fails the record's authentication tag and the connection aborts), and — decisive for MITM — server authentication: the client validates the certificate against a trusted CA chain and checks the name. An on-path attacker can copy ciphertext but cannot present a valid trusted certificate for a domain whose private key they don't hold; terminating and re-originating TLS yields an untrusted cert and a browser warning. So TLS converts a perfect on-path position into a useless one — they hold the wire but not the keys. That's why the real technique is to avoid TLS, not break it: TLS stripping (sslstrip) is an active MITM that keeps the victim on plaintext HTTP. The user's first HTTP request would normally redirect to https://; the on-path attacker intercepts that redirect, speaks HTTPS to the real server itself, and serves the victim plain HTTP with https:// links rewritten to http://, so no certificate error ever appears. HSTS shuts this down by moving the trust decision off the network path: a remembered HSTS header (or, stronger, the browser's preloaded HSTS list shipped in the binary) makes the browser upgrade to HTTPS before sending anything, so the very first request is encrypted. The cleartext hop the attacker depended on never exists, and the strip has nothing to intercept." },
  ]}
/>

<Recap lang="en">
Interception is a position, not a power. A man-in-the-middle must become a hop on the path the packets actually travel — MITRE files it under Lateral Movement for that reason — and ARP spoofing is the classic way in: ARP has no authentication, so forged replies poison the victim's and gateway's caches and reroute traffic through the attacker. Once on-path they can eavesdrop (lose confidentiality) or tamper (lose integrity). The thing that defeats this isn't keeping attackers off the LAN — assume you can't — it's authenticated TLS with integrity: the attacker gets ciphertext they can't read and any tampering aborts the connection, because they can't forge a certificate they don't hold the key for. That's why the live technique is TLS stripping — avoiding TLS by keeping the victim on a cleartext HTTP hop — and why HSTS preload kills it by forcing HTTPS first so no cleartext hop exists. The next time you reason about a hostile network, your first question is no longer "can someone get on the path?" but "if they do, do they get plaintext — or ciphertext and a torn-down connection?"
</Recap>
