---
concepts:
- privilege-escalation
- suid
- misconfiguration
- kernel-exploit
- local-attack
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-privilege-escalation
sources:
- https://attack.mitre.org/tactics/TA0008/
- https://owasp.org/www-project-web-security-testing-guide/
spiral: []
status: ready
summary: Privilege escalation turns a low-priv foothold into root or SYSTEM by abusing a misconfig the host already shipped — a SUID binary, a sudo gap, a writable service, a stale kernel. Learn the escalation path the attacker enumerates, so you close the rung first.
title: Privilege escalation
track: security-offensive
unit: 03-network-and-infra-attacks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
On an authorized test, the foothold is unglamorous: a deploy script leaked a service-account password, and now there's a shell running as `www-data` on a Linux box. That account can read the web root and not much else — it can't touch `/etc/shadow`, can't read other tenants' data, can't install a persistent backdoor. A junior tester stops here and writes "low severity, limited account." A senior keeps going, because the foothold is rarely where the value is. Within ten minutes the enumeration spits out a list: a backup script owned by root is world-writable and runs from cron every five minutes; a custom binary has the SUID bit set and shells out to `tar` without an absolute path; sudo is configured to let `www-data` run one "harmless" admin tool — that happens to spawn a subshell. None of these is an exploit. Each is a configuration the host *shipped that way*. The attacker doesn't break in again — they walk up a staircase the system built for them, and step out as `root`.
</Hook>

By the end of this lesson you'll be able to explain how an attacker enumerates the local escalation path on Linux and Windows, why each rung is a misconfiguration and not a "hack," and the defender's reflex that removes the rung before it's climbed.

<Crux>Privilege escalation turns a foothold into root by abusing a misconfig the host already shipped — SUID, a sudo gap, a stale kernel.</Crux>

<Explanation>

## Authorization first: this is a defender's autopsy

Everything below assumes a **signed engagement with explicit scope** — a deliberately vulnerable lab, a CTF box, a machine you own, or a system named in a bug-bounty program. Running enumeration tooling against a host you have no authorization for, or pivoting from a low account to root on someone else's infrastructure, is unauthorized access in nearly every jurisdiction, full stop. We study the attacker's enumeration for exactly one reason: **you cannot reliably close an escalation path you only understand as a checklist item.** Read this as the autopsy a defender runs on their own host — learning to see the SUID bit and the sudo rule the way the attacker does, so the rung is gone from the design before any tester finds it. No weaponized payloads here, only the mechanism that tells you which rung to remove.

## The shape of the attack: foothold, enumerate, abuse, persist

Privilege escalation is not a single exploit; it is a *phase*. The attacker already has code execution as some low-privilege principal — a web user like `www-data` or `IIS APPPOOL`, a compromised service account, a CI runner. The goal is **vertical** movement: from that principal to a higher one (`root`, `SYSTEM`, `Administrator`, a domain admin), which unlocks reading every file, installing persistence, and pivoting to other hosts. The loop is always the same: enumerate the local environment for anything misconfigured, find a primitive that crosses a trust boundary, abuse it to execute code as the higher principal, then establish persistence so the foothold survives a reboot or a password reset.

The crucial mental model is that escalation lives in the gap between *who runs a thing* and *who controls its inputs*. Every rung is some component that executes with high privilege but takes an input — a path, a file, a binary, an environment variable — that a low-privilege user can influence. The attacker's whole job in this phase is to enumerate where those two facts diverge.

<FlowDiagram
  label="The local privilege-escalation path: an attacker with a low-privilege foothold enumerates the host, finds a misconfigured component that runs with high privilege but trusts low-privilege input, abuses it to execute code as root or SYSTEM, then establishes persistence; least privilege plus a hardened config removes the abusable rung."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "foothold", label: "Low-priv foothold", sub: "www-data shell" },
    { id: "enum", label: "Enumerate host", sub: "SUID, sudo, cron" },
    { id: "primitive", label: "Find misconfig", sub: "high-priv, low input" },
    { id: "root", label: "Execute as root", sub: "boundary crossed" },
    { id: "fix", label: "Least privilege", sub: "remove the rung" },
  ]}
  edges={[
    { from: "foothold", to: "enum" },
    { from: "enum", to: "primitive", label: "find rung" },
    { from: "primitive", to: "root", label: "abuse input" },
    { from: "primitive", to: "fix", label: "the fix" },
  ]}
  caption="Local privesc in one path: a foothold enumerates the host, finds a component that runs privileged but trusts attacker-influenced input, and abuses it to cross the trust boundary to root. Removing the misconfiguration — least privilege on the privileged component, no writable path, no input the low account controls — deletes the rung instead of detecting the climb."
/>

## Linux: the four rungs that recur

On Linux the same handful of misconfigurations appear over and over, and a senior defender learns to spot each in their own fleet before a tester does.

**SUID/SGID binaries.** A file with the SUID bit runs as its *owner* regardless of who launches it — so a SUID-root binary executes as root even when `www-data` runs it. That is by design for tools like `passwd` that need a privileged moment. The danger is a SUID binary that does something it shouldn't: shells out to a helper by relative name (so the attacker prepends a malicious directory to `PATH`), reads or writes a file the caller names, or is a general-purpose interpreter that should never be SUID at all. A SUID-root program that calls `system("tar ...")` hands the attacker root the moment they put a fake `tar` earlier in `PATH`. The fix is *least privilege on the binary*: drop the SUID bit unless it is genuinely required, and if it is, the binary must use absolute paths, sanitize the environment, and expose no file or command the caller can steer.

**sudo misconfiguration.** `sudo` is meant to grant *narrow* elevation, but a sloppy `/etc/sudoers` grants wide. Allowing a low account to run an editor, a pager, or a scripting interpreter as root is effectively granting a root shell, because those programs can spawn subshells or read arbitrary files. `NOPASSWD` on a binary with a shell-escape is the classic gift. The defender's reflex: every sudo rule is an *exception* to deny-by-default, so audit each one by asking "can this exact command, as configured, be turned into arbitrary code execution?" — and if yes, it is a root grant in disguise.

**Writable privileged jobs and services.** A cron job, systemd unit, or service script that runs as root but lives in a world-writable file (or calls a script in a world-writable directory) lets any user rewrite what root will execute next. The misconfig is the *file permission*, not the scheduler. The fix is to lock ownership and permissions on everything a privileged scheduler touches, so a low account can never edit the next thing root runs.

**Kernel and stale-component exploits.** When configuration is clean, the attacker falls back to a known vulnerability in the kernel or a privileged daemon — a CVE in a `setuid` helper, a kernel bug that grants a local user root. This is the rung you close not with permissions but with *patching*: an unpatched, end-of-life kernel is a standing local-root primitive for anyone who already has a shell. Kernel exploits are noisier and can crash the box, so a careful attacker tries them last — which is exactly why they signal a host that fell behind on updates.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Rung</th>
        <th class="border border-border px-3 py-2 text-left">What the attacker enumerates</th>
        <th class="border border-border px-3 py-2 text-left">Why it crosses the boundary</th>
        <th class="border border-border px-3 py-2 text-left">Defender's fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SUID / SGID binary</td>
        <td class="border border-border px-3 py-2">Files with the setuid bit set</td>
        <td class="border border-border px-3 py-2">Runs as owner (root) but trusts caller's PATH/args</td>
        <td class="border border-border px-3 py-2">Drop the bit; absolute paths; clean env</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">sudo rule</td>
        <td class="border border-border px-3 py-2">What this account may run as root</td>
        <td class="border border-border px-3 py-2">Program with a shell-escape = root shell</td>
        <td class="border border-border px-3 py-2">Narrow rules; no escapable binaries</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Writable cron / service</td>
        <td class="border border-border px-3 py-2">Permissions on root-run scripts</td>
        <td class="border border-border px-3 py-2">Low user rewrites what root runs next</td>
        <td class="border border-border px-3 py-2">Lock ownership + perms on the path</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Kernel / stale daemon</td>
        <td class="border border-border px-3 py-2">Kernel version, EOL components</td>
        <td class="border border-border px-3 py-2">A known local-root CVE in privileged code</td>
        <td class="border border-border px-3 py-2">Patch; retire end-of-life kernels</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Windows: unquoted path</td>
        <td class="border border-border px-3 py-2">Service binary paths with spaces</td>
        <td class="border border-border px-3 py-2">SYSTEM service runs an attacker file first</td>
        <td class="border border-border px-3 py-2">Quote paths; lock the install dir</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Windows: token / DLL</td>
        <td class="border border-border px-3 py-2">Privileges, weak service ACLs, DLL paths</td>
        <td class="border border-border px-3 py-2">Token abuse or a planted DLL runs as SYSTEM</td>
        <td class="border border-border px-3 py-2">Least privilege; tight ACLs; signed DLLs</td>
      </tr>
    </tbody>
  </table>
</div>

## Windows: same gap, different names

Windows escalation rhymes with Linux — the same "runs high, trusts low" gap — under different mechanisms. **Unquoted service paths** are the textbook example: a service registered as `C:\Program Files\My App\svc.exe` without quotes, running as `SYSTEM`, makes Windows try `C:\Program.exe` first; if a low user can write to `C:\`, their binary runs as `SYSTEM`. **Weak service permissions** let a low user reconfigure the binary a `SYSTEM` service launches. **DLL hijacking** plants a malicious library where a privileged process loads it by an unsafe search order. **Token impersonation** abuses certain privileges (held by some service accounts) to impersonate a `SYSTEM` token. **AlwaysInstallElevated** — a policy that runs MSI installers as `SYSTEM` — turns any user-supplied installer into a root grant. Different surface, identical root cause: a high-privilege component trusts a path, a permission, or a token that a low-privilege user can influence.

<Inset kind="why" lang="en">
Why do attackers try a clean-config primitive (SUID, sudo, writable cron) *before* a kernel exploit, when the kernel bug is the more powerful tool? Two reasons, both operational. First, reliability: a misconfiguration is deterministic — abuse the SUID binary and you get root every time — whereas a kernel exploit can fail against an unexpected patch level and, worse, panic the box, ending the engagement and lighting up monitoring. Second, stealth: walking up a configuration the host shipped looks like normal use of normal tools, while loading a kernel exploit is loud and detectable. The defender's takeaway is the mirror image: a clean, least-privilege configuration removes the *quiet* rungs an attacker prefers, forcing them onto the noisy kernel path where your patching and your detection actually get a chance to win.
</Inset>

## How a defender removes the staircase

The unifying defense is **least privilege at every layer**, because every rung is a place where privilege was granted more broadly than the task required. Assume a foothold *will* happen — phishing, a leaked credential, an app bug — and design so the foothold is a dead end. Audit SUID/SGID inventory and strip the bit from anything that doesn't earn it; treat each sudo rule as an explicit exception and reject any program with a shell-escape; lock ownership and permissions on every file a privileged scheduler or service touches; keep kernels and privileged daemons patched so there is no standing local-root CVE; on Windows, quote service paths, tighten service ACLs, and disable `AlwaysInstallElevated`. Then add detection for the climb itself — alert on new SUID files, on edits to privileged units, on a service account spawning a shell — so even a rung you missed gets caught. The senior reflex across all of it is the same as everywhere in security: **deny by default, grant the minimum, and assume the lower boundary will be breached.**

<TradeoffMatrix
  id="02-privesc-tradeoff-1"
  lessonSlug="02-privilege-escalation"
  lang="en"
  prompt="On an authorized test, a SUID-root binary shells out to `tar` by relative name, and the low account controls `PATH`. Pick the fix that actually closes the escalation."
  options={[
    { name: "Drop the SUID bit unless it's genuinely required; if it is, make the binary use absolute paths, sanitize the environment, and expose no caller-steerable command or file", summary: "Least privilege on the binary.", correct: true, justification: "The root cause is a privileged binary that trusts caller-controlled input (PATH, then a relative `tar`). The structural fix is to remove the privilege if it isn't needed, and if it is, eliminate the attacker's influence: absolute paths so PATH can't redirect the helper, a scrubbed environment, and no file or command the caller can steer. That's least privilege applied to the exact rung — the only fix that deletes the primitive rather than masking it." },
    { name: "Rename the binary and remove its execute permission for `www-data` only", summary: "Hide the binary.", justification: "Per-user permission tweaks are brittle obscurity. The SUID misconfiguration remains for any account that can reach it (and footholds land in many accounts), and renaming changes nothing about the relative-path trust. The privileged-trusts-untrusted-input gap is still open." },
    { name: "Set `PATH` to a safe value in the low user's shell profile", summary: "Fix the caller's PATH.", justification: "The attacker controls their own environment, so a default in a profile they can override is no control at all — they simply set PATH back before invoking the binary. You cannot fix an input the attacker owns by setting it on their side; the binary itself must not trust it." },
    { name: "Add an EDR rule that alerts when the SUID binary is executed", summary: "Detect the run.", justification: "Detection is a valuable supplement but not the fix — it tells you root was taken, after the fact, and only if someone acts on the alert in time. The escalation still succeeds. The rung has to be removed (least privilege on the binary), with detection layered on top, not in place of it." },
  ]}
/>

<Quiz
  id="02-privesc-quiz-1"
  lessonSlug="02-privilege-escalation"
  lang="en"
  question="A Windows service runs as SYSTEM with the binary path `C:\Program Files\My App\svc.exe` registered without quotes, and a low user can write to `C:\`. What's the class and the precise fix?"
  choices={[
    { label: "Unquoted service path — quote the registered path and lock down the writable directory so SYSTEM can't be tricked into running an attacker's binary first", correct: true },
    { label: "Kernel exploit — patch the Windows kernel to the latest build", misconception: "Nothing here exploits a kernel bug. The service simply runs an attacker-writable path because the unquoted path makes Windows try C:\\Program.exe first. The fix is quoting the path and tightening the directory, not patching the kernel." },
    { label: "DLL hijacking — sign every DLL the service loads", misconception: "This is path resolution of the executable itself, not a DLL search-order problem. Signing DLLs doesn't change that Windows tries C:\\Program.exe before the real binary; quote the path instead." },
    { label: "Not a vulnerability — only an admin can register services", misconception: "An admin registered it insecurely, but the abuse is by a low user who can write to C:\\ — they didn't need admin to plant the binary. The unquoted path plus the writable directory is the escalation." },
  ]}
/>

<Quiz
  id="02-privesc-quiz-2"
  lessonSlug="02-privilege-escalation"
  lang="en"
  question="Why does a careful attacker try misconfiguration-based escalation (SUID, sudo, writable cron) before reaching for a kernel exploit?"
  choices={[
    { label: "Misconfig abuse is deterministic and quiet; kernel exploits can fail on an unexpected patch level, may panic the box, and are loud to detection", correct: true },
    { label: "Kernel exploits don't actually grant root, only misconfigurations do", misconception: "Kernel exploits absolutely can grant local root — that's why they exist. The reason to try them last is reliability and stealth, not that they don't work." },
    { label: "Misconfigurations are illegal to exploit but kernel bugs are not", misconception: "Authorization, not the rung type, determines legality — on an unauthorized host both are illegal, on an authorized test both are in scope. Legality isn't the selection criterion." },
    { label: "Kernel exploits require physical access to the machine", misconception: "Local kernel exploits run from an existing shell with no physical access. The attacker already has a foothold; the choice is about reliability and noise, not physical proximity." },
  ]}
/>

<DragOrder
  id="02-privesc-drag-1"
  lessonSlug="02-privilege-escalation"
  lang="en"
  prompt="Order the phases of a local privilege-escalation attack, from the starting foothold to a durable hold on the host:"
  items={[
    { id: "a", label: "Attacker has code execution as a low-privilege account (e.g. www-data)" },
    { id: "b", label: "Enumerate the host for misconfigurations (SUID, sudo, writable jobs, kernel version)" },
    { id: "c", label: "Find a component that runs privileged but trusts low-privilege input" },
    { id: "d", label: "Abuse that input to execute code as root / SYSTEM" },
    { id: "e", label: "Establish persistence so the elevated access survives reboot or reset" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Privilege escalation is a phase, not an exploit: a foothold enumerates the host for a component that runs privileged but trusts low-privilege input — a SUID binary, a sudo gap, a writable cron, an unquoted Windows service, a stale kernel — and abuses that gap to reach root or SYSTEM. The defense is least privilege at every layer plus patching, so the foothold is a dead end and any climb is detected.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-privesc-retrieval"
  lang="en"
  questions={[
    { q: "Explain the mental model of local privilege escalation and why every rung is a misconfiguration rather than a 'hack'.", a: "Privilege escalation is a phase, not a single exploit: the attacker already has code execution as a low-privilege principal (www-data, IIS APPPOOL, a service account, a CI runner) and wants vertical movement to a higher one — root, SYSTEM, Administrator — which unlocks reading every file, persistence, and pivoting. The unifying mental model is that escalation lives in the gap between *who runs a thing* and *who controls its inputs*: every rung is a component that executes with high privilege but takes an input — a PATH, a file, a binary, a permission, a token — that a low-privilege user can influence. That's why each rung is a misconfiguration and not a 'hack': the SUID bit, the sudo rule, the world-writable cron file, the unquoted service path were all shipped that way by configuration. The attacker doesn't break in again; they walk up a staircase the system built. The loop is always: enumerate the local environment for these gaps, find a primitive that crosses a trust boundary, abuse it to run code as the higher principal, then persist. The defender's job is to find where 'runs high' and 'trusts low' diverge and remove the divergence." },
    { q: "Name the recurring Linux and Windows escalation rungs and the single defensive principle that closes them, and explain why attackers prefer misconfig over kernel exploits.", a: "Linux rungs: (1) SUID/SGID binaries that run as root but trust caller input — a relative-path helper the attacker redirects via PATH, or a file the caller names — fixed by dropping the bit unless required and, if required, absolute paths plus a clean environment; (2) sudo rules that let a low account run a program with a shell-escape (editor, pager, interpreter), which is a root shell in disguise — fixed by narrow, escape-free rules; (3) world-writable cron jobs or service scripts run by root, where the misconfig is the file permission — fixed by locking ownership and permissions on everything a privileged scheduler touches; (4) kernel or stale-daemon CVEs — fixed by patching and retiring EOL kernels. Windows rungs are the same gap under different names: unquoted service paths, weak service ACLs, DLL hijacking, token impersonation, AlwaysInstallElevated. The single principle that closes all of them is least privilege at every layer: assume a foothold will happen and grant each component only the privilege its task needs, so the foothold is a dead end, with detection on the climb as a backstop. Attackers prefer misconfiguration over a kernel exploit because misconfig abuse is deterministic (root every time) and quiet (normal tools, normal use), whereas a kernel exploit can fail on an unexpected patch level, may panic the box and end the engagement, and is loud to detection — so a clean least-privilege config removes the quiet rungs and forces the attacker onto the noisy kernel path where patching and monitoring can win." },
  ]}
/>

<Recap lang="en">
Local privilege escalation is a phase, not a single exploit: an attacker with a low-privilege foothold (www-data, a service account) enumerates the host for a component that runs with high privilege but trusts an input a low user controls, abuses that gap to execute code as root or SYSTEM, and then persists. Every rung is a misconfiguration the host already shipped, not a magic hack — on Linux a SUID binary that trusts a relative path or the caller's PATH, a sudo rule for a program with a shell-escape, a world-writable cron or service file, or a stale kernel with a known local-root CVE; on Windows an unquoted service path, weak service ACLs, DLL hijacking, token impersonation, or AlwaysInstallElevated. They all share one shape — high privilege trusting low-privilege input — and one fix: least privilege at every layer, so a foothold is a dead end, plus patching to remove standing kernel CVEs and detection to catch the climb you missed. Attackers reach for misconfigurations before kernel exploits because they're deterministic and quiet, which is exactly why a clean config removes the rungs they prefer. Now when you read a privileged binary, sudo rule, cron job, or service definition, your first question is: does this run with more privilege than its task needs, and can a low account influence what it does?
</Recap>
