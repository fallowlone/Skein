---
concepts:
- lateral-movement
- pivoting
- pass-the-hash
- network-pivot
- segmentation
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-pivoting-and-lateral-movement
sources:
- https://attack.mitre.org/tactics/TA0008/
- https://owasp.org/www-project-web-security-testing-guide/
spiral: []
status: ready
summary: One host gets popped; the breach happens when the attacker turns it into a stepping stone. Pivoting routes traffic through it, lateral movement reuses identity. Segmentation plus identity hygiene break both.
title: Pivoting and lateral movement
track: security-offensive
unit: 03-network-and-infra-attacks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Everything here is lab-and-authorization-only. Tunnelling through a machine you popped to reach hosts you weren't asked to touch, or reusing harvested credentials to log into other systems, is exactly the activity an engagement's *scope* exists to bound — do it outside written scope and on a network you don't own and it is unauthorized access, a crime in most jurisdictions regardless of intent. Stand this up in an isolated lab, a CTF range, or an internal engagement with a signed rules-of-engagement that names the segments and the credential-reuse you're cleared to attempt. We teach the mechanism so a defender can see exactly where the chain breaks — because the same move that wins for the attacker is the one your architecture is supposed to make worthless.
</Hook>

By the end of this lesson you'll know the difference between pivoting and lateral movement, why a single foothold so rarely *is* the breach, and the two design properties — segmentation and identity hygiene — that turn one popped host into a dead end.

<Crux>A foothold isn't the breach: pivoting reuses reach, lateral movement reuses identity. Segmentation and identity hygiene stop both.</Crux>

<Explanation>

## A foothold is not a breach

When an attacker first lands on a machine — a vulnerable web server, a phished laptop, an exposed Jenkins box — they almost never have what they actually came for. The crown jewels (the database, the domain controller, the secrets vault) live elsewhere, usually on a different network segment the initial host can reach but the internet cannot. The entire mid-game of a real intrusion is getting *from* the foothold *to* the target. MITRE ATT&CK calls this whole tactic **Lateral Movement (TA0008)**, and it is where a contained incident becomes a company-ending one.

There are two distinct moves under that umbrella, and conflating them is the most common junior mistake:

- **Pivoting** is a *network* move. The compromised host becomes a relay: the attacker tunnels traffic *through* it to reach systems they can't route to directly. Their tools still run on their own machine; the popped box just forwards packets. This is how an attacker on the public web server reaches the private database subnet behind it.
- **Lateral movement** is an *identity* move. The attacker uses credentials, tokens, keys, or trust relationships harvested from the foothold to *authenticate* to other hosts as a legitimate user. No exploit fires — they log in. This is how one set of cached admin credentials becomes the entire fleet.

In practice they interleave: you pivot to gain network reach to a host, then move laterally by authenticating to it with stolen identity. Hold that distinction, because the defenses are different — one is network architecture, the other is identity discipline.

## Pivoting: turning a host into a relay

The defining property of a pivot is that it costs the attacker no new credential — it's pure reachability. Say the foothold is a DMZ web server that, by design, can talk to an internal app subnet (it has to; it serves the app). The attacker can't route to that subnet from the internet, but the web server can. So they turn the web server into a proxy: a SOCKS tunnel, an SSH port-forward, or a reverse connection back to their infrastructure. Now every tool on the attacker's laptop — scanners, clients, exploit frameworks — reaches the internal subnet *as if the attacker were sitting on the DMZ box*, because, network-wise, they are.

This is why **flat networks are catastrophic**. If every host can reach every other host on a useful port, then a single foothold gives the attacker line-of-sight to everything; pivoting is trivial because there's nothing to pivot *around*. The defensive property that breaks this is **segmentation**: split the network into zones with explicit, least-privilege allow-rules between them, so the DMZ web server can reach *only* the one app port it genuinely needs and nothing else. Now a pivot through it buys the attacker reach to one tightly-scoped service instead of the whole estate — the relay still works, but it relays into a wall.

<FlowDiagram
  label="The pivot-then-move chain: an attacker lands on a DMZ foothold, turns it into a SOCKS or SSH relay to reach an internal subnet they couldn't route to directly, scans and authenticates onward with credentials harvested from the foothold, and reaches the crown-jewel target; segmentation that limits what the foothold can reach is what collapses the relay into a dead end."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "foothold", label: "Foothold popped", sub: "DMZ web host" },
    { id: "relay", label: "Pivot relay", sub: "SOCKS / SSH fwd" },
    { id: "reach", label: "Internal reach", sub: "app subnet" },
    { id: "move", label: "Lateral move", sub: "reused creds" },
    { id: "seg", label: "Segmentation", sub: "relay → wall" },
  ]}
  edges={[
    { from: "foothold", to: "relay" },
    { from: "relay", to: "reach", label: "tunnel" },
    { from: "reach", to: "move", label: "authenticate" },
    { from: "relay", to: "seg", label: "the fix" },
  ]}
  caption="Pivot then move in one path: a foothold becomes a relay (pivot, a network move), then the attacker authenticates onward with harvested identity (lateral movement, an identity move). Segmentation scopes what the relay can reach to a single allowed service; identity hygiene strips the credentials the onward authentication needs."
/>

## Lateral movement: reusing identity, not exploits

The unsettling thing about real lateral movement is how little of it looks like hacking. Once an attacker has admin on one Windows host, they can often read credential material straight out of memory or the local secrets store, then *reuse* it to authenticate elsewhere — and the most-feared form is **pass-the-hash**. In Windows NTLM authentication, the server never needs the user's plaintext password: it accepts a cryptographic *hash* of it as proof of identity. So an attacker who steals the NTLM hash of a privileged account from one machine can present that hash directly to another machine and authenticate as that user — without ever cracking the password. The hash *is* the credential. Crack nothing, phish nothing; just replay the secret.

This is why **credential reuse is the multiplier that turns one host into a thousand.** The classic disaster is a single local-administrator password shared across every workstation in the fleet: pop one machine, harvest that hash, and you can pass-the-hash into every other machine that shares it. The same shape appears everywhere outside Windows — a long-lived cloud access key on a build server, an SSH private key with no passphrase, a service-account token mounted into a pod that's valid cluster-wide. The pattern is identical: a secret with broad scope and long life, cached somewhere an attacker who owns the host can read it.

The defensive property is **identity hygiene**, and it attacks the reuse directly: unique per-host local credentials (so a stolen hash unlocks exactly one box — Microsoft's LAPS exists for exactly this), short-lived and tightly-scoped tokens instead of long-lived broad keys, no privileged credentials cached on low-trust machines, and phishing-resistant MFA so a replayed secret alone isn't enough to authenticate. Pair that with segmentation and the attacker's two moves are both starved: nowhere useful to pivot *to*, and no reusable identity to move *with*.

<Inset kind="why" lang="en">
Why does pass-the-hash work at all — isn't a hash supposed to be the *safe* thing to store? The subtlety is that a hash is only one-way with respect to *recovering the password*. But NTLM uses the hash itself as the authentication secret in a challenge-response exchange, so for the purpose of *logging in*, possessing the hash is equivalent to possessing the password — you never need to reverse it. That's the design flaw the attack exploits: the system treats "knows the hash" and "knows the password" as the same proof. Modern mitigations don't try to make the hash un-stealable; they make a stolen secret useless on a second machine (unique per-host credentials) or insufficient on its own (a second, phishing-resistant factor). The senior reflex: assume any secret cached on a host you don't fully trust is already in the attacker's hands, and design so that *having* it doesn't grant *reach*.
</Inset>

## How a defender reasons about the whole class

The senior move is to stop defending the perimeter as if landing on one host were the loss, and instead **assume breach**: take it as given that *some* host will be popped, and ask "what does that one foothold actually let them reach and become?" That question has exactly two answers to attack. Segmentation bounds the *reach* — least-privilege network paths so a relay relays into a wall, not the whole estate. Identity hygiene bounds the *become* — unique, short-lived, narrowly-scoped credentials so harvested identity unlocks one box, not the fleet. Detection is the third leg: lateral movement is unusually *visible* if you're watching for it — a workstation suddenly authenticating to twenty hosts, or an admin hash used from an IP it's never logged in from, is a loud signal — so log authentication centrally and alert on the impossible-shaped login. The pentester's whole pivot-and-move chain is just the inverse of these three controls; every link is a defense you either built or didn't.

<TradeoffMatrix
  id="04-pivoting-lateral-movement-tradeoff-1"
  lessonSlug="04-pivoting-and-lateral-movement"
  lang="en"
  prompt="In an authorized internal engagement you've popped a DMZ web server and harvested the local-admin NTLM hash. You confirm the same hash authenticates to several internal workstations. Which design fix should your report lead with as the root cause?"
  options={[
    { name: "Shared local-admin credential across hosts enabled pass-the-hash reuse; enforce unique per-host local credentials (e.g. LAPS) and segment the DMZ so it can't reach the workstation subnet", summary: "Kill reuse + reach.", correct: true, justification: "This names both roots at once. The reused hash is an identity-hygiene failure — one shared local-admin secret turns a single pop into fleet-wide access; unique per-host credentials (LAPS) make a stolen hash unlock exactly one box. And the DMZ being able to reach the workstation subnet at all is a segmentation failure — least-privilege paths would have given the relay nowhere useful to go. Fixing reuse and reach together starves both the lateral-movement and the pivot." },
    { name: "Rotate the compromised local-admin password and call it remediated", summary: "Rotate the secret.", justification: "Rotation cleans up this one exposed secret but leaves the architecture intact: if the new password is still shared across hosts, the very next pop reproduces the entire attack. You've treated the incident, not the design flaw. The fix is uniqueness per host, not a fresher shared secret." },
    { name: "Add an EDR agent to the popped web server so the next intrusion is detected", summary: "Detect on the foothold.", justification: "Detection is a real and valuable third leg, but it's not the root cause and it's watching the wrong place — the damage in this chain happens on the hosts the attacker moves *to*. EDR on one foothold doesn't stop a passed hash authenticating to twenty other machines; it complements, but cannot replace, segmentation and identity hygiene." },
    { name: "Force the local-admin password to 24 characters so the hash can't be cracked", summary: "Make it uncrackable.", justification: "This is the precise misconception pass-the-hash exploits: the attacker never cracks the password. NTLM accepts the hash itself as proof of identity, so a longer password produces a different but equally replayable hash. Complexity is irrelevant when the secret is reused as-is — uniqueness and a second factor are what matter." },
  ]}
/>

<Quiz
  id="04-pivoting-lateral-movement-quiz-1"
  lessonSlug="04-pivoting-and-lateral-movement"
  lang="en"
  question="What is the key difference between pivoting and lateral movement?"
  choices={[
    { label: "Pivoting is a network move — relaying traffic through a compromised host to reach unreachable systems; lateral movement is an identity move — reusing harvested credentials to authenticate to other hosts", correct: true },
    { label: "Pivoting reuses credentials while lateral movement tunnels traffic", misconception: "This reverses the two. Pivoting is the network/reachability move (tunnelling through a relay); lateral movement is the identity move (authenticating with reused credentials). The names are easy to swap, but the defenses differ — network segmentation vs identity hygiene — so the distinction matters." },
    { label: "They're two names for the same thing — moving from one host to another", misconception: "They interleave but are mechanically distinct: pivoting gives you network reach to a host without any credential, while lateral movement gives you authenticated access to a host. You often pivot to reach a target, then move laterally to log into it — different controls break each." },
    { label: "Pivoting only works over the internet; lateral movement only works on a LAN", misconception: "Neither is bound to a network location like that. Pivoting is precisely about reaching internal segments you can't route to from outside, and lateral movement happens wherever reusable identity is cached — including across cloud accounts and clusters." },
  ]}
/>

<Quiz
  id="04-pivoting-lateral-movement-quiz-2"
  lessonSlug="04-pivoting-and-lateral-movement"
  lang="en"
  question="Why does pass-the-hash let an attacker authenticate without ever cracking the password?"
  choices={[
    { label: "Windows NTLM accepts the password's hash itself as proof of identity in the challenge-response, so possessing the stolen hash is equivalent to knowing the password for logging in", correct: true },
    { label: "The attacker uses a fast GPU to reverse the hash back into the plaintext password in seconds", misconception: "No reversal happens. The whole point of pass-the-hash is that you don't need the plaintext — NTLM uses the hash directly as the authentication secret, so the attacker replays the hash as-is. Cracking is unnecessary, which is what makes it so effective." },
    { label: "The hash contains the password in a lightly-encoded form that the server decodes", misconception: "A cryptographic hash is one-way and doesn't contain the recoverable password. The flaw isn't that the hash leaks the password — it's that the protocol treats 'knows the hash' as equivalent to 'knows the password' for authentication purposes." },
    { label: "Hashing is disabled by default, so the password travels in plaintext anyway", misconception: "Hashing isn't disabled; the hash is exactly what's used. The vulnerability is architectural — the hash functions as a reusable credential — not a matter of the password being sent in the clear." },
  ]}
/>

<DragOrder
  id="04-pivoting-lateral-movement-drag-1"
  lessonSlug="04-pivoting-and-lateral-movement"
  lang="en"
  prompt="Order the stages of a pivot-then-lateral-movement chain, from the initial foothold to where a hardened defense breaks it:"
  items={[
    { id: "a", label: "Attacker gains a foothold on a single internet-facing host" },
    { id: "b", label: "Foothold is turned into a relay (SOCKS/SSH tunnel) to reach an internal subnet — pivot" },
    { id: "c", label: "Credentials/hashes are harvested from the foothold's memory or secret store" },
    { id: "d", label: "Harvested identity is reused to authenticate to internal hosts — lateral movement" },
    { id: "e", label: "Segmentation + unique per-host credentials leave nowhere to pivot and nothing to reuse; chain stalls" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>A single foothold isn't the breach — the breach is what the attacker does next. Pivoting is a *network* move (relay traffic through the popped host to reach unreachable systems); lateral movement is an *identity* move (reuse harvested credentials, e.g. pass-the-hash, to authenticate onward). Segmentation bounds the reach so a relay relays into a wall; identity hygiene — unique, short-lived, scoped credentials plus phishing-resistant MFA — bounds the reuse so a stolen secret unlocks one box, not the fleet. Assume breach, and design so one popped host is a dead end.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-pivoting-lateral-movement-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between pivoting and lateral movement, and why a single foothold is so rarely the actual breach.", a: "A foothold is rarely the breach because the assets an attacker wants — databases, domain controllers, secret stores — almost always live on a different segment than the host they first land on; the entire mid-game of an intrusion is getting from the foothold to the target, which MITRE ATT&CK calls Lateral Movement (TA0008). Within that, two mechanically distinct moves interleave. Pivoting is a network move: the compromised host becomes a relay (a SOCKS tunnel, SSH port-forward, or reverse connection) so the attacker's own tools reach internal systems they can't route to directly — it costs no new credential, it's pure reachability, and it's how an attacker on a DMZ web server reaches a private subnet behind it. Lateral movement is an identity move: the attacker uses credentials, hashes, tokens, or keys harvested from the foothold to authenticate to other hosts as a legitimate user — no exploit fires, they simply log in. In practice you pivot to gain network reach to a host, then move laterally by authenticating to it with stolen identity. The distinction matters because the defenses differ: segmentation breaks the pivot (network architecture), identity hygiene breaks the lateral movement (credential discipline)." },
    { q: "What is pass-the-hash, why does it work without cracking the password, and what design properties stop the lateral-movement-and-pivot chain?", a: "Pass-the-hash is a lateral-movement technique where an attacker who has stolen the NTLM hash of a privileged account from one machine presents that hash directly to another machine and authenticates as that user — without ever recovering the plaintext password. It works because Windows NTLM authentication uses the hash itself as the proof of identity in a challenge-response exchange: the server never needs the plaintext, so for the purpose of logging in, possessing the hash is equivalent to knowing the password. A cryptographic hash is one-way only with respect to recovering the password; the protocol's flaw is treating 'knows the hash' as the same proof as 'knows the password.' This makes credential reuse the multiplier that turns one host into a fleet — the classic disaster being a single shared local-admin password across every workstation, where one pop yields a hash that passes into all of them; the same shape appears as long-lived cloud keys, passphrase-less SSH keys, and cluster-wide service tokens. Two design properties stop the chain. Segmentation bounds reach: least-privilege network paths so a pivot relay can touch only the one service it genuinely needs, not the whole estate. Identity hygiene bounds reuse: unique per-host credentials (e.g. LAPS) so a stolen hash unlocks exactly one box, short-lived and narrowly-scoped tokens instead of long-lived broad keys, no privileged credentials cached on low-trust hosts, and phishing-resistant MFA so a replayed secret alone isn't enough. Detection is the third leg — lateral movement is loud (a workstation authenticating to twenty hosts, an admin hash used from a new IP) if authentication is logged centrally and you alert on impossible-shaped logins." },
  ]}
/>

<Recap lang="en">
A single foothold is almost never the breach — the breach is what the attacker does next, and it splits into two mechanically distinct moves. Pivoting is a network move: the popped host becomes a relay (SOCKS tunnel, SSH forward) so the attacker reaches internal systems they couldn't route to, costing no new credential — pure reachability. Lateral movement is an identity move: harvested credentials, hashes, tokens, or keys are reused to authenticate onward as a legitimate user, the most-feared form being pass-the-hash, where NTLM accepts the stolen hash itself as proof of identity so nothing needs cracking. The two defenses map one-to-one onto the two moves. Segmentation bounds the reach — least-privilege network zones so a relay relays into a wall instead of the whole estate. Identity hygiene bounds the reuse — unique per-host credentials (LAPS), short-lived scoped tokens, no privileged secrets cached on low-trust hosts, and phishing-resistant MFA — so a harvested secret unlocks one box, not the fleet. Add central authentication logging to catch the loud, impossible-shaped login, and assume breach throughout. The next time you reason about a popped host, your question is no longer "did they get in?" but "what can this one foothold reach, and what identity can it reuse — and have I made both answers 'nothing useful'?"
</Recap>
