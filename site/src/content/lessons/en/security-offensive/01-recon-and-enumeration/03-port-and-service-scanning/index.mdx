---
concepts:
- port-scanning
- nmap
- service-fingerprinting
- banner-grabbing
- scan-detection
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-port-and-service-scanning
sources:
- https://owasp.org/www-project-web-security-testing-guide/
- https://attack.mitre.org/tactics/TA0043/
spiral: []
status: ready
summary: A port scan finds which services are listening; fingerprinting names their versions. Both are reconnaissance against authorized targets only — and every probe also leaves a line in the defender's log, which is what makes scanning a two-sided story.
title: Port and service scanning
track: security-offensive
unit: 01-recon-and-enumeration
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
On an authorized lab host you point a scanner at a single IP and, eight seconds later, you have a map you didn't have before: port 22 is open and answers as `OpenSSH 7.4`, port 5432 is open and speaks the PostgreSQL protocol, port 6379 has Redis listening with no auth banner at all. Nobody handed you that inventory — you inferred all of it from how the host replied to a few thousand carefully shaped packets. That eight-second map is reconnaissance, and the same eight seconds wrote roughly three thousand connection attempts into a log on the other side. Scanning is never a one-way read; every question you ask the network, the network can hear you ask.
</Hook>

By the end of this lesson you'll know how a port scan actually works at the TCP level, how service fingerprinting turns an open port into a named version, and exactly what a defender sees while you do it — all against systems you're authorized to test.

<Crux>A port scan finds open services; fingerprinting names their versions — and every probe also writes a line in the defender's log.</Crux>

<Explanation>

## Authorization first — scanning is reconnaissance, not a free read

Before any of the mechanism below, the rule that opens this whole track still holds: a port scan is only legitimate against a system you own or are explicitly authorized to test. In MITRE ATT&CK terms, scanning is **Active Scanning** under the **Reconnaissance** tactic (TA0043) — and "active" is the operative word. Unlike reading a target's public DNS records, a scan sends packets *to* the target and waits for replies, which means it touches infrastructure you don't control and leaves evidence on it. Pointing a scanner at a host you have no authorization for is unauthorized access in nearly every jurisdiction, regardless of how gentle the scan is. Everything here is framed for a CTF box, a deliberately vulnerable VM you deployed, or an in-scope engagement — never a real target "just to see what's open."

We learn it defensively: if you understand exactly what a scan reveals and exactly what it leaves behind, you can both reduce what your own systems expose and recognize the scan in your logs when someone runs it against you.

## How a port scan works: the TCP handshake, weaponized as a question

A port scan is just the TCP three-way handshake used as an interrogation. To open a normal connection a client sends `SYN`, the server replies `SYN-ACK` if the port is listening, and the client finishes with `ACK`. A scanner exploits the *middle* of that exchange: it sends a `SYN`, and the **reply alone** tells it the port's state, before any connection is fully established.

- **`SYN-ACK` back** → the port is **open**: something is listening.
- **`RST` (reset) back** → the port is **closed**: the host is reachable but nothing is listening there.
- **Nothing back** (after retries/timeout) → **filtered**: a firewall is silently dropping the probe, so you can't tell open from closed.

The classic default, a **SYN scan** (often called a "half-open" or "stealth" scan), sends the `SYN`, reads the reply, and then sends `RST` to tear down the half-built connection *before* the handshake completes. Historically this mattered because a connection that never fully opened often wasn't written to the application's own logs. The contrast is a **connect scan**, which completes the full three-way handshake using the OS's normal connection call — more reliable and the fallback when the scanner lacks the privilege to craft raw packets, but louder, because a completed connection is exactly what application logging records.

<FlowDiagram
  label="A SYN port scan as an interrogation of the TCP handshake: the scanner sends a SYN probe; an open port replies SYN-ACK, a closed port replies RST, and a filtered port (firewall dropping the probe) replies with nothing. On an open port the scanner sends RST to tear down the half-open connection before the handshake completes — historically quieter than a full connect, but still visible to the network and firewall layer."
  perRow={4}
  hue="var(--d-network)"
  nodes={[
    { id: "syn", label: "Scanner SYN", sub: "probe sent" },
    { id: "open", label: "Open", sub: "SYN-ACK back" },
    { id: "closed", label: "Closed", sub: "RST back" },
    { id: "filtered", label: "Filtered", sub: "no reply" },
    { id: "rst", label: "Scanner RST", sub: "tear down" },
  ]}
  edges={[
    { from: "syn", to: "open", label: "listening" },
    { from: "syn", to: "closed", label: "reachable" },
    { from: "syn", to: "filtered", label: "dropped" },
    { from: "open", to: "rst", label: "half-open" },
  ]}
  caption="The reply to a single SYN classifies the port: SYN-ACK = open, RST = closed, silence = filtered. The SYN scan resets the open connection before it completes — quieter at the application layer, but every one of these probes is still a packet the firewall and IDS can see and count."
/>

## From open port to named service: fingerprinting and banners

An open port number is a weak guess. Port 22 *usually* means SSH and 5432 *usually* means PostgreSQL — but services run on non-standard ports all the time, and "open" alone tells you nothing about *which* software or *which version* is listening. Turning a port number into a confident identification is **service fingerprinting**, and it has two flavors.

The cheap one is **banner grabbing**: many services announce themselves the moment you connect. Open a raw TCP connection to an SSH port and it greets you with a version string like `SSH-2.0-OpenSSH_7.4`; an SMTP server answers with a `220` line naming the mail software; a misconfigured web server returns a `Server:` header. You read the banner the service volunteers and you know what it is — no cleverness required, because the service told you.

The deeper one is **active fingerprinting** (nmap's `-sV` service/version detection). When a banner is missing, lies, or is ambiguous, the scanner sends a sequence of crafted probes and matches the *pattern* of responses — timing, exact byte sequences, error behavior, protocol quirks — against a database of known service signatures. It can also infer the OS from subtle differences in how the TCP/IP stack itself responds (TTL values, window sizes, option ordering), because no two stacks behave identically. This is more accurate than trusting a banner, but it is also far louder: instead of one connection it fires dozens of unusual probes per port, which is exactly the anomalous traffic an IDS is tuned to notice.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Technique</th>
        <th class="border border-border px-3 py-2 text-left">What the scanner does</th>
        <th class="border border-border px-3 py-2 text-left">What it learns</th>
        <th class="border border-border px-3 py-2 text-left">Noise / detectability</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SYN scan (`-sS`)</td>
        <td class="border border-border px-3 py-2">SYN, read reply, RST before handshake completes</td>
        <td class="border border-border px-3 py-2">Open / closed / filtered per port</td>
        <td class="border border-border px-3 py-2">Quieter at app layer; visible to firewall/IDS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Connect scan (`-sT`)</td>
        <td class="border border-border px-3 py-2">Completes the full 3-way handshake via the OS</td>
        <td class="border border-border px-3 py-2">Same states, no raw-packet privilege needed</td>
        <td class="border border-border px-3 py-2">Loudest: each connection hits app logs</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Banner grab</td>
        <td class="border border-border px-3 py-2">Connects, reads the greeting the service sends</td>
        <td class="border border-border px-3 py-2">Service name + version, if volunteered</td>
        <td class="border border-border px-3 py-2">Low: looks like one ordinary connection</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Version detection (`-sV`)</td>
        <td class="border border-border px-3 py-2">Sends crafted probes, matches response signatures</td>
        <td class="border border-border px-3 py-2">Confident version even when banner lies/missing</td>
        <td class="border border-border px-3 py-2">High: many unusual probes per port</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why does scan speed trade directly against stealth and accuracy? A scanner has a few knobs — how many ports per second, how many parallel probes, how long to wait for a reply. Crank them up and a full scan finishes in seconds, but the burst of identical packets in a tight window is the single easiest thing for a defender to alert on (e.g. "30 SYNs to 30 ports from one source in one second"). Slow it down — randomize the order, space probes across minutes — and you blend into normal traffic, but the scan now takes hours and a flaky network drops replies, costing you accuracy. There is no setting that is fast, accurate, *and* invisible; you are always spending one to buy another. The defender's whole job is to make the fast-and-accurate corner trip an alarm.
</Inset>

## What the defender sees — and why scanning is two-sided

This is the half newcomers forget: while you are reading the target, the target is reading you. Every probe is a packet with your source IP, and the defensive stack is built to notice exactly the patterns a scan creates.

- **The firewall / connection logs** record the flood: many SYNs to many ports from one source in a short window. A run of `RST`-terminated half-open connections (the SYN-scan signature) or a fan-out across the whole port range is a textbook scanning fingerprint.
- **An IDS/IPS** (Snort, Suricata, or a cloud equivalent) has rules tuned for precisely this — a threshold of distinct ports touched per source per interval — and can alert, rate-limit, or block the source IP outright.
- **Version detection is even more visible**: the unusual, non-protocol-conforming probes that `-sV` and OS detection send don't look like any normal client, so they light up signature-based detection far more than a plain port sweep.

The defensive lessons fall straight out of this. Reduce what's exposed in the first place (close ports, bind services to internal interfaces, put a firewall in front so most probes get *filtered*, not answered). Strip or genericize banners so a successful connection volunteers nothing — though remember that's only hiding the label, not the service, so it slows an attacker rather than stopping them. And monitor for the scan signature so you detect reconnaissance early, because in the kill chain recon is the *first* stage, and catching an attacker there is the cheapest place to break the chain.

<TradeoffMatrix
  id="01-recon-port-scan-tradeoff-1"
  lessonSlug="03-port-and-service-scanning"
  lang="en"
  prompt="On an authorized engagement you need to identify the exact software versions on a host, but the client's SOC is actively monitoring and you want to delay detection as long as possible. Which approach best fits the goal?"
  options={[
    { name: "Run a slow, timed scan: limit the rate, randomize port order, and only do version detection on the handful of ports you most need identified", summary: "Pace and target.", correct: true, justification: "This respects the real trade-off. Version detection (`-sV`) is inherently noisy because it sends unusual probes, so you can't make it invisible — but you can shrink and slow its footprint. Scanning at a low rate, randomizing order, and aiming version detection only at the specific ports that matter (rather than firing crafted probes at everything) minimizes the burst pattern a SOC alerts on while still getting the identifications you need. You're spending time to buy stealth, which is the correct lever when avoiding detection is the priority." },
    { name: "Run the fastest possible all-ports scan with aggressive version detection on every port, to finish before anyone notices", summary: "Race the SOC.", justification: "Speed is exactly what a SOC alerts on. A fast, all-ports scan with version detection everywhere produces a dense burst of identical and unusual probes in a tight window — the single easiest pattern for an IDS threshold rule to catch. 'Finish before they notice' assumes detection is slow and manual; threshold-based alerting fires in seconds, so this maximizes detectability rather than minimizing it." },
    { name: "Use only banner grabbing on every port and never send version-detection probes, since banners are reliable", summary: "Banners only.", justification: "Banner grabbing is quiet, but it's not reliable enough to be the whole answer: banners are often stripped, genericized, or deliberately falsified, so you'll mis-identify or fail to identify exactly the services that matter. Quiet but wrong defeats the goal of identifying exact versions. The right move is to combine quiet banner reads with targeted, paced version detection where banners fail — not to abandon version detection entirely." },
    { name: "Skip scanning and just assume standard ports map to standard services (22=SSH, 5432=Postgres)", summary: "Assume defaults.", justification: "Port-to-service assumptions are a weak guess, not an identification. Services run on non-standard ports constantly, versions can't be read from a port number at all, and the task explicitly requires exact versions. Assuming defaults produces confident-sounding but unverified claims — the opposite of the careful identification an authorized assessment is supposed to deliver." },
  ]}
/>

<Quiz
  id="01-recon-port-scan-quiz-1"
  lessonSlug="03-port-and-service-scanning"
  lang="en"
  question="In a SYN scan, what does the host's reply tell the scanner, and why was the SYN scan historically called 'half-open'?"
  choices={[
    { label: "SYN-ACK means open, RST means closed, and silence means filtered; it's 'half-open' because the scanner sends RST to abort before completing the three-way handshake", correct: true },
    { label: "The reply is always the full banner of the service, and 'half-open' means it only reads half the banner", misconception: "The handshake reply (SYN-ACK / RST / nothing) classifies the port's state; it isn't the service banner. Banner grabbing is a separate step after a port is found open. 'Half-open' refers to the incomplete handshake, not a partial banner." },
    { label: "A closed port returns SYN-ACK and an open port returns RST; 'half-open' means half the ports respond", misconception: "It's reversed: an open (listening) port returns SYN-ACK and a closed port returns RST. 'Half-open' refers to the scanner tearing down the connection with RST before the handshake completes, not to a fraction of ports replying." },
    { label: "There is no reply for any port state, so the scanner infers everything purely from timing", misconception: "Open and closed ports do reply (SYN-ACK and RST respectively); only a filtered port is silent. The whole point of a SYN scan is that the reply to one SYN classifies the port." },
  ]}
/>

<Quiz
  id="01-recon-port-scan-quiz-2"
  lessonSlug="03-port-and-service-scanning"
  lang="en"
  question="Why is nmap's active version detection (`-sV`) far more visible to an IDS than a plain banner grab?"
  choices={[
    { label: "It sends many crafted, non-protocol-conforming probes per port to match response signatures, which doesn't look like any normal client", correct: true },
    { label: "It uses a completely different network protocol that firewalls always block", misconception: "Version detection still runs over normal TCP/IP; it isn't a separate blocked protocol. Its visibility comes from the volume and abnormality of its probes, not from using something firewalls categorically reject." },
    { label: "It encrypts its probes, and encrypted traffic always triggers an IDS alert", misconception: "Encryption isn't what makes it loud, and encrypted traffic isn't inherently alerted on. The noise comes from sending many unusual, non-standard probes that don't resemble a legitimate client's behavior." },
    { label: "Banner grabbing is actually the louder of the two because it opens more connections", misconception: "It's the reverse. A banner grab is roughly one ordinary connection that reads what the service volunteers; version detection fires dozens of unusual probes per port, making it the louder technique." },
  ]}
/>

<DragOrder
  id="01-recon-port-scan-drag-1"
  lessonSlug="03-port-and-service-scanning"
  lang="en"
  prompt="Order the steps a scanner takes to go from an IP address to a named, versioned service:"
  items={[
    { id: "a", label: "Send a SYN probe to a target port" },
    { id: "b", label: "Read the reply to classify the port (open / closed / filtered)" },
    { id: "c", label: "On an open port, read any banner the service volunteers" },
    { id: "d", label: "If the banner is missing or ambiguous, send crafted version-detection probes" },
    { id: "e", label: "Match the response pattern against a signature database to name the version" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>A port scan uses the TCP handshake as a question: SYN-ACK = open, RST = closed, silence = filtered. Fingerprinting then names the service — cheaply via banners, more accurately (and more loudly) via crafted version-detection probes. Every probe is also a packet in the defender's log, so scanning is reconnaissance and an alert at the same time. Authorized targets only.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-recon-port-scan-retrieval"
  lang="en"
  questions={[
    { q: "Explain how a SYN port scan works at the TCP level, what the three port states mean, and why a SYN scan is quieter than a connect scan.", a: "A port scan repurposes the TCP three-way handshake (SYN → SYN-ACK → ACK) as an interrogation. The scanner sends a single SYN and reads the reply: a SYN-ACK means the port is open (something is listening), an RST means it's closed (the host is reachable but nothing is listening there), and no reply after retries means it's filtered (a firewall is silently dropping the probe, so you can't distinguish open from closed). A SYN scan ('half-open') sends RST to tear down the connection right after the SYN-ACK, before the handshake completes — historically quieter because a connection that never fully opened often wasn't written to the application's own logs. A connect scan instead completes the full handshake using the OS's normal connection call: it's more reliable and works without raw-packet privileges, but louder, because a completed connection is exactly what application logging records. Either way, every probe is still a packet the firewall and IDS can see and count, so 'quieter' is relative, not invisible." },
    { q: "Compare banner grabbing and active version detection, and describe what a defender sees during scanning and how they reduce exposure.", a: "Both turn an open port into a named service, but at different cost. Banner grabbing just reads the greeting a service volunteers on connect (e.g. an SSH version string, an SMTP 220 line, a Server header) — cheap, quiet, and looks like one ordinary connection, but unreliable when banners are stripped or falsified. Active version detection (nmap `-sV`) sends a sequence of crafted probes and matches the response pattern — timing, exact bytes, error behavior, protocol quirks — against a signature database, and can even infer the OS from TCP/IP stack differences (TTL, window size, option order). It's far more accurate when banners are missing or lie, but much louder: dozens of unusual, non-protocol-conforming probes per port that don't look like any normal client, so they light up signature-based IDS detection. A defender sees the scan as a flood of SYNs to many ports from one source in a short window — RST-terminated half-open connections or a full port-range fan-out is the classic signature — and IDS/IPS rules threshold on distinct ports per source per interval to alert, rate-limit, or block. Defenses follow: reduce exposure (close ports, bind to internal interfaces, firewall so probes are filtered not answered), strip or genericize banners (which hides the label, not the service), and monitor for the scan signature to catch recon early — the first and cheapest kill-chain stage to break." },
  ]}
/>

<Recap lang="en">
A port scan is the TCP three-way handshake turned into an interrogation: the scanner sends a SYN and the reply alone classifies the port — SYN-ACK is open, RST is closed, and silence is filtered (a firewall dropping the probe). The default SYN ('half-open') scan resets the connection before the handshake completes, historically quieter at the application layer than a connect scan, which completes the full handshake and lands in app logs. Finding an open port is only half the job; turning it into a named, versioned service is fingerprinting — cheaply through banner grabbing, where the service volunteers its identity, and more accurately but far more loudly through active version detection, which fires crafted probes and matches response signatures (and can infer the OS from stack quirks). The unifying senior idea is that scanning is two-sided: every probe carries your source IP into the defender's firewall logs and IDS, where threshold rules on distinct ports per source are tuned to fire on exactly this pattern — and version detection's abnormal probes are louder still. So the trade-off is permanent — fast and accurate is loud, quiet is slow or partial — and the defensive moves fall straight out of it: shrink the attack surface so probes are filtered, strip banners so a connection reveals nothing, and alert on the scan signature to break the kill chain at its first, cheapest stage. And always, only against systems you're authorized to test.
</Recap>
