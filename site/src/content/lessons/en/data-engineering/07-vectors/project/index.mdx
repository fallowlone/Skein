---
concepts:
- recall-measurement
- hnsw-tuning
- post-filtering
- hybrid-search
- pgvector
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
status: ready
summary: "Hands-on project — build a pgvector RAG search, expose its silent recall drop with an exact baseline, then tune ef_search, fix post-filtering, and add hybrid search, proving each step with recall numbers."
title: 'Vector search: catch the silent recall drop'
track: data-engineering
unit: 07-vectors
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about silent recall is not the same as catching it. Build a small pgvector search, measure its recall against an exact baseline, then watch the number move as you turn ef_search, fix a selective filter, and bolt on hybrid search — with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: build vector search, measure recall@k against ground truth, expose the silent failure modes, and fix them with the right lever, proving each change with before/after recall.
</Goal>

<ProjectBrief
  id="07-vectors-project"
  lessonSlug="07-vectors"
  lang="en"
  objective="Build a pgvector-backed semantic search over a real document corpus, then prove with measured recall@10 that you can detect and fix the silent failure modes — low ef_search, post-filtered selective filters, and missing exact-match support — without ever relying on row count or latency to tell you something is wrong."
  requirements={[
    "Load a corpus of at least 100k chunks into Postgres with pgvector: embed each chunk with a real model (e.g. a 384 or 768-dim open model, or text-embedding-3-small), store the vector and the source text plus a tenant_id or category column.",
    "Build an HNSW index with vector_cosine_ops, matching the operator (<=>) to how the embeddings were trained. Confirm the query plan uses the index, not a sequential scan.",
    "Write a recall@10 harness: for a sample of ~200 real queries, compute the exact top-10 with an index-disabled ORDER BY scan (ground truth), run the same queries through the HNSW index, and report recall@10 as the mean ID-set overlap.",
    "Capture a baseline at the default hnsw.ef_search = 40 and show recall is well below 1.0 while latency and row count look perfectly healthy — this is the silent failure made visible.",
    "Sweep ef_search across several values (e.g. 40, 100, 200, 500) and plot or table recall@10 vs p99 latency, then pick the point that meets a stated recall SLO (e.g. recall ≥ 0.95).",
    "Add a selective WHERE tenant_id = ? for a small tenant and re-measure recall on the filtered path; show recall collapses under post-filtering, then recover it with hnsw.iterative_scan and record the latency cost.",
    "Add a hybrid leg: run BM25 / Postgres full-text search in parallel with the vector query and fuse with Reciprocal Rank Fusion (k≈60); show recall@10 improves on a set of exact-token queries (error codes, identifiers) that pure vector search missed.",
  ]}
  acceptance={[
    "A before/after table of recall@10 and p99 latency at the default ef_search vs the tuned value, measured against the exact baseline, not estimated.",
    "Evidence that the filtered-query recall collapsed under post-filtering and was restored by iterative scan, with the latency cost recorded.",
    "A short list of exact-token queries where hybrid RRF beats pure vector search on recall@10, with the numbers.",
    "A one-paragraph write-up stating the recall SLO, the ef_search you chose, and why recall@k against an exact baseline — not latency or row count — is the metric you alert on.",
  ]}
  stretch={[
    "Add an on-call runbook: how to detect a silent recall drop, the recall@k harness command, the ef_search / probes / iterative_scan levers, and a verification checklist.",
    "Compare HNSW against IVFFlat on the same corpus: measure recall drift after inserting 20% more rows without reindexing, showing IVF centroids go stale while HNSW holds.",
    "Add IVF-PQ (or an external store like Qdrant/Faiss) and measure the recall loss from quantization, then recover it with a full-precision rerank step on the top candidates.",
    "Wire a CI gate that runs the recall@10 harness on a canary and fails the build if recall drops more than 2 points against main.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real vector-search incident: build the index with a matching metric, measure recall@k against an exact baseline, expose the silent failures (default ef_search, post-filtered selective filters, missing exact-match support), and fix each with the right lever — dial ef_search, switch on iterative scan, add hybrid RRF — verifying every change with measured recall, never row count or latency. Doing it once on a real corpus makes the production version muscle memory.
</Recap>
