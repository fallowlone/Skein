---
concepts:
- ann-recall-tradeoff
- hnsw-tuning
- ivf-pq
- distance-metric
- post-filtering
- hybrid-search
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
- https://weaviate.io/blog/hybrid-search-explained
status: ready
summary: "Multiple-choice synthesis across the vector-search unit — recall vs latency, HNSW vs IVF-PQ, distance metrics, post-filtering, and the silent-recall failure mode."
title: 'Vector search: multiple-choice review'
track: data-engineering
unit: 07-vectors
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a decision you make staring at a RAG pipeline that returns ten ranked rows, fast, with no errors — and the wrong ten.
</Hook>

<Goal lang="en">
Confirm you can connect embeddings, ANN index choice, the recall–latency–memory triangle, distance metrics, and the production traps — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="07-vectors-choice-1"
  lessonSlug="07-vectors"
  lang="en"
  question="A RAG search returns ten ranked rows in 2ms with zero errors, but support reports the bot 'can't find' docs it clearly has. What is the most likely root cause, and how do you confirm it?"
  choices={[
    { label: "Recall has silently collapsed — likely a too-low hnsw.ef_search (default 40), often worsened by a selective WHERE filter. Confirm by sampling queries, running an exact ORDER BY scan for ground truth, and computing recall@k overlap.", correct: true },
    { label: "The database is overloaded and dropping rows.", misconception: "It returns the full ten rows in 2ms with no errors — the DB is healthy. Low recall is invisible: it looks identical to high recall in row count and latency." },
    { label: "The embedding model API is timing out.", misconception: "Queries succeed in 2ms; the symptom is wrong results, not slow or failed ones. That points at recall, not the embedding call." },
    { label: "You need more replicas to handle the load.", misconception: "Throughput is not the problem — correctness is. Replicating a low-recall index just multiplies the wrong answers." },
  ]}
/>

<Quiz
  id="07-vectors-choice-2"
  lessonSlug="07-vectors"
  lang="en"
  question="A RAG service over ~5M chunks has continuous ingestion, needs high recall, and has RAM to spare. Which index is the right default and why?"
  choices={[
    { label: "HNSW with a tuned ef_search measured against an exact baseline. Its inserts are constant-cost so continuous ingestion does not degrade the graph, and with RAM to spare the 2–5× overhead is affordable for the highest recall.", correct: true },
    { label: "IVFFlat built once with a large lists value.", misconception: "IVF centroids are fixed at build time, so continuous ingestion steadily drifts recall until you reindex. With RAM available and high recall required, you would be choosing the index that drifts for a memory win you do not need." },
    { label: "IVF-PQ to compress vectors as small as possible.", misconception: "PQ is for corpora that will not fit in RAM at all (100M+). At ~5M with RAM to spare, you pay PQ's quantization recall loss for compression you do not need." },
    { label: "Brute-force exact kNN with no index.", misconception: "Exact kNN is recall 1.0 but O(N·d); at 5M times 1536 dims every query is billions of multiply-adds, so p99 is unshippable. It is the offline baseline, not the serving path." },
  ]}
/>

<Quiz
  id="07-vectors-choice-3"
  lessonSlug="07-vectors"
  lang="en"
  question="Raising hnsw.ef_search from 100 to 500 moves recall from ~85% to ~98% but latency from ~1ms to ~5ms. How should a senior engineer read this?"
  choices={[
    { label: "Expected — ef_search is the runtime recall/latency dial. A larger candidate pool finds more true neighbors at the cost of more graph work per query. Pick the point that meets the recall SLO at acceptable p99.", correct: true },
    { label: "The index is misconfigured — recall and latency should both improve together.", misconception: "No ANN index improves recall and latency together at query time; they trade against each other along ef_search. Those numbers are the normal shape of the dial." },
    { label: "Rebuild with a higher M instead.", misconception: "M is a build-time knob affecting graph density and memory; it does not replace ef_search. Rebuilding is far more expensive than turning the runtime dial to hit the target." },
    { label: "Switch to IVFFlat to avoid the tradeoff.", misconception: "IVF has the same recall/latency tradeoff via probes, plus centroid drift. Switching indexes does not escape the triangle — every ANN method trades recall for speed." },
  ]}
/>

<Quiz
  id="07-vectors-choice-4"
  lessonSlug="07-vectors"
  lang="en"
  question="A team stores normalized embeddings and debates cosine similarity vs inner (dot) product. What is the correct call?"
  choices={[
    { label: "For normalized vectors cosine and dot product rank identically, so inner product is the cheaper equivalent — it skips the magnitude normalization. Match the metric to how the embeddings were trained and stay consistent across index and query.", correct: true },
    { label: "Cosine is always more accurate than dot product.", misconception: "Cosine is dot product divided by magnitudes. On already-normalized vectors the magnitudes are 1, so the two produce identical rankings — cosine just adds redundant work." },
    { label: "L2 distance is always the safest default.", misconception: "L2 ranks differently from cosine unless vectors are normalized, and using a metric the embeddings were not trained for degrades ranking. The metric must match the embedding model, not default to L2." },
    { label: "The metric does not affect results, only speed.", misconception: "The metric defines what 'nearest' means and changes ranking unless vectors are normalized. Picking the wrong one for the model silently worsens relevance." },
  ]}
/>

<Quiz
  id="07-vectors-choice-5"
  lessonSlug="07-vectors"
  lang="en"
  question="A tenant-scoped search adds WHERE tenant_id = ? on top of an HNSW query, and recall drops sharply for small tenants. Why, and what is the fix?"
  choices={[
    { label: "pgvector post-filters: it walks the graph for ef_search candidates, then applies the filter. If the filter keeps 1% of rows, almost none of the ef_search candidates survive. Fix: hnsw.iterative_scan (0.8.0+) to keep pulling candidates, trading latency for recall.", correct: true },
    { label: "The tenant_id column needs a B-tree index.", misconception: "A B-tree on tenant_id speeds the filter but does not change that ANN candidates are gathered before the filter is applied — the recall collapse is from post-filtering order, not filter speed." },
    { label: "Small tenants simply have too few documents to rank.", misconception: "Even with plenty of matching docs, post-filtering throws most ef_search candidates away because they belong to other tenants. The problem is ordering, not corpus size." },
    { label: "Switch from cosine to L2 for filtered queries.", misconception: "The metric is irrelevant here. The recall loss comes from the filter being applied after the graph walk has already narrowed to ef_search candidates." },
  ]}
/>

<Quiz
  id="07-vectors-choice-6"
  lessonSlug="07-vectors"
  lang="en"
  question="Users search for the exact error string 'ECONNREFUSED' and pure vector search buries the matching doc beneath vaguely-related paragraphs. Best fix?"
  choices={[
    { label: "Hybrid search: run BM25 keyword and vector retrieval in parallel and fuse with Reciprocal Rank Fusion. Fusing by rank position avoids comparing the two incompatible score scales and recovers exact-token matches.", correct: true },
    { label: "Raise ef_search to 1000.", misconception: "More candidates of the same kind do not help — the need is lexical, and embeddings smear an exact token like an error code into 'semantically near'. ef_search tunes vector recall, not exact-match precision." },
    { label: "Re-embed everything with a larger model.", misconception: "A bigger model still measures semantic similarity; it does not give exact-token precision. Error codes, SKUs, and identifiers are a keyword need that BM25 handles." },
    { label: "Switch the distance metric from cosine to dot product.", misconception: "Metric choice nudges ranking but never gives lexical precision. Vector search lacks exact-match capability regardless of metric; hybrid adds the keyword leg that has it." },
  ]}
/>

<Recap lang="en">
The through-line is one triangle — recall, latency, memory — and one habit: recall drops are silent. The candidate pool (ef_search / probes) sets recall vs latency, the index family sets memory and drift (HNSW for changing data, IVFFlat for static, IVF-PQ when it will not fit RAM), the metric must match how embeddings were trained, post-filtering wrecks selective filters (use iterative scan), and exact-token needs demand hybrid BM25 + vector with rank fusion. Above all, measure recall@k against an exact baseline or you are flying blind.
</Recap>
