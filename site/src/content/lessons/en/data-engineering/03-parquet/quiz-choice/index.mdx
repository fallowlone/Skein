---
concepts:
- columnar-storage
- row-groups
- predicate-pushdown
- encoding-compression
- small-files-problem
- table-formats
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://parquet.apache.org/docs/concepts/
- https://arrow.apache.org/blog/2022/12/26/querying-parquet-with-millisecond-latency/
status: ready
summary: "Multiple-choice synthesis across the Parquet unit — columnar layout, footer stats and pushdown, encoding vs compression, row-group sizing, and the small-files trap."
title: 'Parquet: multiple-choice review'
track: data-engineering
unit: 03-parquet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make when designing a lake table — not a definition to recite, but a tradeoff to weigh against a real query and a real cost.
</Hook>

<Goal lang="en">
Confirm you can connect columnar layout, footer statistics, encoding versus compression, row-group sizing, and the production traps — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="03-parquet-choice-1"
  lessonSlug="03-parquet"
  lang="en"
  question="A 40-column events table is queried with SELECT user_id, country WHERE day = '2024-02-01'. Two independent Parquet mechanisms make this far cheaper than CSV. What are they?"
  choices={[
    { label: "Column pruning reads only the user_id and country column chunks, and row-group skipping uses the footer's per-row-group day min/max to skip groups whose range can't contain that date.", correct: true },
    { label: "A built-in secondary index maps each day value directly to its byte offset, so the engine seeks straight to the matching rows.", misconception: "Parquet has no external value-to-offset index. Skipping comes from min/max/null-count stats in the file's own footer per row group, plus optional page indexes — coarse range pruning, not a precise seek." },
    { label: "Parquet decompresses with snappy much faster than CSV decompresses with gzip, so the same full scan just finishes sooner.", misconception: "The win is reading less, not decompressing faster. Column pruning and row-group skipping mean most bytes are never read or decompressed at all; a faster codec is secondary." },
    { label: "Parquet stores the whole table globally sorted, so a binary search finds the matching rows in log time.", misconception: "Parquet does not globally sort data for you; clustering is something you arrange when writing. Pruning is range-based row-group/page elimination via stats, not a binary search." },
  ]}
/>

<Quiz
  id="03-parquet-choice-2"
  lessonSlug="03-parquet"
  lang="en"
  question="Your table has per-row-group day stats, yet WHERE day = '2024-02-01' still scans nearly every row group. What is the most likely cause?"
  choices={[
    { label: "The day values are scattered across row groups, so every group's min/max spans almost the whole year and nothing can be skipped — the data isn't clustered by the filter column.", correct: true },
    { label: "Min/max statistics only work for numeric columns, so a date-typed predicate can't prune.", misconception: "Parquet keeps min/max for ordered types including dates and strings; pruning works for them. The problem here is range overlap from scattered values, not the column type." },
    { label: "Predicate pushdown is disabled unless you also build a separate Bloom filter for the column.", misconception: "Range pushdown from footer min/max works without Bloom filters. Bloom filters help equality on high-cardinality columns; they aren't a prerequisite for date-range skipping." },
    { label: "The file uses snappy instead of zstd, and only zstd files expose statistics.", misconception: "Statistics live in the footer regardless of the compression codec. Codec choice affects size and read speed, never whether stats exist or can prune." },
  ]}
/>

<Quiz
  id="03-parquet-choice-3"
  lessonSlug="03-parquet"
  lang="en"
  question="A teammate says 'Parquet compresses the data, so encoding and compression are the same thing.' What is the precise correction?"
  choices={[
    { label: "They are two stacked, separate layers. Encoding is type-aware and structural (dictionary, RLE, bit-packing, delta); a general-purpose codec (snappy/zstd/gzip) then compresses the already-encoded bytes. Encoding alone shrinks data before any codec runs.", correct: true },
    { label: "Correct — 'encoding' is just the marketing name for the snappy/zstd compression step.", misconception: "Encoding and compression are distinct. Dictionary/RLE/delta encoding restructures values by type before the byte-level codec ever runs, and accounts for much of Parquet's size win on its own." },
    { label: "Encoding happens only when you pick gzip; snappy files are stored without any encoding.", misconception: "Encoding is independent of the codec choice. A snappy file still uses dictionary/RLE/bit-packing on its pages; the codec is layered on top of whatever encoding was applied." },
    { label: "They're the same because both are lossless, so distinguishing them is academic.", misconception: "Both are lossless, but they are different mechanisms with different knobs and failure modes — dictionary blowup is an encoding problem, codec CPU cost is a compression problem. Conflating them hides real tuning decisions." },
  ]}
/>

<Quiz
  id="03-parquet-choice-4"
  lessonSlug="03-parquet"
  lang="en"
  question="You enable dictionary encoding on a column of random UUIDs. What is the likely outcome, and why?"
  choices={[
    { label: "The dictionary grows past its page-size budget because values barely repeat, Parquet falls back to plain encoding, and you may end up with a file no smaller — sometimes larger — than plain alone.", correct: true },
    { label: "UUIDs compress perfectly because every value is replaced by a small integer code.", misconception: "Dictionary encoding only pays when values repeat. With near-unique UUIDs the dictionary is as large as the column itself, overflows its budget, and Parquet abandons it." },
    { label: "The column is silently corrupted because UUIDs can't be dictionary-encoded.", misconception: "There is no corruption — Parquet simply falls back to plain encoding for that column. The cost is wasted CPU and possibly larger files, never correctness." },
    { label: "Nothing changes; dictionary encoding is always free to enable.", misconception: "It is not free on high-cardinality data: you pay to build a dictionary that shrinks nothing and then discard it, which can produce a larger file than plain encoding would have." },
  ]}
/>

<Quiz
  id="03-parquet-choice-5"
  lessonSlug="03-parquet"
  lang="en"
  question="A Kafka consumer flushes a tiny Parquet file to S3 every 10 seconds. Dashboards over the table have become painfully slow. What is the highest-leverage fix?"
  choices={[
    { label: "Run a periodic compaction job that rewrites the swarm of tiny files into files in the 128 MB to 1 GB range — listing and footer-reading millions of objects is what dominates query planning.", correct: true },
    { label: "Switch the files from snappy to zstd to make them ~15-20% smaller.", misconception: "zstd is a real size win for a tiny read penalty, but it does nothing about the file count. The bottleneck is listing and reading millions of footers during planning, not the bytes per file — right idea, wrong problem." },
    { label: "Raise the writer's row-group size to 1 GB so each file holds one big row group.", misconception: "A 10-second flush only has a few KB to write, so it can't fill a 1 GB row group — you still get many tiny files, now with tiny row groups. Row-group sizing matters for large batch writes, not a high-frequency streaming writer." },
    { label: "Convert the table to CSV so there is no per-file footer overhead to read.", misconception: "Dropping the footer also drops column pruning and predicate pushdown, so every query full-scans and full-parses — far worse. And you'd still have millions of small files to list. This trades a tuning issue for a fundamental one." },
  ]}
/>

<Quiz
  id="03-parquet-choice-6"
  lessonSlug="03-parquet"
  lang="en"
  question="Raw Parquet files in a directory give you columnar storage and pushdown. What do Iceberg, Delta Lake, and Hudi add on top, and why does it matter?"
  choices={[
    { label: "A metadata/manifest layer over the Parquet files: ACID commits, snapshots and time travel, metadata-only schema evolution, and manifest-level pruning that eliminates whole files from per-file stats before any footer is opened.", correct: true },
    { label: "A faster columnar storage format that replaces Parquet entirely.", misconception: "They don't replace Parquet — they keep data in Parquet and add a table layer on top. Storage stays columnar Parquet; the format adds transactions, snapshots, and planning metadata." },
    { label: "Built-in compression that makes the underlying Parquet files smaller.", misconception: "Compression is still Parquet's job (snappy/zstd/gzip). Table formats add correctness and planning — a current-file-set manifest, ACID, time travel — not a new codec." },
    { label: "Nothing functional — they're just a directory-naming convention for partitions.", misconception: "They are far more than naming: a manifest tracks exactly which files belong to the table right now with per-file stats, enabling ACID commits, time travel, and file-level pruning that raw directory listing cannot." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one design loop: columnar layout enables column pruning, the footer's min/max stats enable row-group and page skipping (predicate pushdown) — but only when data is clustered by the filter columns. Encoding and compression are separate stacked wins, and dictionary encoding backfires on high-cardinality columns. The recurring production traps are the small-files problem (fix with compaction, not a better codec) and row-group sizing. And because raw Parquet has no transactions, table formats — Iceberg, Delta Lake, Hudi — wrap it with a manifest layer for ACID, time travel, safe schema evolution, and manifest-level pruning.
</Recap>
