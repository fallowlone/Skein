---
slug: 01-overview
lang: en
track: data-engineering
unit: 03-parquet
order: 1
level: junior
lessonType: topic
status: ready
title: "Parquet: why analytics stores columns, not rows"
summary: "Parquet is columnar, self-describing, and carries per-row-group min/max stats in its footer — so a filter can skip whole row groups and read only the columns it needs. CSV must parse every byte. The senior traps are small files, wrong row-group size, and dictionary blowup."
prereqs: []
deepensInto: []
spiral: []
concepts: ["columnar-storage", "row-groups", "predicate-pushdown", "column-pruning", "small-files-problem", "table-formats"]
estMin: 17
sources:
  - https://parquet.apache.org/docs/concepts/
  - https://arrow.apache.org/blog/2022/12/26/querying-parquet-with-millisecond-latency/
  - https://motherduck.com/learn/why-choose-parquet-table-file-format/
  - https://medium.com/data-engineering-with-dremio/all-about-parquet-part-10-performance-tuning-and-best-practices-with-parquet-d697ba4e8a57
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A nightly Spark job writes the events table to S3 as `gzip` CSV. A simple `SELECT user_id, country FROM events WHERE day = '2024-02-01'` reads 1.9 GB, decompresses every byte, parses every column, throws away 38 of 40 columns, and takes 90 seconds. The analyst reruns it forty times tuning a dashboard. Same data as Parquet with `snappy`: 220 MB on disk, the engine reads two column chunks and one day's row groups, and the query returns in under two seconds. Nothing about the data changed — only the file format.
</Hook>

<Crux>Parquet is columnar and self-describing: its footer stats let a query skip row groups and read only the columns it needs.</Crux>

<Explanation>

## Columns on disk, not rows

CSV and JSON store data row by row: every field of row 1, then every field of row 2. To read one column you still walk every byte of every row, parse the delimiters, and discard 95% of what you parsed. There are no statistics, no types — `"2024"` could be a string or a number, the reader can't know without parsing it.

Parquet flips the layout. Inside each file, data is split into **row groups** (a horizontal slice of rows, default target ~128 MB), and inside a row group each column is stored as a contiguous **column chunk**, itself split into **pages** (the unit of encoding and compression, ~1 MB by default). Because all values of one column sit together, two things follow that are impossible in CSV. First, **column pruning**: a query that needs `user_id` and `country` reads only those two column chunks and never touches the other 38. Second, encoding gets brutally efficient — a million rows of `country` is mostly the same handful of strings, and a column of similar values compresses far better than a row of mixed types.

## The footer is where the speed lives

Every Parquet file ends with a footer: the schema, plus per-row-group, per-column metadata — and crucially **min/max/null-count statistics** for each column in each row group. This is the single most important structure for query performance.

When you run `WHERE day = '2024-02-01'`, the engine reads the footer first, looks at each row group's `day` min and max, and **skips any row group whose range can't contain the value** without reading a single data byte. This is *predicate pushdown* plus *row-group skipping*. On a year of daily data sorted by day, that filter touches roughly 1 of 365 row groups. Page-level stats (and optional page indexes) push the same idea one level deeper, skipping pages inside a surviving row group. CSV can do none of this — no stats means the only way to know if a row matches is to read and parse it.

<Inset kind="why" lang="en">
Skipping only works if the data is laid out so the filter column's min/max ranges are *narrow and non-overlapping*. Sort or partition by the columns you filter on most. If `day` values are randomly scattered across row groups, every row group's `[min, max]` spans the whole year, no row group can be skipped, and you've paid for statistics that buy nothing. Stats are necessary but not sufficient — clustering is what makes them pay.
</Inset>

## Encoding and compression: two separate wins

Parquet shrinks data in two stacked layers, and seniors keep them mentally separate. **Encoding** is type-aware and lossless-by-construction: *dictionary encoding* replaces repeated values with small integer codes (ideal for low-cardinality strings like `country`, `status`), *run-length encoding (RLE)* collapses runs of identical values, *bit-packing* uses only as many bits as the value range needs, and *delta encoding* stores differences for sorted integers and timestamps. **Compression** (`snappy`, `zstd`, `gzip`) then runs a general-purpose codec over the already-encoded bytes.

The numbers are why the format won. A CSV that is 1.9 GB lands around 1.2 GB as raw uncompressed Parquet (encoding alone), and with compression typically **9–15% of the original CSV size**. `snappy` is the common default — fast, cheap CPU, ~8x ratio. `zstd` gives roughly **15–20% smaller files than snappy for under a 1% read penalty**, which is why it's the better choice for cold or archival data; `gzip` compresses tighter still but is markedly slower to read.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Property</th>
        <th class="border border-border px-3 py-2 text-left">CSV / JSON</th>
        <th class="border border-border px-3 py-2 text-left">Parquet</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Layout</td>
        <td class="border border-border px-3 py-2">Row-oriented</td>
        <td class="border border-border px-3 py-2">Columnar (row group → column chunk → page)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Read only some columns</td>
        <td class="border border-border px-3 py-2">No — full parse</td>
        <td class="border border-border px-3 py-2">Yes — column pruning</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Skip rows by filter</td>
        <td class="border border-border px-3 py-2">No stats → read everything</td>
        <td class="border border-border px-3 py-2">Footer min/max → skip row groups + pages</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Schema / types</td>
        <td class="border border-border px-3 py-2">None — guessed at read</td>
        <td class="border border-border px-3 py-2">Embedded, typed, self-describing</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Size vs CSV</td>
        <td class="border border-border px-3 py-2">1x (baseline)</td>
        <td class="border border-border px-3 py-2">~0.09–0.15x compressed</td>
      </tr>
    </tbody>
  </table>
</div>

## Where it bites in production

Parquet is not free of footguns, and every one of them shows up at scale, not in the demo.

The first is the **small-files problem**. Each Parquet file carries its own footer and costs one I/O to open and read. A streaming job that writes a 4 KB file every few seconds produces millions of tiny files, and now the slowest part of every query is *listing* them — recursively enumerating millions of objects in S3 is notoriously slow and expensive, and the query planner must read every footer before it can plan. The fix is **compaction**: periodically rewrite the swarm into files in the 128 MB – 1 GB range.

The second is **row-group size**. Too large and you lose granularity for skipping and need huge memory to write a single group; too small and metadata overhead balloons and you read more footers than data. The third is **dictionary blowup**: dictionary encoding is fantastic for low-cardinality columns, but on a high-cardinality column (a UUID, a free-text field) the dictionary grows past its page-size budget (~1 MB), Parquet falls back to plain encoding, and you've paid to build a dictionary that bought nothing — sometimes a *larger* file. The fourth is **schema evolution**: adding, renaming, or reordering columns across thousands of files leaves readers reconciling mismatched schemas, and a renamed-then-reused column name can silently mix two meanings.

## Table formats: Parquet plus a brain

Raw Parquet is just files in a directory; it has no notion of a transaction, a snapshot, or "the current set of files." This is exactly the gap **Iceberg, Delta Lake, and Hudi** fill. They keep your data in Parquet but add a metadata/manifest layer on top that tracks which files belong to the table right now, giving you ACID commits, time travel, safe schema evolution (column renames become metadata-only operations, not data rewrites), and **manifest-level pruning** — the engine consults the manifest's per-file stats to eliminate whole files before it even opens a footer. When people say "data lakehouse," this is the layer they mean: Parquet for storage, a table format for correctness and planning.

<TradeoffMatrix
  id="03-parquet-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A Kafka consumer writes click events to S3 as Parquet, flushing a tiny file every 10 seconds. Dashboards over this table have gotten painfully slow. Pick the fix."
  options={[
    { name: "Run a periodic compaction job that rewrites the many tiny files into 128 MB–1 GB Parquet files", summary: "Compact the swarm.", correct: true, justification: "This is the textbook small-files fix. Each tiny file carries its own footer and costs an I/O to open; with millions of them the dominant cost becomes recursively listing objects and reading every footer during planning. Compacting into fewer large files collapses listing and metadata overhead and is exactly why table formats ship compaction. The streaming write stays cheap; a background job consolidates." },
    { name: "Switch the files from snappy to zstd compression", summary: "Compress harder.", justification: "zstd gives ~15–20% smaller files for a <1% read penalty, which is a real win — but it does nothing about the count of files. The bottleneck here is listing and footer-reading millions of objects during query planning, not the bytes per file. Better codec, wrong problem." },
    { name: "Convert the table to CSV so there is no per-file footer overhead", summary: "Drop the format.", justification: "CSV removes the footer but also removes column pruning and predicate pushdown, so every query now full-scans and full-parses everything — far worse. And you'd still have a million small files; the listing problem is unchanged. This trades a tuning issue for a fundamental one." },
    { name: "Increase the row-group size to 1 GB on the streaming writer", summary: "Bigger groups.", justification: "A 10-second flush only has a few KB to write, so it can't fill a 1 GB row group anyway — you'd just get tiny files with tiny row groups. Row-group sizing matters for large batch writes; it doesn't address a high-frequency streaming writer producing many small files." },
  ]}
/>

<Quiz
  id="03-parquet-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="A query is WHERE day = '2024-02-01' on a year of data. What lets Parquet read far less than CSV would?"
  choices={[
    { label: "The footer's per-row-group min/max stats let the engine skip every row group whose day range can't contain that date", correct: true },
    { label: "Parquet keeps a separate index file that maps every value to its byte offset", misconception: "There is no external value-to-offset index; skipping comes from min/max/null statistics stored in the file's own footer per row group (with optional page indexes)." },
    { label: "Parquet decompresses faster than CSV so the full scan is just quicker", misconception: "The win is reading less, not decompressing faster. Row-group skipping and column pruning mean most bytes are never read at all." },
    { label: "CSV is slower only because it isn't compressed by default", misconception: "Even compressed CSV must be fully decompressed and parsed — it has no statistics, so the engine can't skip rows it doesn't need." },
  ]}
/>

<Quiz
  id="03-parquet-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="You enable dictionary encoding on a column of random UUIDs. What's the likely outcome?"
  choices={[
    { label: "The dictionary exceeds its page-size budget, Parquet falls back to plain encoding, and the file may end up larger", correct: true },
    { label: "UUIDs compress perfectly because every value becomes a small integer code", misconception: "Dictionary encoding only helps when values repeat. With near-unique values the dictionary is as large as the column, so it overflows its ~1 MB budget and Parquet abandons it." },
    { label: "It silently corrupts the column", misconception: "There's no corruption — Parquet just falls back to a different, plain encoding. The cost is size and wasted effort, not correctness." },
    { label: "Nothing changes; dictionary encoding is free", misconception: "It's not free on high-cardinality data: you pay to build a dictionary that doesn't shrink anything, sometimes producing a larger file than plain encoding alone." },
  ]}
/>

<DragOrder
  id="03-parquet-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the physical containment, outermost to innermost:"
  items={[
    { id: "a", label: "File (ends with a footer holding schema + per-row-group stats)" },
    { id: "b", label: "Row group (a horizontal slice of rows, ~128 MB target)" },
    { id: "c", label: "Column chunk (all values of one column within that row group, contiguous)" },
    { id: "d", label: "Page (the unit of encoding + compression, ~1 MB)" },
    { id: "e", label: "Encoded + compressed values (dictionary / RLE / delta, then snappy/zstd)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Parquet stores columns and keeps per-row-group min/max stats in its footer — so queries prune columns and skip row groups instead of full-parsing like CSV. Watch small files, row-group size, and dictionary blowup.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-parquet-retrieval"
  lang="en"
  questions={[
    { q: "Explain to a teammate, end to end, why a filtered query on Parquet reads so much less than the same query on CSV.", a: "Two independent mechanisms stack. First, column pruning: because Parquet is columnar, all values of one column live together in a column chunk, so a query that needs two of forty columns reads only those two chunks and never touches the rest — CSV is row-oriented, so reading any column means walking every byte of every row and parsing the delimiters. Second, row-group skipping via predicate pushdown: the file's footer stores min/max/null-count statistics for each column in each row group, so for WHERE day = '2024-02-01' the engine checks each row group's day range and skips any group whose [min, max] can't contain that date without reading a single data byte; optional page-level stats push this one level deeper. CSV has no statistics and no embedded types, so the only way to know whether a row matches is to read and parse it — the engine is forced into a full scan. The catch is that skipping only pays off when the data is sorted or partitioned by the filter columns, so the min/max ranges are narrow and non-overlapping; randomly scattered values make every range span everything and nothing gets skipped." },
    { q: "What is the small-files problem, why does it cripple query planning, and how do table formats help?", a: "The small-files problem happens when a job — usually a streaming writer flushing on a short interval — produces enormous numbers of tiny Parquet files instead of a few large ones. Each file carries its own footer and costs one I/O just to open and read. Before an engine can plan a query it must first list the files in the table and read each footer for its statistics. On cloud object stores, recursively listing millions of objects is notoriously slow and expensive, and reading millions of footers dominates planning time, so the query is slow before it has scanned any actual data. The standard fix is compaction: a periodic background job rewrites the swarm of tiny files into files in the 128 MB to 1 GB range, collapsing both listing and footer overhead. Table formats like Iceberg, Delta Lake, and Hudi address this directly: they keep data in Parquet but add a manifest layer that tracks the current set of files with per-file stats, so the engine prunes whole files at the manifest level before opening any footer, and they ship compaction plus ACID commits, snapshots, and metadata-only schema evolution on top." },
  ]}
/>

<Recap lang="en">
Parquet stores data column by column instead of row by row, which makes two things possible that CSV and JSON cannot do: column pruning, where a query reads only the column chunks it needs, and row-group skipping, where the engine uses min/max/null-count statistics in the file footer to skip whole row groups (and pages) that can't satisfy the filter — predicate pushdown. Inside, the layout nests file → row group (~128 MB) → column chunk → page (~1 MB), and each page is first encoded (dictionary, RLE, bit-packing, delta) and then compressed (snappy for hot data, zstd for ~15–20% smaller cold files), landing typically at 9–15% of the original CSV size. The senior failure modes are the small-files problem (millions of tiny files make listing and footer-reading dominate query planning — fix with compaction), wrong row-group size, dictionary blowup on high-cardinality columns (fallback to plain encoding, sometimes a larger file), and messy schema evolution across many files. Skipping only pays when data is clustered by the filter columns, so sort and partition deliberately. And because raw Parquet has no transactions or snapshots, table formats — Iceberg, Delta Lake, Hudi — wrap it with a manifest layer for ACID, time travel, safe schema evolution, and manifest-level pruning, which is what turns a pile of Parquet files into a lakehouse table.
</Recap>
