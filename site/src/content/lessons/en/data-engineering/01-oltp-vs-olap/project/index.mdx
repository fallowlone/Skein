---
concepts:
- workload-split
- column-pruning
- cdc-etl
- buffer-cache-eviction
- denormalization
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://clickhouse.com/resources/engineering/why-columnar-databases-are-fast
- https://clickhouse.com/resources/engineering/unifying-oltp-and-olap
- https://aws.amazon.com/blogs/database/improve-postgresql-performance-diagnose-and-mitigate-lock-manager-contention/
status: ready
summary: "Hands-on project — reproduce the analytics-on-OLTP outage, then split the workload into a columnar store fed by CDC/ETL and prove the fix with before/after numbers."
title: 'OLTP vs OLAP: split the workload'
track: data-engineering
unit: 01-oltp-vs-olap
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about the replica-analytics outage is not the same as causing it and then fixing it. Stand up a row-store OLTP database, drive an analytical scan into it until checkout degrades, then split the workload into a columnar store fed by CDC/ETL — and prove every step with measurements.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: reproduce the contention, measure it, stand up the right OLAP layout, wire a feed, and verify both that analytics got faster and that the transactional path stopped suffering.
</Goal>

<ProjectBrief
  id="01-oltp-vs-olap-project"
  lessonSlug="01-oltp-vs-olap"
  lang="en"
  objective="Reproduce the analytics-on-OLTP failure on a row store, then move analytics to a separate columnar store fed by CDC or ETL — proving the analytical query got dramatically faster AND that the transactional workload is no longer harmed, with before/after numbers under identical load."
  requirements={[
    "Load a row-store OLTP database (Postgres/MySQL) with a wide orders table — at least 20M rows, 20+ columns, a normalized schema with a country/dimension FK. Drive a steady transactional load: point lookups by id and small writes at a fixed rate.",
    "Run an analytical query against the same instance (or its read replica): a multi-month aggregate such as SUM(total) GROUP BY country with no selective WHERE. Capture EXPLAIN (ANALYZE, BUFFERS) — confirm a sequential scan and record bytes/pages read and execution time.",
    "Measure the contention: transactional p99 latency, buffer-cache hit ratio, and (if using a replica) replication lag, both with and without the analytical query running. Show the OLTP path degrades when analytics runs.",
    "Demonstrate that adding an index does NOT fix it: create a plausible composite index, re-run the aggregate, and show the planner still chooses a sequential scan (or the index only adds write cost).",
    "Stand up a separate columnar store (ClickHouse, DuckDB, or a cloud warehouse) with a denormalized wide fact table; fold the dimension in as a low-cardinality/dictionary-encoded column and choose a sensible sort/order key.",
    "Feed the columnar store from OLTP: a CDC stream (Debezium → Kafka or logical replication) or a batch ETL/ELT job. State the data-latency window your feed introduces.",
    "Re-run the identical aggregate against the columnar store and re-run the transactional load on OLTP simultaneously; record the after numbers next to before.",
  ]}
  acceptance={[
    "A before/after table: analytical query time, bytes/pages scanned, OLTP p99, buffer-cache hit ratio, and replication lag — all measured under the same load, not estimated.",
    "The columnar store's plan shows it reads only the referenced columns (pruning), and the analytical query is at least 10× faster than the row-store scan.",
    "With analytics moved off the OLTP instance, transactional p99 and cache hit ratio return to baseline while the analytical query runs — the contention is gone.",
    "A one-paragraph write-up: why the index did not help (selectivity vs scan volume), which column-store property gave most of the speedup, and the data-latency cost you accepted for isolation.",
  ]}
  stretch={[
    "Add a compression comparison: report on-disk size of the table in the row store vs the columnar store, and break down which columns compressed most and why (low cardinality, same type).",
    "Make the CDC feed near-real-time and measure end-to-end lag from an OLTP commit to its visibility in the columnar store; chart lag under a write spike.",
    "Add a guardrail that prevents analytical queries from ever hitting the OLTP instance (a separate role/endpoint, statement timeout, or a query router) and show it blocks the destructive scan.",
    "Stand up an HTAP option (e.g. a column replica or a HTAP engine) for the same workload and compare it against the two-store split on analytical latency, OLTP p99 under load, data freshness, and operational cost.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real data-architecture decision: reproduce the contention so you understand it, prove an index cannot fix a full-table aggregate, stand up the right columnar layout (denormalized, low-cardinality, pruned), feed it with CDC or ETL, and verify with before/after numbers that analytics got faster and the transactional path stopped suffering — accepting a bounded data-latency window for isolated storage, caches, and failure domains. Doing it once on a toy makes the production version muscle memory.
</Recap>
