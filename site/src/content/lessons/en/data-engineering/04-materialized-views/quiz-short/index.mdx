---
concepts:
- materialized-view
- refresh-strategy
- refresh-concurrently
- incremental-view-maintenance
- staleness-window
- streaming-materialized-view
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/sql-refreshmaterializedview.html
- https://github.com/sraoss/pg_ivm
- https://materialize.com/blog/self-correcting-materialized-views/
status: ready
summary: "Free-recall prompts across the materialized-views unit. Answer each from memory first, then reveal the model answer and compare."
title: 'Materialized views: free-recall review'
track: data-engineering
unit: 04-materialized-views
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the tradeoffs stick when you are in the design review.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms — what a materialized view buys, the refresh ladder, CONCURRENTLY's hidden costs, incremental maintenance, streaming MVs, and the staleness failure mode — without looking back at the lesson.
</Goal>

<RetrievalDrawer
  client:load
  id="04-materialized-views-short-retrieval"
  lang="en"
  questions={[
    { q: "What does a materialized view actually buy you compared to a plain view, and what is the one decision that matters once you have one?", a: "A plain view is just a saved query: every read re-executes the full SELECT against live base tables, so it is always fresh but pays the full query cost on every read. A materialized view runs the query once, stores the result rows as a real relation on disk, and serves reads as a cheap scan or index lookup. You pay storage and compute up front, once, so that many reads are cheap — that is the entire value proposition. The cost you take on is correctness over time: the view is stale the instant a base row changes after the last refresh. So the only decision that matters becomes how and how often you refresh, and what staleness window the consumer can tolerate." },
    { q: "Contrast plain REFRESH MATERIALIZED VIEW with REFRESH ... CONCURRENTLY, including the requirements and costs of CONCURRENTLY.", a: "Plain REFRESH re-runs the defining query from scratch and takes an ACCESS EXCLUSIVE lock for its entire duration, so every read of the view blocks until it finishes — refresh a 90-second aggregate and the dashboard is unavailable for 90 seconds. REFRESH ... CONCURRENTLY builds the new result in the background and swaps it in via row-level diffs, so readers keep seeing the old version with no read-blocking lock. The costs: it requires at least one UNIQUE index on the view (plain columns, no expressions, no WHERE); it is generally slower than a plain refresh because it diffs against existing rows; only one refresh can run against a given view at a time; and it waits for in-flight transactions to drain before it starts, so long analytical queries can stall it and let a refresh backlog build that never catches up." },
    { q: "What is incremental view maintenance, how does pg_ivm implement it in Postgres, and when does it pay off versus backfire?", a: "Incremental view maintenance (IVM) computes only the delta — the rows affected by recent writes — and applies it to the stored view, instead of recomputing everything. Vanilla Postgres has no native incremental refresh; pg_ivm fills the gap. create_immv() populates a real table and installs AFTER triggers on every base table in the query, so each INSERT/UPDATE/DELETE computes and applies its delta inside the same transaction, giving near-zero staleness. It pays off when the base table is read-heavy and modified infrequently: reads stay cheap and fresh for little maintenance cost. It backfires under a high write rate — every write now pays trigger and delta-maintenance overhead and contends on view locks, throttling throughput — so a scheduled full refresh is better there. It also imposes SQL restrictions on what the view may contain." },
    { q: "How do streaming materialized views (ClickHouse, Materialize) change the model, and what is the catch a senior must internalize for each?", a: "Streaming MVs remove the refresh decision: the view is maintained continuously as data arrives, so there is no refresh job to schedule, monitor, or have fail silently. ClickHouse's incremental MV behaves like an AFTER INSERT trigger on the source — each inserted block is transformed and written to the target table at insert time, reading only the new rows. The catch: it only ever sees the inserted block, so it ignores data that existed before the MV was created unless you POPULATE, and it aggregates only within a single insert block — groups split across blocks reconcile only if the target engine (e.g. AggregatingMergeTree) merges them. Materialize compiles SQL into a differential dataflow that does work proportional to the size of the change, maintaining complex joins and aggregations at millisecond latency with strict serializability. Its catch is operational and conceptual: it is a separate system to run, and you must reason about its consistency model — eventually-consistent reads in some setups surprise users who expect read-your-writes." },
    { q: "A teammate proposes a materialized view refreshed nightly to speed a slow report. What three risks do you raise before approving?", a: "First, staleness: a nightly refresh means the report can be up to 24 hours behind, so confirm the consumer tolerates that window — finance and compliance dashboards often cannot. Second, the lock: a plain REFRESH takes an ACCESS EXCLUSIVE lock for its whole duration and blocks every read of the view, so if it runs while anyone is reading, the report goes unavailable; switch to REFRESH ... CONCURRENTLY and add the unique index it requires. Third, and the one that bites hardest: ownership and monitoring. A refresh that fails silently — a lock timeout, an OOM, a dropped cron — leaves the view frozen at its last good state while reads keep succeeding, so nothing looks broken; the data is wrong but the system is green. You need alerting on refresh success and duration plus a visible last-refreshed timestamp so stale data is detectable, not discovered weeks later by an angry stakeholder." },
    { q: "Order the decisions a senior makes when reaching for a materialized view, and say why ownership comes last but matters most.", a: "(1) Confirm the read is genuinely expensive and repeated — otherwise a plain view or a base-table index is enough and you avoid the staleness problem entirely. (2) Decide the staleness window the consumer can actually tolerate: seconds, minutes, or a day. (3) Pick refresh to match the write rate: minute-scale tolerance and a high write rate point to scheduled REFRESH CONCURRENTLY; fresh-now on a low-write read-heavy table points to incremental (pg_ivm); real-time on a stream points to a streaming MV. (4) If CONCURRENTLY, add the required UNIQUE index on the view. (5) Add monitoring and alerting on the refresh job. Ownership comes last in sequence but matters most because every other choice degrades gracefully into mere latency or cost, whereas an unmonitored refresh degrades into silently serving wrong data — the failure that recurs across every strategy in the unit." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: a materialized view trades freshness for read speed, so refresh is the only decision that matters; plain refresh blocks reads with an ACCESS EXCLUSIVE lock while CONCURRENTLY avoids that for the cost of a unique index, slower diffs, and waiting on old transactions; pg_ivm gives near-zero staleness but taxes every write, so it suits read-heavy low-write tables only; streaming MVs remove the refresh job but add a system and a consistency model to reason about; and across all of them the recurring failure is an unmonitored refresh serving stale data while everything looks green — so own the refresh, bound the staleness window, and alert on it.
</Recap>
