---
concepts:
- elt-vs-etl
- schema-on-read
- medallion-architecture
- idempotent-loads
- incremental-models
- storage-compute-separation
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.getdbt.com/blog/elt-best-practices-snowflake
- https://docs.getdbt.com/docs/build/incremental-models
- https://docs.getdbt.com/docs/build/incremental-microbatch
- https://coalesce.io/data-insights/snowflake-cost-optimization-a-practical-engineering-guide-to-reducing-snowflake-costs/
spiral: []
status: ready
summary: ETL transforms before load in a separate engine; ELT loads raw into the warehouse
  and transforms in SQL. Cheap columnar compute and split storage/compute flipped
  the default — and moved the cost onto the warehouse bill.
title: 'ELT vs ETL: where the Transform runs, and why the industry flipped'
track: data-engineering
unit: 02-elt-vs-etl
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A finance analyst pings you on a Monday: "Why did our Snowflake bill jump 40% last week?" Nothing shipped — except a pipeline change. Someone set a dbt model to `full-refresh` by default and scheduled it hourly. Every run rescans the entire 2 TB fact table and rebuilds it from scratch. The transform is correct. The bill is the bug. This is the ELT tax nobody warned them about: when the warehouse does the T, the warehouse meters every byte you touch.

By the end of this lesson you'll understand why the industry flipped from ETL to ELT, what replayability actually buys you, and exactly how to avoid paying for compute you don't need.
</Hook>

<Crux>ETL transforms before load in a separate engine; ELT loads raw and transforms in-warehouse. Where the T runs decides cost, replay, fidelity.</Crux>

<Explanation>

## The pipeline split: where does T live

When you're designing a data pipeline, the first question isn't which tool to use — it's where the transform runs. That one decision determines whether a bug costs you a one-line SQL fix or a week of re-extraction from a source that may no longer hold the data you need.

Both patterns move data from sources into a warehouse. The only real difference is the order of the middle letter — and that one swap changes everything downstream.

**ETL** (extract, transform, load) was born when storage and compute were the same expensive box. You could not afford to dump everything into the warehouse, so you transformed first — in a separate engine (Informatica, a Spark/Hadoop cluster, a Python job) — cleaned, joined, filtered, and aggregated, then loaded only the polished result. The warehouse only ever saw the finished table.

**ELT** (extract, load, transform) flips the last two steps. You load the raw source data into the warehouse first, untouched, then transform it *in place* with SQL. The transformation is no longer a separate system — it is queries running on the same engine that stores the data. dbt is the dominant tool for the "T": version-controlled, tested, documented SQL models instead of a black-box pipeline.

The reason the industry flipped is architectural, not fashion. Snowflake and BigQuery decouple storage from compute. Storage is cheap object storage; compute is a separate, elastic resource you spin up only when a query runs. That breaks the old constraint completely — you *can* afford to land raw, because storing it costs almost nothing, and you only pay for compute when you actually transform.

## Why ELT won: raw is replayable

The deepest reason ELT became the default is not cost — it is **replayability**. In ETL, the raw source data is thrown away after transformation; the warehouse holds only the transformed output. So when (not if) you discover a bug in the transform — a wrong join key, a timezone error, a currency that should have been converted — you cannot fix history. The raw input is gone. You have to re-extract from the source, and the source may have changed, rate-limited you, or no longer hold the old rows.

In ELT, the raw data lives permanently in the warehouse. A transform bug is a one-line SQL fix plus a re-run against data you already have. No re-extract. This is the heart of the **medallion architecture**: a **bronze** layer of raw, append-only ingested data; a **silver** layer of cleaned, conformed, deduplicated tables; a **gold** layer of business-ready aggregates and marts. Each layer is rebuildable from the one below it, and bronze is rebuildable from the source only if you must. The contract is: never mutate bronze, always transform forward.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">ETL (transform before load)</th>
        <th class="border border-border px-3 py-2 text-left">ELT (transform in-warehouse)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Raw fidelity</td>
        <td class="border border-border px-3 py-2">Lost — only transformed output is kept</td>
        <td class="border border-border px-3 py-2">Preserved — bronze layer is replayable</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Fix a transform bug</td>
        <td class="border border-border px-3 py-2">Re-extract from source (may be gone)</td>
        <td class="border border-border px-3 py-2">Edit SQL, re-run on existing raw</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Where compute is billed</td>
        <td class="border border-border px-3 py-2">Separate engine (your own cluster)</td>
        <td class="border border-border px-3 py-2">The warehouse — every transform is on the bill</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">PII / cost control before load</td>
        <td class="border border-border px-3 py-2">Strong — strip/mask before it lands</td>
        <td class="border border-border px-3 py-2">Weak — raw (incl. PII) lands first</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Schema discipline</td>
        <td class="border border-border px-3 py-2">Schema-on-write (enforced at load)</td>
        <td class="border border-border px-3 py-2">Schema-on-read (you enforce in silver)</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="Two pipelines compared. ETL extracts, then transforms in a separate engine, then loads the finished result into the warehouse. ELT extracts, loads the raw data into the warehouse first, then transforms it in place inside the warehouse."
  hue="var(--d-data)"
  perRow={4}
  nodes={[
    { id: "etl-e", label: "Extract", sub: "from source", col: 0, row: 0 },
    { id: "etl-t", label: "Transform", sub: "separate engine", col: 1, row: 0 },
    { id: "etl-l", label: "Load", sub: "polished result", col: 2, row: 0 },
    { id: "etl-w", label: "Warehouse", sub: "finished table", col: 3, row: 0 },
    { id: "elt-e", label: "Extract", sub: "from source", col: 0, row: 1 },
    { id: "elt-l", label: "Load", sub: "raw, untouched", col: 1, row: 1 },
    { id: "elt-w", label: "Warehouse", sub: "raw bronze", col: 2, row: 1 },
    { id: "elt-t", label: "Transform", sub: "in-warehouse SQL", col: 3, row: 1 },
  ]}
  edges={[
    { from: "etl-e", to: "etl-t" },
    { from: "etl-t", to: "etl-l" },
    { from: "etl-l", to: "etl-w" },
    { from: "elt-e", to: "elt-l" },
    { from: "elt-l", to: "elt-w" },
    { from: "elt-w", to: "elt-t", label: "in place" },
  ]}
  caption="ETL (top) transforms before the load, so the warehouse only ever sees the finished table. ELT (bottom) loads raw first, then transforms in place inside the warehouse — the Transform box moves to the right of Load."
/>

## The cost moves onto the warehouse bill

ELT does not make transformation free — it moves the cost from a CapEx cluster you owned to a metered OpEx line on the warehouse bill, and that line is brutally sensitive to how you write SQL. The Hook is the canonical failure: a model materialized as a full table rebuild, scheduled often, scanning everything every run. On Snowflake you pay per warehouse-second; on BigQuery you pay per byte scanned. A `SELECT *` over an unpartitioned 2 TB table, run hourly, is a four-figure monthly line item by itself.

The fix is **incremental models**. Instead of rebuilding the whole table, you process only the new or changed rows since the last run. In dbt this is the `is_incremental()` macro wrapping a filter:

```sql
{% if is_incremental() %}
  where event_time >= (select max(event_time) from {{ this }})
{% endif %}
```

On the first run the table is built fully; on every run after, only rows newer than the current max are touched. A nightly job that scanned 2 TB now scans the day's delta — often a few GB — cutting both runtime and the metered bytes by one to two orders of magnitude. When you're setting up a new dbt model, the default posture is: incremental by default, full refresh only when logic changes (`dbt run --full-refresh`), and a per-team warehouse so a heavy transform never starves the BI dashboards sharing compute. Teams that skip auto-suspend on idle warehouses bleed credits for compute that is doing nothing.

<Inset kind="why" lang="en">
"Schema-on-read" sounds like freedom but it is a deferred bill. ETL's schema-on-write rejects a malformed row at load time — you find out immediately. ELT happily lands anything into bronze; the contract violation surfaces later, in silver, often as a silent NULL or a wrong join. ELT does not remove the schema work — it moves it downstream and makes it *your* job to enforce in tests, not the loader's.
</Inset>

## Idempotency: the retry that doubles your data

The failure that wakes people up is duplicate data. EL tools like Fivetran and Airbyte, and your own loaders, retry on transient failure — that is correct behavior. But if a load is *not idempotent*, a retry that re-runs a partially-succeeded batch inserts the same rows twice. Now your revenue total is inflated and nobody trusts the dashboard.

The cure is to make loads and incremental transforms **idempotent**: running them twice produces the same result as running them once. In dbt incremental models this is the `merge` strategy with a `unique_key`:

```sql
{{ config(materialized='incremental', incremental_strategy='merge', unique_key='event_id') }}
```

On each row, `merge` updates if the `unique_key` already exists and inserts if it does not — an upsert. A re-run of the same batch updates rows in place instead of duplicating them. The trap: an incremental model with **no** `unique_key` and **no** `is_incremental()` filter silently *appends* its full output on every run, so it both duplicates and re-scans everything — the worst of both worlds. Microbatch models go further, treating each time-window batch as an atomic, independently-replaceable unit, so a failed batch reruns cleanly without touching its neighbors.

<TradeoffMatrix
  id="01-elt-vs-etl-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A regulated fintech ingests payment events containing card PANs and PII. Compliance forbids raw cardholder data from ever sitting in the analytics warehouse. Pick the pattern."
  options={[
    { name: "ETL: mask/tokenize PANs and drop disallowed PII in a pre-load step, then load only the compliant result", summary: "Transform before load.", correct: true, justification: "When a hard compliance line says raw PII must never land in the warehouse, you have to act before the load — that is exactly what ETL's transform-before-load gives you. You strip, mask, or tokenize in a controlled engine, and the warehouse only ever stores compliant data, shrinking your audit surface. You lose raw replayability for those fields, which is the accepted price of the regulation." },
    { name: "ELT: land raw payment events in bronze, then mask in the silver transform", summary: "Mask in-warehouse.", justification: "This is the modern default for most data, but it loses here on the explicit constraint: raw PANs physically land in bronze before any masking runs, so cardholder data sits in the warehouse — exactly what compliance forbids. Schema-on-read can't retroactively un-land data that already touched disk." },
    { name: "ELT with column-level masking policies applied at query time", summary: "Mask on read.", justification: "Dynamic masking hides PII from readers, but the raw values are still physically stored in bronze — masking is a view-time control, not a storage control. If the rule is 'never sits in the warehouse,' stored-but-hidden still violates it." },
    { name: "Skip the warehouse for PII fields and email a CSV to compliance", summary: "Side channel.", justification: "An out-of-band CSV is a worse PII exposure than the warehouse, with no access controls, no audit trail, and no lifecycle. This trades a governed system for an ungoverned one." },
  ]}
/>

<Quiz
  id="01-elt-vs-etl-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="You find a timezone bug in a transform that has been running for six months. Under ELT with a medallion architecture, what's the fast fix?"
  choices={[
    { label: "Fix the SQL in the silver model and re-run it against the raw bronze data you already have — no re-extract", correct: true },
    { label: "Re-extract six months of history from the source API and rebuild everything", misconception: "That's the ETL failure mode — you only re-extract when raw is gone. ELT keeps raw in bronze precisely so you can replay transforms without touching the source." },
    { label: "Patch the warehouse output table in place with an UPDATE statement", misconception: "Mutating gold by hand breaks the rebuildable contract and isn't reproducible — the next full run silently reverts your patch. Fix the transform, not the output." },
    { label: "Nothing can be done; transformed history is immutable", misconception: "That's only true in ETL where raw was discarded. ELT's whole point is that bronze is preserved, so history is replayable." },
  ]}
/>

<Quiz
  id="01-elt-vs-etl-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="An incremental dbt model is configured with no unique_key and no is_incremental() filter. What happens on every scheduled run?"
  choices={[
    { label: "It appends its full output again — duplicating rows and re-scanning everything, the worst of both worlds", correct: true },
    { label: "It safely upserts by primary key", misconception: "Upsert requires unique_key with the merge strategy. Without it, dbt can't match rows, so it appends." },
    { label: "It processes only new rows automatically", misconception: "Incremental filtering only happens inside an is_incremental() block. Without it, dbt has no WHERE clause limiting the scan." },
    { label: "dbt refuses to run and errors out", misconception: "dbt runs happily — that's the danger. It silently appends, so the bug shows up as duplicate data and a rising bill, not an error." },
  ]}
/>

<DragOrder
  id="01-elt-vs-etl-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the stages of a modern ELT pipeline from source to dashboard:"
  items={[
    { id: "a", label: "Extract + Load: EL tool (Fivetran/Airbyte) copies raw source data into the warehouse" },
    { id: "b", label: "Bronze: raw, append-only landed data, never mutated" },
    { id: "c", label: "Silver: cleaned, deduplicated, conformed tables (dbt models, schema enforced here)" },
    { id: "d", label: "Gold: business-ready aggregates and marts" },
    { id: "e", label: "BI / dashboards read from gold" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

Together, these five stages form the medallion contract: raw is preserved in bronze so every layer above it is rebuildable. Without that order — specifically without landing raw before you transform — you lose the ability to replay history when logic changes, which is the most costly thing to lose.

</Explanation>

<KeyTakeaway>ELT lands raw first so transforms stay replayable, but the T runs on the metered warehouse — go incremental and idempotent (merge on a unique_key) or face exploding spend and dupes. ETL still wins when PII can't land.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-elt-vs-etl-retrieval"
  lang="en"
  questions={[
    { q: "Explain why the industry flipped from ETL to ELT, and what you gave up in the trade.", a: "The flip was enabled by cloud warehouses (Snowflake, BigQuery) separating storage from compute. Storage became cheap object storage, so landing all the raw data costs almost nothing; compute became elastic, so you only pay when a transform actually runs. That removed the old constraint that forced you to transform before load. The decisive win is replayability: ELT keeps raw data permanently in a bronze layer, so when you find a transform bug — a wrong join, a timezone error — you fix the SQL and re-run against data you already have, instead of re-extracting from a source that may have changed or disappeared (the ETL failure mode). What you give up: the transform cost now lands on the metered warehouse bill instead of a cluster you owned, raw PII physically lands before you can mask it, and schema enforcement moves from load-time (schema-on-write) to your own downstream tests (schema-on-read). For most data the trade is worth it; for hard compliance lines on PII, ETL's transform-before-load still wins." },
    { q: "A retry duplicated rows in a fact table and inflated revenue. What design would have prevented it, and why?", a: "The root cause is a non-idempotent load: re-running a partially-succeeded batch inserted the same rows again. EL tools and loaders retry on transient failure by design, so any load that isn't idempotent will eventually double its data. The fix is to make loads and incremental transforms idempotent — running twice yields the same result as running once. In dbt, that's an incremental model using incremental_strategy='merge' with a unique_key (e.g. event_id): merge updates the row if the key already exists and inserts only if it doesn't, so a re-run upserts in place instead of appending. The classic trap is an incremental model with no unique_key and no is_incremental() filter — dbt silently appends its whole output every run, duplicating rows and re-scanning everything. Microbatch models harden this further by making each time-window batch an atomic, independently-replaceable unit, so a failed batch reruns without disturbing its neighbors." },
  ]}
/>

<Recap lang="en">
ETL and ELT differ only in where the Transform runs, and that one swap decides cost, replayability, and data fidelity. ETL transforms in a separate engine before load and keeps only the polished output — strong for stripping PII and controlling cost before data lands, but raw is discarded, so a transform bug means re-extracting from a source that may be gone. ELT loads raw into the warehouse first and transforms in SQL (dbt), which the cloud's separation of storage and compute made cheap to do. Its decisive advantage is replayability via the medallion pattern: immutable bronze raw, cleaned silver, business-ready gold, each rebuildable from below. The price is that transform compute now meters on the warehouse bill — so you go incremental (process only the delta, not a full rebuild) and idempotent (merge on a unique_key) or you get exploding spend and duplicate rows on retry. Choose ELT by default; reach for ETL when a hard rule says raw PII must never touch the warehouse. Now when you see a Snowflake bill spike after a pipeline change, your first question is: is this model a full rebuild running on a schedule — and if so, what does the incremental filter look like?
</Recap>
