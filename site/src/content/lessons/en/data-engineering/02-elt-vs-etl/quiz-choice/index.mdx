---
concepts:
- elt-vs-etl
- storage-compute-separation
- medallion-architecture
- idempotent-loads
- incremental-models
- warehouse-cost
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.getdbt.com/blog/elt-best-practices-snowflake
- https://docs.getdbt.com/docs/build/incremental-models
status: ready
summary: "Multiple-choice synthesis across the ELT-vs-ETL unit: where the Transform runs, replayability, warehouse cost, idempotency, and the medallion contract."
title: 'ELT vs ETL: multiple-choice review'
track: data-engineering
unit: 02-elt-vs-etl
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you actually make designing a pipeline — not a definition to recite, but a tradeoff to weigh against cost, replay, and compliance.
</Hook>

<Goal lang="en">
Confirm you can connect where the Transform runs to its downstream consequences: replayability, the warehouse bill, schema discipline, and the idempotency that keeps a retry from doubling your data.
</Goal>

<Quiz
  id="02-elt-vs-etl-choice-1"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="What single architectural change in cloud warehouses (Snowflake, BigQuery) is the real reason the industry flipped from ETL to ELT?"
  choices={[
    { label: "Storage and compute were decoupled. Storage became cheap object storage, so landing all raw data costs almost nothing; compute became elastic, so you only pay when a transform runs. That removed the constraint that forced you to transform before load.", correct: true },
    { label: "SQL engines got fast enough to out-perform Spark on every workload.", misconception: "Raw query speed is not the driver — Spark still wins many heavy jobs. The flip was economic and architectural: cheap decoupled storage made landing raw affordable, which ETL could never do." },
    { label: "dbt was released, giving teams a tool for in-warehouse transforms.", misconception: "dbt rode the wave; it did not cause it. The enabling change was storage/compute separation. dbt is just the dominant way to author the in-warehouse T once that became viable." },
    { label: "Data volumes shrank, so transforming in the warehouse became cheap.", misconception: "Volumes grew, not shrank. ELT became viable despite larger volumes precisely because separated storage made keeping all of it cheap." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-2"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="You discover a timezone bug in a transform that has shipped wrong numbers for six months. Under ELT with a medallion architecture, what is the fast, correct fix?"
  choices={[
    { label: "Fix the SQL in the silver model and re-run it against the raw bronze data you already have — no re-extract from the source.", correct: true },
    { label: "Re-extract six months of history from the source API and rebuild everything.", misconception: "That is the ETL failure mode — you only re-extract when raw was discarded. ELT keeps raw permanently in bronze precisely so you can replay transforms without touching a source that may have changed or rate-limited you." },
    { label: "Patch the gold output table in place with an UPDATE statement.", misconception: "Mutating gold by hand breaks the rebuildable contract and is not reproducible — the next full run silently reverts your patch. Fix the transform, not the output." },
    { label: "Nothing can be done; transformed history is immutable once written.", misconception: "That is only true in ETL where raw was thrown away. ELT's entire premise is that bronze is preserved, so transformed history is always replayable." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-3"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="A dbt model was set to full-refresh by default and scheduled hourly; it rebuilds a 2 TB fact table from scratch every run and the Snowflake bill jumped 40%. The output is correct. Where is the bug and what is the fix?"
  choices={[
    { label: "The bug is the materialization, not the logic. ELT moves transform compute onto the metered warehouse, so a full rebuild scanned hourly bills for every byte. Make the model incremental — process only rows newer than the current max — and reserve full-refresh for when logic changes.", correct: true },
    { label: "The SQL is wrong; correct output cannot also be a cost bug.", misconception: "Correctness and cost are independent axes in ELT. A perfectly correct transform materialized as an hourly full rebuild is a four-figure line item — the bill is the bug even when the numbers are right." },
    { label: "Snowflake mispriced the workload; open a support ticket.", misconception: "The pricing works as designed — you pay per warehouse-second for the compute you requested. The fix is on your side: stop rescanning 2 TB hourly by going incremental." },
    { label: "Raise the warehouse size so each run finishes faster and costs less.", misconception: "A bigger warehouse finishes faster but bills more credits per second; total cost for the same full scan barely moves. The leverage is scanning less data (incremental), not scanning the same data faster." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-4"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="A regulated fintech ingests payment events containing card PANs, and compliance forbids raw cardholder data from ever sitting in the analytics warehouse. Which pattern fits, and why is the modern ELT default wrong here?"
  choices={[
    { label: "ETL: mask/tokenize PANs in a controlled pre-load engine and load only the compliant result. The warehouse never stores raw PII, shrinking the audit surface — the accepted price is losing raw replayability for those fields.", correct: true },
    { label: "ELT: land raw events in bronze, then mask in the silver transform.", misconception: "This is the right default for most data but loses on the hard constraint: raw PANs physically land in bronze before any masking runs, so cardholder data sits in the warehouse — exactly what compliance forbids." },
    { label: "ELT with column-level dynamic masking applied at query time.", misconception: "Dynamic masking hides PII from readers, but the raw values are still physically stored in bronze. Masking is a view-time control, not a storage control — stored-but-hidden still violates 'never sits in the warehouse'." },
    { label: "ELT is always correct now; satisfy compliance with row access policies.", misconception: "Access policies govern who reads the data, not whether it is stored. The rule here is about storage, so no in-warehouse control satisfies it — the masking must happen before load." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-5"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="An EL tool retried a partially-succeeded load and your revenue fact table now shows inflated totals. What property was missing, and what is the durable design fix?"
  choices={[
    { label: "The load was not idempotent — re-running a partial batch re-inserted the same rows. Make loads and incremental transforms idempotent with a merge/upsert on a unique_key so a re-run updates rows in place instead of appending duplicates.", correct: true },
    { label: "The EL tool is buggy; disable retries so a load never runs twice.", misconception: "Retries on transient failure are correct behaviour and you cannot rely on never re-running. The fix is to make the load safe to re-run (idempotent), not to forbid re-running it." },
    { label: "The warehouse should have rejected the duplicate rows automatically.", misconception: "An append-style load has no key to dedupe on, so the warehouse happily stores duplicates. Deduplication is your design choice — a unique_key with merge — not an automatic guarantee." },
    { label: "Add a nightly job that deletes duplicate rows after the fact.", misconception: "A cleanup job is a band-aid that races every new load and can delete legitimate rows. Idempotent loads prevent the duplicate from ever being written, which is the real fix." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-6"
  lessonSlug="02-elt-vs-etl"
  lang="en"
  question="Someone calls schema-on-read 'pure freedom — no schema to fight at load time.' What does the unit's framing say they are missing?"
  choices={[
    { label: "Schema-on-read is a deferred bill, not free. ETL's schema-on-write rejects a malformed row at load and you find out immediately; ELT lands anything into bronze and the contract violation surfaces later in silver as a silent NULL or wrong join. The schema work moves downstream and becomes your job to enforce in tests.", correct: true },
    { label: "Nothing — schema-on-read genuinely removes the schema work.", misconception: "It removes the schema work at load time only. The work reappears downstream: you must enforce structure in silver with tests, or bad data silently corrupts gold." },
    { label: "Schema-on-read is slower to query than schema-on-write.", misconception: "The point is not query speed — it is when validation happens. The cost of skipping load-time validation is silent data-quality bugs that surface late, not slower reads." },
    { label: "Schema-on-read only applies to JSON, not tabular data.", misconception: "It applies to any format landed raw. The framing is about deferring enforcement to read/transform time regardless of the source shape." },
  ]}
/>

<Recap lang="en">
The through-line: where the Transform runs decides everything downstream. Decoupled storage/compute made landing raw cheap, which buys replayability through the medallion contract (immutable bronze, cleaned silver, business-ready gold). But the T now meters on the warehouse bill, so you go incremental by default. And because loaders retry, every load must be idempotent — merge on a unique_key — or a retry doubles your data. ELT is the default; ETL still wins when a hard rule says raw PII must never touch the warehouse. Now when you're reviewing a pipeline design or a dbt model, your first three questions are: where does the transform run, is it incremental, and does it have a unique_key — because those three answers predict the bill, the data quality, and the retry safety.
</Recap>
