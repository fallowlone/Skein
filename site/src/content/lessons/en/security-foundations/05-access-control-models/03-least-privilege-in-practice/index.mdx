---
concepts:
- least-privilege
- privilege-creep
- just-in-time-access
- deny-by-default
- authorization-bug
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-least-privilege-in-practice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
- https://csrc.nist.gov/projects/role-based-access-control
spiral: []
status: ready
summary: 'Least privilege is the principle everyone cites and almost nobody holds. The decay is privilege creep — grants accumulate, nothing is ever revoked. The cure is operational: deny by default, just-in-time elevation, and a revocation loop.'
title: Least privilege in practice
track: security-foundations
unit: 05-access-control-models
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You pull the IAM policy attached to your team's main service role to debug a permissions error, and there it is: `s3:*` on `Resource: "*"`. Nobody decided the billing worker should be able to delete every bucket in the account — it just *ended up* that way. Eighteen months ago someone needed read access to one bucket, hit a deny, slapped a wildcard on it to unblock a release, and moved on. The grant outlived the task, the person, and the reason. That role is now a single leaked key away from wiping production storage, and the scariest part is that everything works perfectly. Least privilege didn't fail loudly. It eroded, one expedient grant at a time.
</Hook>

By the end of this lesson you'll know why least privilege decays into over-privilege, and the three operational moves — deny by default, just-in-time access, and a revocation loop — that actually hold the line.

<Crux>Least privilege is a practice, not a setting: fight privilege creep with deny-by-default and just-in-time access.</Crux>

<Explanation>

## The principle, and why stating it isn't enough

The principle of least privilege says every subject — a user, a service, a process — should hold exactly the permissions it needs to do its job, and not one more. NIST calls it out as a foundation of access control; the OWASP Authorization Cheat Sheet opens with "enforce least privileges" and "deny by default." Every engineer nods along. Almost no production system actually has it.

The reason is that least privilege is not a property you *configure once* — it's a property you have to *maintain against constant pressure*. Permissions are sticky in one direction. Granting is easy: someone is blocked, you add an allow, the ticket closes, everyone is happy *today*. Revoking is hard: nobody is blocked by a permission you still have, so there is no pull to remove it, no failing test, no paged alert. The system that grants on demand and never revokes doesn't drift toward least privilege — it drifts away from it, monotonically. That asymmetry is the whole problem, and it has a name.

## Privilege creep: the slow leak

**Privilege creep** is the gradual accumulation of permissions a subject no longer needs. It comes from a handful of utterly mundane sources, none of which feels like a security decision at the time:

- **The role change that adds without subtracting.** An engineer moves from the payments team to platform. They get the platform group's access on day one. The payments access? It's still there a year later — nobody owns the subtraction, and revoking it might break *something*, so it stays.
- **The expedient wildcard.** A `deny` blocks a release at 4pm. The fix that ships is `s3:*` / `Resource: *`, because narrowing it to the three actions and one bucket actually needed takes twenty minutes you don't have. The wildcard is permanent; the deadline was temporary.
- **The standing admin.** Break-glass access for a 2am incident is never revoked the next morning. Now it's just "the access I have," and three months later it's load-bearing for some routine task it was never meant for.
- **The copied role.** New service needs permissions? Clone the role from the service next to it. Now it inherits every grant *that* service accreted, including the ones nobody can explain.

Each step is locally rational and globally corrosive. The endpoint is a fleet of identities that each hold far more power than their actual job requires — which means the blast radius of any single compromised credential is enormous, and totally invisible until the day it's exploited.

<FlowDiagram
  label="The privilege-creep ratchet: a subject starts at least privilege, then a role change, an expedient wildcard, and unrevoked break-glass access each add permissions that are never removed, so effective privilege only climbs; deny-by-default plus a just-in-time grant that expires is the lever that pushes it back down."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "start", label: "Least privilege", sub: "scoped grants" },
    { id: "move", label: "Role change", sub: "add, no subtract" },
    { id: "wild", label: "Expedient wildcard", sub: "s3:* on *" },
    { id: "glass", label: "Break-glass", sub: "never revoked" },
    { id: "jit", label: "JIT + expiry", sub: "auto-revoke" },
  ]}
  edges={[
    { from: "start", to: "move", label: "creep" },
    { from: "move", to: "wild", label: "creep" },
    { from: "wild", to: "glass", label: "creep" },
    { from: "glass", to: "jit", label: "the lever" },
  ]}
  caption="Privilege creep is a ratchet: each grant clicks effective privilege up and nothing clicks it back down. Just-in-time access with a hard expiry is the only step that moves the ratchet the other way — it grants when needed and revokes automatically when the window closes."
/>

## Deny by default: making "no" the starting point

The structural fix for creep starts at the foundation: **deny by default**. Authorization begins from a refusal, and access is granted only by an explicit, positive rule that matches the subject, the action, and the resource. Anything not explicitly allowed is denied — including, critically, anything *new*.

This sounds obvious until you contrast it with the failure mode it prevents. An *allow-by-default* system (or any allowlist with a wildcard fallback) fails open: add a new endpoint, a new resource type, a new admin action, and it's reachable *unless someone remembered* to write a deny rule for it. Deny by default fails closed: the new thing is unreachable until someone writes the allow. The difference is whether a forgotten rule leaks access or merely produces a 403 that someone files a ticket about. Fail-closed turns your omissions into inconveniences instead of breaches.

In practice this means: no `Resource: "*"` and no `action: *` in policies you can avoid them in; permissions scoped to specific actions on specific resources; and the default answer to "can this identity do this thing" is *no* unless a rule says yes. The cost is real — you write more, narrower rules, and you eat more 403s during development. That cost is the point. It's the friction that keeps the wildcard out.

<Inset kind="why" lang="en">
Why not just audit permissions once a quarter and prune the excess? Because a periodic audit fights an asymmetry it can't win. Grants accrue continuously — every day, driven by real deadlines — while pruning happens in a batch four times a year and is nobody's favorite work. Worse, you can't safely remove a permission you can't *prove* is unused, and proving a negative ("nothing relies on this") across a live system is genuinely hard, so audits err toward keeping things. The audit isn't useless, but it's a mop, not a faucet. The durable fix changes how grants are *made* — short-lived and self-expiring — so the excess never accumulates in the first place.
</Inset>

## Just-in-time access: grants that expire on their own

The asymmetry that drives creep is "easy to grant, hard to revoke." **Just-in-time (JIT) access** attacks it directly by making revocation automatic: instead of holding a standing permission forever, a subject *requests* elevated access for a specific task, gets it for a bounded window, and the system *takes it back* when the window closes — no human has to remember.

The pattern shows up at every layer:

- **Human admins:** standing administrator membership is removed; an engineer requests elevation (often with a reason and an approval) and gets it for, say, 60 minutes, after which the role membership is automatically stripped. The default state of every human is *not* privileged.
- **Services:** instead of a long-lived API key with broad scope baked into the deployment, the workload assumes a role and receives short-lived credentials (minutes to an hour) that auto-rotate and auto-expire. A leaked credential is only useful inside its tiny window.
- **Break-glass:** emergency access is itself a JIT grant — explicitly requested, time-boxed, loudly logged, and auto-revoked — so the 2am incident doesn't leave a permanent admin grant behind.

JIT flips the steady state. With standing access, the default is "everyone has more than they need, all the time," and you spend effort *removing* excess you'll never fully catch. With JIT, the default is "nobody has anything until they ask," and privilege exists only for the minutes it's actually in use. You're no longer fighting creep with cleanup — you've removed the surface creep accumulates on.

<TradeoffMatrix
  id="03-least-privilege-tradeoff-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="en"
  prompt="A service role has `s3:*` on `Resource: *` because someone added a wildcard 18 months ago to unblock a release. The service actually only ever calls GetObject and PutObject on one bucket. What's the right fix?"
  options={[
    { name: "Scope the policy to exactly `s3:GetObject` and `s3:PutObject` on the one bucket's ARN, and move the role to short-lived assumed-role credentials", summary: "Least privilege + JIT.", correct: true, justification: "This is the actual fix: narrow the permissions to the specific actions on the specific resource the workload uses, and replace any long-lived key with short-lived, auto-expiring credentials so a leak has a tiny window. Deny-by-default plus least privilege, applied to both the grant and its lifetime." },
    { name: "Leave the wildcard but rotate the access key every 90 days", summary: "Rotate the key.", justification: "Rotation limits how long a *leaked* key stays valid, but it does nothing about the scope — the role can still delete every bucket in the account the entire time it's valid. You've capped credential lifetime without touching the over-privilege that makes the credential dangerous." },
    { name: "Keep `s3:*` but add a deny rule for `s3:DeleteBucket` so the scariest action is blocked", summary: "Deny the scary action.", justification: "Blocklisting individual dangerous actions is a losing game: you can't enumerate every harmful action, and the next risky API you forget to deny is reachable. This is allow-by-default with patches, the inverse of deny-by-default. Allow only what's needed instead of denying what you fear." },
    { name: "Restrict the role by attaching it only to the production VPC so external attackers can't use it", summary: "Network-scope it.", justification: "Network controls are defense in depth, not authorization. A compromise that already runs inside the VPC (the common case for a leaked workload credential) still inherits `s3:*` on everything. It doesn't reduce the privilege the identity holds, which is the actual problem." },
  ]}
/>

## How seniors hold the line

Holding least privilege is an operational loop, not a one-time config. The senior moves: make deny the default so new surface fails closed; make grants narrow (specific actions on specific resources, no reflexive wildcards) and short-lived (JIT with hard expiry) so privilege exists only while it's used; and close the loop with revocation — when someone changes teams, when a project ends, when break-glass fires, the *subtraction* must be owned, not left to a quarterly audit that errs toward keeping. The grant that has no expiry and no owner is the one that becomes next year's `s3:*` incident.

<Quiz
  id="03-least-privilege-quiz-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="en"
  question="Why does privilege creep almost always make permissions grow over time rather than shrink, even on well-run teams?"
  choices={[
    { label: "Granting is pulled by a real, immediate blocker (someone's stuck now) while revoking has no equivalent pull — nothing breaks if you keep a permission you no longer need", correct: true },
    { label: "Because IAM systems technically don't support removing permissions once granted", misconception: "Removal is fully supported everywhere. The problem is incentive asymmetry, not a missing feature — nobody is forced to revoke because unused permissions don't cause visible failures." },
    { label: "Because attackers add permissions to roles as part of their exploitation", misconception: "Creep is an internal, benign-intent process — role changes, expedient wildcards, unrevoked break-glass. It's how normal teams operate under deadlines, not an attack step." },
    { label: "Because least privilege only applies to humans, and services legitimately need broad access", misconception: "Least privilege applies to every subject, services especially — a service with s3:* is exactly the over-privileged identity that turns one leaked credential into a full breach." },
  ]}
/>

<Quiz
  id="03-least-privilege-quiz-2"
  lessonSlug="03-least-privilege-in-practice"
  lang="en"
  question="What is the core security advantage of just-in-time access over a standing admin grant?"
  choices={[
    { label: "A leaked or abused credential is only useful inside a short, bounded window because the access auto-expires — the default state of every identity is unprivileged", correct: true },
    { label: "It encrypts the credentials so they can't be stolen in the first place", misconception: "JIT is about the lifetime and scope of authorization, not encryption. The credential can still be captured while live; the point is that it's worthless minutes later, not that it can't be taken." },
    { label: "It makes audits unnecessary because the system is automatically compliant", misconception: "JIT reduces standing privilege but doesn't remove the need to review who can request elevation and what approvals exist. It shrinks the surface; it doesn't make oversight obsolete." },
    { label: "It grants more permissions faster, so engineers are never blocked", misconception: "The goal is the opposite — nobody holds elevated access by default; they request it per task and lose it automatically. Speed of unblocking isn't the security property; bounded lifetime is." },
  ]}
/>

<DragOrder
  id="03-least-privilege-drag-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="en"
  prompt="Order these steps to remediate an over-privileged service role without an outage, from first to last:"
  items={[
    { id: "a", label: "Observe which actions/resources the role actually uses (access logs / IAM access analyzer)" },
    { id: "b", label: "Write a new policy scoped to exactly those observed actions on those specific resources" },
    { id: "c", label: "Deploy the scoped policy and watch for deny errors / 403s in staging and canary" },
    { id: "d", label: "Replace the long-lived key with short-lived assumed-role credentials" },
    { id: "e", label: "Add an owner and an expiry/review date so the grant can't silently become standing again" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Least privilege decays because granting has urgency and revoking has none — that asymmetry is privilege creep. Hold the line operationally: deny by default so new surface fails closed, scope grants to specific actions on specific resources, make them just-in-time with hard expiry, and own the revocation loop. The grant with no owner and no expiry becomes next year's `s3:*` incident.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-least-privilege-retrieval"
  lang="en"
  questions={[
    { q: "Explain why least privilege erodes into over-privilege over time, and name the mechanism.", a: "The mechanism is privilege creep: the gradual accumulation of permissions a subject no longer needs. It erodes because grant and revoke are asymmetric. Granting has a real, immediate pull — someone is blocked, you add an allow, the problem is solved today — while revoking has none: nobody is blocked by a permission you still hold, nothing fails, no test goes red, no alert pages. So permissions are sticky in one direction and the system drifts monotonically away from least privilege. The sources are mundane and feel like nothing at the time: a role change that adds new-team access without subtracting old-team access; an expedient wildcard (s3:* on Resource: *) shipped to beat a deadline instead of the narrow grant that would take twenty more minutes; break-glass admin access that's never revoked after the incident; and roles cloned from a neighbor, inheriting grants nobody can explain. Each step is locally rational and globally corrosive, and the endpoint is a fleet of identities each holding far more power than their job needs — so the blast radius of any one leaked credential is enormous and invisible until it's exploited." },
    { q: "What do deny-by-default and just-in-time access each fix, and why is JIT the more durable cure?", a: "Deny by default fixes the *fail mode* of new surface. Authorization starts from a refusal and access is granted only by an explicit positive rule matching subject, action, and resource; anything not allowed — including anything new — is denied. The contrast is an allow-by-default system that fails open: a new endpoint or resource is reachable unless someone remembered to write a deny, so a forgotten rule leaks access. Deny-by-default fails closed: a forgotten rule produces a 403 someone files a ticket about, not a breach. Just-in-time access fixes the *asymmetry itself* — 'easy to grant, hard to revoke' — by making revocation automatic. Instead of standing permission, a subject requests elevated access for a specific task, gets it for a bounded window, and the system strips it when the window closes: humans request 60-minute elevation, services assume roles for short-lived auto-expiring credentials, break-glass is time-boxed and auto-revoked. JIT is the more durable cure because deny-by-default and periodic audits still fight an accumulating pile — you're forever cleaning up excess you can't fully catch — whereas JIT removes the surface that excess accumulates on: privilege exists only for the minutes it's in use, the default state of every identity is unprivileged, and a leaked credential is useless outside its tiny window." },
  ]}
/>

<Recap lang="en">
Least privilege is the principle everyone cites and almost nobody holds, because it isn't a setting you configure once — it's a property you maintain against constant pressure. Granting is pulled by a real, immediate blocker; revoking has no equivalent pull, so permissions drift monotonically upward. That decay is privilege creep, fed by role changes that add without subtracting, expedient wildcards shipped to beat deadlines, unrevoked break-glass, and copied roles. The operational cure has three moves. Deny by default makes authorization start from "no," so new surface fails closed — a forgotten rule yields a 403, not a breach. Narrow, short-lived grants scope permissions to specific actions on specific resources. And just-in-time access makes revocation automatic: elevation is requested per task, held for a bounded window, and stripped when it closes, so the default state of every identity is unprivileged and a leaked credential is worthless minutes later. Now when you see `s3:*` on `Resource: *`, your first question is: who owns the subtraction, and when does this grant expire?
</Recap>
