---
concepts:
- hashing
- encryption
- digital-signature
- one-way-function
- integrity-vs-confidentiality
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-hashing-encryption-signing
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'Hashing, encryption, and signing are three different primitives engineers
  constantly confuse. Each guarantees something distinct — and using the wrong one
  is how passwords leak, sessions get forged, and "it''s encrypted" turns out to mean
  nothing.'
title: Hashing vs encryption vs signing
track: security-foundations
unit: 02-cryptography-for-engineers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A junior on your team ships a "secure" password store: `AES-256` over each password, key in the config. Code review nods — AES-256, sounds strong. Six months later a backup leaks, the key leaks with it, and every password in the database is recoverable in plaintext. The bug wasn't a weak algorithm. It was the *wrong primitive entirely*: passwords must be **hashed**, not encrypted, precisely because hashing is the one operation you can't reverse. The same week, another engineer "signs" an API payload by SHA-256-ing it — proving nothing, because anyone can recompute that hash. Three primitives, three different guarantees, and reaching for the wrong one is one of the most common ways otherwise-careful systems get breached.
</Hook>

By the end of this lesson you'll be able to say, for hashing, encryption, and signing, exactly what each one guarantees, what it does *not*, and which one a given problem actually needs.

<Crux>Hashing proves integrity, encryption hides content, signing proves origin — confuse them and "it's secure" means nothing.</Crux>

<Explanation>

## The three primitives, and the one property that separates them

The fastest way to keep these straight is to ask one question of each: **can you get the original back?**

- **Hashing** is a *one-way function*. `SHA-256("hello")` always yields the same 256-bit digest, but there is no `unhash()` — the input is destroyed. You verify by recomputing the hash of a candidate and comparing. Its job is **integrity** and **fingerprinting**: did this bytes-for-bytes match? It says nothing about confidentiality (the digest is public) and nothing about *who* produced it.
- **Encryption** is *reversible with a key*. `encrypt(plaintext, key)` produces ciphertext that `decrypt(ciphertext, key)` turns back into plaintext. Its job is **confidentiality**: keep the content secret from anyone without the key. By itself, classic encryption does *not* prove the message wasn't tampered with — which is why modern systems use authenticated encryption (AES-GCM, ChaCha20-Poly1305) that bolts an integrity tag on.
- **Signing** is the *asymmetric* one. You sign with a **private** key and anyone verifies with the matching **public** key. Its job is **authenticity** (this came from the holder of the private key) plus **integrity** (and wasn't changed since). It deliberately does *not* hide the content — a signed JWT is readable by anyone.

<FlowDiagram
  label="The three primitives sorted by reversibility and key model: hashing is one-way with no key and yields a digest for integrity; encryption is reversible with one shared secret key and yields ciphertext for confidentiality; signing uses a private key to produce a signature and a public key to verify it, giving authenticity and integrity."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "hash", label: "Hashing", sub: "one-way, no key" },
    { id: "enc", label: "Encryption", sub: "reversible, key" },
    { id: "sign", label: "Signing", sub: "private/public" },
    { id: "digest", label: "Digest", sub: "integrity" },
    { id: "cipher", label: "Ciphertext", sub: "confidentiality" },
    { id: "sig", label: "Signature", sub: "authenticity" },
  ]}
  edges={[
    { from: "hash", to: "digest", label: "fingerprint" },
    { from: "enc", to: "cipher", label: "hide" },
    { from: "sign", to: "sig", label: "prove origin" },
  ]}
  caption="One question separates them: can you reverse it? Hashing — never (no key). Encryption — yes, with the key. Signing — verify with a different (public) key than you signed with. The output column names the single property each one buys you."
/>

## What each one does *not* guarantee — the failure modes

The breaches come from the negative space: assuming a primitive gives you a property it never promised.

**Hashing is not encryption.** A hash is public-safe but not secret-safe. "We hash the SSNs" protects nothing if the input space is small — an attacker hashes every possible SSN (there are only ~10⁹) and matches them in seconds. Hashing only hides what is *high-entropy and unguessable*. This is also why a plain hash is wrong for passwords: passwords are low-entropy, so you need a *slow, salted* password hash (`bcrypt`, `scrypt`, `Argon2id`) whose entire point is to be expensive to compute, defeating offline brute-force. `SHA-256(password)` is billions of guesses per second on a GPU; Argon2id with proper parameters is a handful.

**Encryption is not integrity, and not authentication.** Classic CBC-mode ciphertext can be *modified* by an attacker who can't read it — flip bits in the ciphertext, flip bits in the decrypted plaintext (the padding-oracle and bit-flipping attack families). "It's encrypted" does not mean "it's untampered" unless you use an AEAD mode that authenticates. And encryption says nothing about *who* sent it: a shared key means anyone with the key could have produced the message.

**Signing is not encryption.** A signature is attached to data that stays in the clear. People routinely think a signed JWT is "secure" and put secrets in its payload — but the payload is just base64, readable by anyone who intercepts it. Signing protects against forgery and tampering, never against eavesdropping.

<Inset kind="why" lang="en">
Why is signing *asymmetric* (private to sign, public to verify) when a shared-key MAC like HMAC also proves integrity? Because of *who can do what*. With HMAC, the verifier holds the same secret as the signer — so the verifier could have forged the message too; it proves integrity only between mutually trusting parties. A digital signature splits the key: only the private-key holder can produce a valid signature, and the *whole world* can verify it without being able to forge. That's why TLS certificates, software releases, and JWTs signed with `RS256`/`ES256` use asymmetric signatures — you need third parties to verify origin without trusting them with forging power. HMAC (`HS256`) is the right tool only when one party both issues and checks the token.
</Inset>

## Choosing the right primitive per problem

The senior move is to map the *requirement* to the primitive, not the algorithm name to a vibe of "strong." Walk the requirement: do I need to keep this secret (confidentiality → encryption), prove it wasn't changed (integrity → hash/MAC/signature), prove who produced it (authenticity → signature or MAC), or store a secret I only ever need to *check*, never read back (one-way → password hash)?

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Requirement</th>
        <th class="border border-border px-3 py-2 text-left">Primitive</th>
        <th class="border border-border px-3 py-2 text-left">Concrete choice</th>
        <th class="border border-border px-3 py-2 text-left">The trap to avoid</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Store a password</td>
        <td class="border border-border px-3 py-2">Slow password hash</td>
        <td class="border border-border px-3 py-2">Argon2id (or bcrypt/scrypt) + per-user salt</td>
        <td class="border border-border px-3 py-2">Encrypting it (key leak = plaintext) or plain SHA-256 (GPU brute-force)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Keep data secret at rest / in transit</td>
        <td class="border border-border px-3 py-2">Authenticated encryption</td>
        <td class="border border-border px-3 py-2">AES-256-GCM or ChaCha20-Poly1305</td>
        <td class="border border-border px-3 py-2">Unauthenticated CBC (tamperable); reusing a nonce</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Verify a file/download is intact</td>
        <td class="border border-border px-3 py-2">Hash (fingerprint)</td>
        <td class="border border-border px-3 py-2">SHA-256 digest, compared constant-time</td>
        <td class="border border-border px-3 py-2">MD5/SHA-1 (collisions); trusting a hash from the same channel</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Prove origin to third parties</td>
        <td class="border border-border px-3 py-2">Asymmetric signature</td>
        <td class="border border-border px-3 py-2">Ed25519 / ECDSA (RS256/ES256 for JWT)</td>
        <td class="border border-border px-3 py-2">Putting secrets in a signed-but-unencrypted payload</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Tamper-proof a token between two trusting services</td>
        <td class="border border-border px-3 py-2">MAC (shared-key)</td>
        <td class="border border-border px-3 py-2">HMAC-SHA-256</td>
        <td class="border border-border px-3 py-2">Treating HMAC as a third-party-verifiable signature</td>
      </tr>
    </tbody>
  </table>
</div>

A real system usually *composes* them rather than picking one. TLS 1.3 — the protocol behind every `https://` — uses all three in one handshake: an asymmetric **signature** to authenticate the server's certificate, asymmetric key exchange to agree on a secret, that secret feeding **authenticated encryption** for the session, and **hashing** (HKDF) woven through key derivation and the transcript that proves the handshake itself wasn't tampered with. Knowing which primitive does which job inside that stack is the difference between configuring TLS and cargo-culting it.

<TradeoffMatrix
  id="01-hashing-encryption-signing-tradeoff-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="en"
  prompt="You're storing user passwords for a login system. Pick the correct approach."
  options={[
    { name: "Hash each password with a slow, salted password hash (Argon2id / bcrypt / scrypt); on login, hash the attempt and compare", summary: "One-way, slow, salted.", correct: true, justification: "Passwords are a one-way storage problem: you never need the original back, only to check a candidate. A slow salted password hash is purpose-built for this — the salt defeats precomputed rainbow tables, and the deliberate cost (memory/iterations) makes offline brute-force of leaked hashes economically painful. Even if the database leaks, there is no key and no reverse function to recover the plaintext." },
    { name: "Encrypt each password with AES-256 and store the key in the application config", summary: "Reversible with a key.", justification: "This is the classic wrong primitive. Encryption is reversible by design, so the entire security collapses to protecting the key — and keys leak through configs, backups, env dumps, and source history. The moment the key is exposed, every password is plaintext. Passwords must be irreversible, which is exactly what a hash gives and encryption cannot." },
    { name: "Hash each password with a single round of SHA-256 (fast and standard)", summary: "Fast hash, no salt.", justification: "It's the right family (one-way) but the wrong member. A fast unsalted hash is verifiable at billions of guesses per second on a GPU and is vulnerable to precomputed rainbow tables. Passwords are low-entropy, so you need a deliberately slow, salted, memory-hard hash — speed is a vulnerability here, not a feature." },
    { name: "Sign each password with the server's private key so it can't be forged", summary: "Asymmetric signature.", justification: "Signing solves authenticity, not secret storage. A signature doesn't hide the password (the data stays in the clear) and doesn't give you a one-way comparison for login. Wrong problem, wrong primitive." },
  ]}
/>

<Quiz
  id="01-hashing-encryption-signing-quiz-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="en"
  question="A teammate says 'the JWT is signed with our key, so it's safe to put the user's API token inside the payload.' What's wrong?"
  choices={[
    { label: "Signing proves the token wasn't forged or altered, but it doesn't encrypt the payload — anyone who intercepts the JWT can base64-decode and read the API token", correct: true },
    { label: "Nothing is wrong; a signed JWT is encrypted end-to-end", misconception: "A standard signed JWT (JWS) is not encrypted. The header and payload are base64url-encoded, not ciphertext — readable by anyone holding the token. You'd need JWE (encrypted JWT) to hide the payload." },
    { label: "The problem is that JWTs should use a hash, not a signature", misconception: "A plain hash can't authenticate a token between parties — anyone can recompute it. The signature is the correct mechanism for integrity/authenticity; the actual mistake is assuming the signature also provides confidentiality." },
    { label: "It's fine as long as you use HS256 instead of RS256", misconception: "The algorithm choice doesn't change that signing leaves the payload in cleartext. Neither HS256 nor RS256 encrypts the body; both only sign it." },
  ]}
/>

<Quiz
  id="01-hashing-encryption-signing-quiz-2"
  lessonSlug="01-hashing-encryption-signing"
  lang="en"
  question="Why is encrypting data with AES-256-CBC alone (no MAC, no AEAD) insufficient when an attacker can modify the ciphertext in transit?"
  choices={[
    { label: "CBC provides confidentiality but not integrity — an attacker can flip ciphertext bits to predictably alter the decrypted plaintext, so you need authenticated encryption (AES-GCM) or a MAC over the ciphertext", correct: true },
    { label: "AES-256 is too weak; you should use AES-512", misconception: "AES-512 isn't a thing, and key size isn't the issue. The gap is integrity, not confidentiality strength — unauthenticated CBC can be tampered with regardless of key length." },
    { label: "CBC leaks the key over time, so the attacker eventually decrypts it", misconception: "CBC doesn't leak the key. The vulnerability is that the mode authenticates nothing, so ciphertext can be modified (bit-flipping, padding oracles) without the key." },
    { label: "It's fine — encryption always guarantees the message can't be changed", misconception: "This is the core misconception. Plain encryption guarantees confidentiality, not integrity. You only get tamper-detection from an AEAD mode or an explicit MAC." },
  ]}
/>

<DragOrder
  id="01-hashing-encryption-signing-drag-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="en"
  prompt="Match each requirement to its primitive — order these requirements from 'keep it secret' to 'prove who sent it':"
  items={[
    { id: "a", label: "Keep the content unreadable to anyone without the key → encryption" },
    { id: "b", label: "Detect any change to a downloaded file → hash (fingerprint)" },
    { id: "c", label: "Store a password you only ever check, never read back → slow salted password hash" },
    { id: "d", label: "Prove a message came from a specific party, verifiable by anyone → asymmetric signature" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Match the primitive to the requirement: encryption for confidentiality (and use an AEAD mode for integrity too), hashing for integrity/fingerprinting, a slow salted hash for passwords, and signatures for third-party-verifiable origin. "It's encrypted/hashed/signed" is meaningless until you name which property you actually bought.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-hashing-encryption-signing-retrieval"
  lang="en"
  questions={[
    { q: "For hashing, encryption, and signing, state what each one guarantees and the single property that distinguishes them.", a: "The distinguishing question is 'can you reverse it, and with what key?' Hashing is a one-way function with no key: it maps input to a fixed-size digest you can only verify by recomputing, so it buys integrity and fingerprinting (did these bytes match?) but never confidentiality (the digest is public) and never origin (anyone can compute it). Encryption is reversible with a key: encrypt then decrypt with that key, buying confidentiality — keeping content secret from anyone without the key — but on its own (classic modes) it does not guarantee integrity or authenticity, which is why modern systems use authenticated encryption (AES-GCM, ChaCha20-Poly1305) that adds an integrity tag. Signing is asymmetric: you sign with a private key and anyone verifies with the matching public key, buying authenticity (it came from the private-key holder) plus integrity (unchanged since), but it deliberately does not hide the data — a signed JWT is readable by anyone. So: hash = integrity, encrypt = confidentiality, sign = origin + integrity, separated by reversibility and key model." },
    { q: "Why must passwords be hashed with a slow salted hash rather than encrypted or plain-SHA-256'd, and why is a signed JWT not safe for secrets?", a: "Passwords are a one-way storage problem: at login you only ever need to check a candidate, never recover the original. Encrypting them is the wrong primitive because encryption is reversible — the whole scheme collapses to protecting a key, and keys leak through configs, backups, and source history, turning every password back to plaintext the moment the key is exposed. A plain single-round SHA-256 is the right family but wrong member: it's fast (billions of guesses/sec on a GPU) and, unsalted, vulnerable to precomputed rainbow tables. Passwords are low-entropy, so you need a deliberately slow, salted, memory-hard hash (Argon2id, bcrypt, scrypt): the salt kills rainbow tables and the cost makes offline brute-force of a leaked hash economically painful. Separately, a standard signed JWT (JWS) only signs the payload — the header and body are base64url, not ciphertext — so anyone who intercepts the token can read any 'secret' inside it. Signing proves the token wasn't forged or altered; it provides no confidentiality. To hide payload contents you'd need an encrypted JWT (JWE), not a signed one." },
  ]}
/>

<Recap lang="en">
Hashing, encryption, and signing are three distinct primitives, separated by one question: can you reverse it, and with which key? Hashing is one-way and keyless — it buys integrity and fingerprinting, never confidentiality or origin. Encryption is reversible with a key — it buys confidentiality, but classic modes give no integrity, so reach for authenticated encryption (AES-GCM, ChaCha20-Poly1305). Signing is asymmetric — private key to sign, public key to verify — buying third-party-verifiable authenticity plus integrity, while leaving the data readable. The breaches live in the negative space: encrypting passwords instead of hashing them (key leak = plaintext), plain-SHA-256-ing low-entropy data (GPU brute-force), trusting unauthenticated CBC for integrity (bit-flipping), or stuffing secrets into a signed-but-unencrypted JWT. The senior reflex is to name the requirement first — secret, intact, attributable, or check-only-never-read — and only then pick the primitive. Real systems like TLS 1.3 compose all three; knowing which does which is what separates configuring crypto from cargo-culting it.
</Recap>
