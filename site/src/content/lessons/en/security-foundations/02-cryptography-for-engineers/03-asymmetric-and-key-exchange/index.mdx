---
concepts:
- asymmetric-encryption
- rsa
- elliptic-curve
- diffie-hellman
- forward-secrecy
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-asymmetric-and-key-exchange
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'Public-key crypto solves the key-distribution problem symmetric ciphers can''t, but it''s 1000x slower — so you use it only to agree a symmetric key. This lesson covers RSA vs ECC, Diffie-Hellman key exchange, and why ephemeral DH gives forward secrecy.'
title: 'Asymmetric crypto and key exchange'
track: security-foundations
unit: 02-cryptography-for-engineers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Symmetric encryption is fast and unbreakable — and useless until two parties share a key. That's the catch nobody warns you about: AES-256 is great, but how does the browser and the server *both* know the same 32 random bytes when their first packet crosses an open Wi-Fi network full of people who can read everything? You can't ship the key in the clear; an eavesdropper grabs it and decrypts the whole session. You can't pre-share it; you've never met the server. For decades this was a genuine wall — until 1976, when Diffie and Hellman published a trick that lets two strangers derive a shared secret over a wiretapped line, where the attacker watches every byte go by and still can't compute it. Everything you call "HTTPS" rests on that one move.
</Hook>

By the end of this lesson you'll know why asymmetric crypto exists, where it's used and where it isn't, how Diffie-Hellman lets strangers agree on a key in the open, and what forward secrecy actually buys you.

<Crux>You use slow asymmetric crypto only to agree a fast symmetric key; ephemeral Diffie-Hellman makes that exchange forward-secret.</Crux>

<Explanation>

## Why a second kind of crypto exists

Symmetric ciphers — AES, ChaCha20 — use *one* key for both encrypt and decrypt. They are fast (a modern CPU does gigabytes per second with AES-NI) and, at 256 bits, beyond brute force. Their fatal limitation is the **key-distribution problem**: both sides need the same secret before they can talk, and getting it there safely over an untrusted network is exactly the problem you were trying to solve.

Asymmetric (public-key) crypto breaks the symmetry. Each party has a **key pair**: a public key it hands out freely and a private key it never shares. The two are mathematically linked so that what one does, only the other undoes. That gives you two distinct powers:

- **Encryption:** anyone encrypts to your *public* key; only your *private* key decrypts. Confidentiality without a pre-shared secret.
- **Signatures:** you sign with your *private* key; anyone verifies with your *public* key. Authenticity and integrity — proof the message came from the holder of that private key and wasn't altered.

The catch is cost. RSA-2048 decryption is on the order of a thousand times slower than AES per byte, and asymmetric algorithms also have a hard size limit on what they can directly encrypt (RSA can't encrypt more than its key size minus padding). So nobody encrypts a 4 MB request body with RSA. The universal pattern is **hybrid encryption**: use the expensive asymmetric step *once* to agree on a fresh random symmetric key, then encrypt the actual bulk traffic with fast AES. Asymmetric crypto is the handshake; symmetric crypto is the conversation.

## RSA vs elliptic curve: same job, different math

Two families dominate. **RSA** rests on the difficulty of factoring the product of two large primes. It's old, battle-tested, and simple to reason about — but its keys are large and getting larger as factoring attacks improve. **Elliptic-curve cryptography (ECC)** rests on a different hard problem (the elliptic-curve discrete log) that resists attack far better per bit, so it delivers equivalent security with dramatically smaller keys and faster operations. This is why mobile, TLS, and modern protocols moved to curves like P-256 and X25519.

The security-per-bit gap is the whole story — here is the equivalence the standards bodies publish:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Security level</th>
        <th class="border border-border px-3 py-2 text-left">Symmetric (AES)</th>
        <th class="border border-border px-3 py-2 text-left">RSA key size</th>
        <th class="border border-border px-3 py-2 text-left">ECC key size</th>
        <th class="border border-border px-3 py-2 text-left">What it means</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">~112-bit</td>
        <td class="border border-border px-3 py-2">3DES (legacy)</td>
        <td class="border border-border px-3 py-2">2048-bit</td>
        <td class="border border-border px-3 py-2">224-bit</td>
        <td class="border border-border px-3 py-2">Minimum acceptable today</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">128-bit</td>
        <td class="border border-border px-3 py-2">AES-128</td>
        <td class="border border-border px-3 py-2">3072-bit</td>
        <td class="border border-border px-3 py-2">256-bit</td>
        <td class="border border-border px-3 py-2">The modern default (P-256 / X25519)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">192-bit</td>
        <td class="border border-border px-3 py-2">AES-192</td>
        <td class="border border-border px-3 py-2">7680-bit</td>
        <td class="border border-border px-3 py-2">384-bit</td>
        <td class="border border-border px-3 py-2">High assurance (P-384)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">256-bit</td>
        <td class="border border-border px-3 py-2">AES-256</td>
        <td class="border border-border px-3 py-2">15360-bit</td>
        <td class="border border-border px-3 py-2">521-bit</td>
        <td class="border border-border px-3 py-2">Note RSA's brutal scaling: 5x the bits for 2x the security</td>
      </tr>
    </tbody>
  </table>
</div>

Read the bottom row carefully: to match AES-256, RSA needs a 15360-bit key — at which point key generation and operations are painfully slow — while ECC needs only 521 bits. RSA key size scales roughly with the *cube root* of the work to break it, so doubling the security level more than doubles the key. That non-linear penalty is why new systems default to curves and you mostly meet RSA only in legacy certificates and existing PKI.

## Diffie-Hellman: agreeing on a secret in the open

RSA-style key transport (one side picks the symmetric key and encrypts it to the other's public key) works, but it has a flaw we'll get to. The more important primitive is **Diffie-Hellman key exchange (DH)**, and its elliptic-curve form **ECDH**. DH is not encryption — it's a way for two parties to *jointly compute* a shared secret that neither one chose alone, over a channel an attacker is reading in full.

The mechanism, stripped to its essence: both sides agree on public parameters. Each side picks a **private** random value, derives a **public** value from it, and sends only the public value across the wire. Then each side combines *its own private value* with *the other's public value* — and by the algebra of the construction, both arrive at the identical shared secret. The eavesdropper saw both public values but neither private one, and recovering the secret from what crossed the wire is the hard problem (discrete log) that no efficient attack solves. The shared secret is then run through a key-derivation function to produce the symmetric session key.

<FlowDiagram
  label="Diffie-Hellman key exchange over an open channel: Alice and Bob each pick a private value, derive and send a public value, then combine their own private value with the other's public value to arrive at the same shared secret; the eavesdropper sees both public values but cannot compute the secret because the discrete-log problem has no efficient solution."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "param", label: "Public params", sub: "agreed openly" },
    { id: "apriv", label: "Alice private", sub: "a (secret)" },
    { id: "apub", label: "Alice public", sub: "sent on wire" },
    { id: "bpub", label: "Bob public", sub: "sent on wire" },
    { id: "shared", label: "Shared secret", sub: "→ AES key" },
  ]}
  edges={[
    { from: "param", to: "apriv" },
    { from: "apriv", to: "apub", label: "derive" },
    { from: "apub", to: "shared", label: "combine" },
    { from: "bpub", to: "shared", label: "with b" },
  ]}
  caption="DH lets two strangers compute the same secret without ever transmitting it. The attacker observes both public values on the wire; computing the shared secret from them is the discrete-log problem, which has no efficient solution at these sizes."
/>

There's a critical caveat: raw DH gives you confidentiality against a *passive* eavesdropper, but it does **not** tell you who's on the other end. An active attacker who sits in the middle can run a separate DH with each side and relay between them — a man-in-the-middle. DH must therefore be combined with **authentication**: in TLS the server *signs* its DH public value with the private key behind its certificate, so the client knows the value came from the real server and not an impostor. Key exchange establishes a secret; signatures and certificates establish *who you're sharing it with*. You need both.

<Inset kind="why" lang="en">
Why not just keep using RSA key transport, where the client encrypts the chosen session key to the server's public key? Because of what happens *after* a breach. With RSA key transport, the session key is protected by the server's long-lived private key — so an attacker who records your encrypted traffic today and steals that private key in two years can go back and decrypt every recorded session. The static key never changed, so one theft unlocks the whole archive. This is precisely the weakness ephemeral Diffie-Hellman removes, and it's why TLS 1.3 dropped RSA key transport entirely and mandates ephemeral (EC)DH for every handshake.
</Inset>

## Forward secrecy: making the past un-decryptable

The fix for that archive problem is **forward secrecy** (also called perfect forward secrecy). The idea: generate a *brand-new, throwaway* DH key pair for every session — that's the **ephemeral** in ECDHE — and discard the private values the moment the handshake completes. The long-term certificate key is used only to *sign* the ephemeral exchange, proving identity; it never encrypts the session key and never touches the shared secret.

The payoff is the property that an attacker who compromises the server's long-term private key *later* cannot decrypt traffic captured *earlier*. Each session's secret died with the ephemeral keys that produced it; there is nothing left on disk to recover it from. Without forward secrecy, one stolen private key retroactively unlocks years of recorded sessions — the "harvest now, decrypt later" attack, where an adversary records ciphertext today specifically to crack it once it obtains the key. With forward secrecy, each session is its own island. This is why TLS 1.3 makes ephemeral (EC)DH non-negotiable.

<TradeoffMatrix
  id="03-asym-key-exchange-tradeoff-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="en"
  prompt="You're configuring TLS for a service that handles sensitive long-lived data, and you must choose the key-exchange mode. Which choice gives forward secrecy and why does it matter here?"
  options={[
    { name: "Ephemeral ECDH (ECDHE): a fresh DH key pair per session, signed by the certificate; private values discarded after the handshake", summary: "Forward secret.", correct: true, justification: "This is forward secrecy. Each session's shared secret comes from throwaway ephemeral keys that are destroyed once the handshake finishes, so the session key exists nowhere afterward. If the server's long-term certificate key is stolen next year, recorded traffic from this year stays unreadable — the ephemeral secrets that produced those session keys are gone. For long-lived sensitive data this directly defeats harvest-now-decrypt-later. It's the TLS 1.3 default for exactly this reason." },
    { name: "RSA key transport: the client encrypts the chosen session key to the server's static RSA public key", summary: "No forward secrecy.", justification: "This has no forward secrecy. The session key is protected solely by the server's long-lived private key, so an attacker who records today's ciphertext and steals that one key later can decrypt every recorded session retroactively. TLS 1.3 removed this mode entirely. The static key is a single point of catastrophic, backward-reaching failure." },
    { name: "Static (non-ephemeral) Diffie-Hellman, reusing the same DH key pair across all sessions", summary: "DH without the point.", justification: "Reusing a fixed DH key pair throws away the entire benefit of DH. Because the same private value protects every session, compromising it once again unlocks all recorded sessions — you've recreated the static-key weakness while paying for DH. Forward secrecy requires the keys to be ephemeral; a reused DH key is not ephemeral." },
    { name: "A larger AES key (AES-256 instead of AES-128) for the bulk encryption", summary: "Wrong layer.", justification: "AES key size is about resisting brute force on the symmetric cipher; it has nothing to do with forward secrecy, which is a property of how the key was *agreed*. A bigger AES key still leaves the session decryptable if the key-exchange mode lets a future private-key theft recover that session key. You'd be hardening the conversation while leaving the handshake's archive weakness untouched." },
  ]}
/>

## Where asymmetric crypto is — and isn't — used

The senior mental model: asymmetric crypto is for *bootstrapping trust and keys*, not for *bulk work*. It shows up at exactly three moments — agreeing a session key (ECDHE), proving identity (the certificate's signature over the handshake), and verifying authenticity at rest (signed software updates, signed JWTs, code signing). Everywhere else — the gigabytes of actual request and response bodies, the data encrypted in your database, the files on disk — is symmetric AES, because asymmetric is too slow and size-limited to do that work. When you see "encrypt this 50 MB upload," the answer is never "RSA"; it's "generate a random AES key, encrypt the data with AES, and protect that one small key with the asymmetric layer."

<Quiz
  id="03-asym-key-exchange-quiz-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="en"
  question="Why do real systems use hybrid encryption (asymmetric to agree a key, then symmetric for the data) instead of just encrypting everything with the recipient's public key?"
  choices={[
    { label: "Asymmetric crypto is ~1000x slower than symmetric and has a hard size limit on what it can directly encrypt, so it's used only to protect a small symmetric key", correct: true },
    { label: "Public-key encryption isn't secure enough to protect real data on its own", misconception: "It's not a strength problem — properly used RSA/ECC are secure. The reason is performance and the direct-encryption size limit, which is why asymmetric only protects the small session key." },
    { label: "Symmetric ciphers can't be broken but asymmetric ones can, so you minimize asymmetric use", misconception: "Both families are secure at proper key sizes. Hybrid encryption is about speed and size limits, not about one family being breakable." },
    { label: "Browsers don't support public-key encryption of large payloads", misconception: "The constraint is the math, not browser support: asymmetric algorithms are slow and size-limited by design, so the standard everywhere is to encrypt bulk data with symmetric AES." },
  ]}
/>

<Quiz
  id="03-asym-key-exchange-quiz-2"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="en"
  question="An attacker has been silently recording your TLS traffic for two years. Today they steal your server's long-term private key. With TLS 1.3's ephemeral ECDH, what can they decrypt?"
  choices={[
    { label: "Nothing from the recorded past — each session's secret came from throwaway ephemeral keys that were destroyed after the handshake; the stolen long-term key only signed, it never produced the session keys", correct: true },
    { label: "Everything they recorded, because the private key was used to derive every session key", misconception: "That describes RSA key transport, not ephemeral ECDH. With ECDHE the long-term key only signs the handshake; the session keys come from ephemeral DH values that no longer exist, so past traffic stays sealed." },
    { label: "Only sessions from the last 24 hours, because keys rotate daily", misconception: "Forward secrecy isn't about a rotation window — the ephemeral keys are per-session and discarded immediately, so there's no daily-key concept here; all past sessions are protected, not just recent ones." },
    { label: "All future traffic, but not the recorded past", misconception: "With the key stolen they could impersonate the server going forward (until you rotate), but the question is about the recorded past — and forward secrecy means the past stays un-decryptable regardless." },
  ]}
/>

<DragOrder
  id="03-asym-key-exchange-drag-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="en"
  prompt="Order the steps of a forward-secret TLS handshake + session, from first to last:"
  items={[
    { id: "a", label: "Each side generates a fresh ephemeral DH key pair for this session" },
    { id: "b", label: "Each side sends its ephemeral DH public value across the wire" },
    { id: "c", label: "The server signs its ephemeral value with its certificate's private key (authentication)" },
    { id: "d", label: "Both sides combine their private value with the peer's public value to derive the same shared secret" },
    { id: "e", label: "Bulk application data flows encrypted with the fast symmetric (AES) key derived from that secret" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Asymmetric crypto solves key distribution but is ~1000x slower, so it only agrees a symmetric key (hybrid encryption). ECC beats RSA per bit. Diffie-Hellman lets strangers derive a shared secret over an open wire; making it ephemeral (ECDHE) gives forward secrecy — a future private-key theft can't decrypt recorded past sessions.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-asym-key-exchange-retrieval"
  lang="en"
  questions={[
    { q: "Explain why systems use hybrid encryption and what each crypto family does in a TLS session.", a: "Symmetric ciphers like AES are fast and unbreakable at 256 bits but suffer the key-distribution problem: both sides need the same secret before they can talk, which you can't safely send over an untrusted network. Asymmetric crypto solves distribution — each party has a public key it shares and a private key it keeps — but it's roughly 1000x slower per byte than AES and has a hard size limit on what it can directly encrypt. So real systems use hybrid encryption: the expensive asymmetric step runs once to agree a fresh random symmetric key, and then fast AES encrypts all the bulk traffic. In a TLS session asymmetric crypto does exactly three jobs — agreeing the session key (ephemeral ECDH), authenticating identity (the server signs the handshake with its certificate's private key), and verifying authenticity of things at rest (signed updates, JWTs). Everything else — the actual request/response bodies — is symmetric AES because asymmetric is too slow and size-limited for bulk work." },
    { q: "What is Diffie-Hellman, why does it need authentication, and how does ephemeral DH give forward secrecy?", a: "Diffie-Hellman is a key-exchange primitive (not encryption) that lets two strangers jointly compute a shared secret over a channel an attacker is reading in full. Both sides agree on public parameters, each picks a private random value and sends only the derived public value; each then combines its own private value with the peer's public value to reach the identical secret. The eavesdropper sees both public values but recovering the secret is the discrete-log problem, which has no efficient solution at these sizes. The elliptic-curve form is ECDH. Raw DH protects only against a passive eavesdropper — it doesn't prove who's on the other end, so an active man-in-the-middle could run separate exchanges with each side. That's why DH must be combined with authentication: in TLS the server signs its DH public value with its certificate key. Forward secrecy comes from making the DH keys ephemeral (ECDHE): a brand-new throwaway DH key pair per session, discarded right after the handshake, with the long-term certificate key only signing — never producing the session key. So if an attacker records traffic for years and later steals the long-term private key, they still can't decrypt the recorded past, because each session's ephemeral secret is already gone. This defeats harvest-now-decrypt-later, and it's why TLS 1.3 mandates ephemeral (EC)DH and removed RSA key transport." },
  ]}
/>

<Recap lang="en">
Symmetric crypto (AES) is fast and secure but can't solve key distribution: both parties need the same secret first. Asymmetric crypto fixes that with public/private key pairs — anyone encrypts to your public key, only your private key decrypts; you sign with your private key, anyone verifies with your public one. But it's ~1000x slower and size-limited, so the universal pattern is hybrid encryption: use asymmetric once to agree a symmetric key, then AES for the bulk. ECC beats RSA dramatically per bit (a 256-bit curve matches a 3072-bit RSA key), which is why modern protocols use P-256 and X25519. Diffie-Hellman lets two strangers derive a shared secret over a wiretapped line because recovering it is the discrete-log problem — but it needs authentication (a signature) to stop a man-in-the-middle. Making the DH keys ephemeral and per-session gives forward secrecy: a future theft of the long-term key can't decrypt recorded past traffic, which is why TLS 1.3 mandates ephemeral (EC)DH. Next time you see "encrypt this large payload," your reflex is: generate a random AES key, encrypt the data with AES, and protect that one small key with the asymmetric layer — never RSA the whole thing.
</Recap>
