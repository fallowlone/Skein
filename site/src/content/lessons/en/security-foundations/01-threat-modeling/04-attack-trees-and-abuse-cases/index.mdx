---
concepts:
- attack-tree
- abuse-case
- misuse-case
- and-or-decomposition
- attacker-goal
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-attack-trees-and-abuse-cases
sources:
- https://owasp.org/www-community/Threat_Modeling_Process
- https://www.schneier.com/academic/archives/1999/12/attack_trees.html
spiral: []
status: ready
summary: "Attack trees decompose one attacker goal into AND/OR sub-goals down to costed leaf attacks, and abuse cases write the evil user story beside the real one. Goal-centric where STRIDE is element-centric — reach for trees when you need to reason about the cheapest path."
title: "Attack trees and abuse cases: thinking like an attacker"
track: security-foundations
unit: 01-threat-modeling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A security engineer drops a STRIDE table on the table: thirty rows, one per data-flow element, every threat category dutifully filled in. The room nods. Then someone asks the only question that matters — "so what's the *cheapest* way an attacker actually steals a logged-in session?" — and the table goes quiet. STRIDE told you which elements could be attacked; it never connected them into a path with a price tag. The attacker doesn't care about your element list. They have one goal, and they will walk whichever branch is cheapest. That branch — the one your element-by-element review never assembled — is an attack tree.
</Hook>

By the end of this lesson you'll be able to decompose an attacker's goal into an AND/OR attack tree, cost its leaves to find the cheapest path, and write abuse cases that bake the attacker's intent into the requirements alongside the user story.

<Crux>An attack tree breaks an attacker goal into costed AND/OR sub-goals; abuse cases write the evil user story. Goal-centric, not element.</Crux>

<Explanation>

## The attacker has a goal, not a checklist

STRIDE (the spoofing/tampering/repudiation/info-disclosure/DoS/elevation taxonomy you walk against a DFD) is **element-centric**: you stand at each box and arrow in the data-flow diagram and ask "what could go wrong *here*." It is exhaustive and great for coverage — but it fragments the threat. The attacker never thinks in elements. They think in *goals*: "I want a valid admin session," "I want to read another tenant's data," "I want to bypass payment." An attack tree is the **goal-centric** complement: you write the attacker's goal at the root and decompose downward until you reach concrete leaf attacks you can actually cost.

Bruce Schneier formalized the structure in 1999. The root node is the attacker's objective. Every node below it is a sub-goal — a thing that, if achieved, contributes to its parent. You keep decomposing until the leaves are atomic actions: "phish the credential," "replay a leaked token," "exploit a known CVE in the auth library." The value isn't the diagram; it's that once the tree exists, you can annotate every leaf and let the *structure* compute which whole-path attacks are feasible.

## Reading AND vs OR (this is the whole game)

Every interior node is either an **OR node** or an **AND node**, and confusing them inverts your defense priorities.

- An **OR node** means *any one* child achieves the parent. The sub-goals are alternative routes. To reach the parent, the attacker picks the **cheapest, most feasible** child — so the cost of an OR node is the **minimum** over its children. OR nodes are where attackers shop for the easy path.
- An **AND node** means *all* children must be achieved together. The cost of an AND node is the **sum** of its children, and its feasibility is gated by the **hardest** child. AND nodes are where your defenses multiply — adding one more required step (a second factor, a signed nonce) raises the whole subtree's cost.

This is the senior reflex: **defenders want to turn ORs into ANDs.** If "steal a session" currently has three cheap OR branches, you have three independent ways to lose. If you can force *every* path to also require something expensive — say, a device-bound credential the attacker can't replay — you have converted alternatives into a conjunction, and the attacker now pays for the union, not the minimum.

Cost and feasibility on leaves can be anything you can reason about consistently: dollar cost, skill required, time, detectability, or a simple boolean "possible / impossible." Even a coarse "cheap / moderate / expensive" labeling is enough to make the cheapest path jump out, and the cheapest *feasible* path is exactly the one you fix first.

<FlowDiagram
  label="An attack tree for the goal 'steal an authenticated session'. The root is an OR node with three alternative branches: steal the session token, hijack a live session, or trick the user into authenticating for the attacker. The token branch is itself an OR over XSS exfiltration, a leaked log, and a stolen device. The session-fixation branch is an AND node — the attacker must both set a known session id AND get the victim to authenticate with it — which is why it is more expensive than the single-step token theft."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "goal", label: "GOAL: steal session", sub: "OR (cheapest wins)" },
    { id: "token", label: "Steal token", sub: "OR branch" },
    { id: "hijack", label: "Hijack live session", sub: "OR branch" },
    { id: "fixation", label: "Session fixation", sub: "AND branch" },
    { id: "xss", label: "XSS exfiltrate", sub: "leaf · cheap" },
    { id: "log", label: "Token in logs", sub: "leaf · cheap" },
    { id: "setid", label: "Set known id", sub: "AND leaf 1" },
    { id: "victimauth", label: "Victim logs in", sub: "AND leaf 2" },
  ]}
  edges={[
    { from: "goal", to: "token", label: "or" },
    { from: "goal", to: "hijack", label: "or" },
    { from: "goal", to: "fixation", label: "or" },
    { from: "token", to: "xss" },
    { from: "token", to: "log" },
    { from: "fixation", to: "setid", label: "and" },
    { from: "fixation", to: "victimauth", label: "and" },
  ]}
  caption="Goal at the root, OR alternatives below, AND conjunction inside the fixation branch. The attacker takes the minimum-cost OR path — here a cheap token-theft leaf — so the cheapest feasible leaf is what you defend first. Forcing every branch to also require a device-bound, replay-resistant credential turns those ORs into ANDs and raises the price of the whole goal."
/>

## A worked tree: "steal an authenticated session"

Walk the tree above as an attacker would, bottom-up. The root goal is *obtain a valid authenticated session for a victim*. It's an OR — three independent ways to win:

1. **Steal the session token.** Itself an OR: exfiltrate it via an XSS bug (token in `localStorage`, readable by any script), scrape it from a log or referrer header where it leaked, or pull it off a compromised device. Each leaf is cheap-to-moderate and *single-step*. This branch is usually the minimum, which is why it's usually how real session theft happens.
2. **Hijack a live session.** Ride an already-authenticated connection — network-position attacks, or a predictable session id you can guess. Moderate cost, often needs a privileged network position, so frequently more expensive than branch 1.
3. **Session fixation.** An **AND** node: the attacker must *both* plant a session id they know *and* get the victim to authenticate under it. Two required steps means summed cost and the harder child (getting the victim to log in on your seeded session) gates feasibility — strictly more expensive than the one-step token theft.

Now read the structure: the attacker's effective cost is the **minimum over the OR root**, and that minimum is almost always the cheapest leaf in branch 1. So your first fix targets that leaf — kill the XSS surface and stop putting tokens where script can read them. But notice what a single control does to the whole tree: bind the session to the device (a credential that can't be replayed off-device) and you have raised the cost of *every* branch at once, because stolen-token-replay, hijack, and fixation all assumed a bearer token that works anywhere. That is the power of reading the tree top-down: one control placed at the root assumption beats three patches at the leaves.

<Inset kind="why" lang="en">
Why bother costing leaves at all, instead of just listing attacks? Because security is a budget problem, not a completeness problem. A list of twenty attacks tells you nothing about *order*; a tree with even rough cost labels tells you the attacker's actual path is the single cheapest feasible leaf — and that your scarce remediation time belongs there, not on the scary-sounding-but-expensive attack nobody will bother with. Costing is what turns a threat catalog into a prioritized work queue. Get the labels roughly right and the ranking is robust even when the exact numbers are guesses.
</Inset>

## Abuse cases: the evil user story, written beside the real one

Attack trees are an analysis tool. **Abuse cases** (and their close sibling, **misuse cases**) are a *requirements* tool — they get the attacker into the conversation while you're still writing the feature, not after it ships.

The idea is disarmingly simple. For every user story you write, write the inverted story from the attacker's point of view. The user story says *"As a customer, I want to reset my password by email so I can regain access."* The abuse case says *"As an attacker, I want to trigger password resets for arbitrary accounts and harvest the reset tokens, so I can take over accounts."* Now the requirement is no longer just "build password reset" — it's "build password reset *such that* an attacker can't enumerate accounts, can't brute-force the token, and can't flood a victim's inbox." The control (rate limiting, single-use high-entropy tokens, neutral responses that don't reveal whether an account exists) becomes an acceptance criterion, not an afterthought.

A **misuse case** is the formal UML cousin: on the same diagram as your use cases, you draw the misuser and their misuse cases, linked to the use cases they *threaten* and the security use cases that *mitigate* them. The distinction is mostly notation — both encode the same move: name the hostile actor and their goal as a first-class artifact alongside the friendly one. The payoff is cultural as much as technical. A user story with a paired abuse case can't quietly ship without someone deciding what to do about the abuse — the evil twin is right there in the backlog, demanding an answer.

## When to reach for trees vs STRIDE vs abuse cases

These are complements, not competitors, and a senior picks by the question being asked:

- Reach for **STRIDE** when you need **coverage** — you have a DFD and you want to be sure no element's spoofing/tampering/etc. went unconsidered. It's a breadth-first sweep of the system's surface.
- Reach for an **attack tree** when you need to reason about a **specific goal and the cheapest path to it** — "how does someone actually get admin?" — and especially when you want to *prioritize* defenses by cost or prove a control raises the attacker's price across many paths.
- Reach for **abuse / misuse cases** when you're at the **requirements stage** and want the threat baked into acceptance criteria, so the control ships with the feature instead of being retrofitted after the pentest.

In practice they chain: STRIDE finds the threats element-by-element, an abuse case turns the scary ones into requirements, and an attack tree is what you draw when one goal (session theft, payment bypass) is important enough to deserve a costed, path-level analysis.

<TradeoffMatrix
  id="04-attack-trees-tradeoff-1"
  lessonSlug="04-attack-trees-and-abuse-cases"
  lang="en"
  prompt="Your team has a finished data-flow diagram and wants to be confident no element's possible threats (spoofing, tampering, info disclosure, etc.) were overlooked before the next release. Which technique fits this question best?"
  options={[
    { name: "STRIDE walked against the DFD, element by element", summary: "Breadth-first coverage.", correct: true, justification: "The question is about coverage across every element of a known DFD — exactly what STRIDE is for. It is element-centric and breadth-first: stand at each box and arrow and enumerate spoofing/tampering/repudiation/info-disclosure/DoS/elevation. That systematic sweep is the right tool when the goal is 'did we miss any element's threat category', not 'what is the cheapest path to one goal'." },
    { name: "An attack tree rooted at one attacker goal", summary: "Goal-centric path analysis.", justification: "Attack trees are goal-centric: they shine when you have one specific objective (steal a session, bypass payment) and want the cheapest feasible path and cost-based prioritization. They are the wrong tool for breadth-first 'did we cover every element' coverage — a single tree analyzes one goal, not the whole surface." },
    { name: "Abuse cases for every user story", summary: "Requirements-stage tool.", justification: "Abuse cases turn threats into acceptance criteria at the requirements stage, before code. They are excellent for baking intent into a feature, but they don't give you a systematic per-element sweep of a finished DFD — that's a coverage question STRIDE answers better." },
    { name: "A penetration test before release", summary: "Find-it-empirically.", justification: "A pentest finds real exploitable bugs empirically, but it's an after-the-build validation, not a design-time review of whether every DFD element's threats were considered. It also won't systematically enumerate categories the way a STRIDE sweep does, and it's far later and more expensive than a diagram review." },
  ]}
/>

<Quiz
  id="04-attack-trees-quiz-1"
  lessonSlug="04-attack-trees-and-abuse-cases"
  lang="en"
  question="An interior node in an attack tree is an OR node with three children whose costs are cheap, moderate, and expensive. What is the cost the attacker effectively pays to achieve that node, and why?"
  choices={[
    { label: "Cheap — an OR node is satisfied by any one child, so the attacker takes the minimum-cost branch", correct: true },
    { label: "The sum (cheap + moderate + expensive) — all three must be done for an OR node", misconception: "Summing all children is how you cost an AND node, where every child is required. An OR node needs only one child, so its cost is the minimum, not the sum." },
    { label: "Expensive — security cost is always governed by the hardest child", misconception: "The hardest child gates an AND node's feasibility, not an OR node. For an OR the attacker is free to pick the easiest branch, so the relevant cost is the cheapest, not the hardest." },
    { label: "Moderate — you take the median of the children's costs", misconception: "There is no median rule. An OR node's cost is the minimum over its children, because the attacker rationally chooses the single cheapest feasible route." },
  ]}
/>

<Quiz
  id="04-attack-trees-quiz-2"
  lessonSlug="04-attack-trees-and-abuse-cases"
  lang="en"
  question="You're writing the user story 'As a customer, I want to reset my password by email.' What does pairing it with an abuse case actually buy you?"
  choices={[
    { label: "It names the attacker's goal beside the feature, turning controls (rate limiting, single-use tokens, neutral responses) into acceptance criteria the feature can't ship without", correct: true },
    { label: "It replaces the need for any authorization checks at runtime", misconception: "An abuse case is a requirements artifact; it informs what to build but doesn't enforce anything at runtime. You still need the actual server-side controls — it just makes sure they get specified." },
    { label: "It guarantees the feature is exploit-free once shipped", misconception: "Nothing guarantees exploit-free. An abuse case raises the right questions at design time, but it's not a proof of security and doesn't replace testing or review." },
    { label: "It is just documentation with no effect on what gets built", misconception: "The whole point is the opposite: by sitting in the backlog beside the user story, the abuse case forces a decision about the threat, so controls become acceptance criteria rather than afterthoughts." },
  ]}
/>

<DragOrder
  id="04-attack-trees-drag-1"
  lessonSlug="04-attack-trees-and-abuse-cases"
  lang="en"
  prompt="Order the steps of building an attack tree, from starting point to the result you act on:"
  items={[
    { id: "a", label: "Write the attacker's goal as the root node" },
    { id: "b", label: "Decompose it into OR/AND sub-goals" },
    { id: "c", label: "Keep decomposing until leaves are concrete, atomic attacks" },
    { id: "d", label: "Annotate each leaf with cost / feasibility" },
    { id: "e", label: "Read the minimum-cost feasible path and fix it first" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>An attack tree roots one attacker goal, decomposes it into OR (any child wins — cost is the minimum) and AND (all children required — cost is the sum) sub-goals, and costs the leaves so the cheapest feasible path is what you fix first. Defenders turn ORs into ANDs. Abuse cases write that attacker intent as the evil user story beside the real one, making controls into acceptance criteria. Trees are goal-centric where STRIDE is element-centric.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-attack-trees-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between an OR node and an AND node in an attack tree, how each is costed, and what 'defenders turn ORs into ANDs' means.", a: "An OR node is satisfied if any one of its children is achieved — the children are alternative routes to the same sub-goal — so its cost is the minimum over its children, because a rational attacker takes the single cheapest feasible branch. An AND node requires all of its children together, so its cost is the sum of its children and its feasibility is gated by the hardest child; adding one more required step raises the whole subtree's price. 'Defenders turn ORs into ANDs' is the strategic reflex that follows: if a goal currently has several cheap OR branches, you have several independent ways to lose, and the attacker pays only the minimum. By introducing a control that every path must also satisfy — for example a device-bound credential that can't be replayed — you convert those alternatives into a conjunction, so the attacker must now defeat your control on top of whichever branch they chose, and the cost of the whole goal jumps. Costing the leaves (even coarsely as cheap/moderate/expensive) is what makes the cheapest feasible path visible, and that path is what you remediate first." },
    { q: "What is an abuse case, how does it differ from a misuse case, and when do you reach for abuse cases versus an attack tree versus STRIDE?", a: "An abuse case is the inverted, attacker's-point-of-view version of a user story, written alongside it at the requirements stage: where the user story says 'as a customer I want to reset my password by email', the abuse case says 'as an attacker I want to trigger resets for arbitrary accounts and harvest the tokens' — which turns the security controls (rate limiting, single-use high-entropy tokens, neutral account-existence responses) into acceptance criteria rather than afterthoughts. A misuse case is the formal UML cousin: on the same diagram as the use cases you draw the misuser and their misuse cases, linked to the use cases they threaten and the security use cases that mitigate them; the difference from an abuse case is mostly notation — both name the hostile actor and goal as a first-class artifact. You reach for STRIDE when you want breadth-first coverage across every element of a DFD; for an attack tree when you have one specific goal and want the cheapest path and cost-based prioritization; and for abuse/misuse cases when you're at the requirements stage and want the threat baked into acceptance criteria so the control ships with the feature. They chain: STRIDE finds the threats, an abuse case turns the important ones into requirements, and an attack tree gives a costed path-level analysis of a goal worth that depth." },
  ]}
/>

<Recap lang="en">
An attack tree is the goal-centric complement to element-centric STRIDE: you root the attacker's objective ("steal an authenticated session"), decompose it into sub-goals, and keep going until the leaves are concrete, costable attacks. Interior nodes are OR (any one child wins — cost is the minimum, so attackers shop the cheapest path) or AND (all children required — cost is the sum, gated by the hardest child). Annotating leaves with even rough cost makes the cheapest feasible path pop out — that's what you fix first — and the strategic move is to add a control that every branch must also satisfy, turning ORs into ANDs and raising the price of the whole goal. Abuse cases bring the attacker into the requirements stage: write the evil user story beside the real one so controls become acceptance criteria, with misuse cases as the formal UML notation of the same move. Pick by question: STRIDE for breadth-first coverage of a DFD, an attack tree for the cheapest path to one goal, abuse cases to bake the threat into the spec. Next time you scope a sensitive feature, ask not "which elements could be attacked" but "what's the attacker's goal, and what's the cheapest leaf that gets them there?"
</Recap>
