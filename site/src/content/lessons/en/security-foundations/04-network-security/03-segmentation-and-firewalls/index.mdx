---
concepts:
- network-segmentation
- firewall
- microsegmentation
- blast-radius
- defense-in-depth
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-segmentation-and-firewalls
sources:
- https://csrc.nist.gov/pubs/sp/800/207/final
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'One compromised web box should not reach your database, your secrets store, and your CI runner. Segmentation, firewalls, security groups, and microsegmentation are how you bound that blast radius — and where flat networks quietly fail.'
title: 'Segmentation and firewalls'
track: security-foundations
unit: 04-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
An attacker pops one of your web servers through a vulnerable image-upload endpoint. On a flat network that single foothold is the whole game: from that box they can open a TCP connection to the database on 5432, to the internal admin panel on 8080, to the secrets sidecar, to the CI runner that holds a deploy token, and to every other service in the VPC — because nothing between them ever said no. The web box never *needed* to talk to the CI runner, but the network let it, so the attacker inherits everything the web box can reach. The breach was one host; the loss was the data center. Segmentation is the discipline that makes those two numbers different.
</Hook>

By the end of this lesson you'll be able to bound the blast radius of a compromised host by drawing zone boundaries and enforcing them with firewalls, security groups, and microsegmentation.

<Crux>Segmentation cuts the network into zones so a breached host cannot reach everything; firewalls and microsegmentation hold the borders.</Crux>

<Explanation>

## Blast radius: the number segmentation actually moves

Start from the assumption a senior never drops: *something will be compromised.* A dependency CVE, a leaked credential, an SSRF that pivots inward — eventually one host is the attacker's. The only question left is **how far that foothold reaches.** That reachable surface is the **blast radius**, and it is the metric segmentation exists to shrink.

A **flat network** — one big subnet, every host able to dial every other host on any port — maximizes blast radius by construction. Compromise of any single node equals reachability to all of them. This is exactly the structure that turns a contained web-tier bug into a lateral-movement spree: the 2013 Target breach famously began with credentials stolen from an HVAC vendor and reached the point-of-sale network because the segments that should have separated vendor access from card-processing systems weren't enforced. The lesson generalizes: lateral movement is only possible across paths the network permits, so the defense is to permit fewer paths.

Segmentation slices that flat space into **zones** with controlled crossings. A classic three-tier cut is web / app / data: the web tier accepts traffic from the internet on 443, the app tier accepts traffic *only from the web tier*, and the data tier accepts traffic *only from the app tier*. Now a compromised web box can reach the app tier on one port — not the database, not the secrets store, not CI. The attacker has to find a *second* vulnerability at each border to keep moving, and every border is a place you get to detect and stop them.

<FlowDiagram
  label="Flat network versus segmented network: on a flat network a compromised web host reaches the database, secrets store, and CI runner directly; segmenting into web, app, and data zones forces traffic through firewall borders so the web host can only reach the app tier on one port, leaving the database, secrets, and CI unreachable from a single foothold."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "web", label: "Web host", sub: "compromised" },
    { id: "app", label: "App tier", sub: "from web only" },
    { id: "data", label: "Database", sub: "from app only" },
    { id: "loot", label: "Secrets + CI", sub: "unreachable" },
  ]}
  edges={[
    { from: "web", to: "app", label: "443/8080" },
    { from: "app", to: "data", label: "5432" },
    { from: "web", to: "data", label: "blocked" },
    { from: "web", to: "loot", label: "blocked" },
  ]}
  caption="Segmentation in one picture: the only path out of a compromised web host is a single allowed port into the app tier. The database, secrets store, and CI runner are not reachable from the web zone, so one foothold is not the whole network."
/>

## The enforcement layers: firewalls, security groups, microsegmentation

A zone boundary is only as real as the thing enforcing it. Three layers do the work, at three different granularities.

A **network firewall** sits at a subnet or perimeter boundary and filters traffic by 5-tuple (source IP, destination IP, source port, destination port, protocol). The non-negotiable posture is **default-deny**: the ruleset starts by dropping everything, and you explicitly allow only the flows the architecture requires. A default-*allow* firewall with a few block rules is a sieve — it fails open the moment a new service appears that nobody wrote a block rule for. Default-deny fails *closed*: a flow nobody allowed simply doesn't happen.

**Security groups** (the cloud's native primitive — AWS, GCP, Azure all have an equivalent) push that filter down to the *instance* and make it **identity-based** instead of IP-based. Instead of "allow 10.0.2.0/24 → 5432," you write "allow the `app-sg` source group → the `db-sg` on 5432." The rule references *roles*, not addresses, so it survives autoscaling and IP churn, and it is **stateful**: you allow the inbound request and the response is permitted automatically. Security groups are deny-by-default by nature — an instance with no inbound rule accepts nothing.

**Microsegmentation** takes it to the limit: policy per *workload*, often per service or pod, enforced regardless of where it runs. In Kubernetes this is a `NetworkPolicy` (or a service-mesh authorization policy) that says pods labeled `app=checkout` may receive traffic only from pods labeled `app=gateway` on 8080 — and the default in a namespace with any policy is *deny all else*. This is the structural core of **zero trust** (NIST SP 800-207): the network location of a request grants it nothing; every flow is authorized on its own identity. Microsegmentation is what shrinks east-west blast radius from "the whole namespace" to "the one neighbor this service legitimately talks to."

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layer</th>
        <th class="border border-border px-3 py-2 text-left">Granularity</th>
        <th class="border border-border px-3 py-2 text-left">Selector</th>
        <th class="border border-border px-3 py-2 text-left">Bounds which blast radius</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Network firewall</td>
        <td class="border border-border px-3 py-2">Subnet / perimeter</td>
        <td class="border border-border px-3 py-2">5-tuple (IP, port, proto)</td>
        <td class="border border-border px-3 py-2">North-south, zone-to-zone</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Security group</td>
        <td class="border border-border px-3 py-2">Instance</td>
        <td class="border border-border px-3 py-2">Source group / identity</td>
        <td class="border border-border px-3 py-2">Tier-to-tier, survives IP churn</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Microsegmentation</td>
        <td class="border border-border px-3 py-2">Workload / pod</td>
        <td class="border border-border px-3 py-2">Label / service identity</td>
        <td class="border border-border px-3 py-2">East-west, pod-to-pod</td>
      </tr>
    </tbody>
  </table>
</div>

## Defense in depth, and where segmentation stops helping

Segmentation is one layer of **defense in depth**, not the whole defense. It controls *who can reach whom*; it does not validate *what they send once allowed*. If the app tier is permitted to query the database and the app has a SQL injection flaw, the firewall waves the malicious query straight through — the 5-tuple is on the allowlist. So segmentation must be paired with controls at the next layer in (parameterized queries, authn/authz on the service, input validation) and the layer above (WAF, TLS). The point of depth is that no single control failing should be game over.

Two honest costs keep this from being free. **Operational drag:** fine-grained policy is a lot of rules, and rules drift — a stale allow nobody removed becomes the exact path the attacker uses. The discipline is to treat policy as code, review it, and audit for unused allows. **The failure mode of doing it badly:** over-segment without good observability and you get mystery outages where a legitimate flow is silently dropped, the team can't tell a misconfiguration from an attack, and — worst of all — under pressure someone "fixes" it by adding an `allow all` rule that quietly re-flattens the network you spent a quarter segmenting.

<Inset kind="why" lang="en">
Why prefer identity-based security groups over IP-based firewall rules in the cloud? Because IPs are ephemeral there. Autoscaling, spot reclamation, and rolling deploys recycle instances constantly, so an IP-pinned rule (`allow 10.0.2.37 → 5432`) is either wrong within the hour or so broad (`allow 10.0.2.0/24`) that it re-creates a flat zone inside the "segmented" one. Referencing a *source security group* binds the rule to a role, not an address, so it stays correct as instances come and go — and it reads as intent ("app may reach db") instead of a CIDR puzzle a future engineer has to reverse-engineer.
</Inset>

## Reading a policy like a senior

When you review a segmentation design, you're really asking two questions. First, *is it default-deny?* If the baseline is allow-with-exceptions, the design is already lost — find the implicit any-any rule and you've found the whole network's true reachability. Second, *what does a single compromise reach?* Pick the most exposed node (the internet-facing tier), assume it's owned, and trace every flow it's allowed to make. That set is your blast radius. If it includes the database, the secrets store, or CI, you have lateral-movement paths to cut.

<TradeoffMatrix
  id="03-segmentation-tradeoff-1"
  lessonSlug="03-segmentation-and-firewalls"
  lang="en"
  prompt="Your three-tier app runs in a cloud VPC with autoscaling on every tier. You need the app tier to reach the database on 5432 and nothing else to. Pick the rule that bounds blast radius best."
  options={[
    { name: "Security group: allow source `app-sg` → `db-sg` on 5432; db-sg has no other inbound rule", summary: "Identity-based, default-deny.", correct: true, justification: "This is the right primitive for the cloud. It references roles, not addresses, so it survives autoscaling and IP churn; it's stateful, so the response is allowed automatically; and because security groups are deny-by-default, the database accepts nothing except 5432 from the app tier. A compromised web box can't reach the database at all — it isn't in app-sg." },
    { name: "Network ACL: allow 10.0.0.0/16 → 5432", summary: "Whole-VPC CIDR allow.", justification: "This allows every host in the VPC — web, CI, bastion, anything that lands in 10.0.0.0/16 — to reach the database on 5432. It re-creates a flat data tier inside a nominally segmented network. A compromised web box reaches the DB directly. Far too broad." },
    { name: "Firewall: allow 10.0.2.37 → 5432, the current app instance IP", summary: "IP-pinned to one instance.", justification: "It's correctly scoped today and wrong within the hour: autoscaling recycles instances, so the IP changes and either the app loses DB access (outage) or someone widens it to a CIDR (back to the broad-allow problem). IP-pinning fights the cloud's ephemerality instead of using identity." },
    { name: "Leave the tiers flat but enable VPC flow logs so you can see lateral movement", summary: "Observe, don't prevent.", justification: "Flow logs are valuable for detection, but they record movement after it happens — they don't stop it. Visibility is not a control. On a flat network the blast radius is still the whole VPC; you'd be watching the breach spread, not bounding it." },
  ]}
/>

<Quiz
  id="03-segmentation-quiz-1"
  lessonSlug="03-segmentation-and-firewalls"
  lang="en"
  question="A firewall is configured default-allow with a handful of explicit block rules. Why is this the wrong posture for a zone boundary?"
  choices={[
    { label: "It fails open: any flow nobody wrote a block rule for is permitted, so a new service or port is reachable by default", correct: true },
    { label: "Default-allow firewalls are slower because they evaluate every block rule first", misconception: "Performance isn't the issue. The problem is the security posture: default-allow permits anything not explicitly blocked, so coverage depends on remembering every bad flow — which you never do." },
    { label: "Block rules can't reference ports, only IP addresses", misconception: "Firewalls filter on the full 5-tuple including ports. The posture problem is unrelated to what fields a rule can match." },
    { label: "It's fine as long as you also have a security group", misconception: "A second layer helps, but a default-allow boundary is still a sieve. Default-deny is the baseline you want at every enforcement layer, not something another layer excuses." },
  ]}
/>

<Quiz
  id="03-segmentation-quiz-2"
  lessonSlug="03-segmentation-and-firewalls"
  lang="en"
  question="Your Kubernetes namespace has a NetworkPolicy allowing `app=gateway` → `app=checkout` on 8080. The checkout pod has a remote-code-execution bug and is compromised. What does microsegmentation buy you here?"
  choices={[
    { label: "It bounds east-west reach: the compromised checkout pod can't freely dial other pods, since the namespace defaults to deny and only the gateway→checkout flow is allowed", correct: true },
    { label: "It prevents the RCE from being exploited in the first place", misconception: "Network policy controls who can reach whom, not whether the reachable code is buggy. The RCE still fires; segmentation limits where the attacker goes next — that's why it's defense in depth, not the only defense." },
    { label: "It encrypts traffic between the gateway and checkout pods", misconception: "That's mTLS/a service mesh's transport job, not what a NetworkPolicy does. NetworkPolicy authorizes flows; it doesn't provide confidentiality." },
    { label: "Nothing — once one pod is compromised the whole namespace is reachable", misconception: "That's the flat-network outcome microsegmentation specifically prevents. With a default-deny policy in place, the compromised pod can only use flows the policy allows, not reach every pod." },
  ]}
/>

<DragOrder
  id="03-segmentation-drag-1"
  lessonSlug="03-segmentation-and-firewalls"
  lang="en"
  prompt="Order these enforcement scopes from broadest (coarsest segmentation) to narrowest (finest), as you'd layer them in defense in depth:"
  items={[
    { id: "a", label: "Perimeter firewall — north-south, subnet/zone boundary, 5-tuple" },
    { id: "b", label: "Security group — tier-to-tier, instance-level, identity-based" },
    { id: "c", label: "Microsegmentation / NetworkPolicy — east-west, per-workload, label-based" },
  ]}
  correctOrder={["a", "b", "c"]}
/>

</Explanation>

<KeyTakeaway>Assume a host will be compromised; segmentation decides how far it reaches. Cut the network into default-deny zones — firewalls at the perimeter, identity-based security groups tier-to-tier, microsegmentation pod-to-pod — so one foothold isn't the whole network. It's one layer of defense in depth, not a substitute for app-level controls.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-segmentation-retrieval"
  lang="en"
  questions={[
    { q: "Explain blast radius, why a flat network maximizes it, and how a three-tier segmentation shrinks it.", a: "Blast radius is the set of systems an attacker can reach once they've compromised a single host — the assumption a senior starts from is that something eventually will be compromised, so the design question is how far that foothold reaches. A flat network (one subnet, every host able to dial every other host on any port) maximizes blast radius by construction: compromise of any one node equals reachability to all of them, which is exactly what enables lateral movement from a contained web-tier bug to the whole estate. Segmenting into web/app/data zones with controlled crossings shrinks it: the web tier accepts internet traffic on 443, the app tier accepts traffic only from web, and the data tier accepts traffic only from app. Now a compromised web box can reach the app tier on one port — not the database, not the secrets store, not CI — so the attacker needs a fresh vulnerability at each border to keep moving, and every border is a place to detect and stop them." },
    { q: "Compare network firewalls, security groups, and microsegmentation, and explain why each is default-deny and why identity-based rules beat IP-based ones in the cloud.", a: "All three enforce zone boundaries but at different granularities. A network firewall filters at a subnet/perimeter boundary on the 5-tuple (source/dest IP, source/dest port, protocol) and governs north-south, zone-to-zone traffic. A security group pushes the filter to the instance and makes it identity-based (allow source `app-sg` → `db-sg` on 5432) and stateful, governing tier-to-tier flows; because an instance with no inbound rule accepts nothing, it's deny-by-default by nature. Microsegmentation goes to per-workload policy (a Kubernetes NetworkPolicy or mesh authz policy keyed on pod labels), bounding east-west pod-to-pod reach, with deny-all the default once any policy exists in the namespace — the structural core of zero trust (NIST SP 800-207), where network location grants no trust and every flow is authorized on its own identity. Default-deny everywhere matters because it fails closed: a flow nobody explicitly allowed simply doesn't happen, whereas default-allow fails open the moment a new service appears with no block rule. Identity-based rules beat IP-based ones in the cloud because IPs are ephemeral — autoscaling and rolling deploys recycle instances, so an IP-pinned rule is wrong within the hour or so broad it re-flattens the zone; referencing a source group binds the rule to a role that survives IP churn and reads as intent." },
  ]}
/>

<Recap lang="en">
Segmentation starts from the senior assumption that some host will be compromised, and its job is to bound the blast radius — the set of systems that foothold can reach. A flat network maximizes blast radius because every host can dial every other; cutting the network into web/app/data zones with controlled crossings means a compromised web box reaches one port into the app tier, not the database, secrets store, or CI. Boundaries are enforced at three granularities, all default-deny: network firewalls filter north-south by 5-tuple at the perimeter, security groups push an identity-based, stateful filter to the instance (the right primitive in a cloud where IPs churn), and microsegmentation applies per-workload policy pod-to-pod — the structural core of zero trust, where location grants no trust. Segmentation is one layer of defense in depth: it controls who can reach whom, not what they send once allowed, so it must be paired with app-level controls, and its real costs are policy drift and outages from over-segmenting without observability. When you read a design, ask two things: is it default-deny, and what does a single compromise actually reach?
</Recap>
