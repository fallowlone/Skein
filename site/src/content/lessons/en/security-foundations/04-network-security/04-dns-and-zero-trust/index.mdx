---
concepts:
- dns-security
- dnssec
- zero-trust
- beyondcorp
- identity-aware-proxy
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-dns-and-zero-trust
sources:
- https://csrc.nist.gov/pubs/sp/800/207/final
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'DNS resolves trust before TLS ever proves it, which makes the lookup an attack surface most teams forget. This lesson covers cache poisoning, DNSSEC and DoH, and why the industry traded the network perimeter for per-request identity.'
title: 'DNS security and zero-trust networking'
track: security-foundations
unit: 04-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your service makes one outbound call: `https://payments.internal/charge`. You pinned the TLS cert, you rotated the key, you enabled HSTS. You feel safe. But before any of that runs, your resolver asks "what IP is `payments.internal`?" over UDP port 53 — a plaintext request, no authentication, no integrity check, that any on-path device or a poisoned cache can answer with an attacker's address. TLS then dutifully establishes an encrypted, authenticated channel to *that* host. The crypto worked perfectly; it just protected a conversation with the wrong server. DNS is the trust decision that happens before your security stack wakes up, and for most of the internet's history it was unauthenticated by design.
</Hook>

By the end of this lesson you'll know why DNS is an attack surface that sits underneath TLS, what DNSSEC and DoH actually protect (and don't), and why the industry abandoned the network perimeter for a zero-trust model built on per-request identity.

<Crux>DNS picks who you reach before TLS proves who they are; DNSSEC/DoH harden the lookup, zero-trust trades the perimeter for identity.</Crux>

<Explanation>

## DNS is a trust decision, not a lookup

Treat DNS as plumbing and you'll defend the wrong layer. The name-to-address resolution is the moment your system *chooses who to trust*, and classic DNS makes that choice over an unauthenticated, plaintext UDP exchange. Two failure modes dominate in production.

**Cache poisoning.** A resolver that caches answers can be tricked into caching a forged one. The 2008 Kaminsky attack made this brutal: by spraying forged responses for many random subdomains, an attacker only needs to win one race against the legitimate reply, and because the early DNS protocol had just a 16-bit transaction ID (65,536 values) and a known source port, that race was winnable in seconds on a fast link. The poisoned record then serves every downstream client until the TTL expires. The patch — randomizing the source port to add ~16 more bits of entropy — turned a seconds-long attack into a statistically impractical one, but it was a mitigation, not a fix: nothing in the protocol *authenticated* the answer.

**On-path and the resolver's privilege.** Anyone between you and your resolver — a malicious Wi-Fi AP, a compromised router, a hostile ISP — can read every name you look up (a privacy leak) and rewrite the answer (an integrity attack). Your resolver is one of the most privileged hosts in your stack: it silently decides the destination of every connection you make.

<FlowDiagram
  label="The DNS poisoning attack path: an application asks its resolver to look up an internal hostname over plaintext UDP port 53, an on-path attacker or a poisoned cache answers first with an attacker-controlled IP, TLS then builds a perfectly valid encrypted channel to the wrong server, and the fix is to authenticate the answer itself with DNSSEC and encrypt the query with DoH or DoT."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "app", label: "App resolves", sub: "payments.internal" },
    { id: "udp", label: "Plaintext UDP/53", sub: "no auth" },
    { id: "forge", label: "Attacker answers", sub: "spoofed IP" },
    { id: "tls", label: "TLS to wrong host", sub: "crypto 'works'" },
    { id: "fix", label: "DNSSEC + DoH", sub: "sign + encrypt" },
  ]}
  edges={[
    { from: "app", to: "udp" },
    { from: "udp", to: "forge", label: "race / on-path" },
    { from: "forge", to: "tls", label: "wrong dest" },
    { from: "udp", to: "fix", label: "the fix" },
  ]}
  caption="Classic DNS in one path: the resolver trusts whichever answer arrives first over an unauthenticated channel. DNSSEC authenticates the record; DoH/DoT encrypt the query so an on-path party can neither read nor rewrite it."
/>

## DNSSEC and DoH protect different things

These two get conflated constantly, and conflating them produces a false sense of safety. They solve orthogonal problems.

**DNSSEC** signs DNS records with public-key cryptography, building a chain of trust from the root zone down to the answer. It guarantees **integrity and authenticity**: the record you got is the record the zone owner published, unmodified. It does *not* encrypt anything — a DNSSEC query and its signed reply are still visible on the wire — and it solves cache poisoning at the root, since a forged answer fails signature validation. Its cost is real: deployment is operationally heavy (key rollovers, chain-of-trust breakage takes a zone offline), and adoption has stalled for years well under half of zones, so you usually can't rely on it end-to-end.

**DoH / DoT** (DNS over HTTPS / over TLS) wrap the query in an encrypted transport. They guarantee **confidentiality and transport integrity** against an on-path observer: the coffee-shop attacker can no longer see or rewrite your lookups. They do *not* authenticate the record itself — your resolver could still return a poisoned or censored answer; you've simply moved your trust to whoever runs the resolver. DoH's tunnelling DNS inside ordinary HTTPS (port 443) is also why it's contentious: it bypasses the network-level DNS monitoring that enterprises and parental filters rely on.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Mechanism</th>
        <th class="border border-border px-3 py-2 text-left">Protects</th>
        <th class="border border-border px-3 py-2 text-left">Does NOT protect</th>
        <th class="border border-border px-3 py-2 text-left">Real cost / catch</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">DNSSEC</td>
        <td class="border border-border px-3 py-2">Integrity + authenticity of the record (kills cache poisoning)</td>
        <td class="border border-border px-3 py-2">Confidentiality — query and answer are still in cleartext</td>
        <td class="border border-border px-3 py-2">Heavy ops (key rollover, chain breakage); &lt;50% zone adoption</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">DoH / DoT</td>
        <td class="border border-border px-3 py-2">Confidentiality vs on-path; transport integrity</td>
        <td class="border border-border px-3 py-2">Authenticity of the record — resolver can still lie</td>
        <td class="border border-border px-3 py-2">Trust shifts to the resolver operator; DoH bypasses network filtering</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Source-port randomization</td>
        <td class="border border-border px-3 py-2">Raises poisoning difficulty (~+16 bits entropy)</td>
        <td class="border border-border px-3 py-2">Anything an authenticated channel would — it's a probability tax</td>
        <td class="border border-border px-3 py-2">Mitigation, not a fix; assumes a non-on-path attacker</td>
      </tr>
    </tbody>
  </table>
</div>

The senior reading: **DNSSEC + an encrypted transport is the complete answer** — sign the record so it can't be forged, encrypt the query so it can't be read or rewritten in flight. Either alone leaves a real gap. And neither answers the deeper question this lesson is really about: even with a perfectly resolved, perfectly encrypted connection to the right host, *should this caller be allowed to reach it at all?* That's where the perimeter model breaks.

## Why the perimeter died: zero-trust

The classic model was a castle: a hard network perimeter (firewall, VPN) around a soft, trusted interior. Get inside the VPN and you were "on the corporate network," which implicitly meant *trusted* — flat east-west access to internal services. That model has a single catastrophic failure mode: **once an attacker is inside, they own everything.** A phished laptop, a compromised contractor VPN credential, one exploited edge device, and the flat interior becomes a free-roaming lateral-movement playground. The 2020 SolarWinds intrusion is the canonical example — trusted internal position turned one supply-chain foothold into wide reach.

Zero-trust, codified in **NIST SP 800-207**, inverts the assumption: **never trust, always verify.** There is no trusted network. The location of a request — inside the VPN or on hotel Wi-Fi — confers *zero* implicit authorization. Every single request to a resource is authenticated and authorized on its own merits, evaluated against the *identity* of the user and the device, not the IP it came from. Trust is never granted by network position and is continuously re-evaluated, not handed out once at login.

<Inset kind="why" lang="en">
Why does "the network is the perimeter" stop working specifically now? Two structural shifts. First, the perimeter dissolved: SaaS, public-cloud workloads, and remote work mean your users and your services are no longer inside any single network you control — there's nothing left to draw a wall around. Second, the assumption that "inside = safe" was always a latent bug; it just used to be survivable. As breaches got bigger and lateral movement became the dominant post-compromise tactic, the cost of the implicit-trust assumption finally exceeded the cost of authenticating every request. Zero-trust isn't a new product you buy — it's removing an assumption you can no longer afford.
</Inset>

## What zero-trust looks like in practice: BeyondCorp and the IAP

Google's **BeyondCorp** is the reference implementation, born from the 2009 Aurora attack that walked straight through their perimeter. The keystone component is the **identity-aware proxy (IAP)**: instead of a VPN that drops you onto the network, every request to an internal app passes through a proxy that, *per request*, checks the authenticated user identity, the device's posture (is it managed, patched, healthy?), and an authorization policy for that specific resource — then allows or denies. There is no network to be "on." An engineer reaches an internal dashboard from a café exactly the way they reach it from the office: through the proxy, proving who they are and what device they're on, every time.

This collapses the attack surface in a way the VPN never could. A stolen session or a phished credential no longer yields the network; it yields, at most, the narrow set of resources that one identity-plus-device is authorized for, re-checked on every request — and device posture can revoke access the moment a machine falls out of compliance. The TLS 1.3 handshake (**RFC 8446**) that authenticates each of these connections is the cryptographic floor zero-trust stands on: mutual identity, forward secrecy, and a faster handshake make per-request verification cheap enough to do everywhere.

<TradeoffMatrix
  id="04-dns-and-zero-trust-tradeoff-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="en"
  prompt="A remote engineer needs access to an internal admin dashboard. Your VPN-based access has just had a credential phished. Pick the design that follows zero-trust."
  options={[
    { name: "Put the dashboard behind an identity-aware proxy that verifies user identity, device posture, and a per-resource policy on every request", summary: "Per-request identity.", correct: true, justification: "This is the BeyondCorp / NIST SP 800-207 model: no trusted network, every request authenticated and authorized on the identity of the user and device against a policy for that specific resource, re-evaluated continuously. A phished credential alone yields, at most, the narrow set that one identity-plus-healthy-device is authorized for — and posture checks can revoke it. Trust is never inferred from network location." },
    { name: "Keep the VPN but require it from a corporate IP allowlist, so only office or VPN-exit IPs can reach the dashboard", summary: "IP allowlist.", justification: "This doubles down on network location as the trust signal — the exact assumption zero-trust removes. The phished credential still works from the allowlisted exit, and anyone who lands inside the VPN gets flat east-west reach. You've narrowed the source IP, not authenticated the request." },
    { name: "Add a second VPN hop and a longer shared secret so the tunnel is harder to break into", summary: "Harder tunnel.", justification: "Hardening the tunnel doesn't change the model: once inside, the interior is still implicitly trusted and flat. A stolen credential authenticates into the tunnel and then roams. More VPN is more castle wall around the same soft, trusted interior." },
    { name: "Block the dashboard at the firewall for all external IPs and require engineers to be physically in the office", summary: "Office-only.", justification: "This is the perimeter model in its purest, most brittle form — it conflates physical location with authorization, breaks remote work entirely, and still grants flat trust to anyone (or anything) already inside the office network. It mistakes 'where' for 'who'." },
  ]}
/>

<Quiz
  id="04-dns-and-zero-trust-quiz-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="en"
  question="You enable DoH (DNS over HTTPS) for all clients and assume DNS spoofing is now solved. What's still exposed?"
  choices={[
    { label: "The record's authenticity — your resolver can still return a poisoned or censored answer; DoH only encrypts the query against on-path observers", correct: true },
    { label: "Nothing — encrypting the query end-to-end fully authenticates the answer", misconception: "Encryption of the transport is not authentication of the payload. DoH protects confidentiality and transport integrity against an on-path attacker, but the resolver you trust can still hand back a forged record. Only DNSSEC authenticates the record itself." },
    { label: "Confidentiality — DoH leaks every queried name to the network", misconception: "It's the opposite: DoH's whole point is to hide the queried names from on-path observers. The gap is authenticity, not confidentiality." },
    { label: "TLS — DoH downgrades your application's TLS to an older version", misconception: "DoH has no effect on your application-layer TLS. The two are independent; the unsolved problem is whether the DNS answer itself is genuine." },
  ]}
/>

<Quiz
  id="04-dns-and-zero-trust-quiz-2"
  lessonSlug="04-dns-and-zero-trust"
  lang="en"
  question="In a zero-trust architecture (NIST SP 800-207), an engineer is connected to the corporate VPN. What does that fact grant them?"
  choices={[
    { label: "Nothing implicit — every request is still authenticated and authorized per resource against their identity and device posture", correct: true },
    { label: "Trusted access to all internal services, because they're now inside the network", misconception: "That's the perimeter model zero-trust explicitly rejects. Network location confers zero authorization; being 'on the VPN' is not a trust signal under SP 800-207." },
    { label: "A signed DNSSEC record for every internal hostname", misconception: "Zero-trust is an authorization model, not a DNS mechanism. DNSSEC and zero-trust solve different layers; VPN membership has nothing to do with record signing." },
    { label: "Permanent access until logout, since they authenticated at connection time", misconception: "Zero-trust re-evaluates trust continuously and per request, not once at login. A single connection-time auth grant is exactly the static-trust assumption the model removes." },
  ]}
/>

<DragOrder
  id="04-dns-and-zero-trust-drag-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="en"
  prompt="Order what happens in a zero-trust (IAP) request to an internal app, from the client's call to the allow/deny decision:"
  items={[
    { id: "a", label: "Client requests the internal app; the request hits the identity-aware proxy (no VPN, no trusted network)" },
    { id: "b", label: "Proxy authenticates the user identity (SSO / mutual TLS)" },
    { id: "c", label: "Proxy checks device posture — is it managed, patched, healthy?" },
    { id: "d", label: "Proxy evaluates the authorization policy for this specific resource" },
    { id: "e", label: "Allow or deny — re-evaluated on the next request, not cached as 'trusted'" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>DNS chooses who you trust before TLS proves it. DNSSEC authenticates the record (kills poisoning); DoH/DoT encrypt the query (kills on-path snooping) — you need both. Zero-trust then removes network location as a trust signal entirely: every request is authenticated and authorized per resource on identity + device, via an identity-aware proxy, never inferred from being "inside."</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-dns-and-zero-trust-retrieval"
  lang="en"
  questions={[
    { q: "Why is DNS an attack surface that sits underneath TLS, and what do DNSSEC and DoH each protect?", a: "DNS resolution is the moment your system decides who to trust: it turns a name into an IP, and TLS only ever runs against whatever address resolution returned. Classic DNS does this over unauthenticated plaintext UDP/53, so an on-path attacker or a poisoned cache (the Kaminsky attack: forge answers and win a race against a 16-bit transaction ID with a known source port) can substitute an attacker-controlled IP. TLS then builds a flawless encrypted, authenticated channel — to the wrong host. The crypto worked; it protected a conversation with an attacker. DNSSEC and DoH fix orthogonal halves. DNSSEC signs records with a chain of trust from the root, guaranteeing integrity and authenticity (the record is what the zone owner published, so forged answers fail validation and cache poisoning dies) — but it does not encrypt, so queries are still visible, and adoption is under half of zones because key rollovers are operationally heavy. DoH/DoT wrap the query in TLS, guaranteeing confidentiality and transport integrity against an on-path observer — but they do not authenticate the record, so the resolver you now trust can still return a poisoned answer. The complete defense is both: sign the record (DNSSEC) and encrypt the query (DoH/DoT)." },
    { q: "What does zero-trust (NIST SP 800-207) replace, and how does an identity-aware proxy enforce it?", a: "Zero-trust replaces the perimeter ('castle and moat') model, where a hard network boundary surrounds a soft, implicitly trusted interior and being 'on the network' (e.g. on the VPN) means trusted, flat east-west access. That model's catastrophic failure is that one foothold — a phished credential, a compromised VPN account, an exploited edge device — yields the whole interior to lateral movement (SolarWinds, Aurora). NIST SP 800-207 inverts the assumption to 'never trust, always verify': there is no trusted network, location confers zero authorization, and every request to every resource is authenticated and authorized on its own merits against the identity of the user and the posture of the device, re-evaluated continuously rather than granted once at login. Google's BeyondCorp implements this with an identity-aware proxy (IAP): instead of a VPN that drops you onto the network, every request to an internal app passes through a proxy that per-request verifies who the user is, whether the device is managed/patched/healthy, and the authorization policy for that specific resource, then allows or denies. The payoff is a collapsed attack surface — a stolen credential yields only the narrow set that one identity-plus-healthy-device is authorized for, re-checked every request — riding on TLS 1.3 (RFC 8446) mutual authentication as its cryptographic floor." },
  ]}
/>

<Recap lang="en">
DNS is the trust decision that runs before your security stack wakes up: classic resolution happens over unauthenticated plaintext UDP/53, so cache poisoning (Kaminsky) or an on-path attacker can redirect you to a malicious host that your perfectly configured TLS then connects to flawlessly — protecting a conversation with the wrong server. DNSSEC and DoH fix orthogonal halves: DNSSEC signs records for integrity and authenticity (killing poisoning, but leaving queries in cleartext and stuck under 50% adoption), while DoH/DoT encrypt the query for confidentiality against on-path observers (but can't stop a lying resolver) — so the complete answer is both. Above resolution sits the deeper shift: the network perimeter died because SaaS, cloud, and remote work dissolved the wall, and "inside = trusted" turned every foothold into flat lateral movement. Zero-trust (NIST SP 800-207) removes network location as a trust signal entirely — never trust, always verify — and BeyondCorp's identity-aware proxy enforces it by authenticating user identity, device posture, and a per-resource policy on every single request, riding on TLS 1.3. Now when you see a system that grants access because a request came "from inside the VPN," your first question is: what's actually being authenticated here — the identity and device, or just the IP?
</Recap>
