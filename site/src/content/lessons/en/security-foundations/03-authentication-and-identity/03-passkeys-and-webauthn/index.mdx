---
concepts:
- passkey
- webauthn
- origin-bound-credential
- phishing-resistance
- user-verification
- credential-recovery
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-passkeys-and-webauthn
sources:
- https://www.w3.org/TR/webauthn-2/
- https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html
- https://fidoalliance.org/passkeys/
spiral: []
status: ready
summary: Passkeys are origin-bound public-key credentials; the browser signs over the page's true origin so a phished or relayed login is rejected. But WebAuthn only secures the login event — session and recovery stay your problem.
title: Passkeys and WebAuthn
track: security-foundations
unit: 03-authentication-and-identity
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A polished proxy at `accounts-secure-login.com` mirrors your real login page pixel for pixel. The victim arrives via a convincing email, the page calls `navigator.credentials.get`, the victim does Face ID, and the attacker captures a genuine, cryptographically valid signature — relayed straight from the victim's real authenticator. With a password they'd own the account. With a TOTP code they'd have a working 30-second token. With a passkey they have a signature that your server rejects on the first check, because the victim's browser stamped the *true* origin — `accounts-secure-login.com` — into the signed bytes, and your server expects `accounts.example.com`. The attacker did everything right and still got nothing. That single, browser-controlled field is the entire reason passkeys are called phishing-resistant — and understanding exactly why it works, and exactly what it does *not* cover, is the difference between deploying WebAuthn and getting breached anyway.
</Hook>

By the end of this lesson you'll know the mechanism that makes a passkey login unphishable, the tradeoffs you accept when you adopt it (recovery, synced-credential trust, device theft), and the failure modes that bite teams who think a passkey secures the whole account.

<Crux>Passkeys swap shared secrets for an origin-bound key pair, so the signed login resists phishing and replay — but only the login.</Crux>

<Explanation>

## What a passkey actually is

A **passkey** is a WebAuthn credential: an asymmetric key pair where the **private key never leaves the authenticator** (a phone's secure enclave, a laptop's TPM, or a roaming hardware key like a YubiKey) and the server stores only the matching **public key**. There is no shared secret. The server has nothing an attacker can phish, no password database to leak, no seed an attacker can mirror. That alone is a category change from passwords and TOTP, where the server holds a verifiable secret and so does anyone who steals it.

Two ceremonies define the protocol. **Registration** (`navigator.credentials.create`) generates a fresh key pair scoped to your site's **rpId** (the registrable domain, e.g. `example.com`) and ships the public key to your server, which stores it against the user. **Authentication** (`navigator.credentials.get`) proves possession of the private key: the server sends a one-time random **challenge**, the authenticator signs over it (plus context), and the server verifies the signature against the stored public key. Possession of the key, gated by a local **user-verification** gesture (biometric or PIN), is the proof of identity.

The crucial part is *what* gets signed. The authenticator does not sign the bare challenge. It signs a structure that includes the **`clientDataJSON`** — and the browser, not the page's JavaScript, fills that object with the page's real **`origin`** and the server's **`challenge`**. The signature therefore binds the assertion to *the exact origin the user's browser was actually on*. That binding is the whole game.

<FlowDiagram
  label="A WebAuthn authentication ceremony: the server issues a one-time challenge; the browser builds clientDataJSON stamping in the page's true origin and that challenge; the authenticator signs over it after a local user-verification gesture; the server verifies the signature with the stored public key and then checks that the signed origin and challenge match what it expects — only then is the user authenticated."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "chal", label: "Server issues challenge", sub: "fresh, single-use nonce" },
    { id: "cdj", label: "Browser builds clientDataJSON", sub: "stamps real origin + challenge" },
    { id: "sign", label: "Authenticator signs", sub: "after UV gesture, private key local" },
    { id: "verify", label: "Server verifies signature", sub: "against stored public key" },
    { id: "origin", label: "Server checks origin + challenge", sub: "must equal expected values" },
    { id: "auth", label: "Authenticated", sub: "only if all checks pass" },
  ]}
  edges={[
    { from: "chal", to: "cdj", label: "to client" },
    { from: "cdj", to: "sign", label: "over these bytes" },
    { from: "sign", to: "verify", label: "assertion" },
    { from: "verify", to: "origin", label: "sig valid" },
    { from: "origin", to: "auth", label: "match" },
  ]}
  caption="The signature being valid is necessary but not sufficient. The phishing resistance lives in the last step: the server compares the browser-stamped origin to its own expected origin. Skip that comparison and you keep the cryptography but throw away the property it exists for."
/>

## Why the relay attack fails: the origin is signed, by the browser

Walk the adversary-in-the-middle (AiTM) attack that defeats passwords and TOTP, and watch it die against a passkey. The attacker runs a reverse proxy on a look-alike domain that faithfully forwards your real pages. The victim lands there and authenticates. With a password, the proxy reads the typed secret and replays it to the real site — game over. With TOTP, the proxy reads the typed 6-digit code and replays it inside its validity window — game over. These work because the secret is a **readable bearer value**: whoever captures it can present it anywhere.

A passkey breaks the pattern at the byte level. When the victim's browser builds `clientDataJSON`, it writes the *page's* origin into it — and the page is `accounts-secure-login.com`. The authenticator signs over those bytes. The proxy relays the resulting assertion to your real server, which expects origin `accounts.example.com`. On verification the signature is **cryptographically valid but bound to the wrong origin**, so the server's origin check rejects it. The proxy cannot fix this: it cannot make the browser lie about the origin (the browser fills the field, not the page's JS), and it cannot re-sign for the correct origin because it never had the private key. In practice the authenticator usually won't even surface the credential, because the look-alike domain's **rpId won't match** what the passkey was registered to — so the ceremony fails before a signature exists. The attacker is left holding a valid signature that is worthless everywhere except the phishing domain it was bound to.

<Inset kind="note" lang="en">
This is the precise sense of "phishing-resistant." It is not that users are smarter or that the page is harder to clone — the clone can be perfect. It is that the proof is **cryptographically scoped to an origin the attacker does not control**, and that scope is enforced by the browser and re-checked by the server, two parties the attacker can't impersonate. Contrast TOTP, which is "phishing-*resistant* in marketing, phishable in practice": the code is a readable value the user hands over, and a relay forwards it just fine.
</Inset>

## The tradeoffs you actually accept

Adopting passkeys is not free, and the costs are not where beginners look.

**Recovery becomes the hardest part, not the login.** A passkey lives on a device or in a sync fabric. Users lose phones, wipe laptops, and switch ecosystems. So you must offer recovery — and recovery is where the security of the whole account is silently decided. If your recovery path is "email me a reset link," and that email account is protected only by a password, then the **true strength of the account equals a phishable password on a third-party mailbox**. You built an unphishable front door and left a phishable side door wide open. The rule is blunt: *recovery must be at least as strong as the primary factor.* The cleanest answer is to register **two passkeys at enrollment** (one platform, one roaming hardware key), so the second credential is the recovery method — no weaker channel needed.

**Synced vs device-bound passkeys is a real trust decision.** Modern consumer passkeys **sync** across a user's devices through their platform account (iCloud Keychain, Google Password Manager). That is wonderful for adoption — lose a phone, your passkeys are on the new one — but it means the credential's security now also depends on the **platform account's** security, and it means the old assumption that "the private key lives in one tamper-resistant chip" is no longer true. **Device-bound** passkeys (a hardware security key, or platform keys with a no-sync policy) stay in one place and are stronger against account-level compromise, but they make loss catastrophic unless you've registered a backup. For high-assurance contexts (admin, finance, regulated systems) you often require device-bound keys and attestation; for consumer scale you accept synced keys and lean on platform-account security. Knowing which you have is part of your threat model.

**`signCount` is not the tamper-evidence it once was.** WebAuthn's `authenticatorData` carries a **signature counter** meant to detect a cloned hardware authenticator: if the counter ever goes *backwards*, you've seen a clone. That signal is real for single-device hardware keys. But synced passkeys are copied across devices by design, so their counter is frequently **0 or meaningless** across the fleet — a regression isn't proof of compromise. Treat `signCount` as a soft signal, never a hard gate, and never as the thing standing between you and a cloned credential on a synced platform.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">Password / TOTP (shared secret)</th>
        <th class="border border-border px-3 py-2 text-left">Passkey (WebAuthn)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">What the server stores</td>
        <td class="border border-border px-3 py-2">A verifiable secret (hash / shared seed) — leakable</td>
        <td class="border border-border px-3 py-2">A public key only — nothing to phish or leak</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Phishing / AiTM relay</td>
        <td class="border border-border px-3 py-2">Relayed straight through — secret is a bearer value</td>
        <td class="border border-border px-3 py-2">Rejected — signature is bound to the wrong origin</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Replay of a captured proof</td>
        <td class="border border-border px-3 py-2">Works within validity window (TOTP) / forever (password)</td>
        <td class="border border-border px-3 py-2">Blocked by the one-time server challenge</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Weakest reachable path</td>
        <td class="border border-border px-3 py-2">The secret itself</td>
        <td class="border border-border px-3 py-2">Recovery, session handling, or device theft — not the login</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Clone detection</td>
        <td class="border border-border px-3 py-2">N/A</td>
        <td class="border border-border px-3 py-2"><code>signCount</code> — real for hardware keys, 0/meaningless when synced</td>
      </tr>
    </tbody>
  </table>
</div>

## Where it breaks: WebAuthn only secures the login event

The most expensive passkey mistake is believing the credential secures the account. It secures exactly one thing: **the authentication event**. Everything around it is still your responsibility, and that is where teams who "went passwordless" still get taken over.

**The session is still a bearer token.** A passkey login mints a session cookie — and that cookie is cash, exactly like before. If you never rotate it, give it a long lifetime, and let third-party script run on the authenticated page, a single XSS exfiltrates a live session and the attacker is in *without ever touching the passkey*. WebAuthn made the login unphishable; it said nothing about what happens after. You still need session rotation on login and privilege change, short lifetimes with re-auth, `HttpOnly`/`Secure`/`SameSite` cookies, and a CSP that stops injected script from reading the session.

**Possession on an unlocked device defeats user verification.** WebAuthn proves possession of the key, gated by a *local* UV gesture — but on an already-unlocked laptop the OS may have the gesture cached or auto-satisfy it. A thief with a stolen, unlocked device can use a synced passkey to sign in, and the cryptography is working exactly as designed. There is no crypto flaw to find. The defense is layered controls *above* the login: explicit **step-up re-authentication** (`userVerification: 'required'`) for sensitive actions, risk signals (new device, new geo, impossible travel), short bound sessions, and — critically — keeping **authentication separate from authorization**, so a privileged action re-checks both "is this the right human right now" and "is this principal allowed," instead of trusting a passive session minted hours ago.

<Inset kind="mistake" lang="en">
The trap migration is keeping the password as a permanent parallel login "just in case." Now the account has two front doors: an unphishable passkey and a phishable password — and attackers always take the weaker one, so your real security is the password you thought you'd upgraded away from. The fix is a migration with teeth: once a user has 2+ passkeys, **downgrade or remove the password** for that account, and never leave email-OTP recovery as a no-friction reset that quietly becomes the account's true strength.
</Inset>

## How a senior actually deploys passkeys

Treat the passkey as a strong **authenticator**, not a complete identity system, and harden the chain around it. Register **two credentials at enrollment** so recovery is a second passkey, not a phishable reset link. **Migrate the password away** once coverage is real, rather than keeping a permanent weak parallel path. Make **recovery at least as strong as the login** — second passkey or hardware key first, and where an out-of-band reset must exist, add friction (delay, notification to all devices, step-up, an enrollment-time recovery code). Add **step-up for sensitive actions** so a passive session can't act, and **rotate and bound sessions** so a stolen one expires fast. Decide **synced vs device-bound** deliberately per assurance level. And write down the standing assumption — *a passkey secures the login, not the session, the recovery, or the device* — so nobody re-makes the "login is enough" mistake. The test for any passkey deployment is one walk: trace every path that can land an attacker in the account, and confirm none of them is more phishable than the passkey itself.

<TradeoffMatrix
  id="03-passkeys-and-webauthn-tradeoff-1"
  lessonSlug="03-passkeys-and-webauthn"
  lang="en"
  prompt="You're adding passkeys to an app that has password + email-OTP login today. You must keep the account at least as phishing-resistant via every path, including recovery. Pick the design."
  options={[
    { name: "Register 2 passkeys at enrollment; once a user has 2+, remove the password; recovery is a second passkey/hardware key with friction (delay, notify, step-up)", summary: "Every path is origin-bound.", correct: true, justification: "This makes recovery at least as strong as the login: the recovery method is itself a passkey, so there's no weaker channel for an attacker to walk. Removing the password once two passkeys exist closes the permanent phishable parallel path. The friction on any out-of-band reset stops a quiet social-engineering bypass. Every route into the account is now origin-bound or gated, which is exactly the requirement." },
    { name: "Add passkeys as a new option, keep the password forever as a fallback, keep email-OTP reset as-is", summary: "Phishable side door stays open.", justification: "The account's real strength is now the weakest reachable path — the still-phishable password and the no-friction email-OTP reset on a password-only mailbox. You built an unphishable front door and left two phishable ones, so an attacker simply ignores the passkey. This fails the 'at least as strong via every path' requirement." },
    { name: "Passkey-only login, but recovery is a one-click 'email me a reset link' that sets a new password", summary: "Recovery undoes the passkey.", justification: "The login is unphishable, but recovery silently becomes the true authenticator: a phishable email link on a mailbox that's often only password-protected, with no friction. Attackers skip the front door and walk recovery. Recovery must be at least as strong as the primary factor; a bare reset link is strictly weaker." },
    { name: "Require passkeys and trust signCount as the compromise signal across all devices, no step-up", summary: "Wrong signal, missing layer.", justification: "On synced passkeys signCount is often 0/meaningless, so it can't be your tamper-evidence — and trusting it gives false assurance. It also ignores that WebAuthn secures only the login: with no step-up, a stolen unlocked device or a hijacked session owns the account anyway. This solves a non-problem and skips the real layers." },
  ]}
/>

<Quiz
  id="03-passkeys-and-webauthn-quiz-1"
  lessonSlug="03-passkeys-and-webauthn"
  lang="en"
  question="An AiTM proxy on a look-alike domain relays a victim's full WebAuthn ceremony and captures a cryptographically valid signature. Why is it useless against your real server?"
  choices={[
    { label: "The browser stamped the proxy's true origin into the signed clientDataJSON, so the server's origin check rejects it — and the proxy can't re-sign for the right origin without the private key", correct: true },
    { label: "The signature expires after 30 seconds, like a TOTP code, so the proxy is too slow to use it", misconception: "WebAuthn assertions aren't time-boxed like TOTP. Replay is stopped by the one-time challenge, and phishing is stopped by the signed origin — neither is a 30-second timer." },
    { label: "The authenticator encrypts the assertion so only the real server can read it", misconception: "The assertion is signed, not encrypted, and is readable in transit. The defense is that the signature is bound to the wrong origin, so it fails verification — not that it's hidden." },
    { label: "The proxy can't capture biometrics, so it never gets a valid signature in the first place", misconception: "Biometrics never leave the device; the proxy relays the resulting signature just fine. It's a real, valid signature — it's simply bound to the wrong origin, which is what defeats it." },
  ]}
/>

<Quiz
  id="03-passkeys-and-webauthn-quiz-2"
  lessonSlug="03-passkeys-and-webauthn"
  lang="en"
  question="A team migrated to passkeys and still suffered account takeovers, with no passkey ever stolen. What's the most likely cause?"
  choices={[
    { label: "The surrounding chain was weak — a no-friction email-OTP recovery and/or a long-lived, never-rotated session with third-party script — and WebAuthn never covered either", correct: true },
    { label: "Passkeys are only as strong as the RSA key length, and theirs was too short", misconception: "Key strength isn't the issue; passkeys use strong curves by default and the takeovers happened with no passkey stolen. The weakness is the chain around the login — recovery and session — not the key." },
    { label: "Synced passkeys are inherently insecure and should never be used", misconception: "Synced passkeys are a deliberate adoption tradeoff, not the cause here. Takeovers with no credential stolen point to recovery and session handling, which WebAuthn doesn't secure regardless of sync." },
    { label: "The server forgot to hash the public keys it stored", misconception: "Public keys are public by design — there's nothing to hash or protect. Storing them in the clear is fine; the real gap is the recovery and session paths around the login." },
  ]}
/>

<DragOrder
  id="03-passkeys-and-webauthn-drag-1"
  lessonSlug="03-passkeys-and-webauthn"
  lang="en"
  prompt="Order the server-side checks of a WebAuthn authentication assertion, from receiving it to issuing a session:"
  items={[
    { id: "a", label: "Look up the stored public key for the presented credential id" },
    { id: "b", label: "Verify the signature over authenticatorData + hash(clientDataJSON) with that public key" },
    { id: "c", label: "Check the signed origin equals your expected origin, and the challenge matches the one you issued (then consume it)" },
    { id: "d", label: "Enforce policy flags (user-verified bit if required), then issue the session" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>A passkey is an origin-bound public-key credential: the browser signs over the page's true origin and a one-time challenge, so a phished or relayed login fails the server's origin check. That makes the <em>login</em> unphishable — but recovery, sessions, and a stolen unlocked device are still yours to secure. The account is only as strong as its weakest reachable path.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-passkeys-and-webauthn-retrieval"
  lang="en"
  questions={[
    { q: "Explain precisely why a passkey login survives an adversary-in-the-middle relay when a password and a TOTP code do not.", a: "A password and a TOTP code are readable bearer values: the user hands the secret to whatever page they're on, so an AiTM proxy on a look-alike domain captures it and replays it to the real site — the password works forever, the TOTP code works inside its validity window. A passkey is not a shared secret; it's an asymmetric key whose private half never leaves the authenticator, and the proof is a signature. The decisive detail is what gets signed: the browser (not the page's JavaScript) builds clientDataJSON and stamps in the page's true origin and the server's one-time challenge, and the authenticator signs over those bytes. When the victim is on accounts-secure-login.com, that origin is what gets signed. The proxy relays a real, cryptographically valid assertion to your real server, which expects accounts.example.com, so the origin check rejects it. The proxy can't make the browser lie about the origin and can't re-sign for the right origin without the private key. In practice the authenticator often won't even surface the credential because the rpId doesn't match. So the proof is cryptographically scoped to an origin the attacker doesn't control — that scope, enforced by the browser and re-checked by the server, is the entire phishing-resistance property." },
    { q: "A passkey login is unphishable. List what WebAuthn does NOT secure, and what you must add for each.", a: "WebAuthn secures one thing: the authentication event. (1) Sessions: the login mints a bearer cookie that's still cash — add session rotation on login and privilege change, short lifetimes with re-auth, HttpOnly/Secure/SameSite, and a CSP so XSS can't exfiltrate a live session. (2) Recovery: if recovery is a no-friction email-OTP on a password-only mailbox plus a social-engineerable agent, the account's true strength equals a phishable password — make recovery at least as strong as the login (prefer a second registered passkey/hardware key; add delay, notification, step-up, an enrollment-time recovery code). (3) Device theft / possession: on an already-unlocked device the local user-verification gesture can be cached, so a thief uses a valid synced passkey with no crypto flaw — add step-up re-auth (userVerification:'required') for sensitive actions, risk signals (new device/geo, impossible travel), and short bound sessions. (4) Authorization: a valid login isn't permission — keep access control server-side and separate, and re-check it per privileged action. (5) signCount: it's a clone signal for hardware keys but often 0/meaningless on synced passkeys, so treat it as soft, never a hard gate. The standing rule: the account is only as strong as its weakest reachable path, so audit recovery and session handling, not just the credential." },
  ]}
/>

<Recap lang="en">
A passkey is a WebAuthn credential — an asymmetric key pair whose private half never leaves the authenticator, with the server holding only the public key, so there's no shared secret to phish or leak. Authentication proves possession by signing a one-time server challenge, and the decisive detail is that the browser stamps the page's true origin and the challenge into clientDataJSON before the authenticator signs. That binds the proof to a specific origin, so an adversary-in-the-middle proxy that relays a real, valid signature still fails the server's origin check — the signature is bound to the attacker's domain, and the proxy can't re-sign for yours without the private key. That's what "phishing-resistant" means, and it's why a passkey survives a relay that a password or a TOTP code does not. But WebAuthn secures only the login event. The session is still a bearer cookie you must rotate, bound, and shorten; recovery becomes the hardest part and must be at least as strong as the login (prefer a second registered passkey, never a no-friction email reset on a password-only mailbox); a stolen unlocked device can use a valid synced passkey with no crypto flaw, which is why you add step-up, risk signals, and separated authorization; and signCount is a clone signal for hardware keys but 0/meaningless on synced passkeys, so it's soft, never a gate. Deploy passkeys as a strong authenticator, register two at enrollment, migrate the password away, and remember the account is only as strong as its weakest reachable path.
</Recap>
