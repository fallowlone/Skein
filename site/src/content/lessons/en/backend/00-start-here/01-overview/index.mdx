---
concepts:
- what-is-backend
- server
- request-response
- http-method
- endpoint-route
- handler
- status-code
- database
- statelessness
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: zero
order: 1
prereqs: []
slug: 01-overview
sources:
- https://developer.mozilla.org/en-US/docs/Learn/Server-side/First_steps/Introduction
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview
- https://nodejs.org/en/learn/getting-started/introduction-to-nodejs
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Status
spiral: []
status: ready
summary: A backend is the program that answers requests on a server — the half of the
  web you never see. This is the from-zero map and the eight words Unit 01 and every
  unit after it assumes you already know.
title: 'Start from zero: what a backend actually is'
track: backend
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import EditorialDiagram from "~/components/diagram/EditorialDiagram.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
You open a browser, type a URL, and a webpage appears. You log in and your profile shows up. You click "buy" and a confirmation email arrives seconds later. None of that happens in your browser. Somewhere a program on a machine you will never see received your click, figured out what you wanted, asked a database, and wrote back an answer — all in under a second. That program is the backend. Everything in this track exists to teach you how to build one that is fast, correct, and does not fall over when things go wrong. This lesson is the map before the climb.
</Hook>

<Crux>A backend is a program on a server that receives requests, runs your logic, talks to a database, and sends responses back.</Crux>

By the end of this lesson you will have the eight terms that every senior unit assumes — so when they appear in context, you already know what role each one plays.

<Explanation>

## The one problem a backend solves

A web browser can display HTML and run JavaScript, but it cannot safely store passwords, charge a credit card, or hold a shared database that all users read and write. Someone has to live outside the browser, remember things across sessions, enforce rules, and answer everyone's questions consistently. That someone is the backend: a program running on a machine in a data centre, reachable over the network, waiting for requests and replying with data.

When you think about it, every backend feature you will learn — authentication, caching, rate limiting — is just a more specific answer to one of those constraints.

Everything else this track teaches — middleware, async I/O, connection pools, circuit breakers — is detail on top of that single job: **receive a request, do useful work, return a response, survive the load.**

## The eight words the rest of the track assumes

The senior units ahead drop these terms without stopping to define them. Here they are, one sentence each — what it is and why it exists.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Word</th>
        <th class="border border-border px-3 py-2 text-left">What it is</th>
        <th class="border border-border px-3 py-2 text-left">Why it exists</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Server</strong></td>
        <td class="border border-border px-3 py-2">A program (or machine) that listens for network connections and answers them.</td>
        <td class="border border-border px-3 py-2">So many clients can reach your code without running it themselves.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Request / Response</strong></td>
        <td class="border border-border px-3 py-2">The two-message conversation: a client asks, the server answers.</td>
        <td class="border border-border px-3 py-2">So client and server can live on different machines and still talk.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>HTTP method</strong></td>
        <td class="border border-border px-3 py-2">A verb on the request — GET, POST, PUT, DELETE — that signals intent.</td>
        <td class="border border-border px-3 py-2">So the server knows whether the client is reading, creating, or removing data.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Endpoint / Route</strong></td>
        <td class="border border-border px-3 py-2">A URL path + method pair the server promises to handle (e.g. POST /orders).</td>
        <td class="border border-border px-3 py-2">So the client knows where to aim each kind of request.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Handler</strong></td>
        <td class="border border-border px-3 py-2">The function that runs when a request matches a route.</td>
        <td class="border border-border px-3 py-2">So each route has its own piece of logic, easy to read and test.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Status code</strong></td>
        <td class="border border-border px-3 py-2">A three-digit number on the response (200 OK, 404 Not Found, 500 Error).</td>
        <td class="border border-border px-3 py-2">So the client can react correctly without parsing the body to detect failure.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Database</strong></td>
        <td class="border border-border px-3 py-2">Persistent storage the backend reads from and writes to across requests.</td>
        <td class="border border-border px-3 py-2">So data survives restarts and is shared across all users and server instances.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Statelessness</strong></td>
        <td class="border border-border px-3 py-2">Each request carries everything the server needs to answer it; no memory between calls.</td>
        <td class="border border-border px-3 py-2">So any server instance can handle any request, making scale-out and restarts safe.</td>
      </tr>
    </tbody>
  </table>
</div>

## How they fit together

Read in order, the words tell one story: a **request** arrives carrying an **HTTP method** and a URL; the server's **router** matches that URL to an **endpoint** and calls the **handler** function; the handler reads or writes the **database** and builds a reply; the reply goes back as a **response** with a **status code** the client checks. Because the server is **stateless**, it remembers nothing between calls — all context either travels in the request itself or lives in the database. That single paragraph is the entire track in miniature; every unit ahead zooms into one step.

<Inset kind="why" lang="en">
Why does statelessness matter if it just shifts the burden to a database? Because a stateful server is a bottleneck: every user's second request must reach the same machine that handled the first, or their session data is lost. A stateless server has no such constraint — any instance can answer any request, so you can add more instances when traffic spikes and remove them when it drops, with zero coordination. The database handles the shared truth; the server stays disposable.
</Inset>

## You do not need to memorise this

Two honest notes before the climb. First: nobody holds all of this perfectly on day one — you will meet each word again in its own unit, deeper, and it will stick then. This page is a coat-hook to hang the details on, not a test. Second: not every backend needs every feature. A tiny side project might be a single server with three routes and no load balancer. The track teaches the full picture because that is what production under load demands — but "start simple, add complexity when the pain shows up" is itself the senior instinct.

<Quiz
  id="00-start-here-backend-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Why must a backend server be stateless — what does that enable?"
  choices={[
    { label: "Any instance can handle any request, so you can scale out freely and restart safely without routing users to a specific machine", correct: true },
    { label: "It makes the code faster because the server never reads from disk", misconception: "Statelessness is about where session data lives, not disk I/O speed. A stateless server still reads from a database on disk — it just doesn't hold per-user state in memory between requests." },
    { label: "It removes the need for a database altogether", misconception: "The opposite: statelessness pushes shared state into the database. Without a database, data would vanish every time the server restarts." },
    { label: "It means the server can only handle one request at a time", misconception: "Statelessness says nothing about concurrency. A stateless server can handle thousands of simultaneous requests — the point is that it remembers nothing about a user between those requests." },
  ]}
/>

<DragOrder
  id="00-start-here-backend-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the journey from a client click to a response arriving back:"
  items={[
    { id: "a", label: "Client sends a request with an HTTP method and URL" },
    { id: "b", label: "Router matches the URL to an endpoint and calls the handler" },
    { id: "c", label: "Handler reads or writes the database" },
    { id: "d", label: "Server sends a response with a status code and data" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<EditorialDiagram
  kind="timeline"
  genre="the request cycle"
  labels={["request", "route", "handler + DB", "response"]}
  note="stateless, repeat"
  label="The four-step backend cycle: a request arrives, the router picks a handler, the handler reads or writes the database, and a response goes back."
  caption="That four-step cycle is all of backend — every senior unit lands on one of these steps."
  hue="var(--d-backend, var(--accent))"
/>

<KeyTakeaway>A request arrives, the router picks a handler, the handler reads or writes the database, and a response goes back. That four-step cycle is all of backend — learn these eight terms now and every senior unit lands cleanly.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-start-here-backend-retrieval"
  lang="en"
  questions={[
    { q: "In one breath, what is a backend and why does it have to exist?", a: "A backend is a program running on a machine in a data centre that listens for network requests, runs logic, reads and writes a database, and sends responses back to clients. It has to exist because a browser cannot safely store passwords, hold shared data that all users read and write, or enforce rules that clients should not be able to bypass. The backend lives outside every client and answers all of them consistently, making it the single source of truth for the application's data and rules." },
    { q: "Trace the path of a request from the moment a client sends it to the moment a response arrives, naming each concept.", a: "The client sends a request carrying an HTTP method (GET, POST, PUT, DELETE) and a URL path. The server's router compares that method and path against its registered endpoints and, when it finds a match, calls the corresponding handler function. The handler runs the business logic — which almost always means reading from or writing to the database — and builds a response. That response travels back to the client with a three-digit status code (200 for success, 404 for not found, 500 for server error) and a body containing the data. Because the server is stateless, it held nothing in memory about this user between requests; all context either arrived in the request or was fetched from the database." },
  ]}
/>

<Recap lang="en">
A backend is one idea with a lot of machinery hung off it: a program on a server receives requests, runs logic, reads or writes a database, and returns responses — all while keeping no memory between calls so any server instance can handle any request. The eight words map the full surface: a server listens; a request carries a method and a URL; the router matches it to an endpoint and fires the handler; the handler talks to the database; a response with a status code goes back; and statelessness keeps the whole thing scalable. Now when you see any of these eight terms in a senior unit, you will know exactly which step of that cycle it belongs to — and you will know what breaks when that step goes wrong.
</Recap>
