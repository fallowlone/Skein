infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Eight words, four jobs
    children
      - label Listen & scale
        children
          - label Server — listens for connections
          - label Statelessness — any instance answers
      - label The conversation
        children
          - label Request / Response — client asks, server answers
          - label HTTP method — GET, POST, PUT, DELETE
          - label Status code — 200, 404, 500
      - label Route & run
        children
          - label Endpoint / Route — URL path + method
          - label Handler — function for a matched route
      - label Persist
        children
          - label Database — shared, survives restarts
theme
  palette #6366f1 #14b8a6 #f59e0b #8b5cf6
