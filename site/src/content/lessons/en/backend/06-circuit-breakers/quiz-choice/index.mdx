---
concepts:
- circuit-breaker
- half-open
- failure-rate-threshold
- bulkhead
- timeout
- retry-storm
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martinfowler.com/bliki/CircuitBreaker.html
- https://resilience4j.readme.io/docs/circuitbreaker
- https://sre.google/sre-book/addressing-cascading-failures/
status: ready
summary: "Multiple-choice synthesis across the circuit-breaker unit — fast-fail, the three-state machine, trip thresholds, bulkheads, fallbacks, and fleet-scale failure modes."
title: 'Circuit breakers: multiple-choice review'
track: backend
unit: 06-circuit-breakers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a call you make in a real incident — not a definition to recite, but a tradeoff to weigh when a downstream is sick and your threads are filling.
</Hook>

<Goal lang="en">
Confirm you can connect fast-fail, the state machine, trip thresholds, bulkheads, fallbacks, and the fleet-scale failure modes — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="06-circuit-breakers-choice-1"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="A payment provider slows from 50 ms to 5 s but never returns errors. Within seconds the whole service — including routes that never touch payments — stops responding. Why is the slow case worse than an outright outage, and what is the breaker's role?"
  choices={[
    { label: "Each caller blocks for 5 s holding a worker thread and pooled connection, so a shared pool fills and unrelated routes starve; a dead dependency would fail in 1 ms and free the thread. The breaker trips after the failure rate climbs and fast-fails subsequent calls, converting an unbounded outage into a bounded one.", correct: true },
    { label: "The slow responses corrupt request state, and the breaker rolls them back.", misconception: "Nothing is corrupted — the harm is resource occupancy, every caller pinned on a slow call. A breaker frees resources by fast-failing; it does not roll back data." },
    { label: "Slowness only affects the payment route, but the breaker accidentally widens it.", misconception: "The parked threads come from a shared pool, so unrelated routes starve too. The breaker narrows the blast radius, it does not widen it." },
    { label: "A slow dependency is actually safer than a dead one because the call still eventually succeeds.", misconception: "A slow success costs everything a real success would plus the wait; it pins resources for the full timeout. Slow is the dangerous state precisely because callers keep waiting." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-2"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="A breaker is set with a cooldown (resilience4j waitDurationInOpenState) of 1 s in front of a dependency that takes about 20 s to restart after it falls over. What goes wrong?"
  choices={[
    { label: "The breaker probes again every second — long before the dependency has recovered — so the half-open trial fails and it reopens, oscillating and firing bursts of doomed probe calls at a service that needs quiet time to come back.", correct: true },
    { label: "Nothing — a short cooldown always recovers faster.", misconception: "A cooldown shorter than the dependency's real recovery time guarantees the probe lands on a still-broken service; the breaker flaps instead of recovering. The cooldown should track real recovery time." },
    { label: "The breaker can never leave the open state with a 1 s cooldown.", misconception: "It leaves open every second to probe — that is the problem. It re-enters open because the probe fails, not because it is stuck." },
    { label: "A 1 s cooldown disables the half-open state entirely.", misconception: "The cooldown is just how long open lasts before half-open; a short one still goes half-open, just too soon." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-3"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="A low-traffic admin endpoint sees about 10 requests/minute. One transient timeout at 3 a.m. trips its breaker for the full cooldown, fast-failing every later request over a single blip. Which setting is the right fix, and why not just raise the failure-rate threshold?"
  choices={[
    { label: "A minimum-volume floor (Hystrix requestVolumeThreshold, resilience4j minimumNumberOfCalls) so the breaker stays closed until it has seen enough calls for the rate to be statistically meaningful. Raising the rate threshold cannot help because 1 failure out of 1 is 100%, which beats any threshold below 100%.", correct: true },
    { label: "A shorter cooldown so the false trip clears faster.", misconception: "A shorter cooldown only reduces the damage of a false trip; it does not stop the breaker tripping on a single call. The volume floor is what prevents the trip." },
    { label: "Disable slow-call detection on this endpoint.", misconception: "Slow-call detection is unrelated — the false trip came from a failure rate computed on too few calls, not from a slow call." },
    { label: "Switch from a count-based to a time-based window.", misconception: "The window type changes how 'recent' is defined, but a single failure still produces a 100% rate within either window. Only a minimum-volume floor guards against the single-call trip." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-4"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="Three downstreams — payments, recommendations, search — share one 50-thread pool. Recommendations (the least important) slows to 5 s and the whole service fails, even though its breaker eventually trips. What does a bulkhead add that the breaker alone cannot?"
  choices={[
    { label: "A per-dependency concurrency cap, so the slow downstream can pin at most its own allowance while payments and search keep their threads. The breaker only limits how long you keep calling a sick dependency; the bulkhead limits how much of total capacity any one dependency can ever hold — and it acts before the reactive breaker trips.", correct: true },
    { label: "A faster failure-rate window so the breaker trips before the pool drains.", misconception: "A faster window trips the breaker sooner but the pool can still drain before it reacts; only partitioning the budget contains the failure. The bulkhead is the missing isolation, not faster reaction." },
    { label: "Automatic retries for the slow dependency so calls succeed.", misconception: "Retries add load to an already-sick dependency; they do not isolate. A bulkhead partitions the concurrency budget per dependency." },
    { label: "Nothing new — a breaker on each dependency already isolates them.", misconception: "Per-dependency breakers still draw from one shared thread pool, so a slow dependency drains the shared budget before its breaker reacts. Isolation requires capping concurrency per dependency, which is the bulkhead." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-5"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="A team wraps a flaky recommendations service in a breaker and ships it. During the next incident the breaker never trips and the homepage hangs — the recommendations calls do not error, they take 30 s each. What is the missing piece, and what second piece do they still need once it trips?"
  choices={[
    { label: "A timeout — without one a hang is neither success nor failure, so it is invisible to a breaker that counts failures. They also need a fallback: once the breaker is open, calls are rejected instantly, but the caller must return something useful (drop the carousel, keep the page) rather than a hard 500.", correct: true },
    { label: "A higher failure-rate threshold so slow calls register.", misconception: "A threshold cannot help when zero failures are being counted — the hangs never register as failures without a timeout. The timeout is what converts a hang into a countable failure." },
    { label: "A bigger thread pool so the hangs do not starve threads.", misconception: "A bigger pool delays starvation but the breaker still never counts the hangs. The timeout is what makes a hang look like a failure." },
    { label: "More aggressive retries on the recommendations call.", misconception: "Retrying a 30 s hang multiplies the damage. The breaker needs a timeout to bound and count each call, plus a fallback for what to return once open." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-6"
  lessonSlug="06-circuit-breakers"
  lang="en"
  question="Service A calls B calls C, each retrying up to 4 times on failure, across a fleet of fifty instances. C wobbles briefly. The wobble becomes a sustained, self-inflicted outage. What is the dominant mechanism, and which fix bounds it?"
  choices={[
    { label: "Retry amplification: retries multiply down the chain, so one request to A becomes 4×4×4 = 64 calls at C — a retry storm. Keeping the breaker above the retries short-circuits the whole stack once it opens; pair that with retrying at one layer only, an absolute per-process retry budget, and exponential backoff with jitter.", correct: true },
    { label: "Each instance's breaker independently corrupts C's data.", misconception: "Breakers do not corrupt data, and the harm here is multiplicative load, not correctness. The 64x retry amplification is the mechanism." },
    { label: "The fix is to centralize all fifty breakers' state in Redis so they share one verdict.", misconception: "Shared state buys a shared view at the cost of a hot-path network call and a new dependency; it does not address the retry storm beneath the breaker. The storm is bounded by capping retries and keeping the breaker above them." },
    { label: "Exponential backoff alone fully prevents the storm.", misconception: "Backoff spreads one client's attempts but clients that failed together re-synchronize into waves; jitter is what breaks the correlation, and a breaker above the retries is what bounds the multiplier." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one decision chain: a slow dependency starves a shared pool, so a breaker fast-fails it (closed counts failures, open rejects instantly, half-open probes), tripping on a failure rate over a sliding window gated by a minimum-volume floor, with slow counted as failure. A bulkhead isolates the budget per dependency so one sick downstream cannot drain the whole pool before the reactive breaker trips; a timeout converts a hang into a countable failure and a fallback decides what to return when open; and at fleet scale the real danger is retry amplification (4×4×4 = 64), tamed by keeping the breaker above the retries, capping the absolute retry rate, and jittering both retries and half-open probes. Every per-instance safety device needs a fleet-level story.
</Recap>
