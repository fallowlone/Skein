---
concepts:
- idempotency-key
- state-machine
- backoff-jitter
- outbox-inbox
- effectively-once
- concurrency-race
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://stripe.com/blog/idempotency
- https://brandur.org/idempotency-keys
- https://www.codereliant.io/p/retries-backoff-jitter
status: ready
summary: "Multiple-choice synthesis across the idempotency + retries unit — keys, the four-state machine, jittered backoff, outbox/inbox, concurrency races, and production failure modes."
title: 'Idempotency and retries: multiple-choice review'
track: backend
unit: 05-idempotency-retries
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None is a definition to recite — each is a decision you make under load, where the wrong call is a double charge, a lost event, or a self-inflicted outage.
</Hook>

<Goal lang="en">
Confirm you can connect the moving parts the lessons built toward: the idempotency key, the server-side state machine, jittered retries, the outbox + inbox composition, and the concurrency races that turn a tidy design into a duplicate side effect.
</Goal>

<Quiz
  id="05-idempotency-retries-choice-1"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="A mobile client fires a POST /charge, the response is lost, and it retries. On the retry it generates a fresh UUID for the Idempotency-Key header. What is the consequence and why?"
  choices={[
    { label: "The customer is charged twice. The key must be one-per-logical-operation; a new key per HTTP attempt makes the server see every retry as a brand-new charge, defeating dedup entirely.", correct: true },
    { label: "The customer is charged once — the server fingerprints the body and recognises the duplicate regardless of the key.", misconception: "The fingerprint is keyed off the idempotency key. With no matching key the server has no row to compare against, so the body hash never gets consulted — it processes a new charge." },
    { label: "The retry is rejected with 422 because the key changed.", misconception: "422 (mismatch) requires an existing row for that key with a different fingerprint. A brand-new key has no row, so it is treated as new (200/201), not 422." },
    { label: "It is fine — UUID v4 collisions are so rare that two charges sharing intent will reconcile downstream.", misconception: "There is no automatic reconciliation. The whole point of the key is to make the server dedup; a new key per attempt removes that mechanism." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-2"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="A request arrives with Idempotency-Key K1. The server finds a row for K1 with status=in_progress. Why return 409 immediately instead of blocking until the first request finishes?"
  choices={[
    { label: "Blocking pins a server connection for the full duration of the first request — including its slow external API call — so 409 lets the client back off and retry, by which time the row is usually in replay state.", correct: true },
    { label: "RFC 9110 mandates 409 for concurrent idempotency-key use.", misconception: "RFC 9110 says nothing about idempotency-key concurrency; 409 here is Stripe's widely-adopted convention, chosen for connection efficiency, not spec compliance." },
    { label: "Because the server cannot read the row while it is in_progress.", misconception: "The row is readable at any time — that is exactly how the server knows to return 409. The choice is about not holding a connection, not about read access." },
    { label: "409 forces the client to regenerate the key, which prevents the double charge.", misconception: "The client must reuse the same key on retry. 409 signals 'already in flight, back off' — regenerating the key would create a second charge." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-3"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="A service returns 503 with Retry-After: 30 during an outage. Across the fleet, which client behaviour keeps the recovering service alive?"
  choices={[
    { label: "Honour Retry-After, then apply full jitter — random(0, min(cap, base × 2^n)) — and enforce a fleet-wide retry budget so total retry rate is capped even if every caller hits the cap.", correct: true },
    { label: "Ignore Retry-After and use a tight fixed 1s backoff so the client recovers as fast as possible.", misconception: "A synchronised 1s retry from every client is the thundering herd — the same spike that broke the service. Ignoring Retry-After is the textbook anti-pattern." },
    { label: "Plain exponential backoff with no jitter is enough — the doubling delay already spreads the load.", misconception: "Without jitter all clients that failed at the same instant pick the identical delay and retry in the same tick. The doubling does not desynchronise them." },
    { label: "A per-request cap of 6 attempts makes a retry budget unnecessary.", misconception: "A 6-attempt cap per request still lets 10k concurrent callers generate 60k retries in a short window. The budget is a fleet-wide ceiling the per-request cap cannot provide." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-4"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="A team must publish an 'order.created' event to Kafka whenever an order row commits to Postgres. Why is the outbox pattern correct where 'write Postgres then publish Kafka' is not?"
  choices={[
    { label: "Outbox writes the event into a Postgres table inside the same transaction as the order, so atomicity is Postgres's job; a relay then delivers at-least-once and the consumer's inbox dedups — effectively-once with no dual-write crash window.", correct: true },
    { label: "Publishing to Kafka first and writing Postgres second closes the window because Kafka is durable.", misconception: "Kafka-then-Postgres just moves the window: a crash after publish but before commit leaves a phantom event for a write that never happened." },
    { label: "Kafka producers are asynchronous, so the publish can never fail after the DB write.", misconception: "Async or not, the publish and the commit are two separate operations with a crash window between them. There is no atomic 'two separate writes'." },
    { label: "The outbox makes delivery exactly-once, removing the need for an idempotent consumer.", misconception: "The relay still delivers at-least-once (it may re-publish after crashing before marking a row published). The consumer's inbox dedup is what makes it effectively-once." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-5"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="Two requests carrying the same idempotency key reach the server within a millisecond of each other. The handler does SELECT-then-INSERT into the idempotency_keys table. What happens, and what is the fix?"
  choices={[
    { label: "Both SELECTs see no row, both INSERT, both process — a double charge. Fix: INSERT ... ON CONFLICT (key) DO NOTHING RETURNING, one atomic statement; the loser of the race finds RETURNING empty and re-reads the row to 409 or replay.", correct: true },
    { label: "The database serialises the two automatically, so the second request waits for the first.", misconception: "Plain SELECT-then-INSERT has no lock between the statements; nothing serialises the two requests. The race window is real." },
    { label: "The primary-key constraint makes the second INSERT fail and the app surfaces 500.", misconception: "If a unique constraint exists you should use ON CONFLICT to turn the collision into a controlled 409/replay, not crash with 500. The naive code may also have no constraint at all." },
    { label: "Raising the transaction isolation level to SERIALIZABLE on its own makes SELECT-then-INSERT safe with no code change.", misconception: "SERIALIZABLE can abort one transaction, but you still must retry and handle the conflict; the clean, single-statement fix is ON CONFLICT DO NOTHING RETURNING." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-6"
  lessonSlug="05-idempotency-retries"
  lang="en"
  question="A payment API uses Redis alone (async fsync, SETNX) as its idempotency store. A node crashes a millisecond after a SETNX, losing that key. What is the production-grade reading of this design?"
  choices={[
    { label: "It is unsafe for money: the lost key means the next retry is treated as new and may double-charge. The fix is a two-tier cache — Redis hot path backed by a durable Postgres ledger written via the outbox in the same transaction.", correct: true },
    { label: "Safe enough — Redis async replication guarantees the key survives the crash.", misconception: "Default Redis replication is async; a crash before the replica receives the write loses the key. Replication reduces but does not eliminate the window." },
    { label: "The customer is never charged because losing the key rolls back the payment.", misconception: "If the charge already hit the payment processor before the crash, it stands. Losing the dedup key does not undo a side effect that already happened." },
    { label: "Capping client retries at 3 makes the probability of a double charge low enough to ignore.", misconception: "A legal liability for double charges needs an architectural guarantee, not a low probability. Capping retries lowers the odds but does not remove them." },
  ]}
/>

<Recap lang="en">
The unit is one chain: the client retries with one key per operation; the server resolves that key to new / in-flight (409) / replay / mismatch (422) and stores the result atomically with ON CONFLICT; retries use full jitter, honour Retry-After, and live under a fleet retry budget; the outbox + at-least-once relay + inbox dedup turn the impossible dual-write into effectively-once; and the durable store must survive a crash, because losing a key is how a tidy design becomes a double charge. Every production failure in the unit — Stripe 2017, Knight Capital, AWS S3, GitHub 2018 — is at-least-once without idempotent consumers, or retries without jitter.
</Recap>
