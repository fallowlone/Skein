---
slug: 03-retry-strategies
lang: en
track: backend
unit: 05-idempotency-retries
order: 3
title: "Retry strategies: backoff, jitter, and thundering herd"
summary: "Exponential backoff without jitter creates synchronized retry storms. Full jitter spreads retries across time. Retry-After and retry budgets prevent recovering services from being re-killed."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs:
  - backend/05-idempotency-retries/01-why-idempotency
deepensInto:
  - backend/05-idempotency-retries/06-idempotency-senior
spiral: ["latency"]
concepts: ["exponential-backoff", "full-jitter", "decorrelated-jitter", "thundering-herd", "retry-after", "retry-budget", "retry-condition"]
sources:
  - https://www.codereliant.io/p/retries-backoff-jitter
  - https://en.wikipedia.org/wiki/Thundering_herd_problem
  - https://stripe.com/blog/idempotency
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A service returns 503. One thousand clients all schedule a retry for exactly 2 seconds later. At T+2s, 1000 requests hit the recovering server simultaneously — the same spike that broke it the first time. The retry mechanism just made the outage worse.
</Hook>

<Crux>Backoff without jitter is a thundering-herd trap. Jitter spreads retries; Retry-After and retry budgets stop re-killing recovering services.</Crux>

<Explanation>

## Exponential backoff: the base formula

The minimum viable retry loop waits longer before each successive attempt:

```
delay = min(cap, base × 2^attempt)
```

`base` is typically 100–500 ms. `cap` is typically 30–60 s. Attempts cap at 3–6 depending on operation criticality.

With `base = 200ms` and `cap = 30s`:
- Attempt 1 → wait 200 ms
- Attempt 2 → wait 400 ms
- Attempt 3 → wait 800 ms
- Attempt 4 → wait 1 600 ms
- ...
- Attempt 8 → wait 30 s (capped)

**Problem:** if 1,000 clients fail at the same instant, they all pick the same delay and retry at the exact same time — the synchronized spike is the thundering herd.

## Jitter: spreading retries across time

Three jitter variants (named by the AWS Architecture Blog):

**Full jitter** (recommended default):
```
delay = random(0, min(cap, base × 2^attempt))
```
Picks a random value from 0 to the exponential cap. Smoothest distribution. Simplest to implement.

**Equal jitter:**
```
delay = (base × 2^attempt)/2 + random(0, (base × 2^attempt)/2)
```
Half deterministic, half random. Harder to reason about.

**Decorrelated jitter** (used by AWS SDK v3):
```
delay = min(cap, random(base, prev_delay × 3))
```
Each retry's window depends on the previous delay. Grows organically. Empirically similar load profile to full jitter.

Avoid plain exponential backoff without jitter — it is a textbook thundering-herd trap.

<div data-lesson-visual class="overflow-x-auto my-6">

| Jitter type | Formula | Used by |
|---|---|---|
| Full | `random(0, min(cap, base × 2^n))` | Most SDKs |
| Equal | `half_fixed + random(0, half_fixed)` | Some Java libs |
| Decorrelated | `random(base, prev × 3)` capped | AWS SDK v3 |
| None | `min(cap, base × 2^n)` | Thundering herd |

</div>

## What to retry — and what not to

Retry conditions matter as much as retry timing:

**Retry:** 5xx errors, network timeouts, connection refused, 429 Too Many Requests (with `Retry-After`).

**Do not retry:** 4xx errors (except 408 and 429) — the client's request is wrong and retrying produces the same 4xx. 410 Gone — the resource is permanently absent.

Many libraries naively retry every non-2xx, generating retry storms for permanent client bugs. Explicitly enumerate the retry condition in your configuration.

## Retry-After: the server's backoff hint

When a server returns 429 or 503, it can include `Retry-After: 30` (seconds) or an absolute date. A well-behaved client honors this before applying its own backoff. gRPC uses the `grpc-retry-pushback-ms` trailer for the same purpose.

A client that ignores `Retry-After` and applies its own short backoff is the thundering-herd pattern in action — the server asked for breathing room and the client refused.

## Retry budget: capping total retries across the call site

Individual retry caps (3–6 attempts) are not enough at scale. A **retry budget** is a token-bucket of allowed retries per second across the entire call site. When the bucket empties, the client opens a circuit and returns a deterministic error immediately — it stops hammering the downstream.

Without a retry budget, a service outage causes every client to burn through its 6-attempt budget simultaneously, amplifying traffic by 6× and turning a 30-second blip into a minutes-long outage.

<Inset kind="why" lang="en">
Netflix and Google published tail-latency papers showing uncapped retries cascade: a slow downstream causes retries, retries increase load on the downstream, increased load creates more slow responses, which create more retries. A retry budget breaks this loop by making the retry rate a hard limit, not a per-request policy.
</Inset>

<Quiz
  id="retry-strat-q1"
  lessonSlug="03-retry-strategies"
  lang="en"
  question="Why is jitter added on top of exponential backoff?"
  choices={[
    { label: "It makes failures faster to detect", misconception: "Jitter does not affect detection latency — it affects the distribution of retry traffic across time." },
    { label: "It spreads synchronized retry storms across time so the recovering service is not hit by a thundering herd", correct: true },
    { label: "It tests the server's clock skew tolerance", misconception: "Backoff is a client behavior. The server is not involved in the delay calculation." },
    { label: "It satisfies RFC requirements for idempotent endpoints", misconception: "No RFC mandates jitter. It is an operational best practice that emerged from production experience." },
  ]}
/>

<Quiz
  id="retry-strat-q2"
  lessonSlug="03-retry-strategies"
  lang="en"
  question="A server returns 503 with Retry-After: 30. Three clients: A honors Retry-After, B ignores it and uses its own 1s backoff, C does not retry at all. Which client behaves correctly?"
  choices={[
    { label: "C — failing fast is the only safe option", misconception: "Failing fast loses transient recoveries. The user never gets the chance to succeed after a brief outage." },
    { label: "B — its own backoff is more predictable", misconception: "Ignoring Retry-After is exactly the thundering-herd anti-pattern. The server is asking for breathing room." },
    { label: "A — honors the server's backoff hint, falls back to client backoff only after the indicated wait", correct: true },
    { label: "All three are equivalent under load", misconception: "Retry-After is a load-management signal. Honoring it materially reduces load on the recovering service." },
  ]}
/>

<DragOrder
  id="retry-strat-drag1"
  lessonSlug="03-retry-strategies"
  lang="en"
  prompt="Put the thundering herd scenario steps in order (no jitter, 1000 clients):"
  items={[
    "All 1000 clients POST to the server; the downstream is overloaded and returns 503",
    "All 1000 clients schedule a retry at exactly T + 1s (no jitter)",
    "At T + 1s, all 1000 retry simultaneously — the same spike that broke the server",
    "Server returns 503 again; all 1000 schedule for T + 2s",
    "The pattern repeats indefinitely, preventing recovery",
  ]}
/>

</Explanation>

<KeyTakeaway>Retry with full jitter: `random(0, min(cap, base × 2^n))`. Honor `Retry-After`. Only retry 5xx and timeouts — never 4xx. Cap total attempts (3–6) and enforce a retry budget across the call site to avoid amplifying outages.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="retry-strat-retrieval"
  lang="en"
  questions={[
    { q: "Explain why a retry loop without jitter is more dangerous than the original failure it is trying to recover from.", a: "Synchronized retries from N clients all hit the recovering service at the same instant, multiplying load by N. The service that broke under 1× load is now hit by N× load on each retry tick — guaranteed to fail again. Without jitter the pattern repeats indefinitely and what should have been a 30-second blip becomes a 30-minute outage. Jitter spreads retries across time so the recovering service sees load smoothly instead of in synchronized spikes." },
    { q: "What is the full jitter formula and what does each variable mean?", a: "delay = random(0, min(cap, base × 2^attempt)). base is the minimum wait (100–500ms). cap is the maximum wait (30–60s). attempt is the zero-indexed attempt number. random(0, X) picks uniformly in [0, X], spreading retries across the full exponential window. The min(cap, ...) prevents waits from growing unboundedly past the cap." },
    { q: "What is a retry budget and why is an individual attempt cap of 6 not enough?", a: "A retry budget is a token-bucket that limits the total number of retries per second across all callers at a call site. An individual cap of 6 per request is insufficient because 10,000 concurrent requests each burning through 6 attempts becomes 60,000 total retries in a short window — enough to re-kill a recovering service. The budget imposes a fleet-wide ceiling and, when exhausted, opens a circuit breaker so all callers fail fast rather than amplifying the problem." },
  ]}
/>

<Recap lang="en">
Exponential backoff — `min(cap, base × 2^n)` — grows the wait between retries, but without jitter it synchronizes all clients to the same moment and causes thundering herds. Full jitter adds `random(0, ...)` to spread retries uniformly across the window. The AWS SDK v3 uses decorrelated jitter, which behaves similarly in practice. Retry only 5xx and timeouts, not 4xx. Honor the `Retry-After` header from 429/503 responses. Cap attempts at 3–6 per request, and enforce a retry budget (token-bucket) across the call site to prevent a failing downstream from being re-killed on every retry cycle.
</Recap>
