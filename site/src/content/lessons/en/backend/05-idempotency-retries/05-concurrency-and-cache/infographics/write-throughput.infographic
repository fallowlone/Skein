infographic chart-column-simple
data
  title Write throughput per node (thousands/sec)
  desc One Postgres primary saturates near 10k writes/sec; one Redis Cluster node sustains ~100k SETNX/sec — so 50k req/sec needs the sharded cache
  values
    - label Postgres single primary
      value 10
    - label Redis Cluster node
      value 100
theme
  palette #6366f1 #14b8a6
