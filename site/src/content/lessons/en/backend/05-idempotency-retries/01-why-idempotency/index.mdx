---
slug: 01-why-idempotency
lang: en
track: backend
unit: 05-idempotency-retries
order: 1
title: "Why idempotency: making retries safe"
summary: "A network can swallow the server's response. Without an idempotency key the client has no way to retry safely — every duplicate POST may fire the side effect twice."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs:
  - backend/03-async-blocking
  - backend/04-pooling
deepensInto:
  - backend/05-idempotency-retries/02-server-state-machine
spiral: ["statefulness"]
concepts: ["idempotency", "idempotency-key", "at-least-once", "at-most-once", "retry-safe"]
sources:
  - https://stripe.com/blog/idempotency
  - https://docs.stripe.com/api/idempotent_requests
  - https://brandur.org/idempotency-keys
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
Bea hits "Pay Now" on a checkout page. Her wifi drops before the response arrives. She presses again. The server has no way to tell the second click from the first — so it charges her twice.
</Hook>

<Crux>How to make every retry-safe POST cost the user exactly one outcome, even when the network swallows the response.</Crux>

<Explanation>

## The network guarantee problem

The internet guarantees **at-least-once** delivery if the client retries until it sees an answer, and **at-most-once** if it never retries. Neither is acceptable on its own:

- **At-most-once** — a payment that never finished because the client gave up.
- **At-least-once without dedup** — a double charge.

The gap between them is the application's job to close. The tool is idempotency.

## What an idempotency key is

An **idempotency key** is a client-generated opaque string — typically a UUID v4 (36 characters) — sent in the `Idempotency-Key` HTTP request header. The client picks **one key per logical operation**, not per request. Every retry of the same operation reuses the exact same key.

The server uses the key as a primary token to answer: "have I already processed this?" If yes, it replays the original response without re-running the business logic. If no, it processes and records the result.

<div data-lesson-visual class="overflow-x-auto my-6">

| Scenario | Without key | With key |
|---|---|---|
| Response delivered | 1 charge | 1 charge |
| Response lost, client retries once | 2 charges | 1 charge (replay) |
| Response lost, client retries 3× | 4 charges | 1 charge (replay) |
| Key expires before retry | N/A | Treated as new — risk of duplicate |

</div>

## The metaphor: envelope + clipboard

Imagine sending money via courier. You mark the envelope **"Order #A7-payment"** and tell the courier: "If this envelope was already delivered, do not deliver another." On every retry you write the same order number on the new envelope. The recipient checks a clipboard (the idempotency cache) and either delivers the envelope or says "already done — here is the receipt."

The clipboard **remembers**. Without it, every envelope looks new.

## HTTP convention

Stripe popularized the `Idempotency-Key` header in 2014. Square, PayPal, Adyen, and the IETF draft `draft-ietf-httpapi-idempotency-key-header` all follow the same shape. RFC 9110 §9.2.2 lists which HTTP methods are idempotent by spec (GET, HEAD, PUT, DELETE, OPTIONS) — POST is not, which is exactly why the header exists.

<Quiz
  id="why-idem-q1"
  lessonSlug="01-why-idempotency"
  lang="en"
  question="What problem does an idempotency key solve?"
  choices={[
    { label: "Encrypting payment data in transit", misconception: "Encryption is TLS's job. Idempotency keys are about duplicate side effects on retry." },
    { label: "Making it safe to retry a POST without performing the side effect twice", correct: true },
    { label: "Replacing authentication tokens", misconception: "Authentication is a separate concern. The key identifies the operation, not the caller." },
    { label: "Speeding up the first request", misconception: "The goal is correctness on retry, not speed." },
  ]}
/>

<Quiz
  id="why-idem-q2"
  lessonSlug="01-why-idempotency"
  lang="en"
  question="A flaky network lost the response to a POST /charge. What is the safest thing the client can do?"
  choices={[
    { label: "Retry without any special marker", misconception: "Plain retries can create duplicate charges if the server already processed the first request." },
    { label: "Retry with the exact same Idempotency-Key as the first attempt", correct: true },
    { label: "Switch to GET to be safe", misconception: "GET cannot create a charge. Changing the method does not fix the duplicate-write problem." },
    { label: "Wait an hour and hope the server figured it out", misconception: "Waiting does nothing about whether the original write happened or not." },
  ]}
/>

<DragOrder
  id="why-idem-drag1"
  lessonSlug="01-why-idempotency"
  lang="en"
  prompt="Put the steps of a safe retry in order:"
  items={[
    "Client generates a unique Idempotency-Key (UUID v4) for the operation",
    "Client sends POST with the Idempotency-Key header",
    "Network or server fails before the client sees a response",
    "Client retries the same POST with the SAME Idempotency-Key",
    "Server recognizes the key and replays the cached response or finishes the original work",
    "Client gets one confirmation and stops retrying",
  ]}
/>

<MetaphorComplete
  id="why-idem-metaphor1"
  lessonSlug="01-why-idempotency"
  lang="en"
  setup="Fill in the blank: an idempotency key is like marking the envelope with an order number so the recipient checks a _______ before delivering it again."
  accepted={["clipboard", "ledger", "log", "list", "register", "record"]}
  canonical="clipboard (or ledger / log / list / register — any record of what has already been delivered)"
  explanation="The point is server-side memory: the recipient must check whether this exact marker was already seen and act accordingly. Without that memory, every envelope is treated as new."
/>

</Explanation>

<KeyTakeaway>An idempotency key lets a client retry any POST without the server doing the work twice. One key per logical operation — every retry reuses the same key, and the server replays the stored response.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="why-idem-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between at-least-once and at-most-once delivery, and why is neither acceptable alone for a payment API?", a: "At-most-once means the client never retries — so a lost response leaves the payment incomplete. At-least-once means the client retries until it gets an answer — but without deduplication, each retry may create a new charge. The idempotency key closes the gap: at-least-once delivery combined with server-side dedup gives exactly-once observable behavior." },
    { q: "Why is one key per logical operation the rule, not one key per HTTP request?", a: "Every retry of the same operation must carry the same key so the server can recognize the retry and replay the original response. If the client generated a new key for each HTTP attempt, the server would see each attempt as a brand-new operation and charge the customer again. The key is tied to the intent, not to the wire attempt." },
    { q: "Which HTTP methods are idempotent by RFC 9110 definition, and why does POST need the header?", a: "RFC 9110 §9.2.2 defines GET, HEAD, PUT, DELETE, and OPTIONS as idempotent — applying them multiple times produces the same state as applying them once. POST is not idempotent by spec because 'create a charge' is a new side effect each time. The Idempotency-Key header lets a POST behave as if it were idempotent by having the server deduplicate using the key." },
  ]}
/>

<Recap lang="en">
The internet cannot deliver a request exactly once — only at-most-once (no retry) or at-least-once (retry until acknowledged). A payment API needs exactly-once behavior, which requires both sides to cooperate: the client retries with the same Idempotency-Key header, and the server deduplicates using that key as a primary lookup token. Stripe popularized the `Idempotency-Key` HTTP header in 2014; IETF draft `draft-ietf-httpapi-idempotency-key-header` formalizes it. The next lesson covers the server-side state machine that makes this work.
</Recap>
