---
concepts:
- seven-stops
- accept-queue
- middleware-ordering
- backpressure
- keep-alive
- timeout-budget
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://nodejs.org/api/http.html
- https://nodejs.org/api/stream.html#backpressuring-in-streams
- https://www.barroso.org/publications/TheTailAtScale.pdf
status: ready
summary: "Free-recall prompts across the request lifecycle. Answer each in your own words first, then reveal the model answer and compare against the unit's mechanisms."
title: 'Request lifecycle: free-recall review'
track: backend
unit: 01-request-lifecycle
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before opening the model answer — the effort of reconstructing the mechanism is what makes it stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: the seven stops, the two kernel queues, why middleware order is a security boundary, the backpressure mechanism, keep-alive, and how timeouts must compose into a budget.
</Goal>

<RetrievalDrawer
  client:load
  id="01-request-lifecycle-short-retrieval"
  lang="en"
  questions={[
    { q: "Name the seven stops of the request lifecycle in order, and one failure that lives at each.", a: "1) Accept — the kernel completes the handshake and hands up a socket; the accept queue can overflow and refuse connections with no app log. 2) Parse — bytes become a request; oversized headers (a bloated cookie or JWT) trip the size cap and return 431/400. 3) Route — method and path matched to one handler; overlapping patterns or method confusion send a request to the wrong handler. 4) Middleware — cross-cutting layers run in order; the wrong order bypasses auth or a body-size limit. 5) Handle — business logic runs, mostly waiting on I/O; a slow downstream dominates p99. 6) Serialize — the result becomes bytes; JSON.stringify on a large object is synchronous CPU that blocks the loop. 7) Drain — the response is written to the socket; a slow client plus ignored backpressure buffers it in the heap. Total latency is the sum across all seven plus queueing between them." },
    { q: "What are the two kernel queues behind a listen() socket, what bounds each, and why is somaxconn = 128 a production trap?", a: "The SYN queue holds half-open connections mid-handshake (server sent SYN-ACK, awaiting the final ACK), bounded by net.ipv4.tcp_max_syn_backlog (~1024–2048). The accept queue holds fully established connections waiting for the app to call accept(), bounded by min(listen backlog, net.core.somaxconn). somaxconn defaulted to 128 on Linux for ~20 years (raised to 4096 in kernel 5.4), and many container images still ship 128. Even a framework requesting listen(511) (Node's default) is silently capped to 128, so under a burst 129 simultaneously pending connections begins dropping — clients see timeouts/resets and the application logs nothing because no code ran." },
    { q: "Why is middleware registration order a security boundary, and what is the ordering rule?", a: "Middleware wraps the handler like an onion; any layer that responds without calling next() short-circuits the request, so the handler never runs. Because of that, the order of registration decides what actually guards the handler: register a route before auth and the handler runs unauthenticated; put the body parser before a size limit and you buffer a huge upload before rejecting it; compress before auth and you waste CPU on requests that will 401. The rule: cheap and protective layers first (size limits, rate limits, auth), expensive and optional layers last (body parsing, compression), and the error handler outermost so it wraps everything downstream." },
    { q: "Trace exactly how a slow client causes server-side OOM during a streamed response, layer by layer.", a: "The client app reads slowly, so its TCP receive buffer fills and it advertises a smaller receive window. The server's kernel send buffer can no longer drain into that shrinking window, so it fills. The userland writable stream can no longer push into the kernel buffer, so its own buffer fills and crosses the high-water mark (Node default 16 KB for byte streams), making res.write() return false. That false is a flow-control signal, not an error — the data was accepted. If the app honours it (pauses production until 'drain', or uses pipeline()), memory stays flat at about the high-water mark. If it ignores false and keeps writing, the gap between production rate and the client's read rate accumulates in the one layer with no flow control — the process heap — until the kernel OOM-kills it." },
    { q: "What does serialization cost the request, and why does the status code function as a contract rather than decoration?", a: "Turning the result object into bytes is CPU the request always pays. JSON.stringify is synchronous and scales with object size, so a large array (say 5,000 rows) costs several ms and blocks the event loop for every other request; precompiled schema serializers (fast-json-stringify, Protobuf) run 2–4× faster, and pagination is a serialization-cost control, not just UX. The status code is part of the API contract: 201 created, 204 no body, 4xx the client must change the request, 5xx a server fault safe to retry for idempotent calls. Clients, proxies, retries, and alerts all key off the status, so returning 200 with an error object inside hides failures from every layer that monitors them." },
    { q: "What does keep-alive optimize, and why must per-hop timeouts compose into a propagated deadline?", a: "Keep-alive reuses one TCP/TLS connection for many requests so the handshake — one or more round trips — is amortized instead of paid per request; after a response drains, the connection sits in an idle pool until a keep-alive timeout closes it, which is why the response is only done when bytes drain and the connection is correctly kept or closed. Timeouts are separate but equally a lifecycle concern: every outbound call needs one ('no timeout' is the worst default, letting one hung dependency exhaust pools), but independent per-hop timeouts do not compose — a 1 s SLA request calling A (1 s) calling B (1 s) lets A keep working after the client gave up. The fix is a propagated deadline: the entry point allocates a budget and each hop uses min(local default, remaining), the model gRPC standardizes. This matters because at scale the tail dominates — fan out to N services and you are slow if any is slow, 1 − (1 − p)^N ≈ 63% at p=1%, N=100 — so deadlines plus hedging at p95 defend the tail." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: seven stops whose latencies sum plus queueing; two kernel queues where somaxconn silently caps the accept depth; middleware order as a security boundary; backpressure as the userland surface of TCP flow control that OOMs when ignored; serialization as real synchronous CPU and the status code as a contract; and timeouts that must compose into a propagated deadline because the fan-out tail, not the average, sets user-perceived latency.
</Recap>
