infographic chart-bar-plain-text
data
  title Latency is cumulative, not located
  desc A 5 ms handler still yields a 200 ms response — the other 195 ms is queueing plus the six other stops
  values
    - label Handler (your code)
      value 5
    - label Queueing + the other six stops
      value 195
    - label Total response the user feels
      value 200
theme
  palette #14b8a6 #f59e0b #f43f5e
