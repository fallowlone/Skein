---
concepts:
- request-lifecycle
- reverse-proxy
- middleware
- handler
- response-pipeline
deepensInto:
- backend/01-request-lifecycle/02-accept-and-parse
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://nginx.org/en/docs/http/ngx_http_core_module.html
- https://nodejs.org/api/http.html
- https://www.barroso.org/publications/TheTailAtScale.pdf
spiral:
- latency
status: ready
summary: Every HTTP request makes the same trip — accept, parse, route, middleware,
  handle, serialize, drain. Knowing the map tells you where latency, failure, and
  security each hide.
title: 'The journey of a request: seven stops from socket to response'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyCumulative from "./infographics/latency-cumulative.svg?raw";

<Hook>
A user clicks "Save". 180 ms later they see a green checkmark. In those 180 ms the request crossed a load balancer, sat in a kernel queue, was parsed byte by byte, matched against a route table, walked a stack of middleware, ran your handler, was serialized to JSON, and streamed back out — passing through seven distinct stages, each with its own failure mode. Most engineers can name the handler. Few can name the other six.
</Hook>

<Crux>An HTTP request makes the same seven stops every time — and latency, failure, and security each hide at a different stop.</Crux>

<Explanation>

## The seven stops

Strip away the framework and every server-side request follows one shape. The names differ between Express, Spring, Go's net/http, and Rails, but the stages do not:

1. **Accept** — the kernel completes the TCP handshake and hands your process a connected socket.
2. **Parse** — bytes off the socket are turned into a request line, headers, and a body.
3. **Route** — the method and path are matched to one handler.
4. **Middleware** — cross-cutting layers run in order: auth, logging, body parsing, rate limiting.
5. **Handle** — your business logic runs, usually waiting on a database or another service.
6. **Serialize** — the result becomes bytes: JSON, HTML, a stream.
7. **Drain** — the response is written to the socket, which may be slower than your handler, and the connection is kept alive or closed.

Together these seven stops form a chain: skip or misconfigure any one of them and you get a failure that looks mysterious until you know the map. The next ten minutes will make that map automatic.

Before stop 1, a **reverse proxy** (nginx, Envoy, a cloud load balancer) usually terminates TLS, picks a backend, and may buffer the whole request. That proxy is its own lifecycle — it has the same seven stops on the other side.

## Where each problem hides

The reason the map matters is that each class of bug lives at a specific stop. Debugging blind means checking all seven; debugging with the map means going straight to one.

<div data-lesson-visual class="overflow-x-auto my-6">

| Symptom | The stop to suspect |
|---|---|
| Connections refused under load | Accept — the kernel accept queue overflowed |
| 431 / 400 on large cookies | Parse — header buffer exceeded |
| Wrong handler runs | Route — overlapping path patterns |
| Auth bypass on one endpoint | Middleware — ordering: handler ran before the auth layer |
| p50 fast, p99 terrible | Handle — a slow downstream dependency |
| High CPU on big responses | Serialize — `JSON.stringify` on a huge object |
| Memory climbs with slow clients | Drain — backpressure ignored, response buffered in RAM |

</div>

## Latency is cumulative, not located

A request's total time is the **sum** of every stop, plus the time it spends waiting in queues between them. A handler that runs in 5 ms can still produce a 200 ms response if the accept queue was deep, the body was buffered by the proxy, and the client read the response slowly. This is why "the code is fast" and "the endpoint is slow" are both true at once — they describe different stops.

<Infographic
  svg={latencyCumulative}
  label="Horizontal bar chart of one request's timing: the handler is 5 ms, queueing plus the six other stops add 195 ms, and the total response the user feels is 200 ms."
  caption="The handler is a sliver of the response; latency is the sum across all seven stops plus queueing, which is why fast code can still feel slow."
  hue="var(--d-backend)"
/>

The senior skill is not memorizing the seven names. It is forming the reflex: *given this symptom, which stop?* When you see a p99 spike or a connection flood, your first question should be "which stop is this?" — not "what does the code do?" The rest of this unit walks each stop in production detail.

<Quiz
  id="lifecycle-overview-q1"
  lessonSlug="01-overview"
  lang="en"
  question="A service has a 3 ms handler but users report 250 ms responses under load. Which stops are the most likely culprits?"
  choices={[
    { label: "Only the handler — the measurement must be wrong", misconception: "The handler is one of seven stops. A fast handler does not make a fast response; queueing and draining happen outside it." },
    { label: "Accept (queue depth) and Drain (slow client / backpressure), since they add time outside the handler", correct: true },
    { label: "Routing — matching a path is the expensive part", misconception: "Route matching is microseconds even with hundreds of routes. It almost never dominates a 250 ms response." },
    { label: "Serialization — turning a small object into JSON takes 250 ms", misconception: "Serializing a small response is sub-millisecond. Only very large objects make serialization visible." },
  ]}
/>

<Quiz
  id="lifecycle-overview-q2"
  lessonSlug="01-overview"
  lang="en"
  question="Why does a reverse proxy in front of your app server matter to the request lifecycle?"
  choices={[
    { label: "It replaces the need for the seven stops in your app", misconception: "The proxy has its own lifecycle. Your app still accepts, parses, routes, and so on — the proxy sits in front of all of it." },
    { label: "It often terminates TLS and may buffer the entire request before your app sees a single byte", correct: true },
    { label: "It runs your business logic so the app server stays idle", misconception: "A reverse proxy forwards requests; it does not run your handlers. Business logic stays in the app server." },
    { label: "It guarantees the handler never has to wait on a database", misconception: "A proxy has no effect on how long your handler waits on a database — that is downstream of the proxy entirely." },
  ]}
/>

<DragOrder
  id="lifecycle-overview-drag1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Put the seven stops of a request in the order they happen:"
  items={[
    "Accept — kernel hands the process a connected socket",
    "Parse — bytes become request line, headers, body",
    "Route — method + path matched to a handler",
    "Middleware — auth, logging, body parsing run in order",
    "Handle — business logic runs, waits on dependencies",
    "Serialize — result becomes bytes",
    "Drain — response written to socket, connection kept or closed",
  ]}
/>

</Explanation>

<StackDiagram
  label="The seven stops of a request, top to bottom, and the bug that hides at each"
  hue="var(--d-backend)"
  layers={[
    { label: "1. Accept", note: "kernel queue overflow → refused" },
    { label: "2. Parse", note: "oversized headers → 431/400" },
    { label: "3. Route", note: "overlapping paths → wrong handler" },
    { label: "4. Middleware", note: "wrong order → auth bypass" },
    { label: "5. Handle", note: "slow dependency → p99 spike" },
    { label: "6. Serialize", note: "big object → CPU on the loop" },
    { label: "7. Drain", note: "ignored backpressure → memory" },
  ]}
  caption="Every request makes the same seven stops in order; total latency is their sum plus queueing, and each bug class lives at exactly one stop."
/>

<KeyTakeaway>Every request makes seven stops: accept, parse, route, middleware, handle, serialize, drain. Latency is the sum across all of them plus queueing — so a fast handler can still produce a slow response. Each bug class lives at one stop; the map turns blind debugging into a single jump.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="lifecycle-overview-retrieval"
  lang="en"
  questions={[
    { q: "Name the seven stops of the request lifecycle in order, and one thing that can go wrong at each.", a: "1) Accept — kernel completes the handshake; accept queue can overflow and refuse connections. 2) Parse — bytes become a request; oversized headers trip the buffer limit (431/400). 3) Route — path matched to a handler; overlapping patterns send a request to the wrong handler. 4) Middleware — cross-cutting layers run in order; wrong order can bypass auth. 5) Handle — business logic runs; a slow downstream dependency dominates p99. 6) Serialize — result becomes bytes; large objects make JSON.stringify a CPU cost. 7) Drain — response written to the socket; a slow client plus ignored backpressure buffers the response in memory." },
    { q: "Why can a service have a 3 ms handler but a 250 ms response time?", a: "Total response time is the sum of all seven stops plus the time spent queueing between them — not just the handler. Under load, a request can wait in the kernel accept queue before parsing even begins, the proxy may buffer the body, and the client may read the response slowly while backpressure stalls the drain. All of that is outside the handler, so 'the code is fast' and 'the endpoint is slow' can both be true." },
    { q: "What does a reverse proxy add in front of the seven stops, and why does it have its own lifecycle?", a: "A reverse proxy (nginx, Envoy, a cloud load balancer) usually terminates TLS, selects a backend, and may buffer the entire request or response. It forwards requests but does not run your handlers. It has the same accept/parse/route/forward/drain shape on its own side, so a single user request actually passes through two lifecycles — the proxy's and the app server's — and latency or failure can hide in either." },
  ]}
/>

<Recap lang="en">
Every server-side HTTP request follows the same seven stops: accept (kernel hands over the socket), parse (bytes become a request), route (path to handler), middleware (cross-cutting layers in order), handle (business logic), serialize (result to bytes), and drain (write to socket, keep-alive or close). A reverse proxy in front adds its own copy of this lifecycle. Total latency is the sum across stops plus queueing, which is why a fast handler can still yield a slow endpoint. Each class of bug lives at one specific stop — the value of the map is the reflex of jumping straight to the right one. Now when you see a service that is "slow but the code is fine," your first move is to name the stop, not read the handler. The next lesson zooms into the first two stops: how the kernel accepts a connection and how raw bytes become a parsed request.
</Recap>
