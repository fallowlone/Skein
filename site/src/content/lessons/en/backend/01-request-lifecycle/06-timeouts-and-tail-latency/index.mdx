---
slug: 06-timeouts-and-tail-latency
lang: en
track: backend
unit: 01-request-lifecycle
order: 6
title: "Timeouts and tail latency: budgets, deadlines, and the fan-out trap"
summary: "Every hop needs a timeout, and timeouts must compose into a request-wide budget. At scale the tail dominates: fan out to enough services and the slowest one decides almost every user's latency."
estMin: 18
status: ready
lessonType: topic
level: senior
prereqs:
  - backend/01-request-lifecycle/05-streaming-and-backpressure
deepensInto:
  - backend/06-circuit-breakers/01-overview
  - backend/08-putting-it-together/01-overview
spiral: ["latency"]
concepts: ["timeout", "deadline-propagation", "timeout-budget", "tail-latency", "fan-out", "p99", "hedged-requests"]
sources:
  - https://www.barroso.org/publications/TheTailAtScale.pdf
  - https://grpc.io/docs/guides/deadlines/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Each service in the chain is healthy: every dependency answers in 10 ms at the 99th percentile. The product page fans out to 100 of them in parallel and waits for all. Yet 63% of page loads take over a second. No single service is slow. The math of the tail is slow — and most engineers' intuition about averages hides it completely.
</Hook>

<Crux>Every hop needs a timeout that composes into a request budget — and past enough fan-out, the slowest dependency sets everyone's latency.</Crux>

<Explanation>

## A timeout on every hop, or a hang on every incident

A network call without a timeout is a bug waiting for an incident. When a dependency stops responding (not refuses — *hangs*), a call with no timeout waits forever, holding a thread or connection. Enough such calls exhaust the pool, and a single slow dependency takes down a healthy service. Every outbound call — DB query, cache, HTTP, RPC — needs an explicit timeout. The default in most clients is *no timeout*, which is the worst default in backend engineering.

## Timeouts must compose into a budget

Per-hop timeouts set in isolation lie. If a request has a 1 s SLA but calls service A (timeout 1 s) which calls service B (timeout 1 s), then when B is slow, A waits the full second and the client has already given up — A is now doing work nobody is waiting for. The fix is a **timeout budget** (a deadline): the entry point allocates a total, and each hop passes the *remaining* time downward. gRPC formalizes this as a **deadline** propagated in metadata; every service computes its local timeout as `min(local default, remaining budget)`.

<div data-lesson-visual class="overflow-x-auto my-6">

| Approach | What each hop uses | Failure mode |
|---|---|---|
| No timeouts | ∞ | One hung dependency exhausts pools, cascades |
| Independent per-hop timeouts | A fixed local value | Inner work outlives the caller's patience |
| Propagated deadline (budget) | min(local, remaining) | Bounded; inner hops stop when the budget is spent |

</div>

## Why the tail, not the average, is the SLA

Users do not experience your average. They experience their *own* request, and the slow ones are what they remember and what trips alerts. So latency is reported as percentiles: **p50** (median), **p99** (1 in 100 is worse), **p99.9**. The gap between p50 and p99 is "tail latency," caused by GC pauses, queueing, cache misses, lock contention, and retries.

The danger is **tail amplification under fan-out**. If one request to a service is slow with probability *p*, then a request that fans out to *N* services in parallel and waits for all is slow if *any* one is slow — probability `1 − (1 − p)^N`. With a per-service p99 (p = 1%) and N = 100, that is `1 − 0.99^100 ≈ 63%`. This is the Hook's number, straight from Dean and Barroso's *The Tail at Scale*: a service fanning out to 2,000 leaves about 20% of requests over a second even when each backend's p99 is fine.

## Defending the tail: hedging, not just timeouts

A timeout caps the worst case but does not improve the typical tail. The technique from *The Tail at Scale* is **hedged requests**: send the request, and if no answer arrives by the p95 latency, send a *second* copy to another replica and take whichever returns first. Because only the slow ~5% get hedged, the extra load is small (~5%) while the tail collapses — in Google's measurements, sending a hedge after a 10 ms delay cut p99.9 from 1,800 ms to 74 ms at the cost of ~2% more requests. **Tied requests** go further: the duplicates tell each other to cancel once one starts executing, trimming wasted work.

<Inset kind="why" lang="en">
Why not just retry on timeout instead of hedging? A retry fires only *after* you have already paid the full timeout — so it improves availability but not latency, and naive retries amplify load exactly when a service is already struggling (the retry storm). Hedging fires speculatively at p95, before the timeout, so it attacks latency directly; and because it is capped to the slow tail it adds bounded load. The two are complementary: hedge to cut the tail, retry with backoff and jitter to survive failures, and a circuit breaker (next unit) to stop both when the dependency is truly down.
</Inset>

<Quiz
  id="timeouts-tail-q1"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="en"
  question="A page fans out to 100 independent services in parallel and waits for all. Each has a p99 of 10 ms (1% chance a call exceeds it). Roughly what fraction of page loads exceed 10 ms on at least one call?"
  choices={[
    { label: "About 1% — the same as a single service", misconception: "That would be true for one call. With 100 independent calls, the chance that none is slow is 0.99^100, so the slow fraction is far higher." },
    { label: "About 63% — 1 − 0.99^100", correct: true },
    { label: "About 10% — it scales linearly with the percentile", misconception: "Tail amplification is not linear. The compounding 1 − (1 − p)^N grows much faster than a linear estimate." },
    { label: "0% — parallel calls cannot all be slow at once", misconception: "You wait for ALL of them, so you are slow if ANY one is slow — not only if all are. That is what amplifies the tail." },
  ]}
/>

<Quiz
  id="timeouts-tail-q2"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="en"
  question="Why do independent per-hop timeouts fail to protect a request with an overall SLA?"
  choices={[
    { label: "Per-hop timeouts are always too short", misconception: "Length is not the issue; composition is. Even well-chosen local timeouts can sum past the SLA without a shared budget." },
    { label: "Inner hops keep working after the caller has already given up, because nothing propagates the remaining budget", correct: true },
    { label: "Timeouts cannot be set on database calls", misconception: "Database clients support timeouts. The problem is that isolated timeouts do not compose into a deadline." },
    { label: "The kernel ignores application timeouts", misconception: "Application timeouts are honored. The failure is architectural — no deadline is propagated down the call chain." },
  ]}
/>

<Quiz
  id="timeouts-tail-q3"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="en"
  question="How does a hedged request reduce tail latency without large extra load?"
  choices={[
    { label: "It retries every request twice from the start", misconception: "That would roughly double load. Hedging sends a second copy only for the slow minority, not for every request." },
    { label: "It sends a second copy only if no response arrives by ~p95, so only the slow ~5% are duplicated", correct: true },
    { label: "It lowers every service's p50", misconception: "Hedging targets the tail, not the median. The typical fast request never triggers a hedge." },
    { label: "It disables timeouts entirely", misconception: "Hedging works alongside timeouts; it does not replace them. The timeout still caps the absolute worst case." },
  ]}
/>

</Explanation>

<KeyTakeaway>Put an explicit timeout on every outbound call — "no timeout" is the worst default. Compose them into a propagated deadline so inner hops stop when the budget is spent. Track p99/p99.9, not the average: fan out to N services and you are slow if any is slow (1 − (1−p)^N ≈ 63% at p=1%, N=100). Hedge slow requests at p95 to collapse the tail for ~5% extra load.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="timeouts-tail-retrieval"
  lang="en"
  questions={[
    { q: "Why does every outbound call need an explicit timeout, and why is a propagated deadline better than independent per-hop timeouts?", a: "A call with no timeout waits forever when a dependency hangs (rather than refuses), holding a thread or connection; enough hung calls exhaust the pool and a single slow dependency cascades into an outage — and 'no timeout' is the default in most clients. Independent per-hop timeouts still fail because they do not compose: if a 1 s SLA request calls A (1 s) which calls B (1 s), then when B is slow A keeps working after the client has already given up. A propagated deadline (a budget) fixes this: the entry point allocates a total and each hop passes the remaining time down, computing its local timeout as min(local default, remaining). gRPC formalizes this as a deadline carried in metadata." },
    { q: "Explain tail amplification under fan-out with the math and the canonical numbers.", a: "If a single call to a service is slow with probability p, a request that fans out to N services in parallel and waits for all of them is slow if any one is slow: probability 1 − (1 − p)^N. With a per-service p99 (p = 1%) and N = 100, that is 1 − 0.99^100 ≈ 63% of requests slow — even though every backend's own p99 is fine. From Dean and Barroso's The Tail at Scale, a service fanning out to ~2,000 backends leaves roughly 20% of requests over a second. The lesson: at scale the tail, not the average, sets user-perceived latency, so you must measure p99/p99.9 and design for the slowest dependency." },
    { q: "What are hedged requests and tied requests, and why are they preferred over retries for cutting the tail?", a: "A hedged request sends the request, and if no answer arrives by the p95 latency, sends a second copy to another replica and takes whichever returns first. Because only the slow ~5% trigger a hedge, extra load is bounded (~5%) while the tail collapses — Google measured a 10 ms-delayed hedge cutting p99.9 from 1,800 ms to 74 ms for ~2% more requests. Tied requests go further: the duplicates cancel each other once one begins executing, reducing wasted work. They beat retries for latency because a retry only fires after the full timeout is already paid (helping availability, not latency) and naive retries amplify load during trouble; hedging fires speculatively before the timeout and is capped to the tail. They are complementary: hedge for latency, retry with backoff and jitter for failures, and a circuit breaker to stop both when a dependency is down." },
  ]}
/>

<Recap lang="en">
The last stop turns a request from "it returns" into "it returns in time." Every outbound call needs an explicit timeout, because the default of waiting forever lets one hung dependency exhaust pools and cascade. But isolated timeouts do not compose, so they must roll up into a propagated deadline where each hop uses min(local, remaining) — the model gRPC standardizes. At scale, the average is a lie: users feel their own request, so you track p99/p99.9, and fan-out amplifies the tail brutally — 1 − (1 − p)^N reaches 63% slow at p=1%, N=100. Timeouts cap the worst case but do not fix the typical tail; hedged and tied requests, fired at p95, collapse it for a few percent extra load. This is the bridge to resilience: when a dependency is not just slow but failing, timeouts and hedging are not enough, and the next unit's circuit breakers and bulkheads take over.
</Recap>
