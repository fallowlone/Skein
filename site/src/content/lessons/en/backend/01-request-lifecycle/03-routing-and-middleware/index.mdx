---
concepts:
- routing
- radix-trie
- middleware-chain
- ordering
- short-circuit
- cross-cutting-concerns
deepensInto:
- backend/01-request-lifecycle/04-handler-and-response
- backend/02-middleware-di/01-overview
estMin: 13
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- backend/01-request-lifecycle/02-accept-and-parse
slug: 03-routing-and-middleware
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://github.com/julienschmidt/httprouter
- https://nginx.org/en/docs/http/ngx_http_core_module.html
spiral:
- latency
status: ready
summary: Routing maps one request to one handler; the middleware chain decides what
  runs around it. Order is not cosmetic — it determines whether auth, body limits,
  and compression actually protect you.
title: 'Routing and middleware: choosing what runs, and in what order'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import routingMatchCost from "./infographics/routing-match-cost.svg?raw";

<Hook>
A security review finds that one admin endpoint accepts unauthenticated requests. The auth middleware exists, it is well-tested, and it is applied — but it was registered *after* the route in question. The handler ran first. Nobody wrote a bug; someone wrote the lines in the wrong order.
</Hook>

<Crux>Routing picks one handler; middleware order picks what protects it — and the wrong order silently disables auth, limits, and logging.</Crux>

<Explanation>

## Routing: matching a path to a handler

Routing turns `POST /users/42/orders` into a single handler plus extracted params (`userId=42`). How it matches determines its cost:

- **Linear scan** — try each registered route in order. Simple, but O(n) in route count; fine for dozens of routes.
- **Radix trie** (httprouter, Echo, Fastify's find-my-way) — a prefix tree over path segments. O(length of path), independent of route count, so 10 routes and 10,000 routes match in the same time.

In practice routing is microseconds either way; it almost never dominates latency. The real routing bugs are **correctness**: overlapping patterns (`/users/:id` vs `/users/me`), trailing-slash mismatches, and method confusion (a `GET` route shadowing a `POST` on the same path).

<Infographic
  svg={routingMatchCost}
  label="Side-by-side comparison of two routers: a linear scan matches in O(n) of the route count and degrades as routes accumulate, while a radix trie matches in O(path length), independent of route count, so 10 and 10,000 routes match in the same time."
  caption="Both routers match in microseconds, but only the radix trie stays flat as the route table grows — its cost tracks the URL, not the number of routes."
  hue="var(--d-infra)"
/>

## Middleware: the chain around the handler

Middleware are functions that run before and/or after the handler, each able to read the request, modify it, short-circuit it, or pass control onward. They handle **cross-cutting concerns** — things every endpoint needs but no endpoint should re-implement: authentication, request logging, body parsing, rate limiting, compression, error handling.

The model is an onion. A request travels *inward* through each layer to the handler, then the response travels *outward* through the same layers in reverse:

```
request →  logging → auth → bodyParser → handler
response ← logging ← auth ← bodyParser ← handler
```

A layer that calls `next()` passes control inward. A layer that responds *without* calling `next()` **short-circuits** — the handler never runs. That is exactly how a rate limiter returns 429 or an auth layer returns 401 before any business logic executes.

## Order is a security boundary

Because each layer can short-circuit, the *order* of registration decides what actually protects the handler:

<div data-lesson-visual class="overflow-x-auto my-6">

| Wrong order | Consequence |
|---|---|
| Route registered before auth middleware | Handler runs unauthenticated |
| Body parser before a body-size limit | Server buffers a 2 GB upload before rejecting it |
| Compression before auth | Compressed error responses leak; CPU spent on requests that 401 |
| Error handler registered first | It never catches anything — errors propagate past it |
| Rate limiter after expensive work | You do the work, then decline to charge for it |

</div>

The rule that prevents all five: **cheap and protective layers go first** (size limits, rate limits, auth), **expensive and optional layers go last** (body parsing, compression), and the **error handler goes outermost** so it wraps everything. When you look at a middleware chain and something feels off, ask yourself: "could this layer run before the request has been authenticated?" — that single question catches most ordering bugs before they reach production.

## Where time actually goes

Middleware is also where invisible latency accumulates. Each layer runs on every request. A body parser that JSON-parses a 1 MB payload, a logging layer that does a synchronous DNS lookup, an auth layer that calls a remote token service without caching — these add milliseconds to *every* request, not just the slow ones. Profiling a slow endpoint means timing the chain, not just the handler.

<Quiz
  id="routing-mw-q1"
  lessonSlug="03-routing-and-middleware"
  lang="en"
  question="An admin endpoint is reachable without authentication, even though auth middleware is implemented and tested. What is the most likely cause?"
  choices={[
    { label: "The auth token format changed", misconception: "A token-format issue would reject valid users, not allow unauthenticated ones. This is an ordering problem, not a token problem." },
    { label: "The route was registered before the auth middleware, so the handler runs first and auth never gets to short-circuit", correct: true },
    { label: "The radix trie matched the wrong path", misconception: "Reaching the correct handler means routing worked. The failure is that nothing authenticated the request before it." },
    { label: "The database rejected the auth query", misconception: "A failed auth query would deny access, not grant it. The handler ran without auth being invoked at all." },
  ]}
/>

<Quiz
  id="routing-mw-q2"
  lessonSlug="03-routing-and-middleware"
  lang="en"
  question="Why place a request-size limit BEFORE the body parser in the middleware chain?"
  choices={[
    { label: "The body parser cannot read sizes", misconception: "The parser can read size, but only after it has already buffered the body — which is the cost you are trying to avoid." },
    { label: "So an oversized upload is rejected before the server buffers the whole payload into memory", correct: true },
    { label: "Because the parser is slower than the limit check on small bodies", misconception: "The point is not relative speed on small bodies; it is avoiding buffering a huge body at all before rejecting it." },
    { label: "Limits only work on the first middleware position", misconception: "There is no positional magic. The reason is buffering order: reject before you allocate." },
  ]}
/>

<DragOrder
  id="routing-mw-drag1"
  lessonSlug="03-routing-and-middleware"
  lang="en"
  prompt="Order a sound middleware chain from outermost (runs first) to the handler:"
  items={[
    "Error handler wraps everything (catches downstream throws)",
    "Request logging / tracing (assign a request ID)",
    "Rate limiting (short-circuit 429 before doing work)",
    "Authentication (short-circuit 401 before the handler)",
    "Body size limit, then body parsing",
    "Handler (business logic)",
  ]}
/>

</Explanation>

<FlowDiagram
  label="A middleware chain in registration order, from request to handler"
  hue="var(--d-backend)"
  perRow={6}
  nodes={[
    { id: "req", label: "Request" },
    { id: "log", label: "Logging", sub: "request id" },
    { id: "rate", label: "Rate limit", sub: "429" },
    { id: "auth", label: "Auth", sub: "401" },
    { id: "body", label: "Body parse", sub: "size limit" },
    { id: "handler", label: "Handler" },
  ]}
  edges={[
    { from: "req", to: "log" },
    { from: "log", to: "rate" },
    { from: "rate", to: "auth" },
    { from: "auth", to: "body" },
    { from: "body", to: "handler" },
  ]}
  caption="Order is the security boundary: each layer can short-circuit (rate limit → 429, auth → 401) before the handler runs, and the response unwinds outward in reverse. Cheap protective layers first, expensive optional ones last, error handler outermost."
/>

<KeyTakeaway>Routing maps a request to one handler (radix tries make match time independent of route count); the real routing risks are overlap and method confusion. Middleware is an onion of cross-cutting layers, and any layer can short-circuit — so registration order is a security boundary. Cheap protective layers first, expensive optional ones last, error handler outermost.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="routing-mw-retrieval"
  lang="en"
  questions={[
    { q: "How does a radix-trie router differ from a linear-scan router, and does routing usually dominate latency?", a: "A linear-scan router tries each registered route in order, so match time is O(n) in the number of routes. A radix trie is a prefix tree over path segments, so match time is O(path length) and independent of route count — 10 routes and 10,000 routes match in the same time. In practice both are microseconds and routing rarely dominates latency; the routing risks that matter are correctness ones: overlapping patterns like /users/:id vs /users/me, trailing-slash mismatches, and method confusion." },
    { q: "Explain the onion model of middleware and what 'short-circuiting' means.", a: "Middleware layers wrap the handler like an onion: a request travels inward through each layer to the handler, then the response travels outward through the same layers in reverse. A layer that calls next() passes control inward; a layer that responds without calling next() short-circuits, so the handler never runs. Short-circuiting is how a rate limiter returns 429 or an auth layer returns 401 before any business logic executes." },
    { q: "Why is middleware order a security boundary, and what is the ordering rule?", a: "Because any layer can short-circuit, the order of registration decides what actually guards the handler. Register a route before auth and the handler runs unauthenticated; put the body parser before a size limit and you buffer a huge upload before rejecting it; compress before auth and you waste CPU on requests that will 401. The rule: cheap and protective layers first (size limits, rate limits, auth), expensive and optional layers last (body parsing, compression), and the error handler outermost so it wraps everything." },
  ]}
/>

<Recap lang="en">
With a parsed request in hand, the server chooses what code runs. Routing matches the method and path to exactly one handler; a radix trie keeps match time independent of route count, but the routing bugs that bite are correctness ones — overlapping patterns and method confusion. Around the handler sits the middleware chain, an onion of cross-cutting layers (auth, logging, body parsing, rate limiting, compression, error handling) where any layer can short-circuit the request. That power makes registration order a security and performance boundary: protective cheap layers first, expensive optional layers last, error handler outermost. Middleware is also where per-request latency quietly accumulates. Now when you see an auth bypass or a mysterious p99 spike, check the middleware order before checking the handler — the bug is almost always there. The next stop is the center of the onion: the handler itself and turning its result into a response.
</Recap>
