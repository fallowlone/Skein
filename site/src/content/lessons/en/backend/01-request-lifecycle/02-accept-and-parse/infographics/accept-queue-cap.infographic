infographic chart-column-simple
data
  title The 128 trap: effective accept queue = min(backlog, somaxconn)
  desc Node asks for 511, but the somaxconn default of 128 caps the effective accept queue to 128 — overflow drops connections with no app log
  values
    - label listen(511) requested
      value 511
    - label somaxconn default
      value 128
    - label Effective accept queue
      value 128
theme
  palette #6366f1 #14b8a6 #f43f5e
