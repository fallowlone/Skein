infographic chart-bar-plain-text
data
  title A pool deletes the 34 ms setup tax
  desc Same 1 ms query — connect-per-request adds 34 ms of TCP + TLS + auth on every call; a pool pays that once
  values
    - label Query — the actual work
      value 1
    - label Setup ritual, paid every request
      value 34
    - label Same query, but pooled
      value 1
theme
  palette #14b8a6 #f43f5e #6366f1
