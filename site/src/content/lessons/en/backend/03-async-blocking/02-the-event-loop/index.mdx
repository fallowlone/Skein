---
concepts:
- event-loop
- libuv-phases
- macrotask
- microtask
- process-nexttick
- cooperative-concurrency
- poll-phase
deepensInto:
- backend/03-async-blocking/03-what-blocks-the-loop
estMin: 13
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- backend/03-async-blocking/01-overview
slug: 02-the-event-loop
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://nodejs.org/api/process.html
- https://docs.libuv.org/en/v1.x/design.html
spiral:
- latency
status: ready
summary: The event loop is one thread running queued callbacks in fixed phases — timers,
  poll, check, close — and draining a higher-priority microtask queue between each.
  That order explains setTimeout vs setImmediate vs Promise, and why the concurrency
  is cooperative.
title: 'The event loop: one thread, ordered phases'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A developer writes `setTimeout(fn, 0)` next to `setImmediate(fn)` next to `Promise.resolve().then(fn)`, expecting them to fire in source order. They do not. The promise runs first, then — depending on *where* the code sits — either the timer or the immediate. This is not a bug or a race; it is the event loop's published schedule. The loop is not a black box that "runs async stuff eventually." It is a strict, ordered machine, and once you can see its phases, async timing stops being folklore.
</Hook>

<Crux>One thread runs queued callbacks in fixed phases, draining microtasks between each — so this concurrency is cooperative, not parallel.</Crux>

<Explanation>

## The loop is a sequence of phases

The single thread from the last lesson does not run callbacks in arrival order. It cycles through a fixed sequence of **phases** (in libuv, Node's loop engine), and each phase has its own queue of callbacks to drain before moving on. The ones that matter day to day:

- **Timers** — callbacks whose `setTimeout`/`setInterval` delay has elapsed.
- **Poll** — the heart: retrieve new I/O events and run their callbacks (an HTTP request arrived, a file read finished). If nothing is pending, the loop *blocks here* waiting on epoll/kqueue — this is where idle time is spent.
- **Check** — callbacks scheduled with `setImmediate`, run right after poll.
- **Close** — close-event callbacks (`socket.on('close')`).

One full trip through the phases is a **tick** of the loop. Within a phase, callbacks run one at a time, to completion — there is no preemption.

## Macrotasks vs microtasks

Two of those words hide the whole timing model. The phase queues hold **macrotasks**: a timer callback, an I/O callback, a `setImmediate`. Separate from them are two **microtask** queues with *higher priority*:

- **`process.nextTick`** callbacks (Node-specific), drained first.
- **Promise** reactions (`.then`/`await` continuations).

The rule: **after every single callback finishes, the loop fully drains the microtask queues before running the next callback or advancing a phase.** That is why `Promise.resolve().then(fn)` beats a `setTimeout(fn, 0)` — the promise reaction is a microtask that runs at the next drain, while the timer waits for the timers phase on a later tick.

<Inset kind="why" lang="en">
Why a separate, higher-priority microtask queue at all? Because promise chains need to settle *before* the program returns to the I/O scheduler, or ordering would be unpredictable across ticks. Microtasks let a unit of synchronous-looking async work (an `await` resolving, then the next `await`) finish as a group before the loop does anything else. The danger is the mirror image: a microtask can schedule another microtask, which schedules another — an unbounded chain (`process.nextTick` recursion or a runaway promise loop) starves the loop, because microtasks are drained *to empty* before any I/O callback runs. So microtasks give you tight ordering, but a microtask flood can block the poll phase just as hard as a synchronous loop — the loop never gets to its sockets.
</Inset>

## setTimeout(0) vs setImmediate

These two are the classic confusion. `setImmediate` always runs in the **check** phase, right after poll; `setTimeout(fn, 0)` is clamped to a ~1 ms minimum and runs in the **timers** phase. Inside an I/O callback (i.e., already in or just past poll), `setImmediate` reliably fires *before* the next timers phase, so it wins. At the top level of a script, the order is not guaranteed — it depends on how much time the loop spent starting up. The senior takeaway is not the trivia but the model: "immediately after I/O" and "after the timer delay" are *different phases*, not the same queue.

## Cooperative, not parallel

Because one thread runs each callback to completion with no preemption, the event loop's concurrency is **cooperative**: every callback must voluntarily *yield* — by finishing, or by hitting an `await` that hands control back — for any other callback to run. Two requests are never executing JavaScript *at the same instant*; they interleave at yield points. This is the whole strength (no locks, no data races on shared state within a tick) and the whole weakness (one callback that never yields freezes everything), which the next lesson makes concrete.

<div data-lesson-visual class="overflow-x-auto my-6">

| Scheduler | Queue type | Phase / timing | Priority |
|---|---|---|---|
| `process.nextTick(fn)` | Microtask | After current callback, before promises | Highest |
| `Promise.then` / `await` | Microtask | After current callback, after nextTick | High |
| `setTimeout(fn, 0)` | Macrotask | Timers phase, next eligible tick | Normal |
| I/O callback | Macrotask | Poll phase | Normal |
| `setImmediate(fn)` | Macrotask | Check phase, right after poll | Normal |

</div>

<Quiz
  id="event-loop-q1"
  lessonSlug="02-the-event-loop"
  lang="en"
  question="Inside an I/O callback you schedule `setTimeout(a, 0)`, `setImmediate(b)`, and `Promise.resolve().then(c)`. In what order do they run?"
  choices={[
    { label: "a, b, c — in source order", misconception: "Source order does not determine execution; the loop's phases and the microtask drain do." },
    { label: "c, then b, then a — the promise (microtask) drains first, then setImmediate (check, right after poll), then the timer (timers, later tick)", correct: true },
    { label: "b, a, c — macrotasks always beat microtasks", misconception: "Microtasks have higher priority; the promise reaction drains before any macrotask runs." },
    { label: "All three run simultaneously on separate threads", misconception: "The loop is one thread; callbacks never run simultaneously, they are queued and ordered." },
  ]}
/>

<Quiz
  id="event-loop-q2"
  lessonSlug="02-the-event-loop"
  lang="en"
  question="Why can a recursive `process.nextTick` (or a runaway promise chain) stall I/O even though no synchronous loop is running?"
  choices={[
    { label: "nextTick runs on a background thread that competes for the CPU", misconception: "nextTick runs on the single loop thread; there is no background thread competing." },
    { label: "Microtasks are drained to empty after every callback before any I/O callback runs, so a self-rescheduling microtask never lets the poll phase execute", correct: true },
    { label: "nextTick has lower priority, so I/O starves it", misconception: "It is the reverse: nextTick is highest priority and is drained first, which is exactly why it can starve I/O." },
    { label: "It throws an error that crashes the poll phase", misconception: "No error is thrown; the loop simply never advances past the microtask drain to reach poll." },
  ]}
/>

<DragOrder
  id="event-loop-drag1"
  lessonSlug="02-the-event-loop"
  lang="en"
  prompt="Order one tick of the event loop across its main phases:"
  items={[
    "Timers phase: run elapsed setTimeout/setInterval callbacks",
    "Drain microtasks (nextTick, then promises)",
    "Poll phase: run ready I/O callbacks (or wait on epoll/kqueue)",
    "Check phase: run setImmediate callbacks",
    "Close phase: run close-event callbacks",
  ]}
/>

</Explanation>

<KeyTakeaway>The event loop is one thread cycling through fixed phases — timers, poll (where I/O callbacks run and idle time is spent), check (setImmediate), close — running each callback to completion with no preemption. Between every callback it fully drains higher-priority microtask queues: process.nextTick first, then promise reactions. That ordering is why a resolved promise beats setTimeout(0), and why setImmediate (check) and setTimeout(0) (timers) live in different phases. The model is cooperative concurrency: callbacks interleave only at yield points, so a self-rescheduling microtask or a non-yielding callback freezes the loop.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="event-loop-retrieval"
  lang="en"
  questions={[
    { q: "What are the main phases of the event loop and what does each do?", a: "The single loop thread cycles through a fixed sequence of phases, each with its own queue drained before moving on. Timers runs callbacks whose setTimeout/setInterval delay has elapsed. Poll is the heart: it retrieves new I/O events and runs their callbacks, and if nothing is pending it blocks here waiting on epoll/kqueue, which is where idle time is spent. Check runs setImmediate callbacks right after poll. Close runs close-event callbacks like socket 'close'. One full trip is a tick, and within a phase callbacks run one at a time to completion with no preemption." },
    { q: "What is the difference between macrotasks and microtasks, and what is the draining rule?", a: "The phase queues hold macrotasks: timer callbacks, I/O callbacks, setImmediate. Separate from them are two higher-priority microtask queues — process.nextTick callbacks (drained first) and promise reactions from .then/await. The rule is that after every single callback finishes, the loop fully drains the microtask queues before running the next callback or advancing to the next phase. That is why Promise.resolve().then(fn) runs before a setTimeout(fn, 0): the promise reaction is a microtask that runs at the very next drain, while the timer must wait for the timers phase on a later tick. The hazard is symmetric — a microtask that schedules another microtask forms an unbounded chain that is drained to empty before any I/O callback, so it starves the poll phase just like a synchronous loop would." },
    { q: "Why is event-loop concurrency described as cooperative rather than parallel, and what follows from that?", a: "Because one thread runs each callback to completion with no preemption, every callback must voluntarily yield — by finishing or by hitting an await that hands control back — before any other callback can run. Two requests are therefore never executing JavaScript at the same instant; they interleave only at yield points. The strength is that within a tick there are no locks or data races on shared state, since nothing else runs concurrently. The weakness is the mirror image: a single callback that never yields — a long synchronous CPU loop or a microtask flood — freezes every other connection, because the loop cannot move on until that callback cooperatively returns. This is the foundation for understanding what blocks the loop in production." },
  ]}
/>

<Recap lang="en">
The event loop is a strict, ordered machine, not a vague 'runs async later' box. One thread cycles through fixed phases — timers, poll, check, close — and within each phase runs queued callbacks one at a time to completion. Poll is the center of gravity: it runs ready I/O callbacks and, when idle, blocks on epoll/kqueue. Crosscutting the phases are two higher-priority microtask queues, process.nextTick then promise reactions, and the defining rule is that the loop drains microtasks to empty after every callback before advancing — which is exactly why a resolved promise beats setTimeout(0), and why setImmediate (check phase) and setTimeout(0) (timers phase) are not interchangeable. Because callbacks run to completion without preemption, the concurrency is cooperative: requests interleave only where they yield, giving lock-free safety within a tick but making the loop hostage to any callback that refuses to yield. That hostage scenario — what actually blocks the loop and how to see it — is the next lesson.
</Recap>
