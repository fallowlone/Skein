infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Runtime model is a choice: match it to the workload
    children
      - label Node event loop — one loop = one core
        children
          - label Excels at high-concurrency I/O on little memory
          - label No parallelism for CPU work
          - label Saturation signal: event-loop utilization (ELU)
      - label Go goroutines — M:N scheduler, preemptive
        children
          - label ~2 KB initial stacks
          - label Millions of cheap blocking-style threads
          - label Real multicore parallelism
      - label Java virtual threads — on carrier threads
        children
          - label ~hundreds of bytes of overhead
          - label Millions of cheap blocking-style threads
          - label Real multicore parallelism
theme
  palette #6366f1 #14b8a6 #f59e0b
