---
concepts:
- blocking-io
- event-loop-schedule
- event-loop-blocking
- libuv-threadpool
- bounded-concurrency
- tail-latency
estMin: 13
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://nodejs.org/learn/asynchronous-work/dont-block-the-event-loop
- https://nodejs.org/learn/modules/backpressuring-in-streams
status: ready
summary: "Free-recall prompts across the async/blocking unit. Answer each from memory first, then reveal the model answer and compare."
title: 'Async vs blocking: free-recall review'
track: backend
unit: 03-async-blocking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what turns the unit from notes into instinct.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: the two I/O models, the loop's phases and microtask rule, what blocks it and how to see it, where CPU work belongs, how to bound concurrency, and why the tail explodes near saturation.
</Goal>

<RetrievalDrawer
  client:load
  id="03-async-blocking-short-retrieval"
  lang="en"
  questions={[
    { q: "Contrast blocking thread-per-connection with the non-blocking event-loop model, and explain why the event loop wins for 50,000 idle connections.", a: "In the blocking model a thread calls read() and the OS suspends it until bytes arrive, so each concurrent connection needs its own thread — and each thread reserves roughly 1–2 MB of stack plus a scheduler slot. Fifty thousand connections therefore cost tens of GB of stacks and a storm of context switches, even when the connections are idle, because the parked threads still hold their resources. The non-blocking model sets sockets to non-blocking and registers them with a kernel facility (epoll on Linux, kqueue on BSD/macOS); one thread asks 'which descriptors are ready now?' and the kernel returns only those, in roughly O(1) regardless of how many are watched. So idle connections cost almost nothing because the loop never visits them until they have data — which is exactly why one event-loop thread holds 50k idle keep-alives comfortably while thread-per-connection falls over on memory. The price is callback- or await-shaped code instead of straight-line logic." },
    { q: "Describe the event loop's phases and the microtask draining rule, and use them to order setTimeout(0), setImmediate, and a resolved promise inside an I/O callback.", a: "One thread cycles through fixed phases: timers (elapsed setTimeout/setInterval), poll (the heart — runs ready I/O callbacks and, when idle, blocks on epoll/kqueue), check (setImmediate), and close (close-event callbacks). Within a phase callbacks run one at a time to completion with no preemption. Crosscutting the phases are two higher-priority microtask queues — process.nextTick first, then promise reactions — and the defining rule is that after every single callback the loop drains those microtask queues to empty before running the next callback or advancing a phase. So inside an I/O callback a resolved Promise.then runs first (it is a microtask drained immediately), then setImmediate (check phase, right after poll), then setTimeout(0) (timers phase, on a later tick). The hazard is symmetric: a self-rescheduling microtask is drained to empty before any I/O callback, so it starves the poll phase just like a synchronous loop would." },
    { q: "Why does one synchronous span freeze the whole server, and what is the best signal for catching it?", a: "Event-loop concurrency is cooperative: each callback runs to completion with no preemption and only yields by finishing or hitting an await. So a synchronous span that takes real time — fs.readFileSync (~1200 ms), JSON.parse of a multi-MB body (~800 ms), bcrypt.hashSync (~200–400 ms), or a catastrophic-backtracking regex (seconds, attacker-controlled) — prevents the loop from reaching poll, so no other I/O callback or timer runs for that whole duration. That is head-of-line blocking for the entire process: every concurrent request pays the full stall, which is how a slow line three routes away times out a two-line health check. The best signal is event-loop lag (delay): schedule a timer and measure how late it actually fires, exposed via perf_hooks.monitorEventLoopDelay() as a p50/p99 histogram, alerted around 100 ms. It beats CPU because high CPU can be healthy well-chunked work that still yields, whereas lag measures the gap between when a callback was due and when the loop reached it — exactly the delay users feel." },
    { q: "What are the two thread pools behind the loop, what does each run, and what does a worker thread cost?", a: "The libuv thread pool (default 4 threads, up to 1024 via UV_THREADPOOL_SIZE) runs native operations with no async OS primitive — file-system calls, dns.lookup, async crypto and zlib — and never your JavaScript; it is why async bcrypt does not freeze the loop. Worker threads are entirely different: separate V8 isolates each with their own loop, giving true multicore parallelism for CPU-bound JavaScript like image resizing or large parsing. The classic trap is enlarging the libuv pool to fix a JS CPU bottleneck — it cannot, because the pool never runs JS. A worker thread's main cost is data transfer: postMessage deep-copies via structured clone (a multi-MB buffer copy was ~268 ms versus ~29 ms when transferred), so use transferables (ownership moves, no copy) or a SharedArrayBuffer (shared memory with Atomics), plus a reused worker pool (about one per core) rather than per-request workers, which pay tens of ms of startup each. Only offload when the CPU work dwarfs the message-passing cost; otherwise chunk on the loop with setImmediate, or scale horizontally when CPU is the whole-service ceiling." },
    { q: "Explain backpressure and bounded concurrency, and how you choose the concurrency limit k.", a: "Both fight the same disease — producing faster than you consume. Backpressure is a slow consumer's feedback to a fast producer: writable.write(chunk) returns false once the internal buffer passes the highWaterMark (16 KB for byte streams, 16 objects in object mode), your cue to stop writing until a drain event fires; pipe() and pipeline() wire this handshake automatically so a 50 GB file flows through a few hundred KB of live buffer, while manual write loops that ignore the boolean OOM. Note the mark is only a signal threshold, not a hard cap — a single huge write still buffers fully. Bounded concurrency handles N independent async tasks: instead of await Promise.all(huge.map(fn)) which starts everything at once and OOMs while flooding downstreams, cap at most k in flight with a limiter (p-limit) or a worker-pool/for-await pattern that pulls new work as capacity frees. k is set by what the downstream can absorb — a connection pool of 20, an API rate limit, disk IOPS — not by the item count and not infinity, because past that point extra in-flight work only adds queueing, memory, and failures." },
    { q: "Why does latency explode near saturation, why watch the tail over the average, and what does 'one loop is one core' mean?", a: "A server is a queueing system, and waiting time scales roughly with 1/(1−ρ): about 2× at ρ=0.5, 3.3× at 0.7, 5× at 0.8, 20× at 0.95. So it absorbs load invisibly up to a knee around 70–80% utilization and then falls off a cliff, which is why CPU can go 75% to 82% while p99 jumps from 80 ms to 1,400 ms — you crossed the knee. Little's Law (L = λW) ties that latency blowup to a matching blowup in concurrent occupancy, the same spiral as unbounded fan-out. The average folds slow requests into fast ones and hides this, so you watch the tail — p95/p99/p99.9 — where saturation, GC pauses, and slow dependencies surface first, and fan-out amplifies it: a 1-in-100 bad tail nearly guarantees a 100-call page hits it. 'One loop is one core' means a single Node event loop executes one core's worth of JavaScript — it scales across concurrent I/O but offers no CPU parallelism — so event-loop utilization (ELU) near 1.0 says the loop is the bottleneck, fixable only by doing less per request, offloading CPU, or adding loops. Run with headroom; capacity planning is really tail-latency planning." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: the I/O model decides how you spend the wait, the loop's phases plus the microtask drain decide ordering, a single synchronous span freezes everyone (caught by event-loop lag, not CPU), the libuv pool runs native I/O while CPU-bound JS needs a worker thread you pay to feed data, backpressure and a concurrency cap match production speed to consumption speed, and near saturation the tail explodes nonlinearly — so watch p99 and ELU and run with headroom, because one loop is one core.
</Recap>
