---
concepts:
- event-loop-lag
- cpu-offloading
- bounded-concurrency
- backpressure
- tail-latency
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://nodejs.org/learn/asynchronous-work/dont-block-the-event-loop
- https://nodejs.org/api/worker_threads.html
- https://nodejs.org/api/perf_hooks.html
status: ready
summary: "Hands-on project — build a service that blocks its own event loop, instrument the lag, then offload CPU, bound the fan-out, and prove the tail recovered with before/after numbers."
title: 'Async vs blocking: unfreeze the loop'
track: backend
unit: 03-async-blocking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about a frozen loop is not the same as pulling a service out of one. Build a small server that blocks itself in three different ways, watch a trivial health check time out, then apply the unit's fixes — offload, bound, backpressure — until the tail comes back, with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: instrument event-loop lag and the tail, reproduce a self-inflicted freeze, move CPU work off the loop, bound the fan-out, and verify with before/after numbers under identical load.
</Goal>

<ProjectBrief
  id="03-async-blocking-project"
  lessonSlug="03-async-blocking"
  lang="en"
  objective="Take a deliberately loop-blocking HTTP service (your own or a starter) with a trivial /health endpoint, drive it into event-loop lag and tail-latency collapse, then apply the unit's fixes — offload CPU, bound concurrency, honour backpressure — to keep p99 health latency under target and event-loop delay p99 under ~50 ms at sustained load, proving each step with measurements."
  requirements={[
    "Build or take a small Node HTTP service with three offenders: (a) a /report route that runs JSON.parse or a CPU aggregation on a multi-MB body, (b) a /login route that calls bcrypt.hashSync, and (c) a /migrate route that does await Promise.all over a large array hitting a downstream with a fixed connection pool. Add a two-line /health route that only returns 200.",
    "Wire observability: event-loop delay (perf_hooks.monitorEventLoopDelay, p50/p99), event-loop utilization (ELU), request latency percentiles (p50/p99/p99.9) per route, and in-flight concurrency. Four panels, or the equivalent logged.",
    "Load-test each offender hard enough to push event-loop delay p99 above ~200 ms and watch /health time out. Capture the baseline: which offender freezes the loop, and the lag and p99 it produces while CPU may still read moderate.",
    "Fix the CPU-bound route by moving the work off the loop: async API where one exists (fs.promises, async bcrypt on the libuv pool) and a reused worker thread / worker pool for the pure-JS aggregation — choosing transferables or SharedArrayBuffer if the payload copy dominates. Document which mechanism each route uses and why.",
    "Fix the fan-out route by replacing the unbounded Promise.all with a concurrency cap (e.g. p-limit) set to the downstream's pool size, and add a queue timeout / shedding so a burst cannot starve the pool indefinitely.",
    "Add backpressure to any streaming path: use pipeline() (or honour write()'s false / drain) so a large response or upload flows through a bounded buffer instead of loading whole.",
    "Re-run the identical load test and record after numbers next to before; event-loop delay p99 should fall under ~50 ms, /health p99 should hold under target, and no offender should freeze unrelated routes.",
  ]}
  acceptance={[
    "A before/after table per route: event-loop delay p99, ELU, request p99 and p99.9, and in-flight concurrency — measured under the same load, not estimated.",
    "A demonstration that /health stays fast (p99 under target) while every offender is hammered, proving the loop no longer head-of-line-blocks the whole process.",
    "Event-loop delay p99 holds under ~50 ms at sustained load and the freeze signature is gone from the lag histogram.",
    "A one-paragraph write-up naming the fix used for each offender (async API vs worker thread vs concurrency cap vs backpressure) and why it ranked above tuning UV_THREADPOOL_SIZE or adding cores.",
  ]}
  stretch={[
    "Add a ReDoS offender (a catastrophic-backtracking regex on a user-controlled field), show one crafted request freezes the loop for seconds, then fix it with a safe regex / input validation / match timeout and prove the freeze is gone.",
    "Add a one-page on-call runbook: triage from the four panels, the question 'is this span bounded and fast or could it run tens of ms on a big input?', the offload-vs-chunk-vs-bound decision, and a verification checklist.",
    "Run the service under cluster (or multiple instances behind a load balancer) and show how 'one loop is one core' changes the saturation point and tail under the same load.",
    "Reproduce the same blocking workload on a second runtime (Go goroutines or Java virtual threads) and compare how the identical CPU span and fan-out manifest under a preemptive, multicore scheduler.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real freeze incident: instrument event-loop delay and the tail first, reproduce the self-inflicted block, then fix at the right layer — async API or worker thread for CPU work, a concurrency cap for fan-out, pipeline backpressure for streams — never a bigger libuv pool for JS CPU and never more cores for one loop. Verify with before/after numbers under identical load, with a trivial /health route as the canary. Doing it once on a toy service makes the production version muscle memory.
</Recap>
