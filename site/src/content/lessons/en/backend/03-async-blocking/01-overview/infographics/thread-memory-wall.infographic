infographic chart-column-simple
data
  title Blocking I/O hits a memory wall
  desc Thread-per-connection reserves ~1–2 MB of stack per thread, so stack memory in GB grows linearly with concurrent connections — 10k ≈ 10+ GB
  values
    - label 1k connections
      value 1
    - label 5k connections
      value 5
    - label 10k connections
      value 10
theme
  palette #14b8a6 #f59e0b #f43f5e
