---
concepts:
- blocking-io
- non-blocking-io
- thread-per-connection
- event-loop
- epoll
- c10k
- concurrency-vs-parallelism
deepensInto:
- backend/03-async-blocking/02-the-event-loop
estMin: 11
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- backend/01-request-lifecycle/05-streaming-and-backpressure
slug: 01-overview
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://www.systemoverflow.com/learn/os-systems-fundamentals/io-models/blocking-vs-non-blocking-io-memory-and-threading-trade-offs
- https://tuhuynh.com/en/posts/nio-under-the-hood/
spiral:
- latency
status: ready
summary: A server spends most of its life waiting on I/O. Blocking I/O parks a whole
  thread on each wait, so concurrency costs memory; non-blocking I/O hands the wait
  to the kernel and lets one thread juggle thousands of sockets through an event loop.
title: 'Blocking vs non-blocking I/O: two ways to wait'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Time a typical request handler and the surprise is how little of it is *your* code. It reads a row from Postgres, calls a payment API, writes a log line — and spends 95% of its wall-clock time doing nothing but waiting for those to come back. The whole game of backend concurrency is: what does the program do while it waits? Two answers split the entire field. One parks a thread on every wait. The other refuses to park anyone and asks the kernel to tap it on the shoulder when data is ready.
</Hook>

<Crux>Blocking I/O parks a thread per connection; non-blocking I/O lets one thread multiplex thousands of sockets through an event loop.</Crux>

<Explanation>

## Waiting is the job

A backend is mostly an I/O machine. Disk reads, database queries, outbound HTTP, socket writes — each is *slow* relative to the CPU (microseconds to milliseconds, while the CPU runs billions of instructions a second). So the design question is never "how fast is my code" first; it is "how does the runtime spend the wait." Two I/O models give opposite answers, and the choice shapes how the server scales, how much memory it eats, and how it fails under load.

## Blocking I/O: one thread per connection

In the **blocking** model, a thread calls `read()` and the operating system *suspends that thread* until bytes arrive. The thread is parked — consuming its stack and a scheduler slot — doing nothing useful. To serve a second connection concurrently you need a second thread, a third needs a third, and so on: **thread-per-connection**.

This is simple and easy to reason about — the code reads top to bottom, each line waits for the last — but it scales by *adding threads*, and threads are not free. Each OS thread reserves roughly **1–2 MB** of stack, so 10,000 concurrent connections imply on the order of **10+ GB** of memory just for stacks, plus thousands of context switches per second as the scheduler shuffles parked threads. The model trades memory for simplicity.

## Non-blocking I/O: one thread, many sockets

In the **non-blocking** model, a socket is set to non-blocking mode and `read()` returns *immediately* — either with data or with "not ready yet." Instead of parking, the thread registers interest in many sockets with a kernel facility — **epoll** on Linux, **kqueue** on BSD/macOS — and asks one question: "which of these thousands of file descriptors are ready right now?" The kernel returns only the ready ones, in roughly **O(1)** time regardless of how many are being watched. The thread services those, then asks again. That loop is the **event loop**.

One thread can therefore drive tens of thousands of connections, because it only ever touches sockets that have actual work. The cost is a different shape of code: you cannot read top-to-bottom and "wait" — you register a callback (or `await`) and the loop calls you back later. Logic that was a straight line becomes a set of continuations.

<Inset kind="why" lang="en">
Why does the kernel facility matter so much? The naive way to watch many sockets is to loop over all of them asking "ready? ready? ready?" — that is `select`/`poll`, and it costs O(n) per pass, so watching 10,000 sockets means scanning 10,000 every time even if one is ready. `epoll`/`kqueue` invert this: you register the set once, and the kernel hands back *only* the descriptors that became ready, so the cost tracks the number of *active* connections, not *total* connections. This is the mechanism that makes "one thread, 50,000 idle keep-alive connections" actually cheap — the idle ones cost almost nothing because the loop never visits them until they have data.
</Inset>

## The C10k framing and the real tradeoff

This split was named by the **C10k problem** (~1999): how do you serve 10,000 concurrent clients on one box? Thread-per-connection hit a memory and context-switch wall; the event-loop model — Nginx, Node.js, Netty, Redis — was the answer. The honest summary:

- **Blocking / thread-per-connection** trades *memory and context-switch overhead* for *simplicity*. Great when connection counts are modest or work is CPU-heavy; the code stays linear.
- **Non-blocking / event loop** trades *code complexity* (callbacks, continuations, no parking) for *scalability under many concurrent, mostly-idle connections*.

Neither is universally "faster." For I/O-bound workloads with high concurrency, the event loop wins decisively on memory and connection count. For CPU-bound work, a single event-loop thread is no faster than any other single thread — a limit the next lessons make sharp.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Blocking (thread-per-connection) | Non-blocking (event loop) |
|---|---|---|
| Waiting | Thread parked by OS | Kernel watches FDs, thread moves on |
| 10k connections | ~10+ GB stacks, many context switches | One thread, memory ~ active conns |
| Code shape | Linear, top-to-bottom | Callbacks / `await`, continuations |
| Scales by | Adding threads | Adding ready-event throughput |
| Best for | Modest concurrency, CPU-heavy | High concurrency, I/O-bound |

</div>

<Quiz
  id="async-overview-q1"
  lessonSlug="01-overview"
  lang="en"
  question="Why does a thread-per-connection server struggle to hold 50,000 mostly-idle keep-alive connections?"
  choices={[
    { label: "Each idle connection burns CPU spinning in a loop", misconception: "Idle blocking threads are parked by the OS, not spinning; the cost is memory and scheduling, not CPU spin." },
    { label: "Each connection needs its own thread, and each thread reserves ~1–2 MB of stack plus a scheduler slot, so memory and context-switching dominate", correct: true },
    { label: "The kernel cannot open more than a few thousand sockets", misconception: "File-descriptor limits are tunable; the binding constraint here is per-thread memory and context-switch overhead." },
    { label: "Non-blocking sockets are required to accept any connection", misconception: "Blocking sockets accept connections fine; the problem is the per-connection thread cost, not acceptance." },
  ]}
/>

<Quiz
  id="async-overview-q2"
  lessonSlug="01-overview"
  lang="en"
  question="What does `epoll`/`kqueue` give the event loop that a naive `select`/`poll` scan does not?"
  choices={[
    { label: "It makes each socket read faster at the hardware level", misconception: "Per-read speed is unchanged; the win is in discovering *which* sockets are ready without scanning all of them." },
    { label: "It returns only the file descriptors that are actually ready, so cost scales with active connections, not total watched", correct: true },
    { label: "It runs the callbacks on multiple CPU cores in parallel", misconception: "The event loop is still one thread; epoll only reports readiness, it does not parallelize callbacks." },
    { label: "It converts blocking reads into non-blocking ones automatically", misconception: "Sockets must be set non-blocking explicitly; epoll is a readiness-notification facility, not a mode switch." },
  ]}
/>

<DragOrder
  id="async-overview-drag1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order what a non-blocking server does to serve a read on one of many sockets:"
  items={[
    "Set the socket to non-blocking mode and register it with epoll/kqueue",
    "Ask the kernel which of the watched descriptors are ready",
    "Kernel returns only the ready descriptors",
    "Run the callback for each ready socket, reading the available bytes",
    "Loop back and ask the kernel again",
  ]}
/>

</Explanation>

<FlowDiagram label="Blocking vs non-blocking: what the thread does while waiting" hue="var(--d-backend)" perRow={4} nodes={[{id:"req",label:"Request",sub:"arrives"},{id:"block",label:"Thread parked",sub:"1–2 MB stack"},{id:"nb",label:"epoll/kqueue",sub:"O(1) ready"},{id:"loop",label:"Event loop",sub:"services ready"}]} edges={[{from:"req",to:"block",label:"blocking"},{from:"req",to:"nb",label:"non-blocking"},{from:"nb",to:"loop"}]} caption="Blocking parks the thread on every wait (~1–2 MB each, ~10 GB for 10k conns); non-blocking registers with epoll/kqueue, returns only ready descriptors, and one thread services thousands." />

<KeyTakeaway>A server spends most of its time waiting on I/O, and the I/O model decides what happens during the wait. Blocking I/O parks a thread per connection — simple linear code, but ~1–2 MB per thread means 10k connections cost 10+ GB and heavy context switching. Non-blocking I/O sets sockets non-blocking and uses epoll/kqueue so one thread services only the ready descriptors, scaling to tens of thousands of connections at the price of callback-shaped code. The C10k problem named this split; the event loop won for high-concurrency I/O-bound work.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="async-overview-retrieval"
  lang="en"
  questions={[
    { q: "Why is 'how the runtime spends the wait' the central question for a backend, rather than raw code speed?", a: "A backend is mostly an I/O machine: disk reads, database queries, outbound HTTP, and socket writes each take microseconds to milliseconds while the CPU runs billions of instructions per second, so a request handler spends the large majority of its wall-clock time waiting, not computing. That means the dominant design decision is what the runtime does during those waits, not how fast the small slice of your own code runs. The two I/O models answer this oppositely — one parks a thread on each wait, the other refuses to park and lets the kernel signal readiness — and that single choice drives how the server scales, how much memory it uses, and how it behaves under load." },
    { q: "How does blocking thread-per-connection work and what is its scaling cost?", a: "In the blocking model a thread calls read() and the operating system suspends that thread until bytes arrive, so the thread is parked doing nothing but still holding its stack and a scheduler slot. To serve N concurrent connections you need roughly N threads. The code is simple and linear — each line waits for the previous — but threads are expensive: each OS thread reserves about 1–2 MB of stack, so 10,000 concurrent connections imply on the order of 10+ GB just for stacks, plus thousands of context switches per second as the scheduler shuffles parked threads. The model trades memory and scheduling overhead for simplicity, which is fine at modest concurrency or for CPU-heavy work but hits a wall under many connections." },
    { q: "How does non-blocking I/O with an event loop serve many connections on one thread, and what does epoll/kqueue contribute?", a: "Sockets are put in non-blocking mode so read() returns immediately, either with data or with 'not ready.' Instead of parking, the single thread registers interest in many sockets with a kernel facility — epoll on Linux, kqueue on BSD/macOS — and repeatedly asks 'which of these descriptors are ready now?' The kernel returns only the ready ones in roughly O(1) regardless of how many are watched, the thread services those, then asks again; that loop is the event loop. The key contribution of epoll/kqueue over a naive select/poll scan is that you register the set once and get back only the descriptors that became ready, so cost tracks the number of active connections rather than total watched — which is why tens of thousands of mostly-idle keep-alive connections are cheap. The price is callback- or await-shaped code instead of straight-line logic." },
  ]}
/>

<Recap lang="en">
A backend spends most of its life waiting on I/O, so the model for *how it waits* decides everything downstream. Blocking I/O parks a thread on each wait: linear, easy code, but each thread costs roughly 1–2 MB and a scheduler slot, so thread-per-connection turns 10,000 connections into 10+ GB of stacks and a storm of context switches — memory traded for simplicity. Non-blocking I/O sets sockets non-blocking, returns immediately, and registers them with epoll or kqueue so one thread asks the kernel which descriptors are ready and services only those — scaling to tens of thousands of connections because idle ones cost almost nothing, at the price of callback- or await-shaped code. The C10k problem named this divide, and the event loop became the standard answer for high-concurrency I/O-bound servers. The next lesson opens that loop up: the ordered phases it runs, the microtask queue it drains between them, and why this concurrency is cooperative rather than parallel. Now when you see a thread-count limit or an epoll configuration, you know exactly which half of the tradeoff you are tuning.
</Recap>
