---
concepts:
- io-models
- event-loop-schedule
- event-loop-blocking
- libuv-threadpool
- bounded-concurrency
- tail-latency
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://nodejs.org/learn/asynchronous-work/dont-block-the-event-loop
- https://nodejs.org/api/worker_threads.html
- https://nodejs.org/learn/modules/backpressuring-in-streams
status: ready
summary: "Multiple-choice synthesis across the async/blocking unit — I/O models, the event-loop schedule, what blocks it, offloading CPU work, bounded concurrency, and tail latency under load."
title: 'Async vs blocking: multiple-choice review'
track: backend
unit: 03-async-blocking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real incident — not a definition to recite, but a tradeoff to weigh while the loop is on fire and the tail is climbing.
</Hook>

<Goal lang="en">
Confirm you can connect the I/O model, the event-loop schedule, what freezes it, where CPU work belongs, how to bound fan-out, and why the tail explodes near saturation — the synthesis the six lessons built toward.
</Goal>

<Quiz
  id="03-async-blocking-choice-1"
  lessonSlug="03-async-blocking"
  lang="en"
  question="Service A is a thread-per-connection app holding 50,000 mostly-idle keep-alive sockets; Service B is an event-loop app doing the same. Both are nearly idle on CPU. Why does A fall over while B stays comfortable?"
  choices={[
    { label: "Each of A's connections pins a thread that reserves ~1–2 MB of stack plus a scheduler slot, so 50k idle connections cost tens of GB and heavy context-switching; B's loop touches only the ready descriptors via epoll/kqueue, so idle ones cost almost nothing.", correct: true },
    { label: "A spins CPU on each idle connection while B sleeps them.", misconception: "Idle blocking threads are parked by the OS, not spinning — A's CPU stays low. The cost is memory for stacks and scheduling overhead, not CPU spin." },
    { label: "B can open more file descriptors because event loops bypass the FD limit.", misconception: "FD limits are a tunable per-process setting independent of the I/O model; both hit the same kernel limit. The real difference is per-connection thread cost." },
    { label: "A is slower per read because blocking reads are inherently slower than non-blocking ones.", misconception: "Per-read latency is essentially the same; epoll/kqueue only changes how you discover which sockets are ready, not how fast a single read completes." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-2"
  lessonSlug="03-async-blocking"
  lang="en"
  question="Inside an I/O callback you schedule setTimeout(a, 0), setImmediate(b), and Promise.resolve().then(c). What runs first, and what principle decides it?"
  choices={[
    { label: "c, then b, then a — the loop drains microtasks (promise reactions) to empty after the current callback before any macrotask; setImmediate runs in the check phase right after poll; the timer waits for the timers phase on a later tick.", correct: true },
    { label: "a, b, c — callbacks fire in source order.", misconception: "Source order never decides event-loop execution; the phase a callback belongs to and the microtask drain do. The promise reaction jumps ahead of both macrotasks." },
    { label: "b, a, c — macrotasks always beat microtasks.", misconception: "It is the reverse: microtasks are higher priority and are drained to empty after every callback, so the promise reaction runs before any timer or setImmediate." },
    { label: "All three at once, on separate libuv threads.", misconception: "The loop is one thread; these callbacks are queued and ordered, never simultaneous. libuv threads run native I/O, not your JS callbacks." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-3"
  lessonSlug="03-async-blocking"
  lang="en"
  question="A two-line /health endpoint starts timing out, but only when a reporting route is hit. The reporting route runs JSON.parse on a 40 MB body. Why does health suffer, and what is the right diagnosis signal?"
  choices={[
    { label: "The parse is synchronous CPU on the single loop thread, so the loop cannot return to poll to serve any other request — head-of-line blocking for the whole process. Event-loop lag (a late timer) quantifies the freeze better than CPU usage.", correct: true },
    { label: "Health and reporting share a database connection that the parse holds open.", misconception: "Health touches no database and JSON.parse opens no connection — it is pure in-memory CPU. The monopolised resource is the loop thread, not a connection." },
    { label: "CPU hits 100%, so the scheduler deprioritises the cheap health route.", misconception: "There is no per-route OS scheduling here — it is one thread. And high CPU can be healthy; the truer signal is event-loop lag, the gap between when a callback was due and when the loop reached it." },
    { label: "Raising UV_THREADPOOL_SIZE would let health run on a spare libuv thread.", misconception: "The libuv pool runs native I/O, never your JS request handlers, and the parse is JS on the loop. Enlarging the pool does nothing for a blocked loop." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-4"
  lessonSlug="03-async-blocking"
  lang="en"
  question="A CPU-heavy image resize (pure JS) freezes the loop. One engineer bumps UV_THREADPOOL_SIZE 4 to 32; another moves the resize into a worker thread. Who fixed it and why?"
  choices={[
    { label: "The worker-thread engineer. The libuv pool runs native fs/dns/crypto/zlib, never your JavaScript, so enlarging it cannot help JS CPU work; a worker thread is a separate V8 isolate giving real multicore parallelism for that resize.", correct: true },
    { label: "The UV_THREADPOOL_SIZE engineer — a bigger pool gives the resize more threads.", misconception: "The libuv pool never executes JavaScript, so it cannot run the resize regardless of size. Enlarging it only helps when you are bottlenecked on native fs/dns/crypto throughput." },
    { label: "Both fixes are equivalent; pick whichever is simpler.", misconception: "They are not equivalent — only the worker thread runs the JS off the loop. The pool change is a no-op for this bug and is the classic week-losing trap." },
    { label: "Neither — image resizing is I/O-bound, so it belongs on the loop.", misconception: "Resizing is CPU-bound computation, which is exactly why it freezes the loop and why it must be offloaded to a worker (or chunked)." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-5"
  lessonSlug="03-async-blocking"
  lang="en"
  question="A migration runs await Promise.all(millionIds.map(processUser)) and is OOM-killed before the first thousand finish, while the database refuses new connections. Root cause and fix?"
  choices={[
    { label: "Unbounded concurrency: Promise.all starts all million operations at once, so a million pending promises, closures, and downstream connections pile up. Fix: a concurrency cap (e.g. p-limit at k matched to the DB pool size) so only k run in flight.", correct: true },
    { label: "Promise.all has a bug with very large arrays.", misconception: "Promise.all behaves exactly as specified — it starts every promise immediately. The harm is the unbounded count of simultaneous operations, not a defect." },
    { label: "await inside .map blocks the event loop.", misconception: "The map callbacks do not block the loop; they are I/O-bound and yield. The problem is how many run concurrently, not that any single one blocks." },
    { label: "Raising the heap limit would let all million run safely.", misconception: "More heap only delays the OOM and still floods the database past its connection limit. The durable fix is bounding concurrency to what the downstream can absorb." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-6"
  lessonSlug="03-async-blocking"
  lang="en"
  question="A Node service sits at 75% CPU with a 20 ms average; a small traffic spike pushes CPU to 82% and p99 jumps from 80 ms to 1,400 ms while the average barely moves. How do you read this?"
  choices={[
    { label: "Queueing wait scales like 1/(1−ρ), so past the ~70–80% knee small load increases cause large latency increases. The average folds slow requests into fast ones and hides it; the tail (p99) and event-loop utilization are the real signals. Run with headroom.", correct: true },
    { label: "A memory leak triggered at 82% utilization.", misconception: "Nothing implies a leak; the nonlinear jump is the queueing curve near saturation, a property of load. The average reading 'fine' is exactly the trap." },
    { label: "p99 is just noisy at high load and can be ignored.", misconception: "p99 is the meaningful signal here — it reveals the saturation the average conceals, and fan-out makes that 1-in-100 tail the typical experience of a multi-call page." },
    { label: "Add CPU cores and the loop will scale linearly out of it.", misconception: "One Node loop is one core's worth of JavaScript; extra cores need extra loops (cluster/instances). The deeper fix is running with headroom below the knee, not chasing the cliff with hardware." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain of decisions: the I/O model sets how you spend the wait (park a thread vs multiplex on a loop), the loop's phases and microtask drain set callback ordering, any synchronous span on the loop freezes every connection at once, CPU-bound JS belongs on a worker thread (not a bigger libuv pool) while long-but-splittable work can chunk, fan-out must be bounded to what the downstream absorbs, and near saturation queueing makes the tail explode — so you watch p99 and event-loop utilization, not the average, and run with headroom.
</Recap>
