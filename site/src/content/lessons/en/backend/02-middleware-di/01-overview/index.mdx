---
concepts:
- middleware
- dependency-injection
- cross-cutting-concerns
- inversion-of-control
- composition-root
- coupling
deepensInto:
- backend/02-middleware-di/02-writing-middleware
- backend/02-middleware-di/03-inversion-of-control
estMin: 11
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- backend/01-request-lifecycle/03-routing-and-middleware
slug: 01-overview
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://docs.nestjs.com/providers
- https://martinfowler.com/articles/injection.html
spiral:
- latency
status: ready
summary: 'Two patterns sit underneath every framework: middleware intercepts each
  request in order, and dependency injection wires services once at startup. One controls
  what runs around your code; the other controls what your code can reach and how
  you test it.'
title: 'Middleware and DI: the two patterns that shape every backend'
track: backend
unit: 02-middleware-di
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Two engineers describe the same backend. One says "every request flows through auth, logging, and validation before the handler." The other says "the handler gets a database client, a cache, and a mailer it never created." They are describing the same code from two angles: the request flowing *through* layers, and the dependencies flowing *into* objects. Those two angles are middleware and dependency injection — and almost every framework decision you will make is one or the other.
</Hook>

<Crux>Middleware intercepts each request in order; DI wires services once at startup — together they decide what runs and how testable it is.</Crux>

<Explanation>

## Two orthogonal axes

Middleware and dependency injection answer different questions, and confusing them is a common source of muddled architecture:

- **Middleware** is about the **request axis** — what runs *around* a handler on every request, in what order. It is dynamic, per-request, and ordered: auth, logging, body parsing, rate limiting. The previous unit showed why that order is a security boundary.
- **Dependency injection** is about the **wiring axis** — what objects a piece of code is *given* instead of constructing itself. It is mostly static, decided at startup, and about coupling and testability: a handler receives a `UserRepository` rather than calling `new UserRepository()` inside itself.

One is a pipeline; the other is a graph. A request travels the pipeline; the graph is assembled once before any request arrives.

## Why middleware exists: cross-cutting concerns

Auth, logging, tracing, compression, and error handling are needed by almost every endpoint but belong to none of them. Without middleware, each handler re-implements them — inconsistently. Middleware factors these **cross-cutting concerns** into ordered layers that wrap the handler, so they are written once and applied uniformly.

## Why DI exists: inversion of control

When a class calls `new EmailService()` internally, it is welded to that exact implementation: you cannot swap it for a fake in a test, point it at a different config, or reuse the class elsewhere. **Inversion of control** flips this — the class declares *what* it needs (an emailer) and something external decides *which* concrete one it gets. That external place is the **composition root**, the single spot near startup that knows the real implementations and wires the object graph.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Middleware | Dependency injection |
|---|---|---|
| Axis | The request (what runs around code) | The wiring (what code is given) |
| When | Per request, dynamically | Once at startup, statically |
| Shape | Ordered pipeline | Object graph |
| Solves | Cross-cutting concerns | Coupling, testability |
| Gets wrong | Ordering → security/perf bugs | Hidden `new` → untestable code |

</div>

## They meet in the framework

Real frameworks fuse the two. NestJS controllers are resolved from a DI container *and* sit behind a middleware/guard/interceptor pipeline. Express keeps DI implicit (you wire by hand or `req`-attach), but the middleware model is explicit. Recognizing which axis a problem lives on is the senior reflex: "this endpoint is slow on every request" is usually middleware; "this class is impossible to test" is usually DI.

The two axes also have very different cost profiles, which is why they are worth separating. Middleware cost is *per request and recurring*: each layer in an Express chain typically adds single-digit microseconds of overhead, but a bloated 12–15-layer chain (auth, logging, body parse, compression, CORS, helmet, rate limit, etc.) can add roughly 0.3–0.5ms at p50 and 1–3ms at p99 once GC and a slow async layer like an audit log are in play — multiplied by every request, that is real throughput. DI cost is mostly *paid once at startup*: a NestJS app with a few hundred providers spends on the order of tens to low-hundreds of milliseconds building its graph at boot, then serves requests with singletons at near-zero per-request wiring cost. So a middleware mistake taxes every request forever; a DI mistake usually taxes only the boot (with one sharp exception — request-scoped providers, covered in lesson 04, that drag wiring cost back into the per-request hot path).

<Quiz
  id="mw-di-overview-q1"
  lessonSlug="01-overview"
  lang="en"
  question="A service class calls `new StripeClient()` directly in its constructor, and the team finds it impossible to unit-test without hitting the real Stripe API. Which axis is the problem on?"
  choices={[
    { label: "Middleware ordering — the Stripe call runs in the wrong layer", misconception: "Ordering is about what runs around a request. The problem here is that the class constructs its own dependency, which is a wiring/DI concern." },
    { label: "Dependency injection — the hard-coded `new` couples the class to the real implementation, removing the test seam", correct: true },
    { label: "Routing — the route matched the wrong handler", misconception: "Routing picks the handler. The difficulty testing a class in isolation is about how its dependencies are supplied, not how it is reached." },
    { label: "Serialization — the Stripe response is too large to encode", misconception: "Serialization is about turning results into bytes. The issue is that the dependency cannot be substituted in a test." },
  ]}
/>

<Quiz
  id="mw-di-overview-q2"
  lessonSlug="01-overview"
  lang="en"
  question="Which best describes the difference in *when* middleware and DI act?"
  choices={[
    { label: "Both run fresh on every request", misconception: "Middleware runs per request, but the dependency graph is normally assembled once at startup and reused — that is the whole point of singletons." },
    { label: "Middleware runs per request in order; the DI graph is mostly assembled once at startup and reused", correct: true },
    { label: "Both are assembled once at startup", misconception: "The DI graph is assembled at startup, but middleware executes on each individual request — it is dynamic, not static." },
    { label: "DI runs per request; middleware is assembled once", misconception: "It is the reverse: middleware is the per-request pipeline; DI wiring is the startup-time graph." },
  ]}
/>

<DragOrder
  id="mw-di-overview-drag1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order these events from process start to a served request:"
  items={[
    "Composition root wires the object graph (repositories, clients, services)",
    "Framework registers the middleware pipeline in order",
    "Server starts listening for connections",
    "A request arrives and flows through the middleware pipeline",
    "The handler runs using the dependencies it was injected",
    "The response is produced and returned",
  ]}
/>

</Explanation>

<StackDiagram
  label="Middleware (request axis) vs DI wiring (startup axis): two orthogonal backend patterns"
  hue="var(--d-backend)"
  layers={[
    { label: "Request axis: Middleware", note: "runs per-request, in order — auth → logging → body → rate-limit → handler" },
    { label: "Wiring axis: DI graph", note: "assembled once at startup — composition root wires concrete objects" },
  ]}
  caption="Middleware is the per-request pipeline (dynamic, ordered); the DI graph is assembled once at startup (static). One is about what runs around code; the other is about what code is given."
/>

<KeyTakeaway>Middleware and DI are orthogonal. Middleware is the per-request pipeline that factors cross-cutting concerns (auth, logging) into ordered layers; getting the order wrong is a security/perf bug. DI is the startup-time wiring that gives a class its dependencies instead of letting it construct them, via a composition root; hidden `new` calls destroy testability. Knowing which axis a problem lives on points you straight at the fix.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="mw-di-overview-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between the request axis and the wiring axis, and why keep them separate?", a: "The request axis is middleware: what runs around a handler on every request, in order — auth, logging, body parsing, rate limiting. It is dynamic and per-request, and its ordering is a security and performance boundary. The wiring axis is dependency injection: what objects a piece of code is given rather than constructing itself, decided mostly once at startup. It is about coupling and testability. One is an ordered pipeline a request travels through; the other is an object graph assembled before any request arrives. Keeping them separate lets you reason about 'what runs on every request' independently from 'what this class depends on,' and most backend problems clearly belong to one axis." },
    { q: "Why does middleware exist, and what problem do cross-cutting concerns create without it?", a: "Cross-cutting concerns — auth, logging, tracing, compression, error handling — are needed by almost every endpoint but belong to none of them. Without middleware, each handler re-implements these concerns, which is duplicated and inevitably inconsistent (one endpoint forgets auth, another logs a different shape). Middleware factors them into ordered layers that wrap the handler, so each concern is written once and applied uniformly across routes, and the ordering of those layers becomes the single place that decides what protects the handler." },
    { q: "What is inversion of control and what role does the composition root play?", a: "Inversion of control means a class declares what it needs (an emailer, a repository) rather than constructing the concrete implementation itself with `new`. Something external then decides which concrete instance it receives. That external place is the composition root: a single location near application startup that knows the real implementations and assembles the whole object graph. This removes the hard coupling created by in-class `new` calls, which is what makes classes swappable for fakes in tests, reconfigurable for different environments, and reusable — the testability seam lives exactly where the dependency is injected rather than constructed." },
  ]}
/>

<Recap lang="en">
Underneath every backend framework sit two orthogonal patterns. Middleware is the request axis: ordered layers that wrap the handler and factor cross-cutting concerns — auth, logging, body parsing, rate limiting — so they are written once and applied uniformly, with their order acting as a security and performance boundary. Dependency injection is the wiring axis: instead of a class constructing its own collaborators with `new`, it declares what it needs and a composition root assembles the object graph once at startup, which is what makes code swappable and testable. One is a per-request pipeline; the other is a startup-time graph. Frameworks fuse them — a NestJS controller is DI-resolved and sits behind a guard/interceptor pipeline — but the senior reflex is to ask which axis a problem lives on. The next lessons go deep on each: first the anatomy of writing correct middleware, then the mechanics of inversion of control and DI scopes.
</Recap>
