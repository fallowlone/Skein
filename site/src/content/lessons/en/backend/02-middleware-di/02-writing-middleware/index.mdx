---
concepts:
- middleware-signature
- next
- error-middleware
- onion-model
- fastify-hooks
- encapsulation
- double-send
deepensInto:
- backend/02-middleware-di/03-inversion-of-control
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- backend/02-middleware-di/01-overview
slug: 02-writing-middleware
sources:
- https://expressjs.com/en/guide/error-handling/
- https://fastify.dev/docs/latest/Reference/Hooks/
- https://koajs.com/
spiral:
- latency
status: ready
summary: 'A middleware is a function with a contract: read the request, optionally
  act, then call next() or terminate. The contract differs across Express, Koa, and
  Fastify — and the most common production bugs come from breaking it in the same
  three ways.'
title: 'Writing middleware: signatures, next(), and the three framework models'
track: backend
unit: 02-middleware-di
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A middleware that adds a request ID works perfectly. A copy of it that also calls an async audit log starts throwing `Cannot set headers after they are sent` under load — but only sometimes. The bug is not the audit call. It is that the async error escapes the middleware, the framework's default handler responds, and then the original code path responds too. Two responses, one socket. The middleware contract was broken in the most common way there is.
</Hook>

<Crux>A middleware reads the request, optionally acts, then does exactly one: call next() to pass control, or end the response — never both.</Crux>

<Explanation>

## The Express signature

In Express, a middleware is `(req, res, next) => {}`. It has exactly three legal endings:

- **Pass through** — call `next()` to hand control to the next middleware.
- **Terminate** — send a response (`res.json(...)`, `res.end()`) and *do not* call `next()`. This short-circuits the chain.
- **Fail** — call `next(err)` to skip ahead to error-handling middleware.

The two failure modes are doing **both** (send a response *and* call `next()`, leading to a double-send) or **neither** (no response, no `next()` — the request hangs until it times out). Together these three endings form a complete, closed contract — and every production bug in middleware traces back to breaking exactly one of them.

## Error middleware is a different shape

Express recognizes error-handling middleware by **arity**: it must declare **four** parameters, `(err, req, res, next)`. Drop the unused `next` and Express treats it as ordinary middleware and never routes errors to it — a silent, infuriating bug. Error middleware also must be registered **last**, after all routes, because the chain reaches it only via `next(err)` from earlier layers.

<Inset kind="why" lang="en">
Why did Express 5 change async error handling? In Express 4, a route handler that returned a rejected promise did *not* automatically reach error middleware — an unhandled rejection escaped, which is exactly the double-response bug in the Hook. Teams wrapped every async handler in an `asyncHandler` helper to catch and forward. Express 5 makes route handlers and middleware that return a promise call `next(err)` automatically on rejection, so the wrapper is no longer needed. The four-argument error-middleware signature is unchanged; only the forwarding became automatic.
</Inset>

## Koa: the async onion

Koa replaces the three-arg callback with `async (ctx, next) => {}` and a true **onion model**. Code before `await next()` runs on the way in; code after it runs on the way out, in reverse order. This makes wrap-around timing trivial:

```
async (ctx, next) => {
  const start = Date.now();      // inbound
  await next();                  // run everything inside
  ctx.set('X-Time', `${Date.now() - start}`); // outbound
}
```

Because it is promise-based, a `throw` anywhere propagates naturally to an upstream `try/catch` middleware — error handling is just a middleware that wraps the rest in `try/catch` around `await next()`.

## Fastify: hooks, not one primitive, plus encapsulation

Fastify splits the single middleware idea into named **lifecycle hooks** that fire in a fixed order:

`onRequest → preParsing → preValidation → preHandler → handler → onSend → onResponse`

This lets you reject early at `onRequest` (before body parsing — the cheapest rejection point) or run logic at `preHandler` once the validated body exists. The bigger difference is **encapsulation**: hooks (and decorators) registered inside a plugin apply only to that plugin's routes, not globally. Express middleware, by contrast, leaks down the whole chain from where it is mounted. Encapsulation is why large Fastify apps stay scoped where large Express apps accrete global middleware.

The ordering choice is not free, and it is where the chain-length-vs-latency tradeoff bites. Each middleware is a function call plus whatever work it does; a lean layer (set a header, attach a request ID) costs well under a microsecond, but every layer is paid *on every request whether or not it is needed*. A global `body-parser` mounted before the router runs `JSON.parse` on bodies for routes that have none; an auth middleware mounted globally hashes and verifies a token even for a public health check. Rejecting at the wrong point compounds it: reject an unauthenticated request at `onRequest` and you skip body parsing, validation, and the handler — reject it inside the handler and you have paid for all of them first. The discipline is to *order cheap, high-rejection-rate layers first and scope expensive layers narrowly*. In practice this is why Express benchmarks at roughly 11–15k req/s while Fastify reaches 30k+ on the same hardware: not raw routing speed alone, but Fastify's encapsulation keeping each route's chain short instead of forcing every request through one fat global stack.

<div data-lesson-visual class="overflow-x-auto my-6">

| Model | Signature | Wrap-around | Scope |
|---|---|---|---|
| Express | `(req, res, next)` | Manual; error mw needs 4 args | Global from mount point |
| Koa | `async (ctx, next)` | Native onion via `await next()` | Global stack |
| Fastify | Named hooks | Hooks at fixed lifecycle points | Encapsulated per plugin |

</div>

<Quiz
  id="writing-mw-q1"
  lessonSlug="02-writing-middleware"
  lang="en"
  question="An Express error handler `(err, req, res) => {...}` never receives errors even though earlier code calls next(err). Why?"
  choices={[
    { label: "Error handlers must be registered first", misconception: "Error middleware must be registered last, not first — but that is not why this one is skipped. The signature is the problem." },
    { label: "Express identifies error middleware by arity: it must declare four parameters, so a three-arg function is treated as ordinary middleware", correct: true },
    { label: "next(err) is not valid in Express", misconception: "next(err) is the correct way to forward errors. The reason it is not caught here is the handler's parameter count." },
    { label: "Errors can only be handled inside the route", misconception: "Centralized error middleware is standard. The handler is skipped purely because it does not declare four parameters." },
  ]}
/>

<Quiz
  id="writing-mw-q2"
  lessonSlug="02-writing-middleware"
  lang="en"
  question="What is the root cause of a `Cannot set headers after they are sent` error from a middleware?"
  choices={[
    { label: "The response body was too large", misconception: "Body size does not cause this error. It is caused by writing to the response after it has already been sent once." },
    { label: "The response was ended twice — e.g. the code sent a response and also let an error path send another", correct: true },
    { label: "The middleware was registered in the wrong file", misconception: "File location is irrelevant. The error means two code paths both wrote a response to the same request." },
    { label: "next() was called with no arguments", misconception: "Calling next() to pass control is normal. The error is specifically about responding more than once." },
  ]}
/>

<Quiz
  id="writing-mw-q3"
  lessonSlug="02-writing-middleware"
  lang="en"
  question="Why does Fastify's encapsulation matter compared to Express middleware?"
  choices={[
    { label: "It makes hooks run faster than middleware", misconception: "Encapsulation is about scope, not raw speed. The point is where a hook applies, not how fast it runs." },
    { label: "Hooks registered in a plugin apply only to that plugin's routes, so cross-cutting logic does not leak globally the way Express middleware does", correct: true },
    { label: "It allows middleware to be async", misconception: "Async support is separate from encapsulation. Koa and Express middleware can be async too; encapsulation is about scoping." },
    { label: "It removes the need for a handler", misconception: "Hooks wrap the handler; they do not replace it. Encapsulation only controls which routes a hook covers." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Middleware onion: request flows in through layers, response flows back out in reverse"
  hue="var(--d-backend)"
  perRow={5}
  nodes={[
    { id: "mw1", label: "Middleware 1", sub: "inbound ↓" },
    { id: "mw2", label: "Middleware 2", sub: "inbound ↓" },
    { id: "handler", label: "Handler", sub: "produces res" },
    { id: "mw2out", label: "Middleware 2", sub: "outbound ↑" },
    { id: "mw1out", label: "Middleware 1", sub: "outbound ↑" },
  ]}
  edges={[
    { from: "mw1", to: "mw2", label: "next()" },
    { from: "mw2", to: "handler", label: "next()" },
    { from: "handler", to: "mw2out", label: "return" },
    { from: "mw2out", to: "mw1out", label: "return" },
  ]}
  caption="Koa's async onion: code before await next() runs inbound (left→right); code after runs outbound (right→left). Express uses the same shape but each middleware terminates, passes, or fails — no automatic outbound pass."
/>

<KeyTakeaway>A middleware must do exactly one of: call next() to pass through, terminate by sending a response, or call next(err) — never both (double-send → "headers already sent") and never neither (hang). Express identifies error middleware by its four-argument arity and Express 5 auto-forwards rejected promises. Koa uses a native async onion via `await next()`; Fastify uses fixed lifecycle hooks plus encapsulation so plugin hooks do not leak globally.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="writing-mw-retrieval"
  lang="en"
  questions={[
    { q: "What are the legal endings of an Express middleware, and what are the two ways to break the contract?", a: "An Express middleware (req, res, next) has three legal endings: call next() to pass control to the next middleware; terminate by sending a response (res.json, res.end) without calling next, which short-circuits the chain; or call next(err) to jump to error-handling middleware. The contract breaks in two ways: doing both — sending a response and also calling next, or letting two paths each respond — which causes a double-send and the 'Cannot set headers after they are sent' error; or doing neither — no response and no next() — which hangs the request until it times out." },
    { q: "How does Express recognize error-handling middleware, and what changed in Express 5?", a: "Express recognizes error middleware by arity: the function must declare exactly four parameters, (err, req, res, next). If you drop the unused next and write three parameters, Express treats it as ordinary middleware and never routes errors to it — a silent bug. Error middleware must also be registered last, after all routes, since it is reached only via next(err). In Express 4, a route handler returning a rejected promise did not automatically reach error middleware, so teams wrapped handlers in an asyncHandler. Express 5 changed this: handlers and middleware returning a promise automatically call next(err) on rejection. The four-argument signature itself did not change." },
    { q: "Contrast the Koa onion model with Fastify's hooks and encapsulation.", a: "Koa middleware is async (ctx, next): code before await next() runs on the way in, and code after it runs on the way out in reverse order — a true onion that makes wrap-around timing and try/catch error handling natural, since a throw propagates to an upstream catch around await next(). Fastify instead splits the single middleware idea into named lifecycle hooks that fire in a fixed order (onRequest, preParsing, preValidation, preHandler, handler, onSend, onResponse), letting you reject as early as onRequest before body parsing. Crucially, Fastify hooks and decorators are encapsulated: registered inside a plugin, they apply only to that plugin's routes, whereas Express middleware leaks down the whole chain from its mount point. Encapsulation keeps large apps scoped." },
  ]}
/>

<Recap lang="en">
A middleware is a function bound by a contract: read the request, optionally act, then do exactly one of pass control with next(), terminate by sending a response, or forward an error with next(err). Breaking it produces the two classic bugs — responding twice ("headers already sent") or responding never (a hang). Express identifies error middleware by its four-parameter arity and, since version 5, auto-forwards rejected promises so the asyncHandler wrapper is obsolete. Koa expresses the same idea as a native async onion where logic before and after await next() runs inbound and outbound. Fastify breaks middleware into fixed lifecycle hooks and adds encapsulation, so plugin-scoped hooks do not leak globally the way Express middleware does. With the request-axis mechanics in hand, the next lesson turns to the wiring axis: inversion of control and how dependencies actually reach a class. Now when you see a `Cannot set headers after they are sent` crash or a middleware that silently swallows errors, you know exactly which part of the contract broke — and where to look first.
</Recap>
