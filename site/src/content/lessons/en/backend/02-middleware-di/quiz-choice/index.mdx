---
concepts:
- middleware-ordering
- dependency-injection
- injection-scopes
- test-doubles
- circular-dependency
- composition-root
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.nestjs.com/fundamentals/injection-scopes
- https://martinfowler.com/articles/injection.html
- https://expressjs.com/en/guide/error-handling/
status: ready
summary: "Multiple-choice synthesis across the middleware and DI unit — the two axes, the middleware contract, injection scopes, the testing seam, and container resolution."
title: 'Middleware and DI: multiple-choice review'
track: backend
unit: 02-middleware-di
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a judgement you make on a real backend — which axis a problem lives on, what the contract forbids, what lifetime an object should have — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can place a problem on the right axis (request vs wiring), reason about middleware ordering and the response contract, choose an injection scope, and read a container failure — the synthesis the six lessons built toward.
</Goal>

<Quiz
  id="02-middleware-di-choice-1"
  lessonSlug="02-middleware-di"
  lang="en"
  question="A handler is slow on every single endpoint, and separately a billing class is impossible to unit-test without hitting the real Stripe API. On which axis does each problem live?"
  choices={[
    { label: "The uniform per-request slowness is a middleware (request-axis) problem; the untestable class is a DI (wiring-axis) problem — something runs on every request, versus a class constructing its own dependency.", correct: true },
    { label: "Both are DI problems, because both involve services.", misconception: "Only the untestable class is a wiring concern. Slowness on every endpoint points at something in the per-request pipeline — the request axis — not at how a class is constructed." },
    { label: "Both are middleware problems, since both happen at request time.", misconception: "The testability problem exists at construction, decided once at startup, independent of any request. It is a wiring-axis (DI) issue, not middleware." },
    { label: "Neither — both are routing problems.", misconception: "Routing only picks which handler runs. Uniform slowness is the pipeline around handlers; untestability is hidden construction. The senior reflex is to name the axis first." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-2"
  lessonSlug="02-middleware-di"
  lang="en"
  question="An Express middleware sends res.json(...) on a validation failure and then also calls next(). Under load it intermittently throws 'Cannot set headers after they are sent'. What rule was broken?"
  choices={[
    { label: "The middleware contract: a middleware must do exactly one of pass control with next(), terminate by sending a response, or forward with next(err) — never both. Responding and calling next() lets a second path respond too, a double-send.", correct: true },
    { label: "The middleware was registered in the wrong order.", misconception: "Order decides what runs around the handler, not this error. The failure is responding twice on one socket — doing both terminate and pass-through in the same middleware." },
    { label: "Express 5 no longer supports res.json().", misconception: "res.json() is unchanged. The defect is calling next() after already sending the response, which causes a second write to a finished response." },
    { label: "The response body was too large for the socket.", misconception: "Body size does not produce this error. It is specifically a second write after the response was already ended once." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-3"
  lessonSlug="02-middleware-di"
  lang="en"
  question="A central Express error handler written as (err, req, res) never receives errors even though earlier code calls next(err). Why, and what is the fix?"
  choices={[
    { label: "Express identifies error middleware by arity — it must declare four parameters (err, req, res, next). A three-arg function is treated as ordinary middleware, so errors are never routed to it. Add the unused next parameter.", correct: true },
    { label: "Error middleware must be registered first, before the routes.", misconception: "It must be registered last, after all routes — but that is not why this one is skipped. Express skips it because it declares three parameters instead of four." },
    { label: "next(err) is not a valid call in Express.", misconception: "next(err) is the correct way to forward an error. The handler is bypassed purely because its parameter count is three, not four." },
    { label: "Errors can only be handled inside the route that threw them.", misconception: "Centralized error middleware is standard practice. The single reason this one is skipped is its arity — it must take four parameters to be recognized." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-4"
  lessonSlug="02-middleware-di"
  lang="en"
  question="Intermittently, user A sees user B's account name. Auth is correct. A service registered as a singleton stores this.currentUser per request. What happened, and what is the cleanest fix?"
  choices={[
    { label: "A singleton is one instance shared across all concurrent requests, so the currentUser field is shared state: request B overwrites what request A set, then A reads B's value. Fix: keep the singleton stateless and pass the user as a method argument.", correct: true },
    { label: "Make every service transient so each call gets a fresh instance.", misconception: "Transient creates many instances but does not cleanly give per-request identity, and it is heavier than simply passing the value. The shared-field race is solved by not storing request state on a singleton." },
    { label: "The database returned the wrong row under concurrency.", misconception: "The data layer is correct. The leak is a shared mutable field on a process-wide singleton being overwritten by concurrent requests, not a query defect." },
    { label: "Make the whole module request-scoped to be safe.", misconception: "That maximizes scope-bubbling cost, recreating the module's graph on every request to avoid a value you could simply pass as an argument. Prefer the stateless singleton." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-5"
  lessonSlug="02-middleware-di"
  lang="en"
  question="A behavior-preserving refactor splits one repo.save() into two saves inside a transaction. Final state is identical, yet forty tests go red. What does this reveal?"
  choices={[
    { label: "Those tests assert interactions — mock expectations on call count and arguments — rather than the resulting state, so they are coupled to the implementation. A mock expectation is itself an assertion about the code's internal moves.", correct: true },
    { label: "The refactor introduced a real bug.", misconception: "Behavior is unchanged and the final persisted state is identical. The tests broke on call shape, not on any observable defect — that is the signature of over-mocking." },
    { label: "The repository should not have been injected at all.", misconception: "Injection created the seam that made the tests possible; the problem is what the tests assert through that seam. State-based assertions would have survived the refactor." },
    { label: "Code coverage was too low to catch the change.", misconception: "Coverage was high — that is the irony. The issue is the kind of assertion (interaction vs state), not the amount of coverage." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-6"
  lessonSlug="02-middleware-di"
  lang="en"
  question="An app boots in dev but crashes in prod with 'Nest can't resolve dependencies of UserService'. AuthService now imports UserService, which already imported AuthService. How do you read this, and what is the real fix?"
  choices={[
    { label: "A circular dependency. Resolution is a topological sort that exists only for an acyclic graph, so the container refuses; it passed in dev only because module load order papered over the cycle. Real fix: extract shared logic into a third class both depend on. forwardRef only hides it.", correct: true },
    { label: "Wrap one side in forwardRef and ship it — that resolves the cycle.", misconception: "forwardRef defers one side's resolution to break the deadlock but preserves the design flaw. A cycle almost always means two classes that should be one, or a missing shared third class." },
    { label: "The container is buggy; downgrade the framework version.", misconception: "The container is correctly reporting that a circle has no valid construction order. The bug is in the dependency graph, not the resolver." },
    { label: "The two services must be moved into the same module.", misconception: "Module boundaries are not the cause — two classes in one module can still cycle. The problem is the dependency cycle itself, which only an acyclic redesign removes." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one habit: name the axis, then apply its discipline. The request axis (middleware) demands one ending per middleware and a deliberate order; error middleware needs four parameters. The wiring axis (DI) means classes declare needs and a composition root supplies them — default to stateless singletons, escalate scope only when forced, and read test pain as a design signal (mock the boundaries you don't own, fake what you do). A container is that composition root mechanized: it topologically sorts the graph, refuses cycles, and should fail fast at boot. Every question here resolves back to those two axes and the cost each carries.
</Recap>
