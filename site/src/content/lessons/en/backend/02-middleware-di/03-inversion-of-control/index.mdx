---
slug: 03-inversion-of-control
lang: en
track: backend
unit: 02-middleware-di
order: 3
title: "Inversion of control: how dependencies reach a class"
summary: "A class that constructs its own collaborators is welded to them. Inversion of control hands a class what it needs from the outside — and the choice between constructor injection, a service locator, and in-class `new` decides how testable and honest your code is."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs:
  - backend/02-middleware-di/02-writing-middleware
deepensInto:
  - backend/02-middleware-di/04-di-scopes-lifecycles
spiral: ["latency"]
concepts: ["inversion-of-control", "constructor-injection", "service-locator", "composition-root", "coupling", "test-seam"]
sources:
  - https://martinfowler.com/articles/injection.html
  - https://docs.nestjs.com/providers
  - https://blog.ploeh.dk/2011/07/28/CompositionRoot/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A team wants to test their `OrderService`. The test creates one — and a real Postgres connection opens, a real Stripe call fires, and a real email is sent to a real address. They cannot test the logic without the entire production stack, because the constructor does `this.db = new PgClient()`, `this.payments = new StripeClient()`, `this.mail = new Mailer()`. The logic was never the problem. The wiring was.
</Hook>

<Crux>A class should declare what it needs and receive it from outside, not construct it — that inversion turns welded code into swappable code.</Crux>

<Explanation>

## The coupling created by `new`

When a class writes `new EmailService()` inside itself, it has made an irreversible decision for every caller: this code *is* the real email service, always. You cannot substitute a fake in a test, swap a different implementation per environment, or reuse the class with a different collaborator. The dependency is **hidden** (invisible in the type) and **hard** (welded at construction).

## Inversion of control

**Inversion of control (IoC)** flips who decides. Instead of the class choosing its concrete collaborator, the class declares an abstract need and *something else* supplies the concrete one. Dependency injection is the most common form of IoC: dependencies are *injected* from outside rather than constructed inside.

The cleanest injection is **constructor injection**:

```
class OrderService {
  constructor(
    private db: Database,
    private payments: PaymentGateway,
    private mail: Mailer,
  ) {}
}
```

Now the constructor signature is an honest, compile-time-checked list of everything the class needs. A test passes in fakes; production passes in real ones. The dependency became **visible** and **soft**.

## Service locator: injection's tempting cousin

A **service locator** also avoids `new`, but the class asks a global registry for its dependencies: `const db = Container.get('Database')`. This looks like DI but reintroduces the original sin — the dependencies are hidden again (the constructor no longer lists them), and the class is coupled to the locator. It is widely considered an anti-pattern in application code precisely because it hides what a class needs and only fails at runtime, not at compile time or startup.

<Inset kind="why" lang="en">
Why is service locator an anti-pattern if it also removes the `new`? Both remove the hard-coded construction, but they differ on *honesty*. With constructor injection, the dependencies are right there in the signature — the compiler and the reader both see them, and a missing one fails at startup. With a service locator, the constructor looks empty while the class secretly reaches into a global container at runtime; a missing or misconfigured dependency surfaces as a runtime crash deep in a code path, and tests must configure the global locator instead of just passing arguments. The locator is legitimate in one place — inside the DI framework or composition root itself — but not scattered through business logic.
</Inset>

## The composition root

If classes no longer construct their dependencies, *something* must. That something is the **composition root**: a single place near the application's entry point where the entire object graph is assembled — the only place that names concrete implementations. Everything downstream depends only on abstractions. Centralizing construction means there is exactly one spot to change wiring, swap implementations, or read off the whole dependency graph. A DI container automates this assembly, but the principle holds even with hand-wiring: keep construction at the edge, keep logic in the middle dependency-free.

<div data-lesson-visual class="overflow-x-auto my-6">

| Approach | Dependencies are | Fails when | Testable |
|---|---|---|---|
| `new` inside class | Hidden + hard-wired | Never swappable | No |
| Service locator | Hidden, fetched at runtime | At runtime, deep in a path | Awkward |
| Constructor injection | Visible in signature | At startup / compile | Yes |

</div>

<Quiz
  id="ioc-q1"
  lessonSlug="03-inversion-of-control"
  lang="en"
  question="Why is `new PgClient()` inside a service constructor a testability problem?"
  choices={[
    { label: "Constructors cannot perform I/O", misconception: "Constructors can perform I/O; that is not the rule being broken. The problem is that the dependency is welded in and cannot be substituted." },
    { label: "It hard-wires the class to the real implementation, so a test cannot substitute a fake without a real database", correct: true },
    { label: "`new` is slower than injection", misconception: "Construction speed is not the issue. The harm is the inability to swap the dependency for a test double." },
    { label: "PgClient is not thread-safe", misconception: "Thread safety is unrelated. The testability problem is the hard coupling created by constructing the dependency internally." },
  ]}
/>

<Quiz
  id="ioc-q2"
  lessonSlug="03-inversion-of-control"
  lang="en"
  question="A class has an empty constructor but calls `Container.get('Mailer')` internally whenever it needs to send mail. What is the main objection?"
  choices={[
    { label: "It is slower than constructor injection", misconception: "Speed is not the core objection. The problem is that the dependency is hidden and only fails at runtime." },
    { label: "It is a service locator: the dependency is hidden from the constructor and fails at runtime rather than startup, hurting clarity and testing", correct: true },
    { label: "Containers cannot store mailers", misconception: "Containers can store any service. The objection is the locator pattern itself hiding the dependency, not what is stored." },
    { label: "It uses too much memory", misconception: "Memory is not the issue. The locator hides dependencies and defers failure to runtime, which is the real cost." },
  ]}
/>

<DragOrder
  id="ioc-drag1"
  lessonSlug="03-inversion-of-control"
  lang="en"
  prompt="Order the steps of refactoring a welded class to constructor injection:"
  items={[
    "Identify the `new` calls hidden inside the class",
    "Define abstractions (interfaces/types) for those dependencies",
    "Add them as constructor parameters typed by the abstractions",
    "Move the concrete construction to the composition root",
    "In tests, pass fakes through the constructor instead of real services",
  ]}
/>

</Explanation>

<KeyTakeaway>Constructing dependencies with `new` inside a class welds it to concrete implementations and hides them, making it untestable. Inversion of control flips this: the class declares abstract needs and receives concretes from outside. Constructor injection is the honest form — dependencies are visible in the signature and fail at startup. A service locator avoids `new` but re-hides dependencies and fails at runtime. The composition root is the single edge where the real graph is assembled.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ioc-retrieval"
  lang="en"
  questions={[
    { q: "What two properties make in-class `new` calls a coupling problem, and how does constructor injection fix them?", a: "An in-class `new EmailService()` makes the dependency hidden — it is invisible in the class's type or constructor signature — and hard — welded at construction so no caller can substitute another implementation. Together that means you cannot use a fake in a test, swap implementations per environment, or reuse the class with a different collaborator. Constructor injection fixes both: the class lists its dependencies as constructor parameters typed by abstractions, so they become visible (the compiler and reader see them, a missing one fails at startup) and soft (any caller, including a test, can pass in a different concrete instance). The construction decision moves out of the class." },
    { q: "Why is a service locator considered an anti-pattern even though it removes the hard-coded `new`?", a: "A service locator has the class ask a global registry for its dependencies, e.g. Container.get('Database'), so the constructor looks empty while the class secretly depends on whatever the locator holds. It removes the explicit `new` but reintroduces hidden dependencies: the signature no longer tells you what the class needs, and the class is now coupled to the locator. Worse, a missing or misconfigured dependency fails at runtime deep in a code path rather than at startup or compile time, and tests must configure the global locator instead of just passing arguments. It is legitimate only inside the DI framework or composition root, not scattered through business logic." },
    { q: "What is the composition root and why centralize construction there?", a: "The composition root is a single place near the application entry point where the entire object graph is assembled — the only location that names concrete implementations, while everything downstream depends only on abstractions. Centralizing construction there means there is exactly one spot to change wiring, swap an implementation, or read off the whole dependency graph, and it keeps business logic free of construction concerns. A DI container automates this assembly, but the principle holds even with manual wiring: construct at the edge, keep the logic in the middle dependency-free. This is what makes the test seam exist precisely where dependencies are injected." },
  ]}
/>

<Recap lang="en">
Constructing collaborators with `new` inside a class welds it to concrete implementations and hides them in the process, which is why a service built that way drags the whole production stack into every test. Inversion of control flips the decision: the class declares abstract needs and receives concrete instances from outside. Constructor injection is the honest form — dependencies are listed in the signature, visible to compiler and reader, and a missing one fails fast at startup. A service locator removes the `new` but re-hides dependencies behind a global registry and defers failure to runtime, which is why it is an anti-pattern outside framework internals. All of this requires a composition root: one edge where the real object graph is assembled, leaving the logic in the middle dependency-free. The next question is the lifetime of those wired objects — singletons, request scope, transient — and the subtle bugs each scope invites.
</Recap>
