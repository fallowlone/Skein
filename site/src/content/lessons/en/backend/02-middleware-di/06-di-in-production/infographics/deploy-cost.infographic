infographic compare-binary-horizontal-underline-text-vs
data
  title Long-running server vs serverless cold start
  desc The same ~150ms reflection-and-decorator graph build at boot — amortized to nothing vs paid on every invocation
  compares
    - label Long-running server
      children
        - label 150ms boot tax paid once
        - label Invisible against months of uptime
        - label Amortizes to effectively zero
        - label Container automation earns its cost
    - label Serverless cold start
      children
        - label 150ms on every cold start
        - label Billed per millisecond
        - label p99 latency for the unlucky request
        - label Hand-wire or use build-time DI
theme
  palette #14b8a6 #f43f5e
