---
concepts:
- singleton-scope
- request-scope
- transient-scope
- scope-bubbling
- shared-mutable-state
- lifecycle
deepensInto:
- backend/02-middleware-di/05-testing-and-seams
estMin: 15
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- backend/02-middleware-di/03-inversion-of-control
slug: 04-di-scopes-lifecycles
sources:
- https://docs.nestjs.com/fundamentals/injection-scopes
- https://learn.microsoft.com/en-us/dotnet/core/extensions/dependency-injection
- https://docs.spring.io/spring-framework/reference/core/beans/factory-scopes.html
spiral:
- latency
status: ready
summary: 'A wired object has a lifetime: created once, once per request, or once per
  injection. Choosing wrong is two opposite bugs — a singleton holding per-request
  state leaks data between users, while request scope quietly recreates half your
  graph on every call.'
title: 'DI scopes and lifecycles: singleton, request, transient'
track: backend
unit: 02-middleware-di
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import scopeBubblingCost from "./infographics/scope-bubbling-cost.svg?raw";

<Hook>
A bug report says user A occasionally sees user B's account name. The auth code is correct. The leak is a `currentUser` field on a service that was registered as a singleton — one instance shared by every concurrent request. Request A sets the field, request B overwrites it a millisecond later, and request A reads B's value. Nobody wrote a security bug. Someone chose the wrong lifetime.
</Hook>

<Crux>Every injected object has a lifetime; the traps are a singleton holding request state and request scope silently recreating the graph.</Crux>

<Explanation>

## Three lifetimes

A DI container can create an instance at three cadences:

- **Singleton** — one instance for the whole process, created once and shared by every request. The default in NestJS, Spring, and most containers. Fast and memory-light, because nothing is recreated.
- **Request-scoped** — one instance per incoming request, shared within that request, discarded after. Useful for per-request context (the current user, a request ID, a per-request transaction).
- **Transient** — a fresh instance every time it is injected. Useful for stateful helpers that must not be shared at all.

Together these three lifetimes map the full cost spectrum: singleton amortizes construction to zero per request, request scope pays one allocation per request, and transient pays one per injection site. The wrong choice in either direction — singleton holding state, or request scope bubbling — turns that cost into a correctness bug.

## The singleton-with-state trap

The Hook's bug is the canonical singleton mistake: a singleton must be **stateless** with respect to a request, because it is shared across all concurrent requests. Storing `this.currentUser` on a singleton is a data-leak waiting to happen — concurrent requests race on the shared field. The fix is either to make the per-request data flow through method arguments (pass `user` in, do not store it) or to make the service request-scoped so each request gets its own instance.

## The opposite trap: scope bubbling

Request scope looks like the safe default, so teams reach for it — and pay a hidden cost. **Scope bubbles up the injection chain**: if a request-scoped provider is injected into a controller, that controller becomes request-scoped too, and so does anything that depends on *it*. One request-scoped logger near the bottom can "infect" the entire graph above it, so the container reinstantiates a large slice of your services on **every single request** instead of reusing singletons. That is extra allocation, extra GC pressure, and measurable latency — for what is often just a convenience.

Put numbers on it. A singleton is constructed once, ever — zero per-request allocations on the wiring axis. Flip one low-level provider to request scope and bubble it through a 10-deep controller subgraph, and now the container runs ~10 constructor calls and allocates ~10 objects *per request*. At 5,000 req/s that is 50,000 short-lived objects per second the GC must reclaim; in a NestJS service this commonly shows up as a few hundred microseconds to ~1ms added at p99 and a visibly busier young-generation GC. Worse is the *request-scoped leak*: if a request-scoped provider registers a listener, opens a handle, or pushes into a module-level array and nothing tears it down at request end, each request leaves residue behind. A handler that subscribes to an `EventEmitter` per request without unsubscribing can grow heap on the order of tens of MB/hour under steady load — a slow leak that looks fine in a 60-second local test and OOM-kills the pod at 3 a.m. The senior reflex: request scope is not "free safety," it is a per-request allocation budget and a teardown obligation.

<Infographic
  svg={scopeBubblingCost}
  label="Column chart of per-request object allocations by DI scope: a singleton allocates 0, the request-scoped provider you intended allocates 1, but letting it bubble up a 10-deep injection graph forces about 10 constructor calls per request."
  caption="One request-scoped provider that bubbles up a 10-deep graph turns a zero-allocation singleton into ~10 objects per request — 50,000/sec at 5,000 req/s for the GC to reclaim."
  hue="var(--d-backend)"
/>

<Inset kind="why" lang="en">
Why does request scope bubble but transient does not? Scope bubbling is about correctness of sharing. If service A is request-scoped (one instance per request) and controller C injects A, then C cannot be a singleton — a single shared C would have to hold a reference to *some* A, but there is a different A per request. So C must also be created per request; the request lifetime propagates up to every consumer. Transient is different: a singleton can safely hold a transient dependency because the transient was created fresh at injection time and the singleton simply keeps that one instance. Transient changes how many instances exist, not how long the consumer must live, so it does not force its consumers to a shorter lifetime.
</Inset>

## Picking the lifetime

The rule that avoids both traps: **default to singleton and keep singletons stateless**; reach for request scope only when you genuinely need per-request identity that cannot be passed as an argument; use transient sparingly for deliberately unshared stateful helpers. Most "I need the current user here" cases are better solved by passing it through the call than by making the service request-scoped and dragging the whole graph down with it.

<div data-lesson-visual class="overflow-x-auto my-6">

| Scope | Instances | Best for | Trap |
|---|---|---|---|
| Singleton | One per process | Stateless services, clients, pools | Holding per-request state → data leak |
| Request | One per request | Current user, request transaction | Bubbles up, recreates the graph per request |
| Transient | One per injection | Unshared stateful helpers | Surprising count of instances |

</div>

<Quiz
  id="scopes-q1"
  lessonSlug="04-di-scopes-lifecycles"
  lang="en"
  question="A singleton service stores `this.currentUser` and occasionally one user sees another's data. Why?"
  choices={[
    { label: "The database returned the wrong row", misconception: "The data layer is correct. The leak is from a shared singleton field being overwritten by concurrent requests." },
    { label: "A singleton is shared across all concurrent requests, so one request overwrites the field another request later reads", correct: true },
    { label: "Request scope is too slow", misconception: "Speed is not the issue here; correctness is. The singleton is sharing per-request state across users." },
    { label: "Transient scope duplicated the user", misconception: "Transient creates fresh instances; it would not cause cross-user leakage. The cause is shared singleton state." },
  ]}
/>

<Quiz
  id="scopes-q2"
  lessonSlug="04-di-scopes-lifecycles"
  lang="en"
  question="Why can making one low-level provider request-scoped hurt performance across many services?"
  choices={[
    { label: "Request scope disables caching of database rows", misconception: "It does not touch query caching. The cost is that request scope bubbles up and forces re-instantiation of dependent services." },
    { label: "Scope bubbles up the injection chain, so every consumer of the request-scoped provider also becomes request-scoped and is recreated per request", correct: true },
    { label: "Request-scoped providers cannot be garbage collected", misconception: "They are collected after the request; the cost is the per-request re-creation of the whole dependent subgraph, not a leak." },
    { label: "It forces all providers to become transient", misconception: "It does not convert others to transient. It propagates the request lifetime upward to consumers." },
  ]}
/>

<Quiz
  id="scopes-q3"
  lessonSlug="04-di-scopes-lifecycles"
  lang="en"
  question="A singleton needs the current request's user. What is the cleanest fix that avoids both traps?"
  choices={[
    { label: "Store the user on the singleton as a field", misconception: "That is exactly the data-leak trap: concurrent requests race on the shared field." },
    { label: "Pass the user as a method argument so the singleton stays stateless, instead of making the service request-scoped", correct: true },
    { label: "Make every service transient", misconception: "Transient creates many instances and still does not give per-request identity cleanly; passing the value is simpler and avoids bubbling." },
    { label: "Make the whole module request-scoped", misconception: "That maximizes the bubbling cost, recreating the entire module's graph per request to avoid a value you could simply pass." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Three DI lifetimes from longest to shortest, with their traps"
  hue="var(--d-backend)"
  layers={[
    { label: "Singleton", note: "one per process — stateless services, clients, pools. Trap: storing per-request state leaks data across concurrent users" },
    { label: "Request-scoped", note: "one per request — current user, request transaction. Trap: scope bubbles up, forcing re-instantiation of the whole graph per request" },
    { label: "Transient", note: "one per injection — unshared stateful helpers. Does not bubble; changes instance count, not consumer lifetime" },
  ]}
  caption="Lifetimes flow top (longest) to bottom (shortest). Mixing them incorrectly causes either a data-leak (singleton holds request state) or a hidden perf cost (request scope infects singletons above it)."
/>

<KeyTakeaway>Injected objects have a lifetime: singleton (one per process, the default), request-scoped (one per request), or transient (one per injection). A singleton holding per-request state leaks data across concurrent requests; the fix is to stay stateless and pass per-request data as arguments. Request scope bubbles up the injection chain, so one request-scoped provider can force re-instantiation of the whole graph every request. Default to stateless singletons; use request scope only when truly needed.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="scopes-retrieval"
  lang="en"
  questions={[
    { q: "What are the three DI lifetimes and what is each best for?", a: "Singleton creates one instance for the whole process, shared by every request — the default in NestJS, Spring, and most containers, ideal for stateless services, clients, and pools because nothing is recreated. Request-scoped creates one instance per incoming request, shared within that request and discarded after, useful for genuine per-request context like the current user, a request ID, or a per-request transaction. Transient creates a fresh instance every time it is injected, useful for stateful helpers that must never be shared. The default should be singleton with stateless services; the other two are exceptions for specific needs." },
    { q: "Why is storing per-request state on a singleton a data-leak bug, and how is it fixed?", a: "A singleton is one instance shared across all concurrent requests, so any field on it is shared global state. If it stores this.currentUser, request A sets the field and request B overwrites it a moment later, so A then reads B's value — a cross-user data leak even though the auth code is correct. The fix is to keep singletons stateless with respect to the request: either flow per-request data through method arguments (pass the user in rather than storing it) or, if per-request identity is genuinely required, make that service request-scoped so each request gets its own instance. Passing the argument is usually simpler and avoids the scope-bubbling cost." },
    { q: "Explain scope bubbling and why request scope can be expensive while transient is not.", a: "Scope bubbles up the injection chain because of sharing correctness: if a request-scoped provider A is injected into controller C, C cannot be a singleton — a single shared C would need to reference a single A, but there is a different A per request — so C must also become request-scoped, and so must anything depending on C. One request-scoped provider low in the graph can therefore force the container to reinstantiate a large slice of services on every request, adding allocation, GC pressure, and latency. Transient does not bubble: a singleton can safely hold a transient dependency because the transient is created fresh at injection and then kept, so transient changes how many instances exist, not how long the consumer must live." },
  ]}
/>

<Recap lang="en">
Every object the container wires has a lifetime, and the choice is a correctness-and-cost decision. Singleton — one instance per process — is the default and the right home for stateless services, clients, and pools, but storing per-request state on one leaks data across concurrent users, the canonical singleton bug. Request scope gives each request its own instance and is the honest fix for genuine per-request identity, but it bubbles up the injection chain: one request-scoped provider can force its consumers, and theirs, to be recreated on every request, trading reuse for allocation and latency. Transient creates a fresh instance per injection without forcing consumers to a shorter lifetime. The discipline is to default to stateless singletons and pass per-request values as arguments, escalating scope only when there is no cleaner option. Lifetimes set up the next concern: with the graph wired and scoped, how DI becomes the seam that makes the system testable. Now when you see a data-leak bug or a latency spike that only appears under load, check scope first — a singleton holding mutable per-request state or a request-scoped provider quietly infecting the graph above it accounts for more incidents than most engineers expect.
</Recap>
