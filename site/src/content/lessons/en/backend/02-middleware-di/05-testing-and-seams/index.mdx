---
slug: 05-testing-and-seams
lang: en
track: backend
unit: 02-middleware-di
order: 5
title: "DI as a testing seam: fakes, mocks, and the boundary that matters"
summary: "The whole point of injecting dependencies is the seam it creates: a place to substitute a test double. But there are two doubles with opposite purposes, and the most common testing failure is mocking everything until the test asserts the implementation instead of the behavior."
estMin: 16
status: ready
lessonType: topic
level: senior
prereqs:
  - backend/02-middleware-di/04-di-scopes-lifecycles
deepensInto:
  - backend/02-middleware-di/06-di-in-production
spiral: ["latency"]
concepts: ["test-seam", "fake", "mock", "stub", "test-double", "boundary", "over-mocking", "london-vs-classicist"]
sources:
  - https://martinfowler.com/articles/mocksArentStubs.html
  - https://martinfowler.com/bliki/TestDouble.html
  - https://docs.nestjs.com/fundamentals/testing
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
A team is proud of their `OrderService` test suite: 100% coverage, every dependency mocked, all green. Then a refactor that changes nothing about behavior — splitting one repository method into two — turns forty tests red. The tests were not checking that orders get placed. They were checking that `repo.save` was called exactly once with exactly these arguments. The seam DI gave them was real; they just pointed it at the wrong thing.
</Hook>

<Crux>DI creates a seam for a test double: a fake checks state, a mock checks interactions — confusing them welds tests to the implementation.</Crux>

<Explanation>

## The seam is the payoff

Everything in this unit — constructor injection, the composition root, abstractions instead of `new` — pays off here. Because `OrderService` receives a `PaymentGateway` rather than constructing a `StripeClient`, a test can pass in a substitute. That substitute is a **test double**, and the injection point is the **seam**: the join where production wiring is swapped for test wiring. No seam, no isolated unit test. This is why "is it testable?" and "are the dependencies injected?" are nearly the same question.

## Two doubles, opposite purposes

The word "mock" is used loosely for every substitute, but the distinction is the whole lesson:

- A **stub / fake** stands in for a dependency and provides *state*. A fake `UserRepository` backed by an in-memory `Map` behaves like the real thing: you save a user, you can read it back. Your assertions check the **result** — the order ended up persisted, the returned total is correct.
- A **mock** is programmed with *expectations about calls*. It asserts that `payment.charge(amount)` was called once with this argument. Your assertions check the **interaction**, not the outcome.

The first verifies *what the system did*; the second verifies *how it did it*. Both are legitimate, but they fail differently — and the Hook is what happens when you use mocks for something a fake should have covered.

## Classicist vs London, and why it matters

This is the **classicist vs mockist** ("London school") split. Mockists mock every collaborator and assert interactions, so each unit is tested in total isolation. Classicists use real objects or fakes for collaborators they own, and reserve mocks for awkward boundaries. The practical consequence is **coupling to structure**: a fully-mocked test knows the exact call shape of its dependency, so any refactor that preserves behavior but changes call shape breaks the test. That is the forty-red-tests bug. Tests that assert through state survive refactors because they only care about the observable result.

<Inset kind="why" lang="en">
Why do interaction tests break on refactors that change nothing? Because a mock expectation *is* an assertion about the implementation. `expect(repo.save).toHaveBeenCalledTimes(1)` encodes "the production code calls save exactly once." Split that into two saves inside a transaction — identical behavior, identical final state — and the expectation is now false even though nothing a user could observe changed. The test was measuring the code's internal moves, not its output. State-based tests don't have this problem: they ask "after running, is the order persisted and the total right?", which is invariant under any refactor that preserves behavior. Mocks are not wrong — they are the right tool for verifying an effect you cannot observe through state, like "an email was sent" — but every mock is a small bet that this particular call shape is part of the contract.
</Inset>

## Mock at the boundary, fake what you own

The discipline that avoids over-mocking: **mock at the edges of your system, use real objects or fakes inside it.** Code you own and control — domain services, your own repositories — can be wired together with real instances or in-memory fakes, so tests exercise actual collaboration. The things worth mocking are the *boundaries you do not control or cannot afford in a test*: the payment gateway, the email sender, the clock, the third-party HTTP call. These are exactly the dependencies where you want to assert "we called Stripe with this amount" because the call itself is the externally-visible effect. The seam is most valuable precisely at the system boundary — which is also where DI matters most.

## Over-mocking is a design smell

When a unit test needs ten mocks to construct the subject, the test is not the problem — the design is. A class that requires ten collaborators is doing too much, and the painful test is the messenger. The reflex of a senior engineer is to read test pain as feedback about coupling, not as a reason to reach for more mocking machinery. Hard-to-test usually means hard-to-change.

<div data-lesson-visual class="overflow-x-auto my-6">

| Double | Provides | You assert | Breaks on |
|---|---|---|---|
| Fake / stub | Realistic state | The result/outcome | Behavior change only |
| Mock | Recorded expectations | The interaction (calls) | Any call-shape change |
| Real object | Actual behavior | The result/outcome | Behavior change only |

</div>

<Quiz
  id="seams-q1"
  lessonSlug="05-testing-and-seams"
  lang="en"
  question="A behavior-preserving refactor splits one `repo.save()` into two saves inside a transaction, and dozens of tests go red. What does this reveal about those tests?"
  choices={[
    { label: "The refactor introduced a bug", misconception: "Behavior is unchanged and final state is identical; the tests broke on call shape, not on a real defect." },
    { label: "The tests assert interactions (mock expectations on call count) rather than the resulting state, so they are coupled to the implementation", correct: true },
    { label: "The repository should not be injected", misconception: "Injection is what made the seam possible; the problem is what the tests assert through that seam, not that it exists." },
    { label: "Coverage was too low", misconception: "Coverage was high; the issue is the kind of assertion, not the amount. Interaction tests coupled to structure broke." },
  ]}
/>

<Quiz
  id="seams-q2"
  lessonSlug="05-testing-and-seams"
  lang="en"
  question="Which dependency is the best candidate to replace with a mock that asserts the call, rather than a fake that provides state?"
  choices={[
    { label: "Your own in-memory-backed UserRepository", misconception: "Code you own is better tested with a real object or fake so assertions check state; mocking it couples the test to its call shape." },
    { label: "An external payment gateway, where 'we charged this amount' is itself the externally-visible effect", correct: true },
    { label: "A pure formatting function with no dependencies", misconception: "Pure functions need no double at all; you call them and assert the return value directly." },
    { label: "A domain service you wrote and control", misconception: "Internal collaborators are best wired as real objects or fakes; mocking them tests structure, not behavior." },
  ]}
/>

<Quiz
  id="seams-q3"
  lessonSlug="05-testing-and-seams"
  lang="en"
  question="A unit test needs ten mocks just to instantiate the class under test. What is the senior reading of this pain?"
  choices={[
    { label: "The test framework is inadequate; switch to a better mocking library", misconception: "Tooling is not the cause. Needing ten mocks signals the class has too many collaborators." },
    { label: "It is a design signal: the class has too many dependencies and is doing too much; hard-to-test means hard-to-change", correct: true },
    { label: "Mocks should be replaced with real network calls", misconception: "Real external calls make tests slow and flaky; the issue is the class's coupling, not the use of doubles per se." },
    { label: "The composition root is misconfigured", misconception: "The composition root assembles production wiring; the test pain comes from the subject's own dependency count, not the root." },
  ]}
/>

</Explanation>

<KeyTakeaway>DI's payoff is the seam: the injection point where a test substitutes a double. A fake/stub provides realistic state and lets you assert the outcome, surviving any behavior-preserving refactor. A mock records expectations and asserts interactions, so it breaks whenever the call shape changes — useful only when the call itself is the externally-visible effect. The discipline: mock at boundaries you don't control (payment, email, clock), use real objects or fakes for code you own, and read "needs ten mocks" as a design smell, not a tooling gap.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="seams-retrieval"
  lang="en"
  questions={[
    { q: "What is the test seam, and how does DI create it?", a: "The seam is the join where production wiring is swapped for test wiring — the injection point where a class receives a collaborator. Because constructor injection has the class accept, say, a PaymentGateway rather than constructing a StripeClient internally, a test can pass in a substitute (a test double) at that exact point. Without injection there is no seam and no way to isolate the unit, which is why 'is it testable?' and 'are the dependencies injected?' are almost the same question. The composition root assembles the real graph in production; the test assembles a different graph with doubles at the seams." },
    { q: "What is the difference between a fake/stub and a mock, and how do they fail differently?", a: "A fake or stub stands in for a dependency and provides state — for example an in-memory-Map-backed repository you can save to and read back — so your assertions check the result or outcome (the order is persisted, the total is right). A mock is programmed with expectations about calls and asserts the interaction, e.g. that payment.charge(amount) was called once with this argument. They fail differently: a state-based fake only breaks when behavior actually changes, so it survives behavior-preserving refactors; a mock breaks on any change to call shape, because a mock expectation is itself an assertion about the implementation. Splitting one save into two inside a transaction keeps the final state identical but makes 'called once' false." },
    { q: "What is the 'mock at the boundary, fake what you own' rule and why does over-mocking signal a design problem?", a: "The discipline is to mock at the edges of the system — boundaries you don't control or can't afford in a test, like the payment gateway, email sender, clock, or third-party HTTP call — and use real objects or in-memory fakes for code you own, like domain services and your own repositories. At a boundary the call itself is the externally-visible effect, so asserting 'we called Stripe with this amount' is legitimate; inside the system, real collaboration tested through state is more robust. Over-mocking — needing ten mocks just to instantiate the subject — is a design smell: the class has too many collaborators and is doing too much. Senior engineers read hard-to-test as hard-to-change and fix the design rather than adding more mocking machinery." },
  ]}
/>

<Recap lang="en">
The seam that dependency injection creates is the entire reason testability and injection are the same conversation: the injection point is where production wiring gives way to a test double. But "double" hides a fork. A fake or stub supplies realistic state and lets assertions check the outcome, so it only breaks when behavior truly changes; a mock records call expectations and asserts interactions, so it breaks on any refactor that alters call shape — the cause of a behavior-preserving change turning dozens of tests red. The classicist discipline keeps tests robust: mock the boundaries you do not own (payment, email, clock, external HTTP), where the call itself is the visible effect, and wire real objects or fakes for the code you control, asserting through state. And when a test needs ten mocks just to stand the subject up, the pain is the design talking — too many collaborators, too much responsibility. With the seam understood, the last lesson turns to what a real DI container does in production: resolution graphs, circular dependencies, eager startup, and when not to use one at all.
</Recap>
