infographic compare-binary-horizontal-underline-text-vs
data
  title Two clocks, started together — ending apart
  desc Why SIGTERM lands instantly but routing keeps sending you traffic
  compares
    - label Signal path
      children
        - label Signal path — the fast clock
        - label kubelet sends it, one local hop
        - label Delivered in milliseconds
        - label Listener closes immediately
    - label Routing path
      children
        - label Routing path — the slow clock
        - label API server to every kube-proxy
        - label External LB / polling ingress lag
        - label Drains under 1s small, 10–30s large
theme
  palette #14b8a6 #f43f5e
