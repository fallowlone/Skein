---
concepts:
- termination-sequence
- deregistration-race
- reverse-dependency-order
- grace-period-budget
- requeue-idempotency
- fleet-coordination
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://cloud.google.com/blog/products/containers-kubernetes/kubernetes-best-practices-terminating-with-grace
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
status: ready
summary: "Free-recall prompts across the graceful-shutdown unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Graceful shutdown: free-recall review'
track: backend
unit: 07-graceful-shutdown
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the shutdown sequence stick when you need it during a 2 a.m. deploy.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — the termination sequence, the deregistration race, reverse-dependency teardown, the deadline budget, requeue safety, and fleet coordination — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="07-graceful-shutdown-short-retrieval"
  lang="en"
  questions={[
    { q: "Walk the Kubernetes pod termination sequence, and explain why SIGTERM and SIGKILL are categorically different.", a: "The pod is marked Terminating and removed from the Service's endpoints; the preStop hook runs if defined and the orchestrator blocks on it; SIGTERM (signal 15) is delivered to PID 1; the orchestrator waits terminationGracePeriodSeconds (default 30s, covering preStop plus post-SIGTERM combined); then SIGKILL (signal 9) fires if the process is still alive. SIGTERM is a request — it interrupts the process and runs whatever handler you registered, giving you the chance to drain and clean up. SIGKILL is a command to the kernel, never delivered to your code: no handler, no cleanup, no final flush. So the entire game is to finish during SIGTERM so SIGKILL never has to fire, which makes the grace period a hard deadline enforced by an uncatchable signal." },
    { q: "What is the PID 1 trap, and what are the two fixes?", a: "Inside a container the first process started becomes PID 1, which is the process the orchestrator signals and which the kernel treats specially. Two traps follow. First, signals go only to PID 1, so launching the app under a shell — sh -c 'node server.js' — makes the shell PID 1, and many shells do not forward SIGTERM to their children, so the app never sees the signal, drains nothing, and is SIGKILLed at the deadline. Second, PID 1 has no default signal handlers: the kernel withholds the usual 'terminate on SIGTERM' default from the protected init slot, so even as PID 1 a missing handler means SIGTERM is silently ignored. The fixes are to make your app PID 1 with the exec form (CMD ['node','server.js'] or exec node server.js) or run a tiny init like tini/dumb-init that forwards signals and reaps zombies — and always register the handler explicitly." },
    { q: "Describe the deregistration race and the two-lever fix; why fail readiness but keep liveness passing?", a: "Termination starts two clocks from one event: the kubelet sends SIGTERM in milliseconds (fast, local), while endpoint deregistration is slow and distributed — the not-ready status must reach the API server, propagate to every node's kube-proxy, and reach any external or polling load balancer, an eventually-consistent path taking under a second on small clusters but 10–30s on large ones. If the handler closes the listener the instant SIGTERM lands, every request the load balancer still routes during that window hits a closed port and the client gets connection-refused. The fix is ordering: fail the readiness probe first to start the deregistration clock, and add a preStop sleep (≈5–15s, sized to real propagation) so the process keeps accepting until routing has caught up, after which the listener closes safely. You fail readiness (no new traffic) but keep liveness passing because a failing liveness probe tells the orchestrator the container is broken and it may kill or restart it, cutting the drain short — readiness controls routing, liveness controls restarts." },
    { q: "Why does server.close() alone hang, and what is reverse dependency order?", a: "server.close() stops accepting new connections and fires its callback only once all existing connections have ended, but it does not touch idle keep-alive sockets — HTTP/1.1 keeps them open by default — which it still counts as active, so the callback never fires and the process hangs until SIGKILL. You must drain keep-alive deliberately: send Connection: close on shutdown responses so clients reconnect elsewhere, and force-close idle sockets (server.closeIdleConnections / closeAllConnections) while active requests finish. Then tear down in reverse dependency order, the mirror of startup: stop the HTTP server and finish in-flight requests first, stop background workers and flush queues, and close datastores last (database pool, then Redis). The reason is concrete — an in-flight request mid-drain may still issue a query, so closing the pool early severs it and turns a graceful drain into the request loss you were preventing; datastores are the deepest dependency and close last. Wrap the whole drain in a guardian timeout set below the grace period so a stuck request cannot hold the process hostage to SIGKILL." },
    { q: "Why is the grace period a budget, and what is the disposition for long requests versus background jobs?", a: "The grace period (default 30s) is a single window that must cover the preStop sleep (≈5–15s), the keep-alive drain, the reverse-dependency teardown, and the guardian-timeout margin — leaving often under twenty seconds for actually finishing in-flight work, so any unit of work whose remaining runtime exceeds that residual structurally cannot finish, and awaiting it just gets you SIGKILLed mid-write. You cannot raise the grace period without bound because the orchestrator, node drain, and spot-reclaim deadlines cap it, so for genuinely long work a different disposition is needed. A long synchronous request cannot be requeued (a client holds the socket), so finish it if close or reject it cleanly with 503 + Retry-After (or refuse to start new long operations once shutdown begins) — never sever it silently mid-stream. A background job can be requeued: stop pulling new jobs, finish what fits, and release the rest, or let the visibility timeout redeliver it." },
    { q: "Why does requeue demand idempotency, and why doesn't a fleet of perfect per-instance shutdowns guarantee a zero-downtime deploy?", a: "Requeue and visibility-timeout redelivery are at-least-once: the job may run again from the start, so a worker killed after a partial side effect (e.g. a card charged before the ack) charges again on redelivery. Requeue is therefore safe only when the consumer is idempotent — running the job twice yields the same end state via an idempotency key, an inbox/dedup table, or a no-op-on-applied state machine — with checkpointing to cut redo time. As for the fleet: every property that matters during a deploy is a relationship between instances a single process cannot see. Capacity depends on whether the replacement is already serving, so you surge new pods to ready before draining old ones (maxUnavailable: 0, positive maxSurge). The deregistration delay must be sized to the fleet's p99 (≈3× p99, with cloud LB defaults of 30–60s, up to 600s+ for long connections). And the thundering reconnect is emergent — thousands of correct Connection: close calls landing at once topple the cold pods — so you jitter the closes. Local correctness does not compose into global correctness for free; the failure modes live in the interactions." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: the termination sequence ends in an uncatchable SIGKILL, so finish during SIGTERM — and make sure it actually reaches a handler in PID 1; the deregistration race means fail readiness and wait out propagation before you stop accepting; teardown runs in reverse dependency order with datastores last, bounded by a guardian timeout; the grace period is a budget, so reject long requests and requeue jobs — but requeue is at-least-once and demands idempotency; and zero-downtime deploy is the fleet-level orchestration (surge before drain, deregister before terminate, jitter the closes) that arranges individually-correct shutdowns so they never collide.
</Recap>
