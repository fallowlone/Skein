---
concepts:
- composition
- failure-cascade
- timeout-budget
- load-shedding
- observability
- production-readiness
estMin: 14
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://sre.google/sre-book/addressing-cascading-failures/
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
- https://aws.amazon.com/builders-library/using-load-shedding-to-avoid-overload/
status: ready
summary: "Cross-track synthesis MCQs for the backend capstone: composition over addition, the latency cascade, nested timeout budgets, goodput vs throughput, the p99 tail, and readiness as a checklist."
title: 'Putting it together: multiple-choice review'
track: backend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole backend track at once. None tests a single mechanism in isolation — each puts two or three of them on the same request, under the same load, and asks you to read the interaction the way you would in a real incident.
</Hook>

<Goal lang="en">
Confirm you can reason about the seven mechanisms as one composed system: how a correct part becomes another part's bad input, how the timeout budget threads the stack, why goodput beats throughput, and why readiness is verified, not felt.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Every one of the seven backend mechanisms passes its own tests, yet the team's worst incidents come from places no single mechanism explains. What is the capstone's diagnosis?"
  choices={[
    { label: "Production runs all seven concerns at once on the same requests and load, so the hardest failures are emergent properties of the interactions — composition is not addition, and correct parts can still sum to incorrect behaviour.", correct: true },
    { label: "One of the seven mechanisms must be subtly broken; find and fix it.", misconception: "Each mechanism can be individually correct and the system still fail. The point is that the bug lives in the seams between mechanisms, not inside any one — there is no broken part to find." },
    { label: "The team is missing an eighth mechanism it has not learned yet.", misconception: "The gap is composition of the existing mechanisms, not a missing one. The unit adds observability and load control as connective tissue that ties the seven together, not a new isolated concern." },
    { label: "Passing unit tests on each mechanism guarantees the composed system is correct.", misconception: "Production-ready is an emergent property of the system that cannot be verified by inspecting parts; passing parts do not sum to a correct whole because each mechanism changes the environment the others run in." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A payment provider's p99 climbs from 40ms to 4s — no errors, just slow answers. Minutes later endpoints that never call the provider are also slow. Which mechanism's correct behaviour propagated the fault, and through what?"
  choices={[
    { label: "Each slow call holds its pooled connection for the full 4s, so the bounded pool drains; now every request — even unrelated ones — blocks on connection acquisition. The fault generalizes through the shared pool, not through any direct dependency.", correct: true },
    { label: "The slow provider directly slows the database, which slows everything.", misconception: "The database is healthy. The coupling is the shared pool and event loop, not a dependency between provider and database — a fault spreads through shared resources, not through the downstreams themselves." },
    { label: "The breaker tripping is what made the unrelated endpoints slow.", misconception: "The breaker tripping sheds the provider calls and frees connections; it relieves the cascade rather than causing the generalization. The pool draining on held connections is what spreads the slowness first." },
    { label: "Unrelated endpoints must secretly call the provider too.", misconception: "They need not touch the provider at all. They slow because they compete for the same exhausted pool — which is precisely how a single-dependency fault becomes everyone's problem." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A POST /payments handler has a 3-second total budget set in middleware, but the downstream provider call, the pool acquire, and the DB write each carry their own 3-second timeout. Why is this wrong even though every individual timeout looks reasonable?"
  choices={[
    { label: "Inner deadlines must be strictly smaller than the outer budget and sum, with margin, to less than it; if each inner call can consume the whole 3s, a single hung step holds its connection and loop slot for the full budget and the outer deadline fires too late to fall back or answer.", correct: true },
    { label: "It is fine — each layer should get the full budget so nothing is cut off early.", misconception: "Giving every inner layer the full budget lets any one of them consume it alone, making the failure later, less diagnosable, and resource-draining. Budgets are nested, not independent." },
    { label: "The idempotency key makes the timeout nesting irrelevant.", misconception: "Idempotency makes a retry safe; it does nothing about a hung call holding a pooled connection for the full budget. That is a timeout-nesting problem, not a correctness-of-retry problem." },
    { label: "The fix is to raise the outer budget to 9 seconds so all three fit.", misconception: "That makes every request potentially 9s slow and still lets a hung step starve the pool for that long. The fix is smaller, nested inner deadlines so the innermost slow thing fails first and frees its resources in time." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="During a cascade the slow provider fully recovers — latency back to 40ms — but the service stays collapsed. What does this reveal, and what is the right response?"
  choices={[
    { label: "The failure is metastable: a built-up queue and ongoing retries now sustain the cascade independently of the original trigger, so fixing the dependency does not help — you must attack the feedback loop by shedding load and capping retries until the queue drains faster than it refills.", correct: true },
    { label: "The provider did not really recover; the metrics are lying.", misconception: "Even with a genuinely recovered provider the system stays collapsed, because the queue and retries it created now feed the loop themselves. That self-sustaining loop is the definition of metastability." },
    { label: "The breaker is stuck open and just needs more time to close.", misconception: "Waiting does not help a metastable state; the loop is self-sustaining. You need active intervention — shed load, drain queues, cut retries — not patience." },
    { label: "Bouncing one pod will clear the whole cascade.", misconception: "One restart does not drain a fleet-wide queue or stop clients retrying. Metastable failures need load shedding and retry control across the system, not a single bounce." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A service is past saturation — demand exceeds capacity — and currently accepts and queues every request. Why does adding fast 503-with-Retry-After shedding at the edge increase the number of users served well?"
  choices={[
    { label: "Accepted-but-unfinishable requests hold connections and lengthen queues, dragging the in-capacity requests past their own deadlines until they too become waste; shedding the excess cheaply at admission keeps queues short so accepted requests finish within deadline as goodput.", correct: true },
    { label: "Shedding permanently reduces total demand, so the server has less to do.", misconception: "Rejected requests do not vanish from the world — clients may retry — but shedding them cheaply at the door protects capacity for the requests you accept. The win is goodput, not lower demand." },
    { label: "A 503 is cheaper to compute than a real response, so raw throughput goes up.", misconception: "The mechanism is queue control, not error speed. Not accepting excess work keeps queues short so in-capacity requests finish in time; cheap rejection merely enables that." },
    { label: "Load shedding raises the server's actual capacity C.", misconception: "Capacity is fixed by the hardware and downstreams. Shedding does not raise C; it stops you wasting C on work beyond it, preserving goodput within the fixed capacity." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A readiness review on a complex new payment service comes back 'all eight gates fully green, no caveats.' Why is a senior reviewer more skeptical than reassured?"
  choices={[
    { label: "Readiness depth is calibrated to blast radius and real services involve conscious tradeoffs; 'all green, no caveats' on a complex service usually means someone was not honest about which gates are partly open and why — the checklist was gamed, not reasoned through.", correct: true },
    { label: "All-green is impossible because no service can ever be safe to launch.", misconception: "Services do launch safely. The skepticism is not that safety is impossible but that a complex new service with zero caveats signals undocumented tradeoffs rather than genuine completeness." },
    { label: "A new service should always return all-red until it has run in production.", misconception: "There is no fixed verdict color. The point is honest calibration to blast radius with explicit caveats, not blanket pessimism." },
    { label: "Eight gates are simply too few to assess a payment service.", misconception: "The number of gates is not the issue; honest depth per gate proportional to the cost of failure is, with tradeoffs made explicit rather than papered over." },
  ]}
/>

<Recap lang="en">
The through-line across the track is one claim: composition is not addition. A correct pool draining under a slow downstream becomes the breaker's bad input; a correct retry becomes a storm; a correct drain races a stuck in-flight job. The timeout budget must nest down the stack so the innermost slow thing fails first; under overload you protect goodput by shedding the excess on purpose; you can only operate what you can see at the p99 tail, not the mean; and readiness is a checklist calibrated to blast radius, not a feeling. Senior backend work is reading the interaction graph before it cascades.
</Recap>
