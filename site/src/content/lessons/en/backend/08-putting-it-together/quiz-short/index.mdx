---
concepts:
- composition
- timeout-budget
- metastability
- goodput
- observability
- production-readiness
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://sre.google/sre-book/addressing-cascading-failures/
- https://sre.google/sre-book/handling-overload/
- https://sre.google/sre-book/reliable-product-launches/
status: ready
summary: "Free-recall prompts spanning the backend track — composition vs addition, the nested timeout budget, the metastable cascade, goodput vs throughput, RED and the p99 tail, and readiness as a checklist."
title: 'Putting it together: free-recall review'
track: backend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory across the whole track before you open the model answer — the effort of pulling the connections back together is what makes the synthesis stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: why composition is not addition, how the timeout budget nests, what makes a cascade metastable, why goodput is the target under overload, how RED and percentiles make the system visible, and why readiness is a checklist.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is 'composition is not addition' the core claim of the backend track, and where do the hardest bugs live?", a: "The naive model is additive: implement all seven mechanisms correctly and you have a correct backend, the way seven passing unit tests suggest a passing system. But correctness does not add up cleanly, because each mechanism changes the environment the others run in. A sensibly sized pool is correct alone, yet its correct behaviour under exhaustion is to make callers wait; that waiting is what a circuit breaker reads as slowness; the breaker tripping reshapes the load the retry layer generates. None of those mechanisms is wrong — the surprise lives in the interaction. So production-ready is an emergent property of the system, not a checkable property of any single mechanism: you cannot unit-test your way to it, and the hardest bugs live in the seams between mechanisms, not inside any one, because the interaction does not exist until all the pieces run together under load." },
    { q: "Walk one POST /payments request down the stack and name the guard at each layer, then explain why the timeout budget must be nested.", a: "Accept and parse (lifecycle): read off the socket and parse, enforcing body-size limits and backpressure. Middleware and DI: authenticate, set the request's total timeout budget, and have the DI container supply scoped dependencies. Handler on the event loop (async I/O): every I/O call is awaited so the loop keeps serving thousands of other requests. Pool: borrow a connection with an acquisition timeout so a saturated pool fails fast. Breaker: wrap the provider call in a breaker and its own call timeout, short-circuiting to a fallback if the provider is failing. Idempotency: persist under an idempotency key so a retried POST returns the original result instead of charging twice. Shutdown: write the response, and a SIGTERM mid-charge keeps this request alive and tears down only after it answers. The budget must nest because a single generous outer timeout lets any inner layer consume the whole budget alone; a hung provider then holds its connection, loop slot, and the request for the full budget while the outer deadline fires too late to fall back. Nested inner deadlines — acquisition, call, write, each strictly smaller and summing with margin to less than the whole — make the innermost slow thing fail first and free its resources in time." },
    { q: "Walk the canonical latency cascade link by link, and explain why the request path and the shutdown path are the same graph.", a: "A downstream slows (provider p99 goes 40ms to 4s, late not down). The pool drains: each slow call holds its connection for the full 4s until the bounded pool is empty. Latency generalizes: any request needing a connection — even one that never touches the provider — blocks on acquisition, so one dependency's fault becomes the whole service's. Retries amplify: clients and middleware retry the slow requests, but the service is saturated, so each retry is another connection grab — the retry storm. The breaker trips: it opens on the provider's latency, shedding work and reshaping the error rate everyone sees. The drain races the in-flight job: a rolling deploy is draining a pod whose graceful-shutdown handler is trying to finish the same charges that are timing out, so the drain misses the grace period and SIGKILL takes the half-finished job. The request path acquires resources outermost-to-innermost (parse, pool, call, write); shutdown releases them in reverse — stop accepting, drain in-flight, close the pool last — so the two are one dependency graph read in opposite directions." },
    { q: "What is metastability, why does fixing the original cause not fix it, and what must you do instead?", a: "Once a cascade is running the system becomes its own load source. Even if the provider recovers to 40ms, the pool is still full of connections held behind a queue that built up during the slow period; clients are still retrying because they still see the queue's timeouts; and every retry adds to that backlog. The feedback loop the slow provider started is now fed entirely by the queue and retries it created — the cause has been replaced by its own effects. The system has two stable equilibria, a healthy one (short queue, fast responses, few retries) and a collapsed one (full queue, slow responses, many retries), and a large enough shock flips it from the first to the second where it stays. So you cannot wait it out or fix it by fixing the dependency; you must attack the loop — shed load so the queue drains faster than retries refill it, cap or disable retries to cut amplification, reset breakers in a controlled way, sometimes shrink concurrency. Resilience is not the absence of failure but the absence of amplifying feedback under failure." },
    { q: "Under overload, why is rejecting requests the correct behaviour, and how is goodput different from throughput?", a: "A server has finite capacity C of useful work per second; when demand D exceeds C you cannot complete D, only choose what happens to the D − C requests you have no capacity for. Accepting them does not make them vanish — they sit in queues holding resources, so even the C requests you could have served now wait behind them and miss their deadlines too. Accepting work beyond capacity subtracts served requests rather than adding them. Goodput is the right target: requests completed usefully, fast enough that the answer still matters — as opposed to throughput, which counts every request handled including ones answered after the client already timed out (pure waste that consumed resources and produced negative value). Shedding the excess cheaply at admission — a fast 503 with Retry-After, dropping the least-valuable load first — keeps queues short so accepted requests finish within deadline as goodput. Its dual is backpressure: signal upstream to slow down via bounded queues and fail-fast acquisition. And every mechanism is a load-control knob: the pool bounds work in flight, timeouts free doomed work, the breaker sheds a failing dependency, capped retries tame the amplifier, shutdown sheds a departing node, and observability tells you where you are on the goodput curve." },
    { q: "What does RED measure, why must you watch percentiles instead of the mean, and what makes a readiness checklist externalized expertise rather than bureaucracy?", a: "RED is the outermost, caller-centric view: Rate (requests per second, the load), Errors (the failed fraction, including breaker rejections, not just 5xx), and Duration (the distribution of response times, not the average). You must never trust the mean latency because it blends fast and slow into a number nobody experiences — 99 requests at 20ms and one at 4s average to ~60ms; watch p50, p99, and p999, because the cascade begins as a rising p99 long before the mean moves, latency distributions are heavy-tailed and multimodal, and a user making 100 requests per page hits their personal p99 on almost every load. Each mechanism emits a leading-indicator state: pool in-use vs limit and acquire wait, loop lag, breaker transitions, retry rate, drain duration. The error budget turns this into a shipping governor: ship features while it is healthy, switch to reliability work when it is nearly spent. The readiness checklist is externalized expertise, not bureaucracy: the eight failure modes are independent and do not cue each other, expertise degrades under fatigue and time pressure, so the list makes complete consideration reliable, teachable, and auditable — the same reason expert pilots read pre-flight checklists. It only works backed by the understanding the track built, which is why it comes last, and readiness depth is calibrated to blast radius with explicit caveats." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory you hold the track's spine: composition is not addition, so correct parts compose into incorrect wholes; the timeout budget nests down the stack so the innermost slow thing fails first; the cascade goes metastable and must be broken at the feedback loop, not the cause; goodput, not throughput, is the overload target, defended by shedding on purpose; RED and the p99 tail make the composed system visible; and readiness is a blast-radius-calibrated checklist that only means something to someone who understands every gate it lists.
</Recap>
