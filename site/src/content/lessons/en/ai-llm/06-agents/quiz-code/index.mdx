---
concepts:
- react-loop-skeleton
- termination-exits
- context-trimming
- dedup-thrash-guard
- error-recovery-cap
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://www.ibm.com/think/topics/react-agent
status: ready
summary: "Read real agent-loop snippets — tool dispatch, step caps, context trimming, error recovery — predict the behaviour, and pick the highest-leverage fix."
title: 'Agents: code and loop reading'
track: ai-llm
unit: 06-agents
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
The agent loop is where the money is spent and the bugs hide. Read each snippet the way you would read it in a code review on a service that bills per token, then choose the fix a senior makes first.
</Hook>

<Goal lang="en">
Practise the loop you run on every agent: read the control flow, predict where it runs forever, overflows, or thrashes, and reach for the structural guard before blaming the model.
</Goal>

## Snippet 1 — the loop with one exit

```python
def run_agent(task, tools):
    messages = [SYSTEM, {"role": "user", "content": task}]
    while True:
        resp = model(messages, tools)          # THINK
        if not resp.tool_calls:
            return resp.content                 # only exit: model stops calling tools
        messages.append(resp)
        for call in resp.tool_calls:
            result = dispatch(call, tools)       # ACT
            messages.append(result)              # OBSERVE
```

<Quiz
  id="06-agents-code-1"
  lessonSlug="06-agents"
  lang="en"
  question="This loop is correct ReAct but unsafe for production. What is the single most important guard to add, and why that one first?"
  choices={[
    { label: "A hard step cap (e.g. `for _ in range(max_steps)`), plus a wall-clock/token budget. The only current exit is the model choosing to stop; a stuck or thrashing model never reaches it, so the run can grow unbounded and quadratic in cost.", correct: true },
    { label: "Wrap dispatch() in a try/except so tool errors don't crash the loop.", misconception: "Error handling matters, but it is secondary — and feeding errors back without a cap can itself cause an infinite retry. The first defect is that nothing bounds the number of iterations at all." },
    { label: "Cache model() responses so repeated states are cheaper.", misconception: "Caching shaves cost but does not stop a runaway loop; an agent thrashing on new-but-useless states still runs forever. You need a hard bound on iterations and spend." },
    { label: "Switch to a streaming API to reduce latency per call.", misconception: "Streaming changes how output arrives, not whether the loop terminates. The missing piece is a termination guard, not a transport change." },
  ]}
/>

## Snippet 2 — the step cap that hides a bug

```python
for step in range(MAX_STEPS):          # MAX_STEPS = 100
    resp = model(messages, tools)
    if not resp.tool_calls:
        return resp.content
    messages.append(resp)
    for call in resp.tool_calls:
        messages.append(dispatch(call, tools))
# fell out of the loop: cap hit
return "Sorry, I couldn't complete that."
```

<Quiz
  id="06-agents-code-2"
  lessonSlug="06-agents"
  lang="en"
  question="Telemetry shows ~30% of runs fall through and return the apology. The cap is doing its job — so what does a senior conclude?"
  choices={[
    { label: "Hitting the cap routinely is a symptom, not a solution: the cap is a seatbelt catching a real bug — a bad tool, vague prompt, or missing dedup. Investigate why the model never reaches a genuine stop, and lower MAX_STEPS so the quadratic bill is bounded while you fix it.", correct: true },
    { label: "It's working as designed — raise MAX_STEPS to 500 so more runs finish.", misconception: "Raising the cap lets a quadratic loop burn far more tokens before tripping, and a loop that needs hundreds of steps has usually lost the plot. The fix is the underlying failure, not more rope." },
    { label: "The apology message is the bug; return the partial work instead.", misconception: "Returning partial work can be reasonable, but it doesn't address why a third of runs can't finish. The cap firing that often points to a structural defect upstream." },
    { label: "Nothing to do — 70% success is acceptable for an agent.", misconception: "A 30% cap-hit rate is a loud signal of thrashing or a broken tool, not a baseline to accept. The cap masks it; diagnosis from traces is required." },
  ]}
/>

## Snippet 3 — context trimming

```python
def trim(messages, budget=8000):
    # keep most recent messages until we're under the token budget
    kept = []
    total = 0
    for m in reversed(messages):
        total += count_tokens(m)
        if total > budget:
            break
        kept.append(m)
    return list(reversed(kept))
```

<Quiz
  id="06-agents-code-3"
  lessonSlug="06-agents"
  lang="en"
  question="This keeps the loop under the window, but it has a failure mode that surfaces on long tasks. What is it, and the fix?"
  choices={[
    { label: "It drops the oldest messages first — which include the SYSTEM prompt and original task — so on a long run the agent loses its own instructions and finishes the wrong thing. Fix: always pin the system/task messages, then trim or summarise the middle.", correct: true },
    { label: "It miscounts tokens because count_tokens is approximate.", misconception: "Approximate counting is fine with a safety margin; that is not the structural bug. The real defect is that the unconditional 'keep most recent' rule evicts the instructions the task depends on." },
    { label: "Reversing the list twice is a performance problem on large histories.", misconception: "Two reversals are O(n) and negligible next to model calls. The substantive bug is what gets evicted — the pinned context — not the list mechanics." },
    { label: "It should keep the oldest messages instead, since they're most important.", misconception: "Keeping only the oldest would discard recent tool results the model needs to act now. The right answer is to pin the instructions and trim/summarise the middle, not to flip which end you keep." },
  ]}
/>

## Snippet 4 — error recovery

```python
for step in range(MAX_STEPS):
    resp = model(messages, tools)
    if not resp.tool_calls:
        return resp.content
    messages.append(resp)
    for call in resp.tool_calls:
        try:
            result = dispatch(call, tools)
        except ToolError as e:
            result = {"role": "tool", "content": f"Error: {e}"}  # feed error back
        messages.append(result)
```

<Quiz
  id="06-agents-code-4"
  lessonSlug="06-agents"
  lang="en"
  question="Feeding the error back lets the model adapt — but a load test shows runs where the same tool fails identically dozens of times. What guard closes the gap?"
  choices={[
    { label: "Track per-tool failure/identical-call counts and a dedup check: after N identical failing calls, stop feeding it back and either switch strategy or accept 'I tried X and it failed' as a terminal answer. Error-feedback without a retry cap is an infinite loop of valid calls.", correct: true },
    { label: "Catch a broader exception class so no error escapes the loop.", misconception: "Catching more exceptions makes the loop more resilient to crashing but more prone to thrashing — it keeps feeding failures back forever. The missing piece is a cap on identical/failed retries." },
    { label: "Retry dispatch() with exponential backoff inside the except block.", misconception: "Backoff helps for transient/rate-limit errors, but for a deterministic failure (bad args, not-found) it just delays the same identical failure. You still need a dedup/retry cap at the loop level." },
    { label: "Remove the try/except and let the loop crash on the first error.", misconception: "Crashing throws away the model's ability to recover from recoverable errors. The goal is bounded recovery — feed back, but cap identical retries — not no recovery." },
  ]}
/>

<Recap lang="en">
Every agent incident is read in the loop: a single 'model stops' exit is unsafe, so add a hard step cap plus a wall-clock/token budget; a cap that fires routinely is a seatbelt catching a real bug, not a fix to loosen; naive context trimming evicts the pinned system/task messages and makes the agent forget its job, so pin instructions and summarise the middle; and error-feedback without a per-tool retry/dedup cap turns recovery into an infinite loop of valid calls. Read the control flow, find the unbounded path, add the structural guard, then re-run under load to confirm.
</Recap>
