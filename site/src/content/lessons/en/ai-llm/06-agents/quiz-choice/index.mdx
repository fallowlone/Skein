---
concepts:
- agentic-loop
- quadratic-context-cost
- termination-guards
- error-recovery-loop
- scripted-vs-agent
- step-cap-seatbelt
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://www.ibm.com/think/topics/react-agent
- https://online.stevens.edu/blog/hidden-economics-ai-agents-token-costs-latency/
status: ready
summary: "Multiple-choice synthesis across the agents unit — the ReAct loop, quadratic context cost, termination exits, error-recovery traps, and when to script instead of agentify."
title: 'Agents: multiple-choice review'
track: ai-llm
unit: 06-agents
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a decision you make designing or debugging a real agent — not a definition to recite, but a tradeoff to weigh when the loop is running on someone's bill.
</Hook>

<Goal lang="en">
Confirm you can connect the loop mechanics, the economics of growing context, the several exits a loop needs, error-recovery hygiene, and the scripted-vs-agent call — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="06-agents-choice-1"
  lessonSlug="06-agents"
  lang="en"
  question="A task that takes 5 agent steps costs about $0.05. The same agent on a 15-step task costs about $0.40 — roughly 8×, not 3×. Why?"
  choices={[
    { label: "The full message history is re-sent on every iteration, so total tokens are the sum 1+2+…+N — quadratic in step count. Triple the steps and you roughly square the transcript you pay to re-read.", correct: true },
    { label: "The model switches to a more expensive tier once a run gets long.", misconception: "The model is the same throughout. What changes is the token volume: each call re-reads the entire accumulated transcript, and that history grows every turn." },
    { label: "Each extra tool call adds a large fixed network and serialization overhead.", misconception: "Per-call overhead is roughly linear in step count; it cannot explain superlinear blowup. The quadratic term is re-sending a transcript that lengthens every turn." },
    { label: "Longer runs trigger more retries, and retries are billed at a premium.", misconception: "Retries cost the same per token as any call. Even a clean 15-step run with zero retries is ~8× a 5-step run purely from the re-sent, growing context." },
  ]}
/>

<Quiz
  id="06-agents-choice-2"
  lessonSlug="06-agents"
  lang="en"
  question="An agent request that should cost $0.08 ran nine minutes and burned $12. The trace shows search_orders called 142 times with identical arguments, each returning the same empty result. No exception was thrown. What is the root cause?"
  choices={[
    { label: "The loop had only one exit — the model declining to call a tool — and a confused model kept making valid calls forever. The missing controls are a dedup/progress check plus a wall-clock or token budget.", correct: true },
    { label: "search_orders has a bug that should have raised an exception.", misconception: "An empty result is not an error — the tool worked. The failure is in the loop: nothing detected the repeated identical call or capped the spend. Treating 'not found' as terminal is the fix." },
    { label: "The recursion_limit was set too low, so the agent never reached its answer.", misconception: "A low cap would have stopped the bleed sooner, not caused it. The problem is the absence of dedup and a budget; raising the cap would make the bill worse." },
    { label: "The model needed a larger context window to hold the search results.", misconception: "Window size is irrelevant — the result was empty each time. The agent was thrashing on an identical failing call, which more context cannot fix." },
  ]}
/>

<Quiz
  id="06-agents-choice-3"
  lessonSlug="06-agents"
  lang="en"
  question="A support flow always does the same three steps: look up the order, check refund eligibility, issue or deny the refund. A teammate proposes an open-ended agent with all three tools and recursion_limit=25. What is the better design and why?"
  choices={[
    { label: "Script it — three explicit calls with if/else branches. The path is fully known at code-time, so an agent loop buys nothing and adds quadratic cost, nondeterminism, and a runaway-loop failure surface. A script is cheap, deterministic, and unit-testable.", correct: true },
    { label: "Use the agent — it is more flexible and future-proof if the flow ever changes.", misconception: "Flexibility you do not need is speculative complexity. The order never branches, so handing the model that freedom only adds cost, latency, and a class of thrashing failures you must now guard against." },
    { label: "Use the agent but lower recursion_limit to 3 to bound the cost.", misconception: "A cap of 3 still pays the agent tax — nondeterminism and re-sent context — to solve a problem with zero branching uncertainty. The fixed path should simply be scripted." },
    { label: "Either is fine; the choice is purely stylistic.", misconception: "It is an engineering tradeoff with real consequences: cost, latency, testability, and failure surface all favor the script when the path is known. Reserve the agent for steps you cannot enumerate in advance." },
  ]}
/>

<Quiz
  id="06-agents-choice-4"
  lessonSlug="06-agents"
  lang="en"
  question="A senior says 'treat the max-step cap as a seatbelt, not a steering wheel.' What does that mean operationally?"
  choices={[
    { label: "The cap bounds the worst case, but if the agent routinely hits it that is hiding a real bug — a bad tool, vague prompt, or missing termination signal. The primary exit should be the model genuinely finishing; the cap is what saves you the night it doesn't.", correct: true },
    { label: "It means the cap should be set as high as possible so it never interferes with normal runs.", misconception: "A very high cap lets a quadratic loop run up a serious bill before it trips. The cap is a safety net to set low; the point is that hitting it regularly signals a deeper defect, not that it should be loosened." },
    { label: "It means you should rely on the cap as the main way the agent stops.", misconception: "That inverts the metaphor. A loop that depends on the cap to stop is steering with the seatbelt — it should reach a genuine done state on its own, with the cap only catching the failure case." },
    { label: "It means caps are unnecessary once the prompt is good enough.", misconception: "Even a perfect prompt meets empty results and confused models. The seatbelt is exactly for the night the steering fails; you keep it regardless of prompt quality." },
  ]}
/>

<Quiz
  id="06-agents-choice-5"
  lessonSlug="06-agents"
  lang="en"
  question="A long-horizon agent reaches step 40, and the history has grown past the model's context window. The framework silently truncates the oldest messages. What is the most likely consequence?"
  choices={[
    { label: "The original system/task instructions get dropped, so the agent forgets what it was asked and confidently finishes the wrong task. Models also weight the start/end of long context, so mid-chain facts can effectively vanish even before truncation.", correct: true },
    { label: "The agent crashes with a context-overflow exception, which is caught and retried.", misconception: "Silent truncation does not throw — that is what makes it dangerous. The run continues with its own instructions missing, producing a plausible but wrong answer." },
    { label: "Throughput improves because each call now sends fewer tokens.", misconception: "Fewer tokens per call is true but irrelevant; the dropped tokens were the task definition and early findings. Cheaper calls that solve the wrong problem are not a win." },
    { label: "Nothing — recent messages always contain everything the model needs.", misconception: "The oldest messages usually hold the original instructions and the earliest tool results, exactly the context a long task depends on. Truncating them is how the agent loses the plot." },
  ]}
/>

<Quiz
  id="06-agents-choice-6"
  lessonSlug="06-agents"
  lang="en"
  question="When a tool call fails, the common pattern is to feed the error text back to the model as the next observation. Why is this both a superpower and a trap?"
  choices={[
    { label: "Superpower: the model can read the error and adapt — fix arguments, switch tools. Trap: with no per-tool retry cap or dedup, a model that reads an unhelpful result as 'I made a mistake' will retry the identical call forever — a valid-call infinite loop.", correct: true },
    { label: "It is purely a superpower; feeding errors back has no downside if the model is capable enough.", misconception: "Even capable models loop on identical empty/failed results. Without a retry cap, dedup, and 'I tried X, cannot find it' as an acceptable terminal answer, error-feedback becomes the thrashing bug." },
    { label: "It is purely a trap; you should never show the model tool errors.", misconception: "Hiding errors removes the model's main recovery channel. The fix is structural guards around error-feedback — caps and dedup — not suppressing the errors." },
    { label: "The trap is that error text leaks internal details to the user.", misconception: "That is a separate redaction concern. The loop-level trap is unbounded retries of an identical failing call, which is what produces runaway cost." },
  ]}
/>

<Recap lang="en">
The through-line is one design discipline: the loop re-sends its whole history each turn, so cost is quadratic in steps and a long run overflows the window and drops its own instructions. A naive loop has a single exit the model may never reach, so you add independent ones — natural stop, a step cap (the seatbelt), a wall-clock/token budget that actually caps the dollar figure, and a dedup/progress check that kills thrashing. Error-feedback is the recovery superpower and the runaway trap; bound it with per-tool retry caps. And before any of that, ask whether the path is known — if it is, script it, and reserve the open-ended agent for steps you genuinely cannot enumerate in advance. Now when you see an agent question in design or in a review, the first thing you check is: does this loop have at least four independent exits — and does the path even need an agent at all?
</Recap>
