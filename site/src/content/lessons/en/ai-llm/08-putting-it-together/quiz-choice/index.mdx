---
concepts:
- cache-prefix-invalidation
- tool-call-streaming
- agent-budget-cap
- rag-eval-coverage
- request-tracing
- seam-failure
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://dev.to/waxell/the-47000-agent-loop-why-token-budget-alerts-arent-budget-enforcement-389i
- https://dev.to/adamo_software/tool-use-api-design-for-llms-5-patterns-that-prevent-agent-loops-and-silent-failures-f29
status: ready
summary: "Cross-track synthesis MCQs for the AI/LLM capstone — where caching, tools, RAG, streaming, budgets, agents, and evals fail at the seams between them."
title: 'Composing LLM apps: multiple-choice review'
track: ai-llm
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions across the whole track. None tests a single layer — each puts two correct layers together and asks where the composition breaks. That seam is where production LLM apps actually fail.
</Hook>

<Goal lang="en">
Confirm you can reason about the seams: how RAG interacts with the cache, how a tool call interacts with the stream, how an agent loop interacts with a budget, and why evals must include the live retrieval path.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A RAG-backed assistant has a long static system prompt marked with cache_control, yet its prod cache hit rate is near 0% — though it looked cached in the demo. What is the cause and the fix?"
  choices={[
    { label: "Retrieved chunks are stitched into the cached system block, so the byte-for-byte prefix differs every query and the cache busts. Move the per-request RAG context after the cache_control breakpoint, into the messages; keep static rules + tool schemas before it.", correct: true },
    { label: "The 5-minute TTL is too short for production traffic; raise it to 1 hour.", misconception: "TTL controls how long an entry survives, not whether the prefix matches. If the prefix differs every request, a longer-lived entry still never matches — the hit rate stays at zero. The problem is prefix instability, not eviction." },
    { label: "The demo only cached because of warm-up; prod just needs more traffic to warm the cache.", misconception: "Volume doesn't help when every request carries a different prefix. The demo 'cached' only because it repeated one identical question; real queries each embed different retrieved chunks inside the prefix." },
    { label: "Disable RAG so the prefix is stable again.", misconception: "That trades the grounding feature for a cache metric — optimizing the wrong layer. The fix keeps retrieval and relocates it past the breakpoint so the static prefix stays cached." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A streamed turn ends with stop_reason: tool_use partway through and the UI hangs on a spinner forever. Which mental model fixes it?"
  choices={[
    { label: "The stream is a state machine, not a token pipe. tool_use is a transition (pause render, run the tool, feed the tool_result back in the next request, resume) — not the end of the turn.", correct: true },
    { label: "The model errored mid-stream; retry the whole request from scratch.", misconception: "Nothing errored — the model deliberately requested a tool. Restarting re-runs work and can loop; you must handle the tool_use transition and continue, not restart." },
    { label: "max_tokens was hit; raise it so the model finishes the prose instead of stopping.", misconception: "max_tokens limits length, not the model's decision to call a tool. It stopped to request a tool; a higher limit doesn't change that branch." },
    { label: "Buffer the entire response before rendering so partial tokens never show.", misconception: "Buffering throws away streaming's latency benefit and still doesn't run the tool — you'd buffer, then still need to execute and resume. The tool-use branch is unhandled either way." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Your agent has a token-budget dashboard with alerts, yet a bad input drove it into a retry loop that burned thousands of dollars overnight. Why didn't the budget protect you?"
  choices={[
    { label: "Alerts fire after the spend; they are not enforcement. The fix is a hard gate before every model call — assert step ≤ MAX_STEPS and spent ≤ BUDGET_USD and refuse the call when a ceiling is hit, plus dedupe identical tool calls to kill repeat loops.", correct: true },
    { label: "The dashboard was sampling too slowly; a 1-second refresh would have caught it.", misconception: "Faster observability still only observes. By the time any alert fires the dollars are already spent; only a pre-call gate that refuses the next request actually bounds cost." },
    { label: "The model is too expensive; switch to a cheaper model and the loop stops.", misconception: "A cheaper model makes a runaway loop cost less per step, but it still runs forever on bad input. The defect is the missing terminating condition, not the price per token." },
    { label: "Add more retries with backoff so the malformed result eventually parses.", misconception: "More retries is the opposite of the fix — it feeds the loop. The famous case ran 11 days for $47,000 precisely because nothing capped the iteration count." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Your offline eval suite is green on every deploy, but users report worse answers right after a retriever change. Why did the evals miss the regression?"
  choices={[
    { label: "The suite scores generation on fixed golden context and never runs the live retrieval path, so a retrieval regression ships green. Add end-to-end evals that vary retrieval, scoring the answer produced from what the live retriever actually returns.", correct: true },
    { label: "The evals are flaky and need more retries to stabilize.", misconception: "Flakiness shows as noisy pass/fail, not consistent green hiding a real regression. This is a coverage gap: the retrieval seam is simply not in the test path." },
    { label: "The model silently changed; re-run the same generation evals on the new model.", misconception: "The change was the retriever, not the model. Re-running generation-only evals on frozen context still skips the path that actually regressed." },
    { label: "The generation metrics are wrong and should be replaced wholesale.", misconception: "The generation metrics can be fine in isolation; the problem is they run on frozen context. You need an end-to-end eval that includes live retrieval, not different generation metrics." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A network blip truncates a streamed turn with stop_reason: tool_use but zero tool_use content blocks, and the agent goes idle silently. A later request is rejected with 'unexpected tool_use_id found in tool_result block'. What is the common root cause?"
  choices={[
    { label: "The composition doesn't treat tool_use/tool_result as a strict pairing across requests. Every tool_use id must be matched by exactly one tool_result in the next request; truncated or orphaned blocks must be detected and the turn repaired or restarted, not left dangling.", correct: true },
    { label: "Two unrelated bugs — one in streaming, one in the tools API; fix them separately.", misconception: "Both are the same seam: the stream and the tool protocol share state across requests, and the composition never validated that pairing. A blank tool_use stop and an orphaned tool_use_id are two symptoms of unvalidated transitions." },
    { label: "The model is hallucinating tool calls; tighten the system prompt.", misconception: "Neither symptom is hallucination — one is a truncated stream with no tool block, the other is a stale id from a crashed turn. The fix is protocol-level pairing and recovery, not prompt wording." },
    { label: "Idempotency keys on the HTTP layer would prevent both.", misconception: "HTTP idempotency dedupes retries; it does nothing about an empty tool_use stop or a tool_result referencing an id the model never emitted. You need state-machine tracking of tool_use ↔ tool_result." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A RAG-backed agentic assistant ships: every component passed its own test, but the bill triples and answers feel slower in prod. What is the senior debugging move?"
  choices={[
    { label: "Trace one real production request through every layer (cache → RAG → tools → stream → loop) and at each boundary ask what the next layer assumed that this layer just changed. The bug lives in a seam, not inside any component.", correct: true },
    { label: "Re-run each component's unit test in prod to find which one regressed.", misconception: "They all still pass — that's the trap. The failure is between correct components (e.g. RAG busting the cached prefix while looping requests grow the transcript), so per-component tests can't surface it." },
    { label: "Roll back to the last single-feature version and ship features one at a time forever.", misconception: "The features have to coexist; the goal is a correct composition, not avoiding composition. Tracing the seam lets you keep all six layers and fix the boundary." },
    { label: "Scale up the hardware to absorb the cost and latency.", misconception: "More hardware pays for the broken seam instead of fixing it — the cache is still busted and the loop still grows the transcript. Cost and latency both come from the seam, not capacity." },
  ]}
/>

<Recap lang="en">
The track's through-line is one sentence: each layer is correct by its own contract, and the failure lives where one layer's output silently violates the next layer's input assumption. RAG changes the prefix the cache assumed was stable; a tool_use stop changes the stream the renderer assumed was prose; real input changes the loop the budget assumed would terminate; the retrieval path changes under an eval suite that assumed fixed context. You don't debug these by re-testing pieces — you trace one real request end to end, and at every boundary ask what the next layer assumed that the last one just changed. Now when you see a composed app behave correctly in every unit test yet fail in prod, you reach for the request trace before you reach for the debugger.
</Recap>
