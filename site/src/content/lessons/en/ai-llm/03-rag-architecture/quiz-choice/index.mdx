---
concepts:
- chunking
- embedding
- top-k-retrieval
- reranking
- context-assembly
- retrieval-failure
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://arxiv.org/abs/2307.03172
- https://developers.openai.com/api/docs/guides/embeddings
status: ready
summary: "Multiple-choice synthesis across the RAG unit — chunking, embedding cost, wide-then-narrow retrieval, lost-in-the-middle, and the confident-hallucination failure mode."
title: 'RAG architecture: multiple-choice review'
track: ai-llm
unit: 03-rag-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole pipeline. None is a definition to recite — each is a decision you make while a RAG system is silently returning confident, wrong answers in production.
</Hook>

<Goal lang="en">
Confirm you can connect chunking, embedding cost, two-stage retrieval, context assembly, and the retrieval-driven failure mode into one diagnosis — the synthesis the unit built toward.
</Goal>

<Quiz
  id="03-rag-architecture-choice-1"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="A support bot confidently reports a specific but wrong Q3 churn number; the answer survived review for a month. Where does the fault most often lie, and why does it stay invisible?"
  choices={[
    { label: "Retrieval — it pulled nearby-but-wrong chunks (Q2 churn, Q3 revenue), and because a miss still hands the model some context, it blends them into a fluent, specific, confident answer instead of abstaining.", correct: true },
    { label: "Generation quality — a newer, larger model would have caught the error.", misconception: "The generator can only reason over the chunks retrieval hands it. The right chunk was never in the prompt, so a bigger model just hallucinates more fluently over the same missing evidence." },
    { label: "The context window was too small to hold the answer.", misconception: "A miss means the answer-bearing chunk was never fetched at all; window size can't include text retrieval never returned." },
    { label: "A code bug threw an exception that got swallowed.", misconception: "The defining RAG failure is not a crash — it produces a plausible answer with no error, which is exactly why it survives review for weeks." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-2"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="A policy doc states a rule whose exception lives one paragraph later. Queries about the exception return half-truths. Which chunking change is the right first move?"
  choices={[
    { label: "The rule and its exception are split across a chunk boundary; add ~10–15% overlap (and/or size chunks to the answer-bearing unit) so a fact straddling the seam survives whole in at least one chunk.", correct: true },
    { label: "Shrink chunks to 64 tokens so each embeds more precisely.", misconception: "Smaller chunks embed precisely but fragment spanning rules even worse — you'd split the rule and its exception across even more boundaries, deepening the half-truth problem." },
    { label: "Raise top-k to 100 so more chunks come back.", misconception: "A bigger net can help recall but doesn't fix a fact chunking severed; it floods context with noise and invites lost-in-the-middle while the seam problem remains." },
    { label: "Switch to a larger embedding model.", misconception: "Embedding accuracy isn't the issue — the answer-bearing text was physically cut in two at index time. No embedding model recovers a fact that isn't whole in any single chunk." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-3"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="Index search p99 is too high at 3072 dims and storage cost is climbing. What's the senior trade before changing infrastructure?"
  choices={[
    { label: "Truncate the embeddings (Matryoshka-style) — run text-embedding-3-large at ~1024 dims, landing near 3072-dim quality at roughly one-third the storage and faster ANN search.", correct: true },
    { label: "Embeddings are fixed-size; you must shard across more nodes to cut latency.", misconception: "Matryoshka-trained embeddings are explicitly truncatable — you can drop dimensions and keep most of the accuracy, cutting both storage and search latency with no extra hardware." },
    { label: "Switch from HNSW to a brute-force exact scan for accuracy.", misconception: "Exact scan removes ANN's millisecond speedup; at million-scale it makes latency far worse. The lever here is vector size, not abandoning the ANN index." },
    { label: "Lower top-k so fewer vectors are compared.", misconception: "ANN search compares the query against the index regardless of k; reducing k trims candidates returned, not the per-query search cost driven by dimensionality." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-4"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="Why is 'retrieve wide (k=20–50) then rerank narrow to 3–8 with a cross-encoder' better than a single embedding top-3?"
  choices={[
    { label: "The wide ANN stage is recall-first (cheap bi-encoder dot-products) so the right chunk lands in the candidate set; the cross-encoder then reads query+chunk together for accurate relevance, pushing the true match to the top and trimming noise.", correct: true },
    { label: "The cross-encoder is fast enough to run over the whole corpus, so reranking is free.", misconception: "Cross-encoders are slow per item precisely because they jointly encode query+chunk — that's why you only run them on the shortlist from stage one, never the full corpus." },
    { label: "A single top-3 is fine; reranking only adds latency for no recall gain.", misconception: "Top-3 with no rerank is brittle: one ANN miss or boundary split and the answer isn't in the prompt. The two-stage pattern decouples recall (stage 1) from precision (stage 2)." },
    { label: "Reranking increases the number of chunks sent to the model, improving grounding.", misconception: "It does the opposite — it narrows 20–50 candidates down to 3–8, which cuts tokens, cost, and lost-in-the-middle while raising precision." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-5"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="You have a 128k-token window, so you stuff all 40 retrieved chunks in retrieval order and put the decisive one near the middle. What does the literature predict?"
  choices={[
    { label: "Accuracy can fall 30%+ versus placing that chunk at the start or end — attention is U-shaped, strongest at the edges and weakest in the middle, regardless of relevance.", correct: true },
    { label: "Nothing changes — modern long-context models attend uniformly across the window.", misconception: "Empirically attention is U-shaped: the 'lost in the middle' effect persists even in long-context models, so position within the window matters." },
    { label: "It improves, because the middle is the most-attended region.", misconception: "It's the reverse: the middle is the least-attended region. Rerank, keep few, and place the strongest evidence at the very start or end." },
    { label: "A larger window always neutralises ordering effects.", misconception: "A bigger window is not a substitute for reranking; stuffing irrelevant chunks still buries the decisive one in the low-attention middle and costs tokens." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-6"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="A RAG bot that worked last week now returns last quarter's policy with no code change and no error. Most likely cause, and the structural fix?"
  choices={[
    { label: "A stale index — the source changed but the embeddings didn't, so retrieval serves outdated chunks silently. Fix: a re-index/freshness pipeline plus a retrieval-score gate and 'answer only from context' instruction so confidence reflects what's actually fetched.", correct: true },
    { label: "The model was silently downgraded; pin the model version.", misconception: "A frozen generator wouldn't flip a previously-correct answer to a specific older value — that pattern is the index serving stale embeddings, not a model change." },
    { label: "Embedding drift — re-embed the query with a different model.", misconception: "The query embedding didn't change; the indexed document embeddings are stale relative to the updated source. The fix is re-indexing the corpus, not re-embedding queries." },
    { label: "Top-k is too low; raise it to surface the new policy.", misconception: "If the new policy was never re-indexed, no value of k retrieves it — it isn't in the index. Freshness is the missing piece, not breadth." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one diagnosis: retrieval, not generation, is where production RAG fails. Chunking sets the ceiling (size to the answer-bearing unit, overlap so seams survive); embedding dimensionality is a real cost lever you can truncate; the recall-vs-precision split is solved by retrieve-wide-then-rerank-narrow; context ordering must dodge lost-in-the-middle by placing best evidence at the edges; and stale or poisoned indexes turn a silent miss into a confident wrong answer — which you defend against with a score gate, freshness, and an instruction to answer only from context or say "I don't know."
</Recap>
