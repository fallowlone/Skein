---
concepts:
- chunking
- cosine-similarity
- reranking
- context-assembly
- vector-search
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://developers.openai.com/api/docs/guides/embeddings
- https://arxiv.org/abs/2307.03172
status: ready
summary: "Read real RAG pipeline code — a chunker, a cosine-similarity search, a retrieve-then-rerank stage, and a context assembler — and pick the highest-leverage fix."
title: 'RAG architecture: code and pipeline reading'
track: ai-llm
unit: 03-rag-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
RAG bugs hide in code that runs without error and returns plausible text. Read each snippet the way you'd read it in review, then pick the defect a senior catches before it ships a confident wrong answer.
</Hook>

<Goal lang="en">
Practise the review loop for a RAG pipeline: spot the chunking flaw, the similarity-math bug, the missing rerank stage, and the context-assembly mistake that quietly tanks answer quality.
</Goal>

## Snippet 1 — the chunker

```python
def chunk(text, size=1000):
    # split on a fixed character count, no overlap
    return [text[i:i + size] for i in range(0, len(text), size)]
```

<Quiz
  id="03-rag-architecture-code-1"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="What is the highest-leverage problem with this chunker for a prose corpus, and the first fix?"
  choices={[
    { label: "Fixed-size, zero-overlap splitting cuts sentences and rules mid-thought, so a fact that straddles a boundary lands half in each chunk and embeds in neither cleanly. Fix: split on semantic units (paragraphs/sentences) and add ~10–15% overlap.", correct: true },
    { label: "1000 characters is too few; raise size to 100000 so every doc is one chunk.", misconception: "One giant chunk dilutes the embedding so badly that narrow-query recall collapses — one vector now averages the whole document. Bigger isn't the fix; respecting semantic boundaries with overlap is." },
    { label: "It should split on bytes, not characters, for Unicode safety.", misconception: "Splitting on raw bytes can break multi-byte characters and makes things worse. The real defect is severing meaning at arbitrary offsets with no overlap, not the character-vs-byte unit." },
    { label: "Nothing — fixed-size chunking with no overlap is the recommended default.", misconception: "Zero overlap guarantees boundary facts get split; ~10–15% overlap is the cheap insurance the unit calls for, and splitting on semantic units preserves whole ideas." },
  ]}
/>

## Snippet 2 — the similarity score

```python
import numpy as np

def score(query_vec, chunk_vec):
    # rank candidates by this value
    return np.dot(query_vec, chunk_vec)
```

<Quiz
  id="03-rag-architecture-code-2"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="Ranking by raw dot product instead of cosine similarity is risky when the embeddings are not normalised. Why, and what's the fix?"
  choices={[
    { label: "Raw dot product mixes direction (semantic similarity) with magnitude (vector length). On un-normalised vectors a longer chunk vector can outrank a more semantically-relevant shorter one. Fix: use cosine similarity (dot product divided by the norms) or L2-normalise vectors before indexing.", correct: true },
    { label: "Dot product is undefined for high-dimensional vectors; you must reduce dimensions first.", misconception: "Dot product is well-defined at any dimensionality. The issue is that without normalisation it conflates magnitude with relevance, not the number of dimensions." },
    { label: "It's fine — dot product and cosine similarity always produce the same ranking.", misconception: "They agree only when all vectors share the same norm. On un-normalised embeddings, magnitude leaks into the score and can reorder results; normalise first to make them equivalent." },
    { label: "Dot product is too slow; switch to Euclidean distance for speed.", misconception: "Dot product is the cheap operation ANN indexes are built on. The defect is correctness on un-normalised vectors, and the fix is normalisation/cosine — not swapping the metric for speed." },
  ]}
/>

## Snippet 3 — retrieve and rerank

```python
def answer(query):
    qv = embed(query)
    candidates = vector_store.search(qv, k=3)   # top-3 by cosine
    context = "\n".join(c.text for c in candidates)
    return llm(f"Answer using:\n{context}\n\nQ: {query}")
```

<Quiz
  id="03-rag-architecture-code-3"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="A senior flags this for low answer quality on hard queries. Which change has the most leverage?"
  choices={[
    { label: "Make it two-stage: retrieve wide (k=20–50) for recall, then run a cross-encoder reranker over those candidates and keep the top 3–8. k=3 with no rerank means one ANN miss leaves the answer out of the prompt entirely.", correct: true },
    { label: "Raise k to 50 and feed all 50 chunks straight to the model — skip reranking.", misconception: "That fixes recall but wrecks precision: 50 chunks bloat tokens and cost and bury the decisive one in the low-attention middle. Wide retrieval needs a narrowing rerank stage." },
    { label: "Swap the LLM for a larger model with a 1M-token window.", misconception: "The bottleneck is what k=3 retrieves, not generation. If the right chunk wasn't in the top-3, a bigger model and window can't reason over text that was never fetched." },
    { label: "Lower k to 1 to remove all noise from the context.", misconception: "k=1 maximises brittleness — a single ANN miss yields zero relevant context and a guaranteed hallucination. The pattern is retrieve wide, then rerank narrow, not retrieve narrow." },
  ]}
/>

## Snippet 4 — context assembly

```python
def assemble(reranked, query):
    # reranked is sorted best-first
    blocks = [c.text for c in reranked]          # best chunk first, rest after
    context = "\n\n".join(blocks)
    return f"{context}\n\nQuestion: {query}"
```

<Quiz
  id="03-rag-architecture-code-4"
  lessonSlug="03-rag-architecture"
  lang="en"
  question="Given a long assembled context, what does 'lost in the middle' say about this ordering, and how should you assemble instead?"
  choices={[
    { label: "Putting the best chunk first and the weakest ones trailing means the decisive evidence can still be fine, but mediocre middle chunks land in the model's weakest-attention zone. Place the strongest evidence at both edges (best first AND a strong one last), keep few chunks, and put the question at the very end.", correct: true },
    { label: "Ordering is irrelevant once chunks are reranked — relevance, not position, drives the answer.", misconception: "Reranking sets which chunks you keep, but position still matters: attention is U-shaped, so even relevant text in the middle gets under-used. Order best evidence at the edges." },
    { label: "Always put the single best chunk dead center so the model focuses on it.", misconception: "The center is the least-attended region. Dead-center placement is the worst spot for the decisive chunk; the edges are best." },
    { label: "Reverse-sort so the worst chunk is first — models attend most to the start.", misconception: "Models attend well to the start, so you'd be wasting the strongest position on the worst chunk. Strong evidence belongs at the start and end, not the weakest." },
  ]}
/>

<Recap lang="en">
Every RAG bug is read in code: fixed-size zero-overlap chunking severs facts at boundaries; ranking by raw dot product on un-normalised vectors lets magnitude beat meaning, so use cosine; a single k=3 retrieve with no rerank is one ANN miss away from a hallucination, so retrieve wide then rerank narrow; and assembly order matters because attention is U-shaped — keep few chunks and place the strongest evidence at the edges. Fix the pipeline stage with the most leverage, then re-evaluate on a held-out set to confirm.
</Recap>
