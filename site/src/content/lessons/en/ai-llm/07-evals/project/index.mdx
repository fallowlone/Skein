---
concepts:
- golden-dataset
- programmatic-scorer
- llm-as-judge
- judge-calibration
- regression-gate
- online-eval
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://github.com/openai/evals/blob/main/docs/build-eval.md
- https://www.braintrust.dev/articles/llm-evaluation-guide
status: ready
summary: "Hands-on project — build a golden-set eval suite plus a calibrated judge and a CI regression gate for one real LLM feature, with measurable pass criteria and an online drift check."
title: 'LLM evals: build an eval suite and CI gate'
track: ai-llm
unit: 07-evals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about evals is not the same as having a gate that blocks your own regression. Take one real LLM feature, build the golden set, score it honestly, calibrate the judge, and wire a CI gate that fails the build when quality drops — then prove it catches a regression you deliberately inject.
</Hook>

<Goal lang="en">
Turn the unit's model into a working pipeline: a golden set from real inputs, programmatic + calibrated-judge scoring, an offline regression gate in CI with measurable thresholds, and an online drift check — verified by a regression the gate actually blocks.
</Goal>

<ProjectBrief
  id="07-evals-project"
  lessonSlug="07-evals"
  lang="en"
  objective="Pick one LLM feature (a RAG Q&A endpoint, a classifier, a structured-extraction call, or a summarizer) and ship an eval suite plus a CI regression gate for it, such that a deliberately injected quality regression fails the build — and prove every claim with numbers."
  requirements={[
    "Pick one LLM feature with a clear quality contract. Write down the dimensions you score (e.g. correctness, on-format/valid JSON, on-policy/no refusal of valid input, faithfulness to retrieved context) and what 'good enough' means per dimension.",
    "Build a golden set of 50+ cases from real or realistic inputs, deliberately oversampling edge cases, past failures, and high-value flows — not the clean happy-path examples you'd use while building. Tag each case with a category so you can score per-category, and record where each case came from.",
    "Implement scorers per output type: programmatic (regex/JSON-schema/exact-match/code-exec) wherever the output has structure, and an LLM-as-judge only for the open-ended dimensions. The judge must emit a parseable verdict (a JSON field or fixed enum) against an explicit rubric and reference answer.",
    "Calibrate the judge: hand-label 20-30 cases, measure the judge's agreement with your labels, and report the agreement number. Only use the judge as a gate if agreement clears a stated bar (target ~75-90%). Check for position bias by running pairwise comparisons in both orders.",
    "Wire a CI regression gate (GitHub Actions or equivalent) that runs the suite against a baseline on every PR, reports overall AND per-category scores, and exits non-zero — failing the build — when any per-category or overall delta drops below your threshold.",
    "Inject a real regression (a worse prompt, a smaller/cheaper model, or a broken retrieval step) on a branch and show the gate blocks the PR, naming which category caught it.",
    "Add an online check: sample 1-5% of (real or simulated) production traffic, score it with the same automated methods, and log the trend; describe what provider drift or input-distribution drift would look like in that signal.",
  ]}
  acceptance={[
    "A documented golden set of 50+ categorized cases with provenance, plus the per-dimension definition of 'good enough'.",
    "A reported judge-calibration number (agreement with human labels) that clears your stated bar, with the position-bias check shown; if it doesn't clear the bar, the judge is not used as a gate and that decision is documented.",
    "A CI run that PASSES on the baseline and a separate CI run that FAILS on the injected-regression branch, with the gate output showing the per-category delta that triggered the failure (not just an aggregate).",
    "A short write-up: which scorer you chose per dimension and why, the threshold you gated on, and how the online sample would surface a regression the offline gate structurally cannot.",
  ]}
  stretch={[
    "Add an embedding-distribution drift alert on the online sample: flag when production inputs land meaningfully far from anything in the golden set, and feed those cases back into the set.",
    "Run an A/B test: serve a candidate prompt to a traffic slice, score both arms with the suite, and tie the quality delta to one business metric (resolution rate, escalation rate, etc.).",
    "Add a judge-robustness harness that runs each judged case in both answer orders and reports the position-bias rate, failing CI if it exceeds a threshold.",
    "Make the gate widen automatically: a script that turns each new logged production failure into a golden case (with a category tag) as part of the incident workflow.",
  ]}
/>

<Recap lang="en">
This is the loop you run for every real LLM feature: define the quality contract, build a golden set from real traffic, score with the cheapest honest method (programmatic before a calibrated judge), gate offline in CI on per-category deltas — not the mean — and sample online for the drift offline can't see. The proof is not a green suite; it's a suite that turns red on a regression you injected. Build it once on one feature and the production version becomes muscle memory.
</Recap>
