---
concepts:
- sse-protocol
- time-to-first-token
- delta-accumulation
- partial-json-tool-args
- proxy-buffering
- stream-reconnect
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://platform.openai.com/docs/api-reference/responses-streaming
status: ready
summary: "Multiple-choice synthesis across the streaming unit — TTFT vs total time, the SSE event lifecycle, delta accumulation, partial-JSON tool args, reconnect, and the buffering proxy."
title: 'Streaming: multiple-choice review'
track: ai-llm
unit: 04-streaming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real streaming incident — not a definition to recite, but a tradeoff to weigh when the spinner hangs in production.
</Hook>

<Goal lang="en">
Confirm you can connect the latency model, the SSE lifecycle, delta accumulation, the partial-JSON contract, and the buffering failure mode — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="04-streaming-choice-1"
  lessonSlug="04-streaming"
  lang="en"
  question="A 400-token answer generates at 60 tok/s. Product asks you to 'make the response twice as fast' by enabling streaming. What do you tell them, in senior terms?"
  choices={[
    { label: "Streaming does not change the ~7s total generation time — it collapses time-to-first-token from 7s to a few hundred ms. The win is perceived responsiveness: the user reads early tokens while later ones generate. Real speedups need fewer tokens or a faster model.", correct: true },
    { label: "Streaming roughly halves total time because tokens transfer in parallel with generation.", misconception: "Tokens are still generated serially at the same rate; SSE only delivers each one as it is produced. Total time is unchanged — only TTFT drops." },
    { label: "Streaming makes the model generate tokens faster by avoiding the final buffering step.", misconception: "Generation speed (TPOT) is set by the model and hardware, not by whether you stream. Streaming is a transport/UX choice, not a compute optimization." },
    { label: "Streaming reduces the token count, so the response is genuinely shorter.", misconception: "Token count is fixed by the prompt and the answer. Streaming changes when bytes arrive, never how many tokens the model produces." },
  ]}
/>

<Quiz
  id="04-streaming-choice-2"
  lessonSlug="04-streaming"
  lang="en"
  question="Your chat app streams flawlessly against localhost but in staging every reply appears all at once after a long pause. Where do you look first?"
  choices={[
    { label: "The network path, not the code. 'Works on localhost, batches in prod' is the fingerprint of buffering in a reverse proxy / CDN / serverless layer — nginx defaults to proxy_buffering on, holding the whole response until it ends. Fix: proxy_buffering off / X-Accel-Buffering: no, no gzip on the SSE route.", correct: true },
    { label: "Your SSE parser — it must be accumulating deltas and only flushing at the end.", misconception: "If the same code streams incrementally on localhost, the parser is provably fine. The only new variable in staging is the infrastructure in the path between server and client." },
    { label: "The model — switch to a faster model so the first token arrives sooner.", misconception: "A faster model would still be buffered into one flush by the proxy. TTFT stays equal to total time until the buffering layer is fixed." },
    { label: "Switch from SSE to WebSockets, since SSE clearly cannot stream through a proxy.", misconception: "SSE streams token-by-token perfectly when nothing buffers it; the same proxy can also break WS upgrades. You would swap transports and still hit the buffering layer." },
  ]}
/>

<Quiz
  id="04-streaming-choice-3"
  lessonSlug="04-streaming"
  lang="en"
  question={"The model calls a tool; its arguments stream as input_json_delta chunks: '{\"city\": \"San Fran', then 'cisco\", \"unit\":', then ' \"celsius\"}'. When may you JSON.parse the buffer?"}
  choices={[
    { label: "Only after content_block_stop for that block. Each input_json_delta is a fragment of one JSON document, not valid JSON on its own — the document is complete only when the block closes. You may show the partial string as a 'calling tool…' hint but must not act on it.", correct: true },
    { label: "After each delta, so the parsed arguments object is always current.", misconception: "Parsing an intermediate buffer like '{\"city\": \"San Fran' throws a syntax error every time. The fragments are not independently valid JSON." },
    { label: "Right after message_start, since that frame begins the arguments.", misconception: "message_start carries only the envelope with empty content — no argument deltas have arrived yet, so there is nothing to parse." },
    { label: "Never parse — pass the raw partial string straight to the tool function.", misconception: "The tool needs structured arguments. Passing a half-formed JSON string runs the function with broken or missing inputs. Parse the completed buffer at the block boundary." },
  ]}
/>

<Quiz
  id="04-streaming-choice-4"
  lessonSlug="04-streaming"
  lang="en"
  question="A tool call fires in production with input = {} (empty args), but the same prompt works locally against the provider directly. Most likely cause?"
  choices={[
    { label: "A middle layer (proxy or SDK adapter — LiteLLM has shipped such regressions) is dropping or misrouting the input_json_delta frames. The tool-call envelope arrives but the argument deltas were swallowed before your accumulator saw them.", correct: true },
    { label: "Your accumulator has a bug that resets the buffer on every delta.", misconception: "If it works locally against the provider directly, your accumulator is fine — the difference is the layer added in production. Suspect the path before your own code." },
    { label: "The model decided the tool needs no arguments this time.", misconception: "The tool schema requires arguments; the model emitted them. An empty input despite a working direct path points to lost deltas in transit, not a model choice." },
    { label: "You parsed too late — parse on each delta to avoid losing args.", misconception: "Parsing per-delta throws on fragments and would never produce {}. Empty args is a delivery problem (frames lost), not a parse-timing problem." },
  ]}
/>

<Quiz
  id="04-streaming-choice-5"
  lessonSlug="04-streaming"
  lang="en"
  question="A connection drops at token 200 of a 400-token generation. What is the honest senior default for handling it?"
  choices={[
    { label: "Treat a dropped stream as a repeatable whole-turn retry and make a fresh request safe to repeat (idempotency). True mid-generation resume via Last-Event-ID requires server-side delta replay and is rarely worth building.", correct: true },
    { label: "Always implement Last-Event-ID resume so the stream continues from token 200.", misconception: "SSE supports Last-Event-ID, but resuming mid-generation means buffering and replaying deltas server-side — significant complexity most apps skip in favor of retrying the whole turn." },
    { label: "Render the 200 tokens you have and call it done.", misconception: "A truncated answer is a wrong answer. The stop reason never arrived, so you do not have a complete message — you have half of one." },
    { label: "Switch to a non-streaming request so drops can't happen.", misconception: "Non-streaming requests drop too, and you lose the TTFT win entirely while waiting for the full generation. Drops are handled by safe retries, not by abandoning streaming." },
  ]}
/>

<Quiz
  id="04-streaming-choice-6"
  lessonSlug="04-streaming"
  lang="en"
  question="A reasoning model with chain-of-thought sits at 30s TTFT before emitting any token; users report the app is 'frozen.' What is the correct read and fix?"
  choices={[
    { label: "Expected: there is nothing to stream while the model thinks, so a naive token-only UI looks frozen. Fix the UX — show a thinking/progress state (or stream reasoning summaries if the provider exposes them), not the transport.", correct: true },
    { label: "The stream is buffered by a proxy; apply proxy_buffering off.", misconception: "Buffering shows all-at-once after a pause regardless of model type. A reasoning model genuinely emits no tokens during the think phase — the gap is real generation latency, not buffering." },
    { label: "The SSE connection has dropped; add automatic reconnect.", misconception: "The connection is open and healthy; the model simply has not produced a first content token yet. Reconnecting would restart the expensive think phase." },
    { label: "Reduce max_tokens so the model thinks less and answers sooner.", misconception: "max_tokens caps output length, not the reasoning effort that drives TTFT here. The fix is communicating progress to the user during the think phase." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one model: streaming pays down perceived latency with the user's reading speed by collapsing TTFT, while total time and token count are unchanged. SSE delivers a typed lifecycle (message_start → content_block_start → content_block_delta ×N → content_block_stop → message_delta/stop) that you accumulate; text deltas render immediately but tool-call input_json_delta fragments are parseable only at content_block_stop. Empty-args tool calls mean a middle layer ate the deltas; dropped streams are repeatable whole-turn retries; and the failure that erases every gain — needing no dropped connection — is a buffering proxy turning TTFT back into total time. Now when you see "works on localhost, batches in prod," you know which layer to blame and which config line to change first.
</Recap>
