---
concepts:
- sse-protocol
- time-to-first-token
- client-abort
- error-recovery
- proxy-buffering
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://platform.openai.com/docs/api-reference/responses-streaming
status: ready
summary: "Hands-on project — build a robust streaming endpoint and client end-to-end: SSE relay, TTFT/TPOT measurement, mid-stream cancellation, error recovery, and a buffering-proxy deployment check."
title: 'Streaming: build a robust streaming endpoint and client'
track: ai-llm
unit: 04-streaming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about TTFT, partial JSON, and buffering proxies is not the same as shipping a stream that survives a slow client, a Stop button, a dropped connection, and a default nginx. Build the endpoint and client end-to-end, measure the latency that matters, and prove it holds under each failure.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working system: relay a provider's SSE stream through your own endpoint, accumulate deltas correctly (text and tool args), measure TTFT and TPOT, cancel cleanly all the way to the upstream, recover from errors, and verify a real proxy doesn't buffer the stream to death.
</Goal>

<ProjectBrief
  id="04-streaming-project"
  lessonSlug="04-streaming"
  lang="en"
  objective="Build a streaming chat endpoint (server) and client that relays an LLM provider's SSE stream, measures TTFT/TPOT, supports mid-stream cancellation that propagates to the upstream, recovers from errors and drops, and is verified to stream incrementally through a real reverse proxy — not just localhost."
  requirements={[
    "Build a server endpoint (Node/Express, Go, or similar) that calls a streaming LLM provider (Anthropic/OpenAI or a local model) and relays it to the client as SSE: Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no, and compression disabled on the route.",
    "Implement a correct SSE client parser: buffer raw bytes, split on the blank-line event delimiter (TCP chunks do not align with events), and decode with streaming so a frame split across chunks is never parsed half-formed.",
    "Accumulate deltas correctly: append text_delta into a snapshot and render incrementally; accumulate input_json_delta into a per-block string buffer and JSON.parse it only at content_block_stop. Show at least one tool call streaming its args and parsing cleanly at the boundary.",
    "Measure and log TTFT (request sent → first content token) and TPOT (mean ms/token after first) per request; expose them in the response trailer/log and on the client. Capture token usage from message_delta for cost accounting.",
    "Implement client cancellation: a Stop button wired to an AbortController whose signal aborts the fetch; the server must detect the closed connection and abort the upstream provider stream so generation and billing stop server-side, not just in the UI. Prove the upstream call actually ends.",
    "Implement error recovery: on a mid-stream provider error or dropped connection, surface a clear state and make a fresh whole-turn retry safe (idempotent request id). Decide and document whether you do Last-Event-ID resume or retry-the-turn, and why.",
    "Deploy behind a real reverse proxy (nginx with default config, or a serverless platform/CDN) and demonstrate the stream still arrives incrementally — then flip proxy_buffering on (or remove X-Accel-Buffering) and capture the all-at-once failure to prove you understand the signature.",
  ]}
  acceptance={[
    "A demo showing tokens rendering incrementally end-to-end through the proxy, with measured TTFT in the hundreds of ms (not equal to total time) and a TPOT figure — captured numbers, not estimates.",
    "A tool call whose arguments stream as fragments and parse exactly once at content_block_stop; show a log line proving no intermediate JSON.parse was attempted and the final args object is complete.",
    "Clicking Stop visibly halts tokens AND a server log / provider dashboard confirms the upstream generation aborted (token count stops climbing) — not just a frozen UI.",
    "A before/after capture of the buffering proxy: with the fix, incremental delivery; with proxy_buffering on, the all-at-once flush after a long pause — plus a one-paragraph note naming the exact config that caused and cured it.",
  ]}
  stretch={[
    "Add server backpressure handling: when res.write() returns false, pause the upstream stream and resume on 'drain', and prove memory stays bounded against an artificially slow client (throttled consumer).",
    "Add a heartbeat/keepalive comment frame (': ping') on an interval so idle-timeout proxies and load balancers don't kill a slow-to-first-token reasoning request.",
    "Add a reasoning-model path: stream thinking/reasoning summaries (or a progress state) so a 10–60s TTFT never shows a bare frozen spinner, and measure the perceived-latency difference.",
    "Add an on-call runbook page: the 'works on localhost, batches in prod' triage, the proxy/CDN/serverless buffering checklist, the empty-tool-args (lost input_json_delta) diagnosis, and the cancellation/idempotency contract.",
  ]}
/>

<Recap lang="en">
This is the loop you will run on every real streaming feature: relay SSE correctly (right headers, buffer-and-split parsing, delta accumulation, tool args parsed only at content_block_stop), measure the latency that actually matters (TTFT and TPOT, not total time), cancel all the way to the upstream so a Stop button stops billing, recover from drops with a safe whole-turn retry, and verify against a real proxy because the number-one production failure — buffering that turns TTFT back into total time — never shows up on localhost. Build it once on a toy chat app and the production version becomes muscle memory.
</Recap>
