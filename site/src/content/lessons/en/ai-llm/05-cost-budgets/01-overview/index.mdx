---
slug: 01-overview
lang: en
track: ai-llm
unit: 05-cost-budgets
order: 1
level: junior
lessonType: topic
status: ready
title: "LLM cost budgets: token asymmetry, routing, and the kill switch"
summary: "Output tokens cost ~5x input, and the system prompt + history + RAG context get re-sent every turn — so a runaway loop burns thousands overnight. Routing, caching, and hard per-request budget caps keep spend bounded."
prereqs: []
deepensInto: []
spiral: []
concepts: ["token-pricing-asymmetry", "context-accumulation", "model-routing", "prompt-caching", "budget-caps", "runaway-agent-loop"]
estMin: 17
sources:
  - https://www.cloudzero.com/blog/claude-api-pricing/
  - https://platform.claude.com/docs/en/build-with-claude/prompt-caching
  - https://relayplane.com/blog/agent-runaway-costs-2026
  - https://www.truefoundry.com/blog/rate-limiting-ai-agents-preventing-llm-api-exhaustion
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Friday night, an agent ships behind a feature flag. It has a tool loop — observe, think, act, repeat — and no iteration cap. A document it fetches references another document, which references another. The agent keeps appending each one to its context and re-sends the whole growing transcript to the model every turn. By Monday the bill shows $4,300 for one "task" that never finished. The monthly $1,000 spend alert fired — at 2am, into a Slack channel nobody reads on weekends.
</Hook>

<Crux>LLM cost is dominated by re-sent context and output tokens; an uncapped loop can burn thousands before any alert fires.</Crux>

<Explanation>

## Output tokens are the expensive half

The first thing that surprises engineers reading their first invoice: input and output are not priced the same. Across current frontier models, output runs roughly 5x the input rate. As of May 2026 Anthropic lists Haiku 4.5 at $1/M input and $5/M output, Sonnet 4.6 at $3/$15, and Opus 4.7 at $5/$25. OpenAI's GPT-4o sits at $2.50/$10 — the same 4x asymmetry.

The lever this hands you is concrete: a verbose answer costs far more than a verbose question. Capping `max_tokens`, asking for terse or structured output, and stopping the model from "thinking out loud" in the final response all attack the expensive side of the ledger. A senior reviewing a cost spike checks output volume first, not input.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Model (May 2026)</th>
        <th class="border border-border px-3 py-2 text-left">Input /M</th>
        <th class="border border-border px-3 py-2 text-left">Output /M</th>
        <th class="border border-border px-3 py-2 text-left">Output : input ratio</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Claude Haiku 4.5</td>
        <td class="border border-border px-3 py-2">$1.00</td>
        <td class="border border-border px-3 py-2">$5.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Claude Sonnet 4.6</td>
        <td class="border border-border px-3 py-2">$3.00</td>
        <td class="border border-border px-3 py-2">$15.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Claude Opus 4.7</td>
        <td class="border border-border px-3 py-2">$5.00</td>
        <td class="border border-border px-3 py-2">$25.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">OpenAI GPT-4o</td>
        <td class="border border-border px-3 py-2">$2.50</td>
        <td class="border border-border px-3 py-2">$10.00</td>
        <td class="border border-border px-3 py-2">4x</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">GPT-4o mini</td>
        <td class="border border-border px-3 py-2">$0.15</td>
        <td class="border border-border px-3 py-2">$0.60</td>
        <td class="border border-border px-3 py-2">4x</td>
      </tr>
    </tbody>
  </table>
</div>

## Where the money actually accumulates

The pricing table is a trap if you stop there, because it prices a single call. The bill is built from the fact that LLMs are stateless: every turn re-sends the entire context. Three things quietly inflate that re-sent payload.

The **system prompt** is sent on every single turn. A 4,000-token system prompt in a 50-turn conversation is not 4,000 tokens — it is 200,000 input tokens, paid 50 times. The **conversation history** grows linearly: turn 20 carries turns 1 through 19 with it, so cost per turn climbs as the chat goes on, and a long session is quadratic in total. **RAG context** is the heaviest single addition — stuffing 30 retrieved chunks (say 15,000 tokens) into every request, when 3 would have answered, is a 5x input cost multiplier that no one sees until the invoice.

This is why the runaway-loop postmortem is the canonical AI cost incident. An agent that appends a tool result to context each iteration, with no cap, doesn't just make more calls — each call is *bigger* than the last. Cost grows superlinearly. Reported real cases include an agent making 14,000 redundant tool calls and developers watching $15 evaporate in under 10 minutes. The damage is done before a human ever looks.

<Inset kind="why" lang="en">
Why didn't the monthly cap save the Friday-night agent? Provider spend limits operate at the billing-cycle level — they are a backstop measured in days, not seconds. A `$1,000/month` cap cannot stop a loop that burns `$6` in 30 seconds; by the time the cycle total crosses the line, thousands are already spent. The brake has to live in-process, on cost velocity per request and per session, not in the billing dashboard.
</Inset>

## Routing: don't pay Opus prices for a Haiku job

Most requests in a real product are not hard. Classification, extraction, short rewrites, routing decisions — a small cheap model handles these at a fraction of the cost. The senior pattern is a cascade: send the request to the cheap model first, and *escalate to the expensive model only when a quality check fails* or the task is detected as hard up front. Routing the easy 80% to Haiku ($1/$5) instead of Opus ($5/$25) is a 5x cut on that slice with no user-visible loss.

The tradeoff is real and worth naming: routing adds a classification step (latency, and sometimes its own model call), and a wrong route degrades quality silently. You pay for an escalation path — the cheap call plus the expensive retry costs *more* than going straight to the expensive model when the cheap one fails. Routing wins only when the cheap model succeeds often enough that the average lands well below the flat expensive rate. Measure the escalation rate; if it's above ~30%, your router or your cheap model is wrong.

## Prompt caching: stop paying for the unchanging prefix

The single biggest lever against re-sent context is prompt caching. You mark the stable prefix — the system prompt, tool definitions, a fixed RAG corpus — and the provider caches it. Anthropic's cache reads cost `0.1x` the base input rate (a 90% discount), while the write to populate the cache costs `1.25x` (5-minute TTL) or `2x` (1-hour TTL) the base rate. The arithmetic: a 5-minute cache pays for itself after a single hit; a 1-hour cache after the second.

That turns the "4,000-token system prompt sent 50 times" disaster into one full-price write plus 49 reads at a tenth of the price. Caching does not help the parts that change every turn (the latest user message, fresh retrieval), so the engineering move is to order your prompt cache-friendly: stable content first, volatile content last, so the cacheable prefix is as long as possible.

<TradeoffMatrix
  id="ai-cost-budgets-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A chat product re-sends a 4,000-token system prompt every turn across 50-turn sessions, and the input bill is dominating spend. Pick the highest-leverage fix."
  options={[
    { name: "Prompt-cache the stable prefix (system prompt + tool defs), volatile content last", summary: "Cache the unchanging part.", correct: true, justification: "The system prompt is identical every turn — the textbook caching target. At 0.1x read cost it turns 50 full-price sends into one write plus 49 reads at a tenth of the rate, ~90% off the prefix. Ordering stable-first, volatile-last maximises the cacheable span. This attacks the exact thing inflating the bill: re-sent unchanging input." },
    { name: "Switch every request to a cheaper model regardless of difficulty", summary: "Downgrade globally.", justification: "Cuts the rate but ignores why the bill is high — re-sent context — and trades quality blindly on hard turns. A cheaper model still re-sends the 4,000-token prompt every turn; you've reduced the multiplier, not removed the waste. Routing should be selective, not a global downgrade." },
    { name: "Raise the monthly spend cap so the alerts stop firing", summary: "Silence the alert.", justification: "This is the failure mode, not a fix — it removes the only signal while the underlying waste continues. Caps are a backstop, not an optimisation, and silencing them is how a runaway loop goes unnoticed until the invoice." },
    { name: "Truncate conversation history to the last 2 turns", summary: "Drop history.", justification: "Helps the history-growth axis and is sometimes right, but here the named cost is the system prompt re-sent every turn — truncating history doesn't touch that fixed prefix, and aggressive truncation can break multi-turn coherence. Caching is the targeted fix; history trimming is a separate, complementary lever." },
  ]}
/>

<Quiz
  id="ai-cost-budgets-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Across current frontier models, how do output token prices compare to input?"
  choices={[
    { label: "Output costs roughly 4–5x input, so a verbose answer is far pricier than a verbose prompt", correct: true },
    { label: "Input and output are billed at the same rate", misconception: "They are not symmetric — output runs about 4–5x input across Claude and GPT-4o, which is why capping max_tokens and asking for terse output is a real cost lever." },
    { label: "Input is more expensive because the model has to read it", misconception: "It's the reverse — generation is the costly half. Reading input is cheap; producing output tokens is what the higher rate prices." },
    { label: "Output is free once you pay for input", misconception: "Output is billed per token at the highest rate on the price sheet; it is the half a senior checks first on a cost spike." },
  ]}
/>

<Quiz
  id="ai-cost-budgets-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="An autonomous agent loop with no per-request budget runs overnight and bills $4,300. Why didn't the $1,000 monthly spend cap stop it?"
  choices={[
    { label: "Provider caps act at the billing-cycle level — too coarse and too slow to halt a loop burning cost in seconds", correct: true },
    { label: "The cap only applies to output tokens, not input", misconception: "Spend caps cover total spend, not one token class. The problem is timing and granularity: a monthly backstop can't catch a per-second runaway." },
    { label: "The agent was on a different API key the cap didn't cover", misconception: "Even on the right key the monthly cap fires after the cycle total crosses the line — far too late for a loop that spends thousands in hours." },
    { label: "Caching disabled the cap", misconception: "Caching is unrelated to cap enforcement. The real gap is that the brake must live in-process on cost velocity, not in the monthly billing total." },
  ]}
/>

<DragOrder
  id="ai-cost-budgets-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the cost controls from cheapest/first-line to last-resort kill switch:"
  items={[
    { id: "a", label: "Route the easy majority to a small cheap model; escalate only on failure" },
    { id: "b", label: "Prompt-cache the stable prefix so re-sent context drops to 0.1x" },
    { id: "c", label: "Cap max_tokens and trim history/RAG to what the task needs" },
    { id: "d", label: "Enforce a per-request and per-user budget in-process, before each call" },
    { id: "e", label: "Hard kill switch: trip the circuit breaker on cost velocity or loop count" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Output ~5x input; prompt, history, and RAG re-send every turn. Route cheap-first, cache the stable prefix, cap output, and enforce per-request budgets with an in-process kill switch.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ai-cost-budgets-retrieval"
  lang="en"
  questions={[
    { q: "Walk through exactly why a long agent loop costs superlinearly, not just linearly, with respect to the number of iterations.", a: "Two effects compound. First, more iterations means more model calls — that alone is linear. Second, and this is the trap, each iteration appends its tool result (or new retrieval, or reasoning) to the context that gets re-sent on the next call. So iteration 1 sends a small payload, iteration 20 re-sends everything from iterations 1 through 19 plus the system prompt, and iteration 50 carries the whole accumulated transcript. Cost per call grows with the iteration number, and total cost is the sum of a growing series — roughly quadratic in iterations rather than linear. That's why a loop with no cap doesn't fail gracefully: it accelerates. The fix is a per-request token budget and an iteration cap enforced in-process, plus context pruning so old tool results don't ride along forever. A monthly spend cap can't help because it measures the wrong thing on the wrong timescale — by the time the cycle total trips, the superlinear burn has already spent thousands." },
    { q: "Your input bill is dominating output, and tracing shows the same 4,000-token system prompt sent on every turn. What concrete steps cut this, and what's the order of leverage?", a: "First, recognise the shape: a fixed prefix re-sent every turn is the textbook prompt-caching target. Mark the system prompt and tool definitions as the cached prefix; with cache reads at 0.1x the base input rate you turn 50 full-price sends into one write (at 1.25x for the 5-minute TTL, or 2x for 1-hour) plus 49 reads at a tenth of the price — about a 90% cut on that prefix, paying for itself after the first hit. Order the prompt stable-first and volatile-last so the cacheable span is as long as possible. Second, attack history growth separately: trim or summarise old turns so the conversation doesn't carry its full past on every call. Third, audit RAG — if you stuff 30 chunks when 3 answer, you're paying a 5x input multiplier; tighten retrieval. Caching is the highest-leverage move here because it targets the exact thing named — the unchanging re-sent prefix — without degrading quality." },
  ]}
/>

<Recap lang="en">
LLM spend is driven less by the headline per-token rate than by what gets re-sent every turn and how much output you generate. Output costs roughly 5x input across current models (Haiku 4.5 $1/$5, Sonnet 4.6 $3/$15, Opus 4.7 $5/$25, GPT-4o $2.50/$10), so cap output and ask for terse answers first. The real accumulator is context: a stateless model re-sends the system prompt, the whole growing history, and all RAG chunks on every call, which is why a long agent loop costs superlinearly and a runaway loop can burn thousands overnight. Route the easy majority to a cheap model and escalate only on failure; prompt-cache the stable prefix to drop re-sent context to 0.1x; trim history and tighten retrieval. Above all, enforce per-request and per-user budgets in-process with a hard kill switch on cost velocity — monthly provider caps are a backstop measured in days, far too slow to stop a loop spending dollars per second.
</Recap>
