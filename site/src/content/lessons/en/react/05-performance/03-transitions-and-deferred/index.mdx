---
concepts:
- react-transitions
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-transitions-and-deferred"
sources:
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useDeferredValue
- https://react.dev/reference/react/Suspense
spiral: []
status: ready
summary: 'Transitions put updates in urgent and background lanes: the urgent commit lands first, the heavy render is interruptible per keystroke. useDeferredValue mirrors it for received values; with Suspense old UI holds, no fallback flash. One giant component cannot be interrupted.'
title: 'Transitions and useDeferredValue: making slow renders interruptible, not faster'
track: "react"
unit: "05-performance"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
The product search team had read about concurrent React, so when the catalog filter started dropping keystrokes — 280 ms of render per character across 20,000 product cards — the fix seemed obvious: wrap the state update in `startTransition`. They wrapped `setQuery(e.target.value)` and shipped. Nothing improved. Worse: now the *input itself* lagged, because the text in the box was driven by `query`, and the transition had politely deprioritized the very update that echoes the user's keystroke. The senior on the team spotted it in review the following week: a transition does not make work cheaper, it splits work into lanes — and they had pushed the urgent lane's only job into the slow lane. The fix was two states: `setText(value)` stays urgent so the input echoes instantly, `startTransition(() => setQuery(value))` carries the 280 ms list render in the background, restartable on every new keystroke. Typing went glass-smooth while results trailed by a beat — the 280 ms render still happened, the user just stopped paying for it with their keystrokes.
</Hook>

<Crux>Transitions mark updates non-urgent: React renders them in background, interruptible, and keeps old UI shown — no fallback flash.</Crux>

<Explanation>

## Two lanes: urgent and non-urgent

`startTransition` does not speed anything up, debounce anything, or move work off the main thread. It changes the *priority* of the state updates inside its callback: they become **transitions** — non-urgent updates that React renders in the background while the screen keeps showing the last committed UI. Urgent updates (typing, clicking, anything not wrapped) interrupt that background render: React abandons the half-built work-in-progress tree, commits the urgent update first, then restarts the transition render from the latest state. During the background render React also yields to the main thread between components (roughly every 5 ms), so the browser can paint and handle events mid-render instead of freezing for the full 280 ms.

```jsx
function CatalogSearch({ products }) {
  const [text, setText] = useState("");        // urgent: echoes the keystroke
  const [query, setQuery] = useState("");      // transition: drives the heavy list
  const [isPending, startTransition] = useTransition();

  function handleChange(e) {
    setText(e.target.value);                   // commits immediately
    startTransition(() => {
      setQuery(e.target.value);                // renders in background, interruptible
    });
  }

  return (
    <>
      <input value={text} onChange={handleChange} />
      <div style={{ opacity: isPending ? 0.6 : 1 }}>
        <ProductGrid products={products} query={query} />
      </div>
    </>
  );
}
```

The split is the whole design: the urgent state's render is tiny (one input), so it commits within a frame or two; the expensive tree depends only on the transition state, so its render is interruptible and restartable. `isPending` gives you an honest loading signal for the stale-but-visible content. The Hook team's first attempt failed because they had one state doing both jobs — wrapping it in a transition made the input echo non-urgent too. **Rule of thumb:** the value the user is directly manipulating must never be inside the transition.

<DiagramFrame label="Time-slicing on the transition lane: render attempts of the heavy tree travel toward commit along a track whose gaps mark the ~5 ms yields, and each new keystroke abandons the attempt in flight and restarts it" hue="var(--d-frontend)" caption="Each dot is one render attempt of the heavy tree: it travels interruptibly — the gaps are yields back to the main thread — and only an attempt that reaches the end commits. The urgent lane (the input echo) commits within a frame, and it is exactly what keeps interrupting this track: abandon, restart from the latest state, repeat.">
  <svg viewBox="0 0 360 130" width="360" height="130" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="180" y="20" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">transition lane: background render, yields ~5 ms</text>
    <rect x="6" y="46" width="70" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="41" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">keystroke</text>
    <rect x="290" y="46" width="64" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="322" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">commit</text>
    <path data-path d="M76 62 L290 62" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="12 6" />
    <text x="180" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">next keystroke = abandon attempt, restart from latest state</text>
    <PacketDot color="var(--d-frontend)" durationMs={1400} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1400} delayMs={450} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1400} delayMs={900} size={8} />
  </svg>
</DiagramFrame>

<Quiz
  id="03-transitions-and-deferred-quiz-1"
  lessonSlug="03-transitions-and-deferred"
  lang="en"
  question="A controlled search input drives an expensive results list via one state value. A developer wraps the whole setState in startTransition and the input starts lagging. What happened?"
  choices={[
    { label: "The input's displayed value is driven by the same state, so its echo update became non-urgent too — the fix is splitting state: an urgent value for the input, a transition value for the expensive list", correct: true },
    { label: "startTransition runs the update on a background thread, and the input is waiting for it to sync back", misconception: "There is no second thread — all rendering stays on the main thread. Transitions change scheduling priority, not execution location: React time-slices the background render and lets urgent work interrupt it. The lag came from priority, not threading." },
    { label: "Transitions add an intentional delay so React can batch more updates together", misconception: "Transitions add no fixed delay and are not a debounce. A transition render starts immediately if nothing urgent is pending; what changed is that the input's own update was now waiting behind nothing — it had itself become the deprioritized work." },
    { label: "The expensive list inside the transition blocks the main thread anyway, so nothing could ever improve", misconception: "Time-slicing yields between components during a transition render, so the main thread stays responsive — the pattern works, as the team's second attempt proved. The failure was putting the urgent update inside the transition, not a limitation of slicing." },
  ]}
/>

## useDeferredValue: deferring values you do not own

`useTransition` requires owning the `setState` call. Often you do not — the prop comes from a parent, a router, or a library hook. `useDeferredValue(value)` is the receiving side's equivalent: it returns the *previous* value while a background re-render with the new value is in flight, letting you keep an expensive subtree on the stale value without touching the producer.

```jsx
function SearchResults({ query }) {           // query updates urgently upstream
  const deferredQuery = useDeferredValue(query);
  const isStale = deferredQuery !== query;
  const grid = useMemo(
    () => <HeavyGrid query={deferredQuery} />,
    [deferredQuery]
  );
  return <div style={{ opacity: isStale ? 0.6 : 1 }}>{grid}</div>;
}
```

Mechanically: when `query` changes, React first re-renders urgently with `deferredQuery` still holding the old value — this render must be cheap, which is why the expensive subtree is memoized on `deferredQuery` and bails out — then starts a background render where `deferredQuery` catches up. That background render is a transition: interruptible, restartable on the next change. The memoization is not optional decoration here; without it the urgent render re-renders `HeavyGrid` with the *old* value anyway, and you pay the full cost twice per keystroke. **Choosing between them:** own the setter → `useTransition` (plus `isPending` for free); receive the value → `useDeferredValue` (compare values for staleness).

## Transitions and Suspense: no fallback flash

The second superpower is what transitions do to **Suspense**. An urgent update that suspends — say, changing a tab makes a component throw a promise for data it does not have — hides the already-visible content behind the nearest fallback: the page you were reading is replaced by a spinner. The same update inside a transition behaves differently: React keeps the *current, committed* UI on screen, renders the suspended tree in the background, and commits only when the data is ready. No fallback flash; the old tab stays visible (use `isPending` to dim it). This is why router navigations in modern frameworks are wrapped in transitions by default — clicking a link should not blank a working page into a spinner. The exception is precise: only *already revealed* content is protected. A `Suspense` boundary mounting for the first time inside the transition still shows its fallback — there is no previous UI to hold.

## When transitions do nothing

Transitions buy you interruptibility *between* components, applied to *render-phase* work. Three situations defeat them. **One giant component:** React yields between component renders, never inside one — a single component that takes 200 ms (a monolithic chart building 10,000 SVG elements in one function) blocks the lane regardless of priority; break it into smaller components or virtualize. **The cost is not render:** if the slowness is a synchronous `filter` over 100k items inside the event handler itself, or layout thrash in effects, the work happens outside the render phase that transitions schedule. **One cheap commit:** if the update was a 20 ms render committing once, there is nothing to slice — wrapping it changes nothing observable. A transition is a scheduler hint, not a speedup: total CPU work is identical or slightly higher (abandoned renders are paid for and thrown away — that is the deal: wasted watts for responsive input).

<Quiz
  id="03-transitions-and-deferred-quiz-2"
  lessonSlug="03-transitions-and-deferred"
  lang="en"
  question="Clicking a tab swaps in a component that suspends while fetching. Without a transition the whole page is replaced by a spinner; inside startTransition it is not. What exactly does the transition change?"
  choices={[
    { label: "React keeps the committed UI on screen and renders the suspended tree in the background, committing only when ready — already-revealed content is never replaced by a fallback during a transition", correct: true },
    { label: "The transition makes the fetch start earlier, so the data arrives before the fallback is needed", misconception: "Transitions do not touch data fetching at all — the fetch starts at the same moment and takes as long as it takes. What changes is what is shown while waiting: the previous committed UI instead of the boundary's fallback." },
    { label: "The transition suppresses Suspense entirely, so the component renders with empty data", misconception: "The component still suspends — rendering cannot proceed without the data. The transition changes Suspense's visible behavior for revealed content: hold the old UI rather than hide it behind a fallback. New boundaries mounting inside the transition still show fallbacks." },
    { label: "Fallbacks are only skipped because the transition delays the update until the data is cached", misconception: "There is no waiting-for-cache delay; the background render genuinely starts, suspends, and resumes when data arrives. If the user navigates again mid-flight, the transition is interrupted and restarted — behavior a simple delay could not provide." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A keystroke fans out into two lanes: the urgent echo commits within a frame, while the transition renders the heavy tree in the background, interruptible by the next keystroke"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "keystroke", label: "Keystroke", sub: "event handler" },
    { id: "urgent", label: "Urgent lane", sub: "setText — input echo" },
    { id: "commit1", label: "Commit ≤1 frame", sub: "input updates" },
    { id: "transition", label: "Transition lane", sub: "setQuery — heavy tree" },
    { id: "bg", label: "Background render", sub: "yields every ~5 ms" },
    { id: "commit2", label: "Commit when done", sub: "or discard + restart" },
  ]}
  edges={[
    { from: "keystroke", to: "urgent", label: "unwrapped" },
    { from: "keystroke", to: "transition", label: "startTransition" },
    { from: "urgent", to: "commit1", label: "immediate" },
    { from: "transition", to: "bg", label: "schedule" },
    { from: "bg", to: "commit2", label: "complete" },
    { from: "commit1", to: "bg", label: "interrupts + restarts" },
  ]}
  caption="The screen always shows the last committed UI: the input echoes instantly from the urgent lane while the transition render is abandoned and restarted on each new keystroke. With Suspense, the same mechanism holds revealed content on screen instead of flashing a fallback."
/>

<KeyTakeaway>startTransition reclassifies updates as non-urgent: React renders them in the background, yielding to the main thread and restarting when urgent updates land — same CPU cost, responsive input. Keep the directly-manipulated value out of the transition; use useDeferredValue when you receive the value instead of owning the setter, and memoize the expensive subtree on the deferred value. Inside a transition, suspending keeps shown UI instead of flashing a fallback. None of it helps when the cost sits in one giant component, in event handlers, or in a single cheap commit.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-transitions-and-deferred-retrieval"
  lang="en"
  questions={[
    { q: "Walk through what React does, frame by frame, when a user types two characters quickly into an input whose handler does setText urgently and setQuery inside startTransition.", a: "First character: setText schedules an urgent update; React renders the small urgent tree (the input echoes 'a') and commits within a frame. The transition update for query starts rendering the heavy list in the background — React processes components one at a time, yielding to the main thread roughly every 5 ms, so the page stays interactive while the work-in-progress tree grows. Second character arrives mid-render: the urgent setText interrupts; React abandons the half-built transition tree entirely (the screen never saw it — only commits are visible), commits the input echo 'ab' immediately, then restarts the transition render from the latest state, query = 'ab' — it does not finish the stale 'a' render first. When a transition render finally completes without interruption, it commits in one atomic pass and the list catches up. Net effect: input latency is one cheap commit regardless of list size; the heavy render is paid possibly multiple times (abandoned attempts are wasted CPU), which is the explicit trade — watts for responsiveness. isPending is true throughout, so the UI can dim the stale list honestly." },
    { q: "Name the three situations where wrapping an update in startTransition changes nothing, and what the actual fix is in each.", a: "One: the expensive work lives in a single giant component. React yields between component renders, never inside one function, so a monolithic 200 ms chart component blocks the main thread in any lane. Fix: decompose into many small components so there are yield points, or virtualize/limit what it draws. Two: the cost is not render-phase work — a synchronous 100k-item filter inside the event handler, a heavy useLayoutEffect, layout thrash in commit. Transitions schedule render work only; handler code runs before any of it at full price. Fix: move the computation into render derived from transition state (making it sliceable), memoize it, or take it off the main thread (worker). Three: the update is one cheap commit anyway — a 20 ms render commits once whether wrapped or not; there is nothing to interrupt and no observable difference. Fix: nothing — there was no problem, and the wrapper is noise. The unifying mental model: a transition is a priority label on state updates, read by the scheduler during the render phase. Work that is indivisible, outside render, or already fast has nothing for that label to act on." },
  ]}
/>

<Recap lang="en">
Concurrent React's answer to expensive updates is not making them faster but making them interruptible. startTransition labels the state updates in its callback non-urgent: React renders them in the background against a work-in-progress tree, yields to the main thread between components roughly every 5 ms, lets any urgent update interrupt — abandoning the half-built tree and restarting from the newest state — and commits only a finished result. The screen always shows the last committed UI, and isPending reports the gap. The cardinal mistake is wrapping the value the user is directly manipulating: a controlled input driven by transition state echoes keystrokes at background priority, which is the lag the catalog team shipped. Split the state — urgent for the echo, transition for the heavy tree. When the changing value arrives as a prop instead of your own setState, useDeferredValue is the receiving-side mirror: it holds the old value through a cheap urgent render (memoize the expensive subtree on the deferred value, or the trick buys nothing) and catches up in a background render. With Suspense, transitions remove the fallback flash: an update that suspends keeps already-revealed content on screen and commits when data arrives — the reason framework routers wrap navigations in transitions — though boundaries mounting for the first time still show their fallback. And know the limits: yields happen between components, so one monolithic 200 ms component blocks any lane; handler-side computation and effects run outside the scheduled render; a single cheap commit has nothing to slice. Total CPU goes up, not down — abandoned renders are the price of an input that never stutters.
</Recap>
