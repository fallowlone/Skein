---
concepts:
- react-virtualization
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-rendering-cost-and-virtualization"
sources:
- https://tanstack.com/virtual/latest
- https://developer.mozilla.org/en-US/docs/Web/CSS/content-visibility
- https://react.dev/learn/rendering-lists
spiral: []
status: ready
summary: 'Render volume kills: a 10k-row table is 300k DOM nodes and a second per keystroke. Virtualization renders only the viewport plus overscan (TanStack Virtual: estimateSize, measureElement, translate); content-visibility is the CSS cousin for static content. Keys must be item ids.'
title: 'Rendering cost and virtualization: the 10,000-row table that renders 30 rows'
track: "react"
unit: "05-performance"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The back-office team inherited a transactions table that "worked fine in staging". Staging had 200 rows; the biggest customer had 11,400. On that account, typing in the filter box cost 1.3 seconds per keystroke, and scrolling stuttered even with the filter untouched. The first response was reflexive: `React.memo` on the row component, `useMemo` on the formatter. The commit time dropped from 1.3 s to 1.1 s — memoization helped the React half a little, but the page was still dragging 340,000 DOM nodes around, and every filter change still asked the browser to restyle and re-layout a 90,000-pixel-tall document. The actual fix was admitting that no user ever sees 11,400 rows at once. A virtualizer rendered the 25 rows in the viewport plus a few of overscan; the DOM fell from 340,000 nodes to under 1,500, keystroke commits fell to 18 ms, and scroll went back to native speed. Same data, same row component — two orders of magnitude less work, because the work was never necessary.
</Hook>

<Crux>Render volume kills: 10k rows is 10k renders per update. Virtualization renders only the viewport plus overscan — the rest never exists.</Crux>

<Explanation>

## Why 10,000 rows is slow twice

A big list hurts you in two separate engines, and memoization only talks to one of them. First, the **React cost**: an update that touches the list — a filter keystroke, a sort, a websocket tick — re-renders row components. Even a cheap 0.1 ms row times 10,000 is a full second of render phase, and `React.memo` only helps for rows whose props did not change, which a filter by definition defeats. Second, the **browser cost**, which exists even when React is idle: a row of eight cells is easily 30+ DOM nodes, so 10k rows is ~300k nodes that consume memory, slow down style recalculation, and make every layout pass touch a document tens of thousands of pixels tall. This is why the Hook team's memoization shaved 0.2 s and no more — they optimized the React engine while the layout engine was the one drowning.

The structural observation that fixes both: the viewport shows maybe 25 rows. Everything else is rendered, laid out, and painted for nobody. **Virtualization (windowing)** renders only the visible slice into a container with one tall spacer element preserving the scrollbar geometry, and repositions the slice as the user scrolls. React renders 30 components instead of 10,000; the browser owns 1,500 nodes instead of 300,000. The list becomes O(viewport) instead of O(data).

## TanStack Virtual: the mechanics

A headless virtualizer like TanStack Virtual does three jobs: track scroll position, decide which indexes are visible, and tell you where to absolutely position them. You keep full ownership of the markup:

```jsx
function TransactionList({ items }) {
  const parentRef = useRef(null);
  const virtualizer = useVirtualizer({
    count: items.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 44,   // px guess per row — corrected by measurement
    overscan: 5,              // extra rows above/below the viewport
  });

  return (
    <div ref={parentRef} style={{ height: 600, overflow: "auto" }}>
      <div style={{ height: virtualizer.getTotalSize(), position: "relative" }}>
        {virtualizer.getVirtualItems().map((v) => (
          <div
            key={items[v.index].id}          // data id — never v.index
            ref={virtualizer.measureElement} // measures real height
            data-index={v.index}
            style={{ position: "absolute", top: 0, left: 0, width: "100%",
                     transform: `translateY(${v.start}px)` }}
          >
            <Row item={items[v.index]} />
          </div>
        ))}
      </div>
    </div>
  );
}
```

The moving parts are worth naming. `estimateSize` is a guess; for dynamic heights, `measureElement` observes each mounted row and replaces the guess with reality, shifting the offsets of everything below — a wildly wrong estimate makes the scrollbar jump as measurements come in, so estimate from your real median row. `overscan` renders a few extra rows beyond the viewport so fast scrolling shows content instead of blank flashes; each unit of overscan is rendering you paid for that may never be seen, so 3–10 is the usual range, not 50. **Failure modes to expect:** `position: sticky` headers do not work naturally inside a transform-positioned list (the virtualizer's own translate breaks the sticky containing block — pin headers outside the scroll container, or use the virtualizer's sticky-item support); and remote data needs the count from the server before the scrollbar can be honest.

<Quiz
  id="02-rendering-cost-and-virtualization-quiz-1"
  lessonSlug="02-rendering-cost-and-virtualization"
  lang="en"
  question="In a virtualized list, a developer keys rows with key={v.index} (the virtual index). Rows expand when clicked. The user expands row 3, scrolls 500 rows down — and some unrelated row appears expanded. Why?"
  choices={[
    { label: "The visible window keys components by position, so after scrolling, index 3 names a different data item — React matches the old component state (expanded) to the new occupant of that index", correct: true },
    { label: "The virtualizer failed to unmount the expanded row when it left the viewport", misconception: "The virtualizer did unmount it — that is exactly the problem. With index keys, React sees a 'row 3' before and after the scroll and treats them as the same component, reattaching the surviving slot's state to whatever data now sits at that index. Keying by item id makes the identity follow the data." },
    { label: "measureElement cached the expanded height and applied it to the wrong row", misconception: "Measurement affects layout offsets, not component state — a stale measurement would misposition rows by some pixels, not expand them. The expanded flag lives in React state, and state follows keys; the bug is identity, not geometry." },
    { label: "Overscan rendered the expanded row off-screen and it got cloned during scroll", misconception: "Overscan just widens the rendered window; nothing is cloned. The state bleed happens because index-based keys recycle component identity across different data items — the classic list-key rule, made vicious by virtualization because the window's indexes shift constantly." },
  ]}
/>

## content-visibility: the CSS alternative

Sometimes you cannot virtualize — the markup comes from a CMS, the list is semantic content where find-in-page and Ctrl-F matter, or the refactor cost is not worth it. CSS `content-visibility: auto` tells the browser to skip rendering work (style, layout, paint) for off-screen elements while keeping them in the DOM; pair it with `contain-intrinsic-size: auto 44px` so skipped elements still reserve estimated space and the scrollbar stays sane. The crucial difference from windowing: **the nodes still exist**. React still rendered all 10,000 components, memory still holds 300k nodes, and find-in-page still works because the content is real. So `content-visibility` rescues *scrolling and initial layout* (the browser engine) but does nothing for *update cost* (the React engine) — a keystroke that re-renders 10,000 rows is exactly as slow as before. **Decision rule:** browser-side cost on mostly-static content → `content-visibility`, one line of CSS; React-side update cost on interactive data → virtualization, a real refactor. The Hook's table needed the refactor because typing, not just scrolling, was slow.

<Quiz
  id="02-rendering-cost-and-virtualization-quiz-2"
  lessonSlug="02-rendering-cost-and-virtualization"
  lang="en"
  question="A long article page (thousands of DOM nodes, no interactivity) scrolls badly. A 10k-row live-updating table is slow on every data tick. Which tool fits which problem?"
  choices={[
    { label: "Article: content-visibility — one line of CSS skips off-screen rendering work and keeps find-in-page. Table: virtualization — only it reduces how many components React renders per update", correct: true },
    { label: "Both should be virtualized; content-visibility is just the inferior older technique", misconception: "They solve different layers. Virtualizing a semantic article costs a refactor, breaks find-in-page and native anchors (the content stops existing in the DOM), and buys nothing extra for static content. content-visibility is not older or inferior — it is browser-side skipping for exactly this case." },
    { label: "Both need only content-visibility; the browser will skip the table's off-screen updates too", misconception: "content-visibility skips the browser's style/layout/paint for off-screen nodes, but React still renders all 10,000 row components into the DOM on every tick — the JavaScript cost is untouched. The table's pain is in the React engine, which only windowing addresses." },
    { label: "Neither: the right fix is React.memo on rows in both cases", misconception: "Memoization helps only rows whose props are unchanged, does not reduce DOM size at all, and does nothing for the article (which has no React updates). The Hook's team tried exactly this and shaved 0.2 s of 1.3 — volume problems need volume solutions." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A scroll event flows through the virtualizer: compute the visible range, map it to data, render only that slice into a positioned window inside a fixed-height spacer"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "scroll", label: "Scroll / data update", sub: "trigger" },
    { id: "range", label: "Visible range", sub: "scrollTop / row size" },
    { id: "overscan", label: "+ overscan", sub: "3–10 extra rows" },
    { id: "slice", label: "Render slice", sub: "~30 components" },
    { id: "position", label: "translateY(start)", sub: "absolute positioning" },
    { id: "spacer", label: "Total-size spacer", sub: "honest scrollbar" },
  ]}
  edges={[
    { from: "scroll", to: "range", label: "recompute" },
    { from: "range", to: "overscan", label: "widen" },
    { from: "overscan", to: "slice", label: "map to items" },
    { from: "slice", to: "position", label: "offset each row" },
    { from: "spacer", to: "scroll", label: "keeps geometry" },
    { from: "position", to: "spacer", label: "inside" },
  ]}
  caption="The DOM holds the window, not the data: 10,000 items become ~30 mounted rows positioned by transform inside a spacer that preserves scroll height. measureElement corrects estimateSize for dynamic heights; keys come from item ids so state follows data, not slots."
/>

<KeyTakeaway>Large lists are slow in two engines: React re-renders O(n) components per update, and the browser drags O(n) DOM nodes through style and layout even at rest. Memoization trims neither volume. Virtualization renders only the viewport plus a small overscan into a spacer-backed window — O(viewport) in both engines; estimate row size honestly, let measureElement correct it, and always key by item id. content-visibility is the CSS escape hatch for static content: it skips browser rendering work but leaves React's update cost untouched.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-rendering-cost-and-virtualization-retrieval"
  lang="en"
  questions={[
    { q: "A 10k-row table is slow both while typing in its filter and while scrolling. Explain the two distinct cost centers and why React.memo meaningfully fixes neither.", a: "Cost one is the React engine: any update touching the list — each filter keystroke — re-renders row components, and 10,000 rows at even 0.1 ms each is a second of render phase per keystroke. React.memo only skips rows whose props are unchanged, and a filter changes which rows exist and what they show, so most of the work survives. Cost two is the browser engine, present even with React idle: at ~30 nodes per row the table is ~300k DOM nodes, which bloat memory, slow every style recalculation, and make layout passes crawl a document tens of thousands of pixels tall — that is the scroll stutter, and memoization is invisible to it. Virtualization attacks the volume itself: render the ~25 visible rows plus overscan, absolutely positioned inside a total-height spacer, so both engines do O(viewport) work. Typical result mirrors the lesson's case: 340k nodes to ~1,500, keystroke commits from over a second to ~18 ms." },
    { q: "Name the three classic operational pitfalls of a windowed list — row heights, overscan, and keys — and what goes wrong in each if handled naively.", a: "Heights: the virtualizer starts from estimateSize to lay out unmounted rows; with dynamic content the estimate is wrong, so measureElement observes each mounted row and corrects offsets. If the estimate is far off, total size keeps being revised as real measurements arrive and the scrollbar visibly jumps under the user's thumb — estimate from the real median row. Overscan: extra rows rendered beyond the viewport hide blank flashes during fast scrolling, but every overscanned row is paid-for rendering that may never be seen; 3–10 is the working range, and cranking it to 50 quietly rebuilds the original volume problem. Keys: keying by the virtual index recycles component identity as the window shifts — expand row 3, scroll, and a different data item appears expanded, because React matched the slot's state to the new occupant of index 3. Key by stable item id so identity and state follow the data. Bonus trap: position: sticky headers break inside transform-positioned rows — pin them outside the scroll container or use the virtualizer's sticky support." },
  ]}
/>

<Recap lang="en">
A 10,000-row table is slow twice, in two different engines. React pays O(n) per update: a filter keystroke re-renders the rows, and memoization only rescues rows whose props are identical — which a filter defeats by design. The browser pays O(n) continuously: ~30 nodes per row means ~300,000 DOM nodes that inflate memory, style recalculation, and layout, which is why the Hook team's React.memo campaign moved 1.3 s to 1.1 s and stopped. Virtualization fixes the volume itself: render only the visible ~25 rows plus a small overscan, absolutely positioned with translateY inside a spacer div whose height (getTotalSize) keeps the scrollbar honest. TanStack Virtual supplies the mechanics — estimateSize as the initial guess, measureElement to replace guesses with real heights for dynamic rows (a bad estimate makes the scrollbar jump as corrections land), overscan of 3–10 to cover fast scrolling without re-buying the volume problem. Two discipline points: key rows by item id, never by virtual index, or component state bleeds onto whatever data scrolls into the recycled slot; and sticky headers need to live outside the transformed rows. When the content is static and semantic — long articles, CMS output — content-visibility: auto with contain-intrinsic-size is the one-line alternative: the browser skips off-screen style, layout and paint while the DOM stays real (find-in-page works), but React's update cost is untouched, because all 10,000 components still render. Browser-side pain on static content takes the CSS; React-side update pain on live data takes the refactor.
</Recap>
