infographic compare-binary-horizontal-underline-text-vs
data
  title Two sprints on noise, the signal untouched
  desc The memo campaign moved about 4 ms; users felt a 620 ms remount nobody profiled
  compares
    - label Memoized: what they fixed
      children
        - label Re-renders down 60%
          desc a PR demo gif proved it
        - label About 4 ms saved per commit
          desc the wasted re-renders that got eliminated
        - label actualDuration 42 to 38 ms
          desc on the commit that mattered, within noise
        - label Two sprints of work
          desc React.memo, useCallback, a custom compare fn
    - label Measured: what users felt
      children
        - label Filter dropdown INP 750 ms
          desc field monitoring, unchanged by the campaign
        - label One 620 ms commit
          desc a 9,000-point SVG chart remounted
        - label Cause was a fresh key
          desc parent passed a new key on every filter change
        - label Bypassed memoization
          desc a remount skips the memo comparison entirely
theme
  palette #6366f1 #f43f5e
