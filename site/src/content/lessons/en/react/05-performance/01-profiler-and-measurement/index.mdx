---
concepts:
- react-profiling
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-profiler-and-measurement"
sources:
- https://react.dev/reference/react/Profiler
- https://react.dev/learn/react-developer-tools
- https://web.dev/articles/inp
spiral: []
status: ready
summary: 'Profile before optimizing: the React DevTools flamegraph shows which commits cost what, the Profiler API turns that into numbers you can ship, and INP — not the 16 ms frame myth — is the budget users actually feel. A team that memoized for two sprints fixed 4 ms and missed 620.'
title: 'Profiler and measurement: find the 600 ms commit before touching React.memo'
track: "react"
unit: "05-performance"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import noiseVsSignal from "./infographics/noise-vs-signal.svg?raw";

<Hook>
The dashboard team spent two sprints on a memoization campaign: forty components wrapped in `React.memo`, every callback in `useCallback`, a hand-written comparison function for the chart props. The PR description bragged that re-renders were down 60%, with a demo gif to prove it. Then the on-call channel lit up anyway — support kept forwarding the same complaint: "the filters freeze". Field monitoring put INP at 750 ms on the filter dropdown, exactly where it had been before the campaign. One hour with the React DevTools Profiler told the real story. The wasted re-renders the team had eliminated cost about 4 ms per commit. The freeze was a single 620 ms commit in which changing the date range forced a 9,000-point SVG chart to remount, because a parent passed it a fresh `key` on every filter change. Two sprints had optimized noise; the signal was one flamegraph bar wide enough to read from across the room. Nobody had profiled before optimizing.
</Hook>

<Crux>Measure first: the Profiler shows which commits cost what, INP shows what users feel. Unmeasured optimization fixes the wrong thing.</Crux>

<Explanation>

## Reading the flamegraph: commits, not components

The React DevTools Profiler records a session of interactions and slices it into **commits** — every time React actually flushed changes to the DOM, you get one bar in the timeline. Selecting a commit shows a **flamegraph**: each row of the tree is a component, the width of its bar is how long it (plus its children) took to render in that commit, and gray bars are components that did not render at all. This framing matters: the question is never "which component is slow" in the abstract, it is "which commit is slow, and what inside it burned the time". A 600 ms commit with one wide bar is a different disease than sixty 10 ms commits triggered by a chatty subscription — and they need opposite cures.

Two settings turn the tool from a chart into a diagnosis. **"Record why each component rendered"** annotates every bar with the cause: props changed (and which ones), state changed, a hook changed, or the parent rendered. That single annotation kills most guesswork — the chart remount in the Hook story showed up as "key changed", which no amount of staring at component code would reveal. Second, the **"Highlight updates"** checkbox paints rectangles over everything that re-renders live, which is the fastest way to spot a context provider rerendering half the page on every keystroke. Profile in development first for the rich data, but remember development React is materially slower — confirm any number that drives a decision against a production profiling build (`react-dom/profiling`), which keeps timing hooks at a few percent overhead.

## The Profiler API: numbers you can ship

DevTools is interactive; the `<Profiler>` component is programmable. Wrap a subtree, and React calls `onRender` after every commit inside it with timing data you can log, aggregate, or send to your monitoring backend:

```jsx
import { Profiler } from "react";

function onRender(id, phase, actualDuration, baseDuration, startTime, commitTime) {
  // actualDuration: time spent rendering this commit (with memoization applied)
  // baseDuration: estimated worst case — every descendant rendering, no memo
  metrics.send("react-commit", {
    id,                       // "FilterPanel"
    phase,                    // "mount" | "update" | "nested-update"
    actual: actualDuration,   // e.g. 4.2 (ms)
    base: baseDuration,       // e.g. 38.7 (ms)
  });
}

<Profiler id="FilterPanel" onRender={onRender}>
  <FilterPanel />
</Profiler>
```

The pair worth internalizing is `actualDuration` versus `baseDuration`. `actualDuration` is what this commit really cost; `baseDuration` is the sum of each descendant's most recent render time — an estimate of the worst case where nothing is memoized. When `actualDuration` sits far below `baseDuration`, your memoization is earning its keep; when the two track each other, the `React.memo` wrappers are decoration. That comparison is exactly the evidence the Hook team never collected: their memo campaign moved `actualDuration` from 42 ms to 38 ms on the commit that mattered — within noise — because the 620 ms remount bypassed memoization entirely. **Tradeoff:** `<Profiler>` is cheap but not free (React does extra bookkeeping per commit inside profiled trees), so in production wrap the two or three interactions you actually monitor, not the root.

<Infographic
  svg={noiseVsSignal}
  label="Side-by-side comparison: the memoization campaign (re-renders down 60%, about 4 ms saved per commit, actualDuration 42 to 38 ms) versus what users actually felt (filter dropdown INP 750 ms, one 620 ms remount commit caused by a fresh key that bypassed memoization)."
  caption="Two sprints moved a ~4 ms re-render saving the user can never feel, while the 620 ms remount that defined INP went unprofiled — the case for measuring before optimizing."
  hue="var(--d-frontend)"
/>

<Quiz
  id="01-profiler-and-measurement-quiz-1"
  lessonSlug="01-profiler-and-measurement"
  lang="en"
  question="In onRender data for a commit, actualDuration is 39 ms and baseDuration is 41 ms, even though the subtree is wrapped in React.memo extensively. What does this tell you?"
  choices={[
    { label: "The memoization is doing almost nothing for this commit — nearly the full unmemoized cost is being paid, so either props genuinely change or the memo checks are defeated (fresh objects, changing keys)", correct: true },
    { label: "The Profiler overhead is inflating actualDuration, so the real render is much faster", misconception: "Profiling adds a small bookkeeping cost, but it inflates both numbers together — it cannot explain actual tracking base. When the two are close, descendants really are rendering; the comparison is meaningful precisely because both come from the same instrumented run." },
    { label: "baseDuration includes commit-phase DOM mutation time, so the numbers are not comparable", misconception: "Both durations measure render-phase work only — baseDuration is the sum of each descendant's most recent render time, an estimate of the no-memoization worst case. DOM mutation time is not in either number; they are directly comparable by design." },
    { label: "This is expected: actualDuration should always be within a few ms of baseDuration", misconception: "In a well-memoized subtree actualDuration is often a small fraction of baseDuration, because most descendants bail out and contribute nothing to the actual time. The two converging is the signature of memoization that has stopped working." },
  ]}
/>

## INP and the 16 ms myth

The number everyone quotes — 16.7 ms, one frame at 60 Hz — is a budget for **continuous motion**: animation, scrolling, dragging. Miss it during a drag and the user sees stutter. But a click is not an animation. For discrete interactions the user-facing metric is **INP (Interaction to Next Paint)**: from the keypress or click until the next frame paints, including **input delay** (the main thread was busy when the event arrived), **processing** (your handlers, React's render and commit), and **presentation delay** (style, layout, paint). The thresholds are 200 ms for "good" and 500 ms for "poor" — roughly 12 to 30 frame-budgets, and your page's INP is approximately its *worst* such interaction over the visit, not the average.

This is why the 16 ms framing misleads optimization work in both directions. It makes a 30 ms commit on a button click look like a violation worth a refactor, when the user cannot distinguish 30 ms from instant. And it says nothing about the real killer: any main-thread task over 50 ms is a **long task** that adds input delay to whatever the user does next — a 620 ms commit doesn't just delay its own interaction, it freezes every click that arrives during it. The operational loop, in order: read INP from field data (it is interaction-specific, so lab numbers on a fast laptop routinely miss it), find the worst interaction, profile *that* interaction, fix the widest bar, measure again. The Hook team ran this loop backwards — fix first, measure never.

<Quiz
  id="01-profiler-and-measurement-quiz-2"
  lessonSlug="01-profiler-and-measurement"
  lang="en"
  question="A click handler triggers a React commit measured at 35 ms. A teammate flags it: 'that is more than two frames, we must memoize'. What is the correct call?"
  choices={[
    { label: "Leave it: 16.7 ms is a budget for continuous motion, while a discrete click is judged by INP, where 35 ms is deep inside the 200 ms 'good' threshold — optimization effort belongs on the worst interaction, not this one", correct: true },
    { label: "The teammate is right: every commit that misses one frame is perceivable jank", misconception: "Frame budget applies when the user is watching continuous motion — animation, scroll, drag. For a discrete click the user perceives responsiveness via time to next paint, and 35 ms is indistinguishable from instant; INP's 'good' threshold is 200 ms, not 16." },
    { label: "Split the commit into multiple smaller commits to fit each under 16 ms", misconception: "Splitting one cheap commit into several adds work (more reconciliation passes, more paints) and improves nothing the user can perceive. Chunking only pays off for long tasks over 50 ms that block input — 35 ms is not one." },
    { label: "Measure the average commit duration across the session; if the mean is under 16 ms the page is fine", misconception: "INP is approximately the worst interaction of the visit, not a mean — averages hide exactly the 600 ms outlier that defines the user's experience. One terrible filter click outweighs a thousand fast ones in both the metric and the user's memory." },
  ]}
/>

</Explanation>

<DiagramFrame label="One discrete interaction rides the INP timeline: the tap waits out the input delay while the main thread is busy, passes through processing — handlers, render, commit — and the clock stops only at the next paint" hue="var(--d-frontend)" caption="Three phases sum to one interaction's latency: input delay, processing (the React share lives here — what the Profiler records per commit), and presentation to the next painted frame. Field INP is roughly the worst interaction, not the mean: optimization runs right to left — field data names the worst interaction, the flamegraph names the expensive commit inside it. The 16 ms frame budget governs continuous motion; discrete interactions get 200 ms.">
  <svg viewBox="0 0 360 130" width="360" height="130" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="180" y="20" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">the INP clock runs from tap to next paint</text>
    <rect x="6" y="46" width="42" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="27" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">tap</text>
    <rect x="60" y="46" width="86" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="103" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">input delay</text>
    <text x="103" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">main thread busy</text>
    <rect x="158" y="46" width="100" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="208" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">processing</text>
    <text x="208" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">handlers + render + commit</text>
    <rect x="270" y="46" width="84" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="312" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">presentation</text>
    <text x="312" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">next paint</text>
    <path data-path d="M48 62 L350 62" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <text x="180" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">INP ≈ worst interaction on the page — budget 200 ms</text>
    <PacketDot color="var(--d-frontend)" durationMs={1500} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1500} delayMs={500} size={8} />
  </svg>
</DiagramFrame>

<KeyTakeaway>Performance work starts with a measurement, not a hypothesis. The DevTools Profiler slices a session into commits and shows, per commit, which component burned the time and why it rendered; the Profiler API ships the same timing (actualDuration vs baseDuration) to monitoring, exposing whether memoization earns anything. Judge user impact with INP — 200 ms for discrete interactions, dominated by the worst case — not the 16 ms frame budget, which applies to continuous motion. Fix the widest bar in the worst interaction, then measure again.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-profiler-and-measurement-retrieval"
  lang="en"
  questions={[
    { q: "Your monitoring shows INP of 700 ms on a dropdown. Describe the full diagnostic loop you would run before changing any code, naming the tools and the numbers you would read.", a: "Start in field data: confirm which interaction is the worst (INP is approximately the worst interaction of a visit, so the dropdown number is one specific event, not an average). Reproduce it locally with the React DevTools Profiler recording: the session splits into commits, and the bar chart immediately shows whether the 700 ms is one giant commit or a storm of small ones — opposite diseases. Open the worst commit's flamegraph: bar width is render cost, gray means did-not-render, and 'record why each component rendered' annotates the cause (props, state, parent, key change). Distinguish the INP components: input delay (was the main thread already blocked by a long task over 50 ms?), processing (handlers plus render plus commit — the React share), presentation delay. Confirm any decisive number on a production profiling build, since dev React is materially slower. Only then change code, targeting the widest bar — and re-measure the same interaction afterwards to prove the fix moved INP, not just render counts." },
    { q: "Explain what actualDuration and baseDuration mean in the Profiler onRender callback, and how comparing them tells you whether a memoization campaign was worth it.", a: "Both describe the render phase of one commit inside the profiled subtree. actualDuration is the time that commit really spent rendering — with all bail-outs and memoization applied. baseDuration is a synthetic worst case: the sum of every descendant's most recent individual render time, i.e. what the commit would roughly cost if nothing memoized and the whole subtree re-rendered. A healthy memoized tree shows actualDuration as a small fraction of baseDuration — most children bailed out and contributed nothing. When actualDuration tracks baseDuration closely despite React.memo everywhere, the wrappers are being defeated: fresh object or function props on every render, or a changing key forcing remounts (which skip memo comparison entirely). And the comparison guards against the inverse mistake too — if baseDuration itself is only 40 ms on the interaction users complain about, memoization cannot be the fix, because even the worst case is cheap; the real cost lives in some other commit, which is exactly the trap of optimizing without reading these two numbers first." },
  ]}
/>

<Recap lang="en">
React performance work has a non-negotiable first step: measure, because intuition reliably points at the wrong subtree. The React DevTools Profiler records interactions as a series of commits; each commit's flamegraph shows render cost as bar width, gray bars for components that bailed out, and — with "record why each component rendered" on — the reason each bar exists: changed props, changed state, a parent render, or a changed key (the silent remount trigger that defeated the Hook team's two-sprint memo campaign while a 620 ms chart remount hid in plain sight). The programmable counterpart is the Profiler component: its onRender callback reports actualDuration (what the commit cost with memoization applied) against baseDuration (the estimated no-memoization worst case), and the gap between them is the only honest measure of whether React.memo is earning anything — wrap the few interactions you monitor, not the root, since profiling has a small per-commit cost. Judge impact in the user's units, not React's: discrete interactions are governed by INP — input delay, processing, presentation delay, good under 200 ms, poor over 500 ms, dominated by the worst interaction of the visit — while the famous 16.7 ms budget applies to continuous motion like scrolling and animation. A 35 ms click commit needs nothing; a 620 ms one is a long task that also freezes every other click that lands during it. The loop is: field INP names the interaction, the flamegraph names the commit, fix the widest bar, measure again. Now when you see a teammate reach for `React.memo` before opening the Profiler, you know exactly what to ask: which commit, which bar, and what does field INP say about this interaction?
</Recap>
