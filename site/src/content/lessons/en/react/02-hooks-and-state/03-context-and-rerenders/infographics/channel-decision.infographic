infographic compare-quadrant-quarter-simple-card
data
  title Choosing the channel: frequency x fanout
  desc Two axes decide it — how often the value changes, and how many components read it. Only hot + wide read needs a selector store.
  compares
    - label Rare + wide read — Context
    - label Rare + 2-3 hops — props
    - label Hot + wide read — store selectors
    - label Hot + narrow — local state
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
