infographic chart-column-simple
data
  title Client JS per article route (kB)
  desc Same markdown, highlighter and i18n — RSC runs them on the server, shipping output not libraries
  values
    - label SPA — all libs ship
      value 1100
    - label RSC — output only
      value 210
theme
  palette #f43f5e #14b8a6
