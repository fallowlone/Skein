---
concepts:
- react-rsc-serialization
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-serialization-boundary"
sources:
- https://react.dev/reference/rsc/use-client
- https://react.dev/reference/rsc/use-server
- https://react.dev/reference/rsc/server-components
spiral: []
status: ready
summary: 'use client marks a module-graph cut: everything a client file imports joins the bundle. Props crossing must serialize — plain data, Date, Map yes; functions and class instances no. Children put server output inside a client shell; use server is the reverse channel.'
title: 'The serialization boundary: use client splits the module graph'
track: "react"
unit: "11-react-server-components"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The security review found it in fifteen minutes with view-source. A profile page — server component — fetched the current user with the ORM and passed the row straight into a client component: `<ProfileCard user={user} />`. The ORM returned a plain object, so serialization succeeded without a whisper, and the Flight payload inlined into every profile page contained the full row: `email`, `phone`, `stripeCustomerId`, `passwordHash`, `mfaSecret`. No request was hacked; the server politely published its database columns to anyone who pressed Ctrl+U. The same audit caught the inverse failure a week earlier: a shared `format.ts` helper had been imported into a `'use client'` file, and `format.ts` imported `config.ts`, which read `process.env.STRIPE_SECRET_KEY` into an exported constant — one hop later the secret was sitting in a public chunk. Neither bug is exotic. Both are the same misunderstanding: `'use client'` is not a component annotation, it is a cut through the module graph, and props crossing that cut are serialized and published. The boundary has exact rules, and this lesson is those rules.
</Hook>

<Crux>'use client' cuts the module graph, not a component. Whatever crosses the cut is serialized and public — plan props like an API.</Crux>

In ten minutes you will know exactly which values can cross the server-to-client boundary, which ones silently leak your database columns, and how the module graph cut works — because both bugs in the Hook are entirely avoidable once you see the rules clearly.

<Explanation>

## A directive on a module, not a flag on a component

`'use client'` goes at the top of a *file*, and it means: from here on, this module and **everything it imports, transitively** belong to the client bundle. It is a graph cut, not a component property. Mark a 40-line `Tabs.tsx` and you have not added 40 lines to the bundle — you have added `Tabs.tsx` plus its imports, plus *their* imports, until the graph bottoms out. One careless `import { format } from "../lib/format"` where `format.ts` pulls a charting utility pulls the chart library too. This is also why a server component imported *by* a client file silently becomes a client component: there is no marker on the component itself, only its position in the graph relative to the nearest `'use client'` cut. The corollary discipline: keep client files small and leaf-like, and treat every import added to one as a bundle-size and secrecy decision.

The defensive tool for the secrecy half is the `server-only` package: import it in any module that must never reach the client (`db.ts`, `config.ts`, anything touching `process.env`), and the build fails the moment some refactor drags that module across the cut. It costs one import line and converts a silent leak into a compile error. The mirror package `client-only` exists for modules that must never run server-side.

## What may cross: the serialization rules

Props passed from a server component to a client component are serialized into the Flight payload. The allowed list is wider than JSON but strict: primitives (string, number, bigint, boolean, undefined, null, symbols only via `Symbol.for`), plain objects and arrays, `Date`, `Map`, `Set`, TypedArrays and `ArrayBuffer`, `FormData`, JSX produced by server components, Promises (the client unwraps them with `use()` — this is how streaming data crosses), and references to server functions. The rejected list is what defines the architecture: **functions** (other than `'use server'` functions), **class instances** (anything whose prototype is not `Object.prototype` — an ORM entity, a `Temporal` object, your domain model), and unregistered symbols. A rejected value throws at render time with a serialization error naming the prop.

The dangerous case is the one that *does not* throw. Most ORMs return plain objects — so a full database row sails through serialization and lands, column by column, in a payload any user can read. The Hook's `passwordHash` leak is this exact shape. When you review a PR that passes a model object as a prop, ask yourself: which columns am I comfortable publishing to anyone with view-source? The rule that survives audits: treat the server→client prop surface as a public API. Map rows to explicit DTOs at the boundary — `{ id, name, avatarUrl }` — the same way you would never return `SELECT *` from a public endpoint.

<Quiz
  id="02-serialization-boundary-quiz-1"
  lessonSlug="02-serialization-boundary"
  lang="en"
  question="A server component passes four props to a client component: a Date, a Map of settings, a formatRow function, and a full ORM user row (plain object with passwordHash). What happens to each?"
  choices={[
    { label: "Date and Map serialize fine; the function throws a render-time serialization error; the user row serializes silently and publishes every column — including passwordHash — in the Flight payload", correct: true },
    { label: "All four fail: only JSON-safe values (string, number, boolean, plain object) can cross the boundary", misconception: "The Flight serializer is deliberately richer than JSON — Date, Map, Set, TypedArrays, FormData, Promises and server-rendered JSX all cross intact. The strictness is aimed at code and identity: functions and class instances are the rejects. Assuming JSON rules makes you both over-restrict (banning Map) and under-protect (forgetting that plain-object rows sail through)." },
    { label: "All four succeed: React serializes whatever it is given, functions included, by sending their source code", misconception: "Functions never cross — their closures, scope and environment cannot be reconstructed in the browser, and shipping arbitrary server function source would be a security hole. React throws a render-time error naming the offending prop. The only function-shaped thing that crosses is a use server reference, which serializes as an opaque endpoint id, not as code." },
    { label: "The ORM row is the one that throws, because database entities are class instances", misconception: "Whether it throws depends on the shape, not the origin: an entity that is a real class instance does throw, but most ORMs return plain objects, which serialize without a sound. That silence is the trap — the error-free path is the data leak. The defense is a DTO mapping at the boundary, not reliance on the serializer to reject secrets." },
  ]}
/>

## Interleaving without importing: the children pattern

The graph cut seems to imply that anything below a client component must be client too — it does not, and the escape hatch is the oldest prop in React. A client component may not *import* a server component, but it can *receive server-rendered output* as `children` (or any JSX-typed prop), because server-rendered JSX is on the serializable list:

```tsx
// Tabs.tsx — 'use client': owns activeTab state, ~2 kB
export function Tabs({ children }: { children: React.ReactNode }) {
  const [active, setActive] = useState(0);
  return <div>{/* tab headers */}{children[active]}</div>;
}

// page.tsx — server component
<Tabs>
  <ReportPanel data={await db.reports.q3()} />   {/* stays server */}
  <AuditPanel rows={await db.audit.recent()} />  {/* stays server */}
</Tabs>
```

The panels render on the server — direct database access, zero bundle weight — and arrive inside the payload as already-resolved trees; `Tabs` just decides which one to show. The composition rule in one line: the *importer* determines the environment, the *renderer* does not. A client shell holding server children is the workhorse layout of every serious RSC codebase, and reaching for it is what separates a surgical `'use client'` leaf from the page-level directive that drags a whole route into the bundle.

## The reverse channel: server functions

Data flows server→client through props; the *only* sanctioned way back is a server function. Mark a function (or a whole module) with `'use server'`, and React replaces it, in the client bundle, with a serializable reference; calling it from the browser issues a POST under the hood, with arguments serialized by the same Flight rules and the return value streamed back. Two production consequences follow directly. First, **every server function is a public, unauthenticated-by-default endpoint** — the framework wires the route for you, and an attacker does not need your UI to call it; validate and authorize inside the function, every time, as you would any handler. Second, the argument and return types obey the same serialization rules as props — no class instances in, no class instances out — so the DTO discipline applies in both directions.

<Quiz
  id="02-serialization-boundary-quiz-2"
  lessonSlug="02-serialization-boundary"
  lang="en"
  question="A client file imports formatPrice from lib/format.ts; format.ts also imports config.ts which reads process.env.STRIPE_SECRET_KEY into an exported constant. The build succeeds. What just happened, and what would have prevented it?"
  choices={[
    { label: "The transitive import dragged config.ts — secret included — into a public client chunk; an import of the server-only package inside config.ts would have turned this refactor into a build error", correct: true },
    { label: "Nothing happened: bundlers strip process.env reads from client chunks automatically", misconception: "Bundlers do the opposite — they inline the value of process.env reads at build time, which is precisely how the literal secret string ends up in the chunk. Tree-shaking removes unused exports, but the constant is used by the imported helper, so it stays. There is no automatic secrecy analysis; the cut is purely structural." },
    { label: "The build should have failed: importing any server file from a use client module is an error", misconception: "There is no such thing as a server file by default — an unmarked module is environment-neutral and joins whichever graph imports it. That neutrality is the feature (shared utils work everywhere) and the trap (secrets travel). Only an explicit server-only import gives the module a side and makes the build enforce it." },
    { label: "The secret is safe because env vars are resolved at runtime on the server only", misconception: "That holds for code that runs on the server — but this module was pulled into the client graph, where the bundler resolves process.env at build time and bakes the value in. Whether an env read is safe depends on which side of the use client cut the module lands, which is exactly why the cut must be guarded, not assumed." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The module-graph cut: what use client pulls into the bundle, and what may cross as props"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "page", label: "page.tsx", sub: "server component" },
    { id: "db", label: "db.ts + config.ts", sub: "secrets, server-only guard" },
    { id: "props", label: "props across the cut", sub: "serializable only, DTO" },
    { id: "chart", label: "Chart.tsx", sub: "'use client' directive" },
    { id: "deps", label: "chart-lib + every import", sub: "joins the client bundle" },
    { id: "action", label: "'use server' function", sub: "POST back, validate inside" },
  ]}
  edges={[
    { from: "page", to: "db", label: "imports, stays server" },
    { from: "page", to: "props", label: "renders Chart" },
    { from: "props", to: "chart", label: "Flight-serialized" },
    { from: "chart", to: "deps", label: "transitive imports" },
    { from: "chart", to: "action", label: "call → POST" },
    { from: "action", to: "db", label: "runs on server" },
  ]}
  caption="The directive cuts the module graph: Chart.tsx and everything it imports become client bundle, while page.tsx and db.ts never ship. Props crossing the cut are serialized into the payload — public by definition, so map rows to DTOs. The only road back is a server function: a POST endpoint that must validate and authorize like any handler."
/>

<KeyTakeaway>'use client' cuts the module graph: the file plus all transitive imports join the bundle, so guard secret modules with server-only. Props crossing the cut serialize by exact rules — plain data, Date, Map, Set, promises, server JSX yes; functions and class instances no. Plain-object rows leak silently: map to DTOs. Children interleave server output into client shells; 'use server' is the audited way back.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-serialization-boundary-retrieval"
  lang="en"
  questions={[
    { q: "State the serialization rules for props crossing server to client — what passes, what throws, and which case neither throws nor is safe?", a: "Passes: primitives (string, number, bigint, boolean, undefined, null, Symbol.for symbols), plain objects and arrays, Date, Map, Set, TypedArrays and ArrayBuffer, FormData, JSX rendered by server components, Promises (unwrapped client-side with use()), and references to use server functions. Throws at render time: functions that are not server functions, class instances (any prototype other than Object.prototype — ORM entities, domain models), unregistered symbols. The dangerous third case: a plain-object database row passes silently, publishing every column — passwordHash included — into a payload readable via view-source. Neither the serializer nor TypeScript flags it, because structurally it is fine. The discipline that survives audits is treating the prop surface as a public API: explicit DTO mapping at the boundary, selecting exactly the fields the UI needs, exactly as you would never expose SELECT * from a public endpoint." },
    { q: "Why is use client a module-graph statement rather than a component flag, and what two patterns keep the bundle and the secrets under control?", a: "The directive sits at the top of a file and assigns that module plus every transitive import to the client bundle; components have no environment marker of their own — a component is server or client purely by where its module sits relative to the nearest cut, which is why a server component imported by a client file silently becomes client. Pattern one, for bundle weight: keep use client files as small leaves and interleave via children — a client shell (Tabs holding state) receives server-rendered children as props, since server JSX is serializable; the heavy panels keep their direct data access and zero bundle cost, because the importer determines environment, not the renderer. Pattern two, for secrets: import server-only in every module touching process.env or the database, converting an accidental drag across the cut from a silent leak into a build error. The reverse channel is use server functions — serializable args, a POST under the hood, and a public endpoint by definition, so authorization lives inside the function." },
  ]}
/>

<Recap lang="en">
The boundary between server and client is a cut through the module graph, declared by use client at the top of a file. Everything that file imports, transitively, joins the client bundle — which makes every import added to a client file a bundle-size and secrecy decision, and makes the well-run codebase keep its client files as small interactive leaves. Unmarked modules are environment-neutral and join whichever graph imports them; that neutrality is what lets a shared helper drag a config module holding a Stripe secret into a public chunk, and the one-line guard is the server-only package, which turns that drag into a build error. Props crossing the cut are serialized into the Flight payload under exact rules: primitives, plain objects and arrays, Date, Map, Set, TypedArrays, FormData, server-rendered JSX, Promises and server-function references pass; functions and class instances throw at render time. The case that neither throws nor is safe is the plain-object ORM row — it serializes silently and publishes every column to view-source, which is why the prop surface is treated as a public API with explicit DTO mapping. Interleaving does not require importing: a client shell receives server-rendered children as props, keeping heavy panels on the server while a two-kilobyte Tabs owns the state. And the only road back across the cut is a use server function — a real POST endpoint with Flight-serialized arguments, public by construction, so validation and authorization live inside it, every time. Now when you see a fat prop crossing into a client component, you will reach for the DTO checklist before the PR ships — not after a security review does it for you.
</Recap>
