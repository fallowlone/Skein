infographic chart-column-simple
data
  title Render + commit passes for 3 setState in one tick
  desc React 18 batches the three updates into one pass; React 17 and flushSync pay one each (~40 ms per pass on a heavy page)
  values
    - label React 17 (promise/timer)
      value 3
    - label React 18 (batched)
      value 1
    - label React 18 + 3x flushSync
      value 3
theme
  palette #f43f5e #10b981 #f59e0b
