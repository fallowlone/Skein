---
concepts:
- react-reconciliation
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-reconciliation-and-keys"
sources:
- https://react.dev/learn/preserving-and-resetting-state
- https://react.dev/learn/rendering-lists
- https://react.dev/learn/render-and-commit
spiral: []
status: ready
summary: 'React diffs by element type and key, not by your component instances: same type at the same position keeps the fiber and its state, a type change unmounts the subtree. Index-as-key pins state to positions, so a reorder silently hands one row''s state to another.'
title: 'Reconciliation and keys: where your state actually lives'
track: "react"
unit: "01-rendering-model"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import diffComplexity from "./infographics/diff-complexity.svg?raw";

<Hook>
The bug report read like a ghost story: "I was typing a reply to my manager and the draft moved to my conversation with a candidate. I almost sent a salary discussion to the candidate." The messenger's sidebar listed conversations sorted by last activity, each row owning an uncontrolled draft input, rendered with `key={index}`. While the support engineer was typing in row 0, a new message arrived in another thread, the list re-sorted, and a different conversation became row 0. React did exactly what it was told: the element at position 0 had the same type and the same key as before, so it kept the existing fiber — and the existing fiber owned the input's state. The draft text never "moved" anywhere. It stayed bolted to position 0 while the data underneath it slid away. No exception, no warning, no failed test — the diff was *correct* by React's rules; the rules were just told that position equals identity. Three users hit it before anyone could reproduce it, because the repro required a background event landing mid-keystroke.
</Hook>

<Crux>React matches elements to fibers by type + key; state lives on the fiber. Index keys make position the identity — reorder, state stays.</Crux>

<Explanation>

## The diff is a heuristic, not a tree search

A true minimal diff between two trees costs O(n³) — for a thousand elements, on the order of a billion comparisons, dead on arrival for a UI that re-renders on keystroke. React refuses to play that game and instead bets on two heuristics that are almost always true in real UIs. First: **two elements of different types produce different trees** — if a position held a `div` and now holds a `section`, or held a `UserProfile` and now holds a `LoginForm`, React does not look inside. It unmounts the entire old subtree (destroying all state in it, running effect cleanups, removing DOM) and mounts the new one from scratch. Second: **the developer can mark stable identity across renders with a key**. With these two assumptions, reconciliation runs in O(n).

<Infographic
  svg={diffComplexity}
  label="Column chart comparing the number of comparisons to diff 1,000 elements: an optimal tree diff is O(n³) at roughly one billion comparisons, while React's heuristic diff is O(n) at about one thousand — a millionfold reduction."
  caption="The optimal minimal-diff is O(n³) — a billion comparisons for a thousand elements, dead on arrival on every keystroke. React's two heuristics (type-change means new subtree; keys mark identity) collapse it to O(n) — about a thousand. That gap is why reconciliation is a heuristic, not a tree search."
  hue="var(--d-frontend)"
/>

The comparison itself works position by position. Same type at the same position: React keeps the existing fiber, updates its props, and recurses into the children — the component instance survives, its hooks survive. Different type: full teardown of that subtree. This is also why the comparison is between *element descriptions*, never between component "instances" — elements are recreated as fresh objects on every render and carry no state at all. They are throwaway descriptions; the fiber is the persistent thing they get matched against.

## State lives on the fiber, and the key is the matching rule

When you wonder why your input keeps its typed text across re-renders, the answer is: everything `useState` gives you is stored on the fiber, not in your JSX and not in the closure of your function — the function ran and returned; its locals are gone. So the question "where is my input's text?" has a precise answer: on the fiber that React matched to your element during the last reconciliation. For a single child, matching is by position and type. For an array of siblings, position is meaningless — lists get reordered, filtered, prepended — so React matches siblings **by key**: the old fiber with key "a" pairs with the new element with key "a", wherever it moved. A matched fiber keeps its state and its DOM node, and the move costs one DOM reposition instead of a teardown.

Now the failure mechanism from the Hook, precisely. With `key={index}`, the key *is* the position — you have re-stated the default and disabled the entire matching machinery. Reorder the data and React sees: position 0 still has key 0, same type → same fiber → same state. The fiber holding the manager-draft now receives the candidate-conversation's props. Everything that lives on the fiber or its DOM node sticks to the position instead of following the data: uncontrolled input text, checkbox state, focus, scroll position, `useState` inside the row, even memoized values. Deleting the first item is the same bug in another costume: every fiber shifts to a new data item, and the *last* fiber gets unmounted — so "I deleted row one, but row two's notes vanished and row one kept the deleted ticket's notes."

```jsx
const [convos, setConvos] = useState(initialConvos);
// convos re-sorts when any thread gets a new message

// Bug: key restates the position. After a re-sort, the fiber (and the
// uncontrolled input state on it) at position 0 is matched to whatever
// conversation now sits at position 0.
{convos.map((c, index) => (
  <ConversationRow key={index} convo={c} />
))}

// Fix: key = stable identity from the data. The fiber follows the
// conversation wherever it moves; React repositions the DOM node.
{convos.map((c) => (
  <ConversationRow key={c.id} convo={c} />
))}
```

Two corollaries seniors get asked in review. `key={Math.random()}` is the *opposite* bug and strictly worse: no key ever matches, so every render unmounts and remounts every row — state wiped each render, effects re-run, full DOM rebuild; a 200-row table goes from a handful of patched nodes to 200 teardown-and-create cycles per keystroke. And keys only need to be unique **among siblings**, not globally — `item.id` is fine even if another list elsewhere uses the same ids.

<Inset kind="why" lang="en">
Why did the React team not just diff by component instance identity and skip keys entirely? Because there is no instance identity to diff by. Your component function runs and returns plain element objects; `convos.map` produces a brand-new array of brand-new objects every render. Nothing in JavaScript connects "the second element of last render's array" to "the second element of this render's array" except what React can read off the objects: type, position, key. Keys are the developer lending React the one fact it cannot infer — which description refers to the same logical entity as before. That is also why the key warning is a warning and not an error: index matching is genuinely fine for a list that never reorders, never filters, and holds no state — a static footer-link list loses nothing.
</Inset>

## Element identity vs component identity: the resets you did not order

The flip side of "same type keeps state" is that *same component type at the same position keeps state even across completely different JSX branches*. Render `<Counter person="Taylor" />` in the true branch of a ternary and `<Counter person="Sarah" />` in the false branch: toggling does **not** reset the count. React sees position 1, type `Counter`, both times — same fiber, props updated, state preserved. If those are logically different counters, that is a bug you fix by giving the branches different keys — `key="taylor"` and `key="sarah"` — turning the key into a deliberate reset lever. The same lever resets a form when the entity changes: `<ProfileForm key={userId} />` remounts the whole form on user switch instead of leaking one user's edits into another's profile.

And the inverse failure: **defining a component inside another component**. The inner function is recreated on every parent render, so its identity differs every time; React compares by reference for function types, sees a "different type" at the same position, and unmounts the subtree — every parent render wipes the child's state, refocuses nothing, replays mount effects. It looks like "my input loses focus on every keystroke" and is among the most-filed non-bugs in React's tracker. Hoist the definition to module scope; if it needs the parent's data, pass props.

<TradeoffMatrix
  id="02-reconciliation-and-keys-tradeoff-1"
  lessonSlug="02-reconciliation-and-keys"
  lang="en"
  prompt="A chat sidebar renders a list of conversations sorted by last activity. Each row owns an uncontrolled draft input. A new message arrives mid-keystroke and re-sorts the list. Which key strategy is correct?"
  options={[
    { name: "key={convo.id} — stable data identity", summary: "State follows the conversation, not the slot.", correct: true, justification: "A stable id from the data tells React which fiber is which conversation. When the list re-sorts, the fiber with key='conv-42' matches the element with key='conv-42' wherever it moved — state travels with the data, and React performs a cheap DOM reposition instead of a teardown. The draft stays attached to the right conversation regardless of sort order." },
    { name: "key={index} — array position", summary: "Position masquerades as identity.", justification: "Index keys make position the identity. After a re-sort, the fiber at position 0 — which owns the draft input's state — is matched to whatever conversation now sits at position 0. The draft text 'moves' to the wrong conversation with no warning. This is the exact bug described in the Hook. Safe only for static, never-reordered, stateless lists." },
    { name: "key={Math.random()} — always fresh", summary: "Forces full remount every render.", justification: "A random key never matches anything from the previous render, so every render unmounts all rows and mounts them fresh — state wiped, effects re-run, full DOM rebuild. A 200-row sidebar rebuilt on every keystroke. Strictly worse than index keys in every measurable way; there is no correct use case for this in a list." },
    { name: "No key — let React use its default", summary: "Index matching by another name.", justification: "Omitting key on list siblings triggers the missing-key warning and falls back to positional matching — identical behavior to key={index}. React requires explicit keys on dynamic lists precisely because implicit positional matching is incorrect for anything that reorders, filters, or prepends." },
  ]}
/>

<Quiz
  id="02-reconciliation-and-keys-quiz-1"
  lessonSlug="02-reconciliation-and-keys"
  lang="en"
  question="A sorted list of rows with uncontrolled inputs uses key={index}. The list re-sorts while a user is typing in the top row. Why does the typed text end up under a different data item?"
  choices={[
    { label: "The input's state lives on the fiber, and with index keys the fiber at position 0 matches the new element at position 0 — same key, same type — so the old state is paired with whatever data now sits at that position", correct: true },
    { label: "React batched the keystroke and the re-sort into one render and applied them in the wrong order", misconception: "Batching collapses multiple state updates into one render but never changes matching rules. Even rendered one update at a time, index keys still tell reconciliation that position equals identity, so the re-sort still hands the position-0 fiber to a different data item." },
    { label: "Uncontrolled inputs are not managed by React, so they are never moved during a re-render", misconception: "The DOM node is owned by its fiber, and React happily repositions DOM nodes when fibers match by key — that is exactly what stable keys achieve. The text stuck to position 0 not because the input is uncontrolled, but because the index key told React the position-0 fiber was the same logical row." },
    { label: "The re-sort mutated the array in place, which corrupts React's diff", misconception: "In-place mutation can cause missed re-renders (the reference does not change), but here a render clearly happened — rows visibly re-sorted. The corruption is in identity, not change detection: key={index} re-states the position default, so matching ignores which conversation each row represents." },
  ]}
/>

<Quiz
  id="02-reconciliation-and-keys-quiz-2"
  lessonSlug="02-reconciliation-and-keys"
  lang="en"
  question="A developer defines ChildInput as a function inside the Parent component's body. Users report the input loses focus and clears on every keystroke. What is the mechanism?"
  choices={[
    { label: "Each Parent render creates a new function object, so the element's type is a different reference every time; React treats it as a type change and unmounts/remounts the subtree, destroying state and focus", correct: true },
    { label: "Inner components cannot use hooks, so the state silently fails to persist", misconception: "Hooks work in any function component regardless of where it is defined — the rules of hooks concern call order, not definition site. The state is created correctly each mount; the problem is that every parent render triggers a fresh mount because the component's identity (the function reference) changed." },
    { label: "The keystroke re-renders Parent, and any re-render of a parent always remounts its children", misconception: "Re-renders flow down, but reconciliation keeps children's fibers whenever type and key match — that is the entire point of the diff. Stable, module-scope component types survive millions of parent renders. Only a changed type (or key) forces remount, and a recreated inner function is a changed type by reference." },
    { label: "The input is missing a key, and React requires keys on all stateful children", misconception: "Keys are required only for dynamic arrays of siblings, where position cannot express identity. A single static child matches by position and type just fine. Adding a key here would not help — the type reference itself changes every render, and a type mismatch overrides any key match." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Reconciliation routes every new element through a type-and-key match that decides between a cheap in-place update and a full subtree remount"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "elements", label: "New elements", sub: "fresh objects each render" },
    { id: "match", label: "Match vs old fiber", sub: "type + key" },
    { id: "keep", label: "Keep fiber", sub: "state + DOM survive" },
    { id: "remount", label: "Unmount subtree", sub: "state destroyed" },
    { id: "siblings", label: "List siblings", sub: "matched by key" },
    { id: "dom", label: "Commit", sub: "minimal DOM patches" },
  ]}
  edges={[
    { from: "elements", to: "match", label: "per position" },
    { from: "match", to: "keep", label: "same type+key" },
    { from: "match", to: "remount", label: "type changed" },
    { from: "elements", to: "siblings", label: "arrays" },
    { from: "siblings", to: "keep", label: "key found" },
    { from: "keep", to: "dom", label: "update/move" },
  ]}
  caption="Elements are throwaway descriptions; fibers persist and hold state. A type+key match keeps the fiber (props updated, DOM possibly repositioned); a type change tears the subtree down. Index keys collapse identity into position — the match succeeds, but against the wrong logical row."
/>

<KeyTakeaway>State lives on fibers; elements are recreated every render and matched to fibers by type and key. Same type+key keeps the fiber and its state; a type change unmounts the whole subtree. Index-as-key makes position the identity, so reorders hand one row's state to another; random keys remount everything; a stable data id makes state follow the data. Keys are also the deliberate reset lever.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-reconciliation-and-keys-retrieval"
  lang="en"
  questions={[
    { q: "Explain the exact mechanism by which key=index corrupts state on reorder, including where the state physically lives.", a: "Hook state, uncontrolled input values, focus, and the DOM node itself all live on the fiber — the persistent node React keeps per component instance — not in the elements, which are recreated as fresh throwaway objects every render. During reconciliation React matches each new element to an old fiber; among list siblings the match is by key. With key=index, the key just restates the array position, so after a reorder the fiber that was at position 0 is matched to whatever element is now at position 0: same key, same type, match succeeds — against the wrong logical row. The fiber keeps its state (the half-typed draft, the checked checkbox, the useState values inside the row) and receives the new row's props, so the state appears to 'jump' to a different data item. Deleting the first item shifts every pairing by one and unmounts the last fiber instead of the deleted row's. The fix is a key with stable data identity (item.id): then the old fiber pairs with the same logical item wherever it moved, React repositions its DOM node, and state travels with the data. key=Math.random() is the opposite extreme — nothing ever matches, so every render unmounts and remounts all rows, wiping state and rebuilding DOM each time." },
    { q: "When does React unmount a subtree even though you never removed it from your JSX, and how do you use keys to force or prevent resets deliberately?", a: "React unmounts a subtree whenever the element's type at a position changes between renders: div to section, UserProfile to LoginForm, or — the stealth version — a component function recreated inside a parent's body, whose reference differs every render and therefore reads as a new type every time, remounting the child and destroying its state on every parent render (the classic 'input loses focus on each keystroke'). Conversely, the same component type at the same position keeps its fiber and state even across different JSX branches: a ternary rendering Counter in both arms preserves the count when toggling, because React sees the same type at the same position and just updates props. Both directions are controllable with keys. To force a reset, change the key: ProfileForm with key=userId remounts the entire form when the user switches, guaranteeing no stale edits leak across entities — far more robust than manually clearing every field in an effect. To prevent the accidental reset, give the component a stable identity: hoist inner component definitions to module scope so the type reference is constant, and keep keys stable across renders. The rule of thumb: type and key together define identity; identity decides whether state survives." },
  ]}
/>

<Recap lang="en">
Reconciliation is React's O(n) answer to an O(n³) problem, built on two heuristics: different element types produce different trees, and keys mark stable identity. Each render produces brand-new element objects — pure descriptions with no state — and React matches them against the persistent fiber tree, where hook state, uncontrolled input values, focus, and DOM nodes actually live. Same type and key at a position: the fiber survives, props update, children are reconciled in turn. A changed type: the entire subtree is unmounted — state destroyed, cleanups run, DOM removed — and rebuilt. For arrays of siblings the match is by key, which is why index-as-key is the canonical production bug: the key restates the position, so a re-sort matches the position-0 fiber (and the draft typed into it) to whatever data item now occupies position 0, and a delete shifts every pairing by one while unmounting the last row instead of the deleted one. Random keys fail the other way, remounting every row on every render. A stable data id makes state and DOM follow the logical item, costing a node reposition instead of a teardown. Identity cuts both ways: the same component type at the same position keeps state across JSX branches whether you wanted that or not, and a key change is the deliberate reset lever — key equals userId remounts a form on entity switch. The stealth type-change — defining a component inside another component, recreating the function reference each render — remounts its subtree on every parent render and presents as inputs losing focus per keystroke. Type plus key is identity; identity decides whether state survives. Now when you see state behaving as if it followed the wrong data, or an input losing focus on every keystroke, you have the vocabulary to read the reconciliation trace and know exactly which rule was broken.
</Recap>
