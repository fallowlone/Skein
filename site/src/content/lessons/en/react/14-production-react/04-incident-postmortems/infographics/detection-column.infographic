infographic compare-hierarchy-row-letter-card-rounded-rect-node
data
  title Three React incidents — read by the detection column
  compares
    - label Render loop (silent CPU burn)
      children
        - label Detection: RUM long-task 12×, no exception
        - label Root cause: unstable object dep + async setState
        - label Prevention: long-task alert per release ring
    - label Hydration mismatch storm
      children
        - label Detection: 40,000 errors/min via onRecoverableError
        - label Root cause: timezone-dependent toLocaleString
        - label Prevention: mismatch-rate metric gates the ramp
    - label Subscription leak
      children
        - label Detection: tickets track session age, no alert
        - label Root cause: subscribe without cleanup
        - label Prevention: long-session heap synthetic
theme
  palette #6366f1 #f59e0b #f43f5e
