infographic compare-hierarchy-row-letter-card-rounded-rect-node
data
  title Three creep mechanisms, one failure class
  desc Each adds cost below the perception threshold — with its own number, detector, and fix
  compares
    - label Bundle creep
      children
        - label +70 kB min: whole CJS lodash
        - label Detect: hard-fail CI byte budget
        - label Fix: per-method import path
    - label Hydration growth
      children
        - label +90 kB: use client cascade
        - label Detect: client-bundle budget
        - label Fix: isolate tiny client leaf
    - label Memo decay
      children
        - label 4 ms then 41 ms per commit
        - label Detect: Profiler + RUM INP
        - label Fix: stable ref / React Compiler
theme
  palette #6366f1 #14b8a6 #f59e0b
