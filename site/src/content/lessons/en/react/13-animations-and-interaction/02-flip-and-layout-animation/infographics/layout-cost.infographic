infographic compare-binary-horizontal-underline-text-vs
data
  title Where you pay for layout: naive tween vs FLIP
  desc 200-card reorder at 60 Hz, 16.7 ms frame budget
  compares
    - label Naive top tween, every frame
      children
        - label Style recalc
          value 1.5
          desc ms, per frame
        - label Layout
          value 8
          desc ms, per frame
        - label Paint
          value 4
          desc ms, per frame
        - label Result
          value 22
          desc fps, 13.5 ms floor leaves no room for JS
    - label FLIP transform, endpoints only
      children
        - label Layout passes
          value 2
          desc total per drop, Last forces ~8 ms once
        - label el.animate calls
          value 200
          desc ~1 ms, once
        - label Per-frame cost
          desc transform only, microseconds on compositor
        - label Result
          value 60
          desc fps, flat, no layout mid-flight
theme
  palette #f43f5e #14b8a6
