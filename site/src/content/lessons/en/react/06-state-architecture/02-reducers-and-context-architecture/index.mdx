---
concepts:
- react-reducer-architecture
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-reducers-and-context-architecture"
sources:
- https://react.dev/reference/react/useReducer
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'useReducer models multi-field updates as named transitions in one pure, unit-testable function instead of scattered setState calls. Split state and dispatch into two contexts — dispatch is stable, so write-only components never re-render. Often this replaces a state library.'
title: 'Reducers and context: turning scattered setState into testable transitions'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The checkout bug that reached the CFO was simple to state and miserable to reproduce: an order shipped with free shipping *and* the shipping fee included in the total. The checkout component held eleven `useState` fields — items, promo code, discount, shipping method, shipping cost, totals — and the invariant "total = items − discount + shipping" was maintained by hand in six different event handlers. The "apply promo" handler updated discount and total; the "change shipping" handler updated shipping and total; a teammate had added express-checkout a month earlier and updated five of the six places. QA never caught it because the broken path required applying a promo *while* the shipping estimate was still loading. The rewrite was almost mechanical: one `useReducer`, actions named `promo-applied` and `shipping-selected`, totals recomputed in exactly one place — the reducer — and a twelve-line unit test that replayed the failing action sequence with no DOM, no React, no mocking. The test failed against the old logic, passed against the new, and the bug class was gone.
</Hook>

<Crux>A reducer turns multi-field updates into named, pure, testable transitions; splitting state and dispatch contexts contains re-renders.</Crux>

<Explanation>

## Transitions, not fields: what useReducer actually changes

A form with ten `useState` calls has no architecture — it has ten independent values and a prayer that every event handler updates the right subset. The failure mode is **invariant drift**: a rule like "total reflects discount and shipping" lives in no single place, so each new handler is a chance to update four of the five fields it should. `useReducer` inverts the structure. State becomes one object; updates become **actions** — named events describing *what happened* (`promo-applied`), not *which fields to write* (`setDiscount` + `setTotal`). The reducer is the single place where events turn into the next state:

```jsx
function checkoutReducer(state, action) {
  switch (action.type) {
    case "promo-applied": {
      const discount = computeDiscount(state.items, action.code);
      return withTotals({ ...state, promoCode: action.code, discount });
    }
    case "shipping-selected": {
      return withTotals({ ...state, shipping: action.method });
    }
    default:
      throw new Error(`Unknown action: ${action.type}`);
  }
}

// withTotals is the ONE place the invariant lives:
function withTotals(state) {
  return { ...state, total: subtotal(state) - state.discount + shippingCost(state) };
}

const [state, dispatch] = useReducer(checkoutReducer, initialCheckout);
// Handlers shrink to intent: dispatch({ type: "promo-applied", code });
```

Mechanically, `dispatch` schedules a re-render and React calls your reducer with the current state and the action during the next render — which is why the reducer **must be pure**: no fetches, no `Date.now()`, no mutation of the incoming state (in StrictMode the reducer is double-invoked to catch exactly this). Components shrink to dispatching intent; the *decision logic* leaves the component entirely.

## Pure logic is testable logic

Because the reducer is a pure function `(state, action) => state`, the most bug-prone logic in the feature — multi-field invariants, edge-case sequencing — becomes unit-testable without rendering anything. The Hook's regression test is three lines of replay: start from a known state, dispatch `shipping-estimate-loaded` then `promo-applied`, assert the total. No JSDOM, no `act()`, no mocked network — reducer tests run in microseconds and pin down *sequences*, which is precisely where scattered-setState bugs hide. This is the underrated payoff: teams adopt reducers "for organization" and discover the real win is that state logic finally has the same test discipline as the rest of the codebase. **Tradeoff:** the ceremony is real — action types, a switch, an indirection between the click and the write. For two fields with no invariants, `useState` is strictly better; a reducer earns its keep when fields update *together* under rules.

## Splitting state and dispatch: re-render hygiene as architecture

To share reducer state across a feature subtree, you pass it through context — and here the re-render hygiene from unit 02 becomes an architectural decision. A context re-renders every consumer when its value changes identity. If you publish `{ state, dispatch }` as one object, every consumer re-renders on every state change — including toolbar buttons that only ever *dispatch*. The fix is structural: **two contexts**.

```jsx
const CheckoutStateContext = createContext(null);
const CheckoutDispatchContext = createContext(null);

function CheckoutProvider({ children }) {
  const [state, dispatch] = useReducer(checkoutReducer, initialCheckout);
  return (
    <CheckoutDispatchContext.Provider value={dispatch}>
      <CheckoutStateContext.Provider value={state}>
        {children}
      </CheckoutStateContext.Provider>
    </CheckoutDispatchContext.Provider>
  );
}
```

React guarantees `dispatch` is **referentially stable** across renders — the dispatch context's value never changes, so write-only components (buttons, menu items, the express-checkout banner) subscribe to it and *never re-render* from state changes. Read-only components subscribe to the state context and re-render when state changes — which is honest, because they display it.

<Inset kind="why" lang="en">
Why does this pattern often replace a state library? Because for a feature-scoped domain — checkout, an editor pane, a wizard — it already provides the three things people import Redux for: centralized transitions, testable pure logic, and shared access down a subtree. What it does *not* provide is what libraries actually add: selector-grade subscription precision (every state-context consumer re-renders on any state change — there is no way to subscribe to just `state.total`), devtools with action history, and middleware. The decision rule: feature-scoped, modest write rate, consumers mostly read overlapping state → reducer + context, zero dependencies. App-wide, write-heavy, or consumers reading disjoint slices → an external store, next lesson.
</Inset>

<Quiz
  id="02-reducers-and-context-architecture-quiz-1"
  lessonSlug="02-reducers-and-context-architecture"
  lang="en"
  question="A checkout form maintains the invariant 'total = items − discount + shipping' across six event handlers using multiple useState calls, and the invariant keeps breaking. What does moving to useReducer structurally change?"
  choices={[
    { label: "Updates become named actions describing what happened, and the invariant is recomputed in exactly one pure function — handlers can no longer update an inconsistent subset of fields", correct: true },
    { label: "Reducers batch the field updates so they commit atomically, which useState cannot do", misconception: "React batches state updates within an event handler for useState too — atomicity of commits was never the problem. The bug was that each handler re-implemented the invariant by hand; the reducer fixes it by centralizing the transition logic, not by changing batching." },
    { label: "useReducer makes the state updates faster because there is only one state object", misconception: "Performance is essentially identical — dispatch schedules the same re-render a setState would. The reducer's value is correctness architecture: one place where events become state, unit-testable without React. Choosing it for speed misses what it is for." },
    { label: "The reducer can await the shipping estimate before computing the total, eliminating the race", misconception: "Reducers must be pure and synchronous — no fetches, no awaits; React may even double-invoke them in StrictMode. Async work stays in handlers or effects, which then dispatch result actions like shipping-estimate-loaded; the reducer just sequences the resulting transitions correctly." },
  ]}
/>

<Quiz
  id="02-reducers-and-context-architecture-quiz-2"
  lessonSlug="02-reducers-and-context-architecture"
  lang="en"
  question="Why does splitting reducer state and dispatch into two separate contexts stop write-only components from re-rendering, when one combined context could not?"
  choices={[
    { label: "dispatch is referentially stable across renders, so the dispatch context's value never changes identity — its consumers are never notified, while a combined object is a new reference on every state change", correct: true },
    { label: "Two contexts let React diff the value more precisely and notify only the consumers that used the changed half", misconception: "Context has no diffing — each context compares its own value by identity and notifies all its consumers when it changes. The split works only because dispatch never changes identity; React is not inspecting which property a consumer read." },
    { label: "The dispatch context bypasses rendering entirely because functions cannot trigger re-renders", misconception: "Any context value, function or not, re-notifies consumers when its identity changes. If you passed a new inline wrapper around dispatch each render, dispatch-context consumers would re-render every time — stability of the reference is the mechanism, not the type." },
    { label: "It halves the work because each consumer now subscribes to half as much data", misconception: "Data volume is irrelevant to context cost — a consumer re-renders when its context value changes identity, whether that value is one function or a thousand-field object. The split changes which values change, not how much data flows." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="An event flows through dispatch into a pure reducer that owns all invariants; new state notifies only state-context consumers, while dispatch-context consumers stay untouched because dispatch never changes identity"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "event", label: "User event", sub: "click, input" },
    { id: "dispatch", label: "dispatch(action)", sub: "named intent" },
    { id: "reducer", label: "Pure reducer", sub: "invariants in one place" },
    { id: "state", label: "Next state", sub: "one object" },
    { id: "readers", label: "State context", sub: "readers re-render" },
    { id: "writers", label: "Dispatch context", sub: "writers: no re-render" },
  ]}
  edges={[
    { from: "event", to: "dispatch", label: "intent" },
    { from: "dispatch", to: "reducer", label: "action" },
    { from: "reducer", to: "state", label: "transition" },
    { from: "state", to: "readers", label: "new identity" },
    { from: "dispatch", to: "writers", label: "stable ref" },
    { from: "readers", to: "event", label: "next interaction" },
  ]}
  caption="The reducer is the single chokepoint where events become state, which makes invariants enforceable and the logic unit-testable without React. The two-context split is re-render hygiene applied architecturally: stable dispatch for writers, changing state for readers."
/>

<KeyTakeaway>useReducer replaces scattered setState calls with named transitions processed by one pure function — the single place invariants live, and the reason state logic becomes unit-testable without rendering. Share it through two contexts, not one: dispatch is referentially stable, so write-only components subscribed to the dispatch context never re-render. For a feature-scoped domain this is a state library minus selectors and devtools — reach for an external store only when you need subscription precision.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-reducers-and-context-architecture-retrieval"
  lang="en"
  questions={[
    { q: "A multi-field form keeps breaking an invariant that several event handlers each maintain by hand. Explain how a reducer eliminates that bug class and why the reducer must stay pure.", a: "With multiple useState fields, an invariant like 'total reflects discount and shipping' has no home — every handler that touches any participating field must re-implement it, so each new handler or code path is a chance to update an inconsistent subset. A reducer restructures this: state is one object, handlers dispatch named actions describing what happened, and a single pure function maps (state, action) to the next state. The invariant is recomputed in exactly one place per transition, so it cannot drift between handlers. Purity is required by the execution model: dispatch only schedules a re-render, and React runs the reducer with the current state during rendering — possibly twice in StrictMode, possibly for renders that get discarded. Any fetch, Date.now(), or mutation of the incoming state inside the reducer therefore misfires unpredictably. Async work lives in handlers or effects and re-enters the reducer as result actions. The purity payoff is testability: replay an action sequence against the reducer in a plain unit test — no DOM, no act(), microsecond-fast — which is exactly how sequence-dependent bugs like 'promo applied while shipping estimate loads' get pinned down." },
    { q: "When does reducer + context legitimately replace a state library, and what are you giving up — name the precise re-render limitation.", a: "Reducer + split contexts covers a feature-scoped domain — checkout, editor pane, wizard — with the three things people import a library for: centralized named transitions, pure unit-testable logic, and shared access down a subtree, at zero dependency cost. The two-context split gives partial re-render control: dispatch is referentially stable, so components that only write never re-render from state changes. The precise limitation is on the read side: context notifies every consumer when its value changes identity, and there is no selector mechanism — a component reading only state.total still re-renders when an unrelated field changes, because the state object is a new reference on every transition. You also forgo devtools action history and middleware. So the boundary is: feature-scoped, modest write rate, readers mostly consume overlapping state → reducer + context. App-wide state, write-heavy updates, or many consumers reading disjoint slices → an external store with selector subscriptions, where each component re-renders only when its selected slice changes." },
  ]}
/>

<Recap lang="en">
Multiple useState fields that update together under rules are an invariant waiting to drift: the rule lives in every handler and therefore in none, and the bug arrives with the next handler someone adds. useReducer restructures the feature around transitions — handlers dispatch named actions describing what happened, and one pure function turns the current state and the action into the next state. Invariants get a single home; components shrink to expressing intent. Mechanically, dispatch schedules a re-render and React runs the reducer during rendering, which is why it must be pure — no fetches, no clock reads, no mutation; StrictMode double-invokes it to catch violations. Purity buys the practical prize: state logic becomes unit-testable by replaying action sequences in microseconds, no DOM or mocking, which is how sequence-dependent bugs get regression tests. Sharing the reducer down a subtree applies re-render hygiene architecturally: publish state and dispatch as two separate contexts. Dispatch is referentially stable, so write-only components never re-render; state-context consumers re-render on every transition — honestly for displays, wastefully for components reading an unchanged slice, because context has no selectors. That missing precision, plus devtools and middleware, is what a state library still adds: reducer + context for feature-scoped domains, external stores when writes are heavy or readers are disjoint.
</Recap>
