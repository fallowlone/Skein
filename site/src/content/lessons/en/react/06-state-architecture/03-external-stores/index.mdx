---
concepts:
- react-external-stores
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-external-stores"
sources:
- https://react.dev/reference/react/useSyncExternalStore
- https://github.com/pmndrs/zustand
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'useSyncExternalStore is the contract for reading state React does not own: subscribe + cached getSnapshot, with tearing checks. Stores like zustand build selectors on it — no provider, re-render only when your slice changes — and beat context for write-heavy, cross-tree state.'
title: 'External stores: useSyncExternalStore, selector subscriptions, and tearing'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import contextVsStore from "./infographics/context-vs-store.svg?raw";

<Hook>
The trading dashboard had a bug that screenshots couldn't prove and QA couldn't script: occasionally the portfolio header showed a total that didn't equal the sum of the visible rows. By the time anyone clicked, it was correct again. The team's hand-rolled store hook — `useEffect` to subscribe, `useState` to mirror the value — had worked for three years. What changed was a React 18 upgrade and a `startTransition` around a heavy filter. Now renders were interruptible: the header read the store at the start of a long render pass, a price tick mutated the store mid-render, and the rows read the *new* value when rendering resumed. One render, two versions of the truth — **tearing**. The screenshot finally came from a Bloomberg-obsessed customer with a 144 Hz monitor. The fix was deleting forty lines of subscription code for one `useSyncExternalStore` call — React's contract for exactly this problem: it checks the snapshot after rendering and re-renders synchronously if the store moved underneath it. The bug was unreproducible because it required losing a race nobody knew was being run.
</Hook>

<Crux>useSyncExternalStore reads outside state without tearing; stores like zustand re-render only components whose selector output changed.</Crux>

By the end of this lesson you'll understand why upgrading to React 18 can silently break a store hook that worked for years — and exactly what the API contract requires to prevent it.

<Explanation>

## The contract: subscribe, getSnapshot, and the tearing check

State React owns (useState, useReducer) is versioned with the fiber tree, so concurrent rendering can never see two versions in one pass. State living *outside* React — a module singleton, a WebSocket cache, `navigator.onLine` — has no such guarantee: an interruptible render can span a mutation, and components rendered before and after it disagree in the same committed frame. That is **tearing**, and `useSyncExternalStore` is the contract that prevents it:

```jsx
const value = useSyncExternalStore(
  subscribe,    // (callback) => unsubscribe; call callback on every store change
  getSnapshot,  // () => current value; MUST return cached value if nothing changed
  getServerSnapshot // optional: value for SSR + hydration
);
```

`subscribe` registers React's own change-callback with your store and returns a cleanup. `getSnapshot` returns the current value — and **must return the same reference until the store actually changes**, because React calls it repeatedly and uses `Object.is` to decide if anything moved. Return a fresh object each call (`() => ({ ...state })`) and React sees an eternally-changing store: the infamous "The result of getSnapshot should be cached" error, followed by an infinite re-render loop. The anti-tearing mechanism: when a store update arrives during a non-blocking render, React re-checks snapshots and forces the conflicting update to render **synchronously**, sacrificing time-slicing for consistency. Your old `useEffect`-subscription hook had a second flaw besides tearing: updates firing between render and effect-subscription were silently lost — uSES closes that gap too.

## Zustand: selector subscriptions on top of the contract

`useSyncExternalStore` is deliberately low-level — one store, whole-value subscription. Zustand is what the pattern looks like productized. The store is created *outside* React as a module-level singleton — **no provider**, no context, importable from anywhere including non-React code:

```jsx
const useCartStore = create((set, get) => ({
  items: [],
  promo: null,
  addItem: (item) => set((s) => ({ items: [...s.items, item] })),
  total: () => get().items.reduce((sum, i) => sum + i.price, 0),
}));

// Component subscribes to a SLICE via selector:
function CartBadge() {
  const count = useCartStore((s) => s.items.length);
  return <span>{count}</span>; // re-renders ONLY when items.length changes
}
```

The mechanism is **selector subscription**: the hook runs your selector against the snapshot, and re-renders the component only when the selector's *output* changes (`Object.is` by default; a custom equality like shallow-compare is opt-in). A promo-code write re-renders zero components that only selected `items.length`. Compare context: every consumer re-renders on any change to the value's identity, full stop. The classic self-inflicted wound is a selector returning a fresh object — `useCartStore((s) => ({ a: s.a, b: s.b }))` — new reference every snapshot, re-render on every store write, the precision silently gone (fix: two selectors, or shallow equality).

## When external beats context — and when state should bypass React entirely

Before reaching for an external store, ask: how many consumers re-render per write, and how often do writes happen? The decision is not fashion; each side has a mechanism. **Write-heavy state**: context re-notifies every consumer per write; a store notifies only matching selectors — at 20 writes/second with 50 consumers, that is 1,000 component renders per second versus a handful. **Cross-tree access**: a store is a module import — event handlers, route loaders, analytics, and code outside the provider hierarchy can read and write it; context requires being under the provider. **Tooling**: middleware (persist, immer, devtools with action history) attaches at the store boundary. Context still wins for low-write, scope-shaped values — theme, locale, the current user — where the provider hierarchy *is* the feature and a dependency is overhead.

<Infographic
  svg={contextVsStore}
  label="A side-by-side comparison of context versus an external store under the same write-heavy load: context notifies every consumer per write (~1,000 renders/sec at 20 writes across 50 consumers), needs a provider in the tree, and wins for low-write scoped values; an external store re-renders only matching selectors (a handful of renders for the same load), is a module singleton importable cross-tree, and carries store-boundary tooling like persist and devtools"
  caption="The case against context for write-heavy state is mechanical, not fashion: who gets notified per write decides the cost — every consumer versus only the matching selectors."
  hue="var(--d-infra)"
/>

<Inset kind="how" lang="en">
Transient updates are the senior move for 60 Hz data: subscribe imperatively and skip rendering altogether. `useCartStore.subscribe(...)` (or zustand's subscribeWithSelector middleware) inside an effect, writing to a ref or directly mutating a DOM node — a cursor position, a live chart, a drag ghost — updates at frame rate with zero reconciliation. React renders the component once; the store drives the pixels. The rule of thumb: if the value changes faster than the user can read it, it probably should not be React state at all.
</Inset>

<Quiz
  id="03-external-stores-quiz-1"
  lessonSlug="03-external-stores"
  lang="en"
  question="A getSnapshot implemented as () => ({ ...store.state }) triggers the error 'The result of getSnapshot should be cached' and an infinite loop. What is the mechanism?"
  choices={[
    { label: "React calls getSnapshot repeatedly and compares results with Object.is; a fresh object every call looks like a store that changed every time, so React schedules another render, which calls getSnapshot again — forever", correct: true },
    { label: "Spreading the state is too slow, so React times out and restarts the render", misconception: "There is no timing involved — a spread of a small object is nanoseconds. The loop is purely about reference identity: React cannot know the new object is structurally equal; Object.is says it changed, and a changed store mandates a re-render, which produces another new object." },
    { label: "getSnapshot is only allowed to return primitives, and objects always fail", misconception: "Objects are fine — every real store returns objects. The requirement is that the same store state yields the same reference across calls, i.e. the store caches its snapshot and only swaps it on actual writes. Immutable-update stores satisfy this naturally." },
    { label: "The error means subscribe is broken and never unsubscribes, leaking listeners until React loops", misconception: "Listener leaks cause memory growth and stale callbacks, not this error. The cached-snapshot rule is specifically about getSnapshot's return identity — React detects it by calling getSnapshot twice and seeing different references with no store change in between." },
  ]}
/>

<Quiz
  id="03-external-stores-quiz-2"
  lessonSlug="03-external-stores"
  lang="en"
  question="A dashboard receives ~20 store writes per second and has ~50 subscribed components, each caring about a different slice. Why does a zustand-style store fundamentally outperform context here?"
  choices={[
    { label: "The store runs each component's selector and re-renders only components whose selector output changed, while context re-notifies all consumers on every identity change of its value — roughly 1,000 renders/sec versus a handful", correct: true },
    { label: "Zustand batches the 20 writes into one render per second, while context cannot batch", misconception: "Neither tool changes React's batching; writes in the same task batch either way, and 20 distinct ticks still mean up to 20 notification rounds. The difference is who gets notified per round: matching selectors only, versus every consumer of the context." },
    { label: "Stores live outside React, so their updates do not cause re-renders at all", misconception: "Subscribed components must re-render to show new data — that is the point of subscribing. Living outside React removes the provider and enables non-React access; the performance win comes from selector precision, not from skipping rendering. (Transient subscriptions skip rendering, but those are opt-in per subscriber.)" },
    { label: "Context is slower because it deep-compares the value on every render to decide who updates", misconception: "Context never deep-compares — it is a cheap Object.is on the value's identity, and that cheapness is the problem: any change notifies everyone, because React has no idea which part of the value each consumer reads. Selectors are exactly the missing declaration of intent." },
  ]}
/>

</Explanation>

<StackDiagram
  label="The layers between a store living outside React and the components on screen: the module-scope store, the useSyncExternalStore subscription contract, per-component selectors, and only the components whose slice changed"
  hue="var(--d-frontend)"
  layers={[
    { label: "External store", note: "module scope · set()" },
    { label: "useSyncExternalStore", note: "subscribe + getSnapshot" },
    { label: "Selectors", note: "Object.is on output" },
    { label: "Components", note: "only changed slices render" },
  ]}
  caption="A write notifies the subscription layer; getSnapshot must return a cached reference until the store truly changes — React compares with Object.is. Selectors gate which components re-render, a transient (imperative) subscription skips the bottom two layers and writes to refs or the DOM with no render, and when a write lands mid-render React detects the snapshot mismatch and re-renders synchronously rather than committing a torn frame."
/>

<KeyTakeaway>useSyncExternalStore is React's contract for state it does not own: subscribe wires the store to React, getSnapshot must return a cached reference until a real change, and React re-checks snapshots to prevent tearing — sacrificing time-slicing for consistency when a write lands mid-render. Zustand productizes this with module-singleton stores and selector subscriptions: no provider, re-render only when your selected slice changes. Choose external over context for write-heavy, cross-tree state with devtools needs; go transient — subscribe without rendering — for frame-rate data.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-external-stores-retrieval"
  lang="en"
  questions={[
    { q: "Explain tearing: why can a hand-rolled useEffect-plus-useState store subscription show two versions of the store in one frame under concurrent React, and how does useSyncExternalStore prevent it?", a: "React-owned state is versioned with the fiber tree: a render pass sees one consistent value everywhere. An external store has no versioning, and concurrent rendering is interruptible — a transition render can pause between components. With a hand-rolled subscription, component A reads the store at the start of the pass, a write mutates the store while the render is paused, and component B reads the new value when rendering resumes. Both results commit in the same frame: a header that disagrees with its rows — tearing. The effect-based mirror has a second gap: writes landing between render and the effect's subscription are lost entirely. useSyncExternalStore fixes both: it subscribes React's own callback to the store, and after rendering it re-reads getSnapshot and compares — if the store changed during a non-blocking render, React abandons consistency-violating output and re-renders the update synchronously, trading time-slicing for a single version of the truth. The contract's price is that getSnapshot must return a cached reference until a genuine change, because React detects 'changed' with Object.is — a fresh object per call reads as an always-changing store and loops infinitely." },
    { q: "Walk the decision: context versus a zustand-style external store versus a transient subscription — what mechanism makes each the right tool, and for which state?", a: "Context notifies every consumer whenever the provided value changes identity; there are no selectors, so precision is all-or-nothing. That is fine for low-write, scope-shaped values — theme, locale, current user — where changes are rare and the provider hierarchy expresses scoping; a dependency would buy nothing. A zustand-style store is a module singleton built on useSyncExternalStore with selector subscriptions: each component declares what it reads, and the store re-renders only components whose selector output changed (Object.is, optional shallow equality). The mechanism pays off when writes are frequent (20 writes/sec across 50 context consumers is ~1,000 renders/sec; with selectors, only affected slices render), when access is cross-tree (module import works in event handlers and non-React code; no provider needed), and when you want store-boundary tooling — persist, devtools action history, middleware. The known foot-gun: object-returning selectors recreate a reference per snapshot and silently nullify the precision. Transient subscription is the third tier for frame-rate data: subscribe imperatively in an effect and write to a ref or DOM node, no re-render at all — cursors, live charts, drag previews. If users cannot read the value as fast as it changes, it should not be React state." },
  ]}
/>

<Recap lang="en">
External state — anything living outside useState/useReducer — breaks an invariant React otherwise guarantees: one render pass, one version of every value. Concurrent rendering makes the break visible as tearing: an interruptible render spans a store write, and components committed in the same frame disagree. useSyncExternalStore is the contract that restores consistency: subscribe gives React a change-callback into your store; getSnapshot returns the current value and must keep returning the same reference until a genuine change, because React compares snapshots with Object.is — a fresh object per call means the cached-snapshot error and an infinite loop. When a write lands mid-render, React detects the mismatch and re-renders synchronously, deliberately sacrificing time-slicing for correctness; it also closes the lost-update gap of effect-based subscriptions. Zustand is this contract productized: stores are module singletons created outside React — no provider, importable from event handlers and non-React code — and components subscribe through selectors, re-rendering only when the selected output changes. That selector precision is the quantitative case against context for write-heavy state: notify matching slices, not every consumer. Context keeps the low-write, scope-shaped jobs. And for data changing at frame rate, skip rendering entirely: transient subscriptions write to refs or DOM nodes from an imperative subscribe, letting the store drive pixels while React renders once. Now when you see a dashboard where unrelated components flicker on every tick, you'll reach for selectors first — and know that the classic foot-gun is a selector that returns a new object every call.
</Recap>
