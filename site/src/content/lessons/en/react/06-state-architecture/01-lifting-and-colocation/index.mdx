---
concepts:
- react-state-colocation
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-lifting-and-colocation"
sources:
- https://react.dev/learn/sharing-state-between-components
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'State belongs to the lowest component that needs it. Lift it only to the nearest common owner of its readers; every extra level widens the re-render blast radius. Global-by-default turns each keystroke into an app-wide render; composition via children isolates state instead.'
title: 'Lifting and colocation: state lives at the lowest common owner'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import blastRadius from "./infographics/blast-radius.svg?raw";

<Hook>
The settings page was the slowest screen in the product, and nobody could explain why: it was a form with nine fields. The profiler told the story in one flame graph — a single keystroke in the "display name" input re-rendered 1,400 components, including the navigation shell, a notifications badge, and a pricing table on a screen the user wasn't even looking at. The cause was a team convention written down two years earlier: "put state in the app store so anything can reach it later." Every input's draft value lived at the root. Each keystroke updated root-level state, and React did exactly what it promises: re-rendered the owner and walked its entire subtree. The fix took an afternoon and deleted code — draft values moved into the form, the form moved its shared bits to the lowest component that actually had both readers, and the keystroke render dropped from 1,400 components to 12. Nothing was memoized. The state just moved to where it belonged.
</Hook>

<Crux>State lives at the lowest common owner. Lift only to the nearest shared parent — every level higher widens the re-render blast radius.</Crux>

By the end of this lesson you'll know exactly where to move state when a keystroke triggers a 1,400-component render — and why the fix deletes code instead of adding memoization.

<Explanation>

## One owner, props down, events up

React state is local by design: a `useState` belongs to exactly one component instance, and that instance is the **single owner** — the only place the value can change. When two components need the same changing value, you do not duplicate it (two copies *will* drift; the bug report reads "the filter chip says active but the list is unfiltered"). You **lift it up**: find the lowest component that is an ancestor of every reader — the *lowest common owner* — move the state there, pass the value down as props, and pass change-handlers down so children can request updates. Data flows down, events flow up, and there is one source of truth per piece of state.

```jsx
function SearchSection() {
  // Lowest common owner of <SearchInput> and <ResultsList>:
  // both need `query`, so it lives here — and no higher.
  const [query, setQuery] = useState("");
  return (
    <section>
      <SearchInput value={query} onChange={setQuery} />
      <ResultsList query={query} />
    </section>
  );
}

function SearchInput({ value, onChange }) {
  // Controlled: this component owns nothing; it reports intent upward.
  return <input value={value} onChange={(e) => onChange(e.target.value)} />;
}
```

The key word is *lowest*. Lifting is not "move it to the top" — it is "move it exactly as high as the nearest shared ancestor and stop." Each level you lift past that point adds subscribers it never needed.

## Blast radius: why height is a performance decision

When you see components re-rendering with no apparent reason, ask: how high does their owning state sit? When state changes, React re-renders the owner component and then, by default, **its entire subtree** — every descendant gets called again, whether or not it reads the changed value. (Re-render is the cheap render phase, not DOM mutation — but calling 1,400 functions and reconciling their output 60 times while someone types is not free.) The height of a piece of state therefore determines its **blast radius**: state in a leaf re-renders a leaf; state at the root re-renders the app. This is the mechanism behind the "global by default" anti-pattern — putting everything in a root store or top-level context doesn't just make code harder to trace, it makes *every* write an app-wide render and then forces you to fight back with `React.memo` on hundreds of components. **Colocation** is the inverse discipline: when you notice only one subtree reads a value, push the state back *down* into it. Moving state down is a pure win — fewer renders, less prop-threading, and the component becomes deletable as a unit.

<Infographic
  svg={blastRadius}
  label="Column chart of components re-rendered by a single keystroke in the same nine-field settings form: with draft state in a root-level store, 1,400 components re-render; with the state colocated at the lowest common owner, only 12."
  caption="Same form, same keystroke — only the owner's height changed. Moving state down from the root store to the lowest common owner cut the blast radius from 1,400 components to 12, with no memoization."
  hue="var(--d-frontend)"
/>

<Inset kind="why" lang="en">
Why does React re-render the whole subtree instead of only components that read the changed state? Because with props, "reads" is not a subscription React can see — any descendant might receive a value derived from that state through arbitrary JavaScript. The only safe assumption after an owner re-renders is that any child's props may have changed, so React re-runs them and lets reconciliation discard identical output. Subscription-precise updates require an explicit contract — selectors in external stores, which is lesson 3 of this unit. With plain state, your lever is structural: keep the owner low.
</Inset>

## Composition: isolating state with the children prop

There is a structural escape hatch that beats memoization for the common case: **pass expensive subtrees as `children`**. JSX children are elements — objects created where the JSX is *written*, not where it is rendered. If a stateful wrapper receives its content via `children`, the wrapper's re-render reuses the exact same element objects it was given (its parent, which created them, did not re-render). React sees the identical element reference and **bails out** of re-rendering that subtree entirely.

```jsx
function ScrollTracker({ children }) {
  const [y, setY] = useState(0); // updates on every scroll tick
  // children was created by the parent and hasn't changed —
  // same element reference, so React skips re-rendering it.
  return <div onScroll={(e) => setY(e.target.scrollTop)}>
    <ProgressBar y={y} />
    {children}
  </div>;
}

// <ScrollTracker><HeavyTable rows={rows} /></ScrollTracker>
// HeavyTable does NOT re-render on scroll.
```

This is state isolation by architecture: high-frequency state (scroll, hover, input drafts) gets a narrow component to live in, and the heavy content passes *through* it untouched. **Tradeoff:** lifting and colocating is a refactor every time requirements shift — a value used by one component today gets a second reader tomorrow, and you must move it. That friction is real, and it is the honest price of blast-radius control; teams that refuse to pay it drift back to global-by-default and pay in renders instead. **Failure mode:** duplicating state instead of lifting ("I'll just keep a local copy too") — two owners, eventual divergence, and the class of bug where the UI disagrees with itself.

<Quiz
  id="01-lifting-and-colocation-quiz-1"
  lessonSlug="01-lifting-and-colocation"
  lang="en"
  question="Two sibling components need to read and update the same filter value. What is the correct move, and why exactly there?"
  choices={[
    { label: "Lift the filter into the nearest common ancestor of both siblings — the lowest common owner — and pass value plus change-handler down, keeping one source of truth with the smallest possible re-render subtree", correct: true },
    { label: "Give each sibling its own useState and synchronize them with an effect", misconception: "Two owners means two sources of truth that drift between renders — the sync effect fires after commit, so there is always a frame where they disagree, and the pattern grows into effect loops. Lifting removes the duplication instead of patching it." },
    { label: "Put the filter in a root-level context so any future component can also reach it", misconception: "That trades a two-component need for an app-wide blast radius: every filter change re-renders everything below the provider. Lift exactly as high as the nearest shared ancestor and stop — promote it further only when a distant reader actually appears." },
    { label: "Keep it in the first sibling and read it from the second via a ref", misconception: "Refs are invisible to rendering — the second sibling would not re-render when the value changes, so its UI goes stale. Shared changing values must flow through state and props, which is exactly what lifting arranges." },
  ]}
/>

<Quiz
  id="01-lifting-and-colocation-quiz-2"
  lessonSlug="01-lifting-and-colocation"
  lang="en"
  question="A wrapper holding fast-changing scroll state receives a heavy table via the children prop. Why does the table skip re-rendering when the wrapper's state updates?"
  choices={[
    { label: "The children elements were created by the parent, which didn't re-render — the wrapper passes the same element references through, and React bails out on identical elements", correct: true },
    { label: "React automatically wraps children in React.memo", misconception: "No memoization is involved and no props are compared. The bailout is reference identity of the element objects themselves: the wrapper re-rendered, but it returns the very same children objects it received, so React has nothing new to reconcile." },
    { label: "Children render in the parent's render cycle, so the wrapper can never affect them", misconception: "Children render wherever they are placed in the tree — under the wrapper. If the wrapper created those elements itself in its own JSX, they would be new objects every render and would re-render normally; the isolation comes from who created the elements." },
    { label: "Scroll state is special — React batches it so descendants don't re-render", misconception: "There is nothing special about scroll state; any useState in the wrapper behaves the same. Batching coalesces multiple set-calls into one render, it does not exempt descendants from that render — element identity does." },
  ]}
/>

<TradeoffMatrix
  id="01-lifting-and-colocation-tradeoff"
  lessonSlug="01-lifting-and-colocation"
  lang="en"
  prompt="A filter value is read and updated by two sibling components on the same page. Where should the single useState live?"
  options={[
    {
      name: "Lift to nearest common ancestor",
      summary: "One source of truth at the lowest shared parent, props down, handlers up.",
      correct: true,
      justification: "The lowest common owner is the deepest component that is an ancestor of both readers. Placing useState there keeps the blast radius minimal — only that subtree re-renders on each keystroke — while maintaining a single source of truth. Props carry the value down; a change handler carries intent up. This is the canonical React data-flow contract and the option that lets you delete code later if readers narrow.",
    },
    {
      name: "Each sibling keeps its own useState, synced via useEffect",
      summary: "Two independent copies, synchronized after commit.",
      justification: "Two owners means two sources of truth that drift between renders. The sync effect fires after commit, so there is always at least one frame where siblings disagree. Worse, the effect can trigger a loop: A updates, effect updates B, which may re-render A. This pattern is universally considered a bug, not a solution — remove the duplication by lifting instead of patching divergence with effects.",
    },
    {
      name: "Move to root-level context so any future component can reach it",
      summary: "App-wide broadcast, no prop threading.",
      justification: "Promoting to global context maximizes the re-render blast radius: every keystroke now re-renders every context consumer in the tree, not just the two siblings. Context is the right tool when state is genuinely cross-tree and low-frequency (theme, auth, locale). A filter used by two siblings on one page is neither — putting it in root context trades a two-component need for an app-wide render tax, then forces you to add React.memo everywhere to reclaim performance.",
    },
    {
      name: "Keep it in the first sibling; second sibling reads via a ref",
      summary: "First sibling owns state; ref punches through to second.",
      justification: "Refs are outside React's render cycle — mutations to ref.current do not trigger re-renders. The second sibling would display stale UI every time the filter changes; there is no mechanism to tell it to update. Refs are for reading DOM nodes or storing non-reactive values (timer ids, previous values). Shared changing state must flow through useState and props, which is exactly what lifting provides.",
    },
  ]}
/>

</Explanation>

<StackDiagram
  label="The ladder of state residence, root at the top and leaf at the bottom: colocate at the leaf by default, lift only to the lowest common owner of the readers, and treat every level above that as paid-for re-render blast radius"
  hue="var(--d-frontend)"
  layers={[
    { label: "App root — context / store", note: "last resort: truly cross-tree" },
    { label: "Page section — too high", note: "re-renders without reading" },
    { label: "Lowest common owner — lift here", note: "siblings share the value" },
    { label: "Leaf component — colocate", note: "one reader: the default" },
  ]}
  caption="State lives at exactly one level of this ladder, and each rung above the lowest common owner adds components that re-render without reading the value. Push state back down when readers narrow; before climbing to the top rung, isolate hot state in a thin wrapper via children — heavy content passes through with stable element identity."
/>

<KeyTakeaway>Every piece of state has one owner, and the owner's height sets the re-render blast radius: React re-renders the owner and its whole subtree on every write. Lift shared state to the lowest common ancestor of its readers and no higher; push it back down when readers narrow. Global-by-default inverts this and turns keystrokes into app-wide renders. The children prop isolates hot state structurally — elements created by a non-re-rendering parent are reference-identical, so React bails out without any memoization.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-lifting-and-colocation-retrieval"
  lang="en"
  questions={[
    { q: "Explain the mechanism that links the height of a piece of state to rendering cost, and what 'lowest common owner' means operationally.", a: "When state changes, React re-renders the owning component and then its entire subtree by default — every descendant function runs again, whether or not it reads the value, because props-based reads are not a subscription React can observe. So the higher the owner, the larger the subtree re-rendered per write: state in a leaf costs a leaf, state at the root costs the app. The lowest common owner is the deepest component that is an ancestor of every reader of the value. Operationally: when two components need the same changing value, walk up from both until the paths meet, put the single useState there, pass the value down as props and updates up as handlers. Lift no higher than that meeting point, and when readers narrow (a value ends up used by one subtree), push the state back down. This is why global-by-default is an architecture bug, not a style choice: it maximizes the subtree for every write and then forces memoization as damage control." },
    { q: "Why does passing an expensive subtree as children isolate it from a stateful wrapper's re-renders, and when does this beat React.memo?", a: "JSX elements are plain objects created where the JSX is written. If a parent renders a wrapper and passes content as children, those element objects are created by the parent. When the wrapper's own state changes, the wrapper re-renders, but it returns the same children objects it received — the parent didn't re-render, so nothing recreated them. React compares element references during reconciliation, finds them identical, and bails out of the whole subtree. No props comparison, no memo, just reference identity. It beats React.memo for the structural case — high-frequency state like scroll position, hover, or input drafts living in a narrow wrapper — because it needs no comparison function, can't be broken by an unstable prop (a new inline object or callback would silently defeat memo), and documents the design: this wrapper owns hot state, content flows through it. Memo remains the tool when the same component re-renders from its own parent with genuinely unchanged props." },
  ]}
/>

<Recap lang="en">
React state has exactly one owner, and architecture is deciding how high that owner sits. The default is colocation: state lives in the lowest component that needs it. When siblings share a value, you lift it — find the lowest common ancestor of all readers, move the single useState there, pass the value down as props and intent up as handlers. One source of truth, no duplication, no sync effects. The cost model that makes height matter: a state write re-renders the owner and its entire subtree, because React cannot know which descendants read the value through props — so each level above the lowest common owner adds components that re-render for nothing. Global-by-default is this mistake institutionalized: a root store for draft input values turned one keystroke into a 1,400-component render in the Hook story, and moving state down — not memoizing — cut it to 12. The structural complement is composition: a wrapper that owns fast-changing state and receives heavy content via children re-renders alone, because the children elements were created by the parent and keep the same reference, so React bails out of them entirely. Lifting has an honest cost — state moves when readers change, and you pay that refactor repeatedly — but the alternative is paying in blast radius on every write. Push state down when readers narrow; lift it only when they genuinely spread. Now when you see a profiler flame graph full of components that shouldn't be re-rendering, your first question is structural: how high does the owning state sit, and how far can it come down?
</Recap>
