infographic compare-binary-horizontal-underline-text-vs
data
  title What a boundary catches vs the four blind spots
  desc Boundaries catch render-path throws; every other path escapes
  compares
    - label Caught by a boundary
      children
        - label Render errors
          desc Thrown during a render pass
        - label Lifecycle methods
          desc Errors in subtree lifecycle methods
        - label Constructors
          desc Errors while constructing components
    - label Slips past — the 4 blind spots
      children
        - label Event handlers
          desc onClick throw goes uncaught to window; UI silently intact
        - label Async code
          desc setTimeout and promises surface as unhandledrejection
        - label Server-side rendering
          desc Stream onError plus Suspense, not componentDidCatch
        - label The boundary itself
          desc A throw in its own fallback escapes one level up
theme
  palette #10b981 #f43f5e
