infographic compare-quadrant-quarter-simple-card
data
  title When to bet on optimism: success odds x retraction cost
  desc Optimism wins only when success is near-certain AND a retraction is cheap; the other three quadrants stay pending-honest — disable, show progress, confirm on truth.
  compares
    - label Likely + cheap undo — bet it
    - label Likely + costly undo — refuse
    - label Routine fail + cheap — refuse
    - label Routine fail + costly — refuse
theme
  palette #10b981 #f59e0b #f43f5e #8b5cf6
