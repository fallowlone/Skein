---
concepts:
- react-actions
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-actions-and-useactionstate"
sources:
- https://react.dev/reference/react/useActionState
- https://react.dev/reference/react-dom/components/form
- https://react.dev/reference/react-dom/hooks/useFormStatus
spiral: []
status: ready
summary: 'React 19 forms take action={fn}: submission runs as an async transition, FormData in, new state out. useActionState gives state, a wrapped action, isPending; useFormStatus reads pending from inside. With server functions the form submits before hydration — enhancement built in.'
title: 'Form actions and useActionState: mutations as a first-class React primitive'
track: "react"
unit: "04-forms-and-mutations"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
The growth team ran the numbers after a Black Friday campaign and found a hole: the email-capture form on the landing page converted at 4.1% on fast connections and 1.3% on slow ones. The page was server-rendered and *looked* ready in under a second — but the form's onSubmit lived in a 400 KB hydration bundle that took eight to fourteen seconds to arrive on a median 3G connection. During that window the form was a painting of a form: users typed their email, hit a button wired to nothing, watched nothing happen, and left. The team's own QA never saw it — office wifi hydrates in 300 ms. The eventual fix was not a loading spinner; it was moving the mutation into a form action. A real `<form>` with an `action` posts natively — the browser has known how to submit a form since 1995 — so the submission worked before a single byte of JavaScript executed, and after hydration the very same form upgraded in place to pending states and inline errors. The mutation logic did not change. What changed was who owned the submission: not an onSubmit handler that needs JS alive, but a primitive React and the browser share.
</Hook>

<Crux>action gets FormData and returns new state in a transition; useActionState exposes state and isPending. Native POST works pre-hydration.</Crux>

<Explanation>

## The primitive: a form whose action is a function

React 19 extends the HTML `action` attribute: instead of a URL, you pass a function. When the form submits, React prevents the default navigation, builds a `FormData` from the form's named fields, and calls your function with it **inside a transition** — so the UI stays responsive while the async work runs, and React tracks its pending state for you. After the action resolves, React resets uncontrolled fields automatically (the post-submit clearing you used to write by hand). The fields arrive via `name` attributes, which quietly rehabilitates the uncontrolled style from lesson one: with actions, most form fields need no per-keystroke state at all.

`useActionState` is the state half of the primitive. You give it a reducer-shaped async function — previous state in, FormData in, new state out — plus the initial state, and it returns three things: the current state, a *wrapped* action to put on the form, and `isPending`.

```jsx
async function subscribe(prevState, formData) {
  const email = formData.get("email");
  const parsed = emailSchema.safeParse(email);
  if (!parsed.success) {
    return { ok: false, error: "Enter a valid email", email };
  }
  const res = await api.subscribe(parsed.data);
  if (!res.ok) {
    return { ok: false, error: "Try again in a minute", email };
  }
  return { ok: true, error: null, email: "" };
}

function SignupForm() {
  const [state, formAction, isPending] = useActionState(subscribe, {
    ok: false,
    error: null,
    email: "",
  });

  return (
    <form action={formAction}>
      <input name="email" defaultValue={state.email} />
      <button disabled={isPending}>
        {isPending ? "Subscribing…" : "Subscribe"}
      </button>
      {state.error ? <p role="alert">{state.error}</p> : null}
    </form>
  );
}
```

Read the shape carefully: the action is a **reducer over submissions**. Each submit takes the previous state and produces the next — which is why returning the rejected `email` back in state matters: on a server-rendered failure the input can be re-seeded instead of wiped. Sequential submissions queue: if the user submits twice, the second action waits for the first, each receiving the latest state — no interleaved writes.

## isPending and useFormStatus: two scopes of pending

`isPending` from `useActionState` tells you *this action* is in flight — use it where the form's owner lives. `useFormStatus` answers a different question from a different place: called inside a component rendered **under a form**, it returns the pending state (plus the in-flight FormData) **of the nearest enclosing form** — like a context provided by the form element itself. That makes a `SubmitButton` component reusable across every form in the codebase without prop-drilling pending flags:

```jsx
function SubmitButton({ children }) {
  const { pending } = useFormStatus();
  return <button disabled={pending}>{pending ? "Working…" : children}</button>;
}
```

The classic failure: calling `useFormStatus` in the same component that renders the `<form>`. It reads the status of a *parent* form, and there is none — `pending` is forever `false`, the button never disables, and nothing errors. The hook must live in a child of the form.

<Inset kind="why" lang="en">
Why route mutations through a transition at all? Because it gives mutations the same scheduling guarantees as concurrent rendering gives reads: the submit does not block the main thread's urgent work, pending state is tracked by React rather than by your hand-rolled isSubmitting flag (which historically leaked — set true, early return on validation failure, never set false), and related state updates batch coherently. The transition is also what lets React queue sequential submissions of the same action instead of racing them.
</Inset>

## Progressive enhancement: the form that works before React does

Here is the part that changes architecture. With framework-provided server functions (a function marked `"use server"` passed to `action`), the framework serializes a reference to that function into the HTML. If the user submits **before hydration** — the Black Friday window — the browser performs a plain native form POST to the server, the framework routes it to the same function, and the page re-renders with the result. No JavaScript required on the client. After hydration, the same form upgrades: submission becomes the in-place transition with `isPending`, no navigation, fields preserved. `useActionState` participates too — its optional third argument, `permalink`, tells the no-JS POST which URL to target so the round-trip lands on the right page.

This is the unification the unit has been building toward: client mutation and server mutation are the same declaration. The action function is the single place where the mutation lives — validation (the shared zod schema from the previous lesson slots in verbatim), the write, and the next state. Whether it executes as a local async call or as a serialized server roundtrip is a deployment detail, not an architectural fork. The discipline it demands: model the form so it *could* work without JS — named fields, state returned rather than imperatively toasted — and the enhanced version inherits correctness from the degraded one, never the reverse.

<Quiz
  id="03-actions-and-useactionstate-quiz-1"
  lessonSlug="03-actions-and-useactionstate"
  lang="en"
  question="A reusable SubmitButton calls useFormStatus, but its pending is always false even while the action is clearly in flight. The button is rendered in the same component that renders the form element. What is wrong?"
  choices={[
    { label: "useFormStatus reads the status of the nearest parent form — called beside the form rather than from a child component inside it, there is no enclosing form, so pending never becomes true", correct: true },
    { label: "useFormStatus only works with server functions, not client-side actions", misconception: "The hook is agnostic to where the action runs — it reports the in-flight status of the enclosing form element for any action. Client-only actions trigger it identically; the placement rule is what was violated." },
    { label: "The button must also receive isPending from useActionState as a prop", misconception: "That would work, but it is the prop-drilling the hook exists to eliminate. useFormStatus is form-scoped precisely so shared components need no wiring — they just must render below the form element in the tree." },
    { label: "disabled={pending} blocks the event before React can update pending", misconception: "Disabling happens after pending turns true and is the desired behavior, not a cause. The reported symptom is pending never turning true at all, which is a reading-position problem, not an event-ordering one." },
  ]}
/>

<Quiz
  id="03-actions-and-useactionstate-quiz-2"
  lessonSlug="03-actions-and-useactionstate"
  lang="en"
  question="What concretely happens when a user submits a form wired to a server function before the hydration bundle has loaded?"
  choices={[
    { label: "The browser performs a native form POST; the framework routes it to the same server function and responds with a re-rendered page — the mutation succeeds with zero client JavaScript", correct: true },
    { label: "The submit is queued in the browser and replayed automatically once hydration completes", misconception: "Nothing in the platform queues form submissions for a framework to replay. The enhancement model is the opposite: the un-hydrated path is not a buffered version of the JS path, it is the plain HTML form POST working as it has since the nineties." },
    { label: "Nothing — form actions require React event handlers, so pre-hydration submits are lost", misconception: "That is exactly the onSubmit-handler world actions were designed to escape. Because action serializes to a real target for a native POST, the form is functional from first paint; lost pre-hydration submits are the bug this primitive eliminates." },
    { label: "The browser shows a JavaScript-disabled warning and blocks the submission", misconception: "Browsers never block native form submission for lack of JavaScript — POSTing forms is core HTML behavior. No warning exists; the request simply goes to the server like any classic form." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="One mutation declaration, two submits in time: before hydration the browser POSTs natively to the server function and gets a full page back; after hydration the same form runs the action as a tracked transition with pending state and no navigation"
  hue="var(--d-frontend)"
  actors={["Form", "React", "Action fn"]}
  messages={[
    { from: "Form", to: "Action fn", label: "pre-JS: native POST" },
    { from: "Action fn", to: "Form", label: "full-page response" },
    { from: "Form", to: "React", label: "hydrated: submit" },
    { from: "React", to: "Form", label: "isPending = true" },
    { from: "React", to: "Action fn", label: "action(prev, formData)" },
    { from: "Action fn", to: "React", label: "next state" },
    { from: "React", to: "Form", label: "re-render, no nav" },
  ]}
  caption="The action is a reducer over submissions: previous state and FormData in, next state out, sequential submits queued. useActionState exposes the state and isPending; useFormStatus provides form-scoped pending to any child. Because the action serializes to a real POST target, the degraded top path works first and the enhanced bottom path inherits from it."
/>

<KeyTakeaway>React 19 makes the mutation a property of the form: `action={fn}` receives FormData and runs as a transition, with React owning pending state and post-submit resets. useActionState shapes the mutation as a reducer — previous state and FormData in, next state out, sequential submits queued — returning state, the wrapped action, and isPending; useFormStatus gives any child of the form the same pending signal without prop-drilling, and silently returns false if called outside one. With server functions the identical form submits as a native POST before hydration, so the no-JS path is the foundation and the hydrated path is the enhancement — one declaration unifying client and server mutations.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-actions-and-useactionstate-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the full anatomy of useActionState: what you pass in, what each returned element is for, and how sequential submissions behave.", a: "You pass an async reducer over submissions — (previousState, formData) => nextState — and an initial state, optionally a permalink string for the pre-hydration POST target. Back come three things. First, the current state: whatever the last completed action returned, starting at the initial state; teams use it for inline errors, success flags, and echoing rejected input back into defaultValue so a server-rendered failure does not wipe the field. Second, a wrapped action to put on the form's action attribute (or a button's formAction) — the wrapping is what connects the submission to this state cell and to the transition machinery. Third, isPending, true from submit until the action settles, owned by React rather than by a hand-maintained isSubmitting flag that can leak on early returns. Because the action runs in a transition, the UI stays interactive during the await, and if the user submits again before the first action settles, React queues the second invocation behind the first — each call receives the genuinely previous state, so writes do not interleave. Fields are read from FormData via name attributes, which means most action-driven forms need no controlled state at all." },
    { q: "Explain why an action-based form works before hydration and what discipline that imposes on how you write the mutation.", a: "When the action is a framework server function, the framework serializes a reference to it into the HTML — the form has a real POST target, not just a client-side handler. A submit during the pre-hydration window is therefore a plain native browser POST: it travels to the server, the framework routes it to the same function the hydrated path would call, the mutation runs, and the response is a re-rendered page. JavaScript on the client participates zero percent; this is the window in which onSubmit-based forms are paintings that silently drop user input — the Black Friday 1.3%-conversion failure mode. After hydration the identical form upgrades in place: submission becomes a transition, isPending and useFormStatus animate the pending UI, and there is no navigation. The discipline: the form must be modeled as if JS might be absent — every field carries a name attribute because FormData is the only transport; outcomes are returned as state (which both paths can render) rather than fired imperatively as toasts only the hydrated path could show; and useActionState's permalink is set when the no-JS roundtrip must land on a specific URL. Build the degraded path first and the enhanced path inherits its correctness — the reverse order is how forms end up JS-dependent by accident." },
  ]}
/>

<Recap lang="en">
React 19 turns form submission into a first-class mutation primitive. Passing a function to a form's action attribute makes React intercept the submit, package the named fields into FormData, and run your function inside a transition — pending state tracked by React, uncontrolled fields reset after completion, sequential submissions queued rather than raced. useActionState shapes the mutation as a reducer over submissions: it takes (previousState, formData) => nextState plus an initial state, and returns the current state, the wrapped action for the form, and isPending; returning rejected input inside the state lets a failed submit re-seed the fields even on a full server roundtrip. useFormStatus complements it from below: any component rendered inside a form can read the nearest form's pending flag and in-flight FormData — which makes shared submit buttons wiring-free, with the one trap that calling it beside (not below) the form yields a permanent false. The architectural payoff is progressive enhancement: a server function as the action serializes to a real POST target, so the form mutates correctly before hydration via native browser submission and upgrades afterward to the in-place transition — one declaration for client and server mutations, provided you keep the discipline of named fields and state-returned outcomes so the no-JS path stays the foundation.
</Recap>
