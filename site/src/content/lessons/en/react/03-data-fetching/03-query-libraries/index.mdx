---
concepts:
- react-query-libraries
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: "03-query-libraries"
sources:
- https://tanstack.com/query/latest/docs/framework/react/overview
- https://tanstack.com/query/latest/docs/framework/react/guides/important-defaults
- https://tanstack.com/query/latest/docs/framework/react/guides/caching
spiral: []
status: ready
summary: 'TanStack Query treats server data as a cache, not state: queryKey is cache identity (dedup, sharing), staleTime and gcTime are two independent clocks (refetch vs memory), refetch-on-focus keeps data live, and mutations plus invalidateQueries close the write loop.'
title: 'Query libraries: queryKey identity, the two clocks, and what TanStack Query actually saves you'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The backend team escalated first: "/api/me is 30% of our traffic — why does your SPA call it six times per navigation?" The frontend audit was embarrassing in a familiar way. The header fetched the current user for the avatar. The sidebar fetched it for permissions. Three widgets fetched it for personalization. Each component had its own dutiful `useEffect`, its own `loading` flag, its own copy of the answer in its own `useState` — six requests for one resource, none of them shared, none of them ever updated after mount. A user who renamed themselves in settings saw the old name in the header until they hit F5. The team's first instinct was a hand-rolled context-plus-cache; two sprints later it had a memory leak, no retry logic, and a race bug the original effects never had. What they were rebuilding, badly, was a server-state cache — request deduplication, staleness tracking, background refetch, garbage collection. That is the actual product of TanStack Query and its siblings: not "fetching made easy", but the admission that server data is a *cache* with cache problems, and cache problems have forty years of known solutions.
</Hook>

<Crux>queryKey is cache identity: same key, one shared entry. Two clocks — staleTime gates refetching, gcTime gates memory. Mutations invalidate.</Crux>

<Explanation>

## queryKey is cache identity, not a label

The mental shift: server data is not your state — it is a *cache of someone else's state*. TanStack Query makes that explicit. Every query is identified by its `queryKey`, an array hashed structurally (object key order does not matter, array order does): `['todos', { status, page }]`. The key is the cache's address. Every component calling `useQuery` with the same key reads *the same cache entry* — six components asking for `['me']` produce one request, deduplicated in flight, and one shared answer. The discipline that follows: **every variable your queryFn reads must be in the key**, exactly like a useEffect dependency array. Leave `page` out of the key and page 2 overwrites page 1 under the same address; the symptom is "wrong data flickers in", the diagnosis is almost always a key that under-describes the request.

```jsx
function Todos({ status, page }) {
  const { data, isPending, isError, error } = useQuery({
    queryKey: ["todos", { status, page }],   // identity = all inputs
    queryFn: ({ signal }) =>
      fetchTodos({ status, page, signal }),  // abort wired for free
    staleTime: 30_000,                        // fresh for 30s
  });
  // isPending → first load, no cached data yet
  // data can be present *and* stale: shown instantly, refetched behind
}

function AddTodo() {
  const queryClient = useQueryClient();
  const mutation = useMutation({
    mutationFn: createTodo,
    onSuccess: () =>
      queryClient.invalidateQueries({ queryKey: ["todos"] }), // prefix match
  });
  return <button onClick={() => mutation.mutate({ title: "ship" })}>Add</button>;
}
```

## The two clocks: staleTime decides refetching, gcTime decides memory

Two independent timers govern every cache entry, and conflating them is the number-one TanStack Query misunderstanding. **`staleTime`** answers "is this data fresh enough to serve without asking the server again?" While an entry is fresh, any mount or focus event serves it from cache, full stop — zero requests. Once it goes stale, the data is still served instantly, but a background refetch fires on the next *trigger*: a new component mounting, the window regaining focus, the network reconnecting. That is stale-while-revalidate: the user always sees data immediately; freshness arrives quietly behind it. **`gcTime`** answers a different question: "after the last component using this entry unmounts, how long do we keep it in memory?" An *active* entry is never collected; an inactive one survives `gcTime` (default 5 minutes) so that navigating back within that window is an instant cache hit instead of a spinner.

The defaults are deliberately aggressive: `staleTime: 0` — everything is stale immediately, so every mount and every window focus triggers a background refetch; plus failed queries retry 3 times with exponential backoff. Teams discover this as "why is my API getting hammered?" — the answer is not to disable `refetchOnWindowFocus` globally (that focus refetch is what keeps a dashboard truthful when the user comes back from lunch) but to set `staleTime` honestly per resource: 30 seconds for a todo list, 10 minutes for a country list, 0 for a stock price. The two clocks fail differently when misconfigured: `staleTime` too high serves wrong data; `gcTime` too high holds dead data in memory; setting `gcTime` *lower* than `staleTime` quietly defeats the point, since the entry is collected before its freshness ever matters to a returning visitor.

<Quiz
  id="03-query-libraries-quiz-1"
  lessonSlug="03-query-libraries"
  lang="en"
  question="A query has staleTime: 60_000 and gcTime: 300_000. The component unmounts after 10 seconds; the user navigates back 40 seconds later. What happens at remount?"
  choices={[
    { label: "Instant cache hit and no request: the entry survived in memory (inactive 40s < gcTime 5min) and is still fresh (50s of life < staleTime 60s), so the cache serves it and no background refetch fires", correct: true },
    { label: "Cache hit, but a background refetch fires because unmounting made the data stale", misconception: "Unmounting doesn't touch freshness — it only starts the gcTime countdown for memory eviction. Staleness is purely the staleTime clock running from when the data was fetched; at 50 seconds of age against a 60-second staleTime, the entry is still fresh and remount triggers nothing." },
    { label: "Full loading spinner: unmounting removed the entry from the cache", misconception: "Eviction happens gcTime (here 5 minutes) after the last observer unmounts, not at unmount. That grace window is the whole point of gcTime — back-navigation within it lands on cached data instantly. Only after 5 inactive minutes would remount start from scratch." },
    { label: "A request fires because remounting always revalidates server data", misconception: "Mount is a refetch trigger only for stale entries. That's what staleTime controls: while data is fresh, mounts and window focus serve pure cache hits with zero network. With the default staleTime of 0 every mount does refetch — which is a configuration choice, not the mechanism." },
  ]}
/>

## Mutations close the loop — and the library's real invoice

Reads are half the story. `useMutation` wraps a write and, in `onSuccess`, you reconcile the cache with the new server reality — most simply with `invalidateQueries({ queryKey: ['todos'] })`, which prefix-matches every key starting with `'todos'`, marks them stale, and immediately refetches the *active* ones (inactive entries just get the stale flag and refetch on next use). This is the piece the effect pattern never had: after a POST, your hand-rolled `useState` caches across five components are all silently wrong, and no amount of effect hygiene fixes data that no longer reflects the server.

Tally what the library actually saves versus last lesson's baseline: in-flight **deduplication** (six subscribers, one request) and shared cache entries; the **race guard** built in — responses are matched to their request, and `queryFn` receives an `AbortSignal` wired to query cancellation; **retries** with exponential backoff; **lifecycle** — staleness, focus/reconnect refetch, garbage collection; and the **write path** — invalidation, plus optimistic updates (next lesson). The honest costs: a dependency in the critical path (~12 kB gzipped core), key-design discipline (keys are your cache schema — centralize them in a factory, because scattered ad-hoc keys make targeted invalidation guesswork), and defaults you must consciously tune. The tradeoff is the same one as ever: you are not avoiding a cache — you are choosing between a tested one and the one you will write by accident.

<Quiz
  id="03-query-libraries-quiz-2"
  lessonSlug="03-query-libraries"
  lang="en"
  question="A todo list is paginated: queryFn fetches /api/todos?page=N, but the queryKey is just ['todos']. The user flips from page 1 to page 2. What does the cache do, and what does the user see?"
  choices={[
    { label: "Both responses write to the same cache entry — page 2's response overwrites page 1 under the address ['todos'], components briefly show the old page's data, and back-navigation can't restore page 1 from cache; the key must include the page, since identity = every queryFn input", correct: true },
    { label: "TanStack Query detects the URL difference and forks the entry automatically", misconception: "The library never inspects your queryFn or its URLs — the queryKey is the sole identity. Anything the function reads that isn't in the key is invisible to the cache, exactly like a stale closure with a missing useEffect dependency. The fix is structural: ['todos', { page }]." },
    { label: "The second fetch is deduplicated against the first, so page 2 never loads", misconception: "Dedup applies to concurrent requests for the same key, and here it would only merge overlapping in-flight calls — a sequential page flip still fires a real request. The corruption isn't a missing request; it's two different resources fighting over one cache address." },
    { label: "Nothing breaks, but you lose the ability to prefetch the next page", misconception: "Prefetching is lost too, but the primary failure is data integrity: one address holding whichever page wrote last means components reading ['todos'] show the wrong page after any refetch, focus event, or remount. Under-keyed queries corrupt the cache, not just optimizations." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A useQuery mount resolves through cache identity: fresh entries serve with no request, stale ones serve instantly and revalidate behind, and mutations invalidate matching keys"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "mount", label: "useQuery mount", sub: "key = ['todos', p]" },
    { id: "hit", label: "Cache entry?", sub: "structural key hash" },
    { id: "fresh", label: "Fresh: serve", sub: "no request" },
    { id: "stale", label: "Stale: serve + refetch", sub: "background revalidate" },
    { id: "mutate", label: "Mutation success", sub: "invalidateQueries" },
    { id: "gc", label: "Inactive entry", sub: "gcTime → evict" },
  ]}
  edges={[
    { from: "mount", to: "hit", label: "lookup" },
    { from: "hit", to: "fresh", label: "age < staleTime" },
    { from: "hit", to: "stale", label: "age ≥ staleTime" },
    { from: "mutate", to: "stale", label: "mark + refetch active" },
    { from: "hit", to: "gc", label: "last observer gone" },
  ]}
  caption="staleTime gates the refetch decision; gcTime gates memory after the last subscriber unmounts. Focus and reconnect re-run the same fresh/stale check — which is why honest per-resource staleTime, not disabling focus refetch, is the fix for refetch storms."
/>

<KeyTakeaway>A query library is a server-state cache, and the queryKey is its identity: every input the queryFn reads belongs in the key, same key means one shared deduplicated entry. Two independent clocks govern each entry — staleTime decides whether mount/focus/reconnect triggers serve cache silently or revalidate behind shown data; gcTime decides how long inactive entries survive for instant back-navigation. Mutations reconcile the cache via invalidateQueries prefix-matching. You get dedup, races handled, retries with backoff, and cache lifecycle — in exchange for key discipline and consciously tuned defaults.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-query-libraries-retrieval"
  lang="en"
  questions={[
    { q: "Explain staleTime versus gcTime precisely: what question does each clock answer, what are the defaults, and what distinct failure does each cause when misconfigured?", a: "staleTime answers: how long after a fetch is this data fresh enough to serve without revalidation? While fresh, every trigger — component mount, window focus, network reconnect — is a pure cache hit with zero requests. Once stale, the data is still served instantly (the UI never waits), but the next trigger fires a background refetch: stale-while-revalidate. Default is 0 — everything is immediately stale, so every mount and focus refetches; that's safe-by-default freshness, and it's why untuned apps look like they're hammering the API. gcTime answers an orthogonal question: after the last subscribed component unmounts, how long does the inactive entry stay in memory? Active entries are never collected; inactive ones survive gcTime (default 5 minutes), making back-navigation within the window an instant hit. Distinct failure modes: staleTime too high serves wrong data — a price or permission set that lies for minutes; staleTime untuned at 0 causes refetch storms and backend load; gcTime too high retains dead data in memory (matters with large lists); gcTime shorter than staleTime quietly wastes the freshness window, since the entry is evicted before a returning visitor could benefit. The fix for refetch storms is honest per-resource staleTime — 0 for a stock price, 30s for todos, 10min for a country list — not globally disabling focus refetch, which is what keeps long-lived tabs truthful." },
    { q: "A teammate says: \"TanStack Query is just useEffect with extra steps — I can write the fetch myself.\" Itemize what the library replaces, mapping each item to the failure it prevents.", a: "Per endpoint, the effect baseline is ~20 lines of loading/error/data state plus an ignore-flag-and-abort race guard — and that's the easy part. The library replaces: (1) request deduplication and cache sharing — N components reading the same queryKey produce one in-flight request and one shared entry, versus N copies in N useStates that desynchronize after any write (the six-calls-to-/api/me audit); (2) the race guard — responses are matched to their request, and queryFn receives an AbortSignal tied to cancellation, replacing the hand-written ignore/abort pair in every effect; (3) retries — 3 attempts with exponential backoff by default, where hand-rolled fetches usually fail on the first blip; (4) staleness lifecycle — staleTime/gcTime, refetch on focus and reconnect, garbage collection: the entire 'is this still true and may I free it' problem that hand caches (the team's context-plus-cache with a memory leak) get wrong; (5) the write path — invalidateQueries prefix-matching to reconcile every dependent read after a mutation, which the effect pattern simply does not have: after a POST, five copied useStates are silently wrong. Honest costs in return: ~12 kB gzipped in the critical path, key-design discipline (keys are a cache schema — centralize them in a factory or invalidation becomes guesswork), and aggressive defaults (staleTime 0) you must tune per resource. The argument isn't convenience; it's that server data is a cache, and you either adopt a tested cache or accidentally write an untested one." },
  ]}
/>

<Recap lang="en">
Query libraries start from a reframe: server data is not component state, it is a client-side cache of state owned by the server — and caches have known problems with known solutions. In TanStack Query the `queryKey` is cache identity: an array hashed structurally, where every input the `queryFn` reads must appear (an under-keyed paginated query lets page 2 overwrite page 1 under one address), and every component using the same key shares one deduplicated entry — six avatar/permission/personalization readers of `['me']` collapse into one request. Each entry runs on two independent clocks: `staleTime` gates revalidation — fresh entries serve mounts and focus events from cache with zero requests, stale entries serve instantly while a background refetch fires on the next trigger (stale-while-revalidate); `gcTime` gates memory — active entries are never collected, inactive ones survive five minutes by default so back-navigation is an instant hit. The defaults are aggressive (staleTime 0, three retries with exponential backoff, refetch on focus and reconnect), so refetch storms are fixed by honest per-resource staleTime, not by disabling the focus refetch that keeps long-lived tabs truthful. Writes close the loop: `useMutation` plus `invalidateQueries` prefix-matches affected keys, marks them stale, and refetches active ones — the reconciliation step the effect pattern never had. The invoice: a ~12 kB dependency, key discipline (centralize key factories), and consciously tuned defaults — against the cache you would otherwise write by accident.
</Recap>
