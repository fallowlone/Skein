---
concepts:
- react-suspense-data
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-suspense-for-data"
sources:
- https://react.dev/reference/react/Suspense
- https://react.dev/reference/react/use
- https://react.dev/reference/react/Component
spiral: []
status: ready
summary: 'Suspense inverts data loading: a component reads a promise with use(), React suspends to the nearest boundary, and resolve retries the render. Boundary placement is UX design, rejected promises route to error boundaries — and without a cache, raw fetch in render suspends forever.'
title: 'Suspense for data: the throw-a-promise contract, use(), and boundaries as UX design'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import boundaryPlacement from "./infographics/boundary-placement.svg?raw";

<Hook>
The migration PR was titled "Suspense all the things" and it demoed beautifully: every `isLoading` ternary deleted, components that just *read* data. Two incidents followed within a week. First, staging started pinning a CPU core at 100% — a developer had written `use(fetch('/api/user'))` directly in a component body, and the profiler showed the same component rendering thousands of times per second: fetch a promise, suspend, retry, fetch a *new* promise, suspend again, forever. Nothing on screen but the fallback, network tab filling with hundreds of identical requests. Second, after that was patched with a cache, the support channel lit up: "the whole dashboard goes blank when I change the date filter." The team had wrapped the entire page in a single `<Suspense>` at the root, so any widget refetching anywhere replaced *all twelve widgets* with one giant spinner. Same week, same feature, two opposite lessons: Suspense is a contract between a thrown promise and a boundary — and where you put the boundary is not plumbing, it is the loading UX itself, designed in JSX.
</Hook>

<Crux>Suspense: a pending read throws the promise; nearest boundary shows fallback; resolve retries. No cache → new promise each try, forever.</Crux>

<Explanation>

## The contract: suspend by throwing, resume by retrying

Why does this matter to you as an engineer? Because understanding the throw-and-retry contract is the only way to diagnose the two failure modes from the Hook — infinite loops and page-wide blanks — and to place boundaries intentionally rather than by cargo-cult.

Mechanically, Suspense is an exception protocol. When a component cannot render because its data is not ready, it throws a *thenable* (promise-like object). React catches it while unwinding the render — remember, render is pure and discardable, so abandoning the attempt is free — walks up to the nearest `<Suspense>` boundary, and commits that boundary's `fallback` instead of the subtree. React then subscribes to the thrown promise; when it resolves, React *retries rendering the subtree from scratch*. On the retry, the component runs again, the data source now has a value, the read returns synchronously, and the real UI commits. The component itself contains no loading branch — "not ready" is not a state it models, it is an exception it throws.

`use()` is the sanctioned read primitive: `const user = use(userPromise)` returns the resolved value, throws the promise to suspend if pending, and throws the rejection reason if rejected. Unlike hooks, `use` may be called inside conditionals and loops — it is a read, not a state registration. The promise itself should be *created elsewhere* (a parent, a router loader, a cache) and passed down or looked up; the component's job is only to read.

```jsx
const cache = new Map();
function fetchUser(id) {              // identity: same id → same promise
  if (!cache.has(id)) {
    cache.set(id, fetch(`/api/users/${id}`).then((r) => {
      if (!r.ok) throw new Error(`HTTP ${r.status}`);
      return r.json();
    }));
  }
  return cache.get(id);
}

function Profile({ id }) {
  const user = use(fetchUser(id));    // suspends while pending
  return <h1>{user.name}</h1>;
}

// <ErrorBoundary fallback={<Oops />}>
//   <Suspense fallback={<Skeleton />}>
//     <Profile id={42} />
//   </Suspense>
// </ErrorBoundary>
```

## Why raw fetch in render loops forever

Here is the trap from the Hook, mechanically. Render is pure and re-runnable: React may call your component many times, and *each suspension discards the attempt entirely*. If the component body itself creates the promise — `use(fetch(url))` — then the retry after resolution runs the body again and creates a *brand-new, pending* promise. The one that resolved is gone with the discarded render; the new one suspends; React subscribes, it resolves, retry, new promise, suspend. The loop never converges, and every iteration fires a real network request. The fix is **identity**: some store outside render must return *the same promise* for the same logical request, so the retry's `use()` call receives the promise that already resolved and reads through synchronously. That store is a cache — a `Map` keyed by request parameters in the toy version, a query library or framework loader in production. This is the structural reason React's docs say Suspense-enabled data fetching is meant to be provided by frameworks and caching libraries rather than hand-rolled: the contract is easy, the cache (invalidation, eviction, deduplication) is the actual product.

<Quiz
  id="02-suspense-for-data-quiz-1"
  lessonSlug="02-suspense-for-data"
  lang="en"
  question="A component calls use(fetch('/api/user')) directly in its body. The fallback renders forever and the network tab fills with identical requests. What is the mechanism?"
  choices={[
    { label: "Each suspension discards the render attempt; the retry re-runs the body and creates a brand-new pending promise, so use() never sees a resolved one — the loop only converges if a cache outside render returns the same promise for the same request", correct: true },
    { label: "fetch is not allowed inside components, so React blocks the request", misconception: "React doesn't intercept or block fetch — every one of those requests really hits the server, which is why the network tab fills up. The problem isn't that fetching is forbidden in render; it's that the promise's identity changes on every attempt, so resolution of any individual promise never helps the next render." },
    { label: "use() only works with promises created by React.cache or a framework", misconception: "use() accepts any thenable — a plain fetch promise is structurally fine. What it needs is a stable one: created once per logical request and reused across render attempts. React.cache and frameworks are conventional ways to get that stability, not a type requirement." },
    { label: "The Suspense boundary is missing, so React retries as fast as it can", misconception: "A missing boundary would be a different failure (the suspension propagates further up, or errors). The infinite loop happens with a perfectly placed boundary — the fallback you see rendering forever is that boundary working correctly while the component beneath it keeps manufacturing fresh pending promises." },
  ]}
/>

## Boundaries are UX design, errors are the boundary's twin

Where you place `<Suspense>` decides where spinners appear — that is a design decision expressed in component structure. One boundary at the page root means the entire page is all-or-nothing: simplest, but any slow widget blanks everything (the Hook's second incident). A boundary per widget means each section reveals independently — fast widgets appear immediately, slow ones show local skeletons — at the cost of "popcorn UI", content popping in random order and shifting layout. The senior pattern is *deliberate grouping*: wrap the parts users perceive as one unit (header + summary) in one boundary so they appear together, give long-tail sections (analytics, recommendations) their own. Nested boundaries compose: a suspension is caught by the *nearest* ancestor boundary, so inner boundaries scope inner loading states while the outer one covers initial mount. To control reveal *ordering* — top-to-bottom instead of whoever-resolves-first — React's experimental `SuspenseList` coordinated multiple boundaries; until it stabilizes, the same effect comes from boundary structure or from awaiting combined promises higher up.

<Infographic
  svg={boundaryPlacement}
  label="A vertical comparison of three React Suspense boundary placements: one root boundary gives an all-or-nothing reveal, a boundary per widget reveals independently but causes popcorn UI, and deliberate grouping wraps perceptual units together while isolating long-tail sections."
  caption="Boundary placement is the loading UX: coarse root all-or-nothing vs popcorn per-widget vs deliberate grouping — choose by what users perceive as one unit."
  hue="var(--d-frontend)"
/>

Rejection is the mirrored path: when the awaited promise rejects, the retry *throws the rejection* during render, and that exception travels to the nearest **error boundary** — not the Suspense boundary. A production-grade tree therefore pairs them: ErrorBoundary outside, Suspense inside, per region. Tradeoff to keep honest: fallbacks *replace* content. For updates after the initial load, replacing live data with a spinner is often worse than showing slightly stale data — which is why `startTransition` exists: updates marked as transitions do not trigger an already-revealed boundary's fallback; React keeps the old UI while the new tree suspends in the background.

<Quiz
  id="02-suspense-for-data-quiz-2"
  lessonSlug="02-suspense-for-data"
  lang="en"
  question="A dashboard wraps twelve widgets in one root Suspense boundary. Changing a filter makes one widget refetch and the whole dashboard blanks to the spinner. What change fixes the UX without giving up Suspense?"
  choices={[
    { label: "Restructure boundaries — give widgets or perceptual groups their own nested Suspense (suspensions are caught by the nearest boundary) and mark the filter change as a transition so already-revealed content stays on screen during refetch", correct: true },
    { label: "Catch the promise in the widget and render a local if/else spinner instead", misconception: "Hand-catching the thrown promise reimplements isLoading state and abandons the contract — you lose discardable renders, transition integration, and SSR streaming. The mechanism already supports local spinners; it just needs a boundary placed at the widget, not the root." },
    { label: "Increase the cache staleTime so the widget never refetches", misconception: "That hides this reproduction but doesn't fix the design: any future suspension — a new widget, a cold cache after deploy — still blanks the whole page, and you've traded a UX bug for serving stale data. Boundary placement is the actual lever; freshness policy is a separate decision." },
    { label: "Suspense only supports one boundary per page, so this is the expected cost", misconception: "Boundaries nest arbitrarily and compose — that's the core design. A suspension propagates to the nearest ancestor boundary, so inner boundaries scope loading states to regions. The root-only layout is a choice (usually a default nobody revisited), not a constraint of the API." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="The Suspense data handshake over time: the component reads a pending promise and throws, the nearest boundary shows its fallback, settlement triggers a retry that now reads the value synchronously"
  hue="var(--d-frontend)"
  actors={["Component", "Suspense", "Promise cache"]}
  messages={[
    { from: "Component", to: "Promise cache", label: "render: use(promise)" },
    { from: "Promise cache", to: "Component", label: "pending → throw" },
    { from: "Component", to: "Suspense", label: "unwind → fallback" },
    { from: "Promise cache", to: "Suspense", label: "settles → retry" },
    { from: "Suspense", to: "Component", label: "re-render" },
    { from: "Component", to: "Promise cache", label: "read sync → value" },
  ]}
  caption="The retry only converges if the same promise (now settled) is returned for the same request — that identity is what a cache provides; a bare fetch in render mints a fresh pending promise per attempt and loops forever. Boundary placement scopes which region shows the fallback; rejections skip Suspense and hit the nearest error boundary."
/>

<KeyTakeaway>Suspense is an exception protocol: a component reads a promise with use(), a pending read throws it, the nearest boundary shows its fallback, and resolution retries the render from scratch. The retry only converges if a cache returns the same promise for the same request — raw fetch in render manufactures a new pending promise per attempt and loops forever. Boundary placement is loading UX designed in JSX: nearest-boundary scoping, deliberate grouping over root-level all-or-nothing, ErrorBoundary paired outside for rejections, and transitions to keep revealed content on screen during refetches.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-suspense-for-data-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the full Suspense lifecycle for a component reading data with use(), covering the pending, resolved, and rejected paths — and explain precisely why the mechanism requires a cache.", a: "During render the component calls use(promise). If the promise is pending, use throws it; React catches the thenable while unwinding — legal because render is pure and discardable — finds the nearest <Suspense> boundary, commits its fallback in place of the subtree, and subscribes to the promise. On resolution React retries the subtree from scratch: the component function runs again, use(promise) now returns the value synchronously, and the real UI commits. On rejection the retry throws the rejection reason during render, which propagates past Suspense to the nearest error boundary — so production trees pair ErrorBoundary (outside) with Suspense (inside) per region. The cache requirement falls out of the retry semantics: the retry re-executes the component body, so if the body creates the promise (use(fetch(url))), every attempt manufactures a new pending promise — the resolved one died with the discarded render — and the component suspends forever while firing a real request per iteration. Convergence needs promise identity: a store outside render (Map keyed by request params, React.cache, a query library) must return the same — now settled — promise for the same logical request, so the retry reads through. The contract is trivial; the cache with its deduplication, invalidation, and eviction is the hard part, which is why React positions Suspense data fetching as framework/library territory." },
    { q: "Your team asks where to put Suspense boundaries on a new dashboard and how updates should behave. What is the senior playbook, including the tradeoffs of each placement?", a: "Treat boundary placement as loading-UX design, not plumbing. One root boundary: simplest, coherent single reveal, but all-or-nothing — any slow or refetching widget blanks the entire page, and time-to-first-content equals the slowest query. Per-widget boundaries: independent reveal, fast sections appear immediately — but popcorn UI, content popping in resolution order with layout shifts. The deliberate middle: group elements users perceive as one unit (page header + key numbers) under one boundary so they appear atomically; give heavy long-tail sections (charts, recommendations) their own boundaries with skeletons sized to prevent shift. Suspensions bubble to the nearest ancestor boundary, so nesting composes: the outer boundary covers initial mount, inner ones scope later, local loading. Reveal ordering (top-to-bottom rather than whoever-resolves-first) was SuspenseList's job; while it remains experimental, approximate it through boundary structure or by combining promises higher up. For updates: a fallback replaces content, and replacing live data with a spinner on every filter change is usually worse than briefly stale data — wrap the update in startTransition, which keeps already-revealed content on screen instead of re-triggering the boundary fallback while the new tree suspends in the background. And every Suspense region needs its rejection story: an error boundary outside it, with a retry affordance that resets the cache entry." },
  ]}
/>

<Recap lang="en">
Suspense replaces modeled loading state with an exception protocol. A component reads its data with `use(promise)`: a resolved promise returns its value synchronously, a pending one is thrown — React catches it while discarding the pure render attempt, commits the `fallback` of the nearest `<Suspense>` boundary, subscribes, and retries the subtree from scratch on resolution; a rejected one throws its reason to the nearest error boundary, which is why ErrorBoundary-outside-Suspense-inside is the standard pairing. The retry semantics dictate the cache requirement: because the retry re-runs the component body, a body that creates its own promise (`use(fetch(url))`) produces a fresh pending promise per attempt and suspends in an infinite loop of real network requests — convergence demands that a store outside render return the *same* promise for the same logical request, which is exactly what query libraries and framework loaders provide, and why hand-rolled Suspense fetching stops at toy caches. Boundary placement is the UX: suspensions bubble to the nearest boundary, so a single root boundary means all-or-nothing reveal while per-region boundaries reveal independently at the cost of popcorn ordering — group what users perceive as a unit, scope the rest, and use `startTransition` for updates so refetching replaces nothing that is already on screen. `use` may be called conditionally — it is a read, not a hook registration — and the promise should be created by a parent, loader, or cache, never by the reading component itself. Now when you see a boundary blanking too much of the page, or a component that suspends in an infinite loop, you have the exact lever: boundary placement is UX design, and promise identity is the cache's job — reach for whichever one the symptom points at.
</Recap>
