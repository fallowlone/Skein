---
concepts:
- react-cache-invalidation
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-cache-invalidation-patterns"
sources:
- https://tanstack.com/query/latest/docs/framework/react/guides/query-invalidation
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://tanstack.com/query/latest/docs/framework/react/guides/infinite-queries
spiral: []
status: ready
summary: 'After a write, the cache lies until you reconcile it. Invalidate buys server truth for an extra request; setQueryData is instant but replicates server logic; optimistic updates need cancel + snapshot + rollback. Plus infinite-query refetch costs and websocket sync.'
title: 'Cache invalidation patterns: invalidate vs write-through, optimistic rollback, and live sync'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import requestCost from "./infographics/request-cost.svg?raw";

<Hook>
Two tickets, two weeks apart, same root cause family. Ticket one: "I created an invoice, got the success toast, landed on the invoice list — and my invoice isn't there. Refreshed, there it is. Your app loses data." It doesn't lose data; the list query had `staleTime: 5 minutes` for performance, the mutation never invalidated `['invoices']`, and the cache faithfully served its five-minute-old truth. Ticket two came from the infra side: API p99 latency spiking every afternoon. The trace showed single users generating 40 requests in one second — a "fix" for ticket one had added `invalidateQueries()` *with no key* to every mutation's `onSuccess`, so saving one checkbox refetched every active query on a dashboard with forty widgets. A refetch storm, self-inflicted. This is the lesson where the two famous hard things meet: after every write, your cache is wrong until you reconcile it — and reconciliation is a dial between "refetch everything true" and "patch exactly what changed", where one end costs requests and the other costs correctness.
</Hook>

<Crux>After a write the cache lies. Invalidate refetches server truth; setQueryData is instant but copies server logic. Optimism needs rollback.</Crux>

<Explanation>

## Invalidate vs setQueryData: truth by refetch or truth by replication

`invalidateQueries({ queryKey: ['invoices'] })` is the conservative move: prefix-match the affected keys, mark them stale, refetch the active ones. You get *server truth* — sort order, computed totals, rows other users added — at the price of one extra round trip and a brief window where the old data is still on screen. `setQueryData(['invoices'], updater)` is **write-through**: patch the cached value synchronously, zero requests, instant UI. Its price is sneakier: your updater must replicate server logic. Insert the new invoice — at which position? The list is sorted by a server-assigned number. Does it match the current filter? Did the server set `dueDate`, normalize the currency, trigger a side effect on another row? Every divergence is a *silent* lie in the cache. The senior default: **write-through with the server's response** (`onSuccess: (created) => setQueryData(...)` patching the created object in), or write-through *then* invalidate — instant UI now, truth reconciled in the background. Reserve pure `setQueryData` for caches whose shape you fully control, and never invalidate without a key: scope it to the resource family that actually changed, or a 40-widget dashboard turns every checkbox into a request storm.

## Optimistic updates: the three-step contract with a rollback

For interactions where even one round trip feels broken — likes, toggles, drag-reorder — update the cache *before* the server confirms. The contract has three mandatory steps, all in `onMutate`:

```jsx
const mutation = useMutation({
  mutationFn: toggleTodo,
  onMutate: async (todo) => {
    // 1. Cancel: an in-flight refetch would overwrite our optimistic write
    await queryClient.cancelQueries({ queryKey: ["todos"] });
    // 2. Snapshot: what we roll back to if the server says no
    const previous = queryClient.getQueryData(["todos"]);
    // 3. Optimistic write-through
    queryClient.setQueryData(["todos"], (old) =>
      old.map((t) => (t.id === todo.id ? { ...t, done: !t.done } : t))
    );
    return { previous };                      // → context
  },
  onError: (_err, _todo, context) =>
    queryClient.setQueryData(["todos"], context.previous), // rollback
  onSettled: () =>
    queryClient.invalidateQueries({ queryKey: ["todos"] }), // reconcile
});
```

Each step exists because of a specific failure. Skip **cancel** and a refetch that started before your click can land *after* your optimistic write and revert it mid-flight — the checkbox flickers off, then on again when the mutation settles; users file it as "the app fought me". Skip **snapshot/rollback** and a server rejection leaves the UI permanently claiming something false — the like that never happened. Skip **reconcile in onSettled** and your optimistic guess (even a correct one) drifts from server-computed fields. The tradeoff is honest: optimistic UI converts p50 latency into zero *perceived* latency, but buys an error path you must design — rollback plus a visible "couldn't save" affordance — and it composes badly with many concurrent mutations on the same entity (the snapshot of mutation B may capture mutation A's unconfirmed guess; TanStack Query's docs route heavy cases through per-mutation variables or single-flight mutations instead).

<SequenceDiagram
  label="The optimistic mutation's failure timeline: cancel and snapshot precede the optimistic write, the server's rejection triggers a rollback to the snapshot, and onSettled invalidation refetches server truth"
  hue="var(--d-frontend)"
  actors={["mutate()", "Query cache", "Server"]}
  messages={[
    { from: "mutate()", to: "Query cache", label: "cancelQueries(key)" },
    { from: "mutate()", to: "Query cache", label: "snapshot previous" },
    { from: "mutate()", to: "Query cache", label: "optimistic write" },
    { from: "mutate()", to: "Server", label: "POST toggle" },
    { from: "Server", to: "mutate()", label: "rejected (500)" },
    { from: "mutate()", to: "Query cache", label: "rollback snapshot" },
    { from: "mutate()", to: "Query cache", label: "onSettled: invalidate" },
    { from: "Query cache", to: "Server", label: "refetch → truth" },
  ]}
  caption="The three onMutate steps in their real order: cancel kills the in-flight refetch that would stomp the optimistic value, the snapshot makes rejection recoverable, the write lands last. On success only the middle differs — the rollback message never fires, but onSettled still reconciles the guess with server-computed truth."
/>

<Quiz
  id="04-cache-invalidation-patterns-quiz-1"
  lessonSlug="04-cache-invalidation-patterns"
  lang="en"
  question="An optimistic toggle implements snapshot and rollback but skips cancelQueries in onMutate. QA reports the checkbox sometimes flips back by itself a moment after clicking, then flips on again. What happened?"
  choices={[
    { label: "A background refetch that was already in flight at click time resolved after the optimistic write and overwrote the cache with pre-toggle server data; cancelQueries exists precisely to kill in-flight fetches whose responses would stomp the optimistic value", correct: true },
    { label: "The rollback ran because the server returned an error", misconception: "An error rollback flips the checkbox back once and leaves it — it doesn't flip on again moments later. The flicker-then-restore signature means the cache was overwritten by a stale fetch response and then corrected when the mutation settled and reconciliation refetched fresh truth." },
    { label: "setQueryData is asynchronous, so the write landed late", misconception: "setQueryData applies synchronously — subscribers see the optimistic value in the same tick. The delayed reversal isn't the write arriving late; it's an older network response arriving later and replacing the entry, which only cancellation of in-flight queries prevents." },
    { label: "The snapshot accidentally captured the optimistic value instead of the previous one", misconception: "Snapshot ordering bugs (snapshotting after the write) corrupt the rollback path — visible only when the server errors. Here the flicker happens on successful mutations too, which points at the read path: an uncancelled refetch racing the optimistic write." },
  ]}
/>

## Beyond a single list: dependencies, pages, and pushed truth

Once you have the single-list patterns down, ask yourself: where does the data graph get more complex? Dependent queries, pagination, and real-time push each break one of the assumptions above — and each has its own failure cost that doesn't show up until traffic or page count grows.

**Dependent queries** chain with `enabled`: `useQuery({ queryKey: ['projects', user?.id], enabled: !!user })` keeps the second query idle until the first resolves. This is the deliberate version of last lesson's waterfall — fine when the dependency is real, but every `enabled` chain is serialized latency you chose, so audit chains the way you audit `await` sequences. **Infinite queries** make invalidation expensive in a way that surprises: an invalidated `useInfiniteQuery` refetches *every loaded page sequentially* to keep cursors consistent — a user 10 pages deep triggers 10 serial requests per invalidation. Mitigations: cap retained pages with `maxPages`, prefer targeted `setQueryData` page-patching for single-row edits, and use `placeholderData: keepPreviousData` on ordinary paginated queries so page flips show the old page instead of a spinner. **Websocket-driven sync** is the endgame for multi-user staleness, and the senior pattern is unintuitive: on a push event, *do not* write the payload into the cache — call `invalidateQueries` with the event's entity key. Pushed payloads arrive out of order, may omit server-computed fields, and duplicate your write-through logic; using events as "something changed, ask the server" signals keeps one source of truth, and at high event rates you debounce invalidations per key — collapsing 50 events/second into at most a few refetches — which is exactly the storm-throttling that ticket two needed.

<Infographic
  svg={requestCost}
  label="Horizontal bar chart of how many refetch requests one trigger fires: write-through setQueryData 0, a scoped invalidate 1, an invalidated 10-page infinite list 10, a keyless invalidate on a 40-widget dashboard 40, and a naive websocket sync at 50 events per second 50."
  caption="The dial measured in requests: write-through is free but replicates server logic, a scoped invalidate costs exactly one, and every mis-scoped pattern — a deep infinite list, a keyless invalidate, raw websocket writes — explodes into a refetch storm. That gap is why you reach for the narrowest key that covers what changed."
  hue="var(--d-frontend)"
/>

<Quiz
  id="04-cache-invalidation-patterns-quiz-2"
  lessonSlug="04-cache-invalidation-patterns"
  lang="en"
  question="A realtime board receives websocket events for card changes and writes each event payload straight into the cache with setQueryData. Cards intermittently show stale titles and a missing computed &quot;blocked&quot; badge. What is the senior fix?"
  choices={[
    { label: "Treat events as signals, not data: on each event invalidate the entity's queryKey (debounced per key under high rates) so the cache refetches server truth — pushed payloads arrive out of order and omit server-computed fields, so writing them through reintroduces both problems", correct: true },
    { label: "Buffer events client-side and sort them by timestamp before applying", misconception: "Client-side ordering can't be made reliable — clocks skew, deliveries drop and retry, and the payload still lacks fields only the server computes (the blocked badge derives from other cards). You'd be rebuilding replication ordering, which the refetch-on-signal pattern sidesteps entirely." },
    { label: "Increase staleTime so the websocket writes are trusted longer", misconception: "staleTime governs when triggers like mount and focus refetch; it does nothing about corrupt writes you applied yourself. Trusting bad data longer makes the symptom worse — the badge stays missing until something else forces a refetch." },
    { label: "Replace the websocket with polling every 2 seconds", misconception: "Polling at that rate costs more requests than debounced invalidation and still adds up to 2 s of staleness. The push channel is the efficient part — the bug is only in using payloads as truth instead of as change notifications keyed to invalidation." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A mutation reconciles the cache along one of three paths: optimistic write with rollback, write-through from the server response, or invalidate-and-refetch"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "mutate", label: "mutate()", sub: "user action" },
    { id: "onmutate", label: "onMutate", sub: "cancel + snapshot + write" },
    { id: "server", label: "Server", sub: "accept / reject" },
    { id: "rollback", label: "Rollback", sub: "restore snapshot" },
    { id: "settle", label: "onSettled", sub: "invalidate key" },
    { id: "truth", label: "Refetch", sub: "server truth in cache" },
  ]}
  edges={[
    { from: "mutate", to: "onmutate", label: "optimistic path" },
    { from: "onmutate", to: "server", label: "request" },
    { from: "server", to: "rollback", label: "error" },
    { from: "server", to: "settle", label: "success or error" },
    { from: "settle", to: "truth", label: "reconcile" },
  ]}
  caption="Cancel kills in-flight refetches that would stomp the optimistic value; snapshot makes rejection recoverable; onSettled invalidation reconciles even correct guesses with server-computed truth. Scope invalidation keys tightly — a keyless invalidate on a 40-widget page is a self-inflicted refetch storm."
/>

<KeyTakeaway>Every write makes the cache wrong until you reconcile it, and the patterns form a dial. Invalidate: scoped prefix refetch, server truth, one extra request — never keyless. Write-through setQueryData: instant, free, but your updater replicates server logic and drifts silently — prefer patching with the server's response. Optimistic: cancel in-flight queries, snapshot, write, rollback on error, invalidate on settle — skip any step and you get flicker, stuck lies, or drift. Infinite queries refetch all pages on invalidation; websocket events should trigger debounced invalidation, not direct writes.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-cache-invalidation-patterns-retrieval"
  lang="en"
  questions={[
    { q: "Lay out the reconciliation dial after a mutation: the three strategies, what each costs, what each risks, and the production bug signature of getting each wrong.", a: "Strategy one, invalidate: mark matching keys stale and refetch active ones. Cost: one extra round trip and a brief stale window. Risk: under-invalidation — miss a dependent key and you get the classic 'created an item, success toast, item missing from the list until F5' (a list cached with staleTime serving old truth); over-invalidation — keyless or too-broad invalidate on a busy page refetches dozens of queries per mutation, the self-inflicted refetch storm that shows up as afternoon p99 spikes. Strategy two, write-through setQueryData: synchronous cache patch, zero requests, instant UI. Risk: your updater replicates server logic — insertion position under server sort, filter membership, server-computed fields like normalized currency or derived badges — and every divergence is a silent lie that persists until something else refetches. Signature: data that is subtly wrong (misordered row, missing computed field) rather than missing. Best practice: patch with the server's response object, not a client-side guess, or write-through then invalidate for instant-UI-plus-reconciliation. Strategy three, optimistic update: write before the server answers — perceived latency zero. Mandatory steps: await cancelQueries (or an in-flight refetch resolves after your write and reverts it — the 'checkbox fought me' flicker), snapshot previous data, setQueryData the guess, rollback to snapshot in onError (or rejections leave permanent false UI), invalidate in onSettled (or even correct guesses drift from server-computed truth). It composes badly with concurrent mutations on one entity — snapshots can capture other mutations' unconfirmed guesses — so heavy cases go through single-flight mutations." },
    { q: "How do invalidation costs change for infinite queries, and why is 'invalidate on event' rather than 'write the payload' the standard pattern for websocket-driven cache sync?", a: "Infinite queries store an array of pages behind one key, and invalidation refetches every loaded page sequentially to keep cursors consistent — a user 10 pages deep pays 10 serial round trips per invalidation, so a mutation pattern that casually invalidates the infinite list turns one row edit into a multi-second refetch cascade. Mitigations: maxPages to cap retained pages, targeted setQueryData to patch the affected row in place for single-entity edits, and keepPreviousData via placeholderData on ordinary paginated queries so page transitions show the previous page instead of a spinner. For websocket sync, the temptation is to treat each pushed payload as truth and setQueryData it in. That fails three ways: ordering — events arrive out of order under reconnects and retries, so an older payload can overwrite a newer state; completeness — payloads typically omit server-computed fields (a 'blocked' badge derived from other entities, normalized values), so written-through entries are subtly incomplete; duplication — every entity type needs hand-written merge logic that shadows the server's. The standard pattern inverts it: the event is a change notification, not data — on event, invalidateQueries the entity's key and let the refetch fetch complete, ordered, server-computed truth. One source of truth remains the API. At high event rates, debounce invalidations per key — 50 events a second collapse into a few refetches — and for genuinely high-frequency numeric streams (tickers, cursors) bypass the query cache entirely and feed a dedicated realtime store, because a request-response cache is the wrong tool for sub-second push data." },
  ]}
/>

<Recap lang="en">
A mutation makes every cached read of that data wrong, and this lesson is the catalogue of reconciliation strategies. Invalidation is truth by refetch: `invalidateQueries` prefix-matches a scoped key, marks entries stale, refetches active ones — its failure modes are under-invalidation (the created invoice missing from a five-minute-fresh list until F5) and over-invalidation (a keyless invalidate turning one checkbox into a 40-request storm and afternoon p99 spikes). Write-through `setQueryData` is truth by replication: instant and request-free, but the updater must mirror server logic — sort position, filter membership, computed fields — and every divergence lies silently, so prefer patching with the server's actual response or following the write with an invalidate. Optimistic updates move the write before the server's answer and are a three-step contract in `onMutate`: await `cancelQueries` so an in-flight refetch cannot stomp the optimistic value (the tell-tale flicker), snapshot for rollback in `onError` so rejection cannot leave permanent false UI, and invalidate in `onSettled` so even correct guesses reconcile with server-computed truth. Dependent queries via `enabled` are chosen waterfalls — audit them like `await` chains. Infinite queries refetch all loaded pages sequentially on invalidation, so cap pages and patch rows in place. Websocket events are signals, not data: invalidate the entity's key (debounced under load) instead of writing payloads through, because pushed payloads arrive unordered and incomplete. The dial runs from "refetch everything" to "patch exactly this" — requests on one end, correctness risk on the other. Now when you write a mutation's onSuccess, you reach for the narrowest key that covers what actually changed — and when you see a flicker or a stale list after a save, you know which of the three steps to audit first.
</Recap>
