---
concepts:
- react-fetch-effects
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-fetching-in-effects"
sources:
- https://react.dev/learn/synchronizing-with-effects
- https://react.dev/reference/react/useEffect
- https://react.dev/learn/you-might-not-need-an-effect
spiral: []
status: ready
summary: 'Fetching in useEffect is the honest baseline: an effect keyed on params, three states of boilerplate, and two structural bugs — races, because responses return out of order, and waterfalls, because the component tree serializes requests. Data libraries exist to fix exactly these.'
title: 'Fetching in effects: races, waterfalls, and why the baseline is worth knowing'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The bug report came with a screen recording: a user types "warsaw" into the flight search, the correct list flashes for half a second, then the page silently replaces it with flights to "wa" — every city in Washington state. No error, no crash, and it reproduced maybe one time in ten, always on the office Wi-Fi, never on the developer's fiber connection. The fetch lived in a `useEffect` keyed on the query, exactly like the tutorial showed. What the tutorial didn't show: the request for "wa" hit a cold cache and took 900 ms, the request for "warsaw" hit a warm one and returned in 120 ms, and the effect's `setResults(data)` happily committed whichever response arrived *last*. The network had reordered the answers, and the component had no idea which question each answer belonged to. The fix was four lines — an `ignore` flag in the effect's cleanup — and the team spent the rest of the sprint finding the same race in eleven other components. Fetching in effects is not wrong; it is the baseline. But the baseline has two structural failure modes, and you are about to meet both.
</Hook>

<Crux>Effect fetches race: the last response wins, not the last request. Guard with ignore/abort — and tree-driven fetching makes waterfalls.</Crux>

<Explanation>

## The honest baseline: an effect, a param, three states

A fetch-in-effect is a synchronization: "while this component shows query *q*, the screen should hold the server's answer for *q*." The effect re-runs whenever `query` changes, and you hand-write the three states every data UI needs — loading, error, data:

```jsx
function FlightSearch({ query }) {
  const [results, setResults] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    let ignore = false;                    // race guard
    const controller = new AbortController(); // network cancel
    setLoading(true);
    setError(null);

    fetch(`/api/flights?q=${encodeURIComponent(query)}`, {
      signal: controller.signal,
    })
      .then((res) => {
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        return res.json();
      })
      .then((data) => {
        if (!ignore) { setResults(data); setLoading(false); }
      })
      .catch((err) => {
        if (err.name === "AbortError") return; // expected on cleanup
        if (!ignore) { setError(err); setLoading(false); }
      });

    return () => {            // runs before re-run and on unmount
      ignore = true;
      controller.abort();
    };
  }, [query]);
  // render loading / error / results...
}
```

That is roughly twenty lines per endpoint, and every line earns its place. The shape to internalize: the effect body *starts* a request tied to one value of `query`; the cleanup *disowns* it. React guarantees cleanup runs before the next effect body and on unmount — that ordering is the entire correctness story.

## The race: responses do not return in the order you sent them

Type "w", "wa", "war" quickly and you issue three requests. HTTP gives no ordering guarantee across requests — caches, retries, and server load reorder them freely. Without a guard, each response calls `setResults`, and the *slowest* request paints last. The two guards do different jobs. The **`ignore` flag** is correctness: a closure variable per effect run; when cleanup flips it, the stale closure's `setResults` becomes a no-op. The response still downloads — you just refuse to commit it. **`AbortController`** is efficiency: it cancels the network work itself, freeing the browser's per-origin connection slots (HTTP/1.1 allows 6) and the server's time. Abort alone is *almost* enough — the rejected promise exits via `AbortError` — but the ignore flag also protects non-abortable async work after the fetch (a `res.json()` already in flight, a chained transform), so the pair is the idiom. StrictMode runs setup→cleanup→setup on dev mount precisely to prove your cleanup actually disowns the first request: if you see doubled commits in dev, the production race is real.

<Quiz
  id="01-fetching-in-effects-quiz-1"
  lessonSlug="01-fetching-in-effects"
  lang="en"
  question="A search effect uses only AbortController, no ignore flag. The query changes from &quot;wa&quot; to &quot;warsaw&quot;; cleanup aborts the first request. Is the race fully closed?"
  choices={[
    { label: "Mostly — the aborted fetch rejects with AbortError and commits nothing; but async steps that are not tied to the signal (an in-flight res.json(), a chained transform) can still resolve and call setState, so the ignore flag remains the airtight guard", correct: true },
    { label: "Yes — abort guarantees no code after the fetch ever runs", misconception: "abort() rejects the fetch promise, but only the parts wired to that signal. If the response headers already arrived and you called res.json(), or you chained work that isn't signal-aware, those promises can still resolve after cleanup and commit stale state. The ignore flag covers everything the signal can't reach." },
    { label: "No — AbortController only works for POST requests", misconception: "AbortController works for any fetch regardless of method. The gap isn't the HTTP verb; it's that abort only cancels work attached to the signal, while an ignore flag neutralizes every code path of the stale effect run, abortable or not." },
    { label: "Yes, as long as the server processes requests in order", misconception: "The server's processing order is irrelevant and not guaranteed anyway — caches, connection reuse, and load make responses arrive in any order. Client-side correctness can't depend on server-side ordering; the guard has to live in the effect's cleanup." },
  ]}
/>

## Waterfalls: the component tree becomes your network schedule

The second structural problem is invisible in any single component. `ProfilePage` fetches the user, renders `<Posts userId={...}>` only after the user arrives, and `Posts` fetches posts, then renders `<Comments>` which fetches again. Each fetch starts only after the parent's data has landed and the child has *mounted* — so three logically independent requests run in series. At 200 ms per round trip that is 600 ms of pure structure-imposed latency before counting server time; on a 75 ms-RTT LTE connection with TLS setup, real traces routinely show 1.5–2 s waterfalls on pages that could load in one round trip. The network panel signature is unmistakable: a staircase. The tree decided your schedule, and the tree knows nothing about the network. Cures, in increasing order of ambition: hoist the fetches to the highest component that knows all the parameters and run them with `Promise.all`; start fetches *before* rendering (in the route loader or event handler) and pass promises down; or hand the problem to a library or framework whose cache decouples "who renders this" from "who fetches this". That last move is the subject of the rest of this unit.

<Quiz
  id="01-fetching-in-effects-quiz-2"
  lessonSlug="01-fetching-in-effects"
  lang="en"
  question="ProfilePage fetches a user, then renders Posts which fetches posts, which renders Comments which fetches comments. RTT is 200 ms. What's the minimum structure-imposed delay before comments start downloading, and why?"
  choices={[
    { label: "About 400 ms: each child's effect can only start after its parent's data committed and the child mounted, so the three independent requests serialize — comments wait two full round trips that parallel fetching would have eliminated", correct: true },
    { label: "Zero — React runs all effects in parallel at mount", misconception: "React does run effects of mounted components together, but Posts and Comments aren't mounted yet — they render only after the parent's data arrives. The serialization isn't in the effect scheduler; it's in the tree shape gating when each component exists at all." },
    { label: "About 200 ms total, because the browser pipelines the requests", misconception: "The browser can only pipeline requests it has been given. Each fetch here is issued by a component that doesn't exist until the previous response lands, so nothing is in the pipe to parallelize — the staircase is created before the network layer ever sees it." },
    { label: "600 ms, because every useEffect waits one extra render cycle", misconception: "Effects fire after paint, which adds milliseconds, not round trips. The 400 ms figure comes from two upstream fetch+commit cycles gating the third request; the effect timing itself is a rounding error next to the serialized network hops." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="One effect run owns one request: cleanup disowns it on param change, while tree-gated children turn independent fetches into a waterfall"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "mount", label: "Effect runs", sub: "query = \"wa\"" },
    { id: "req1", label: "Request A", sub: "in flight" },
    { id: "change", label: "query changes", sub: "\"warsaw\"" },
    { id: "cleanup", label: "Cleanup", sub: "ignore + abort" },
    { id: "req2", label: "Request B", sub: "commits state" },
    { id: "waterfall", label: "Child mounts late", sub: "serial fetch chain" },
  ]}
  edges={[
    { from: "mount", to: "req1", label: "start" },
    { from: "change", to: "cleanup", label: "before re-run" },
    { from: "cleanup", to: "req1", label: "disown" },
    { from: "change", to: "req2", label: "new effect" },
    { from: "req2", to: "waterfall", label: "data gates child" },
  ]}
  caption="The ignore flag makes the stale closure's setState a no-op; AbortController cancels the network work itself. Children that fetch on mount start only after parent data commits — three independent resources become a 3-step staircase."
/>

<KeyTakeaway>An effect fetch is a synchronization with two built-in failure modes. Races: HTTP responses return in any order, so every effect run must disown its request in cleanup — ignore flag for correctness, AbortController for canceling the actual network work. Waterfalls: components that fetch on mount serialize logically parallel requests down the tree; hoist fetches, start them before render, or adopt a cache. The ~20 lines of loading/error/race boilerplate per endpoint are exactly what query libraries sell back to you.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-fetching-in-effects-retrieval"
  lang="en"
  questions={[
    { q: "Explain the fetch-in-effect race mechanically: where does the stale write come from, and what exactly do the ignore flag and AbortController each contribute?", a: "Each effect run closes over one value of the dependency (one query) and starts one request. HTTP gives no cross-request ordering: a cold-cache request for \"wa\" can take 900 ms while a warm one for \"warsaw\" returns in 120 ms, so without a guard the .then of the older request runs last and setState commits an answer to a question the UI is no longer asking. React's guarantee — cleanup runs before the next effect body and on unmount — is the hook for the fix. The ignore flag is a per-run closure variable: cleanup flips it, so the stale run's setState becomes a no-op; this is the correctness guard, and it covers every async step of that run, including non-abortable ones like an in-flight res.json() or chained transforms. AbortController is the efficiency guard: controller.abort() rejects the fetch with AbortError and actually cancels the network work, freeing per-origin connection slots (6 on HTTP/1.1) and server time. Abort alone misses async work not wired to the signal, ignore alone wastes bandwidth — production code uses both. StrictMode's setup→cleanup→setup on dev mount is a free test that the cleanup truly disowns the first request." },
    { q: "A page shows a 4-step staircase in the network panel. Why does fetching in effects produce this shape, what does it cost, and what are three escalating fixes?", a: "The staircase is the component tree acting as a network scheduler. A child that fetches in its mount effect cannot start until it mounts, and it mounts only after its parent's data has arrived and committed — so each tree level adds a full fetch+commit cycle before the next request is even issued. Four levels at 200 ms RTT is ~600 ms of structure-imposed serial latency before any server processing; on mobile RTTs the same shape stretches to multiple seconds for data that is logically parallel. The browser cannot help because it never sees the later requests until earlier responses land. Fixes, in escalating order: (1) hoist — move all fetches into the highest component that knows the parameters and issue them together with Promise.all, passing data down as props; (2) fetch-before-render — start requests outside the component lifecycle (route loaders, event handlers) so rendering and fetching overlap instead of alternating; (3) adopt a cache layer (a query library or framework data APIs) that decouples \"which component renders the data\" from \"when the request starts\", enabling preloading and deduplication. The deeper lesson: where data dependencies are real (posts genuinely need user.id), the waterfall is essential; where they're an artifact of component structure, it's pure waste." },
  ]}
/>

<Recap lang="en">
Fetching in `useEffect` is the baseline pattern every improvement is measured against: an effect keyed on the request parameters starts a fetch, hand-rolled `loading`/`error`/`data` state covers the three UI moments, and cleanup — guaranteed to run before the next effect body and on unmount — is where correctness lives. The first structural bug is the race: HTTP responses return in any order, so the slowest request paints last unless each effect run disowns its request in cleanup; the `ignore` flag turns the stale closure's `setState` into a no-op (covering even non-abortable steps like an in-flight `res.json()`), while `AbortController` cancels the actual network work and frees connection slots — production code pairs them, and StrictMode's dev setup→cleanup→setup cycle smoke-tests the guard. The second structural bug is the waterfall: components that fetch on mount can only start after their parents' data committed, so logically parallel requests serialize down the tree — three levels at 200 ms RTT is 600 ms of latency the network panel draws as a staircase. Hoisting fetches to a common ancestor with `Promise.all`, starting requests before render, or adopting a cache that separates fetching from rendering are the escalating cures. Twenty lines of boilerplate per endpoint, two systemic failure modes — that is the honest cost the rest of this unit's tools exist to repay.
</Recap>
