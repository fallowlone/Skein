infographic chart-column-simple
data
  title Green locally, red in CI: render vs the waitFor budget
  desc Milliseconds — the 1.3 s CI render breaches the 1 s default; the 90 ms laptop render never does
  values
    - label Laptop render
      value 90
    - label waitFor budget
      value 1000
    - label CI render (2-vCPU)
      value 1300
theme
  palette #14b8a6 #f59e0b #f43f5e
