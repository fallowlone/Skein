---
concepts:
- frequency_map
- frequency_counting
- anagram
estMin: 18
lang: en
mathPrereqs: []
order: 2
prereqs:
- 01-hash-maps-and-sets
slug: 02-frequency-counting
sources:
- https://algomaster.io/learn/dsa/frequency-counting-introduction
- https://teddysmith.io/frequency-counters-algorithm-pattern/
- https://medium.com/@mrinmayeerane2810/how-to-solve-the-anagram-problem-a-simple-guide-1d780ebd6d48
status: ready
summary: 'Count how many times each value appears using a hash map: one O(n) pass
  builds a frequency tally. Solve anagrams, find duplicates, and group by count.'
title: Frequency counting
track: algorithms
unit: 05-hashing
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You have a string "mississippi" and you want to know: how many times does each letter appear? One approach: for each letter, scan the string and count. That is O(n²) — n letters, each requiring a scan. But here is the key insight: **traverse the string once, and as you go, tally each letter in a hash map.** When you reach the end, your hash map contains the full frequency count. One pass, O(n) time. This is the frequency counting pattern, and it is the most common use of hash maps.
</Hook>

<Goal lang="en">
After this lesson you can build a frequency map in one O(n) pass, use a frequency map to solve "is this an anagram?", find the most common element in an array, detect non-repeating elements, understand why a fixed-size alphabet allows an array-based frequency table, and recognize that frequency counting replaces the O(n²) "scan and count for each element" approach.
</Goal>

<Idea lang="en">
**The problem we are solving:**

You are asked: count how many times each letter appears in "hello". One method:

```js
function countNaive(str) {
  // For each letter, scan the string and count
  for (let i = 0; i < str.length; i++) {
    let count = 0;
    for (let j = 0; j < str.length; j++) {
      if (str[i] === str[j]) count++;
    }
    console.log(str[i] + ": " + count);  // h: 1, e: 1, l: 2, l: 2, o: 1
  }
}
```

This is O(n²): for each of n letters, you scan the entire string. For large inputs, this is wasteful.

**The frequency counting idea:**

Instead, traverse the string once. For each letter you encounter, update its count in a hash map:

```js
function countFreq(str) {
  const freq = new Map();  // frequency map: letter → count
  
  for (let char of str) {
    // Increment the count for this character
    freq.set(char, (freq.get(char) ?? 0) + 1);
  }
  
  // freq now contains: h→1, e→1, l→2, o→1
  return freq;
}
```

One loop, O(n) time. One hash map, O(k) space where k is the number of distinct letters.

**Why it works:**

- **First pass:** traverse each element once. Add to map if new, increment if seen before.
- **O(1) updates:** `freq.set()` and `freq.get()` are O(1) on average.
- **Result:** after one pass, the map is complete.

**Hash map vs. array for small alphabets:**

If the keys are a small fixed range (e.g., lowercase letters a–z), you can use a plain array instead of a hash map:

```js
function countFreqArray(str) {
  const freq = new Array(26).fill(0);  // 26 slots for a-z
  
  for (let char of str) {
    const index = char.charCodeAt(0) - 'a'.charCodeAt(0);  // 'a'→0, 'b'→1, etc.
    freq[index]++;
  }
  
  // freq[0] is count of 'a', freq[1] is count of 'b', etc.
  return freq;
}
```

Array access is O(1) and slightly faster than hash map operations (no hashing overhead). Both work; the array is optimized when the keys are small and predictable.

**Common uses of frequency maps:**

1. **Anagrams:** two strings are anagrams if they have the same character frequencies.
2. **Duplicates and non-repeating:** find elements that appear exactly once or more than once.
3. **Most/least common:** track the element with the highest or lowest count.
4. **Group by count:** organize elements by frequency.
</Idea>

<DiagramFrame label="Frequency counting in one pass. On the left, the input sequence a, b, a, c, a is drawn as five boxes read left to right. A single sweep arrow underneath, labelled one pass, O(n), moves across all five boxes. Each box feeds an arrow into a key-to-count table on the right that accumulates: the row for a is hit by the 1st, 3rd, and 5th boxes and reaches count 3; the row for b is hit once and reads count 1; the row for c is hit once and reads count 1. The final table reads a maps to 3, b maps to 1, c maps to 1." hue="var(--d-systems)" caption="One left-to-right pass over the input. Each element increments its own key in the map: a is seen 3 times, b once, c once. After the single O(n) pass the count-by-key map is complete: a to 3, b to 1, c to 1.">
  <svg viewBox="0 0 720 320" width="720" height="320" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="120" y="28" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--muted)">input sequence (scanned once)</text>
    <text x="560" y="28" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--muted)">key {"->"} count map</text>
    <rect x="40" y="48" width="48" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="64" y="78" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">a</text>
    <rect x="100" y="48" width="48" height="48" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="124" y="78" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">b</text>
    <rect x="160" y="48" width="48" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="184" y="78" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">a</text>
    <rect x="220" y="48" width="48" height="48" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="244" y="78" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">c</text>
    <rect x="280" y="48" width="48" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="304" y="78" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">a</text>
    <line x1="40" y1="118" x2="320" y2="118" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#sweep)" />
    <text x="180" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">one pass, O(n)</text>
    <path d="M88 72 L188 72 L300 168" fill="none" stroke="var(--d-systems)" stroke-width="1.4" stroke-dasharray="4 4" marker-end="url(#feed)" opacity="0.5" />
    <path d="M148 72 L420 224" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 4" marker-end="url(#feed)" opacity="0.5" />
    <path d="M328 72 L420 280" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 4" marker-end="url(#feed)" opacity="0.5" />
    <rect x="440" y="56" width="220" height="36" rx="4" fill="var(--card-2)" stroke="var(--hairline-2)" stroke-width="1" />
    <text x="466" y="80" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">key</text>
    <text x="600" y="80" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">count</text>
    <rect x="440" y="92" width="220" height="44" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="466" y="120" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">a</text>
    <text x="520" y="120" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--muted)">{"|||"}</text>
    <text x="600" y="121" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--d-systems)">3</text>
    <rect x="440" y="136" width="220" height="44" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="466" y="164" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">b</text>
    <text x="520" y="164" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--muted)">{"|"}</text>
    <text x="600" y="165" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <rect x="440" y="180" width="220" height="44" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="466" y="208" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">c</text>
    <text x="520" y="208" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--muted)">{"|"}</text>
    <text x="600" y="209" text-anchor="middle" font-size="18" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="550" y="262" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">result: {"{a:3, b:1, c:1}"}</text>
    <text x="550" y="286" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">each element increments its own key, in O(1)</text>
    <defs>
      <marker id="sweep" markerWidth="9" markerHeight="9" refX="6" refY="4.5" orient="auto"><path d="M1 1 L7 4.5 L1 8" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
      <marker id="feed" markerWidth="8" markerHeight="8" refX="5" refY="4" orient="auto"><path d="M1 1 L6 4 L1 7" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" /></marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="en">
**1. Building a frequency map:**

```js
function frequency(arr) {
  const freq = new Map();
  for (let item of arr) {
    freq.set(item, (freq.get(item) ?? 0) + 1);
  }
  return freq;
}

const counts = frequency([5, 2, 5, 9, 5, 3, 2]);
// counts: 5→3, 2→2, 9→1, 3→1
console.log(counts.get(5));  // 3
```

**2. Checking if two strings are anagrams:**

Two strings are anagrams if they contain the same characters with the same frequencies.

```js
function isAnagram(s, t) {
  if (s.length !== t.length) return false;
  
  const freq = new Map();
  
  // Count characters in s
  for (let char of s) {
    freq.set(char, (freq.get(char) ?? 0) + 1);
  }
  
  // Decrement for characters in t
  for (let char of t) {
    if (!freq.has(char)) return false;
    freq.set(char, freq.get(char) - 1);
    if (freq.get(char) < 0) return false;
  }
  
  // All counts should be 0
  for (let count of freq.values()) {
    if (count !== 0) return false;
  }
  
  return true;
}

console.log(isAnagram("listen", "silent"));  // true
console.log(isAnagram("hello", "world"));    // false
```

**3. Finding the first non-repeating character:**

```js
function firstNonRepeating(str) {
  const freq = new Map();
  
  // Count all characters
  for (let char of str) {
    freq.set(char, (freq.get(char) ?? 0) + 1);
  }
  
  // Find the first character with count 1
  for (let char of str) {
    if (freq.get(char) === 1) {
      return char;
    }
  }
  
  return null;  // No non-repeating character
}

console.log(firstNonRepeating("abacabad"));  // 'c'
console.log(firstNonRepeating("aabbcc"));    // null
```

Try the examples:

<CodeRunner
  id="cr-frequency"
  lang="en"
  code={`function frequency(arr) {
  const freq = new Map();
  for (let item of arr) {
    freq.set(item, (freq.get(item) ?? 0) + 1);
  }
  return freq;
}

function isAnagram(s, t) {
  if (s.length !== t.length) return false;
  const freq = new Map();
  for (let char of s) {
    freq.set(char, (freq.get(char) ?? 0) + 1);
  }
  for (let char of t) {
    if (!freq.has(char)) return false;
    freq.set(char, freq.get(char) - 1);
    if (freq.get(char) < 0) return false;
  }
  for (let count of freq.values()) {
    if (count !== 0) return false;
  }
  return true;
}

function firstNonRepeating(str) {
  const freq = new Map();
  for (let char of str) {
    freq.set(char, (freq.get(char) ?? 0) + 1);
  }
  for (let char of str) {
    if (freq.get(char) === 1) return char;
  }
  return null;
}

const counts = frequency([5, 2, 5, 9, 5, 3, 2]);
console.log("Frequency of [5,2,5,9,5,3,2]:");
console.log("  5 appears", counts.get(5), "times");
console.log("  2 appears", counts.get(2), "times");

console.log("\\nAnagrams:");
console.log("  'listen' and 'silent':", isAnagram("listen", "silent"));
console.log("  'hello' and 'world':", isAnagram("hello", "world"));

console.log("\\nFirst non-repeating:");
console.log("  'abacabad':", firstNonRepeating("abacabad"));
console.log("  'aabbcc':", firstNonRepeating("aabbcc"));`}
/>
</Code>

<Trace lang="en">
Let us trace building a frequency map on the string "aabab":

<AlgoTrace
  id="trace-frequency"
  lang="en"
  code={[
    "function frequency(str) {",
    "  const freq = new Map();",
    "  for (let char of str) {",
    "    freq.set(char, (freq.get(char) ?? 0) + 1);",
    "  }",
    "  return freq;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: empty map, iterate over 'aabab'",
      line: 2,
      cells: []
    },
    {
      caption: "char = 'a': get('a') is undefined, so 0 + 1 = 1. Set a→1. freq = {a: 1}",
      line: 4,
      cells: ["a: 1"]
    },
    {
      caption: "char = 'a': get('a') is 1, so 1 + 1 = 2. Set a→2. freq = {a: 2}",
      line: 4,
      cells: ["a: 2"]
    },
    {
      caption: "char = 'b': get('b') is undefined, so 0 + 1 = 1. Set b→1. freq = {a: 2, b: 1}",
      line: 4,
      cells: ["a: 2", "b: 1"]
    },
    {
      caption: "char = 'a': get('a') is 2, so 2 + 1 = 3. Set a→3. freq = {a: 3, b: 1}",
      line: 4,
      cells: ["a: 3", "b: 1"]
    },
    {
      caption: "char = 'b': get('b') is 1, so 1 + 1 = 2. Set b→2. freq = {a: 3, b: 2}",
      line: 4,
      cells: ["a: 3", "b: 2"]
    },
    {
      caption: "Done. One pass, five operations. Final frequency: {a: 3, b: 2}.",
      line: 6,
      cells: [{ value: "a: 3", mark: "done" }, { value: "b: 2", mark: "done" }]
    }
  ]}
/>

Notice: we visited each character exactly once. Each `set` and `get` is O(1). Total time: O(n).
</Trace>

<Complexity lang="en">
**Frequency counting:**

| Operation | Time | Space |
|---|---|---|
| Build frequency map | O(n) | O(k) |
| Check if anagram | O(n) | O(k) |
| Find first non-repeating | O(n) | O(k) |
| Find most common element | O(n) | O(k) |

Where n is the size of the input (string length or array size) and k is the number of distinct values.

**Comparison: naive O(n²) vs. frequency counting O(n):**

- **Naive approach:** for each element, scan the entire input to count it. n × n = O(n²).
- **Frequency counting:** one pass to build the map, then use the map. O(n) + O(n) = O(n).

For n = 1000, naive is 1,000,000 operations. Frequency counting is 2,000. The difference grows fast.

**Space for the array-based variant:**

If the alphabet is fixed (e.g., lowercase letters, 26 values), the space is O(26) = O(1) constant. The frequency counting time is still O(n), but space is O(1) instead of O(k).
</Complexity>

<AlgoPractice
  id="practice-freq-1"
  lessonSlug="02-frequency-counting"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "Why does frequency counting count each element's occurrences in O(n) time instead of O(n²)?",
      choices: [
        "Because it uses a hash map to store counts in O(1) time, visiting each element once instead of n times",
        "Because it sorts the array first",
        "Because the elements are usually small",
        "Because O(n²) is impossible"
      ],
      answer: 0,
      hint: "Hash map operations (set/get) are O(1). One pass through the data visits each element once: O(n)."
    },
    {
      kind: "choice",
      prompt: "Two strings are anagrams if they contain the same characters with the same frequencies. Which approach is more efficient?",
      choices: [
        "Build one frequency map for both strings combined",
        "Build a frequency map for each string, then compare the maps",
        "Sort both strings and check if they are identical",
        "All approaches take the same time"
      ],
      answer: 1,
      hint: "Two separate frequency maps: build in O(n), compare in O(k). Sorting is O(n log n). Frequency is better."
    },
    {
      kind: "numeric",
      prompt: "In a string of length 100 with 8 distinct characters, how much space does a frequency map use?",
      answer: 8,
      hint: "The frequency map stores one entry per distinct character, not per element. 8 distinct characters = 8 entries in the map."
    },
    {
      kind: "choice",
      prompt: "When counting character frequencies in a string of lowercase letters only (a–z), which data structure is fastest?",
      choices: [
        "A hash map: O(1) per operation",
        "An array of size 26: O(1) per operation and no hashing overhead",
        "A linked list: O(1) per operation if you maintain pointers",
        "All are equally fast for this purpose"
      ],
      answer: 1,
      hint: "An array is simpler (direct indexing) and has no hashing cost. For a small fixed alphabet, array beats hash map on constants."
    },
    {
      kind: "choice",
      prompt: "To find the first non-repeating character in a string, do you need to build the frequency map first, or can you check while building?",
      choices: [
        "You can check while building",
        "You must build the entire map first, then scan again to find the first non-repeating",
        "Both approaches take the same time",
        "You need to sort the string first"
      ],
      answer: 1,
      hint: "You must build the complete frequency map first. Then iterate through the original string again to find the first character with count 1."
    }
  ]}
/>

<Inset kind="misconception" lang="en">
**"Frequency counting is only for strings."** Not true. Frequency counting works on any array of comparable values: integers, objects (if hashable), pairs, etc. The pattern is the same: one pass, count in a hash map, use the result.
</Inset>

<Inset kind="edgecase" lang="en">
**Empty or single-character input:** An empty string has no characters, so the frequency map is empty. A single-character string has one entry: the character with count 1. Always test edge cases.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-freq-1"
    pieceSlug="02-frequency-counting"
    lang="en"
    question="Why is the frequency counting pattern O(n) time, even though you might think 'I have to count each element' sounds like O(n²)?"
    choices={[
      {
        label: "Because you traverse the input once, and each hash map operation (set/get) is O(1) on average. Total: one loop of n iterations with O(1) work per iteration = O(n).",
        correct: true
      },
      {
        label: "Because the hash map automatically counts all elements without you iterating.",
        misconception: "You do iterate—one pass through the input. The hash map just stores and retrieves in O(1), not automatically."
      },
      {
        label: "Because O(n²) would require checking every pair of elements, and frequency counting does not do that.",
        misconception: "True, frequency counting avoids pairs. But the reason it's O(n) is the one-pass + O(1) operations combination."
      },
      {
        label: "Because sorting is O(n log n), and frequency counting is faster than sorting.",
        misconception: "Yes, frequency counting is faster than sorting. But the reason is one-pass + O(1) operations, not just speed comparison."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Frequency counting** is the practice of counting how many times each value appears in a dataset using a hash map.

Key facts:
1. **One-pass algorithm:** traverse the input once. For each element, increment its count in the map using `map.set(key, (map.get(key) ?? 0) + 1)`.
2. **Time complexity:** O(n), where n is the size of the input.
3. **Space complexity:** O(k), where k is the number of distinct values.
4. **Common uses:**
   - Check if two strings are anagrams (same character frequencies).
   - Find the first non-repeating element.
   - Detect elements that appear exactly once or more than once.
   - Find the most or least common element.
5. **Array optimization:** if the keys are a small fixed range (e.g., lowercase letters a–z), use an array instead of a hash map for O(1) space and slightly faster operations.
6. **Replaces O(n²):** the naive approach of scanning for each element is O(n²). Frequency counting solves the same problem in O(n).

You now have the most common hash map pattern. Next, you will explore collision handling and learn how hash functions distribute keys.
</Recap>
