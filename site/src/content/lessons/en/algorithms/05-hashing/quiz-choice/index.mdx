---
concepts:
- hash-function
- collision-resolution
- load-factor
- amortized-resize
- seen-pattern
- worst-case-degradation
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.cs.cornell.edu/courses/cs3110/2008fa/lectures/lec22_amort.html
status: ready
summary: "Multiple-choice synthesis across the hashing unit: hash functions, collisions, chaining, load factor, amortized resize, and the patterns built on hash maps and sets."
title: 'Hashing: multiple-choice review'
track: algorithms
unit: 05-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make when you reach for a hash map under interview pressure or in real code — not a definition to recite, but a tradeoff to defend.
</Hook>

<Goal lang="en">
Confirm you can connect the hash function, collision handling, load factor, and amortized resize to the everyday patterns — seen, frequency, grouping — that the individual lessons built toward.
</Goal>

<Quiz
  id="05-hashing-choice-1"
  lessonSlug="05-hashing"
  lang="en"
  question="A hash set gives O(1) average lookup. What single fact is that average actually resting on?"
  choices={[
    { label: "The hash function spreads keys roughly uniformly across buckets, so the load factor stays small and chains stay short. Average lookup is O(1 + alpha), which is O(1) only while alpha is a small constant.", correct: true },
    { label: "The set keeps its elements sorted, so lookup is a binary search rounded down to O(1).", misconception: "A hash set stores nothing in sorted order, and iteration is unordered. The O(1) comes from computing an index directly, not from any search over sorted data." },
    { label: "Hashing is O(1) by definition, so there is no condition to satisfy.", misconception: "O(1) is the AVERAGE under uniform spreading and a bounded load factor. With a bad hash function or adversarial keys it degrades to O(n) — the average is a claim about typical inputs, not a guarantee." },
    { label: "JavaScript optimises Set internally, so cost depends only on the engine, not on the keys.", misconception: "The engine helps with constants, but the asymptotic guarantee still rests on uniform hashing and a managed load factor. Pathological key distributions degrade any implementation." },
  ]}
/>

<Quiz
  id="05-hashing-choice-2"
  lessonSlug="05-hashing"
  lang="en"
  question="Two different keys hash to the same bucket index. Under separate chaining, what happens, and what does it cost?"
  choices={[
    { label: "Both key-value pairs live in that bucket's list; lookup hashes to the bucket then scans the short list for the matching key. Cost is O(1 + chain length), and chain length averages the load factor.", correct: true },
    { label: "The second key overwrites the first, since each bucket holds one entry.", misconception: "That would silently lose data. Chaining stores both — the bucket is a list, and lookup compares the full key, not just the index, so colliding keys coexist." },
    { label: "The table immediately doubles in size to give the second key its own bucket.", misconception: "A single collision does not trigger a resize. Resize is driven by the overall load factor crossing a threshold, not by any one collision; collisions are expected and handled in-place by the chain." },
    { label: "The lookup returns the wrong value because the index is shared.", misconception: "The index is shared but the stored key is not. Lookup scans the chain and matches on the actual key, so it returns the correct value despite the collision." },
  ]}
/>

<Quiz
  id="05-hashing-choice-3"
  lessonSlug="05-hashing"
  lang="en"
  question="A table holds 10 items in 5 buckets and never resizes. As you keep inserting without resizing, what happens to lookup cost, and why?"
  choices={[
    { label: "The load factor alpha = items / buckets keeps rising, so average chain length grows with it. Lookup is O(1 + alpha), so it drifts toward a linear scan. That is exactly why real tables resize when alpha crosses a threshold.", correct: true },
    { label: "Nothing changes; hashing is always O(1) regardless of how full the table gets.", misconception: "O(1) holds only while alpha is bounded. With buckets fixed and items growing, alpha grows without limit and chains lengthen — lookup degrades toward O(n)." },
    { label: "Lookup gets faster because more items means more cache hits.", misconception: "More items in fixed buckets means longer chains and more comparisons per lookup, not fewer. The load factor is the driver, and it is going up." },
    { label: "The keys start to collide less often as the table fills.", misconception: "Collisions get MORE frequent as alpha rises — fewer free buckets per item. A fuller table means more crowding, not less." },
  ]}
/>

<Quiz
  id="05-hashing-choice-4"
  lessonSlug="05-hashing"
  lang="en"
  question="Inserting into a hash table is called O(1) amortized, yet a single insert can cost O(n). How are both true at once?"
  choices={[
    { label: "Most inserts are O(1), but when alpha crosses the threshold the table doubles and rehashes every item — one O(n) insert. Because the table doubles, you resize only O(log n) times, and total resize work over n inserts is O(n), averaging O(1) per insert.", correct: true },
    { label: "Amortized means best case, and the best case is O(1); the O(n) resize is simply ignored.", misconception: "Amortized is not best case — it is the average over a sequence, and it explicitly INCLUDES the O(n) resizes. They are counted, then spread across the cheap inserts via the doubling geometry." },
    { label: "The resize is really O(1) because copying is fast in practice.", misconception: "Copying and rehashing n items is genuinely O(n) — every item is re-placed. Amortization does not make that step cheap; it makes it rare enough that the average stays O(1)." },
    { label: "Inserts are O(1) amortized only if you never delete.", misconception: "Deletion does not break the insert amortization. The doubling-on-growth argument stands on its own; the amortized bound is about the geometric resize schedule, not deletes." },
  ]}
/>

<Quiz
  id="05-hashing-choice-5"
  lessonSlug="05-hashing"
  lang="en"
  question="You must detect whether any value repeats in a stream and, separately, find two numbers that sum to a target. Which hashing pattern fits each?"
  choices={[
    { label: "Duplicate detection is the seen pattern: keep a hash set, check membership in O(1) before adding. Two-sum is the complement pattern: for each x, look up target - x in a hash map of values seen so far. Both are one O(n) pass.", correct: true },
    { label: "Both need a frequency map counting occurrences of every value.", misconception: "A frequency map works but is overkill. Membership needs only presence (a set), and two-sum needs a map from value to index of what you have already seen — neither needs full counts." },
    { label: "Both require sorting first, then a two-pointer scan.", misconception: "Sorting is O(n log n) and destroys the index information two-sum needs. The hash approaches are O(n) single passes and preserve original indices." },
    { label: "Duplicate detection needs a hash map; two-sum needs a hash set.", misconception: "It is the reverse pairing: presence-only detection wants a set, and complement lookup wants a map (value to index) so you can return the pair. A bare set cannot return the matching index for two-sum." },
  ]}
/>

<Quiz
  id="05-hashing-choice-6"
  lessonSlug="05-hashing"
  lang="en"
  question="A service hashes user-supplied strings into a table, and an attacker floods it with inputs all crafted to collide into one bucket. What is the failure, and what is the real fix?"
  choices={[
    { label: "All keys land in one chain, so every lookup degrades to O(n) and the endpoint stalls — an algorithmic-complexity DoS. The real fix is a randomized, seeded hash function (a per-process random seed) so the attacker cannot predict colliding inputs.", correct: true },
    { label: "There is no failure; chaining handles all collisions in O(1).", misconception: "Chaining handles collisions correctly but not cheaply when they all land in one bucket — the chain becomes length n and lookups are O(n). Correctness survives; performance collapses." },
    { label: "Just lower the load factor threshold so the table resizes more often.", misconception: "Resizing redistributes by the same deterministic hash, so adversarial keys recollide after the resize. A smaller threshold does not help when the attacker controls the hash inputs; you need an unpredictable seed." },
    { label: "Switch from chaining to open addressing to eliminate the attack.", misconception: "Open addressing changes how collisions are stored, not whether the attacker's keys collide. With a predictable hash they still cluster; the defense is a randomized hash, independent of the resolution scheme." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one chain of reasoning: a hash function turns a key into an index for O(1) average access; collisions are inevitable and chaining stores them in per-bucket lists; the load factor sets average chain length, so the table resizes — doubling and rehashing — to keep it bounded, O(n) per resize but O(1) amortized; and on top of this sit the everyday patterns — seen, complement, frequency, grouping. The same machinery degrades to O(n) when the hash is poor or adversarial, which is why a randomized hash matters for untrusted input.
</Recap>
