---
concepts:
- seen_set
- complement
- two_sum
estMin: 18
lang: en
mathPrereqs: []
order: 3
prereqs:
- 01-hash-maps-and-sets
slug: 03-the-seen-pattern
sources:
- https://neetcode.io/solutions/two-sum
- https://dev.to/ggorantala/two-sum-problems-4c9j
- https://neetcode.io/solutions/contains-duplicate
status: ready
summary: Keep a hash set of values you have seen. For each new element, check the
  set in O(1), then add it. One pass solves what O(n²) brute force once required.
title: The ''''seen'''' pattern
track: algorithms
unit: 05-hashing
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Imagine you are given an array [5, 2, 9, 11] and asked: do any two numbers add up to 14? A naive approach: for each number, scan the rest of the array to see if its complement is there. That is O(n²). But here is the key: as you traverse the array once, keep a set of numbers you have already seen. For each new number x, check whether (target - x) is in the set. If yes, you found the pair. If no, add x to the set. One pass, O(n) time. This is the **seen pattern**, and it transforms many O(n²) scans into one O(n) journey.
</Hook>

<Goal lang="en">
After this lesson you can build and use a "seen" hash set to solve two-sum on an unsorted array in O(n) time, understand why the seen pattern turns nested loops into single passes, detect duplicates using a seen set, find the intersection of two arrays, detect cycles in a linked list by remembering visited nodes, and recognize the shape: "have I seen something that completes the answer?" — store as you go, check as you go.
</Goal>

<Idea lang="en">
**The problem we are solving:**

You are given an array [5, 2, 11, 9] and a target sum of 14. Find two numbers that add up to 14.

One approach: nested loops.

```js
function twoSumNaive(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] + arr[j] === target) {
        return [i, j];  // Return indices
      }
    }
  }
  return null;  // No pair found
}
```

This is O(n²): for each element, you scan the rest of the array. For large inputs, this is slow.

**The seen pattern idea:**

Instead, traverse the array once. As you go, keep a hash set of numbers you have seen so far. For each number x, ask: is (target - x) in the set? If yes, you found the pair and you are done. If no, add x to the set and move on.

```js
function twoSumSeen(arr, target) {
  const seen = new Set();
  
  for (let num of arr) {
    const complement = target - num;
    
    // Have I seen the complement before?
    if (seen.has(complement)) {
      return [complement, num];  // Found the pair
    }
    
    // No, so add this number to the set and continue
    seen.add(num);
  }
  
  return null;  // No pair found
}
```

One loop, O(n) time. One set, O(n) space. The set lets you check "have I seen X?" in O(1).

**Why it works:**

- **First element:** add it to the set.
- **Second element:** check if its complement is in the set. If yes, done. If no, add it.
- **Third element, onwards:** same logic.

Because the set stores all elements seen so far, and set membership is O(1), the total time is O(n) with one pass.

**The seen pattern shape:**

The pattern is: **store as you go, check as you go.**

1. Iterate through the data.
2. For each element, ask: "have I seen something that completes the answer?"
3. If yes, you found it.
4. If no, remember this element and continue.

This shape works for:
- **Two-sum:** have I seen the complement?
- **Duplicate detection:** have I seen this value before?
- **Intersection:** have I seen this element in the first array?
- **First duplicate:** have I seen this value before (return it)?
- **Cycle detection:** have I visited this node before?

**Two-sum tradeoff: hash map vs. two-pointer.**

If the array is **unsorted**, the seen pattern (hash map) is O(n) time, O(n) space.

If the array is **sorted**, the two-pointer pattern is O(n) time, O(1) space — no extra memory. But sorting an unsorted array takes O(n log n), which is slower than hashing.

Choose the seen pattern for unsorted data. Choose two-pointer for sorted data.

</Idea>

<DiagramFrame
  label="The seen pattern scanning the array 5, 2, 9, 11 for two numbers that sum to target 14, left to right in one pass. The input row sits on top with a moving cursor. Below it the seen hash set starts empty and grows as the scan advances. Step 1: element 5, complement target minus 5 equals 9, the set is empty so 9 is not found, and 5 is added to the set. Step 2: element 2, complement 12 is not in the set holding 5, so 2 is added; the set now holds 5 and 2. Step 3: element 9, complement target minus 9 equals 5, and 5 IS already in the set — a lookup hit, drawn as an arrow from element 9 into the 5 chip in the set. The pair 5 plus 9 equals 14 is the answer, so the scan stops and element 11 is never reached. The set lookup is O(1) and there is a single pass, giving O(n) time and O(n) space."
  hue="var(--d-systems)"
  caption="One left-to-right pass. For each element x compute the complement target − x and ask the seen set: is it there? Empty set on 5 and 2 misses, so they are added. On 9 the complement 5 is already in the set — an O(1) hit — so 5 + 9 = 14 is returned and 11 is never scanned. Remember what you have seen for O(1) lookup: single pass, O(n) time, O(n) space."
>
  <svg viewBox="0 0 720 360" width="720" height="360" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="seen-arrow-en" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="40" y="30" text-anchor="start" font-size="13" font-weight="700" fill="var(--ink, currentColor)">{"input — scanned once, left to right    target = 14"}</text>

    <rect x="40" y="46" width="56" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="68" y="77" text-anchor="middle" font-size="20" font-weight="700" fill="var(--muted, #888)">5</text>
    <rect x="108" y="46" width="56" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="136" y="77" text-anchor="middle" font-size="20" font-weight="700" fill="var(--muted, #888)">2</text>
    <rect x="176" y="46" width="56" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="204" y="77" text-anchor="middle" font-size="20" font-weight="700" fill="var(--ink, currentColor)">9</text>
    <rect x="244" y="46" width="56" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" stroke-dasharray="5 4" />
    <text x="272" y="77" text-anchor="middle" font-size="20" font-weight="700" fill="var(--muted, #888)">11</text>

    <text x="204" y="116" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">cursor here</text>
    <text x="272" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">never reached</text>

    <text x="40" y="168" text-anchor="start" font-size="13" font-weight="700" fill="var(--d-systems)">seen set — grows as we scan (O(1) lookup)</text>
    <rect x="40" y="182" width="260" height="64" rx="10" fill="none" stroke="var(--d-systems)" stroke-width="1.5" stroke-dasharray="5 4" />

    <rect x="60" y="196" width="56" height="36" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="88" y="220" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">5</text>
    <rect x="128" y="196" width="56" height="36" rx="8" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="156" y="220" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">2</text>
    <text x="232" y="220" text-anchor="middle" font-size="13" fill="var(--muted, #888)">added on steps 1, 2</text>

    <path d="M204 94 Q204 150 100 192" fill="none" stroke="var(--d-systems)" stroke-width="2.5" marker-end="url(#seen-arrow-en)" />
    <text x="360" y="130" text-anchor="start" font-size="12" font-weight="600" fill="var(--d-systems)">{"look up complement = 14 − 9 = 5"}</text>
    <text x="360" y="150" text-anchor="start" font-size="12" font-weight="700" fill="var(--d-systems)">{"5 is in the set — HIT"}</text>

    <line x1="360" y1="174" x2="360" y2="318" stroke="var(--hairline-2, #888)" stroke-width="1" stroke-dasharray="4 4" />

    <text x="392" y="60" text-anchor="start" font-size="13" font-weight="700" fill="var(--ink, currentColor)">per element x:</text>
    <text x="392" y="84" text-anchor="start" font-size="12" fill="var(--muted, #888)">{"1. complement = target − x"}</text>
    <text x="392" y="106" text-anchor="start" font-size="12" fill="var(--muted, #888)">2. complement in seen?</text>
    <text x="392" y="126" text-anchor="start" font-size="12" fill="var(--muted, #888)">   yes → answer found, stop</text>
    <text x="392" y="146" text-anchor="start" font-size="12" fill="var(--muted, #888)">   no  → add x, keep scanning</text>

    <rect x="392" y="260" width="300" height="58" rx="10" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="542" y="284" text-anchor="middle" font-size="15" font-weight="700" fill="var(--ink, currentColor)">{"answer: 5 + 9 = 14"}</text>
    <text x="542" y="304" text-anchor="middle" font-size="11" fill="var(--muted, #888)">found in one pass, no nested loop</text>

    <text x="40" y="344" text-anchor="start" font-size="13" font-weight="700" fill="var(--ink, currentColor)">{"remember what you've seen → O(1) lookup, single pass → O(n) time, O(n) space"}</text>
  </svg>
</DiagramFrame>

<Code lang="en">
**1. Two-sum with a hash map (return indices):**

```js
function twoSum(nums, target) {
  const map = new Map();  // value → index
  
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    
    // Have I seen the complement before?
    if (map.has(complement)) {
      return [map.get(complement), i];  // Return the two indices
    }
    
    // No, so store this number and its index
    map.set(nums[i], i);
  }
  
  return null;  // No pair found
}

const result = twoSum([2, 11, 15, 3], 9);
console.log(result);  // [0, 1] — indices of 2 and 7? No, wait...
// Actually: 2+11=13, 2+15=17, 2+3=5, 11+15=26, 11+3=14, 15+3=18
// Hmm, no pair sums to 9. Try [2, 7, 11, 15], target 9:
// 2+7=9 ✓

const result2 = twoSum([2, 7, 11, 15], 9);
console.log(result2);  // [0, 1]
```

**2. Detect the first duplicate using a seen set:**

```js
function findFirstDuplicate(arr) {
  const seen = new Set();
  
  for (let num of arr) {
    if (seen.has(num)) {
      return num;  // Found the first duplicate
    }
    seen.add(num);
  }
  
  return null;  // No duplicates
}

console.log(findFirstDuplicate([1, 2, 3, 2, 4]));  // 2
console.log(findFirstDuplicate([1, 2, 3, 4]));     // null
```

**3. Find the intersection of two arrays:**

```js
function intersection(arr1, arr2) {
  const set1 = new Set(arr1);
  const result = new Set();
  
  // For each element in arr2, check if it was in arr1
  for (let num of arr2) {
    if (set1.has(num)) {
      result.add(num);  // Add to result (avoid duplicates with a set)
    }
  }
  
  return Array.from(result);
}

console.log(intersection([1, 2, 2, 1], [2, 2]));           // [2]
console.log(intersection([4, 9, 5], [9, 4, 9, 8, 4]));     // [9, 4] or [4, 9]
```

Try the two-sum example:

<CodeRunner
  id="cr-seen-twosum"
  lang="en"
  code={`function twoSum(nums, target) {
  const map = new Map();
  
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }
  
  return null;
}

function findFirstDuplicate(arr) {
  const seen = new Set();
  for (let num of arr) {
    if (seen.has(num)) return num;
    seen.add(num);
  }
  return null;
}

function intersection(arr1, arr2) {
  const set1 = new Set(arr1);
  const result = new Set();
  for (let num of arr2) {
    if (set1.has(num)) result.add(num);
  }
  return Array.from(result);
}

console.log("Two-sum [2, 7, 11, 15], target 9:");
console.log("  Result:", twoSum([2, 7, 11, 15], 9));

console.log("\\nTwo-sum [3, 3], target 6:");
console.log("  Result:", twoSum([3, 3], 6));

console.log("\\nFirst duplicate in [1, 2, 3, 2, 4]:");
console.log("  Result:", findFirstDuplicate([1, 2, 3, 2, 4]));

console.log("\\nIntersection of [1, 2, 2, 1] and [2, 2]:");
console.log("  Result:", intersection([1, 2, 2, 1], [2, 2]));`}
/>
</Code>

<Trace lang="en">
Let us trace the two-sum seen pattern on [2, 7, 11, 15] with target 9:

<AlgoTrace
  id="trace-twosum"
  lang="en"
  code={[
    "function twoSum(nums, target) {",
    "  const map = new Map();",
    "  for (let i = 0; i < nums.length; i++) {",
    "    const complement = target - nums[i];",
    "    if (map.has(complement)) {",
    "      return [map.get(complement), i];",
    "    }",
    "    map.set(nums[i], i);",
    "  }",
    "  return null;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: empty map. Target = 9. Iterate [2, 7, 11, 15].",
      line: 2,
      cells: []
    },
    {
      caption: "i=0, nums[0]=2. complement = 9-2 = 7. map.has(7)? No. Add 2→0. map = {2: 0}.",
      line: 8,
      cells: ["2: 0"]
    },
    {
      caption: "i=1, nums[1]=7. complement = 9-7 = 2. map.has(2)? Yes! Return [0, 1].",
      line: 6,
      cells: [{ value: "2: 0", mark: "target" }, { value: "7: found", mark: "done" }]
    }
  ]}
/>

Notice: we found the pair on the second element, without looping back. The map stores the first element's index, and we check it instantly via O(1) lookup.
</Trace>

<Complexity lang="en">
**The seen pattern:**

| Operation | Time | Space |
|---|---|---|
| Two-sum (hash map) | O(n) | O(n) |
| Detect first duplicate | O(n) | O(n) |
| Find intersection | O(n + m) | O(n) |
| Detect cycle (linked list) | O(n) | O(n) |

Where n and m are the sizes of the input(s).

**Comparison: nested loops vs. seen pattern:**

- **Nested loops:** for each element, scan the rest. n × n = O(n²).
- **Seen pattern:** one loop, O(1) lookups. O(n).

For n = 1000, nested loops: 1,000,000 operations. Seen pattern: 1,000. The difference grows fast.

**Why O(1) lookup matters:**

In the two-sum problem, the complement check must be instant. If you searched linearly (O(n) per check), the total would be O(n²) again. Hash maps give O(1) lookup, which is essential.
</Complexity>

<AlgoPractice
  id="practice-seen-1"
  lessonSlug="03-the-seen-pattern"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "Why does the seen pattern solve two-sum in O(n) time instead of O(n²)?",
      choices: [
        "Because it uses a sorted array",
        "Because the hash map stores all seen values, and complement lookup is O(1). One loop with O(1) checks = O(n).",
        "Because it sorts the array first",
        "Because O(n²) is impossible"
      ],
      answer: 1,
      hint: "Hash map operations (has/set) are O(1). One pass through the array with O(1) checks per element = O(n) total."
    },
    {
      kind: "choice",
      prompt: "In two-sum, why do we store the value and its index in the map, not just add it to a set?",
      choices: [
        "Because we need the indices of the pair, not just whether the pair exists",
        "Because sets are slower than maps",
        "Because the array might be sorted",
        "Because we need to count how many times each value appears"
      ],
      answer: 0,
      hint: "The problem asks for the two indices of the pair. A set does not store indices, but a map does (value → index)."
    },
    {
      kind: "choice",
      prompt: "If you use the seen pattern to find the first duplicate in [1, 2, 3, 2, 4], which number is returned?",
      choices: [
        "1",
        "2",
        "3",
        "4"
      ],
      answer: 1,
      hint: "As you iterate, 1 is new, 2 is new, 3 is new, 2 is seen! Return 2 immediately."
    },
    {
      kind: "choice",
      prompt: "For unsorted data, is the seen pattern (hash map) or two-pointer better, and why?",
      choices: [
        "Two-pointer, because it uses no extra space",
        "Seen pattern, because O(n) hash-based is faster than O(n log n) sort + two-pointer",
        "They are equally fast",
        "It depends on the language"
      ],
      answer: 1,
      hint: "Sorting unsorted data takes O(n log n). The seen pattern is O(n). For unsorted data, seen pattern wins."
    },
    {
      kind: "numeric",
      prompt: "If you check for duplicates in an array of 5000 elements using the seen pattern, how many hash operations (add/check) do you perform (approximately)?",
      answer: 5000,
      hint: "One add or check per element, as you iterate once through the array. 5000 elements = ~5000 operations."
    }
  ]}
/>

<Inset kind="misconception" lang="en">
**"The seen pattern is just for two-sum."** Not true. The seen pattern works for any problem where you ask "have I seen something before?" — duplicate detection, intersection, cycle detection, finding the first repeating element, and many more. The shape is universal: store as you go, check as you go.
</Inset>

<Inset kind="edgecase" lang="en">
**Empty or single-element input:** An empty array has no pairs, so return null. A single-element array cannot have a pair with another element, so return null. Always test these.

**Target not reachable:** If no pair sums to the target, the loop completes and you return null. The set contains all elements, but none form a pair.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-seen-1"
    pieceSlug="03-the-seen-pattern"
    lang="en"
    question="Why is the seen pattern O(n) while a nested loop is O(n²)?"
    choices={[
      {
        label: "The seen pattern iterates once and uses O(1) hash set lookups, so one pass with O(1) work per element = O(n). Nested loops check every pair, which is n pairs × O(n) work = O(n²).",
        correct: true
      },
      {
        label: "The seen pattern does not store any values.",
        misconception: "The seen pattern stores all values in a hash set. The key is that lookups are O(1), not that storage is avoided."
      },
      {
        label: "The seen pattern sorts the data first.",
        misconception: "The seen pattern does not sort. It uses a hash function to jump to values instantly, not order."
      },
      {
        label: "Nested loops are O(n²) because they use nested conditionals.",
        misconception: "Nested loops are O(n²) because they iterate twice (outer loop n times, inner loop up to n times). The conditionals are secondary."
      }
    ]}
  />
</Check>

<Recap lang="en">
**The seen pattern** is the practice of storing values you have encountered in a hash set or map as you traverse data, then checking instantly whether you have seen something before.

Key facts:
1. **Store as you go, check as you go:** for each element, check if it (or its complement) is in the set. If yes, you found your answer. If no, add it and continue.
2. **Time complexity:** O(n) because you iterate once and each check is O(1).
3. **Space complexity:** O(n) because the set stores up to n values.
4. **Common uses:**
   - Two-sum: have I seen the complement?
   - Duplicate detection: have I seen this value?
   - Intersection: have I seen this in the first array?
   - First duplicate: have I seen this before?
   - Cycle detection: have I visited this node?
5. **Replaces O(n²):** the naive approach of nested loops. Seen pattern solves it in one pass.
6. **Trade memory for speed:** O(n) space buys O(n) time instead of O(n²).

You now have a powerful pattern that applies to many real problems. Next, you will explore how collisions in hash functions affect performance, and dive deeper into hash function design.
</Recap>
