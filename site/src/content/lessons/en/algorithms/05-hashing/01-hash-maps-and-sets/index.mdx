---
concepts:
- hash_function
- hash_map
- hash_set
- hash_table
- collision
estMin: 20
lang: en
mathPrereqs: []
order: 1
prereqs:
- 02-arrays-strings/01-the-array
slug: 01-hash-maps-and-sets
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.freecodecamp.org/news/what-is-a-hash-map/
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set
status: ready
summary: A hash set answers ''''is X present?'''' in O(1) time. A hash map stores
  key–value pairs and looks up by key in O(1). Both trade memory for speed using a
  hash function to jump straight to the answer.
title: Hash maps and hash sets
track: algorithms
unit: 05-hashing
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import StructureFigure from "~/components/algo/StructureFigure.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
In the time-and-space lesson, you learned that checking "is value X in the array?" with a nested loop costs O(n²) time. Then you learned that a Set—an unordered collection of unique values—can do it in O(n) time and O(n) space, a time–space tradeoff. But there is something faster: a hash set. It answers "is X present?" in O(1) average time, without any loop. The secret is a hash function that acts like a key to a filing cabinet: it turns the value into a drawer number, and you look there instantly.
</Hook>

<Goal lang="en">
After this lesson you can explain how a hash function maps a key to an array index, understand why hash maps and sets give O(1) average lookup, build a simple hash set or hash map using a JavaScript Set or Map, solve "does the array contain a duplicate?" using a hash set in O(n) time, and recognize the tradeoff: O(1) average time costs memory and does not preserve order.
</Goal>

<Idea lang="en">
**The problem we are solving:**

Imagine a phonebook. You have 1 million names and phone numbers. If you store them in an array and search for "Alice", you scan from the start: Alice? No. Bob? No. Charlie? No. ... — O(n) time.

But a phonebook is organized alphabetically. You jump to the "A" section, then scan just a few names. Still linear, but faster in practice.

Can we do better? **Yes, if we can jump straight to the answer without scanning at all.** That is what hashing does.

**The idea of hashing:**

A **hash function** is a function that takes a key (a name, a number, a word) and returns an **index** — a number between 0 and n-1, where n is the size of the array. The idea is clever: instead of scanning an array, use the hash function to **calculate** where the data should be, then look there instantly.

```js
function hash(key, arraySize) {
  // Turn the key into a number between 0 and arraySize - 1
  return someFormula(key) % arraySize;
}

const index = hash("Alice", 1000);  // Returns, say, 342
// Look at position 342 — Alice's phone number is there (ideally)
```

In a good hash function, different keys map to different indices. "Alice" always maps to 342. "Bob" might map to 157. "Charlie" to 899. The array has a bucket at each index, and the key–value pair sits in that bucket.

**Why O(1)?** Calculate the index (one formula) and look at that index (one operation). Done. No loop.

**Hash set vs. hash map:**

A **hash set** (or unordered set) stores only values — it answers "is X in the set?". It uses a hash function to map each value to an array index, then stores the value (or a marker) at that index.

<AnnotatedCode
  code={`const mySet = new Set();
mySet.add(10);
mySet.add(20);
mySet.add(30);

console.log(mySet.has(20));  // true, found instantly via hash
console.log(mySet.has(99));  // false, looked and not there`}
  annotations={[
    { line: 1, text: "Create an empty set" },
    { line: 2, text: "Hash 10, store at resulting index" },
    { line: 6, text: "Hash 20, check index, found in O(1)" },
    { line: 7, text: "Hash 99, check index, not found, still O(1)" }
  ]}
/>

A **hash map** (or dictionary) stores key–value pairs. For each key, it calculates an index using a hash function, then stores both the key and the value at that index. Lookups by key are O(1) on average.

<AnnotatedCode
  code={`const myMap = new Map();
myMap.set("Alice", "555-1234");
myMap.set("Bob", "555-5678");

console.log(myMap.get("Alice"));  // "555-1234", found in O(1)
console.log(myMap.get("Charlie"));  // undefined, not there, still O(1)`}
  annotations={[
    { line: 1, text: "Create an empty map" },
    { line: 2, text: "Hash 'Alice', store key–value pair at resulting index" },
    { line: 5, text: "Hash 'Alice', jump to index, retrieve value" }
  ]}
/>

**Core operations on a hash set or map:**

1. **Add/set by key** — Hash the key, jump to the index, place the value. **O(1) average**.
2. **Lookup/contains** — Hash the key, jump to the index, check if it is there. **O(1) average**.
3. **Delete by key** — Hash the key, jump to the index, remove it. **O(1) average**.

The word "average" is important: in a badly designed hash function or under heavy collisions (see the edge case), performance can degrade. But with a good hash function and reasonable load factor (number of items / array size), O(1) average is reliable.

**The tradeoff:**

- **Memory cost:** A hash set or map uses extra memory—an array to hold the buckets, usually with spare capacity to keep collisions rare. You spend memory to save time.
- **No order:** Iteration over a hash set or map does not follow any predictable order (except that JavaScript Maps iterate in insertion order, a concession to developer expectations).
- **Keys must be hashable:** In some languages, only certain types can be keys (immutable types in Python, for example). JavaScript is more forgiving: you can hash any value.
</Idea>

<Code lang="en">
**Building a set — "does the array contain a duplicate?"**

Recall from time-and-space: the nested-loop version is O(n²).

```js
function hasDuplicateNested(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] === arr[j]) return true;
    }
  }
  return false;
}
```

With a hash set, we do one pass and O(1) lookups:

```js
function hasDuplicateSet(arr) {
  const seen = new Set();
  for (let num of arr) {
    if (seen.has(num)) return true;  // O(1) lookup
    seen.add(num);                   // O(1) add
  }
  return false;
}
```

Time: **O(n)** (one loop, each operation O(1)).
Space: **O(n)** (store up to n values).

**Building a map — "store and retrieve phone numbers by name"**

```js
function createPhonebook() {
  const book = new Map();

  // Add entries
  book.set("Alice", "555-1234");
  book.set("Bob", "555-5678");
  book.set("Charlie", "555-9999");

  // Retrieve
  console.log(book.get("Alice"));    // "555-1234", O(1) lookup
  console.log(book.get("David"));    // undefined, still O(1)

  // Check if exists
  console.log(book.has("Bob"));      // true
  console.log(book.has("Eve"));      // false

  // Delete
  book.delete("Bob");

  // Iterate (insertion order in JS)
  for (const [name, phone] of book) {
    console.log(`${name}: ${phone}`);
  }

  return book;
}
```

Try the comparison:

<CodeRunner
  id="cr-hash-duplicate"
  lang="en"
  code={`function hasDuplicateNested(arr) {
  let comps = 0;
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      comps++;
      if (arr[i] === arr[j]) return { found: true, comps };
    }
  }
  return { found: false, comps };
}

function hasDuplicateSet(arr) {
  const seen = new Set();
  for (let num of arr) {
    if (seen.has(num)) return { found: true, ops: arr.length };
    seen.add(num);
  }
  return { found: false, ops: arr.length };
}

const test1 = [1, 2, 3, 4, 5];
const test2 = [1, 2, 3, 4, 5, 3];

console.log("Test [1,2,3,4,5]:");
console.log("  Nested loops:", hasDuplicateNested(test1));
console.log("  Hash set:     ", hasDuplicateSet(test1));

console.log("\\nTest [1,2,3,4,5,3] (has duplicate):");
console.log("  Nested loops:", hasDuplicateNested(test2));
console.log("  Hash set:     ", hasDuplicateSet(test2));`}
/>
</Code>

<Trace lang="en">
Let us trace the hash set approach on [1, 2, 3, 4, 5, 3]:

<AlgoTrace
  id="trace-hash-duplicate"
  lang="en"
  code={[
    "function hasDuplicateSet(arr) {",
    "  const seen = new Set();",
    "  for (let num of arr) {",
    "    if (seen.has(num)) return true;",
    "    seen.add(num);",
    "  }",
    "  return false;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: create empty set, iterate arr = [1, 2, 3, 4, 5, 3]",
      line: 2,
      cells: []
    },
    {
      caption: "num = 1: has(1)? No. add(1). seen = {1}",
      line: 5,
      cells: ["1"]
    },
    {
      caption: "num = 2: has(2)? No. add(2). seen = {1, 2}",
      line: 5,
      cells: ["1", "2"]
    },
    {
      caption: "num = 3: has(3)? No. add(3). seen = {1, 2, 3}",
      line: 5,
      cells: ["1", "2", "3"]
    },
    {
      caption: "num = 4: has(4)? No. add(4). seen = {1, 2, 3, 4}",
      line: 5,
      cells: ["1", "2", "3", "4"]
    },
    {
      caption: "num = 5: has(5)? No. add(5). seen = {1, 2, 3, 4, 5}",
      line: 5,
      cells: ["1", "2", "3", "4", "5"]
    },
    {
      caption: "num = 3 (second occurrence): has(3)? Yes! Return true.",
      line: 4,
      cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "target" }, { value: "4", mark: "done" }, { value: "5", mark: "done" }]
    }
  ]}
/>

Notice: we found the duplicate on the 6th element without comparing it to all 5 previous elements. The hash set jumps straight to the bucket for 3 and finds it already there.
</Trace>

<Complexity lang="en">
**Hash set and hash map operations:**

| Operation | Time (average) | Space |
|---|---|---|
| Add / Set | O(1) | — |
| Lookup / Has | O(1) | — |
| Delete | O(1) | — |
| Iterate all | O(n) | O(1) aux |
| Contains duplicate (array) | O(n) | O(n) aux |

**Why is "average" important?**

A hash function should spread keys uniformly across the array. If it does, most operations are O(1). But if many keys hash to the same index (a collision), the buckets get crowded, and you may need to scan within a bucket. This is rare with a good hash function and proper load factor management, but it is possible. Worst case: all keys hash to one bucket, time becomes O(n).

In practice, with JavaScript's built-in Set and Map, the implementation is optimized, and O(1) average is reliable.
</Complexity>

<AlgoPractice
  id="practice-hash-1"
  lessonSlug="01-hash-maps-and-sets"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "Why does a hash set answer 'is X present?' in O(1) average time?",
      choices: [
        "Because it stores elements in sorted order and uses binary search",
        "Because the hash function calculates the index directly; no scanning needed",
        "Because JavaScript optimizes set operations automatically",
        "Because the set shrinks as you query it"
      ],
      answer: 1,
      hint: "The key is the hash function: it turns a value into an index instantly. Direct lookup, no loop."
    },
    {
      kind: "choice",
      prompt: "You have an array [5, 2, 9, 5, 3]. Which data structure detects the duplicate (5) most efficiently?",
      choices: [
        "A for loop comparing each pair (nested loop)",
        "A sorted array and binary search",
        "A hash set: one pass, O(n) time",
        "All take the same time"
      ],
      answer: 2,
      hint: "Nested loops: O(n²). Hash set: O(n). Hash set wins."
    },
    {
      kind: "choice",
      prompt: "In a hash map with entries 'Alice' → '555-1234' and 'Bob' → '555-5678', what does map.get('Alice') do?",
      choices: [
        "Scans the map from the beginning until it finds 'Alice'",
        "Hashes 'Alice', jumps to an index, retrieves the value there—O(1)",
        "Searches alphabetically for 'Alice'",
        "Loops through all entries to match the key"
      ],
      answer: 1,
      hint: "Hash function calculates the index. We jump there directly and return the value."
    },
    {
      kind: "choice",
      prompt: "What is the main tradeoff of using a hash set instead of storing values in an array?",
      choices: [
        "Hash set is always faster; no tradeoff",
        "Trade memory (extra array) for speed (O(1) vs O(n) lookup)",
        "Trade time for memory; hash set is slower",
        "Hash set uses less space but more time"
      ],
      answer: 1,
      hint: "Hash sets use extra memory for the hash buckets. In exchange, lookup is O(1) instead of O(n)."
    },
    {
      kind: "numeric",
      prompt: "If you check for duplicates in an array of 1000 elements using a hash set, how many times does the hash function need to be called (approximately)?",
      answer: 1000,
      hint: "One hash call per element added/checked. In the worst case, one per element = 1000."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Collisions:** If two different keys hash to the same index, they collide. Resolving collisions (storing multiple key–value pairs at one index, via chaining or open addressing) is the subject of the next lesson in this unit. For now, know that a good hash function and proper load factor (typically keeping utilization below 75%) minimize collisions and maintain O(1) average time.
</Inset>

<Inset kind="misconception" lang="en">
**"Hash sets are always faster than arrays."** Not quite. Hashing has O(1) average lookup, but the constant factor is larger than array indexing (hashing is a bit of work). For very small sets or when iteration order matters, an array or linked list might be simpler. Hash sets shine when you have large collections and frequent lookups.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-hash-1"
    pieceSlug="01-hash-maps-and-sets"
    lang="en"
    question="Why does hashing solve the 'find a duplicate in O(n) time' problem, while a nested loop takes O(n²)?"
    choices={[
      {
        label: "Hashing uses a hash function to jump straight to stored values, so each lookup is O(1), avoiding nested loops and making the total time O(n). Nested loops check every pair, which is O(n²).",
        correct: true
      },
      {
        label: "Hashing sorts the array, and sorting is faster than loops.",
        misconception: "Hashing does not sort; it uses a hash function to map keys to indices. No sorting involved."
      },
      {
        label: "Hashing uses less memory, so it must be faster.",
        misconception: "Hashing actually uses more memory (extra buckets). Speed comes from O(1) lookup via hash function, not less memory."
      },
      {
        label: "Nested loops are actually O(n), and hashing is O(1). Only one iteration is needed.",
        misconception: "Nested loops are O(n²): two loops, each up to n. Hashing is O(n) because one loop with O(1) operations. 'One iteration' misses the cost structure."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **hash set** and **hash map** are data structures that trade memory for speed.

Key facts:
1. **Hash function:** Maps a key to an array index instantly. Core idea: `index = hash(key) % arraySize`.
2. **O(1) average lookup:** Calculate the index, jump there, done. No scanning required (average case).
3. **Hash set:** Stores unique values. Answers "is X present?" in O(1) average.
4. **Hash map:** Stores key–value pairs. Looks up value by key in O(1) average.
5. **Time–space tradeoff:** Spend O(n) memory to save O(n) time (compared to nested-loop O(n²)).
6. **JavaScript tools:** Use the built-in `Set` for unique values, `Map` for key–value pairs.
7. **Collisions:** Different keys hashing to the same index degrade performance. A good hash function and load factor keep this rare.

Next, you will learn how collisions happen and how to resolve them, and you will build hash functions that distribute keys fairly.
</Recap>
