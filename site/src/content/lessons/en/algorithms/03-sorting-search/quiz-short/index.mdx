---
concepts:
- sorting-as-preprocessing
- adaptive-insertion-sort
- merge-sort-guarantee
- quicksort-worst-case
- binary-search-invariant
- monotonic-predicate
estMin: 13
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Merge_sort
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Binary_search_algorithm
status: ready
summary: "Free-recall prompts across the sorting and search unit. Answer each from memory first, then reveal the model answer and compare."
title: 'Sorting and search: free-recall review'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the unit stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core ideas — sorting as preprocessing, why merge sort and quicksort behave so differently, the binary-search invariant, and binary search on the answer — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="03-sorting-search-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is sorting called preprocessing, and what is the rule for deciding whether to sort?", a: "Sorting is preprocessing because you pay O(n log n) once and then every later operation on that data is cheaper: membership becomes O(log n) binary search, duplicate detection becomes an O(n) neighbour scan instead of O(n²), the median is direct O(1) access at index n/2, and the closest pair is an O(n) walk of neighbours. The decision rule: sort only when you reuse the order — many queries on the same data, or a problem whose answer is the order itself. For a single operation, O(n) linear work beats O(n log n) sort plus the query, so do not sort 'just to be safe'." },
    { q: "All simple sorts are O(n²), yet insertion sort is still useful. Why, and how does it differ from selection sort on nearly-sorted input?", a: "Insertion sort is adaptive: its work is proportional to the number of inversions (out-of-order pairs), so on already-sorted or nearly-sorted input the inner while loop almost never runs and it finishes in O(n). Selection sort has no early exit — every pass scans the entire unsorted suffix to find the minimum, so it is O(n²) even on sorted input. That adaptivity is why insertion sort is used for small or nearly-sorted subarrays inside hybrid O(n log n) sorts, and why it is the right pick for data that arrives almost in order." },
    { q: "Why is merge sort O(n log n) on every input, and what does it cost you for that guarantee?", a: "Merge sort always halves the array — n to n/2 to n/4 down to 1, which is log n levels — and at each level the merges touch every element once for O(n) work, so the product is O(n log n) regardless of input order. It never adapts and never degrades. The cost is memory: the merge step needs an O(n) auxiliary buffer (plus O(log n) call stack), so it is not in-place. The upside beyond the guarantee is stability — when both merge pointers tie, taking from the left half first preserves the original order of equal elements." },
    { q: "Quicksort is O(n log n) on average but O(n²) in the worst case. Explain the gap and how production code defends against it.", a: "Quicksort's cost is set by partition balance. A pivot near the median splits the array roughly in half, giving log n levels of O(n) partitioning = O(n log n). A consistently extreme pivot (e.g. always the last element on already-sorted data) splits into an empty side and an n-1 side, giving n levels of O(n) work = O(n²). Defences pick better pivots: randomize the pivot, or use median-of-three (first, middle, last). Both make the degenerate split astronomically unlikely, restoring O(n log n) expected time. Quicksort is chosen despite this because it sorts in place with O(log n) stack and has small constants and good cache behaviour." },
    { q: "State the binary-search invariant and the three classic bugs that violate it.", a: "Invariant: the target, if present, always lies within the current inclusive range [lo, hi]; each step inspects mid, and based on the comparison discards the half that provably cannot contain it. The three classic bugs: (1) loop condition — using lo < hi instead of lo <= hi skips the final single-element range and misses targets at lo == hi; (2) boundary update — setting lo = mid (or hi = mid) instead of lo = mid + 1 / hi = mid - 1 fails to exclude the already-checked mid, so the range stops shrinking and the loop spins forever; (3) midpoint overflow — (lo + hi) / 2 can overflow fixed-width integers, so use mid = lo + (hi - lo) / 2." },
    { q: "What is 'binary search on the answer', and what precondition must hold for it to be correct?", a: "Instead of searching an index in a sorted array, you search the space of possible answer values for the smallest (or largest) one that satisfies a yes/no test feasible(x). You set lo and hi to the min and max plausible answers, then halve the range: if feasible(mid) is true, the answer is mid or smaller (move hi); if false, it is larger (move lo). The precondition is a monotonic predicate — as x increases, feasible flips from false to true at most once and never flips back. Monotonicity is what guarantees that one side of mid is entirely infeasible and can be discarded, exactly like sortedness does for array binary search. Classic uses: minimum ship capacity to deliver in D days, minimum eating speed, smallest largest-sum split." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: sort only to reuse order; insertion sort adapts while selection sort never does; merge sort guarantees O(n log n) and stability at the price of O(n) memory; quicksort trades the worst case for in-place speed and defends it with smart pivots; and binary search — whether over an array or a monotonic answer space — lives or dies by its inclusive invariant.
</Recap>
