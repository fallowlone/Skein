---
slug: 02-simple-sorts
lang: en
track: algorithms
unit: 03-sorting-search
order: 2
title: "Simple sorts"
summary: "Selection sort, insertion sort, and bubble sort run in O(n²) time. Selection sort always scans the whole rest of array to find the minimum. Insertion sort adapts: O(n) on nearly sorted input. All are in-place and educational; real systems use O(n log n) algorithms."
estMin: 19
status: ready
prereqs: ["01-why-sort"]
mathPrereqs: []
concepts: ["selection_sort", "insertion_sort", "bubble_sort", "adaptive_sort", "swap"]
sources:
  - https://en.wikipedia.org/wiki/Selection_sort
  - https://en.wikipedia.org/wiki/Insertion_sort
  - https://en.wikipedia.org/wiki/Bubble_sort
  - https://www.geeksforgeeks.org/dsa/adaptive-and-non-adaptive-sorting-algorithms/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
You want to sort an array. The fast algorithms (merge sort, quicksort) are complex: divide the array, merge or partition, recurse. Before you learn those, understand the simple ones: selection sort, insertion sort, and bubble sort. They all run in O(n²) time—one nested loop inside another. They are slow on large data but fast to code and easy to understand. And insertion sort has a secret: when the array is nearly sorted, it runs in O(n) time. Learn these, and the fast ones will make sense.
</Hook>

<Goal lang="en">
After this lesson you understand three simple sorting algorithms (selection sort, insertion sort, and bubble sort), why they all run in O(n²) time (nested passes), how insertion sort adapts to O(n) on nearly sorted input, why all three are in-place and use O(1) space, and when it is safe to use them (small arrays or pre-sorted data) versus when to upgrade to O(n log n) algorithms.
</Goal>

<Idea lang="en">
**The core insight: nested loops = O(n²)**

All simple sorts use a nested structure: an outer loop over passes, and an inner loop to find/place one element. n passes × n comparisons per pass = O(n²).

**Selection sort: repeatedly find the minimum**

Selection sort divides the array into two parts: a sorted prefix (left) and an unsorted suffix (right). In each pass, scan the unsorted part, find the minimum, and swap it into the next sorted position.

```js
function selectionSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    let minIndex = i;
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] < arr[minIndex]) minIndex = j;
    }
    [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
  }
}
```

Why O(n²)? On pass i, you scan n - i elements to find the minimum. Total: n + (n-1) + (n-2) + ... + 1 = n(n+1)/2 = O(n²), regardless of input order.

**Insertion sort: grow a sorted prefix**

Insertion sort builds the sorted array from left to right, one element at a time. For each new element, scan the sorted prefix from right to left, shift larger elements right, and insert the new element into its correct spot.

```js
function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    const key = arr[i];
    let j = i - 1;
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j--;
    }
    arr[j + 1] = key;
  }
}
```

Why O(n²) worst case? When the array is reversed (worst case), each insertion requires shifting all previous elements. But here is the magic: **insertion sort is adaptive**. If the array is already sorted or nearly sorted, the while loop never runs (or runs once), so the algorithm runs in O(n) time.

**Bubble sort: repeatedly swap adjacent pairs**

Bubble sort repeatedly walks through the array and swaps adjacent elements if they are out of order. Larger elements "bubble up" to the end. After each pass, the rightmost unsorted element is in place.

```js
function bubbleSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length - i - 1; j++) {
      if (arr[j] > arr[j + 1]) {
        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
      }
    }
  }
}
```

Also O(n²) worst case. Pass after pass, each comparing neighbors. Bubble sort is the slowest of the three in practice—more comparisons and swaps than insertion sort.

**Why all three are O(n²) and in-place**

All three use O(1) space—only a few variables (indices, a temporary for swap). All have worst-case O(n²) because of the nested loop structure. But insertion sort is special: adaptive.
</Idea>

<Code lang="en">
Let us code and trace **selection sort** and **insertion sort** with full detail.

**Selection sort:**
```js
function selectionSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    // Find the minimum in arr[i..end]
    let minIndex = i;
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] < arr[minIndex]) {
        minIndex = j;
      }
    }
    // Swap arr[i] and arr[minIndex]
    [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
  }
}
```

**Insertion sort:**
```js
function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    const key = arr[i];  // Element to insert
    let j = i - 1;
    // Shift elements greater than key one position right
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j--;
    }
    // Insert key into its correct position
    arr[j + 1] = key;
  }
}
```

Let us run both on unsorted and sorted input:

<CodeRunner
  id="cr-simple-sorts"
  lang="en"
  code={`function selectionSort(arr) {
  const arr2 = [...arr];
  let comparisons = 0;
  for (let i = 0; i < arr2.length; i++) {
    let minIndex = i;
    for (let j = i + 1; j < arr2.length; j++) {
      comparisons++;
      if (arr2[j] < arr2[minIndex]) minIndex = j;
    }
    [arr2[i], arr2[minIndex]] = [arr2[minIndex], arr2[i]];
  }
  return { sorted: arr2, comparisons };
}

function insertionSort(arr) {
  const arr2 = [...arr];
  let comparisons = 0;
  for (let i = 1; i < arr2.length; i++) {
    const key = arr2[i];
    let j = i - 1;
    while (j >= 0 && arr2[j] > key) {
      comparisons++;
      arr2[j + 1] = arr2[j];
      j--;
    }
    arr2[j + 1] = key;
  }
  return { sorted: arr2, comparisons };
}

const unsorted = [5, 3, 7, 1, 4];
const sorted = [1, 2, 3, 4, 5];

console.log("Unsorted [5, 3, 7, 1, 4]:");
console.log("  Selection:", selectionSort(unsorted).comparisons, "comparisons");
console.log("  Insertion:", insertionSort(unsorted).comparisons, "comparisons");

console.log("Already sorted [1, 2, 3, 4, 5]:");
console.log("  Selection:", selectionSort(sorted).comparisons, "comparisons");
console.log("  Insertion:", insertionSort(sorted).comparisons, "comparisons (insertion adapts!)");
`}
/>
</Code>

<Trace lang="en">
Let us trace **insertion sort** on `[5, 3, 2, 1, 4]` step by step:

<AlgoTrace
  id="trace-insertion-sort"
  lang="en"
  code={[
    "function insertionSort(arr) {",
    "  for (let i = 1; i < arr.length; i++) {",
    "    const key = arr[i];",
    "    let j = i - 1;",
    "    while (j >= 0 && arr[j] > key) {",
    "      arr[j + 1] = arr[j];",
    "      j--;",
    "    }",
    "    arr[j + 1] = key;",
    "  }",
    "}"
  ]}
  frames={[
    {
      caption: "Start: arr = [5, 3, 2, 1, 4]. i = 1, key = 3.",
      line: 2,
      cells: [
        { value: "5", mark: "done" },
        { value: "3", mark: "active" },
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "j = 0: arr[0] = 5 > 3. Shift 5 right. arr[1] = 5.",
      line: 6,
      cells: [
        { value: "5", mark: "active" },
        "5",
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "j = -1. Stop. Insert key=3 at arr[0]. arr = [3, 5, 2, 1, 4].",
      line: 9,
      cells: [
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "i = 2, key = 2. j = 1: arr[1] = 5 > 2. Shift right.",
      line: 6,
      cells: [
        "3",
        { value: "5", mark: "active" },
        "5",
        "1",
        "4"
      ]
    },
    {
      caption: "j = 0: arr[0] = 3 > 2. Shift right. arr[1] = 3.",
      line: 6,
      cells: [
        "3",
        { value: "3", mark: "active" },
        "5",
        "1",
        "4"
      ]
    },
    {
      caption: "j = -1. Stop. Insert key=2 at arr[0]. arr = [2, 3, 5, 1, 4].",
      line: 9,
      cells: [
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "1",
        "4"
      ]
    },
    {
      caption: "i = 3, key = 1. Shift 2, 3, 5 all right (3 shifts).",
      line: 6,
      cells: [
        "2",
        "3",
        "5",
        { value: "5", mark: "active" },
        "4"
      ]
    },
    {
      caption: "Insert key=1 at arr[0]. arr = [1, 2, 3, 5, 4].",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "4"
      ]
    },
    {
      caption: "i = 4, key = 4. j = 3: arr[3] = 5 > 4. Shift right.",
      line: 6,
      cells: [
        "1",
        "2",
        "3",
        { value: "5", mark: "active" },
        "5"
      ]
    },
    {
      caption: "j = 2: arr[2] = 3 ≤ 4. Stop. Insert key=4 at arr[3]. Final: [1, 2, 3, 4, 5].",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "4", mark: "done" },
        { value: "5", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="en">
| Algorithm | Worst case | Best case | Adaptive? | Space |
|---|---|---|---|---|
| Selection sort | O(n²) | O(n²) | No | O(1) |
| Insertion sort | O(n²) | O(n) | Yes | O(1) |
| Bubble sort | O(n²) | O(n) | Yes (with optimization) | O(1) |

**Selection sort details:**
- Always O(n²) because you must scan the entire unsorted suffix to find the minimum, even if the array is already sorted.
- Number of swaps: O(n).

**Insertion sort details:**
- Worst case O(n²): array is reverse-sorted. Each insertion shifts all previous elements.
- Best case O(n): array is already sorted. The while loop condition fails immediately, and you only do n comparisons.
- Adaptive: the number of comparisons depends on the number of inversions (out-of-order pairs). Nearly sorted input = few inversions = O(n).

**Bubble sort details:**
- O(n²) worst case: repeated swaps of adjacent pairs.
- O(n) best case (with optimization): if no swaps occur in a pass, the array is sorted. Stop early.
- Less efficient than insertion sort in practice—more swaps and comparisons.
</Complexity>

<AlgoPractice
  id="practice-simple-sorts-1"
  lessonSlug="02-simple-sorts"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In selection sort, why is the time complexity O(n²) even when the array is already sorted?",
      choices: [
        "Because the sorting is too slow",
        "Because you must scan the entire unsorted suffix to find the minimum, regardless of order",
        "Because selection sort creates a temporary copy of the array",
        "Because sorting takes longer on larger arrays"
      ],
      answer: 1,
      hint: "Selection sort has no early exit. Every pass scans the remaining unsorted portion to find the minimum. The array's initial order does not matter."
    },
    {
      kind: "choice",
      prompt: "Insertion sort runs in O(n) time on a nearly sorted array. Why?",
      choices: [
        "It skips most elements",
        "The while loop rarely executes because elements are already in order",
        "It uses a hash table internally",
        "Nearly sorted arrays are special cases that have less data"
      ],
      answer: 1,
      hint: "Insertion sort runs in O(n + k) time, where k is the number of inversions (out-of-order pairs). A nearly sorted array has few inversions, so k is small."
    },
    {
      kind: "numeric",
      prompt: "How many passes does selection sort make to sort an array of 5 elements?",
      answer: 5,
      hint: "Selection sort makes n passes (one per element position). With n=5, that is 5 passes."
    },
    {
      kind: "choice",
      prompt: "Which algorithm is best for a large, already-sorted array?",
      choices: [
        "Selection sort (always O(n²))",
        "Bubble sort (O(n) best case with optimization)",
        "Insertion sort (O(n) best case, adaptive)",
        "All three are equally good"
      ],
      answer: 2,
      hint: "Insertion sort adapts to sorted input and runs in O(n) time. Selection sort is always O(n²). Use insertion sort for pre-sorted or nearly-sorted data."
    },
    {
      kind: "choice",
      prompt: "What does 'in-place' mean for sorting algorithms?",
      choices: [
        "The array is sorted in the location where it was created",
        "The algorithm uses O(1) auxiliary space (only a few variables, no new array)",
        "The algorithm sorts from left to right",
        "The algorithm never modifies the input"
      ],
      answer: 1,
      hint: "In-place means the algorithm rearranges the array's own cells without building a new array. Selection, insertion, and bubble sort are all in-place."
    }
  ]}
/>

<Inset kind="advantage" lang="en">
**When to use simple sorts.** Selection sort, insertion sort, and bubble sort are O(n²). Why use them? Small arrays (n ≤ ~50) or nearly sorted input. Insertion sort especially: it is simple, in-place, and highly adaptive. Many real systems use insertion sort for small data inside optimized O(n log n) algorithms (hybrid sorting).
</Inset>

<Inset kind="mistake" lang="en">
**Thinking selection sort can exit early.** Selection sort always scans the entire unsorted suffix, even if it is already sorted. It cannot "notice" that the array is sorted and stop early. Bubble sort can (with an optimization: track if any swap occurred), but selection sort cannot. This is why insertion sort is better for pre-sorted data.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-simple-sorts-1"
    pieceSlug="02-simple-sorts"
    lang="en"
    question="You need to sort a list of 10 elements. All three simple sorts (selection, insertion, bubble) are O(n²). Which should you choose?"
    choices={[
      {
        label: "Selection sort, because it is the most famous",
        misconception: "Fame does not determine efficiency. Insertion sort is simpler and adaptive; it is better for small data."
      },
      {
        label: "Insertion sort, because it is simple, adaptive to sorted input, and efficient on small arrays",
        correct: true
      },
      {
        label: "Bubble sort, because it is easiest to understand",
        misconception: "Bubble sort is slowest in practice—more swaps and comparisons than insertion sort. It is rarely used outside education."
      },
      {
        label: "None; all three are the same for n=10",
        misconception: "All are O(n²), but insertion sort adapts to sorted input, making it faster in practice."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Selection sort:** Repeatedly find the minimum in the unsorted part and swap it into place. O(n²) always. No adaptivity.

**Insertion sort:** Grow a sorted prefix by inserting each new element into its correct spot. O(n²) worst case, but O(n) on sorted or nearly sorted input. Adaptive.

**Bubble sort:** Repeatedly swap adjacent out-of-order pairs. O(n²) worst case, O(n) best case (with optimization). Slower than insertion sort in practice.

**All three are in-place and use O(1) auxiliary space.** This matters on very large datasets or memory-constrained systems.

**Key insight:** All simple sorts are O(n²) due to the nested loop structure. Insertion sort is special: it adapts to input order, running O(n) on nearly sorted data. For large unsorted data, upgrade to O(n log n) algorithms (merge sort, quicksort) covered in the next lessons.

**Why learn them?** Understanding simple sorts teaches you the nested-loop structure. It prepares you for divide-and-conquer sorting (merge sort) and partitioning-based sorting (quicksort).
</Recap>
