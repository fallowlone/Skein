---
concepts:
- binary-search-off-by-one
- lomuto-partition
- merge-stability
- quickselect
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Binary_search_algorithm
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Quickselect
status: ready
summary: "Read real sorting and search snippets — binary-search off-by-ones, the Lomuto partition, merge stability, and quickselect — predict the bug or behaviour, and pick the fix."
title: 'Sorting and search: code reading'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Sorting and search bugs hide in the boundaries: a wrong comparison, a half discarded one index too far, a swap that loses stability. Read each snippet the way you would in review and name the defect — or the exact behaviour — before you pick.
</Hook>

<Goal lang="en">
Practise the reviewer's loop for this unit: trace the invariant in your head, find the off-by-one or the lost guarantee, and choose the minimal correct fix.
</Goal>

## Snippet 1 — the infinite loop

```js
function bsearch(arr, target) {
  let lo = 0, hi = arr.length - 1;
  while (lo <= hi) {
    const mid = Math.floor((lo + hi) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) lo = mid;   // discard left
    else hi = mid - 1;                 // discard right
  }
  return -1;
}
```

<Quiz
  id="03-sorting-search-code-1"
  lessonSlug="03-sorting-search"
  lang="en"
  question="On many inputs this hangs forever. What is the bug and the fix?"
  choices={[
    { label: "lo = mid does not shrink the range when mid == lo (e.g. a two-element range where arr[mid] < target): lo stays put, mid recomputes to the same index, and the loop spins. Fix: lo = mid + 1, since arr[mid] is already excluded.", correct: true },
    { label: "The loop condition should be lo < hi.", misconception: "lo <= hi is correct for an inclusive range — it is what lets the final single-element range be checked. The hang comes from lo = mid never advancing, not from the condition." },
    { label: "Math.floor is wrong; mid should round up.", misconception: "Rounding direction is not the cause. With lo = mid the range fails to shrink regardless of rounding. The fix is lo = mid + 1." },
    { label: "arr[mid] === target should be the last check, not the first.", misconception: "Check order does not cause the infinite loop. The defect is the boundary update lo = mid that fails to make progress." },
  ]}
/>

## Snippet 2 — the integer-overflow midpoint

```c
int bsearch(int arr[], int n, int target) {
    int lo = 0, hi = n - 1;
    while (lo <= hi) {
        int mid = (lo + hi) / 2;        // <-- here
        if (arr[mid] == target) return mid;
        if (arr[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
```

<Quiz
  id="03-sorting-search-code-2"
  lessonSlug="03-sorting-search"
  lang="en"
  question="This passed every small test but corrupted results on a multi-gigabyte sorted array of 32-bit ints. Why, and what is the canonical fix?"
  choices={[
    { label: "lo + hi can exceed INT_MAX when both indices are large, overflowing to a negative value so mid becomes a bad index. The fix is mid = lo + (hi - lo) / 2, which never overflows because hi - lo fits in the range.", correct: true },
    { label: "Arrays that large cannot be binary-searched at all.", misconception: "They can; the algorithm is fine. The only defect is the lo + hi sum overflowing a 32-bit int. Compute mid = lo + (hi - lo) / 2." },
    { label: "The loop needs lo < hi to avoid reading past the end.", misconception: "lo <= hi is correct and reads only valid indices. The bug is arithmetic overflow in (lo + hi), not the loop boundary." },
    { label: "Integer division truncates, so mid is always too small.", misconception: "Truncation toward lo is exactly what binary search expects and is harmless here. The real defect is the lo + hi overflow on large indices." },
  ]}
/>

## Snippet 3 — the Lomuto partition

```js
function partition(arr, low, high) {
  const pivot = arr[high];
  let i = low - 1;
  for (let j = low; j < high; j++) {
    if (arr[j] < pivot) {
      i++;
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }
  [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];
  return i + 1;
}
```

<Quiz
  id="03-sorting-search-code-3"
  lessonSlug="03-sorting-search"
  lang="en"
  question="What does the returned index guarantee, and why does this scheme make quicksort unstable?"
  choices={[
    { label: "It returns the pivot's final sorted position: everything left of it is < pivot, everything right is >= pivot, and the pivot never moves again. It is unstable because the final swap can jump the pivot past equal elements, reordering equal keys.", correct: true },
    { label: "It returns the array's median index.", misconception: "It returns wherever the chosen pivot lands after partition, which is its sorted position — not the median unless the pivot happened to be the median. Median-finding is quickselect, a different use of partition." },
    { label: "It guarantees both sides are equal in size.", misconception: "Partition guarantees order relative to the pivot, not balance. An extreme pivot leaves one side empty — the O(n²) worst case. Balance is what good pivot choice buys you." },
    { label: "Quicksort is unstable because it uses recursion.", misconception: "Recursion has nothing to do with stability. Instability comes from swaps moving equal elements across the pivot boundary, losing their original relative order." },
  ]}
/>

## Snippet 4 — quickselect for the k-th smallest

```js
function quickselect(arr, k, lo = 0, hi = arr.length - 1) {
  const p = partition(arr, lo, hi);    // pivot's final index
  if (p === k) return arr[p];
  if (p < k) return quickselect(arr, k, p + 1, hi);
  return quickselect(arr, k, lo, p - 1);
}
```

<Quiz
  id="03-sorting-search-code-4"
  lessonSlug="03-sorting-search"
  lang="en"
  question="Quickselect finds the k-th smallest element by reusing partition. What is its complexity, and why is it better than sorting for this task?"
  choices={[
    { label: "O(n) on average (n + n/2 + n/4 + … = 2n), because it only recurses into the one side containing k, not both. Full sorting is O(n log n); quickselect skips the work of ordering everything you do not need.", correct: true },
    { label: "O(n log n), the same as sorting, so there is no advantage.", misconception: "Sorting does both sides at every level; quickselect recurses into only the side holding k, giving an O(n) average. That is the whole point of selecting instead of sorting." },
    { label: "O(log n), because it halves the array like binary search.", misconception: "Each partition still scans O(n) elements, so a single level is linear. The geometric sum of one-sided recursion is O(n) average, not O(log n)." },
    { label: "It cannot work because the array is not sorted.", misconception: "Quickselect deliberately works on unsorted input — partition needs no sortedness. Its worst case is O(n²) on bad pivots, fixed by randomization or median-of-medians." },
  ]}
/>

<Recap lang="en">
Every defect here lives at a boundary: binary search must shrink the range (lo = mid + 1, never lo = mid) and compute mid = lo + (hi - lo) / 2 to dodge overflow; Lomuto partition returns the pivot's final sorted position and is unstable because swaps cross equal keys; and quickselect reuses that partition to find the k-th element in O(n) average by recursing into only one side. Read the invariant first, then the fix is usually one token.
</Recap>
