---
concepts:
- sorting-as-preprocessing
- stability
- divide-and-conquer
- pivot-choice
- binary-search-invariant
- monotonic-predicate
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Merge_sort
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Binary_search_algorithm
status: ready
summary: "Multiple-choice synthesis across the sorting and search unit: when sorting pays off, stability, merge vs quicksort tradeoffs, the binary-search invariant, and binary search on the answer."
title: 'Sorting and search: multiple-choice review'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make when you reach for sorting or search in real code — not a definition to recite, but a tradeoff to weigh.
</Hook>

<Goal lang="en">
Confirm you can connect the unit's spine: sorting is preprocessing you pay for once, the O(n log n) sorts trade stability against memory and worst-case behaviour, and binary search generalises from arrays to any monotonic answer space.
</Goal>

<Quiz
  id="03-sorting-search-choice-1"
  lessonSlug="03-sorting-search"
  lang="en"
  question="You receive an unsorted array of 1,000,000 elements and need to answer exactly one membership query: is value X present? What is the cheapest correct approach?"
  choices={[
    { label: "Linear scan, O(n). Sorting to enable binary search costs O(n log n) up front, which already dominates the single O(log n) query — sorting only pays off when you reuse the order across many queries.", correct: true },
    { label: "Sort first, then binary search — O(n log n) + O(log n) is always faster.", misconception: "For a single query, O(n log n) sort plus O(log n) search is strictly worse than one O(n) scan. Sorting is preprocessing: it pays off only when amortised over many later queries." },
    { label: "Build a hash set and look up — that beats both.", misconception: "Building the set is still O(n), the same order as the scan, with extra memory and no benefit for one lookup. The point is that any preprocessing is wasted for a single query." },
    { label: "Binary search the unsorted array directly.", misconception: "Binary search requires a sorted array. On unsorted input it discards the wrong half and returns wrong answers — sortedness is its precondition." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-2"
  lessonSlug="03-sorting-search"
  lang="en"
  question="You sort a list of orders by amount, where several orders share the same amount, and you need ties to stay in their original arrival order. Which sort is safe?"
  choices={[
    { label: "A stable sort such as merge sort: equal elements keep their input order because the merge takes from the left half first on ties (left[i] <= right[j]). Plain quicksort is not stable.", correct: true },
    { label: "Quicksort — it is the fastest, so it preserves order best.", misconception: "Speed has nothing to do with stability. Lomuto-partition quicksort can reorder equal keys when it swaps them across the pivot boundary; it is not stable." },
    { label: "Selection sort, because it only swaps minimums.", misconception: "Selection sort is also unstable: swapping the minimum into place can leap an equal element over another, breaking arrival order. Insertion sort and merge sort are the stable choices." },
    { label: "Any O(n log n) sort, since stability is a property of complexity.", misconception: "Stability is independent of complexity. Merge sort (O(n log n)) is stable; quicksort and heapsort (also O(n log n)) are not. You must pick the algorithm, not just the bound." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-3"
  lessonSlug="03-sorting-search"
  lang="en"
  question="Merge sort and quicksort are both O(n log n) on average. For sorting a huge array in place under tight memory, with no stability requirement, why is quicksort often the production choice?"
  choices={[
    { label: "Quicksort partitions in place, using only O(log n) stack space, while merge sort allocates O(n) extra memory for the merge buffer. With good pivots quicksort also has smaller constant factors and better cache locality.", correct: true },
    { label: "Quicksort is O(n log n) in the worst case and merge sort is not.", misconception: "It is the reverse: merge sort is O(n log n) always, quicksort degrades to O(n²) on bad pivots. Quicksort wins on memory and constants, not on worst case." },
    { label: "Merge sort is unstable, so it reorders data unpredictably.", misconception: "Merge sort is the stable one. Its O(n) merge buffer is the reason to prefer in-place quicksort here, not any stability problem." },
    { label: "Quicksort does not use recursion, so it avoids stack growth.", misconception: "Quicksort is recursive too; it uses O(log n) stack on balanced partitions. The advantage is partitioning in place versus merge sort's O(n) auxiliary array." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-4"
  lessonSlug="03-sorting-search"
  lang="en"
  question="A quicksort that always picks the last element as pivot runs fine on random data but takes minutes on data that arrives already sorted. Why, and what is the standard fix?"
  choices={[
    { label: "On sorted input the last-element pivot is always the largest, so every partition splits into an empty side and an n-1 side: n levels of O(n) work = O(n²). Fix: randomize the pivot or use median-of-three so partitions stay balanced.", correct: true },
    { label: "Sorted input makes comparisons more expensive.", misconception: "Comparisons cost the same; the problem is the partition shape. A fixed last-element pivot on sorted data is always extreme, producing maximally unbalanced O(n²) recursion." },
    { label: "Quicksort cannot sort already-sorted data correctly.", misconception: "It sorts correctly — it is just slow. The output is right; the recursion is degenerate, turning O(n log n) into O(n²)." },
    { label: "The recursion runs out of stack and silently restarts.", misconception: "There is no silent restart; deep recursion may overflow the stack, but the core issue is the O(n²) work from unbalanced partitions. Randomized pivots restore O(n log n) expected time." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-5"
  lessonSlug="03-sorting-search"
  lang="en"
  question="A binary search uses while (lo < hi) with hi set to arr.length - 1 and returns -1 after the loop. It works on most inputs but misses the target when it sits at the position where lo equals hi. What is the defect?"
  choices={[
    { label: "The loop condition lo < hi exits while one candidate index (lo == hi) is still unexamined. For an inclusive [lo, hi] range the condition must be lo <= hi so the final single-element range is checked.", correct: true },
    { label: "hi should start at arr.length, not arr.length - 1.", misconception: "With an inclusive range hi = arr.length - 1 is correct; using arr.length would read out of bounds. The bug is the strict < in the loop condition leaving the lo == hi cell unchecked." },
    { label: "mid must be computed as (lo + hi + 1) / 2 to avoid the miss.", misconception: "The rounding of mid is not the cause here; the loop simply terminates before inspecting the lo == hi index. Switch the condition to lo <= hi." },
    { label: "Binary search inherently cannot find the last element of an array.", misconception: "It can, with a correct invariant. The miss is purely the off-by-one in the loop boundary, not a limitation of binary search." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-6"
  lessonSlug="03-sorting-search"
  lang="en"
  question="You need the minimum ship capacity to deliver all packages within D days, with no sorted array of capacities to index. When can you still binary search?"
  choices={[
    { label: "When feasible(capacity) is monotonic: once a capacity is large enough to finish in D days, every larger capacity also finishes. You binary search the answer range [max_package, sum_of_packages] for the smallest feasible value.", correct: true },
    { label: "Never — binary search needs an explicit sorted array to index into.", misconception: "Binary search needs a sorted/monotonic search space, not a literal array. A monotonic feasible() predicate lets you halve the answer range the same way." },
    { label: "Only if the package weights are themselves sorted.", misconception: "The weights' order is irrelevant; the feasibility check handles them as given. What must be monotonic is feasible(capacity) as capacity increases." },
    { label: "When feasible() returns true, then false, then true again as capacity grows.", misconception: "That is exactly the non-monotonic case where binary search fails: a flip that reverses breaks the half-discarding logic. You need a single false-to-true transition." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one habit: decide whether order is worth buying. Sort only when you reuse the order; pick a stable sort (merge, insertion) when ties must hold their place; trade merge sort's O(n) buffer for quicksort's in-place O(log n) stack when memory is tight, and accept that bad pivots risk O(n²) unless you randomize. Once order exists, binary search finds answers in O(log n) — but only with a correct inclusive invariant, and it generalises to any monotonic predicate, not just arrays.
</Recap>
