---
concepts:
- one_dimensional_dp
- house_robber
- climbing_stairs
- decode_ways
- dp_state
- recurrence
- base_case
- rolling_variables
- constant_space
estMin: 28
lang: en
mathPrereqs: []
order: 4
prereqs:
- 03-tabulation
slug: 04-one-dimensional-dp
sources:
- https://leetcode.com/problems/house-robber/
- https://leetcode.com/problems/climbing-stairs/
- https://leetcode.com/problems/decode-ways/
- https://www.geeksforgeeks.org/dsa/house-robber/
- https://en.wikipedia.org/wiki/Dynamic_programming
status: ready
summary: 'The 1D DP pattern: state is a single position i and dp[i] depends on a few
  earlier cells. House Robber is max(dp[i-1], dp[i-2]+nums[i]); Climbing Stairs is
  Fibonacci dp[i]=dp[i-1]+dp[i-2]; Decode Ways counts decodings with zero edge cases.
  O(n) time, O(1) space with rolling.'
title: 'One-dimensional DP: house robber, climbing stairs, decode ways'
track: algorithms
unit: 10-dynamic-programming
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You now know the two ways to run a DP: <Term slug="memoization">memoization</Term> top-down and <Term slug="tabulation">tabulation</Term> bottom-up. But both of those are just *engines*. The real skill is reading a brand-new problem and seeing the DP hiding inside it: what is the state, and how does one answer build on the answers before it? This lesson drills the most common shape of all—**one-dimensional DP**, where the state is a single position `i` in a sequence and `dp[i]` is decided by just a couple of earlier cells. Three classic problems, all the same skeleton: House Robber, Climbing Stairs, and Decode Ways. Learn to spot this shape and a huge slice of "hard" interview problems collapses into a one-line recurrence and a loop.
</Hook>

<Goal lang="en">
After this lesson you can recognize and solve one-dimensional dynamic programming problems. You can carry out the four-step routine for any of them: (1) define the <Term slug="dp_state">state</Term>—what does `dp[i]` mean in one precise sentence; (2) write the <Term slug="recurrence">recurrence</Term> that relates `dp[i]` to a few earlier cells; (3) set the <Term slug="base_case">base cases</Term>; (4) name the answer cell. You can apply it to three classics: House Robber (`dp[i] = max(dp[i-1], dp[i-2] + nums[i])`, never rob adjacent houses), Climbing Stairs (`dp[i] = dp[i-1] + dp[i-2]`—it is Fibonacci), and Decode Ways (count decodings of a digit string, handling the `'0'` edge cases). You can shrink each from an O(n) array to **O(1) space** using rolling variables, because the recurrence only looks back a fixed distance. Next lesson moves to two-dimensional DP, where the state is a pair `(i, j)`.
</Goal>

<Idea lang="en">
**What "one-dimensional DP" means**

A <Term slug="one_dimensional_dp">one-dimensional DP</Term> is a dynamic program whose <Term slug="dp_state">state</Term> is a single index `i`—usually a position in an array or string. You build an array `dp` where `dp[i]` is the answer to the subproblem ending at (or using) position `i`, and the <Term slug="recurrence">recurrence</Term> expresses `dp[i]` using only a *fixed* number of earlier cells (almost always `dp[i-1]` and `dp[i-2]`). That "looks back only a constant distance" property is exactly what later lets you drop the whole array and keep just a few variables.

**The four-step routine**

Every problem below is solved with the same checklist. Memorize the questions, not the answers:

1. **State.** Finish this sentence precisely: "`dp[i]` is ...". A vague state is the #1 cause of wrong DPs.
2. **Recurrence.** How is `dp[i]` built from earlier cells? This is the one creative line.
3. **Base cases.** The smallest inputs the recurrence cannot reach (`dp[0]`, sometimes `dp[1]`).
4. **Answer cell.** Which entry holds the final result—usually `dp[n-1]` or `dp[n]`.

**Problem 1 — House Robber**

You are a burglar on a street of houses, `nums[i]` cash in house `i`. Robbing two **adjacent** houses trips the alarm. Maximize the loot.

- **State:** `dp[i]` = the most you can rob considering houses `0..i`.
- **Recurrence:** at house `i` you either *skip* it (keep `dp[i-1]`) or *rob* it (take `nums[i]` plus the best from two back, `dp[i-2]`, because `i-1` is now off-limits): `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`.
- **Base cases:** `dp[0] = nums[0]`; `dp[1] = max(nums[0], nums[1])`.
- **Answer:** `dp[n-1]`.

Watch the table fill for `nums = [2, 7, 9, 3, 1]`. Each cell is the better of "skip" (the cell to its left) and "rob" (cell two to the left plus this house):

```
index     i=0   i=1   i=2    i=3    i=4
nums       2     7     9      3      1
                |     |      |      |
skip  dp[i-1]:  -     2      7     11     11
rob   dp[i-2]+n:-    (-+7)  (2+9) (7+3) (11+1)
                            = 11  = 10   = 12
                |     |      |      |
dp:        2     7    11     11     12   <- answer = 12
```

<DiagramFrame label="One-dimensional DP state and transition for House Robber on nums = 2, 7, 9, 3, 1. The dp row is one cell per index 0 to 4 holding 2, 7, 11, 11, 12. Cell dp[2] = 11 is computed from a fixed two-cell window of earlier cells: an arrow from dp[1] = 7 labelled skip carries the value 7, and an arrow from dp[0] = 2 labelled rob carries dp[0] plus nums[2] = 2 + 9 = 11. The recurrence dp[i] = max(dp[i-1], dp[i-2] + nums[i]) selects the larger, 11. The final cell dp[4] = 12 is the answer cell, highlighted." hue="var(--d-systems)" caption="1-D DP: the state is a single index i, and dp[i] reads a fixed two-cell window — dp[i-1] (skip) and dp[i-2] + nums[i] (rob) — then keeps the max. Shown filling dp[2]; the last cell dp[4] = 12 is the answer.">
  <svg viewBox="0 0 540 260" width="540" height="260" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="20" y="36" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">{"i"}</text>
    <text x="20" y="78" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">{"nums[i]"}</text>
    <text x="20" y="146" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">{"dp[i]"}</text>
    <text x="78" y="36" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"0"}</text>
    <text x="150" y="36" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"1"}</text>
    <text x="222" y="36" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"2"}</text>
    <text x="294" y="36" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"3"}</text>
    <text x="366" y="36" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"4"}</text>
    <text x="78" y="78" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"2"}</text>
    <text x="150" y="78" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"7"}</text>
    <text x="222" y="78" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"9"}</text>
    <text x="294" y="78" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"3"}</text>
    <text x="366" y="78" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"1"}</text>
    <path d="M150 120 C150 106, 222 106, 222 120" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" marker-end="url(#dp-arrow-en)" />
    <path d="M78 168 C78 196, 222 196, 222 156" fill="none" stroke="var(--d-systems)" stroke-width="1.4" marker-end="url(#dp-arrow-en-acc)" />
    <text x="186" y="100" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"skip = dp[1] = 7"}</text>
    <text x="150" y="214" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">{"rob = dp[0] + nums[2] = 2 + 9 = 11"}</text>
    <rect x="50" y="122" width="56" height="34" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="78" y="144" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"2"}</text>
    <rect x="122" y="122" width="56" height="34" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="150" y="144" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"7"}</text>
    <rect x="194" y="122" width="56" height="34" rx="5" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="222" y="144" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"11"}</text>
    <rect x="266" y="122" width="56" height="34" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="294" y="144" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"11"}</text>
    <rect x="338" y="122" width="56" height="34" rx="5" fill="var(--d-systems)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="366" y="144" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--card)">{"12"}</text>
    <text x="450" y="139" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"← answer"}</text>
    <text x="222" y="244" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">{"dp[2] = max(7, 11) = 11"}</text>
    <defs>
      <marker id="dp-arrow-en" viewBox="0 0 8 8" refX="6" refY="4" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M0 0 L8 4 L0 8 z" fill="var(--hairline-strong)" />
      </marker>
      <marker id="dp-arrow-en-acc" viewBox="0 0 8 8" refX="6" refY="4" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M0 0 L8 4 L0 8 z" fill="var(--d-systems)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

The greedy "always grab the richest house" would fail; DP weighs every skip-vs-rob choice using the answers it already computed.

**Problem 2 — Climbing Stairs (it is Fibonacci)**

You climb a staircase of `n` steps, taking 1 or 2 steps at a time. How many distinct ways to reach the top?

- **State:** `dp[i]` = the number of distinct ways to reach step `i`.
- **Recurrence:** the last move landed on step `i` either from step `i-1` (a 1-step) or from step `i-2` (a 2-step), so `dp[i] = dp[i-1] + dp[i-2]`. That is the Fibonacci rule.
- **Base cases:** `dp[0] = 1` (one way to "stand at the bottom"—do nothing); `dp[1] = 1` (a single 1-step).
- **Answer:** `dp[n]`.

```
step i:   0   1   2   3   4   5
ways:     1   1   2   3   5   8   <- climb(5) = 8
                  ^   ^
            2=1+1   3=2+1  (each = sum of the two before it)
```

**Problem 3 — Decode Ways**

`A=1, B=2, ..., Z=26`. Given a digit string like `"226"`, count how many ways it decodes (`"226"` → `"BBF"`, `"BZ"`, `"VF"` = 3). The twist is the zeros and the 1–26 range.

- **State:** `dp[i]` = the number of ways to decode the first `i` characters of the string. (Note the off-by-one: `dp` is indexed by *length processed*, not by character position, so `dp` has `n+1` cells.)
- **Recurrence:** look at the last one or two characters before length `i`.
  - If the single digit `s[i-1]` is `'1'`–`'9'` (not `'0'`), it forms a valid letter on its own → add `dp[i-1]`.
  - If the two digits `s[i-2..i-1]` form a number `10`–`26`, they form a valid letter together → add `dp[i-2]`.
  - `dp[i] = (s[i-1] != '0' ? dp[i-1] : 0) + (10 <= twoDigit <= 26 ? dp[i-2] : 0)`.
- **Base cases:** `dp[0] = 1` (one way to decode the empty prefix); `dp[1] = 1` if `s[0]` is not `'0'`, else `0` (a leading zero decodes nothing).
- **Answer:** `dp[n]`.

The zero is what makes this hard. `'0'` is never a letter by itself (there is no letter `0`), so a lone `'0'` contributes nothing, and only `"10"` and `"20"` ever consume a `'0'` validly (as `J` and `T`). Anything like `"30"`, `"06"`, or a trailing `"...0"` with no valid two-digit partner means **zero** total decodings—the whole count collapses to 0.

```
"226":  dp[0]=1  dp[1]=1  dp[2]=2  dp[3]=3
"06" :  s[0]='0' -> dp[1]=0 -> answer 0   (leading zero, dead)
"100":  ...the final '0' has no valid 2-digit partner ("00") -> 0
```

</Idea>

<Code lang="en">
**House Robber, the textbook 1D DP**

Here is House Robber with an explicit `dp` array so the recurrence is visible, then we collapse it.

<AnnotatedCode
  code={`function rob(nums) {
  const n = nums.length;
  if (n === 0) return 0;
  if (n === 1) return nums[0];

  const dp = new Array(n);
  dp[0] = nums[0];                       // base: only house 0
  dp[1] = Math.max(nums[0], nums[1]);    // base: best of the first two

  for (let i = 2; i < n; i++) {
    // skip house i  -> dp[i-1]
    // rob  house i  -> dp[i-2] + nums[i]  (i-1 is now forbidden)
    dp[i] = Math.max(dp[i - 1], dp[i - 2] + nums[i]);
  }

  return dp[n - 1];                      // answer cell
}`}
  annotations={[
    { line: 3, text: "Edge case: no houses means no loot." },
    { line: 4, text: "Edge case: a single house is robbed outright." },
    { line: 7, text: "Base case dp[0]: the only option is to rob house 0." },
    { line: 8, text: "Base case dp[1]: rob whichever of the first two houses holds more." },
    { line: 13, text: "The recurrence: best of skipping house i (dp[i-1]) versus robbing it (dp[i-2] + nums[i])." },
    { line: 16, text: "The answer is the last cell: best loot over all houses." }
  ]}
/>

**Collapsing to O(1) space**

`dp[i]` only ever reads `dp[i-1]` and `dp[i-2]`. So we never need the whole array—two rolling variables suffice. This same trick works for all three problems because each recurrence looks back a fixed distance.

```
prev2 = dp[i-2]   prev1 = dp[i-1]
cur = recurrence(prev1, prev2)
then slide: prev2 = prev1, prev1 = cur
```

**Running all three (rolling, O(1) space)**

<CodeRunner
  id="cr-1d-dp"
  lang="en"
  code={`// House Robber: dp[i] = max(dp[i-1], dp[i-2] + nums[i])
function rob(nums) {
  let prev2 = 0;            // best up to i-2
  let prev1 = 0;            // best up to i-1
  for (const x of nums) {
    const take = prev2 + x; // rob this house
    const skip = prev1;     // skip this house
    const cur = Math.max(skip, take);
    prev2 = prev1;
    prev1 = cur;
  }
  return prev1;
}

// Climbing Stairs: dp[i] = dp[i-1] + dp[i-2]  (Fibonacci)
function climbStairs(n) {
  let prev2 = 1;            // ways to reach step 0
  let prev1 = 1;            // ways to reach step 1
  for (let i = 2; i <= n; i++) {
    const cur = prev1 + prev2;
    prev2 = prev1;
    prev1 = cur;
  }
  return prev1;
}

// Decode Ways: 1-digit and valid 2-digit predecessors, zeros are deadly
function numDecodings(s) {
  if (s.length === 0 || s[0] === "0") return 0;
  let prev2 = 1;            // dp[0]: empty prefix
  let prev1 = 1;            // dp[1]: first char is non-zero here
  for (let i = 2; i <= s.length; i++) {
    let cur = 0;
    if (s[i - 1] !== "0") cur += prev1;          // single digit 1..9
    const two = Number(s.slice(i - 2, i));       // last two digits
    if (two >= 10 && two <= 26) cur += prev2;    // valid pair 10..26
    prev2 = prev1;
    prev1 = cur;
  }
  return prev1;
}

console.log("House Robber:");
console.log("  [2,7,9,3,1] ->", rob([2, 7, 9, 3, 1]));
console.log("  [1,2,3,1]   ->", rob([1, 2, 3, 1]));
console.log("Climbing Stairs:");
console.log("  n=5  ->", climbStairs(5));
console.log("  n=10 ->", climbStairs(10));
console.log("Decode Ways:");
console.log("  '226' ->", numDecodings("226"));
console.log("  '06'  ->", numDecodings("06"));
console.log("  '100' ->", numDecodings("100"));`}
  expected={`House Robber:
  [2,7,9,3,1] -> 12
  [1,2,3,1]   -> 4
Climbing Stairs:
  n=5  -> 8
  n=10 -> 89
Decode Ways:
  '226' -> 3
  '06'  -> 0
  '100' -> 0`}
/>

The `'06'` returns 0 (leading zero) and `'100'` returns 0 (the final `'0'` has no valid two-digit partner)—the zero edge cases the recurrence must respect.
</Code>

<Trace lang="en">
Let us fill the House Robber table for `nums = [2, 7, 9, 3, 1]` cell by cell. Each `dp[i]` is the better of *skip* (`dp[i-1]`) and *rob* (`dp[i-2] + nums[i]`).

<AlgoTrace
  id="trace-house-robber"
  lang="en"
  code={[
    "dp[0] = nums[0];",
    "dp[1] = max(nums[0], nums[1]);",
    "for (let i = 2; i < n; i++)",
    "  dp[i] = max(dp[i-1], dp[i-2] + nums[i]);",
    "return dp[n-1];"
  ]}
  frames={[
    { caption: "Base dp[0] = nums[0] = 2. Only house 0 considered.", line: 1, cells: [{ value: "2", mark: "done" }] },
    { caption: "Base dp[1] = max(nums[0], nums[1]) = max(2, 7) = 7. Best of the first two houses.", line: 2, cells: [{ value: "2", mark: "done" }, { value: "7", mark: "done" }] },
    { caption: "i=2: max(skip dp[1]=7, rob dp[0]+nums[2]=2+9=11) = 11. Robbing wins.", line: 4, cells: [{ value: "2" }, { value: "7" }, { value: "11", mark: "active" }] },
    { caption: "i=3: max(skip dp[2]=11, rob dp[1]+nums[3]=7+3=10) = 11. Skipping wins.", line: 4, cells: [{ value: "2" }, { value: "7" }, { value: "11" }, { value: "11", mark: "active" }] },
    { caption: "i=4: max(skip dp[3]=11, rob dp[2]+nums[4]=11+1=12) = 12. Robbing wins.", line: 4, cells: [{ value: "2" }, { value: "7" }, { value: "11" }, { value: "11" }, { value: "12", mark: "active" }] },
    { caption: "Answer = dp[n-1] = dp[4] = 12. Best plan: rob houses 0, 2, 4 (2+9+1). dp = [2,7,11,11,12].", line: 5, cells: [{ value: "2" }, { value: "7" }, { value: "11" }, { value: "11" }, { value: "12", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time: O(n)**

All three problems do one pass over an input of length `n`, and each cell costs O(1) work (a `max`, or one or two additions, or a couple of digit checks). So time is the number of states times the per-state work: `n` states x O(1) = **O(n)**.

```
problem           states   work/state   time
House Robber      n        O(1)         O(n)
Climbing Stairs   n+1      O(1)         O(n)
Decode Ways       n+1      O(1)         O(n)
```

**Space: O(n) with an array, O(1) with rolling variables**

The naive table stores every `dp[i]`: O(n) space. But every recurrence here reads back only a *fixed* distance—`dp[i-1]` and `dp[i-2]`—so you only ever need the last two values. Replace the array with two variables (`prev1`, `prev2`) and slide them forward each step:

```
                  array      rolling
House Robber      O(n)       O(1)
Climbing Stairs   O(n)       O(1)
Decode Ways       O(n)       O(1)
```

**The rule that licenses O(1) space:** if `dp[i]` depends only on the last `k` cells, you need only `k` variables, not the whole array. Here `k = 2` for all three. This is *the* signature optimization of one-dimensional DP, and it is why interviewers love these problems—they want to see you spot the bounded look-back and drop the array.

(Caveat: if a later question asks you to *reconstruct* the actual choices—which houses were robbed, which decoding was used—you must keep the full array or parent pointers. Rolling variables give you the count or the optimum value, but they throw away the path.)
</Complexity>

<AlgoPractice
  id="practice-1d-dp"
  lessonSlug="04-one-dimensional-dp"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "What is the House Robber recurrence (cannot rob two adjacent houses)?",
      choices: [
        "dp[i] = dp[i-1] + nums[i]",
        "dp[i] = max(dp[i-1], dp[i-2] + nums[i])",
        "dp[i] = max(dp[i-1] + nums[i], dp[i-2])",
        "dp[i] = dp[i-1] + dp[i-2]"
      ],
      answer: 1,
      hint: "At house i you either skip it (carry dp[i-1]) or rob it (take nums[i] plus dp[i-2], since house i-1 must then be skipped). Take the max."
    },
    {
      kind: "choice",
      prompt: "Why is Climbing Stairs the same as Fibonacci?",
      choices: [
        "Because stairs always come in groups of 5",
        "Because the last move onto step i came from step i-1 (a 1-step) or step i-2 (a 2-step), so dp[i] = dp[i-1] + dp[i-2]",
        "Because you must take exactly 2 steps every time",
        "Because the answer is always n itself"
      ],
      answer: 1,
      hint: "Count ways by the last move. Reaching step i from i-1 is a 1-step; from i-2 is a 2-step. Total ways = sum of the two predecessors — the Fibonacci rule."
    },
    {
      kind: "choice",
      prompt: "In Decode Ways, what does the state dp[i] represent?",
      choices: [
        "The digit at position i",
        "The number of ways to decode the first i characters of the string",
        "Whether character i is a zero",
        "The largest letter formed so far"
      ],
      answer: 1,
      hint: "dp is indexed by length processed, not character position. dp[i] counts decodings of the prefix of length i, so dp has n+1 cells and the answer is dp[n]."
    },
    {
      kind: "choice",
      prompt: "How many ways can the string '226' be decoded (A=1..Z=26)?",
      choices: [
        "1",
        "2",
        "3",
        "6"
      ],
      answer: 2,
      hint: "BBF (2,2,6), BZ (2,26), VF (22,6). The pair '22' and '26' are both valid (<=26); the lone digits 2,2,6 are all valid. dp[0]=1, dp[1]=1, dp[2]=2, dp[3]=3."
    },
    {
      kind: "choice",
      prompt: "Why does the string '06' decode in 0 ways?",
      choices: [
        "Because 6 is greater than 5",
        "Because a leading '0' is not a valid letter and '06' is not in the range 10..26, so no decoding starts validly",
        "Because the string has even length",
        "Because '0' decodes to the letter 'O'"
      ],
      answer: 1,
      hint: "There is no letter '0'. A leading zero kills the count immediately: s[0]='0' makes dp[1]=0, and '06' as a pair is 6, not in 10..26. Zero total ways."
    },
    {
      kind: "choice",
      prompt: "Why can House Robber, Climbing Stairs, and Decode Ways all run in O(1) space?",
      choices: [
        "Because the input is always tiny",
        "Because each recurrence looks back only a fixed distance (dp[i-1] and dp[i-2]), so two rolling variables replace the whole dp array",
        "Because they do not store any state at all",
        "Because they sort the input first"
      ],
      answer: 1,
      hint: "dp[i] depends only on the last two cells. Keep prev1 and prev2, slide them forward each step, and you never need the full O(n) array."
    },
    {
      kind: "numeric",
      prompt: "For House Robber with nums = [2, 7, 9, 3, 1], what is the maximum loot (the answer cell dp[4])?",
      answer: 12,
      hint: "dp = [2, 7, 11, 11, 12]. The best plan robs houses 0, 2, and 4: 2 + 9 + 1 = 12."
    },
    {
      kind: "numeric",
      prompt: "Climbing Stairs with n = 5: how many distinct ways to reach the top (1 or 2 steps at a time)?",
      answer: 8,
      hint: "Fibonacci: dp = [1, 1, 2, 3, 5, 8] for steps 0..5. The answer is dp[5] = 8."
    }
  ]}
/>

<Inset kind="why" lang="en">
**Why "define the state in one precise sentence" matters so much.** Most wrong DPs are not wrong recurrences—they are vague states. "dp[i] is the answer so far" means nothing you can write a recurrence against. Compare the three crisp definitions here: House Robber's dp[i] is "the most you can rob considering houses 0..i"; Decode Ways' dp[i] is "the number of ways to decode the first i characters." Once the sentence is exact, the recurrence usually writes itself, because you can ask "given that definition, how does cell i depend on earlier cells?" and the answer is forced. Spend your thinking time on the state, not the code.
</Inset>

<Inset kind="edgecase" lang="en">
**The zeros in Decode Ways.** This is the edge case that breaks naive solutions. Three things to get right: (1) A **leading** '0' (`s[0] === '0'`) means the whole string decodes 0 ways—bail out immediately. (2) A lone '0' is never a letter, so the single-digit branch must check `s[i-1] !== '0'` before adding dp[i-1]. (3) A '0' is only valid as the *second* digit of "10" or "20"; "30".."90" and "00" are invalid pairs, so a '0' with no valid partner (like the trailing '0' in "100", or "06", or "30") forces dp[i]=0 and the count dies. Test your code on "10" (=1, valid as J), "100" (=0), "2101" (=1), and "06" (=0).
</Inset>

<Inset kind="mistake" lang="en">
**Forgetting that Decode Ways is indexed by length, not position.** It is tempting to make dp[i] mean "decodings ending exactly at character i," but the clean version indexes dp by the *number of characters consumed*: dp[i] = ways to decode the first i characters. That gives dp.length = n+1, the empty-prefix base case dp[0]=1, and the answer dp[n]. Mixing the two conventions is a classic off-by-one that produces correct-looking-but-wrong counts. Pick the length convention and the base cases line up cleanly.
</Inset>

<Inset kind="practice" lang="en">
**The four-step routine on a fresh problem.** Try "Min Cost Climbing Stairs": each step i has a cost[i]; you may start at step 0 or 1, climb 1 or 2 at a time, pay the cost of each step you stand on, and reach past the top. (1) State: dp[i] = minimum cost to reach step i. (2) Recurrence: dp[i] = cost[i] + min(dp[i-1], dp[i-2]). (3) Base: dp[0] = cost[0], dp[1] = cost[1]. (4) Answer: min(dp[n-1], dp[n-2]) (you can step past the top from either of the last two). Same skeleton, new recurrence — that is the whole pattern. Then collapse it to O(1) space with two rolling variables.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-1d-dp"
    pieceSlug="04-one-dimensional-dp"
    lang="en"
    question="Explain the one-dimensional DP pattern: what the state is, how the three problems' recurrences work, the Decode Ways zero edge cases, and why O(1) space is possible."
    choices={[
      {
        label: "In 1D DP the state is a single index i and dp[i] depends on a fixed number of earlier cells. House Robber: dp[i] = max(dp[i-1], dp[i-2] + nums[i]) because robbing house i forbids house i-1. Climbing Stairs: dp[i] = dp[i-1] + dp[i-2], which is Fibonacci, because the last move came from one or two steps below. Decode Ways: dp[i] (ways to decode the first i chars) adds dp[i-1] when the single digit is non-zero and dp[i-2] when the last two digits form 10..26; a leading '0' or a '0' with no valid partner gives 0. Time is O(n); space is O(1) because each recurrence looks back only two cells, so two rolling variables replace the array.",
        correct: true
      },
      {
        label: "All three problems require a 2D table indexed by (i, j), and the recurrences need every previous cell, not just the last two. Because dp[i] depends on all earlier cells, you can never reduce the space below O(n), and Climbing Stairs has nothing to do with Fibonacci. Decode Ways treats '0' as the letter 'O', so zeros are harmless.",
        misconception: "These are one-dimensional DPs: the state is a single index and each recurrence looks back only a constant distance (dp[i-1], dp[i-2]). That bounded look-back is exactly what allows O(1) space with rolling variables. Climbing Stairs IS Fibonacci, and there is no letter '0' — a stray zero collapses Decode Ways to 0."
      },
      {
        label: "House Robber should always rob the single richest house and then greedily take the next non-adjacent richest, no DP needed. Climbing Stairs is just n, the number of steps. Decode Ways equals 2 raised to the string length because each digit can be split or not.",
        misconception: "Greedy fails for House Robber — you must weigh skip vs rob at every house, which is what dp[i]=max(dp[i-1], dp[i-2]+nums[i]) does. Climbing Stairs grows like Fibonacci, not linearly. Decode Ways is constrained by the 1..26 range and by zeros, so it is far smaller than 2^n and is often 0."
      },
      {
        label: "The state dp[i] in Decode Ways means 'the digit at position i', and you can read the answer off dp[0]. Base cases are unnecessary because the loop covers everything, and zeros never need special handling.",
        misconception: "dp[i] is the number of ways to decode the first i characters, not a digit; the answer is dp[n], and base cases dp[0]=1 (empty prefix) plus the leading-zero check are essential. Zeros are the whole difficulty: a leading '0' or an unpaired '0' forces the count to 0."
      }
    ]}
  />
</Check>

<Recap lang="en">
<Term slug="one_dimensional_dp">One-dimensional DP</Term> has a single-index <Term slug="dp_state">state</Term>: `dp[i]` answers the subproblem at position `i`, and the <Term slug="recurrence">recurrence</Term> reads only a fixed number of earlier cells. Solve any of them with four steps: **define the state precisely, write the recurrence, set the base cases, name the answer cell.**

**The three classics:**
- **House Robber** — `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`; skip vs rob, never two adjacent. Answer `dp[n-1]`.
- **Climbing Stairs** — `dp[i] = dp[i-1] + dp[i-2]`; it is Fibonacci, counting ways by the last (1- or 2-) step. Answer `dp[n]`.
- **Decode Ways** — `dp[i]` = ways to decode the first `i` characters; add `dp[i-1]` when the single digit is non-zero and `dp[i-2]` when the last two digits are `10`–`26`. A **leading `'0'`** or an **unpaired `'0'`** (like `'06'`, `'100'`, `'30'`) drops the count to 0.

**Complexity:** O(n) time for all three. **O(1) space** with rolling variables, because each recurrence looks back only two cells—the signature optimization of 1D DP. (Keep the full array only if you must reconstruct the actual choices.)

Next: two-dimensional DP, where the state becomes a pair `(i, j)`—grids, edit distance, and subsequence problems—but the same four-step routine still drives every one.

Now when you see "maximize profit with no two adjacent choices" or "count ways with 1 or 2 steps," pause and ask: what is `dp[i]` in one sentence, and how far back does the recurrence reach? Once you answer both, the rolling-variable O(1) solution almost writes itself.
</Recap>
