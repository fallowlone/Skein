---
concepts:
- overlapping-subproblems
- optimal-substructure
- memoization-vs-tabulation
- state-design
- knapsack
- interval-dp
estMin: 14
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
- https://en.wikipedia.org/wiki/Knapsack_problem
status: ready
summary: "Multiple-choice synthesis across the DP unit: when DP applies, memoization vs tabulation, state design, classic recurrences, and the complexity each one buys."
title: 'Dynamic programming: multiple-choice review'
track: algorithms
unit: 10-dynamic-programming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make at the start of a real DP problem — not a definition to recite, but which property to check, which state to pick, and what complexity it costs.
</Hook>

<Goal lang="en">
Confirm you can connect the two enabling properties, the two implementation styles, state design, and the classic recurrences — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="10-dynamic-programming-choice-1"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="A problem splits into subproblems, and you confirm those subproblems repeat. Is that enough to justify a DP solution?"
  choices={[
    { label: "No. You also need optimal substructure: an optimal answer must be composable from optimal answers to its subproblems. Overlapping subproblems alone only tells you caching will help; without optimal substructure the recurrence that combines subanswers is not valid.", correct: true },
    { label: "Yes — repeated subproblems are the single requirement for DP.", misconception: "Overlapping subproblems justify caching, but DP needs both properties. Without optimal substructure there is no correct recurrence to combine subanswers, so memoizing a wrong recurrence just caches wrong answers fast." },
    { label: "No — you additionally need the subproblems to be independent and non-overlapping.", misconception: "That describes divide and conquer (e.g. merge sort), not DP. DP is precisely the case where subproblems DO overlap, which is why a cache pays off." },
    { label: "Yes, as long as a greedy choice also works at each step.", misconception: "If a greedy choice provably works you often do not need DP at all. DP is the tool for when local greedy choices fail but optimal substructure still holds." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-2"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="You have a working memoized (top-down) solution. When is rewriting it as tabulation (bottom-up) worth the effort?"
  choices={[
    { label: "When recursion depth risks a stack overflow, when you want to drop memory to a rolling window, or when the constant-factor cost of recursion and hashing matters. Tabulation removes the call stack and lets you keep only the last few rows.", correct: true },
    { label: "Always — tabulation has strictly better asymptotic time than memoization.", misconception: "Both compute each distinct state once, so they share the same asymptotic time. Tabulation wins on constants, stack safety, and space optimization, not on big-O." },
    { label: "Never — memoization is always preferable because it only computes reachable states.", misconception: "Memoization's sparse-state advantage is real, but it is not always preferable: deep recursion overflows the stack and a hash-map cache costs more per access than an array. The choice is a tradeoff, not absolute." },
    { label: "Only when the problem has no base case.", misconception: "Every DP has base cases; that is unrelated. Tabulation specifically requires you to know an iteration order that fills cells before they are read." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-3"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="For House Robber, the recurrence is dp[i] = max(dp[i-1], dp[i-2] + nums[i]). What does each branch encode, and why is two indices of history enough?"
  choices={[
    { label: "Skip house i (dp[i-1]) or rob it and add it to the best up to i-2 (dp[i-2]+nums[i]). Two cells suffice because the only constraint is no two adjacent houses — so the decision at i depends solely on i-1 and i-2, which means O(1) rolling space works.", correct: true },
    { label: "dp[i-1] robs the current house and dp[i-2]+nums[i] skips it.", misconception: "It is the reverse. dp[i-1] is the best when you do NOT take house i; dp[i-2]+nums[i] is the best when you DO take it, since taking i forbids i-1." },
    { label: "You need the full dp array because robbing house i could depend on any earlier house.", misconception: "The adjacency constraint is local: taking i only forbids i-1. So dp[i] reads only dp[i-1] and dp[i-2], and two rolling variables replace the whole array." },
    { label: "The recurrence is greedy — always rob every other house.", misconception: "Robbing alternate houses fails on inputs like [2,1,1,2]; the optimum takes houses 0 and 3. The max over the two branches is exactly why DP beats greedy alternation." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-4"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="0/1 knapsack runs in O(n*W) time. Why is this called pseudo-polynomial rather than polynomial?"
  choices={[
    { label: "W is a numeric value, and its size in the input is the number of bits, about log W. So O(n*W) is exponential in the bit-length of W, not polynomial in the input size. A capacity of 10^9 makes the table enormous even with few items.", correct: true },
    { label: "Because the recursion makes two calls per item, which is exponential.", misconception: "The memoized or tabulated version computes each state once — it is not exponential in the number of states. Pseudo-polynomial refers to the dependence on the magnitude of W, not branching." },
    { label: "Because n*W is only an average case; the worst case is O(2^n).", misconception: "O(n*W) is the worst case of the DP, not an average. The 2^n figure is the naive recursion without memoization." },
    { label: "Because W must be a prime number for the bound to hold.", misconception: "W being prime is irrelevant. The pseudo-polynomial label is purely about W being a value whose encoding length is logarithmic." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-5"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="The O(n log n) Longest Increasing Subsequence algorithm maintains a 'tails' array. What is the answer to the problem, and what does the tails array actually contain?"
  choices={[
    { label: "The answer is the LENGTH of the tails array. The array holds, at index k, the smallest possible tail value of any increasing subsequence of length k+1 — so it is not itself a valid LIS, just a per-length frontier you binary-search to place each value.", correct: true },
    { label: "The tails array IS the longest increasing subsequence, so you return its contents.", misconception: "Tails is not a real subsequence — each slot is overwritten with the smallest tail for that length, so the stored values may not even appear in order in the input. Only its length is meaningful." },
    { label: "The answer is the maximum value stored in tails.", misconception: "The maximum stored value is just the largest element placed; it has no relation to the LIS length. The number of slots used is the answer." },
    { label: "Tails stores dp[i] for each index, the LIS ending at i.", misconception: "That is the O(n^2) DP's array, not tails. The tails array is indexed by subsequence LENGTH, not by input position, which is what enables the binary search." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-6"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="Interval DP (matrix chain, burst balloons) iterates by increasing interval length and picks a split point k inside [i..j]. Why iterate by length rather than the usual left-to-right i?"
  choices={[
    { label: "dp[i][j] combines smaller sub-intervals dp[i][k] and dp[k+1][j], both strictly shorter than [i..j]. Filling by increasing length guarantees every shorter interval is already computed before the longer one reads it — a plain i-loop would read cells not yet filled.", correct: true },
    { label: "Iterating by length is just a stylistic convention; left-to-right also works.", misconception: "It is a correctness requirement, not style. A left-to-right i-loop reaches dp[i][j] before dp[i+1][j-1] is filled, reading garbage. The dependency is on shorter intervals, so length must be the outer loop." },
    { label: "Because interval DP has no base case to start from.", misconception: "It has a clear base case: length-1 (or length-0) intervals. Those are exactly the shortest intervals filled first when you iterate by length." },
    { label: "Because the split point k must always be the midpoint.", misconception: "k ranges over every position inside the interval; you take the best over all splits. Fixing k to the midpoint would break optimality — the optimal last action is rarely the middle." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one checklist you run before writing any DP: confirm overlapping subproblems AND optimal substructure; design the smallest state that captures the decision (one index for House Robber, two for knapsack and LCS, an interval for matrix chain); pick memoization or tabulation by stack depth and space needs, not big-O; and know what the table really holds — including the LIS tails array, whose length, not contents, is the answer. Same complexity either way: states times work per state.
</Recap>
