---
concepts:
- memoization-bug
- state-design
- transition
- space-optimization
- complexity
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
- https://en.wikipedia.org/wiki/Knapsack_problem
status: ready
summary: "Read real DP snippets, find the cache bug, the wrong state, the unsafe space optimization, and the true complexity — the diagnosis a senior makes before trusting any DP."
title: 'Dynamic programming: code reading'
track: algorithms
unit: 10-dynamic-programming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
DP bugs are silent — they return a confidently wrong number, not a crash. Read each snippet, predict what it actually computes, and pick the fix a senior would make first.
</Hook>

<Goal lang="en">
Practise the loop you run on every DP: check the cache key matches the full state, check the transition covers every choice, check a space optimization does not destroy a value it still needs, and read the real complexity off the loops.
</Goal>

## Snippet 1 — the memo that never hits

```python
def lcs(a, b):
    memo = {}
    def go(i, j):
        if i == len(a) or j == len(b):
            return 0
        if a in memo:                 # bug: keying on the wrong thing
            return memo[a]
        if a[i] == b[j]:
            res = 1 + go(i + 1, j + 1)
        else:
            res = max(go(i + 1, j), go(i, j + 1))
        memo[a] = res
        return res
    return go(0, 0)
```

<Quiz
  id="10-dynamic-programming-code-1"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="This memoized LCS is both wrong and slow. What is the defect and the fix?"
  choices={[
    { label: "The cache is keyed on the string a, not on the subproblem state (i, j). So it returns the first computed value for every call and never reflects the actual position. Fix: key on (i, j) — memo[(i, j)] — the variables the subproblem actually depends on.", correct: true },
    { label: "The base case should return 1, not 0.", misconception: "An empty subsequence has length 0; returning 1 would overcount. The base case is correct — the cache key is the defect." },
    { label: "max(go(i+1, j), go(i, j+1)) should be a sum, not a max.", misconception: "LCS takes the better of the two skip options, so max is right. Summing would double-count overlapping subsequences. The bug is the cache key." },
    { label: "Recursion will overflow the stack; rewrite it iteratively.", misconception: "Depth here is at most len(a)+len(b), which is fine for normal inputs. The real bug is keying the cache on a instead of (i, j)." },
  ]}
/>

## Snippet 2 — the coin-change state

```python
def min_coins(coins, amount):
    # one coin per denomination, no reuse
    dp = [0] + [float('inf')] * amount
    for c in coins:
        for x in range(amount, c - 1, -1):   # capacity downward
            dp[x] = min(dp[x], dp[x - c] + 1)
    return dp[amount] if dp[amount] != float('inf') else -1
```

<Quiz
  id="10-dynamic-programming-code-2"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="The author wants UNLIMITED use of each coin (classic coin change). Does this 1D code do that, and what does the iteration direction control?"
  choices={[
    { label: "No — iterating capacity DOWNWARD is the 0/1 form, which uses each coin at most once. For unlimited reuse the inner loop must run UPWARD (range(c, amount+1)), so dp[x-c] may already include coin c from this same pass.", correct: true },
    { label: "Yes, this is correct unbounded coin change — direction does not matter in 1D.", misconception: "Direction is exactly what separates the two variants. Downward (0/1) forbids reusing a coin within one pass; upward (unbounded) allows it. Here downward gives the wrong, at-most-once answer." },
    { label: "No — the bug is the base case; dp[0] should be inf.", misconception: "dp[0]=0 is correct: zero coins make amount 0. The defect is the downward loop direction, which silently limits each coin to one use." },
    { label: "No — coins must be sorted first for this to work.", misconception: "Coin order does not affect correctness of this DP. The defect is the loop direction selecting the 0/1 semantics instead of unbounded." },
  ]}
/>

## Snippet 3 — the rolling-row optimization

```python
def edit_distance(a, b):
    n, m = len(a), len(b)
    prev = list(range(m + 1))          # row for i = 0
    for i in range(1, n + 1):
        cur = [i] + [0] * m            # cur[0] = i
        for j in range(1, m + 1):
            if a[i - 1] == b[j - 1]:
                cur[j] = prev[j - 1]
            else:
                cur[j] = 1 + min(prev[j], cur[j - 1], prev[j - 1])
        prev = cur
    return prev[m]
```

<Quiz
  id="10-dynamic-programming-code-3"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="This edit-distance code rolls the 2D table down to two rows. Is the space optimization correct, and what is the dependency that makes it safe?"
  choices={[
    { label: "Correct. Each cell dp[i][j] depends only on dp[i-1][j], dp[i][j-1], dp[i-1][j-1] — the cell above, left, and diagonal. Keeping the previous row (prev) plus the current row (cur) preserves exactly those three, so O(n*m) memory drops to O(m).", correct: true },
    { label: "Incorrect — you cannot reduce edit distance below the full 2D table because it needs all rows.", misconception: "Each cell reads only the previous row and the current row's left neighbor, so two rows suffice. The full table is only needed if you must reconstruct the actual edit script." },
    { label: "Incorrect — overwriting cur[j-1] destroys prev[j-1] before it is read.", misconception: "cur and prev are separate arrays, so writing cur[j-1] never touches prev[j-1]. The diagonal prev[j-1] stays intact for the next column. The optimization is safe." },
    { label: "Correct, but it changes the answer because the diagonal is now approximate.", misconception: "Nothing is approximated; the three reads (prev[j], cur[j-1], prev[j-1]) are the exact same values the full table holds. Same answer, less memory." },
  ]}
/>

## Snippet 4 — the interval-DP loops

```python
def interval_cost(n, weight):
    dp = [[0] * n for _ in range(n)]
    for length in range(2, n + 1):                # interval length
        for i in range(0, n - length + 1):        # left endpoint
            j = i + length - 1                    # right endpoint
            best = float('inf')
            for k in range(i, j):                 # split point
                best = min(best, dp[i][k] + dp[k + 1][j] + weight(i, k, j))
            dp[i][j] = best
    return dp[0][n - 1]
```

<Quiz
  id="10-dynamic-programming-code-4"
  lessonSlug="10-dynamic-programming"
  lang="en"
  question="What is the time and space complexity of this interval DP, and where does the dominant cost come from?"
  choices={[
    { label: "O(n^3) time, O(n^2) space. There are O(n^2) intervals (i, j) and each scans up to O(n) split points k, so the three nested loops give n^3; the dp table is n-by-n, hence n^2 space.", correct: true },
    { label: "O(n^2) time, O(n^2) space — two loops over the table.", misconception: "There are three nested loops: length, left endpoint, and split point k. The inner split scan is what pushes it to O(n^3), not O(n^2)." },
    { label: "O(n^3) time, O(n^3) space.", misconception: "Time is O(n^3), but space is only the dp table, which is n-by-n, so O(n^2). There is no third dimension stored." },
    { label: "O(2^n) time because every split is tried recursively.", misconception: "Splits are tried, but each (i, j) is computed once and cached in dp, so it is polynomial. The exponential figure is the naive recursion without the table." },
  ]}
/>

<Recap lang="en">
Four checks catch most DP defects: the cache key must equal the full subproblem state (Snippet 1 keyed on the wrong variable); the transition and loop direction must match the intended semantics (Snippet 2 — downward is 0/1, upward is unbounded); a rolling-row space cut is safe only when each cell reads just the previous row and the current row's neighbors (Snippet 3); and the real complexity is the product of the loop bounds — O(n^2) states times O(n) work per state is O(n^3) (Snippet 4). Read the loops, do not trust the comment.
</Recap>
