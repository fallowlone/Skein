---
concepts:
- longest_increasing_subsequence
- lis
- subsequence
- one_dimensional_dp
- tails_array
- patience_sorting
- binary_search
- lower_bound
- strictly_increasing
estMin: 30
lang: en
mathPrereqs: []
order: 7
prereqs:
- 04-one-dimensional-dp
slug: 07-longest-increasing-subsequence
sources:
- https://en.wikipedia.org/wiki/Longest_increasing_subsequence
- https://en.wikipedia.org/wiki/Patience_sorting
- https://cp-algorithms.com/sequences/longest_increasing_subsequence.html
- https://leetcode.com/problems/longest-increasing-subsequence/
status: ready
summary: 'LIS is the longest strictly-increasing subsequence (not contiguous). The
  O(n^2) DP: dp[i] = 1 + max(dp[j]) over j<i with nums[j]<nums[i]. The O(n log n)
  trick keeps a tails array and binary-searches each value; the tails LENGTH is the
  answer, not its contents.'
title: 'Longest increasing subsequence: O(n^2) DP and the O(n log n) tails trick'
track: algorithms
unit: 10-dynamic-programming
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
You have an array of numbers and want the length of the longest run that keeps going up—but the numbers do not have to sit next to each other. From `[10, 9, 2, 5, 3, 7, 101, 18]` you can pick `2, 5, 7, 101` (or `2, 3, 7, 18`): four numbers, each bigger than the last, scattered across the array. That is a <Term slug="subsequence">subsequence</Term>, not a contiguous slice. This is the longest increasing subsequence (LIS) problem. The natural <Term slug="one_dimensional_dp">one-dimensional DP</Term> solves it in O(n^2). Then a clever trick—keep a tiny array of "best tails" and binary-search it—drops the cost to O(n log n). The twist that trips everyone up: that tails array gives the right *length*, but it is not itself a valid subsequence.
</Hook>

<Goal lang="en">
After this lesson you can define LIS precisely: the longest strictly-increasing <Term slug="subsequence">subsequence</Term> of an array, where elements keep their relative order but need not be adjacent. You can write the O(n^2) DP—`dp[i]` is the length of the longest increasing subsequence that *ends at* index `i`, computed as `1 + max(dp[j])` over all `j < i` with `nums[j] < nums[i]`, and the answer is `max(dp)`. You can write the O(n log n) method: maintain a `tails` array where `tails[k]` is the smallest possible last value of an increasing subsequence of length `k+1`, and for each new value binary-search the first tail that is `>= x` and overwrite it (or append). You understand exactly why `tails.length` is the LIS length while `tails` itself is *not* a real subsequence. You can state both complexities. Next lesson continues the DP-on-sequences arc.
</Goal>

<Idea lang="en">
**What is the longest increasing subsequence?**

A <Term slug="subsequence">subsequence</Term> is what you get by deleting zero or more elements from an array without reordering the rest. So `[2, 7, 18]` is a subsequence of `[10, 9, 2, 5, 3, 7, 101, 18]`, but `[7, 2]` is not (wrong order). The <Term slug="longest_increasing_subsequence">longest increasing subsequence (LIS)</Term> is the longest such subsequence whose values are <Term slug="strictly_increasing">strictly increasing</Term> (each element strictly greater than the previous).

```
nums = [10, 9, 2, 5, 3, 7, 101, 18]
one LIS = 2, 5, 7, 101   (length 4)
another = 2, 3, 7, 18    (length 4)
```

There can be several LIS of the same length; we usually only want the length. Note "subsequence" (scattered, order kept) is different from "subarray/substring" (contiguous). LIS does **not** require the picked elements to be neighbors.

**Approach 1: the O(n^2) one-dimensional DP**

Define the state `dp[i]` = the length of the longest increasing subsequence that **ends exactly at index `i`**. Every increasing subsequence ends somewhere, so the answer is `max(dp)` over all `i`.

How do we fill `dp[i]`? A subsequence ending at `i` is some shorter subsequence ending at an earlier index `j < i` with `nums[j] < nums[i]`, plus `nums[i]` tacked on. To make it as long as possible, pick the best such `j`:

```
dp[i] = 1 + max( dp[j] )   over all j < i with nums[j] < nums[i]
dp[i] = 1                  if no such j exists (nums[i] starts fresh)
answer = max(dp[0..n-1])
```

Every element alone is a subsequence of length 1, so we initialize every `dp[i] = 1`. Two nested loops (`i` outer, `j` inner) give O(n^2).

**Approach 2: the O(n log n) tails array**

The O(n^2) DP re-scans all earlier indices for every `i`. The faster method replaces that scan with a binary search by maintaining one small array, `tails`:

> `tails[k]` = the **smallest possible last value** of any increasing subsequence of length `k + 1`.

Why "smallest possible"? Because a smaller tail is easier to extend later—it leaves more room for a future element to be larger than it. Keeping the minimum tail per length is greedy in exactly the right way. The length of `tails` at the end is the LIS length.

For each value `x` in `nums`, do one of two things:
- **Append:** if `x` is larger than every tail (larger than `tails[last]`), it extends the longest run by one, so push it.
- **Replace:** otherwise, find the **first** tail that is `>= x` and overwrite it with `x`. This says "a subsequence of that length can now end in a smaller value `x`," which can only help future extensions. Finding that position is a <Term slug="binary_search">binary search</Term> (a <Term slug="lower_bound">lower bound</Term>: the first index where `tails[idx] >= x`), because `tails` is always sorted in increasing order.

This is the patience-sorting idea (deal cards onto piles, each pile sorted; the number of piles is the LIS length).

**Watch tails evolve**

Walk `nums = [10, 9, 2, 5, 3, 7, 101, 18]` through the tails rule. Each step either appends (new longest length found) or replaces the first tail `>= x`:

```
x = 10  append            tails = [10]
x = 9   replace tails[0]  tails = [9]
x = 2   replace tails[0]  tails = [2]
x = 5   append            tails = [2, 5]
x = 3   replace tails[1]  tails = [2, 3]
x = 7   append            tails = [2, 3, 7]
x = 101 append            tails = [2, 3, 7, 101]
x = 18  replace tails[3]  tails = [2, 3, 7, 18]
```

Final `tails = [2, 3, 7, 18]`, length 4. The LIS length is **4**. (Here the final tails happens to be a real subsequence, but that is luck—see the Inset on why it is not guaranteed.)

**Why is `tails` always sorted?** Length-`k` subsequences can end in a value at least as small as length-`(k-1)` ones plus one more step up, so longer runs need larger minimum tails. The invariant "tails strictly increasing" holds throughout, which is exactly what lets us binary-search it.

</Idea>

<Code lang="en">
**The O(n^2) DP (clear and direct)**

State `dp[i]` = length of the longest increasing subsequence ending at `i`.

<AnnotatedCode
  code={`function lisN2(nums) {
  if (nums.length === 0) return 0;
  // dp[i] = length of LIS ending exactly at index i. Every element alone is length 1.
  const dp = new Array(nums.length).fill(1);

  for (let i = 1; i < nums.length; i++) {
    for (let j = 0; j < i; j++) {
      // Can we extend the subsequence ending at j by appending nums[i]?
      if (nums[j] < nums[i]) {
        dp[i] = Math.max(dp[i], dp[j] + 1);
      }
    }
  }
  // The LIS can end at any index, so take the best dp value.
  return Math.max(...dp);
}`}
  annotations={[
    { line: 2, text: "Empty input has no subsequence: length 0." },
    { line: 4, text: "Initialize every dp[i] to 1 - a single element is an increasing subsequence of length 1." },
    { line: 6, text: "Outer loop: compute dp[i] for each position in turn." },
    { line: 7, text: "Inner loop: scan all earlier indices j as candidate predecessors." },
    { line: 9, text: "Only a strictly smaller earlier value can sit before nums[i] in an increasing run." },
    { line: 10, text: "Take the best predecessor: dp[i] = 1 + max(dp[j]) over valid j." },
    { line: 15, text: "The answer is the maximum dp[i] - the LIS may end anywhere." }
  ]}
/>

**The O(n log n) tails method**

Maintain `tails`, where `tails[k]` is the smallest tail of any increasing subsequence of length `k+1`. For each `x`, binary-search the first tail `>= x` (lower bound). Append if none exists, otherwise overwrite that tail.

<AnnotatedCode
  code={`function lengthOfLIS(nums) {
  const tails = [];

  for (const x of nums) {
    // Binary search: find the first index where tails[idx] >= x (lower bound).
    let lo = 0, hi = tails.length;
    while (lo < hi) {
      const mid = lo + Math.floor((hi - lo) / 2);
      if (tails[mid] < x) {
        lo = mid + 1;   // tails[mid] too small - x belongs to the right
      } else {
        hi = mid;       // tails[mid] >= x - candidate, but keep looking left
      }
    }

    if (lo === tails.length) {
      tails.push(x);    // x beats every tail: a new longest length
    } else {
      tails[lo] = x;    // overwrite the first tail >= x with the smaller x
    }
  }

  return tails.length; // LENGTH is the LIS length (tails itself is NOT a subsequence)
}`}
  annotations={[
    { line: 2, text: "tails grows to the LIS length; tails[k] is the smallest possible last value of a length-(k+1) run." },
    { line: 6, text: "Lower-bound binary search over the sorted tails array - this is the O(log n) step." },
    { line: 9, text: "tails[mid] < x means x must go further right: move lo past mid." },
    { line: 11, text: "tails[mid] >= x is a candidate replacement spot: shrink hi to mid, never below the answer." },
    { line: 16, text: "lo == tails.length means no tail was >= x, so x extends the longest run: append it." },
    { line: 19, text: "Otherwise overwrite tails[lo] with x: same length, but now a smaller tail (easier to extend)." },
    { line: 23, text: "Return the length only. The contents of tails are not guaranteed to be a real subsequence." }
  ]}
/>

**Run both and compare**

Both methods must agree on the length. The tails method does it in O(n log n).

<CodeRunner
  id="cr-lis"
  lang="en"
  code={`function lengthOfLIS(nums) {
  const tails = [];
  for (const x of nums) {
    let lo = 0, hi = tails.length;
    while (lo < hi) {
      const mid = lo + Math.floor((hi - lo) / 2);
      if (tails[mid] < x) lo = mid + 1;
      else hi = mid;
    }
    if (lo === tails.length) tails.push(x);
    else tails[lo] = x;
  }
  return tails.length;
}

function lisN2(nums) {
  if (nums.length === 0) return 0;
  const dp = new Array(nums.length).fill(1);
  for (let i = 1; i < nums.length; i++) {
    for (let j = 0; j < i; j++) {
      if (nums[j] < nums[i]) dp[i] = Math.max(dp[i], dp[j] + 1);
    }
  }
  return Math.max(...dp);
}

const a = [10, 9, 2, 5, 3, 7, 101, 18];
console.log("nums:", a.join(", "));
console.log("LIS length (O(n log n)):", lengthOfLIS(a));
console.log("LIS length (O(n^2)) :", lisN2(a));

const b = [0, 1, 0, 3, 2, 3];
console.log("nums:", b.join(", "));
console.log("LIS length:", lengthOfLIS(b));

const c = [7, 7, 7, 7, 7];
console.log("nums:", c.join(", "));
console.log("LIS length:", lengthOfLIS(c));`}
  expected={`nums: 10, 9, 2, 5, 3, 7, 101, 18
LIS length (O(n log n)): 4
LIS length (O(n^2)) : 4
nums: 0, 1, 0, 3, 2, 3
LIS length: 4
nums: 7, 7, 7, 7, 7
LIS length: 1`}
/>

The all-equal array `[7, 7, 7, 7, 7]` returns 1: "strictly increasing" rejects equal neighbors, so each 7 just keeps replacing `tails[0]` and the length never grows past 1.
</Code>

<Trace lang="en">
Watch the tails array fill for `nums = [10, 9, 2, 5, 3, 7, 101, 18]`. Each step is one value `x`: either an **append** (a longer run is now possible) or a **replace** (the first tail `>= x`, found by binary search, becomes the smaller `x`). The cells show the current `tails`.

<AlgoTrace
  id="trace-lis-tails"
  lang="en"
  code={[
    "for (const x of nums) {",
    "  // lower bound: first idx with tails[idx] >= x",
    "  if (lo === tails.length) tails.push(x);  // append",
    "  else tails[lo] = x;                      // replace",
    "}"
  ]}
  frames={[
    { caption: "x = 10. tails empty, nothing is >= 10, lo = 0 = length. Append. tails = [10].", line: 3, cells: [{ value: "10", mark: "active" }] },
    { caption: "x = 9. First tail >= 9 is tails[0] = 10. Replace it with 9. tails = [9]. Same length, smaller tail.", line: 4, cells: [{ value: "9", mark: "target" }] },
    { caption: "x = 2. First tail >= 2 is tails[0] = 9. Replace with 2. tails = [2]. The minimum tail keeps dropping.", line: 4, cells: [{ value: "2", mark: "target" }] },
    { caption: "x = 5. No tail is >= 5 (only 2). lo = 1 = length. Append. tails = [2, 5]. Length grows to 2.", line: 3, cells: [{ value: "2" }, { value: "5", mark: "active" }] },
    { caption: "x = 3. First tail >= 3 is tails[1] = 5. Replace with 3. tails = [2, 3]. A length-2 run can now end in 3.", line: 4, cells: [{ value: "2" }, { value: "3", mark: "target" }] },
    { caption: "x = 7. No tail is >= 7. Append. tails = [2, 3, 7]. Length 3.", line: 3, cells: [{ value: "2" }, { value: "3" }, { value: "7", mark: "active" }] },
    { caption: "x = 101. No tail is >= 101. Append. tails = [2, 3, 7, 101]. Length 4.", line: 3, cells: [{ value: "2" }, { value: "3" }, { value: "7" }, { value: "101", mark: "active" }] },
    { caption: "x = 18. First tail >= 18 is tails[3] = 101. Replace with 18. tails = [2, 3, 7, 18]. Length stays 4.", line: 4, cells: [{ value: "2" }, { value: "3" }, { value: "7" }, { value: "18", mark: "target" }] },
    { caption: "Done. tails has length 4, so the LIS length is 4. (Its contents [2,3,7,18] are not always a real subsequence - here they happen to be.)", line: 1, cells: [{ value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "7", mark: "done" }, { value: "18", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
| Method | Time | Space |
|---|---|---|
| **O(n^2) DP** | O(n^2) — two nested loops over the array | O(n) — the `dp` array |
| **Tails + binary search** | O(n log n) — n values, each a O(log n) search | O(n) — the `tails` array |

**Why the DP is O(n^2)**

The outer loop runs `n` times; for each `i` the inner loop scans up to `i` earlier indices. That is `1 + 2 + ... + (n-1) ≈ n^2 / 2` comparisons = **O(n^2)**. Space is the single `dp` array of length `n` = O(n).

**Why the tails method is O(n log n)**

Each of the `n` values triggers exactly one binary search over `tails`, and `tails` never exceeds length `n`, so each search is **O(log n)**. The append/replace is O(1). Total: `n × O(log n)` = **O(n log n)**. Space is the `tails` array, at most `n` entries = O(n).

```
                time          space
O(n^2) DP       O(n^2)        O(n)
tails + bsearch O(n log n)    O(n)   <- this lesson's speedup
```

**When does the speedup matter?** For `n = 100,000`, `n^2` is ten billion operations; `n log n` is about 1.7 million. The tails method is the standard answer when `n` is large. The O(n^2) DP is easier to reason about and is the one to write first if you also need to *reconstruct* the actual subsequence (the tails method needs extra bookkeeping for that).
</Complexity>

<AlgoPractice
  id="practice-lis"
  lessonSlug="07-longest-increasing-subsequence"
  lang="en"
  problems={[
    {
      kind: "numeric",
      prompt: "What is the length of the LIS of [3, 1, 8, 2, 5]?",
      answer: 3,
      hint: "An increasing subsequence keeps order but need not be contiguous. 1, 2, 5 has length 3. So does 3, 8 (length 2) or 1, 8 (length 2). The longest is 3."
    },
    {
      kind: "choice",
      prompt: "In the O(n^2) DP, what does dp[i] represent?",
      choices: [
        "The length of the LIS of the whole array up to and including index i",
        "The length of the longest increasing subsequence that ends exactly at index i",
        "The value nums[i] itself",
        "The number of elements smaller than nums[i]"
      ],
      answer: 1,
      hint: "dp[i] is anchored at index i: the best increasing run that finishes with nums[i]. The whole-array answer is max(dp) over all i, because the LIS can end anywhere."
    },
    {
      kind: "choice",
      prompt: "In the tails array, what is tails[k]?",
      choices: [
        "The k-th element of the actual longest increasing subsequence",
        "The smallest possible last value of any increasing subsequence of length k+1",
        "The largest value seen so far among the first k+1 elements",
        "The number of increasing subsequences of length k+1"
      ],
      answer: 1,
      hint: "tails[k] is the smallest tail achievable for length k+1. Keeping the minimum tail per length is greedy: a smaller tail is easier to extend later."
    },
    {
      kind: "choice",
      prompt: "For each value x, the tails method binary-searches for which position?",
      choices: [
        "The last tail that is strictly less than x",
        "The first tail that is >= x (lower bound), then replaces it; if none exists, appends x",
        "The exact position where x already appears in tails",
        "The middle of the tails array, always"
      ],
      answer: 1,
      hint: "It is a lower-bound search: the first index where tails[idx] >= x. Overwrite that tail with the smaller x, or append x if every tail is smaller than x."
    },
    {
      kind: "choice",
      prompt: "Why is `tails.length` the LIS length, even though `tails` is not necessarily a real subsequence?",
      choices: [
        "Because tails is always a copy of the actual LIS",
        "Because tails grows by one exactly when a strictly longer increasing run becomes possible, so its length tracks the maximum achievable length; replacements never change the length, only lower a tail",
        "Because tails is sorted, and sorted arrays always equal the LIS",
        "It is a coincidence that only works on some inputs"
      ],
      answer: 1,
      hint: "Appends happen only when x exceeds every tail (a new, longer run is possible), so length = max run length. Replacements keep the length but shrink a tail for easier future extension. The contents can mix values from different runs, so they need not form one real subsequence."
    },
    {
      kind: "numeric",
      prompt: "What is the LIS length of the strictly-increasing array [1, 2, 3, 4, 5]?",
      answer: 5,
      hint: "Already strictly increasing, so the whole array is the LIS. Every value appends to tails; tails grows to length 5."
    }
  ]}
/>

<Inset kind="mistake" lang="en">
**The tails array is NOT the actual subsequence.** Its *length* is the LIS length, but its contents can be values that never appeared together in one increasing run. Concrete proof: `nums = [2, 5, 3, 7, 11, 8, 10, 13, 6]` ends with final `tails = [2, 3, 6, 8, 10, 13]` (length 6, which is the correct LIS length). But `[2, 3, 6, 8, 10, 13]` is **not** a subsequence of `nums`: the `6` is the very last element of `nums`, yet here it sits in the middle of tails ahead of 8, 10, 13. The last replacement (`6` overwriting `7`) lowered a tail for a length that was already finalized, corrupting the contents while leaving the length correct. To recover the real subsequence you must store predecessor links separately; the tails array alone only gives you the length.
</Inset>

<Inset kind="why" lang="en">
**Why keep the smallest tail per length?** Greedy intuition: if two increasing subsequences both have length k, the one ending in the smaller value is strictly more useful, because any future element that can extend the larger-tailed one can also extend the smaller-tailed one (and possibly more). So we never lose anything by always remembering the minimum tail for each length. That is the invariant the tails array maintains, and it is why overwriting the first tail `>= x` with the smaller `x` is always safe.
</Inset>

<Inset kind="edgecase" lang="en">
**Strictly increasing vs. non-decreasing.** This lesson computes the *strictly* increasing LIS, so equal neighbors do not count: `[7, 7, 7]` has LIS length 1. If instead you want the *longest non-decreasing* subsequence (allowing equal values), change the binary search from lower bound (first tail `>= x`) to upper bound (first tail `> x`). That one-line change flips strict to non-strict. Mixing them up is the most common LIS bug: always confirm which the problem wants.
</Inset>

<Inset kind="practice" lang="en">
**Recipe to apply.** (1) State the variant: strictly increasing or non-decreasing? (2) For a first correct solution or when you must reconstruct the actual subsequence, write the O(n^2) DP: `dp[i] = 1 + max(dp[j])` over `j < i` with `nums[j] < nums[i]`, answer `max(dp)`. (3) For large `n`, switch to the tails method: for each `x`, lower-bound binary search, then append or replace, and return `tails.length`. (4) Remember the caveat: the tails array's length is the answer, but its contents are not a guaranteed subsequence.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-lis"
    pieceSlug="07-longest-increasing-subsequence"
    lang="en"
    question="Explain the LIS problem, the O(n^2) DP, the O(n log n) tails method, and why tails.length is the answer even though tails is not a real subsequence."
    choices={[
      {
        label: "LIS is the longest strictly-increasing subsequence (order kept, elements need not be adjacent). The O(n^2) DP sets dp[i] = the LIS length ending at index i = 1 + max(dp[j]) over all j < i with nums[j] < nums[i], and the answer is max(dp). The O(n log n) method keeps a tails array where tails[k] is the smallest last value of an increasing subsequence of length k+1; for each value x it binary-searches the first tail >= x (lower bound) and either appends x (if every tail is smaller) or overwrites that tail with the smaller x. tails.length is the LIS length because appends happen exactly when a longer run becomes possible, while replacements only lower a tail without changing the length. The contents of tails can mix values from different runs, so tails itself is not necessarily a real subsequence.",
        correct: true
      },
      {
        label: "LIS asks for the longest contiguous run of increasing numbers (a subarray). The O(n^2) DP and the O(n log n) tails method both return that run, and the final tails array is exactly the longest increasing subsequence, so you can read the answer straight out of it.",
        misconception: "LIS is a subsequence, not a contiguous subarray - the elements may be scattered as long as order is preserved. And tails is not the actual subsequence: only its length is the answer; its contents can be impossible as one real run."
      },
      {
        label: "Both methods are O(n^2). The tails array helps only by sorting the input first; after sorting, the LIS is just the whole array, so tails.length equals n every time.",
        misconception: "The tails method is O(n log n), not O(n^2), and it never sorts the input - sorting would destroy the original order that defines a subsequence. tails.length is the LIS length, which is generally far less than n."
      },
      {
        label: "In the tails method you binary-search for the first tail strictly less than x and replace it. Equal values count as increasing, so [7, 7, 7] has LIS length 3, and the tails array is always a valid increasing subsequence of the input.",
        misconception: "The search is a lower bound (first tail >= x), not 'strictly less than x'. Strict increase rejects equal neighbors, so [7, 7, 7] has LIS length 1 (use upper bound only for the non-decreasing variant). And tails is not guaranteed to be a valid subsequence of the input."
      }
    ]}
  />
</Check>

<Recap lang="en">
The <Term slug="longest_increasing_subsequence">longest increasing subsequence (LIS)</Term> is the longest <Term slug="strictly_increasing">strictly-increasing</Term> <Term slug="subsequence">subsequence</Term> of an array—elements keep their order but need not be contiguous.

**O(n^2) DP:** `dp[i]` = length of the LIS ending at index `i` = `1 + max(dp[j])` over all `j < i` with `nums[j] < nums[i]`; initialize each `dp[i] = 1`; the answer is `max(dp)`. Time O(n^2), space O(n).

**O(n log n) tails method:** keep `tails` where `tails[k]` is the smallest last value of any increasing subsequence of length `k+1`. For each `x`, binary-search the first tail `>= x` (a <Term slug="lower_bound">lower bound</Term>); append `x` if every tail is smaller, otherwise overwrite that tail with the smaller `x`. The answer is `tails.length`. Time O(n log n), space O(n).

**The key caveat:** `tails.length` is the LIS length, but `tails` itself is **not** a guaranteed subsequence—its values can come from different runs (proof: `[2, 5, 3, 7, 11, 8, 10, 13, 6]` ends with tails `[2, 3, 6, 8, 10, 13]`, length 6 correct, contents impossible as one run). To recover the real subsequence, track predecessor links separately.

**Strict vs. non-decreasing:** lower bound (first tail `>= x`) gives strictly increasing; switch to upper bound (first tail `> x`) for the non-decreasing variant.
</Recap>
