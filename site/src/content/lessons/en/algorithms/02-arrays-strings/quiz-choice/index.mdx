---
concepts:
- two-pointers
- sliding-window
- prefix-sums
- in-place
- amortized-time
- string-immutability
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://usaco.guide/silver/prefix-sums
- https://www.geeksforgeeks.org/dsa/window-sliding-technique/
- https://labuladong.online/en/algo/essential-technique/array-two-pointers-summary/
status: ready
summary: "Multiple-choice synthesis across the arrays-and-strings unit: contiguity costs, two pointers, sliding window, prefix sums, in-place writes, and the string concatenation trap."
title: 'Arrays and strings: multiple-choice review'
track: algorithms
unit: 02-arrays-strings
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a choice you make at a whiteboard or in a code review — not a definition to recite, but the right pattern to reach for and the precondition it quietly depends on.
</Hook>

<Goal lang="en">
Confirm you can connect the array's contiguity cost model to the patterns built on it — two pointers, sliding window, prefix sums, in-place writes, and strings-as-arrays — and recognise the precondition each one silently assumes.
</Goal>

<Quiz
  id="02-arrays-strings-choice-1"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="A profiler shows a hot loop that inserts at index 0 of a 1M-element array on every request. Why is this the bottleneck, and what is the right fix?"
  choices={[
    { label: "Insert at the front is O(n) because every existing element must shift one slot right; at 1M elements that is ~1M copies per call. Append to the end (O(1) amortized) and read the result reversed, or switch to a structure with O(1) front insertion.", correct: true },
    { label: "Indexing got slow because the array is large — reads are no longer O(1) at this size.", misconception: "Indexing is O(1) regardless of size: address = base + i × element_size needs no scan. The cost here is the shifting that front insertion forces, not the lookup." },
    { label: "The array needs to be sorted first to make insertion fast.", misconception: "Sorting does not change insertion cost — inserting into the middle or front of a sorted array still shifts O(n) elements. Sorting helps search, not insertion." },
    { label: "Nothing is wrong; insertion into a dynamic array is always O(1) amortized like append.", misconception: "Only append is O(1) amortized. Insertion at the front or middle is O(n) because of the shift, no matter how the array grows." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-2"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="You apply the opposite-ends two pointers two-sum to find a pair summing to a target, but it misses pairs you can spot by hand. What is the most likely cause?"
  choices={[
    { label: "The array is not sorted. Opposite-ends two-sum relies on the invariant that moving left up increases the sum and moving right down decreases it — that only holds on sorted data, so on an unsorted array the movement rule is unsound and pairs are missed.", correct: true },
    { label: "Two pointers can only find one pair total, so any miss is expected.", misconception: "The converging scan considers every viable pair in one pass; it does not stop after one. The defect is running it on unsorted input, where the move rule no longer tracks the sum direction." },
    { label: "The pointers should both start at index 0, not at opposite ends.", misconception: "Both at index 0 is the same-direction (fast/slow) shape, used for in-place compaction. Two-sum needs opposite ends converging — the bug is the missing sorted precondition, not the start positions." },
    { label: "Two pointers is O(n²), so it times out and returns partial results.", misconception: "Two pointers is O(n): each pointer moves at most n times. It does not time out here — it returns wrong answers because the sorted precondition is violated." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-3"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="For 'smallest subarray with sum ≥ target' on positive integers, the variable-size sliding window runs in O(n) even though it has a nested while loop. Why is it not O(n²)?"
  choices={[
    { label: "left never moves backwards. Across the whole run, right advances at most n times and left advances at most n times, so total pointer movement is bounded by 2n — the inner while shares one budget with the outer for, it does not restart it.", correct: true },
    { label: "The inner while loop runs a constant number of times per outer iteration, so it is O(1) each.", misconception: "The inner loop can run many times in one outer step and zero in another. The O(n) bound comes from left's total forward-only movement being ≤ n across the whole run, not from a per-iteration cap." },
    { label: "The window has fixed size k, so each step is O(1).", misconception: "This is the variable-size window — its length changes. The fixed-size window is a different shape; here the O(n) bound rests on left and right each moving forward at most n times total." },
    { label: "Sliding window requires the array to be sorted, which removes the inner loop.", misconception: "Sliding window works on unsorted arrays and still has the inner shrink loop. The bound is amortized over monotonic, forward-only pointer movement, not from sorting." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-4"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="A service answers thousands of range-sum queries over a fixed-length log. Each query currently traverses the range. When is precomputing a prefix sum the right call, and what does it cost?"
  choices={[
    { label: "When the array is static and queries are many: build the prefix array once in O(n) time and O(n) space, then answer each range sum in O(1) via prefix[j+1] − prefix[i]. Total drops from O(n·q) to O(n + q).", correct: true },
    { label: "Always — prefix sums make every array operation O(1).", misconception: "Prefix sums only accelerate range-sum-style queries on a static array. They do nothing for insertion, search, or per-element updates, and any element change forces an O(n) rebuild." },
    { label: "Never here, because building the prefix array is O(n) and that is slower than one query.", misconception: "Building is O(n) once, but it is amortised over thousands of O(1) queries. For large q the precompute pays for itself many times over." },
    { label: "Only if the array is sorted, so the cumulative sums are monotonic.", misconception: "Prefix sums need no sorting — they work on any values, including negatives. The only real requirement is that the array stays static between queries." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-5"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="A reviewer flags that your in-place 'remove all zeros' using a write pointer mutates the caller's array, breaking a later read of the original. What is the correct framing of the tradeoff?"
  choices={[
    { label: "In-place buys O(1) auxiliary space by overwriting the input, and the cost is destroying it. If the caller still needs the original, copy it first (const backup = [...arr]); otherwise the destructive write is exactly the point.", correct: true },
    { label: "The write-pointer technique is buggy — it should build a new array to be safe.", misconception: "Building a new array is the O(n)-space alternative, not a bug fix. The write-pointer approach is correct; the issue is a contract about whether the caller may keep the original." },
    { label: "In-place algorithms are always faster, so the mutation is an acceptable price for speed.", misconception: "The advantage is memory (O(1) auxiliary), not guaranteed speed. The real tradeoff is space saved versus the original input being overwritten." },
    { label: "The write pointer should move backwards from the end to avoid mutating the input.", misconception: "Direction does not undo mutation — any in-place compaction overwrites cells. To preserve the original you copy it first; you do not change scan direction." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-6"
  lessonSlug="02-arrays-strings"
  lang="en"
  question="A function builds a large CSV by doing result += line inside a loop over n rows and is unexpectedly slow at scale. What is happening and what is the fix?"
  choices={[
    { label: "Strings are immutable, so each += allocates a new string and copies everything accumulated so far; iteration i copies ~i characters, summing to O(n²). Collect the pieces in an array and join('') once for O(n).", correct: true },
    { label: "The loop is O(n) and the slowness is just the disk write, not the concatenation.", misconception: "The concatenation itself is O(n²): each += copies the whole string so far. Even with no I/O, the quadratic copying dominates at scale." },
    { label: "Indexing into the growing string is slow, so cache s.length in a variable.", misconception: "Indexing and length are O(1). The cost is the repeated full copies that immutability forces on each +=, not index or length access." },
    { label: "Switching += to the + operator makes it O(n).", misconception: "+ and += do the same allocate-and-copy on immutable strings — both are O(n²) in a loop. The fix is array push plus a single join, not a different operator." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one cost model and the patterns that exploit it. Contiguity gives O(1) indexing but O(n) shifts, so front/middle insertion is the trap and append is cheap. Two pointers turn nested pair-scans into one pass — but opposite-ends two-sum needs sorted input. Sliding window is O(n) because the pointers only move forward. Prefix sums trade O(n) space for O(1) static range queries. In-place writes buy O(1) space at the cost of the original. And strings are arrays with one sting: immutability makes loop concatenation O(n²) — collect and join instead.
</Recap>
