---
slug: 01-the-array
lang: en
track: algorithms
unit: 02-arrays-strings
order: 1
title: "The array"
summary: "An array is a row of boxes in memory, each holding one value. Because the boxes are contiguous, you can jump to any box in O(1) time. Understand this one fact, and you unlock the cost of every array operation: read/write O(1), search O(n), insert in the middle O(n)."
estMin: 17
status: ready
prereqs: ["04-big-o-notation"]
mathPrereqs: []
concepts: ["array", "index", "contiguous_memory", "dynamic_array", "amortized_time"]
sources:
  - https://en.wikipedia.org/wiki/Array_(data_structure)
  - https://www.freecodecamp.org/news/data-structures-101-arrays-a-visual-introduction-for-beginners-7f013bcc355a
  - https://dev.to/ggorantala/array-strengths-weaknesses-and-big-o-complexity-analysis-4aho
  - https://www.interviewcake.com/concept/java/dynamic-array-amortized-analysis
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Imagine a row of numbered boxes, each holding one value. Your array is exactly that: a sequence of boxes in memory, all lined up next to each other. This simplicity is the source of the array's power. Because the boxes are contiguous, the computer can find any box instantly.
</Hook>

<Goal lang="en">
After this lesson you can visualize an array as contiguous memory, explain why indexing is O(1), measure the time cost of read, write, search, insert, delete, and append operations, and recognize why the array's strength (fast random access) and weakness (slow middle insert) come from the same fact: contiguity.
</Goal>

<Idea lang="en">
An **array** is a sequence of elements stored in contiguous memory. "Contiguous" means the elements sit next to each other — box 0 at address 1000, box 1 at address 1004, box 2 at address 1008, and so on. Each element takes the same size (e.g., 4 bytes for a 32-bit number).

**Why contiguity matters:** The computer can calculate the address of any element using a simple formula:

```
address of element[i] = base_address + (i × element_size)
```

For example, if the array starts at memory address 1000 and each element is 4 bytes:
- `array[0]` is at 1000 + (0 × 4) = 1000
- `array[1]` is at 1000 + (1 × 4) = 1004
- `array[5]` is at 1000 + (5 × 4) = 1020

The computer jumps straight to address 1020 without reading any other element. That is **O(1)**.

**Array operations and their costs:**

1. **Read or write by index** — O(1). Calculate the address, fetch or store the value. Done.

2. **Search for a value** — O(n). You do not know which index holds your target, so you scan from the start until you find it (or reach the end). In the worst case, you check every element.

<AnnotatedCode
  code={`function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;  // not found
}`}
  annotations={[
    { line: 2, text: "Loop through every element (O(n) worst case)" },
    { line: 3, text: "O(1) index lookup inside the loop" }
  ]}
/>

3. **Insert in the middle** — O(n). To insert at index 3, you must shift all elements at indices 3, 4, 5, ... one step to the right to make room. If the array has 100 elements, you shift ~97 elements. Shifting is the cost.

4. **Delete from the middle** — O(n). Same as insert: you must shift all elements after the deleted index one step left to close the gap.

5. **Append to the end** — O(1) amortized. Just place the new element at the next empty spot. No shifting needed (unless the array runs out of capacity, which happens rarely).

The crux: **contiguity gives you O(1) reading, but O(n) writing in the middle.** This is the array's fundamental tradeoff.

**Dynamic vs. fixed-size arrays:**

A **fixed-size array** has a capacity set at creation time. Once full, you cannot add more elements.

A **dynamic array** (like JavaScript arrays) grows automatically. When it runs out of space, it allocates a larger block, copies all elements to it, then deallocates the old block. The cost of this copy is O(n), but it happens rarely — only when capacity is exhausted. If the array doubles its capacity each time, you spread the copying cost thinly over many appends. We say the average cost per append is **O(1) amortized**.
</Idea>

<Code lang="en">
Let us code the basic array operations:

**O(1) indexing:**
```js
function get(arr, i) {
  return arr[i];  // Instant: calculate address, return value
}
```

**O(n) linear search:**
```js
function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) {
      return i;
    }
  }
  return -1;  // Not found
}
```

**O(n) insert at index i:**
```js
function insertAt(arr, i, value) {
  // Shift all elements from index i onward one step right
  for (let j = arr.length - 1; j >= i; j--) {
    arr[j + 1] = arr[j];
  }
  // Place the new value
  arr[i] = value;
}
```

The shifting loop runs from the end backward (to avoid overwriting). For an array of 100 elements, inserting at index 0 shifts all 100 elements. For an array of 1000 elements, inserting at index 0 shifts all 1000. That is O(n).

Try calling these functions:

<CodeRunner
  id="cr-array-ops"
  lang="en"
  code={`const arr = [10, 20, 30, 40];

// O(1): get element at index 2
console.log("Element at index 2:", get(arr, 2));

// O(n): search for value 30
console.log("Index of 30:", search(arr, 30));

// O(n): insert 25 at index 2
insertAt(arr, 2, 25);
console.log("After insertAt(arr, 2, 25):", arr);

function get(arr, i) {
  return arr[i];
}

function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;
}

function insertAt(arr, i, value) {
  for (let j = arr.length - 1; j >= i; j--) {
    arr[j + 1] = arr[j];
  }
  arr[i] = value;
}`}
/>
</Code>

<Trace lang="en">
Watch the `insertAt` function step by step. We are inserting the value 25 at index 2 in the array [10, 20, 30, 40]:

<AlgoTrace
  id="trace-insert"
  lang="en"
  code={[
    "function insertAt(arr, i, value) {",
    "  for (let j = arr.length - 1; j >= i; j--) {",
    "    arr[j + 1] = arr[j];",
    "  }",
    "  arr[i] = value;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: arr = [10, 20, 30, 40], we want to insert 25 at index 2.",
      line: 1,
      cells: [
        { value: "10", mark: "value" },
        { value: "20", mark: "value" },
        { value: "30", mark: "target" },
        { value: "40", mark: "value" }
      ]
    },
    {
      caption: "Loop iteration j = 3: shift arr[3] = 40 to arr[4]. Array grows by one slot.",
      line: 3,
      cells: [
        "10",
        "20",
        "30",
        { value: "40", mark: "done" },
        { value: "40", mark: "shifted" }
      ]
    },
    {
      caption: "Loop iteration j = 2: shift arr[2] = 30 to arr[3].",
      line: 3,
      cells: [
        "10",
        "20",
        { value: "30", mark: "done" },
        { value: "30", mark: "shifted" },
        "40"
      ]
    },
    {
      caption: "Loop ends. Now arr[2] is empty. Place value 25 at arr[2].",
      line: 5,
      cells: [
        "10",
        "20",
        { value: "25", mark: "done" },
        "30",
        "40"
      ]
    },
    {
      caption: "Done: arr = [10, 20, 25, 30, 40]. We shifted 2 elements. For a larger array, we would shift many more.",
      line: 6,
      cells: [
        "10",
        "20",
        "25",
        "30",
        "40"
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="en">
Here is the time complexity table for array operations:

| Operation | Example | Time |
|---|---|---|
| Get element | `arr[5]` | O(1) |
| Set element | `arr[5] = 99` | O(1) |
| Search for value | `search(arr, 99)` | O(n) |
| Insert at index i | `insertAt(arr, 2, 50)` | O(n) |
| Delete at index i | `deleteAt(arr, 2)` | O(n) |
| Append (push) | `arr.push(99)` | O(1) amortized |

**Why is append O(1) amortized?** In a dynamic array, append is free as long as there is spare capacity. When capacity is exhausted, the array doubles in size and copies all n elements (O(n) work). But it only has to do this once per n appends. Spread over n operations, the copying cost averages to O(1) per append.

**The core insight:** Random access is cheap. Sequential modification is expensive. This makes arrays ideal for read-heavy workloads and terrible for frequent insertion in the middle.
</Complexity>

<AlgoPractice
  id="practice-array-1"
  lessonSlug="01-the-array"
  lang="en"
  problems={[
    {
      kind: "numeric",
      prompt: "An array of 1000 elements is stored starting at memory address 2000. Each element takes 8 bytes. What is the memory address of array[10]?",
      answer: 2080,
      hint: "address = base + (index × element_size) = 2000 + (10 × 8)"
    },
    {
      kind: "choice",
      prompt: "Why is reading array[i] always O(1), even for very large arrays?",
      choices: [
        "Because JavaScript optimizes array access automatically",
        "Because the memory address can be calculated directly without scanning other elements",
        "Because the array keeps track of where each element is",
        "Because modern computers have fast processors"
      ],
      answer: 1,
      hint: "The key is contiguity: address = base + (i × element_size). No scanning needed."
    },
    {
      kind: "choice",
      prompt: "You want to insert a new element at index 0 of an array with 500 elements. How many elements must shift?",
      choices: [
        "0 — just place it at the beginning",
        "1 — only the element at index 0",
        "500 — all elements shift right",
        "499 — all except the first"
      ],
      answer: 2,
      hint: "To make room at index 0, all 500 existing elements must shift one position to the right."
    },
    {
      kind: "choice",
      prompt: "What does 'amortized O(1)' mean for appending to a dynamic array?",
      choices: [
        "Append always takes O(1) time",
        "On average, append takes O(1) time, even though occasionally it triggers a resize (O(n))",
        "Append takes less time than O(n)",
        "Dynamic arrays do not use real memory, so the time does not matter"
      ],
      answer: 1,
      hint: "Most appends are instant (free capacity). Rare resizes are O(n), but spread thinly over many operations."
    },
    {
      kind: "order",
      prompt: "Arrange these array operations from fastest to slowest (best to worst time complexity):",
      items: [
        "Append to end (dynamic array)",
        "Read element by index",
        "Search for a value",
        "Insert at the beginning"
      ],
      hint: "O(1) amortized < O(1) < O(n) < O(n). The first two are constant time. Search and insert are linear."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Fixed-size arrays run out of space.** If an array has capacity 4 and you try to append a 5th element, you get an error or lose the element (in a fixed-size language like C). JavaScript arrays are always dynamic — they grow silently. But the growth is not free; it costs O(n) work. That cost is why append is O(1) "amortized" and not O(1) always.
</Inset>

<Inset kind="mistake" lang="en">
**Confusing array size with cost.** Inserting into an array of 10 elements is much faster than inserting into an array of 1 million elements. But both are "O(n)". Do not think O(n) means "any time is fine"; it means the time grows proportionally to n. For 1 million elements, you really will shift 1 million times.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-array-1"
    pieceSlug="01-the-array"
    lang="en"
    question="Why does the contiguity of an array (elements stored next to each other in memory) lead to both its strength and weakness?"
    choices={[
      {
        label: "Contiguity allows fast indexing (O(1)) but forces slow insertion in the middle (O(n)) because shifting is required",
        correct: true
      },
      {
        label: "Contiguity is not related to insertion cost; insertion is slow because computers are slow",
        misconception: "Insertion cost comes from the need to shift elements to make room. Contiguity is the reason shifting is required."
      },
      {
        label: "Contiguity has no weakness; arrays are always better than other data structures",
        misconception: "Arrays are fast for reading but slow for middle insertion. Different data structures have different tradeoffs."
      },
      {
        label: "Contiguity means the array can only store one type of element",
        misconception: "Contiguity describes memory layout, not element type. Modern arrays can store mixed types (in JavaScript)."
      }
    ]}
  />
</Check>

<Recap lang="en">
An **array** is a row of contiguous memory boxes, all the same size.

Key facts:
1. **Contiguous memory:** Elements sit next to each other, enabling address calculation: `base + (index × element_size)`.
2. **O(1) read and write:** Looking up or modifying an element by index takes constant time—no scanning.
3. **O(n) search:** Finding a value requires scanning elements one by one (worst case: all n).
4. **O(n) insert/delete in middle:** Must shift all elements after the change point, linear in the worst case.
5. **O(1) amortized append:** Free most of the time; rarely triggers a resize, cost spread thinly.
6. **The tradeoff:** Contiguity gives fast access but slow middle modification. Choose arrays for read-heavy tasks.

Next, you will learn patterns that exploit this knowledge: how to search faster (binary search), how to modify faster (two pointers, sliding window), and when to use other structures (lists, queues) instead.
</Recap>
