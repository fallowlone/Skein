---
concepts:
- contiguous-memory
- two-pointers
- sliding-window
- prefix-sums
- in-place
- string-immutability
estMin: 13
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Array_(data_structure)
- https://usaco.guide/silver/prefix-sums
- https://www.geeksforgeeks.org/dsa/window-sliding-technique/
status: ready
summary: "Free-recall prompts across the arrays-and-strings unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Arrays and strings: free-recall review'
track: algorithms
unit: 02-arrays-strings
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the pattern and its precondition stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — why contiguity sets every cost, why two pointers and sliding window are O(n), what prefix sums trade, what in-place costs, and the string concatenation trap — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="02-arrays-strings-short-retrieval"
  lang="en"
  questions={[
    { q: "Why does the array's contiguity produce both its strength (O(1) indexing) and its weakness (O(n) middle insertion)?", a: "Contiguity means elements sit in adjacent, equal-size memory cells, so the address of element i is base + i × element_size — a direct calculation with no scan, giving O(1) read and write by index. The same layout is the weakness: to insert or delete in the middle you must keep the cells contiguous, so every element after the change point shifts one slot, which is O(n). Append is the exception at O(1) amortized: it writes to the next free slot, only paying O(n) on the rare resize-and-copy when capacity is exhausted, spread thinly over many appends. One fact — adjacency — explains the whole cost table." },
    { q: "Two pointers comes in two shapes. Name them, what each is for, and the precondition opposite-ends two-sum depends on.", a: "Opposite-ends: left starts at 0, right at the end, and they converge under a rule — used for palindrome checks, in-place reversal, and two-sum on a sorted array. Same-direction (fast/slow): both start at the front, fast scouts ahead and slow records — used for in-place compaction like removing duplicates or a target value. Both replace an O(n²) nested pair-scan with a single O(n) pass because each pointer moves at most n times. The precondition for opposite-ends two-sum is that the array is sorted: only then does moving left up reliably increase the sum and moving right down decrease it. On unsorted data that move rule is unsound and can miss pairs." },
    { q: "Why is the sliding window O(n) and not O(n²), even when the variable-size version contains a nested while loop?", a: "The window carries running state (a sum, a count) that updates in O(1) per step: adding the new right element and, when shrinking, removing the left element. Both left and right only ever move forward, never backward. So across the entire run right advances at most n times and left advances at most n times — total movement is bounded by 2n. The inner while loop is therefore amortized: it shares left's single forward budget rather than restarting a fresh O(n) scan per outer iteration. That monotonic, forward-only movement is what collapses the naive O(n²) recompute-every-subarray approach to O(n)." },
    { q: "What does a prefix-sum array buy you, what does it cost, and when is it the wrong tool?", a: "Build prefix once where prefix[k] is the sum of the first k elements (length n+1, prefix[0] = 0) in O(n) time and O(n) space. Then any range sum from i to j is prefix[j+1] − prefix[i] in O(1) — the subtraction cancels everything before index i. For q queries this turns O(n·q) into O(n + q), a huge win when q is large. It is the classic time–space tradeoff: spend memory and a one-time pass to make repeated queries free. It is the wrong tool when there are only one or two queries, or when the array changes often — any element update invalidates every downstream prefix value and forces an O(n) rebuild, so it shines only on static data." },
    { q: "What does in-place mean precisely, what does it cost, and name the two core in-place techniques from the unit.", a: "In-place means rearranging the array by overwriting its own cells using only O(1) auxiliary space — a few pointers and temporaries — rather than building a new O(n) output array. The cost is that it destroys the original input, so if the caller still needs it you must copy first. The two core techniques are: (1) the write pointer — a scan pointer reads while a write pointer marks where the next kept element goes, used to compact, filter, or remove a value or duplicates; and (2) two-pointer swap — opposite-ends pointers swap and move inward, used to reverse in place. It matters most for large datasets and memory-constrained environments where an extra O(n) buffer is unaffordable." },
    { q: "A string is algorithmically an array, yet building one by repeated += in a loop is O(n²). Explain why, and give the fix.", a: "Strings are immutable: a character cannot be changed in place, so every += allocates a brand-new string and copies all characters accumulated so far into it. Iteration i copies about i characters, so the total work is 1 + 2 + ... + n = O(n²) — for 10,000 appends that is roughly 50 million copies, hidden inside innocent-looking code. The fix is to collect the pieces in an array (push is O(1) amortized, no copying) and join('') once, a single O(n) pass that builds the final string. Apart from this trap, strings support O(1) indexing and every Unit 02 pattern — two pointers for palindromes, sliding window for substrings, traversal for counting characters." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: contiguity sets the whole cost table (O(1) index, O(n) shift, O(1) amortized append); two pointers and sliding window both hit O(n) through forward-only, single-pass movement; opposite-ends two-sum needs sorted input; prefix sums trade O(n) space for O(1) static range queries; in-place buys O(1) space by destroying the input; and strings are arrays that punish loop concatenation with O(n²) — collect and join instead.
</Recap>
