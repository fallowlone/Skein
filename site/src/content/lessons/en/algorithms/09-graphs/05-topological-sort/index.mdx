---
slug: 05-topological-sort
lang: en
track: algorithms
unit: 09-graphs
order: 5
title: "Topological sort: ordering a DAG with Kahn and DFS"
summary: "A topological order lists every vertex of a DAG before all vertices it points to. It exists only for an acyclic graph. Kahn's algorithm peels off in-degree-0 vertices with a queue; the DFS variant reverses post-order. Both run in O(V+E) time, O(V) space, and detect cycles."
estMin: 28
status: ready
prereqs: ["02-depth-first-search", "04-connected-components"]
mathPrereqs: []
concepts: ["topological_sort", "directed_acyclic_graph", "dag", "in_degree", "kahns_algorithm", "dfs_post_order", "cycle_detection", "directed_graph", "graph_traversal", "dependency_order"]
sources:
  - https://en.wikipedia.org/wiki/Topological_sorting
  - https://www.geeksforgeeks.org/topological-sorting/
  - https://cp-algorithms.com/graph/topological-sort.html
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
You know how to traverse a graph: DFS dives deep, BFS spreads wide, and connected components count the separate pieces. Now a sharper question. Suppose the graph is a list of tasks where an arrow A → B means "A must happen before B": course prerequisites, build steps, package installs, a spreadsheet's formula dependencies. In what order can you do all the tasks so that nothing is ever started before something it depends on is finished? That order is called a <Term slug="topological_sort">topological order</Term>. It is not unique—often many valid orders exist—but it only exists at all if the dependency graph has no cycle. If task A needs B and B needs A, no order can satisfy both. This lesson teaches two algorithms to produce a topological order and, as a free bonus, to detect when none exists because the graph contains a cycle.
</Hook>

<Goal lang="en">
After this lesson you can define a topological order: a linear listing of the vertices of a <Term slug="directed_acyclic_graph">directed acyclic graph (DAG)</Term> such that for every edge u → v, u appears before v. You understand why such an order exists if and only if the graph is acyclic. You can write Kahn's algorithm: compute every vertex's <Term slug="in_degree">in-degree</Term>, enqueue all in-degree-0 vertices, repeatedly pop one into the output and decrement its neighbors' in-degrees, enqueuing any that reach 0. You know the cycle test: if the output has fewer than V vertices, the graph has a cycle and no topological order. You can also write the DFS variant: run DFS and push each vertex onto a stack when it finishes (post-order), then reverse the stack. You know both run in O(V + E) time and O(V) space. Next lessons build on ordered DAGs for shortest paths and scheduling.
</Goal>

<Idea lang="en">
**What is a topological order?**

A <Term slug="topological_sort">topological order</Term> (or topological sort) of a <Term slug="directed_graph">directed graph</Term> is a linear arrangement of all its <Term slug="vertex">vertices</Term> such that for every directed <Term slug="edge">edge</Term> u → v, vertex u comes **before** vertex v in the list. Read an edge u → v as "u must come first." A valid topological order respects every such constraint at once.

**It exists only for a DAG**

A topological order exists if and only if the graph is a <Term slug="directed_acyclic_graph">directed acyclic graph (DAG)</Term>—a directed graph with no <Term slug="cycle">cycle</Term>. The reason is simple. If there were a cycle a → b → c → a, then a must come before b, b before c, and c before a. That demands a comes before itself, which is impossible. So a cycle blocks any valid order. Conversely, every DAG has at least one valid order (often many). Topological order is usually **not unique**: two tasks with no dependency between them can go in either relative order.

**A real example: course prerequisites**

Each arrow means "prerequisite of." You must finish the source course before taking the target.

```
            intro
           /      \
          v        v
data-structures   discrete-math
   |       \       /
   |        v     v
   v        algorithms
databases       |
                v
               ml
```

Edges (the adjacency list):

```
intro:           [data-structures, discrete-math]
data-structures: [algorithms, databases]
discrete-math:   [algorithms]
algorithms:      [ml]
databases:       []
ml:              []
```

A valid plan must take `intro` first (nothing depends on having done anything before it) and `ml` last (it depends on `algorithms`, which depends on two earlier courses). Several valid orders exist; the algorithm below produces one specific one.

**Kahn's algorithm: peel off the things with no prerequisites left**

The <Term slug="kahns_algorithm">Kahn's algorithm</Term> idea is intuitive. The <Term slug="in_degree">in-degree</Term> of a vertex is how many edges point **into** it—how many unmet prerequisites it has. A vertex with in-degree 0 has nothing blocking it, so it can go next. Take one, output it, and "remove" it from the graph by decrementing the in-degree of each vertex it pointed to. Some of those may now drop to 0 and become available. Repeat until every vertex is placed.

1. Compute the in-degree of every vertex.
2. Put all in-degree-0 vertices into a <Term slug="queue">queue</Term>.
3. While the queue is not empty:
   - Pop a vertex v, append it to the output order.
   - For each neighbor w of v: decrement `inDegree[w]`. If it becomes 0, enqueue w.
4. If the output contains all V vertices, it is a topological order. If it has **fewer than V**, the remaining vertices are stuck in a cycle—no order exists.

**Worked Kahn trace on the course graph**

In-degrees to start (count incoming edges):

```
intro: 0   data-structures: 1   discrete-math: 1
algorithms: 2   databases: 1   ml: 1
```

```
Initial: only intro has in-degree 0.
  queue = [intro], order = []

Pop intro. order = [intro].
  decrement data-structures -> 0 (enqueue), discrete-math -> 0 (enqueue).
  queue = [data-structures, discrete-math]

Pop data-structures. order = [intro, data-structures].
  decrement algorithms 2->1 (not 0), databases 1->0 (enqueue).
  queue = [discrete-math, databases]

Pop discrete-math. order = [intro, data-structures, discrete-math].
  decrement algorithms 1->0 (enqueue).
  queue = [databases, algorithms]

Pop databases. order = [..., databases]. No neighbors.
  queue = [algorithms]

Pop algorithms. order = [..., algorithms].
  decrement ml 1->0 (enqueue).
  queue = [ml]

Pop ml. order = [..., ml]. No neighbors.
  queue = []

Output has all 6 vertices -> valid topological order:
intro -> data-structures -> discrete-math -> databases -> algorithms -> ml
```

Every arrow in the graph points forward in that list, so the order is valid.

**The DFS variant: reverse the finish order**

A second algorithm uses <Term slug="depth_first_search">depth-first search</Term>. Run DFS over the whole graph. When a vertex **finishes**—after all its outgoing edges have been fully explored—push it onto a stack. This is <Term slug="dfs_post_order">post-order</Term>. After DFS completes, **reverse** the stack (or pop it) to get a topological order.

Why it works: a vertex finishes only after everything reachable from it has already finished. So in post-order, a vertex appears before all of its descendants. Reversing flips that to "before all things it depends on"—exactly the topological constraint. For the course graph one DFS run produces the post-order `ml, algorithms, databases, data-structures, discrete-math, intro`; reversed, that is `intro -> discrete-math -> data-structures -> databases -> algorithms -> ml`, another valid order (different from Kahn's, but equally correct).

**Two ways to detect a cycle**

Both algorithms double as cycle detectors:

- **Kahn**: if the final output has fewer than V vertices, a cycle remains. Vertices inside a cycle never reach in-degree 0, so they are never enqueued.
- **DFS**: track vertices currently on the recursion stack (the "in-progress" set). If DFS reaches a vertex already in-progress, that back edge closes a cycle—no topological order exists.

</Idea>

<Code lang="en">
**Kahn's algorithm with in-degrees and a queue**

<AnnotatedCode
  code={`function topoSortKahn(adj) {
  // 1. Compute in-degree of every vertex
  const inDegree = {};
  for (const v of Object.keys(adj)) inDegree[v] = 0;
  for (const v of Object.keys(adj)) {
    for (const w of adj[v]) inDegree[w]++;
  }

  // 2. Enqueue every vertex with no prerequisites
  const queue = [];
  for (const v of Object.keys(adj)) {
    if (inDegree[v] === 0) queue.push(v);
  }

  // 3. Repeatedly take a free vertex and relax its edges
  const order = [];
  while (queue.length > 0) {
    const v = queue.shift();
    order.push(v);
    for (const w of adj[v]) {
      inDegree[w]--;
      if (inDegree[w] === 0) queue.push(w);
    }
  }

  // 4. Fewer than V placed => a cycle blocked the rest
  if (order.length < Object.keys(adj).length) {
    return null; // no topological order exists
  }
  return order;
}`}
  annotations={[
    { line: 3, text: "Initialize every vertex's in-degree to 0." },
    { line: 5, text: "Scan all edges; each edge u -> w adds 1 to w's in-degree." },
    { line: 11, text: "A vertex with in-degree 0 has no unmet prerequisite: it can go first." },
    { line: 18, text: "Pop a free vertex from the front of the queue (FIFO)." },
    { line: 19, text: "Append it to the output order." },
    { line: 21, text: "Removing v: each neighbor loses one prerequisite." },
    { line: 22, text: "If a neighbor's in-degree hits 0, it is now free; enqueue it." },
    { line: 27, text: "If not every vertex was placed, the leftovers form a cycle: return null." }
  ]}
/>

**Key point: in-degree 0 means "ready"**

A vertex is ready to be output only when every edge pointing into it has already been removed—that is, when its in-degree drops to 0. Kahn's queue holds exactly the set of "currently ready" vertices. A vertex inside a cycle can never reach in-degree 0 (one of its prerequisites is itself, indirectly), so it never enters the queue. That is precisely why a leftover count below V signals a cycle.

**Running Kahn's algorithm on the course graph**

<CodeRunner
  code={`// Directed acyclic graph of course prerequisites (u -> v: u before v)
const adj = {
  intro: ["data-structures", "discrete-math"],
  "data-structures": ["algorithms", "databases"],
  "discrete-math": ["algorithms"],
  algorithms: ["ml"],
  databases: [],
  ml: []
};

function topoSortKahn(adj) {
  const inDegree = {};
  for (const v of Object.keys(adj)) inDegree[v] = 0;
  for (const v of Object.keys(adj)) {
    for (const w of adj[v]) inDegree[w]++;
  }

  const queue = [];
  for (const v of Object.keys(adj)) {
    if (inDegree[v] === 0) queue.push(v);
  }

  const order = [];
  while (queue.length > 0) {
    const v = queue.shift();
    order.push(v);
    for (const w of adj[v]) {
      inDegree[w]--;
      if (inDegree[w] === 0) queue.push(w);
    }
  }

  if (order.length < Object.keys(adj).length) {
    return null;
  }
  return order;
}

const order = topoSortKahn(adj);
if (order === null) {
  console.log("Cycle detected: no topological order exists");
} else {
  console.log("Topological order:");
  console.log(order.join(" -> "));
}`}
  expected={`Topological order:
intro -> data-structures -> discrete-math -> databases -> algorithms -> ml`}
/>

**DFS variant: push on exit, then reverse**

<AnnotatedCode
  code={`function topoSortDFS(adj) {
  const visited = new Set();
  const stack = [];

  function dfs(v) {
    visited.add(v);
    for (const w of adj[v]) {
      if (!visited.has(w)) dfs(w);
    }
    stack.push(v); // push on EXIT: post-order
  }

  for (const v of Object.keys(adj)) {
    if (!visited.has(v)) dfs(v);
  }

  return stack.reverse(); // reverse post-order = topological order
}`}
  annotations={[
    { line: 6, text: "Mark v visited before exploring its outgoing edges." },
    { line: 8, text: "Recurse into each unvisited neighbor (everything v depends on first)." },
    { line: 10, text: "Push v only after all its descendants finished: this is post-order." },
    { line: 13, text: "Loop over all vertices so disconnected parts are covered too." },
    { line: 16, text: "Reverse the finish order: a vertex now precedes everything it points to." }
  ]}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="kahn-trace"
  lang="en"
  code={[
    "inDegree = { intro:0, data-structures:1, discrete-math:1,",
    "             algorithms:2, databases:1, ml:1 };",
    "queue = [intro];",
    "order = [];"
  ]}
  frames={[
    { caption: "Only intro has in-degree 0. Pop intro into order. Decrement its neighbors: data-structures 1->0 (enqueue), discrete-math 1->0 (enqueue).", line: 3, cells: [{ value: "queue=[data-structures,discrete-math]", mark: "active" }, { value: "order=[intro]" }] },
    { caption: "Pop data-structures. Decrement algorithms 2->1 (not 0), databases 1->0 (enqueue).", line: 3, cells: [{ value: "queue=[discrete-math,databases]", mark: "active" }, { value: "order=[intro,data-structures]" }] },
    { caption: "Pop discrete-math. Decrement algorithms 1->0 (enqueue).", line: 3, cells: [{ value: "queue=[databases,algorithms]", mark: "active" }, { value: "order=[intro,data-structures,discrete-math]" }] },
    { caption: "Pop databases. No outgoing edges, nothing to decrement.", line: 3, cells: [{ value: "queue=[algorithms]", mark: "active" }, { value: "order=[...,databases]" }] },
    { caption: "Pop algorithms. Decrement ml 1->0 (enqueue).", line: 3, cells: [{ value: "queue=[ml]", mark: "active" }, { value: "order=[...,algorithms]" }] },
    { caption: "Pop ml. No outgoing edges. Queue empty.", line: 3, cells: [{ value: "queue=[]", mark: "done" }, { value: "order=[...,ml]" }] },
    { caption: "Output has all 6 vertices, so no cycle. Topological order: intro -> data-structures -> discrete-math -> databases -> algorithms -> ml.", line: 3, cells: [{ value: "done", mark: "done" }, { value: "6 of 6 placed" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity: O(V + E)**

Kahn's algorithm does a constant amount of work per vertex and per edge. Computing in-degrees scans every edge once: O(E). Finding the initial in-degree-0 vertices scans every vertex once: O(V). In the main loop, each vertex is enqueued and popped exactly once (O(V) total), and each edge is examined exactly once when its source vertex is popped and its neighbors are decremented (O(E) total). Summed: **O(V + E)**.

The DFS variant is the same: DFS visits each vertex once and each edge once, plus a push per vertex—**O(V + E)**.

Example: a dependency graph with 6 vertices and 6 edges (the course graph) runs in O(6 + 6) = O(12) work, independent of how the dependencies branch.

**Space complexity: O(V)**

The in-degree map stores one entry per vertex: O(V). The queue can hold up to V vertices when many become ready at once: O(V). The output order list holds up to V vertices: O(V). For the DFS variant, the visited set and the recursion stack are each O(V). Total: **O(V)**.

(The adjacency list itself is O(V + E) of input, not counted as extra working space.)

**Why fewer-than-V means a cycle**

```
Cyclic graph: a -> b -> c -> a (plus an extra a -> d, d in-degree 1)

In-degrees: a:1, b:1, c:1, d:1   -> NO vertex has in-degree 0.

queue starts empty.
The while loop never runs.
order = []  (0 vertices placed)

0 < 4  -> return null: no topological order.

Every vertex in the cycle keeps an unmet prerequisite forever,
so none ever reaches in-degree 0. They are exactly the leftovers.
```

A useful diagnostic: the vertices missing from the output are precisely those trapped in (or downstream of) a cycle.

</Complexity>

<AlgoPractice
  id="practice-topo"
  lessonSlug="05-topological-sort"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In a topological order, an edge u -> v means:",
      choices: [
        "v must appear before u in the order",
        "u must appear before v in the order",
        "u and v must be adjacent in the order",
        "u and v can appear in any order relative to each other"
      ],
      answer: 1,
      hint: "Read u -> v as 'u must come first' (u is a prerequisite of v). A valid topological order places u somewhere before v."
    },
    {
      kind: "choice",
      prompt: "A topological order exists for a directed graph if and only if the graph is:",
      choices: [
        "Strongly connected",
        "Undirected",
        "Acyclic (a DAG)",
        "Complete"
      ],
      answer: 2,
      hint: "A cycle a -> b -> ... -> a would force a to come before itself, which is impossible. Only a directed acyclic graph (DAG) can be topologically sorted."
    },
    {
      kind: "choice",
      prompt: "In Kahn's algorithm, which vertices are enqueued first?",
      choices: [
        "The vertices with the highest in-degree",
        "All vertices with in-degree 0 (no incoming edges)",
        "The vertices with the highest out-degree",
        "A single arbitrary starting vertex"
      ],
      answer: 1,
      hint: "In-degree 0 means no unmet prerequisite, so the vertex is ready to be output. Kahn's seeds the queue with all such vertices."
    },
    {
      kind: "choice",
      prompt: "In the DFS-based topological sort, when is a vertex pushed onto the stack?",
      choices: [
        "When DFS first enters the vertex (pre-order)",
        "When the vertex is enqueued",
        "When DFS finishes the vertex, after exploring all its outgoing edges (post-order); the stack is then reversed",
        "Only if the vertex has in-degree 0"
      ],
      answer: 2,
      hint: "Push on exit (post-order): a vertex finishes only after all its descendants. Reversing the finish order yields a valid topological order."
    },
    {
      kind: "choice",
      prompt: "After Kahn's algorithm runs, how do you know the graph had a cycle (so no topological order exists)?",
      choices: [
        "The queue is empty at the end",
        "The output order contains fewer than V vertices",
        "Some vertex was enqueued twice",
        "The output order is empty even though edges exist"
      ],
      answer: 1,
      hint: "Vertices inside a cycle never reach in-degree 0, so they are never enqueued or output. If the output has fewer than V vertices, a cycle blocked the rest."
    },
    {
      kind: "numeric",
      prompt: "A dependency DAG has 10 vertices and 14 edges. The time complexity O(V + E) corresponds to how many total operations?",
      answer: 24,
      hint: "V + E = 10 + 14 = 24. Kahn's processes each vertex once and each edge once. Total: O(24)."
    }
  ]}
/>

<Inset kind="why" lang="en">
**Why two algorithms?** Kahn's is iterative, uses a queue, and gives cycle detection for free via the leftover count—handy when you also want to report which tasks are stuck. The DFS variant is shorter and reuses the DFS you already know; it is natural when you are already walking the graph for another reason. Both are O(V + E). Pick whichever fits the code you already have; the resulting orders may differ but are equally valid.
</Inset>

<Inset kind="mistake" lang="en">
**Forgetting it is not unique.** Two tasks with no path between them can appear in either order, so the same DAG has many valid topological orders. Tests that check for one exact string are brittle. If you need a deterministic order (for reproducible builds), use a priority queue instead of a plain queue in Kahn's, breaking ties by a fixed key (such as name). The plain-queue version here is deterministic only because the input key order is fixed.
</Inset>

<Inset kind="edgecase" lang="en">
**Cycle means no answer, not a wrong answer.** Topological sort is undefined for a graph with a cycle—there is genuinely no valid order. Do not return a partial list and pretend it is sorted. Return a clear signal (null, an empty result, or an exception) so the caller knows the dependency graph is broken. The leftover-count check (Kahn) or the in-progress back-edge check (DFS) is what surfaces this.
</Inset>

<Inset kind="practice" lang="en">
**Where you will use it.** Build systems order compilation units; package managers order installs; spreadsheets recompute cells; task schedulers run jobs; even resolving import dependencies is a topological sort. Whenever you have "X must happen before Y" constraints over a finite set, model them as a DAG and topologically sort. If the sort fails, you have just detected a circular dependency.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-topo"
    pieceSlug="05-topological-sort"
    lang="en"
    question="Explain what a topological order is, when it exists, how Kahn's algorithm and the DFS variant produce one, how each detects a cycle, and the complexity."
    choices={[
      {
        label: "A topological order lists all vertices of a directed graph so that for every edge u -> v, u comes before v. It exists if and only if the graph is a DAG (no cycle), and is usually not unique. Kahn's algorithm computes in-degrees, enqueues every in-degree-0 vertex, then repeatedly pops one into the output and decrements its neighbors' in-degrees, enqueuing any that hit 0; if fewer than V vertices are output, a cycle remains. The DFS variant pushes each vertex on exit (post-order) and reverses the stack; a back edge to a vertex still in progress signals a cycle. Both run in O(V + E) time and O(V) space.",
        correct: true
      },
      {
        label: "A topological order works on any directed graph, including ones with cycles, and is always unique. Kahn's algorithm starts from the vertex with the highest in-degree and walks downhill. The DFS variant pushes vertices in pre-order (on entry) and does not reverse. Cycles are impossible to detect from either algorithm.",
        misconception: "A topological order exists only for a DAG (cycles make it impossible) and is usually not unique. Kahn's seeds the queue with in-degree-0 vertices, not the max in-degree. The DFS variant pushes in post-order (on exit) and reverses. Both algorithms detect cycles: Kahn via leftover count, DFS via a back edge."
      },
      {
        label: "Topological sort means sorting the vertices by their numeric labels in ascending order. Kahn's algorithm is just a bucket sort on in-degree values, and the DFS variant is a recursive bubble sort. It applies to undirected graphs.",
        misconception: "Topological sort orders by dependency direction (edges), not by vertex labels, and it is unrelated to bucket sort or bubble sort. It applies to directed acyclic graphs, not undirected graphs, because an undirected edge has no 'before/after' direction."
      },
      {
        label: "To topologically sort, run a single BFS from any vertex; the order vertices are dequeued is the topological order, and cycles never matter because BFS handles them automatically.",
        misconception: "Plain BFS dequeue order is not a topological order: it ignores in-degrees, so it can output a vertex before one of its prerequisites. Cycles do matter: they make any topological order impossible, which is exactly what the leftover-count or back-edge check reports."
      }
    ]}
  />
</Check>

<Recap lang="en">
A <Term slug="topological_sort">topological order</Term> of a <Term slug="directed_acyclic_graph">directed acyclic graph (DAG)</Term> lists every <Term slug="vertex">vertex</Term> before all vertices it points to: for each edge u → v, u precedes v. It exists if and only if the graph has no <Term slug="cycle">cycle</Term>, and is usually not unique.

**Kahn's algorithm:** compute every vertex's <Term slug="in_degree">in-degree</Term>, enqueue the in-degree-0 vertices, then repeatedly pop one into the order and decrement its neighbors, enqueuing any that reach 0. If fewer than V vertices come out, a cycle blocked the rest.

**DFS variant:** run DFS, push each vertex on exit (<Term slug="dfs_post_order">post-order</Term>), then reverse the stack. A back edge to an in-progress vertex flags a cycle.

Time: **O(V + E)**. Space: **O(V)**. Both algorithms double as cycle detectors.

Topological sort is the engine behind build systems, package managers, schedulers, and dependency resolution. Next: ordered DAGs power shortest-path and scheduling algorithms.
</Recap>
