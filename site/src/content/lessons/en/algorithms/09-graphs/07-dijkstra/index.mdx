---
concepts:
- dijkstra
- weighted_shortest_path
- min_priority_queue
- binary_heap
- edge_relaxation
- settle_invariant
- lazy_deletion
- decrease_key
- non_negative_weights
- bellman_ford
estMin: 30
lang: en
mathPrereqs: []
order: 7
prereqs:
- 06-shortest-path-bfs
slug: 07-dijkstra
sources:
- https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm
- https://cp-algorithms.com/graph/dijkstra.html
- https://www.geeksforgeeks.org/dijkstras-shortest-path-algorithm-greedy-algo-7/
status: ready
summary: Dijkstra finds shortest paths from a source on graphs with non-negative edge
  weights. A min-heap keyed by tentative distance pops the closest unsettled vertex,
  settles it, then relaxes its edges. Lazy deletion replaces decrease-key. O((V+E)
  log V). Negatives need Bellman-Ford.
title: 'Dijkstra''''''''s algorithm: weighted shortest paths'
track: algorithms
unit: 09-graphs
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Last lesson, BFS gave you shortest paths for free—but only because every edge counted the same. Put a price tag on each edge (this road costs 1, that road costs 7) and BFS breaks: it minimizes the _number_ of edges, not the _total cost_. A 2-edge route can cost far more than a 3-edge detour. Dijkstra's algorithm fixes this. Instead of a plain <Term slug="queue">queue</Term> that pops vertices in arrival order, it uses a <Term slug="min_priority_queue">min-priority-queue</Term> that always pops the vertex with the smallest tentative distance so far. Each pop "settles" a vertex—locks in its final shortest distance—and then "relaxes" its outgoing edges, lowering its neighbors' tentative distances. It is BFS upgraded for weights, and it powers real routing: maps, networks, anything where moving costs differ.
</Hook>

<Goal lang="en">
After this lesson you can implement Dijkstra's algorithm with a binary-heap min-priority-queue. You understand the two core operations: <Term slug="settle_invariant">settle</Term> (pop the closest unsettled vertex and lock its distance as final) and <Term slug="edge_relaxation">relax</Term> (if `dist[u] + w(u,v) < dist[v]`, lower `dist[v]` and record `parent[v] = u`). You maintain `dist[]` and `parent[]` so you can reconstruct a concrete cheapest path. You know the <Term slug="lazy_deletion">lazy-deletion</Term> trick: a binary heap has no decrease-key, so you push duplicate entries and skip stale pops. You understand—and can explain _why_—Dijkstra is correct only with non-negative weights, and that negative edges require <Term slug="bellman_ford">Bellman-Ford</Term> instead. You know BFS is the special case where all weights equal 1. Cost: O((V+E) log V) time, O(V) space.
</Goal>

<Idea lang="en">
**The weighted shortest path question**

When you add weights to a graph, the problem shifts from "how many hops?" to "how much does it cost?"—and that shift breaks BFS entirely. You need a way to always expand the cheapest frontier, not just the nearest one. In a <Term slug="weighted_shortest_path">weighted graph</Term>, every <Term slug="edge">edge</Term> carries a number—its weight or cost. The length of a path is the _sum_ of its edge weights, not the count of edges. The shortest path from a source to a target is the path with the smallest total cost. We want, for every reachable <Term slug="vertex">vertex</Term>, that minimum cost plus a concrete route.

**Why BFS is not enough**

BFS pops vertices in arrival order (FIFO), which equals distance order only when all edges cost the same. With mixed weights, a vertex that is few edges away might be expensive, and a vertex many edges away might be cheap. We need to always expand the _cheapest_ frontier next, not the _nearest-in-edges_ frontier. That is the one change Dijkstra makes: swap the FIFO queue for a <Term slug="min_priority_queue">min-priority-queue</Term> keyed by tentative distance.

**Settle and relax: the two operations**

Dijkstra keeps a tentative distance `dist[v]` for every vertex (start at infinity, except `dist[source] = 0`). It repeats two steps until the queue is empty:

- **Settle:** pop the unsettled vertex `u` with the smallest `dist[u]`. Its distance is now _final_—no cheaper route to `u` can exist. Mark it settled.
- **Relax:** for each outgoing edge `u -> v` of weight `w`, check whether going through `u` beats the best route known to `v`. If `dist[u] + w < dist[v]`, then update `dist[v] = dist[u] + w`, set `parent[v] = u`, and push `v` into the queue with its new key.

The <Term slug="settle_invariant">settle invariant</Term> is the heart of the proof: when a vertex is popped as the minimum, every other route to it must pass through some still-larger tentative distance, so it cannot be cheaper. This is exactly why **non-negative weights are required** (more below).

**Example graph (directed, weighted)**

```
Adjacency list (vertex -> [neighbor, weight]):
A: [B:4, C:1]
B: [E:4]
C: [B:2, D:5]
D: [E:2]
E: [F:3]
F: []

Visual (arrows show direction, numbers are weights):

        4
   A ------> B
   |         |
  1|         |4
   v   2     v   3
   C ------> E ------> F
   |         ^
  5|        2|
   v         |
   D --------+
```

Note the trap the direct edge A->B sets: it costs 4, but the detour A->C->B costs 1+2 = 3. Dijkstra must discover the cheaper route through C.

**Worked relaxation trace (source = A)**

Start with `dist[A]=0`, everything else infinity. The min-heap holds `(distance, vertex)` pairs.

```
heap = [(0,A)]          dist: A=0  B=inf C=inf D=inf E=inf F=inf

Pop (0,A). Settle A. Relax A's edges:
  A->B (4): 0+4=4 < inf  -> dist[B]=4, parent[B]=A, push (4,B)
  A->C (1): 0+1=1 < inf  -> dist[C]=1, parent[C]=A, push (1,C)
heap = [(1,C),(4,B)]    dist: A=0 B=4 C=1 D=inf E=inf F=inf

Pop (1,C). Settle C. Relax C's edges:
  C->B (2): 1+2=3 < 4    -> dist[B]=3, parent[B]=C, push (3,B)   (cheaper!)
  C->D (5): 1+5=6 < inf  -> dist[D]=6, parent[D]=C, push (6,D)
heap = [(3,B),(4,B),(6,D)]   dist: A=0 B=3 C=1 D=6 E=inf F=inf

Pop (3,B). Settle B. Relax B's edges:
  B->E (4): 3+4=7 < inf  -> dist[E]=7, parent[E]=B, push (7,E)
heap = [(4,B),(6,D),(7,E)]   dist: A=0 B=3 C=1 D=6 E=7 F=inf

Pop (4,B). B is already settled -> STALE, skip. (lazy deletion)
heap = [(6,D),(7,E)]

Pop (6,D). Settle D. Relax D's edges:
  D->E (2): 6+2=8 < 7?  No (8 >= 7) -> no update
heap = [(7,E)]

Pop (7,E). Settle E. Relax E's edges:
  E->F (3): 7+3=10 < inf -> dist[F]=10, parent[F]=E, push (10,F)
heap = [(10,F)]

Pop (10,F). Settle F. No outgoing edges.
heap = []  Done.

Final distances: A=0  B=3  C=1  D=6  E=7  F=10
parent: A=null B=C C=A D=C E=B F=E
```

Two things to notice. First, B was _relaxed twice_: tentatively 4 via A, then improved to 3 via C. The stale `(4,B)` entry stayed in the heap and got skipped when popped. Second, the direct edge A->B (cost 4) lost to the detour A->C->B (cost 3)—relaxation found the cheaper route.

**Reconstructing the concrete path A -> F**

Walk `parent[]` backward from the target, then reverse:

```
start at F        path = [F]              parent[F]=E
go to E           path = [F, E]           parent[E]=B
go to B           path = [F, E, B]        parent[B]=C
go to C           path = [F, E, B, C]     parent[C]=A
go to A           path = [F, E, B, C, A]  parent[A]=null -> stop
reverse           path = [A, C, B, E, F]
```

The cheapest path is **A -> C -> B -> E -> F**, total cost 10, matching `dist[F]=10`.

**BFS is Dijkstra with all weights = 1**

If every edge weight is 1, "smallest tentative distance" equals "fewest edges," and the min-priority-queue degenerates into BFS's FIFO order. So BFS is the unit-weight special case of Dijkstra. Dijkstra is the generalization that handles arbitrary non-negative weights.

</Idea>

<DiagramFrame
  label="Dijkstra finished on the example directed weighted graph from source A. Six vertices: A top-left, B top-right, C below A, E center, F right, D bottom-left. Each vertex shows its final shortest distance from A in a badge: A=0, B=3, C=1, D=6, E=7, F=10. Edges carry positive weights: A to B costs 4, A to C costs 1, C to B costs 2, C to D costs 5, B to E costs 4, D to E costs 2, E to F costs 3. The min-priority-queue always pops the closest unfinalized vertex and relaxes its outgoing edges. The highlighted shortest-path tree (thick accent edges) is A to C, C to B, C to D, B to E, E to F. The two thin grey edges A to B and D to E were relaxed but lost: the direct A to B (4) lost to A to C to B (1+2=3), and D to E (6+2=8) lost to B to E (3+4=7). The cheapest path A to F is A to C to B to E to F, total cost 1+2+4+3=10, matching the distance badge on F."
  hue="var(--d-systems)"
  caption="Dijkstra done from source A. Each badge is a vertex's FINAL distance (sum of weights along its cheapest route). The min-priority-queue pops the closest unfinalized vertex, settles it, then relaxes its edges. Thick accent edges form the shortest-path tree; the two thin grey edges (A→B=4 beaten by A→C→B=3; D→E beaten on the way to E) were relaxed but never made the tree. Shortest path A→F = A→C→B→E→F, cost 10."
>
  <svg viewBox="0 0 560 360" width="560" height="360">
    <text x="20" y="22" font-size="12" font-family="var(--font-mono)" fill="var(--muted)">{"thick = shortest-path tree   ·   thin grey = relaxed but not chosen"}</text>
    <line x1="92" y1="60" x2="278" y2="60" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="5 4" />
    <line x1="70" y1="278" x2="278" y2="190" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="5 4" />
    <line x1="70" y1="82" x2="70" y2="158" stroke="var(--accent)" stroke-width="3" />
    <line x1="88" y1="170" x2="282" y2="78" stroke="var(--accent)" stroke-width="3" />
    <line x1="70" y1="202" x2="70" y2="278" stroke="var(--accent)" stroke-width="3" />
    <line x1="300" y1="82" x2="300" y2="158" stroke="var(--accent)" stroke-width="3" />
    <line x1="322" y1="180" x2="468" y2="180" stroke="var(--accent)" stroke-width="3" />
    <text x="185" y="50" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--muted)">4</text>
    <text x="58" y="124" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">1</text>
    <text x="180" y="116" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">2</text>
    <text x="58" y="244" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">5</text>
    <text x="314" y="124" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">4</text>
    <text x="190" y="252" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--muted)">2</text>
    <text x="395" y="170" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">3</text>
    <circle cx="70" cy="60" r="22" fill="var(--card-2)" stroke="var(--accent)" stroke-width="2.4" />
    <text x="70" y="66" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">A</text>
    <circle cx="300" cy="60" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="300" y="66" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">B</text>
    <circle cx="70" cy="180" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="70" y="186" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">C</text>
    <circle cx="300" cy="180" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="300" y="186" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">E</text>
    <circle cx="490" cy="180" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="490" y="186" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">F</text>
    <circle cx="70" cy="300" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="70" y="306" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">D</text>
    <circle cx="92" cy="42" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="92" y="46" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">0</text>
    <circle cx="322" cy="42" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="322" y="46" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">3</text>
    <circle cx="92" cy="162" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="92" y="166" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">1</text>
    <circle cx="322" cy="162" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="322" y="166" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">7</text>
    <circle cx="512" cy="162" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="512" y="166" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">10</text>
    <circle cx="92" cy="282" r="12" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="92" y="286" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--on-accent)">6</text>
    <text x="20" y="338" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"badge ● = final dist from A   ·   path A→F = A→C→B→E→F = 1+2+4+3 = 10"}</text>
  </svg>
</DiagramFrame>

<Code lang="en">
**A min-heap (binary heap) for the priority queue**

We need a structure that returns the smallest tentative distance fast. A binary min-heap gives O(log n) push and pop. It has no efficient decrease-key, so we will use lazy deletion instead.

<AnnotatedCode
  code={`class MinHeap {
  constructor() { this.items = []; }
  size() { return this.items.length; }

  push(item) {              // item = [distance, vertex]
    const a = this.items;
    a.push(item);
    let i = a.length - 1;
    while (i > 0) {          // sift up to keep min at the root
      const parent = (i - 1) >> 1;
      if (a[parent][0] <= a[i][0]) break;
      [a[parent], a[i]] = [a[i], a[parent]];
      i = parent;
    }
  }

  pop() {                   // remove and return the smallest
    const a = this.items;
    const top = a[0];
    const last = a.pop();
    if (a.length > 0) {
      a[0] = last;
      let i = 0; const n = a.length;
      while (true) {         // sift down to restore heap order
        let smallest = i;
        const l = 2 * i + 1, r = 2 * i + 2;
        if (l < n && a[l][0] < a[smallest][0]) smallest = l;
        if (r < n && a[r][0] < a[smallest][0]) smallest = r;
        if (smallest === i) break;
        [a[smallest], a[i]] = [a[i], a[smallest]];
        i = smallest;
      }
    }
    return top;
  }
}`}
  annotations={[
    { line: 5, text: "Each item is [distance, vertex]; we order by item[0], the distance." },
    { line: 9, text: "Sift up: bubble the new item toward the root until its parent is smaller." },
    { line: 18, text: "pop() always returns the smallest-distance pair: the closest frontier vertex." },
    { line: 24, text: "Sift down: push the moved last element back down to its correct level." },
    { line: 27, text: "Compare both children and swap with the smaller, repeating until settled." }
  ]}
/>

**Dijkstra with the heap, dist[], and parent[]**

<AnnotatedCode
  code={`function dijkstra(source, adj) {
  const dist = {};
  const parent = {};
  for (const v in adj) dist[v] = Infinity; // unknown = infinity
  dist[source] = 0;                        // source is 0 from itself
  parent[source] = null;

  const settled = new Set();
  const heap = new MinHeap();
  heap.push([0, source]);

  while (heap.size() > 0) {
    const [d, u] = heap.pop();        // closest unsettled vertex
    if (settled.has(u)) continue;     // stale entry: skip (lazy deletion)
    settled.add(u);                   // u's distance is now FINAL

    for (const [v, w] of adj[u]) {    // relax each outgoing edge
      if (dist[u] + w < dist[v]) {
        dist[v] = dist[u] + w;        // cheaper route to v found
        parent[v] = u;                // remember the breadcrumb
        heap.push([dist[v], v]);      // push, do NOT decrease-key
      }
    }
  }
  return { dist, parent };
}`}
  annotations={[
    { line: 4, text: "Every vertex starts at infinity: no route known yet." },
    { line: 5, text: "The source costs 0 to reach from itself." },
    { line: 8, text: "settled = vertices whose final distance is locked. Drives the skip below." },
    { line: 13, text: "If u is already settled, this is a stale duplicate; ignore it." },
    { line: 14, text: "Settling u commits dist[u] as final: the settle invariant." },
    { line: 17, text: "Relaxation test: does routing through u beat the best known to v?" },
    { line: 18, text: "Lower dist[v] only when strictly cheaper, so we never inflate a distance." },
    { line: 20, text: "No decrease-key: push a fresh (smaller) entry; the old one becomes stale." }
  ]}
/>

**Reconstructing a concrete path**

<AnnotatedCode
  code={`function reconstructPath(target, parent) {
  const path = [];
  let cur = target;
  while (cur !== null && cur !== undefined) {
    path.push(cur);        // collect target back to source
    cur = parent[cur];     // step to the predecessor
  }
  path.reverse();          // flip to read source -> target
  return path;
}`}
  annotations={[
    { line: 4, text: "null marks the source; undefined means the target was never reached." },
    { line: 5, text: "We gather vertices in reverse: target first, source last." },
    { line: 8, text: "Reverse to get the natural source-to-target direction." }
  ]}
/>

**Running it on the example graph**

<CodeRunner
  code={`// Directed, weighted graph: vertex -> [[neighbor, weight], ...]
const adj = {
  A: [["B", 4], ["C", 1]],
  B: [["E", 4]],
  C: [["B", 2], ["D", 5]],
  D: [["E", 2]],
  E: [["F", 3]],
  F: []
};

// Min-heap of [distance, vertex], ordered by distance.
// No decrease-key: push duplicates, skip stale pops (lazy deletion).
class MinHeap {
  constructor() { this.items = []; }
  size() { return this.items.length; }
  push(item) {
    const a = this.items;
    a.push(item);
    let i = a.length - 1;
    while (i > 0) {
      const parent = (i - 1) >> 1;
      if (a[parent][0] <= a[i][0]) break;
      [a[parent], a[i]] = [a[i], a[parent]];
      i = parent;
    }
  }
  pop() {
    const a = this.items;
    const top = a[0];
    const last = a.pop();
    if (a.length > 0) {
      a[0] = last;
      let i = 0; const n = a.length;
      while (true) {
        let smallest = i;
        const l = 2 * i + 1, r = 2 * i + 2;
        if (l < n && a[l][0] < a[smallest][0]) smallest = l;
        if (r < n && a[r][0] < a[smallest][0]) smallest = r;
        if (smallest === i) break;
        [a[smallest], a[i]] = [a[i], a[smallest]];
        i = smallest;
      }
    }
    return top;
  }
}

function dijkstra(source, adj) {
  const dist = {};
  const parent = {};
  for (const v in adj) dist[v] = Infinity;
  dist[source] = 0;
  parent[source] = null;

  const settled = new Set();
  const heap = new MinHeap();
  heap.push([0, source]);

  while (heap.size() > 0) {
    const [d, u] = heap.pop();
    if (settled.has(u)) continue;
    settled.add(u);

    for (const [v, w] of adj[u]) {
      if (dist[u] + w < dist[v]) {
        dist[v] = dist[u] + w;
        parent[v] = u;
        heap.push([dist[v], v]);
      }
    }
  }
  return { dist, parent };
}

function reconstructPath(target, parent) {
  const path = [];
  let cur = target;
  while (cur !== null && cur !== undefined) {
    path.push(cur);
    cur = parent[cur];
  }
  path.reverse();
  return path;
}

const { dist, parent } = dijkstra("A", adj);

console.log("Shortest distances from A:");
for (const v of ["A", "B", "C", "D", "E", "F"]) {
  console.log("  " + v + ": " + dist[v]);
}

const path = reconstructPath("F", parent);
console.log("\\nShortest path A -> F: " + path.join(" -> "));
console.log("Cost: " + dist["F"]);`}
  expected={`Shortest distances from A:
  A: 0
  B: 3
  C: 1
  D: 6
  E: 7
  F: 10

Shortest path A -> F: A -> C -> B -> E -> F
Cost: 10`}
/>

**A note on the priority queue**

For clarity you could replace the binary heap with a sorted-array PQ (scan for the minimum each pop). It is simpler to read but gives O(V&sup2;) overall, fine for small graphs. The binary heap is what delivers the O((V+E) log V) bound stated below; production code often uses a Fibonacci heap (O(V log V + E)) or a d-ary heap, but a binary heap is the standard practical choice.

</Code>

<Trace lang="en">
<AlgoTrace
  id="dijkstra-trace"
  lang="en"
  code={[
    "adj = { A:[B4,C1], B:[E4], C:[B2,D5], D:[E2], E:[F3], F:[] };",
    "dist[A]=0; rest=inf; heap=[(0,A)];",
    "pop min -> settle -> relax outgoing edges;"
  ]}
  frames={[
    { caption: "Init heap=[(0,A)], dist[A]=0. Pop (0,A), settle A. Relax: B=4 (via A), C=1 (via A). Push both.", line: 3, cells: [{ value: "settle A", mark: "done" }, { value: "B=4", mark: "active" }, { value: "C=1", mark: "active" }] },
    { caption: "Pop (1,C), settle C. Relax: B improves 4 -> 3 (via C, cheaper!), D=6 (via C). Push (3,B) and (6,D).", line: 3, cells: [{ value: "settle C", mark: "done" }, { value: "B=3", mark: "active" }, { value: "D=6", mark: "active" }] },
    { caption: "Pop (3,B), settle B. Relax: E=7 (via B). Push (7,E). The stale (4,B) is still in the heap.", line: 3, cells: [{ value: "settle B", mark: "done" }, { value: "E=7", mark: "active" }, { value: "(4,B) stale", mark: "target" }] },
    { caption: "Pop (4,B). B is already settled -> STALE, skip it (lazy deletion). No work done.", line: 3, cells: [{ value: "skip (4,B)", mark: "target" }, { value: "B settled" }, { value: "no change" }] },
    { caption: "Pop (6,D), settle D. Relax D->E: 6+2=8, not < 7, so NO update. E keeps parent B.", line: 3, cells: [{ value: "settle D", mark: "done" }, { value: "E stays 7" }, { value: "no update" }] },
    { caption: "Pop (7,E), settle E. Relax E->F: F=10 (via E). Push (10,F).", line: 3, cells: [{ value: "settle E", mark: "done" }, { value: "F=10", mark: "active" }, { value: "" }] },
    { caption: "Pop (10,F), settle F. No outgoing edges. Heap empty. Done. dist: A0 B3 C1 D6 E7 F10.", line: 3, cells: [{ value: "settle F", mark: "done" }, { value: "heap=[]", mark: "done" }, { value: "all final" }] },
    { caption: "Reconstruct F: walk parent F->E->B->C->A (null stops), reverse to A->C->B->E->F. Cost = dist[F] = 10.", line: 3, cells: [{ value: "A->C->B->E->F", mark: "done" }, { value: "cost=10" }, { value: "matches dist[F]" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity: O((V + E) log V)**

Each vertex is settled exactly once. To settle, we pop the heap; with lazy deletion the heap can hold up to one entry per relaxation, so up to O(E) entries, but its size is bounded by the number of pushes. Each `push` and each `pop` costs O(log of heap size) = O(log V) (the heap never holds more than O(E) entries, and log E = O(log V) because E < V&sup2;). Summed:

- Settling all vertices: V pops, each O(log V) = O(V log V).
- Relaxing all edges: each edge relaxation may push once, E pushes, each O(log V) = O(E log V).

Total: **O((V + E) log V)**. On a connected graph E >= V - 1, so this is often written O(E log V).

```
sorted-array PQ : O(V^2)            -> good for dense graphs (E ~ V^2)
binary heap     : O((V+E) log V)    -> good for sparse graphs (E ~ V)
Fibonacci heap  : O(V log V + E)    -> best asymptotic, rarely needed
```

**Space complexity: O(V)**

`dist[]` holds up to V entries, `parent[]` up to V, the `settled` set up to V. The heap with lazy deletion can transiently hold up to O(E) stale entries, but the bookkeeping arrays are O(V). The dominant working set is **O(V)** (heap is O(E) in the worst case, which equals O(V) on sparse graphs).

**Why non-negative weights are required (the settle invariant breaks otherwise)**

Dijkstra's correctness rests on this claim: when a vertex `u` is popped as the heap minimum, `dist[u]` is final. The argument: any other path to `u` must leave the settled region through some unsettled vertex `x` whose tentative distance is >= `dist[u]` (else `x` would have been popped first). Since edges only _add_ cost, the full path through `x` costs at least `dist[x] >= dist[u]`. So no cheaper route to `u` exists.

That argument silently assumes edges never _reduce_ the running total. A negative edge breaks it:

```
A --1--> B
A --4--> C
B --(-3)--> C       (negative edge!)

Dijkstra from A:
  Settle A. Relax: dist[B]=1, dist[C]=4.
  Pop (1,B) -> settle B (LOCKED). Relax B->C: 1 + (-3) = -2 < 4
      -> dist[C] = -2.
  Pop next... but C might already be settled at the wrong value
      in a larger graph, and B's "final" distance can itself be
      undercut later by a negative edge feeding back.

True shortest A->C = A->B->C = 1 + (-3) = -2, but Dijkstra's
settle step can commit a vertex BEFORE a later negative edge
would have lowered it. The invariant fails.
```

A settled vertex can no longer be improved—but a negative edge is exactly an improvement arriving _after_ settling. So Dijkstra can return wrong answers on negative weights. For graphs with negative edges, use <Term slug="bellman_ford">Bellman-Ford</Term> (O(V*E), relaxes all edges V-1 times and detects negative cycles). Dijkstra trades that generality for speed, and the price is the non-negativity requirement.

</Complexity>

<AlgoPractice
  id="practice-dijkstra"
  lessonSlug="07-dijkstra"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "What does Dijkstra use instead of BFS's FIFO queue, and why?",
      choices: [
        "A stack (LIFO), to go deep first.",
        "A min-priority-queue keyed by tentative distance, so it always settles the cheapest-cost unsettled vertex next.",
        "A plain array scanned left to right.",
        "A max-priority-queue, to settle the farthest vertex first."
      ],
      answer: 1,
      hint: "With weights, 'closest in cost' is not 'first to arrive'. A min-priority-queue pops the smallest tentative distance, which is the next vertex whose distance is final."
    },
    {
      kind: "choice",
      prompt: "What is the relaxation step for an edge u -> v of weight w?",
      choices: [
        "If dist[u] + w < dist[v], set dist[v] = dist[u] + w and parent[v] = u.",
        "Always set dist[v] = dist[u] + w.",
        "If dist[v] + w < dist[u], settle v immediately.",
        "Subtract w from dist[u] if v is unvisited."
      ],
      answer: 0,
      hint: "Relax means: does routing through u give v a cheaper distance than its current best? Only update when strictly cheaper, and record u as v's parent."
    },
    {
      kind: "choice",
      prompt: "A binary heap has no efficient decrease-key. How does the standard Dijkstra implementation cope?",
      choices: [
        "It rebuilds the entire heap after every relaxation.",
        "Lazy deletion: push a new (smaller) entry on each improvement, and skip any popped entry whose vertex is already settled.",
        "It forbids any vertex from being relaxed more than once.",
        "It switches to a stack when decrease-key is needed."
      ],
      answer: 1,
      hint: "Pushing a duplicate is cheap. The old larger entry becomes stale; when it surfaces, the settled check discards it. Correctness holds because the smallest entry is always popped first."
    },
    {
      kind: "choice",
      prompt: "Why does Dijkstra require non-negative edge weights?",
      choices: [
        "Because heaps cannot store negative numbers.",
        "Because settling a vertex commits its distance as final, but a negative edge could lower it AFTER it was settled, breaking the settle invariant.",
        "Because negative weights make the graph disconnected.",
        "It does not; Dijkstra works on any weights."
      ],
      answer: 1,
      hint: "When the heap minimum is popped, Dijkstra assumes no cheaper route can appear later. Adding cost can only grow a path, so that holds. A negative edge can shrink a path after settling, so the assumption fails. Use Bellman-Ford for negatives."
    },
    {
      kind: "choice",
      prompt: "How is BFS related to Dijkstra?",
      choices: [
        "They are unrelated algorithms.",
        "BFS is Dijkstra's special case where every edge weight equals 1, so 'smallest distance' becomes 'fewest edges' and the heap degenerates into a FIFO queue.",
        "Dijkstra is BFS run twice.",
        "BFS handles weighted graphs; Dijkstra handles unweighted ones."
      ],
      answer: 1,
      hint: "When all weights are 1, tentative distance equals edge count, and the priority queue pops in arrival order, which is exactly BFS."
    },
    {
      kind: "numeric",
      prompt: "In the lesson graph (A:[B4,C1], B:[E4], C:[B2,D5], D:[E2], E:[F3]), what is dist[F], the cheapest total cost from A to F?",
      answer: 10,
      hint: "Cheapest route is A->C->B->E->F = 1 + 2 + 4 + 3 = 10. The direct A->B (4) loses to A->C->B (3)."
    }
  ]}
/>

<Inset kind="why" lang="en">
**Why settle then relax, in that order?** Settling a vertex is the act of trusting its tentative distance as final. That trust is only valid for the current heap minimum, because every alternative route must pass through an equal-or-larger tentative distance (non-negative weights guarantee it). Once `u` is trusted, relaxing its edges propagates that final value outward to neighbors. Relaxing before settling would let unfinalized, possibly-wrong distances spread.
</Inset>

<Inset kind="mistake" lang="en">
**Relaxing or re-settling a stale pop.** With lazy deletion the heap holds duplicates. If you forget the `if (settled.has(u)) continue;` guard, you will process an outdated entry: re-settle an already-final vertex and relax its edges with a larger, stale distance. That wastes work and—if combined with overwriting `dist`—can corrupt results. Always skip a pop whose vertex is already settled.
</Inset>

<Inset kind="edgecase" lang="en">
**Unreachable vertices.** If a vertex sits in no path from the source, it is never pushed, so its `dist` stays `Infinity` and it has no `parent`. Reconstruction from such a target stops immediately (cur is undefined) and returns a list that does not start at the source. Check `dist[target] !== Infinity` before reporting a path, and say "no path" otherwise.
</Inset>

<Inset kind="practice" lang="en">
**Negative weights -> Bellman-Ford.** The moment any edge can be negative, drop Dijkstra. <Term slug="bellman_ford">Bellman-Ford</Term> relaxes _every_ edge V-1 times in O(V*E) and, on a Vth pass, detects negative cycles (where "shortest path" is undefined because you can loop to -infinity). It is slower but tolerates negatives. Real systems pick Dijkstra when costs are physical and non-negative (distance, latency, money) and Bellman-Ford when costs can be credits or refunds.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-dijkstra"
    pieceSlug="07-dijkstra"
    lang="en"
    question="Explain how Dijkstra computes weighted shortest paths, what settle and relax mean, why lazy deletion is used with a binary heap, and why the algorithm needs non-negative weights."
    choices={[
      {
        label: "Dijkstra keeps a tentative dist[] (source 0, rest infinity) and a min-priority-queue keyed by distance. It repeatedly settles the popped minimum (its distance is now final) and relaxes its outgoing edges: if dist[u]+w < dist[v], lower dist[v], set parent[v]=u, and push v. A binary heap has no decrease-key, so it uses lazy deletion: push duplicate entries and skip pops of already-settled vertices. It is correct only with non-negative weights, because settling assumes no cheaper route can appear later; a negative edge could lower a distance after settling. Negatives need Bellman-Ford. Time O((V+E) log V), space O(V).",
        correct: true
      },
      {
        label: "Dijkstra works on any weighted graph, positive or negative, because the heap automatically corrects distances later. Relax means always set dist[v] = dist[u] + w. Lazy deletion means deleting the source after settling it. BFS and Dijkstra are unrelated.",
        misconception: "Dijkstra fails on negative weights: settling commits a distance that a later negative edge could undercut. Relaxation updates only when strictly cheaper, not always. Lazy deletion means skipping stale duplicate pops, not deleting the source. BFS is exactly the unit-weight case of Dijkstra."
      },
      {
        label: "Dijkstra settles the vertex with the LARGEST tentative distance first, using a max-priority-queue, then relaxes inward. The parent[] array stores edge weights. Path reconstruction reads parent[] forward from the source without reversing.",
        misconception: "Dijkstra settles the SMALLEST tentative distance via a min-priority-queue. parent[] stores predecessor vertices, not weights. Reconstruction walks backward from the target and must be reversed to read source-to-target."
      },
      {
        label: "Dijkstra needs non-negative weights because heaps cannot represent negative numbers, and because every vertex must be relaxed exactly once. Settling a vertex can be undone if a cheaper route shows up later.",
        misconception: "Heaps store negative numbers fine; the real reason is the settle invariant, which assumes added cost only grows a path. A vertex may be relaxed many times before it is settled, and once settled it is never reopened in Dijkstra."
      }
    ]}
  />
</Check>

<Recap lang="en">
<Term slug="dijkstra">Dijkstra's algorithm</Term> finds shortest paths from a source on a <Term slug="weighted_shortest_path">weighted graph</Term> with non-negative edge weights. It generalizes BFS by swapping the FIFO queue for a <Term slug="min_priority_queue">min-priority-queue</Term> keyed by tentative distance.

**Two operations:** <Term slug="settle_invariant">settle</Term> the popped minimum (its `dist` is now final), then <Term slug="edge_relaxation">relax</Term> its outgoing edges—`if dist[u] + w < dist[v]`, lower `dist[v]` and set `parent[v] = u`.

**Bookkeeping:** `dist[]` for costs, `parent[]` for the breadcrumb trail. Walk `parent[]` backward from the target and reverse to rebuild a concrete cheapest path.

**Lazy deletion:** a binary heap has no decrease-key, so push duplicate entries on each improvement and skip any pop whose vertex is already settled.

Time: **O((V + E) log V)**. Space: **O(V)**.

**The boundary:** non-negative weights only. Settling commits a distance as final, but a negative edge could lower it afterward, breaking the settle invariant—use <Term slug="bellman_ford">Bellman-Ford</Term> for negatives. BFS is the special case where every weight is 1. Now when you see a weighted graph and a "find the cheapest path" requirement, you will know to wire up the min-heap, skip stale pops, and check for non-negative weights before Dijkstra—not after.
</Recap>
