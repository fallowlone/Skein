---
concepts:
- breadth_first_search
- bfs
- graph_traversal
- queue
- visited_set
- level_order
- unweighted_shortest_path
- mark_on_enqueue
estMin: 28
lang: en
mathPrereqs: []
order: 3
prereqs:
- 01-graph-representations
- 02-depth-first-search
slug: 03-breadth-first-search
sources:
- https://en.wikipedia.org/wiki/Breadth-first_search
- https://www.geeksforgeeks.org/breadth-first-search-or-bfs-for-a-graph/
- https://opendsa-server.cs.vt.edu/ODSA/Books/Everything/html/GraphTraversal.html
- https://cp-algorithms.com/graph/breadth-first-search.html
status: ready
summary: Explores a graph level by level using a queue (not a stack). Visited set
  prevents cycles. Mark on enqueue. Time O(V+E), space O(V). BFS finds shortest paths
  (fewest edges) in unweighted graphs.
title: Breadth-first search (BFS)
track: algorithms
unit: 09-graphs
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You learned tree traversal: start at a root, visit children, then grandchildren, in some order. In the last lesson, you learned DFS—depth-first search—which explores as far down as possible along each branch before backtracking. This lesson teaches BFS—breadth-first search—a different order: explore all neighbors at distance 1 before moving to distance 2. Instead of a stack, use a <Term slug="queue">queue</Term> (FIFO). Unlike DFS which dives deep, BFS spreads wide, visiting all vertices at the current "level" before stepping to the next. This matters because BFS has a special property: it finds the shortest path (fewest edges) in graphs with no weights.
</Hook>

<Goal lang="en">
After this lesson you can write BFS iteratively using a queue. You understand the difference between BFS and DFS: queue vs. stack, level-by-level vs. depth-first. You can trace BFS on a concrete graph and predict the visit order. You understand why the <Term slug="visited_set">visited set</Term> prevents infinite loops and how the mark-on-enqueue discipline works. You know BFS's time complexity O(V + E) and space complexity O(V). You understand the key property: BFS from a source finds the shortest path (in edges) to every reachable vertex in an unweighted graph. Next lesson is connected components, which uses BFS (or DFS) to find all disjoint parts of a graph.
</Goal>

<Idea lang="en">
**What is breadth-first search?**

<Term slug="breadth_first_search">Breadth-first search (BFS)</Term> is a graph traversal that explores all <Term slug="vertex">vertices</Term> at distance d from a source before exploring vertices at distance d+1. It visits every vertex reachable from a starting vertex, in level-by-level order. The key data structure is a <Term slug="queue">queue</Term> (FIFO): dequeue a vertex, mark and enqueue its unvisited <Term slug="neighbor">neighbors</Term>, repeat.

**BFS vs. DFS: queue vs. stack**

Both algorithms visit all reachable vertices and have the same time complexity O(V+E). The difference is the order:

- **DFS** (from the last lesson) uses a stack (LIFO). It goes deep along one branch, then backtracks to explore another branch.
- **BFS** uses a queue (FIFO). It explores all neighbors at distance 1, then all at distance 2, and so on.

Example: graph with vertices 0, 1, 2, 3 and edges 0→1, 0→2, 1→3, 2→3:

```
DFS from 0: 0, 1, 3, 2 (depth: goes to 1, then 3, then backtracks to 0, then 2)
BFS from 0: 0, 1, 2, 3 (level-by-level: level 0 = {0}, level 1 = {1,2}, level 2 = {3})
```

**The BFS algorithm: mark-on-enqueue**

Why does the order of marking matter so much? Because one missed mark can cause a vertex to flood the queue with duplicates, silently blowing O(V) space into O(E). The standard BFS discipline is to mark a vertex visited when it is enqueued, not when it is dequeued. This prevents the same vertex from being enqueued multiple times:

1. Start: mark the source visited, enqueue it. queue = [source], visited = &#123;source&#125;.
2. While queue is not empty:
   - Dequeue a vertex v.
   - For each neighbor u of v:
     - If u is not visited, mark u visited and enqueue u.

This ensures each vertex enters the queue once and is processed once.

**Example graph**

Same graph as lesson 02 (DFS):

```
Adjacency list:
0: [1, 2]
1: [3]
2: [3]
3: [2]

Visual:
  0 → 1
  ↓   ↓
  2 → 3
  ↑   ↓
  +---+
```

**BFS trace on the example**

Starting from vertex 0:

```
Initial: queue = [0], visited = {0}

Step 1: Dequeue 0. Neighbors: 1, 2.
  - 1 not visited: mark visited, enqueue. visited = {0, 1}, queue = [1, 2]
  - 2 not visited: mark visited, enqueue. visited = {0, 1, 2}, queue = [1, 2]

Step 2: Dequeue 1. Neighbors: 3.
  - 3 not visited: mark visited, enqueue. visited = {0, 1, 2, 3}, queue = [2, 3]

Step 3: Dequeue 2. Neighbors: 3.
  - 3 already visited: skip.

Step 4: Dequeue 3. Neighbors: 2.
  - 2 already visited: skip.

Step 5: Queue empty. Done.
Visit order (by dequeue): 0, 1, 2, 3
Levels:
  - Level 0 (distance 0): {0}
  - Level 1 (distance 1): {1, 2}
  - Level 2 (distance 2): {3}
```

This is the power of BFS: it naturally computes the shortest path (fewest edges) to each vertex.

<DiagramFrame
  label="A small graph drawn as concentric distance rings around the start node 0. Ring at distance 0 holds the start node 0; the inner ring at distance 1 holds nodes 1 and 2 (direct neighbors of 0); the outer ring at distance 2 holds nodes 3 and 4. Edges: 0 to 1, 0 to 2, 1 to 3, 2 to 4, and a cross edge 1 to 4. The FIFO queue strip below shows the dequeue order 0, then 1, 2, then 3, 4 — every node at distance d is dequeued before any node at distance d+1. The contrast note states that DFS would instead dive 0, 1, 3 deep along one branch before exploring node 2."
  hue="var(--d-systems)"
  caption="BFS explores in rings of increasing distance from the start. The FIFO queue empties an entire ring (distance d) before any node of the next ring (distance d+1) is dequeued, so the visit order is 0 | 1, 2 | 3, 4 — nearest first. DFS instead plunges 0 → 1 → 3 down one branch before ever touching node 2."
>
  <svg viewBox="0 0 560 360" width="560" height="360" font-family="var(--font-body, sans-serif)">
    <circle cx="170" cy="160" r="132" fill="none" stroke="var(--hairline-2, #888)" stroke-width="1" stroke-dasharray="3 5" />
    <circle cx="170" cy="160" r="72" fill="none" stroke="var(--hairline-2, #888)" stroke-width="1" stroke-dasharray="3 5" />

    <text x="170" y="34" text-anchor="middle" font-size="11" fill="var(--muted, #888)">{"distance 2"}</text>
    <text x="170" y="94" text-anchor="middle" font-size="11" fill="var(--muted, #888)">{"distance 1"}</text>
    <text x="170" y="150" text-anchor="middle" font-size="11" fill="var(--muted, #888)">{"distance 0"}</text>

    <line x1="170" y1="160" x2="211" y2="101" stroke="var(--hairline-strong, #555)" stroke-width="1.4" />
    <line x1="170" y1="160" x2="211" y2="219" stroke="var(--hairline-strong, #555)" stroke-width="1.4" />
    <line x1="211" y1="101" x2="284" y2="94" stroke="var(--hairline-strong, #555)" stroke-width="1.4" />
    <line x1="211" y1="219" x2="284" y2="226" stroke="var(--hairline-strong, #555)" stroke-width="1.4" />
    <line x1="211" y1="101" x2="284" y2="226" stroke="var(--hairline-2, #888)" stroke-width="1.2" stroke-dasharray="4 4" />

    <circle cx="170" cy="160" r="24" fill="var(--d-systems)" stroke="var(--card, #fff)" stroke-width="2" />
    <text x="170" y="166" text-anchor="middle" font-size="17" font-weight="700" fill="var(--on-accent, #fff)">0</text>

    <circle cx="211" cy="101" r="22" fill="var(--card-2, #eee)" stroke="var(--d-systems)" stroke-width="2.4" />
    <text x="211" y="107" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">1</text>
    <circle cx="211" cy="219" r="22" fill="var(--card-2, #eee)" stroke="var(--d-systems)" stroke-width="2.4" />
    <text x="211" y="225" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">2</text>

    <circle cx="284" cy="94" r="22" fill="var(--card, #fff)" stroke="var(--hairline-strong, #555)" stroke-width="1.8" />
    <text x="284" y="100" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">3</text>
    <circle cx="284" cy="226" r="22" fill="var(--card, #fff)" stroke="var(--hairline-strong, #555)" stroke-width="1.8" />
    <text x="284" y="232" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">4</text>

    <line x1="360" y1="40" x2="360" y2="300" stroke="var(--hairline-2, #888)" stroke-width="0.8" />

    <text x="380" y="58" font-size="12" font-weight="600" fill="var(--ink, currentColor)">{"FIFO queue — dequeue order"}</text>

    <rect x="380" y="74" width="44" height="34" rx="6" fill="var(--d-systems)" stroke="var(--card, #fff)" stroke-width="1.5" />
    <text x="402" y="96" text-anchor="middle" font-size="15" font-weight="700" fill="var(--on-accent, #fff)">0</text>
    <text x="438" y="96" font-size="11" fill="var(--muted, #888)">{"distance 0"}</text>

    <rect x="380" y="118" width="44" height="34" rx="6" fill="var(--card-2, #eee)" stroke="var(--d-systems)" stroke-width="1.5" />
    <text x="402" y="140" text-anchor="middle" font-size="15" font-weight="700" fill="var(--ink, currentColor)">1</text>
    <rect x="430" y="118" width="44" height="34" rx="6" fill="var(--card-2, #eee)" stroke="var(--d-systems)" stroke-width="1.5" />
    <text x="452" y="140" text-anchor="middle" font-size="15" font-weight="700" fill="var(--ink, currentColor)">2</text>
    <text x="488" y="140" font-size="11" fill="var(--muted, #888)">{"distance 1"}</text>

    <rect x="380" y="162" width="44" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-strong, #555)" stroke-width="1.5" />
    <text x="402" y="184" text-anchor="middle" font-size="15" font-weight="700" fill="var(--ink, currentColor)">3</text>
    <rect x="430" y="162" width="44" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-strong, #555)" stroke-width="1.5" />
    <text x="452" y="184" text-anchor="middle" font-size="15" font-weight="700" fill="var(--ink, currentColor)">4</text>
    <text x="488" y="184" font-size="11" fill="var(--muted, #888)">{"distance 2"}</text>

    <text x="380" y="240" font-size="11" fill="var(--muted, #888)">{"all of ring d before ring d+1"}</text>
    <text x="380" y="278" font-size="11" fill="var(--muted, #888)">{"DFS would dive 0 → 1 → 3 first"}</text>
  </svg>
</DiagramFrame>

**BFS vs. tree level-order traversal**

In lesson 07 (Trees), you learned level-order traversal—process all nodes at depth d before depth d+1. That is exactly BFS on a tree. BFS is the generalization to graphs: same algorithm, but with a <Term slug="visited_set">visited set</Term> to handle cycles and multiple paths to the same vertex.

</Idea>

<Code lang="en">
**BFS with a queue**

<AnnotatedCode
  code={`function bfs(startVertex, adj) {
  const visited = new Set();
  const queue = [startVertex];
  
  // Mark the start vertex as visited when enqueueing
  visited.add(startVertex);
  
  while (queue.length > 0) {
    // Dequeue a vertex
    const vertex = queue.shift();
    console.log(vertex);  // Process the vertex
    
    // Enqueue all unvisited neighbors
    for (const neighbor of adj[vertex]) {
      if (!visited.has(neighbor)) {
        visited.add(neighbor);  // Mark visited when enqueueing
        queue.push(neighbor);
      }
    }
  }
  
  return visited;
}`}
  annotations={[
    { line: 1, text: "Function takes a starting vertex and an adjacency list." },
    { line: 2, text: "Initialize an empty visited set." },
    { line: 3, text: "Initialize the queue with the starting vertex." },
    { line: 6, text: "Mark the start vertex visited (on enqueue, not on dequeue)." },
    { line: 8, text: "Process each vertex in the queue." },
    { line: 10, text: "Dequeue a vertex from the front of the queue." },
    { line: 13, text: "For each neighbor, check if it has been visited." },
    { line: 14, text: "Mark the neighbor visited immediately (on enqueue)." },
    { line: 15, text: "Enqueue the neighbor." }
  ]}
/>

**Key point: mark-on-enqueue discipline**

Mark a vertex visited when you enqueue it, not when you dequeue it. This ensures each vertex is enqueued exactly once. If you marked it visited only when dequeuing, the same vertex could be enqueued many times (once by each neighbor), wasting queue space and time.

**Running BFS on the example graph**

<CodeRunner
  code={`// Build the graph as an adjacency list
const adj = {
  0: [1, 2],
  1: [3],
  2: [3],
  3: [2]
};

// Breadth-first search
function bfs(startVertex, adj) {
  const visited = new Set();
  const queue = [startVertex];
  
  visited.add(startVertex);
  
  while (queue.length > 0) {
    const vertex = queue.shift();
    console.log(vertex);
    
    for (const neighbor of adj[vertex]) {
      if (!visited.has(neighbor)) {
        visited.add(neighbor);
        queue.push(neighbor);
      }
    }
  }
  
  return visited;
}

console.log("BFS starting from 0:");
const visited = bfs(0, adj);
console.log("\\nVisited:", Array.from(visited).join(", "));`}
  expected={`BFS starting from 0:
0
1
2
3

Visited: 0, 1, 2, 3`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="bfs-trace"
  lang="en"
  code={[
    "adj = { 0: [1, 2], 1: [3], 2: [3], 3: [2] };",
    "queue = [0];",
    "visited = {0};"
  ]}
  frames={[
    { caption: "Initialize queue = [0], visited = {0}. Dequeue 0. Process it. Neighbors: 1, 2 (both unvisited). Mark 1 and 2 visited, enqueue them.", line: 3, cells: [{ value: "queue=[1,2]", mark: "active" }, { value: "visited={0,1,2}" }] },
    { caption: "Dequeue 1. Process it. Neighbors: 3. 3 is unvisited. Mark 3 visited, enqueue it.", line: 3, cells: [{ value: "queue=[2,3]", mark: "active" }, { value: "visited={0,1,2,3}" }] },
    { caption: "Dequeue 2. Process it. Neighbors: 3. 3 is already visited. Skip it.", line: 3, cells: [{ value: "queue=[3]", mark: "active" }, { value: "visited={0,1,2,3}" }] },
    { caption: "Dequeue 3. Process it. Neighbors: 2. 2 is already visited. Skip it.", line: 3, cells: [{ value: "queue=[]", mark: "done" }, { value: "visited={0,1,2,3}" }] },
    { caption: "Queue empty. Done. Visit order (by dequeue): 0, 1, 2, 3. Levels: 0 (distance 0), 1, 2 (distance 1), 3 (distance 2).", line: 3, cells: [{ value: "done", mark: "done" }, { value: "visited={0,1,2,3}" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity: O(V + E)**

BFS enqueues each vertex exactly once (because of the mark-on-enqueue discipline) and examines each edge exactly once. Each vertex is dequeued once, and when dequeued, all its neighbors are examined. Total work: V vertices enqueued + E edges examined = **O(V + E)**.

Example: a graph with 5 vertices and 7 edges runs BFS in O(5 + 7) = O(12) time—independent of whether the graph is sparse or dense.

**Space complexity: O(V)**

The visited set stores up to V vertices: O(V).

The queue can hold up to V vertices at once (at most all vertices at the current level): O(V).

Total space: **O(V)**.

In a wide, shallow graph (like a star with one central vertex connected to many others), the queue can grow large. In a deep, narrow graph (a long chain), the queue remains small.

**Why the mark-on-enqueue discipline**

Without mark-on-enqueue, a vertex could be enqueued multiple times:

```
Bad (mark-on-dequeue):
Vertex 1 has two neighbors: 2 and 3.
Vertex 2 has neighbor 4.
Vertex 3 has neighbor 4.

When dequeuing 1:
  - Enqueue 2 (not marked yet).
  - Enqueue 3 (not marked yet).

When dequeuing 2:
  - Neighbor 4 is not marked, so enqueue 4.

When dequeuing 3:
  - Neighbor 4 is not marked, so enqueue 4 again! (duplicate in queue)

Good (mark-on-enqueue):
When processing 1:
  - Mark 2 visited, enqueue 2.
  - Mark 3 visited, enqueue 3.

When processing 2:
  - Mark 4 visited, enqueue 4.

When processing 3:
  - 4 is already visited, skip.

4 enters the queue exactly once.
```

</Complexity>

<AlgoPractice
  id="practice-bfs"
  lessonSlug="03-breadth-first-search"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "What data structure does BFS use to process vertices in the order of increasing distance from the source?",
      choices: [
        "A stack",
        "A queue (FIFO)",
        "A heap",
        "A set"
      ],
      answer: 1,
      hint: "BFS processes vertices level by level. A queue (FIFO) ensures that all vertices at distance d are processed before any at distance d+1. A stack (LIFO) would be DFS."
    },
    {
      kind: "choice",
      prompt: "In BFS, when should you mark a vertex as visited?",
      choices: [
        "When the vertex is dequeued (popped from the queue)",
        "When the vertex is enqueued (added to the queue)",
        "Before adding it to the adjacency list",
        "After processing all its neighbors"
      ],
      answer: 1,
      hint: "Mark-on-enqueue prevents the same vertex from being enqueued multiple times. If you wait until dequeue, other neighbors might add it to the queue again."
    },
    {
      kind: "choice",
      prompt: "What does BFS guarantee in an unweighted graph?",
      choices: [
        "It visits vertices in alphabetical order",
        "It finds the shortest path (fewest edges) from source to every reachable vertex",
        "It sorts the vertices by distance",
        "It detects all cycles in the graph"
      ],
      answer: 1,
      hint: "The queue ensures FIFO order: vertices at distance 1 are processed before distance 2, etc. The first time a vertex is reached is via the fewest edges."
    },
    {
      kind: "choice",
      prompt: "What is the time complexity of BFS on a graph with V vertices and E edges?",
      choices: [
        "O(V)",
        "O(E)",
        "O(V + E)",
        "O(V * E)"
      ],
      answer: 2,
      hint: "Each vertex is enqueued and dequeued once (V). Each edge is examined once (E). Total: O(V + E)."
    },
    {
      kind: "choice",
      prompt: "Why does BFS need a visited set (or array) to avoid infinite loops on cyclic graphs?",
      choices: [
        "Because the queue might overflow if a vertex is added multiple times.",
        "Because a cyclic graph is invalid and must be rejected.",
        "Because without it, a cycle would cause the algorithm to revisit the same vertices infinitely, never terminating.",
        "Because the visited set improves performance, even though DFS doesn't need it."
      ],
      answer: 2,
      hint: "In a cycle like 0→1→2→0, without marking visited vertices, the algorithm would loop forever: 0 → 1 → 2 → 0 → 1 → ... The visited set breaks the cycle."
    },
    {
      kind: "numeric",
      prompt: "A graph has 8 vertices and 12 edges. What is the approximate time complexity O(V + E) in terms of total operations?",
      answer: 20,
      hint: "V + E = 8 + 12 = 20. BFS visits each vertex once and examines each edge once. Total operations: O(20)."
    }
  ]}
/>

<Inset type="why">
**Why BFS instead of DFS?** Both visit all reachable vertices in O(V+E) time. Use BFS when you need the shortest path in an unweighted graph, or when you want to process vertices by distance. Use DFS when you want to detect cycles (more natural with recursion) or when you prefer a simpler code. For this lesson, BFS is essential for understanding shortest paths without weights.
</Inset>

<Inset type="mistake">
**Marking visited too late.** Forgetting to mark neighbors visited when enqueueing them can lead to the same vertex being enqueued multiple times. Each time a different neighbor discovers an already-enqueued vertex, it gets added again. This wastes queue space and time. Always mark-on-enqueue.
</Inset>

<Inset type="edgecase">
**Disconnected graphs.** If a graph has multiple disconnected components, BFS from one vertex will only visit vertices reachable from that vertex. To visit all vertices in a disconnected graph, you must start a new BFS from an unvisited vertex. This is how the connected-components algorithm works (coming in the next lesson).
</Inset>

<Inset type="practice">
**BFS for shortest paths.** BFS answers the question: "What is the shortest path (in edges) from source A to every other reachable vertex?" Run BFS, and track the distance to each vertex (distance increases by 1 each time you move to a neighbor at the next level). This is the foundation for more advanced shortest-path algorithms like Dijkstra (lesson 09/06).
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-bfs"
    pieceSlug="03-breadth-first-search"
    lang="en"
    question="Explain what BFS is, how it differs from DFS, why the visited set and mark-on-enqueue discipline are essential, and describe BFS's key property in unweighted graphs."
    choices={[
      {
        label: "BFS is a graph traversal that explores level by level using a queue (FIFO), unlike DFS which uses a stack (LIFO) and goes deep. BFS marks neighbors visited when enqueueing them (mark-on-enqueue) to prevent duplicates in the queue. The visited set tracks which vertices have been enqueued, preventing infinite loops on cycles. Key property: BFS finds the shortest path (fewest edges) from source to every reachable vertex in an unweighted graph because the queue processes vertices in increasing order of distance. Time: O(V+E), space: O(V).",
        correct: true
      },
      {
        label: "BFS is identical to DFS except it uses a queue instead of a stack. Both explore all vertices and edges identically; the queue vs. stack distinction does not affect the algorithm's behavior. The visited set is optional and only improves performance. BFS always finds the shortest path, regardless of edge weights.",
        misconception: "BFS and DFS differ fundamentally in order: BFS is level-by-level (distance-increasing); DFS is depth-first. The visited set is essential, not optional, to prevent revisiting and infinite loops. BFS finds shortest paths only in unweighted graphs; weights require Dijkstra."
      },
      {
        label: "BFS stands for 'backward-first search' and is used only on directed graphs. Trees are undirected, so BFS does not apply. The queue stores distances, not vertices.",
        misconception: "BFS stands for 'breadth-first search' and works on directed and undirected graphs, including trees. The queue stores vertices; distances are computed separately by tracking level."
      },
      {
        label: "BFS is optimal for all graph problems and always outperforms DFS.",
        misconception: "BFS is optimal for shortest paths in unweighted graphs. DFS is simpler for other problems like cycle detection or topological sorting. Choose the tool for the problem."
      }
    ]}
  />
</Check>

<Recap lang="en">
<Term slug="breadth_first_search">Breadth-first search (BFS)</Term> explores a graph level by level, processing all vertices at distance d before distance d+1. It uses a <Term slug="queue">queue</Term> (FIFO), not a stack, and a <Term slug="visited_set">visited set</Term> to prevent infinite loops and duplicate enqueues.

**The mark-on-enqueue discipline:** Mark a vertex visited when you enqueue it, ensuring each vertex is enqueued exactly once.

Time: **O(V + E)**. Space: **O(V)**.

**Key property:** In an unweighted graph, BFS from a source finds the shortest path (fewest edges) to every reachable vertex—the first time you reach a vertex is via the shortest route.

BFS spreads wide. DFS goes deep. Now when you need the fewest-hops route between two nodes—in a maze, a network, a word-ladder puzzle—you will know to reach for BFS and mark on enqueue. Next lesson: connected components—use BFS or DFS to find all disconnected parts of a graph.
</Recap>
