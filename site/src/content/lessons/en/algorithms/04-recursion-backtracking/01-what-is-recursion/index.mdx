---
concepts:
- recursion
- base_case
- recursive_case
estMin: 18
lang: en
mathPrereqs: []
order: 1
prereqs:
- 02-arrays-strings/01-the-array
slug: 01-what-is-recursion
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://www.geeksforgeeks.org/dsa/what-is-base-case-in-recursion/
status: ready
summary: 'A function that calls itself to solve a smaller version of the same problem.
  Two mandatory parts: the base case (stop here) and the recursive case (shrink and
  call yourself).'
title: What is recursion?
track: algorithms
unit: 04-recursion-backtracking
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Imagine finding your way out of a maze. One strategy: at each junction, try one path, and if that path hits a dead end, go back and try another. You apply the same strategy (explore, backtrack, retry) over and over. That is the essence of recursion—a function that solves a problem by calling itself on a smaller version of the same problem, trusting it will work on the smaller version just as it works on the whole.
</Hook>

<Goal lang="en">
After this lesson you understand what recursion is: how a function can call itself, what a base case and recursive case are and why both are mandatory, how the recursive leap of faith works (trust the recursive call will do the right thing for the smaller input), and how recursion shines when a problem naturally breaks into smaller copies of itself.
</Goal>

<Idea lang="en">
**What is recursion?**

**Recursion** is when a function calls itself to solve a smaller version of the same problem. Instead of solving the problem all at once, you break it into two parts:

1. **Base case:** A version of the problem so small you can answer it directly, with no recursion.
2. **Recursive case:** A version of the problem where you call the function on a smaller input, then use that answer to build the answer for your current input.

Both parts are mandatory. Without a base case, the function calls itself forever. Without a shrinking recursive case, you never reach the base case—you loop forever in a different way.

**The two mandatory parts**

Let us look at a concrete example: computing the factorial of a number.

Factorial is defined as: 5! = 5 × 4 × 3 × 2 × 1 = 120. More generally, n! = n × (n - 1)!.

<AnnotatedCode
  code={`function factorial(n) {
  // Base case: stop here
  if (n <= 1) {
    return 1;
  }
  // Recursive case: shrink and call yourself
  return n * factorial(n - 1);
}`}
  annotations={[
    { line: 2, text: "Base case: the smallest input we handle directly. No recursion." },
    { line: 5, text: "Recursive case: call factorial on a smaller input, then use the result." }
  ]}
/>

Why does this work? When you call `factorial(4)`, it says: "I need 4 × factorial(3)." You trust that `factorial(3)` will return 6 (the correct answer for 3!). Then you multiply 4 × 6 = 24. This trust—that the recursive call will do the right thing for the smaller input—is called the **recursive leap of faith**.

**Why the leap of faith works**

The key insight: every recursive call passes a smaller input. Eventually, the input shrinks to 1 (or 0), and the base case catches it. At that point, the chain of calls unwinds, each one building its answer from the smaller answer below.

Let us trace `factorial(4)`:

```
factorial(4) → need 4 × factorial(3)
  factorial(3) → need 3 × factorial(2)
    factorial(2) → need 2 × factorial(1)
      factorial(1) → base case, return 1
    factorial(2) → return 2 × 1 = 2
  factorial(3) → return 3 × 2 = 6
factorial(4) → return 4 × 6 = 24
```

**Recursion vs iteration**

Many problems can be solved either way. For example, you can compute factorial with a loop:

```
result = 1
for i from 2 to 4:
  result = result × i
return result
```

This is iterative (using a loop, no function calls to itself). It uses less memory (no extra function call stack) and is sometimes faster. But it is less intuitive when the problem is naturally self-similar—like tree traversal, where each node contains smaller subtrees, or merge sort, which splits the array in half recursively.

Recursion shines when the problem structure mirrors recursion: a problem breaks into smaller copies of the same problem. Trees, graphs, permutations, and divide-and-conquer algorithms are natural for recursion.
</Idea>

<DiagramFrame
  label="factorial(4) recursion. Going DOWN the left column, each call delegates to a smaller copy of itself: factorial(4) calls factorial(3) calls factorial(2) calls factorial(1). factorial(1) is the base case and returns 1 directly, no further call. Then going UP the right column, each call combines its returned value: 1 becomes 2 times 1 equals 2, becomes 3 times 2 equals 6, becomes 4 times 6 equals 24."
  hue="var(--d-systems)"
  caption="Down the left: shrink toward the base case (each call delegates to a smaller copy). Up the right: combine returns (each level multiplies n by the answer below). The base case factorial(1) = 1 is the hinge where descent turns into the upward combine."
>
  <svg viewBox="0 0 520 440" width="520" height="440" font-family="var(--font-mono, monospace)" role="presentation">
    <defs>
      <marker id="rec-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="120" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"call down (shrink)"}</text>
    <text x="400" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"return up (combine)"}</text>

    <rect x="40" y="44" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="120" y="74" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(4)"}</text>

    <rect x="64" y="134" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="144" y="164" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(3)"}</text>

    <rect x="88" y="224" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="168" y="254" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(2)"}</text>

    <rect x="112" y="314" width="200" height="56" rx="8" fill="var(--d-systems)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="212" y="338" text-anchor="middle" font-size="15" font-weight="700" fill="#fff">{"factorial(1) = 1"}</text>
    <text x="212" y="357" text-anchor="middle" font-size="12" fill="#fff">{"BASE CASE — return 1"}</text>

    <line x1="120" y1="92" x2="144" y2="132" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />
    <line x1="144" y1="182" x2="168" y2="222" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />
    <line x1="168" y1="272" x2="200" y2="312" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />

    <text x="116" y="118" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"needs 4 × factorial(3)"}</text>
    <text x="140" y="208" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"needs 3 × factorial(2)"}</text>
    <text x="164" y="298" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"needs 2 × factorial(1)"}</text>

    <line x1="312" y1="342" x2="392" y2="262" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />
    <line x1="392" y1="244" x2="392" y2="184" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />
    <line x1="392" y1="166" x2="392" y2="106" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow)" />

    <rect x="352" y="224" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="249" text-anchor="middle" font-size="13" fill="var(--ink, #111)">{"2 × 1 = 2"}</text>

    <rect x="352" y="144" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="169" text-anchor="middle" font-size="13" fill="var(--ink, #111)">{"3 × 2 = 6"}</text>

    <rect x="352" y="64" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="89" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"4 × 6 = 24"}</text>

    <text x="486" y="89" text-anchor="start" font-size="12" font-weight="700" fill="var(--d-systems)">{"← result"}</text>

    <text x="318" y="392" text-anchor="middle" font-size="11" fill="var(--muted, #666)">{"return 1"}</text>
  </svg>
</DiagramFrame>

<Code lang="en">
Let us code recursion in JavaScript and build intuition with two examples.

**Example 1: Factorial**

```js
function factorial(n) {
  // Base case: n <= 1 returns 1
  if (n <= 1) {
    return 1;
  }
  // Recursive case: n * factorial(n - 1)
  return n * factorial(n - 1);
}

console.log(factorial(4)); // 24
console.log(factorial(5)); // 120
```

**Example 2: Sum of integers from 1 to n**

Instead of a loop, use recursion:

```js
function sumTo(n) {
  // Base case: sum to 1 is 1
  if (n <= 1) {
    return 1;
  }
  // Recursive case: n + sum of everything below n
  return n + sumTo(n - 1);
}

console.log(sumTo(5)); // 5 + 4 + 3 + 2 + 1 = 15
console.log(sumTo(10)); // 55
```

The pattern is the same: base case for the smallest input, recursive case that shrinks the input and calls itself.

Try both functions:

<CodeRunner
  id="cr-recursion-1"
  lang="en"
  code={`function factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}

function sumTo(n) {
  if (n <= 1) return 1;
  return n + sumTo(n - 1);
}

console.log("factorial(4) =", factorial(4));
console.log("factorial(5) =", factorial(5));
console.log("sumTo(5) =", sumTo(5));
console.log("sumTo(10) =", sumTo(10));
`}
/>
</Code>

<Trace lang="en">
Let us trace `factorial(4)` step by step, showing the unwinding of recursive calls.

<AlgoTrace
  id="trace-factorial"
  lang="en"
  code={[
    "function factorial(n) {",
    "  if (n <= 1) return 1;",
    "  return n * factorial(n - 1);",
    "}"
  ]}
  frames={[
    {
      caption: "Call factorial(4). 4 > 1, so enter recursive case.",
      line: 3,
      cells: ["n=4"]
    },
    {
      caption: "Call factorial(3). 3 > 1, so enter recursive case.",
      line: 3,
      cells: ["n=4", "n=3"]
    },
    {
      caption: "Call factorial(2). 2 > 1, so enter recursive case.",
      line: 3,
      cells: ["n=4", "n=3", "n=2"]
    },
    {
      caption: "Call factorial(1). 1 <= 1, so hit base case. Return 1.",
      line: 2,
      cells: ["n=4", "n=3", "n=2", "n=1→1"]
    },
    {
      caption: "factorial(2) gets 1. Compute 2 * 1 = 2. Return 2.",
      line: 3,
      cells: ["n=4", "n=3", "n=2→2"]
    },
    {
      caption: "factorial(3) gets 2. Compute 3 * 2 = 6. Return 6.",
      line: 3,
      cells: ["n=4", "n=3→6"]
    },
    {
      caption: "factorial(4) gets 6. Compute 4 * 6 = 24. Return 24.",
      line: 3,
      cells: ["n=4→24"]
    }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time:** Each recursive call processes one input value. For `factorial(n)`, there are n calls (from n down to 1), each doing constant work (one multiplication and one comparison). Total: O(n) time.

**Space:** Each function call takes space on the call stack. The deepest recursion goes from n down to 1, so the stack depth is O(n). This means O(n) space for the stack.

Contrast this with an iterative loop: O(n) time, but O(1) space. Recursion trades space for clarity when the problem is naturally recursive.

**Rule of thumb:** Use recursion when the problem naturally breaks into smaller copies of itself (trees, graphs, backtracking). The cleaner logic is worth the extra stack space—unless the recursion is very deep (hundreds of thousands of calls), which can overflow the stack.
</Complexity>

<AlgoPractice
  id="practice-recursion-1"
  lessonSlug="01-what-is-recursion"
  lang="en"
  problems={[
    {
      kind: "numeric",
      prompt: "How many recursive calls happen when you call factorial(5)?",
      answer: 5,
      hint: "factorial(5) calls factorial(4), which calls factorial(3), etc., down to factorial(1). That is 5 calls total."
    },
    {
      kind: "choice",
      prompt: "What happens if you forget the base case?",
      choices: [
        "The function returns 0",
        "The function loops forever until the stack overflows",
        "The function returns -1",
        "The compiler catches the error before running"
      ],
      answer: 1,
      hint: "Without a base case, there is no stopping point. The function calls itself forever, and eventually the call stack runs out of memory (stack overflow)."
    },
    {
      kind: "choice",
      prompt: "In the sumTo function, what is the base case?",
      choices: [
        "if (n <= 1) return 1;",
        "return n + sumTo(n - 1);",
        "if (n > 1) return n;",
        "return sumTo(n - 2);"
      ],
      answer: 0,
      hint: "The base case is the condition that stops recursion. It is the if statement that handles the smallest input without calling the function again."
    },
    {
      kind: "choice",
      prompt: "Why does factorial(n) call factorial(n - 1) and not factorial(n) again?",
      choices: [
        "Because factorial(n) and factorial(n - 1) are the same thing",
        "Because shrinking the input gets closer to the base case. Calling factorial(n) again would loop forever.",
        "Because the compiler does not allow it",
        "Because n - 1 is always smaller"
      ],
      answer: 1,
      hint: "The recursive case must pass a smaller input, guaranteed to approach the base case. Calling factorial(n) again would not shrink the problem—the input stays the same, and you loop forever."
    },
    {
      kind: "order",
      prompt: "Arrange the steps that happen when you call factorial(3):",
      items: [
        "factorial(3) calls factorial(2)",
        "factorial(2) calls factorial(1)",
        "factorial(1) hits base case, returns 1",
        "factorial(2) receives 1, computes 2 * 1 = 2, returns 2",
        "factorial(3) receives 2, computes 3 * 2 = 6, returns 6"
      ],
      hint: "Recursion goes down first (calls keep shrinking the input), then comes back up (each call computes and returns)."
    }
  ]}
/>

<Inset kind="mistake" lang="en">
**Mistake: No base case.** This function has no base case:

```js
function badFactorial(n) {
  return n * badFactorial(n - 1);
}
```

It calls itself forever. Even though n shrinks, there is no condition to stop. Eventually the call stack overflows and the program crashes. **Every recursive function MUST have a base case.**
</Inset>

<Inset kind="mistake" lang="en">
**Mistake: Input does not shrink.** This function calls itself on the same input:

```js
function badLoop(n) {
  if (n <= 0) return 1;
  return badLoop(n); // BUG: n is not shrinking
}
```

Even though there is a base case, the recursive call passes the same n, not n - 1. The base case is never reached (or only if the input is already ≤ 0). **The recursive case must shrink the input.**
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-recursion-1"
    pieceSlug="01-what-is-recursion"
    lang="en"
    question="You write a recursive function that counts down from n to 1. What must be true?"
    choices={[
      {
        label: "It has a base case (stop at 1) and the recursive case shrinks the input (n - 1)",
        correct: true
      },
      {
        label: "It must be faster than a loop",
        misconception: "Recursion and loops often have the same time complexity. Recursion trades space for clarity, not speed."
      },
      {
        label: "It must have more than one base case",
        misconception: "One base case is usually enough. You need at least one, not more than one."
      },
      {
        label: "It only works if you use a specific programming language",
        misconception: "Recursion is a concept that works in any language. The pattern is the same: function calls itself on a smaller input."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Recursion** is when a function calls itself to solve a smaller version of the same problem.

**Base case:** The smallest input the function can handle directly, with no recursion. Example: `if (n <= 1) return 1;`

**Recursive case:** The function calls itself on a smaller input, then uses the result to build the answer for the current input. Example: `return n * factorial(n - 1);`

**Both are mandatory:** No base case → infinite recursion (crash). Input does not shrink → infinite recursion (crash).

**The recursive leap of faith:** Trust that the recursive call returns the correct answer for the smaller input. This lets you focus on: "Given the answer for n - 1, how do I build the answer for n?"

**Recursion vs iteration:** Both can solve the same problem. Iteration is more memory-efficient (no extra call stack). Recursion is more intuitive for problems with recursive structure (trees, graphs, divide and conquer).

**Why recursion matters:** Trees naturally call recursion (each subtree is a smaller tree). Merge sort and quicksort are easier to understand recursively. Backtracking explores all paths by calling itself on each choice. The next lessons teach the call stack, backtracking, and tree traversal—all built on this foundation.
</Recap>
