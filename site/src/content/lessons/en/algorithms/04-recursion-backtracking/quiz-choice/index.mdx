---
concepts:
- base-case
- recursive-case
- call-stack
- recursion-tree
- backtracking
- pruning
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://www.geeksforgeeks.org/dsa/introduction-to-backtracking-2/
status: ready
summary: "Multiple-choice synthesis across the recursion unit: base/recursive case, call stack depth, recursion-tree complexity, the backtracking template, and pruning."
title: 'Recursion and backtracking: multiple-choice review'
track: algorithms
unit: 04-recursion-backtracking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make when you actually write recursive code, not a definition to recite, but a judgement about correctness, cost, or which fix is right.
</Hook>

<Goal lang="en">
Confirm you can connect the four ideas the unit built: a base case and a shrinking recursive case make recursion terminate, the call stack sets space, the recursion tree sets time, and backtracking with pruning turns brute force into something practical.
</Goal>

<Quiz
  id="04-recursion-backtracking-choice-1"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="A recursive function has a base case but still overflows the stack on every input, including tiny ones. What is the most likely cause?"
  choices={[
    { label: "The recursive case does not shrink the input toward the base case (e.g. it calls f(n) instead of f(n - 1)), so the base condition is never reached.", correct: true },
    { label: "The base case returns the wrong value.", misconception: "A wrong base-case return gives a wrong answer, not a stack overflow. Overflow means the recursion never stops, which is a progress problem in the recursive case." },
    { label: "The function is missing a loop.", misconception: "Recursion does not need a loop. The defect is that each call must move closer to the base case; if the argument does not shrink, the base case is unreachable." },
    { label: "The language stack is just too small; nothing in the code is wrong.", misconception: "If it overflows even on tiny inputs, the recursion is not making progress. A correct shrinking recursion on small n would never overflow." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-2"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="factorial(n) and naive fibonacci(n) both recurse to depth n. Why is factorial fine for large n while fibonacci is not?"
  choices={[
    { label: "Both use O(n) stack space, but factorial branches once per call (O(n) time) while fibonacci branches twice (O(2^n) time). The bottleneck is time, not the stack.", correct: true },
    { label: "Fibonacci uses far more stack space because it makes two calls per level.", misconception: "Stack depth is set by the deepest path, not the total number of calls. Both reach depth n, so both use O(n) stack. The difference is total work, the number of nodes in the recursion tree." },
    { label: "Factorial has a base case and fibonacci does not.", misconception: "Both have base cases and both terminate. Fibonacci is slow because its branching factor of 2 makes the recursion tree exponential, not because it fails to stop." },
    { label: "Fibonacci recurses deeper than factorial.", misconception: "They recurse to the same depth n. The branching factor (2 vs 1), not the depth, is what makes fibonacci's node count explode to O(2^n)." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-3"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="A recursive function has branching factor b = 3 and depth d = 12, doing O(1) work per call. Roughly how many calls does it make, and what does that tell you?"
  choices={[
    { label: "About b^d = 3^12 (roughly 530,000) calls. The node count is the branching factor raised to the depth, so the time is exponential and only practical for small inputs.", correct: true },
    { label: "About b times d = 36 calls; multiply the branching factor by the depth.", misconception: "Branching factor and depth multiply per level, not once. Each level multiplies the node count by b, giving b^d, not b times d." },
    { label: "About d^b = 12^3 (roughly 1,700) calls.", misconception: "The base is the branching factor and the exponent is the depth: b^d = 3^12, not d^b. Swapping them drastically undercounts." },
    { label: "Exactly 12 calls, one per level of depth.", misconception: "That is the stack depth, not the call count. With branching, each level has up to b times as many nodes as the one above it." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-4"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="In the choose-explore-undo backtracking template, what breaks if you omit the undo step (push to current but never pop)?"
  choices={[
    { label: "State from one branch leaks into sibling branches: the partial solution is never reset, so later choices are built on top of stale earlier choices and the outputs are corrupted.", correct: true },
    { label: "The recursion never terminates.", misconception: "Termination is controlled by the base case, not the undo. Without undo the recursion still stops, it just produces wrong partial solutions along the way." },
    { label: "Nothing; undo is only a performance optimisation.", misconception: "Undo is required for correctness with shared mutable state. The shared current array must be clean before the next sibling choice, or every later branch inherits leftover elements." },
    { label: "Pruning stops working.", misconception: "Pruning is the is-valid check before recursing; it is independent of undo. Skipping undo corrupts the partial solution, which is a separate defect." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-5"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="Subsets, permutations, and combinations all use the same backtracking template. What actually distinguishes them?"
  choices={[
    { label: "The choice and stopping rule differ: subsets branch include/exclude per element (2^n), permutations pick which unused item goes next (n!), combinations walk forward from a start index to size k (C(n,k)). The choose-explore-undo skeleton is identical.", correct: true },
    { label: "Subsets use iteration; permutations and combinations use recursion.", misconception: "All three are written with the same recursive backtracking skeleton in this unit. What changes is the choice logic and the base case, not recursion vs iteration." },
    { label: "Only combinations can be pruned; the other two cannot.", misconception: "Combinations have a natural prune (not enough items left to reach k), but pruning is a general backtracking idea. The defining difference is the choice rule, not who can prune." },
    { label: "Permutations are a special case of subsets with the same output count.", misconception: "They have different output sizes: 2^n subsets versus n! permutations. They differ because order matters for permutations but not for subsets." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-6"
  lessonSlug="04-recursion-backtracking"
  lang="en"
  question="A correct recursive tree-walk works in tests, but a colleague says it must be rewritten iteratively before production. When is that warranted?"
  choices={[
    { label: "When the recursion depth can grow with untrusted input size: a deep or adversarial input can drive the stack past its few-megabyte limit and crash the process. Bounded shallow depth is fine to leave recursive.", correct: true },
    { label: "Always, because recursion is inherently slower than iteration.", misconception: "Recursion and iteration usually share the same time complexity. The concern is stack space at large depth, not raw speed; shallow recursion needs no rewrite." },
    { label: "Never, because a correct recursive function is always safe to ship.", misconception: "Correct on small inputs can still overflow the stack on large depth. If depth scales with input, an explicit-stack iterative version removes that hard limit." },
    { label: "Only if the function lacks a base case.", misconception: "A missing base case is a bug to fix outright, not a recursion-vs-iteration tradeoff. The real iterative-rewrite trigger is unbounded stack depth." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain: a base case plus a shrinking recursive case make recursion terminate; the call stack makes depth cost O(depth) in space; the recursion tree makes total work the branching factor raised to the depth in time; and backtracking applies that recursive skeleton (choose, explore, undo) with pruning to skip doomed branches. Get the base case and progress right first, then read time from the tree and space from the stack.
</Recap>
