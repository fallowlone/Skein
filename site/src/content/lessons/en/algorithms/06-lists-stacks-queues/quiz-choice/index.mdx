---
concepts:
- linked-list
- pointer-cost
- dummy-head
- stack-queue-discipline
- deque
- monotonic-stack
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
- https://en.wikipedia.org/wiki/Double-ended_queue
- https://www.geeksforgeeks.org/dsa/introduction-to-monotonic-stack-2/
status: ready
summary: "Multiple-choice synthesis across the unit: linked-list cost model, pointer manipulation, LIFO/FIFO discipline, deque implementation, and the monotonic-stack amortized argument."
title: 'Lists, stacks, queues: multiple-choice review'
track: algorithms
unit: 06-lists-stacks-queues
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision an interviewer pushes on — not a definition to recite, but a cost model to defend and an implementation choice to justify.
</Hook>

<Goal lang="en">
Confirm you can connect the memory layout of a linked list to its cost model, reason about pointer manipulation without losing nodes, pick the right LIFO/FIFO discipline for a problem, and explain why a monotonic stack is linear despite its inner loop.
</Goal>

<Quiz
  id="06-lists-stacks-queues-choice-1"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="A workload pushes at the head 10,000 times and then reads by index 100 times at random positions. You must pick array vs singly linked list. Which is faster overall, and why?"
  choices={[
    { label: "The linked list. Head insertion is O(1) (allocate a node, repoint head) while an array head-insert is O(n) shifting; the 10k inserts dominate the 100 O(n) index reads.", correct: true },
    { label: "The array, because arrays are always faster than linked lists.", misconception: "Arrays win only on indexing and cache locality. Here the workload is insert-heavy at the head, which is exactly the linked list's O(1) strength versus the array's O(n) shift." },
    { label: "They are equal because both support insert and index access.", misconception: "Both support the operations, but the costs differ by an order of magnitude: the array pays O(n) per head insert, the linked list pays O(1). The dominant cost is the 10k head inserts." },
    { label: "The linked list, because index access on a linked list is also O(1).", misconception: "The conclusion is right but the reason is wrong. Index access on a linked list is O(n) — no address formula, you must hop pointers. The linked list wins on the head inserts, not on indexing." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-2"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="You are writing in-place reversal of a singly linked list. A colleague writes current.next = prev; current = current.next; inside the loop. What breaks?"
  choices={[
    { label: "The link to the rest of the list is lost. After current.next = prev, current.next no longer points to the original successor, so current = current.next walks backward into prev instead of forward. You must save next = current.next before overwriting it.", correct: true },
    { label: "Nothing — that is the standard reversal loop.", misconception: "The standard loop saves the successor first: const next = current.next; current.next = prev; prev = current; current = next;. Skipping the save corrupts the traversal because you overwrote the pointer you needed." },
    { label: "It leaks memory because the old nodes are never freed.", misconception: "There is no leak — the GC reclaims unreachable nodes. The bug is a logic error: the traversal moves the wrong direction because the successor pointer was destroyed before being saved." },
    { label: "It only fails on lists with a cycle.", misconception: "It fails on every non-trivial list. As soon as you overwrite current.next and then read it, you follow the reversed pointer back to prev instead of advancing." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-3"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="You repeatedly insert and delete at the head of a singly linked list and keep hitting null-pointer special cases for the empty-list and head positions. What is the cleanest structural fix?"
  choices={[
    { label: "Introduce a dummy (sentinel) head node that always precedes the real head. Now every real node has a predecessor, so insert and delete use one code path with no head special case.", correct: true },
    { label: "Switch to a doubly linked list so every node has a back pointer.", misconception: "A back pointer helps with O(1) predecessor access, but it does not remove the head/empty special cases — you still branch on whether head is null. The sentinel removes the branch by guaranteeing a predecessor." },
    { label: "Wrap every operation in a try/catch to swallow the null-pointer errors.", misconception: "Swallowing the error hides the bug, it does not fix the logic. The structural fix is a sentinel node so the previous-node reference is always valid." },
    { label: "Keep a separate count and skip the operation when the list is empty.", misconception: "A count does not unify the code paths for head versus middle operations. The sentinel is what makes 'insert at head' identical to 'insert in the middle.'" },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-4"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="You are validating nested brackets in a config parser, and separately you are doing a level-order (breadth-first) walk of a tree. Which structures fit, and why?"
  choices={[
    { label: "Brackets need a stack (LIFO): the most recently opened bracket must close first, which is exactly the nesting requirement. Level-order needs a queue (FIFO): nodes are processed in arrival order so siblings are visited before grandchildren.", correct: true },
    { label: "Both should use a stack, since both process characters or nodes one at a time.", misconception: "A stack on the tree gives depth-first, not level-order. Level-order requires FIFO so that a node's children wait behind all nodes already enqueued at the current level — that is a queue." },
    { label: "Both should use a queue, because order matters in both.", misconception: "A queue on brackets would pop the oldest open bracket first, the reverse of nesting. Bracket matching needs the most-recent-first discipline of a stack." },
    { label: "Brackets need a queue and the tree walk needs a stack.", misconception: "Exactly backward. Nesting is last-opened-first-closed (stack); breadth-first is arrival-order (queue). Swapping them gives the wrong traversal and wrong validation." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-5"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="You implement a FIFO queue on a JavaScript array using push to enqueue and shift to dequeue. Under a sustained stream of N enqueue+dequeue pairs, what is the total cost and the fix?"
  choices={[
    { label: "O(N^2): each shift reindexes every remaining element, so dequeue is O(n). Fix it with a linked-list queue (head/tail pointers) or a circular buffer with front/rear indices — both give O(1) dequeue.", correct: true },
    { label: "O(N): shift is O(1) like push, so N operations are linear.", misconception: "push is amortized O(1), but shift is O(n): removing index 0 forces every later element down one slot. N dequeues over a growing array sum to O(N^2)." },
    { label: "O(N log N): array operations are logarithmic.", misconception: "There is nothing logarithmic here. shift is linear in the array length, and summing linear costs over N operations yields quadratic, not N log N." },
    { label: "It is fine because a deque can pop from both ends in O(1).", misconception: "A proper deque (doubly linked list or circular buffer) is O(1) at both ends, but an array with shift is not that — shift still reindexes. The point is to stop using shift." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-6"
  lessonSlug="06-lists-stacks-queues"
  lang="en"
  question="A monotonic-stack solution to next-greater-element has a while loop inside a for loop, yet it is O(n), not O(n^2). What is the precise argument?"
  choices={[
    { label: "Amortized analysis: each index is pushed exactly once and popped at most once across the entire run, so the total number of inner-loop iterations over all outer steps is bounded by n. Total work = n pushes + at most n pops = O(n).", correct: true },
    { label: "Because the inner while runs a constant number of times each iteration.", misconception: "The inner loop can pop many elements in a single outer step (a tall element clears a long run). It is not constant per step — the bound is global: every element is popped at most once over the whole run." },
    { label: "Because the input array is sorted before processing.", misconception: "Nothing is sorted; the stack maintains a monotonic subset internally. The linear bound comes from each element entering and leaving the stack once, not from sorting." },
    { label: "Because we skip half the elements each pass, like binary search.", misconception: "There is no halving. Every element is visited; the efficiency comes from the push-once/pop-once invariant, which caps total stack operations at 2n." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one cost model and three disciplines. Memory layout sets the cost: contiguity buys O(1) indexing, scattering buys O(1) head insertion — you choose based on the dominant operation. Pointer manipulation is about saving links before you overwrite them and using a sentinel to erase head special cases. The stack (LIFO) and queue (FIFO) are disciplines, not data structures: pick by whether you need most-recent-first or arrival-order, and never dequeue with shift. The monotonic stack is the payoff — a stack plus an ordering invariant turns an O(n^2) scan into O(n) by the push-once/pop-once amortized argument.
</Recap>
