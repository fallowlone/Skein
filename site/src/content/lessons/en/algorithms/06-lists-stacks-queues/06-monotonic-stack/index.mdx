---
concepts:
- monotonic-stack
- monotonic-decreasing
- invariant
- next-greater-element
- amortized
- push-pop
estMin: 25
lang: en
mathPrereqs: []
order: 6
prereqs:
- 01-the-linked-list
- 02-pointer-manipulation
- 04-the-stack
slug: 06-monotonic-stack
sources:
- https://www.geeksforgeeks.org/dsa/introduction-to-monotonic-stack-2/
- https://labuladong.online/en/algo/data-structure/monotonic-stack/
- https://medium.com/@manuchaitanya/monotonic-stack-for-efficient-problem-solving-next-greater-next-smaller-previous-greater-and-6c63d0572644
- https://algo.monster/problems/mono_stack_intro
status: ready
summary: 'A stack kept in sorted order (increasing or decreasing) by removing elements
  that violate the invariant before pushing. The canonical application: find the next
  greater element to the right of each item in O(n) time, not O(n²).'
title: The monotonic stack
track: algorithms
unit: 06-lists-stacks-queues
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Term from "~/components/prose/Term.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Last lesson you learned about queues and deques. Today you will learn about a powerful variant of the <Term slug="stack">stack</Term>: the <Term slug="monotonic_stack">monotonic stack</Term>. This is a stack that maintains a special property: the elements inside are kept in a sorted order (either increasing or decreasing). When you want to push a new element, you first pop out any elements that break this order. The key insight: this breaks the classic "last pushed is first popped" rule, yet each element is still pushed and popped at most once, giving O(n) total time. The canonical problem is finding the <Term slug="next_greater_element">next greater element</Term> (NGE) — for each item in an array, find the next item to its right that is larger. A naive solution is O(n²), but monotonic stack solves it in O(n).
</Hook>

<Goal lang="en">
After this lesson you can explain what a <Term slug="monotonic_stack">monotonic stack</Term> is and how the push-with-pop <Term slug="invariant">invariant</Term> maintains order, implement the monotonic-stack algorithm to find the <Term slug="next_greater_element">next greater element</Term> for each item in O(n) time, trace through a concrete example step by step, understand why each element is pushed and popped at most once and how that guarantees O(n) total time, and recognize other problems (next smaller, previous greater) that fit the monotonic-stack pattern.
</Goal>

<Idea lang="en">
**What is a monotonic stack?**

A <Term slug="monotonic_stack">monotonic stack</Term> is a variant of the regular <Term slug="stack">stack</Term> that maintains a strict ordering among its elements. Instead of allowing any element to be pushed, you enforce a rule: elements must be in increasing order (or decreasing order, depending on your choice). When a new element arrives:

1. While the stack is not empty and the top violates the order, pop the top.
2. Push the new element.

The result: after every push, the stack contents form a sorted sequence.

**Increasing vs. decreasing:**

- **Monotonic increasing:** stack = [1, 3, 5, 7]. When you push 2, pop 7, 5, 3 first. Then push 2 → [1, 2].
- **Monotonic decreasing:** stack = [7, 5, 3, 1]. When you push 6, pop 5, 3, 1 first. Then push 6 → [7, 6].

**The next-greater-element problem:**

Given an array, for each element, find the next element to its right that is larger. If no such element exists, return -1.

Example: arr = [2, 1, 2, 4, 3]
- Element 2 (index 0): next greater is 4. Answer: 4.
- Element 1 (index 1): next greater is 2. Answer: 2.
- Element 2 (index 2): next greater is 4. Answer: 4.
- Element 4 (index 3): no greater element to the right. Answer: -1.
- Element 3 (index 4): no greater element to the right. Answer: -1.

**Naive approach: O(n²)**

For each element, scan all elements to its right to find the next greater. Worst case: n * n = O(n²).

```js
function nextGreaterBrute(arr) {
  const result = new Array(arr.length);
  for (let i = 0; i < arr.length; i++) {
    result[i] = -1;  // Default: no greater element
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] > arr[i]) {
        result[i] = arr[j];
        break;
      }
    }
  }
  return result;
}
```

This is slow. As you read more elements, the nested loop cost explodes.

**Monotonic-stack approach: O(n)**

Insight: process the array from right to left. Keep a monotonic decreasing stack of elements seen so far. For each new element:

1. Pop all elements from the stack that are ≤ the new element (they cannot be "next greater" for anything).
2. If the stack is not empty, the top is the next greater element for the current element.
3. Push the current element onto the stack.

Why does this work? The stack acts as a "candidate list." By the time you visit an element, the stack already contains all possible candidates to its right, pre-filtered and sorted.

Why is it O(n)? Each element is pushed once and popped at most once. So total operations = O(2n) = O(n).

</Idea>

<DiagramFrame label="A monotonic decreasing stack scanning the array [5, 3, 1, 4]. The stack on the left holds the values 5, 3, 1 from bottom to top, kept in strictly decreasing order — the monotonic invariant. The incoming value 4 is larger than the stack top, so it pops 1, then pops 3 (each pop resolves a next-greater answer of 4), stopping at 5 which is larger. Then 4 is pushed, leaving the stack 5, 4 from bottom to top — still strictly decreasing." hue="var(--d-systems)" caption="One scan step. The incoming value (4) pops every smaller element off the decreasing stack — 1 and 3, each finding 4 as its next greater element — then is pushed on top. The remaining 5 is larger, so it stays. After the push the stack is still strictly decreasing: the monotonic invariant holds.">
  <svg viewBox="0 0 480 300" width="480" height="300" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="240" y="22" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">array, scanning right to left — incoming value = 4</text>
    <rect x="120" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="142" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="164" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="186" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="208" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="230" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <rect x="252" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="274" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="274" y="84" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">incoming</text>

    <text x="86" y="116" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">BEFORE</text>
    <text x="86" y="276" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">decreasing ↑</text>
    <rect x="44" y="234" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="86" y="254" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="44" y="200" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" />
    <text x="86" y="220" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">3</text>
    <rect x="44" y="166" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" />
    <text x="86" y="186" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">1</text>
    <text x="86" y="158" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">top</text>

    <path d="M150 200 L210 200" fill="none" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#mono-arrow-hue)" />
    <text x="180" y="150" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">4 &gt; 1 and 4 &gt; 3</text>
    <text x="180" y="166" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">pop both</text>
    <text x="180" y="216" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">next greater = 4</text>
    <text x="180" y="252" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">4 &lt; 5: stop, keep 5</text>

    <text x="394" y="116" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">AFTER push 4</text>
    <text x="394" y="276" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">decreasing ↑</text>
    <rect x="352" y="234" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="394" y="254" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="352" y="200" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="394" y="220" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="394" y="192" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">top (pushed)</text>
    <path d="M210 244 L350 244" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" marker-end="url(#mono-arrow)" />

    <defs>
      <marker id="mono-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--hairline-strong)" />
      </marker>
      <marker id="mono-arrow-hue" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="en">
**Monotonic-stack solution for next greater element:**

```js
function nextGreater(arr) {
  const stack = [];  // Stack of indices (not values)
  const result = new Array(arr.length).fill(-1);
  
  // Process from right to left
  for (let i = arr.length - 1; i >= 0; i--) {
    // Pop elements that are <= current element
    while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {
      stack.pop();
    }
    
    // If stack is not empty, top is the next greater element
    if (stack.length > 0) {
      result[i] = arr[stack[stack.length - 1]];
    }
    
    // Push current index
    stack.push(i);
  }
  
  return result;
}
```

**Why we store indices, not values:**

We push indices onto the stack so that later, when we need the actual next greater value, we can index into the array. This pattern is common in monotonic-stack problems.

Try it in the interactive runner:

<CodeRunner
  id="cr-nge"
  lang="en"
  code={`function nextGreater(arr) {
  const stack = [];  // Stack of indices
  const result = new Array(arr.length).fill(-1);
  
  for (let i = arr.length - 1; i >= 0; i--) {
    // Pop while top is <= current
    while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {
      stack.pop();
    }
    
    // Top is next greater (if stack not empty)
    if (stack.length > 0) {
      result[i] = arr[stack[stack.length - 1]];
    }
    
    // Push current index
    stack.push(i);
  }
  
  return result;
}

// Test 1: Basic case
console.log("Test [2, 1, 2, 4, 3]:");
console.log(nextGreater([2, 1, 2, 4, 3]));  // [4, 2, 4, -1, -1]

// Test 2: Increasing array
console.log("\\nTest [1, 2, 3, 4]:");
console.log(nextGreater([1, 2, 3, 4]));  // [2, 3, 4, -1]

// Test 3: Decreasing array (no next greater)
console.log("\\nTest [4, 3, 2, 1]:");
console.log(nextGreater([4, 3, 2, 1]));  // [-1, -1, -1, -1]

// Test 4: Duplicates
console.log("\\nTest [3, 3, 5, 2]:");
console.log(nextGreater([3, 3, 5, 2]));  // [5, 5, -1, -1]`}
/>
</Code>

<Trace lang="en">
Let us trace the next-greater-element algorithm on the array [2, 1, 2, 4, 3].

We process from right to left (i = 4, 3, 2, 1, 0). At each step, we pop indices whose values are ≤ the current value, then push the current index.

<AlgoTrace
  id="trace-nge"
  lang="en"
  code={[
    "for (let i = arr.length - 1; i >= 0; i--) {",
    "  while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {",
    "    stack.pop();",
    "  }",
    "  if (stack.length > 0) {",
    "    result[i] = arr[stack[stack.length - 1]];",
    "  }",
    "  stack.push(i);",
    "}"
  ]}
  frames={[
    {
      caption: "Start. Array: [2, 1, 2, 4, 3]. i = 4 (value 3). Stack empty.",
      line: 1,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "target" }
      ],
      stackState: "Stack: []"
    },
    {
      caption: "i = 4 (value 3). No pops (stack empty). Push index 4. result[4] = -1.",
      line: 8,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Stack: [4] (value 3)"
    },
    {
      caption: "i = 3 (value 4). arr[4] = 3 ≤ 4? Yes, pop 4. Stack empty. Push index 3. result[3] = -1.",
      line: 2,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "target" },
        { value: "3", mark: "done" }
      ],
      stackState: "Stack: [3] (value 4)"
    },
    {
      caption: "i = 2 (value 2). arr[3] = 4 ≤ 2? No. Top of stack is arr[3] = 4. result[2] = 4. Push index 2.",
      line: 6,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "target" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Stack: [3, 2] (values 4, 2)"
    },
    {
      caption: "i = 1 (value 1). arr[2] = 2 ≤ 1? No. Top is arr[2] = 2. result[1] = 2. Push index 1.",
      line: 6,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "target" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Stack: [3, 2, 1] (values 4, 2, 1)"
    },
    {
      caption: "i = 0 (value 2). arr[1] = 1 ≤ 2? Yes, pop 1. arr[2] = 2 ≤ 2? Yes, pop 2. arr[3] = 4 ≤ 2? No. Top is 4. result[0] = 4. Push 0.",
      line: 2,
      cells: [
        { value: "2", mark: "target" },
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Stack: [3, 0] (values 4, 2)"
    },
    {
      caption: "Done. result = [4, 2, 4, -1, -1]",
      line: 9,
      cells: [
        { value: "2", mark: "done" },
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "4", mark: "done" },
        { value: "3", mark: "done" }
      ],
      stackState: "Final result: [4, 2, 4, -1, -1]"
    }
  ]}
/>

</Trace>

<Complexity lang="en">
**Time complexity: O(n)**

Each element is pushed onto the stack exactly once. Each element is popped at most once. Total operations = pushes + pops ≤ 2n. So time complexity is O(n).

This is a dramatic improvement over the naive O(n²) approach.

**Space complexity: O(n)**

In the worst case, the stack stores all n indices (e.g., if the array is in increasing order). The result array also uses O(n) space.

**Comparison with naive approach:**

| Approach | Time | Space |
|---|---|---|
| Nested loop (naive) | O(n²) | O(n) |
| Monotonic stack | O(n) | O(n) |

For large arrays, monotonic stack is orders of magnitude faster.

</Complexity>

<AlgoPractice
  id="practice-nge"
  lessonSlug="06-monotonic-stack"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In the monotonic-stack approach for next greater element, we process the array from which direction?",
      choices: [
        "Left to right",
        "Right to left",
        "Center outward",
        "In any order, it does not matter"
      ],
      answer: 1,
      hint: "We start from the rightmost element and move leftward. This allows us to build the stack of 'candidates to the right' as we go."
    },
    {
      kind: "choice",
      prompt: "When we see a new element in the monotonic stack for next-greater-element, which elements do we pop?",
      choices: [
        "All elements",
        "Elements at even indices",
        "Elements whose values are ≤ the new element",
        "The top element only"
      ],
      answer: 2,
      hint: "We pop elements that are <= the current element because they cannot be the next greater element for anything to their left."
    },
    {
      kind: "choice",
      prompt: "What is stored on the monotonic stack: array values or array indices?",
      choices: [
        "Array values only",
        "Array indices, because we need to access the original array later",
        "Both values and indices",
        "Neither; we use a separate data structure"
      ],
      answer: 1,
      hint: "We store indices so that when we need the next greater value, we can use the index to look up the value in the original array."
    },
    {
      kind: "choice",
      prompt: "For the array [1, 2, 3, 4], what is the next greater element for index 0?",
      choices: [
        "1",
        "2",
        "4",
        "-1 (no greater element)"
      ],
      answer: 1,
      hint: "At index 0, the value is 1. The next greater value to the right is 2 (at index 1)."
    },
    {
      kind: "choice",
      prompt: "Why is the monotonic-stack approach O(n) instead of O(n²)?",
      choices: [
        "Because the array is always sorted",
        "Because each element is pushed and popped at most once",
        "Because we use a hash table",
        "Because we skip half the array"
      ],
      answer: 1,
      hint: "Total operations = pushes + pops ≤ 2n = O(n). Each element moves onto and off the stack exactly once."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**What if there are equal elements?** The problem statement usually says "next greater," which means strictly greater (not equal). In the code, we use `<=` in the pop condition, which ensures that equal elements are also popped. If you need "next greater-or-equal," change `<=` to `<`.
</Inset>

<Inset kind="mistake" lang="en">
**"Monotonic stack = always increasing."** Wrong. A monotonic stack can be increasing or decreasing. For next-greater-element, we use a decreasing stack (so that the top is always the smallest candidate to the right, i.e., the next greater). For next-smaller-element, we use an increasing stack.
</Inset>

<Inset kind="practice" lang="en">
**Follow-up problems:**
- **Next smaller element (NSE):** Use a monotonic increasing stack instead.
- **Previous greater element (PGE):** Process left to right with a monotonic decreasing stack, tracking indices to the left.
- **Largest rectangle in histogram:** A classic monotonic-stack problem where you pop when height decreases, computing areas as you go. The pattern is similar: maintain order and pop to detect when a constraint is violated.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-nge"
    pieceSlug="06-monotonic-stack"
    lang="en"
    question="Why does the monotonic-stack approach work for finding the next greater element?"
    choices={[
      {
        label: "The stack maintains candidates (elements to the right) in decreasing order, so the top is always the next greater element when we pop smaller elements.",
        correct: true
      },
      {
        label: "It works because we sort the array first.",
        misconception: "We never sort the array. The stack naturally keeps a sorted (decreasing) subset of elements to the right."
      },
      {
        label: "It works because we use recursion to explore all possibilities.",
        misconception: "There is no recursion here. The stack iteratively builds and maintains the candidate list."
      },
      {
        label: "It works because the array is always in a specific order.",
        misconception: "The algorithm works for any array order. The monotonic stack imposes the order internally."
      }
    ]}
  />
</Check>

<Recap lang="en">
**The monotonic stack: a sorted candidate list**

1. **What is it:** A stack that maintains elements in sorted order (increasing or decreasing). When you push a new element, you first pop elements that break the order.

2. **The invariant:** After each push, the stack is sorted (e.g., decreasing for NGE). This property ensures you only keep relevant candidates.

3. **Next greater element:** Process the array right-to-left. Pop indices whose values are ≤ current. The top is the answer. Push the current index. Result: O(n) instead of O(n²).

4. **Why O(n):** Each element is pushed once and popped once. Total operations = O(2n) = O(n).

5. **Increasing vs. decreasing:** For next-greater, use decreasing. For next-smaller, use increasing. The choice depends on which direction you need.

6. **Real-world applications:** Stock span, largest rectangle in histogram, trapping rain water, daily temperatures. Any problem asking for the next/previous element satisfying a constraint is a candidate for monotonic stack.

7. **Key insight:** The stack is not a general-purpose container; it is a tool for efficiently filtering and ordering candidates. The sorted invariant is what makes the magic work.

You now have the tools to solve problems that naively seem O(n²) but are actually O(n) with a cleverly maintained stack.
</Recap>
