---
slug: 01-the-linked-list
lang: en
track: algorithms
unit: 06-lists-stacks-queues
order: 1
title: "The linked list"
summary: "A linked list is a sequence of nodes. Each node holds a value and a pointer to the next node. Unlike an array (contiguous memory), a linked list uses scattered memory—the cost of indexing is O(n), but inserting at the head is O(1)."
estMin: 18
status: ready
prereqs: ["02-arrays-strings/01-the-array"]
mathPrereqs: []
concepts: ["linked_list", "node", "pointer", "head", "next"]
sources:
  - https://www.tutorialspoint.com/data_structures_algorithms/linked_list_algorithms.htm
  - https://www.programiz.com/dsa/linked-list
  - https://launchschool.com/books/dsa/read/comparing_arrays_and_linked_lists
  - https://www.happycoders.eu/algorithms/array-vs-linked-list/
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You know that an array is a row of contiguous boxes in memory, and that contiguity gives you O(1) indexing but O(n) insertion in the middle. Now imagine a different structure: nodes scattered across memory, each holding a value and pointing to the next node. This is a linked list. You lose the superpower of O(1) indexing—finding the 5th element now costs O(n)—but you gain the power to insert at the head in O(1) time, no shifting required.
</Hook>

<Goal lang="en">
After this lesson you can explain what a node and a pointer are, draw a linked list in memory, implement push (insert at head) and traverse, understand why indexing by position is O(n) and why insertion at the head is O(1), compare the time and space costs of linked list vs. array operations, and recognize when a linked list is the right choice.
</Goal>

<Idea lang="en">
**The basic idea: nodes and pointers**

An **array** is a row of boxes all lined up together. A **linked list** is a sequence of **nodes**. Each node is a small container that holds two things:
1. A **value** (the data you care about)
2. A **pointer** (a memory address pointing to the next node)

The linked list starts at a special node called the **head**. If you want to visit every value, start at the head, read the value, follow the pointer to the next node, and repeat.

Example: A linked list holding [10, 20, 30]:
```
head → [10 | next → ] → [20 | next → ] → [30 | next → null]
```

Each `[ value | next → ]` is a node. The last node's `next` is `null`, meaning "there is no next node."

**Why scattered memory matters:**

Arrays store all elements in a contiguous block. The computer can calculate the address of any element instantly: `base_address + (index × element_size)`.

Linked lists are different. Nodes can be stored anywhere in memory. To find the node at position 5, you cannot calculate its address. Instead, you must start at the head, follow the pointer, step 1, step 2, step 3, step 4, step 5. This costs O(n) time.

**Why pointers matter:**

Because nodes are scattered, each node must store the address of the next node. That address is the **pointer**. In memory, a pointer is just a number—typically 8 bytes on a 64-bit system—but it tells the computer: "The next piece of data is at this address."

Example memory layout (imaginary):
```
Address 1000: node 1 = [10, pointer to 3008]
Address 3008: node 2 = [20, pointer to 5016]
Address 5016: node 3 = [30, pointer to null]
```

Notice the addresses are not consecutive. Nodes 1, 2, 3 are at 1000, 3008, 5016. But each node points to the next, so we can traverse the list.

**Insert at the head: the linked list superpower**

In an array, inserting at the beginning requires shifting all n elements. Expensive.

In a linked list, insert at the head is trivial:
1. Create a new node with your value.
2. Set its pointer to the current head.
3. Update head to point to the new node.

That is three operations. Time: **O(1)**.

<AnnotatedCode
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

let head = new Node(10);  // List: [10]

// Insert 20 at the head
head = new Node(20, head);  // List: [20, 10]

// Insert 30 at the head
head = new Node(30, head);  // List: [30, 20, 10]`}
  annotations={[
    { line: 1, text: "Each node holds a value and a pointer to the next node" },
    { line: 8, text: "Create the first node" },
    { line: 11, text: "Create a new node pointing to the old head" },
    { line: 14, text: "Update head to the new node. O(1) — no shifting!" }
  ]}
/>

**Traverse: the linked list cost**

To find a value or access position i, you must traverse from the head:

<AnnotatedCode
  code={`function getAtIndex(head, index) {
  let current = head;
  for (let i = 0; i < index; i++) {
    if (current === null) return null;  // End of list
    current = current.next;             // Follow pointer
  }
  return current?.value;
}`}
  annotations={[
    { line: 2, text: "Start at head" },
    { line: 3, text: "Loop index times" },
    { line: 5, text: "Follow the pointer to the next node" },
    { line: 8, text: "Return the value at position index, or null if not found" }
  ]}
/>

For a list of n nodes, finding position i costs O(i) time. In the worst case (i = n), it is O(n).

**Linked list vs. array at a glance:**

| Operation | Array | Linked List |
|---|---|---|
| Access by index | O(1) | O(n) |
| Insert at head | O(n) | O(1) |
| Insert in middle | O(n) | O(n) |
| Insert at tail | O(1) amortized | O(n) |
| Delete at head | O(n) | O(1) |
| Delete in middle | O(n) | O(n) |
| Search for value | O(n) | O(n) |

The tradeoff: arrays are fast for reading; linked lists are fast for inserting at the head.
</Idea>

<Code lang="en">
Let us build a simple linked list class with push (insert at head) and traverse:

```js
class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
  }

  // Insert at the head: O(1)
  push(value) {
    this.head = new Node(value, this.head);
  }

  // Get value at index: O(n)
  getAtIndex(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current?.value;
  }

  // Get node at index (useful for deletion/insertion): O(n)
  getNodeAt(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current;
  }

  // Traverse and print all values: O(n)
  traverse() {
    const values = [];
    let current = this.head;
    while (current !== null) {
      values.push(current.value);
      current = current.next;
    }
    return values;
  }
}
```

Try building and traversing a linked list:

<CodeRunner
  id="cr-linkedlist-1"
  lang="en"
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
  }

  push(value) {
    this.head = new Node(value, this.head);
  }

  getAtIndex(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current?.value;
  }

  traverse() {
    const values = [];
    let current = this.head;
    while (current !== null) {
      values.push(current.value);
      current = current.next;
    }
    return values;
  }
}

const list = new LinkedList();

// Push values: O(1) each
list.push(30);
list.push(20);
list.push(10);

console.log("List after pushes:", list.traverse());

// Get by index: O(n)
console.log("Element at index 0:", list.getAtIndex(0));
console.log("Element at index 2:", list.getAtIndex(2));

// Try accessing out of bounds
console.log("Element at index 5:", list.getAtIndex(5));`}
/>
</Code>

<Trace lang="en">
Let us trace the push operation step by step. We start with an empty list and push three values: 10, 20, 30.

<AlgoTrace
  id="trace-push"
  lang="en"
  code={[
    "const list = new LinkedList();",
    "list.push(10);",
    "list.push(20);",
    "list.push(30);"
  ]}
  frames={[
    {
      caption: "Start: list is empty, head = null",
      line: 1,
      cells: []
    },
    {
      caption: "After push(10): create node [10 | null], set head to it. List: [10]",
      line: 2,
      cells: ["10"]
    },
    {
      caption: "After push(20): create node [20 | next→10], set head to it. List: [20, 10]",
      line: 3,
      cells: ["20", "10"]
    },
    {
      caption: "After push(30): create node [30 | next→20], set head to it. List: [30, 20, 10]",
      line: 4,
      cells: ["30", "20", "10"]
    }
  ]}
/>

Now trace a getAtIndex(2) call on the list [30, 20, 10]:

<AlgoTrace
  id="trace-indexing"
  lang="en"
  code={[
    "function getAtIndex(index) {",
    "  let current = head;",
    "  for (let i = 0; i < index; i++) {",
    "    current = current.next;",
    "  }",
    "  return current?.value;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: index = 2, current = head (node 30)",
      line: 2,
      cells: [
        { value: "30", mark: "target" },
        { value: "20", mark: "value" },
        { value: "10", mark: "value" }
      ]
    },
    {
      caption: "Loop i=0: follow pointer, current = node 20",
      line: 4,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "target" },
        { value: "10", mark: "value" }
      ]
    },
    {
      caption: "Loop i=1: follow pointer, current = node 10",
      line: 4,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "done" },
        { value: "10", mark: "target" }
      ]
    },
    {
      caption: "Loop ends (i=2). Return current.value = 10. Took 2 pointer hops.",
      line: 6,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "done" },
        { value: "10", mark: "done" }
      ]
    }
  ]}
/>

Each hop to the next node is one step. To reach position i, you must hop i times. Time: O(i).
</Trace>

<Complexity lang="en">
**Linked list operations:**

| Operation | Time | Space |
|---|---|---|
| Access element at index i | O(i) | O(1) aux |
| Search for value | O(n) | O(1) aux |
| Push (insert at head) | O(1) | O(1) |
| Insert after node at index i | O(i) to find node, then O(1) to insert | O(1) |
| Delete at head | O(1) | O(1) |
| Delete at index i | O(i) | O(1) |
| Traverse all | O(n) | O(1) aux |

**Why is push O(1)?** You create a new node and update one pointer. Two operations. No traversal needed.

**Why is accessing by index O(n)?** You must follow pointers one by one. No formula to jump straight to position i.

**Contrast with arrays:**

Arrays are O(1) for indexing because memory is contiguous. Linked lists are O(n) because memory is scattered and you must follow pointers.

The intuition: **Contiguity enables calculation. Scattering requires traversal.**
</Complexity>

<AlgoPractice
  id="practice-linkedlist-1"
  lessonSlug="01-the-linked-list"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In a linked list with 1000 nodes, how much time does it take to find the node at index 500?",
      choices: [
        "O(1) — the computer can jump to any node instantly",
        "O(500) — you must follow 500 pointers",
        "O(1000) — you must traverse the entire list",
        "O(log n) — linked lists are like binary search"
      ],
      answer: 1,
      hint: "You start at the head and follow the pointer to the next node, 500 times. Linear in the position, not the list size."
    },
    {
      kind: "choice",
      prompt: "In a linked list, why is inserting at the head O(1)?",
      choices: [
        "Because you create a new node and shift it to the front using a formula",
        "Because you create a new node, point it to the old head, and update the head pointer—no traversal needed",
        "Because the list keeps a sorted index of where to insert",
        "Because linked lists always use O(1) insertion"
      ],
      answer: 1,
      hint: "No shifting, no searching. Just create a node, point it at the old head, and change one reference."
    },
    {
      kind: "choice",
      prompt: "An array of 100 elements allows O(1) access to arr[99]. A linked list of 100 nodes requires O(n) to reach node 99. Why this difference?",
      choices: [
        "Linked lists are always slower than arrays",
        "Arrays use contiguous memory, so the address can be calculated. Linked lists use scattered memory, so you must follow pointers.",
        "Linked lists forgot to add a formula for address calculation",
        "Array indexing is fast because of the cloud; linked lists run on-premise"
      ],
      answer: 1,
      hint: "The key is memory layout. Contiguous → formula. Scattered → traversal."
    },
    {
      kind: "numeric",
      prompt: "You insert values 10, 20, 30 at the head of a linked list (in that order). What is the list after all three pushes?",
      answer: 30,
      hint: "Push 10: [10]. Push 20: [20, 10]. Push 30: [30, 20, 10]. The front element is 30."
    },
    {
      kind: "choice",
      prompt: "In a linked list, why can't you just calculate the memory address of the node at position i, like you do in an array?",
      choices: [
        "Because the linked list is too complicated",
        "Because nodes are scattered across memory, and there is no formula relating position i to memory address",
        "Because computers do not support pointer arithmetic in linked lists",
        "Because JavaScript does not allow memory addresses"
      ],
      answer: 1,
      hint: "Arrays: address = base + (i × size). Linked lists: no such formula; nodes can be anywhere."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Inserting in the middle:** If you have a reference to a node and want to insert after it, that is O(1)—just redirect the pointer. But if you want to insert at position i (where you don't have a reference yet), you must traverse to position i first, costing O(i). So "insert at position i" in a linked list is O(i), not O(1).
</Inset>

<Inset kind="mistake" lang="en">
**"Linked lists are always slower."** False. Linked lists are slower at indexing (O(n)) but faster at insertion at the head (O(1)). The right tool depends on the task. If you frequently push and rarely index, a linked list is better. If you frequently index, an array is better.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-linkedlist-1"
    pieceSlug="01-the-linked-list"
    lang="en"
    question="Why does a linked list achieve O(1) insertion at the head while an array requires O(n) to insert at the beginning?"
    choices={[
      {
        label: "A linked list creates a new node and updates one pointer (head), avoiding the O(n) cost of shifting all elements. An array must shift every element to make room.",
        correct: true
      },
      {
        label: "Linked lists are always faster than arrays.",
        misconception: "Linked lists are faster for specific operations (like insertion at the head) but slower for others (like indexing). Both have tradeoffs."
      },
      {
        label: "Inserting at the head in an array is O(n) because arrays cannot insert; they can only replace. Linked lists are designed for insertion.",
        misconception: "Arrays can insert, but the cost is O(n) because you must shift elements. Linked lists avoid this by changing pointers."
      },
      {
        label: "Linked lists do not actually insert; they append, so no cost is involved.",
        misconception: "Pushing to a linked list creates a new node and updates the head pointer. This is insertion at the head, and it has a clear O(1) cost."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **linked list** is a sequence of **nodes**, each holding a value and a pointer to the next node.

Key facts:
1. **Node:** A container with two fields—value and next (pointer to the next node).
2. **Head:** The first node. Iteration starts here.
3. **Pointer:** A memory address pointing to the next node.
4. **Scattered memory:** Nodes are not contiguous. Pointers link them together.
5. **Indexing is O(n):** No formula to jump to position i. Must follow pointers one by one.
6. **Push is O(1):** Create a new node, point it at the old head, update head. No shifting.
7. **Tradeoff:** Give up O(1) indexing; gain O(1) head insertion.
8. **Use when:** Frequent insertions at the front, infrequent random access.

Next, you will learn the **stack** and **queue**—two other sequential data structures built on linked lists, each with their own discipline and use cases.
</Recap>
