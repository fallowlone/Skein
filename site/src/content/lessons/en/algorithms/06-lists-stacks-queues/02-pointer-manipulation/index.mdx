---
concepts:
- linked_list_insertion
- linked_list_deletion
- linked_list_reversal
- sentinel_node
- pointer_update
estMin: 22
lang: en
mathPrereqs: []
order: 2
prereqs:
- 01-the-linked-list
slug: 02-pointer-manipulation
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://medium.com/@DharunRaju/linked-list-pattern-sentinel-node-4eb2ce7851be
- https://labuladong.online/en/algo/essential-technique/linked-list-skills-summary/
- https://guides.codepath.org/compsci/Dummy-Head
- https://dev.to/devcorner/a-complete-guide-to-mastering-linked-list-problems-3o8n
status: ready
summary: Insert a node into a linked list, delete a node, and reverse the list using
  pointer updates. Learn the dummy-head (sentinel) pattern to eliminate edge cases
  and write cleaner code.
title: Pointer manipulation on linked lists
track: algorithms
unit: 06-lists-stacks-queues
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Last lesson you learned that pushing to the head is O(1), but accessing position i is O(n). Today you will learn how to insert and delete at arbitrary positions, how to reverse a list, and how to use a clever trick—the dummy head—that eliminates most edge cases and makes your code simpler.
</Hook>

<Goal lang="en">
After this lesson you can insert a node at a given position in a linked list by updating pointers, delete a node from a linked list (given a reference to the previous node), reverse an entire linked list by pointer manipulation alone, implement the dummy-head (sentinel) pattern to handle head edge cases elegantly, and recognize when dummy heads make code cleaner and safer.
</Goal>

<Idea lang="en">
**The core problem: linking and unlinking**

To insert or delete a node in a linked list, you must carefully update pointers. It is not about creating or destroying nodes in memory (that is the allocator's job); it is about changing where `next` points.

**Insertion: the splice operation**

Imagine a linked list [10] → [20] → [30] and you want to insert 15 between 10 and 20.

To insert a node after a given node:
1. Create the new node (15).
2. Set the new node's `next` to point to the old successor (20).
3. Set the given node's `next` to point to the new node (15).

This is a "splice"—you break the link from 10 → 20, insert 15 in between, and rebuild the chain: 10 → 15 → 20.

```
Before:  [10 | next → ] → [20 | next → ] → [30 | null]

After:   [10 | next → ] → [15 | next → ] → [20 | next → ] → [30 | null]
                            ↑ newly inserted
```

The operation is **O(1) in time** because you only change pointers; you do not traverse the list. But you must have a reference to the node *before* the insertion point. If you want to insert at position i (and you only have the head), you first traverse to position i-1, costing O(i) time total.

**Deletion: the unlinking operation**

Now delete the 15 from [10] → [15] → [20] → [30].

To delete a node, you need a reference to the node *before* it. Then:
1. Set the previous node's `next` to skip the target, pointing directly to the target's successor.

```
Before:  [10 | next → ] → [15 | next → ] → [20 | next → ] → [30 | null]

After:   [10 | next → ] ─────────────────> [20 | next → ] → [30 | null]
                        (15 is now unreachable)
```

Time: **O(1)** to delete given a reference to the previous node. O(i) if you must traverse to find it first.

**The head edge case: why dummy heads matter**

Insertion and deletion are easy if the target is in the middle. But what if you want to insert at the head or delete the head? Then there is no "previous node." This forces you to write special cases:

```js
if (insertingAtHead) {
  // Special case: create new head
  const newNode = new Node(value);
  newNode.next = head;
  head = newNode;
} else {
  // Normal case: insert after some node
  const prev = findNodeBefore(position);
  const newNode = new Node(value);
  newNode.next = prev.next;
  prev.next = newNode;
}
```

This duplication is ugly. The trick: **introduce a dummy node.**

**The dummy head (sentinel node) pattern**

Create a special node, the dummy head, that always points to the real head. This dummy is not part of your data; it is just an anchor.

```
Dummy (0) → [10 | next → ] → [20 | next → ] → [30 | null]
   ↑
 Always exists; acts as the previous node for insertions at the real head
```

Now every operation has a "previous node"—either a real node or the dummy. Insert and delete use the same logic everywhere:

```js
// No special cases needed
const newNode = new Node(value);
newNode.next = prev.next;  // prev might be the dummy
prev.next = newNode;
```

Benefits:
1. **Unified code.** One insertion/deletion logic works for head and middle.
2. **Cleaner deletions.** Even if you delete the head, the dummy still points to the new head.
3. **Safer.** You eliminate null-pointer checks for "is there a previous node?"
4. **Easier to code correctly.** Fewer edge cases = fewer bugs.

**Reversal: flipping all pointers**

Reversing [10] → [20] → [30] → [null] to [30] → [20] → [10] → [null] means changing every `next` pointer to point backward.

One approach: iterate through the list, and at each node, reverse its pointer.

```
Step 0: prev = null,  current = 10 → 20 → 30
        next = 20 (save it before we change current.next)
        
Step 1: prev = 10 ← null (we flipped it),  current = 20 → 30
        next = 30 (save it)
        
Step 2: prev = 20 ← 10,  current = 30 → null
        next = null (save it)
        
Step 3: prev = 30 ← 20,  current = null (done)
```

Result: prev points to the new head (30).

The key insight: **save the next pointer before you overwrite it.** Otherwise, you lose the link and cannot continue.

```js
let prev = null;
let current = head;

while (current !== null) {
  const next = current.next;  // Save the next node
  current.next = prev;        // Reverse the pointer
  prev = current;             // Move forward
  current = next;
}

head = prev;  // New head is the last node we processed
```

Time: **O(n)** (one pass through the list). Space: **O(1)** (only a few pointers, no new data).

<DiagramFrame
  label="One reverse step. Before: prev points at node 10, curr at node 20, next at node 30, and curr.next still points forward to 30. After: next is saved as 30, then curr.next is redirected backward to prev so node 20 points to node 10, and finally prev advances to 20 and curr advances to 30."
  hue="var(--d-systems)"
  caption="The three-pointer dance for one step: (1) save next = curr.next so the rest of the list is not lost, (2) rewire curr.next = prev so node 20's next points backward to node 10, (3) advance prev = curr and curr = next."
>
  <svg viewBox="0 0 560 320" font-family="var(--font-body)">
    <text x="16" y="24" font-size="13" fill="var(--ink)">{"Reverse step: curr.next = prev"}</text>

    <text x="16" y="92" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"BEFORE"}</text>
    <text x="150" y="56" font-size="11" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"prev"}</text>
    <text x="286" y="56" font-size="11" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"curr"}</text>
    <text x="422" y="56" font-size="11" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"next"}</text>

    <rect x="108" y="64" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="244" y="64" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="380" y="64" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="150" y="93" font-size="14" fill="var(--ink)" text-anchor="middle">{"10"}</text>
    <text x="286" y="93" font-size="14" fill="var(--ink)" text-anchor="middle">{"20"}</text>
    <text x="422" y="93" font-size="14" fill="var(--ink)" text-anchor="middle">{"30"}</text>

    <line x1="192" y1="87" x2="244" y2="87" stroke="var(--muted)" stroke-width="1.4" marker-end="url(#pm-fwd)" />
    <line x1="328" y1="87" x2="380" y2="87" stroke="var(--muted)" stroke-width="1.4" marker-end="url(#pm-fwd)" />
    <text x="218" y="80" font-size="9" font-family="var(--font-mono)" fill="var(--faint)" text-anchor="middle">{"next"}</text>
    <text x="354" y="80" font-size="9" font-family="var(--font-mono)" fill="var(--faint)" text-anchor="middle">{"next"}</text>

    <text x="16" y="232" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"AFTER"}</text>
    <text x="150" y="196" font-size="11" font-family="var(--font-mono)" fill="var(--faint)" text-anchor="middle">{"(old curr)"}</text>
    <text x="286" y="196" font-size="11" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"prev"}</text>
    <text x="422" y="196" font-size="11" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"curr"}</text>

    <rect x="108" y="204" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="244" y="204" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.4" />
    <rect x="380" y="204" width="84" height="46" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="150" y="233" font-size="14" fill="var(--ink)" text-anchor="middle">{"10"}</text>
    <text x="286" y="233" font-size="14" fill="var(--ink)" text-anchor="middle">{"20"}</text>
    <text x="422" y="233" font-size="14" fill="var(--ink)" text-anchor="middle">{"30"}</text>

    <path d="M244 218 C 210 182, 184 182, 150 196" fill="none" stroke="var(--accent)" stroke-width="1.8" marker-end="url(#pm-back)" />
    <text x="197" y="178" font-size="10" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"curr.next = prev"}</text>

    <line x1="328" y1="240" x2="380" y2="240" stroke="var(--faint)" stroke-width="1.2" stroke-dasharray="4 3" marker-end="url(#pm-saved)" />
    <text x="354" y="266" font-size="9" font-family="var(--font-mono)" fill="var(--faint)" text-anchor="middle">{"next saved"}</text>

    <defs>
      <marker id="pm-fwd" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
      <marker id="pm-back" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="8" markerHeight="8" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--accent)" />
      </marker>
      <marker id="pm-saved" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--faint)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>
</Idea>

<Code lang="en">
**Insertion after a node:**

```js
function insertAfter(prevNode, value) {
  if (prevNode === null) {
    throw new Error("Cannot insert after null");
  }
  const newNode = new Node(value);
  newNode.next = prevNode.next;
  prevNode.next = newNode;
}
```

**Deletion of a node (given the previous node):**

```js
function deleteAfter(prevNode) {
  if (prevNode === null || prevNode.next === null) {
    throw new Error("Nothing to delete");
  }
  prevNode.next = prevNode.next.next;
}
```

**Reversal (in-place):**

```js
function reverseList(head) {
  let prev = null;
  let current = head;
  
  while (current !== null) {
    const next = current.next;  // Save next before we change it
    current.next = prev;        // Reverse the pointer
    prev = current;             // Move prev forward
    current = next;             // Move current forward
  }
  
  return prev;  // prev is now the new head
}
```

**The dummy-head pattern:**

```js
function insertDummy(head, position, value) {
  // Create dummy node that points to head
  const dummy = new Node(0);  // Value doesn't matter
  dummy.next = head;
  
  // Traverse to position - 1 (now we can always find a previous node)
  let prev = dummy;
  for (let i = 0; i < position; i++) {
    if (prev.next === null) {
      throw new Error("Position out of bounds");
    }
    prev = prev.next;
  }
  
  // Insert (same logic as always)
  const newNode = new Node(value);
  newNode.next = prev.next;
  prev.next = newNode;
  
  return dummy.next;  // Return the new head
}
```

Try reversing a list and using the dummy head:

<CodeRunner
  id="cr-linkedlist-pointer"
  lang="en"
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

function reverseList(head) {
  let prev = null;
  let current = head;
  
  while (current !== null) {
    const next = current.next;
    current.next = prev;
    prev = current;
    current = next;
  }
  
  return prev;
}

function traverse(head) {
  const values = [];
  let current = head;
  while (current !== null) {
    values.push(current.value);
    current = current.next;
  }
  return values;
}

function insertDummy(head, position, value) {
  const dummy = new Node(0);
  dummy.next = head;
  
  let prev = dummy;
  for (let i = 0; i < position; i++) {
    if (prev.next === null) return null;
    prev = prev.next;
  }
  
  const newNode = new Node(value);
  newNode.next = prev.next;
  prev.next = newNode;
  
  return dummy.next;
}

// Build: [10, 20, 30]
let head = new Node(10);
head.next = new Node(20);
head.next.next = new Node(30);

console.log("Original:", traverse(head));

// Reverse
head = reverseList(head);
console.log("Reversed:", traverse(head));

// Insert 5 at position 1 in reversed list [30, 20, 10]
head = insertDummy(head, 1, 5);
console.log("After insert 5 at pos 1:", traverse(head));`}
/>
</Code>

<Trace lang="en">
Let us trace the reversal of [10] → [20] → [30] step by step.

<AlgoTrace
  id="trace-reverse"
  lang="en"
  code={[
    "let prev = null;",
    "let current = head;",
    "while (current !== null) {",
    "  const next = current.next;",
    "  current.next = prev;",
    "  prev = current;",
    "  current = next;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: prev = null, current = 10. List: [10 → 20 → 30]",
      line: 2,
      cells: [
        { value: "10", mark: "target" },
        { value: "20", mark: "value" },
        { value: "30", mark: "value" }
      ]
    },
    {
      caption: "Save next = 20. Reverse 10's pointer: 10 → null. Move prev = 10, current = 20.",
      line: 7,
      cells: [
        { value: "10", mark: "done" },
        { value: "20", mark: "target" },
        { value: "30", mark: "value" }
      ]
    },
    {
      caption: "Save next = 30. Reverse 20's pointer: 20 → 10. Move prev = 20, current = 30.",
      line: 7,
      cells: [
        { value: "10", mark: "done" },
        { value: "20", mark: "done" },
        { value: "30", mark: "target" }
      ]
    },
    {
      caption: "Save next = null. Reverse 30's pointer: 30 → 20. Move prev = 30, current = null.",
      line: 7,
      cells: [
        { value: "10", mark: "done" },
        { value: "20", mark: "done" },
        { value: "30", mark: "done" }
      ]
    },
    {
      caption: "Current = null, loop ends. Return prev = 30. New list: [30 → 20 → 10 → null]",
      line: 2,
      cells: [
        { value: "30", mark: "target" },
        { value: "20", mark: "value" },
        { value: "10", mark: "value" }
      ]
    }
  ]}
/>

Notice: we never allocate or free nodes. We only change pointers. The list structure is completely reversed by pointer updates alone.
</Trace>

<Complexity lang="en">
**Pointer manipulation on linked lists:**

| Operation | Time | Space |
|---|---|---|
| Insert after a given node | O(1) | O(1) |
| Insert at position i | O(i) | O(1) |
| Delete after a given node | O(1) | O(1) |
| Delete at position i | O(i) | O(1) |
| Reverse the entire list | O(n) | O(1) |

**Why O(1) for insert/delete after a node?** You only update pointers; no traversal needed.

**Why O(i) for insert/delete at position i?** You must traverse from head to position i (or i-1) to find the target. Traversal takes O(i) time.

**Why O(n) for reversal?** You visit each node once and flip its pointer. One pass through all n nodes.

**Why O(1) space for all?** You use only a few pointers (prev, current, next). No new data structure is allocated.
</Complexity>

<AlgoPractice
  id="practice-linkedlist-pointer"
  lessonSlug="02-pointer-manipulation"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "You want to insert a node with value 15 after the node with value 10 in the list [10] → [20] → [30]. What are the pointer updates?",
      choices: [
        "Create node 15, set 15.next = 20, set 10.next = 15. Then shift all later nodes right.",
        "Create node 15, set 15.next = 20, set 10.next = 15. No shifting needed.",
        "Create node 15 at the end, then traverse and reorder the pointers.",
        "Create node 15, append it to the list, then sort all pointers."
      ],
      answer: 1,
      hint: "After insertion, the chain should be 10 → 15 → 20. Only two pointer updates needed: 15's next, and 10's next."
    },
    {
      kind: "choice",
      prompt: "To delete the node with value 20 from [10] → [20] → [30], what do you do?",
      choices: [
        "Find the node before 20 (which is 10), and set 10.next = 30. The node 20 becomes unreachable.",
        "Delete 20 from memory and shift all later nodes left.",
        "Set 20.next = null to mark it for deletion.",
        "Find 20 and copy its value to the previous node, then delete 20."
      ],
      answer: 0,
      hint: "Deletion is unlinking. Set the previous node's next to skip the target and point to its successor."
    },
    {
      kind: "choice",
      prompt: "After reversing [10] → [20] → [30], the new head and the final structure should be:",
      choices: [
        "New head is 30, final list is [30] → [20] → [10] → null",
        "New head is still 10, the list is just marked as reversed",
        "New head is 20, middle node of the reversed list",
        "New head is null, the list is fully disconnected"
      ],
      answer: 0,
      hint: "Reversal flips every pointer. The last node becomes the new head. The original head's next becomes null."
    },
    {
      kind: "numeric",
      prompt: "What is the time complexity of reversing a linked list with n nodes?",
      answer: "n",
      hint: "You must visit every node once to flip its pointer. That is O(n) time."
    },
    {
      kind: "choice",
      prompt: "Why is the dummy-head pattern useful?",
      choices: [
        "It speeds up insertion and deletion by using a special cached pointer.",
        "It eliminates the need to write special cases for head operations; every node (including dummy) has a previous node.",
        "It allows you to insert at the head without allocating a new node.",
        "It reduces memory usage by reusing the same dummy node for all lists."
      ],
      answer: 1,
      hint: "The dummy head always has a predecessor (itself), so you never ask 'Is there a previous node?' It unifies the logic."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Insertion at the head with dummy:** With a dummy head, insertion at position 0 (the real head) is just insertion after the dummy. No special case needed. The new node becomes the first real data node, and dummy.next points to it.
</Inset>

<Inset kind="mistake" lang="en">
**"I can delete a node by just setting node.next = null."** No. This does not unlink the node from the list. The previous node still points to it, and the node still points to its successor. True deletion requires changing the previous node's next pointer.
</Inset>

<Inset kind="mistake" lang="en">
**"When reversing, I can just use recursion to flip pointers."** Recursion works (and is elegant), but it costs O(n) space for the call stack. The iterative approach (with prev, current, next pointers) does it in O(1) space.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-linkedlist-pointer"
    pieceSlug="02-pointer-manipulation"
    lang="en"
    question="Why is deletion of a node in a linked list O(1) only if you have a reference to the previous node, but O(n) if you only have the head?"
    choices={[
      {
        label: "Because deletion requires a pointer update on the previous node's next field. If you have the previous node, one update suffices (O(1)). Without it, you must traverse from the head to find it, costing O(n).",
        correct: true
      },
      {
        label: "Because deletion is always O(n) in linked lists; having the previous node is just a trick to make it seem O(1).",
        misconception: "Deletion is genuinely O(1) given the previous node. The traversal cost is separate."
      },
      {
        label: "Because the previous node is always cached in memory, so accessing it costs O(1), while the head requires a search.",
        misconception: "The cost difference comes from whether you need to traverse, not from caching. Traversal from head to position i costs O(i) time."
      },
      {
        label: "Because deletion in the middle costs O(n), but deletion at the head costs O(1), and the average is somewhere between.",
        misconception: "The time complexity depends on where the target is, not on averages. Deletion at the head (given the previous node, or the dummy) is O(1)."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Pointer manipulation on linked lists:**

1. **Insertion after a node:** Create a new node, set its next to the old successor, set the node's next to the new node. Time: O(1) if you have the node; O(i) if you must traverse to position i first.

2. **Deletion after a node:** Set the previous node's next to skip the target and point to its successor. Time: O(1) given the previous node; O(n) if you must search from the head.

3. **Reversal:** Use three pointers (prev, current, next) to iterate through the list and flip each pointer. Time: O(n), space: O(1). The last node becomes the new head.

4. **The dummy-head (sentinel) pattern:** Place a dummy node before the real head. Now every operation has a guaranteed "previous node," eliminating special cases for head operations. Cleaner code, fewer bugs.

5. **Always save the next pointer before overwriting it.** When you set `current.next = prev`, you lose the link to the successor unless you saved it first.

6. **Pointer manipulation is all about updates, not allocation.** You are rearranging existing nodes, not creating new ones.

Next, you will learn the **stack** and **queue**—two sequential data structures built on linked lists, each imposing a discipline on when you can insert and delete.
</Recap>
