---
concepts:
- fast_slow_pointers
- cycle_detection
- tortoise_hare
- middle_finding
estMin: 24
lang: en
mathPrereqs: []
order: 3
prereqs:
- 01-the-linked-list
- 02-pointer-manipulation
slug: 03-fast-and-slow-pointers
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://www.geeksforgeeks.org/detect-loop-in-a-linked-list/
- https://leetcode.com/problems/linked-list-cycle/solutions/
- https://neetcode.io/courses/dsa-for-beginners
- https://www.algoexpert.io/
status: ready
summary: Use two pointers advancing at different speeds to find the middle of a list,
  detect cycles (Floyd''''s tortoise-and-hare), and prove that the pointers must meet
  inside any cycle. A technique that solves hard linked-list problems with O(1) space.
title: Fast and slow pointers
track: algorithms
unit: 06-lists-stacks-queues
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Last lesson you reversed a linked list by updating pointers one by one. Today you will learn a different pointer trick: send two pointers down the list at different speeds. The slow pointer moves one step; the fast pointer moves two. If the list has a cycle, the pointers will meet. If the list is linear (no cycle), the fast pointer falls off the end. This technique—called the tortoise-and-hare method—can find the middle node and detect cycles with only O(1) extra space.
</Hook>

<Goal lang="en">
After this lesson you can find the middle node of a linked list using two pointers, detect whether a linked list contains a cycle, explain why fast and slow pointers must meet inside a cycle, apply the technique to hard linked-list problems, and recognize when two pointers at different speeds is the right approach.
</Goal>

<Idea lang="en">
**The core idea: two pointers at different speeds**

Imagine walking down a path with a friend. You walk one step per second; your friend walks two steps per second. If the path is linear (has an end), your friend reaches the end while you are still in the middle. If the path loops back on itself, your friend will lap you—eventually your friend catches up and you meet again.

In a linked list, we use two pointers (slow and fast) to model this:
- **Slow pointer:** Moves one step per iteration (to the next node).
- **Fast pointer:** Moves two steps per iteration (skips one node, goes two ahead).

```
slow: 1 step
fast: 2 steps
```

**Finding the middle node**

Given a list [10] → [20] → [30] → [40] → [50], what is the middle?

Use two pointers, both starting at the head. On each iteration:
1. Move slow one step.
2. Move fast two steps.
3. When fast reaches the end, slow is at the middle.

```
Start: slow = 10, fast = 10
After 1: slow = 20, fast = 30
After 2: slow = 30, fast = 50

Loop exits because fast.next is null.
slow is at 30, the middle.
```

The insight: When fast reaches the end, slow has taken half as many steps and is at the middle.

**Detecting a cycle: the tortoise-and-hare algorithm**

A cycle means the list eventually loops back to an earlier node. Example: [10] → [20] → [30] → [20] (the pointer of 30 points back to 20, creating a cycle).

Using two pointers at different speeds, if there is a cycle:
1. Slow moves one step per iteration.
2. Fast moves two steps per iteration.
3. If fast ever reaches null, there is no cycle.
4. If fast and slow ever point to the same node, there is a cycle.

Why must they meet? In a cycle, the fast pointer gains on the slow pointer by one step each iteration (it moves two while slow moves one). Eventually, fast laps slow.

```
Cycle example: [1] → [2] → [3] → [4] → [5] → [3] (loops back to 3)

Start: slow = 1, fast = 1
Iter 1: slow = 2, fast = 3
Iter 2: slow = 3, fast = 5
Iter 3: slow = 4, fast = 4 (fast caught slow! Cycle detected.)
```

**Why they must meet inside the cycle**

Suppose the cycle has length c (the number of nodes in the loop). When slow enters the cycle:
- Fast is ahead and moving twice as fast.
- Each iteration, fast gains one step on slow (moves 2, slow moves 1).
- The gap between them decreases by 1 each step.
- Eventually the gap becomes 0, and they meet.

The key fact: They meet somewhere inside the cycle, never outside it. This is because once slow enters the cycle, it will never leave; fast is always ahead, so it is already inside. Eventually fast catches slow.

**Important caveat: This detects the cycle exists, but does not say where it starts.**

To find the cycle start (the first node of the loop), you need a second pass:
1. After detecting the cycle, place one pointer at the head.
2. Place the other pointer where slow and fast met.
3. Move both at speed 1 (one step each iteration).
4. They meet at the cycle start.

(We will not code this in detail—it is a follow-up technique—but it is important to know the limitation.)

</Idea>

<DiagramFrame
  label="Fast and slow pointers: slow advances 1 node per step while fast advances 2. Top — finding the middle: when fast reaches the end of a linear list, slow sits at the middle. Bottom — cycle detection: inside a loop fast gains 1 node per step until it laps slow and both point to the same node."
  caption="Slow moves +1, fast moves +2 each iteration. Top: fast lands on the last node while slow lands on the middle (find-middle). Bottom: in a cycle fast closes the gap by 1 per step and meets slow on the same node (cycle detection)."
  hue="var(--d-systems)"
>
  <svg viewBox="0 0 640 360" width="640" height="360" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body)">
    <text x="20" y="28" font-size="14" font-weight="600" fill="currentColor">Find the middle (linear list)</text>
    <text x="20" y="46" font-size="11" fill="var(--muted)">{"slow +1 per step  ·  fast +2 per step  ·  fast hits the end when slow is at the middle"}</text>

    <line x1="100" y1="90" x2="156" y2="90" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <line x1="200" y1="90" x2="256" y2="90" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <line x1="300" y1="90" x2="356" y2="90" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <line x1="400" y1="90" x2="456" y2="90" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />

    <rect x="60" y="72" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="80" y="95" font-size="13" text-anchor="middle" fill="currentColor">10</text>
    <rect x="160" y="72" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="180" y="95" font-size="13" text-anchor="middle" fill="currentColor">20</text>
    <rect x="260" y="72" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="280" y="95" font-size="13" text-anchor="middle" fill="currentColor">30</text>
    <rect x="360" y="72" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="380" y="95" font-size="13" text-anchor="middle" fill="currentColor">40</text>
    <rect x="460" y="72" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="480" y="95" font-size="13" text-anchor="middle" fill="currentColor">50</text>
    <text x="520" y="95" font-size="12" fill="var(--muted)">null</text>

    <text x="280" y="134" font-size="12" font-weight="700" text-anchor="middle" fill="var(--d-systems)">slow</text>
    <text x="280" y="150" font-size="10" text-anchor="middle" fill="var(--muted)">{"middle (3rd of 5)"}</text>
    <text x="480" y="134" font-size="12" font-weight="700" text-anchor="middle" fill="var(--d-systems)">fast</text>
    <text x="480" y="150" font-size="10" text-anchor="middle" fill="var(--muted)">{"end (last node)"}</text>

    <line x1="20" y1="186" x2="620" y2="186" stroke="var(--hairline-2)" stroke-width="1" stroke-dasharray="4 4" />

    <text x="20" y="218" font-size="14" font-weight="600" fill="currentColor">Detect a cycle (tortoise and hare)</text>
    <text x="20" y="236" font-size="11" fill="var(--muted)">{"each step fast closes the gap by 1, so it eventually lands on the same node as slow"}</text>

    <line x1="100" y1="290" x2="156" y2="290" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <line x1="200" y1="290" x2="256" y2="290" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <line x1="300" y1="290" x2="356" y2="290" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />
    <path d="M 480 308 C 480 350, 280 350, 280 308" fill="none" stroke="var(--muted)" stroke-width="2" marker-end="url(#fs-arrow)" />

    <rect x="60" y="272" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="80" y="295" font-size="13" text-anchor="middle" fill="currentColor">1</text>
    <rect x="160" y="272" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="180" y="295" font-size="13" text-anchor="middle" fill="currentColor">2</text>
    <rect x="260" y="272" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="280" y="295" font-size="13" text-anchor="middle" fill="currentColor">3</text>
    <rect x="360" y="272" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="380" y="295" font-size="13" text-anchor="middle" fill="currentColor">4</text>
    <rect x="460" y="272" width="40" height="36" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1.5" />
    <text x="480" y="295" font-size="13" text-anchor="middle" fill="currentColor">5</text>

    <text x="280" y="262" font-size="12" font-weight="700" text-anchor="middle" fill="var(--d-systems)">slow = fast</text>
    <text x="565" y="330" font-size="10" text-anchor="middle" fill="var(--muted)">{"5 → 3 (loop)"}</text>

    <defs>
      <marker id="fs-arrow" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
        <path d="M 0 0 L 6 3 L 0 6 Z" fill="var(--muted)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="en">
**Finding the middle node:**

```js
function findMiddle(head) {
  let slow = head;
  let fast = head;
  
  // Move fast two steps, slow one step, until fast reaches the end
  while (fast !== null && fast.next !== null) {
    slow = slow.next;         // Move slow one step
    fast = fast.next.next;    // Move fast two steps
  }
  
  return slow;  // slow is at the middle
}
```

**Detecting a cycle (tortoise-and-hare):**

```js
function hasCycle(head) {
  if (head === null || head.next === null) return false;
  
  let slow = head;
  let fast = head;
  
  // If there is a cycle, fast will eventually catch slow
  while (fast !== null && fast.next !== null) {
    slow = slow.next;         // Move slow one step
    fast = fast.next.next;    // Move fast two steps
    
    if (slow === fast) {
      return true;  // Cycle detected
    }
  }
  
  return false;  // fast reached the end; no cycle
}
```

Try both algorithms interactively:

<CodeRunner
  id="cr-fast-slow"
  lang="en"
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

function findMiddle(head) {
  let slow = head;
  let fast = head;
  
  while (fast !== null && fast.next !== null) {
    slow = slow.next;
    fast = fast.next.next;
  }
  
  return slow;
}

function hasCycle(head) {
  if (head === null || head.next === null) return false;
  
  let slow = head;
  let fast = head;
  
  while (fast !== null && fast.next !== null) {
    slow = slow.next;
    fast = fast.next.next;
    
    if (slow === fast) {
      return true;
    }
  }
  
  return false;
}

// Test 1: Find middle of [10, 20, 30, 40, 50]
let head = new Node(10);
head.next = new Node(20);
head.next.next = new Node(30);
head.next.next.next = new Node(40);
head.next.next.next.next = new Node(50);

const middle = findMiddle(head);
console.log("Middle of [10, 20, 30, 40, 50] is:", middle.value);

// Test 2: No cycle
console.log("Cycle in [10, 20, 30, 40, 50]?", hasCycle(head));

// Test 3: Cycle — make node 50 point back to node 30
const node30 = head.next.next;
head.next.next.next.next.next = node30;  // 50 → 30

console.log("Cycle after creating loop?", hasCycle(head));`}
/>
</Code>

<Trace lang="en">
Let us trace finding the middle of [10] → [20] → [30] → [40] → [50] using two pointers.

<AlgoTrace
  id="trace-middle"
  lang="en"
  code={[
    "let slow = head;",
    "let fast = head;",
    "while (fast !== null && fast.next !== null) {",
    "  slow = slow.next;",
    "  fast = fast.next.next;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: slow = 10, fast = 10",
      line: 3,
      cells: [
        { value: "10", mark: "target" },
        { value: "20", mark: "value" },
        { value: "30", mark: "value" },
        { value: "40", mark: "value" },
        { value: "50", mark: "value" }
      ]
    },
    {
      caption: "Iteration 1: slow → 20, fast → 30",
      line: 6,
      cells: [
        { value: "10", mark: "done" },
        { value: "20", mark: "target" },
        { value: "30", mark: "fast" },
        { value: "40", mark: "value" },
        { value: "50", mark: "value" }
      ]
    },
    {
      caption: "Iteration 2: slow → 30, fast → 50. Loop exits because fast.next is null. slow is at the middle (30).",
      line: 3,
      cells: [
        { value: "10", mark: "done" },
        { value: "20", mark: "done" },
        { value: "30", mark: "target" },
        { value: "40", mark: "value" },
        { value: "50", mark: "fast" }
      ]
    }
  ]}
/>

Now trace cycle detection: [1] → [2] → [3] → [4] → [3] (cycle back to 3).

<AlgoTrace
  id="trace-cycle"
  lang="en"
  code={[
    "let slow = head;",
    "let fast = head;",
    "while (fast !== null && fast.next !== null) {",
    "  slow = slow.next;",
    "  fast = fast.next.next;",
    "  if (slow === fast) return true;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: slow = 1, fast = 1. Cycle: [1 → 2 → 3 → 4 → 3]",
      line: 3,
      cells: [
        { value: "1", mark: "target" },
        { value: "2", mark: "value" },
        { value: "3", mark: "value" },
        { value: "4", mark: "value" }
      ]
    },
    {
      caption: "Iteration 1: slow = 2, fast = 3",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "target" },
        { value: "3", mark: "fast" },
        { value: "4", mark: "value" }
      ]
    },
    {
      caption: "Iteration 2: slow = 3, fast = 4",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "target" },
        { value: "4", mark: "fast" }
      ]
    },
    {
      caption: "Iteration 3: slow = 4, fast = 3 (fast.next.next loops back)",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "fast" },
        { value: "4", mark: "target" }
      ]
    },
    {
      caption: "Iteration 4: slow = 3, fast = 4. (slow advances, fast advances, still in cycle)",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "target" },
        { value: "4", mark: "fast" }
      ]
    },
    {
      caption: "Iteration 5: slow = 4, fast = 3. Pointers swap positions—gap narrows.",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "fast" },
        { value: "4", mark: "target" }
      ]
    },
    {
      caption: "Iteration 6: slow = 3, fast = 4. Still cycling. Eventually...",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "target" },
        { value: "4", mark: "fast" }
      ]
    },
    {
      caption: "At some point: slow = 3, fast = 3. MATCH! Cycle detected.",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "target" },
        { value: "4", mark: "done" }
      ]
    }
  ]}
/>

</Trace>

<Complexity lang="en">
**Fast and slow pointers:**

| Operation | Time | Space |
|---|---|---|
| Find middle of a list | O(n) | O(1) |
| Detect cycle (tortoise-and-hare) | O(n) | O(1) |
| Find cycle start | O(n) | O(1) |

**Why O(n) for all?** We visit each node in the list once (or until we detect a cycle). The fast pointer reaches the end faster, but we still iterate through the list structure.

**Why O(1) space?** We only use two pointers—constant space, no hash set, no array.

**Compare to naive cycle detection:** Using a hash set to store visited nodes also finds cycles in O(n) time but costs O(n) space. Two pointers achieve the same in O(1) space.
</Complexity>

<AlgoPractice
  id="practice-fast-slow"
  lessonSlug="03-fast-and-slow-pointers"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In the list [10] → [20] → [30] → [40] → [50], what node is the middle?",
      choices: [
        "10",
        "20",
        "30",
        "40"
      ],
      answer: 2,
      hint: "The middle of a 5-node list is the 3rd node (index 2): [10, 20, 30, 40, 50]."
    },
    {
      kind: "choice",
      prompt: "Using the fast-and-slow-pointer technique to find the middle, when should you stop?",
      choices: [
        "When slow reaches the last node",
        "When fast reaches the end (null or the last node)",
        "When fast.next is null",
        "After exactly n/2 iterations"
      ],
      answer: 1,
      hint: "The loop condition is `while (fast !== null && fast.next !== null)`. When this condition is false, fast has reached the end, and slow is at the middle."
    },
    {
      kind: "choice",
      prompt: "A linked list is [1] → [2] → [3] → [4] → [3] (node 4's next points back to 3). What does the tortoise-and-hare algorithm return?",
      choices: [
        "false (no cycle)",
        "true (cycle detected)",
        "The node 3 (the cycle start)",
        "null (list is broken)"
      ],
      answer: 1,
      hint: "The algorithm returns true if slow and fast ever meet. In a cycle, they will meet inside the loop."
    },
    {
      kind: "choice",
      prompt: "Why must the fast and slow pointers eventually meet if there is a cycle?",
      choices: [
        "Because they start at the same position",
        "Because fast moves twice as fast, so it gains on slow by 1 step per iteration. In a finite cycle, the gap must eventually become 0.",
        "Because the algorithm forces them to meet by design",
        "Because linked lists are finite, so the pointers must collide"
      ],
      answer: 1,
      hint: "Once slow enters the cycle, it will never exit. Fast is already ahead inside the cycle. Fast gains 1 step each iteration, so eventually slow catches up."
    },
    {
      kind: "choice",
      prompt: "A list has no cycle: [10] → [20] → [30] → [40] → null. What does hasCycle return?",
      choices: [
        "true (always returns true for any list)",
        "false (the fast pointer reaches null, so the loop exits)",
        "40 (the last node)",
        "Infinity (the loop never ends)"
      ],
      answer: 1,
      hint: "The loop condition checks `fast !== null && fast.next !== null`. If fast reaches null, the loop exits and the function returns false."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Even vs. odd length lists:** For a list of even length [10, 20, 30, 40], the middle is ambiguous (is it 20 or 30?). Most implementations return the second middle (30) because when fast falls off the end, slow is at position length/2 + 1. Be aware of this when using the middle-finding technique.
</Inset>

<Inset kind="mistake" lang="en">
**"Fast pointer should move by 3 or 4 steps to go faster."** No. Fast must move by exactly 2 steps. Why? If fast moves by k > 2 steps, it might skip over the slow pointer and never catch it, even if there is a cycle. The 2-step rule guarantees convergence in a cycle.
</Inset>

<Inset kind="practice" lang="en">
**Common follow-up:** Given a list with a cycle, find the first node of the cycle (where the loop starts). Hint: After detecting the cycle with slow and fast, place one pointer at the head and the other where they met. Move both one step at a time. They meet at the cycle start. This requires mathematical proof (related to the cycle length and position where they meet), but the algorithm is elegant.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-fast-slow"
    pieceSlug="03-fast-and-slow-pointers"
    lang="en"
    question="Why is the two-pointer (fast-and-slow) technique for cycle detection better than using a hash set of visited nodes?"
    choices={[
      {
        label: "It uses O(1) space instead of O(n) space, while solving the same problem in the same O(n) time.",
        correct: true
      },
      {
        label: "It is faster in practice because pointer operations are cheaper than hash lookups.",
        misconception: "Both are O(n) time. The advantage is space, not speed."
      },
      {
        label: "It can find the cycle start directly, whereas hash sets cannot.",
        misconception: "The fast-slow technique only detects that a cycle exists. Finding the cycle start requires a second pass (moving both pointers at speed 1 from different starting points)."
      },
      {
        label: "It works for any graph, not just linked lists.",
        misconception: "The fast-slow technique works specifically for linked lists because you can only move forward. In general graphs with multiple edges from one node, fast might skip the slow pointer."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Fast and slow pointers (tortoise-and-hare):**

1. **Core idea:** Use two pointers advancing at different speeds (slow: 1 step, fast: 2 steps).

2. **Finding the middle:** Move both pointers until fast reaches the end. Slow is at the middle (or the second middle for even-length lists).

3. **Detecting cycles:** In a list with a cycle, fast will eventually lap slow. If they ever point to the same node, a cycle exists.

4. **Why they meet in a cycle:** Fast gains 1 step on slow each iteration. In a finite cycle, the gap shrinks until it reaches 0.

5. **Space advantage:** O(1) extra space vs. O(n) for a hash set, both in O(n) time.

6. **Fast must move by 2, not 3 or 4:** Only 2 steps guarantees convergence in a cycle.

7. **Caveat:** This detects cycles but does not directly identify where the cycle starts. A second pass is required for that.

Next, you will learn **stacks** and **queues**—two data structures built on lists that enforce rules about how and when you can add and remove elements. These tools are essential for parsing, undo/redo, and breadth-first search.
</Recap>
