---
slug: 04-the-stack
lang: en
track: algorithms
unit: 06-lists-stacks-queues
order: 4
title: "The stack"
summary: "A stack is a LIFO container where you push elements on top and pop them from the top. Both operations run in O(1) constant time. Stacks model the undo/redo history, expression parsing, and any situation where the last thing added must be the first thing removed."
estMin: 22
status: ready
prereqs: ["01-the-linked-list", "02-pointer-manipulation"]
mathPrereqs: []
concepts: ["stack", "LIFO", "push", "pop", "peek", "balanced_brackets", "expression_validation"]
sources:
  - https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
  - https://www.geeksforgeeks.org/dsa/introduction-to-stack-data-structure-and-algorithm-tutorials/
  - https://runestone.academy/ns/books/published/pythonds/BasicDS/SimpleBalancedParentheses.html
  - https://www.geeksforgeeks.org/dsa/time-and-space-complexity-analysis-of-stack-operations/
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Term from "~/components/prose/Term.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Last lesson you learned the two-pointer technique on a linked list. Today you will learn about the <Term slug="stack">stack</Term>—a simple and powerful data structure. A stack is like a stack of plates: you add plates on top, and you remove plates from the top. The last plate added is the first one to come off. In computing, you use stacks for undo/redo history (each action pushes a new state onto the stack), expression parsing (matching brackets and operators), and anywhere the most recent thing is the most important. A stack enforces one rule: <Term slug="lifo">LIFO</Term> (last-in-first-out).
</Hook>

<Goal lang="en">
After this lesson you can explain the LIFO discipline and the three core stack operations (push, pop, peek), implement a stack using an array or a linked list, explain why all stack operations run in O(1) time, solve bracket-matching and expression-validation problems using a stack, and recognize when a stack is the right data structure for a problem.
</Goal>

<Idea lang="en">
**The LIFO rule:**

A <Term slug="stack">stack</Term> is a <Term slug="linear_data_structure">linear data structure</Term> where all insertions and deletions happen at one end, called the <Term slug="top">top</Term>. The rule is LIFO (Last In, First Out): the element added most recently is the one removed first.

Imagine a stack of dinner plates in a cupboard. When you grab a plate, you take from the top (the last one placed there). When you put a plate back, it goes on top. You never grab from the middle or bottom. That is LIFO.

**The three operations:**

1. **Push:** Add an element to the top of the stack. Returns nothing; updates the stack in-place.
2. **Pop:** Remove and return the top element of the stack. If the stack is empty, either return null or raise an error (implementation-dependent).
3. **Peek (or top):** Return the top element without removing it. Lets you see what is there before you commit to popping.

```js
const stack = [];  // Empty stack

// Push: add 10, 20, 30
stack.push(10);    // Stack: [10]
stack.push(20);    // Stack: [10, 20]
stack.push(30);    // Stack: [10, 20, 30]

// Peek: see the top
console.log(stack[stack.length - 1]);  // 30 (without removing)

// Pop: remove the top
const top = stack.pop();  // Returns 30. Stack: [10, 20]
```

**Array implementation:**

An array naturally models a stack. The end of the array is the top. Push appends to the end; pop removes from the end. Both run in O(1) <Term slug="amortized_time">amortized</Term> time (in JavaScript, the language hides array resizing).

```js
class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
}
```

**Linked-list implementation:**

A stack can also be backed by a linked list. Push and pop both work at the head (no traversal needed). This is also O(1).

```js
class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class StackLinkedList {
  constructor() {
    this.top = null;  // Head pointer
  }
  
  push(value) {
    const newNode = new Node(value, this.top);
    this.top = newNode;
  }
  
  pop() {
    if (this.top === null) return null;
    const value = this.top.value;
    this.top = this.top.next;
    return value;
  }
  
  peek() {
    if (this.top === null) return null;
    return this.top.value;
  }
  
  isEmpty() {
    return this.top === null;
  }
}
```

**A worked application: balanced brackets**

One of the most common uses of stacks is to check if brackets, parentheses, and braces are balanced in an expression.

**The problem:** Given a string with characters `(`, `)`, `[`, `]`, `{`, `}`, check if every opening bracket has a matching closing bracket in the correct order.

Example: `"({[]})"` is balanced. `"({[}])"` is not (the `}` closes the wrong bracket).

**The algorithm:** Process the string from left to right.
1. If you see an opening bracket (`(`, `[`, `{`), push it onto the stack.
2. If you see a closing bracket (`)`, `]`, `}`), pop the stack and check if it matches. If it does not match, return false. If the stack is empty, return false (no opening bracket to match).
3. At the end, the stack must be empty. If not, there are unclosed opening brackets.

Example trace: `"{[]}"`
- See `{`: push `{`. Stack: `[{]`
- See `[`: push `[`. Stack: `[{, []`
- See `]`: pop `[`. Does `[` match `]`? Yes. Stack: `[{]`
- See `}`: pop `{`. Does `{` match `}`? Yes. Stack: `[]`
- End: stack is empty. Balanced!

Example trace: `"{[}]"`
- See `{`: push `{`. Stack: `[{]`
- See `[`: push `[`. Stack: `[{, []`
- See `}`: pop `[`. Does `[` match `}`? No. Return false. Not balanced.

</Idea>

<Code lang="en">
**Array-backed stack:**

```js
class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
  
  size() {
    return this.items.length;
  }
}
```

**Checking for balanced brackets:**

```js
function isBalanced(s) {
  const stack = new Stack();
  const pairs = { ')': '(', ']': '[', '}': '{' };
  
  for (let char of s) {
    if (char === '(' || char === '[' || char === '{') {
      stack.push(char);
    } else if (char === ')' || char === ']' || char === '}') {
      const top = stack.pop();
      if (top !== pairs[char]) {
        return false;
      }
    }
  }
  
  return stack.isEmpty();
}
```

Try both in the interactive runner:

<CodeRunner
  id="cr-stack"
  lang="en"
  code={`class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
}

function isBalanced(s) {
  const stack = new Stack();
  const pairs = { ')': '(', ']': '[', '}': '{' };
  
  for (let char of s) {
    if (char === '(' || char === '[' || char === '{') {
      stack.push(char);
    } else if (char === ')' || char === ']' || char === '}') {
      const top = stack.pop();
      if (top !== pairs[char]) {
        return false;
      }
    }
  }
  
  return stack.isEmpty();
}

// Test 1: Balanced
console.log("Test '({[]}):', " + isBalanced("({[]})"));

// Test 2: Unbalanced (wrong closing bracket)
console.log("Test '({[}]):', " + isBalanced("({[}])"));

// Test 3: Unbalanced (unclosed)
console.log("Test '({[):', " + isBalanced("({[)"));

// Test 4: Unbalanced (extra closing)
console.log("Test '() )', " + isBalanced("())"));

// Test 5: Empty
console.log("Test '':", " + isBalanced(""));`}
/>
</Code>

<Trace lang="en">
Let us trace checking if `"({[]})"` is balanced.

<AlgoTrace
  id="trace-balanced"
  lang="en"
  code={[
    "for (let char of s) {",
    "  if (char === '(' || char === '[' || char === '{') {",
    "    stack.push(char);",
    "  } else if (char === ')' || char === ']' || char === '}') {",
    "    const top = stack.pop();",
    "    if (top !== pairs[char]) return false;",
    "  }",
    "}",
    "return stack.isEmpty();"
  ]}
  frames={[
    {
      caption: "Start: empty stack. Process char '('",
      line: 2,
      cells: [
        { value: "(", mark: "target" },
        { value: "{", mark: "value" },
        { value: "[", mark: "value" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "[]"
    },
    {
      caption: "Char '(' is opening. Push it. Stack: ['(']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "target" },
        { value: "[", mark: "value" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(']"
    },
    {
      caption: "Char '{' is opening. Push it. Stack: ['(', '{']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "target" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{']"
    },
    {
      caption: "Char '[' is opening. Push it. Stack: ['(', '{', '[']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "target" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{', '[']"
    },
    {
      caption: "Char ']' is closing. Pop '['. Does '[' match ']'? Yes. Stack: ['(', '{']",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "target" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{']"
    },
    {
      caption: "Char '}' is closing. Pop '{'. Does '{' match '}'? Yes. Stack: ['(']",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "target" }
      ],
      stackState: "['(']"
    },
    {
      caption: "Char ')' is closing. Pop '('. Does '(' match ')'? Yes. Stack: []",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "done" }
      ],
      stackState: "[]"
    },
    {
      caption: "End of string. Stack is empty. Return true. Balanced!",
      line: 9,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "done" }
      ],
      stackState: "[]"
    }
  ]}
/>

</Trace>

<Complexity lang="en">
**Stack operations:**

| Operation | Time | Space |
|---|---|---|
| Push | O(1) | O(1) |
| Pop | O(1) | O(1) |
| Peek | O(1) | O(1) |
| isEmpty | O(1) | O(1) |

All operations run in O(1) constant time because they only touch the top of the stack. No <Term slug="traversal">traversal</Term> is needed; no loop iterates over the structure.

**Array vs. linked-list implementation:**

Both have O(1) push and pop. The difference is memory layout:
- **Array:** Cache-friendly, but resizing (when the array grows) is O(n) and rare.
- **Linked list:** No resizing, but each <Term slug="node">node</Term> costs extra memory for the <Term slug="pointer">pointer</Term>.

For most problems, array is simpler. Use linked-list if memory is scarce or if resizing is unacceptable.

**Balanced-brackets checker:**

- **Time:** O(n), where n is the length of the string. Each character is processed once.
- **Space:** O(n) in the worst case (all opening brackets), but typically much less.
</Complexity>

<AlgoPractice
  id="practice-stack"
  lessonSlug="04-the-stack"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "After pushing 10, 20, and 30 onto a stack (in that order), what does pop return?",
      choices: [
        "10",
        "20",
        "30",
        "null"
      ],
      answer: 2,
      hint: "A stack is LIFO. The last element pushed (30) is the first to come off."
    },
    {
      kind: "choice",
      prompt: "Which operation lets you check what is on top of the stack without removing it?",
      choices: [
        "pop",
        "peek (or top)",
        "push",
        "isEmpty"
      ],
      answer: 1,
      hint: "Peek reads the top element without popping. It is O(1) and does not modify the stack."
    },
    {
      kind: "choice",
      prompt: "Is the string '([)]' balanced?",
      choices: [
        "Yes, because it has the same number of opening and closing brackets",
        "No, because the ')' closes before the ']', but '[' came after '(' ",
        "Yes, because the brackets are correctly nested",
        "Cannot determine without more information"
      ],
      answer: 1,
      hint: "Brackets must close in the reverse order they opened (LIFO). The ')' closes the '(' before the ']', which violates the nesting order."
    },
    {
      kind: "choice",
      prompt: "For the balanced-brackets algorithm, what is the time complexity?",
      choices: [
        "O(1)",
        "O(n), where n is the length of the string",
        "O(n²)",
        "O(log n)"
      ],
      answer: 1,
      hint: "Each character in the string is processed once (one iteration). Each operation (push or pop) is O(1), so the total is O(n)."
    },
    {
      kind: "choice",
      prompt: "Which implementation of a stack is less memory-efficient because each node stores a pointer?",
      choices: [
        "Array stack",
        "Linked-list stack",
        "Both are equally efficient",
        "Neither—stacks do not store pointers"
      ],
      answer: 1,
      hint: "Linked-list nodes store both the value and a pointer to the next node. Arrays do not need extra pointers per element."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Empty stack operations:** When the stack is empty and you try to pop or peek, what should happen? Different languages differ: some return null, some raise an exception. In JavaScript, `Array.pop()` returns `undefined`. When implementing a stack, you should decide upfront: return null/undefined for an empty pop, or throw an error? For the bracket-matching problem, we return null and check for it before using the value.
</Inset>

<Inset kind="mistake" lang="en">
**"A stack is just an array."** No. A stack is an abstract discipline (LIFO with only top access), not a data structure. An array is a data structure (random access, indexing). You can implement a stack using an array, but the stack hides the array's ability to access the middle or bottom. Do not think of them as the same thing—the stack imposes rules.
</Inset>

<Inset kind="practice" lang="en">
**Follow-up: undo/redo.** A real-world use of stacks is undo/redo in text editors. Each action pushes a state onto the undo stack. When you press Ctrl+Z, pop from the undo stack and restore the previous state, while pushing the current state onto the redo stack. This is a beautiful application of LIFO.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-stack"
    pieceSlug="04-the-stack"
    lang="en"
    question="Why is a stack the ideal data structure for checking balanced brackets instead of, say, a queue or an array?"
    choices={[
      {
        label: "A stack enforces LIFO: the most recently opened bracket must be the first to close, which matches the nesting requirement.",
        correct: true
      },
      {
        label: "A queue is slower than a stack because it has O(n) operations.",
        misconception: "Queues also have O(1) operations. The reason stacks are better is LIFO semantics, not speed."
      },
      {
        label: "An array cannot store brackets, so we must use a stack.",
        misconception: "You can store brackets in an array. But using an array directly would require checking inside it (O(n)), whereas a stack lets you only look at the top (O(1))."
      },
      {
        label: "Stacks are the only data structure that can be implemented with arrays.",
        misconception: "Many data structures can be implemented with arrays: queues, heaps, etc. Stacks are chosen for bracket matching because of LIFO, not because of array implementation."
      }
    ]}
  />
</Check>

<Recap lang="en">
**The stack: LIFO and three operations**

1. **LIFO rule:** The last element pushed is the first one popped. Think of a stack of plates.

2. **Three operations:** push (add to top, O(1)), pop (remove from top, O(1)), peek (see top without removing, O(1)). All are constant time.

3. **Two implementations:** Array (simpler, cache-friendly, rare O(n) resize) or linked list (no resize, extra pointer per node).

4. **Balanced-brackets checker:** Process left to right. Push opening brackets, pop and match closing brackets. If stack is empty at the end, all brackets are balanced. O(n) time, O(n) space.

5. **Real-world uses:** Undo/redo, expression parsing, function call stacks, depth-first search traversal.

6. **Key insight:** A stack is an abstract discipline, not a data structure. You can implement it with an array or a linked list—both give O(1) operations.

Next, you will learn **queues**—the opposite of stacks. A queue enforces FIFO (First In, First Out). You enqueue at the back and dequeue from the front. Queues model waiting lines, breadth-first search, and any situation where fairness (first come, first served) matters.
</Recap>
