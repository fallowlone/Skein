---
concepts:
- heap-property
- build-heap-complexity
- priority-queue-adt
- top-k
- k-way-merge
- min-heap-vs-max-heap
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://en.wikipedia.org/wiki/Priority_queue
- https://en.wikipedia.org/wiki/K-way_merge_algorithm
status: ready
summary: "Multiple-choice synthesis across the heaps unit: the heap invariant, O(n) build-heap, the PQ contract, and choosing the right heap for top-k and k-way merge."
title: 'Heaps: multiple-choice review'
track: algorithms
unit: 08-heaps
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None are definition recall — each is a design decision: which heap, which complexity, which invariant. Pick the answer you would defend in an interview, then read why each distractor fails.
</Hook>

<Goal lang="en">
Confirm you can connect the heap invariant, the O(n) build-heap argument, the priority-queue contract, and the size-k heap pattern that powers both top-k and k-way merge — the synthesis the five lessons built toward.
</Goal>

<Quiz
  id="08-heaps-choice-1"
  lessonSlug="08-heaps"
  lang="en"
  question="An interviewer hands you the array [10, 5, 20, 15, 30] and asks 'is this a valid min-heap?' What is the correct answer and the reason?"
  choices={[
    { label: "No. Read it as a complete tree: root 10 at index 0 has children 5 (index 1) and 20 (index 2). 10 > 5 violates parent ≤ child. The heap property is positional, checked parent-to-child via index arithmetic — not whether the array is sorted.", correct: true },
    { label: "Yes, because the array is not sorted, and heaps are by definition unsorted arrays.", misconception: "Being unsorted is not the test. A heap must satisfy parent ≤ both children at every node. Here 10 > 5 at the root, so it fails — regardless of sortedness." },
    { label: "No, because a min-heap array must be in ascending order.", misconception: "A min-heap is NOT a sorted array. [1, 3, 2, 5, 4] is a valid min-heap but not sorted. Only the parent-child relationship at each index must hold." },
    { label: "Yes, because the minimum value 5 is present somewhere in the array.", misconception: "The minimum being present is not enough; in a min-heap the minimum must be at the root (index 0). Here the root is 10, not 5." },
  ]}
/>

<Quiz
  id="08-heaps-choice-2"
  lessonSlug="08-heaps"
  lang="en"
  question="Building a heap from an n-element array by calling push n times is O(n log n), but the standard build-heap (heapify) is O(n). Where does the speedup come from?"
  choices={[
    { label: "build-heap sift-downs from the last non-leaf to the root, so cost scales with each node's height, not the tree height. Half the nodes are leaves (height 0), a quarter height 1, etc.; Σ 2^h·(levels − h) converges to ≤ 2n. Sifting UP (what push does) costs the full depth for the many bottom nodes — that is the expensive direction.", correct: true },
    { label: "build-heap skips most nodes, only sifting a constant number of them.", misconception: "build-heap sift-downs every non-leaf — about n/2 nodes. It is not skipping nodes; the win is that nodes near the bottom (the majority) have tiny subtree heights, so each sift-down is cheap." },
    { label: "Both are actually O(n log n); the O(n) bound is a myth.", misconception: "build-heap is provably O(n). The summation Σ (n/2^{h+1})·h converges to a constant times n, so heapify is linear — a standard, well-known result." },
    { label: "build-heap is O(n) only on already-sorted input.", misconception: "build-heap is O(n) on ANY input, sorted or not. The bound comes from the height distribution of nodes, which does not depend on the values." },
  ]}
/>

<Quiz
  id="08-heaps-choice-3"
  lessonSlug="08-heaps"
  lang="en"
  question="A teammate says 'a priority queue is just a heap.' Why is that imprecise, and when does the distinction earn its keep?"
  choices={[
    { label: "A priority queue is an ADT — a contract (insert, extractMin/Max, peek). A binary heap is one implementation; a sorted array, a balanced BST, or a Fibonacci heap also satisfy the contract with different complexity profiles. The distinction matters when decrease-key is hot (Dijkstra) — there a Fibonacci heap or an indexed binary heap changes the asymptotics.", correct: true },
    { label: "They are interchangeable; the distinction is academic and never affects code.", misconception: "It affects real designs. A plain binary heap has no efficient decrease-key, so Dijkstra on dense graphs may want a different PQ implementation. Naming the contract separately from the implementation is what lets you swap it." },
    { label: "A priority queue is faster than a heap because it is higher-level.", misconception: "An ADT has no speed of its own — speed comes from the implementation. A PQ backed by a sorted array has O(n) insert; backed by a heap it is O(log n). The ADT is the contract, not a performance claim." },
    { label: "A heap can do peek in O(1) but a priority queue cannot.", misconception: "peek in O(1) is exactly a property the heap implementation gives the PQ contract. The PQ ADT specifies peek; the heap is what makes it O(1)." },
  ]}
/>

<Quiz
  id="08-heaps-choice-4"
  lessonSlug="08-heaps"
  lang="en"
  question="You must keep the k largest elements out of a stream of n numbers (k ≪ n). Which heap, and why that one and not the other?"
  choices={[
    { label: "A MIN-heap of size k. Its root is the smallest of the current top-k, so on each new element you compare against the root in O(1) and evict it in O(log k) if the newcomer is larger. Total O(n log k), space O(k). A max-heap would put the largest at the root, hiding the eviction candidate and forcing an O(k) scan to find the minimum.", correct: true },
    { label: "A MAX-heap of size k, so the largest of the top-k is always visible at the root.", misconception: "For k LARGEST you want to evict the SMALLEST candidate when the heap overflows. A max-heap hides the minimum, so finding the evictee costs O(k). The min-heap keeps the evictee at the root — that is the whole trick." },
    { label: "A max-heap of size n, then pop k times.", misconception: "That is O(n + k log n) time and O(n) space — you stored everything. The size-k min-heap is O(n log k) time and O(k) space, which is the point when k ≪ n and the data is a stream you cannot fully retain." },
    { label: "Either heap works equally well; the choice is stylistic.", misconception: "The choice changes the eviction cost from O(log k) (min-heap, evictee at root) to O(k) (max-heap, must scan for the minimum). It is a correctness-of-complexity decision, not style." },
  ]}
/>

<Quiz
  id="08-heaps-choice-5"
  lessonSlug="08-heaps"
  lang="en"
  question="In k-way merge of k sorted lists with n total elements, each heap entry stores (value, listIndex, position) rather than just the value. Why carry the bookkeeping?"
  choices={[
    { label: "After popping the minimum you must push the NEXT element from the same list to preserve the invariant 'one candidate per list in the heap.' The listIndex/position tells you which list to advance. Without it you could not know where the popped value came from, and the merge would break.", correct: true },
    { label: "It lets the heap sort the lists by length first.", misconception: "List length is irrelevant to the merge. The bookkeeping exists so that when you remove a head, you can replace it with the next element from that specific list — keeping exactly one candidate per active list." },
    { label: "It is only needed to detect duplicate values across lists.", misconception: "Duplicates merge fine without extra metadata. The metadata's job is advancement: knowing which list to pull the successor from after each pop." },
    { label: "Storing the metadata is what makes the algorithm O(n log k) instead of O(n log n).", misconception: "The O(n log k) bound comes from the heap holding at most k elements, so each pop/push is O(log k). The metadata is for correctness (advancing the right list), not for the complexity bound." },
  ]}
/>

<Quiz
  id="08-heaps-choice-6"
  lessonSlug="08-heaps"
  lang="en"
  question="A pop on a min-heap moves the last array element to the root, shrinks the array by one, then sift-downs. Why move the LAST element up rather than promoting the smaller child into the gap and repeating?"
  choices={[
    { label: "Promoting children up would leave a hole that propagates to an arbitrary leaf, possibly breaking the complete-tree shape (gaps not filled left-to-right). Moving the last element to the root keeps the array contiguous — the shape stays a complete tree — and a single sift-down restores ordering in O(log n).", correct: true },
    { label: "Because the last element is always the second-smallest, so it belongs at the root.", misconception: "The last element has no special rank — it is just the rightmost leaf. It is chosen because removing it preserves the complete-tree shape, then sift-down fixes ordering. It is usually large, which is why it sinks back down." },
    { label: "Promoting children is faster, but the standard library forbids it.", misconception: "Promoting children is not forbidden by a library; it is avoided because it can violate the complete-binary-tree shape invariant, which the array encoding depends on for its index arithmetic." },
    { label: "Either approach is fine; both keep the heap valid.", misconception: "Only moving the last element preserves the complete-tree shape that the array representation requires (no internal gaps). Promoting children can create a gap that is not at the end, breaking the index formulas." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one structure earning many jobs. The heap property plus the complete-tree shape give O(1) peek and O(log n) push/pop via sift-up and sift-down; build-heap exploits the height distribution to reach O(n). Wrap that in the priority-queue contract and the same machine becomes a scheduler, a top-k filter (size-k MIN-heap, evict the root), and a k-way merger (one candidate per list, advance by listIndex). Every question here was really 'which invariant or which heap,' which is exactly the call you make in an interview or a real system.
</Recap>
