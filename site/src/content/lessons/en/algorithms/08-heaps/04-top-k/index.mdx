---
slug: 04-top-k
lang: en
track: algorithms
unit: 08-heaps
order: 4
title: "Top-K and the k-th largest element"
summary: "Find the k largest (or k smallest) elements efficiently using a size-k heap. Key insight: to find k largest, keep a min-heap of size k—push each element, evict the min when heap exceeds k. Result: O(n log k) time, O(k) space. Beats sorting O(n log n) when k is small."
estMin: 32
status: ready
prereqs: ["08-heaps/01-the-heap", "08-heaps/02-heap-operations", "08-heaps/03-priority-queues"]
mathPrereqs: []
concepts: ["heap", "priority_queue", "top_k", "selection", "min_heap", "kth_largest", "time_complexity", "space_complexity", "streaming_algorithm"]
sources:
  - https://en.wikipedia.org/wiki/Selection_algorithm
  - https://www.algoexpert.io
  - https://brilliant.org/wiki/heap-sort/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";
import Term from "../../../../../../components/prose/Term.astro";

<Hook>
You have a stream of numbers. You want to know the k largest. Naive answer: wait for all numbers, sort them, take the last k. But that is O(n log n) time and O(n) space—you store everything. The insight: you only care about the top k, so keep a <Term slug="min_heap">min-heap</Term> of size k. Push each number. When the heap exceeds k elements, pop the smallest (the minimum). At the end, your heap holds exactly the k largest, and its root is the boundary—the k-th largest element. Time: O(n log k). Space: O(k). When k is small, this crushes sorting.
</Hook>

<Goal lang="en">
After this lesson you can solve the k-largest problem using a min-heap of size k: push each element and evict the minimum when the heap grows beyond k. You understand why this works: the heap always holds the k candidates, and the root is the k-th largest. You can explain the time complexity O(n log k) and space complexity O(k), and why this beats sorting O(n log n) when k ≪ n. You can recognize the "k-th largest" and "top k frequency" patterns on LeetCode and other platforms, and apply the same heap-based approach. You can code it in one pass (no sorting phase), and trace the algorithm on a concrete array step by step.
</Goal>

<Idea lang="en">
**The problem: find the k largest (or k smallest)**

You are given an array of numbers. Your task: find the k largest elements. For example:
- Array: [3, 2, 1, 5, 6, 4], k = 2 → answer: [5, 6]
- Array: [3, 2, 3, 1, 2, 4, 5, 5, 6], k = 4 → answer: [5, 5, 6, 4] (or [6, 5, 5, 4], order may vary)

**Naive approach: sort**

Sort the array and take the last k elements. Time: O(n log n), space: O(1) if in-place or O(n) with external storage.

Problem: you must sort the entire array, even though you only care about the top k. When k is small, this wastes work.

**The heap insight: keep a size-k heap**

Maintain a <Term slug="min_heap">min-heap</Term> of size exactly k. This heap will always contain the k largest elements seen so far.

Algorithm:
1. For each element in the array:
   - Push the element into the min-heap.
   - If the heap size exceeds k, pop the minimum.
2. After processing all elements, the heap contains exactly the k largest.
3. The root of the min-heap is the k-th largest element (the smallest of the top k).

Why min-heap, not max-heap? Because you want to evict the smallest candidate when you have too many. A min-heap's root is the smallest, so you pop it immediately.

**Why does this work?**

At any point, the heap holds the k largest elements *seen so far*. When a new element comes in:
- If it is larger than the root, it belongs in the top k, so you remove the current minimum (root) and insert the new element.
- If it is smaller than the root, it is not in the top k, so you ignore it.

After processing all n elements, the heap holds the k largest globally. The root is the boundary: it is the largest element among the top k that could be evicted, i.e., the k-th largest.

**Example: find the 2 largest in [3, 2, 1, 5, 6, 4]**

Min-heap of size k=2:

1. Push 3: heap = [3]. Size = 1 < k, continue.
2. Push 2: heap = [2, 3]. Size = 2 = k, continue.
3. Push 1: heap = [1, 3, 2]. Size = 3 > k, pop min (1). heap = [2, 3].
4. Push 5: heap = [2, 3, 5]. Size = 3 > k, pop min (2). heap = [3, 5].
5. Push 6: heap = [3, 5, 6]. Size = 3 > k, pop min (3). heap = [5, 6].
6. Push 4: heap = [4, 5, 6]. Size = 3 > k, pop min (4). heap = [5, 6].

Final heap: [5, 6]. The k largest are 5 and 6. ✓

**Symmetric problem: find the k smallest**

If you want the k *smallest* elements, use a <Term slug="max_heap">max-heap</Term> of size k. The logic is identical: when the heap exceeds k, pop the maximum (the root). At the end, the heap holds the k smallest, and the root is the k-th smallest.

**Common pattern: k-th largest element**

Often, the problem asks only for the k-th largest element, not all k. After building the heap, return `heap.peek()` (the root)—it is the k-th largest.

**Common pattern: top k frequency**

Given an array, find the k most frequent elements. Solution:
1. Count frequency of each element (hash map).
2. Build a min-heap of size k on frequencies (not elements).
3. Evict the least frequent when the heap exceeds k.
4. The heap contains the k most frequent elements.

This is a direct application of the same pattern.

</Idea>

<Code lang="en">
**Top-k using a min-heap**

<AnnotatedCode
  code={`function topK(arr, k) {
  // Min-heap (priority queue with smaller values having higher priority)
  const minHeap = new MinPriorityQueue();
  
  // Push each element and evict the min if heap exceeds k
  for (const num of arr) {
    minHeap.insert(num, num); // insert(value, priority)
    if (minHeap.size() > k) {
      minHeap.extractMin(); // Remove the smallest
    }
  }
  
  // Extract all elements from heap (they are in heap order, not sorted)
  const result = [];
  while (!minHeap.isEmpty()) {
    result.push(minHeap.extractMin());
  }
  return result;
}

// Example
const arr = [3, 2, 1, 5, 6, 4];
const k = 2;
const topTwoLargest = topK(arr, k);
console.log(topTwoLargest); // [5, 6] (order may vary; both are in the heap)`}
  annotations={[
    { line: 2, text: "A min-heap (priority queue with min at root). Size will stay ≤ k." },
    { line: 5, text: "For each element in the array." },
    { line: 6, text: "Push the element with priority = value (smaller priority = higher priority)." },
    { line: 7, text: "If heap size exceeds k, the smallest element (the root) is removed." },
    { line: 12, text: "Extract all remaining elements. Since it is a heap (not a sorted array), the order may not be strictly ascending, but all k largest are present." }
  ]}
/>

**Finding the k-th largest element**

<CodeRunner
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(value) {
    this.heap.push(value);
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root;
  }

  peek() {
    return this.heap[0];
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  size() {
    return this.heap.length;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index] < this.heap[parentIndex]) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left] < this.heap[smallest]) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right] < this.heap[smallest]) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

// Find the k-th largest element
function findKthLargest(arr, k) {
  const minHeap = new MinPriorityQueue();
  
  for (const num of arr) {
    minHeap.insert(num);
    if (minHeap.size() > k) {
      minHeap.extractMin();
    }
  }
  
  // The root is the k-th largest
  return minHeap.peek();
}

// Example
const arr = [3, 2, 1, 5, 6, 4];
const k = 2;
const kthLargest = findKthLargest(arr, k);
console.log("The " + k + "-th largest element:", kthLargest);

const arr2 = [3, 2, 1, 5, 6, 4];
const k2 = 4;
const kthLargest2 = findKthLargest(arr2, k2);
console.log("The " + k2 + "-th largest element:", kthLargest2);`}
  expected={`The 2-th largest element: 5
The 4-th largest element: 3`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="topk-insert-trace"
  lang="en"
  code={[
    "const arr = [3, 2, 1, 5, 6, 4];",
    "const k = 2;",
    "const minHeap = new MinPriorityQueue();",
    "for (const num of arr) {",
    "  minHeap.insert(num);",
    "  if (minHeap.size() > k) minHeap.extractMin();",
    "}"
  ]}
  frames={[
    { caption: "Push 3: heap = [3]. Size = 1 ≤ k.", line: 5, cells: [{ value: "3" }] },
    { caption: "Push 2: heap = [2, 3]. Size = 2 ≤ k, sift-up: 2 < 3, so 2 is root.", line: 5, cells: [{ value: "2", mark: "active" }, { value: "3" }] },
    { caption: "Push 1: heap = [1, 3, 2]. Size = 3 > k. Sift-up: 1 < 2 and 1 < 3, so 1 is root.", line: 5, cells: [{ value: "1", mark: "active" }, { value: "3" }, { value: "2" }] },
    { caption: "Size > k, extract min (1). heap = [2, 3]. Size = 2 ≤ k.", line: 6, cells: [{ value: "2", mark: "done" }, { value: "3" }] },
    { caption: "Push 5: heap = [2, 3, 5]. Size = 3 > k. Sift-up: 5 is added as child of 2 at index 2.", line: 5, cells: [{ value: "2", mark: "active" }, { value: "3" }, { value: "5" }] },
    { caption: "Size > k, extract min (2). heap = [3, 5]. Size = 2 ≤ k.", line: 6, cells: [{ value: "3", mark: "done" }, { value: "5" }] },
    { caption: "Push 6: heap = [3, 5, 6]. Size = 3 > k. Sift-up: 6 is child of 3 at index 2.", line: 5, cells: [{ value: "3", mark: "active" }, { value: "5" }, { value: "6" }] },
    { caption: "Size > k, extract min (3). heap = [5, 6]. Size = 2 ≤ k.", line: 6, cells: [{ value: "5", mark: "done" }, { value: "6" }] },
    { caption: "Push 4: heap = [4, 6, 5]. Size = 3 > k. Sift-up: 4 < 5, 4 becomes root.", line: 5, cells: [{ value: "4", mark: "active" }, { value: "6" }, { value: "5" }] },
    { caption: "Size > k, extract min (4). heap = [5, 6]. Size = 2 ≤ k. Final: the 2 largest are 5 and 6.", line: 6, cells: [{ value: "5", mark: "done" }, { value: "6", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Top-K using a min-heap of size k**

For each of the n elements in the array:
- <Term slug="insert">Insert</Term>: O(log k) (heap has at most k+1 elements before pop).
- <Term slug="extract_min">ExtractMin</Term> (when heap exceeds k): O(log k).

Total: n iterations × O(log k) per iteration = **O(n log k) time**.

Space: The heap holds at most k elements, so **O(k) space**.

**Comparison with sorting**

If you sort the entire array: O(n log n) time, O(1) auxiliary space (if in-place).

Top-K with a heap: O(n log k) time, O(k) space.

When is the heap approach better?
- If k ≪ n (k is much smaller than n), then log k ≪ log n, so O(n log k) ≪ O(n log n).
- If k is close to n, sorting may be simpler and faster in practice.

Example:
- n = 1,000,000, k = 10: O(n log k) = O(1,000,000 × 3.3) ≈ 3.3 million operations. O(n log n) = O(1,000,000 × 20) ≈ 20 million operations. Heap is faster.
- n = 1,000, k = 500: O(n log k) = O(1,000 × 9) ≈ 9,000. O(n log n) = O(1,000 × 10) ≈ 10,000. Similar; sorting may be simpler.

**Space-time tradeoff**

Sorting uses O(n) space (or O(1) with in-place tricks). Top-K uses O(k) space. When k is much smaller than n, the heap saves memory.

</Complexity>

<AlgoPractice
  id="practice-top-k"
  lessonSlug="04-top-k"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "To find the k largest elements, should you use a min-heap or a max-heap of size k?",
      choices: [
        "A max-heap, so the largest element is always at the root.",
        "A min-heap, so the smallest of the top-k is at the root and can be evicted.",
        "Either one works equally well.",
        "Neither—use quickselect instead."
      ],
      answer: 1,
      hint: "You want to evict the smallest candidate when the heap exceeds size k. A min-heap's root is the minimum, so you pop it immediately. A max-heap's root is the maximum—you would have to find the minimum elsewhere, which is slow."
    },
    {
      kind: "choice",
      prompt: "What is the k-th largest element in the context of the top-K heap algorithm?",
      choices: [
        "The largest element in the final heap.",
        "The root of the final min-heap—the smallest of the top k.",
        "The element that was evicted first.",
        "The median of the k elements."
      ],
      answer: 1,
      hint: "The final min-heap contains the k largest elements. Its root is the smallest among them—the boundary, i.e., the k-th largest."
    },
    {
      kind: "choice",
      prompt: "Given arr = [3, 2, 1, 5, 6, 4] and k = 2, what is the k-th largest element?",
      choices: [
        "3",
        "4",
        "5",
        "6"
      ],
      answer: 2,
      hint: "The 2 largest elements are 5 and 6. The 2nd largest (the smaller of the two) is 5."
    },
    {
      kind: "choice",
      prompt: "What is the time complexity of finding the k largest elements using a min-heap?",
      choices: [
        "O(n)",
        "O(n log k)",
        "O(n log n)",
        "O(k log n)"
      ],
      answer: 1,
      hint: "For each of n elements, you insert into a heap of size ≤ k (O(log k) per insert). n iterations × O(log k) = O(n log k)."
    },
    {
      kind: "numeric",
      prompt: "You want to find the 5 largest elements in an array of 1,000,000 numbers. Approximately how many more operations does sorting take compared to the heap approach? (Use log₂(1,000,000) ≈ 20 and log₂(5) ≈ 2.3.)",
      answer: 17700000,
      hint: "Heap: 1,000,000 × 2.3 ≈ 2,300,000. Sorting: 1,000,000 × 20 ≈ 20,000,000. Difference: 20,000,000 - 2,300,000 ≈ 17,700,000. The heap is much faster for small k."
    },
    {
      kind: "choice",
      prompt: "The 'top k frequent' problem asks: given an array, find the k most frequent elements. How would you apply the heap strategy?",
      choices: [
        "Build a max-heap of all elements by frequency.",
        "Count frequencies (hash map), then build a min-heap of size k on frequencies, evicting the least frequent.",
        "Sort elements by frequency and take the first k.",
        "Use quickselect on the frequency counts."
      ],
      answer: 1,
      hint: "You first count frequency (hash map in O(n)). Then you apply the same heap pattern: min-heap of size k, evicting the element with the smallest frequency when the heap exceeds k. At the end, the k most frequent elements are in the heap."
    }
  ]}
/>

<Inset type="why">
**Why top-K matters.** Real-world systems often ask: give me the top 10 search results, or the top 100 users by engagement, or the top k rarest errors. Sorting the entire dataset every time is wasteful. A top-K heap lets you answer these queries efficiently, even on large datasets and streams. Streaming use case: imagine a sensor emits readings continuously. You want to know the 100 largest readings seen so far. You cannot store all readings (memory-limited). A size-100 heap lets you maintain the top 100 online, processing each reading in O(log 100) time.
</Inset>

<Inset type="mistake">
**Using a max-heap instead of a min-heap.** If you keep a max-heap of size k to find the k largest, the largest element is at the root—but you want to evict the smallest candidate when the heap exceeds k. A max-heap forces you to search the entire heap for the minimum, which is O(k), not O(log k). Min-heap keeps the smallest candidate (the evictee) at the root, so one comparison tells you if a new element belongs in the top k.
</Inset>

<Inset type="edgecase">
**k equals n or k is larger than n.** If k ≥ n, every element is in the top k, so just return the entire array. If k = 0, return an empty list. If k is 1, you are finding the single maximum, which is O(n) with a heap (one pass, one insert per element, size never exceeds 1) or O(n) with a linear scan.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-top-k"
    pieceSlug="04-top-k"
    lang="en"
    question="Explain the top-K heap algorithm. Why do you use a min-heap of size k instead of a max-heap or sorting?"
    choices={[
      {
        label: "A min-heap of size k maintains the k largest elements. When a new element arrives, you push it. If the heap exceeds size k, you pop the root (the smallest of the k candidates). This is the k-th largest—the boundary. Time is O(n log k): n iterations of insert/pop, each O(log k). A max-heap would put the largest at the root, making it hard to evict the smallest. Sorting takes O(n log n), which is worse than O(n log k) when k ≪ n.",
        correct: true
      },
      {
        label: "A max-heap of size k is better because the largest element is always visible at the root.",
        misconception: "A max-heap puts the maximum at the root, so you cannot see the minimum (the candidate to evict). You would have to search the heap for the minimum, which is O(k) per insertion—worse than O(log k)."
      },
      {
        label: "Sorting is always the best approach because it produces a sorted result.",
        misconception: "Sorting is O(n log n) and requires O(n) space. The heap approach is O(n log k) and uses O(k) space. When k is small, the heap wins on both time and space."
      },
      {
        label: "You should use a balanced binary search tree (BST) instead of a heap, because it maintains sorted order.",
        misconception: "A BST works for top-K (O(n log k) time), but a heap is simpler and faster in practice because you only need the k largest, not fully sorted order."
      }
    ]}
  />
</Check>

<Recap lang="en">
To find the **k largest elements**, maintain a <Term slug="min_heap">min-heap of size k</Term>. For each element in the array, push it and evict the minimum when the heap exceeds k. The final heap contains exactly the k largest, and its root is the <Term slug="kth_largest">k-th largest element</Term>. Time: **O(n log k)**. Space: **O(k)**. This beats sorting (O(n log n)) when k ≪ n. Symmetric logic applies to k smallest (use a max-heap). The same pattern solves "top k frequent elements": count frequencies, apply the heap strategy on frequency values. **Next:** lesson 05 extends this into k-way merge, where you maintain a size-k heap of streams, pulling the next minimum from the stream whose head is smallest.
</Recap>
