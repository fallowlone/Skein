---
concepts:
- sift-up
- sift-down
- build-heap
- top-k
- heapify-complexity
- last-non-leaf
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://en.wikipedia.org/wiki/Selection_algorithm
- https://en.wikipedia.org/wiki/Heapsort
status: ready
summary: "Read real heap code and predict behaviour: a sift-up bug, the build-heap loop bound, the top-k size-k heap, and the heapify complexity argument."
title: 'Heaps: code reading'
track: algorithms
unit: 08-heaps
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Heap bugs hide in index arithmetic and loop bounds — a sign flip, an off-by-one in the last non-leaf, a min/max mix-up. Read each snippet the way you would in review: trace the invariant, then pick the precise defect or the correct claim.
</Hook>

<Goal lang="en">
Practise reading heap code at the level a senior engineer reviews it: spot a sift-up that compares the wrong way, justify the build-heap loop bound, confirm the top-k heap is sized and oriented correctly, and defend why heapify is O(n).
</Goal>

## Snippet 1 — sift-up

```js
// Intended: min-heap. siftUp restores the property after a push.
function siftUp(heap, i) {
  while (i > 0) {
    const parent = Math.floor((i - 1) / 2);
    if (heap[i] > heap[parent]) {        // <-- comparison
      [heap[i], heap[parent]] = [heap[parent], heap[i]];
      i = parent;
    } else {
      break;
    }
  }
}
```

<Quiz
  id="08-heaps-code-1"
  lessonSlug="08-heaps"
  lang="en"
  question="This is meant to maintain a MIN-heap, but the property breaks after pushes. What is the defect?"
  choices={[
    { label: "The comparison is backwards. In a min-heap a child must be SMALLER than its parent to bubble up, so the swap condition should be heap[i] < heap[parent]. As written it swaps when the child is larger — that is the rule for a max-heap, so it builds a max-heap, not a min-heap.", correct: true },
    { label: "The parent index should be Math.floor(i / 2), not Math.floor((i - 1) / 2).", misconception: "For a 0-indexed array the parent of i is exactly floor((i-1)/2). floor(i/2) is the 1-indexed formula and would be wrong here. The parent arithmetic is correct; the comparison direction is the bug." },
    { label: "The loop should run while i >= 0, not i > 0.", misconception: "At i = 0 you are at the root, which has no parent — stopping at i > 0 is correct. Running to i >= 0 would compute parent of the root and read out of bounds. The real bug is the comparison direction." },
    { label: "There is no bug; it correctly maintains a min-heap.", misconception: "It swaps when heap[i] > heap[parent], pushing larger values up — that maintains a MAX-heap. For a min-heap the condition must be heap[i] < heap[parent]." },
  ]}
/>

## Snippet 2 — build-heap

```js
function buildHeap(arr) {           // sift-down each non-leaf, bottom-up
  const start = Math.floor(arr.length / 2) - 1;
  for (let i = start; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}
```

<Quiz
  id="08-heaps-code-2"
  lessonSlug="08-heaps"
  lang="en"
  question="Why does the loop start at Math.floor(n/2) - 1 and go downward, and what would break if it started at 0 and went upward instead?"
  choices={[
    { label: "Math.floor(n/2) - 1 is the last non-leaf; every index past it is a leaf, which is already a trivially valid heap and needs no sift-down. Going bottom-up guarantees that when you sift-down node i, both its subtrees are already valid heaps — the precondition sift-down relies on. Starting at 0 top-down would sift-down into subtrees that are not yet heapified, so the result may violate the heap property.", correct: true },
    { label: "Starting at n/2 - 1 is just an optimization; starting at n - 1 and going down would also be correct, only slower.", misconception: "Indices past floor(n/2)-1 are leaves with no children, so sift-down on them is a no-op — including them only wastes a constant factor, but it is harmless. The real reason for the bound is that processing must be bottom-up so subtrees are already valid; that is what going downward from the last non-leaf guarantees." },
    { label: "It must start at 0 and go up; the snippet as written is buggy.", misconception: "Bottom-up (from last non-leaf down to root) is the correct heapify. Top-up with sift-down breaks the precondition that a node's subtrees are already heaps, so it can leave the array un-heapified." },
    { label: "The bound n/2 - 1 is wrong; the last non-leaf is at n/2.", misconception: "For 0-indexed arrays the last node is n-1 and its parent is floor((n-1-1)/2) = floor(n/2)-1, which is the last node that has a child. So floor(n/2)-1 is correct." },
  ]}
/>

## Snippet 3 — top-k

```js
function topKLargest(nums, k) {
  const h = new MinHeap();              // min-heap, root is smallest
  for (const x of nums) {
    h.push(x);
    if (h.size() > k) h.pop();          // drop the current minimum
  }
  return h.toArray();                   // the k largest (heap order)
}
```

<Quiz
  id="08-heaps-code-3"
  lessonSlug="08-heaps"
  lang="en"
  question="For nums = [4, 1, 7, 3, 8, 5] and k = 3, what does the heap hold at the end, and what is the running time?"
  choices={[
    { label: "The heap holds {5, 7, 8} (in some heap order); its root 5 is the 3rd-largest. Each of the n elements does a push and at most one pop on a heap of size ≤ k+1, so the work is O(n log k); space is O(k).", correct: true },
    { label: "The heap holds {1, 3, 4} — the three smallest — because it is a min-heap.", misconception: "A min-heap is used precisely so the SMALLEST current candidate sits at the root and gets popped when the heap overflows past k. After all pops, the survivors are the k LARGEST: {5, 7, 8}." },
    { label: "It holds {4, 7, 8}; the first three inserted are kept.", misconception: "Insertion order does not decide survival. Each overflow pops the current minimum, so 4 (popped once 5,7,8 dominate) does not survive. The three largest, {5, 7, 8}, remain." },
    { label: "The running time is O(n log n) because every push is into a heap of size n.", misconception: "The heap never exceeds k+1 elements (it pops back to k on overflow), so each push/pop is O(log k), giving O(n log k) — better than O(n log n) when k ≪ n." },
  ]}
/>

## Snippet 4 — heapify complexity

```text
A heap built bottom-up calls sift-down once per non-leaf.
Node counts by height h (n total):  ~n/2 at h=0, n/4 at h=1, n/8 at h=2, ...
sift-down cost from a node of height h is O(h).
Total work  T(n) = Σ_h (n / 2^(h+1)) · h
```

<Quiz
  id="08-heaps-code-4"
  lessonSlug="08-heaps"
  lang="en"
  question="Evaluating the sum T(n) = Σ_h (n / 2^(h+1)) · h, what is build-heap's complexity, and why is the intuition 'n/2 sift-downs × O(log n) each = O(n log n)' wrong?"
  choices={[
    { label: "T(n) = O(n). The series Σ h/2^h converges to a constant (it sums to 2), so T(n) ≈ n · (constant) = O(n). The O(n log n) intuition over-counts: only the few nodes near the root have height ≈ log n; the bulk of nodes are near the leaves with height 0 or 1, so their sift-down is O(1).", correct: true },
    { label: "T(n) = O(n log n), because there are n/2 internal nodes and each sift-down is O(log n).", misconception: "That treats every internal node as height log n. But ~n/2 are leaves (height 0), ~n/4 height 1, and so on; the weighted sum Σ h/2^h converges, giving O(n), not O(n log n)." },
    { label: "T(n) = O(log n), because the heap has height log n.", misconception: "build-heap touches Θ(n) nodes, so it cannot be sub-linear. The total is O(n) — linear — not O(log n)." },
    { label: "T(n) = O(n²), because each of n nodes may sift down through n levels.", misconception: "A heap of n nodes has height ⌈log₂ n⌉, not n, so no node sifts more than O(log n) levels. The weighted sum over heights converges to O(n)." },
  ]}
/>

<Recap lang="en">
Heap correctness lives in details you read line by line: sift-up's comparison must match min vs max (child &lt; parent for a min-heap); build-heap must run bottom-up from the last non-leaf at floor(n/2)-1 so each node's subtrees are already heaps; top-k uses a size-k MIN-heap and pops the root on overflow, leaving the k largest in O(n log k); and heapify is O(n) because the height-weighted node sum Σ h/2^h converges — most nodes are cheap leaves. Trace the invariant before you trust the code.
</Recap>
