---
concepts:
- abstract_data_type
- priority_queue
- heap
- extract_min
- insert
- peek
- heap_implementation
- adt_vs_implementation
- application
estMin: 28
lang: en
mathPrereqs: []
order: 3
prereqs:
- 08-heaps/01-the-heap
- 08-heaps/02-heap-operations
slug: 03-priority-queues
sources:
- https://en.wikipedia.org/wiki/Priority_queue
- https://www.cs.princeton.edu/courses/archive/spring02/cs226/lectures/pq.4up.pdf
- https://charlesreid1.com/wiki/Priority_Queues/ADT
- https://lucasmagnum.medium.com/sidenotes-priority-queue-abstract-data-type-and-data-structure-52b5fcd7b904
status: ready
summary: 'Priority queue ADT: always extract the highest-priority element. Binary
  heap implementation: insert and extractMin O(log n), peek O(1). Uses: scheduling,
  Dijkstra, event merging. Key insight: ADT is the contract; heap is one way to keep
  it.'
title: Priority queues
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
You have a heap. Now you wrap it in a new name and a promise: "You never search for the max or min. You always get it in one operation—the most urgent, highest-priority, smallest-cost thing first." That promise is a <Term slug="priority_queue">priority queue</Term>—not a data structure (that is the heap underneath), but a <Term slug="abstract_data_type">contract</Term> that says what you can do. The heap is one way to keep that contract. Dijkstra's algorithm uses it. Schedulers use it. Any time you must always process the most important thing next, you reach for a priority queue.
</Hook>

<Goal lang="en">
After this lesson you can explain the difference between an abstract data type and an implementation. You can state the priority queue interface: insert a new element, extractMin (or extractMax) to remove and return the highest-priority element, and peek to see it without removing. You understand that a binary heap is the standard implementation and why (O(log n) insert and extract, O(1) peek). You can recognize when a problem calls for a priority queue instead of a regular queue or array, and name three real-world uses: task scheduling, Dijkstra's shortest path, and event simulation.
</Goal>

<Idea lang="en">
**What is a priority queue?**

A <Term slug="priority_queue">priority queue</Term> is an <Term slug="abstract_data_type">abstract data type</Term>—a contract, not a concrete data structure. It describes what you can do, not how it is built.

The contract says: you have a collection of elements, each with a priority (a number, or a comparison rule). You can:
1. **Insert** a new element.
2. **ExtractMin** (or **extractMax**): remove and return the element with the smallest (or largest) priority.
3. **Peek**: look at the highest-priority element without removing it.

That is the interface. The heap lives underneath.

**ADT vs. Implementation**

An <Term slug="abstract_data_type">abstract data type (ADT)</Term> is a specification—a contract. It says what operations are supported and what they do. It does NOT say how.

A priority queue is an ADT. Its operations are insert, extractMin, and peek.

An **implementation** is the code. A binary heap is one common implementation: it uses a complete binary tree stored in an array, with the min-heap property (every parent ≤ children).

The same ADT can have many implementations. A priority queue could be:
- A binary heap (fast insert and extract, memory efficient).
- A sorted array (fast peek, slow insert and extract).
- A linked list (slow everything).
- A balanced BST like a red-black tree (log n for all operations, but larger constants).

When you code, you use the ADT abstraction. You ask: insert, extractMin, peek. The implementation is pluggable—swap it out, the client code does not change.

**Why binary heap?**

The binary heap is the standard implementation because it is fast (O(log n) insert and extract, O(1) peek) and memory efficient (no pointers, just an array). It is simple enough to code by hand, yet battle-tested.

**Min-heap vs. Max-heap**

In a <Term slug="min_heap">min-heap</Term>, extractMin returns the smallest element. Used when you want to process the smallest cost, shortest time, or least urgent task first.

In a <Term slug="max_heap">max-heap</Term>, extractMax returns the largest element. Used when you want the highest priority, greatest weight, or most urgent task first.

The logic is symmetric—if you need a max-heap and only have a min-heap library, negate the priorities and use min-heap.

**The priority queue interface in pseudocode**

```
PriorityQueue:
  insert(element, priority)
    Add element to the queue.
  
  extractMin()
    Remove and return the element with the smallest priority.
  
  peek()
    Return (without removing) the smallest-priority element.
  
  isEmpty()
    True if the queue is empty.
  
  size()
    Number of elements in the queue.
```

**Example: task scheduling**

You have a task queue. Each task has a priority (1 = urgent, 10 = low priority). Tasks arrive at random times. You want to always work on the most urgent task next.

```
Queue: [print job (priority 3), compile (priority 5), test (priority 1)]

extractMin() → test with priority 1 (even though it was added last)
insert(deploy, priority 2)

Queue: [deploy (priority 2), print job (priority 3), compile (priority 5)]

extractMin() → deploy with priority 2
```

A regular queue (FIFO) would not work here—it would force you to do print job first, then compile, wasting time on low-priority work.

</Idea>

<Code lang="en">
**Priority queue interface in code**

<AnnotatedCode
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  // Insert element with a value (lower = higher priority)
  insert(element, value) {
    this.heap.push({ element, value });
    this.siftUp(this.heap.length - 1);
  }

  // Remove and return the element with the smallest value
  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root.element;
  }

  // Peek at the smallest-value element without removing
  peek() {
    return this.heap.length > 0 ? this.heap[0].element : null;
  }

  // Check if empty
  isEmpty() {
    return this.heap.length === 0;
  }

  // Number of elements
  size() {
    return this.heap.length;
  }

  // Sift-up to restore heap property after insertion
  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index].value < this.heap[parentIndex].value) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  // Sift-down to restore heap property after removal
  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left].value < this.heap[smallest].value) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right].value < this.heap[smallest].value) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}`}
  annotations={[
    { line: 1, text: "MinPriorityQueue wraps a heap array." },
    { line: 5, text: "Insert an element with a priority value. Lower value = higher priority." },
    { line: 9, text: "extractMin removes and returns the smallest-value element (the root of the min-heap)." },
    { line: 17, text: "peek returns the smallest-value element without removing it." },
    { line: 29, text: "siftUp restores the heap property after insertion, moving the element upward." },
    { line: 39, text: "siftDown restores the heap property after removal, moving the element downward." }
  ]}
/>

**Using the priority queue**

<CodeRunner
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(element, value) {
    this.heap.push({ element, value });
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root.element;
  }

  peek() {
    return this.heap.length > 0 ? this.heap[0].element : null;
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index].value < this.heap[parentIndex].value) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left].value < this.heap[smallest].value) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right].value < this.heap[smallest].value) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

// Task scheduling example
const pq = new MinPriorityQueue();

// Add tasks with priorities (lower number = higher priority)
pq.insert("compile", 5);
pq.insert("deploy", 2);
pq.insert("test", 3);
pq.insert("documentation", 7);

console.log("Processing tasks in priority order:");
while (!pq.isEmpty()) {
  const task = pq.extractMin();
  console.log("Executing:", task);
}

// Check what is next without removing
const pq2 = new MinPriorityQueue();
pq2.insert("urgent", 1);
pq2.insert("normal", 5);
console.log("Next task to process:", pq2.peek());`}
  expected={`Processing tasks in priority order:
Executing: deploy
Executing: test
Executing: compile
Executing: documentation
Next task to process: urgent`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="pq-insert-trace"
  lang="en"
  code={[
    "const pq = new MinPriorityQueue();",
    "pq.insert('task1', 5);",
    "pq.insert('task2', 2);",
    "pq.insert('task3', 1);"
  ]}
  frames={[
    { caption: "Insert 'task1' with priority 5. Heap: [(task1, 5)].", line: 2, cells: [{ value: "(task1,5)" }] },
    { caption: "Insert 'task2' with priority 2. Append: [(task1,5), (task2,2)]. Sift-up: 2 < 5, swap.", line: 3, cells: [{ value: "(task2,2)" }, { value: "(task1,5)" }] },
    { caption: "Insert 'task3' with priority 1. Append: [(task2,2), (task1,5), (task3,1)]. Sift-up from index 2.", line: 4, cells: [{ value: "(task2,2)" }, { value: "(task1,5)", mark: "active" }, { value: "(task3,1)" }] },
    { caption: "Index 2, parent at index 0 is (task2,2). 1 < 2, swap.", line: 4, cells: [{ value: "(task3,1)", mark: "active" }, { value: "(task1,5)" }, { value: "(task2,2)" }] },
    { caption: "Index 0 is the root. Heap property restored: (task3,1) ≤ both children. Done.", line: 4, cells: [{ value: "(task3,1)", mark: "done" }, { value: "(task1,5)" }, { value: "(task2,2)", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="pq-extract-trace"
  lang="en"
  code={[
    "const pq = new MinPriorityQueue();",
    "// After inserts: heap has [(task3,1), (task1,5), (task2,2)]",
    "const extracted = pq.extractMin();"
  ]}
  frames={[
    { caption: "Heap: [(task3,1), (task1,5), (task2,2)]. extractMin returns (task3,1).", line: 3, cells: [{ value: "(task3,1)", mark: "active" }, { value: "(task1,5)" }, { value: "(task2,2)" }] },
    { caption: "Move last element (task2,2) to root. Remove last. Heap: [(task2,2), (task1,5)].", line: 3, cells: [{ value: "(task2,2)", mark: "active" }, { value: "(task1,5)" }] },
    { caption: "Sift-down from index 0. Child at index 1 is (task1,5). 2 < 5, no swap needed.", line: 3, cells: [{ value: "(task2,2)", mark: "done" }, { value: "(task1,5)", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Priority queue operations using a binary heap**

<Term slug="insert">Insert</Term>: Appends a new element and calls sift-up. Sift-up moves the element upward one level per swap, and a heap with n elements has height O(log n).
- **Time: O(log n)**

<Term slug="extract_min">ExtractMin</Term>: Removes the root, moves the last element to the root, and calls sift-down. Sift-down also takes O(log n).
- **Time: O(log n)**

<Term slug="peek">Peek</Term>: Returns the root in one array access.
- **Time: O(1)**

<Term slug="isEmpty">IsEmpty</Term> and **size**: Check the array length.
- **Time: O(1)**

**Why not other implementations?**

A **sorted array** could give you O(1) peek, but insert and extract would require shifting O(n) elements—O(n) per operation.

An **unsorted array** gives you O(1) insert, but extractMin requires scanning all elements to find the smallest—O(n) per operation.

A **linked list** is O(n) for everything.

A **binary heap** balances all three: O(log n) insert and extract, O(1) peek. This is why it is the standard choice for priority queues.

**Space complexity**

The heap uses a single array, so space is O(n) for n elements.

</Complexity>

<AlgoPractice
  id="practice-priority-queues"
  lessonSlug="03-priority-queues"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In a priority queue based on a binary min-heap, which operation is guaranteed to be O(1)?",
      choices: [
        "insert",
        "extractMin",
        "peek",
        "decrease-key (lower a priority)"
      ],
      answer: 2,
      hint: "peek just looks at the root, which is always at index 0 in the array. One array access. insert and extractMin both require sift operations—O(log n)."
    },
    {
      kind: "choice",
      prompt: "You are building a task scheduler. Tasks have priorities (lower number = more urgent). You want to always execute the most urgent task next. What should you use?",
      choices: [
        "A regular queue (FIFO)",
        "A stack (LIFO)",
        "A priority queue with a min-heap",
        "A sorted list that you search linearly"
      ],
      answer: 2,
      hint: "A regular queue gives you FIFO, which ignores priority. A min-heap priority queue extracts the smallest priority (most urgent) every time—exactly what you need."
    },
    {
      kind: "choice",
      prompt: "What is the difference between an abstract data type (ADT) and an implementation?",
      choices: [
        "An ADT is code; an implementation is a design on paper.",
        "An ADT is a contract (what operations do); an implementation is how the code is built. One ADT can have many implementations.",
        "An ADT and implementation are the same thing.",
        "An ADT is faster than an implementation."
      ],
      answer: 1,
      hint: "An ADT says 'you can insert, extractMin, and peek.' A binary heap is one way to make that work. A sorted array or red-black tree are other ways—all satisfy the ADT contract."
    },
    {
      kind: "choice",
      prompt: "In Dijkstra's shortest path algorithm, you need to repeatedly extract the vertex with the smallest distance. Which data structure is this?",
      choices: [
        "A regular queue",
        "A stack",
        "A priority queue",
        "An unordered list"
      ],
      answer: 2,
      hint: "You always want the vertex with the minimum distance, not the first one added. That is the definition of a priority queue."
    },
    {
      kind: "numeric",
      prompt: "A min-heap priority queue has 8 elements. How many steps (comparisons and swaps) does extractMin take in the worst case?",
      answer: 3,
      hint: "A heap with 8 elements has height ⌊log₂(8)⌋ = 3 (levels 0, 1, 2). Sift-down traverses from the root down to a leaf — at most 3 levels — so at most 3 swaps in the worst case."
    },
    {
      kind: "choice",
      prompt: "You need to merge 4 sorted lists into one sorted list. Which approach uses a priority queue?",
      choices: [
        "Take the first element from each list and insert into a priority queue. Repeatedly extract the minimum.",
        "Concatenate all lists and sort—O(n log n).",
        "Use a regular queue to track elements from each list.",
        "Manually find the minimum element each time by searching all lists."
      ],
      answer: 0,
      hint: "A priority queue (min-heap) lets you always extract the globally smallest element among the 4 lists. This is the k-way merge algorithm, which runs in O(n log k) time where n is the total number of elements and k is the number of lists."
    }
  ]}
/>

<Inset type="why">
**Why priority queues matter.** Any real system with tasks, events, or requests has priorities. A web server has requests from users; some are more important (paying customers first). A robot has sensors and actuators; urgent safety events take priority. A hospital has patients in a waiting room; critical cases are seen first. Without a priority queue, you either process things randomly (unfair, inefficient) or manually search for the most important thing (slow, O(n)). A priority queue solves both problems: always give me the most important thing, fast.
</Inset>

<Inset type="mistake">
**Confusing the ADT and the implementation.** A priority queue is NOT a heap. A heap is one way to implement a priority queue. If someone asks "Is a priority queue a heap?", the answer is "No, a priority queue is an ADT; a heap is an implementation of that ADT." This distinction matters because in an interview you might say "I will use a priority queue backed by a heap" to make clear you are thinking about both levels.
</Inset>

<Inset type="edgecase">
**Decreasing a priority.** Some priority queue problems (like Dijkstra) need to lower an element's priority after insertion. A binary heap does not support this directly—you would need to rebuild or use a more complex implementation (Fibonacci heap, or re-insert with the new priority and skip the old one). For this lesson, we assume a simple priority queue that supports insert, extractMin, and peek only.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-priority-queues"
    pieceSlug="03-priority-queues"
    lang="en"
    question="Explain the relationship between a priority queue (the ADT) and a binary heap (the implementation). What operations must the ADT support, and why is the heap a good choice?"
    choices={[
      {
        label: "A priority queue is an abstract data type that specifies the interface: insert, extractMin (or extractMax), and peek. A binary heap is a concrete implementation that satisfies this interface by storing elements in a complete binary tree (array-based), maintaining the heap property. The heap is a good choice because it achieves O(log n) insert and extract, O(1) peek, and uses O(n) space with no pointers.",
        correct: true
      },
      {
        label: "A priority queue and a binary heap are the same thing. Both support insert and extractMin.",
        misconception: "A priority queue is an ADT (a contract); a heap is an implementation (concrete code). Other implementations exist: sorted arrays, red-black trees, etc."
      },
      {
        label: "A priority queue is only useful for small datasets because it requires O(log n) per operation.",
        misconception: "O(log n) is very fast. For n=1 million, log₂(n) ≈ 20. That is 20 comparisons per insert or extract—far better than O(n)."
      },
      {
        label: "A binary heap priority queue can be used anywhere you need a queue or stack because it supports any order.",
        misconception: "A priority queue always extracts by priority, not by insertion order (like a queue) or last-in-first-out (like a stack). It solves a different problem."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **priority queue** is an <Term slug="abstract_data_type">abstract data type</Term>—a contract that promises three operations: <Term slug="insert">insert</Term> a new element, <Term slug="extract_min">extractMin</Term> to remove and return the highest-priority element, and <Term slug="peek">peek</Term> to see the highest-priority element without removing. A **binary heap** is the standard <Term slug="heap">implementation</Term>, giving O(log n) insert and extract, O(1) peek. Understand the difference: the ADT is the interface; the implementation is the code. A priority queue is not a FIFO queue or a LIFO stack—it always gives you the most important thing next. Use a priority queue whenever you must always process the most urgent element: task scheduling, Dijkstra's shortest path, event simulation, merging sorted streams. **Next:** Unit 08, lesson 04 applies the priority queue to top-K problems, and lesson 05 shows k-way merge.
</Recap>
