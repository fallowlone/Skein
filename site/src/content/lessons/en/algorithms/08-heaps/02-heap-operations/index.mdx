---
concepts:
- heap
- sift_up
- sift_down
- push
- pop
- extract_min
- build_heap
- heapify
- heap_property
- time_complexity
estMin: 30
lang: en
mathPrereqs: []
order: 2
prereqs:
- 08-heaps/01-the-heap
slug: 02-heap-operations
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://www.geeksforgeeks.org/dsa/binary-heap/
- https://www.programiz.com/dsa/heap-data-structure
- https://brilliant.org/wiki/binary-heap/
status: ready
summary: 'Push, pop, and build-heap: how to mutate a heap while keeping the heap property.
  Sift-up and sift-down restore order. Build-heap turns any array into a heap in O(n)
  time—not O(n log n).'
title: Heap operations
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
You have a heap. Now you want to add a new element, or remove the root. When you do, the heap breaks—a parent becomes larger than its child, or the array no longer represents a complete tree. Two operations restore order: <Term slug="sift_up">sift-up</Term> (bubble a newcomer upward past its parent) and <Term slug="sift_down">sift-down</Term> (sink a misplaced element downward past its children). Together they keep the heap property alive. And there is a third miracle: you can turn any unordered array into a heap by sifting down every non-leaf node, in O(n) time—faster than inserting n elements one by one.
</Hook>

<Goal lang="en">
After this lesson you can insert an element into a heap with sift-up and explain why it takes O(log n) time. You can remove the root by swapping with the last element and sift-down, and explain the O(log n) cost. You can turn an arbitrary array into a valid heap by calling sift-down on every non-leaf node starting from the last one, in O(n) total time—not O(n log n). You can trace all three operations on a concrete heap array, step by step, verifying the heap property afterward.
</Goal>

<Idea lang="en">
**The two fundamental operations: sift-up and sift-down**

A <Term slug="heap">heap</Term> is only valid if the <Term slug="heap_property">heap property</Term> holds: every parent is ≤ its children (min-heap) or ≥ (max-heap). When you add or remove an element, this property breaks. Two operations restore it.

**Sift-up (bubble-up):** When a new element is added at the end of the heap array, it may be smaller (in a min-heap) than its parent. <Term slug="sift_up">Sift-up</Term> repeatedly compares the element with its parent and swaps upward until the heap property is restored or the root is reached.

**Sift-down (bubble-down):** When the root is removed and the last element is moved to the root, it may be larger than its children. <Term slug="sift_down">Sift-down</Term> repeatedly compares the element with its smaller (or larger) child and swaps downward until the heap property is restored or a leaf is reached.

**Why these work:** The heap property is only violated at one node at a time. Sift-up and sift-down fix exactly one parent-child pair per swap, and because the tree is balanced (height O(log n)), at most O(log n) swaps are needed.

**Push (insert)**

<Term slug="push">Push</Term> adds a new element to the heap.

1. Append the new value to the end of the heap array.
2. Call sift-up on the newly added element.
3. Sift-up stops when the new element is in the correct position (heap property restored) or reaches the root.

Example: Push 0 into the min-heap [1, 3, 2, 5, 4].
- Append: [1, 3, 2, 5, 4, 0]
- Sift-up from index 5 (value 0):
  - Parent at index (5-1)/2 = 2 (value 2).
  - 0 < 2, swap: [1, 3, 0, 5, 4, 2].
  - Now at index 2 (value 0), parent at index (2-1)/2 = 0 (value 1).
  - 0 < 1, swap: [0, 3, 1, 5, 4, 2].
  - Now at index 0 (root), stop.
- Result: [0, 3, 1, 5, 4, 2]. Heap property verified: 0 ≤ (3, 1), 3 ≤ (5, 4), 1 ≤ (2). ✓

**Pop (extract-min or extract-max)**

<Term slug="pop">Pop</Term> removes and returns the root (the min or max element).

1. Save the root value (to return it).
2. Move the last element in the array to the root position.
3. Remove the last element from the array (size decreases by 1).
4. Call sift-down on the root.
5. Sift-down stops when the heap property is restored or a leaf is reached.

Example: Pop from the min-heap [1, 3, 2, 5, 4].
- Save root 1 (to return).
- Move last element (4) to root: [4, 3, 2, 5].
- Sift-down from index 0 (value 4):
  - Children at indices 1 (value 3) and 2 (value 2). Smaller child is 2 at index 2.
  - 4 > 2, swap: [2, 3, 4, 5].
  - Now at index 2 (value 4), children at indices 5 and 6 (don't exist).
  - Stop.
- Result: [2, 3, 4, 5]. Heap property verified: 2 ≤ (3, 4), 3 ≤ (5). ✓ Returned value: 1.

**Build-heap (heapify)**

You can build a valid heap from an arbitrary array in O(n) time—faster than calling push n times (which would be O(n log n)).

<Term slug="build_heap">Build-heap</Term> (a.k.a. <Term slug="heapify">heapify</Term>) works by sifting down every non-leaf node, starting from the last non-leaf and working backward to the root.

The last non-leaf node is at index `⌊n/2⌋ - 1`. Why? Because a heap with n elements has height ⌈log₂(n+1)⌉, and the last non-leaf is the last node whose children exist.

For each non-leaf, call sift-down. Even though sift-down is O(log n) per node, build-heap is O(n) total because most nodes are leaves (which take O(1) time), and internal nodes near the bottom have short subtrees.

Example: Turn [5, 3, 8, 1, 4] into a min-heap.
- n = 5. Last non-leaf index = ⌊5/2⌋ - 1 = 1.
- Sift-down index 2 (value 8): No children (indices 5, 6 don't exist). No swap.
- Sift-down index 1 (value 3):
  - Children at indices 3 (value 1) and 4 (value 4). Smaller is 1 at index 3.
  - 3 > 1, swap: [5, 1, 8, 3, 4].
  - Now at index 3, no children. Stop.
- Sift-down index 0 (value 5):
  - Children at indices 1 (value 1) and 2 (value 8). Smaller is 1 at index 1.
  - 5 > 1, swap: [1, 5, 8, 3, 4].
  - Now at index 1, children at indices 3 (value 3) and 4 (value 4). Smaller is 3 at index 3.
  - 5 > 3, swap: [1, 3, 8, 5, 4].
  - Now at index 3, no children. Stop.
- Result: [1, 3, 8, 5, 4]. Heap property verified: 1 ≤ (3, 8), 3 ≤ (5, 4). ✓

</Idea>

<Code lang="en">
**Sift-up in code**

<AnnotatedCode
  code={`function siftUp(heap, index) {
  // Repeatedly swap with parent until heap property holds or root is reached
  while (index > 0) {
    const parentIndex = Math.floor((index - 1) / 2);
    // In a min-heap, child < parent is a violation; swap and continue
    if (heap[index] < heap[parentIndex]) {
      [heap[index], heap[parentIndex]] = [heap[parentIndex], heap[index]];
      index = parentIndex;
    } else {
      break; // Heap property restored
    }
  }
}

function push(heap, value) {
  heap.push(value);
  siftUp(heap, heap.length - 1);
}`}
  annotations={[
    { line: 1, text: "Sift-up restores the heap property after insertion." },
    { line: 3, text: "Continue while the node is not the root." },
    { line: 4, text: "Compute parent index using the standard formula." },
    { line: 6, text: "In a min-heap, if child < parent, the property is violated." },
    { line: 7, text: "Swap child with parent and move up." },
    { line: 10, text: "Push appends the value to the end and sifts it up." }
  ]}
/>

**Sift-down in code**

<AnnotatedCode
  code={`function siftDown(heap, index) {
  // Repeatedly swap with the smaller (or larger) child until the property holds
  while (true) {
    const leftChild = 2 * index + 1;
    const rightChild = 2 * index + 2;
    let smallest = index;
    
    // Check if left child exists and is smaller than the current smallest
    if (leftChild < heap.length && heap[leftChild] < heap[smallest]) {
      smallest = leftChild;
    }
    
    // Check if right child exists and is smaller
    if (rightChild < heap.length && heap[rightChild] < heap[smallest]) {
      smallest = rightChild;
    }
    
    // If a child is smaller, swap and continue; otherwise, stop
    if (smallest !== index) {
      [heap[index], heap[smallest]] = [heap[smallest], heap[index]];
      index = smallest;
    } else {
      break; // Heap property restored
    }
  }
}

function pop(heap) {
  const root = heap[0];
  heap[0] = heap[heap.length - 1];
  heap.pop();
  if (heap.length > 0) {
    siftDown(heap, 0);
  }
  return root;
}`}
  annotations={[
    { line: 1, text: "Sift-down restores the heap property after removing the root." },
    { line: 4, text: "Compute indices of the left and right children." },
    { line: 5, text: "Track the index of the smallest element (for min-heap)." },
    { line: 8, text: "If left child exists and is smaller, update smallest." },
    { line: 13, text: "Check right child in the same way." },
    { line: 18, text: "If smallest is not the current node, a swap is needed." },
    { line: 19, text: "Swap and continue sifting from the new position." },
    { line: 24, text: "Pop returns the root and restores the heap property." }
  ]}
/>

**Build-heap in code**

<AnnotatedCode
  code={`function buildHeap(arr) {
  // Start from the last non-leaf node and sift down each
  const lastNonLeaf = Math.floor(arr.length / 2) - 1;
  for (let i = lastNonLeaf; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}

// Example: turn an unordered array into a min-heap
const unordered = [5, 3, 8, 1, 4];
buildHeap(unordered);
console.log("Heap:", unordered); // [1, 3, 8, 5, 4]`}
  annotations={[
    { line: 1, text: "buildHeap transforms any array into a valid heap." },
    { line: 3, text: "Last non-leaf node is at index floor(n/2) - 1." },
    { line: 4, text: "Iterate from the last non-leaf down to the root (index 0)." },
    { line: 5, text: "Sift down each node to restore the heap property in its subtree." },
    { line: 11, text: "buildHeap is O(n) because most nodes are leaves (O(1) sift) and internal nodes have short subtrees." }
  ]}
/>

<CodeRunner
  code={`function siftDown(heap, index) {
  while (true) {
    const leftChild = 2 * index + 1;
    const rightChild = 2 * index + 2;
    let smallest = index;
    
    if (leftChild < heap.length && heap[leftChild] < heap[smallest]) {
      smallest = leftChild;
    }
    if (rightChild < heap.length && heap[rightChild] < heap[smallest]) {
      smallest = rightChild;
    }
    
    if (smallest !== index) {
      [heap[index], heap[smallest]] = [heap[smallest], heap[index]];
      index = smallest;
    } else {
      break;
    }
  }
}

function buildHeap(arr) {
  const lastNonLeaf = Math.floor(arr.length / 2) - 1;
  for (let i = lastNonLeaf; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}

// Test build-heap on an unordered array
const test = [5, 3, 8, 1, 4];
buildHeap(test);
console.log("Heap:", test.join(", "));

// Verify heap property: every parent <= children
let valid = true;
for (let i = 0; i < test.length; i++) {
  const left = 2 * i + 1;
  const right = 2 * i + 2;
  if (left < test.length && test[i] > test[left]) {
    console.log(\`Violation at index \${i}\`);
    valid = false;
  }
  if (right < test.length && test[i] > test[right]) {
    console.log(\`Violation at index \${i}\`);
    valid = false;
  }
}
console.log("Valid heap:", valid);`}
  expected={`Heap: 1, 3, 8, 5, 4
Valid heap: true`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="sift-up-trace"
  lang="en"
  code={[
    "const minHeap = [1, 3, 2, 5, 4];",
    "// Push 0",
    "minHeap.push(0);  // [1, 3, 2, 5, 4, 0]",
    "// Sift-up from index 5"
  ]}
  frames={[
    { caption: "Start with min-heap [1, 3, 2, 5, 4]. Push 0 to the end.", line: 3, cells: [{ value: "1" }, { value: "3" }, { value: "2" }, { value: "5" }, { value: "4" }, { value: "0", mark: "active" }] },
    { caption: "Index 5, parent at floor((5-1)/2) = 2 (value 2). 0 < 2, swap.", line: 4, cells: [{ value: "1" }, { value: "3" }, { value: "0", mark: "active" }, { value: "5" }, { value: "4" }, { value: "2" }] },
    { caption: "Index 2, parent at floor((2-1)/2) = 0 (value 1). 0 < 1, swap.", line: 4, cells: [{ value: "0", mark: "active" }, { value: "3" }, { value: "1" }, { value: "5" }, { value: "4" }, { value: "2" }] },
    { caption: "Index 0 is the root. Heap property restored: 0 ≤ (3, 1), 3 ≤ (5, 4), 1 ≤ (2). Done.", line: 4, cells: [{ value: "0", mark: "done" }, { value: "3" }, { value: "1", mark: "done" }, { value: "5" }, { value: "4" }, { value: "2", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="sift-down-trace"
  lang="en"
  code={[
    "const minHeap = [1, 3, 2, 5, 4];",
    "// Pop the root (1) and sift down"
  ]}
  frames={[
    { caption: "Min-heap [1, 3, 2, 5, 4]. Pop removes root 1.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "3" }, { value: "2" }, { value: "5" }, { value: "4" }] },
    { caption: "Move last element (4) to root, remove last. Array is [4, 3, 2, 5].", line: 2, cells: [{ value: "4", mark: "active" }, { value: "3" }, { value: "2" }, { value: "5" }] },
    { caption: "Index 0, children at 1 (value 3) and 2 (value 2). Smaller is 2. 4 > 2, swap.", line: 2, cells: [{ value: "2", mark: "active" }, { value: "3" }, { value: "4" }, { value: "5" }] },
    { caption: "Index 2, children at 5, 6 (don't exist). Heap property restored. Done.", line: 2, cells: [{ value: "2", mark: "done" }, { value: "3" }, { value: "4", mark: "done" }, { value: "5", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Sift-up and sift-down time complexity**

<Term slug="sift_up">Sift-up</Term> moves the element upward one level per swap. A min-heap with n elements has height ⌈log₂(n+1)⌉, so at most O(log n) swaps occur. Each swap is O(1) (two comparisons, one array access).
- **Time: O(log n)**

<Term slug="sift_down">Sift-down</Term> moves the element downward one level per swap, also O(log n) in the worst case.
- **Time: O(log n)**

**Push and pop**

<Term slug="push">Push</Term> appends (O(1)) and calls sift-up (O(log n)).
- **Time: O(log n)**

<Term slug="pop">Pop</Term> moves the last to the root (O(1)), removes the last (O(1)), and sifts down (O(log n)).
- **Time: O(log n)**

**Build-heap time complexity: why O(n) and not O(n log n)?**

A naive approach would call push n times: O(n log n). But <Term slug="build_heap">build-heap</Term> works differently. It calls sift-down on n/2 nodes. This seems like O(n * log n), but it is not.

The key insight: sift-down from a node at height h takes O(h) time (not O(log n) for all nodes). A heap has:
- 1 node at height ⌈log₂ n⌉ (the root).
- 2 nodes at height ⌈log₂ n⌉ - 1.
- 4 nodes at height ⌈log₂ n⌉ - 2.
- ...
- n/2 nodes at height 0 (the leaves).

Total work:
```
1*⌈log₂ n⌉ + 2*(⌈log₂ n⌉ - 1) + 4*(⌈log₂ n⌉ - 2) + ... + (n/2)*0
= Σ(2^h * (⌈log₂ n⌉ - h)) for h from 0 to ⌈log₂ n⌉
```

This sum equals n * (constant that depends on the logarithm), which converges to a constant < 2. Therefore, **time is O(n)**.

- **Build-heap: O(n)**

**Space complexity**

All three operations (push, pop, build-heap) work in place. The heap is stored in a single array.
- **Space: O(1)** (not counting the array itself)

</Complexity>

<AlgoPractice
  id="practice-heap-operations"
  lessonSlug="02-heap-operations"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "After pushing a new element into a min-heap, sift-up stops when the element is at a position where its value is greater than its parent's value. How many swaps might be needed in the worst case for a heap with 1000 elements?",
      choices: [
        "About 1000 swaps",
        "About 10 swaps (log₂ 1000 ≈ 10)",
        "Exactly 1 swap",
        "It depends on the CPU speed"
      ],
      answer: 1,
      hint: "The worst case is when the new element is the minimum and must bubble all the way to the root. The heap is a complete binary tree with height at most log₂(n). So about 10 swaps for n=1000."
    },
    {
      kind: "numeric",
      prompt: "You have a min-heap with 16 elements. What is the index of the last non-leaf node when you call build-heap?",
      answer: 7,
      hint: "Last non-leaf index = floor(n/2) - 1 = floor(16/2) - 1 = 8 - 1 = 7."
    },
    {
      kind: "choice",
      prompt: "Why is build-heap O(n) instead of O(n log n)?",
      choices: [
        "Because modern computers are fast",
        "Because most nodes are leaves (sift 0 levels); internal nodes near the bottom have short subtrees. The total work sums to O(n).",
        "Because the algorithm doesn't actually check all n nodes",
        "Because we only call sift-down once per node, not log n times"
      ],
      answer: 1,
      hint: "Half the nodes are leaves (0 work). A quarter are one level up (1 work each). An eighth are two levels up (2 work each). The sum converges."
    },
    {
      kind: "choice",
      prompt: "You pop the root from the min-heap [1, 4, 2, 7, 5]. The standard pop moves the last element to the root, then sifts down. What is the resulting heap array?",
      choices: ["[2, 4, 5, 7]", "[4, 5, 2, 7]", "[2, 4, 7, 5]", "[5, 4, 2, 7]"],
      answer: 0,
      hint: "Pop: swap root 1 with last element 5, remove the old root -> [5, 4, 2, 7]. Sift down 5 from index 0: its children are 4 (index 1) and 2 (index 2); the smaller is 2. Swap 5 and 2 -> [2, 4, 5, 7]. Node 5 is now at index 2 with no children. Stop. Result: [2, 4, 5, 7]."
    },
    {
      kind: "numeric",
      prompt: "After applying build-heap to [10, 5, 20, 15, 30], what is the minimum value at the root of the resulting min-heap?",
      answer: 5,
      hint: "Build-heap processes the array [10, 5, 20, 15, 30] and rearranges it so that the heap property holds. The minimum element should be at the root. The elements are 10, 5, 20, 15, 30; the minimum is 5."
    }
  ]}
/>

<Inset type="why">
**Why heap operations matter.** Push and pop are O(log n), which is much faster than adding to the end of a sorted list (O(n) because you must shift all larger elements). Build-heap in O(n) means you can take any dataset and prepare it for efficient priority queue operations. Dijkstra's algorithm, task scheduling, and median finding all rely on these operations.
</Inset>

<Inset type="mistake">
**Confusing sift directions.** Sift-up is for insertion (start at leaf, move toward root). Sift-down is for deletion (start at root, move toward leaves). They restore the property in different directions, so the logic is not symmetric. Check your code: sift-up goes upward (parent = (i-1)/2), sift-down goes downward (children = 2i+1, 2i+2).
</Inset>

<Inset type="edgecase">
**Single element heap.** Pushing into an empty heap or a heap with one element works fine: the new element is appended, and sift-up stops immediately because the parent condition is not violated. Popping from a heap with one element leaves an empty heap; sift-down should not be called if the heap is empty.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-heap-operations"
    pieceSlug="02-heap-operations"
    lang="en"
    question="Explain the difference between sift-up and sift-down, and when you use each one."
    choices={[
      {
        label: "Sift-up is called after pushing a new element at the end; it bubbles the element upward toward the root by comparing with the parent and swapping if the heap property is violated. Sift-down is called after removing the root; it bubbles an element downward by comparing with children. Sift-up restores the property by moving toward the root; sift-down by moving toward the leaves.",
        correct: true
      },
      {
        label: "Sift-up and sift-down are the same operation; the names are interchangeable. They both restore the heap property by swapping with a neighbor.",
        misconception: "Sift-up and sift-down are fundamentally different. Sift-up compares with the parent (upward); sift-down compares with children (downward). They move in opposite directions."
      },
      {
        label: "Sift-up is faster than sift-down because it has fewer children to check.",
        misconception: "Both are O(log n). Sift-up checks one parent per level; sift-down checks up to two children. The height is the same, so the time is the same."
      },
      {
        label: "You use sift-down for both push and pop, depending on the heap size.",
        misconception: "Push uses sift-up; pop uses sift-down. The operation depends on where the violation is: after insertion at the end (sift-up) or after moving the last element to the root (sift-down)."
      }
    ]}
  />
</Check>

<Recap lang="en">
To maintain a <Term slug="heap">heap</Term> while adding or removing elements, use two operations: **sift-up** (bubble a new element upward by comparing and swapping with its parent, O(log n)) and **sift-down** (sink a misplaced element downward by comparing and swapping with its smaller/larger child, O(log n)). **Push** appends a new value and sifts up; **pop** extracts the root, moves the last element to the root, and sifts down—both O(log n). The miracle operation is **build-heap** (a.k.a. **heapify**): sift down every non-leaf node starting from the last one, turning any array into a valid heap in O(n) total time. This is faster than pushing n elements one by one (O(n log n)) because most nodes are leaves (O(1) work) and internal nodes have shallow subtrees. The total work is the sum of node-count × depth, which converges to O(n). **Next:** Unit 08, lesson 03 uses heaps to build priority queues, the foundational data structure for Dijkstra's algorithm and task scheduling.
</Recap>
