---
slug: 05-k-way-merge
lang: en
track: algorithms
unit: 08-heaps
order: 5
title: "K-way merge and merging sorted lists"
summary: "Merge k sorted lists using a min-heap. Store each list's head in a min-heap, pop the smallest, append to output, push the next element. Time O(n log k), space O(k). Beats naive O(nk) approach."
estMin: 36
status: ready
prereqs: ["08-heaps/01-the-heap", "08-heaps/02-heap-operations", "08-heaps/03-priority-queues", "08-heaps/04-top-k"]
mathPrereqs: []
concepts: ["heap", "priority_queue", "merge", "k_way_merge", "sorted_lists", "streaming", "time_complexity", "space_complexity", "min_heap"]
sources:
  - https://en.wikipedia.org/wiki/K-way_merge_algorithm
  - https://emre.me/coding-patterns/k-way-merge/
  - https://leetcode.com/problems/merge-k-sorted-lists/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";
import Term from "../../../../../../components/prose/Term.astro";

<Hook>
You have k sorted lists. Your task: merge them into one sorted list. Naive answer: scan the head of all k lists, find the smallest, append it to the output, move forward. Repeat until all are exhausted. But scanning k heads every step is O(nk)—slow. The insight: maintain a <Term slug="min_heap">min-heap of size k</Term> holding the head of each list. Pop the smallest head, append it, push the next element from that same list. The heap keeps the k smallest candidates at the top, so each pop/push is O(log k). Total time: O(n log k). Space: O(k). This is the k-way merge pattern.
</Hook>

<Goal lang="en">
After this lesson you can solve the merge k sorted lists problem using a min-heap: initialize a heap with the head of each list (tracking which list each head came from), pop the smallest, append to the output, and push the next element from the same list. You understand why this works: the heap always holds one candidate from each of the k lists, and the root is always the overall minimum at that moment. You can explain the time complexity O(n log k) and space complexity O(k), and why this beats the naive O(nk) approach. You can recognize the "merge k sorted X" shape on LeetCode (linked lists, arrays, iterators), trace the algorithm step by step on concrete input, and code it. You understand the smallest-range and merge-sorted-arrays variants. This is your final heap lesson; next is graphs.
</Goal>

<Idea lang="en">
**The problem: merge k sorted lists**

You are given k sorted lists. Your task: merge them into one sorted output list, preserving order.

Example:
- Lists: [1, 4, 5], [1, 3, 4], [2, 6]
- Merged: [1, 1, 2, 3, 4, 4, 5, 6]

**Naive approach: scan all heads every step**

For each step:
1. Scan the head of all k lists to find the minimum.
2. Append the minimum to the output.
3. Move the pointer in that list forward.

Time: O(n) steps, and each step scans k heads in O(k). Total: O(nk). This is slow when k is large.

**The heap insight: maintain a size-k heap of heads**

Instead of scanning all k heads, use a <Term slug="min_heap">min-heap</Term> to track them. Here is the algorithm:

1. Create a min-heap.
2. For each of the k lists, push its head (the first element) into the heap. Store with each heap entry the list index and the current position in that list, so you know which list to pull from next.
3. While the heap is not empty:
   - Pop the minimum from the heap.
   - Append it to the output.
   - If that list has a next element, push it into the heap (same list index, next position).
4. The output is now merged and sorted.

Why does this work? At any step, the heap holds exactly one candidate from each of the k lists. The root is the overall minimum among these candidates. When you pop it and push the next element from the same list, the heap maintains the invariant: always the k smallest candidates visible so far, one per list. Each pop/push is O(log k), and you do n total pops, so O(n log k) overall.

**Example: merge three lists [1, 4, 5], [1, 3, 4], [2, 6]**

Min-heap holds (value, list_index, position):

1. Initialize: push (1, list 0, pos 0), (1, list 1, pos 0), (2, list 2, pos 0). Heap: min is (1, 0, 0).
2. Pop (1, 0, 0), append 1 to output. Push (4, 0, 1). Output: [1]. Heap min: (1, 1, 0).
3. Pop (1, 1, 0), append 1. Push (3, 1, 1). Output: [1, 1]. Heap min: (2, 2, 0).
4. Pop (2, 2, 0), append 2. Push (6, 2, 1). Output: [1, 1, 2]. Heap min: (3, 1, 1).
5. Pop (3, 1, 1), append 3. Push (4, 1, 2). Output: [1, 1, 2, 3]. Heap min: (4, 0, 1).
6. Pop (4, 0, 1), append 4. Push (5, 0, 2). Output: [1, 1, 2, 3, 4]. Heap min: (4, 1, 2).
7. Pop (4, 1, 2), append 4. List 1 exhausted. Output: [1, 1, 2, 3, 4, 4]. Heap min: (5, 0, 2).
8. Pop (5, 0, 2), append 5. List 0 exhausted. Output: [1, 1, 2, 3, 4, 4, 5]. Heap min: (6, 2, 1).
9. Pop (6, 2, 1), append 6. List 2 exhausted. Output: [1, 1, 2, 3, 4, 4, 5, 6]. Heap empty. Done.

Final: [1, 1, 2, 3, 4, 4, 5, 6]. ✓

**Why heap size stays at k**

At most, the heap holds one element from each of the k lists. When you pop one and push another (from the same list), the heap size does not grow. If a list is exhausted, you do not push, so the heap shrinks. The heap never exceeds k.

**Flagship shape: merge k sorted linked lists**

A common LeetCode problem: given k sorted linked lists, merge them. The algorithm is identical. Instead of arrays, you traverse pointers. Each heap entry stores the node, not just the value. You pop the min node, append it, and push its next node.

**Variant: smallest range**

Given k arrays, find the smallest range that includes at least one element from each array. Solution: use a similar heap to track the current element from each array. Maintain the minimum and maximum of the current k elements. The range [min, max] is a candidate answer. Pop the min and push the next from that array. Keep track of the smallest range seen. Time: O(n log k).

**Variant: merge sorted arrays (different sizes)**

k arrays of different sizes. Merge them in O(n log k) time using the same heap approach. n = total elements across all arrays.

</Idea>

<Code lang="en">
**Merging k sorted arrays using a min-heap**

<AnnotatedCode
  code={`function mergeKSortedLists(lists) {
  // Min-heap holds [value, listIndex, position]
  const minHeap = new MinPriorityQueue();
  const result = [];
  
  // Push the head of each list
  for (let i = 0; i < lists.length; i++) {
    if (lists[i].length > 0) {
      // Store: value, list index, position in that list
      minHeap.insert([lists[i][0], i, 0], lists[i][0]);
    }
  }
  
  // Pop min, append, push next from same list
  while (!minHeap.isEmpty()) {
    const [value, listIdx, pos] = minHeap.extractMin();
    result.push(value);
    
    // If there is a next element in this list, push it
    if (pos + 1 < lists[listIdx].length) {
      const nextValue = lists[listIdx][pos + 1];
      minHeap.insert([nextValue, listIdx, pos + 1], nextValue);
    }
  }
  
  return result;
}

// Example
const lists = [[1, 4, 5], [1, 3, 4], [2, 6]];
const merged = mergeKSortedLists(lists);
console.log(merged); // [1, 1, 2, 3, 4, 4, 5, 6]`}
  annotations={[
    { line: 2, text: "Min-heap where the key is the value itself. Each entry is [value, listIndex, position]." },
    { line: 7, text: "For each of k lists, insert the first element (if the list is non-empty)." },
    { line: 8, text: "Store the value, the list it came from, and its position in that list." },
    { line: 15, text: "While heap is not empty, pop the minimum value (always at root)." },
    { line: 19, text: "Append to result." },
    { line: 22, text: "If the same list has a next element, push it. If not, that list is exhausted." },
    { line: 28, text: "The output is now merged and sorted." }
  ]}
/>

**Merge k sorted linked lists**

<CodeRunner
  code={`class Node {
  constructor(val, next = null) {
    this.val = val;
    this.next = next;
  }
}

class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(item, priority) {
    this.heap.push(item);
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root;
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIdx = Math.floor((index - 1) / 2);
      if (this.heap[index][0] < this.heap[parentIdx][0]) {
        [this.heap[index], this.heap[parentIdx]] = [this.heap[parentIdx], this.heap[index]];
        index = parentIdx;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left][0] < this.heap[smallest][0]) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right][0] < this.heap[smallest][0]) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

function mergeKLinkedLists(lists) {
  const minHeap = new MinPriorityQueue();
  
  // Insert the head of each list
  for (let i = 0; i < lists.length; i++) {
    if (lists[i]) {
      // Store: [node, listIndex] with priority = node.val
      minHeap.insert([lists[i], i]);
    }
  }
  
  const dummyHead = new Node(-1);
  let current = dummyHead;
  
  while (!minHeap.isEmpty()) {
    const [node, listIdx] = minHeap.extractMin();
    current.next = node;
    current = current.next;
    
    // If this node has a next, push it
    if (node.next) {
      minHeap.insert([node.next, listIdx]);
    }
  }
  
  return dummyHead.next;
}

// Example: create three linked lists
const list1 = new Node(1, new Node(4, new Node(5)));
const list2 = new Node(1, new Node(3, new Node(4)));
const list3 = new Node(2, new Node(6));

const merged = mergeKLinkedLists([list1, list2, list3]);

// Convert to array for display
const result = [];
let curr = merged;
while (curr) {
  result.push(curr.val);
  curr = curr.next;
}
console.log(result);`}
  expected={`[1, 1, 2, 3, 4, 4, 5, 6]`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="kway-merge-trace"
  lang="en"
  code={[
    "const lists = [[1, 4, 5], [1, 3, 4], [2, 6]];",
    "const minHeap = new MinPriorityQueue();",
    "const result = [];",
    "// Init: push heads of all lists",
    "for (const [val, idx] of [[1,0], [1,1], [2,2]]) minHeap.insert(val, idx);",
    "while (!minHeap.isEmpty()) {",
    "  const [val, idx] = minHeap.extractMin();",
    "  result.push(val);",
    "  if (hasNext(lists[idx])) minHeap.insert(nextVal(lists[idx]), idx);",
    "}"
  ]}
  frames={[
    { caption: "Init: heap = [(1,L0), (1,L1), (2,L2)]. Heap min: (1, L0).", line: 4, cells: [{ value: "1", mark: "active" }, { value: "1" }, { value: "2" }] },
    { caption: "Pop (1, L0), append 1. Push (4, L0). Heap: [(1,L1), (2,L2), (4,L0)]. Min: (1, L1).", line: 7, cells: [{ value: "1", mark: "done" }, { value: "1" }, { value: "2" }] },
    { caption: "Pop (1, L1), append 1. Push (3, L1). Heap: [(2,L2), (4,L0), (3,L1)]. Min: (2, L2).", line: 7, cells: [{ value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2" }] },
    { caption: "Pop (2, L2), append 2. Push (6, L2). Heap: [(3,L1), (4,L0), (6,L2)]. Min: (3, L1).", line: 7, cells: [{ value: "2", mark: "active" }, { value: "2", mark: "active" }, { value: "2" }] },
    { caption: "Pop (3, L1), append 3. Push (4, L1). Heap: [(4,L0), (4,L1), (6,L2)]. Min: (4, L0).", line: 7, cells: [{ value: "3", mark: "active" }, { value: "3", mark: "active" }, { value: "6" }] },
    { caption: "Pop (4, L0), append 4. Push (5, L0). Heap: [(4,L1), (6,L2), (5,L0)]. Min: (4, L1).", line: 7, cells: [{ value: "4", mark: "active" }, { value: "4", mark: "active" }, { value: "6" }] },
    { caption: "Pop (4, L1), append 4. L1 exhausted. Heap: [(5,L0), (6,L2)]. Min: (5, L0).", line: 7, cells: [{ value: "4", mark: "done" }, { value: "4", mark: "done" }, { value: "6" }] },
    { caption: "Pop (5, L0), append 5. L0 exhausted. Heap: [(6,L2)]. Min: (6, L2).", line: 7, cells: [{ value: "5", mark: "done" }, { value: "5", mark: "done" }, { value: "6" }] },
    { caption: "Pop (6, L2), append 6. L2 exhausted. Heap empty. Result: [1,1,2,3,4,4,5,6].", line: 7, cells: [{ value: "6", mark: "done" }, { value: "6", mark: "done" }, { value: "6", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**K-way merge with a min-heap of size k**

You have n total elements across all k lists.

For each of the n elements:
- <Term slug="extract_min">ExtractMin</Term>: O(log k) (the heap holds at most k elements).
- <Term slug="insert">Insert</Term> (the next element from the same list): O(log k).

Total: n elements × (extract + insert) = n × O(log k) = **O(n log k) time**.

Space: The heap holds at most k elements (one head from each list), so **O(k) space**.

**Comparison with naive approach**

Naive: for each of n elements, scan all k heads to find the minimum. Time: O(nk). Space: O(1) (if you do not count the input).

K-way merge with heap: O(n log k) time, O(k) space.

When is the heap approach better?
- Always. O(n log k) is strictly better than O(nk) because log k ≤ n for reasonable k (and usually log k ≪ n).
- Example: n = 1,000,000 elements, k = 100 lists. O(n log k) = 1,000,000 × 6.6 ≈ 6.6 million operations. O(nk) = 1,000,000 × 100 = 100 million operations. Heap is ~15 times faster.

**Why not other approaches?**

- Pairwise merge (merge two lists, then merge result with the third, etc.): O(n log k) but with larger constants. Heap is simpler.
- Sort all elements globally: O(n log n) time, O(n) space. Worse than O(n log k) when k ≪ n.

</Complexity>

<AlgoPractice
  id="practice-kway-merge"
  lessonSlug="05-k-way-merge"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "To merge k sorted lists, what is the main purpose of storing the list index and position in each heap entry?",
      choices: [
        "To sort the lists by size.",
        "To know which list to pull the next element from after you pop the current minimum.",
        "To balance the heap so all lists contribute equally.",
        "To detect when a list is exhausted without scanning."
      ],
      answer: 1,
      hint: "After you pop the minimum and append it, you must know which list it came from so you can push the next element from that same list. The list index and position tell you exactly that."
    },
    {
      kind: "choice",
      prompt: "In the k-way merge algorithm, what does the heap invariant guarantee at each step?",
      choices: [
        "The smallest element overall is stored somewhere in the input lists.",
        "The root of the heap is the smallest among the current heads of the k lists.",
        "All k lists have been partially consumed in equal amounts.",
        "The output array is sorted after every insertion."
      ],
      answer: 1,
      hint: "The heap holds one candidate from each of the k lists (its current head). The root of the min-heap is the smallest among these k candidates—the overall minimum at that moment."
    },
    {
      kind: "numeric",
      prompt: "You are merging k = 5 sorted lists with n = 1,000,000 total elements. What is the approximate time complexity in operations, using log₂(5) ≈ 2.3?",
      answer: 2300000,
      hint: "O(n log k) = 1,000,000 × 2.3 ≈ 2,300,000 operations."
    },
    {
      kind: "choice",
      prompt: "What is the space complexity of the k-way merge algorithm?",
      choices: [
        "O(n), because you store the output.",
        "O(k), because the heap holds at most one element from each list.",
        "O(1), because you only use pointers.",
        "O(n log k), same as time complexity."
      ],
      answer: 1,
      hint: "The heap holds at most k elements (the current head of each list). If you count only auxiliary space (not the input or output), it is O(k). If you count the output, it is O(n)."
    },
    {
      kind: "choice",
      prompt: "Given three lists [1, 5, 9], [2, 6, 10], [3, 7, 11], what is the first element popped from the min-heap?",
      choices: [
        "1",
        "2",
        "3",
        "It depends on the heap order."
      ],
      answer: 0,
      hint: "Initialize: push (1, L0), (2, L1), (3, L2). The min-heap's root is 1, which is the minimum among the current heads."
    },
    {
      kind: "choice",
      prompt: "After popping (1, L0) and pushing (5, L0) in the previous example, what is the next minimum popped?",
      choices: [
        "1",
        "2",
        "3",
        "5"
      ],
      answer: 1,
      hint: "After pushing (5, L0), the heap holds [(2, L1), (3, L2), (5, L0)]. The root (minimum) is (2, L1)."
    }
  ]}
/>

<Inset type="why">
**Why k-way merge matters.** Real systems often need to combine sorted data from multiple sources: merging k log files sorted by timestamp, merging results from k database shards sorted by ID, merging k sorted iterators in a data pipeline. You cannot afford to store all data in memory, so you must stream through one element at a time. A k-way heap lets you merge online in O(n log k) time and O(k) space, processing each element exactly once.
</Inset>

<Inset type="mistake">
**Storing the entire remaining list instead of just the head.** It is tempting to store the entire tail of the list in the heap. That wastes space and makes comparisons slow. Instead, store only the node/value itself and its list index/position. When you pop it, you compute or fetch the next element on demand. The heap never holds more than k elements.
</Inset>

<Inset type="edgecase">
**Empty lists.** If some input lists are empty, do not push them into the heap. The algorithm handles k-m non-empty lists correctly: the heap size grows to the number of non-empty lists, and the complexity is O(n log (k-m)). If all lists are empty, the output is empty. If k = 1, there is only one list, so just return it.
</Inset>

<Inset type="practice">
**Smallest-range variant.** Given k arrays, find the smallest range [a, b] such that each array contributes at least one element. Maintain the current min and max among the k heads. The range [min, max] is a candidate answer. Pop the min, push the next from that array, update the range. Time: O(n log k). Space: O(k).
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-kway-merge"
    pieceSlug="05-k-way-merge"
    lang="en"
    question="Explain the k-way merge algorithm and its time complexity. Why is it O(n log k) and not O(nk)?"
    choices={[
      {
        label: "Use a min-heap of size k to hold one head from each list. Pop the minimum head, append it to the output, and push the next element from the same list. The heap always contains one candidate from each list; its root is the overall minimum. Time: n elements × O(log k) per pop/push = O(n log k). The naive approach scans all k heads for each element: O(nk). The heap avoids the scan by using the heap invariant to find the min in O(log k).",
        correct: true
      },
      {
        label: "Merge the lists pairwise (merge 1 and 2, merge 3 and 4, etc.) until one list remains. This is O(n log k) by recursion.",
        misconception: "Pairwise merge is O(n log k), but the question asks why the heap approach is O(n log k) and not O(nk). The heap approach is direct and cleaner than pairwise merging."
      },
      {
        label: "Sort all n elements globally. Time: O(n log n). This is less efficient than O(n log k), but still correct.",
        misconception: "Sorting all n elements is O(n log n), which is worse than O(n log k) when k ≪ n. The heap approach is better because it only maintains a heap of size k, not n."
      },
      {
        label: "The complexity is actually O(nk) because you must push and pop k elements for each of the n steps.",
        misconception: "You pop once per step (after which the heap size may shrink), and you push at most once per step (only if the list has a next element). So it is O(n × log k), not O(n × k). The heap size is k, not the cost per operation."
      }
    ]}
  />
</Check>

<Recap lang="en">
To merge k sorted lists, maintain a <Term slug="min_heap">min-heap of size k</Term> holding one head from each list. Pop the minimum head, append it to output, push the next element from the same list. The heap invariant ensures the root is always the minimum among the k current heads. Time: **O(n log k)**. Space: **O(k)**. This is far better than the naive O(nk) approach (scanning all k heads per step). The same pattern solves merge k sorted linked lists, smallest-range, and merge k sorted arrays. **Next:** unit 09 (graphs), where you will explore nodes and edges, BFS, DFS, and shortest paths.
</Recap>
