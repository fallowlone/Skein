---
concepts:
- heap
- complete_binary_tree
- heap_property
- min_heap
- max_heap
- array_representation
- index_arithmetic
- binary_tree
estMin: 24
lang: en
mathPrereqs: []
order: 1
prereqs:
- 07-trees/01-the-binary-tree
- 07-trees/02-traversals
slug: 01-the-heap
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://www.geeksforgeeks.org/dsa/binary-heap/
- https://www.programiz.com/dsa/heap-data-structure
- https://brilliant.org/wiki/binary-heap/
status: ready
summary: 'A complete binary tree where every parent is ≤ (or ≥) its children—enabling
  O(1) root access. Stored as a flat array with no pointers: for node at index i,
  parent is at ⌊(i−1)∕2⌋, left child at 2i+1, right child at 2i+2. Structure, not
  operations.'
title: The heap
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You know trees have a shape. Now imagine a tree shaped so compactly that you can store it in an array with no pointers at all. And imagine a tree designed so that the root is always the smallest (or largest) element. No searching, no comparisons—it is right there at index 0. This is a heap. It combines two ideas: a specific shape (complete binary tree) and a specific ordering rule (heap property). Together, they enable O(1) access to the min or max, and the clever array encoding means no wasted space.
</Hook>

<Goal lang="en">
After this lesson you can explain what a complete binary tree is, state the heap property for min-heaps and max-heaps, use index arithmetic to find parent and child indices in an array-based heap, build a small heap by hand and verify the heap property holds, visualize the relationship between the tree structure and the flat array, predict where the root, min, and max live in a heap array, and recognize a heap when you see one.
</Goal>

<Idea lang="en">
**What is a heap?**

A <Term slug="heap">heap</Term> is a binary tree that satisfies two properties:

1. **Shape property (complete binary tree):** Every level is completely filled except possibly the last level, which is filled from left to right.
2. **Heap property:** Every parent is less than or equal to its children (in a <Term slug="min_heap">min-heap</Term>) or greater than or equal to its children (in a <Term slug="max_heap">max-heap</Term>).

Together, these two rules deliver the core guarantee: the root is always the extreme element. Without the shape rule the array trick breaks; without the ordering rule you lose O(1) access to the min or max.

**What is a complete binary tree?**

A <Term slug="complete_binary_tree">complete binary tree</Term> is a binary tree where:
- Every level except possibly the last is completely full.
- If the last level is not full, its nodes are filled from left to right (no gaps).

Example: A complete tree with 7 nodes:
```
      1
     / \
    2   3
   / \ / \
  4  5 6  7
```

All levels 0–2 are full. This is complete. If we had a tree like:
```
      1
     / \
    2   3
   /     \
  4       5
```
This is NOT complete because node 3 has a right child (5) but node 2 does not have a right child.

**The heap property**

In a <Term slug="min_heap">min-heap</Term>, every parent is **less than or equal to** its children. This means the smallest element is always at the root.

In a <Term slug="max_heap">max-heap</Term>, every parent is **greater than or equal to** its children. This means the largest element is always at the root.

Example min-heap:
```
      1 (smallest)
     / \
    3   2
   / \
  5   4
```
Each parent ≤ its children: 1 ≤ (3, 2), 3 ≤ (5, 4), 2 ≤ (nothing). Min is always at the root.

**The array representation**

Because a heap is a complete binary tree, it can be stored in a flat array with **no pointers**. We simply lay the nodes out in breadth-first order (level by level, left to right). The root is at index 0.

For a node at index `i`:
- **Parent index** = `Math.floor((i - 1) / 2)`
- **Left child index** = `2 * i + 1`
- **Right child index** = `2 * i + 2`

Example: The min-heap above as an array:
```
Index:  0  1  2  3  4
Value: [1, 3, 2, 5, 4]
```

Verify the relationships:
- Index 0 (value 1) has children at indices 1 (value 3) and 2 (value 2). Parent = floor((1-1)/2) = 0. Parent = floor((2-1)/2) = 0. ✓
- Index 1 (value 3) has children at indices 3 (value 5) and 4 (value 4). Parent = floor((3-1)/2) = 1. Parent = floor((4-1)/2) = 1. ✓
- Index 2 (value 2) has no children (indices 5 and 6 don't exist). Parent = floor((2-1)/2) = 0. ✓

**Why the formulas work**

In breadth-first order, if you number nodes 0, 1, 2, ..., the structure of a complete binary tree means:
- Node 0 is the root.
- Node 1 and 2 are the children of node 0.
- Nodes 3 and 4 are the children of node 1.
- Nodes 5 and 6 are the children of node 2.
- And so on.

The pattern is that the children of node `i` are at indices `2*i + 1` and `2*i + 2`. Working backward, the parent of node `j` is at index `floor((j - 1) / 2)`. When you see these formulas in code, they are not magic—they are breadth-first numbering with one line of algebra.

</Idea>

<DiagramFrame
  label="The min-heap [1, 3, 2, 5, 4] shown two ways side by side. Left: a complete binary tree with root 1, its children 3 (left) and 2 (right), and node 3's children 5 (left) and 4 (right). Each node carries a small badge with its array index: 1 is index 0, 3 is index 1, 2 is index 2, 5 is index 3, 4 is index 4. Every parent is less than or equal to both of its children: 1 ≤ 3 and 1 ≤ 2; 3 ≤ 5 and 3 ≤ 4. Right: the same heap as a flat array of five cells, index 0 to 4 holding 1, 3, 2, 5, 4 in breadth-first order. Arrows from the tree edges map each parent index i to its children at 2i+1 and 2i+2."
  hue="var(--d-systems)"
  caption="One heap, two views. Left: the complete binary tree — smallest value at the root, every parent ≤ both children. Right: the same nodes laid out breadth-first in an array. The badge on each tree node is its array index, so node at index i finds its children at 2i+1 and 2i+2 and its parent at floor((i−1)/2) — no pointers, just arithmetic."
>
  <svg viewBox="0 0 520 320" width="520" height="320">
    <text x="20" y="24" font-size="12" font-family="var(--font-mono)" fill="var(--muted)">{"complete binary tree"}</text>
    <text x="330" y="24" font-size="12" font-family="var(--font-mono)" fill="var(--muted)">{"array backing"}</text>
    <line x1="130" y1="62" x2="80" y2="116" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="130" y1="62" x2="180" y2="116" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="80" y1="142" x2="45" y2="196" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="80" y1="142" x2="115" y2="196" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <circle cx="130" cy="56" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="130" y="62" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <circle cx="80" cy="136" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="80" y="142" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <circle cx="180" cy="136" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="180" y="142" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <circle cx="45" cy="216" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="45" y="222" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <circle cx="115" cy="216" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="115" y="222" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <circle cx="152" cy="38" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="152" y="42" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">0</text>
    <circle cx="102" cy="118" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="102" y="122" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">1</text>
    <circle cx="202" cy="118" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="202" y="122" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">2</text>
    <circle cx="23" cy="198" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="23" y="202" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">3</text>
    <circle cx="137" cy="198" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="137" y="202" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">4</text>
    <text x="20" y="270" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"badge ● = array index"}</text>
    <text x="20" y="288" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"parent ≤ children: 1 ≤ 3, 1 ≤ 2, 3 ≤ 5, 3 ≤ 4"}</text>
    <rect x="330" y="64" width="44" height="44" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="374" y="64" width="44" height="44" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="418" y="64" width="44" height="44" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="462" y="64" width="44" height="44" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="330" y="108" width="44" height="44" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <text x="352" y="92" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="396" y="92" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="440" y="92" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="484" y="92" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="352" y="136" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="352" y="124" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">0</text>
    <text x="396" y="124" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">1</text>
    <text x="440" y="124" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">2</text>
    <text x="484" y="124" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">3</text>
    <text x="352" y="168" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">4</text>
    <text x="330" y="206" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"index i (in accent)"}</text>
    <text x="330" y="252" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"left child  of i = 2i + 1"}</text>
    <text x="330" y="270" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"right child of i = 2i + 2"}</text>
    <text x="330" y="288" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"parent of i = floor((i − 1) / 2)"}</text>
  </svg>
</DiagramFrame>

<Code lang="en">
**Building a heap from scratch**

Let's build a min-heap by hand and store it in an array:

<AnnotatedCode
  code={`// Min-heap stored as an array
const minHeap = [1, 3, 2, 5, 4];

// Helper functions for index arithmetic
function parentIndex(i) {
  return Math.floor((i - 1) / 2);
}

function leftChildIndex(i) {
  return 2 * i + 1;
}

function rightChildIndex(i) {
  return 2 * i + 2;
}

// Access the root (the minimum in a min-heap)
console.log("Root (min):", minHeap[0]);  // 1

// Access children of index 1 (value 3)
console.log("Value at index 1:", minHeap[1]);  // 3
console.log("Left child of index 1:", minHeap[leftChildIndex(1)]);  // 5
console.log("Right child of index 1:", minHeap[rightChildIndex(1)]);  // 4

// Access parent of index 3 (value 5)
console.log("Value at index 3:", minHeap[3]);  // 5
console.log("Parent of index 3:", minHeap[parentIndex(3)]);  // 3`}
  annotations={[
    { line: 2, text: "The heap is just an array. Index 0 is the root." },
    { line: 5, text: "Parent index formula: floor((i - 1) / 2)." },
    { line: 8, text: "Left child index formula: 2*i + 1." },
    { line: 11, text: "Right child index formula: 2*i + 2." },
    { line: 15, text: "The root is always at index 0." },
    { line: 18, text: "Access any node's children using the index formulas." },
    { line: 23, text: "Navigate from a child back to its parent." }
  ]}
/>

**A larger heap**

Build a max-heap with 10 nodes. The maximum is always at the root.

<AnnotatedCode
  code={`// Max-heap stored as an array
const maxHeap = [50, 30, 20, 10, 15, 8, 5, 2, 3, 7];

function parentIndex(i) {
  return Math.floor((i - 1) / 2);
}

function leftChildIndex(i) {
  return 2 * i + 1;
}

function rightChildIndex(i) {
  return 2 * i + 2;
}

// Verify the heap property: parent >= children
for (let i = 0; i < maxHeap.length; i++) {
  const left = leftChildIndex(i);
  const right = rightChildIndex(i);
  
  if (left < maxHeap.length) {
    console.log(\`Parent \${maxHeap[i]} >= left child \${maxHeap[left]}: \${maxHeap[i] >= maxHeap[left]}\`);
  }
  if (right < maxHeap.length) {
    console.log(\`Parent \${maxHeap[i]} >= right child \${maxHeap[right]}: \${maxHeap[i] >= maxHeap[right]}\`);
  }
}`}
  annotations={[
    { line: 2, text: "A max-heap: every parent >= its children." },
    { line: 16, text: "Loop through every node." },
    { line: 17, text: "Compute indices of left and right children." },
    { line: 20, text: "If the left child exists, check the heap property." },
    { line: 23, text: "If the right child exists, verify the parent is >= the child." }
  ]}
/>

<CodeRunner
  code={`const maxHeap = [50, 30, 20, 10, 15, 8, 5, 2, 3, 7];

function parentIndex(i) {
  return Math.floor((i - 1) / 2);
}

function leftChildIndex(i) {
  return 2 * i + 1;
}

function rightChildIndex(i) {
  return 2 * i + 2;
}

// Verify heap property: every parent >= children
let isValidMaxHeap = true;
for (let i = 0; i < maxHeap.length; i++) {
  const left = leftChildIndex(i);
  const right = rightChildIndex(i);
  
  if (left < maxHeap.length && maxHeap[i] < maxHeap[left]) {
    console.log(\`Violation: \${maxHeap[i]} < \${maxHeap[left]} at indices \${i}, \${left}\`);
    isValidMaxHeap = false;
  }
  if (right < maxHeap.length && maxHeap[i] < maxHeap[right]) {
    console.log(\`Violation: \${maxHeap[i]} < \${maxHeap[right]} at indices \${i}, \${right}\`);
    isValidMaxHeap = false;
  }
}

console.log("Is valid max-heap:", isValidMaxHeap);
console.log("Root (max element):", maxHeap[0]);`}
  expected={`Is valid max-heap: true
Root (max element): 50`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="heap-array-navigation"
  lang="en"
  code={[
    "const minHeap = [1, 3, 2, 5, 4];",
    "// Navigate from root to node at index 3",
    "let current = 0;  // Start at root",
    "let target = 3;   // Index 3 has value 5",
    "// Find parent: floor((3-1)/2) = 1"
  ]}
  frames={[
    { caption: "Start with the min-heap array. Index 0 is the root (value 1).", line: 1, cells: ["1", "3", "2", "5", "4"] },
    { caption: "Navigate to index 3 (value 5).", line: 4, cells: [{ value: "1" }, { value: "3" }, { value: "2" }, { value: "5", mark: "active" }, { value: "4" }] },
    { caption: "Find parent of index 3: floor((3-1)/2) = floor(2/2) = 1.", line: 5, cells: [{ value: "1" }, { value: "3", mark: "active" }, { value: "2" }, { value: "5" }, { value: "4" }] },
    { caption: "Parent of 5 is 3 (at index 1). Verify: 3 <= 5. ✓ Heap property holds.", line: 5, cells: [{ value: "1" }, { value: "3", mark: "done" }, { value: "2" }, { value: "5", mark: "done" }, { value: "4" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

In a heap array:
- **Access root (min/max):** O(1). It is always at index 0.
- **Index arithmetic (parent/child lookup):** O(1). A single division or multiplication.
- **Access any node by index:** O(1). Direct array lookup.

**Space complexity**

- **Storing the heap:** O(n), where n is the number of nodes. The array uses O(n) space, and no pointers are needed.

Note: The operations of adding or removing elements from a heap (push, pop, sift) are NOT covered in this lesson; those are the focus of the next lesson (08/02). Here, we cover only the structure and the O(1) index arithmetic.

</Complexity>

<AlgoPractice
  id="practice-heap-1"
  lessonSlug="01-the-heap"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In a complete binary tree with 7 nodes, how many nodes must be in level 0 and level 1 (the root and its children)?",
      choices: [
        "1 and 1",
        "1 and 2",
        "2 and 4",
        "1 and 0"
      ],
      answer: 1,
      hint: "Level 0 has the root (1 node). Level 1 has the children of the root. In a binary tree, a node can have at most 2 children, so level 1 has at most 2 nodes. For a complete tree, these should be filled."
    },
    {
      kind: "numeric",
      prompt: "In an array-based heap with the array [10, 20, 30, 40, 50], what is the parent index of the node at index 4?",
      answer: 1,
      hint: "Use the formula: parent index = floor((i - 1) / 2). For i=4: floor((4-1)/2) = floor(3/2) = floor(1.5) = 1."
    },
    {
      kind: "numeric",
      prompt: "In an array-based heap, what is the left child index of the node at index 2?",
      answer: 5,
      hint: "Use the formula: left child index = 2*i + 1. For i=2: 2*2 + 1 = 4 + 1 = 5."
    },
    {
      kind: "choice",
      prompt: "Which of these arrays is NOT a valid min-heap?",
      choices: [
        "[1, 2, 3, 4, 5, 6, 7]",
        "[5, 10, 8, 20, 30, 15, 12]",
        "[1, 3, 2, 5, 4, 8, 6]",
        "[10, 5, 20, 15, 30, 25, 35]"
      ],
      answer: 3,
      hint: "A min-heap satisfies: every parent <= its children. In [10, 5, 20, 15, 30, 25, 35], the root (10) is greater than its left child (5), violating the min-heap property. The other three arrays all satisfy the heap condition at every parent-child pair."
    },
    {
      kind: "choice",
      prompt: "Why can a complete binary tree be stored in an array with no pointers?",
      choices: [
        "Because it uses less memory than any other structure",
        "Because the left-to-right filling of the last level guarantees no gaps; we can compute parent/child indices using arithmetic on the array index alone",
        "Because binary trees are always stored as arrays",
        "Because pointers are not allowed in JavaScript"
      ],
      answer: 1,
      hint: "The key insight is that a complete binary tree has a predictable, gap-free structure. In breadth-first order, we can always compute where a parent or child is using simple arithmetic. No need to store pointers."
    }
  ]}
/>

<Inset type="why">
**Why the heap structure matters.** Many real systems need to repeatedly extract the minimum (or maximum) element—task scheduling, Dijkstra's algorithm, median finding. A heap is the data structure that does this efficiently. No searching, no re-sorting. Just O(1) to peek, and O(log n) to add or remove (operations we cover next lesson). The complete binary tree shape ensures the tree is always balanced, and the array encoding saves space and improves cache locality.
</Inset>

<Inset type="mistake">
**Confusing complete with full.** A **full binary tree** is one where every node has 0 or 2 children (no node has just 1 child). A **complete binary tree** has all levels filled except possibly the last, filled left to right. A heap is complete, not necessarily full.
</Inset>

<Inset type="edgecase">
**Heap with 1 node.** A single-node heap is valid: it is a complete binary tree (just the root) and trivially satisfies the heap property (the root has no children to violate it). The root is both the min and the max.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-heap-1"
    pieceSlug="01-the-heap"
    lang="en"
    question="Explain why a heap can be stored in an array with no pointers, and how you would find the parent of a node at index i."
    choices={[
      {
        label: "A heap is a complete binary tree, which has no gaps. When stored in breadth-first order in an array, the position of any node (and its parent/children) can be computed using arithmetic on its index. Parent of i is at floor((i-1)/2). No pointers needed.",
        correct: true
      },
      {
        label: "Arrays are faster than pointers, so heaps always use arrays instead.",
        misconception: "The reason is about the structure of a complete binary tree, not about speed. The predictable structure allows index arithmetic to replace pointers."
      },
      {
        label: "Pointers are not allowed in the heap data structure.",
        misconception: "Pointers are allowed; it is just that the structure allows us to avoid them and use arithmetic instead."
      },
      {
        label: "The heap property requires an array representation.",
        misconception: "The heap property is about ordering (parent <= or >= children). It works with any tree structure. The array representation is an optimization that works well because the heap uses a complete binary tree shape."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **heap** is a binary tree with two properties: it is a **complete binary tree** (all levels full except possibly the last, filled left to right), and it satisfies the **heap property** (every parent ≤ children in a min-heap, or ≥ in a max-heap). The root is always the minimum (min-heap) or maximum (max-heap). Because of its complete shape, a heap can be stored in an array with no pointers: for a node at index `i`, the parent is at `⌊(i−1)∕2⌋`, the left child at `2i+1`, and the right child at `2i+2`. This encoding saves space and enables O(1) access to the root. The structure ensures the tree is always balanced. **Next:** Unit 08, lesson 02 covers the operations—push, pop, and sift—that maintain the heap property when data changes. Now when you see an array and someone calls it a "heap," you know exactly which two rules to check: is it complete, and does every parent dominate its children?
</Recap>
