---
concepts:
- interval-scheduling
- sort-key
- greedy-vs-dp
- jump-game
- gas-station
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://leetcode.com/problems/jump-game/
- https://leetcode.com/problems/gas-station/
status: ready
summary: "Read real greedy snippets: an interval scheduler with the wrong sort key, a greedy that loses to DP, and a Gas Station scan. Predict the behaviour and pick the highest-leverage fix."
title: 'Greedy: code reading'
track: algorithms
unit: 11-greedy
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Greedy bugs are quiet: the code runs, returns something plausible, and is wrong only on the inputs you did not test. Read each snippet the way you would in review — find the choice that is not provably safe, then pick the fix a senior engineer makes first.
</Hook>

<Goal lang="en">
Practise the loop you run on every greedy: read the local choice, decide whether it is provably optimal, and reach for the right sort key, the right invariant, or DP — before trusting the output.
</Goal>

## Snippet 1 — the interval scheduler

```js
// Goal: pick the MAXIMUM number of non-overlapping intervals.
function maxNonOverlapping(intervals) {
  const sorted = [...intervals].sort((a, b) => a[0] - b[0]); // sort by START
  let count = 0, lastEnd = -Infinity;
  for (const [start, end] of sorted) {
    if (start >= lastEnd) { count++; lastEnd = end; }
  }
  return count;
}
// maxNonOverlapping([[1, 10], [2, 3], [4, 5]])  ->  ?
```

<Quiz
  id="11-greedy-code-1"
  lessonSlug="11-greedy"
  lang="en"
  question="What does this return on [[1,10], [2,3], [4,5]], and what is the one-line fix?"
  choices={[
    { label: "It returns 1. Sorted by start, [1,10] is taken first and its finish (10) blocks both [2,3] and [4,5], even though those two are compatible (optimum is 2). The fix is to sort by FINISH time: sort((a, b) => a[1] - b[1]).", correct: true },
    { label: "It returns 2, which is optimal — sorting by start is correct for this problem.", misconception: "Sort-by-start takes the long [1,10] first and is then blocked from both short intervals, returning 1, not 2. Sort-by-start is the key for MERGING intervals, not maximization." },
    { label: "It returns 3 because all three intervals are eventually counted.", misconception: "[1,10] overlaps both [2,3] and [4,5], so they can never all be chosen. With the start-sort it picks only [1,10], returning 1." },
    { label: "The count is right but it is O(n^2); the fix is to remove the sort.", misconception: "Removing the sort makes it wrong, not faster — the greedy depends on order. The defect is the KEY (start vs finish), and the algorithm is already O(n log n)." },
  ]}
/>

## Snippet 2 — a greedy that loses to DP

```js
// "Fewest coins for `amount`" by always taking the largest coin that fits.
function greedyCoins(coins, amount) {
  const sorted = [...coins].sort((a, b) => b - a);
  let left = amount, count = 0;
  for (const c of sorted) {
    while (left >= c) { left -= c; count++; }
  }
  return left === 0 ? count : Infinity;
}
// greedyCoins([1, 5, 6, 9], 11)  ->  ?   (DP optimum is 2: 5 + 6)
```

<Quiz
  id="11-greedy-code-2"
  lessonSlug="11-greedy"
  lang="en"
  question="greedyCoins([1,5,6,9], 11) returns 3 (9+1+1) but the DP optimum is 2 (5+6). Which statement correctly diagnoses this?"
  choices={[
    { label: "The denomination set {1,5,6,9} lacks the greedy-choice property: grabbing the largest coin (9) is not part of any optimal solution for 11, and no exchange can repair it. The correct algorithm here is the O(amount x coins) DP, which is optimal for any denominations.", correct: true },
    { label: "It is a sorting bug; sorting coins ascending instead of descending fixes it.", misconception: "Ascending order would only make greedy take many 1s — even worse. The failure is intrinsic to the denomination set, not the sort direction. DP is the fix." },
    { label: "Greedy is right and DP is wrong; 9+1+1 = 3 coins is genuinely optimal for 11.", misconception: "5 + 6 = 11 uses exactly 2 coins, beating greedy's 3. DP minimizes exhaustively and is correct; greedy returns the worse answer." },
    { label: "The bug is the `left === 0` guard; removing it makes greedy optimal.", misconception: "The guard only reports infeasibility; it has nothing to do with optimality. Even with a clean guard, greedy still returns 3 for this denomination set. Only DP guarantees the minimum." },
  ]}
/>

## Snippet 3 — the Gas Station scan

```js
function canCompleteCircuit(gas, cost) {
  let total = 0, tank = 0, start = 0;
  for (let i = 0; i < gas.length; i++) {
    const diff = gas[i] - cost[i];
    total += diff;
    tank += diff;
    if (tank < 0) { start = i + 1; tank = 0; }
  }
  return total >= 0 ? start : -1;
}
```

<Quiz
  id="11-greedy-code-3"
  lessonSlug="11-greedy"
  lang="en"
  question="A reviewer wants to delete the `total` variable and return `start` directly, arguing the reset already finds the answer. Why must `total` stay?"
  choices={[
    { label: "`total` is the feasibility test: a valid trip exists iff the net fuel over the whole circle is >= 0. Without it the function returns a `start` index even when no trip is possible (total < 0), where the correct answer is -1. The local reset finds WHERE to start; the global sum decides WHETHER any start works.", correct: true },
    { label: "`total` is just a debug counter; the function is correct without it.", misconception: "Drop `total` and the function can return a bogus start on infeasible inputs (e.g. total net fuel negative), where it must return -1. `total` is the load-bearing feasibility check, not debug output." },
    { label: "`total` is needed to reset the tank; without it the tank never zeroes.", misconception: "The tank is reset by the `tank = 0` line inside the if, independent of `total`. `total` plays no role in resets — its only job is the final feasibility verdict." },
    { label: "`total` should replace `tank`; they are the same accumulator.", misconception: "They are different: `tank` is net fuel since the current candidate start (decides resets), `total` is net fuel over the whole loop (decides feasibility). Collapsing them breaks both roles." },
  ]}
/>

## Snippet 4 — Jump Game II loop bound

```js
function jump(nums) {
  let jumps = 0, curEnd = 0, farthest = 0;
  for (let i = 0; i < nums.length; i++) {   // loops to n-1
    farthest = Math.max(farthest, i + nums[i]);
    if (i === curEnd) { jumps++; curEnd = farthest; }
  }
  return jumps;
}
// jump([2, 3, 1, 1, 4])  ->  ?   (true minimum is 2)
```

<Quiz
  id="11-greedy-code-4"
  lessonSlug="11-greedy"
  lang="en"
  question="This returns 3 on [2,3,1,1,4] but the minimum is 2. What is the off-by-one bug, and the fix?"
  choices={[
    { label: "The loop runs to n-1, so when i reaches the goal index (which equals curEnd at the end) it spends one extra jump to leave a square it never leaves. The fix is to loop to n-2 (i < nums.length - 1): you arrive AT the last index, you never jump FROM it.", correct: true },
    { label: "The bug is starting jumps at 0; it should start at 1.", misconception: "Starting at 1 would over-count from the first jump and break the [0]-length-1 case (which needs 0 jumps). The real bug is the loop bound — it must stop at n-2." },
    { label: "Math.max should be Math.min; the frontier should shrink.", misconception: "The frontier (farthest reachable) only ever grows — Math.max is correct. Using min would collapse it and report unreachable. The defect is the loop bound." },
    { label: "There is no bug; 3 jumps is correct for [2,3,1,1,4].", misconception: "0 -> 1 -> 4 reaches the end in 2 jumps. Looping to n-1 spends a spurious third jump on the goal index itself; stopping at n-2 fixes it." },
  ]}
/>

<Recap lang="en">
Every greedy is read the same way: name the local choice, then ask whether it is provably safe. Snippet 1 used the wrong sort key (start instead of finish) for maximization; snippet 2 used greedy on a denomination set without the greedy-choice property, where only DP is correct; snippet 3 showed the global feasibility invariant (`total >= 0`) is load-bearing, separate from the local reset; snippet 4 was the classic Jump Game II off-by-one (loop to n-2, never jump from the goal). Diagnose the unsafe choice, then fix the key, the invariant, or fall back to DP.
</Recap>
