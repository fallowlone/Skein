---
concepts:
- interval_scheduling
- activity_selection
- earliest_finish_time
- merge_intervals
- meeting_rooms
- greedy_choice
- sweep_line
- counterexample
- o_n_log_n
- sort_dominates
estMin: 28
lang: en
mathPrereqs: []
order: 3
prereqs:
- 02-the-exchange-argument
slug: 03-interval-scheduling
sources:
- https://en.wikipedia.org/wiki/Interval_scheduling
- https://leetcode.com/problems/merge-intervals/
- https://www.geeksforgeeks.org/dsa/activity-selection-problem-greedy-algo-1/
status: ready
summary: Two interval greedies. To pick the most non-overlapping intervals, sort by
  earliest finish time and take each that starts after the last taken finishes. To
  merge overlaps, sort by start and sweep. Both are O(n log n), dominated by the sort;
  shortest-duration is a wrong greedy.
title: 'Interval scheduling: activity selection and merging intervals'
track: algorithms
unit: 11-greedy
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You have one conference room and a stack of meeting requests, each with a start and end time. Many of them clash. You cannot fit them all, so you want to fit the *most* you can. Your gut says: grab the meeting that starts earliest, then the next one that fits, and so on. That gut feeling is wrong, and it is wrong in a way that costs you meetings. The right move is almost the opposite—care about when each meeting *ends*, not when it starts. One sort, one pass, provably optimal. This lesson teaches that greedy, its close cousin "merge the overlapping intervals," and the room-counting variant—three problems that show up constantly and all live or die on what you sort by.
</Hook>

<Goal lang="en">
After this lesson you can solve the three classic interval problems. (1) **Activity selection / interval scheduling maximization**: pick the largest set of non-overlapping intervals by sorting on <Term slug="earliest_finish_time">earliest finish time</Term> and greedily taking each interval whose start is at or after the last taken interval's finish. You can explain *why* finish time is the right key and give a concrete counterexample showing that sorting by start time or by shortest duration fails. (2) **Merge intervals**: collapse a set of intervals into the minimal set of disjoint ones by sorting on start and sweeping, extending the current run while the next interval overlaps it. (3) **Minimum rooms ("meeting rooms II")**: find the peak number of simultaneously active intervals by sorting the start times and end times separately and sweeping a two-pointer counter. You can state that all three are **O(n log n)**, dominated by the sort, with O(n) or O(1) extra work in the sweep. You connect this back to the exchange argument from lesson 02: earliest-finish is provably optimal because any optimal solution can be rewritten to start with the earliest-finishing interval without losing count.
</Goal>

<Idea lang="en">
**Problem 1: pick the most non-overlapping intervals**

You are given intervals like `[start, end]`. Two intervals *overlap* if one starts before the other ends. You want the **largest set with no overlaps**. (Think: the most meetings you can fit in one room, the most jobs one machine can run.)

The winning greedy is one line of intuition: **sort by finish time, then walk left to right taking any interval that starts at or after the last one you took ended.**

```
sorted by finish:  [1,4]  [3,5]  [2,6]  [5,7]  [6,8]
                    ====
take [1,4]          ----  lastEnd = 4
[3,5] starts 3 < 4        skip (overlaps)
[2,6] starts 2 < 4        skip (overlaps)
[5,7] starts 5 >= 4       take, lastEnd = 7
[6,8] starts 6 < 7        skip (overlaps)
result: { [1,4], [5,7] }  -> 2 intervals
```

Why finish time? **The interval that finishes earliest leaves the most room behind it for everything else.** Taking it can never hurt you: whatever optimal solution exists, you can swap its first interval for the earliest-finishing one without creating any new overlap and without losing count. That is exactly the <Term slug="exchange_argument">exchange argument</Term> from lesson 02—the earliest-finish choice is "safe," so greedily repeating it is optimal.

**The tempting wrong greedies**

- **Sort by start time.** A meeting that starts at 9:00 and runs all day would get grabbed first and block everything. Earliest start says nothing about how much room is left.
- **Sort by shortest duration.** This *feels* clever—small meetings should pack tightly—but a single short interval can sit right in the middle and block two longer ones that did not overlap each other. Counterexample: `[0,4]`, `[3,5]`, `[4,8]`. The shortest is `[3,5]` (length 2). Take it and both `[0,4]` and `[4,8]` now clash with it, so you get **1**. But `[0,4]` and `[4,8]` do not overlap each other, so the real answer is **2**. Earliest-finish takes `[0,4]` (finishes at 4), then `[4,8]` (starts at 4) and gets **2**. Shortest-duration loses.

The lesson: with intervals, the *right thing to sort by is rarely the obvious thing*. Finish time wins for maximization.

**Problem 2: merge overlapping intervals**

Before looking at the algorithm, notice why you would need it: any time you receive a list of time ranges from multiple sources—log windows, calendar events, database locks—they may overlap, and you often need the simplest unified picture of what is covered. **Collapse them into the fewest disjoint intervals** that cover the same ground. `[1,3]`, `[2,6]`, `[8,10]`, `[15,18]` becomes `[1,6]`, `[8,10]`, `[15,18]`.

Here you **sort by start time** and sweep:

```
sorted by start: [1,3] [2,6] [8,10] [15,18]
current run = [1,3]
[2,6]: 2 <= 3 (overlaps)  -> extend run to [1,6]
[8,10]: 8 > 6 (gap)       -> close [1,6], open new run [8,10]
[15,18]: 15 > 10 (gap)    -> close [8,10], open new run [15,18]
result: [1,6] [8,10] [15,18]
```

Sorting by start guarantees that once you pass an interval, nothing later can reach back behind the current run's start, so you only ever compare against the *one* run you are currently building. That is what makes a single left-to-right pass correct.

**Problem 3: minimum number of rooms ("meeting rooms II")**

Now you have unlimited time but want the **fewest rooms** so no two meetings share a room. The answer is the **maximum number of meetings active at the same instant**—the peak overlap.

Sort all the start times and all the end times into two separate lists, then sweep both with a two-pointer counter: each time the next event is a *start*, a meeting begins, so add a room and track the peak; each time the next event is an *end*, a meeting finished, so free a room.

```
starts: 0  5  15        ends: 10  20  30
start 0  -> rooms 1 (peak 1)
start 5  -> rooms 2 (peak 2)      [0,30] and [5,10] both active
end 10   -> rooms 1
start 15 -> rooms 2 (peak 2)
...
answer = peak = 2
```

All three problems are the same shape: **sort the intervals on the right key, then make one sweep.** The sort is the expensive part.
</Idea>

<DiagramFrame
  label="Activity selection by earliest finish time. Five intervals are drawn as horizontal bars on a shared timeline running from 0 to 9, stacked top to bottom in order of finish time: [1,4], [3,5], [2,6], [5,7], [6,8]. The greedy walks this finish-sorted order and always takes the next interval that is compatible with the last one taken. It takes [1,4] first because it finishes earliest at time 4; a dashed vertical gate is drawn at time 4 and every interval that starts before 4 is rejected, so [3,5] (starts 3) and [2,6] (starts 2) are greyed out as overlapping. The next compatible interval is [5,7], which starts at 5, at or after the gate at 4, so it is taken and a new gate is drawn at its finish time 7. The last interval [6,8] starts at 6, before 7, so it is rejected. The two chosen bars [1,4] and [5,7] are highlighted; the three overlapping bars are greyed. The result is 2 non-overlapping intervals, the maximum possible."
  hue="var(--d-systems)"
  caption="Sort by finish time, then always take the earliest-finishing interval that is still compatible. Take [1,4] (finishes first at 4); the gate at 4 rejects every interval that starts before it — [3,5] and [2,6] overlap and are greyed. Next compatible is [5,7] (starts 5 >= 4); take it and move the gate to 7, which rejects [6,8] (starts 6 < 7). Chosen: [1,4] and [5,7] — 2 intervals, the maximum."
>
  <svg viewBox="0 0 700 332" width="700" height="332" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <text x="350" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, currentColor)">intervals sorted by finish time, walked top to bottom</text>

    <line x1="316" y1="60" x2="316" y2="300" stroke="var(--d-systems)" stroke-width="2" stroke-dasharray="5 4" />
    <text x="316" y="54" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">gate: finish = 4</text>
    <line x1="508" y1="190" x2="508" y2="300" stroke="var(--d-systems)" stroke-width="2" stroke-dasharray="5 4" />
    <text x="540" y="184" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">gate: finish = 7</text>

    <rect x="124" y="70" width="192" height="30" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="220" y="90" text-anchor="middle" font-size="14" font-weight="700" fill="var(--ink, currentColor)">[1,4]</text>
    <text x="640" y="90" text-anchor="middle" font-size="12" font-weight="700" fill="var(--d-systems)">TAKE (finishes first)</text>

    <rect x="252" y="114" width="128" height="30" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" stroke-dasharray="4 3" />
    <text x="316" y="134" text-anchor="middle" font-size="14" font-weight="700" fill="var(--muted, #888)">[3,5]</text>
    <text x="640" y="134" text-anchor="middle" font-size="12" font-weight="600" fill="var(--muted, #888)">{"skip: start 3 < 4"}</text>

    <rect x="188" y="158" width="256" height="30" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" stroke-dasharray="4 3" />
    <text x="316" y="178" text-anchor="middle" font-size="14" font-weight="700" fill="var(--muted, #888)">[2,6]</text>
    <text x="640" y="178" text-anchor="middle" font-size="12" font-weight="600" fill="var(--muted, #888)">{"skip: start 2 < 4"}</text>

    <rect x="380" y="202" width="128" height="30" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="444" y="222" text-anchor="middle" font-size="14" font-weight="700" fill="var(--ink, currentColor)">[5,7]</text>
    <text x="640" y="222" text-anchor="middle" font-size="12" font-weight="700" fill="var(--d-systems)">{"TAKE: start 5 >= 4"}</text>

    <rect x="444" y="246" width="128" height="30" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" stroke-dasharray="4 3" />
    <text x="508" y="266" text-anchor="middle" font-size="14" font-weight="700" fill="var(--muted, #888)">[6,8]</text>
    <text x="640" y="266" text-anchor="middle" font-size="12" font-weight="600" fill="var(--muted, #888)">{"skip: start 6 < 7"}</text>

    <line x1="60" y1="300" x2="636" y2="300" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <line x1="60" y1="296" x2="60" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="60" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">0</text>
    <line x1="124" y1="296" x2="124" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="124" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">1</text>
    <line x1="188" y1="296" x2="188" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="188" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">2</text>
    <line x1="252" y1="296" x2="252" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="252" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">3</text>
    <line x1="316" y1="296" x2="316" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="316" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">4</text>
    <line x1="380" y1="296" x2="380" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="380" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">5</text>
    <line x1="444" y1="296" x2="444" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="444" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">6</text>
    <line x1="508" y1="296" x2="508" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="508" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">7</text>
    <line x1="572" y1="296" x2="572" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="572" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">8</text>
    <line x1="636" y1="296" x2="636" y2="304" stroke="var(--ink, currentColor)" stroke-width="1.5" />
    <text x="636" y="320" text-anchor="middle" font-size="11" fill="var(--muted, #888)">9</text>
  </svg>
</DiagramFrame>

<Code lang="en">
**Activity selection: sort by earliest finish, then take what fits**

The state to carry is one number: `lastEnd`, the finish time of the most recent interval you took. An interval is takeable exactly when its start is at or after `lastEnd`.

<AnnotatedCode
  code={`function maxActivities(intervals) {
  // Sort by earliest FINISH time - the safe greedy key
  const sorted = [...intervals].sort((a, b) => a[1] - b[1]);
  let count = 0;
  let lastEnd = -Infinity;          // nothing taken yet
  const chosen = [];
  for (const [start, end] of sorted) {
    if (start >= lastEnd) {         // starts after the last taken one finishes
      count++;
      lastEnd = end;               // this interval is now the latest taken
      chosen.push([start, end]);
    }
  }
  return { count, chosen };
}`}
  annotations={[
    { line: 3, text: "Sort by index 1 (the end time). This single line is what makes the whole greedy optimal." },
    { line: 5, text: "lastEnd starts at -Infinity so the very first interval always passes the test." },
    { line: 8, text: "The greedy test: an interval fits if it starts at or after the last finish. '>=' allows back-to-back touching intervals like [1,4] then [4,8]." },
    { line: 10, text: "Commit to this interval: bump the count and advance lastEnd to its finish." },
    { line: 11, text: "Record it (optional - drop 'chosen' if you only need the count)." }
  ]}
/>

**Running it**

Eleven activities; the greedy finds four that never overlap.

<CodeRunner
  id="cr-activity-select"
  lang="en"
  code={`function maxActivities(intervals) {
  const sorted = [...intervals].sort((a, b) => a[1] - b[1]);  // earliest finish first
  let count = 0;
  let lastEnd = -Infinity;
  const chosen = [];
  for (const [start, end] of sorted) {
    if (start >= lastEnd) {        // starts after the last chosen one finishes
      count++;
      lastEnd = end;
      chosen.push([start, end]);
    }
  }
  return { count, chosen };
}

const acts = [[1, 4], [3, 5], [0, 6], [5, 7], [3, 8], [5, 9], [6, 10], [8, 11], [8, 12], [2, 13], [12, 14]];
const r = maxActivities(acts);
console.log("max non-overlapping:", r.count);
console.log("chosen:", JSON.stringify(r.chosen));`}
  expected={`max non-overlapping: 4
chosen: [[1,4],[5,7],[8,11],[12,14]]`}
/>

**The shortest-duration greedy is wrong — proof by counterexample**

Run the wrong strategy against the right one on the trap input `[0,4]`, `[3,5]`, `[4,8]`. The shortest interval `[3,5]` sits in the middle and blocks both others.

<CodeRunner
  id="cr-wrong-greedy"
  lang="en"
  code={`function maxActivities(intervals) {
  const sorted = [...intervals].sort((a, b) => a[1] - b[1]);
  let count = 0, lastEnd = -Infinity;
  for (const [start, end] of sorted) {
    if (start >= lastEnd) { count++; lastEnd = end; }
  }
  return count;
}

// Wrong greedy: take the SHORTEST interval first, skip anything it clashes with.
function shortestFirst(intervals) {
  const sorted = [...intervals].sort((a, b) => (a[1] - a[0]) - (b[1] - b[0]));
  const taken = [];
  for (const iv of sorted) {
    const clash = taken.some(([s, e]) => iv[0] < e && s < iv[1]);
    if (!clash) taken.push(iv);
  }
  return taken.length;
}

const tricky = [[0, 4], [3, 5], [4, 8]];
console.log("earliest-finish picks:", maxActivities(tricky));
console.log("shortest-duration picks:", shortestFirst(tricky));`}
  expected={`earliest-finish picks: 2
shortest-duration picks: 1`}
/>

Two against one. The shortest interval looked harmless and cost a whole slot. Finish time is the only key that is provably safe.

**Merge intervals: sort by start, sweep, extend the current run**

<CodeRunner
  id="cr-merge-intervals"
  lang="en"
  code={`function merge(intervals) {
  const sorted = [...intervals].sort((a, b) => a[0] - b[0]);  // sort by start
  const out = [sorted[0].slice()];
  for (let i = 1; i < sorted.length; i++) {
    const [start, end] = sorted[i];
    const last = out[out.length - 1];
    if (start <= last[1]) {                 // overlaps the current run
      last[1] = Math.max(last[1], end);     // extend; max guards a nested interval
    } else {
      out.push([start, end]);               // a gap: open a new run
    }
  }
  return out;
}

console.log("merge:", JSON.stringify(merge([[1, 3], [2, 6], [8, 10], [15, 18]])));
console.log("touch:", JSON.stringify(merge([[1, 4], [4, 5]])));
console.log("nested:", JSON.stringify(merge([[1, 4], [2, 3]])));`}
  expected={`merge: [[1,6],[8,10],[15,18]]
touch: [[1,5]]
nested: [[1,4]]`}
/>

Note `Math.max(last[1], end)` on the extend line: without it, a fully nested interval like `[2,3]` inside `[1,4]` would wrongly shrink the run to `[1,3]`. The "nested" case proves the guard works—the run stays `[1,4]`.
</Code>

<Trace lang="en">
Let us trace activity selection on `[[3,5], [1,4], [5,7], [2,6], [6,8]]` (deliberately unsorted, so the sort step is visible). The greedy keeps one piece of state, `lastEnd`, and the verdict is "take" when `start >= lastEnd`.

<AlgoTrace
  id="trace-activity-select"
  lang="en"
  code={[
    "const sorted = intervals.sort((a, b) => a[1] - b[1]);",
    "let lastEnd = -Infinity;",
    "for (const [start, end] of sorted) {",
    "  if (start >= lastEnd) {",
    "    take(start, end); lastEnd = end;",
    "  }",
    "}"
  ]}
  frames={[
    { caption: "Sort by finish time. Input [3,5],[1,4],[5,7],[2,6],[6,8] becomes [1,4],[3,5],[2,6],[5,7],[6,8]. lastEnd = -Infinity.", line: 1, cells: [{ value: "sorted: [1,4][3,5][2,6][5,7][6,8]", mark: "active" }, { value: "lastEnd=-inf" }] },
    { caption: "[1,4]: start 1 >= -Infinity -> TAKE. lastEnd becomes 4.", line: 5, cells: [{ value: "[1,4] TAKE", mark: "done" }, { value: "lastEnd=4" }] },
    { caption: "[3,5]: start 3 < lastEnd 4 -> overlaps, SKIP.", line: 4, cells: [{ value: "[3,5] skip", mark: "target" }, { value: "lastEnd=4" }] },
    { caption: "[2,6]: start 2 < lastEnd 4 -> overlaps, SKIP.", line: 4, cells: [{ value: "[2,6] skip", mark: "target" }, { value: "lastEnd=4" }] },
    { caption: "[5,7]: start 5 >= lastEnd 4 -> TAKE. lastEnd becomes 7.", line: 5, cells: [{ value: "[5,7] TAKE", mark: "done" }, { value: "lastEnd=7" }] },
    { caption: "[6,8]: start 6 < lastEnd 7 -> overlaps, SKIP.", line: 4, cells: [{ value: "[6,8] skip", mark: "target" }, { value: "lastEnd=7" }] },
    { caption: "Done. Chosen: [1,4] and [5,7] -> 2 non-overlapping intervals, the maximum possible for this set.", line: 7, cells: [{ value: "result: [1,4],[5,7]", mark: "done" }, { value: "count=2" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time: O(n log n) — the sort dominates**

Every one of the three algorithms has the same cost breakdown:

```
sort the n intervals      O(n log n)   <- the expensive step
one linear sweep          O(n)
total                     O(n log n)
```

The greedy sweep itself is a single pass: each interval is looked at once, with O(1) work (a comparison and maybe an update). That is O(n). The only reason the whole thing is not O(n) is the **sort**, which is O(n log n). So for all three:

```
activity selection   O(n log n)   (sort by finish)
merge intervals      O(n log n)   (sort by start)
minimum rooms        O(n log n)   (sort starts and ends)
```

This is the signature of interval greedies: **the sort is the algorithm's whole cost.** If the intervals arrive already sorted on the key you need (sometimes true for event streams), the sweep alone is O(n).

**Space: O(n) or O(1) beyond the sort**

- **Activity selection** needs only `lastEnd` and a counter: **O(1)** extra (O(n) if you also collect the chosen list).
- **Merge intervals** builds the output list, which is at most n intervals: **O(n)**.
- **Minimum rooms** holds the two sorted arrays of start/end times: **O(n)**.

The sort may use O(log n) to O(n) auxiliary space depending on the implementation, but the dominant story is: cheap sweep, one sort, **O(n log n) time**.
</Complexity>

<AlgoPractice
  id="practice-interval-scheduling"
  lessonSlug="03-interval-scheduling"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "To pick the maximum number of non-overlapping intervals, what should you sort by?",
      choices: [
        "Earliest start time",
        "Earliest finish time, then greedily take each interval that starts at or after the last taken one finishes",
        "Shortest duration first",
        "Longest duration first"
      ],
      answer: 1,
      hint: "The interval that finishes earliest leaves the most room behind it, so taking it is always safe. Start time and duration are both tempting but wrong keys here."
    },
    {
      kind: "choice",
      prompt: "Why is 'sort by shortest duration' a wrong greedy for maximizing non-overlapping intervals?",
      choices: [
        "It is too slow, taking O(n^2) time",
        "A single short interval can sit in the middle and block two longer intervals that did not overlap each other (e.g. [0,4],[3,5],[4,8] gives 1 instead of 2)",
        "It cannot handle intervals with the same length",
        "Shortest duration is actually correct, not wrong"
      ],
      answer: 1,
      hint: "Recall the counterexample [0,4],[3,5],[4,8]. Picking the shortest [3,5] first blocks both [0,4] and [4,8], yielding 1. Earliest-finish yields the optimal 2."
    },
    {
      kind: "choice",
      prompt: "For merging overlapping intervals into the fewest disjoint ones, what is the right approach?",
      choices: [
        "Sort by finish time and count non-overlaps",
        "Sort by start time, then sweep: extend the current run while the next interval overlaps it, otherwise open a new run",
        "Sort by duration and merge the longest first",
        "No sorting needed; just compare every pair"
      ],
      answer: 1,
      hint: "Sorting by start means nothing later can reach behind the current run's start, so a single left-to-right pass comparing against one run is correct."
    },
    {
      kind: "choice",
      prompt: "The 'minimum number of rooms' (meeting rooms II) answer equals which quantity?",
      choices: [
        "The total number of meetings",
        "The maximum number of meetings active at the same instant (the peak overlap)",
        "The number of distinct end times",
        "The longest single meeting's duration"
      ],
      answer: 1,
      hint: "You need exactly as many rooms as the busiest moment requires. Sweep sorted starts and ends, adding a room on each start and freeing one on each end, tracking the peak."
    },
    {
      kind: "choice",
      prompt: "Why are all three interval algorithms O(n log n)?",
      choices: [
        "Because they use nested loops over the intervals",
        "Because the sweep is O(n) but the initial sort is O(n log n), and the sort dominates",
        "Because comparing two intervals takes O(log n)",
        "Because they recurse with depth log n"
      ],
      answer: 1,
      hint: "Each is one sort plus one linear pass. The pass is O(n); the sort is O(n log n). The larger term wins, so the total is O(n log n)."
    },
    {
      kind: "numeric",
      prompt: "Given intervals [1,4], [2,5], [7,9], what is the minimum number of rooms needed (peak overlap)?",
      answer: 2,
      hint: "[1,4] and [2,5] overlap (both active around time 2-4), so they need 2 rooms. [7,9] is separate and reuses a room. Peak overlap = 2."
    },
    {
      kind: "numeric",
      prompt: "Given intervals [0,2], [1,3], [2,4], [3,5], how many non-overlapping intervals can you pick at most?",
      answer: 2,
      hint: "Sort by finish: [0,2],[1,3],[2,4],[3,5]. Take [0,2] (lastEnd 2), skip [1,3] (starts `1<2`), take [2,4] (starts 2>=2), skip [3,5]. That is 2."
    }
  ]}
/>

<Inset kind="mistake" lang="en">
**Mistake: sorting by start time for the maximization problem.** Sort-by-start is correct for *merging* intervals, so it is easy to reach for it everywhere. But for picking the most non-overlapping intervals it fails: a meeting that starts earliest but runs forever gets grabbed first and blocks everything after it. The keys are problem-specific—**finish time for maximization, start time for merging.** Mixing them up silently returns a wrong-but-plausible answer.
</Inset>

<Inset kind="edgecase" lang="en">
**Touching endpoints: is `[1,4]` then `[4,8]` an overlap?** It depends on whether intervals are half-open. In most scheduling problems they are: a meeting ending at 4:00 and one starting at 4:00 do *not* clash, so the test is `start >= lastEnd` (use `>=`, allowing back-to-back). If the endpoints are inclusive and touching counts as a conflict, use `start > lastEnd`. For merge intervals the same choice appears: `start <= last[1]` merges touching intervals into one run; `start < last[1]` keeps them separate. Decide the endpoint convention first, then pick `>`/`>=` accordingly—a single wrong comparison shifts every answer by one.
</Inset>

<Inset kind="why" lang="en">
**Why is earliest-finish provably optimal?** This is the exchange argument from lesson 02. Suppose some optimal solution does *not* start with the earliest-finishing interval `f`. Its first interval `g` finishes no earlier than `f` (since `f` finishes earliest of all). Swap `g` for `f`: because `f` finishes at or before `g`, `f` cannot overlap anything that came after `g`, so the swap stays valid and keeps the same count. Repeating the argument, you can rebuild any optimal solution to begin with the greedy's choice. The greedy choice is therefore "safe," and a sequence of safe choices is optimal. This is the heart of why greedy works here and not for, say, coin change with arbitrary denominations.
</Inset>

<Inset kind="practice" lang="en">
**The interval-greedy checklist.** (1) What are you optimizing—most intervals, fewest merged runs, fewest rooms? (2) Pick the sort key: finish time for "most non-overlapping," start time for "merge," start-and-end lists for "fewest rooms." (3) Carry the minimal state: `lastEnd` for selection, the current run for merge, a running counter and peak for rooms. (4) One sweep, O(1) work per interval. (5) Decide the endpoint convention (`>` vs `>=`) up front. The cost is always O(n log n), and almost all of it is the sort.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-interval-scheduling"
    pieceSlug="03-interval-scheduling"
    lang="en"
    question="Describe how to maximize the number of non-overlapping intervals, why that greedy is correct, how it differs from merging intervals, and the complexity of these interval algorithms."
    choices={[
      {
        label: "To pick the most non-overlapping intervals, sort by earliest finish time and greedily take each interval whose start is at or after the last taken interval's finish. This is correct by an exchange argument: the earliest-finishing interval leaves the most room behind it, so any optimal solution can be rewritten to begin with it without losing count, making the choice safe. Merging intervals is a different problem with a different key—sort by start time and sweep, extending the current run while the next interval overlaps it. Both run in O(n log n) time because the sort dominates a linear O(n) sweep. Sorting by start time or by shortest duration is a wrong greedy for maximization (e.g. [0,4],[3,5],[4,8] gives 1 instead of 2).",
        correct: true
      },
      {
        label: "To pick the most non-overlapping intervals, sort by earliest start time and take each interval greedily. This is optimal because starting as early as possible always fits the most meetings. Merge intervals uses the exact same sort-by-start logic, so the two problems are interchangeable. Both run in O(n) time because there is only one pass over the intervals.",
        misconception: "Sort-by-start is wrong for maximization: a meeting that starts earliest but runs forever blocks everything. The correct maximization key is earliest FINISH time. And the algorithms are O(n log n), not O(n)—the sort dominates the linear sweep, even though there is only one pass."
      },
      {
        label: "To pick the most non-overlapping intervals, sort by shortest duration and take the smallest intervals first, since packing small ones leaves the most room. The complexity is O(n^2) because you must compare each interval against all previously taken ones to check overlaps.",
        misconception: "Shortest-duration is a classic wrong greedy: a short interval in the middle (like [3,5] in [0,4],[3,5],[4,8]) blocks two non-overlapping longer ones, giving 1 instead of 2. The correct key is earliest finish, and tracking a single lastEnd makes the sweep O(n), so the total is O(n log n), not O(n^2)."
      },
      {
        label: "Maximizing non-overlapping intervals and merging intervals are the same problem with the same answer, both solved by sorting on finish time. The minimum-rooms problem is unrelated and cannot be solved greedily; it requires dynamic programming over all subsets, which is exponential.",
        misconception: "They are different problems with different keys: maximization sorts by finish, merging sorts by start, and they produce different outputs. Minimum rooms IS greedy and O(n log n)—sort starts and ends, sweep a counter to find the peak overlap. No exponential subset search is needed."
      }
    ]}
  />
</Check>

<Recap lang="en">
Three interval problems, all solved by **sort on the right key, then one sweep.**

**Activity selection (most non-overlapping intervals):** sort by <Term slug="earliest_finish_time">earliest finish time</Term>, take each interval whose start is `>= lastEnd`, advance `lastEnd`. Correct by the <Term slug="exchange_argument">exchange argument</Term>: the earliest-finishing interval is always a safe choice because it leaves the most room behind it.

**Wrong greedies to avoid:** sort-by-start (an early-but-long interval blocks everything) and sort-by-shortest-duration (a short interval in the middle blocks two non-overlapping neighbors—`[0,4],[3,5],[4,8]` gives 1 instead of 2).

**Merge intervals (fewest disjoint runs):** sort by **start**, sweep, extend the current run while `start <= last[1]` (use `Math.max` on the end to survive nested intervals), else open a new run.

**Minimum rooms (meeting rooms II):** the answer is the **peak overlap**. Sort starts and ends separately, sweep a two-pointer counter—add a room on each start, free one on each end, track the maximum.

**Complexity:** all three are **O(n log n)**, with the sort dominating an O(n) sweep. Space is O(1) (selection, count only) to O(n) (merge output, room arrays). The endpoint convention (`>` vs `>=`, `<` vs `<=`) decides whether touching intervals count as overlapping—choose it before you write the comparison.

The recurring lesson of interval greedies: **the hard part is choosing what to sort by, not the sweep.** Get the key right and the rest is one linear pass. Now when you see an interval problem in a design review, your first question should be: "What am I optimizing, and which key unlocks it?"—because the answer dictates everything that follows.
</Recap>
