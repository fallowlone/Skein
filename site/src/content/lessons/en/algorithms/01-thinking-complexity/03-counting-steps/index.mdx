---
concepts:
- step_count
- basic_operation
- growth_rate
- dominant_term
estMin: 18
lang: en
mathPrereqs: []
order: 3
prereqs:
- 01-what-is-an-algorithm
- 02-correctness
slug: 03-counting-steps
sources:
- https://yourbasic.org/algorithms/time-complexity-explained/
- https://www.geeksforgeeks.org/dsa/step-count-method-for-time-complexity-analysis/
status: ready
summary: The cost of an algorithm is how many basic operations it performs. Count
  the steps for a given input size n, and see how the count grows as n grows.
title: Counting the steps
track: algorithms
unit: 01-thinking-complexity
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Suppose you write two algorithms to solve the same problem. One visits every element in a list once. The other visits every pair of elements. For a list of 100 items, the first does 100 steps, the second does 10,000. How did we know this? We counted the steps. This is how you measure an algorithm's cost before you ever run it.
</Hook>

<Goal lang="en">
After this lesson you can count the number of basic steps an algorithm performs as a function of input size n, predict how that count grows (linear, quadratic, and so on), and see why the growth rate matters more than the exact number.
</Goal>

<Idea lang="en">
A **basic operation** is a step the computer does in constant time: assignment, comparison, arithmetic, array access. Every algorithm is built from many basic operations. The **step count** of an algorithm is how many basic operations it performs on a given input.

The key insight: the step count depends on the size of the input. For the `largestOf` function (from lesson 01), the input is a list of n numbers. The step count is roughly n − 1 (one comparison per element after the first). For a different algorithm that checks every pair of elements, the step count is roughly n × n.

Let us count the steps for `largestOf`:

<AnnotatedCode
  code={`function largestOf(numbers) {
  let largest = numbers[0];           // 1 step
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {       // 1 comparison per loop
      largest = numbers[i];           // 1 assignment (sometimes)
    }
  }
  return largest;                     // 1 step
}`}
  annotations={[
    { line: 1, text: "Setup: 1 assignment" },
    { line: 2, text: "Loop runs n-1 times. Each iteration does 1 comparison + maybe 1 assignment" },
    { line: 6, text: "Return: 1 step" }
  ]}
/>

Inside the loop, the critical operation is the comparison `numbers[i] > largest`. This happens exactly n − 1 times (once for each element after the first). So the step count is about n − 1.

Now consider a **different algorithm**: count how many pairs of equal numbers are in a list. To check every pair, you need nested loops:

```js
function countEqualPairs(numbers) {
  let count = 0;
  for (let i = 0; i < numbers.length; i++) {
    for (let j = i + 1; j < numbers.length; j++) {
      if (numbers[i] === numbers[j]) {
        count++;
      }
    }
  }
  return count;
}
```

The outer loop runs n times. The inner loop runs roughly n times (on average). So the total is roughly n × n = n² comparisons. For a list of 100 items, this is 10,000 comparisons. For a list of 1,000 items, this is 1,000,000 comparisons.

The **step count is a function of n**. We write:
- `largestOf` has a step count of roughly n − 1.
- `countEqualPairs` has a step count of roughly n².

What matters is how the step count grows as n grows. If you double n, the step count for `largestOf` roughly doubles. If you double n for `countEqualPairs`, the step count roughly quadruples (because n² grows much faster than n).
</Idea>

<DiagramFrame label="A table of input size n against step count for two algorithms. Rows for n equal to 1, 2, 4, and 8. The single-loop column shows steps equal to n (1, 2, 4, 8) with a bar growing linearly. The nested-loop column shows steps equal to n times n (1, 4, 16, 64) with a bar that starts tiny and shoots up. Counting the basic operations turns each algorithm into a growth function of n." hue="var(--d-systems)" caption="Count the basic operations for each input size n: a single loop does n steps (linear), nested loops do n×n steps (quadratic). The step count IS a function of n — and n² pulls away fast as n grows.">
  <svg viewBox="0 0 560 268" width="560" height="268" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="50" y="26" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--muted)">n</text>
    <text x="220" y="26" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">1 loop &#8594; n steps</text>
    <text x="420" y="26" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">nested &#8594; n&#215;n steps</text>
    <line x1="20" y1="38" x2="540" y2="38" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="120" y1="38" x2="120" y2="252" stroke="var(--hairline-2)" stroke-width="1" stroke-dasharray="4 4" />
    <line x1="320" y1="38" x2="320" y2="252" stroke="var(--hairline-2)" stroke-width="1" stroke-dasharray="4 4" />

    <text x="50" y="74" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <rect x="130" y="62" width="13.75" height="18" rx="2" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="156" y="76" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">1</text>
    <rect x="330" y="62" width="1.7" height="18" rx="2" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="346" y="76" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">1</text>

    <text x="50" y="118" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <rect x="130" y="106" width="27.5" height="18" rx="2" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="170" y="120" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">2</text>
    <rect x="330" y="106" width="6.9" height="18" rx="2" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="350" y="120" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">4</text>

    <text x="50" y="162" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <rect x="130" y="150" width="55" height="18" rx="2" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="197" y="164" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">4</text>
    <rect x="330" y="150" width="27.5" height="18" rx="2" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="369" y="164" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">16</text>

    <text x="50" y="206" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">8</text>
    <rect x="130" y="194" width="110" height="18" rx="2" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="252" y="208" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">8</text>
    <rect x="330" y="194" width="110" height="18" rx="2" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="452" y="208" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">64</text>

    <text x="220" y="244" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">linear: grows in step with n</text>
    <text x="420" y="244" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">quadratic: pulls away fast</text>
  </svg>
</DiagramFrame>

<Code lang="en">
Let us implement both algorithms and count the steps by hand.

**Algorithm 1: Find the largest number**

```js
function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

**Algorithm 2: Count equal pairs**

```js
function countEqualPairs(numbers) {
  let count = 0;
  for (let i = 0; i < numbers.length; i++) {
    for (let j = i + 1; j < numbers.length; j++) {
      if (numbers[i] === numbers[j]) {
        count++;
      }
    }
  }
  return count;
}
```

Try both on a small list:

<CodeRunner
  id="cr-counting"
  lang="en"
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}

function countEqualPairs(numbers) {
  let count = 0;
  for (let i = 0; i < numbers.length; i++) {
    for (let j = i + 1; j < numbers.length; j++) {
      if (numbers[i] === numbers[j]) {
        count++;
      }
    }
  }
  return count;
}

const list = [3, 9, 2, 9, 7];
console.log("Largest:", largestOf(list));
console.log("Equal pairs:", countEqualPairs(list));`}
/>

Both work, but notice: `largestOf` is fast even on large lists. `countEqualPairs` grows much slower as the list size increases.
</Code>

<Trace lang="en">
Let us trace `countEqualPairs` on a small list `[3, 9, 3]` and count the comparisons:

<AlgoTrace
  id="trace-counting"
  lang="en"
  code={[
    "function countEqualPairs(numbers) {",
    "  let count = 0;",
    "  for (let i = 0; i < numbers.length; i++) {",
    "    for (let j = i + 1; j < numbers.length; j++) {",
    "      if (numbers[i] === numbers[j]) {",
    "        count++;",
    "      }",
    "    }",
    "  }",
    "  return count;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: count = 0, i = 0. Inner loop: j = 1, 2.",
      line: 3,
      cells: ["3", "9", "3"]
    },
    {
      caption: "i = 0, j = 1: is 3 === 9? No. Comparison 1.",
      line: 5,
      cells: [{ value: "3", mark: "active" }, { value: "9", mark: "active" }, "3"]
    },
    {
      caption: "i = 0, j = 2: is 3 === 3? Yes. count = 1. Comparison 2.",
      line: 5,
      cells: [{ value: "3", mark: "active" }, "9", { value: "3", mark: "active" }]
    },
    {
      caption: "i = 1, j = 2: is 9 === 3? No. Comparison 3.",
      line: 5,
      cells: ["3", { value: "9", mark: "active" }, { value: "3", mark: "active" }]
    },
    {
      caption: "Done. Total: 3 comparisons for n=3 items. That is 3 = 1+2.",
      line: 10,
      cells: ["3", "9", { value: "3", mark: "done" }]
    }
  ]}
/>

For n = 3, we did 3 comparisons. For n = 4, we would do 6 comparisons (1 + 2 + 3). For n = 5, we would do 10 comparisons (1 + 2 + 3 + 4). The pattern is n × (n − 1) / 2, which is roughly n².
</Trace>

<Complexity lang="en">
Now let us think about growth. Suppose we know the step count as a function of n:
- `largestOf`: step count ≈ n − 1
- `countEqualPairs`: step count ≈ n × (n − 1) / 2, which is roughly n²

What happens when n doubles?

| Input size n | largestOf steps | countEqualPairs steps |
|---|---|---|
| 10 | 9 | 45 |
| 20 | 19 | 190 |
| 100 | 99 | 4,950 |
| 200 | 199 | 19,900 |

Notice: when n goes from 10 to 100, `largestOf` steps go from 9 to 99 (10× increase). But `countEqualPairs` steps go from 45 to 4,950 (110× increase). Quadratic growth is much worse than linear growth.

Why does this matter? As the input gets larger, an algorithm with quadratic step count becomes impractical much faster than one with linear step count. For processing a million items, linear takes 1 million steps; quadratic takes 1 trillion steps.

The **dominant term** is what matters most. For `countEqualPairs`, the n² term dominates; the linear and constant terms are negligible. For `largestOf`, the n term dominates.

We care about how the step count **grows in proportion to n**, not the exact number of steps.
</Complexity>

<AlgoPractice
  id="practice-counting-1"
  lessonSlug="03-counting-steps"
  lang="en"
  problems={[
    {
      kind: "numeric",
      prompt: "How many comparisons does largestOf perform on a list of 50 items?",
      answer: 49,
      hint: "The algorithm compares each of the remaining n − 1 items to the largest so far."
    },
    {
      kind: "numeric",
      prompt: "For countEqualPairs on a list of 4 items, how many comparisons? (Hint: 1 + 2 + 3.)",
      answer: 6,
      hint: "The outer loop runs 4 times. Inner loop runs (n-1) + (n-2) + ... + 1 = n(n-1)/2 times."
    },
    {
      kind: "choice",
      prompt: "If n goes from 10 to 100, roughly how much do the steps increase for an algorithm with step count ~n?",
      choices: [
        "10 times",
        "100 times",
        "1,000 times",
        "10,000 times"
      ],
      answer: 0,
      hint: "Linear growth: step count goes from ~10 to ~100, a 10× increase."
    },
    {
      kind: "choice",
      prompt: "If n goes from 10 to 100, roughly how much do the steps increase for an algorithm with step count ~n²?",
      choices: [
        "10 times",
        "100 times",
        "1,000 times",
        "10,000 times"
      ],
      answer: 1,
      hint: "Quadratic growth: step count goes from ~100 to ~10,000, a 100× increase."
    },
    {
      kind: "order",
      prompt: "Arrange these step counts in order of growth (slowest first):",
      items: [
        "Step count = n²",
        "Step count = n",
        "Step count = 1 (constant)",
        "Step count = n × (n − 1) / 2"
      ],
      hint: "Constant < linear < quadratic. Remember that n(n−1)/2 is still roughly n², just with a smaller constant."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Counting constants.** In practice, constants matter for actual running time (one algorithm might do 2n steps, another 5n), but for understanding growth, the constant does not change the big picture. An algorithm that does 100n steps and one that does n steps both grow linearly — they scale the same way. The next lesson introduces Big-O notation, which ignores constants and focuses on growth.
</Inset>

<Inset kind="mistake" lang="en">
**Forgetting nested loops.** A common mistake: you see a single loop and assume the step count is linear. But nested loops change everything. A loop inside a loop is not 2n; it is n × n. A loop inside a loop inside a loop is n³. Always count the nesting.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-counting-1"
    pieceSlug="03-counting-steps"
    lang="en"
    question="An algorithm has a step count of roughly 3n + 5. If n doubles, roughly how many times does the step count increase?"
    choices={[
      {
        label: "2 times (doubled)",
        correct: true
      },
      {
        label: "6 times (the constant 3 times the growth factor 2)",
        misconception: "Constants do not multiply the growth factor. Linear step count means the steps grow in proportion to n. If n doubles, steps double, regardless of the constant 3 or the +5."
      },
      {
        label: "5 times (depends on the constant 5)",
        misconception: "Constant terms like +5 do not affect the growth rate. They are negligible for large n. Only the dominant term 3n matters for growth."
      },
      {
        label: "It depends on the actual value of n",
        misconception: "The growth rate is independent of the starting value of n. Linear means linear: double the input, double the step count, always."
      }
    ]}
  />
</Check>

<Recap lang="en">
The **step count** of an algorithm is how many basic operations it performs on an input of size n. Different algorithms have different step counts:
- A single loop through n items: step count ≈ n
- Nested loops over all pairs: step count ≈ n²

As n grows, the step count grows. **Linear algorithms** (step count ~n) grow at a constant rate; **quadratic algorithms** (step count ~n²) grow much faster. What matters is the **dominant term** and the **growth rate**, not the exact number of steps or constants. The next lesson formalizes this idea with Big-O notation.
</Recap>
