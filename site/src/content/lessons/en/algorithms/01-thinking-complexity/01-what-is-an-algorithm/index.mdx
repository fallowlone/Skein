---
concepts:
- algorithm
- input_output
- step_count
estMin: 16
lang: en
mathPrereqs: []
order: 1
prereqs: []
slug: 01-what-is-an-algorithm
sources:
- https://en.wikipedia.org/wiki/Algorithm
- https://developer.mozilla.org/en-US/docs/Glossary/Algorithm
status: ready
summary: An algorithm is a finite list of unambiguous steps that turns an input into
  an output.
title: What is an algorithm?
track: algorithms
unit: 01-thinking-complexity
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You follow a recipe to bake a cake. Each step — mix the flour, add the eggs, bake at 350°F for 30 minutes — is an unambiguous instruction. The recipe works for anyone because no guessing is needed. That is a real-world algorithm.
</Hook>

<Goal lang="en">
After this lesson you can define an algorithm precisely, identify the input and output of a simple algorithm, and count the number of steps it takes to solve a problem.
</Goal>

<Idea lang="en">
An **algorithm** is a finite list of unambiguous steps that transforms an input into an output. The word "unambiguous" is load-bearing — every step must be clear enough that a computer (or a person) could execute it without any guesswork. The "finite" part means the steps must eventually end; an algorithm that loops forever is not an algorithm.

Consider a real algorithm: "Find the largest number in a list." You start with an input — a list of numbers. You end with an output — the largest one. The steps in between are the algorithm.

<AnnotatedCode
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}`}
  annotations={[
    { line: 1, text: "Input: the list of numbers" },
    { line: 5, text: "Core step: compare each number to the running largest" },
    { line: 8, text: "Output: return the largest number found" }
  ]}
/>

Every algorithm has an input (what you start with) and an output (what you end with). The steps in the middle transform the input into the output. If the steps are unambiguous and finite, it is an algorithm.
</Idea>

<FlowDiagram
  label="An algorithm: a defined input flows through a finite sequence of unambiguous, deterministic steps and produces an output."
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "input", label: "Input", sub: "[3, 9, 2, 7]" },
    { id: "step1", label: "Step 1", sub: "largest = first" },
    { id: "step2", label: "Step 2", sub: "compare each" },
    { id: "step3", label: "Step 3", sub: "keep bigger" },
    { id: "output", label: "Output", sub: "9" },
  ]}
  edges={[
    { from: "input", to: "step1" },
    { from: "step1", to: "step2" },
    { from: "step2", to: "step3", label: "loop" },
    { from: "step3", to: "output" },
  ]}
  caption="Defined input → a finite, ordered list of unambiguous steps → an output. The same input always yields the same output, and the steps always end."
/>

<Code lang="en">
The algorithm "find the largest number in a list" is so common that we'll code it in JavaScript. Here is the step-by-step logic:

1. Assume the first number is the largest.
2. Look at each remaining number.
3. If a number is bigger than the current largest, remember it as the new largest.
4. When you reach the end of the list, return the largest.

Here is the function:

```js
function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Try running it with the list `[3, 9, 2, 7]`. The function will:
1. Start with `largest = 3` (the first number).
2. See `9` — it is bigger, so `largest` becomes `9`.
3. See `2` — it is smaller than `9`, so skip it.
4. See `7` — it is smaller than `9`, so skip it.
5. Return `9`.

<CodeRunner
  id="cr-largest"
  lang="en"
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}

console.log(largestOf([3, 9, 2, 7]));`}
/>
</Code>

<Trace lang="en">
Watch the algorithm step by step as it runs on the list `[3, 9, 2, 7]`:

<AlgoTrace
  id="trace-largest"
  lang="en"
  code={[
    "function largestOf(numbers) {",
    "  let largest = numbers[0];",
    "  for (let i = 1; i < numbers.length; i++) {",
    "    if (numbers[i] > largest) {",
    "      largest = numbers[i];",
    "    }",
    "  }",
    "  return largest;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: set largest to the first number, 3.",
      line: 2,
      cells: ["3", "9", "2", "7"].map((val, i) => i === 0 ? { value: val, mark: "active" } : val)
    },
    {
      caption: "Loop iteration 1: compare 9 to largest (3). 9 is bigger, so largest becomes 9.",
      line: 4,
      cells: ["3", { value: "9", mark: "active" }, "2", "7"]
    },
    {
      caption: "After the comparison, largest is updated to 9.",
      line: 5,
      cells: ["3", { value: "9", mark: "done" }, "2", "7"]
    },
    {
      caption: "Loop iteration 2: compare 2 to largest (9). 2 is smaller, so skip.",
      line: 4,
      cells: ["3", "9", { value: "2", mark: "active" }, "7"]
    },
    {
      caption: "Loop iteration 3: compare 7 to largest (9). 7 is smaller, so skip.",
      line: 4,
      cells: ["3", "9", "2", { value: "7", mark: "active" }]
    },
    {
      caption: "Done: return the largest, 9.",
      line: 8,
      cells: ["3", { value: "9", mark: "done" }, "2", "7"]
    }
  ]}
/>
</Trace>

<Complexity lang="en">
How fast does the algorithm work? Count the steps: the algorithm visits each of the n numbers exactly once. For a list of 4 numbers, the loop runs 3 times (we skip the first). For a list of 100 numbers, the loop runs 99 times. For a list of n numbers, the loop runs n − 1 times.

We say the algorithm takes **O(n) time**, meaning the number of steps grows in proportion to the size of the input. If the list doubles in size, the number of steps roughly doubles.

Other algorithms for finding the largest are slower. For example, if you compared every number to every other number (checking if A > B, B > A, and so on), you would need n × n comparisons — **O(n²) time**. For large lists, this is much worse.

The key insight: the number of steps is the cost of an algorithm.
</Complexity>

<AlgoPractice
  id="practice-algo-1"
  lessonSlug="01-what-is-an-algorithm"
  lang="en"
  problems={[
    {
      kind: "numeric",
      prompt: "If the largestOf algorithm runs on a list of 10 numbers, how many comparisons does it make?",
      answer: 9,
      hint: "The algorithm compares each of the remaining n − 1 numbers to the largest so far."
    },
    {
      kind: "numeric",
      prompt: "How many comparisons for a list of 50 numbers?",
      answer: 49,
      hint: "One less than the list size."
    },
    {
      kind: "choice",
      prompt: "Which of these is an algorithm?",
      choices: [
        "A recipe with steps like 'add some flour'",
        "A sequence of unambiguous steps that ends with a result",
        "Any code written in JavaScript",
        "A program that might loop forever"
      ],
      answer: 1,
      hint: "An algorithm must have unambiguous steps and must finish in finite time."
    },
    {
      kind: "choice",
      prompt: "What is the time complexity of the largestOf algorithm?",
      choices: [
        "O(1) — constant time",
        "O(n) — linear time",
        "O(n²) — quadratic time",
        "O(log n) — logarithmic time"
      ],
      answer: 1,
      hint: "The number of steps grows in proportion to the size of the list."
    },
    {
      kind: "order",
      prompt: "Arrange these steps in the correct order for the largestOf algorithm:",
      items: [
        "Set largest to the first number",
        "For each remaining number, check if it is bigger than the current largest",
        "If the number is bigger, update largest",
        "Return the largest"
      ],
      hint: "Start by setting the initial largest, then loop through the list, update as needed, then return the result."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
What if the list is empty? The algorithm will crash when trying to access `numbers[0]`. A more robust version checks if the list is empty first and handles it — perhaps by returning null or throwing an error. Real algorithms need to handle edge cases like this.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-algo-1"
    pieceSlug="01-what-is-an-algorithm"
    lang="en"
    question="Why is it important that the steps in an algorithm are unambiguous?"
    choices={[
      {
        label: "So anyone or any computer can follow the steps without guessing",
        correct: true
      },
      {
        label: "So the algorithm always finds the right answer",
        misconception: "An algorithm can be unambiguous and still give the wrong answer if it is poorly designed. Unambiguous means each step is clear, not that the algorithm is correct."
      },
      {
        label: "So the algorithm runs as fast as possible",
        misconception: "Unambiguity is about clarity, not speed. A slow algorithm can still be unambiguous."
      },
      {
        label: "So the algorithm can be written in any programming language",
        misconception: "Unambiguous steps can be written in many languages, but the unambiguity itself is about the logic, not the language choice."
      }
    ]}
  />
</Check>

<Recap lang="en">
An **algorithm** is a finite list of unambiguous steps that transforms an input into an output. Every algorithm has a cost, measured by the number of steps it takes. We use **big-O notation** (like O(n) or O(n²)) to describe how the number of steps grows as the input size increases. The largestOf algorithm searches a list in O(n) time — visiting each element once.
</Recap>
