---
concepts:
- counting-steps
- nested-loops
- big-o
- space-complexity
- correctness-bug
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.geeksforgeeks.org/dsa/step-count-method-for-time-complexity-analysis/
- https://en.wikipedia.org/wiki/Big_O_notation
- https://en.wikipedia.org/wiki/Space_complexity
status: ready
summary: "Read real code snippets, count the steps, derive the Big-O for time and space, predict behaviour, and spot the complexity or correctness bug."
title: 'Thinking & complexity: code reading'
track: algorithms
unit: 01-thinking-complexity
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Analysis only sticks when you do it on real code. Read each snippet, count the loops and the memory, and decide its complexity — the exact move you make when reviewing a pull request or sizing up a solution.
</Hook>

<Goal lang="en">
Practise the loop you run on any unfamiliar function: find the dominant work, multiply nested loops, separate time from space, and notice when the trouble is not speed at all but a correctness bug.
</Goal>

## Snippet 1 — what does the nesting cost?

```js
function countCloserPairs(points, threshold) {
  let count = 0;
  for (let i = 0; i < points.length; i++) {
    for (let j = i + 1; j < points.length; j++) {
      if (Math.abs(points[i] - points[j]) < threshold) {
        count++;
      }
    }
  }
  return count;
}
```

<Quiz
  id="01-thinking-complexity-code-1"
  lessonSlug="01-thinking-complexity"
  lang="en"
  question="What are the time and space complexities of countCloserPairs on n points?"
  choices={[
    { label: "Time O(n²), space O(1). The inner loop starting at i+1 runs n(n−1)/2 comparisons in total — still ~n², the dominant term — while only count, i, and j are stored.", correct: true },
    { label: "Time O(n), space O(n). It is a single pass and count can grow with n.", misconception: "There are two nested loops, so it is O(n²) time, not O(n). And space counts memory structures, not the value of count — three scalar variables is O(1)." },
    { label: "Time O(n²), space O(n²). The pairs it inspects form an n×n table.", misconception: "It inspects ~n² pairs but never stores them — it only increments a counter. Space is O(1); the n² is the time, not the memory." },
    { label: "Time O(n log n), space O(1). The j-loop starting at i+1 makes it logarithmic.", misconception: "Starting j at i+1 halves the constant (n(n−1)/2 instead of n²) but the dominant term is still n². It is O(n²), not O(n log n)." },
  ]}
/>

## Snippet 2 — predict the comparison count

```js
function linearSearch(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;
}
// arr = [0, 1, 2, ..., 999]  (1000 sorted elements)
```

<Quiz
  id="01-thinking-complexity-code-2"
  lessonSlug="01-thinking-complexity"
  lang="en"
  question="For arr = [0..999], which statement about linearSearch is correct?"
  choices={[
    { label: "Searching for 999 does ~1000 comparisons and searching for a missing value does 1000; worst-case is O(n). Binary search would find 999 in ~10 comparisons because the array is sorted.", correct: true },
    { label: "It is O(log n) because the array is sorted.", misconception: "Being sorted does not make a linear scan logarithmic — this code ignores the order and checks every element. You would need binary search to exploit the sort and get O(log n)." },
    { label: "Worst-case and best-case are both ~500 comparisons (the average).", misconception: "Best-case is 1 (target at index 0); worst-case is n (target last or absent). The average is ~n/2, but Big-O reports the worst case, O(n)." },
    { label: "It is O(1) because returning early makes it constant time.", misconception: "Early return helps only when the target is near the front. For a missing value or one at the end, it scans all n elements — worst-case O(n)." },
  ]}
/>

## Snippet 3 — same output, different cost

```js
// Version A
function sumToNLoop(n) {
  let sum = 0;
  for (let i = 1; i <= n; i++) sum += i;
  return sum;
}

// Version B
function sumToNFormula(n) {
  return n * (n + 1) / 2;
}
```

<Quiz
  id="01-thinking-complexity-code-3"
  lessonSlug="01-thinking-complexity"
  lang="en"
  question="Both functions return the same value. How do their complexities compare?"
  choices={[
    { label: "Version A is O(n) time (the loop runs n times); Version B is O(1) time (a fixed arithmetic expression regardless of n). Both are O(1) space. Same output, very different growth.", correct: true },
    { label: "They are both O(n) because both compute a sum up to n.", misconception: "Version B computes no sum — it applies the closed-form n(n+1)/2 in a constant number of operations. The result matches, but the cost is O(1), not O(n)." },
    { label: "Version B is faster but still O(n) since it depends on n.", misconception: "Depending on the VALUE of n is not the same as scaling with n. B does the same handful of arithmetic ops whether n is 5 or 5 billion — that is O(1)." },
    { label: "Version A is O(1) because a single loop is constant.", misconception: "A loop bounded by n runs n times, so it is O(n), not O(1). Only a loop with a fixed bound independent of n is constant time." },
  ]}
/>

## Snippet 4 — the bug a test might miss

```js
function average(numbers) {
  let sum = 0;
  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }
  return sum / numbers.length;
}
```

<Quiz
  id="01-thinking-complexity-code-4"
  lessonSlug="01-thinking-complexity"
  lang="en"
  question="The complexity is fine — O(n) time, O(1) space. But what edge case breaks correctness, and why does that matter here?"
  choices={[
    { label: "An empty array: the loop never runs, then it returns 0/0 = NaN. The specification must say what to return for an empty input; testing only non-empty lists would never expose it.", correct: true },
    { label: "A very large array, because the O(n) loop will time out.", misconception: "O(n) is feasible for huge inputs — that is not a correctness bug. The defect is the empty-array case producing NaN, independent of size." },
    { label: "Negative numbers, because sum could go negative.", misconception: "A negative average is a perfectly valid result for negative inputs — no bug. The genuine edge case is the empty array dividing by zero." },
    { label: "There is no bug; it passed every test the author ran.", misconception: "Passing tests is not a proof of correctness. The author simply never tested the empty array, where length 0 makes the division NaN." },
  ]}
/>

<Recap lang="en">
Reading complexity off code is a fixed routine: find the dominant work and keep only that term; multiply nested loops and add sequential phases; count memory structures, not loop iterations, for space; and remember a closed form can replace an O(n) loop with O(1). Then check the other axis — an algorithm with perfect Big-O can still be wrong on the edge case (empty input, division by zero) that your tests never touched.
</Recap>
