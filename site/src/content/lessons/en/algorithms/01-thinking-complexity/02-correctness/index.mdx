---
concepts:
- correctness
- loop_invariant
- edge_case
- specification
estMin: 18
lang: en
mathPrereqs: []
order: 2
prereqs:
- 01-what-is-an-algorithm
slug: 02-correctness
sources:
- https://en.wikipedia.org/wiki/Loop_invariant
- https://www.baeldung.com/cs/loop-invariant
status: ready
summary: Correctness means the algorithm produces the right output for every valid
  input, not just the ones you tested. Use loop invariants to reason about correctness.
title: Is the algorithm correct?
track: algorithms
unit: 01-thinking-complexity
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You test your function on `[3, 9, 2, 7]` and it returns 9 — correct. You test it on `[1, 2, 3]` and it returns 3 — correct. You feel confident. Then in production, it crashes on an empty list. Or it returns the wrong answer on all-negative numbers. Testing a few examples is not the same as being sure your algorithm is correct for every possible input.
</Hook>

<Goal lang="en">
After this lesson you can define what **correctness** means for an algorithm, spot bugs by considering **edge cases**, and use a **loop invariant** to reason that a loop solves its problem correctly.
</Goal>

<Idea lang="en">
An algorithm is **correct** if it produces the right output for **every** valid input, not just the ones you tried. This definition requires two things:

1. A **specification** — a precise statement of what output is expected for what input. For example: "Given a list of numbers, return the largest one."
2. **Proof** — a logical argument that the algorithm meets the specification for all possible inputs.

Testing is not proof. Testing can reveal bugs, but it cannot prove correctness, because you cannot test every possible input. Edge cases — unusual or boundary inputs like empty lists, single elements, or all-equal values — are where bugs hide.

The tool to prove a loop is correct is called a **loop invariant**: a statement about the loop that is true before it runs, remains true after each iteration, and is useful when the loop finishes.

Consider the `largestOf` function from lesson 01:

<AnnotatedCode
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}`}
  annotations={[
    { line: 2, text: "After this line, largest = numbers[0]" },
    { line: 3, text: "Loop: we will update largest as we see more numbers" },
    { line: 8, text: "Loop invariant: after processing numbers[0..i], largest holds the max" }
  ]}
/>

The **loop invariant** is: "After processing the first i+1 numbers (from index 0 to i), `largest` holds the largest of those numbers."

- **Before the loop**: We set `largest = numbers[0]`, so the invariant is true for i=0.
- **Each iteration**: We compare `numbers[i]` to `largest`. If it is bigger, we update `largest`. Either way, after the iteration, `largest` is still the largest of the numbers we have seen so far.
- **After the loop**: The loop exits when `i = numbers.length`. By the invariant, `largest` is the largest of all numbers. This is what we promised to return.

This logical argument proves the algorithm is correct — not by testing, but by reasoning.

<DiagramFrame
  label="A loop invariant proves correctness in three checkpoints along one loop. Initialization: before the first iteration, the property INV (largest holds the max of numbers[0..i]) is made true at i=0. Maintenance: each iteration assumes INV is true at the start and proves it stays true at the end, so by repetition INV holds at every step of the loop; a curved back-arrow shows the loop body repeating. Termination: when the loop exits at i = n, INV still holds — and because the whole array has been processed, INV directly states that largest is the max of all numbers, which is exactly the specification. The chain reads: prove the property holds at every step, therefore the output is correct."
  hue="var(--d-systems)"
  caption="A loop invariant is one property INV proved at three checkpoints. INIT makes INV true before the loop; MAINTAIN shows each iteration preserves INV (true at the start of the body implies true at the end), so INV holds at every step; TERMINATE: at loop exit INV plus the exit condition imply the specification holds. Property true at every step ⇒ output correct."
>
  <svg viewBox="0 0 760 340" width="760" height="340" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="inv-arrow-en" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
      <marker id="inv-arrow-mut-en" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--muted, #888)" />
      </marker>
    </defs>

    <text x="380" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">{"INV: largest = max of numbers[0..i] — one property, three checkpoints"}</text>

    <line x1="40" y1="120" x2="700" y2="120" stroke="var(--hairline-2, #888)" stroke-width="2" marker-end="url(#inv-arrow-en)" />
    <text x="370" y="112" text-anchor="middle" font-size="11" fill="var(--muted, #888)">loop runs left to right</text>

    <circle cx="120" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="40" y="150" width="160" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="120" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">1. INITIALIZATION</text>
    <text x="120" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">before the loop, i = 0</text>
    <text x="120" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">largest = numbers[0]</text>
    <text x="120" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV true</text>

    <circle cx="380" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="290" y="150" width="180" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="380" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">2. MAINTENANCE</text>
    <text x="380" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">each iteration, i = 1..n-1</text>
    <text x="380" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">INV in ⇒ INV out</text>
    <text x="380" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV stays true</text>

    <path d="M330 150 C 330 96, 430 96, 430 150" fill="none" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#inv-arrow-en)" />
    <text x="380" y="92" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">repeat: body preserves INV</text>

    <circle cx="640" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="560" y="150" width="160" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="640" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">3. TERMINATION</text>
    <text x="640" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">loop exits, i = n</text>
    <text x="640" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">all of [0..n-1] seen</text>
    <text x="640" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV true</text>

    <line x1="640" y1="234" x2="640" y2="270" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#inv-arrow-en)" />
    <rect x="470" y="278" width="270" height="50" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="605" y="300" text-anchor="middle" font-size="12" font-weight="700" fill="var(--ink, currentColor)">{"INV at i = n ⇒ largest = max of all"}</text>
    <text x="605" y="318" text-anchor="middle" font-size="12" font-weight="700" fill="var(--d-systems)">output is correct</text>

    <text x="240" y="300" text-anchor="middle" font-size="12" font-weight="600" fill="var(--muted, #888)">property true at every step</text>
    <line x1="350" y1="296" x2="466" y2="296" stroke="var(--muted, #888)" stroke-width="1.5" stroke-dasharray="4 3" marker-end="url(#inv-arrow-mut-en)" />
  </svg>
</DiagramFrame>
</Idea>

<Code lang="en">
Let us write the `largestOf` function and then reason about its correctness:

```js
function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Try it on a few inputs:

<CodeRunner
  id="cr-correct"
  lang="en"
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}

console.log(largestOf([3, 9, 2, 7]));      // 9
console.log(largestOf([1]));               // 1
console.log(largestOf([-5, -1, -10]));     // -1
console.log(largestOf([5, 5, 5, 5]));      // 5`}
/>

All these examples pass. But you might be tempted to say "I tested it, so it is correct." That is a trap. The loop invariant is what makes you sure.
</Code>

<Trace lang="en">
Let us trace the algorithm on `[-5, -1, -10]` to see the loop invariant in action:

<AlgoTrace
  id="trace-correct"
  lang="en"
  code={[
    "function largestOf(numbers) {",
    "  let largest = numbers[0];",
    "  for (let i = 1; i < numbers.length; i++) {",
    "    if (numbers[i] > largest) {",
    "      largest = numbers[i];",
    "    }",
    "  }",
    "  return largest;",
    "}"
  ]}
  frames={[
    {
      caption: "Initialize: largest = numbers[0] = -5. Invariant holds for index 0.",
      line: 2,
      cells: [{ value: "-5", mark: "active" }, "-1", "-10"]
    },
    {
      caption: "Iteration 1: i=1. Is -1 > -5? Yes. Update largest to -1.",
      line: 4,
      cells: ["-5", { value: "-1", mark: "active" }, "-10"]
    },
    {
      caption: "After update, largest = -1, the max of numbers[0..1]. Invariant holds.",
      line: 6,
      cells: ["-5", { value: "-1", mark: "done" }, "-10"]
    },
    {
      caption: "Iteration 2: i=2. Is -10 > -1? No. Keep largest = -1.",
      line: 4,
      cells: ["-5", "-1", { value: "-10", mark: "active" }]
    },
    {
      caption: "After this iteration, largest = -1 is still the max of numbers[0..2]. Invariant holds.",
      line: 6,
      cells: ["-5", "-1", { value: "-10", mark: "done" }]
    },
    {
      caption: "Loop exits. By the invariant, largest = -1 is the max of all numbers. Return it.",
      line: 8,
      cells: ["-5", { value: "-1", mark: "done" }, "-10"]
    }
  ]}
/>

The invariant stays true every step. This gives us certainty, not just for this example, but for every valid input.
</Trace>

<Complexity lang="en">
Proving correctness has no direct cost in terms of steps — it is a logical argument, not computation. But understanding the structure of the algorithm (the loop invariant) often makes the code faster or clearer. For now, the key insight: a correct algorithm is one you can reason about with an invariant.
</Complexity>

<AlgoPractice
  id="practice-correct-1"
  lessonSlug="02-correctness"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "What is a loop invariant?",
      choices: [
        "A condition that must be true before and after every iteration",
        "A variable that never changes inside the loop",
        "A statement you write to make the code run faster",
        "A rule that prevents the loop from running forever"
      ],
      answer: 0,
      hint: "A loop invariant is true before the loop, and remains true after each iteration — it helps you prove the loop is correct."
    },
    {
      kind: "choice",
      prompt: "Why is testing alone not enough to prove an algorithm is correct?",
      choices: [
        "Testing is too slow",
        "Testing can only check a finite number of inputs; you cannot test every possible input",
        "Testing requires too much code",
        "Testing always gives wrong answers"
      ],
      answer: 1,
      hint: "You can test a million cases, but there may be one case you did not test where the algorithm fails. Proof by reasoning (like loop invariants) covers all cases."
    },
    {
      kind: "numeric",
      prompt: "If a loop processes elements from index 0 to n-1, and the invariant is 'largest holds the max of elements [0..i]', what is the invariant when the loop finishes and i = n?",
      answer: 1,
      hint: "When i = n, the loop has processed indices 0 through n-1, which is all elements. So largest holds the max of all elements [0..n-1]."
    },
    {
      kind: "choice",
      prompt: "An algorithm fails on negative numbers even though it works on positive ones. What is this an example of?",
      choices: [
        "A loop invariant violation",
        "An edge case — a boundary input where the algorithm fails",
        "A problem with big-O notation",
        "An infinite loop"
      ],
      answer: 1,
      hint: "Negative numbers are an edge case — an unusual input that tests the boundaries of the algorithm's correctness."
    },
    {
      kind: "order",
      prompt: "Arrange the steps to prove a loop is correct using a loop invariant:",
      items: [
        "Prove the invariant is true before the loop (initialization)",
        "Prove the invariant remains true after each iteration (maintenance)",
        "Show that when the loop exits, the invariant tells us the answer is correct (termination)",
        "State what the loop invariant is"
      ],
      hint: "First state the invariant clearly. Then prove three things: it starts true, stays true each step, and leads to the correct answer when done."
    }
  ]}
/>

<Inset kind="edgecase" lang="en">
**Edge case: empty list.** The function crashes on an empty list because `numbers[0]` does not exist. A correct version would check `if (numbers.length === 0)` first. The specification must say what to return for empty input — perhaps null, or throw an error. Without that specification, you cannot say if the function is correct.
</Inset>

<Inset kind="mistake" lang="en">
**A buggy variant.** What if we initialize `largest = 0` instead of `largest = numbers[0]`?

```js
function largestOf_WRONG(numbers) {
  let largest = 0;  // BUG: should be numbers[0]
  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Test: `largestOf_WRONG([-5, -3, -1])` returns 0, not -1. This fails on all-negative lists. The loop invariant would have caught this: if we initialize `largest = 0`, the invariant "largest holds the max of [0..i]" is false before the loop even starts, because the maximum of [-5] is -5, not 0. A faulty invariant means a faulty algorithm.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-correct-1"
    pieceSlug="02-correctness"
    lang="en"
    question="A friend tests an algorithm on 100 random inputs and it passes all of them. Does this prove the algorithm is correct?"
    choices={[
      {
        label: "No — testing some inputs does not prove correctness for all inputs",
        correct: true
      },
      {
        label: "Yes — if it passes 100 tests, it must be correct",
        misconception: "Testing can find bugs but cannot prove correctness. There may be an edge case or rare input you did not test."
      },
      {
        label: "Yes — correctness only means it works on the inputs you test",
        misconception: "Correctness means it works on every valid input, not just the ones you tested. The specification defines what every input should produce."
      },
      {
        label: "Only if the 100 inputs include all edge cases",
        misconception: "Even if you think you covered all edge cases, you may have missed one. A proof by reasoning (like loop invariants) covers all cases logically."
      }
    ]}
  />
</Check>

<Recap lang="en">
An algorithm is **correct** if it produces the right output for every valid input. This is defined by a **specification** — what output is expected for what input. **Testing** can reveal bugs but cannot prove correctness because you cannot test every input. **Edge cases** — unusual or boundary inputs — are where bugs hide.

A **loop invariant** is a statement about a loop that is true before it runs, stays true after each iteration, and helps you prove the loop is correct. By proving three things (initialization, maintenance, termination), you can be certain the loop works for all inputs. This is a logical proof, not just confidence from testing.
</Recap>
