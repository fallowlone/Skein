---
concepts:
- dominant-term
- big-o
- complexity-classes
- time-space-tradeoff
- reading-constraints
- correctness
estMin: 14
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Big_O_notation
- https://www.geeksforgeeks.org/dsa/analysis-algorithms-big-o-analysis/
- https://codeforces.com/blog/entry/21344
status: ready
summary: "Free-recall prompts across the complexity unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Thinking & complexity: free-recall review'
track: algorithms
unit: 01-thinking-complexity
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of pulling it out is what makes the idea stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — why we count steps, what Big-O keeps and drops, the complexity-class ladder, the time-space tradeoff, and how constraints pick your approach — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-thinking-complexity-short-retrieval"
  lang="en"
  questions={[
    { q: "Why do we measure an algorithm by its growth rate (Big-O) instead of by its wall-clock time?", a: "Wall-clock time depends on the machine, the language, the compiler, and the input — run the same code on a faster CPU and it speeds up without becoming a better algorithm. Big-O measures something machine-independent: how the number of basic operations grows as the input size n grows. That is the property that decides whether an algorithm survives at scale. A faster computer buys you a constant factor; a better complexity class buys you orders of magnitude. So we count steps as a function of n and describe how that count grows, because growth is what dominates once n is large." },
    { q: "State the rule for finding the Big-O of a step count, and apply it to 3n² + 100n + 50.", a: "Keep only the fastest-growing (dominant) term and drop its constant multiplier; lower-order terms and additive constants vanish because for large n they are negligible against the dominant term. In 3n² + 100n + 50 the dominant term is 3n²; drop the 100n and the +50 (smaller order) and drop the 3 (constant), leaving O(n²). The intuition: at n = 10,000, 3n² is 300 million while 100n is one million and 50 is nothing — the n² term sets the shape of the curve." },
    { q: "How do you combine complexities for sequential code versus nested loops, and why is the difference so large?", a: "Sequential, independent phases add: an O(n) pass followed by another O(n) pass is O(n) + O(n) = O(n) (you keep the dominant term). Nested loops multiply: a loop of n iterations with an inner loop of n iterations does n × n = O(n²) work. The difference matters enormously because addition keeps you in the same class while nesting jumps you up a class — two sequential scans of a million items is ~2 million ops, but one scan nested inside another is ~10¹² ops. Always ask whether work happens after a phase (add) or inside it (multiply)." },
    { q: "Name the common complexity classes from fastest to slowest and where the practical cliff is.", a: "From fastest to slowest: O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic, O(n²) quadratic, O(2ⁿ) exponential, O(n!) factorial. The practical cliff sits between O(n log n) and O(n²): O(1) through O(n log n) handle a million items comfortably; O(n²) is fine only up to a few thousand; O(2ⁿ) only to about n ≤ 30; O(n!) only to about n ≤ 10. Crossing from polynomial (powers of n) to superpolynomial (2ⁿ, n!) is the line between 'solvable at scale' and 'tiny inputs only'." },
    { q: "Explain the time-space tradeoff with the duplicate-detection example, and how you decide which side to take.", a: "Detecting a duplicate two ways: a nested loop compares every pair — O(n²) time but O(1) space; a hash set remembers everything seen — O(n) time but O(n) space. You spend memory to buy speed, or accept slow time to save memory. The decision is driven by the binding constraint: on a server with spare RAM and a user waiting, take the hash set (speed matters, memory is cheap); on an embedded device with tiny RAM, take the nested loop (memory is the wall, slowness is tolerable). There is no universally 'better' choice — only the one that fits the constraint you actually face." },
    { q: "How does the input constraint n tell you which complexity to aim for, with one worked example?", a: "A computer does roughly 10⁸ simple operations per second, and judges give ~1–2 seconds, so your budget is ~10⁸–2×10⁸ operations. Compare your candidate algorithm's operation count against that budget. Example: a problem says n ≤ 1,000,000. An O(n²) idea would be (10⁶)² = 10¹² operations — about 10,000× over budget, so it times out and is ruled out before you write it. O(n log n) is ~2×10⁷ and O(n) is ~10⁶, both comfortably within budget. So the constraint forces you to design an O(n log n)-or-better solution (sort + two pointers, or a hash set), turning 'where do I start?' into a narrowed target." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: we measure growth not wall-clock; Big-O keeps the dominant term and drops constants; sequential phases add while nested loops multiply; the class ladder runs O(1) to O(n!) with the practical cliff at O(n²); time and space trade against each other and the binding constraint decides; and the input bound n maps straight onto the complexity you must hit. Estimate the cost before you run anything.
</Recap>
