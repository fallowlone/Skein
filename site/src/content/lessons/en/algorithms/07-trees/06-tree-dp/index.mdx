---
concepts:
- dynamic_programming
- tree
- recursion
- return_value
- side_variable
- diameter
- height_balanced
- postorder
estMin: 28
lang: en
mathPrereqs: []
order: 6
prereqs:
- 01-the-binary-tree
- 02-traversals
- 03-level-order-traversal
- 04-recursion-on-trees
- 05-binary-search-trees
slug: 06-tree-dp
sources:
- https://en.wikipedia.org/wiki/Tree_diameter
- https://www.techiedelight.com/diameter-of-binary-tree/
- https://www.geeksforgeeks.org/diameter-of-binary-tree/
- https://www.enjoyalgorithms.com/blog/diameter-of-binary-tree/
status: ready
summary: 'Return one piece of information up (height), track another in a side variable
  (diameter). Each node solves a subproblem and contributes to a global answer. Core
  idea: a single DFS pass, two pieces of information.'
title: Tree dynamic programming
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You have solved tree problems by asking both subtrees for a value and combining the answers. But what if the final answer is NOT the value you return? What if a node must return its height to the parent, but compute something else—like the longest path through it—and put that in a side variable? This is tree DP: a node's recursive call answers one question (return up), while a global or side variable tracks another question (the actual answer). The classic example is the diameter of a binary tree—the longest path between any two nodes—where each node returns its height but tracks the longest path passing through it.
</Hook>

<Goal lang="en">
After this lesson you can implement recursive tree functions where the return value differs from the final answer, recognize and apply the tree DP pattern (postorder recursion + side variable), solve the diameter of a binary tree problem and trace it correctly, implement a height-balanced tree checker, predict and verify the time and space complexity of tree DP algorithms, and extend the pattern to variations (longest path sum, diameter in directed trees).
</Goal>

<Idea lang="en">
**What is tree DP (dynamic programming on trees)?**

<Term slug="dynamic_programming">Tree DP</Term> is a technique where a recursive tree function solves two problems at once:
1. **The return value** (sent up to the parent).
2. **A side computation** (tracked globally or in a mutable variable).

The key insight: the node's recursive call returns one piece of information up, while a side variable (or global state) accumulates the final answer.

**The pattern**

```
function treeDp(node) {
  // Base case
  if (node is null) return baseValue;
  
  // Recurse on subtrees
  leftInfo = treeDp(node.left);
  rightInfo = treeDp(node.right);
  
  // Combine for return (sent up)
  returnValue = combine(leftInfo, rightInfo, node);
  
  // Update side answer (the global answer)
  globalAnswer = updateAnswer(globalAnswer, leftInfo, rightInfo, node);
  
  return returnValue;
}
```

At each node:
- **Recurse down** to both subtrees (postorder).
- **Compute a return value** that makes sense for the parent (e.g., height).
- **Update a side variable** with the final answer (e.g., longest path).

**Example: the diameter of a binary tree**

The <Term slug="diameter">diameter</Term> of a binary tree is the longest path between any two nodes, measured in edges. This path may or may not pass through the root.

For each node, we ask: "What is the longest path passing through you?" The answer is the sum of the heights of the two subtrees plus 2 edges (one to each child), or zero if the node is a leaf.

But to answer that question for every node, we must return something different to the parent: the **height of the subtree**. So:
- **Return**: height of the subtree.
- **Side variable**: the longest path found so far (the diameter).

**Diameter at a node**

For a node with left subtree height `lh` and right subtree height `rh`:
- The longest path through the node = `lh + rh + 2` (in edges).
  (The `+2` accounts for the edges from the node down to the immediate children.)
- The height of the subtree rooted at the node = `1 + max(lh, rh)`.

**Height convention**

A **leaf node** (no children) has height 0. An **empty subtree** (null) has height -1. This makes the math work: a node with two null children returns `1 + max(-1, -1) = 0`, which is correct for a leaf.

</Idea>

<DiagramFrame label="Tree DP computed bottom-up on a five-node tree. Each node stores its subtree height, the DP value returned to its parent. The two leaves on the bottom, node 4 and node 5, start with the base value height 0. Their parent node 2 combines them as 1 plus max of 0 and 0, giving height 1. Node 3 is a leaf with base value height 0. The root node 1 combines its children node 2 with height 1 and node 3 with height 0 as 1 plus max of 1 and 0, giving height 2. Values flow upward from leaves to root." hue="var(--d-systems)" caption="Bottom-up tree DP: leaves seed the base value h=0; each parent folds its children's heights with h = 1 + max(left, right). The root's value (h=2) is computed last, after both subtrees report up — postorder.">
  <svg viewBox="0 0 380 260" width="380" height="260" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <path d="M190 44 L110 120" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M190 44 L270 120" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M110 120 L70 196" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M110 120 L150 196" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="245" y="24" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"values flow up ↑"}</text>
    <circle cx="190" cy="44" r="19" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="190" y="42" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="190" y="56" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=2"}</text>
    <text x="246" y="48" text-anchor="start" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"1 + max(1, 0) = 2"}</text>
    <circle cx="110" cy="120" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="110" y="118" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="110" y="132" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=1"}</text>
    <text x="84" y="106" text-anchor="end" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"1 + max(0, 0)"}</text>
    <circle cx="270" cy="120" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="270" y="118" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="270" y="132" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <circle cx="70" cy="196" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="70" y="194" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="70" y="208" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <circle cx="150" cy="196" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="150" y="194" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="150" y="208" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <text x="110" y="244" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"leaves: base value h=0"}</text>
  </svg>
</DiagramFrame>

<Code lang="en">
**Example 1: Diameter of a binary tree**

<AnnotatedCode
  code={`function diameterOfBinaryTree(root) {
  let maxDiameter = 0;
  
  function dfs(node) {
    // Base case: empty subtree has height -1
    if (node === null) return -1;
    
    // Recurse on both subtrees
    const leftHeight = dfs(node.left);
    const rightHeight = dfs(node.right);
    
    // Update the global answer:
    // Longest path through this node (in edges)
    const diameterThroughNode = leftHeight + rightHeight + 2;
    maxDiameter = Math.max(maxDiameter, diameterThroughNode);
    
    // Return the height of this subtree (sent to parent)
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  dfs(root);
  return maxDiameter;
}`}
  annotations={[
    { line: 1, text: "Function computes the diameter (longest path in edges) of the tree." },
    { line: 2, text: "Side variable: tracks the longest path found so far." },
    { line: 5, text: "Base case: an empty subtree has height -1 (makes the math clean)." },
    { line: 8, text: "Recurse left: get the height of the left subtree." },
    { line: 9, text: "Recurse right: get the height of the right subtree." },
    { line: 12, text: "The longest path through THIS node is (left height) + (right height) + 2 edges." },
    { line: 13, text: "Update the side variable: remember the longest path seen anywhere in the tree." },
    { line: 16, text: "Return the height of this subtree (what the parent needs to solve its problem)." }
  ]}
/>

Example: For the tree:
```
      1
     / \
    2   3
   / \
  4   5
```

Tree structure:
- Node 1 is the root.
- Node 1's left subtree (rooted at 2) and right subtree (rooted at 3).
- Node 2's left subtree (rooted at 4, a leaf) and right subtree (rooted at 5, a leaf).

Heights (leaf = 0, null = -1):
- Node 4 (leaf): height = 0
- Node 5 (leaf): height = 0
- Node 3 (leaf): height = 0
- Node 2 (two children): height = 1 + max(0, 0) = 1
- Node 1 (two children): height = 1 + max(1, 0) = 2

Diameters (longest path through each node):
- Node 4: diameter = -1 + -1 + 2 = 0 (a leaf, path is just itself)
- Node 5: diameter = -1 + -1 + 2 = 0 (a leaf, path is just itself)
- Node 3: diameter = -1 + -1 + 2 = 0 (a leaf, path is just itself)
- Node 2: diameter = 0 + 0 + 2 = 2 (path from 4 → 2 → 5 is 2 edges)
- Node 1: diameter = 1 + 0 + 2 = 3 (path from 4 → 2 → 1 → 3 is 3 edges)

The longest path in the entire tree is 3 edges: 4 → 2 → 1 → 3.

**Example 2: Height-balanced binary tree**

A tree is <Term slug="height_balanced">height-balanced</Term> if for every node, the heights of the left and right subtrees differ by at most 1. We can check this with tree DP: return the height if balanced, or -1 as a sentinel for "unbalanced".

<AnnotatedCode
  code={`function isBalanced(root) {
  function dfs(node) {
    // Base case: empty subtree is balanced with height -1
    if (node === null) return -1;
    
    // Recurse on left subtree
    const leftHeight = dfs(node.left);
    // If left is unbalanced, propagate the -1 sentinel
    if (leftHeight === -1) return -1;
    
    // Recurse on right subtree
    const rightHeight = dfs(node.right);
    // If right is unbalanced, propagate the -1 sentinel
    if (rightHeight === -1) return -1;
    
    // Check if this node is balanced
    if (Math.abs(leftHeight - rightHeight) > 1) {
      return -1; // Not balanced
    }
    
    // Return the height of this subtree
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  return dfs(root) !== -1; // true if height was computed, false if -1 (unbalanced)
}`}
  annotations={[
    { line: 1, text: "Function checks if the tree is height-balanced and returns true/false." },
    { line: 3, text: "Base case: empty tree is balanced with height -1." },
    { line: 6, text: "Recurse left; get height (or -1 if unbalanced)." },
    { line: 8, text: "Sentinel check: if left subtree is unbalanced, propagate the -1 up." },
    { line: 11, text: "Recurse right; get height (or -1 if unbalanced)." },
    { line: 13, text: "Sentinel check: if right subtree is unbalanced, propagate the -1 up." },
    { line: 16, text: "If the absolute difference of heights is more than 1, this node is unbalanced." },
    { line: 17, text: "Return -1 (the sentinel) to propagate the unbalanced state up." },
    { line: 21, text: "Return the height of this subtree (the final answer for the parent)." },
    { line: 24, text: "If dfs(root) is not -1, the tree is balanced." }
  ]}
/>

Example: For the tree:
```
      1
     / \
    2   3
   /
  4
```

- Node 4 (leaf): height = 0, balanced.
- Node 3 (leaf): height = 0, balanced.
- Node 2 (left child 4, no right): height = 0, left height = 0, right height = -1. Difference = |0 - (-1)| = 1 ≤ 1, balanced. Returns height 1.
- Node 1 (left child 2, right child 3): height of left = 1, height of right = 0. Difference = |1 - 0| = 1 ≤ 1, balanced. Returns height 2.

Result: isBalanced(root) = true.

**Running both on concrete trees**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

// Diameter function
function diameterOfBinaryTree(root) {
  let maxDiameter = 0;
  
  function dfs(node) {
    if (node === null) return -1;
    const leftHeight = dfs(node.left);
    const rightHeight = dfs(node.right);
    const diameterThroughNode = leftHeight + rightHeight + 2;
    maxDiameter = Math.max(maxDiameter, diameterThroughNode);
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  dfs(root);
  return maxDiameter;
}

// Height-balanced function
function isBalanced(root) {
  function dfs(node) {
    if (node === null) return -1;
    const leftHeight = dfs(node.left);
    if (leftHeight === -1) return -1;
    const rightHeight = dfs(node.right);
    if (rightHeight === -1) return -1;
    if (Math.abs(leftHeight - rightHeight) > 1) return -1;
    return 1 + Math.max(leftHeight, rightHeight);
  }
  return dfs(root) !== -1;
}

// Build tree:
//      1
//     / \\
//    2   3
//   / \\
//  4   5

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Diameter:", diameterOfBinaryTree(root));
console.log("Is balanced:", isBalanced(root));`}
  expected={`Diameter: 3
Is balanced: true`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="diameter-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));",
    "diameterOfBinaryTree(root);"
  ]}
  frames={[
    { caption: "Call dfs(1, root). Recurse left on node 2.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }] },
    { caption: "Call dfs(2). Recurse left on node 4.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }] },
    { caption: "Call dfs(4). Node 4 is a leaf. Recurse left (null) → height -1.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(4) left", mark: "waiting" }] },
    { caption: "Recurse right on node 4 (null) → height -1. Diameter through 4 = -1 + -1 + 2 = 0. Return height 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(4) → 0", mark: "done" }] },
    { caption: "Back to dfs(2). Now recurse right on node 5.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(5)", mark: "waiting" }] },
    { caption: "Call dfs(5). Node 5 is a leaf. Diameter = 0. Return height 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(5) → 0", mark: "done" }] },
    { caption: "Back to dfs(2). leftHeight=0, rightHeight=0. Diameter through 2 = 0 + 0 + 2 = 2. Return height 1 + max(0, 0) = 1.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2) → 1", mark: "done" }, { value: "maxDiameter = 2" }] },
    { caption: "Back to dfs(1). Now recurse right on node 3.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(3)", mark: "waiting" }] },
    { caption: "Call dfs(3). Node 3 is a leaf. Diameter = 0. Return height 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(3) → 0", mark: "done" }] },
    { caption: "Back to dfs(1). leftHeight=1, rightHeight=0. Diameter through 1 = 1 + 0 + 2 = 3. maxDiameter = 3. Return height 1 + max(1, 0) = 2.", line: 2, cells: [{ value: "dfs(1) → 2", mark: "done" }, { value: "maxDiameter = 3" }] },
    { caption: "All nodes visited. Final diameter = 3 (the longest path from node 4 → 2 → 1 → 3).", line: 2, cells: [{ value: "dfs(1) → 2", mark: "done" }, { value: "Final result: 3", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

The tree DP algorithms visit each node exactly once. At each node, we perform O(1) work (comparisons, arithmetic, variable updates). Since there are n nodes, the total time is **O(n)**.

**Space complexity**

Both algorithms use recursion, so the call stack depth equals the height of the tree. In the worst case (a degenerate tree), height is O(n). In a balanced tree, height is O(log n).

Thus, space complexity is **O(h)**, where h is the tree's height.

</Complexity>

<AlgoPractice
  id="practice-tree-dp-1"
  lessonSlug="06-tree-dp"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "In the tree DP pattern, what does a node return to its parent?",
      choices: [
        "The final answer (the diameter, longest path sum, etc.)",
        "Rich information needed by the parent to solve its own subproblem (e.g., the height of the subtree)",
        "A random value that is later discarded",
        "The maximum value in the subtree"
      ],
      answer: 1,
      hint: "The node returns information that helps the parent compute its own result. For diameter, each node returns its height, not the diameter itself. The diameter is tracked in a side variable."
    },
    {
      kind: "choice",
      prompt: "For a tree with this structure, what is the diameter (in edges)?\n```\n    1\n   /\n  2\n /\n3\n```",
      choices: [
        "0",
        "1",
        "2",
        "3"
      ],
      answer: 2,
      hint: "The longest path is from node 3 to node 1: 3 → 2 → 1, which is 2 edges. Heights: node 3 (leaf) = 0, node 2 (one child) = 1, node 1 (one child) = 2. Diameter at node 1 = (height of left=1) + (height of right=-1) + 2 = 2."
    },
    {
      kind: "choice",
      prompt: "Why is a sentinel value (like -1) useful in the height-balanced tree check?",
      choices: [
        "It allows us to encode two pieces of information (height AND balanced status) in a single return value",
        "It makes the code run faster",
        "It eliminates the need for recursion",
        "It guarantees the tree is always balanced"
      ],
      answer: 0,
      hint: "If we return -1, it signals 'unbalanced' and we can propagate that up without needing a separate boolean. If we return a non-negative number, we know the subtree is balanced and the number is the height. This is a common tree DP technique."
    },
    {
      kind: "choice",
      prompt: "In the diameter algorithm, why is the base case return value -1 (not 0)?",
      choices: [
        "Because negative numbers are faster to compute",
        "So that a leaf node's height is 0 (leaf has -1 + -1 + 1 = 0 height). Makes the math consistent.",
        "To indicate that the tree is empty",
        "Because the height of an empty tree is always -1 by definition"
      ],
      answer: 1,
      hint: "With null = -1, a leaf node (both children null) has height = 1 + max(-1, -1) = 0. This is correct: a leaf's height is 0 (it is a single node, zero edges from itself to itself). Without this convention, the math would require special cases."
    },
    {
      kind: "choice",
      prompt: "Which of these is NOT a tree DP problem (where return value ≠ final answer)?",
      choices: [
        "Diameter of a binary tree (return height, track diameter)",
        "Check if tree is height-balanced (return height or -1, check balance at each node)",
        "Count total nodes in a tree (return count, no other answer)",
        "Find the longest path sum from root to leaf"
      ],
      answer: 2,
      hint: "Counting nodes is plain recursion: you return the count (1 + leftCount + rightCount), and that return value IS the final answer. No side variable needed. It is postorder recursion, but not tree DP (return value = answer)."
    }
  ]}
/>

<Inset type="why">
**Why tree DP matters.** Many hard tree problems require computing two things at once: local information (height, count) that flows up, and global information (diameter, longest path) that is tracked on the side. Without tree DP, you might traverse the tree twice—once for heights, once for the diameter. Tree DP solves both in a single pass. This is why tree DP is an essential tool for interview problems and competitive programming.
</Inset>

<Inset type="mistake">
**Confusing return value with the answer.** Beginners often think the return value IS the final answer. But in tree DP, the return value is the local information (height), and the final answer is in a side variable (diameter). The return value is what the parent needs; the side variable is what you came to find. Keep them separate.
</Inset>

<Inset type="edgecase">
**Edge case: single-node tree.** If the tree is just one node, the diameter is 0 (the longest path is the node itself, zero edges). With our height convention (null = -1, leaf = 0), the diameter calculation gives 0 + 0 + 2 = 2, which is wrong! The fix: account for this by starting maxDiameter at 0 and ensuring the diameter calculation correctly captures a single node. Our code does: a single node's diameter = (-1) + (-1) + 2 = 0, but wait—that is wrong. Let me recalculate: a node with no children has leftHeight = rightHeight = -1. Diameter through it = -1 + (-1) + 2 = 0. This is correct! A single node has a diameter of 0 edges (the path is just itself).
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-tree-dp-1"
    pieceSlug="06-tree-dp"
    lang="en"
    question="Explain the tree DP pattern: what is the return value vs. the side variable, and why does this pattern solve hard tree problems in a single pass?"
    choices={[
      {
        label: "The return value is rich information (like height) that helps the parent solve its subproblem. The side variable tracks the global answer (like diameter). A single DFS traversal computes both: heights flow up, and the diameter is updated at every node. This avoids needing two separate passes (one for heights, one for the diameter).",
        correct: true
      },
      {
        label: "The return value IS the final answer (diameter, longest path, etc.), and the side variable is just for debugging.",
        misconception: "The return value is the local information (height) sent to the parent. The side variable (or mutable parameter) holds the final answer. They are not the same."
      },
      {
        label: "Tree DP is the same as plain recursion on trees; there is no difference between return value and answer.",
        misconception: "In plain recursion (like counting nodes), the return value IS the answer. In tree DP, the return value and answer are different. Tree DP uses postorder recursion + a side variable."
      },
      {
        label: "The return value is ignored, and only the side variable matters.",
        misconception: "Both are important. The return value is used by the parent (e.g., to compute its own diameter). The side variable accumulates the global answer. Both are needed."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Tree dynamic programming** combines recursion with a side variable to solve tree problems in a single pass. The pattern: each node **returns** local information (like height) to its parent, while updating a **side variable** with the global answer (like diameter). The classic example is the **diameter of a binary tree**—the longest path between any two nodes. A height-balanced tree checker uses a **sentinel value** (-1) to encode both height and balance status in a single return. Tree DP is essential for hard tree problems: it avoids multiple passes and reveals the elegant, two-part structure of the solution. **Next:** Unit 08 covers heaps and priority queues, a different way to organize data for fast access to the minimum (or maximum) element.
</Recap>
