---
concepts:
- level-order
- breadth-first
- BFS
- queue
- FIFO
- visit
- traversal
estMin: 22
lang: en
mathPrereqs: []
order: 3
prereqs:
- 01-the-binary-tree
- 02-traversals
slug: 03-level-order-traversal
sources:
- https://www.geeksforgeeks.org/dsa/level-order-tree-traversal/
- https://www.enjoyalgorithms.com/blog/level-order-traversal-of-binary-tree/
- https://neetcode.io/problems/level-order-traversal-of-binary-tree/question
- https://www.geeksforgeeks.org/dsa/level-order-traversal-line-line-set-3-using-one-queue/
status: ready
summary: 'Visit nodes level by level from top to bottom, left to right, using a queue.
  The breadth-first alternative to depth-first recursion. Why queues work for level-order,
  the basic loop, and the key variant: track queue size at level start to process
  nodes by depth.'
title: Level-order traversal (BFS)
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You know three ways to walk a tree recursively—pre-order, in-order, post-order—all exploring deeply down one branch before coming back. But what if you need to see all nodes at depth 2 before any at depth 3? What if you need nodes grouped by level? The depth-first traversals cannot answer that. You need **breadth-first search**: a queue-based traversal that visits nodes level by level, left to right, from the root down. This is level-order traversal, and it unlocks problems where the position and depth of each node matter.
</Hook>

<Goal lang="en">
After this lesson you can implement level-order traversal using a queue, trace the algorithm on a concrete tree, explain why a queue produces level order while recursion produces depth order, implement the "process one level at a time" variant by tracking queue size, and choose level-order traversal for problems that need to understand the tree layer by layer.
</Goal>

<Idea lang="en">
**What is level-order traversal?**

**Level-order traversal** (also called **breadth-first search** or **BFS**) visits every node in a tree layer by layer, top to bottom, left to right. Unlike the recursive depth-first traversals you saw in the last lesson, level-order uses a **queue**—a first-in, first-out container—to control the visiting order.

**Why a queue produces level order**

The magic of level-order is the queue's FIFO property. When you process a node and add its children to the queue, the children go to the back of the queue. All nodes at the current level are still in the queue, ahead of their children. So you process all nodes at level 1, then all at level 2, and so on—you never skip ahead to a deeper level until the current level is exhausted.

Contrast this with recursion: when you call `preOrder(node.left)`, you immediately dive deep into the left subtree before ever seeing the right child. Recursion is depth-first. A queue is breadth-first.

**The basic level-order loop**

1. Create a queue and add the root.
2. While the queue is not empty:
   - Dequeue a node.
   - Process it (record its value).
   - Enqueue its left child (if it exists).
   - Enqueue its right child (if it exists).
3. Done.

**Processing one level at a time**

Often you want to group nodes by level—all level-1 nodes together, all level-2 nodes together. The trick: at the start of each level, record the queue's size. That number tells you exactly how many nodes are at the current level. Iterate that many times, dequeuing and enqueueing children. When you've iterated `size` times, all the children (the next level) are now in the queue, and you loop again.

This variant is essential for problems like "return the level order traversal as a list of lists" or "find the width of each level."

<DiagramFrame
  label="A binary tree drawn over three horizontal level bands. Level 0 holds the root (1). Level 1 holds its two children, 2 on the left and 3 on the right. Level 2 holds node 4, the left child of 2. BFS sweeps the bands top to bottom and, within each band, left to right, driven by a FIFO queue: it dequeues a node, then enqueues that node's children at the back. Visiting 1 enqueues 2 then 3; visiting 2 enqueues 4. The dequeue order, and therefore the level-order sequence, is 1, 2, 3, 4."
  hue="var(--d-systems)"
  caption="A node enters the queue when its parent is visited, so a whole level is dequeued before any of its children. The FIFO order makes the sweep go level by level, left to right: 1, 2, 3, 4."
>
  <svg viewBox="0 0 560 340" width="560" height="340" font-family="var(--font-body)">
    <rect x="16" y="44" width="372" height="72" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.6" />
    <rect x="16" y="132" width="372" height="72" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.6" />
    <rect x="16" y="220" width="372" height="72" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.6" />
    <text x="28" y="38" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"level 0"}</text>
    <text x="28" y="126" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"level 1"}</text>
    <text x="28" y="214" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"level 2"}</text>
    <line x1="202" y1="104" x2="142" y2="156" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="202" y1="104" x2="262" y2="156" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="142" y1="192" x2="110" y2="244" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <circle cx="202" cy="80" r="22" fill="var(--card)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="202" y="85" text-anchor="middle" font-size="15" fill="var(--ink)">1</text>
    <circle cx="142" cy="168" r="22" fill="var(--card)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="142" y="173" text-anchor="middle" font-size="15" fill="var(--ink)">2</text>
    <circle cx="262" cy="168" r="22" fill="var(--card)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="262" y="173" text-anchor="middle" font-size="15" fill="var(--ink)">3</text>
    <circle cx="110" cy="256" r="22" fill="var(--card)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="110" y="261" text-anchor="middle" font-size="15" fill="var(--ink)">4</text>
    <text x="206" y="38" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink-2)" text-anchor="middle">{"visit 1 -> enqueue 2, 3"}</text>
    <text x="206" y="126" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink-2)" text-anchor="middle">{"visit 2 -> enqueue 4 ; visit 3"}</text>
    <text x="206" y="214" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink-2)" text-anchor="middle">{"visit 4"}</text>
    <line x1="420" y1="44" x2="420" y2="292" stroke="var(--hairline)" stroke-width="0.8" />
    <text x="490" y="38" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"FIFO queue"}</text>
    <text x="490" y="62" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">{"front -> back"}</text>
    <rect x="432" y="76" width="116" height="26" rx="4" fill="var(--card)" stroke="var(--accent)" stroke-width="1.2" />
    <text x="490" y="93" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"[ 1 ]"}</text>
    <rect x="432" y="120" width="116" height="26" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="490" y="137" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"[ 2, 3 ]"}</text>
    <rect x="432" y="164" width="116" height="26" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="490" y="181" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"[ 3, 4 ]"}</text>
    <rect x="432" y="208" width="116" height="26" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="490" y="225" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"[ 4 ]"}</text>
    <text x="288" y="320" text-anchor="middle" font-size="12.5" font-family="var(--font-mono)" fill="var(--accent)">{"level-order: 1, 2, 3, 4"}</text>
  </svg>
</DiagramFrame>

</Idea>

<Code lang="en">
**Basic level-order traversal**

The simplest form: enqueue the root, then dequeue, process, and enqueue children until the queue is empty.

<AnnotatedCode
  code={`function levelOrder(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root]; // Start with the root
  
  while (queue.length > 0) {
    const node = queue.shift(); // Dequeue from the front
    
    // Process the node
    result.push(node.value);
    
    // Enqueue children
    if (node.left !== null) queue.push(node.left);
    if (node.right !== null) queue.push(node.right);
  }
  
  return result;
}`}
  annotations={[
    { line: 1, text: "Function takes the root of the tree." },
    { line: 2, text: "If the tree is empty (null root), return an empty result." },
    { line: 5, text: "Use an array as a queue. Start with the root." },
    { line: 7, text: "Loop until the queue is empty." },
    { line: 8, text: "Dequeue (remove) the front node." },
    { line: 11, text: "Process the node: record its value." },
    { line: 14, text: "Enqueue the left child if it exists." },
    { line: 15, text: "Enqueue the right child if it exists." },
    { line: 18, text: "Return the result: nodes in level-order." }
  ]}
/>

Example: For the tree:
```
    1
   / \
  2   3
 /
4
```

Level-order visits in this order: **1 → 2 → 3 → 4**.

**Level-order with grouping (one level at a time)**

To return nodes grouped by level, capture the queue's size at the start of each iteration. That size is the count of nodes at the current level.

<AnnotatedCode
  code={`function levelOrderByLevel(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const levelSize = queue.length; // How many nodes at this level
    const currentLevel = []; // Nodes of this level
    
    // Process all nodes at the current level
    for (let i = 0; i < levelSize; i++) {
      const node = queue.shift(); // Dequeue from the front
      currentLevel.push(node.value); // Record the node's value
      
      // Enqueue children for the next level
      if (node.left !== null) queue.push(node.left);
      if (node.right !== null) queue.push(node.right);
    }
    
    // After processing levelSize nodes, the queue now holds only the next level
    result.push(currentLevel);
  }
  
  return result;
}`}
  annotations={[
    { line: 1, text: "Function returns a list of lists: each inner list is one level." },
    { line: 2, text: "If the tree is empty, return an empty result." },
    { line: 5, text: "Use an array as a queue. Start with the root." },
    { line: 7, text: "Loop while the queue is not empty." },
    { line: 8, text: "Capture the queue's size—this is the count of nodes at the current level." },
    { line: 11, text: "For loop from 0 to levelSize: process exactly that many nodes." },
    { line: 12, text: "Dequeue a node from the front." },
    { line: 13, text: "Record its value in the current level's list." },
    { line: 16, text: "Enqueue its children; they will be processed in the next iteration." },
    { line: 20, text: "After the for loop, add the current level's nodes to the result." }
  ]}
/>

Example: For the same tree:
```
    1
   / \
  2   3
 /
4
```

Result: **[[1], [2, 3], [4]]**—each level as a separate list.

**Running both on a concrete tree**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function levelOrder(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const node = queue.shift();
    result.push(node.value);
    
    if (node.left !== null) queue.push(node.left);
    if (node.right !== null) queue.push(node.right);
  }
  
  return result;
}

function levelOrderByLevel(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const levelSize = queue.length;
    const currentLevel = [];
    
    for (let i = 0; i < levelSize; i++) {
      const node = queue.shift();
      currentLevel.push(node.value);
      
      if (node.left !== null) queue.push(node.left);
      if (node.right !== null) queue.push(node.right);
    }
    
    result.push(currentLevel);
  }
  
  return result;
}

// Build the tree:
//      1
//     / \\
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Simple level-order:");
console.log(levelOrder(root));

console.log("\\nLevel-order by level:");
console.log(levelOrderByLevel(root));`}
  expected={`Simple level-order:
[ 1, 2, 3, 4 ]

Level-order by level:
[ [ 1 ], [ 2, 3 ], [ 4 ] ]`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="levelorder-trace-basic"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "levelOrder(root);"
  ]}
  frames={[
    { caption: "Start: queue = [1]. Dequeue 1, process it. Enqueue left (2) and right (3). Queue = [2, 3].", line: 2, cells: [{ value: "1", mark: "done" }] },
    { caption: "Queue = [2, 3]. Dequeue 2, process it. Enqueue left (4). Queue = [3, 4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }] },
    { caption: "Queue = [3, 4]. Dequeue 3, process it. No children. Queue = [4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }] },
    { caption: "Queue = [4]. Dequeue 4, process it. No children. Queue = [].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Queue is empty. Done. Visiting order: 1, 2, 3, 4.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="levelorder-trace-bylevel"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "levelOrderByLevel(root);"
  ]}
  frames={[
    { caption: "Start: queue = [1]. levelSize = 1. Dequeue 1, process it. Enqueue 2, 3. Queue = [2, 3]. Level 0 done: [1].", line: 2, cells: [{ value: "1", mark: "done" }] },
    { caption: "Queue = [2, 3]. levelSize = 2. Dequeue 2, process it. Enqueue 4. Queue = [3, 4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }] },
    { caption: "Dequeue 3, process it. No children. Queue = [4]. Level 1 done: [2, 3].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }] },
    { caption: "Queue = [4]. levelSize = 1. Dequeue 4, process it. No children. Queue = []. Level 2 done: [4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Queue is empty. Done. Result: [[1], [2, 3], [4]].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

Level-order traversal visits every node in the tree exactly once. Each node is dequeued once and enqueued once. All work done per node (processing, children checks) is O(1). Therefore, for a tree with n nodes, the time is **O(n)**.

**Space complexity**

The space used by the queue is the dominant factor. At any moment, the queue holds nodes from at most two consecutive levels. In the worst case, the maximum width of the tree—the largest number of nodes at any single level—determines the queue's maximum size.

For a tree with maximum width w, the space complexity is **O(w)**.

In a balanced binary tree, w is roughly n / 2 (the last level has many nodes), so O(w) is still better than the O(n) space used by recursion in the worst case. In a skewed tree (where every node has only one child), w = 1, so level-order uses **O(1)** space—far better than the O(n) recursion stack.

</Complexity>

<AlgoPractice
  id="practice-levelorder-1"
  lessonSlug="03-level-order-traversal"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "For the tree:\n    5\n   / \\\n  3   7\n / \\\n2   4\n\nWhat is the level-order traversal?",
      choices: [
        "5, 3, 7, 2, 4",
        "2, 3, 4, 5, 7",
        "5, 3, 2, 4, 7",
        "2, 4, 3, 7, 5"
      ],
      answer: 0,
      hint: "Level-order goes top-to-bottom, left-to-right. Visit 5 (root), then 3 and 7 (level 1), then 2 and 4 (level 2)."
    },
    {
      kind: "choice",
      prompt: "Why does a queue produce level-order while recursion produces depth-first?",
      choices: [
        "Queues are faster than recursion.",
        "A queue's FIFO order ensures all nodes at the current level are processed before their children. Recursion dives deep into one child before exploring the other.",
        "Recursion cannot process all nodes at the same level.",
        "Queues use less memory than recursion."
      ],
      answer: 1,
      hint: "Think about the order of operations. In a queue, you dequeue all level-1 nodes before any level-2 nodes enter the queue. In recursion, you call left subtree completely before calling right."
    },
    {
      kind: "choice",
      prompt: "In the level-order 'by level' variant, what does levelSize represent?",
      choices: [
        "The total number of nodes in the tree.",
        "The number of nodes in the entire queue.",
        "The number of nodes at the current level—how many times to dequeue and enqueue in this iteration.",
        "The depth of the tree."
      ],
      answer: 2,
      hint: "We capture queue.length at the start of each iteration. This tells us how many nodes are waiting—exactly the nodes of the current level."
    },
    {
      kind: "choice",
      prompt: "After processing levelSize nodes and enqueueing all their children, what does the queue contain?",
      choices: [
        "Nodes from the current and next levels mixed together.",
        "Only the nodes of the next level.",
        "Only leaf nodes.",
        "The queue is empty."
      ],
      answer: 1,
      hint: "You dequeue levelSize nodes (the current level). You enqueue all their children. Those children are all at the next level. So the queue now contains only the next level."
    },
    {
      kind: "choice",
      prompt: "For a tree with maximum width w (the most nodes at any level), what is the space complexity of level-order traversal?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(w)",
        "O(n)"
      ],
      answer: 2,
      hint: "The queue holds at most w nodes at any time. So space is O(w)."
    }
  ]}
/>

<Inset type="why">
**Why not recursion?** Recursion explores depth-first: it goes all the way down the left subtree before exploring the right. If you need to know the level of each node or group nodes by depth, recursion obscures that structure. A queue makes the level structure explicit: dequeue level k, enqueue level k+1.
</Inset>

<Inset type="practice">
**Trace the "by level" variant yourself.** Draw a tree with 3 levels. For the level-order 'by level' traversal, draw the queue state after each level is processed. Watch how levelSize = queue.length captures the current level's count. This is the core insight.
</Inset>

<Inset type="edgecase">
**Empty trees and single nodes.** If the root is null, return immediately (empty result). If the tree is a single node, the queue holds just that node, the loop runs once, and you're done. Both cases are handled by the algorithm without special code.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-levelorder-1"
    pieceSlug="03-level-order-traversal"
    lang="en"
    question="Why is level-order traversal useful even though both it and depth-first traversals visit all nodes?"
    choices={[
      {
        label: "Because level-order explicitly processes nodes by level. Problems that need to understand the tree's layer structure (width of each level, values grouped by depth, distance from root) are natural for level-order. Depth-first traversals obscure the level structure.",
        correct: true
      },
      {
        label: "Because level-order is always faster than depth-first traversals.",
        misconception: "Both have O(n) time complexity. The difference is which problems each solves naturally, not speed."
      },
      {
        label: "Because a queue is a data structure and recursion is not.",
        misconception: "Recursion is a technique using the call stack (a data structure). Both are valid; they suit different problems."
      },
      {
        label: "Because level-order can handle trees with cycles, but depth-first cannot.",
        misconception: "Trees have no cycles by definition. Neither traversal handles cycles; both are designed for acyclic structures."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Level-order traversal** (BFS) visits nodes layer by layer, top to bottom, left to right, using a queue's FIFO property. Unlike recursive depth-first traversals, level-order makes the tree's layer structure explicit. The basic algorithm: enqueue the root, loop while the queue is not empty, dequeue and process a node, then enqueue its children. To group nodes by level, capture the queue's size at the start of each iteration—that number tells you exactly how many nodes are at the current level. Time complexity is **O(n)** (visit each node once). Space complexity is **O(w)**, where w is the maximum width of the tree—better than O(n) recursion space in many cases. The next lesson covers recursion on trees: solving tree problems by combining results returned from subtrees.
</Recap>
