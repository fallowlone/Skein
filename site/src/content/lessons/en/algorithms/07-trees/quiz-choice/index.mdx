---
concepts:
- binary-tree
- traversal-order
- bst-invariant
- balance
- bfs-vs-dfs
- tree-dp
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://en.wikipedia.org/wiki/Tree_traversal
- https://en.wikipedia.org/wiki/Tree_(graph_theory)
status: ready
summary: "Multiple-choice synthesis across the trees unit — node structure, traversal choice, the BST invariant, balance and height, BFS vs DFS, and the tree-DP pattern."
title: 'Trees: multiple-choice review'
track: algorithms
unit: 07-trees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make when you reach for a tree in real code — not a definition to recite, but which traversal, which invariant, which complexity is actually in play.
</Hook>

<Goal lang="en">
Confirm you can connect node structure, traversal choice, the BST invariant, balance, BFS-vs-DFS, and the tree-DP pattern — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="07-trees-choice-1"
  lessonSlug="07-trees"
  lang="en"
  question="You must return the values of a binary search tree in ascending sorted order. Which single traversal does this directly, and why?"
  choices={[
    { label: "In-order (left, node, right). The BST invariant puts every smaller value in the left subtree and every larger value in the right, so visiting left-then-node-then-right emits values in non-decreasing order in one O(n) pass.", correct: true },
    { label: "Pre-order (node, left, right), because it visits the root first and the root is the median.", misconception: "The root of a BST is not the median — it is just the first value inserted. Pre-order emits the root before any left-subtree value, so the output is not sorted. Only in-order yields sorted order." },
    { label: "Level-order (BFS), because it visits the tree top to bottom in order.", misconception: "Level-order groups nodes by depth, not by value. A node at depth 1 can be smaller or larger than a node at depth 2. BFS order has no relation to sorted order in a BST." },
    { label: "Post-order (left, right, node), because the root comes last and is the maximum.", misconception: "The root is neither the minimum nor the maximum of a BST. Post-order emits both children before the node, so the sequence is not sorted." },
  ]}
/>

<Quiz
  id="07-trees-choice-2"
  lessonSlug="07-trees"
  lang="en"
  question="A teammate inserts the keys 1, 2, 3, 4, 5, 6, 7 into an empty BST in that exact order, then complains that search is slow. What happened, and what is the height?"
  choices={[
    { label: "Each key is larger than all before it, so every insert goes right: the tree degenerates into a right-leaning chain of 7 nodes with height 6. Search and insert are now O(n), not O(log n) — sorted-order insertion is the classic BST worst case.", correct: true },
    { label: "Nothing is wrong; a BST with 7 nodes always has height 3, so search is O(log n).", misconception: "Height depends on insertion order, not just node count. Only a balanced shape gives height ~log n; sorted insertion produces a chain of height n-1." },
    { label: "The BST invariant was violated, so the tree is corrupt.", misconception: "The invariant (left < node < right) holds perfectly — every node is larger than its parent. The tree is a valid BST; it is just degenerate, which is a balance problem, not an invariant violation." },
    { label: "Search is slow because in-order traversal is O(n).", misconception: "Search does not traverse the whole tree — it follows one root-to-target path. The slowness is the path length: in a height-6 chain that path is up to 7 nodes, i.e. O(n)." },
  ]}
/>

<Quiz
  id="07-trees-choice-3"
  lessonSlug="07-trees"
  lang="en"
  question="You need every node grouped by its depth — all of level 0, then all of level 1, and so on. Which approach fits, and what is the key implementation detail?"
  choices={[
    { label: "Level-order traversal (BFS) with a queue. Capture the queue's size at the start of each outer iteration; dequeue exactly that many nodes to finish one full level before any deeper node, enqueueing children as you go.", correct: true },
    { label: "Recursive in-order traversal, recording each node's depth as you go.", misconception: "In-order is depth-first: it dives down the left spine before touching the right child, so nodes come out interleaved across levels. You would have to bucket by depth afterwards — BFS produces level groups directly." },
    { label: "Pre-order traversal — it visits the root first, which is level 0.", misconception: "Visiting the root first does not give you level grouping. After the root, pre-order plunges into the entire left subtree (all depths) before seeing the right child. DFS cannot finish a level before going deeper." },
    { label: "BFS, but you can skip tracking the queue size — the order alone gives you the levels.", misconception: "A plain BFS gives the correct visit order but not the level boundaries. Without snapshotting the size at each level start you cannot tell where one level ends and the next begins." },
  ]}
/>

<Quiz
  id="07-trees-choice-4"
  lessonSlug="07-trees"
  lang="en"
  question="In the standard recurse-and-combine pattern for trees, what does the base case return for an empty subtree when computing height (with the convention that a leaf has height 0)?"
  choices={[
    { label: "-1. With null = -1, a leaf computes 1 + max(-1, -1) = 0, which is the correct leaf height. The -1 sentinel makes the combine step uniform with no special-casing for leaves.", correct: true },
    { label: "0, because an empty subtree obviously contains zero nodes.", misconception: "Height counts edges, not nodes. If null returned 0, a leaf would compute 1 + max(0, 0) = 1, over-counting by one. Counting nodes is a different problem with a different base case." },
    { label: "null, and the caller checks for it before combining.", misconception: "Returning null forces a special-case branch at every combine, which is exactly what the -1 (or 0-for-counts) numeric sentinel is designed to avoid. The point of the convention is uniform arithmetic." },
    { label: "1, so the leaf comes out as height 2.", misconception: "That double-counts. The whole purpose of choosing -1 for null is to make the leaf land at height 0 under the convention stated in the question." },
  ]}
/>

<Quiz
  id="07-trees-choice-5"
  lessonSlug="07-trees"
  lang="en"
  question="Computing the diameter of a binary tree (longest path between any two nodes), why does each recursive call return the subtree's height while the diameter is kept in a side variable?"
  choices={[
    { label: "The longest path through a node is leftHeight + rightHeight (plus the two edges to its children), but the parent cannot reuse that whole path — it can only extend one branch downward. So each node returns the height (what the parent needs) and updates a shared maximum with the through-node path (the real answer): the tree-DP pattern, one O(n) pass, return value distinct from the global answer.", correct: true },
    { label: "Each node should just return the diameter directly; the side variable is redundant.", misconception: "If a node returned the diameter, the parent could not extend it — a path through the parent uses one child's downward height, not the child's full diameter. The two quantities are different, which is exactly why the pattern splits them." },
    { label: "Returning the height is an optimization; you could equally do two separate passes.", misconception: "Two passes (one for heights, one for diameter) traverses the tree twice and re-derives heights you already computed. The whole point of tree DP is one pass: return local info up, accumulate the global answer on the side." },
    { label: "The diameter must always pass through the root, so only the root's value matters.", misconception: "The longest path can lie entirely within a subtree and never touch the root. That is precisely why the maximum must be updated at every node, not just at the root." },
  ]}
/>

<Quiz
  id="07-trees-choice-6"
  lessonSlug="07-trees"
  lang="en"
  question="Two engineers compare an in-order recursive traversal and a level-order (BFS) traversal of the same balanced tree with n nodes. What is the dominant extra-space cost of each?"
  choices={[
    { label: "In-order uses O(h) call-stack space (h = height, ~log n when balanced); BFS uses O(w) queue space where w is the maximum level width, which for a balanced tree is ~n/2 at the bottom level. So BFS can use far more memory on a wide tree even though both are O(n) time.", correct: true },
    { label: "Both use O(n) extra space because both visit n nodes.", misconception: "Visiting n nodes is the time cost, not the space cost. Recursion holds only the current root-to-node path (O(h)); BFS holds only the current frontier (O(w)). Neither holds all n nodes at once in the general case." },
    { label: "In-order uses O(1) space because recursion has no overhead.", misconception: "Recursion consumes the call stack — one frame per level on the active path, so O(h). On a degenerate (chain) tree that is O(n) and can overflow the stack." },
    { label: "BFS uses O(h) and in-order uses O(w); the two are simply swapped.", misconception: "It is the reverse: DFS depth is bounded by the height h (stack), and BFS breadth is bounded by the widest level w (queue). On a balanced tree h ~ log n while w ~ n/2." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one mapping: the shape of the data dictates the tool. A binary tree is a node with two subtrees, so recursion is the natural walk; the three DFS orders differ only in *when* the node is visited, and in-order is the one that reads a BST out sorted. The BST invariant buys O(h) search — but only balance keeps h near log n, and sorted-order insertion is the worst case that collapses it to O(n). BFS with a per-level size snapshot is the tool when depth matters, trading the stack for a queue. And tree DP returns local information up while accumulating the global answer on the side — one pass, two quantities.
</Recap>
