---
concepts:
- binary_tree
- bst_property
- ordering_invariant
- search
- insertion
- in_order_traversal
- balanced
- degenerate
- height
estMin: 26
lang: en
mathPrereqs: []
order: 5
prereqs:
- 01-the-binary-tree
- 02-traversals
- 03-level-order-traversal
- 04-recursion-on-trees
slug: 05-binary-search-trees
sources:
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://www.geeksforgeeks.org/dsa/complexity-different-operations-binary-tree-binary-search-tree-avl-tree/
- https://www.freecodecamp.org/news/binary-search-trees-bst-explained-with-examples/
- https://www.enjoyalgorithms.com/blog/insertion-in-binary-search-tree/
status: ready
summary: A binary tree where every value in the left subtree is less than the node
  and the right is greater. This ordering invariant makes search and insertion O(h).
  In-order traversal yields sorted order. Height ranges from O(log n) for balanced
  to O(n) for degenerate trees.
title: Binary search trees
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
So far, you have walked trees, built them, measured their height. But search in a tree—any tree—requires walking every branch. A binary search tree changes that. It orders values: smaller on the left, larger on the right. This property lets you search by comparing, going left or right, never backtracking. Search becomes O(h). And since a balanced binary search tree has height O(log n), you search in logarithmic time.
</Hook>

<Goal lang="en">
After this lesson you can identify the BST property (left subtree smaller, right subtree larger), implement BST search and insertion, trace both operations to verify correctness, explain why in-order traversal of a BST yields sorted order, predict the time complexity of search and insertion (O(h) where h is height), and recognize the risk that an unbalanced tree can degenerate to O(n) height.
</Goal>

<Idea lang="en">
**What is a binary search tree?**

A <Term slug="binary_search_tree">binary search tree (BST)</Term> is a binary tree with a property: for every node, all values in the left subtree are less than the node's value, and all values in the right subtree are greater than the node's value.

This **ordering invariant** means the tree encodes a sorted order. It also means you can search by comparing: if you are looking for a value smaller than the current node, go left; if larger, go right. If equal, you found it. Since you never backtrack, searching is fast.

**The BST property (left < node < right)**

For each node:
- All values in the left subtree < node's value.
- All values in the right subtree > node's value.
- Both left and right subtrees are also BSTs (the property applies recursively).

Example:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

Here, node 5 has left subtree {1, 3, 4} (all < 5) and right subtree {7, 8, 9} (all > 5). Node 3 has left subtree {1} (< 3) and right subtree {4} (> 3).

**Search in a BST**

To search for a value:
1. Start at the root.
2. Compare the target with the current node's value.
3. If equal, found it.
4. If target is smaller, recurse on the left subtree.
5. If target is larger, recurse on the right subtree.
6. If you reach a null, the value is not in the tree.

At each step, you eliminate half the remaining tree (or a large part of it, if the tree is not perfectly balanced). This gives O(h) comparisons, where h is the height.

**Insertion in a BST**

To insert a value:
1. Start at the root.
2. Compare the value with the current node.
3. If the value is smaller, recurse left; if larger, recurse right.
4. When you reach a null, that is where you attach the new leaf.

Insertion also takes O(h) time because you must find the insertion spot by comparing at each level.

**In-order traversal yields sorted order**

In-order traversal (left, current, right) of a BST visits nodes in ascending order. This is because:
- You visit the left subtree (all values smaller than the node).
- Then the node itself.
- Then the right subtree (all values larger than the node).

So the values appear in sorted order.

**Height is crucial**

The search and insertion times depend on the height h:
- **Balanced tree**: height is O(log n), so operations are O(log n).
- **Degenerate tree** (e.g., all left children): height is O(n), so operations are O(n).

If you insert values in sorted order [1, 2, 3, 4, 5], the tree degenerates to a linked list, and you lose the logarithmic-time benefit. Self-balancing trees (AVL, red-black) maintain balance, but they are beyond this lesson.

<DiagramFrame
  label="A binary search tree with root 5, left child 3, right child 8, and leaves 1, 4, 7, 9. The search for target 4 is highlighted as a path: at root 5 the comparison 4 < 5 goes left to node 3; at node 3 the comparison 4 > 3 goes right to node 4, where 4 equals 4 and the target is found. The path visits 3 of the 4 levels, illustrating that search costs O(height)."
  hue="var(--d-systems)"
  caption="Search for target 4. At each node one comparison decides the direction: 4 < 5 go left, 4 > 3 go right, 4 = 4 found. The BST invariant (left subtree < node < right subtree) means a wrong subtree never holds the target, so the path is bounded by the height — search is O(height)."
>
  <svg viewBox="0 0 640 340" width="640" height="340" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="bst-arrow-en" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="320" y="24" text-anchor="middle" font-size="13" font-weight="600" fill="var(--ink, currentColor)">target = 4</text>

    <line x1="320" y1="74" x2="190" y2="124" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#bst-arrow-en)" />
    <line x1="320" y1="74" x2="450" y2="124" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="180" y1="166" x2="120" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="180" y1="166" x2="240" y2="214" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#bst-arrow-en)" />
    <line x1="460" y1="166" x2="400" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="460" y1="166" x2="520" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />

    <text x="240" y="106" text-anchor="middle" font-size="13" font-weight="600" fill="var(--d-systems)">{"4 < 5"}</text>
    <text x="225" y="198" text-anchor="middle" font-size="13" font-weight="600" fill="var(--d-systems)">{"4 > 3"}</text>

    <circle cx="320" cy="50" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="320" y="56" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">5</text>

    <circle cx="180" cy="140" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="180" y="146" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">3</text>

    <circle cx="460" cy="140" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="460" y="146" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">8</text>

    <circle cx="110" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="110" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">1</text>

    <circle cx="250" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="250" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">4</text>
    <text x="250" y="288" text-anchor="middle" font-size="12" font-weight="600" fill="var(--d-systems)">{"4 = 4 found"}</text>

    <circle cx="390" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="390" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">7</text>

    <circle cx="530" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="530" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">9</text>

    <text x="612" y="146" text-anchor="end" font-size="11" fill="var(--muted, #888)">{"left subtree < 5 < right subtree"}</text>
  </svg>
</DiagramFrame>

</Idea>

<Code lang="en">
**Example 1: Searching for a value in a BST**

<AnnotatedCode
  code={`function search(node, target) {
  // Base case: value not found
  if (node === null) return false;
  
  // Check current node
  if (node.value === target) return true;
  
  // Recurse left if target is smaller
  if (target < node.value) {
    return search(node.left, target);
  }
  
  // Recurse right if target is larger
  return search(node.right, target);
}`}
  annotations={[
    { line: 1, text: "Search function: takes a node and a target value." },
    { line: 3, text: "Base case: if node is null, the value was not found." },
    { line: 6, text: "If the target equals the node's value, we found it." },
    { line: 9, text: "If target is smaller, it can only be in the left subtree (BST property)." },
    { line: 10, text: "Recurse on the left subtree." },
    { line: 13, text: "Otherwise, target must be larger, so recurse on the right subtree." }
  ]}
/>

Example: Search for 4 in the tree:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

- Start at 5. Target (4) < 5, go left.
- At 3. Target (4) > 3, go right.
- At 4. Target (4) == 4, return true.

Search took 3 comparisons.

**Example 2: Inserting a value into a BST**

<AnnotatedCode
  code={`function insert(node, value) {
  // Base case: attach new node here
  if (node === null) {
    return new Node(value);
  }
  
  // Recurse left if value is smaller
  if (value < node.value) {
    node.left = insert(node.left, value);
  }
  // Recurse right if value is larger
  else if (value > node.value) {
    node.right = insert(node.right, value);
  }
  // Ignore duplicates (optional; some BSTs allow them)
  
  return node;
}`}
  annotations={[
    { line: 1, text: "Insert function: add a value into the tree rooted at node." },
    { line: 3, text: "Base case: if node is null, create and return a new node with this value." },
    { line: 7, text: "If value is smaller than the current node, it belongs in the left subtree." },
    { line: 8, text: "Recursively insert into the left subtree and update the left child." },
    { line: 10, text: "If value is larger, it belongs in the right subtree." },
    { line: 12, text: "Recursively insert into the right subtree and update the right child." },
    { line: 16, text: "Return the (possibly updated) node." }
  ]}
/>

Example: Insert 6 into the tree:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

- Start at 5. Value (6) > 5, go right.
- At 8. Value (6) < 8, go left.
- At 7. Value (6) < 7, go left.
- At null. Create new node with value 6.

Result:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
      /
     6
```

Insertion also took 4 steps (comparisons + tree navigation).

**Running search and insert on a concrete tree**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function search(node, target) {
  if (node === null) return false;
  if (node.value === target) return true;
  if (target < node.value) return search(node.left, target);
  return search(node.right, target);
}

function insert(node, value) {
  if (node === null) return new Node(value);
  if (value < node.value) {
    node.left = insert(node.left, value);
  } else if (value > node.value) {
    node.right = insert(node.right, value);
  }
  return node;
}

function inorder(node, result = []) {
  if (node === null) return result;
  inorder(node.left, result);
  result.push(node.value);
  inorder(node.right, result);
  return result;
}

// Build the tree:
//      5
//     / \\
//    3   8
//   / \\ / \\
//  1  4 7  9

let node1 = new Node(1);
let node4 = new Node(4);
let node3 = new Node(3, node1, node4);
let node7 = new Node(7);
let node9 = new Node(9);
let node8 = new Node(8, node7, node9);
let root = new Node(5, node3, node8);

console.log("Search for 4:", search(root, 4));
console.log("Search for 6:", search(root, 6));

// Insert 6
root = insert(root, 6);
console.log("After inserting 6:");
console.log("In-order traversal:", inorder(root).join(", "));`}
  expected={`Search for 4: true
Search for 6: false
After inserting 6:
In-order traversal: 1, 3, 4, 5, 6, 7, 8, 9`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="bst-search-trace"
  lang="en"
  code={[
    "let root = new Node(5, new Node(3, new Node(1), new Node(4)), new Node(8, new Node(7), new Node(9)));",
    "search(root, 4);"
  ]}
  frames={[
    { caption: "Call search(root=5, target=4). Compare 4 with 5. 4 < 5, go left.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }] },
    { caption: "Call search(node=3, target=4). Compare 4 with 3. 4 > 3, go right.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }, { value: "search(3)", mark: "waiting" }] },
    { caption: "Call search(node=4, target=4). Compare 4 with 4. Found! Return true.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }, { value: "search(3)", mark: "waiting" }, { value: "search(4) → true", mark: "done" }] },
    { caption: "Return true up to search(3). It returns true to search(5).", line: 2, cells: [{ value: "search(5) → true", mark: "done" }, { value: "search(3) → true", mark: "done" }, { value: "search(4) → true", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="bst-insert-trace"
  lang="en"
  code={[
    "let root = new Node(5, new Node(3, new Node(1), new Node(4)), new Node(8, new Node(7), new Node(9)));",
    "insert(root, 6);"
  ]}
  frames={[
    { caption: "Call insert(root=5, value=6). Compare 6 with 5. 6 > 5, go right.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }] },
    { caption: "Call insert(node=8, value=6). Compare 6 with 8. 6 < 8, go left.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }] },
    { caption: "Call insert(node=7, value=6). Compare 6 with 7. 6 < 7, go left.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7)", mark: "waiting" }] },
    { caption: "Call insert(node=null, value=6). Base case: create new node(6). Return new Node(6).", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7)", mark: "waiting" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Return to insert(7): attach the new node as the left child of 7. Return updated 7.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Return to insert(8): attach the updated left subtree. Return updated 8.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8) → updated", mark: "done" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Return to insert(5): attach the updated right subtree. Return root.", line: 2, cells: [{ value: "insert(5) → done", mark: "done" }, { value: "insert(8) → updated", mark: "done" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity: search and insert**

Both search and insert operations traverse a path from the root to either a found node (search) or a leaf (insert). The number of comparisons equals the depth of that path, which is at most the height h of the tree.

- **Best case (balanced tree)**: Height is O(log n), so both search and insert are O(log n).
- **Average case (random insertions)**: Height is approximately O(log n), so both are O(log n).
- **Worst case (degenerate tree)**: Height is O(n) (tree is a linked list), so both are O(n).

For the operations in this lesson's code (search and insert), the time complexity is **O(h)**, where h is the height.

**Space complexity**

Both search and insert use recursion, so the call stack depth is O(h). In-order traversal also uses O(h) recursion depth.

</Complexity>

<AlgoPractice
  id="practice-bst-1"
  lessonSlug="05-binary-search-trees"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "Which of these trees satisfies the BST property?",
      choices: [
        "A tree with root 10, left child 5, right child 15, and 5's right child is 12.",
        "A tree with root 10, left child 15, and right child 5.",
        "A tree with root 10, left child 8, right child 12, and 8's right child is 9.",
        "A tree with root 10, left child 8, right child 12, and 12's right child is 11."
      ],
      answer: 2,
      hint: "The BST property requires that ALL values in the left subtree are less than the node, and ALL values in the right subtree are greater. In option 0 (root 10, left 5, right 15, left-right-child 12): 12 is in the left subtree but 12 > 10, which violates the BST property for the root. Option 1 has 15 on the left (15 > 10), violating the property. Option 3 has 11 as the right child of 12, but 11 < 12, so it should be on the left. Option 2 (root 10, left 8, right 12, 8's right child 9) satisfies the BST: 8 < 10 < 12, and 8 < 9 < 10."
    },
    {
      kind: "choice",
      prompt: "You search for value 6 in this BST. At each step, how many nodes do you compare?",
      choices: [
        "1 (you find it immediately)",
        "The number of nodes on the path from root to the node containing 6",
        "All nodes in the tree",
        "log(n) nodes, always"
      ],
      answer: 1,
      hint: "Each comparison tells you to go left or right. If you are at the root and target is smaller, you go left without checking the right subtree. You compare only nodes on the path from root to the target (or to a null, if the target is not found)."
    },
    {
      kind: "choice",
      prompt: "What does in-order traversal of a BST produce?",
      choices: [
        "A random order of values",
        "The values in ascending order (sorted)",
        "The values in descending order",
        "The values grouped by depth"
      ],
      answer: 1,
      hint: "In-order traversal visits left subtree, then current node, then right subtree. Since left subtree values are smaller and right subtree values are larger, the nodes are visited in ascending order."
    },
    {
      kind: "choice",
      prompt: "A BST is built by inserting values in this order: [1, 2, 3, 4, 5]. What is the height of the resulting tree?",
      choices: [
        "0 (it is a single node)",
        "2",
        "4 (the tree is a linked list: height = 5 - 1)",
        "log(5), approximately 2.3"
      ],
      answer: 2,
      hint: "When values are inserted in sorted order, each new value is larger than the previous root, so it goes to the right. Node 1 is the root, 2 is 1's right child, 3 is 2's right child, etc. The tree is a chain: 1 → 2 → 3 → 4 → 5. A chain of 5 nodes has height 4 (4 edges)."
    },
    {
      kind: "choice",
      prompt: "In the worst case (degenerate BST), what is the time complexity of searching for a value?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(n)",
        "O(n²)"
      ],
      answer: 2,
      hint: "In a degenerate tree, every node has only one child (like a linked list). To search for a value, you might have to check every node, so the worst case is O(n)."
    }
  ]}
/>

<Inset type="why">
**Why BSTs?** Unordered binary trees let you traverse, but not search efficiently. If you want to search without examining every node, you need structure: an ordering. The BST property gives you that structure. At each node, you know which subtree to explore, so you halve the problem (in a balanced tree) at each step.
</Inset>

<Inset type="edgecase">
**Degenerate BSTs.** If you insert values in sorted order, the BST degenerates into a linked list. Height becomes O(n), and you lose the logarithmic benefit. This is why self-balancing trees (AVL, red-black) exist—they maintain balance. But for this lesson, just recognize that height matters.
</Inset>

<Inset type="mistake">
**Confusing BST with sorted order guarantee.** A BST does NOT guarantee that a standard tree traversal (like level-order) produces sorted order. Only in-order traversal produces sorted order. If you want to sort a sequence, you build a BST and in-order traverse it. But a BST is not primarily a sorting algorithm—it is a dynamic data structure for fast search.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-bst-1"
    pieceSlug="05-binary-search-trees"
    lang="en"
    question="What is the BST property and why does it enable fast search?"
    choices={[
      {
        label: "For every node, all values in the left subtree are less than the node, and all values in the right subtree are greater. At each step, a comparison tells you which subtree contains the target (if it exists), so you never explore unnecessary parts of the tree. This reduces search to O(h), where h is the tree's height.",
        correct: true
      },
      {
        label: "The BST property guarantees that the tree is balanced and search is always O(log n).",
        misconception: "The BST property alone does not guarantee balance. A degenerate BST can have height O(n). Only self-balancing BSTs (AVL, red-black) guarantee O(log n) operations."
      },
      {
        label: "The BST property is that the tree is sorted in level-order (breadth-first).",
        misconception: "The BST property is about the node-child relationship, not the traversal order. In-order traversal (not level-order) of a BST yields sorted order."
      },
      {
        label: "The BST property means that every value appears only once in the tree.",
        misconception: "The BST property is about the ordering of values, not uniqueness. A BST can allow duplicates (stored in one of the subtrees) or forbid them, depending on the implementation."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Binary search trees** organize data with an ordering property: left subtree < node < right subtree. This property enables **fast search** and **insertion** in **O(h)** time, where h is the tree's height. For a balanced tree, h is O(log n), making operations logarithmic. **In-order traversal** of a BST yields values in sorted order—a consequence of the ordering property. The critical caveat: if the tree is unbalanced (degenerate), height becomes O(n), and all operations degrade. **Self-balancing trees** (AVL, red-black) maintain balance automatically, but they are significantly more complex and beyond the scope of this lesson. The next lesson covers **tree dynamic programming**—techniques for solving recursive tree problems with memoization.
</Recap>
