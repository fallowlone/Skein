---
slug: 04-recursion-on-trees
lang: en
track: algorithms
unit: 07-trees
order: 4
title: "Recursion on trees"
summary: "Solve tree problems by asking the same question of both subtrees, then combining their answers with the current node's value. The base case is an empty subtree. Two worked examples: computing height and counting nodes. The mental model: return rich information up; recurse down."
estMin: 24
status: ready
prereqs: ["01-the-binary-tree", "02-traversals", "03-level-order-traversal"]
mathPrereqs: []
concepts: ["recursion", "tree", "subtree", "base_case", "return_value", "combine", "height", "postorder"]
sources:
  - https://www.techiedelight.com/calculate-height-binary-tree-iterative-recursive/
  - https://www.enjoyalgorithms.com/blog/find-height-of-a-binary-tree/
  - https://algocademy.com/blog/how-to-handle-recursive-tree-problems-a-comprehensive-guide/
  - https://medium.com/@shaswata.ssaha/understanding-recursive-tree-traversal-in-counting-nodes-9fe44e3511b7
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
You know three ways to walk a tree: pre-order, in-order, post-order. But walking is not solving. To solve a tree problem—find the height, count the nodes, check if it is balanced—you need to think like a tree. A tree is a node with two subtrees. So to solve for the node, ask the same question of both subtrees, then combine their answers with the node's own value. This is recursion on trees, and it is the most powerful tool you have for tree problems.
</Hook>

<Goal lang="en">
After this lesson you can write recursive functions to solve tree problems by working bottom-up (postorder), recognize and implement the "recurse on subtrees and combine" pattern, trace a recursive tree function to verify it is correct, explain why returning a value from each subtree is the key to solving the problem, and choose the right base case (empty subtree) and combination rule for your problem.
</Goal>

<Idea lang="en">
**What does "recursion on trees" mean?**

**Recursion on trees** is a way to solve problems where you:
1. Solve the problem for the left subtree (recursively).
2. Solve the problem for the right subtree (recursively).
3. Combine the two answers with information about the current node.
4. Return the combined result up the call stack.

The key insight: a tree is just a node with two subtrees. So assume your function already works on the subtrees, and build the answer for the current node from those results.

**Base case: the empty subtree**

Every recursive tree function needs a base case. In tree problems, the base case is always when the subtree is null (empty). At that point, you return a value that makes sense for your problem—often 0 for counts, or -1 for heights.

**Combining results: postorder thinking**

The order in which you visit the tree matters. To combine results from the subtrees with the current node, you must:
1. Recursively get the left result.
2. Recursively get the right result.
3. Combine them with the current node.

This is **postorder**: left subtree, right subtree, then the node itself.

**The mental model**

Think of the recursion as messages flowing UP the tree:
- Each leaf sends up its base-case value.
- Each internal node asks both children "what is the answer for your subtree?", then combines the answers and sends the result up to its parent.
- The root returns the answer for the entire tree.

</Idea>

<Code lang="en">
**Example 1: Computing the height of a tree**

The **height** of a tree is the longest path from root to any leaf.

The recursive idea: height of a node = 1 + max(height of left subtree, height of right subtree).

<AnnotatedCode
  code={`function height(node) {
  // Base case: empty tree
  if (node === null) return -1;
  
  // Recursive case: ask both subtrees for their heights
  const leftHeight = height(node.left);
  const rightHeight = height(node.right);
  
  // Combine: take the max and add 1
  return 1 + Math.max(leftHeight, rightHeight);
}`}
  annotations={[
    { line: 1, text: "Function computes the height of the subtree rooted at node." },
    { line: 3, text: "Base case: an empty tree has height -1. This makes the math work: a single node's height = 1 + max(-1, -1) = 0." },
    { line: 6, text: "Recursively ask the left subtree for its height." },
    { line: 7, text: "Recursively ask the right subtree for its height." },
    { line: 10, text: "Combine: the height of this node is 1 plus the taller subtree." }
  ]}
/>

Example: For the tree:
```
      1
     / \
    2   3
   /
  4
```

- `height(4)` is a leaf: its two null children each return -1, so it returns 1 + max(-1, -1) = 0. Node 4 has height 0.
- `height(2)` asks for height(4) and height(null), gets 0 and -1, returns 1 + max(0, -1) = 1. Node 2 has height 1.
- `height(3)` is a leaf: returns 1 + max(-1, -1) = 0. Node 3 has height 0.
- `height(1)` asks for height(2) and height(3), gets 1 and 0, returns 1 + max(1, 0) = 2. The tree has height 2.

**Example 2: Counting nodes in a tree**

The **count** of nodes: count = 1 (for this node) + count of left subtree + count of right subtree.

<AnnotatedCode
  code={`function countNodes(node) {
  // Base case: empty tree has 0 nodes
  if (node === null) return 0;
  
  // Recursive case: ask both subtrees for their node counts
  const leftCount = countNodes(node.left);
  const rightCount = countNodes(node.right);
  
  // Combine: 1 (current node) + left + right
  return 1 + leftCount + rightCount;
}`}
  annotations={[
    { line: 1, text: "Function returns the number of nodes in the subtree rooted at node." },
    { line: 3, text: "Base case: an empty tree has 0 nodes." },
    { line: 6, text: "Recursively count nodes in the left subtree." },
    { line: 7, text: "Recursively count nodes in the right subtree." },
    { line: 10, text: "Combine: add 1 for the current node, plus all nodes in both subtrees." }
  ]}
/>

Example: For the same tree:
- `countNodes(4)` is a leaf: its null children each return 0, so it returns 1 + 0 + 0 = 1.
- `countNodes(2)` asks for countNodes(4) and countNodes(null), gets 1 and 0, returns 1 + 1 + 0 = 2. Subtree rooted at 2 has 2 nodes.
- `countNodes(3)` returns 1 + 0 + 0 = 1. Subtree rooted at 3 has 1 node.
- `countNodes(1)` asks for countNodes(2) and countNodes(3), gets 2 and 1, returns 1 + 2 + 1 = 4. The entire tree has 4 nodes.

**Running both functions on a concrete tree**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function height(node) {
  if (node === null) return -1;
  const leftHeight = height(node.left);
  const rightHeight = height(node.right);
  return 1 + Math.max(leftHeight, rightHeight);
}

function countNodes(node) {
  if (node === null) return 0;
  const leftCount = countNodes(node.left);
  const rightCount = countNodes(node.right);
  return 1 + leftCount + rightCount;
}

// Build the tree:
//      1
//     / \\
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Height of tree:", height(root));
console.log("Count of nodes:", countNodes(root));`}
  expected={`Height of tree: 2
Count of nodes: 4`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="tree-recursion-height-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "height(root);"
  ]}
  frames={[
    { caption: "Call height(1). Need heights of left (2) and right (3).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }] },
    { caption: "Call height(2). Need heights of left (4) and right (null).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }] },
    { caption: "Call height(4). Need heights of left (null) and right (null).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }, { value: "height(4)", mark: "waiting" }] },
    { caption: "height(4): both children are null, each returns -1 (base case). Return 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }, { value: "height(4) → 0", mark: "done" }] },
    { caption: "Back in height(2). Left height = 0, right height (null) = -1. Return 1 + max(0, -1) = 1.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2) → 1", mark: "done" }, { value: "height(4) → 0", mark: "done" }] },
    { caption: "Call height(3). Left and right are null. Base case returns -1. Return 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2) → 1", mark: "done" }, { value: "height(3) → 0", mark: "done" }] },
    { caption: "Back in height(1). Left height = 1, right height = 0. Return 1 + max(1, 0) = 2.", line: 2, cells: [{ value: "height(1) → 2", mark: "done" }, { value: "height(2) → 1", mark: "done" }, { value: "height(3) → 0", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="tree-recursion-count-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "countNodes(root);"
  ]}
  frames={[
    { caption: "Call countNodes(1). Need counts from left (2) and right (3).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }] },
    { caption: "Call countNodes(2). Need counts from left (4) and right (null).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }] },
    { caption: "Call countNodes(4). Left and right are null.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }, { value: "countNodes(4)", mark: "waiting" }] },
    { caption: "countNodes(4) hits base case (null). Return 0. Return 1 + 0 + 0 = 1 (just node 4).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }, { value: "countNodes(4) → 1", mark: "done" }] },
    { caption: "Back in countNodes(2). Left = 1, right (null) = 0. Return 1 + 1 + 0 = 2.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(4) → 1", mark: "done" }] },
    { caption: "Call countNodes(3). Left and right are null. Return 1 + 0 + 0 = 1.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(3) → 1", mark: "done" }] },
    { caption: "Back in countNodes(1). Left = 2, right = 1. Return 1 + 2 + 1 = 4. Total node count.", line: 2, cells: [{ value: "countNodes(1) → 4", mark: "done" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(3) → 1", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

Both `height` and `countNodes` visit every node in the tree exactly once. Each node is processed once: we compute the maximum (for height) or add a count (for node count) in O(1) time. For a tree with n nodes, the time complexity is **O(n)**.

**Space complexity**

The space comes from the recursion call stack. In the worst case, the recursion stack depth equals the height of the tree. In a balanced tree, height is O(log n). In a skewed tree (like a linked list), height is O(n). Therefore, space complexity is **O(h)**, where h is the height of the tree.

</Complexity>

<AlgoPractice
  id="practice-tree-recursion-1"
  lessonSlug="04-recursion-on-trees"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "For a tree with structure:\n    5\n   / \\\n  3   7\n /   / \\\n2   6   8\n\nWhat is the height of the tree?",
      choices: [
        "1",
        "2",
        "3",
        "4"
      ],
      answer: 1,
      hint: "Height is the longest path from root to leaf. The path 5 → 3 → 2 has length 2 (number of edges). Using height = 1 + max(left, right), the leaf 2 has height -1, node 3 has height 0, node 5 has height 1."
    },
    {
      kind: "choice",
      prompt: "In the height function, why do we return -1 for a null subtree instead of 0?",
      choices: [
        "Because negative numbers represent empty trees.",
        "So that a single node (with two null children) returns height 1 + max(-1, -1) = 0. This makes the heights correct.",
        "Because -1 is a common convention in algorithms.",
        "To indicate an error in the tree."
      ],
      answer: 1,
      hint: "A single isolated node should have height 0 (no edges below it). If null returned 0, then height(leaf) = 1 + max(0, 0) = 1, which is wrong. If null returns -1, then height(leaf) = 1 + max(-1, -1) = 0, which is correct."
    },
    {
      kind: "choice",
      prompt: "For countNodes, what does each recursive call return?",
      choices: [
        "The value of the node.",
        "True or false, depending on whether the node exists.",
        "The total number of nodes in that subtree, including the current node.",
        "The height of the subtree."
      ],
      answer: 2,
      hint: "countNodes(node) returns how many nodes are in the subtree rooted at node. For a leaf, that is 1. For a node with children, it is 1 + left count + right count."
    },
    {
      kind: "choice",
      prompt: "When you call height on the root of a tree with height 2, which subtree is explored first?",
      choices: [
        "Always the left subtree.",
        "Always the right subtree.",
        "The left subtree is called first, but you cannot determine the height of the root until the right subtree is also called and returns.",
        "Both are called simultaneously."
      ],
      answer: 2,
      hint: "The function calls height(node.left) first, but that call goes deep before returning. Still, the function cannot compute 1 + max(leftHeight, rightHeight) until rightHeight is also computed. The order of exploration is left-first due to the recursive call order."
    },
    {
      kind: "choice",
      prompt: "What is the space complexity of the height function for a skewed binary tree (where every node has only a right child)?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(n)",
        "O(n²)"
      ],
      answer: 2,
      hint: "A skewed tree has height n-1 (one edge per level). The recursion call stack grows as deep as the tree height, so space is O(n)."
    }
  ]}
/>

<Inset type="why">
**Why recursion on trees?** Iterative tree solutions often require a stack or queue to track nodes you have not yet processed. Recursion does that tracking for you using the call stack. It also matches the tree's structure: a tree is a node plus two subtrees, so the recursive structure mirrors the data structure.
</Inset>

<Inset type="edgecase">
**Empty trees and single nodes.** If the root is null, the base case returns immediately. If the tree is a single node, both children are null, the recursive calls hit the base case, and you combine the base-case values with the current node. No special handling needed.
</Inset>

<Inset type="practice">
**Trace by hand.** Draw a small tree (3–4 nodes). Pick a problem (height, count, or a simple property). Execute the recursion by hand: at each call, note the recursive calls, wait for them to return, then combine. Watch the values flow up the tree. This builds intuition.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-tree-recursion-1"
    pieceSlug="04-recursion-on-trees"
    lang="en"
    question="What is the key insight that makes recursion on trees work?"
    choices={[
      {
        label: "A tree is a node plus two subtrees. To solve a problem for a tree, assume you can already solve it for any subtree, solve both subtrees, combine the answers with the current node, and return the combined result.",
        correct: true
      },
      {
        label: "Recursion is always faster than iteration for tree problems.",
        misconception: "Recursion and iteration can have similar time complexity. The choice depends on readability and problem structure, not performance alone."
      },
      {
        label: "The base case must always return 0.",
        misconception: "The base case returns a value that makes sense for your problem. For height, it is -1; for count, it is 0; for other problems, it may differ."
      },
      {
        label: "You must process nodes in a specific order: pre-order, in-order, or post-order.",
        misconception: "For tree recursion, postorder (left, right, node) is natural because you combine child results with the current node. Other orders are for different problems."
      }
    ]}
  />
</Check>

<Recap lang="en">
**Recursion on trees** solves problems by asking the same question of both subtrees, combining their answers with the current node, and returning the combined result. The base case is always an empty subtree (null). The two worked examples—height and countNodes—show the pattern: assume both subtrees return a value, combine those values with information about the current node (max for height, sum for count), and return the result. Time complexity is **O(n)** (visit each node once). Space complexity is **O(h)** (recursion stack depth = tree height). This pattern extends to any tree problem: check a property, sum values, find a maximum, etc. The next lesson covers binary search trees: trees with a specific property (values in the left subtree are smaller, values in the right are larger) that lets you search in **O(log n)** time.
</Recap>
