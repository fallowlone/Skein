---
slug: 01-the-binary-tree
lang: en
track: algorithms
unit: 07-trees
order: 1
title: "The binary tree"
summary: "A tree is nodes in hierarchy: root at top, each pointing to children below. Binary tree restricts each node to at most 2 children (left, right). Trees fit recursion naturally - visit the node, then recursively process its left and right subtrees."
estMin: 22
status: ready
prereqs: ["01-what-is-recursion"]
mathPrereqs: []
concepts: ["tree", "binary_tree", "node", "root", "leaf", "parent", "child", "sibling", "depth", "height", "subtree", "level"]
sources:
  - https://en.wikipedia.org/wiki/Binary_tree
  - https://www.geeksforgeeks.org/binary-tree-data-structure/
  - https://www.programiz.com/dsa/binary-tree
  - https://www.happycoders.eu/data-structures/binary-tree/
  - https://www.freecodecamp.org/news/binary-trees-explained/
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You know that a linked list is a chain of nodes, each pointing to the next. Now imagine a structure where each node points not to one next node, but to two: a left child and a right child. This is a tree. Instead of a linear chain, you get a branching structure—one root node at the top, spreading downward into leaves. Trees let you organize hierarchies (like a file system), and they fit recursion so naturally that processing a tree feels like breathing.
</Hook>

<Goal lang="en">
After this lesson you can explain what a tree is, distinguish a binary tree from other trees, use the terminology (root, leaf, parent, child, depth, height, subtree, level), implement a node with left and right pointers, navigate a binary tree by following child pointers, and recognize when a tree is the right data structure.
</Goal>

<Idea lang="en">
**What is a tree?**

A **tree** is a hierarchy of **nodes** connected by **edges**. One node is special: the **root**, at the top. Every other node has exactly one **parent** (the node pointing to it). A node can have zero, one, or more **children** (nodes it points to).

Visual:
```
      1 (root)
     / \
    2   3
   / \
  4   5
```

Here, node 1 is the root. Node 2 is the parent of 4 and 5. Node 4 is a child of 2. Nodes 4 and 5 are **siblings** (they share the same parent).

**What is a binary tree?**

A **binary tree** is a tree where each node has **at most 2 children**. We call them the **left child** and the **right child**. A node can have:
- No children (a leaf)
- One child (left only, or right only)
- Two children (both left and right)

The binary tree above:
```
      1
     / \
    2   3
   / \
  4   5
```

Node 1 has a left child (2) and a right child (3). Node 2 has two children. Node 3 has none. Nodes 4 and 5 are leaves.

**Tree terminology**

- **Root**: The topmost node (no parent).
- **Leaf**: A node with no children.
- **Internal node**: A node with at least one child.
- **Parent / Child**: If node A points to node B, A is the parent and B is the child.
- **Sibling**: Two nodes with the same parent.
- **Ancestor / Descendant**: Node A is an ancestor of B if A is on the path from B up to the root. B is a descendant of A.
- **Level**: All nodes at the same distance from the root are at the same level. The root is at level 0.

**Depth vs. Height** (this trips everyone up)

- **Depth** of a node = the number of edges from the root down to that node. The root has depth 0.
- **Height** of a node = the number of edges from that node down to its furthest leaf descendant. A leaf has height 0.
- **Height of the tree** = the height of the root.

Memory aid: Depth is how deep you've gone *down* from the root. Height is how tall the subtree *reaches* below you.

Example:
```
      1  (depth=0, height=2)
     / \
    2   3  (depth=1, height=1 for node 2; height=0 for node 3)
   / \
  4   5  (depth=2, height=0 for both)
```

Node 4 has depth 2 (two edges from root: 1→2→4) and height 0 (no children). Node 2 has depth 1 and height 1 (one edge down to its furthest leaf).

**Subtrees and recursion**

A **subtree** is any node plus all its descendants. The left subtree of a node is the subtree rooted at its left child. The right subtree is rooted at its right child.

A binary tree is defined *recursively*:
- An empty tree is a binary tree.
- A tree with a root and a left subtree (which is a binary tree) and a right subtree (which is a binary tree) is a binary tree.

This recursive definition is the key insight: to process any tree, you process the root, then recursively process the left subtree, then the right subtree. This structure is why recursion and trees are meant for each other.

</Idea>

<Code lang="en">
**Representing a node in code**

In a linked list, each node held a value and a pointer to the next node. In a binary tree, each node holds a value and pointers to the left and right children:

<AnnotatedCode
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

// Build the tree:
//      1
//     / \\
//    2   3
//   / \\
//  4   5

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);`}
  annotations={[
    { line: 1, text: "Node constructor stores value, left, and right." },
    { line: 7, text: "Build bottom-up: leaves first." },
    { line: 13, text: "Root is the entry point to the whole tree." }
  ]}
/>

**Navigating the tree by following pointers**

To read the tree, you follow parent→child links. Each node is a step down:

<AnnotatedCode
  code={`// Access nodes by following left/right pointers
let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));

// Navigate to the left child of root
let leftChild = root.left;   // Node 2

// Navigate to the left child of node 2
let leftGrandchild = root.left.left;  // Node 4

// Navigate to the right child of node 2
let rightGrandchild = root.left.right;  // Node 5

// Each link is a single pointer hop
console.log(leftChild.value);         // 2
console.log(leftGrandchild.value);    // 4`}
  annotations={[
    { line: 5, text: "root.left follows the left pointer from root." },
    { line: 8, text: "root.left.left hops through two pointers." },
    { line: 11, text: "root.left.right hops left then right." },
    { line: 14, text: "Each navigation accesses the value property." }
  ]}
/>

**A simple helper: checking if a node is a leaf**

A leaf has no children (both left and right are null). Here is a small helper function:

<AnnotatedCode
  code={`function isLeaf(node) {
  return node.left === null && node.right === null;
}

let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));

console.log(isLeaf(root.left.left));  // true (node 4 is a leaf)
console.log(isLeaf(root.left));       // false (node 2 has children)
console.log(isLeaf(root));            // false (root has children)`}
  annotations={[
    { line: 1, text: "A leaf is a node with no children." },
    { line: 2, text: "Check if both pointers are null." },
    { line: 6, text: "Construct the tree." },
    { line: 8, text: "Node 4 has no children, so isLeaf returns true." }
  ]}
/>

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function isLeaf(node) {
  return node.left === null && node.right === null;
}

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Node 4 is leaf:", isLeaf(node4));
console.log("Node 2 is leaf:", isLeaf(node2));
console.log("Root is leaf:", isLeaf(root));`}
  expected={`Node 4 is leaf: true
Node 2 is leaf: false
Root is leaf: false`}
/>

</Code>

<Trace lang="en">
<AlgoTrace
  id="binary-tree-navigation"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));",
    "let target = root.left.right;  // Navigate to node 5",
    "console.log(target.value);"
  ]}
  frames={[
    { caption: "Start: root points to node 1", line: 1, cells: ["1"] },
    { caption: "Follow root.left: hop to node 2", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "Follow .right on node 2: hop to node 5", line: 2, cells: [{ value: "2", mark: "active" }, { value: "5", mark: "active" }] },
    { caption: "Access node 5 via root.left.right", line: 2, cells: [{ value: "5", mark: "done" }] },
    { caption: "Read node 5's value: 5", line: 3, cells: [{ value: "5", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

Building the tree from scratch (creating n nodes) takes O(n) time. Navigating the tree by following pointer hops (e.g., `root.left.right`) takes O(1) per hop. Following a path of length d takes O(d). The `isLeaf` helper is O(1).

**Time = O(1)** per pointer hop; **O(n)** to build the entire tree.

**Space complexity**

Space is used to store the tree structure: each of the n nodes occupies O(1) space. The tree itself uses O(n) space in memory.

**Space = O(n)** for the tree structure itself. Individual operations like `isLeaf` use O(1) extra space.

</Complexity>

<AlgoPractice
  id="practice-binarytree-1"
  lessonSlug="01-the-binary-tree"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "What is the depth of a node?",
      choices: [
        "The number of children it has",
        "The number of edges from the root down to that node",
        "The number of ancestors it has",
        "The distance from that node to the nearest leaf"
      ],
      answer: 1,
      hint: "Depth measures distance *upward* from the node toward the root."
    },
    {
      kind: "choice",
      prompt: "What is the height of a node?",
      choices: [
        "The number of edges from the root down to that node",
        "The number of children it has",
        "The number of edges from that node down to its furthest leaf descendant",
        "The level it is on"
      ],
      answer: 2,
      hint: "Height measures distance *downward* from the node to the furthest leaf below it."
    },
    {
      kind: "choice",
      prompt: "What is the height of a leaf node?",
      choices: [
        "0",
        "1",
        "The depth of the node",
        "The height of its parent minus 1"
      ],
      answer: 0,
      hint: "A leaf has no children, so the furthest descendant is itself (0 edges away)."
    },
    {
      kind: "choice",
      prompt: "In code, how do you represent a binary tree node?",
      choices: [
        "A class with value, next, and prev properties",
        "A class with value, left, and right properties",
        "An array [value, children...]",
        "A class with value, parent, and depth properties"
      ],
      answer: 1,
      hint: "A binary tree node points to at most 2 children, so we use left and right pointers."
    },
    {
      kind: "choice",
      prompt: "What is the worst-case height of a binary tree with 7 nodes?",
      choices: [
        "2",
        "3",
        "6",
        "7"
      ],
      answer: 2,
      hint: "If all 7 nodes form a chain (one child per node), the height is 6 edges (7 - 1)."
    }
  ]}
/>

<Inset type="mistake">
**Depth ≠ Height.** Depth measures distance *from the root down to you*. Height measures distance *from you down to the furthest leaf*. They point in opposite directions.
</Inset>

<Inset type="edge-case">
**Empty tree.** A tree can be empty (no nodes). The height of an empty tree is sometimes defined as -1, sometimes as 0, depending on convention. Always check the definition in your problem.
</Inset>

<Inset type="practice">
**Draw a tree.** Draw a binary tree with 5 nodes. Label the root, leaves, and one internal node. Then calculate the depth and height of each node. This is the fastest way to cement the terminology.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-binarytree-1"
    pieceSlug="01-the-binary-tree"
    lang="en"
    question="Why is a tree a natural fit for recursive algorithms?"
    choices={[
      {
        label: "Because a tree is defined recursively: to process a tree, you process the root, then recursively process the left subtree, then the right subtree. The recursive structure of the data matches the recursive structure of the algorithm.",
        correct: true
      },
      {
        label: "Because trees are easier to understand than loops.",
        misconception: "Ease of understanding varies by person. The real reason is structural: trees are hierarchical and self-similar, like a recursive definition."
      },
      {
        label: "Because recursion is always faster than iteration.",
        misconception: "Recursion is not always faster. It uses the call stack, which can be slower and has depth limits. Both recursion and iteration are tools with different tradeoffs."
      },
      {
        label: "Because trees have leaves, and recursion is about going to the bottom of something.",
        misconception: "Recursion is not about reaching the bottom; it is about solving a problem by breaking it into identical smaller versions. Trees happen to fit this pattern naturally."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **tree** is a hierarchy of nodes with one root at the top. A **binary tree** restricts each node to at most 2 children: left and right. Trees use terminology: root, leaf, parent, child, sibling, ancestor, descendant, level. **Depth** is the number of edges from the root down to a node; **height** is the number of edges from a node down to its furthest leaf. A **subtree** is a node plus all its descendants. In code, represent a node as `{ value, left, right }`, where left and right are null if there is no child. Navigate a tree by following left and right pointers from the root. A leaf is any node where both left and right are null. Binary trees are the foundation for heaps, binary search trees, and many other algorithms. Next, you will learn traversals: how to visit every node in a specific order.
</Recap>
