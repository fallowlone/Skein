---
concepts:
- traversal
- pre-order
- in-order
- post-order
- depth-first
- recursive_function
- visit
estMin: 20
lang: en
mathPrereqs: []
order: 2
prereqs:
- 01-the-binary-tree
slug: 02-traversals
sources:
- https://en.wikipedia.org/wiki/Tree_traversal
- https://www.enjoyalgorithms.com/blog/binary-tree-traversals-preorder-inorder-postorder/
- https://opendsa-server.cs.vt.edu/ODSA/Books/Everything/html/BinaryTreeTraversal.html
- https://www.geeksforgeeks.org/dsa/tree-traversals-inorder-preorder-and-postorder/
status: ready
summary: 'Three ways to visit nodes in a tree: pre-order (node, left, right), in-order
  (left, node, right), post-order (left, right, node). Each as a recursive function,
  each solving different problems based on whether you need ancestor context, sorted
  output, or child results.'
title: Tree traversals
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
You know how to walk left and right through a tree by following pointers. But in what order do you visit the nodes? Do you look at the node first, or explore its children first? Different orders answer different questions. Visit the node before its children, and you can build a copy top-down. Visit the node after its left child in a binary search tree, and you get sorted order. Visit it after both children, and you can compute subtree results from the ground up. This lesson teaches the three canonical orders and why you would pick each one.
</Hook>

<Goal lang="en">
After this lesson you can write the three depth-first tree traversals (pre-order, in-order, post-order) as recursive functions, trace each on a concrete tree to predict the visiting order, explain when and why each is used, and recognize when a problem requires a specific traversal order.
</Goal>

<Idea lang="en">
**What is a traversal?**

A **traversal** is a systematic visit to every node in a tree. The order in which you visit matters because it determines what information is available when you process each node.

There are many traversal orders, but the three most common are **depth-first traversals** that visit the root before exploring all its descendants. They differ in **when** the root is visited relative to its children:

- **Pre-order**: Visit root, then left subtree, then right subtree. (Node, Left, Right)
- **In-order**: Visit left subtree, then root, then right subtree. (Left, Node, Right)
- **Post-order**: Visit left subtree, then right subtree, then root. (Left, Right, Node)

All three are **recursive**: to traverse a tree, process the root according to the order, then recursively traverse the left and right subtrees.

**Why three different orders?**

Each order is a tool for different jobs. Pre-order processes a parent before its children (useful for copying a tree, or marking the distance from the root). In-order on a binary search tree gives sorted order—the node's value comes out between all smaller values (in the left subtree) and all larger values (in the right subtree). Post-order processes a parent after both children (useful for deleting a tree or computing subtree heights). The choice depends on whether you need ancestors' context, sorted output, or results from descendants.

</Idea>

<Code lang="en">
**Pre-order traversal: process the root first**

In pre-order, visit the current node, then recursively traverse its left subtree, then its right subtree.

<AnnotatedCode
  code={`function preOrder(node) {
  if (node === null) return;
  
  // Visit (process) the node
  console.log(node.value);
  
  // Traverse left subtree
  preOrder(node.left);
  
  // Traverse right subtree
  preOrder(node.right);
}`}
  annotations={[
    { line: 1, text: "Recursive function: takes a node (or null for an empty subtree)." },
    { line: 2, text: "Base case: if the node is null, there is nothing to visit." },
    { line: 5, text: "Visit the current node (here, print its value)." },
    { line: 8, text: "Recursively traverse the left subtree." },
    { line: 11, text: "Recursively traverse the right subtree." }
  ]}
/>

Example: For the tree:
```
    1
   / \
  2   3
 /
4
```

Pre-order visits in this order: **1 → 2 → 4 → 3**.

**In-order traversal: visit the node between its children**

<AnnotatedCode
  code={`function inOrder(node) {
  if (node === null) return;
  
  // Traverse left subtree
  inOrder(node.left);
  
  // Visit (process) the node
  console.log(node.value);
  
  // Traverse right subtree
  inOrder(node.right);
}`}
  annotations={[
    { line: 1, text: "Recursive function: takes a node (or null for an empty subtree)." },
    { line: 2, text: "Base case: if the node is null, there is nothing to visit." },
    { line: 5, text: "Recursively traverse the left subtree first." },
    { line: 8, text: "Visit the current node after its left subtree." },
    { line: 11, text: "Recursively traverse the right subtree." }
  ]}
/>

Example: For the same tree:
```
    1
   / \
  2   3
 /
4
```

In-order visits in this order: **4 → 2 → 1 → 3**.

**Post-order traversal: visit the node after both children**

<AnnotatedCode
  code={`function postOrder(node) {
  if (node === null) return;
  
  // Traverse left subtree
  postOrder(node.left);
  
  // Traverse right subtree
  postOrder(node.right);
  
  // Visit (process) the node
  console.log(node.value);
}`}
  annotations={[
    { line: 1, text: "Recursive function: takes a node (or null for an empty subtree)." },
    { line: 2, text: "Base case: if the node is null, there is nothing to visit." },
    { line: 5, text: "Recursively traverse the left subtree." },
    { line: 8, text: "Recursively traverse the right subtree." },
    { line: 11, text: "Visit the current node after both subtrees." }
  ]}
/>

Example: For the same tree:
```
    1
   / \
  2   3
 /
4
```

Post-order visits in this order: **4 → 2 → 3 → 1**.

**Running all three on a concrete tree**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function preOrder(node) {
  if (node === null) return;
  console.log(node.value);
  preOrder(node.left);
  preOrder(node.right);
}

function inOrder(node) {
  if (node === null) return;
  inOrder(node.left);
  console.log(node.value);
  inOrder(node.right);
}

function postOrder(node) {
  if (node === null) return;
  postOrder(node.left);
  postOrder(node.right);
  console.log(node.value);
}

// Build the tree:
//      1
//     / \
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Pre-order:");
preOrder(root);

console.log("\nIn-order:");
inOrder(root);

console.log("\nPost-order:");
postOrder(root);`}
  expected={`Pre-order:
1
2
4
3

In-order:
4
2
1
3

Post-order:
4
2
3
1`}
/>

</Code>

<DiagramFrame
  label="The sample binary tree with root 1, left child 2 (whose left child is 4) and right child 3. Each node carries a small badge giving its in-order visit position: 4 is visited 1st, 2 is 2nd, 1 is 3rd, 3 is 4th. Below the tree the three depth-first sequences are listed: pre-order 1, 2, 4, 3; in-order 4, 2, 1, 3; post-order 4, 2, 3, 1."
  hue="var(--d-systems)"
  caption="One tree, three orders. The badge on each node is its in-order (left, node, right) rank 1..4 — read the badges low-to-high and you trace the in-order walk 4, 2, 1, 3. Pre-order and post-order visit the same nodes but move the root to the front or the back of each subtree."
>
  <svg viewBox="0 0 460 280" width="460" height="280">
    <line x1="230" y1="46" x2="140" y2="116" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="230" y1="46" x2="320" y2="116" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <line x1="140" y1="116" x2="90" y2="186" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <circle cx="230" cy="40" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="230" y="46" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <circle cx="140" cy="120" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="140" y="126" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <circle cx="320" cy="120" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="320" y="126" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <circle cx="90" cy="190" r="22" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="90" y="196" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <circle cx="252" cy="22" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="252" y="26" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">3</text>
    <circle cx="162" cy="102" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="162" y="106" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">2</text>
    <circle cx="342" cy="102" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="342" y="106" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">4</text>
    <circle cx="112" cy="172" r="10" fill="var(--accent)" stroke="var(--card)" stroke-width="1.2" />
    <text x="112" y="176" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--on-accent)">1</text>
    <text x="386" y="40" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">{"● = in-order rank"}</text>
    <line x1="20" y1="222" x2="440" y2="222" stroke="var(--hairline-2)" stroke-width="0.8" />
    <text x="20" y="240" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"pre-order  (node,left,right): 1, 2, 4, 3"}</text>
    <text x="20" y="256" font-size="11.5" font-family="var(--font-mono)" fill="var(--accent)">{"in-order   (left,node,right): 4, 2, 1, 3"}</text>
    <text x="20" y="272" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"post-order (left,right,node): 4, 2, 3, 1"}</text>
  </svg>
</DiagramFrame>

<Trace lang="en">
<AlgoTrace
  id="preorder-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "preOrder(root);"
  ]}
  frames={[
    { caption: "Start at root (node 1). Visit it first (pre-order).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "Go left to node 2. Visit it.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "active" }] },
    { caption: "Go left to node 4. Visit it.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "active" }] },
    { caption: "Node 4 has no left child. Go right (null), backtrack to node 2.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Node 2 has no right child. Backtrack to node 1.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Go right to node 3. Visit it.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "Node 3 has no children. Done. Visiting order: 1, 2, 4, 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="inorder-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "inOrder(root);"
  ]}
  frames={[
    { caption: "Start at root (node 1). Go left first (in-order).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "At node 2. Go left first.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "At node 4. Go left (null), then visit node 4.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Visited 4. Go right from node 4 (null). Backtrack to node 2, visit it.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "done" }] },
    { caption: "Visited 2. Go right from node 2 (null). Backtrack to node 1, visit it.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Visited 1. Go right to node 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "At node 3. Go left (null), visit node 3. Go right (null). Done. Visiting order: 4, 2, 1, 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="postorder-trace"
  lang="en"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "postOrder(root);"
  ]}
  frames={[
    { caption: "Start at root (node 1). Go left first, do not visit yet (post-order).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "At node 2. Go left first.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "At node 4. Go left (null).", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Go right from node 4 (null). Now visit node 4 (post-order: after children).", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Visited 4. Backtrack to node 2. Go right (null). Now visit node 2.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "done" }] },
    { caption: "Visited 2. Backtrack to node 1. Go right to node 3.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "At node 3. Go left (null), right (null). Visit node 3. Backtrack to node 1, visit it. Done. Visiting order: 4, 2, 3, 1.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="en">
**Time complexity**

Each of the three traversals visits every node in the tree exactly once. If the tree has n nodes, we make n visits plus the recursive calls to reach those nodes. Every node is processed once, so the time is **O(n)** for all three traversals.

**Space complexity**

The space used is the depth of the call stack. When the recursion goes down the tree, each recursive call stays on the call stack until it returns. The maximum depth of the call stack equals the height of the tree, h.

- Best case (balanced tree): **O(log n)** space for a height of log n.
- Worst case (chain): **O(n)** space for a height of n (if every node has only one child).

In typical balanced trees, **O(log n)** space. In skewed trees, **O(n)** space.

</Complexity>

<AlgoPractice
  id="practice-traversals-1"
  lessonSlug="02-traversals"
  lang="en"
  problems={[
    {
      kind: "choice",
      prompt: "For the tree:    2\n   / \\\n  1   3\n\nWhich is the pre-order traversal?",
      choices: [
        "1, 2, 3",
        "2, 1, 3",
        "1, 3, 2",
        "3, 2, 1"
      ],
      answer: 1,
      hint: "Pre-order visits the node first, then left, then right. Start at 2 (the root)."
    },
    {
      kind: "choice",
      prompt: "For the same tree, which is the in-order traversal?",
      choices: [
        "2, 1, 3",
        "1, 2, 3",
        "3, 1, 2",
        "1, 3, 2"
      ],
      answer: 1,
      hint: "In-order visits left first, then the node, then right. Go left to node 1."
    },
    {
      kind: "choice",
      prompt: "For the same tree, which is the post-order traversal?",
      choices: [
        "2, 1, 3",
        "1, 2, 3",
        "1, 3, 2",
        "3, 1, 2"
      ],
      answer: 2,
      hint: "Post-order visits both children before the parent. Process left (1), right (3), then node (2)."
    },
    {
      kind: "choice",
      prompt: "When you traverse a binary search tree with in-order, what property does the output have?",
      choices: [
        "The output is randomly ordered.",
        "The output is in ascending (sorted) order.",
        "The output is in reverse alphabetical order.",
        "The output is grouped by depth level."
      ],
      answer: 1,
      hint: "In a BST, all left values are smaller and all right values are larger. In-order (left, node, right) respects this."
    },
    {
      kind: "choice",
      prompt: "Pre-order traversal is most useful for which task?",
      choices: [
        "Getting values in sorted order from a BST.",
        "Copying a tree or processing ancestors before descendants.",
        "Deleting a tree from the leaves up.",
        "Finding the height of each node."
      ],
      answer: 1,
      hint: "Pre-order visits parents before children, so you can process a parent's information before needing its children's."
    }
  ]}
/>

<Inset type="mistake">
**Do not confuse the order names.** Pre- means "before" (visit the node before children). Post- means "after" (visit after children). In- means "in between" (visit between left and right children). A mnemonic: Pre (PREsent the root), In (In between), Post (POST-dated, arrives last).
</Inset>

<Inset type="practice">
**Trace it yourself.** Draw a binary tree with 5 nodes. For each traversal (pre, in, post), trace the visiting order by hand. Write down the sequence. This is the fastest way to lock in the order differences.
</Inset>

<Inset type="edgecase">
**Empty trees and single nodes.** If a tree is empty (null), the traversal does nothing. If a tree is a single node with no children, all three traversals visit just that one node. Edge cases make good test inputs.
</Inset>

<Check lang="en">
  <Quiz
    id="quiz-traversals-1"
    pieceSlug="02-traversals"
    lang="en"
    question="Why do we need three different traversal orders instead of just one?"
    choices={[
      {
        label: "Because different problems need nodes visited in different orders relative to their children. Pre-order processes parents before children (copying, top-down). In-order on a BST gives sorted output. Post-order processes parents after children (deletion, bottom-up results). Each order is a tool for different jobs.",
        correct: true
      },
      {
        label: "Because one traversal is faster than the others.",
        misconception: "All three have the same time complexity O(n). The difference is the order of visits, not the speed."
      },
      {
        label: "Because some trees are pre-order, some are in-order, some are post-order.",
        misconception: "A tree is not inherently one type or another. Any tree can be traversed in any of the three orders. The order is a choice based on the problem."
      },
      {
        label: "Because post-order is always best, but pre-order and in-order are kept for backward compatibility.",
        misconception: "No single traversal is 'best'. Each is designed for specific use cases. Choosing the right one is part of the algorithm design."
      }
    ]}
  />
</Check>

<Recap lang="en">
A **traversal** is a systematic visit to every node in a tree. Three depth-first traversals differ in **when** they visit a node relative to its children. **Pre-order** visits the node first, then left, then right (useful for copying, top-down processing). **In-order** visits left, node, right (gives sorted order on a BST). **Post-order** visits left, right, node (useful for deletion, computing subtree results bottom-up). All three are recursive: process the root according to the order, then recursively traverse subtrees. All three run in **O(n) time** (visit each node once) and **O(h) space** (recursion depth equals tree height). Next lesson covers level-order traversal (BFS), which uses a queue instead of recursion.
</Recap>
