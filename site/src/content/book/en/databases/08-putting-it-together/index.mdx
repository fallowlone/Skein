---
slug: 08-putting-it-together
lang: en
pillar: databases
chapter: 06-databases
order: 8
title: "Putting it together: one Postgres from MVP to 1 billion rows"
summary: "Seven prior pieces are seven scale tiers of one growing product. Walk from CREATE TABLE through Citus, naming the exact failure at each tier and the lever that resolves it."
readingMin: 22
status: ready
prereqs: ["01-relational-model","02-indexes","03-execution-plans","04-mvcc-isolation","05-pooling","06-migrations","07-sharding"]
spiral: ["statefulness","latency","multiplexing","encapsulation"]
personas: ["otto","sven"]
depth:
  mechanism: tier-mechanism
  tradeoff: stage-tradeoffs
  failure_mode: m-just-add-index
  numbers: scaling-stage-numbers
sources:
  - https://www.postgresql.org/docs/current/
  - https://wiki.postgresql.org/wiki/Don't_Do_This
  - https://github.blog/2021-08-31-partitioning-githubs-relational-databases-scale/
  - https://docs.citusdata.com/en/stable/
  - https://www.cybertec-postgresql.com/en/postgresql-vacuum-and-bloat/
  - https://www.pgmustard.com/blog/postgres-row-estimates-misleading
---

import TierAccordion from "../../../../../components/pedagogy/TierAccordion.astro";
import RetrievalDrawer from "../../../../../components/pedagogy/RetrievalDrawer.tsx";
import DBLeverSandbox from "../../../../../components/pedagogy/sandboxes/DBLeverSandbox.tsx";
import Crux from "../../../../../components/prose/Crux.astro";
import SpiralCue from "../../../../../components/prose/SpiralCue.astro";
import PersonaTag from "../../../../../components/pedagogy/PersonaTag.astro";
import Misconception from "../../../../../components/layout/Misconception.astro";
import KeyTakeaway from "../../../../../components/prose/KeyTakeaway.astro";
import Quiz from "../../../../../components/pedagogy/Quiz.astro";
import DragOrder from "../../../../../components/pedagogy/DragOrder.astro";
import MetaphorComplete from "../../../../../components/pedagogy/MetaphorComplete.astro";

Day 0 at a SaaS startup. The PM wants "search users by email." One engineer, one Postgres, one table. Three years later: 1 billion rows, six Citus shards, and a runbook the size of a textbook. Between those two points are seven moments when the database broke and the team learned. <PersonaTag id="otto" lang="en" /> kept the rows. <PersonaTag id="sven" lang="en" /> kept calling. <SpiralCue thread="statefulness" lang="en" /> <SpiralCue thread="latency" lang="en" />

<Crux>Seven pieces of this chapter are seven times a product outgrew its database — starting from CREATE TABLE.</Crux>

The walk below names the trigger and the lever at each tier. Each tier maps one-to-one onto a prior piece: skip the lever and the next tier costs more.

## The walk: from 0 to 1 billion rows

**Act 1 — Day 0, schema design (→ piece 01).** <PersonaTag id="sven" lang="en" /> asks <PersonaTag id="otto" lang="en" />: "users(email, name, org_id)." Should `email` be `UNIQUE`? Should `org_id` be a foreign key? Should `prefs` be a side table or `JSONB`? Decisions: `email` is `UNIQUE NOT NULL CITEXT`. `org_id` is `BIGINT REFERENCES orgs(id) ON DELETE CASCADE`. `prefs` is `JSONB` with a GIN index when search demands it. Cost paid: every later decision compounds against this baseline. A surrogate `id BIGSERIAL` insulates from email changes; bend the rule (skip FK) only with measured throughput pressure, never as a default.

**Act 2 — Week 1, 10K rows, the first slow query (→ piece 02).** Email-search endpoint p95 climbs from 30 ms to 800 ms. <PersonaTag id="otto" lang="en" /> reports the planner does a sequential scan. Sven runs `CREATE INDEX users_email_idx ON users(email)`. The leading-column rule of B-tree means `WHERE email = ?` resolves with two page reads. p95 drops to 4 ms.

**Act 3 — Month 1, 100K rows, the planner lies (→ piece 03).** Half the requests are fast, half take 600 ms. `EXPLAIN ANALYZE` shows the planner sometimes picks a seq-scan despite the index. Row estimate is off by 30×: stale statistics. `ANALYZE users;` rebuilds the histograms; `pg_stat_statements` confirms the worst-case plan disappears. The lesson is that the planner picks plans from statistics, not from the data; statistics maintenance is the operational discipline.

**Act 4 — Month 6, the silent bloat (→ piece 04).** A nightly report runner holds one transaction open for four hours. `VACUUM` cannot reclaim dead tuples below the `xmin` horizon. The `users` table swells from 200 MB to 80 GB. Vacuum logs show "removed 0 row versions" for the long-running window. Fix: kill the long transaction, set `idle_in_transaction_session_timeout = 60s`, run `pg_repack` to reclaim disk. Permanent fix: never run reporting on the OLTP database — replicate to a read replica.

**Act 5 — Year 1, 1M users + 50 app pods, the connection storm (→ piece 05).** Each pod has 20 workers, each opens its own Postgres backend on cold-start. At pod-rollout time the cluster sees 1000 concurrent backends; the kernel scheduler thrashes; queries that took 4 ms take 4 s. PgBouncer is deployed in transaction-mode in front of Postgres: 100 server-side backends, 10000 client connections multiplexed. The trap: transaction-mode pooling breaks server-side prepared statements unless PgBouncer ≥ 1.21 with `server_prepared_statements`. Sizing math from piece 05: `pool_size = active_concurrent_transactions × safety_factor`, not `max_app_workers`.

**Act 6 — Year 2, the migration that froze prod (→ piece 06).** Multi-tenancy day. A migration adds `tenant_id BIGINT NOT NULL`. The naive `ALTER TABLE users ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 0` takes an `AccessExclusiveLock` and triggers a full table rewrite. Prod freezes for eight minutes. The expand-contract recipe: (1) add nullable column with no default, (2) backfill in batches, (3) add `CHECK (tenant_id IS NOT NULL) NOT VALID`, (4) `VALIDATE CONSTRAINT`, (5) `SET NOT NULL` (cheap once validated), (6) drop the check. Builders like `pgroll` and `Atlas` codify the recipe.

**Act 7 — Year 3, 1B rows, the hot shard (→ piece 07).** Citus is rolled out. The shard key is `tenant_id`. Reads on most tenants are quick. Tenant `Acme` accounts for 40% of all queries. Acme's shard saturates while the others idle. The fix is co-location: place `users`, `orgs`, `events`, `audit_log` on the same shard key so cross-tenant joins stay local; then either (a) split the Acme tenant across logical shards with a tenant-aware router or (b) move Acme to its own physical cluster. Online resharding (with `citus_rebalance_table_shards`) lets the rebalance happen without downtime — but plan the window because catch-up replication adds load.

---

The acts are the spine. The detail and the synthesis live below.

<TierAccordion id="tier-mechanism" lang="en">
  <Fragment slot="junior">
    <p><strong>What this piece does.</strong> It glues the seven prior pieces into one growth story. Each piece you read earlier is one stage where the product outgrew the database.</p>
    <p><strong>Why care.</strong> Each piece in isolation looked optional — "I'll learn indexes when I need them." This piece shows that the *order* is not optional: a missed index at 10K rows is a one-line fix; a missed shard plan at 1B rows is a six-month project.</p>
    <p><strong>Metaphor.</strong> A database is a growing city. The schema is zoning. Indexes are the street map. Execution plans are the traffic dispatcher. MVCC is multiple lanes per road so cars can pass without colliding. The connection pool is the parking garage. Migrations are construction crews who must not close every road at once. Sharding is annexing new districts when one city block can't hold the traffic. A skipped lesson means the city keeps growing, but the wrong layer is overloaded.</p>
    <p><strong>Persona dialog.</strong> <PersonaTag id="sven" lang="en" />: "Otto, the search endpoint is slow." <PersonaTag id="otto" lang="en" />: "Add an index — leading column on what you filter by." Two months later. Sven: "Index is there, still slow." Otto: "The planner's row estimate is wrong. Run ANALYZE." Six months later. Sven: "We're out of disk." Otto: "A long transaction blocked vacuum. Kill it and set a timeout." A year later. Sven: "Pods can't connect." Otto: "You ran out of backends. Put PgBouncer in transaction-mode in front of me." Each line of the dialog is one piece of this chapter.</p>
    <p><strong>One concrete scenario.</strong> Day 1 the table is 1000 rows; everything is instant. Year 1 the table is 1M rows and one missing index makes every page take 400 ms. Year 3 the table is 1B rows on one node and it falls over. The price of each missed lever is paid at the next scale tier.</p>
    <Quiz
      id="jr-quiz-1"
      pieceSlug="08-putting-it-together"
      lang="en"
      question="At 10K rows, the email-search endpoint is suddenly slow. Cheapest first lever?"
      choices={[
        { label: "Add a PgBouncer pool", misconception: "Connection pooling helps at high pod-count, not at low data volume." },
        { label: "Create an index on the filtered column", correct: true },
        { label: "Shard the table by tenant_id", misconception: "Sharding is a year-3 lever, not a week-1 lever." },
        { label: "Run VACUUM FULL", misconception: "Vacuum helps with bloat, not with cold queries." },
      ]}
    />
    <Quiz
      id="jr-quiz-2"
      pieceSlug="08-putting-it-together"
      lang="en"
      question="The disk is full but the row count hasn't changed. What is the most likely cause?"
      choices={[
        { label: "Missing index", misconception: "Indexes use disk but rarely cause sudden growth without row changes." },
        { label: "Bloat from a long-running transaction blocking vacuum", correct: true },
        { label: "The query planner is broken", misconception: "Planner choices do not change disk usage." },
        { label: "A migration ran twice", misconception: "Migration replays are rare; bloat is the common cause." },
      ]}
    />
    <Quiz
      id="jr-quiz-3"
      pieceSlug="08-putting-it-together"
      lang="en"
      question="Why is sharding a year-3 lever, not a year-1 lever?"
      choices={[
        { label: "Sharding is harder than indexes and irreversible once tenants are spread across shards", correct: true },
        { label: "Sharding does not work below 1B rows", misconception: "Sharding works at any scale; the operational complexity is what costs you." },
        { label: "Postgres does not support sharding", misconception: "Citus, Vitess, and declarative partitioning all exist." },
        { label: "Sharding is illegal in some jurisdictions", misconception: "There is no such restriction." },
      ]}
    />
    <DragOrder
      id="jr-drag-1"
      pieceSlug="08-putting-it-together"
      lang="en"
      prompt="Order the seven scale-tier levers from earliest (day 0) to latest (year 3):"
      items={[
        "Design the relational schema (tables, keys, constraints)",
        "Add the right index for the query",
        "Verify the execution plan uses the index",
        "Hunt the long transaction blocking VACUUM",
        "Put a connection pooler in front of Postgres",
        "Migrate schema safely with expand-contract",
        "Shard the largest table across nodes",
      ]}
    />
    <RetrievalDrawer
      client:load
      pieceSlug="08-putting-it-together"
      lang="en"
      questions={[
        {
          id: "jr-q1",
          q: "Name the seven levers in order, and give one symptom that signals you reached each tier.",
          answer: <p>Schema (greenfield design pressure). Index (single-column query slow). Plan (index exists but seq-scan chosen). MVCC (bloat without row growth). Pool (cold-start connection storm). Migration (ALTER blocks prod). Sharding (one tenant dominates load).</p>,
        },
      ]}
    />
  </Fragment>
  <Fragment slot="middle">
    <p><strong>The seven-tier table.</strong> Each row is one scale tier, one trigger, one piece, one fix. Day 0: schema design. Week 1: 10K rows, first missing index visible. Month 1: stale statistics, planner ignores index. Month 6: bloat from long transactions blocks vacuum. Year 1: connection pool saturation. Year 2: migrations lock production. Year 3: one billion rows, single-node ceiling reached.</p>
    <p><strong>Act 1 detail — schema is irreversible at scale.</strong> A wrong type (TEXT vs CITEXT for email) becomes a multi-day migration at 100M rows. A wrong key (composite natural vs surrogate BIGSERIAL) makes every join expensive forever. The relational model rewards strictness up front: <em>NOT NULL by default</em>, FK constraints unless you have measured throughput pressure, CHECK constraints for business invariants, and surrogate keys for external references. Bend the rules only with evidence — disable FK on the highest-write child tables if the foreign-key trigger cost exceeds the write budget, never as a stylistic preference.</p>
    <p><strong>Act 2 detail — indexes pay back the cost only on the right columns.</strong> A B-tree on `email` resolves `WHERE email = ?` in O(log N) page reads. A B-tree on `(org_id, created_at DESC)` resolves "the 50 latest events for an org" in two page reads. A GIN index on `prefs jsonb` makes `WHERE prefs @> '{"theme":"dark"}'` a 2 ms lookup instead of a 4 s seq-scan. The forgotten cost: every insert and update writes to every index. The leading-column rule is the most-violated principle in the wild — a `(b, a)` index is not the same as `(a, b)` for queries that filter only by `a`. The cost of being wrong here is permanent: legacy apps across the codebase inherit the bad column order, and any attempt to drop the index for remediation risks query regressions in undocumented code paths.</p>
    <p><strong>Act 3 detail — the planner picks plans from histograms.</strong> Postgres samples the table during `ANALYZE` and builds a histogram per column. The planner uses those histograms to estimate row counts; the cost model then ranks plans. When statistics are stale, estimates are wrong. A 1000× off estimate produces a 1000× slower plan: the planner picks a nested loop expecting 10 rows; the real cardinality is 10000 rows; the loop scans 10M index entries instead of one hash join over a 10000-row build side. Defence: `autovacuum_analyze_scale_factor = 0.05` on busy tables; `CREATE STATISTICS` for correlated columns; `pg_stat_statements` to find the offending plans; `auto_explain.log_min_duration = 500ms` for tail-latency triage. The trap at scale: a single bad estimate cascades; the planner caches its decisions, and a human has to find and force a replan by hand.</p>
    <p><strong>Act 4 detail — MVCC and the xmin horizon.</strong> Every row in Postgres has `xmin` (creation transaction id) and `xmax` (deletion transaction id). A `DELETE` doesn't free the row; it sets `xmax`. `VACUUM` reclaims rows whose `xmax` is older than the global `xmin` horizon — the oldest active transaction. If one analyst session leaves a transaction open for hours, the horizon doesn't advance, and no dead tuple created after that horizon can be reclaimed. The table swells; queries that scan the table get slower because they read dead rows. The fix is operational: `idle_in_transaction_session_timeout = 60s`, `pg_stat_activity` alerts on `state = 'idle in transaction'` longer than five minutes, and a clear runbook to kill the offender. Long-running analytics belong on a read replica. At 1B rows, one idle session can bloat the table by 10% in a day.</p>
    <p><strong>Act 5 detail — pool sizing math.</strong> Active concurrent transactions is the load that hits the database simultaneously; idle workers are not active. Sizing rule of thumb: `pool_size = (cores × 2) + spindles`. For a 16-core SSD-only server, the sweet spot is usually 30-60 backends; more starves the OS scheduler. App-side workers can be far higher; PgBouncer multiplexes them onto the small pool. The trap: transaction-mode pooling rebinds connections after each transaction, so server-side prepared statements (`PREPARE foo AS SELECT ...`) are lost. PgBouncer 1.21+ ships `server_prepared_statements = on` which caches plan IDs server-side and replays them; without it, app frameworks that auto-prepare statements pay re-prepare overhead on every transaction. A cold-start surge (app restart, deploy, failover) floods the pool; connection handshake becomes the bottleneck before the first query executes.</p>
    <p><strong>Act 6 detail — the lock matrix.</strong> `ALTER TABLE` takes `AccessExclusiveLock` by default — incompatible with every other lock. While it waits for the lock, every new query waits behind it. While it holds the lock, every query waits. A short ALTER under low load completes in milliseconds; an ALTER behind a long query (Act 4!) waits, builds a queue, and the queue freezes the database. The expand-contract recipe sidesteps this by splitting one big operation into many small lock-cheap ones. `ALTER TABLE ... SET NOT NULL` after a successful `NOT VALID` constraint validation is metadata-only — milliseconds. Index creation is `CREATE INDEX CONCURRENTLY` — slower but no AccessExclusive. The discipline is to treat every migration as a production incident-in-waiting unless every step is provably lock-cheap. A single 60-second lock on a hot table can cascade into a 10-minute incident due to connection timeout and app-level retry storms.</p>
    <p><strong>Act 7 detail — sharding choices.</strong> Three independent decisions. (1) Shard key: tenant_id (multi-tenant SaaS), user_id (consumer product), time bucket (time-series). High cardinality + uniform load + cheap to compute. (2) Distribution method: hash (even spread, range queries fan out), range (range queries local, hot range possible), list (manual), directory (most flexible, most operational cost). (3) Co-location: place tables that join together on the same shard key so cross-shard joins are cheap. Citus + Postgres declarative partitioning is the pragmatic 2026 default. Schema-based sharding (one schema per tenant) shines for ≤ 1000 tenants but breaks down at 10000+ schemas due to catalog overhead. The irreversible choice at Act 7: the shard key, once chosen, dictates resharding strategy forever. A wrong choice (e.g., shard by geography instead of tenant) means multi-month reshard-heavy refactors.</p>
    <p><strong id="stage-tradeoffs">The seven-tier tradeoff cascade.</strong> Each act unlocks the next, but only if you nailed the previous one. Skip Act 1 (schema) → Acts 2–7 fight a losing battle against table-wide mutations and inefficient joins. Skip Act 2 (indexing) → Act 3 planning decisions are made blind; stats are useless if indexes don't exist for the planner to choose. Skip Act 3 (stats) → Act 4 vacuum is the only lever left; tables bloat while you guess at cardinality. Skip Act 4 (MVCC/bloat) → Act 5 pool threads see table scans get slower; query latency climbs and triggers more connection churn. Skip Act 5 (pool sizing) → Act 6 migrations are starved by connection storms; a release that needs a migration waits for capacity that never comes. Skip Act 6 (lock safety) → Act 7 sharding is impossible; every shard rebalance is a downtime event. Skip Act 7 (sharding) → one tenant dominates and the others wait in queue. The order is not a suggestion; it's a constraint imposed by physics and Postgres internals.</p>
    <p><strong id="scaling-stage-numbers">Cross-cutting numbers and capacity limits.</strong> Index lookup cost: 5–50 microseconds in shared_buffers (L3 cache), 100 microseconds–2 milliseconds from OS page cache, 5–10 milliseconds from SSD, 50–500 milliseconds from network (warm standby or read-only replica). Heap fetch after index: same cost class. Sequential scan of 1 GB: 1–3 seconds SSD-bound, proportional to table size. P95 of a properly indexed point-lookup: under 5 milliseconds. P95 of a saturated connection pool (all backends active, no idle slots): seconds or timeout, exponential backoff in application code. Realistic OLTP write rate per backend: 5000 simple transactions per second on modern hardware. Realistic single-node ceiling for write-heavy workload before sharding becomes the only lever: 10–50 K writes/second sustained (depends on write complexity and concurrency). At 100 million rows, a full table scan takes 30–60 seconds on SSD. At 1 billion rows, a full scan takes 5–10 minutes; unplanned full scans cascade into long-running transactions and bloat. Table bloat growth: 1–10% per month on write-heavy workloads without autovacuum tuning. With proper tuning: 0.1–1% per month. With idle-in-transaction bugs: 50%+ per week.</p>
    <p><strong>The compound cost of skipping a step.</strong> A team that omits Act 2 (indexing) discovers at Act 3 that queries are slow; they blame the planner and spend weeks tuning. A team that skips Act 4 (bloat ops) finds at Act 6 that their migrations take 30 minutes instead of 5 seconds because the table is bloated. A team that omits Act 5 (pool sizing) experiences mysterious timeouts at 2× expected QPS and spends a month load testing. The cost of a late fix is exponential: Act 1 mistakes cost days; Act 7 mistakes cost months of resharding. This is why the walk must be walked in order.</p>
    <p><strong>Diagnostic tooling: your eyes into each tier.</strong> Act 1 (schema) has no good tooling — you must read the table definition and trace the migrations. Act 2 (indexes) is visible in <code>pg_indexes</code>, but you need <code>pg_stat_user_indexes</code> to find unused ones. Act 3 (planning) is where <code>pg_stat_statements</code> and <code>EXPLAIN ANALYZE</code> dominate; slow_query_log tuning is a prerequisite. Act 4 (bloat) requires <code>pg_stat_user_tables</code> (n_dead_tup vs n_live_tup) and <code>pg_table_size(...)</code> for actual disk usage; <code>pgstattuple</code> gives precise bloat percentage. Act 5 (pooling) is monitored via <code>pg_stat_activity</code> (connection count, idle_in_transaction, state transitions) and PgBouncer's <code>SHOW STATS</code> (queue depth, avg. query time). Act 6 (migrations) requires <code>pg_locks</code> to detect lock contention and <code>long_query_log</code> to catch blockers. Act 7 (sharding) requires application-level metrics: tail latency by shard, write variance across shards, cross-shard join frequency. Missing or misinterpreted tooling is why teams skip acts: "We can't see the bloat" means autovacuum visibility is off, not that bloat doesn't exist.</p>
    <p><strong>Schema mistakes and their escape hatches.</strong> A surrogate key that is not a primary key will cause join inefficiency — every foreign key reference is a two-column tuple, and the planner can't use the efficiency of SERIAL identity lookups. Fix: add a PK after the fact with <code>ALTER TABLE ... ADD CONSTRAINT ... PRIMARY KEY (...) NOT DEFERRABLE INITIALLY IMMEDIATE</code>, but this blocks all writes during the scan; use <code>NOT DEFERRABLE</code> to prevent transaction-mode evasion. A composite natural key (user_id, organization_id, email) that becomes the PK means every join carries three columns; foreign keys replicate the three columns. Fix: a surrogate id is cheaper at 1 BIGSERIAL vs 3 columns, but you can't retrofit it without a migration that rewrites the table. A missing FK on a child table means no referential integrity; the data decays over time and queries that assume referential integrity produce wrong results. Fix: add the FK retroactively, but you must first fix orphaned child rows. A missing CHECK constraint on a domain-specific column (e.g., status must be one of ACTIVE, PENDING, ARCHIVED) means the application is the only guard; a direct SQL update can create invalid state. Fix: add the constraint, but you must quarantine invalid existing rows and decide: delete, set to default, or correct by hand.</p>
    <p><strong>Index selection and the cost of wrong choices.</strong> A B-tree index on a low-cardinality column (e.g., gender with 2 values or status with 5 values) is almost always a waste; the planner chooses seq-scan instead, and the index eats write throughput. The leading-column rule: an index on (a, b, c) helps queries that filter by a, or by (a, b), or by (a, b, c), but NOT by b alone or c alone. A team that creates (user_id, created_at) to satisfy "find events for a user" then later wants "find recent events by anyone" creates a second index (created_at) because the first doesn't apply; they don't realize a third index (created_at, user_id) would help both queries and the planner would choose it based on the query. JSONB indexes (GIN or GIST) are powerful but expensive on writes; a JSONB column with 100K inserts/second can halve write throughput if you index it. Partial indexes (e.g., <code>CREATE INDEX ... WHERE status != 'ARCHIVED'</code>) solve this for dimension tables but are invisible to teams unfamiliar with Postgres; they rediscover partial indexing years later and wonder why they didn't use it earlier.</p>
    <p><strong>Planner statistics and the forensics of wrong plans.</strong> The planner's estimates are only as good as the last <code>ANALYZE</code>. On a table with 1M rows and autovacuum_analyze_scale_factor = 0.1 (default), the planner only re-analyzes after 100K new rows are inserted. If your table grows by 1M rows a day, the statistics are stale within hours. The fix: set autovacuum_analyze_scale_factor = 0.01 or smaller on high-growth tables. Extended statistics (<code>CREATE STATISTICS</code>) help the planner when columns are correlated; an example: a user_id and created_at are correlated (new users created recently are more likely to appear in recent data), and the planner's histogram estimates assume independence. <code>CREATE STATISTICS col1, col2 ON user_id, created_at</code> teaches the planner the correlation. The planner also has a work_mem limit (default 4MB) that affects sorts and hash joins; if the estimates are wrong and the actual data requires more work_mem than available, the query spills to disk and becomes very slow. The fix: re-ANALYZE, or increase work_mem; neither is a long-term solution.</p>
    <p><strong>Operational runbooks and escalation paths.</strong> The seven acts demand different operational disciplines. Act 1 (schema) requires a schema review process before table creation; the cost of a post-launch review is months of migrations. Act 2 (indexing) requires a before-and-after query plan for every proposed index and a monthly index utilization audit. Act 3 (planning) requires per-table autovacuum tuning and quarterly STATISTICS reviews; teams often leave default settings and wonder why queries degrade. Act 4 (bloat) requires a daily alerting rule: if n_dead_tup > 0.2 × n_live_tup or table_size > 1.5 × relation_size, page someone. Act 5 (pooling) requires connection metrics exported to your observability platform and alerts on pool saturation (all backends active). Act 6 (migrations) requires a runbook for every schema change: identity the AccessExclusiveLock window, validate that expand-contract steps fit your maintenance window, and co-ordinate with on-call to be ready for rollback. Act 7 (sharding) requires a quarterly resharding simulation on test data to validate that your chosen strategy scales to the next 10× of load. Teams that skip these runbooks operate reactively: Act 1 mistakes surface as multi-day outages during peak season; Act 6 mistakes surface as 1-hour freezes that take down the entire product.</p>
    <p><strong>Cross-shard queries and the federation tax.</strong> Once sharded (Act 7), queries that span multiple shards must be federated: send the query to each shard, collect results, and merge on the application side. A naive outer join across two shards becomes two inner queries (one per shard) and a client-side join. A GROUP BY that involves shards becomes a GROUP BY per shard, then a second GROUP BY on the aggregates. Each cross-shard query has latency = max(latency per shard) + merge time; tail latency dominates. Citus handles this with distributed query planning, but only for <code>co-located</code> tables (those with the same shard key). A reference table (replicating a small dimension across all shards) is free; a lookup table (replicated, immutable) costs replication time but amortizes over thousands of queries. Teams that skip federation planning discover at Act 7+1 that their ORM generates single-shard queries efficiently but breaks on cross-shard ones; they spend weeks retrofitting query logic or restructuring data to co-locate tables.</p>
    <Misconception id="m-just-add-index" lang="en">
      <strong>"If it's slow, add an index."</strong> Most common skip. Adding an index when stats are stale wastes write throughput. Adding one when bloated does nothing — vacuum first. When a hot shard is queried, it doesn't help. <em>Read the plan first.</em> Indexes are one lever of seven.
    </Misconception>
    <MetaphorComplete
      id="mm-1"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="xmin horizon: Postgres's xmin (creation transaction ID) is like a building's lease registry. When a lease expires (xmax set), the space is marked for reclamation but not freed until the oldest active lease owner (xmin horizon) has checked out. If one tenant won't leave, the whole building fills with empty units."
      accepted={["trash collector", "garbage collector", "cleanup worker"]}
      canonical="Trash collector"
      explanation="or garbage collector. The horizon is the oldest living tenant; when they leave, the horizon moves, dead space is freed."
    />
    <MetaphorComplete
      id="mm-2"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Table bloat: A warehouse receives shipments (inserts) and ships items (deletes). Deletes mark shelves as empty, but the empty shelves take up warehouse space. If no one cleans (VACUUM), the warehouse fills with empty shelves. Searching for a product now scans 10x more shelves because 9/10 are empty."
      accepted={["overflow", "full", "saturation"]}
      canonical="Overflow"
      explanation="or saturation, full. Dead tuples bloat the table because VACUUM hasn't reclaimed them yet."
    />
    <MetaphorComplete
      id="mm-3"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Connection pool saturation: A restaurant has 30 tables and a line of hungry customers waiting to dine. When all 30 tables are full, new customers wait outside. If customers don't leave fast enough, the line grows. If the kitchen suddenly slows down, every table is occupied longer, and the line freezes."
      accepted={["pool", "queue", "waiting room"]}
      canonical="Queue"
      explanation="or pool, waiting room. Saturation means all backends are active; new requests queue up and timeout."
    />
    <MetaphorComplete
      id="mm-4"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="AccessExclusiveLock: A library closes for renovations. No one can read, write, or even enter while the lock is held. Everyone outside waits. Anyone trying to enter after the lock begins also waits. The building is frozen."
      accepted={["lock", "freeze", "exclusive access"]}
      canonical="Freeze"
      explanation="or lock, exclusive access. AccessExclusiveLock blocks all other access and new queries queue up behind it."
    />
    <MetaphorComplete
      id="mm-5"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Shard key: A bank decides where to open branches. Branches divided by postal code (one shard per postal code) keep customers local; branches divided by customer age would scatter families across multiple cities."
      accepted={["postal code", "zip code", "address", "geography"]}
      canonical="Postal code"
      explanation="or zip code, address, geography. A shard key determines how data is distributed; choose one that keeps related queries local."
    />
    <MetaphorComplete
      id="mm-6"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Query planner statistics: An election pollster samples voters to predict the outcome. If the sample is stale (voters' opinions have changed), the prediction is wrong. If the sample is biased (only young voters), the prediction is skewed. The planner uses ANALYZE samples to guess row counts; stale or biased samples lead to bad plan choices."
      accepted={["sample", "histogram", "statistics"]}
      canonical="Sample"
      explanation="or histogram, statistics. The planner's decisions are only as good as the ANALYZE samples; stale stats lead to wrong plans."
    />
    <MetaphorComplete
      id="mm-7"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Resharding: A post office divides mail by zip code into 10 regional hubs. The city grows; now 15 hubs are needed. To rebalance, every piece of mail must be sorted again and routed to the new hub assignment. Changing the shard key (e.g., from zip to district) means every piece of mail is re-sorted from scratch."
      accepted={["rebalance", "redistribution", "resharding", "migration"]}
      canonical="Rebalance"
      explanation="or redistribution, resharding, migration. Changing shard count or key means all data must be re-routed; this is expensive and irreversible once live."
    />
    <MetaphorComplete
      id="mm-8"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup="Stale statistics: A weather forecast samples past observations to predict tomorrow's weather. If the sample is old (collected last winter), the forecast for today is useless. The planner's row-count estimates are stale when autovacuum hasn't re-run ANALYZE in weeks."
      accepted={["stale", "outdated", "old", "expired"]}
      canonical="Stale"
      explanation="or outdated, old, expired. Statistics older than the recent data distribution lead to bad plan choices."
    />
  </Fragment>

  <Fragment slot="senior">
    <p><strong>Cross-cutting observability: USE + RED for Postgres.</strong> The USE method (Utilization, Saturation, Errors) maps to: backend count vs `max_connections`, replication lag (`pg_stat_replication.replay_lag`), checkpoint pressure (`pg_stat_bgwriter.checkpoints_req` ratio), autovacuum lag (`pg_stat_user_tables.last_autovacuum`), buffer cache hit ratio (`pg_buffercache`). The RED method (Rate, Errors, Duration) on top of `pg_stat_statements`: queries/s per `queryid`, error rate from app logs (timeouts, deadlocks), p95/p99 duration. Together they tell you which lever to pull. A high backend count with low CPU is a pooling problem (Act 5). High dead tuples with stable row count is a vacuum problem (Act 4). A specific `queryid` with rising duration and stable plan_id is a data-growth problem (Act 2 or 7).</p>

    <p><strong>Anti-pattern catalogue when a stage is skipped.</strong> Skip Act 1 → a year later you spend three weeks renaming a column because the schema lacks a surrogate key. Skip Act 2 → every dashboard query is a 30-second seq-scan; the app team adds caches everywhere; the cache becomes the new source of truth and goes inconsistent. Skip Act 3 → you have an index, but the planner ignores it half the time; you write a runbook that says "if slow, restart Postgres" because that flushes the plan cache. Skip Act 4 → the OOM-killer takes Postgres down on Friday; the postmortem says "we'll add monitoring"; you don't. Skip Act 5 → every deploy triggers a connection storm; CI now spreads pod rollouts across 10 minutes "as a workaround." Skip Act 6 → migrations only run during a quarterly maintenance window because nothing else is safe; the queue of pending migrations grows. Skip Act 7 → you spend six months explaining to leadership that one tenant is using 40% of capacity and the only fix is a re-architecture.</p>

    <p><strong>RFC and paper-level grounding.</strong> Concurrency control: Postgres's MVCC is rooted in Bernstein-Goodman's 1981 work and Reed's 1978 thesis; the practical document is `src/backend/access/heap/README.HOT`. WAL: PostgreSQL's `pg_wal/` directory implementing ARIES-style write-ahead logging. Statistics: PostgreSQL planner uses histograms + most-common-values + correlation; the seminal reference is the Postgres source `src/backend/utils/adt/selfuncs.c`. Citus: the 2016 SIGMOD paper "Citus: Distributed PostgreSQL for Data-Intensive Applications" by Cetin et al. covers the architecture. XID wraparound: the 2 billion (2^31) transaction-id ceiling is a hard correctness limit; `autovacuum_freeze_max_age` controls preventive freezing.</p>

    <p><strong>Production observability stack for Postgres.</strong> Metrics scrape: `postgres_exporter` (Prometheus) for the standard set; `pg_stat_statements` and `pg_stat_kcache` enabled for query-level. Logs: structured (CSV or JSON) from Postgres itself, shipped via Vector/Fluent-bit to Loki or Elasticsearch. Slow-query introspection: `auto_explain` with `log_min_duration = '500ms'` for tail latency; `log_lock_waits = on` for lock cascades. Dashboards: Grafana with the `postgres_exporter` and `pgwatch2` boards. Alerting: budget burn on a 7-day rolling window for "% of `pg_stat_statements` total time spent in queries slower than 1 second."</p>

    <p><strong>XID wraparound and the autovacuum freeze.</strong> Every row carries a 32-bit transaction id; once the cluster reaches 2^31 transactions, ids wrap, and rows can appear from the future. Postgres protects against this by freezing old rows (rewriting `xmin` to a special "frozen" value) before wraparound. `autovacuum_freeze_max_age` is the trigger. On a write-heavy cluster (10 K TPS = 864 M transactions/day), the freeze runs every ~2.5 days. A misconfiguration (very high `autovacuum_freeze_max_age`, e.g. 1B) can lead to anti-wraparound emergency vacuums that lock the database. The remedy in production: monitor `age(datfrozenxid)` per database, alert at 80% of `autovacuum_freeze_max_age`.</p>

    <p><strong>Replication lag and read-replica gotchas.</strong> Async replication has an unbounded lag under write load; sync replication trades latency for durability. Hot-standby read replicas serve reads at staleness bounded by `replay_lag`. The trap: long-running queries on the replica delay WAL replay (the recovery process must wait for the snapshot to release before applying conflicting changes), creating cascading lag. Defence: `hot_standby_feedback = on` to keep the replica's `xmin` horizon pushed up to the primary so vacuum doesn't reclaim rows the replica still needs — but this bloats the primary instead. There is no free lunch.</p>

    <p><strong>Sharded systems in 2026 — pragmatic choices.</strong> Citus is the standard for Postgres-native sharding (CrunchyData, AWS RDS, Microsoft Azure CDB-for-PG). Vitess is the standard for MySQL. Aurora DSQL (AWS, 2024) and Spanner (GCP) cover global ACID-on-distributed-storage if budget allows. CockroachDB and TiDB are NewSQL alternatives. Real-world: GitHub partitioned MySQL before migrating critical paths to Vitess; Figma scaled with Vitess; Notion ran for years on a single very-large RDS Postgres and only recently considered Citus. The 2026 default for a new Postgres-shop scaling past one node: declarative partitioning first (single node, fewer ops), Citus when one node truly cannot keep up, Aurora DSQL when global writes are a real product requirement.</p>

    <p><strong>Migration tooling landscape — 2026 snapshot.</strong> Atlas (Ariga), pgroll (Xata), Liquibase, Flyway, sqitch, golang-migrate, Prisma Migrate, Drizzle Kit, TypeORM migrations, Django migrations. The pragmatic 2026 stack for Postgres: `pgroll` for online schema changes (declarative expand-contract), Atlas for declarative schema diff + migration generation, Liquibase if your shop is Java-heavy, and bespoke SQL files versioned with `golang-migrate` for everything that doesn't fit. Avoid: `prisma migrate dev` in production (designed for dev loops, makes data-loss assumptions); blind `python manage.py migrate` on tables larger than 1M rows.</p>

    <p><strong>Capacity planning for a single Postgres before sharding.</strong> Modern hardware (2026 Hetzner CAX21 or AWS r6g.4xlarge): 16 vCPU, 64 GB RAM, NVMe SSD, can sustain 30-50 K simple OLTP TPS, ~10 K complex transactions/s, 5-10 GB/s sequential read for analytics queries. Single-node ceiling for a well-tuned Postgres: 50 K TPS, 1-5 TB data, 99.99% availability with one read replica. Past this, sharding is the only lever. The mental model: every order of magnitude in scale adds a new operational concern. 1 TB is "we should automate backups." 10 TB is "we need a recovery strategy." 100 TB is "we need a team."</p>

    <p><strong>Catalog overhead and schema-based sharding limits.</strong> Schema-based multi-tenancy ("one schema per tenant") works beautifully at low tenant count: clean isolation, easy backups, simple data export. But Postgres catalog tables (`pg_class`, `pg_attribute`, `pg_constraint`) grow linearly with schema count × tables-per-schema. At 10000 schemas × 50 tables, `pg_class` has half a million rows; planner walk-time on queries that touch multiple schemas climbs to seconds. The practical ceiling is 1000-3000 tenants per cluster with schema-based sharding. Past that, row-level multi-tenancy (`tenant_id` column on every table) with Citus shard distribution scales further.</p>

    <p><strong>Connection pooling in 2026 — landscape.</strong> PgBouncer remains the standard (~50 K req/s per instance). Odyssey (Yandex) offers similar performance with hot-reload config. PgCat is a Rust port with multi-tenant routing primitives. Supavisor (Supabase) is a managed alternative for Postgres-as-a-service. The 2026 default: PgBouncer 1.21+ for self-managed; managed pooling from your cloud provider otherwise. Antipattern: running a pooler per app pod — defeats multiplexing.</p>

    <p><strong>Failure modes and their costs at scale.</strong> Act 1 failures (wrong schema) cost months of migration work at scale; Act 2 failures (missing indexes) cost days of emergency index tuning; Act 3 failures (stale stats) cost hours of plan diagnosis; Act 4 failures (bloat) cost minutes of query slowdown but hours of remediation if unaddressed; Act 5 failures (pool saturation) cost seconds of timeout storms but can cascade into cascading application retries that amplify the impact; Act 6 failures (lock contention) cost minutes of database freeze; Act 7 failures (wrong shard key or no sharding when needed) cost weeks or months of data reshuffling. The cost ratio is exponential: early acts are irreversible, later acts are reversible but operationally expensive. Teams that skip early acts pay catastrophic costs later. Teams that over-engineer early acts (e.g., sharding a 10 GB dataset) waste resources on problems that don't exist yet. The art is knowing your growth trajectory and your cost tolerance, then timing each act to land just before the prior act's failure modes become production pain.</p>

    <p><strong>When NOT to apply the seven-act model.</strong> The model assumes traditional OLTP: rows in, rows out, queries that care about latency. Skip Act 2 entirely if your queries are analytical (full table scans by design); instead, use columnstore indexes (Citus, Timescale) or materialized views. Skip Acts 1–6 if you're using a managed cloud database that abstracts them away (e.g., Aurora Serverless, Supabase). Skip Act 7 if your traffic is bursty and stateless (serverless functions querying a managed database); the database provider handles sharding and failover. Skip Act 3 if you have workloads where stale statistics don't matter (write-only logs, append-only event streams). The seven-act walk is a mental model for classical Postgres at scale, not a dogma. The deeper skill is knowing when to apply it and when to step outside the model entirely. Most teams operate at Acts 1–3; many never reach Act 5 because their traffic stays below the pool-saturation threshold; only hyperscale shops regularly face Acts 6–7. Your job is to know the model well enough to recognize which act you're in, then decide whether the next act is worth the engineering cost right now.</p>

    <p><strong>The integration with the rest of the curriculum.</strong> This piece sits at the seam of databases, performance (chapter 13), observability (chapter 11), and engineering practice (chapter 16). Every lever you pulled here has cousins elsewhere: caching (chapter 7) sits in front of indexes and plans; queues (chapter 8) absorb write spikes that would otherwise overwhelm Act 5's pool; distributed-systems (chapter 9) names the consistency questions that drove Act 7's sharding decisions. The fullstack engineer's job is not just to know Postgres but to know which lever costs least at each layer of the system as a whole.</p>

    <section id="sr-tradeoff-matrix" data-tradeoff class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Lever trade-offs per growth tier</h3>
      <div class="overflow-x-auto"><table class="w-full text-[13px] border-collapse"><thead><tr class="border-b-2 border-gray-300"><th class="text-left px-3 py-2 font-semibold">Lever</th><th class="text-left px-3 py-2 font-semibold">Reversible?</th><th class="text-left px-3 py-2 font-semibold">When too early</th><th class="text-left px-3 py-2 font-semibold">When too late</th></tr></thead><tbody><tr class="border-b border-gray-200"><td class="px-3 py-2">Schema (01)</td><td class="px-3 py-2">No (data lives in it)</td><td class="px-3 py-2">Speculative future-proofing</td><td class="px-3 py-2">Migration cost at scale</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">Index (02)</td><td class="px-3 py-2">Yes (drop is instant)</td><td class="px-3 py-2">Write penalty without query benefit</td><td class="px-3 py-2">Slow queries already in prod</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">Plan / stats (03)</td><td class="px-3 py-2">Yes (run ANALYZE)</td><td class="px-3 py-2">Premature CREATE STATISTICS noise</td><td class="px-3 py-2">Tail-latency incidents</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">MVCC discipline (04)</td><td class="px-3 py-2">Yes (kill long tx)</td><td class="px-3 py-2">Aggressive timeouts cancel valid work</td><td class="px-3 py-2">Bloat costs disk + IO</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">Pool (05)</td><td class="px-3 py-2">Yes (remove pooler)</td><td class="px-3 py-2">Adds a network hop for tiny apps</td><td class="px-3 py-2">Connection-storm outages</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">Migration recipe (06)</td><td class="px-3 py-2">Yes (skip until needed)</td><td class="px-3 py-2">Adds 5 steps where 1 worked</td><td class="px-3 py-2">Lock-queue freezes prod</td></tr><tr class="border-b border-gray-200"><td class="px-3 py-2">Sharding (07)</td><td class="px-3 py-2">Partially (resharding is hard)</td><td class="px-3 py-2">Operational complexity tax</td><td class="px-3 py-2">One tenant dominates</td></tr></tbody></table></div>
    </section>

    <section id="sr-design-1" data-design-prompt class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Design Prompt</h3>
      <p class="text-bbg-ink mb-3 text-[14.5px] font-medium">You inherit a Postgres at 200M rows, p95 1500 ms, 600 concurrent backends, 40 GB bloat, no pooler, single node. Three weeks to stabilize. Plan the order of work and justify each step.</p>
      <details class="group"><summary class="cursor-pointer font-semibold text-bbg-teal hover:text-bbg-teal/80">Show rubric</summary><div class="mt-3 pl-4 border-l-2 border-bbg-teal text-[13.5px] space-y-2"><p><strong>First triage:</strong> kill long transactions, set idle_in_transaction_session_timeout — stop the bleeding (Act 4).</p><p><strong>Stabilize connections:</strong> deploy PgBouncer in transaction-mode, size pool from active-tx, not workers (Act 5).</p><p><strong>Reclaim disk:</strong> pg_repack the bloated tables online (Act 4 cleanup).</p><p><strong>Re-establish plan quality:</strong> ANALYZE + CREATE STATISTICS for correlated columns; review pg_stat_statements top-N (Act 3).</p><p><strong>Index audit:</strong> drop unused indexes, add the obviously missing ones based on top-N (Act 2).</p><p><strong>Only then consider sharding (Act 7)</strong> — and only after capacity calculations show a single node truly cannot keep up.</p></div></details>
    </section>

    <section id="sr-trace-1" data-trace-scenario class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Trace Scenario</h3>
      <p class="text-bbg-ink mb-4 text-[14.5px] font-medium">On-call page: replication lag is 12 minutes and rising. Trace the cause.</p>
      <ol class="space-y-3 text-[13.5px]"><li><strong>Step 1:</strong> Confirm the lag. <span class="text-bbg-muted italic">Reveal: SELECT now() - pg_last_xact_replay_timestamp() on the replica. Confirm rising trend on the dashboard.</span></li><li><strong>Step 2:</strong> Is the primary write-rate normal? <span class="text-bbg-muted italic">Reveal: Check pg_stat_database tup_inserted/updated/deleted growth. Normal.</span></li><li><strong>Step 3:</strong> Is the replica I/O-bound? <span class="text-bbg-muted italic">Reveal: iostat on the replica shows the WAL apply process is CPU-bound, not I/O.</span></li><li><strong>Step 4:</strong> Why is replay slow? <span class="text-bbg-muted italic">Reveal: pg_stat_activity on the replica shows a long-running analytics query holding a snapshot. WAL replay must wait for snapshot release on conflict. hot_standby_feedback = on or kill the query.</span></li><li><strong>Step 5:</strong> Long-term fix. <span class="text-bbg-muted italic">Reveal: Move analytics to a dedicated replica with hot_standby_feedback off (and accept staleness) or to a separate analytical store fed by logical replication.</span></li></ol>
    </section>

    <section id="sr-trace-2" data-trace-scenario class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Trace Scenario</h3>
      <p class="text-bbg-ink mb-4 text-[14.5px] font-medium">A planned tenant_id NOT NULL migration freezes prod for 14 minutes despite the team using 'safe' tooling. Trace the failure.</p>
      <ol class="space-y-3 text-[13.5px]"><li><strong>Step 1:</strong> What did the tooling do? <span class="text-bbg-muted italic">Reveal: Atlas generated ALTER TABLE ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 0. Postgres ≥ 11 makes ADD COLUMN ... NOT NULL DEFAULT constant cheap (no rewrite), so the rewrite isn't the cause.</span></li><li><strong>Step 2:</strong> Then what blocked? <span class="text-bbg-muted italic">Reveal: AccessExclusiveLock acquisition queued behind a long-running pg_dump session that held AccessShareLock for the table. Every new query queued behind the ALTER.</span></li><li><strong>Step 3:</strong> Why didn't the team see this? <span class="text-bbg-muted italic">Reveal: Local testing didn't include a concurrent pg_dump. Pre-prod lacked a long-running read that mirrored prod.</span></li><li><strong>Step 4:</strong> The right runbook fix? <span class="text-bbg-muted italic">Reveal: Set lock_timeout = '2s' on the migration; check pg_stat_activity for AccessShareLock holders before running ALTER; or run the migration during a maintenance window that excludes backup processes.</span></li></ol>
    </section>

    <section id="sr-quiz-1" data-quiz class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Quiz</h3>
      <p class="text-bbg-ink mb-4 text-[14.5px]">What is the role of `backend_xmin` in pg_stat_activity for diagnosing bloat?</p>
      <div class="space-y-2 text-[13.5px]">
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-1" class="mt-1" /><span><strong>It is the oldest transaction id this backend can still see; the cluster xmin horizon is the min of all such values</strong><br /><span class="text-bbg-success font-semibold">Correct</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-1" class="mt-1" /><span><strong>It is the transaction id of the next query</strong><br /><span class="text-bbg-warn font-semibold">Wrong direction.</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-1" class="mt-1" /><span><strong>It is the WAL position the backend has replicated to</strong><br /><span class="text-bbg-warn font-semibold">That's pg_stat_replication, not backend_xmin.</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-1" class="mt-1" /><span><strong>It is the timestamp of the last query</strong><br /><span class="text-bbg-warn font-semibold">Not a timestamp.</span></span></label>
      </div>
    </section>

    <section id="sr-quiz-2" data-quiz class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Quiz</h3>
      <p class="text-bbg-ink mb-4 text-[14.5px]">Why does `hot_standby_feedback = on` shift bloat from the replica to the primary?</p>
      <div class="space-y-2 text-[13.5px]">
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-2" class="mt-1" /><span><strong>The replica pushes its oldest snapshot up to the primary, preventing primary vacuum from reclaiming rows still needed for replica reads</strong><br /><span class="text-bbg-success font-semibold">Correct</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-2" class="mt-1" /><span><strong>It corrupts the WAL</strong><br /><span class="text-bbg-warn font-semibold">No corruption.</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-2" class="mt-1" /><span><strong>It forces synchronous replication</strong><br /><span class="text-bbg-warn font-semibold">It's about visibility, not durability.</span></span></label>
        <label class="flex items-start gap-3 cursor-pointer"><input type="radio" name="sr-quiz-2" class="mt-1" /><span><strong>It disables the replica's vacuum</strong><br /><span class="text-bbg-warn font-semibold">Vacuum on standby is recovery-driven; the flag affects xmin propagation.</span></span></label>
      </div>
    </section>

    <section id="sr-retrieval" data-retrieval-drawer class="relative rounded-2xl border-2 border-gray-200 bg-white p-6 transition-opacity duration-200 my-8">
      <h3 class="text-bbg-ink font-semibold mb-4 text-[15.5px]">Retrieval Question</h3>
      <p class="text-bbg-ink mb-4 text-[14.5px]">Explain how the seven acts compound: pick one early skip and trace its downstream cost.</p>
      <details class="group"><summary class="cursor-pointer font-semibold text-bbg-teal hover:text-bbg-teal/80">Show answer</summary><div class="mt-3 pl-4 border-l-2 border-bbg-teal text-[13.5px] leading-relaxed"><p><strong>Skip Act 1 (schema):</strong> use email as the natural key. Act 2 indexes are forced to support text equality on a mutable field. Act 3 plan stability degrades because the planner's column statistics for high-cardinality text columns are less stable than for surrogate integer keys. Act 6 migrations to change email-validation rules require a full table rewrite (a surrogate id would have been a simple column update). Act 7 sharding by email is awful — co-location across users/events/orgs becomes impossible because email is a property of one entity, not a tenant. <strong>Total cost: 10× the engineering effort at Act 7.</strong></p></div></details>
    </section>

    <MetaphorComplete
      id="sr-mm-1"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup={'USE and RED methods: A hospital tracks every patient\'s vital signs (Utilization), wait times (Saturation), and infection outcomes (Errors). The full picture — not just "bed availability" — tells doctors which intervention matters most. Postgres USE/RED is the same: backend count, query latency, and error rate together reveal whether you\'re IO-bound, CPU-starved, or connection-saturated.'}
      accepted={["monitor", "measure", "observe"]}
      canonical="Monitor"
      explanation={'or measure, observe. Observability methods give you the full signal; partial views (e.g., "backend count is high") lie.'}
    />

    <MetaphorComplete
      id="sr-mm-2"
      pieceSlug="08-putting-it-together"
      lang="en"
      setup={'The seven-act walk during an incident: An ambulance arrives at the hospital with a critical patient. The ER doesn\'t debug the entire health history first; it follows the ABCDE protocol (Airway, Breathing, Circulation, Disability, Exposure) in order, stabilizing each before moving to the next. When a production database is melting down, you don\'t blame schema design (Act 1); you kill long transactions (Act 4), then deploy a pooler (Act 5), then optimize queries (Act 2–3). Once stability is restored, you postmortem and write runbooks for each act to prevent the next incident. The seven-act model is both a design framework (uptime) and a triage framework (outtime).'}
      accepted={["postmortem", "runbook", "incident", "debugging"]}
      canonical="Postmortem"
      explanation="or runbook, incident, debugging. During outages, you triage in order (Act 4 → 5 → 2–3), then postmortem to prevent repeats."
    />
  </Fragment>
</TierAccordion>

## Synthesis sandbox

<DBLeverSandbox client:visible lang="en" />

<KeyTakeaway>Seven pieces. Seven scale tiers. Each skipped lever pays back at the next tier, more expensive. Read the plan, not the symptom.</KeyTakeaway>

<SpiralCue thread="statefulness" lang="en">State lives at every layer — schema, plans, MVCC snapshots, pool sessions, shard maps. Every act is one stateful layer giving way.</SpiralCue>

<SpiralCue thread="latency" lang="en">Each lever buys back a band of latency the prior layer leaked. Round-trip math beats micro-optimisation here too.</SpiralCue>

<SpiralCue thread="multiplexing" lang="en">Pooling and sharding multiplex limited resources (backends, nodes) across many callers. The math is the same as TCP streams.</SpiralCue>

## Where to go next

- Back to schema design: [01 — relational model](/en/databases/01-relational-model)
- Back to indexes: [02 — indexes](/en/databases/02-indexes)
- Back to plans: [03 — execution plans](/en/databases/03-execution-plans)
- Back to MVCC: [04 — MVCC and isolation](/en/databases/04-mvcc-isolation)
- Back to pooling: [05 — pooling](/en/databases/05-pooling)
- Back to migrations: [06 — migrations](/en/databases/06-migrations)
- Back to sharding: [07 — sharding](/en/databases/07-sharding)

Next chapter: [Caching all the way down →](/en/caching/)
