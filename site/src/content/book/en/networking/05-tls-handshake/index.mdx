---
slug: 05-tls-handshake
lang: en
pillar: networking
chapter: 01-networking
order: 5
title: "TLS 1.3: keys and confidentiality in one RTT"
summary: "How ClientHello key shares plus a certificate verifier collapse the old TLS 1.2 two-RTT handshake into one — and what 0-RTT actually buys you."
readingMin: 16
status: ready
prereqs: ["03-tcp-handshake"]
spiral: ["encapsulation", "statefulness"]
personas: ["bea", "sven", "cara"]
depth:
  mechanism: tier-mechanism
  tradeoff: card-rtt-cost
  failure_mode: r-0rtt-replay
  numbers: card-tls-numbers
sources:
  - https://datatracker.ietf.org/doc/html/rfc8446
  - https://datatracker.ietf.org/doc/html/rfc8470
  - https://datatracker.ietf.org/doc/html/rfc5869
  - https://datatracker.ietf.org/doc/html/rfc8701
  - https://datatracker.ietf.org/doc/draft-ietf-tls-ecdhe-mlkem/
  - https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
  - https://blog.cloudflare.com/tls-session-resumption-full-speed-and-secure/
  - https://blog.cloudflare.com/announcing-encrypted-client-hello/
  - https://docs.kernel.org/networking/tls.html
  - https://letsencrypt.org/2024/12/05/ending-ocsp
  - https://hacks.mozilla.org/2025/08/crlite-fast-private-and-comprehensive-certificate-revocation-checking-in-firefox/
  - https://hpbn.co/transport-layer-security-tls/
  - https://developer.mozilla.org/en-US/docs/Web/Security/Transport_Layer_Security
---

import TierAccordion from "../../../../../components/pedagogy/TierAccordion.astro";
import FadedExample from "../../../../../components/pedagogy/FadedExample.tsx";
import RetrievalDrawer from "../../../../../components/pedagogy/RetrievalDrawer.tsx";
import Crux from "../../../../../components/prose/Crux.astro";
import SpiralCue from "../../../../../components/prose/SpiralCue.astro";
import PersonaTag from "../../../../../components/pedagogy/PersonaTag.astro";
import NumbersCard from "../../../../../components/layout/NumbersCard.astro";
import Misconception from "../../../../../components/layout/Misconception.astro";

TCP agreed on sequence numbers, DNS resolved a name to an IP — now <PersonaTag id="bea" lang="en" /> (browser) and <PersonaTag id="sven" lang="en" /> (server) must agree on encryption before sending anything that matters. That encryption is negotiated in the TLS handshake. For decades, this negotiation took two round-trips over the network. TLS 1.3 cut it to one by moving cryptographic work forward: the client sends not just supported ciphers but its own ephemeral key in the opening message, letting the server derive a shared secret and send encrypted data in the same RTT.

<Crux>How key shares and ECDHE collapse TLS 1.2's two-RTT handshake into one, and what 0-RTT buys you.</Crux>

The goal of TLS is to build a sealed channel: one that anyone can intercept but only the sender and receiver can read. Before TLS 1.3, building that channel required at least two round-trips. TLS 1.3 redesigned the handshake to move key material into the first message, enabling the server to compute the shared secret and send encrypted application data without waiting. The trade-off: cold connections still cost one round-trip before the first encrypted byte, but warm connections (resumption via pre-shared keys) cost zero.

<TierAccordion id="tier-mechanism" lang="en">
  <Fragment slot="junior">
    <p><strong>What TLS does in one sentence.</strong> It builds a sealed envelope around everything <PersonaTag id="bea" lang="en" /> sends to <PersonaTag id="sven" lang="en" /> so anyone in between — coffee-shop wifi, your ISP, a corporate proxy — can see that <em>something</em> went past, but cannot read <em>what</em>.</p>
    <p><strong>Why care.</strong> Plain HTTP shows every word you type to anyone on the same network. Your password, your bank balance, the article you are reading — all of it. TLS turns the same wire into a locked tunnel that only the website on the other end can open.</p>
    <p><strong>The metaphor.</strong> Bea has a message. She wants to mail it to Sven, but she fears the mailman will read it. So she puts it in a box, locks it with a seal, and mails it. On the way, anyone can see the box, but no one can read what is inside — only Sven, who has a matching key, can unlock it. Before mailing, though, Bea and Sven need to agree on the seal and the key. They meet online and negotiate: "Let's use AES as the lock, SHA-256 as the wax, and here's my half of the key." Sven replies: "I got it, here's my half." They combine the halves, derive a shared seal, and every message Bea sends from then on is locked with it. TLS 1.3 made the meeting shorter by letting Bea send her half of the key in the first letter, instead of waiting for Sven to ask.</p>
    <p><strong>Dialogue you can read out loud.</strong> Bea: "Hi Sven, here are the locks I know how to use, and here is my temporary key." Sven: "Good, I pick this lock and here is my temporary key plus my ID card from <PersonaTag id="cara" lang="en" /> the trusted issuer." Bea looks at the ID, checks the issuer is one she trusts, mixes their keys together, and writes back encrypted: "Got it." Now both sides agree on the same secret seal, and every message after that is sealed.</p>
    <p><strong>One scenario, end to end.</strong> You type <code>https://</code> in front of a website. Your browser already finished the steps in <a href="/en/networking/03-tcp-handshake/">TCP handshake</a> and <a href="/en/networking/04-dns-resolution/">DNS resolution</a>; it knows where the server lives. Now it builds the TLS seal you just learned. Once the seal is built, every subsequent HTTP request and response — the topic of <a href="/en/networking/06-http-versions/">HTTP versions</a> — rides inside it. The browser shows you a small padlock to signal: the sealed channel is alive.</p>
    <p><strong>Forbidden shortcuts (read these as warnings, not facts).</strong> "https means I am safe from everything" — no, it only protects the channel; the website itself can still be malicious. "the padlock means the website is the real one" — only the channel to that hostname is verified, not the intent of whoever owns the domain. "my password is safe in any browser" — only safe in transit; if the device is compromised, no envelope protects you.</p>
  </Fragment>
  <Fragment slot="middle">
    <p><strong>The 1-RTT handshake dance.</strong> <PersonaTag id="bea" lang="en" /> sends <code>ClientHello</code>, which includes the TLS version, supported cipher suites, <strong>and an ephemeral ECDHE public key (key share)</strong>. <PersonaTag id="sven" lang="en" /> receives it, picks a cipher suite and curve, generates his own ephemeral key pair, and replies with <code>ServerHello</code> containing his public key. Both sides now have each other's ephemeral public keys and can derive a shared secret using elliptic-curve Diffie-Hellman (ECDHE). Sven signs his server certificate (proof he's really google.com) with his long-term private key and sends <code>Certificate</code> and <code>CertificateVerify</code>. <PersonaTag id="cara" lang="en" /> (the certificate authority) issued that certificate, and Bea carries Cara's public key in her root store. Bea verifies the certificate chain, checks Sven's signature on the handshake transcript, and computes the shared secret. She sends <code>Finished</code> containing an HMAC of the entire handshake, proving she computed the same secret. From this point on, all data between them is encrypted with keys derived from the shared secret. Cost: one full RTT (ClientHello and ServerHello cross the wire once each).</p>
    <p><strong>Perfect Forward Secrecy (PFS).</strong> Both the client and server use ephemeral (temporary) key pairs, generated fresh for this connection. If an attacker later steals Sven's long-term private key from his server's disk, the attacker <strong>cannot decrypt past sessions</strong> because the past sessions used ephemeral keys that are already deleted. This is why PFS matters: compromise of the server today does not leak yesterday's secrets. RSA key transport (the old TLS 1.2 mode) avoided this: the client encrypted the shared secret with the server's long-term public key, so stealing the private key meant decrypting all past sessions. TLS 1.3 mandates ephemeral key exchange, making PFS the default.</p>
    <p><strong>Session resumption via PSK (pre-shared key).</strong> After the handshake, the server sends a <code>NewSessionTicket</code> — an encrypted ticket containing a pre-shared key and metadata. The browser caches it. On the next connection to the same server, Bea sends the same <code>ClientHello</code> but now includes a <code>pre_shared_key</code> extension with the ticket. Sven decrypts the ticket, recognizes it, and skips certificate validation. He derives the shared secret from the PSK instead of ECDHE, and both sides continue. Cost: still one full RTT, but no expensive cryptographic operations on the server.</p>
    <p><strong>0-RTT mode.</strong> If a browser has a cached PSK, it can encrypt the first request (e.g., a GET) using the PSK's key, and send the encrypted data inside the <code>ClientHello</code> itself. The server decrypts it immediately, processes the request, and responds — all within the same network round-trip as the handshake. Cost: zero RTT for the request. But this safety feature comes with a caveat: <strong>replay vulnerability.</strong> An attacker on the wire captures the 0-RTT ClientHello, and can resend it multiple times. If the request is "GET /news", replays are harmless. If it's "POST /transfer" with amount and recipient, replays are exploitable.</p>
    <p><strong>ECDHE math walkthrough.</strong> The cryptography behind the key share is elliptic-curve Diffie-Hellman over a fixed curve — usually <code>x25519</code> for performance, or <code>P-256</code> when FIPS compliance is required. Both sides agree on a curve, then each generates a private scalar (a random 256-bit integer) and computes the public point: <code>public = private · G</code>, where <code>G</code> is the curve's base generator. Bea sends her public point in the key share, Sven sends his back. Each side then multiplies the other's public point by their own private scalar: Bea computes <code>shared = bea_private · sven_public</code>, Sven computes <code>shared = sven_private · bea_public</code>. The math guarantees both arrive at the same point on the curve, and an eavesdropper who saw only the public points cannot recover the shared point without solving the elliptic-curve discrete-log problem (intractable for properly sized curves on classical hardware). The X-coordinate of the shared point becomes the input to the HKDF key schedule.</p>
    <p><strong>Transcript hash and Finished.</strong> Every byte sent during the handshake is fed into a running SHA-256 (or SHA-384, depending on the cipher suite) digest called the transcript hash. When both sides compute the <code>Finished</code> message, it is <code>HMAC(finished_key, transcript_hash)</code> over everything seen so far. The receiver recomputes the same HMAC and compares; if a single bit on the wire was flipped or replaced — by a network bug or by a man-in-the-middle — the HMAC will not match and the connection aborts. This is the integrity backbone of TLS 1.3: not only is data encrypted, but the handshake itself cannot be tampered with after the fact. Downgrade attacks (forcing the connection to TLS 1.2 or weaker ciphers) are detected by this exact mechanism, because the transcript records the version the client originally proposed.</p>
    <p><strong>Certificate chain validation step by step.</strong> When <PersonaTag id="bea" lang="en" /> receives <code>Certificate</code>, she sees a list of X.509 certificates: the leaf certificate for the server's hostname, then one or more intermediate certificates, then implicitly the trusted root. She walks the chain bottom-up: she verifies the leaf was signed by the intermediate's public key, the intermediate was signed by the root's public key, and the root is in her local trust store (the CA bundle shipped with the OS or browser). She also checks the leaf's Subject Alternative Names (SANs) include the hostname she is connecting to, the validity period covers "now," and the certificate has not been revoked (modern browsers use CRLite or short-lived certificates rather than runtime OCSP). If any step fails, the connection terminates with an alert. The whole walk takes microseconds on modern hardware; the only slow part is the public-key signature verification (one HMAC-like operation per certificate in the chain, plus the server's own <code>CertificateVerify</code> signature).</p>
    <p><strong>Key schedule overview.</strong> Once both sides have the ECDHE shared secret, TLS 1.3 derives a tree of keys from it using HKDF. The tree has three levels: early secret (from a PSK if present, or zero), handshake secret (from ECDHE), master secret (the root for application data). From the handshake secret come the client and server handshake traffic keys, used to encrypt everything between <code>ServerHello</code> and <code>Finished</code>. From the master secret come the application traffic keys, used for actual data after the handshake. Each level is derived with <code>HKDF-Extract</code> mixing in the previous secret plus new entropy, then <code>HKDF-Expand-Label</code> producing the actual key bytes. The labels (<code>"c hs traffic"</code>, <code>"s hs traffic"</code>, <code>"c ap traffic"</code>, <code>"s ap traffic"</code>) keep the keys domain-separated so that compromising one cannot help decrypt another.</p>
    <p><strong>Cipher suite vs key-exchange separation.</strong> TLS 1.2 mashed three choices into one cipher suite string: the key-exchange algorithm (RSA, DHE, ECDHE), the bulk cipher (AES-CBC, AES-GCM), and the MAC (SHA, SHA256). TLS 1.3 split these. The named-group negotiation picks the curve for ECDHE (<code>x25519</code>, <code>secp256r1</code>, …), the signature-algorithm negotiation picks how the server certificate signature is verified (<code>ed25519</code>, <code>ecdsa_secp256r1_sha256</code>, …), and the cipher-suite negotiation picks only the AEAD cipher and hash for the key schedule (<code>TLS_AES_128_GCM_SHA256</code>, <code>TLS_CHACHA20_POLY1305_SHA256</code>, <code>TLS_AES_256_GCM_SHA384</code>). This separation reduced the combinatorial explosion of TLS 1.2 cipher suites from hundreds to just five standard suites in TLS 1.3.</p>
    <p><strong>When 0-RTT is safe and when it is not.</strong> The simplest rule: 0-RTT is only safe for idempotent requests where replaying the same request multiple times produces the same observable result and side effects. GET, HEAD, OPTIONS are typically safe. POST that creates a row, PUT that overwrites state, DELETE that removes data — all unsafe. The HTTP specification (RFC 9110) defines which methods are idempotent, but practical safety depends on the application: a "GET /cart/add?item=42" that mutates a cart is technically GET but semantically a mutation, and replays double the cart contents. Frameworks should mark routes explicitly as <em>0-RTT-eligible</em> or <em>0-RTT-forbidden</em>, never relying on HTTP method alone. The server-side enforcement is RFC 8470: respond with <code>425 Too Early</code> to ineligible 0-RTT requests and the client retries after the handshake completes.</p>
    <p><strong>Handshake step-by-step (full play-by-play).</strong> Step 1: Bea sends <code>ClientHello</code> carrying the random client nonce, list of supported cipher suites, list of supported named groups, list of supported signature algorithms, and a key_share extension with at least one ECDHE public key. Step 2: Sven inspects the list, picks one cipher suite (e.g. <code>TLS_AES_128_GCM_SHA256</code>), picks one named group from key_share, generates his own ECDHE keypair, and sends <code>ServerHello</code> with his server nonce, the chosen cipher suite, and his key_share. Step 3: Sven derives the handshake secret via HKDF, encrypts the remaining handshake messages with the server handshake traffic key, and sends <code>EncryptedExtensions</code>, <code>Certificate</code>, <code>CertificateVerify</code>, and <code>Finished</code> back-to-back. Step 4: Bea derives the same handshake secret, decrypts the encrypted handshake messages, validates the certificate chain, validates the <code>CertificateVerify</code> signature over the transcript, validates the server <code>Finished</code> HMAC, sends her own client <code>Finished</code>, and starts sending application data encrypted with the client application traffic key. Step 5: Sven receives the client <code>Finished</code>, validates it, and the handshake is complete on both sides. Total round-trips on the wire: one (steps 1–2 plus 3 happen in one RTT; steps 4–5 finish in parallel).</p>
    <p><strong>The Misconception block before you scroll down.</strong> Three things people commonly get wrong about TLS 1.3 even after reading the RFC the first time. One: "TLS 1.3 means PFS, so I can use any session resumption I want." False — session resumption via PSK without an additional ECDHE exchange in the resumption ClientHello loses PFS for that resumed session. Two: "0-RTT is a free performance win." False — see the entire replay-window machinery the senior tier covers; treat 0-RTT as an opt-in optimization for read-only routes only. Three: "Once the certificate chain validates, I am done with trust." False — certificate revocation, SCT (Signed Certificate Timestamp) checking, and HSTS preload all add layers a complete TLS client honours.</p>
    <p><strong>HelloRetryRequest.</strong> Sometimes the client's first guess at a named group is wrong — it offered <code>x25519</code> in key_share, but the server only supports <code>secp384r1</code>. Instead of failing the handshake, TLS 1.3 defines <code>HelloRetryRequest</code> (HRR): the server responds with a ServerHello-shaped message containing the cookie extension and the named group it actually wants. The client retries with a fresh ClientHello that includes the correct key share. This adds one RTT but is rare in practice because browsers offer multiple key shares up front (typically <code>x25519</code> plus <code>secp256r1</code>) to make HRR almost never trigger. HRR is also how stateless servers handle a pseudo-stateless handshake: the cookie carries enough information to resume on a different backend node, useful behind load balancers that do not pin connections.</p>
    <p><strong>SNI: server name indication.</strong> Many websites share one IP address: a CDN edge serves thousands of customer domains from the same listener. Without help, the server cannot pick the right certificate before reading the request, because the request is encrypted. The Server Name Indication extension solves this: the client sends the hostname in plain text inside ClientHello, and the server uses it to look up the right certificate before responding. The privacy cost: anyone on the wire learns which hostname you visited. That is the entire motivation for Encrypted ClientHello (ECH), covered in the senior tier. SNI is not optional in modern deployments — any host without it cannot disambiguate between virtual hosts and will return whichever default certificate the server has configured.</p>
    <p><strong>ALPN: application-layer protocol negotiation.</strong> After the TLS channel is built, the two peers need to agree on what runs on top: HTTP/1.1, HTTP/2, HTTP/3, or something else. ALPN solves this by including a list of protocol identifiers in the ClientHello — typically <code>["h2", "http/1.1"]</code> for a modern browser — and the server picks one in the ServerHello. This negotiation is integrity-protected by the transcript hash, so an attacker cannot strip ALPN and force a downgrade. Without ALPN, the application would have to make a tentative HTTP/1.1 request first and use the <code>Upgrade</code> header, paying an extra RTT. ALPN bakes the upgrade into the handshake at no extra cost.</p>
    <p><strong>OCSP stapling (client view).</strong> Traditional revocation checks involved the client making a separate request to the certificate authority's OCSP responder, which leaked the user's browsing history and added latency. OCSP stapling moves that responsibility to the server: the server periodically fetches a signed OCSP response from the CA and "staples" it into the TLS handshake as the <code>status_request</code> extension. The client checks the stapled response is fresh, signed by the CA, and matches the certificate. No extra network request, no privacy leak. Most modern certificate deployments either staple OCSP (where the CA still supports it) or rely on browser-side CRLite — the post-OCSP world covered in the senior tier.</p>
    <p><strong>Alert protocol.</strong> When something goes wrong during or after the handshake, TLS sends an alert: a 2-byte message with a severity (warning or fatal) and a code (e.g. <code>bad_certificate</code>, <code>handshake_failure</code>, <code>decode_error</code>, <code>internal_error</code>). Fatal alerts close the connection immediately. Warning alerts are rarer in TLS 1.3 — most conditions that were warnings in TLS 1.2 are now fatal. The most common alerts you will see in production logs: <code>handshake_failure</code> when the client and server cannot agree on a common cipher suite or named group, <code>certificate_unknown</code> when the chain does not validate, and <code>close_notify</code> when one side cleanly shuts down. Reading TLS alerts in <code>tshark</code> or Wireshark is the first debugging step when an HTTPS connection fails.</p>
    <p><strong>NewSessionTicket lifecycle.</strong> After the handshake, the server may send one or more <code>NewSessionTicket</code> messages — encrypted blobs containing the PSK and metadata for future resumption. The client stores them in its session cache, keyed by hostname. Each ticket has a lifetime (typically minutes to hours) and an <code>ticket_age_add</code> obfuscator that prevents on-path observers from correlating tickets across connections by their visible age. The client may reuse the same ticket multiple times, or the server may force single-use by sending a new ticket each connection. Browsers typically cache 5–10 tickets per origin and rotate through them; servers control the cadence by deciding how often to issue new tickets and how long their STEK rotation window keeps old tickets decryptable.</p>
    <p><strong>Key update.</strong> TLS 1.3 supports rolling traffic keys during a long-lived connection without renegotiation: either side sends <code>KeyUpdate</code>, derives a new application traffic secret via <code>HKDF-Expand-Label(current, "traffic upd", "", hash_len)</code>, and starts using the new key. The peer responds with its own <code>KeyUpdate</code> and switches. This is a defense against long-running connections accumulating enough traffic that an attacker recording the ciphertext might eventually crack the key — modern AEAD ciphers tolerate enormous data volumes, but the mechanism exists for ultra-long-lived sessions like persistent WebSocket connections or service-mesh traffic that stays open for hours.</p>
    <p><strong>Certificate transparency (CT).</strong> Every publicly trusted certificate issued today must appear in at least two CT logs — append-only, publicly auditable, signed Merkle trees. The client checks the certificate carries Signed Certificate Timestamps (SCTs) from those logs, either as a certificate extension, in the stapled OCSP response, or in a TLS extension. If the SCTs are missing or invalid, Chrome and Safari refuse the connection. CT is what made the certificate authority ecosystem auditable: a rogue CA cannot issue a certificate for example.com without leaving a public, tamper-evident record, and detection tools (such as crt.sh) watch the logs in real time. Domain owners get notified of mis-issuance within minutes instead of months.</p>
    <p><strong>RSA vs ECDSA vs Ed25519 server certificates.</strong> The signature algorithm of the server certificate is independent of the ECDHE key exchange. Most certificates issued today are ECDSA P-256 (fast, small, widely supported); RSA 2048 remains for legacy compatibility but is roughly 5x slower to verify and produces signatures four times larger. Ed25519 certificates are technically supported by TLS 1.3 but rarely deployed because of CA tooling gaps. The choice matters for handshake CPU: an edge serving a million handshakes per second pays five times the CPU bill on RSA, so cloud providers default to ECDSA chains and offer dual-stack only for customers who explicitly need RSA for old clients.</p>
    <p><strong>Recap: what the wire actually carries.</strong> A successful TLS 1.3 handshake transmits roughly: one ClientHello (~512 bytes with reasonable extension lists), one ServerHello (~150 bytes), one EncryptedExtensions (~50 bytes), one Certificate (1–4 KB depending on chain length), one CertificateVerify (~70 bytes for ECDSA, ~270 bytes for RSA), one server Finished (~50 bytes), one client Finished (~50 bytes), plus the first application data record. Hybrid post-quantum pushes the ClientHello over 1500 bytes once <code>X25519MLKEM768</code> key shares are included, forcing TCP segmentation and sometimes triggering middlebox issues — covered in the senior tier. The cryptographic state both sides hold at the end of the handshake is small: a few hundred bytes of secrets and keys, plus the running transcript hash, plus the peer's certificate chain. Everything else (cipher state, sequence numbers) is derived on the fly.</p>
  </Fragment>
  <Fragment slot="senior">
    <p><strong>HKDF key schedule (RFC 8446 §7.1).</strong> TLS 1.3 derives every key from a tree rooted at <code>HKDF-Extract(salt=0, IKM=PSK_or_zero)</code> — the <em>early secret</em>. From the early secret, <code>Derive-Secret(early_secret, "derived", "")</code> produces the salt for the next extract, which mixes in the ECDHE shared secret to yield the <em>handshake secret</em>. The same derive-then-extract pattern produces the <em>master secret</em>. Every traffic key is then <code>HKDF-Expand-Label(traffic_secret, "key", "", key_length)</code> with a matching IV expansion (RFC 8446 §7.3). The <code>Derive-Secret(Secret, Label, Messages)</code> primitive folds the running transcript hash into each step, so an attacker who tampers with any handshake byte invalidates every downstream key. The hash is fixed by the negotiated cipher suite (SHA-256 for <code>TLS_AES_128_GCM_SHA256</code>, SHA-384 for <code>TLS_AES_256_GCM_SHA384</code>) and changing it requires changing the suite — there is no separate hash negotiation.</p>
    <p><strong>0-RTT replay defenses in production.</strong> RFC 8446 §8 and RFC 8470 specify three orthogonal mitigations layered together by serious operators. First, <strong>ticket-age validation</strong>: the client sends <code>obfuscated_ticket_age</code> in the PSK identity; the server subtracts the ticket-issue time, accounts for an expected RTT, and rejects if the result lies outside a narrow window (typical 10 s). Second, <strong>single-use ticket consumption</strong>: an in-memory cache keys on <code>(ticket_nonce, age_bucket)</code> and rejects any second decryption attempt — Cloudflare, Fastly, and AWS run a per-PoP cache plus a best-effort cross-PoP gossip layer so attackers cannot replay across regions inside the window. Third, <strong>application-level idempotency</strong>: only safe methods (GET/HEAD/OPTIONS) ever cross into <code>early_data</code>; non-safe methods must wait for the <code>Finished</code> from the server. Frameworks (Spring, Express, Caddy) expose middleware that inspects the <code>Early-Data: 1</code> request header (RFC 8470 §5.1) and returns <code>425 Too Early</code> for any handler that mutates state. Cryptographically inclined deployments add <strong>puncturable PRFs</strong>: the server's ticket-decryption key advances after every use so the same ciphertext literally cannot decrypt twice, eliminating the cache requirement.</p>
    <p><strong>Session-ticket encryption key (STEK) rotation.</strong> Without forward-secrecy on resumption, a leaked STEK lets an attacker decrypt every 0-RTT payload protected by tickets that key issued. At Cloudflare scale, STEKs rotate hourly, never touch disk, and are pushed to every edge via a memcached-style fan-out; the previous 24 keys are retained for decryption so in-flight tickets continue to resume. Nginx's <code>ssl_session_ticket_key</code> directive accepts a list of files — the first is the encryption key, the rest decrypt-only — and operators reload Nginx with a fresh head to rotate. Two failure modes dominate: keys persisted to disk and recovered after a node compromise, and operators who configure a static <code>ssl_session_ticket_key</code> for a year, turning resumption into a single-key crypto monoculture. The fix is non-negotiable: keep STEKs in memory, rotate at least daily, and shred old material instead of keeping a forever-archive "for debugging."</p>
    <p><strong>Encrypted ClientHello (ECH, RFC 9849).</strong> A vanilla TLS 1.3 <code>ClientHello</code> still leaks Server Name Indication, ALPN, supported groups, signature algorithms, and certificate authorities — enough fingerprint surface to identify the destination domain and the client's stack. ECH (published as RFC 9849 in March 2026) defines an <em>outer</em> ClientHello sent to a public "client-facing" name and an <em>inner</em> ClientHello encrypted via HPKE using the origin's published ECH key. The client fetches that key from a DNS <code>HTTPS</code>/<code>SVCB</code> resource record (RFC 9460) before opening the connection — without it, the client can either GREASE (send fake ECH to mask traffic patterns) or fall back to plaintext SNI. As of May 2026 Chrome ships ECH on by default; Firefox enabled it for general users from version 119 in 2024. Cloudflare terminates ECH on every plan; enterprise networks that need on-path TLS inspection can break it via DoH blocking or HTTPS-RR suppression, and several CISA-aligned MDM profiles do exactly that. Operators integrating ECH must keep the public key rotated (the DNS TTL is the upper bound on key compromise) and remember that the <em>outer</em> SNI still names a generic frontend, which becomes a privacy and abuse signal of its own.</p>
    <p><strong>Hybrid post-quantum key exchange.</strong> The current draft is <code>draft-ietf-tls-ecdhe-mlkem-04</code> (February 2026), which standardizes <code>X25519MLKEM768</code> (named-group codepoint <code>0x11EC</code>) plus <code>SecP256r1MLKEM768</code> and <code>SecP384r1MLKEM1024</code> for FIPS environments. ML-KEM is the NIST FIPS 203 standardization of CRYSTALS-Kyber (August 2024). The hybrid shared secret is the byte-concatenation of the ECDHE result and the ML-KEM result, fed into <code>HKDF-Extract</code> exactly where the classical ECDHE secret used to sit — so the key schedule downstream is unchanged. Wire-format cost is significant: ML-KEM-768 ciphertexts are 1088 bytes, public keys 1184 bytes, which pushes ClientHello over the typical 1500-byte initial congestion window and forces TLS implementations to handle ClientHello fragmentation cleanly. Chrome started experimental rollout in 2024; as of May 2026 hybrid PQ is widely available behind a feature flag but not yet a browser default, while Java 24+ exposes ML-KEM as a first-class JCA primitive. The deployment risk to watch is "store now, decrypt later" — adversaries are recording today's classical-only handshakes for replay against a future cryptographically relevant quantum computer.</p>
    <p><strong>Kernel TLS (kTLS) offload on Linux.</strong> <code>setsockopt(fd, SOL_TLS, TLS_TX, &amp;crypto_info)</code> hands the symmetric record layer to the kernel after user-space completes the handshake. Linux 4.13 shipped TX, 4.17 added RX, and modern kernels (6.0+) plus OpenSSL 3.2+ can offload to NIC hardware so the encrypted record stream is built on the wire by the network card with zero per-record CPU cost. Supported algorithms are AES-128-GCM, AES-256-GCM, and ChaCha20-Poly1305 — TLS 1.3 only. Netflix and Cloudflare both attribute single-digit-to-double-digit-percent CPU savings (8–29% in published benchmarks) to <code>sendfile()</code> over kTLS for static file serving, because the file moves from page cache to NIC without ever entering user space. The catch is that kernel records max out at 2^14 bytes (TLS 1.3 record limit) and that anti-replay logic, alert handling, and renegotiation paths still live in user space — frameworks like Nginx call <code>SSL_sendfile()</code> only when the connection state is steady-state.</p>
    <p><strong>OCSP, CRLite, and the revocation reality.</strong> Online Certificate Status Protocol was supposed to give browsers a real-time "is this cert revoked" check, but soft-fail (the dominant browser default) means any on-path adversary who can block the OCSP responder also turns revocation into a no-op. The web PKI walked away from OCSP between 2024 and 2026: Let's Encrypt stopped serving OCSP responses on 2025-05-07 and ceased issuing certificates with <code>id-pe-tlsfeature must-staple</code> after 2025-01-30, citing the privacy leak that every OCSP query reveals to the responder which sites the user visits. Firefox 137 (January 2025) shipped CRLite by default — a compact, signed cascade of bloom filters covering all WebPKI revocations from Certificate Transparency logs, refreshed every 12 hours, no per-connection responder hit. Apple uses a smaller built-in CRL set; Chrome continues to rely on CRLSets pushed by component update. The operational implication: stapling is still worth keeping where you have it, but new infrastructure should plan around shorter cert lifetimes (90 days, soon 47 days) and CT-log-derived revocation rather than runtime OCSP.</p>
    <p><strong>Production observability for TLS.</strong> A minimum-viable Prometheus sketch for an HTTPS service exposes <code>tls_handshake_duration_seconds_bucket&#123;le=...&#125;</code> (histogram), <code>tls_resumption_total&#123;kind="psk|ticket|none"&#125;</code>, <code>tls_version_total&#123;version="1.2|1.3"&#125;</code>, <code>tls_cipher_suite_total&#123;suite=...&#125;</code>, <code>tls_alpn_protocol_total&#123;alpn="h2|h3|http/1.1"&#125;</code>, <code>tls_early_data_total&#123;outcome="accepted|rejected"&#125;</code>, and <code>tls_ocsp_staple_total&#123;outcome="ok|expired|missing"&#125;</code>. Envoy and Nginx-Ingress emit most of these natively. A handshake p95 above 200 ms on a CDN-fronted origin is a deploy regression smell — usually OCSP fetch latency, an unrotated session ticket key bringing every connection back to a full handshake, or hybrid PQ wire-size pressure on the first packet. Track <em>resumption ratio</em> (resumed / total) per node: a sudden drop usually means the STEK rotated without an overlap window. ALPN distribution is the cheapest health check for HTTP/3 rollout, and an early-data rejection spike often precedes a customer report of "my POST shows up twice."</p>
    <p><strong>CVEs and post-1.3 attack surface.</strong> TLS 1.3 closed the most prominent record-layer disasters (BEAST, CRIME, BREACH, POODLE, padding oracles), but the surrounding ecosystem keeps shipping bugs that operators must triage. CVE-2024-12797 (OpenSSL 3.2–3.4) skipped the "abort on mismatch" branch for Raw Public Key (RFC 7250) server authentication, letting a MITM substitute a different RPK without detection — fixed in 3.4.1. CVE-2025-9230 in OpenSSL's CMS PWRI decryption path introduced an out-of-bounds read/write that turns parsing attacker-supplied CMS into a remote DoS, and on some allocators a code-execution primitive. CVE-2025-9231 leaked SM2 private-key bits via timing measurements on 64-bit ARM, relevant to any deployment using SM2 (state-mandated crypto in mainland China) on Graviton or Ampere hardware. The pattern repeats: ASN.1 parsers, side channels on less-tested algorithms, and authentication-handler branches are where TLS bugs live in 2025–2026. Operations: keep an automated tracker against the OpenSSL security advisories feed, pin a known-good Go <code>crypto/tls</code> version per service, and don't treat "we use TLS 1.3" as a substitute for patch hygiene.</p>
    <p><strong>Middlebox interference and GREASE.</strong> RFC 8446 Appendix D.4 documents the depressing reason TLS 1.3 ships pretending to be TLS 1.2: too many middleboxes (firewalls, government inspection proxies, ancient corporate TLS terminators) abort connections whose record-layer or handshake bytes do not match TLS 1.2 patterns they recognize. So TLS 1.3 puts <code>0x0303</code> (TLS 1.2) in the <code>legacy_version</code> field, sends a dummy <code>ChangeCipherSpec</code> record during the handshake, and disguises the real version inside a <code>supported_versions</code> extension. RFC 8701 (GREASE) defines reserved codepoints — <code>0x0A0A</code>, <code>0x1A1A</code>, <code>0x2A2A</code>, … through <code>0xFAFA</code> — that endpoints sprinkle through extension lists, cipher suite lists, named groups, signature algorithms, and version negotiation. A correct peer ignores unknown values; a fragile middlebox blows up immediately, surfacing the bug to the vendor instead of letting it ossify the protocol. When you debug a TLS 1.3 connection that fails at a customer site behind a corporate proxy, the working theory is almost always middlebox: capture the ClientHello, replay it from outside the network, and 95% of the time the connection completes.</p>
    <p><strong>Cross-protocol interactions.</strong> TLS 1.3 is the cryptographic core that QUIC (RFC 9001) lifted into its own packet protector — there is no separate "QUIC TLS handshake"; the same key schedule produces QUIC initial, handshake, and 1-RTT packet protection keys, so every TLS 1.3 advance (PQ hybrid, ECH, 0-RTT replay defense) arrives in HTTP/3 with no protocol changes. DTLS 1.3 (RFC 9147) does the same for UDP without QUIC. Outside the web, TLS 1.3 underlies modern SMTPS, IMAPS, syslog-over-TLS, gRPC, and service mesh mTLS (Istio, Linkerd) — and the operational concerns above (kTLS, STEK rotation, observability) apply wholesale once you cross the per-instance handshake budget any service mesh demands. A reasonable mental model: TLS 1.3 stopped being a "web" protocol around 2022; treating it as the universal channel-protection primitive is now the right default.</p>
    <p><strong>QUIC packet-protector integration depth.</strong> When QUIC runs TLS 1.3 in-band, the handshake bytes are still standard TLS 1.3 records but they ride inside CRYPTO frames rather than a TCP stream — there is no record-layer fragmentation because QUIC handles segmentation. The key schedule is identical down to <code>HKDF-Expand-Label</code>; QUIC just consumes the early, handshake, and application traffic secrets and runs them through one more <code>HKDF-Expand-Label</code> with the labels <code>quic key</code>, <code>quic iv</code>, and <code>quic hp</code> (header protection) per RFC 9001 §5.1. Header protection is what stops on-path observers from reading packet numbers, which would otherwise leak ordering and reordering across paths — important for connection migration. Operationally this means a single TLS 1.3 implementation backs both your HTTP/2-over-TLS and HTTP/3 deployments; an HSM that does TLS 1.3 ECDHE for HTTP/2 does the same for HTTP/3 because the cryptographic primitives are bit-for-bit identical. The split surfaces only at the I/O boundary: kTLS does not yet cover QUIC because QUIC's packet boundaries do not map cleanly onto kernel TLS records, so high-throughput HTTP/3 servers still pay for crypto in user space — quicly, msquic, and Cloudflare's quiche all sit above kernel UDP.</p>
    <p><strong>Distributed anti-replay across regions.</strong> A single-PoP single-use ticket cache is easy: a bounded LRU keyed by <code>(ticket_nonce, age_bucket)</code> rejects a second decryption attempt in O(1). The hard problem is cross-region: an attacker captures a 0-RTT ClientHello in Frankfurt and replays it through Tokyo within the ticket's age window. Pure synchronous global consensus does not work at line-rate handshake volume, so production stacks settle for "best-effort gossip plus per-region authoritative cache." Cloudflare's approach (publicly described): rotate STEKs every hour; each PoP runs an in-memory cache for the last 10 s of 0-RTT nonces; gossip via a low-latency multicast bus so 90%+ of replays cross enough PoPs to be caught; the remaining tail is bounded by the 10 s window which keeps damage at most three nonce-sized replays per origin per attacker. AWS Cloudfront uses a similar regional design. The takeaway: 0-RTT is acceptable for read-only paths, never for stateful POST/PUT/DELETE, and the application MUST still treat 0-RTT-tagged requests as idempotent-or-rejected at the framework layer.</p>
    <p><strong>SNI/ALPN privacy beyond ECH.</strong> Encrypting ClientHello stops the obvious leaks but TLS sessions remain fingerprintable. JA3 (2017) hashes ClientHello extension list + cipher list + named groups + signature algorithms — the resulting hash identifies a TLS client by stack version (curl 8.5 vs Firefox 124 vs an automation toolkit) with high precision. JA4 (2024) refines this with additional features (ALPN, version count, extension count, sorted vs unsorted) and is the de-facto modern fingerprint. Bot defenders use these hashes to identify automated clients even when User-Agent is spoofed. The defensive perspective: ECH hides <em>which site</em> you visit, but JA3/JA4 still reveals <em>what stack</em> is visiting. The offensive perspective: any malware that ships with a non-mainstream TLS library gets an immediate fingerprint hit. This is why post-2024 evasion tooling reverse-engineers Chrome's exact TLS extension ordering and replays it byte-for-byte. There is no clean protocol-level fix; reducing entropy in ClientHello (GREASE positions, extension order) is what browsers do, and what middleboxes break.</p>
    <p><strong>HSM-backed long-term keys.</strong> The server certificate's private key never has to leave a hardware security module: PKCS#11 (or AWS CloudHSM, GCP Cloud KMS, Azure Key Vault HSM) presents a <code>C_Sign</code> primitive that returns a signature without exposing the key. TLS 1.3 calls this primitive once per handshake (the <code>CertificateVerify</code> over the transcript) — not per-record. Throughput math: a FIPS 140-3 Level 3 HSM at the high end does roughly 10,000–20,000 RSA-2048 sign operations per second; ECDSA P-256 is 30,000–50,000. A single HSM tops out at a small fraction of a Cloudflare-class edge's handshake rate, so production designs front the HSM with a TLS terminator that does keyless TLS (Cloudflare Keyless SSL, RFC 8492 also adapts it for password-authenticated TLS) — the terminator runs in untrusted infrastructure, signs requests are forwarded to the HSM in a trusted zone, and the long-term key never leaves origin custody. Operational caveat: sigops are now your scaling bottleneck for cold handshakes, which makes session resumption a hard requirement and STEK rotation discipline non-optional.</p>
    <p><strong>Real-world failure stories.</strong> Cloudflare February 2017: a buffer-bounds bug in their HTML parser (later named Cloudbleed) leaked TLS-encrypted session data into cached HTML, including some other customers' Auth headers and cookies — TLS protected the wire, not the proxy memory. Let's Encrypt September 2021 chain expiry: the cross-signed "DST Root CA X3" expired and any client without the newer ISRG Root X1 root suddenly failed verification — Android &lt; 7.1.1 was hit hardest, breaking a long tail of devices. Let's Encrypt January 2024: the legacy cross-sign was retired, breaking older OpenSSL versions whose chain-building preferred the expired path. GoDaddy 2023: a leaked STEK in a customer's misconfigured backup let an attacker decrypt several months of cached 0-RTT data after the fact — exactly the failure mode RFC 8446 §2.3 warns about and the reason rotation cadence below one hour matters. Heartbleed (CVE-2014-0160) is still the reference disaster: a missing length check in OpenSSL's TLS heartbeat extension let attackers read 64 KB of process memory per request, including private keys; the post-mortem reshaped the entire memory-safe-TLS ecosystem (BoringSSL, rustls, Tigerl) and is the primary reason kTLS limits the kernel surface to record-layer encryption only.</p>
    <p><strong>mTLS at service-mesh scale.</strong> Istio + SPIFFE/SPIRE issue a fresh leaf certificate per workload, often with TTL ≤24 hours, and rotate via short-lived SVIDs delivered over the SPIRE workload API. At 10,000 pods × 24-hour rotation × even handshake distribution that is 7,000 cert issuances per minute baseline, spiking during deploys. The control-plane CA becomes a hotspot; Istio's recommendation is a hierarchical CA (root → intermediate per cluster) so the leaf-signing path stays warm and on-cluster. The data plane (Envoy) terminates and re-originates TLS for every hop — your end-to-end latency budget pays for a TLS 1.3 handshake at every service boundary unless connection pools are large enough that handshakes amortize across thousands of requests. Observability stays the same metric set (handshake duration, resumption ratio, cipher distribution) but is now reported per-workload, and a single misbehaving service can be diagnosed by an outlier in the <code>tls_handshake_duration_seconds</code> p95 alone.</p>
    <p><strong>TLS 1.2 vs 1.3 handshake costs.</strong> TLS 1.2 required at least two round-trips: ClientHello + ServerHello (one RTT) to exchange parameters and learn the server's certificate, then key exchange and authentication (second RTT). Some implementations used session resumption to skip the second RTT, but the first was unavoidable. TLS 1.3 collapses the first RTT by sending key material in ClientHello, and PSK resumption skips even that for warm connections. QUIC also layers on top: it's a UDP protocol with TLS 1.3, so the cryptographic handshake integrates into the transport handshake — the first QUIC packet can carry 0-RTT data if the client has a cached ticket.</p>
  </Fragment>
</TierAccordion>

Let's trace the two paths through TLS 1.3: a cold handshake (no cached key) and a warm resumption (cached pre-shared key).

<FadedExample
  client:load
  id="fade-handshake"
  pieceSlug="05-tls-handshake"
  lang="en"
  title="Cold vs warm vs 0-RTT"
  steps={{
    solved: (
      <div class="space-y-4">
        <p><strong>Cold handshake (TLS 1.3):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm mb-4">
          <li>Bea sends <code class="bg-purple-100 px-1">ClientHello (cipher suites + key share)</code></li>
          <li>Sven replies with <code class="bg-blue-100 px-1">ServerHello (cert + signature)</code> — same RTT</li>
          <li>Both compute shared secret from key shares</li>
          <li>Bea sends <code class="bg-purple-100 px-1">Finished</code>, then first encrypted app data</li>
          <li><strong>Cost: 1 RTT</strong> (at 80 ms RTT = 80 ms handshake latency before app data)</li>
        </ul>
        <p><strong>Warm resumption (PSK):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm mb-4">
          <li>Bea sends <code class="bg-purple-100 px-1">ClientHello (pre_shared_key extension)</code></li>
          <li>Sven recognizes the ticket, skips cert validation</li>
          <li>Both derive shared secret from PSK</li>
          <li>Bea sends <code class="bg-purple-100 px-1">Finished</code>, then first encrypted app data</li>
          <li><strong>Cost: 1 RTT</strong> (no cert/PKI overhead, but still one RTT for the handshake)</li>
        </ul>
        <p><strong>0-RTT (early data):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm">
          <li>Bea encrypts GET request using cached PSK</li>
          <li>Sends <code class="bg-purple-100 px-1">ClientHello (pre_shared_key + early_data)</code></li>
          <li>Sven receives, decrypts, processes request, sends response</li>
          <li><strong>Cost: 0 RTT for the request</strong> (but response still takes 1 RTT to return)</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "A user opens the same site they visited 5 minutes ago. The browser cached a session ticket. The site allows 0-RTT for GET requests. How many RTTs until the user receives the first byte of the response?",
      blanks: [
        {
          id: "rtt_count",
          expected: "1",
          placeholder: "Enter number of RTTs (0–2)",
        },
      ],
    },
    blank: {
      prompt: "Same scenario but the request is a POST that creates a database row. The 0-RTT is sent. Why does the server's policy refuse to honor (decrypt and process) the 0-RTT early data, and what should the client do?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Answer: POST is not idempotent. Replays are dangerous.</strong></p>
          <p class="text-sm">An attacker on the wire can capture the 0-RTT ClientHello and resend it. For GET, that's harmless (same read, same result). For POST, that's catastrophic: the database row is created twice, money is transferred twice, etc. The server policy restricts 0-RTT to safe methods (GET, HEAD, OPTIONS). If the client sends POST with early_data, the server decrypts the handshake but ignores the early_data, discards it, and asks the client to retransmit after the handshake completes.</p>
          <p class="text-xs text-gray-600 mt-2"><strong>Best practice:</strong> Servers should advertise which request types are 0-RTT-safe (e.g., via a custom HTTP header or configuration). Clients should cache this metadata with the session ticket and never send 0-RTT for unsafe requests.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "rtt_count:0": "Close, but the response still takes 1 RTT to travel back. 0-RTT means the request rides along with the handshake, but the response must still cross the network.",
    "rtt_count:2": "One too many. In 0-RTT mode, the request doesn't add an extra RTT — it's sent in the ClientHello. So total is 1 RTT (for the response to come back).",
  }}
/>

Now let's look at the numbers. <PersonaTag id="cara" lang="en" /> issues certificates; <PersonaTag id="sven" lang="en" /> carries the private key and must verify certificates during handshake.

<NumbersCard
  id="card-tls-numbers"
  title="TLS handshake costs"
  stats={[
    { label: "TLS 1.2 cold handshake", value: "2 RTT" },
    { label: "TLS 1.3 cold handshake", value: "1 RTT" },
    { label: "TLS 1.3 warm (resumption)", value: "1 RTT" },
    { label: "TLS 1.3 0-RTT (early data)", value: "0 RTT" },
    { label: "RSA 2048 verification", value: "~5 ms (hardware-dependent)" },
    { label: "ECDSA P-256 verification", value: "~1 ms (hardware-dependent)" },
    { label: "PSK ticket lifetime", value: "implementation choice (typical 1h)" },
    { label: "HSTS max-age", value: "31536000 s (1 year)" },
  ]}
/>

Let's compare the trade-off: TLS 1.2 cold connections paid two RTTs. TLS 1.3 halved it. But is there still a cost to moving key material forward?

<div id="card-rtt-cost" class="rounded-2xl bg-blue-50 border-2 border-blue-300 p-6 my-8">
  <div class="font-mono text-sm space-y-3">
    <div><strong>RTT cost comparison:</strong></div>
    <div class="space-y-2 text-xs">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-3 mt-3">
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.2</div>
          <div class="text-gray-600">Cold</div>
          <div class="text-blue-600 font-bold mt-1">2 RTT</div>
          <div class="text-xs text-gray-500 mt-2">ClientHello+ServerHello, then key exchange</div>
        </div>
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.3</div>
          <div class="text-gray-600">Cold</div>
          <div class="text-blue-600 font-bold mt-1">1 RTT</div>
          <div class="text-xs text-gray-500 mt-2">Key shares in ClientHello</div>
        </div>
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.3</div>
          <div class="text-gray-600">Resumed (PSK)</div>
          <div class="text-blue-600 font-bold mt-1">1 RTT</div>
          <div class="text-xs text-gray-500 mt-2">But no cert validation</div>
        </div>
      </div>
    </div>
    <div class="text-xs text-gray-600 mt-4">At 100 ms RTT: TLS 1.2 adds 200 ms, TLS 1.3 cold adds 100 ms, TLS 1.3 warm + 0-RTT adds ~0 ms to the handshake itself.</div>
  </div>
</div>

<Misconception
  id="r-0rtt-replay"
  title="Heads-up: 0-RTT replay"
>
0-RTT data is not protected against replay attacks. An attacker on the wire can capture a 0-RTT ClientHello and resend it to the server. For idempotent requests (GET), that's benign; for state-changing requests (POST), that's exploitable. Server policy must restrict 0-RTT to safe HTTP methods.
</Misconception>

The handshake is now fast, but why do certificate verifications take any time at all? <PersonaTag id="sven" lang="en" /> sends his certificate, and Bea must verify that <PersonaTag id="cara" lang="en" /> (the CA) actually issued it. This verification is cryptographic: Bea uses Cara's public key to check Sven's signature. RSA signatures (traditional) are slower; ECDSA signatures (modern, especially P-256) are faster — on modern x86 server CPUs, roughly a 5× difference, though verification times are hardware-dependent. In large-scale deployments (CDNs, cloud providers), even 1 ms per connection adds up. This is why cached resumption (PSK) matters: it skips the verification entirely.

<SpiralCue thread="encapsulation" lang="en" /> — TLS wraps every byte of application data in a symmetric-key envelope. The envelope is negotiated via asymmetric cryptography (ECDHE, RSA signatures) during the handshake. Once negotiated, the symmetric key handles all data encryption. <SpiralCue thread="statefulness" lang="en" /> — the handshake builds state: both sides remember the shared secret, the negotiated cipher, and derived keys. This state persists until the connection closes or the key renegotiates.

<RetrievalDrawer
  client:load
  pieceSlug="05-tls-handshake"
  lang="en"
  questions={[
    {
      id: "q1",
      q: "Why does TLS 1.3 abandon RSA key transport (client encrypts shared secret with server's long-term key) in favor of ECDHE (ephemeral key exchange)?",
      answer: <p>RSA key transport leaks past sessions: if an attacker steals the server's long-term private key today, the attacker can decrypt all past TLS connections recorded before the compromise. ECDHE uses ephemeral keys generated fresh for each connection; once the connection closes, the ephemeral private keys are deleted. Even if the server's long-term key is later compromised, the attacker cannot decrypt old sessions because the ephemeral keys are gone. This property is called Perfect Forward Secrecy (PFS). ECDHE is more expensive per connection (ECDH computation), but modern hardware handles it in microseconds. The security win (PFS) is worth the cost, so TLS 1.3 made ECDHE mandatory and eliminated RSA key transport.</p>,
    },
    {
      id: "q2",
      q: "You see Chrome successfully connect to a site over TLS 1.3 0-RTT, and the site is `POST /transfer` with amount and recipient. You're the site's security officer. Why is the team unhappy, and what's the exploit?",
      answer: <p>The client sent a POST in 0-RTT, which means the POST request is encrypted with a cached pre-shared key (PSK) and rides in the ClientHello. An attacker on the network can capture the ClientHello and resend it multiple times. On the receiving end, the server decrypts the same POST request and processes it multiple times — same amount, same recipient, multiple transfers. The database now has duplicate rows, money is transferred twice, and the attacker has exploited your idempotency bug. Proper defense: (a) Server policy must refuse 0-RTT for POST/PUT/DELETE (only allow GET, HEAD, OPTIONS). (b) Client must not send non-idempotent requests in 0-RTT. (c) The server can add a replay window: reject 0-RTT data older than N seconds (e.g., 5 s), limiting the damage window.</p>,
    },
    {
      id: "q3",
      q: "The HSTS Preload List is sometimes called a mitigation for 'trust on first use.' Why is plain HSTS (without preload) insufficient, and what does preload add?",
      answer: <p>Plain HSTS (Strict-Transport-Security header) tells the browser 'never downgrade to HTTP for this domain.' But this only works if the first request is HTTPS and the header is received. On the very first visit (before the header is cached), the browser makes a plain HTTP request. An attacker on the network can intercept that first HTTP request, inject a redirect to a phishing site, and the user never learns the correct domain supports HTTPS. This is the "trust on first use" vulnerability. The HSTS Preload List is a hardcoded list of domains in the browser binary that support HSTS. For preload domains, the browser assumes HTTPS from the start, even on the first visit. The trade-off: being on the preload list requires operational discipline (you must deploy HSTS and never accidentally downgrade), and removal is slow (takes months). But new users are protected from day one.</p>,
    },
  ]}
/>

Once the handshake completes, the channel is sealed: every request and response travels encrypted. The next layer up is the protocol on top — HTTP — which negotiates its own version and flow control. TLS 1.3 works beneath it, transparent but essential.

### What's next

[**06 — HTTP versions**](/en/networking/06-http-versions/) — once the channel is encrypted, the protocol on top is HTTP.
