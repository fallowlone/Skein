---
slug: 05-tls-handshake
lang: en
pillar: networking
chapter: 01-networking
order: 5
title: "TLS 1.3: keys and confidentiality in one RTT"
summary: "How ClientHello key shares plus a certificate verifier collapse the old TLS 1.2 two-RTT handshake into one — and what 0-RTT actually buys you."
readingMin: 16
status: ready
prereqs: ["03-tcp-handshake"]
spiral: ["encapsulation", "statefulness"]
personas: ["bea", "sven", "cara"]
depth:
  mechanism: tier-mechanism
  tradeoff: card-rtt-cost
  failure_mode: r-0rtt-replay
  numbers: card-tls-numbers
sources:
  - https://datatracker.ietf.org/doc/html/rfc8446
  - https://datatracker.ietf.org/doc/html/rfc8470
  - https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
  - https://hpbn.co/transport-layer-security-tls/
  - https://developer.mozilla.org/en-US/docs/Web/Security/Transport_Layer_Security
---

import TierAccordion from "../../../../../components/pedagogy/TierAccordion.astro";
import FadedExample from "../../../../../components/pedagogy/FadedExample.tsx";
import RetrievalDrawer from "../../../../../components/pedagogy/RetrievalDrawer.tsx";
import Crux from "../../../../../components/prose/Crux.astro";
import SpiralCue from "../../../../../components/prose/SpiralCue.astro";
import PersonaTag from "../../../../../components/pedagogy/PersonaTag.astro";
import NumbersCard from "../../../../../components/layout/NumbersCard.astro";
import Misconception from "../../../../../components/layout/Misconception.astro";

TCP agreed on sequence numbers, DNS resolved a name to an IP — now <PersonaTag id="bea" lang="en" /> (browser) and <PersonaTag id="sven" lang="en" /> (server) must agree on encryption before sending anything that matters. That encryption is negotiated in the TLS handshake. For decades, this negotiation took two round-trips over the network. TLS 1.3 cut it to one by moving cryptographic work forward: the client sends not just supported ciphers but its own ephemeral key in the opening message, letting the server derive a shared secret and send encrypted data in the same RTT.

<Crux>How key shares and ECDHE collapse TLS 1.2's two-RTT handshake into one, and what 0-RTT buys you.</Crux>

The goal of TLS is to build a sealed channel: one that anyone can intercept but only the sender and receiver can read. Before TLS 1.3, building that channel required at least two round-trips. TLS 1.3 redesigned the handshake to move key material into the first message, enabling the server to compute the shared secret and send encrypted application data without waiting. The trade-off: cold connections still cost one round-trip before the first encrypted byte, but warm connections (resumption via pre-shared keys) cost zero.

<TierAccordion id="tier-mechanism" lang="en">
  <Fragment slot="junior">
    <p>TLS is a sealed envelope. <PersonaTag id="bea" lang="en" /> has a message. She wants to mail it to <PersonaTag id="sven" lang="en" />, but she fears the mailman will read it. So she puts it in a box, locks it with a seal, and mails it. On the way, anyone can see the box, but no one can read what's inside — only Sven, who has a matching key, can unlock it. Before mailing, though, Bea and Sven need to agree on the seal and the key. They meet online and negotiate: "Let's use AES as the lock, SHA-256 as the wax, and here's my half of the key." Sven replies: "I got it, here's my half." They combine the halves, derive a shared seal, and now every message Bea sends is locked with it. TLS 1.3 made this meeting shorter by letting Bea send her half of the key in her first message instead of waiting for Sven to ask for it.</p>
  </Fragment>
  <Fragment slot="middle">
    <p><strong>The 1-RTT handshake dance.</strong> <PersonaTag id="bea" lang="en" /> sends <code>ClientHello</code>, which includes the TLS version, supported cipher suites, <strong>and an ephemeral ECDHE public key (key share)</strong>. <PersonaTag id="sven" lang="en" /> receives it, picks a cipher suite and curve, generates his own ephemeral key pair, and replies with <code>ServerHello</code> containing his public key. Both sides now have each other's ephemeral public keys and can derive a shared secret using elliptic-curve Diffie-Hellman (ECDHE). Sven signs his server certificate (proof he's really google.com) with his long-term private key and sends <code>Certificate</code> and <code>CertificateVerify</code>. <PersonaTag id="cara" lang="en" /> (the certificate authority) issued that certificate, and Bea carries Cara's public key in her root store. Bea verifies the certificate chain, checks Sven's signature on the handshake transcript, and computes the shared secret. She sends <code>Finished</code> containing an HMAC of the entire handshake, proving she computed the same secret. From this point on, all data between them is encrypted with keys derived from the shared secret. Cost: one full RTT (ClientHello and ServerHello cross the wire once each).</p>
    <p><strong>Perfect Forward Secrecy (PFS).</strong> Both the client and server use ephemeral (temporary) key pairs, generated fresh for this connection. If an attacker later steals Sven's long-term private key from his server's disk, the attacker <strong>cannot decrypt past sessions</strong> because the past sessions used ephemeral keys that are already deleted. This is why PFS matters: compromise of the server today does not leak yesterday's secrets. RSA key transport (the old TLS 1.2 mode) avoided this: the client encrypted the shared secret with the server's long-term public key, so stealing the private key meant decrypting all past sessions. TLS 1.3 mandates ephemeral key exchange, making PFS the default.</p>
    <p><strong>Session resumption via PSK (pre-shared key).</strong> After the handshake, the server sends a <code>NewSessionTicket</code> — an encrypted ticket containing a pre-shared key and metadata. The browser caches it. On the next connection to the same server, Bea sends the same <code>ClientHello</code> but now includes a <code>pre_shared_key</code> extension with the ticket. Sven decrypts the ticket, recognizes it, and skips certificate validation. He derives the shared secret from the PSK instead of ECDHE, and both sides continue. Cost: still one full RTT, but no expensive cryptographic operations on the server.</p>
    <p><strong>0-RTT mode.</strong> If a browser has a cached PSK, it can encrypt the first request (e.g., a GET) using the PSK's key, and send the encrypted data inside the <code>ClientHello</code> itself. The server decrypts it immediately, processes the request, and responds — all within the same network round-trip as the handshake. Cost: zero RTT for the request. But this safety feature comes with a caveat: <strong>replay vulnerability.</strong> An attacker on the wire captures the 0-RTT ClientHello, and can resend it multiple times. If the request is "GET /news", replays are harmless. If it's "POST /transfer" with amount and recipient, replays are exploitable.</p>
  </Fragment>
  <Fragment slot="senior">
    <p><strong>Cipher suite selection and HKDF.</strong> TLS 1.3 supports curves like P-256, P-384, and x25519 for ECDHE, and symmetric ciphers like AES-128-GCM, AES-256-GCM, and ChaCha20-Poly1305. The handshake transcriptHash (SHA-256 or SHA-384) is part of the cipher suite selection. Key derivation uses HKDF (HMAC-based key derivation function, RFC 5869): handshake_secret = HKDF-Extract(0, ECDHE-shared-secret), then client_write_key = HKDF-Expand(handshake_secret, ...). Later, after both sides send <code>Finished</code>, they derive the application_secret for long-lived application data encryption. This staged key hierarchy means early handshake keys differ from later data keys, so compromise of one does not leak the other.</p>
    <p><strong>Certificate pinning and public-key pinning (HPKP).</strong> A browser can pin the expected public key or certificate of a domain, refusing to connect if the certificate authority issues a new certificate for that domain. This protects against CA compromise. However, HPKP (RFC 7469) is deprecated due to operational complexity: a misconfigured pin locks users out forever. Modern sites use the HSTS Preload List instead: a hardcoded list in the browser of domains that support HSTS and should never be downgraded to HTTP. Being on the list requires a proven record of HSTS deployment and is manually managed by browser vendors.</p>
    <p><strong>Encrypted ClientHello (ECH).</strong> The TLS handshake currently sends <code>ClientHello</code> in plaintext, including the Server Name Indication (SNI) — the domain the client wants to visit. On-path observers (ISP, middleboxes) can see which domain you're visiting even if the handshake is encrypted. ECH encrypts the <code>ClientHello</code> (except a few fields like TLS version and supported groups) to the server's public key, hiding SNI from on-path observers. This is the next frontier for TLS deployment, but it requires the server to publish its ECH public key via DNS.</p>
    <p><strong>Mitigating 0-RTT replay.</strong> The server must NOT honor 0-RTT for state-changing operations (POST, PUT, DELETE). To further limit replay risk, the server can use a "replay window": reject 0-RTT data that arrives more than N seconds (e.g., 5–10 s) after the session ticket was issued. The client's ticket includes a timestamp; the server compares it to now and rejects if too old. Some servers also use a per-origin obfuscated ticket format to prevent cross-origin replay. RFC 8470 formalizes these mitigations.</p>
    <p><strong>TLS 1.2 vs 1.3 handshake costs.</strong> TLS 1.2 required at least two round-trips: ClientHello + ServerHello (one RTT) to exchange parameters and learn the server's certificate, then key exchange and authentication (second RTT). Some implementations used session resumption to skip the second RTT, but the first was unavoidable. TLS 1.3 collapses the first RTT by sending key material in ClientHello, and PSK resumption skips even that for warm connections. QUIC also layers on top: it's a UDP protocol with TLS 1.3, so the cryptographic handshake integrates into the transport handshake — the first QUIC packet can carry 0-RTT data if the client has a cached ticket.</p>
  </Fragment>
</TierAccordion>

Let's trace the two paths through TLS 1.3: a cold handshake (no cached key) and a warm resumption (cached pre-shared key).

<FadedExample
  client:load
  id="fade-handshake"
  pieceSlug="05-tls-handshake"
  lang="en"
  title="Cold vs warm vs 0-RTT"
  steps={{
    solved: (
      <div class="space-y-4">
        <p><strong>Cold handshake (TLS 1.3):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm mb-4">
          <li>Bea sends <code class="bg-purple-100 px-1">ClientHello (cipher suites + key share)</code></li>
          <li>Sven replies with <code class="bg-blue-100 px-1">ServerHello (cert + signature)</code> — same RTT</li>
          <li>Both compute shared secret from key shares</li>
          <li>Bea sends <code class="bg-purple-100 px-1">Finished</code>, then first encrypted app data</li>
          <li><strong>Cost: 1 RTT</strong> (at 80 ms RTT = 80 ms handshake latency before app data)</li>
        </ul>
        <p><strong>Warm resumption (PSK):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm mb-4">
          <li>Bea sends <code class="bg-purple-100 px-1">ClientHello (pre_shared_key extension)</code></li>
          <li>Sven recognizes the ticket, skips cert validation</li>
          <li>Both derive shared secret from PSK</li>
          <li>Bea sends <code class="bg-purple-100 px-1">Finished</code>, then first encrypted app data</li>
          <li><strong>Cost: 1 RTT</strong> (no cert/PKI overhead, but still one RTT for the handshake)</li>
        </ul>
        <p><strong>0-RTT (early data):</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm">
          <li>Bea encrypts GET request using cached PSK</li>
          <li>Sends <code class="bg-purple-100 px-1">ClientHello (pre_shared_key + early_data)</code></li>
          <li>Sven receives, decrypts, processes request, sends response</li>
          <li><strong>Cost: 0 RTT for the request</strong> (but response still takes 1 RTT to return)</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "A user opens the same site they visited 5 minutes ago. The browser cached a session ticket. The site allows 0-RTT for GET requests. How many RTTs until the user receives the first byte of the response?",
      blanks: [
        {
          id: "rtt_count",
          expected: "1",
          placeholder: "Enter number of RTTs (0–2)",
        },
      ],
    },
    blank: {
      prompt: "Same scenario but the request is a POST that creates a database row. The 0-RTT is sent. Why does the server's policy refuse to honor (decrypt and process) the 0-RTT early data, and what should the client do?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Answer: POST is not idempotent. Replays are dangerous.</strong></p>
          <p class="text-sm">An attacker on the wire can capture the 0-RTT ClientHello and resend it. For GET, that's harmless (same read, same result). For POST, that's catastrophic: the database row is created twice, money is transferred twice, etc. The server policy restricts 0-RTT to safe methods (GET, HEAD, OPTIONS). If the client sends POST with early_data, the server decrypts the handshake but ignores the early_data, discards it, and asks the client to retransmit after the handshake completes.</p>
          <p class="text-xs text-gray-600 mt-2"><strong>Best practice:</strong> Servers should advertise which request types are 0-RTT-safe (e.g., via a custom HTTP header or configuration). Clients should cache this metadata with the session ticket and never send 0-RTT for unsafe requests.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "rtt_count:0": "Close, but the response still takes 1 RTT to travel back. 0-RTT means the request rides along with the handshake, but the response must still cross the network.",
    "rtt_count:2": "One too many. In 0-RTT mode, the request doesn't add an extra RTT — it's sent in the ClientHello. So total is 1 RTT (for the response to come back).",
  }}
/>

Now let's look at the numbers. <PersonaTag id="cara" lang="en" /> issues certificates; <PersonaTag id="sven" lang="en" /> carries the private key and must verify certificates during handshake.

<NumbersCard
  id="card-tls-numbers"
  title="TLS handshake costs"
  stats={[
    { label: "TLS 1.2 cold handshake", value: "2 RTT" },
    { label: "TLS 1.3 cold handshake", value: "1 RTT" },
    { label: "TLS 1.3 warm (resumption)", value: "1 RTT" },
    { label: "TLS 1.3 0-RTT (early data)", value: "0 RTT" },
    { label: "RSA 2048 verification", value: "~5 ms (hardware-dependent)" },
    { label: "ECDSA P-256 verification", value: "~1 ms (hardware-dependent)" },
    { label: "PSK ticket lifetime", value: "implementation choice (typical 1h)" },
    { label: "HSTS max-age", value: "31536000 s (1 year)" },
  ]}
/>

Let's compare the trade-off: TLS 1.2 cold connections paid two RTTs. TLS 1.3 halved it. But is there still a cost to moving key material forward?

<div id="card-rtt-cost" class="rounded-2xl bg-blue-50 border-2 border-blue-300 p-6 my-8">
  <div class="font-mono text-sm space-y-3">
    <div><strong>RTT cost comparison:</strong></div>
    <div class="space-y-2 text-xs">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-3 mt-3">
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.2</div>
          <div class="text-gray-600">Cold</div>
          <div class="text-blue-600 font-bold mt-1">2 RTT</div>
          <div class="text-xs text-gray-500 mt-2">ClientHello+ServerHello, then key exchange</div>
        </div>
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.3</div>
          <div class="text-gray-600">Cold</div>
          <div class="text-blue-600 font-bold mt-1">1 RTT</div>
          <div class="text-xs text-gray-500 mt-2">Key shares in ClientHello</div>
        </div>
        <div class="bg-white p-3 rounded border border-gray-200">
          <div class="font-bold">TLS 1.3</div>
          <div class="text-gray-600">Resumed (PSK)</div>
          <div class="text-blue-600 font-bold mt-1">1 RTT</div>
          <div class="text-xs text-gray-500 mt-2">But no cert validation</div>
        </div>
      </div>
    </div>
    <div class="text-xs text-gray-600 mt-4">At 100 ms RTT: TLS 1.2 adds 200 ms, TLS 1.3 cold adds 100 ms, TLS 1.3 warm + 0-RTT adds ~0 ms to the handshake itself.</div>
  </div>
</div>

<Misconception
  id="r-0rtt-replay"
  title="Heads-up: 0-RTT replay"
>
0-RTT data is not protected against replay attacks. An attacker on the wire can capture a 0-RTT ClientHello and resend it to the server. For idempotent requests (GET), that's benign; for state-changing requests (POST), that's exploitable. Server policy must restrict 0-RTT to safe HTTP methods.
</Misconception>

The handshake is now fast, but why do certificate verifications take any time at all? <PersonaTag id="sven" lang="en" /> sends his certificate, and Bea must verify that <PersonaTag id="cara" lang="en" /> (the CA) actually issued it. This verification is cryptographic: Bea uses Cara's public key to check Sven's signature. RSA signatures (traditional) are slower; ECDSA signatures (modern, especially P-256) are faster — on modern x86 server CPUs, roughly a 5× difference, though verification times are hardware-dependent. In large-scale deployments (CDNs, cloud providers), even 1 ms per connection adds up. This is why cached resumption (PSK) matters: it skips the verification entirely.

<SpiralCue thread="encapsulation" lang="en" /> — TLS wraps every byte of application data in a symmetric-key envelope. The envelope is negotiated via asymmetric cryptography (ECDHE, RSA signatures) during the handshake. Once negotiated, the symmetric key handles all data encryption. <SpiralCue thread="statefulness" lang="en" /> — the handshake builds state: both sides remember the shared secret, the negotiated cipher, and derived keys. This state persists until the connection closes or the key renegotiates.

<RetrievalDrawer
  client:load
  pieceSlug="05-tls-handshake"
  lang="en"
  questions={[
    {
      id: "q1",
      q: "Why does TLS 1.3 abandon RSA key transport (client encrypts shared secret with server's long-term key) in favor of ECDHE (ephemeral key exchange)?",
      answer: <p>RSA key transport leaks past sessions: if an attacker steals the server's long-term private key today, the attacker can decrypt all past TLS connections recorded before the compromise. ECDHE uses ephemeral keys generated fresh for each connection; once the connection closes, the ephemeral private keys are deleted. Even if the server's long-term key is later compromised, the attacker cannot decrypt old sessions because the ephemeral keys are gone. This property is called Perfect Forward Secrecy (PFS). ECDHE is more expensive per connection (ECDH computation), but modern hardware handles it in microseconds. The security win (PFS) is worth the cost, so TLS 1.3 made ECDHE mandatory and eliminated RSA key transport.</p>,
    },
    {
      id: "q2",
      q: "You see Chrome successfully connect to a site over TLS 1.3 0-RTT, and the site is `POST /transfer` with amount and recipient. You're the site's security officer. Why is the team unhappy, and what's the exploit?",
      answer: <p>The client sent a POST in 0-RTT, which means the POST request is encrypted with a cached pre-shared key (PSK) and rides in the ClientHello. An attacker on the network can capture the ClientHello and resend it multiple times. On the receiving end, the server decrypts the same POST request and processes it multiple times — same amount, same recipient, multiple transfers. The database now has duplicate rows, money is transferred twice, and the attacker has exploited your idempotency bug. Proper defense: (a) Server policy must refuse 0-RTT for POST/PUT/DELETE (only allow GET, HEAD, OPTIONS). (b) Client must not send non-idempotent requests in 0-RTT. (c) The server can add a replay window: reject 0-RTT data older than N seconds (e.g., 5 s), limiting the damage window.</p>,
    },
    {
      id: "q3",
      q: "The HSTS Preload List is sometimes called a mitigation for 'trust on first use.' Why is plain HSTS (without preload) insufficient, and what does preload add?",
      answer: <p>Plain HSTS (Strict-Transport-Security header) tells the browser 'never downgrade to HTTP for this domain.' But this only works if the first request is HTTPS and the header is received. On the very first visit (before the header is cached), the browser makes a plain HTTP request. An attacker on the network can intercept that first HTTP request, inject a redirect to a phishing site, and the user never learns the correct domain supports HTTPS. This is the "trust on first use" vulnerability. The HSTS Preload List is a hardcoded list of domains in the browser binary that support HSTS. For preload domains, the browser assumes HTTPS from the start, even on the first visit. The trade-off: being on the preload list requires operational discipline (you must deploy HSTS and never accidentally downgrade), and removal is slow (takes months). But new users are protected from day one.</p>,
    },
  ]}
/>

Once the handshake completes, the channel is sealed: every request and response travels encrypted. The next layer up is the protocol on top — HTTP — which negotiates its own version and flow control. TLS 1.3 works beneath it, transparent but essential.

### What's next

[**06 — HTTP versions**](/en/networking/06-http-versions/) — once the channel is encrypted, the protocol on top is HTTP.
