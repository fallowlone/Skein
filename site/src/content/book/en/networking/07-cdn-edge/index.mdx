---
slug: 07-cdn-edge
lang: en
pillar: networking
chapter: 01-networking
order: 7
title: "CDN: serving bytes from close"
summary: "How an Anycast IP, a smart DNS, and a tiered cache cooperate so the response comes from 20 ms away instead of 200 ms."
readingMin: 16
status: ready
prereqs: ["04-dns-resolution", "06-http-versions"]
spiral: ["latency"]
personas: ["bea", "patty", "sven"]
depth:
  mechanism: tier-mechanism
  tradeoff: card-edge-cost
  failure_mode: r-cache-key-bypass
  numbers: card-edge-numbers-list
sources:
  - https://blog.cloudflare.com/argo-intelligent-routing/
  - https://www.akamai.com/us/en/why-akamai/
  - https://datatracker.ietf.org/doc/html/rfc7234
  - https://web.dev/articles/stale-while-revalidate/
  - https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/
---

import TierAccordion from "../../../../../components/pedagogy/TierAccordion.astro";
import FadedExample from "../../../../../components/pedagogy/FadedExample.tsx";
import RetrievalDrawer from "../../../../../components/pedagogy/RetrievalDrawer.tsx";
import Crux from "../../../../../components/prose/Crux.astro";
import SpiralCue from "../../../../../components/prose/SpiralCue.astro";
import PersonaTag from "../../../../../components/pedagogy/PersonaTag.astro";
import NumbersCard from "../../../../../components/layout/NumbersCard.astro";
import Misconception from "../../../../../components/layout/Misconception.astro";
import Card from "../../../../../components/layout/Card.astro";

When <PersonaTag id="bea" lang="en" /> (the browser) loads a page from a server in California, every request travels 200 ms round-trip from her network in London. The page stalls. But what if her request never left London? What if a copy of the server's data lived in a data center next door, served the response in 20 ms, and stayed synchronized? That is a CDN — Content Delivery Network. It is the most direct way to defeat latency: put content near the user. Three mechanisms work in concert: <SpiralCue thread="latency" lang="en" /> Anycast IP routing (same address advertised from many cities), edge DNS (returning different server addresses by geography), and tiered caching (so the origin server answers only 1% of requests). Together, they move the performance bottleneck from the speed of light to the strategy of the cache.

<Crux>How CDN caching defeats latency: edge locations, cache keys, and why forgetting `Vary` breaks everything.</Crux>

<TierAccordion id="tier-mechanism" lang="en">
  <Fragment slot="junior">
    <p>A CDN is a copy of your website living in 200+ data centers around the world. When someone visits, the network automatically sends them to the nearest copy. Same content, much shorter trip. That's it. The site owner pushes content to the CDN once, and the CDN replicates it to every edge location. <PersonaTag id="bea" lang="en" /> in London hits the London edge, <PersonaTag id="patty" lang="en" /> (a proxy server in Tokyo) connects to the Tokyo edge, and <PersonaTag id="sven" lang="en" /> (the origin server in California) only sees traffic from the CDN, not from users.</p>
  </Fragment>
  <Fragment slot="middle">
    <p><strong>Three pieces cooperate: Anycast IP, GeoDNS, and tiered cache.</strong></p>
    <p><strong>Anycast IP:</strong> The CDN advertises the same IP address (say, `93.184.216.1`) from multiple geographic locations. BGP routing selects the "nearest" edge location (by hop count or AS path length). When Bea makes a request, her local router sends it to the closest edge POP (Point of Presence) that advertises that IP. No special DNS — just routing.</p>
    <p><strong>GeoDNS:</strong> Most CDNs prefer explicit control via DNS. The CDN operator configures DNS to return different A records based on the resolver's geographic location (inferred from IP). A query from a London resolver returns the London edge IP; a Tokyo resolver gets Tokyo. The resolver is the client requesting DNS, not the end browser. A global resolver like 8.8.8.8 appears to come from Google's data center, so GeoDNS returns a regional IP based on that location, not the browser's. This is why users sometimes get misrouted — their DNS provider is far from home.</p>
    <p><strong>Tiered caching:</strong> The edge has limited storage. Frequent requests (top 20% of assets) live at the edge. Rare requests miss the edge and go to an origin shield — an intermediate cache tier between edge and origin. The shield absorbs ~99% of misses, protecting the origin from request storms. Cache key is URL + variant headers (Accept-Encoding, Accept-Language). The response header `Vary: Accept-Language` tells the cache: "Keep separate entries for each language." `Cache-Control` directives govern duration: `max-age=3600` (1 hour, for browser and CDN), `s-maxage=86400` (1 day, CDN only, browser ignores), `private` (browser only, skip CDN), `no-store` (never cache), `stale-while-revalidate=604800` (serve stale, refresh in background for 7 days after expiry).</p>
  </Fragment>
  <Fragment slot="senior">
    <p><strong>Anycast and smart routing layering.</strong> Anycast is simple but suboptimal. BGP selects by hop count, not latency. Two cities 10 hops away via optimal backbone might measure worse by BGP metrics. Cloudflare Argo and AWS Global Accelerator add a smart layer: measure actual latency via probes, then use a private backbone (not the public internet) to route requests to the lowest-latency POP regardless of BGP. This can save 50+ ms on inter-continental hops.</p>
    <p><strong>Cache keys and Vary header semantics.</strong> The cache key is built from: URL (required), method (GET vs POST), Host header, and headers listed in `Vary`. If a response carries `Vary: Accept-Encoding, Accept-Language`, the cache creates separate entries for [URL + gzip], [URL + brotli], [URL + en], [URL + ru]. When a request arrives, the cache matches URL + method + Vary-listed headers. A missing Vary header means subsequent requests with different Accept-Language get the first response regardless. RFC 7234 § 4.1 formalizes this; it's a silent footgun.</p>
    <p><strong>Cookies and cache poisoning.</strong> Cookies are NOT included in the cache key by default. If an endpoint reads a session cookie and returns personalized content, a missing `Vary: Cookie` or `Cache-Control: private` means one user's response can serve to another. This is a real vulnerability: developer forgets to mark `/account` as private; first user logs in, hits the cache; second user (with no cookie) gets the first user's account page. Solution: either `private` (no CDN cache) or `Vary: Set-Cookie` plus a unique identifier in the cookie that becomes part of the key.</p>
    <p><strong>Stale-while-revalidate (SWR).</strong> After a cached response expires, the next request would cause a cache miss (round-trip to origin). SWR flips this: serve the stale response immediately, then refresh it in the background. Users get fast loads even after expiry; staleness window is `max-age + stale-while-revalidate`. For a news feed (max-age=60, SWR=604800), users can fetch a 10-day-old cache, which hits within milliseconds, while the CDN refreshes in the background. Great for availability; not for correctness-critical data.</p>
    <p><strong>Cache tags and surrogate invalidation.</strong> Fastly and Cloudflare Enterprise support cache tags: the origin assigns tags to responses (e.g., `Cache-Tag: user:42, article:1001`). Later, a single API call (`PURGE tag user:42`) invalidates all cached responses tagged with `user:42` without touching others. This beats URL purging (which requires knowing every URL) and beats time-based expiry (which forces stale data). Origin shield becomes critical here: without it, a large invalidation (e.g., popular user's post updated) causes a thundering herd of origin hits as all edges request fresh data simultaneously.</p>
    <p><strong>Edge workers and compute.</strong> Modern CDNs (Cloudflare Workers, Fastly Compute, AWS Lambda@Edge) run code at the edge — V8 isolates with ~5 ms cold start. Transform requests (add headers, merge microservices, A/B test variants), compose responses (ESI — Edge Side Includes, or dynamic fragments), or decide cache strategy on the fly. Worker cold starts are negligible; the latency win over a 200 ms round-trip to origin dwarfs the startup cost.</p>
    <p><strong>TLS resumption and session state at the edge.</strong> A new TLS connection costs 1 RTT. Session resumption (via session tickets or TLS 1.3 pre-shared keys) amortizes this. Modern CDNs generate tickets at one edge POP and replicate them across the network — if Bea's connection was interrupted and she reconnects via a different POP, that POP already has her ticket, skipping re-handshake. The payoff is massive on mobile (frequent reconnects).</p>
  </Fragment>
</TierAccordion>

The magic of a CDN is invisible to <PersonaTag id="bea" lang="en" />. She sees a response in 20 ms instead of 200 ms. Behind that, three forces dance: Anycast sends her to the nearest POP, GeoDNS returns the right edge IP, and the cache either has the data or fetches it from an upper tier. Let's trace a cache hit and a miss.

<FadedExample
  client:load
  id="fade-cache-key"
  pieceSlug="07-cdn-edge"
  lang="en"
  title="Cache key surprises"
  steps={{
    solved: (
      <div class="space-y-4">
        <p><strong>Scenario: the same URL, different Accept-Language.</strong></p>
        <ul class="list-disc list-inside space-y-2 text-sm mb-4">
          <li><strong>Step 1:</strong> User A (London) fetches `/api/products`. Accept-Language: en. CDN cache miss. Origin responds with a JSON list of products and header `Cache-Control: max-age=3600, Vary: Accept-Language`.</li>
          <li><strong>Step 2:</strong> CDN stores this in cache with key: [URL=/api/products, Accept-Language=en]. Entry lives 1 hour.</li>
          <li><strong>Step 3:</strong> User B (also London) fetches `/api/products`. Accept-Language: ru. CDN checks cache. Key [URL=/api/products, Accept-Language=ru] does NOT exist. Cache miss. Origin is queried again, returns Russian products. CDN stores a second entry.</li>
          <li><strong>Result:</strong> Both users get correct language. The `Vary` header ensures separate cache entries.</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "Same scenario. Server responds with `Cache-Control: max-age=3600` but FORGETS to add `Vary: Accept-Language`. User A fetches in en, then User B fetches in ru. What does User B see?",
      blanks: [
        {
          id: "bug",
          expected: "english",
          placeholder: "What language does User B see? (english/russian/error)",
        },
      ],
    },
    blank: {
      prompt: "A logged-in user navigates to `/account`, sees their name and email. The CDN logs show a cache hit. An hour later, a different user (not logged in) visits the same `/account` URL and sees... the first user's name and email. Cache logs show hits for both. What did the engineer forget?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Answer: They forgot to mark the response as `private` or to add `Vary: Cookie`.</strong></p>
          <p class="text-sm">The endpoint reads a session cookie (e.g., session_id=abc123) and returns personalized content. Cookies are NOT part of the default cache key. The first request caches [URL=/account, method=GET, ...] and stores personalized content. The second request (different user, different session_id cookie) matches the same cache key, and the cached personalized content serves to the wrong user.</p>
          <p class="text-xs text-gray-600 mt-2"><strong>Fix:</strong> Either add `Cache-Control: private` (bypass CDN, only browser caches), or add `Vary: Cookie` and include the session ID or a hash of it in the cache key. Modern practices prefer `private` for user-specific endpoints.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "bug:russian": "Not quite. The CDN treats both requests as cache-equivalent (same URL, no Vary header). The first response (English) is cached. The second request (Russian Accept-Language) matches the same key and retrieves the cached English response. No language negotiation happens.",
  }}
/>

Now the latency win. When the cache hits, what's the payoff?

<div id="card-edge-cost" class="rounded-2xl border-2 border-orange-300 bg-orange-50 p-6 my-8">
  <h3 class="font-bold text-lg mb-4">Edge cache hit vs. origin round-trip</h3>
  <div class="grid grid-cols-2 gap-4 text-sm">
    <div class="bg-white p-4 rounded border border-orange-200">
      <div class="font-bold text-orange-600 mb-2">Origin (California → London)</div>
      <div class="space-y-1">
        <div>TCP handshake: ~100 ms</div>
        <div>TLS handshake: ~100 ms</div>
        <div>HTTP request/response: ~50 ms</div>
        <div class="font-bold text-orange-700 mt-2">Total: ~250 ms</div>
      </div>
    </div>
    <div class="bg-white p-4 rounded border border-green-200">
      <div class="font-bold text-green-600 mb-2">Edge cache hit (London edge)</div>
      <div class="space-y-1">
        <div>DNS (cached): &lt;1 ms</div>
        <div>TCP SYN: ~1 ms (local)</div>
        <div>TLS (warm, ticket): ~2 ms</div>
        <div>HTTP GET (in-memory cache): ~1 ms</div>
        <div class="font-bold text-green-700 mt-2">Total: ~10 ms</div>
      </div>
    </div>
  </div>
  <div class="text-xs text-gray-600 mt-4 text-center">A cache hit at the edge is 20–25× faster than an origin request.</div>
</div>

The numbers tell the story.

<NumbersCard
  id="card-edge-numbers-list"
  title="Typical CDN edge numbers"
  stats={[
    { label: "Edge POPs (Cloudflare 2026)", value: "200+ across 100+ countries (vendor figure)" },
    { label: "Typical edge → origin RTT (private backbone)", value: "30–100 ms" },
    { label: "Edge cache hit ratio (well-configured site)", value: "90–98%" },
    { label: "Origin shield traffic absorption", value: "90%+ of misses (vendor configurations)" },
    { label: "Edge worker cold start", value: "0–5 ms" },
    { label: "Anycast IP advertisements per POP", value: "1–2 globally routable blocks" },
  ]}
/>

The tradeoff is real. CDN caching trades consistency for speed. A cache hit means the origin's updated data may not have reached the edge yet. Invalidation strategies (TTL expiry, tag-based purging, or soft-purge + revalidation) decide whether staleness is acceptable.

<Misconception
  id="r-cache-key-bypass"
  title="Heads-up: cookies aren't in the cache key by default"
>
CDNs cache by URL and headers in the `Vary` response. Cookies are NOT in the cache key. If your endpoint reads cookies, mark it `private` (no CDN cache) or add `Vary: Cookie` — else one user's page serves to another.
</Misconception>

Why does this matter? Because every cache mistake is invisible until it happens. A forgotten `Vary` header serves Russian content to English-speaking users. A missing `private` on `/account` leaks user data. Stale-while-revalidate buys speed but risks serving outdated information. As <SpiralCue thread="latency" lang="en" /> — reducing response time — becomes a competitive advantage, engineers tune cache strategies obsessively. High hit ratios (95%+) mean most requests never reach the origin. Low TTLs (60 seconds) keep data fresh but increase origin load. The balance is a dial you turn per endpoint.

<RetrievalDrawer
  client:load
  pieceSlug="07-cdn-edge"
  lang="en"
  questions={[
    {
      id: "q1",
      q: "Why is `stale-while-revalidate` important for cache stampede defense?",
      answer: <p>A cache stampede happens when a cached response expires and many concurrent requests hit the cache miss simultaneously, causing a thundering herd of requests to the origin. Imagine max-age=1 hour. At 1:00:00 +1s, the cache expires; 1000 concurrent users refresh, all requesting the origin at once. The origin gets hammered. `stale-while-revalidate=604800` inverts this: at expiry, serve the stale response immediately to all users, and send just ONE revalidation request to the origin in the background. Other users are unaffected. By the time the next user arrives, the revalidation is done and the cache is fresh again. The stampede never happened.</p>,
    },
    {
      id: "q2",
      q: "You see a page mixing static assets (CSS, JS) and authenticated content (user's name, email). Where do you set `Cache-Control: private` vs `public, max-age=...`?",
      answer: <p>Static assets (CSS, JS, images) are not user-specific. Mark them `public, max-age=31536000` (1 year). Let the CDN cache them aggressively; every user reuses the same cached file. Authenticated content (name, email, account details) is user-specific. Mark it `private` or set a short `max-age=0, must-revalidate` with a session-based cache key. Some teams use `Cache-Control: private, max-age=300` to let the browser cache it for 5 minutes, but skip the CDN entirely. The division between public and private assets is architectural: separate URLs for static content (`/assets/style.css`) from authenticated data (`/api/user`), and cache accordingly.</p>,
    },
    {
      id: "q3",
      q: "Anycast routes you to the wrong geographic region during a re-balancing event. What measurement would catch this, and what's the operator's fix?",
      answer: <p>Measure response latency from the client's perspective. If a London user suddenly gets 100+ ms responses (instead of 10 ms), but origin latency is 200 ms, something is wrong — BGP might have shifted the route to a distant POP. Operator's fix: (1) monitor latency per client IP / geographic region; (2) compare to baseline; (3) if an edge POP's latency climbs, either withdraw its Anycast advertisement (force BGP to re-route) or use GeoDNS explicitly to bypass Anycast. Some CDNs layer smart routing (Cloudflare Argo) on top: measure real latencies continuously, select the lowest-latency POP regardless of BGP, and reroute via private backbone. This trades complexity for robustness.</p>,
    },
  ]}
/>

CDNs are not free. Storage at 300 POPs is expensive. Synchronizing content across the world is operationally complex. But the latency win (10 ms instead of 250 ms) justifies the cost for any site that serves global users. Static assets (images, CSS, JS) are the easiest wins — cache them aggressively, distribute globally, let the CDN do the work. Dynamic content (APIs, personalized pages) requires more care: cache what you can, invalidate strategically, and understand the trade-off between stale data and origin load.

### What's next

[**08 — Putting it together**](/en/networking/08-putting-it-together/) — we walk a complete HTTPS request end-to-end, from DNS to TLS to HTTP to CDN, with a sandbox to test timing budgets.
