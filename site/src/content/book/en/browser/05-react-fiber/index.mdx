---
slug: 05-react-fiber
lang: en
pillar: browser
chapter: 02-browser
order: 5
title: "React Fiber: the reconciler, render vs commit, lanes, and time-slicing"
summary: "How React turns a tree of components into DOM mutations — the fiber unit of work, the double-buffered tree, the interruptible render phase, the atomic commit phase, priority lanes, and the 5 ms time-slice that keeps input responsive."
readingMin: 25
status: ready
prereqs: ["01-event-loop", "03-v8-internals"]
spiral: ["statefulness", "encapsulation"]
personas: ["bea", "sven"]
depth:
  mechanism: tier-fiber
  tradeoff: card-fiber-cost
  failure_mode: mc-key-index
  numbers: card-fiber-numbers
sources:
  - https://react.dev/learn/render-and-commit
  - https://react.dev/reference/react/useTransition
  - https://react.dev/reference/react/useDeferredValue
  - https://github.com/acdlite/react-fiber-architecture
  - https://github.com/facebook/react/blob/main/packages/react-reconciler/src/ReactFiberLane.js
  - https://react.dev/learn/preserving-and-resetting-state
  - https://react.dev/reference/react/memo
  - https://web.dev/articles/inp
---

import Crux from "../../../../../components/prose/Crux.astro";
import KeyTakeaway from "../../../../../components/prose/KeyTakeaway.astro";
import SpiralCue from "../../../../../components/prose/SpiralCue.astro";
import Term from "../../../../../components/prose/Term.astro";
import TierAccordion from "../../../../../components/pedagogy/TierAccordion.astro";
import RetrievalDrawer from "../../../../../components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "../../../../../components/pedagogy/FadedExample.tsx";
import MetaphorComplete from "../../../../../components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "../../../../../components/pedagogy/NumberDrill.astro";
import PersonaTag from "../../../../../components/pedagogy/PersonaTag.astro";
import Quiz from "../../../../../components/pedagogy/Quiz.astro";
import DragOrder from "../../../../../components/pedagogy/DragOrder.astro";
import TraceScenario from "../../../../../components/pedagogy/TraceScenario.astro";
import DebugLog from "../../../../../components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "../../../../../components/pedagogy/TradeoffMatrix.astro";
import RFCQuiz from "../../../../../components/pedagogy/RFCQuiz.astro";
import DesignPrompt from "../../../../../components/pedagogy/DesignPrompt.astro";
import NumbersCard from "../../../../../components/layout/NumbersCard.astro";
import Misconception from "../../../../../components/layout/Misconception.astro";

You call `setState`. Somewhere between that call and a pixel changing on screen, React decided which components to re-run, compared two trees, worked out the minimum set of DOM mutations, and applied them — possibly pausing halfway to let a keystroke through. The machine that does this is the fiber reconciler. It is the reason a React app can re-render a 5 000-node tree without freezing the input box, and the reason a single misplaced `key` can silently corrupt your component state.

<Crux>A tree of components becomes DOM mutations: render computes the diff (interruptible), commit applies it (atomic).</Crux>

React splits every update into two phases. The **render phase** walks the component tree, runs your function components, and builds a new tree of *fiber* nodes — this phase is pure, produces no visible change, and since React 18 can be paused, resumed, or thrown away. The **commit phase** takes the finished new tree and applies the DOM mutations in one synchronous, uninterruptible burst. Everything good and everything dangerous about React performance lives in the gap between "interruptible render" and "atomic commit."

<TierAccordion id="tier-fiber" lang="en">
  <Fragment slot="junior">
    <p><strong>What the reconciler does.</strong> When your data changes, React figures out the smallest set of page changes that match the new data, and applies only those.</p>
    <p><strong>The blueprint metaphor.</strong> A naive builder would demolish the house and rebuild it whenever the blueprint changes. React is a smart builder: it keeps the <em>current</em> blueprint on the wall, draws the <em>new</em> one on a second sheet, lays them side by side, and marks only the differences. Then it sends the crew one work order with exactly those changes. Drawing the second blueprint is the render phase; handing the order over is the commit phase.</p>
    <p><strong>Why care.</strong> The DOM is slow to mutate — touching it a thousand times is far slower than ten. React's job is to compute the ten real changes instead of blindly redoing a thousand. And because drawing the new blueprint can be paused, React can stop mid-draw to handle a click, then resume — so the page never feels frozen.</p>
    <p><strong>Read-aloud frame.</strong> <PersonaTag id="bea" lang="en" /> types in a search box that filters a long list. <PersonaTag id="sven" lang="en" /> narrates: "Each keystroke is a state change. React re-runs the components and diffs the new fiber tree against the current one. The list of 800 rows changed — but React finds only the 12 rows that differ and mutates just those 12 DOM nodes. The other 788 are untouched. That is why the box feels instant."</p>
    <DragOrder
      id="jr-drag-1"
      pieceSlug="05-react-fiber"
      lang="en"
      prompt="A setState call travels through React to the screen. Drag the steps into order."
      items={[
        { id: "setstate", label: "setState is called — React schedules an update" },
        { id: "render", label: "Render phase: re-run components, build the new fiber tree" },
        { id: "diff", label: "Reconcile: diff the new tree against the current tree" },
        { id: "commit", label: "Commit phase: apply the DOM mutations in one burst" },
        { id: "paint", label: "Browser paints the changed pixels" },
      ]}
      correctOrder={["setstate", "render", "diff", "commit", "paint"]}
    />
    <Quiz
      id="jr-quiz-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Why does React diff two trees instead of just rewriting the whole DOM?"
      choices={[
        { label: "Mutating the DOM is expensive — diffing finds the minimal set of changes so only what actually changed is touched", correct: true },
        { label: "Diffing is required by the JavaScript language", misconception: "Nothing in JS requires it. It is React's design choice, made precisely because blind DOM rewriting is slow." },
        { label: "The browser cannot rewrite the whole DOM at once", misconception: "The browser can rewrite the whole DOM — it is just slow. React diffs to avoid that slowness, not because it is impossible." },
        { label: "Diffing makes the JavaScript bundle smaller", misconception: "The reconciler is code that adds to the bundle. Its payoff is runtime speed, not bundle size." },
      ]}
    />
    <Quiz
      id="jr-quiz-2"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Which phase can React pause partway through?"
      choices={[
        { label: "The render phase — building and diffing the tree produces no visible change, so it is safe to interrupt", correct: true },
        { label: "The commit phase", misconception: "Commit applies DOM mutations and must be atomic — pausing it would leave the page in a half-updated, inconsistent state." },
        { label: "Both phases equally", misconception: "Only render is interruptible. Commit is always one synchronous, uninterruptible burst." },
        { label: "Neither — React always runs start to finish", misconception: "Since React 18, the render phase is interruptible. That is the core of concurrent rendering." },
      ]}
    />
    <MetaphorComplete
      id="jr-metaphor-1"
      pieceSlug="05-react-fiber"
      lang="en"
      setup="In the builder metaphor, React keeps the current blueprint pinned to the wall and draws the new one on a second sheet, so it can compare them. Holding two versions of the tree at once — one shown, one being built — is called what?"
      accepted={["double buffering", "double-buffering", "double buffer", "two trees", "current and workInProgress"]}
      canonical="double buffering"
      explanation="Double buffering means React keeps two fiber trees: the 'current' tree that matches what is on screen, and the 'workInProgress' tree it is building for the next update. It works on the workInProgress copy without disturbing the current one, so a half-built update is never visible. When the workInProgress tree is finished and committed, the two swap roles — the new one becomes current. This is the same technique games use to avoid showing a half-drawn frame."
    />
    <NumberDrill
      id="jr-drill-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="React's scheduler does render work in small chunks, then yields to the browser. Roughly how many milliseconds is one chunk before React checks whether it should yield?"
      expected={5}
      tolerance={1}
      unit="ms"
      explanation="React works in slices of about 5 ms, then asks 'should I yield?' If the browser has input or rendering to do, React pauses and resumes later. 5 ms is small enough that even a few of them back-to-back fit inside one 16.67 ms frame alongside the browser's own work — so input stays responsive even during a large render."
    />
  </Fragment>
  <Fragment slot="middle">
    <p><strong>What a fiber actually is.</strong> A fiber is a plain JavaScript object — one per component instance, one per DOM element in the tree. Each fiber holds: the component `type`, its `props`, its `key`, its current `state` and hooks, pointers to its `child`, its next `sibling`, and its `return` (parent), plus an `alternate` pointer to its counterpart in the other tree. The fibers form a linked tree, not an array — and that linked structure is the whole point. A recursive tree walk lives on the call stack and cannot be paused; a linked-list walk holds its position in a plain variable, so React can stop after any fiber, store the pointer, and resume from exactly there later. Fiber is the data structure that made interruptible rendering possible. The name predates React 18 — the architecture landed in React 16 (2017) — but only React 18 turned on the concurrent features that exploit it.</p>

    <p><strong>The two trees: current and workInProgress.</strong> React always holds two fiber trees. The <em>current</em> tree is the one whose state matches what is painted on screen right now. When an update starts, React builds a <em>workInProgress</em> tree — cloning fibers from current as it goes, applying new props and state. Each fiber's `alternate` pointer links it to its twin in the other tree, so cloning is cheap: unchanged subtrees can be reused by reference. When the workInProgress tree is complete and committed, the two trees swap: workInProgress becomes current, and the old current tree is kept around to become the next workInProgress. This is double buffering. It guarantees the on-screen tree is never observed in a half-updated state — and it is why a render that gets interrupted and thrown away costs nothing permanent: the discarded workInProgress tree simply never commits.</p>

    <p><strong>Render phase: interruptible, must be pure.</strong> The render phase walks the workInProgress tree, calls each function component, and runs reconciliation. Because React 18 can pause this phase, resume it, or discard it and start over, your component function may be called more than once for a single visible update — and a paused render may sit half-finished while the user interacts. This is why a render must be <em>pure</em>: no side effects, no mutating external variables, no DOM reads or writes, no network calls in the function body. A side effect in render runs an unpredictable number of times. React's StrictMode deliberately double-invokes components in development precisely to surface impure renders early. The disciplined place for side effects is `useEffect` (after commit) or an event handler (outside render entirely).</p>

    <p><strong>Commit phase: atomic, synchronous, three sub-steps.</strong> Once the workInProgress tree is fully built, commit runs in one uninterruptible block. It has three sub-steps. <em>Before-mutation</em>: React reads any pre-mutation state (this is where `getSnapshotBeforeUpdate` and the cleanup snapshot for `useLayoutEffect` happen). <em>Mutation</em>: React applies every DOM change — insertions, deletions, attribute updates, text changes — driven by the effect flags it tagged onto fibers during render. <em>Layout</em>: React runs `useLayoutEffect` callbacks and updates refs, synchronously, before the browser paints. Because commit is synchronous and uninterruptible, a huge tree with thousands of mutations produces one long task — commit is the part of React you cannot time-slice, so the way to keep it short is to keep the number of mutations small (virtualisation, memoisation).</p>

    <p><strong>Reconciliation: the diffing rules.</strong> Reconciliation compares the new element tree against the current fiber tree to decide what to keep, update, or replace. A full tree diff is O(n³) in the general case; React makes it O(n) with two heuristics. First: <em>different element type means full replacement</em> — if a `<div>` becomes a `<span>`, or `<ComponentA>` becomes `<ComponentB>`, React tears down the whole subtree (unmounting it, destroying its state) and builds the new one fresh. It does not try to diff across types. Second: <em>keys identify list children across renders</em> — within a list, React matches old and new children by their `key`, not their position. A stable key tells React "this is the same logical item, even if it moved." This is the entire reason keys exist, and getting them wrong is the most common React state bug.</p>

    <p><strong>Why `key` matters and why `key={index}` is a trap.</strong> When you render a list, React needs to pair each new child with an old fiber so it can preserve that fiber's state (and its DOM node). With a stable key — a database id, a slug — React tracks each item correctly through insertions, deletions, and reorders. With `key={index}`, the key is the position, so when the list reorders or an item is inserted at the front, every item's key shifts. React then thinks item-at-index-0 is "the same item" even though the data moved — it keeps the old fiber's state and DOM but feeds it the new item's props. The visible bug: a checkbox checked on row 3 stays checked after you delete row 1, now attached to what used to be row 4. `key={index}` is safe only for a list that is static and never reorders; for anything dynamic it silently corrupts state.</p>

    <p><strong>Priority lanes.</strong> React 18 assigns every update a <em>lane</em> — a priority bucket. The lane model (a bitmask of 31 lanes) lets React work on high-priority updates first and defer low-priority ones. The important tiers: <em>discrete</em> input (a click, a keypress) is synchronous-priority — React renders it immediately and blocking, because the user is waiting on that exact interaction. <em>Default</em> priority covers ordinary updates. <em>Transition</em> priority (set via `useTransition`) is for updates the user is not directly waiting on — filtering a big list, navigating to a new view — and these can be interrupted by anything more urgent. <em>Idle</em> is the lowest. When a keypress arrives mid-transition-render, React abandons the in-progress transition render, processes the keypress at high priority, and restarts the transition afterward. Lanes are how React decides what to render now and what to render later.</p>

    <p><strong>Time-slicing: how the render phase yields.</strong> A long synchronous render blocks input — the exact failure mode the event-loop piece described. React's scheduler breaks the render phase into slices. After about 5 ms of reconciliation work, the scheduler checks `shouldYield()`; if true, it stops, posts a continuation task via `MessageChannel` (the cross-browser way to schedule a task with no clamping), and lets the browser run input handlers and rendering. The continuation task resumes the render from the stored fiber pointer. A 50 ms render becomes ten 5 ms slices interleaved with the browser's own work, and input stays responsive throughout. This only applies to <em>concurrent</em> renders — transitions and other non-urgent updates. A synchronous-priority update (discrete input) still renders in one blocking pass, because making the user wait for a yield would defeat the purpose.</p>

    <p><strong>useTransition and useDeferredValue.</strong> These are the two hooks that put work into the transition lane. `useTransition` gives you a `startTransition` function — state updates inside it are marked low-priority and interruptible, plus an `isPending` flag for showing a spinner. The canonical use: a search input where the input value itself updates synchronously (so typing feels instant) but the expensive filtered-list update is wrapped in `startTransition` (so it yields to further keystrokes). `useDeferredValue` is the same idea from the consuming side: it gives you a copy of a value that "lags behind" — React updates it at transition priority, so a component reading the deferred value re-renders only when React has spare time. Use `useTransition` when you own the state update; `useDeferredValue` when the value comes from props or context you do not control.</p>

    <p><strong>The virtual DOM, honestly.</strong> "Virtual DOM" is the marketing name for the element tree React diffs. It is worth being precise: the virtual DOM is not faster than direct DOM manipulation — a hand-written, optimal sequence of DOM calls always beats React. What the virtual DOM buys is a <em>programming model</em>: you describe the UI as a pure function of state and never write imperative DOM code, and React makes the result fast <em>enough</em> by diffing. The cost is real — building and diffing the element tree is CPU work that a compiler-based framework (Svelte) or a fine-grained reactive one (SolidJS) avoids. React's bet is that the developer-experience win of "UI = f(state)" is worth the reconciliation overhead, and that time-slicing keeps that overhead from ever blocking the user. Knowing this lets you reason about when React's model is and is not the right tool — a dashboard with thousands of cheap nodes updating constantly is exactly where reconciliation overhead bites, and exactly where a fine-grained reactive model would win; a form-heavy app with mostly-static structure is where React's model costs almost nothing and the developer-experience win is pure profit.</p>

    <Quiz
      id="mid-quiz-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="A list of items renders with `key={index}`. You delete the first item. A checkbox that was checked on the old second item is now checked on the wrong row. Why?"
      choices={[
        { label: "With index keys, deleting item 0 shifts every key down one — React matches fibers by key, so it keeps each fiber's state but pairs it with the next item's data", correct: true },
        { label: "React lost the checkbox state during the delete", misconception: "State was not lost — it was preserved on the wrong fiber. The fibers stayed; the data they were paired with shifted." },
        { label: "Checkboxes always reset when a list changes", misconception: "Checkboxes keep their state fine with stable keys. The bug is specifically the index-as-key mismatch." },
        { label: "The commit phase applied mutations out of order", misconception: "Commit order is correct. The mismatch happened in reconciliation, where index keys made React pair fibers with the wrong items." },
      ]}
    />
    <Quiz
      id="mid-quiz-2"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Why must a function component's body be pure (no side effects)?"
      choices={[
        { label: "React 18 can pause, resume, or discard a render — the component may run more than once per visible update, so side effects would run an unpredictable number of times", correct: true },
        { label: "Pure functions run faster", misconception: "Purity is not primarily about speed. It is about correctness under interruptible rendering — an impure render misbehaves when React re-runs it." },
        { label: "JavaScript requires component functions to be pure", misconception: "JS imposes no such rule. It is React's contract, required because the reconciler may call the function multiple times." },
        { label: "Side effects break the bundler", misconception: "The bundler does not care. The constraint is runtime: a render that may be replayed must not cause observable effects each time." },
      ]}
    />
    <TraceScenario
      id="mid-trace-1"
      pieceSlug="05-react-fiber"
      lang="en"
      scenario="A dashboard re-renders slowly on every keystroke in an unrelated search box. The React Profiler shows almost every component on the page re-rendering, even ones whose props did not change. Where is the problem?"
      steps={[
        { prompt: "A context provider near the top of the tree has a value that is a fresh object literal every render — every consumer re-renders because the context value is a new reference", reveal: "Correct. `<Ctx.Provider value={{ user, theme }}>` creates a new object every parent render. Context consumers re-render whenever the provider's value changes by reference — and a fresh literal always differs. Every keystroke updates some state high up, the provider re-runs, the value object is new, and the whole subtree of consumers re-renders. Fix: memoise the context value with useMemo so its reference is stable when its contents are." },
        { prompt: "The search box is missing a debounce", reveal: "Debounce would reduce how often this happens, but it does not explain why unrelated components re-render at all. The root cause is the unstable context value; debounce only masks the frequency." },
        { prompt: "React's diffing algorithm is O(n³)", reveal: "React's diff is O(n) thanks to the type and key heuristics. The mass re-render is not the diff being slow — it is too many components being marked dirty by the context change." },
        { prompt: "The list needs virtualization", reveal: "Virtualization helps if a long list is the cost. But here the Profiler shows unrelated components re-rendering — that is a context-value identity problem, not a list-length problem." },
      ]}
    />
    <Quiz
      id="mid-quiz-3"
      pieceSlug="05-react-fiber"
      lang="en"
      question="You wrap an expensive list update in `startTransition`. A keystroke arrives while that render is in progress. What does React do?"
      choices={[
        { label: "Abandons the in-progress transition render, processes the keystroke at high priority, then restarts the transition render", correct: true },
        { label: "Finishes the transition render first, then handles the keystroke", misconception: "That would make the keystroke wait — the opposite of what transitions are for. React interrupts the transition for the urgent input." },
        { label: "Runs both renders in parallel on two threads", misconception: "React's reconciler runs on the single main thread. There is no parallel rendering — there is interruption and resumption." },
        { label: "Drops the keystroke", misconception: "The keystroke is never dropped. It is processed at high priority, ahead of the interruptible transition work." },
      ]}
    />
    <Quiz
      id="mid-quiz-4"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Which kind of update does React render in one blocking pass, with no time-slicing?"
      choices={[
        { label: "A discrete input update — a click or keypress — because the user is waiting on that exact interaction", correct: true },
        { label: "Any update inside startTransition", misconception: "Transition updates are exactly the ones that ARE time-sliced and interruptible. Discrete input is the opposite." },
        { label: "All updates — React never time-slices", misconception: "React 18 time-slices transition and other non-urgent updates. Discrete input is the exception that renders synchronously." },
        { label: "Updates triggered by useDeferredValue", misconception: "useDeferredValue updates run at transition priority — interruptible and time-sliced, not blocking." },
      ]}
    />
    <DragOrder
      id="mid-drag-1"
      pieceSlug="05-react-fiber"
      lang="en"
      prompt="Order the sub-steps of the commit phase, from the moment the workInProgress tree is finished."
      items={[
        { id: "before", label: "Before-mutation: read pre-mutation state (getSnapshotBeforeUpdate)" },
        { id: "mutation", label: "Mutation: apply DOM insertions, deletions, updates" },
        { id: "swap", label: "Swap: workInProgress tree becomes the current tree" },
        { id: "layout", label: "Layout: run useLayoutEffect, update refs (before paint)" },
        { id: "paint", label: "Browser paints; passive effects (useEffect) run after" },
      ]}
      correctOrder={["before", "mutation", "swap", "layout", "paint"]}
    />
    <MetaphorComplete
      id="mid-metaphor-1"
      pieceSlug="05-react-fiber"
      lang="en"
      setup="React 18 assigns every update a priority bucket so it can render urgent work first and defer the rest. A keypress goes in the synchronous bucket; a big list filter wrapped in startTransition goes in a low, interruptible bucket. What does React call these priority buckets?"
      accepted={["lanes", "lane", "lane model", "priority lanes", "the lanes"]}
      canonical="lanes"
      explanation="Lanes are React 18's priority model — internally a 31-bit bitmask, each bit a lane. Discrete input lands in a synchronous lane and renders immediately and blocking; transitions land in a transition lane that any more-urgent lane can interrupt; idle work lands in the lowest lanes. The lane bitmask lets React batch compatible updates and pick which set to work on next, which is how it decides what to render now versus later."
    />
    <NumberDrill
      id="mid-drill-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="React's scheduler does about 5 ms of render work per slice. The frame budget at 60 fps is 16.67 ms. Roughly how many React render slices can fit in one frame alongside ~6 ms of browser overhead?"
      expected={2}
      tolerance={1}
      unit="slices"
      explanation="16.67 ms minus ~6 ms browser overhead leaves ~10 ms for everything else. At ~5 ms per slice that is about 2 slices per frame. Time-slicing means a 50 ms render is spread across roughly 5 frames — each frame still paints, input still flows — instead of one 50 ms long task that drops 3 frames and spikes INP."
    />
  </Fragment>
  <Fragment slot="senior">
    <h4 class="mt-2 mb-3 text-[15px] font-semibold text-ink tracking-tight">Failure mode: index keys and state corruption</h4>
    <p><strong>The mechanism in full.</strong> Reconciliation of a list builds a map from `key` to old fiber, then walks the new children matching each to its old fiber by key. The matched fiber carries forward everything stateful: `useState` values, `useRef` contents, uncontrolled DOM state (an input's typed text, a checkbox's checked-ness, scroll position, focus), and the actual DOM node. When keys are array indices, the key is bound to position, not identity. Insert at the front, delete from the middle, sort, or reverse, and the index-to-item mapping shifts while the index-to-fiber mapping does not. React faithfully does what you told it: it keeps fiber-at-key-2's state and pairs it with whatever item is now at index 2. Controlled props update correctly (they are passed fresh each render); uncontrolled state does not (it lives on the fiber). The result is a class of bug that passes every test using static fixtures and only appears when a real user reorders, deletes, or inserts. The fix is always a key derived from the item's stable identity — never the index, never `Math.random()` (which forces a remount every render and destroys state plus performance).</p>

    <h4 class="mt-8 mb-3 text-[15px] font-semibold text-ink tracking-tight">The double-buffer and bailout in depth</h4>
    <p><strong>Bailout: how React skips work.</strong> Not every component in a re-rendering subtree actually re-runs. When React reaches a fiber during render, it checks: did props change (by `Object.is` on each prop)? did state change? is there a pending context update this fiber consumes? If none of these is true, React <em>bails out</em> — it clones the fiber from the alternate, reuses the entire existing child subtree by reference, and does not call the component function at all. This is why referential stability matters so much: a child wrapped in `React.memo` bails out only if its props are referentially equal. Pass it an inline `{}` or an arrow function as a prop and the bailout fails every render, because a fresh literal is never `Object.is`-equal to the last one. `useMemo` and `useCallback` exist to preserve those references so the bailout can fire. The mental model: re-rendering a parent does not automatically re-render children — it offers them the chance to bail out, and stable references are what let them take it.</p>

    <p><strong>State is stored on the fiber, not the component.</strong> A function component has no instance — it is just a function React calls. So where does `useState` keep its value across renders? On the fiber. Each fiber holds a linked list of hook records, one per `useState`/`useReducer`/`useEffect` call, in call order. React advances a cursor through that list as your component calls hooks. This is the deep reason for the Rules of Hooks: hooks must be called unconditionally and in the same order every render, because React identifies each hook purely by its <em>position</em> in the call sequence, not by any name. Put a `useState` behind an `if` and on the render where the condition flips, every subsequent hook reads the wrong record. It also explains state preservation: state survives a re-render because the fiber survives; state is destroyed when the fiber is destroyed — which happens on unmount, on a type change, or on a key change.</p>

    <h4 class="mt-8 mb-3 text-[15px] font-semibold text-ink tracking-tight">Lanes, batching, and interruption</h4>
    <p><strong>The lane bitmask.</strong> A lane is one bit in a 31-bit integer. Representing priority as a bitmask lets React do set operations in single CPU instructions: "are there any high-priority lanes pending?" is a mask-and-test; "render all lanes at or above this priority" is a bitmask comparison. When updates arrive, each is assigned to a lane based on its trigger — a click handler's `setState` gets a sync lane, a `startTransition` update gets a transition lane, a `useDeferredValue` recomputation gets a transition lane too. React picks the highest-priority non-empty lane set, renders it, and commits. Updates in compatible lanes are <em>batched</em> — multiple `setState` calls in one event handler (or, since React 18, across promises and timeouts via automatic batching) collapse into a single render. This is why you cannot read the new state synchronously right after `setState`: the render that applies it has not happened yet.</p>

    <p><strong>Interruption and tearing.</strong> When a high-priority update arrives mid-transition-render, React discards the half-built workInProgress tree and starts over at the higher priority. Discarding is cheap precisely because of double buffering — the workInProgress tree never committed, so nothing visible reverts. But interruptible rendering introduces a hazard called <em>tearing</em>: if an external store changes between two time-slices, the first slice's components saw the old value and the later slice's components see the new one, so the committed UI is internally inconsistent — "torn". This is exactly why `useSyncExternalStore` exists: it gives React a way to detect that an external store changed during a concurrent render and to restart the render, guaranteeing every component in one commit observed the same store value. Any state library integrating with React 18 (Redux, Zustand, Jotai) routes through `useSyncExternalStore` for this reason.</p>

    <h4 class="mt-8 mb-3 text-[15px] font-semibold text-ink tracking-tight">Effects and their timing</h4>
    <p><strong>useLayoutEffect vs useEffect.</strong> The two differ in <em>when</em> they run relative to paint. `useLayoutEffect` fires synchronously in the commit phase's layout step — after DOM mutation, before the browser paints. `useEffect` (a "passive effect") fires asynchronously <em>after</em> paint, on a later task. The rule: anything that must run before the user sees the frame — measuring a just-mutated DOM node and adjusting layout to prevent a visible flash — belongs in `useLayoutEffect`. Everything else — data fetching, subscriptions, logging — belongs in `useEffect`, because doing it in `useLayoutEffect` delays paint. Overusing `useLayoutEffect` is a real INP regression: every layout effect is synchronous commit-phase work that pushes the paint later. The other commit-phase trap is the forced reflow from the render pipeline: a `useLayoutEffect` that reads `getBoundingClientRect` then writes a style triggers a synchronous layout inside commit.</p>

    <h4 class="mt-8 mb-3 text-[15px] font-semibold text-ink tracking-tight">React Compiler and the future of memoisation</h4>
    <p><strong>Why manual memoisation may be ending.</strong> For a decade, keeping the bailout firing meant scattering `useMemo`, `useCallback`, and `React.memo` by hand — tedious, error-prone, and itself not free (each memo cell costs memory and a comparison). The React Compiler (formerly "React Forget", stable-track as of React 19) is a build-time tool that analyses component code and inserts the memoisation automatically and correctly, at finer granularity than a human would. It understands which values depend on which inputs and memoises exactly those, so the bailout fires without any hand-written hooks. The senior takeaway: the <em>mechanism</em> — referential stability gating the bailout — is not going away; it is the foundation the compiler builds on. Understanding why a stable reference lets a fiber skip work is what lets you read the compiler's output, debug it when it bails out conservatively, and reason about performance whether the memoisation is hand-written or compiler-generated.</p>

    <h4 class="mt-8 mb-3 text-[15px] font-semibold text-ink tracking-tight">Production observability</h4>
    <p><strong>Reading the React Profiler.</strong> The React DevTools Profiler records a commit-by-commit timeline. For each commit it shows which fibers rendered, why each rendered ("props changed", "hook changed", "parent rendered", "context changed"), and how long each took. The senior workflow: record an interaction, find the commit that is too long, sort fibers by self-time, and read the "why did this render" reason. "Parent rendered" on a component whose props did not change means a missing bailout — a memo opportunity or an unstable prop. A flame of components all citing "context changed" means an unstable context value. Pair this with production INP telemetry and <Term k="loaf" lang="en">Long Animation Frames</Term>: when a LoAF entry attributes a slow frame to a React bundle, the Profiler reproduces it locally and names the exact fibers. The two together close the loop from "users report lag" to "this component re-renders 200× because of an inline style object on line 44."</p>

    <DebugLog
      id="sr-debug-1"
      pieceSlug="05-react-fiber"
      lang="en"
      label="React DevTools Profiler — commit detail"
      output={`Commit #47  —  render: 38.2 ms  (1 of 1 committed)

  <App>                rendered  — 0.4 ms  — "hook changed"
    <Header>           rendered  — 0.2 ms  — "parent rendered"
    <Sidebar>          rendered  — 0.3 ms  — "parent rendered"
    <ProductGrid>      rendered  — 36.9 ms — "parent rendered"
      <ProductCard> x240  rendered  — 0.15 ms each — "parent rendered"

  Why did <App> render?  -> useState (searchTerm) changed
  Note: <Header>, <Sidebar>, <ProductGrid> props unchanged`}
      outputLang="log"
      question="Typing in the search box triggers a 38 ms commit. Header, Sidebar, and all 240 ProductCards re-render with reason 'parent rendered', and the Profiler says their props did not change. What is the fix?"
      hint="The reason is 'parent rendered', not 'props changed'. What stops a child from re-rendering when its parent does but its props are equal?"
      answer="Every child re-renders because <App> re-rendered and nothing lets them bail out. 'Parent rendered' plus 'props unchanged' is the exact signature of a missing bailout. The fix is layered. First, wrap Header, Sidebar, and ProductCard in React.memo so they bail out when their props are referentially equal — that immediately spares Header and Sidebar, whose props genuinely never change. Second, make ProductCard's props referentially stable: if App passes inline objects or arrow functions (onClick={() => ...}, style={{...}}), memoise them with useCallback/useMemo, or the React.memo on ProductCard will never fire because a fresh literal is never Object.is-equal. Third — the structural fix — move searchTerm state down: if only the search box and the grid need it, lifting it out of <App> stops the entire tree re-rendering on every keystroke. Fourth, for 240 cards, virtualize so only the visible ~20 mount. And wrap the grid update in startTransition so even a heavy render yields to keystrokes. Verify in the Profiler: after the fix the commit should show only ProductGrid and the changed cards rendering, well under 16 ms."
    />
    <TradeoffMatrix
      id="card-fiber-cost"
      pieceSlug="05-react-fiber"
      lang="en"
      prompt="A search box filters a 5 000-row table. Typing is janky. Pick the primary fix."
      options={[
        { name: "Virtualize the table + wrap the filtered update in startTransition", summary: "Caps mounted nodes; yields to keystrokes.", correct: true, justification: "Virtualization makes commit cheap — only ~20 visible rows mount regardless of dataset size, so the uninterruptible commit phase stays tiny. startTransition puts the filtered re-render in the transition lane, so it time-slices and any keystroke interrupts it. Together they fix both halves: small commit, interruptible render. This is the canonical fix." },
        { name: "Wrap every row component in React.memo", summary: "Helps, but does not cap commit cost.", justification: "React.memo lets unchanged rows bail out of the render phase — useful — but all 5 000 rows are still mounted, so the commit phase and the DOM itself stay huge. Memo without virtualization treats a symptom; the 5 000-node tree is the disease." },
        { name: "Debounce the search input by 300 ms", summary: "Reduces frequency, not cost.", justification: "Debounce makes the expensive render happen less often, which masks the jank, but each render is still a 5 000-row blocking update. When it does fire it still drops frames. Acceptable as a complement, never as the primary fix." },
        { name: "Move the table render into a web worker", summary: "Impossible — workers have no DOM.", justification: "A worker cannot render React DOM. It could pre-filter the 5 000-row dataset off-thread and hand back a small result, which helps if the filter computation is the bottleneck — but the rendering itself must stay on the main thread." },
      ]}
    />
    <RFCQuiz
      id="sr-rfc-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Which React API exists specifically to prevent 'tearing' — components in one commit observing different values of an external store during a concurrent render?"
      choices={[
        { rfc: "useSyncExternalStore", title: "Lets React detect an external store change mid-render and restart, so one commit is consistent", correct: true },
        { rfc: "useState", title: "Manages React-internal state; tearing is about external stores" },
        { rfc: "useMemo", title: "Caches a computed value; unrelated to external-store consistency" },
        { rfc: "useEffect", title: "Runs side effects after commit; does not guarantee mid-render store consistency" },
      ]}
    />
    <DesignPrompt
      id="sr-design-1"
      pieceSlug="05-react-fiber"
      lang="en"
      prompt="Design the rendering strategy for a real-time trading dashboard: a 2 000-row price grid updating ~10× per second, plus interactive filters and a detail panel. It must keep INP under 200 ms and never drop the input box's responsiveness."
      constraints={[
        "Price ticks arrive ~10 times per second from a WebSocket.",
        "Only visible rows need to be correct; the grid scrolls.",
        "Filter and sort controls must feel instant.",
        "The commit phase must stay short — no long uninterruptible blocks.",
        "External price store must not tear during concurrent renders.",
        "INP target: under 200 ms p75.",
      ]}
      canonical="Virtualize the grid so only the ~30 visible rows plus overscan are ever mounted — this caps the commit phase regardless of the 2 000-row dataset. Subscribe to the WebSocket price store through useSyncExternalStore so concurrent renders cannot tear: every component in a commit sees one consistent snapshot. Batch incoming ticks: instead of a setState per tick (10/s × many symbols), accumulate ticks and flush once per animation frame via a single store update, collapsing the work into one render per frame. Route the price-grid update through startTransition (or rely on useDeferredValue for the grid's price values) so it renders at transition priority — interruptible, time-sliced, never blocking the filter input. Keep filter/sort state local and synchronous so those controls render in the sync lane and feel instant. Wrap each row in React.memo and keep its props referentially stable (memoised handlers, no inline objects) so unchanged rows bail out. Key every row by its stable symbol id, never the index, so reorders from sorting preserve per-row state correctly. Verify: in the React Profiler, a tick flush should re-render only the visible rows whose price changed, commit under 8 ms; in DevTools Performance, no long tasks during a 30 s session of live ticks plus continuous typing in the filter."
      keyPoints={[
        "Virtualize to cap the commit phase regardless of dataset size.",
        "useSyncExternalStore for the price store — prevents tearing under concurrent rendering.",
        "Batch WebSocket ticks to one store flush per frame, not one setState per tick.",
        "Price grid renders at transition priority; filter input stays in the sync lane.",
        "Stable symbol-id keys + React.memo + stable props so unchanged rows bail out.",
      ]}
    />
    <Quiz
      id="sr-quiz-1"
      pieceSlug="05-react-fiber"
      lang="en"
      question="A component wrapped in `React.memo` still re-renders every time its parent does. Its props look unchanged. What is the most likely cause?"
      choices={[
        { label: "A prop is an inline object, array, or arrow function — a fresh reference each render, so the memo's Object.is comparison always reports a change", correct: true },
        { label: "React.memo only works on class components", misconception: "React.memo wraps function components specifically. It works fine here — it is being defeated by an unstable prop reference." },
        { label: "React.memo is disabled in development", misconception: "React.memo is active in development. The re-render is real and caused by referential inequality of a prop." },
        { label: "The component uses hooks, which disables memo", misconception: "Hooks do not disable memo. memo gates the render-phase entry; hooks run normally once a render proceeds." },
      ]}
    />
    <Quiz
      id="sr-quiz-2"
      pieceSlug="05-react-fiber"
      lang="en"
      question="Why can you not read the updated state value synchronously on the line right after calling `setState`?"
      choices={[
        { label: "setState schedules an update in a lane; the render that produces the new state has not run yet — the current function still closes over the old value", correct: true },
        { label: "setState is asynchronous because it makes a network request", misconception: "setState makes no network request. It schedules a render; the delay is the scheduled render, not I/O." },
        { label: "React intentionally hides the new value for security", misconception: "There is no security mechanism. The new value simply does not exist yet — the render that computes it is scheduled, not done." },
        { label: "The new value is on a different thread", misconception: "React runs on the single main thread. The value is not elsewhere — it has not been computed yet." },
      ]}
    />
    <FadedExample
      client:visible
      id="sr-faded-1"
      pieceSlug="05-react-fiber"
      lang="en"
      title="Keep an expensive filtered list from blocking the input"
      steps={{
        solved: (
          <pre><code>{`function Search({ allItems }) {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();
  const [results, setResults] = useState(allItems);

  function onChange(e) {
    setQuery(e.target.value);              // urgent: input stays instant
    startTransition(() => {                // low priority, interruptible
      setResults(filter(allItems, e.target.value));
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      {isPending && <Spinner />}
      <List items={results} />
    </>
  );
}`}</code></pre>
        ),
        semi: {
          prompt: (
            <p>Fill in the two missing pieces: the hook that creates the transition, and the wrapper that marks the expensive update as low-priority.</p>
          ),
          blanks: [
            { id: "hook", expected: /useTransition/i, placeholder: "hook for the transition" },
            { id: "wrap", expected: /startTransition/i, placeholder: "wrapper for the expensive update" },
          ],
        },
        blank: {
          prompt: (
            <p>An input filters a large list. Every keystroke runs an expensive <code>filter</code> and the input lags behind the user's typing. Rewrite the change handler so the input stays instant while the list update can be interrupted by the next keystroke. What is the structural change?</p>
          ),
          reveal: (
            <p>Split the one state update into two priorities. The <code>query</code> update that drives the input stays a plain <code>setState</code> — synchronous lane, so the input reflects each keystroke immediately. The expensive <code>setResults(filter(...))</code> goes inside <code>startTransition</code> — transition lane, interruptible and time-sliced. Now when the next keystroke arrives mid-filter, React abandons the in-progress list render, applies the keystroke at high priority, and restarts the filter. The input never lags; the list catches up when there is spare time. <code>isPending</code> drives a spinner so the lag is visible feedback, not a freeze.</p>
          ),
        },
      }}
    />
  </Fragment>
</TierAccordion>

<Misconception id="mc-key-index">
**Myth:** `key={index}` is fine — keys just need to be unique.

**Reality:** Keys identify items *across renders*. An index is a position, not an identity. When the list reorders, inserts, or deletes, every index shifts — React keeps each fiber's state but pairs it with the wrong item. Use a stable id.
</Misconception>

<NumbersCard
  id="card-fiber-numbers"
  title="React reconciler numbers"
  stats={[
    { label: "Time-slice before shouldYield()", value: "~5 ms" },
    { label: "Reconciliation complexity", value: "O(n) (heuristic)" },
    { label: "Naive tree-diff complexity", value: "O(n³)" },
    { label: "Lane bitmask width", value: "31 lanes" },
    { label: "Commit phase", value: "Atomic, not sliceable" },
    { label: "Bailout comparison", value: "Object.is per prop" },
  ]}
/>

<KeyTakeaway>Two phases: render builds the workInProgress tree and is interruptible; commit applies DOM mutations and is atomic. Fibers store state, so a key change destroys it — use stable ids, never the index. Lanes prioritise; `startTransition` keeps a heavy render from blocking input. Referential stability is what lets a fiber bail out.</KeyTakeaway>

<p class="text-ink-2 my-6">A fiber is where component state lives between renders — the `useState` values, the refs, the hook list all hang off the fiber, not the component function. Render after render, the fiber persists and carries that state forward; destroy the fiber (unmount, type change, key change) and the state is gone. The reconciler is, at bottom, a machine for deciding which fibers survive and which are rebuilt — the same statefulness question, of what persists across a transition and what is re-derived, that runs through every layer of the stack. <SpiralCue thread="statefulness" lang="en" /></p>

<RetrievalDrawer
  client:load
  pieceSlug="05-react-fiber"
  lang="en"
  questions={[
    {
      id: "sr-r1",
      q: "Explain, at the level of fibers, why `key={index}` corrupts state when a list reorders — and why controlled props look fine while uncontrolled state does not.",
      answer: <p>Reconciliation matches new list children to old fibers by key. With index keys, the key is the position: the fiber at key 2 is whatever currently sits at index 2. When the list reorders or an item is inserted at the front, the data at index 2 changes but React still matches the new index-2 item to the old key-2 fiber. That fiber carries everything stateful — useState values, refs, and crucially uncontrolled DOM state like a checkbox's checked-ness or an input's typed text, plus the actual DOM node. Controlled props (value, checked passed explicitly from state) are re-passed fresh on every render, so they update correctly and look fine. Uncontrolled state lives on the fiber and is not re-passed — so it stays with the wrong item. The result: a checkbox checked on one row appears checked on a different row after a reorder. A stable id key binds the fiber to the item's identity instead of its position, so the fiber moves with its data and state stays correct.</p>,
    },
    {
      id: "sr-r2",
      q: "A component is wrapped in React.memo but re-renders on every parent render. Walk through diagnosing and fixing it.",
      answer: <p>React.memo gates the render phase: the component runs only if its props are not referentially equal to last render, compared with Object.is per prop. If it re-renders anyway, a prop is getting a fresh reference each time. Diagnose in the React DevTools Profiler: the render reason will say "props changed" (not "parent rendered"), and hovering shows which prop. The usual culprits: an inline object (style={{color:'red'}}), an inline array, or an arrow function (onClick={() => f()}) created in the parent's JSX — each is a new reference every render, so Object.is always reports a change and the memo never bails out. The fix is to stabilise those references: wrap callbacks in useCallback, wrap derived objects/arrays in useMemo, or hoist truly constant values out of the component entirely. After fixing, the Profiler should show the component bailing out (greyed, "did not render") when only unrelated state changes. The deeper point: React.memo is only as good as the referential stability of what you feed it.</p>,
    },
    {
      id: "sr-r3",
      q: "What is 'tearing' in concurrent React, why does interruptible rendering make it possible, and how does useSyncExternalStore prevent it?",
      answer: <p>Tearing is a single committed UI showing inconsistent data — some components rendered with an old value, others with a new one. It is possible only because React 18's render phase is interruptible: a render is split into time-slices, and the browser runs other tasks between slices. If an external store (a Redux store, a Zustand store, anything outside React's own state) is mutated by one of those between-slice tasks, components rendered in an early slice already read the old value while components in a later slice read the new one. When that render commits, the UI is internally inconsistent — "torn". React's own state cannot tear because React controls when it changes; external stores can, because React does not. useSyncExternalStore fixes this by giving React a subscribe function and a getSnapshot function: React reads the snapshot, and if it detects the snapshot changed during the concurrent render, it throws away the in-progress render and restarts, so every component in the final commit observed the same store value. This is why every modern state library integrates through useSyncExternalStore rather than a bare useState mirror.</p>,
    },
  ]}
/>

**Prereqs:** [01-event-loop](/en/browser/01-event-loop/) — React's scheduler yields via MessageChannel tasks; [03-v8-internals](/en/browser/03-v8-internals/) — why a megamorphic hot path slows reconciliation.

**Next:** [06-ssr-vs-ssg](/en/browser/06-ssr-vs-ssg/) — where the tree is first rendered and what hydration costs.

**See also:** [02-render-pipeline](/en/browser/02-render-pipeline/) — what the browser does with the DOM mutations commit produces.
