{
  "lessonKey": "react/13-animations-and-interaction/03-view-transitions-and-gestures",
  "track": "react",
  "tasks": [
    {
      "id": "view-transitions-predict-drag-reads",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict the drag handler on a 120 Hz digitizer",
        "ru": "Предскажите drag-обработчик на 120-Гц дигитайзере"
      },
      "prompt": {
        "en": "The drawer drag handler below runs on a mid-range Android phone whose digitizer delivers ~120 pointermove events per second while the display refreshes at 60 Hz. A forced synchronous layout costs ~6 ms on this SoC. Predict the per-frame cost breakdown, the resulting frame rate, and what changes after the standard two fixes (cache measurements on pointerdown; rAF-batched transform writes).",
        "ru": "Drag-обработчик ящика ниже выполняется на среднем Android, чей дигитайзер выдаёт ~120 событий pointermove в секунду при дисплее 60 Гц. Форсированный синхронный layout стоит ~6 мс на этом SoC. Предскажите покадровую раскладку затрат, итоговую частоту кадров и что изменится после двух стандартных лечений (кэш измерений на pointerdown; записи transform батчем через rAF)."
      },
      "scenario": {
        "en": "function onPointerMove(e) {\n  const max = content.offsetHeight;          // read on EVERY move event\n  const y = clamp(e.clientY - grabStart, 0, max);\n  drawer.style.height = `${max - y}px`;      // write a layout property\n}\n// digitizer: ~120 pointermove/s; display: 60 Hz; forced layout: ~6 ms",
        "ru": "function onPointerMove(e) {\n  const max = content.offsetHeight;          // чтение на КАЖДОЕ move-событие\n  const y = clamp(e.clientY - grabStart, 0, max);\n  drawer.style.height = `${max - y}px`;      // запись layout-свойства\n}\n// дигитайзер: ~120 pointermove/с; дисплей: 60 Гц; форсированный layout: ~6 мс"
      },
      "reveal": {
        "en": "Two move events land per 60 Hz frame. Each one writes height (dirtying layout) and the next one reads offsetHeight against that dirty layout — a forced synchronous reflow at ~6 ms. That is ~12 ms of forced layout per frame before the height change itself pays its own style, layout, and paint in the normal pipeline pass — comfortably past the 16.7 ms budget every frame, so the drawer runs near 10–15 fps and visually lags the finger (the Hook's 12 fps was this exact arithmetic). After the fixes: offsetHeight is read once on pointerdown and cached, so move handlers do zero layout reads; the latest clientY is stored and a single rAF callback writes transform: translateY per frame — microseconds of JS, no layout, no paint, only composite. Per-frame cost drops from ~14+ ms to well under 1 ms of main-thread work, and the drawer tracks at a solid 60 fps on the same hardware. The deeper rule: pointermove frequency is input frequency, not frame frequency — any per-event cost is multiplied by 2–4× before it even hits the frame budget.",
        "ru": "На один кадр 60 Гц приходится два move-события. Каждое пишет height (пачкая layout), а следующее читает offsetHeight поверх этого грязного layout — форсированный синхронный reflow по ~6 мс. Это ~12 мс форсированного layout на кадр ещё до того, как само изменение высоты оплатит свои style, layout и paint в обычном проходе конвейера — стабильно мимо бюджета в 16,7 мс каждый кадр, так что ящик идёт около 10–15 fps и видимо отстаёт от пальца (12 fps из Hook — ровно эта арифметика). После лечений: offsetHeight читается один раз на pointerdown и кэшируется — move-обработчики не делают ни одного чтения layout; последний clientY сохраняется, и единственный rAF-колбэк пишет transform: translateY раз на кадр — микросекунды JS, без layout, без paint, только composite. Покадровая цена падает с ~14+ мс до заметно меньше 1 мс работы главного потока, и ящик держит ровные 60 fps на том же железе. Глубокое правило: частота pointermove — это частота ввода, а не кадров: любая цена на событие умножается на 2–4× ещё до встречи с бюджетом кадра."
      }
    },
    {
      "id": "view-transitions-diagnose-snapshot-pop",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the transition that animates nothing",
        "ru": "Диагностируйте переход, который ничего не анимирует"
      },
      "prompt": {
        "en": "A React SPA wires route changes through document.startViewTransition, but users see a brief fade into the same frame, then the new screen pops in. The hero image, which should glide between pages, also never animates separately. Fill in the blanks naming what the browser captured, the React API that fixes the timing, and the CSS property that pairs the hero across states.",
        "ru": "React-SPA проводит смены маршрута через document.startViewTransition, но пользователи видят короткий фейд в тот же кадр, после чего новый экран появляется скачком. Hero-изображение, которое должно перелетать между страницами, тоже никогда не анимируется отдельно. Заполните пропуски: что захватил браузер, какой API React чинит тайминг и какое CSS-свойство сшивает hero между состояниями."
      },
      "evidence": {
        "en": "document.startViewTransition(() => {\n  setRoute(next);            // queued update — DOM unchanged when callback returns\n});\n\nThe callback settles before React commits, so the browser captures the new ___b1___ identical to the old one and animates between two equal images; the real commit lands after the transition, as a pop. Wrapping the state update in ___b2___ forces React to commit inside the callback, before the second capture. The hero glides independently only if the element carries a unique ___b3___ in both the old and new states, pairing its group across the snapshot.",
        "ru": "document.startViewTransition(() => {\n  setRoute(next);            // отложенное обновление — DOM не изменён к возврату колбэка\n});\n\nКолбэк завершается до коммита React, поэтому браузер захватывает новый ___b1___, идентичный старому, и анимирует между двумя равными картинками; настоящий коммит приходит после перехода, скачком. Оборачивание обновления состояния в ___b2___ заставляет React закоммитить внутри колбэка, до второго захвата. Hero перелетает независимо, только если элемент несёт уникальный ___b3___ в старом и новом состояниях, сшивая его группу через снимок."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "snapshot",
              "state snapshot",
              "snapshot (new state)",
              "capture",
              "снимок",
              "снапшот",
              "снимок состояния",
              "захват"
            ],
            "hint": {
              "en": "What startViewTransition captures twice — once before the callback, once after it settles.",
              "ru": "То, что startViewTransition захватывает дважды — до колбэка и после его завершения."
            }
          },
          {
            "id": "b2",
            "accept": [
              "flushSync",
              "flushSync()",
              "flushSync from react-dom",
              "react-dom flushSync",
              "флашсинк"
            ],
            "hint": {
              "en": "The react-dom API that forces a synchronous render and commit.",
              "ru": "API react-dom, форсирующий синхронные рендер и коммит."
            }
          },
          {
            "id": "b3",
            "accept": [
              "view-transition-name",
              "view transition name",
              "viewTransitionName",
              "view-transition-name (CSS)",
              "вью-транзишен-нейм"
            ],
            "hint": {
              "en": "The CSS property that gives an element its own ::view-transition-group — and must be unique per captured state.",
              "ru": "CSS-свойство, дающее элементу собственную ::view-transition-group — и обязанное быть уникальным в захваченном состоянии."
            }
          }
        ]
      }
    },
    {
      "id": "view-transitions-design-bottom-sheet",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design an interruptible bottom sheet with no library",
        "ru": "Спроектируйте прерываемый нижний шит без библиотеки"
      },
      "prompt": {
        "en": "Design a draggable bottom sheet for a mobile web app: drag to any position, release into a spring toward the nearest detent (closed / half / full), grabbable at any moment including mid-spring, scrollable content inside the sheet. Specify the event model, the per-frame write path, the measurement strategy, the release and interruption protocol, and the acceptance numbers on a mid-range Android profile.",
        "ru": "Спроектируйте перетаскиваемый нижний шит для мобильного веб-приложения: перетаскивание в любую позицию, отпускание в пружину к ближайшему детенту (закрыт / половина / полный), захват в любой момент, включая середину пружины, скроллируемый контент внутри шита. Задайте модель событий, путь покадровой записи, стратегию измерений, протокол отпускания и прерывания и приёмочные числа на профиле среднего Android."
      },
      "constraints": {
        "en": "Pointer events only (no touch/mouse forks), no animation or gesture library. The sheet must translate, never resize, because its content includes wrapping text. Inner content must keep native scrolling; the drag must not fight it. prefers-reduced-motion replaces springs with snaps. Target: 60 fps drag and zero dropped frames during interruption on a mid-range Android profile.",
        "ru": "Только события указателя (без веток touch/mouse), без анимационных и жестовых библиотек. Шит обязан транслироваться, а не менять размер: внутри переносимый текст. Внутренний контент сохраняет нативный скролл; перетаскивание не должно с ним воевать. prefers-reduced-motion заменяет пружины снапами. Цель: перетаскивание 60 fps и ноль выброшенных кадров при прерывании на профиле среднего Android."
      },
      "rubric": [
        {
          "en": "Builds the event model on pointer events correctly: pointerdown on the handle with setPointerCapture, touch-action: none scoped to the handle only (the content area keeps touch-action: pan-y so native scroll survives), and a documented rule for when a drag may start from the content area (sheet at full + content scrolled to top + downward pull).",
          "ru": "Корректно строит модель событий на pointer events: pointerdown на ручке с setPointerCapture, touch-action: none только на ручке (зона контента сохраняет touch-action: pan-y, чтобы жил нативный скролл) и задокументированное правило, когда перетаскивание может начаться из зоны контента (шит развёрнут + контент проскроллен к верху + тянут вниз)."
        },
        {
          "en": "Specifies the hot path: move handlers store coordinates only, one rAF callback per frame writes transform: translateY, all measurements (sheet height, detent positions) taken once on pointerdown — and states the arithmetic this avoids (input-frequency events × forced-layout cost).",
          "ru": "Задаёт горячий путь: move-обработчики только сохраняют координаты, один rAF-колбэк на кадр пишет transform: translateY, все измерения (высота шита, позиции детентов) снимаются один раз на pointerdown — и приводит арифметику, которую это избегает (события с частотой ввода × цена форсированного layout)."
        },
        {
          "en": "Designs release and interruption as one mechanism: velocity from the last few move samples picks the detent and feeds the spring; a new pointerdown reads the current transform (DOMMatrixReadOnly from computed style or tracked JS state), cancels the running animation, and continues from the current value — explicitly rejecting wait-for-finished and jump-to-target with the user-facing reason.",
          "ru": "Проектирует отпускание и прерывание как один механизм: скорость по последним move-сэмплам выбирает детент и питает пружину; новый pointerdown читает текущий трансформ (DOMMatrixReadOnly из computed style или ведомое JS-состояние), отменяет идущую анимацию и продолжает от текущего значения — явно отвергая «ждать finished» и «прыгнуть к цели» с пользовательской мотивировкой."
        },
        {
          "en": "Sets measurable acceptance: Performance trace on the throttled profile showing no layout or paint blocks during drag frames (transform-only), a clean Frames track through a grab-mid-spring sequence, and the reduced-motion path verified to change state without playing springs.",
          "ru": "Задаёт измеримую приёмку: трейс Performance на затроттленном профиле без блоков layout и paint в кадрах перетаскивания (только transform), чистый трек Frames на сценарии «захват посреди пружины» и проверенный путь reduced-motion, меняющий состояние без проигрывания пружин."
        }
      ],
      "model": {
        "en": "Events: one pointerdown handler on the grab handle calls setPointerCapture(e.pointerId) and registers move/up on the handle — capture keeps the stream when the thumb outruns the sheet. touch-action: none on the handle only; the content keeps pan-y, and a content-initiated drag is allowed in exactly one state: sheet full, scrollTop === 0, dy > 0 — checked on pointerdown, not per move. Hot path: onPointerMove writes e.clientY into a ref and arms a single rAF that computes y = clamp(grabOffset + latestY, detents.full, detents.closed) and writes transform: translateY(y) — translate only, the sheet never resizes. Measurements (sheet height, detent pixel positions, grabOffset from the current transform) happen once in pointerdown; the move path contains zero DOM reads. Release: pointerup derives velocity from a 3-sample window (getCoalescedEvents to densify), projects y + v·k, picks the nearest detent, and runs a rAF spring toward it, storing the spring handle. Interruption is the same code path as a fresh grab: pointerdown reads the current translateY from the spring state (source of truth in JS; computed-style DOMMatrix as the fallback), cancels the spring, and sets grabOffset from the current value — wait-for-finished ignores a touching finger and reads as broken; jump-to-target teleports the surface under the thumb; both rejected in the design doc with the one-line law: the finger owns the value. Reduced motion: the spring function degenerates to an instant set + state commit. Acceptance on the mid-range profile: drag trace shows transform-only frames (no Layout/Paint blocks after pointerdown), the grab-mid-spring scenario shows zero dropped frames in the Frames track, and the INP during drag stays under 200 ms because move handlers complete in microseconds.",
        "ru": "События: один обработчик pointerdown на ручке вызывает setPointerCapture(e.pointerId) и регистрирует move/up на ручке — захват удерживает поток, когда большой палец обгоняет шит. touch-action: none только на ручке; контент сохраняет pan-y, а перетаскивание из контента разрешено ровно в одном состоянии: шит развёрнут, scrollTop === 0, dy > 0 — проверяется на pointerdown, не на каждом move. Горячий путь: onPointerMove пишет e.clientY в ref и взводит единственный rAF, который считает y = clamp(grabOffset + latestY, detents.full, detents.closed) и пишет transform: translateY(y) — только translate, шит никогда не меняет размер. Измерения (высота шита, пиксельные позиции детентов, grabOffset из текущего трансформа) происходят один раз в pointerdown; move-путь не содержит ни одного чтения DOM. Отпускание: pointerup выводит скорость из окна в 3 сэмпла (getCoalescedEvents для уплотнения), проецирует y + v·k, выбирает ближайший детент и запускает rAF-пружину к нему, сохраняя её хэндл. Прерывание — тот же кодовый путь, что и свежий захват: pointerdown читает текущий translateY из состояния пружины (источник истины в JS; DOMMatrix из computed style как фолбэк), отменяет пружину и выставляет grabOffset от текущего значения — «ждать finished» игнорирует касающийся палец и читается как «сломано»; «прыгнуть к цели» телепортирует поверхность под пальцем; оба варианта отвергнуты в дизайн-доке однострочным законом: значением владеет палец. Reduced motion: функция пружины вырождается в мгновенную установку + коммит состояния. Приёмка на среднем профиле: трейс перетаскивания показывает кадры только с transform (без блоков Layout/Paint после pointerdown), сценарий «захват посреди пружины» — ноль выброшенных кадров в треке Frames, а INP во время перетаскивания остаётся под 200 мс, потому что move-обработчики завершаются за микросекунды."
      }
    }
  ]
}
