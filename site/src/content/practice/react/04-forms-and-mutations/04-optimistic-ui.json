{
  "lessonKey": "react/04-forms-and-mutations/04-optimistic-ui",
  "track": "react",
  "tasks": [
    {
      "id": "optimistic-predict-failed-comment",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict the comment list through a failed mutation",
        "ru": "Предскажите список комментариев при провалившейся мутации"
      },
      "prompt": {
        "en": "comments currently holds 3 items. The user submits a comment, postComment rejects after 1.5 seconds, and the parent never updates comments. Predict what the list renders at t=0 (submit), during the 1.5s, and after the rejection — and name the code that removed the optimistic item.",
        "ru": "В comments сейчас 3 элемента. Пользователь отправляет комментарий, postComment отклоняется через 1,5 секунды, родитель comments не обновляет. Предскажите, что список рендерит в t=0 (сабмит), в течение 1,5 с и после отклонения — и назовите код, который убрал оптимистичный элемент."
      },
      "scenario": {
        "en": "const [optimisticComments, addOptimistic] = useOptimistic(\n  comments, // 3 items, from the parent\n  (current, c) => [...current, { ...c, pending: true }]\n);\n\nasync function action(formData) {\n  addOptimistic({ id: crypto.randomUUID(), text: formData.get(\"text\") });\n  await postComment(formData.get(\"text\")); // rejects after 1.5s\n}",
        "ru": "// postComment отклонится через 1,5 с — что видит пользователь на каждой фазе?\nconst [optimisticComments, addOptimistic] = useOptimistic(\n  comments, // 3 items, from the parent\n  (current, c) => [...current, { ...c, pending: true }]\n);\n\nasync function action(formData) {\n  addOptimistic({ id: crypto.randomUUID(), text: formData.get(\"text\") });\n  await postComment(formData.get(\"text\")); // rejects after 1.5s\n}"
      },
      "reveal": {
        "en": "t=0: addOptimistic runs inside the form action's transition, so the hook renders mergeFn(comments, newComment) — 4 items, the new one dimmed via pending: true. During the 1.5s: still 4 items; the overlay holds exactly as long as the transition is pending. After the rejection: the action settles (failed), the transition ends, the overlay expires — and the hook returns the canonical comments, which never changed: 3 items. The fourth comment fades out. The removal code does not exist: nothing was ever written to comments, so there is nothing to undo — no catch block, no splice, no snapshot restore. That is the structural difference from hand-rolled optimism, where the catch-and-splice is an imperative write that can race a concurrent refetch and delete the wrong thing. One sharp edge: if addOptimistic were called outside any transition, React warns and the optimistic item would snap away almost immediately — the transition is the overlay's lifetime.",
        "ru": "t=0: addOptimistic выполняется внутри transition form action, и хук рендерит mergeFn(comments, newComment) — 4 элемента, новый приглушён через pending: true. В течение 1,5 с: по-прежнему 4 элемента; оверлей держится ровно столько, сколько висит transition. После отклонения: action завершился (провалом), transition закончился, оверлей истёк — и хук возвращает каноничный comments, который не менялся: 3 элемента. Четвёртый комментарий гаснет. Кода удаления не существует: в comments никто ничего не писал, поэтому отменять нечего — ни catch-блока, ни splice, ни восстановления снапшота. В этом структурное отличие от самодельного оптимизма, где catch-и-splice — императивная запись, способная сгоняться с параллельным refetch и удалить не то. Острая кромка: будь addOptimistic вызван вне transition, React предупредил бы, и оптимистичный элемент отщёлкнул бы почти мгновенно — transition и есть время жизни оверлея."
      }
    },
    {
      "id": "optimistic-diagnose-double-like",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the like counter stuck at 2",
        "ru": "Диагностируйте счётчик лайков, застрявший на 2"
      },
      "prompt": {
        "en": "Support escalates: some users' likes count double, and unliking leaves the count at 1. Logs show pairs of identical POST /like requests 3-8 seconds apart from the same user, mostly on mobile networks. Fill in the blanks naming the root cause and the server-side contract that fixes it.",
        "ru": "Эскалация из поддержки: у части пользователей лайки считаются вдвое, а снятие лайка оставляет счётчик на 1. Логи показывают пары одинаковых POST /like с разницей 3–8 секунд от одного пользователя, в основном на мобильных сетях. Заполните пропуски, назвав корневую причину и серверный контракт, который это чинит."
      },
      "evidence": {
        "en": "12:01:04 POST /like {post: 88, user: 7} -> 200 (response lost: radio handoff)\n12:01:09 POST /like {post: 88, user: 7} -> 200\nDB: two like rows for (88, 7); counter = 2\n\nThe first request succeeded but its ___b1___ never reached the client, so the user retried a mutation that had already executed. The server, keeping no memory of processed intents, executed it ___b2___. The fix: the client generates an ___b3___ per gesture and resends the same one on retry; the server stores processed keys and replays the recorded result instead of executing again.",
        "ru": "// Пары одинаковых POST /like с интервалом 3–8 с, в БД две строки лайка\n12:01:04 POST /like {post: 88, user: 7} -> 200 (response lost: radio handoff)\n12:01:09 POST /like {post: 88, user: 7} -> 200\nDB: two like rows for (88, 7); counter = 2\n\nПервый запрос успешен, но его ___b1___ не дошёл до клиента, и пользователь повторил мутацию, которая уже исполнилась. Сервер, не храня памяти об обработанных намерениях, исполнил её ___b2___. Фикс: клиент генерирует ___b3___ на жест и при ретрае шлёт тот же самый; сервер хранит обработанные ключи и переигрывает записанный результат вместо повторного исполнения."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "response",
              "reply",
              "200",
              "answer",
              "ответ",
              "отклик",
              "ответ сервера"
            ],
            "hint": {
              "en": "The request arrived and executed; what got lost on the way back?",
              "ru": "Запрос дошёл и исполнился; что потерялось на обратном пути?"
            }
          },
          {
            "id": "b2",
            "accept": [
              "twice",
              "again",
              "a second time",
              "two times",
              "дважды",
              "повторно",
              "второй раз",
              "ещё раз"
            ],
            "hint": {
              "en": "What does a memoryless server do with a duplicate of an already-processed mutation?",
              "ru": "Что сервер без памяти делает с дубликатом уже обработанной мутации?"
            }
          },
          {
            "id": "b3",
            "accept": [
              "idempotency key",
              "idempotency-key",
              "idempotent key",
              "ключ идемпотентности",
              "идемпотентный ключ",
              "ключ-идемпотентности"
            ],
            "hint": {
              "en": "The unique token, minted per intent (not per request), that payment APIs require for exactly this scenario.",
              "ru": "Уникальный токен, чеканящийся на намерение (не на запрос), который платёжные API требуют ровно для этого сценария."
            }
          }
        ]
      }
    },
    {
      "id": "optimistic-design-reaction-system-policy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design the optimism policy for a collaboration app's mutations",
        "ru": "Спроектируйте политику оптимизма для мутаций приложения совместной работы"
      },
      "prompt": {
        "en": "Your collaboration app ships four mutations: (a) emoji reactions on messages, (b) posting messages into channels with profanity moderation that rejects ~4% of posts in strict workspaces, (c) deleting a channel (irreversible, admin-only), (d) upgrading the workspace to a paid plan (card charge). Design the per-mutation policy: optimistic or pending-honest, the reconciliation rule where optimistic, idempotency strategy, and the user-facing failure behavior. Justify each choice with the wager logic.",
        "ru": "В приложении совместной работы четыре мутации: (a) эмодзи-реакции на сообщения, (b) отправка сообщений в каналы с модерацией мата, отклоняющей ~4% постов в строгих воркспейсах, (c) удаление канала (необратимое, только админ), (d) апгрейд воркспейса на платный план (списание с карты). Спроектируйте политику по каждой мутации: оптимизм или честный pending, правило сверки там, где оптимизм, стратегию идемпотентности и видимое пользователю поведение при провале. Обоснуйте каждый выбор логикой пари."
      },
      "constraints": {
        "en": "All four run through React 19 form actions. Mobile users retry aggressively. The moderation verdict arrives only in the server response. Finance requires that no UI ever shows a successful charge before the processor confirms.",
        "ru": "Все четыре идут через form actions React 19. Мобильные пользователи агрессивно ретраят. Вердикт модерации приходит только в ответе сервера. Финансовый отдел требует, чтобы UI никогда не показывал успешное списание до подтверждения процессинга."
      },
      "rubric": [
        {
          "en": "Reactions: full optimism via useOptimistic (success ~99.9%, rollback is a fade); reconciliation replaces the overlay with server truth; idempotency via a per-gesture key or a server-side unique constraint on (message, user, emoji) so the lost-response retry cannot double-react.",
          "ru": "Реакции: полный оптимизм через useOptimistic (успех ~99,9%, откат — затухание); сверка замещает оверлей истиной сервера; идемпотентность — ключ на жест или серверное уникальное ограничение на (сообщение, пользователь, эмодзи), чтобы ретрай после потерянного ответа не задвоил реакцию."
        },
        {
          "en": "Messages: recognizes 4% rejection as routine, not exceptional — either pending-honest, or optimism with explicit pending styling and a visible non-silent failure state (the message flips to failed-with-retry, never silently vanishes); matches optimistic item to confirmed one by an echoed client key.",
          "ru": "Сообщения: распознаёт 4% отказов как рутину, а не исключение — либо честный pending, либо оптимизм с явной pending-стилизацией и видимым, не тихим провалом (сообщение переходит в failed-with-retry, никогда не исчезает молча); сшивает оптимистичный элемент с подтверждённым по клиентскому ключу-эху."
        },
        {
          "en": "Channel deletion: refuses optimism — irreversible and admin-gated; pending-honest with confirmation, disabled button via isPending/useFormStatus, and UI removal only after the server confirms.",
          "ru": "Удаление канала: отказывается от оптимизма — необратимо и под админ-правами; честный pending с подтверждением, кнопка отключена через isPending/useFormStatus, удаление из UI только после подтверждения сервера."
        },
        {
          "en": "Plan upgrade: refuses optimism on finance's rule — a retracted receipt is a false statement the user may act on; pending-honest with processor confirmation gating the success screen, and an idempotency key on the charge so an aggressive retry can never double-bill.",
          "ru": "Апгрейд плана: отказывается от оптимизма по правилу финансов — отозванный чек есть ложное утверждение, на которое пользователь может опереться; честный pending, экран успеха открывается подтверждением процессинга, и ключ идемпотентности на списании, чтобы агрессивный ретрай не списал дважды."
        },
        {
          "en": "States the general wager rule that generated the four answers: optimism when success is near-certain and retraction is cheap; pending-honest when failure is routine, the act is irreversible, or the confirmation is itself a commitment (money, legal).",
          "ru": "Формулирует общее правило пари, породившее четыре ответа: оптимизм — когда успех почти гарантирован и изъятие дёшево; честный pending — когда провал рутинен, акт необратим или само подтверждение есть обязательство (деньги, юридика)."
        }
      ],
      "model": {
        "en": "Reactions (optimistic): addOptimistic toggles the emoji instantly inside the form action; canonical state updates from the server response; the retry problem is closed server-side with a unique constraint on (message, user, emoji) — semantically idempotent, so even a duplicate POST is a no-op — making a client key optional. Messages (optimistic, honestly): 4% rejection means one in 25 posts in strict workspaces would vanish if failure were silent — so the optimistic message renders dimmed with pending: true, carries a client UUID the server echoes for reconciliation, and on rejection converts to a failed state with the text preserved and a retry affordance, never a silent disappearance. The alternative — pending-honest with a subtle send indicator — is defensible if the team cannot invest in failed-state UX. Channel deletion (pending-honest): type-the-name confirmation, submit disabled via useFormStatus while in flight, channel disappears only on server confirmation; an optimistic vanish would confirm an irreversible act the server might reject (permissions, last-channel rule). Plan upgrade (pending-honest, mandated): the action returns processor-confirmed state only; the success screen renders from that state, never before; the charge carries an idempotency key minted when the user lands on the payment step, so the lost-response retry replays the recorded result instead of double-billing. Rule: optimism is a bet placed only where success is near-certain and the UI can cheaply take its word back.",
        "ru": "Реакции (оптимизм): addOptimistic мгновенно переключает эмодзи внутри form action; каноничное состояние обновляется из ответа сервера; проблема ретрая закрыта на сервере уникальным ограничением на (сообщение, пользователь, эмодзи) — семантически идемпотентно, дубликат POST становится no-op — клиентский ключ опционален. Сообщения (оптимизм, но честный): 4% отказов — это каждый 25-й пост в строгих воркспейсах, который исчез бы при тихом провале; поэтому оптимистичное сообщение рендерится приглушённым с pending: true, несёт клиентский UUID, который сервер возвращает эхом для сверки, и при отклонении переходит в состояние failed с сохранённым текстом и кнопкой ретрая — никогда не исчезает молча. Альтернатива — честный pending с лёгким индикатором отправки — защитима, если команда не готова вложиться в UX failed-состояния. Удаление канала (честный pending): подтверждение вводом имени, сабмит отключён через useFormStatus на время полёта, канал исчезает только по подтверждению сервера; оптимистичное исчезновение подтвердило бы необратимый акт, который сервер может отвергнуть (права, правило последнего канала). Апгрейд плана (честный pending, предписан): action возвращает только состояние, подтверждённое процессингом; экран успеха рендерится из него и никогда раньше; списание несёт ключ идемпотентности, отчеканенный при входе на шаг оплаты, и ретрай после потерянного ответа переигрывает записанный результат вместо двойного списания. Правило: оптимизм — ставка, которую делают только там, где успех почти гарантирован, а UI может дёшево взять слово назад."
      }
    }
  ]
}
