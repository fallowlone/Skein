{
  "lessonKey": "react/07-testing/02-async-tests-and-msw",
  "track": "react",
  "tasks": [
    {
      "id": "async-msw-predict-waitfor-sideeffect",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict how many times the form submits",
        "ru": "Предскажите, сколько раз отправится форма"
      },
      "prompt": {
        "en": "The success toast appears about 180 ms after a submit click. waitFor polls every 50 ms. Predict: how many times does the click fire, what does MSW log, and does the test pass? Then state what the same pattern hides in a component without submit deduplication.",
        "ru": "Тост успеха появляется примерно через 180 мс после клика по отправке. waitFor опрашивает каждые 50 мс. Предскажите: сколько раз выстрелит клик, что залогирует MSW и пройдёт ли тест? Затем скажите, что этот же паттерн прячет в компоненте без дедупликации отправки."
      },
      "scenario": {
        "en": "await waitFor(() => {\n  fireEvent.click(screen.getByRole(\"button\", { name: /pay/i }));\n  expect(screen.getByText(/payment sent/i)).toBeInTheDocument();\n});\n// MSW handler: http.post(\"/api/pay\", ...) — responds in ~180ms\n// waitFor: 50ms polling interval, 1000ms timeout",
        "ru": "// Сколько POST /api/pay увидит MSW к моменту появления тоста?\nawait waitFor(() => {\n  fireEvent.click(screen.getByRole(\"button\", { name: /pay/i }));\n  expect(screen.getByText(/payment sent/i)).toBeInTheDocument();\n});\n// Хендлер MSW: http.post(\"/api/pay\", ...) — отвечает за ~180 мс\n// waitFor: интервал опроса 50 мс, таймаут 1000 мс"
      },
      "reveal": {
        "en": "waitFor re-runs the whole callback every 50 ms until it stops throwing. Each retry fires a fresh click before the assertion throws on the missing toast — so by the time the response lands around 180 ms, the click has fired roughly four times (at 0, 50, 100, 150 ms) and MSW logs about four POST /api/pay requests. The test then passes on the first poll where the toast exists. Green test, four payments. In a component without submit dedup, this pattern actively masks a double-charge bug: the test cannot distinguish one submission from four because it caused all four itself. Correct shape: perform the action exactly once outside waitFor, then await the outcome — await user.click(payButton); expect(await screen.findByText(/payment sent/i)).toBeInTheDocument(). Side effects inside a polling callback are never reliability; they are repeated actions.",
        "ru": "waitFor перезапускает весь колбэк каждые 50 мс, пока тот не перестанет бросать. Каждый ретрай стреляет свежим кликом до того, как проверка бросит исключение на отсутствующем тосте, — и к моменту прихода ответа около 180 мс клик выстрелил примерно четыре раза (на 0, 50, 100, 150 мс), а MSW логирует около четырёх POST /api/pay. Затем тест проходит на первом опросе, где тост существует. Зелёный тест, четыре платежа. В компоненте без дедупликации отправки этот паттерн активно маскирует баг двойного списания: тест не может отличить одну отправку от четырёх, потому что сам вызвал все четыре. Правильная форма: действие ровно один раз снаружи waitFor, затем ожидание результата — await user.click(payButton); expect(await screen.findByText(/payment sent/i)).toBeInTheDocument(). Побочные эффекты внутри опрашивающего колбэка — никогда не надёжность; это повторённые действия."
      }
    },
    {
      "id": "async-msw-diagnose-green-local-red-ci",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the test that only fails on CI",
        "ru": "Диагностируйте тест, падающий только в CI"
      },
      "prompt": {
        "en": "A spec passes on 10-core laptops and fails about 1 run in 4 on a shared 2-vCPU CI runner with: 'Timed out in waitFor after 1000ms'. The grid renders ~90 ms after fetch resolution locally, ~1.3 s under saturated CI CPU. Fill in the blanks naming the mechanism and the correct fix.",
        "ru": "Спек проходит на 10-ядерных ноутбуках и падает примерно в 1 прогоне из 4 на расшаренном CI-раннере с 2 vCPU с ошибкой: «Timed out in waitFor after 1000ms». Грид рендерится ~90 мс после резолва fetch локально и ~1,3 с под насыщенным CPU CI. Заполните пропуски: механизм и корректное лечение."
      },
      "evidence": {
        "en": "FAIL  orders-grid.test.tsx  (CI, 4 Vitest workers, 2 vCPU)\n  ✕ shows fetched orders (1043 ms)\n  Timed out in waitFor after 1000ms.\n\nLocal: render-after-fetch ≈ 90 ms.  CI under load: ≈ 1300 ms.\n\nfindBy retries its query every 50 ms inside a default budget of ___b1___ ms. The CI render legitimately exceeds that budget, so this is a correct test with a wrong budget, not a broken component. The scalable fix is to raise the ___b2___ for the environment (per call or via configure), never to add a fixed ___b3___ that sleeps and slows every run.",
        "ru": "FAIL  orders-grid.test.tsx  (CI, 4 воркера Vitest, 2 vCPU)\n  ✕ показывает загруженные заказы (1043 мс)\n  Timed out in waitFor after 1000ms.\n\nЛокально: рендер после fetch ≈ 90 мс.  CI под нагрузкой: ≈ 1300 мс.\n\nfindBy перезапускает запрос каждые 50 мс внутри дефолтного бюджета ___b1___ мс. Рендер на CI легитимно превышает этот бюджет — это корректный тест с неверным бюджетом, а не сломанный компонент. Масштабируемое лечение — поднять ___b2___ для среды (на вызов или через configure), а не добавлять фиксированный ___b3___, который спит и замедляет каждый прогон."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "1000",
              "1000ms",
              "1 000",
              "1000 мс",
              "тысяча",
              "1s",
              "1 с"
            ],
            "hint": {
              "en": "The default waitFor/findBy budget — one second, expressed in milliseconds.",
              "ru": "Дефолтный бюджет waitFor/findBy — одна секунда, выраженная в миллисекундах."
            }
          },
          {
            "id": "b2",
            "accept": [
              "timeout",
              "the timeout",
              "timeout option",
              "asyncUtilTimeout",
              "таймаут",
              "опцию timeout",
              "бюджет таймаута"
            ],
            "hint": {
              "en": "The named option findBy accepts as its third-argument config, also settable suite-wide.",
              "ru": "Именованная опция, которую findBy принимает третьим аргументом; настраивается и на весь сьют."
            }
          },
          {
            "id": "b3",
            "accept": [
              "sleep",
              "delay",
              "setTimeout",
              "arbitrary sleep",
              "слип",
              "сон",
              "задержку",
              "setTimeout-задержку"
            ],
            "hint": {
              "en": "The anti-pattern: an unconditional pause that waits the full duration on every run, fast or slow.",
              "ru": "Антипаттерн: безусловная пауза, ждущая полную длительность в каждом прогоне, быстром или медленном."
            }
          }
        ]
      }
    },
    {
      "id": "async-msw-design-handler-architecture",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design the MSW layer for a 40-endpoint app",
        "ru": "Спроектируйте слой MSW для приложения с 40 эндпоинтами"
      },
      "prompt": {
        "en": "Your app talks to ~40 REST endpoints and is mid-migration from raw fetch to react-query. Tests today mock fetch ad hoc per file. Design the MSW architecture: handler organization, default versus override policy, error/empty/slow state coverage, unhandled-request policy, and how payloads stay true to the API schema.",
        "ru": "Приложение ходит в ~40 REST-эндпоинтов и находится в середине миграции с голого fetch на react-query. Сегодня тесты мокают fetch ад-хок по файлам. Спроектируйте архитектуру MSW: организацию хендлеров, политику дефолтов и оверрайдов, покрытие состояний ошибок/пустоты/медленности, политику необработанных запросов и поддержание верности нагрузок схеме API."
      },
      "constraints": {
        "en": "Six engineers; the react-query migration lands incrementally over a quarter, so both fetch and react-query code paths must stay tested throughout. Storybook should reuse the same handlers. No request may silently pass through to a real network in tests.",
        "ru": "Шесть инженеров; миграция на react-query приземляется инкрементально в течение квартала, поэтому оба пути — fetch и react-query — должны оставаться покрытыми всё время. Storybook должен переиспользовать те же хендлеры. Ни один запрос в тестах не должен молча уходить в реальную сеть."
      },
      "rubric": [
        {
          "en": "Organizes handlers by API domain (orders.ts, auth.ts, ...) exporting a composed happy-path default set for setupServer; error, empty, and slow responses are expressed only via server.use inside the tests that need them — never committed into defaults.",
          "ru": "Организует хендлеры по доменам API (orders.ts, auth.ts, ...), экспортируя собранный happy-path-набор для setupServer; ответы с ошибками, пустотой и задержкой выражаются только через server.use внутри нуждающихся тестов — и никогда не коммитятся в дефолты."
        },
        {
          "en": "Sets the safety rails: onUnhandledRequest: 'error' in server.listen, server.resetHandlers() in a global afterEach, and a fresh QueryClient per test in the render wrapper so react-query cache cannot leak across tests.",
          "ru": "Ставит предохранители: onUnhandledRequest: 'error' в server.listen, server.resetHandlers() в глобальном afterEach и свежий QueryClient на тест в обёртке рендера, чтобы кеш react-query не тёк между тестами."
        },
        {
          "en": "Exploits the seam for the migration: because handlers describe HTTP traffic, the same tests stay green as each module moves from fetch to react-query — and names this explicitly as the acceptance criterion for the migration PRs.",
          "ru": "Использует шов для миграции: поскольку хендлеры описывают HTTP-трафик, те же тесты остаются зелёными по мере переезда каждого модуля с fetch на react-query — и явно называет это критерием приёмки миграционных PR."
        },
        {
          "en": "Keeps payloads schema-true with shared typed factories (buildOrder(overrides)) used by every handler, plus one mechanism (generated types or contract check) so an API field rename is a single-file change that fails loudly, not a 60-test archaeology dig.",
          "ru": "Держит нагрузки верными схеме общими типизированными фабриками (buildOrder(overrides)) во всех хендлерах плюс один механизм (сгенерированные типы или контрактная проверка), чтобы переименование поля API было правкой одного файла с громким падением, а не археологией по 60 тестам."
        }
      ],
      "model": {
        "en": "Layout: src/mocks/handlers/{orders,auth,billing,...}.ts each exporting happy-path handlers built from typed factories in src/mocks/factories/; src/mocks/handlers/index.ts composes the default array; src/mocks/server.ts wraps setupServer(...defaultHandlers); src/mocks/browser.ts wraps setupWorker with the same array for Storybook and local dev. Test setup file: beforeAll listen({ onUnhandledRequest: 'error' }) — every unmocked request fails loudly instead of hanging into a waitFor timeout; afterEach resetHandlers(); afterAll close(). The render helper builds a fresh QueryClient per test with retries disabled. Deviation policy: error (new HttpResponse(null, { status: 500 })), network failure (HttpResponse.error()), empty list, and delayed responses live in the test via server.use, ideally as named helpers (serverRespondsWith.serverError('/api/orders')) so intent reads in one line. Migration leverage: a module's tests must pass unchanged before and after its fetch-to-react-query PR — the seam makes that the cheapest possible regression gate, and it is written into the PR checklist. Schema fidelity: factories are typed against generated API types (openapi-typescript or equivalent); CI regenerates types weekly so a backend field rename breaks the factory compile, one file, loudly. Storybook imports the same default handlers, so fixtures, tests, and offline dev share one source of truth about what the API does.",
        "ru": "Структура: src/mocks/handlers/{orders,auth,billing,...}.ts — каждый экспортирует happy-path-хендлеры, собранные из типизированных фабрик в src/mocks/factories/; src/mocks/handlers/index.ts композирует дефолтный массив; src/mocks/server.ts оборачивает setupServer(...defaultHandlers); src/mocks/browser.ts оборачивает setupWorker тем же массивом для Storybook и локальной разработки. Файл настройки тестов: beforeAll listen({ onUnhandledRequest: 'error' }) — каждый незамоканный запрос падает громко, а не виснет до таймаута waitFor; afterEach resetHandlers(); afterAll close(). Хелпер рендера строит свежий QueryClient на тест с выключенными ретраями. Политика отклонений: ошибка (new HttpResponse(null, { status: 500 })), сетевой сбой (HttpResponse.error()), пустой список и задержанные ответы живут в тесте через server.use, в идеале именованными хелперами (serverRespondsWith.serverError('/api/orders')), чтобы намерение читалось одной строкой. Рычаг миграции: тесты модуля обязаны проходить без изменений до и после его PR с переездом fetch → react-query — шов делает это самым дешёвым регрессионным гейтом, и это вписано в чек-лист PR. Верность схеме: фабрики типизированы по сгенерированным типам API (openapi-typescript или эквивалент); CI перегенерирует типы еженедельно, и переименование поля на бэкенде ломает компиляцию фабрики — один файл, громко. Storybook импортирует те же дефолтные хендлеры: фикстуры, тесты и офлайн-разработка делят один источник правды о том, что делает API."
      }
    }
  ]
}
