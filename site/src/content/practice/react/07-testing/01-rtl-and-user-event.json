{
  "lessonKey": "react/07-testing/01-rtl-and-user-event",
  "track": "react",
  "tasks": [
    {
      "id": "rtl-predict-fireevent-vs-userevent",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict which input test lies",
        "ru": "Предскажите, какой тест инпута врёт"
      },
      "prompt": {
        "en": "An input has maxLength={5}. Two tests fill it with the 8-character string \"abcdefgh\" — one via fireEvent.change, one via user.type. Predict the value each test observes, which assertion passes, and which test describes something a real user can experience.",
        "ru": "У инпута maxLength={5}. Два теста заполняют его 8-символьной строкой «abcdefgh» — один через fireEvent.change, другой через user.type. Предскажите значение, которое увидит каждый тест, какая проверка пройдёт и какой тест описывает то, что реально может пережить пользователь."
      },
      "scenario": {
        "en": "render(<input maxLength={5} onChange={(e) => setQ(e.target.value)} />);\n\n// Test A\nfireEvent.change(input, { target: { value: \"abcdefgh\" } });\nexpect(input).toHaveValue(\"abcdefgh\");\n\n// Test B\nconst user = userEvent.setup();\nawait user.type(input, \"abcdefgh\");\nexpect(input).toHaveValue(\"abcdefgh\");",
        "ru": "// Какой из двух тестов проверяет достижимое пользователем состояние?\nrender(<input maxLength={5} onChange={(e) => setQ(e.target.value)} />);\n\n// Тест A\nfireEvent.change(input, { target: { value: \"abcdefgh\" } });\nexpect(input).toHaveValue(\"abcdefgh\");\n\n// Тест B\nconst user = userEvent.setup();\nawait user.type(input, \"abcdefgh\");\nexpect(input).toHaveValue(\"abcdefgh\");"
      },
      "reveal": {
        "en": "Test A passes: fireEvent.change assigns the value programmatically, and the value setter is not constrained by maxLength — the input reports \"abcdefgh\", a state no user can produce by typing. Test B fails: user.type fires keydown/keypress/input/keyup per character, and the simulated typing respects maxLength, so the value stops at \"abcde\" and the assertion on \"abcdefgh\" fails. Test B is the truthful one — its failure is the spec: a user gets at most five characters. The general rule: fireEvent.change teleports state past browser constraints (maxLength, readonly, keydown handlers), so a green fireEvent test can certify behavior that does not exist in production. Default to user-event for interactions; keep fireEvent for events a user cannot produce directly.",
        "ru": "Тест A проходит: fireEvent.change присваивает значение программно, а сеттер value не ограничен maxLength — инпут сообщает «abcdefgh», состояние, которое пользователь набором не создаст. Тест B падает: user.type диспатчит keydown/keypress/input/keyup на символ, симулированный набор уважает maxLength, значение останавливается на «abcde», и проверка на «abcdefgh» проваливается. Правдив тест B — его падение и есть спецификация: пользователь получает максимум пять символов. Общее правило: fireEvent.change телепортирует состояние мимо ограничений браузера (maxLength, readonly, обработчики keydown), поэтому зелёный fireEvent-тест может сертифицировать поведение, не существующее в продакшене. Для взаимодействий по умолчанию user-event; fireEvent — для событий, которые пользователь не порождает напрямую."
      }
    },
    {
      "id": "rtl-diagnose-testid-survived-refactor",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the suite that survived a broken button",
        "ru": "Диагностируйте сьют, переживший сломанную кнопку"
      },
      "prompt": {
        "en": "A refactor replaced a button element with a styled clickable div. All 1,400 tests stayed green, but keyboard users can no longer submit and screen readers announce nothing. Fill in the blanks naming what the suite lost and which query would have caught it.",
        "ru": "Рефакторинг заменил элемент button стилизованным кликабельным div. Все 1 400 тестов остались зелёными, но пользователи клавиатуры больше не могут отправить форму, а скринридеры ничего не объявляют. Заполните пропуски: что потерял сьют и какой запрос это поймал бы."
      },
      "evidence": {
        "en": "- <button data-testid=\"save-btn\" onClick={save}>Save</button>\n+ <div data-testid=\"save-btn\" className=\"btn\" onClick={save}>Save</div>\n\nSuite: 1,400 passed, 0 failed.  Production: Tab skips the control, Enter does nothing.\n\nThe div exposes no button ___b1___ in the accessibility tree, and native keyboard activation (Enter/Space) plus focusability were lost with the element. Every test located the node by ___b2___, which matches any markup, and clicked with fireEvent — which dispatches on unfocusable nodes. A query like ___b3___ asserts the element is exposed as a named control and would have failed in the first PR.",
        "ru": "- <button data-testid=\"save-btn\" onClick={save}>Save</button>\n+ <div data-testid=\"save-btn\" className=\"btn\" onClick={save}>Save</div>\n\nСьют: 1 400 прошло, 0 упало.  Продакшен: Tab пропускает контрол, Enter не делает ничего.\n\nDiv не даёт ___b1___ button в дереве доступности, а нативная активация с клавиатуры (Enter/Space) и фокусируемость потерялись вместе с элементом. Каждый тест находил узел по ___b2___, который совпадает при любой разметке, и кликал через fireEvent — а тот диспатчит и на нефокусируемых узлах. Запрос вроде ___b3___ утверждает, что элемент показан как именованный контрол, и упал бы в первом же PR."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "role",
              "a role",
              "button role",
              "the role",
              "роль",
              "роли",
              "роль button"
            ],
            "hint": {
              "en": "The accessibility-tree property that getByRole queries — a div with onClick has none.",
              "ru": "Свойство дерева доступности, которое запрашивает getByRole, — у div с onClick его нет."
            }
          },
          {
            "id": "b2",
            "accept": [
              "testid",
              "data-testid",
              "test id",
              "getByTestId",
              "тестид",
              "тест-айди",
              "атрибуту data-testid"
            ],
            "hint": {
              "en": "The escape-hatch query that asserts only a string match between test and markup.",
              "ru": "Запрос-лазейка, утверждающий лишь совпадение строки между тестом и разметкой."
            }
          },
          {
            "id": "b3",
            "accept": [
              "getByRole",
              "getByRole('button')",
              "getByRole(\"button\", { name: ... })",
              "screen.getByRole",
              "findByRole",
              "запрос по роли"
            ],
            "hint": {
              "en": "The top-priority Testing Library query — it resolves the accessibility tree and the accessible name.",
              "ru": "Запрос высшего приоритета в Testing Library — разрешает дерево доступности и доступное имя."
            }
          }
        ]
      }
    },
    {
      "id": "rtl-design-query-migration-policy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design a testid-to-role migration for 1,400 tests",
        "ru": "Спроектируйте миграцию 1 400 тестов с testid на роли"
      },
      "prompt": {
        "en": "You own a suite where ~90% of queries are getByTestId and interactions are fireEvent. After an accessibility incident, leadership wants contract-grade tests. Design the migration policy: target query rules, interaction rules, enforcement, performance guardrails, and what legitimately stays on testid.",
        "ru": "Вы владеете сьютом, где ~90% запросов — getByTestId, а взаимодействия — fireEvent. После инцидента с доступностью руководство хочет тестов уровня контракта. Спроектируйте политику миграции: правила целевых запросов, правила взаимодействий, принуждение, ограждения производительности и что легитимно остаётся на testid."
      },
      "constraints": {
        "en": "Six engineers shipping features in parallel; no feature freeze. Suite runtime may grow at most 20%. Several third-party widgets render role-less markup you cannot change. The migration must be enforceable in CI, not by reviewer memory.",
        "ru": "Шесть инженеров параллельно везут фичи; фиче-фриза нет. Время сьюта может вырасти максимум на 20%. Несколько сторонних виджетов рендерят разметку без ролей, которую вы не можете менять. Миграция должна принуждаться в CI, а не памятью ревьюеров."
      },
      "rubric": [
        {
          "en": "Defines the target rules precisely: role-with-name first, label for fields, text for static content, testid only behind a documented exception; interactions migrate fireEvent to awaited user-event with setup() per test, keeping fireEvent only for events users cannot produce.",
          "ru": "Точно задаёт целевые правила: сначала role с name, label для полей, text для статики, testid только за задокументированным исключением; взаимодействия мигрируют с fireEvent на await user-event с setup() в каждом тесте, fireEvent остаётся только для событий, которые пользователь не породит."
        },
        {
          "en": "Designs ratchet enforcement rather than a big bang: lint rules (testing-library plugin) at error level for new and touched files, a counted allowlist of legacy violations that may only shrink, and CI failing on any growth of the count.",
          "ru": "Проектирует принуждение-храповик вместо большого взрыва: lint-правила (плагин testing-library) уровня error для новых и затронутых файлов, посчитанный allowlist легаси-нарушений, которому разрешено только уменьшаться, и CI, падающий на любом росте счётчика."
        },
        {
          "en": "Handles performance honestly: role queries cost more on large DOMs, so the policy mandates within() scoping for big tables/lists and budgets the suite (+20% cap) with measurement before/after on the slowest specs.",
          "ru": "Честно обрабатывает производительность: запросы по роли дороже на больших DOM, поэтому политика предписывает скоупинг within() для больших таблиц/списков и бюджетирует сьют (потолок +20%) с замерами до/после на самых медленных спеках."
        },
        {
          "en": "Scopes the testid exception correctly: role-less third-party widgets and genuinely non-semantic regions keep testid with a comment tag, and each exception names what UX property is consequently untested — making residual risk visible instead of implicit.",
          "ru": "Правильно ограничивает исключение для testid: сторонние виджеты без ролей и действительно несемантические области сохраняют testid с комментарием-тегом, и каждое исключение называет, какое свойство UX из-за этого не тестируется, — остаточный риск становится видимым, а не подразумеваемым."
        }
      ],
      "model": {
        "en": "Policy doc, four sections. One — target rules: queries follow the Testing Library priority (role+name, label, text); any new getByTestId requires an inline justification comment naming the role-less region; all interactions use await user.setup()-based APIs, fireEvent allowed only for scroll/media/custom events. Two — ratchet enforcement: enable eslint-plugin-testing-library and eslint-plugin-jest-dom at error for changed files; generate a baseline count of legacy testid queries (script in CI) committed as a number; CI fails if the number rises, so every touched file pays down debt without a freeze. Three — performance guardrails: the migration PR template requires before/after timing on the spec; large-collection queries must scope with within(getByRole(\"table\")) rather than document-wide role scans; suite runtime tracked in CI with a +20% alarm. Estimated reality: role queries add tens of milliseconds on the biggest DOMs, and the 30 slowest specs need scoping to stay inside budget. Four — exceptions: a TESTID-OK(reason) tag for third-party role-less widgets; each tagged site lists the untested UX property (e.g., keyboard activation), and the list is reviewed quarterly — the exceptions are a risk register, not a loophole. Outcome metric: percentage of queries at role/label/text tier, tracked weekly; the incident class (semantic regression with green suite) becomes structurally impossible for migrated files.",
        "ru": "Документ политики, четыре раздела. Один — целевые правила: запросы следуют приоритету Testing Library (role+name, label, text); любой новый getByTestId требует инлайн-комментария с обоснованием и именем области без ролей; все взаимодействия — await user-event через setup(), fireEvent разрешён только для scroll/медиа/кастомных событий. Два — принуждение-храповик: включить eslint-plugin-testing-library и eslint-plugin-jest-dom уровня error для изменённых файлов; сгенерировать базовый счётчик легаси-запросов testid (скрипт в CI), закоммиченный числом; CI падает при росте числа — каждый затронутый файл гасит долг без фриза. Три — ограждения производительности: шаблон миграционного PR требует тайминги до/после на спеке; запросы по большим коллекциям обязаны скоупиться within(getByRole(\"table\")), а не сканировать документ; время сьюта отслеживается в CI с тревогой на +20%. Ожидаемая реальность: запросы по роли добавляют десятки миллисекунд на крупнейших DOM, и тридцати самым медленным спекам нужен скоупинг, чтобы остаться в бюджете. Четыре — исключения: тег TESTID-OK(причина) для сторонних виджетов без ролей; каждое помеченное место перечисляет нетестируемое свойство UX (например, активацию с клавиатуры), список ревьюится ежеквартально — исключения это реестр рисков, а не лазейка. Метрика результата: доля запросов на ярусе role/label/text, еженедельно; класс инцидента (семантическая регрессия при зелёном сьюте) становится структурно невозможным для мигрированных файлов."
      }
    },
    {
      "id": "incident-green-test-broken-modal",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 13,
      "title": {
        "en": "The checkout test that's green while checkout is dead",
        "ru": "Зелёный тест чекаута при мёртвом чекауте"
      },
      "prompt": {
        "en": "Conversion dropped 12% overnight after a deploy. The 'Place order' confirm modal opens, but clicking Confirm does nothing for real users — no order, no error. The checkout spec is green and has been for weeks. Reading it: it does fireEvent.click(getByTestId('confirm-btn')) then immediately expect(getByTestId('order-success')).toBeInTheDocument(), no await. The order POST is async (~120 ms). The button was changed last sprint from <button> to a <div role-less onClick handler. Work it step by step; commit before revealing.",
        "ru": "Конверсия упала на 12% за ночь после деплоя. Модалка подтверждения «Оформить заказ» открывается, но клик по «Подтвердить» у реальных пользователей не делает ничего — ни заказа, ни ошибки. Спек чекаута зелёный и был таким неделями. Читаем: он делает fireEvent.click(getByTestId('confirm-btn')), затем сразу expect(getByTestId('order-success')).toBeInTheDocument(), без await. POST заказа асинхронный (~120 мс). Кнопку в прошлом спринте сменили с <button> на <div без роли с onClick>. Веди по шагам; сформулируй ответ до раскрытия."
      },
      "steps": [
        {
          "label": {
            "en": "1 · How is the test green while the feature is broken?",
            "ru": "1 · Как тест зелёный при сломанной фиче?"
          },
          "prompt": {
            "en": "Real users can't complete checkout, yet the spec passes. Identify what the test is actually verifying versus what it claims to verify.",
            "ru": "Реальные пользователи не могут завершить чекаут, но спек проходит. Определите, что тест реально проверяет против того, что заявляет."
          },
          "reveal": {
            "en": "The test queries by data-testid, which matches any markup that carries the attribute. When the <button> became a role-less <div>, the testid moved with it, so getByTestId still resolves and fireEvent.click still dispatches a synthetic click on the div. But a div with onClick is not keyboard-operable and exposes no button role — exactly what broke for users. The test verifies 'an element with this attribute exists and receives a synthetic click', not 'a button that a user can activate works'. It was always testing markup identity, never the accessible contract, so the semantic regression slid straight through.",
            "ru": "Тест ищет по data-testid, который совпадает с любой разметкой, несущей атрибут. Когда <button> стал <div> без роли, testid переехал вместе с ним, поэтому getByTestId всё ещё находит, а fireEvent.click всё ещё диспатчит синтетический клик на div. Но div с onClick не управляется с клавиатуры и не даёт роли button — ровно то, что сломалось для пользователей. Тест проверяет «элемент с этим атрибутом существует и получает синтетический клик», а не «кнопка, которую пользователь может активировать, работает». Он всегда тестировал идентичность разметки, а не доступный контракт, поэтому семантическая регрессия проскользнула."
          }
        },
        {
          "label": {
            "en": "2 · Why wouldn't a real user even reach the broken click?",
            "ru": "2 · Почему реальный пользователь даже не дойдёт до сломанного клика?"
          },
          "prompt": {
            "en": "Beyond the markup swap, the assertion has a second flaw. What about the async order POST and the missing await?",
            "ru": "Помимо подмены разметки, у проверки есть второй изъян. Что насчёт асинхронного POST заказа и отсутствующего await?"
          },
          "reveal": {
            "en": "Even with a real button, the test would be unsound: it asserts order-success synchronously, immediately after the click, but the order POST takes ~120 ms. The success node does not exist yet at assertion time — so for the test to be green, something else must already be wrong (e.g., the testid is also on a hidden pre-rendered node, or the click path is faked). The correct shape is await user.click(button) then expect(await screen.findByText(/order placed/i))... — user.click would also have failed to activate the role-less div via keyboard semantics, and findBy would await the real async result. The missing await means the test never actually observes the network outcome; it checks the DOM one tick too early.",
            "ru": "Даже с настоящей кнопкой тест был бы несостоятелен: он проверяет order-success синхронно, сразу после клика, но POST заказа занимает ~120 мс. Узла успеха на момент проверки ещё нет — значит, чтобы тест был зелёным, что-то уже не так (например, testid висит и на скрытом предрендеренном узле, или путь клика фейковый). Правильная форма — await user.click(button), затем expect(await screen.findByText(/order placed/i))... — user.click к тому же не активировал бы div без роли по клавиатурной семантике, а findBy дождался бы реального асинхронного результата. Отсутствующий await означает, что тест вообще не наблюдает сетевой исход; он проверяет DOM на тик раньше."
          }
        },
        {
          "label": {
            "en": "3 · What rewrite makes this class of regression impossible?",
            "ru": "3 · Какая переработка делает этот класс регрессии невозможным?"
          },
          "prompt": {
            "en": "Rewrite the test so the markup swap and the async race both fail loudly. State the rule.",
            "ru": "Перепишите тест так, чтобы и подмена разметки, и асинхронная гонка падали громко. Сформулируйте правило."
          },
          "reveal": {
            "en": "Query by role and accessible name: getByRole('button', { name: /confirm/i }) — this fails the instant the button becomes a role-less div, catching the regression in the PR. Interact with awaited user-event: await user.click(confirmBtn), which also respects keyboard/focus semantics. Await the outcome: expect(await screen.findByText(/order placed/i)).toBeInTheDocument(), so the test observes the real async POST. The rule: query by accessibility-tree role+name (what the user perceives), drive with user-event (how the user acts), and await results with findBy (when the user sees them). data-testid is an escape hatch for genuinely non-semantic nodes only; a clickable control is never one of them.",
            "ru": "Запрос по роли и доступному имени: getByRole('button', { name: /confirm/i }) — падает в тот же миг, когда кнопка становится div без роли, ловя регрессию в PR. Взаимодействие через await user-event: await user.click(confirmBtn), который к тому же уважает семантику клавиатуры/фокуса. Ожидание исхода: expect(await screen.findByText(/order placed/i)).toBeInTheDocument(), чтобы тест наблюдал реальный асинхронный POST. Правило: запрашивать по роли+имени из дерева доступности (что пользователь воспринимает), управлять через user-event (как пользователь действует) и ждать результаты через findBy (когда пользователь их видит). data-testid — лазейка только для действительно несемантических узлов; кликабельный контрол никогда не из них."
          }
        }
      ]
    },
    {
      "id": "review-form-test-querying",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Review a login form test",
        "ru": "Ревью теста формы входа"
      },
      "prompt": {
        "en": "This RTL test covers a login form's happy path. Review like a senior: name the real defects, don't flag non-issues.",
        "ru": "Этот RTL-тест покрывает счастливый путь формы входа. Ревью как сеньор: назови реальные дефекты, не цепляйся к не-проблемам."
      },
      "diff": {
        "lang": "tsx",
        "code": "import { render, screen, fireEvent } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\n\ntest(\"logs in with valid credentials\", () => {\n  render(<LoginForm onSubmit={onSubmit} />);\n  const user = userEvent.setup();\n\n  const email = screen.getByTestId(\"email-input\");\n  fireEvent.change(email, { target: { value: \"a@b.com\" } });\n\n  const pwd = screen.getByPlaceholderText(\"Password\");\n  user.type(pwd, \"hunter2\");\n\n  fireEvent.click(screen.getByText(\"Log in\"));\n\n  expect(onSubmit).toHaveBeenCalledWith({\n    email: \"a@b.com\",\n    password: \"hunter2\",\n  });\n});"
      },
      "findings": [
        {
          "id": "unawaited-user-type",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "user.type is not awaited, so the password never gets typed before submit",
            "ru": "user.type не ожидается, поэтому пароль не вводится до отправки"
          },
          "explanation": {
            "en": "userEvent APIs are async. `user.type(pwd, 'hunter2')` returns a promise that the test never awaits, and the synchronous fireEvent.click submits before the typing resolves — onSubmit receives an empty password. The whole test is non-deterministic; it must be an async test that awaits user.type and user.click.",
            "ru": "API userEvent асинхронны. `user.type(pwd, 'hunter2')` возвращает промис, который тест не ожидает, а синхронный fireEvent.click отправляет форму до завершения ввода — onSubmit получает пустой пароль. Весь тест недетерминирован; он должен быть async и ожидать user.type и user.click."
          }
        },
        {
          "id": "testid-for-labeled-field",
          "severity": "tradeoff",
          "planted": true,
          "label": {
            "en": "Email field queried by testid instead of its accessible label",
            "ru": "Поле email ищется по testid вместо доступной метки"
          },
          "explanation": {
            "en": "A login email input has a label; getByTestId('email-input') bypasses the accessibility tree, so the test passes even if the label/role is broken or the input is not associated with its label. Use getByRole('textbox', { name: /email/i }) or getByLabelText('Email') to assert the field is reachable as users and screen readers reach it.",
            "ru": "У поля email формы входа есть метка; getByTestId('email-input') обходит дерево доступности, поэтому тест проходит, даже если метка/роль сломаны или инпут не связан со своей меткой. Используйте getByRole('textbox', { name: /email/i }) или getByLabelText('Email'), чтобы утверждать, что поле достижимо так же, как его достигают пользователи и скринридеры."
          }
        }
      ],
      "decoys": [
        {
          "id": "fireevent-change-on-email",
          "label": {
            "en": "fireEvent.change on the email input instead of user.type",
            "ru": "fireEvent.change на поле email вместо user.type"
          },
          "explanation": {
            "en": "looks wrong, is fine",
            "ru": "выглядит подозрительно, но нормально"
          }
        },
        {
          "id": "getbytext-login-button",
          "label": {
            "en": "Submit button located by getByText('Log in')",
            "ru": "Кнопка отправки найдена через getByText('Log in')"
          },
          "explanation": {
            "en": "looks wrong, is fine",
            "ru": "выглядит подозрительно, но нормально"
          }
        }
      ]
    },
    {
      "id": "rtl-recall-query-priority-ladder",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": {
        "en": "Recall the query-priority ladder and what each rung asserts",
        "ru": "Вспомните лестницу приоритета запросов и что утверждает каждая ступень"
      },
      "prompt": {
        "en": "Fill in the blanks to state the Testing Library query priority and what each query level actually asserts, so you can name the contract strength of any query in review.",
        "ru": "Заполните пропуски: приоритет запросов Testing Library и что реально утверждает каждый уровень, чтобы в ревью назвать силу контракта любого запроса."
      },
      "evidence": {
        "en": "The top-priority query is ___b1___ with its name option: it resolves the computed accessibility tree, so it asserts the element is exposed to assistive tech as the right kind of control with the right name. Next, getByLabelText asserts the ___b2___ binding a screen reader resolves on a form field. getByText asserts visible content. The lowest rung, ___b3___, asserts only that the test string and the markup string agree — it survives any markup change and is reserved for genuinely role-less regions.",
        "ru": "Запрос высшего приоритета — ___b1___ с опцией name: он разрешает вычисленное дерево доступности, поэтому утверждает, что элемент показан вспомогательным технологиям как нужный контрол с нужным именем. Дальше getByLabelText утверждает ___b2___ поля формы, которую разрешает скринридер. getByText утверждает видимый контент. Низшая ступень — ___b3___ — утверждает лишь, что строка теста и строка разметки совпадают; она переживает любую правку разметки и оставлена для действительно несемантических областей."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "getByRole",
              "getByRole + name",
              "getByRole('button', { name })",
              "findByRole",
              "запрос по роли",
              "getByRole с name"
            ],
            "hint": {
              "en": "The query that reads the accessibility tree and the accessible name.",
              "ru": "Запрос, читающий дерево доступности и доступное имя."
            }
          },
          {
            "id": "b2",
            "accept": [
              "label",
              "label binding",
              "label association",
              "the label",
              "метку",
              "связь с меткой",
              "привязку метки"
            ],
            "hint": {
              "en": "The htmlFor / aria-labelledby association on a field that getByLabelText verifies.",
              "ru": "Связь htmlFor / aria-labelledby поля, которую проверяет getByLabelText."
            }
          },
          {
            "id": "b3",
            "accept": [
              "getByTestId",
              "testid",
              "data-testid",
              "тестид",
              "data-testid-запрос",
              "запрос по testid"
            ],
            "hint": {
              "en": "The escape-hatch query — a string match that asserts nothing about semantics.",
              "ru": "Запрос-лазейка — совпадение строки, ничего не утверждающее о семантике."
            }
          }
        ]
      }
    }
  ]
}
