{
  "lessonKey": "react/12-design-systems/03-polymorphic-and-aschild",
  "track": "react",
  "tasks": [
    {
      "id": "polymorphic-predict-handler-chain",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict the click that fires twice",
        "ru": "Предскажите клик, который срабатывает дважды"
      },
      "prompt": {
        "en": "A Button with asChild wraps a router Link. The Button attaches an analytics onClick; the Link attaches its own onClick that calls event.preventDefault() and navigates client-side. Slot merges handlers with the naive composition below — no defaultPrevented check. Predict the firing order, whether the browser navigates, whether analytics records, and what changes if track() also calls a hard window.location redirect.",
        "ru": "Button с asChild оборачивает роутерный Link. Button вешает аналитический onClick; Link вешает собственный onClick, который вызывает event.preventDefault() и навигирует на клиенте. Slot сливает обработчики наивной композицией из кода ниже — без проверки defaultPrevented. Предскажите порядок срабатывания, навигирует ли браузер, запишется ли аналитика и что изменится, если track() вдобавок делает жёсткий редирект через window.location."
      },
      "scenario": {
        "en": "// Slot's naive handler merge:\nmerged.onClick = (event) => {\n  childProps.onClick?.(event);  // the Link's handler\n  slotProps.onClick?.(event);   // the Button's handler\n};\n\n<Button asChild onClick={(e) => track(\"upgrade_click\")}>\n  <Link to=\"/billing\" />\n</Button>\n// Link's internal onClick: e.preventDefault(); router.push(\"/billing\")",
        "ru": "// Наивное слияние обработчиков в Slot:\nmerged.onClick = (event) => {\n  childProps.onClick?.(event);  // обработчик Link\n  slotProps.onClick?.(event);   // обработчик Button\n};\n\n<Button asChild onClick={(e) => track(\"upgrade_click\")}>\n  <Link to=\"/billing\" />\n</Button>\n// Внутренний onClick у Link: e.preventDefault(); router.push(\"/billing\")"
      },
      "reveal": {
        "en": "Order: the Link handler first (child-first is the Slot convention), then the Button handler — the naive merge runs it unconditionally. The browser does not do a full navigation: preventDefault cancels the anchor default, and router.push performs the client-side transition. Analytics records: track() runs after the child handler, because preventDefault flags the default action only — it never stops other listeners or composed handlers. With a hard window.location redirect inside track(), you get the double-navigation bug: router.push starts the SPA transition, then the redirect tears the page down — the classic asChild double-fire. The fix is the convention well-built primitives use: compose with a defaultPrevented check, so the slot-side handler runs only if the child did not call preventDefault.",
        "ru": "Порядок: сначала обработчик Link (ребёнок первым — конвенция Slot), затем обработчик Button — наивное слияние выполняет его безусловно. Полной навигации браузера нет: preventDefault отменяет дефолт анкора, а router.push выполняет клиентский переход. Аналитика записывается: track() выполняется после детского обработчика, потому что preventDefault помечает только дефолтное действие — он никогда не останавливает другие слушатели или скомпонованные обработчики. С жёстким редиректом window.location внутри track() вы получаете баг двойной навигации: router.push начинает SPA-переход, затем редирект сносит страницу — классический двойной выстрел asChild. Лечение — конвенция хорошо построенных примитивов: компоновать с проверкой defaultPrevented, чтобы обработчик стороны слота выполнялся, только если ребёнок не вызвал preventDefault."
      }
    },
    {
      "id": "polymorphic-diagnose-swallowed-merge",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the tooltip that never opens",
        "ru": "Диагностируйте тултип, который никогда не открывается"
      },
      "prompt": {
        "en": "Tooltip.Trigger asChild wraps a custom Chip component, and the tooltip silently never opens. The rendered DOM and the Chip source are below. Fill in the blanks naming the broken contract, the second half of the same contract, and the fastest diagnostic.",
        "ru": "Tooltip.Trigger asChild оборачивает кастомный компонент Chip, и тултип молча никогда не открывается. Отрендеренный DOM и исходник Chip ниже. Заполните пропуски, назвав сломанный контракт, вторую половину того же контракта и быстрейшую диагностику."
      },
      "evidence": {
        "en": "// Rendered DOM — note what is missing:\n<span class=\"chip\">Beta</span>\n// expected: data-state=\"closed\" aria-describedby=... + pointer handlers\n\nfunction Chip({ label }) {\n  return <span className=\"chip\">{label}</span>;\n}\n\nSlot merged its props into the Chip element, but Chip must ___b1___ unknown props onto its DOM node for them to land. The same contract requires Chip to accept and attach a ___b2___ so the primitive can measure and position. The fastest diagnostic for a silent asChild failure is inspecting the DOM for the expected ___b3___ attributes.",
        "ru": "// Отрендеренный DOM — заметьте, чего не хватает:\n<span class=\"chip\">Beta</span>\n// ожидалось: data-state=\"closed\" aria-describedby=... + pointer-обработчики\n\nfunction Chip({ label }) {\n  return <span className=\"chip\">{label}</span>;\n}\n\nSlot слил свои пропсы в элемент Chip, но Chip обязан ___b1___ незнакомые пропсы на свой DOM-узел, чтобы они приземлились. Тот же контракт требует от Chip принять и прикрепить ___b2___, чтобы примитив мог измерять и позиционировать. Быстрейшая диагностика тихого провала asChild — инспекция DOM на ожидаемые атрибуты ___b3___."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "spread",
              "forward",
              "spread/forward",
              "pass through",
              "спредить",
              "распылить",
              "пробросить",
              "прокинуть",
              "передать"
            ],
            "hint": {
              "en": "The {...props} operation — what must Chip do with props it does not recognize?",
              "ru": "Операция {...props} — что Chip обязан сделать с пропсами, которых не знает?"
            }
          },
          {
            "id": "b2",
            "accept": [
              "ref",
              "a ref",
              "forwarded ref",
              "ref (React 19: as a regular prop)",
              "реф",
              "ref-проп",
              "проброшенный ref"
            ],
            "hint": {
              "en": "The handle to the DOM node — a regular prop on function components since React 19.",
              "ru": "Ручка к DOM-узлу — с React 19 обычный проп функциональных компонентов."
            }
          },
          {
            "id": "b3",
            "accept": [
              "data-state",
              "data-* and aria-*",
              "data attributes",
              "aria",
              "data-state/aria-*",
              "data-атрибуты",
              "data-state и aria-*",
              "aria-атрибуты"
            ],
            "hint": {
              "en": "The machine-state markers a primitive stamps onto its trigger element.",
              "ru": "Маркеры состояния машины, которые примитив штампует на свой триггер-элемент."
            }
          }
        ]
      }
    },
    {
      "id": "polymorphic-design-system-policy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design the polymorphism policy for a monorepo design system",
        "ru": "Спроектируйте политику полиморфизма для дизайн-системы монорепы"
      },
      "prompt": {
        "en": "You own the design system in a 600-developer monorepo with an enforced tsc time budget. Components needing polymorphism: style primitives (Box, Text, Stack — tens of thousands of call sites) and behavior wrappers (Button, Tooltip.Trigger, Dialog.Trigger — wrapping router Links and custom components). Design the policy: which mechanism where, the TypeScript cost rationale, the child contract for asChild, and the guard rails that keep both failure modes out of production.",
        "ru": "Вы владеете дизайн-системой в монорепе на 600 разработчиков с контролируемым бюджетом времени tsc. Компоненты, которым нужен полиморфизм: стилевые примитивы (Box, Text, Stack — десятки тысяч колл-сайтов) и поведенческие обёртки (Button, Tooltip.Trigger, Dialog.Trigger — оборачивающие роутерные Link и кастомные компоненты). Спроектируйте политику: какой механизм где, обоснование стоимости TypeScript, контракт ребёнка для asChild и ограждения, не пускающие оба режима отказа в продакшен."
      },
      "constraints": {
        "en": "tsc budget: no single component may add more than seconds to the cold check. Product teams stack behaviors (tooltip on a dialog trigger on a router link). Several teams maintain custom components that do not spread props today. React 19 is already adopted monorepo-wide.",
        "ru": "Бюджет tsc: ни один компонент не должен добавлять к холодной проверке больше считаных секунд. Продуктовые команды складывают поведения (тултип на триггере диалога на роутерном линке). Несколько команд поддерживают кастомные компоненты, которые сегодня не спредят пропсы. React 19 уже принят во всей монорепе."
      },
      "rubric": [
        {
          "en": "Assigns as to style primitives only, with a deliberately narrowed element union (intrinsic tags actually used, not all of ElementType), and justifies it by instantiation cost scaling with call sites — tens of thousands of Box usages make wide conditional types a budget violation.",
          "ru": "Назначает as только стилевым примитивам, с намеренно суженным объединением элементов (реально используемые intrinsic-теги, а не весь ElementType), и обосновывает это ростом стоимости инстанцирования с колл-сайтами — десятки тысяч использований Box делают широкие условные типы нарушением бюджета."
        },
        {
          "en": "Assigns asChild to all behavior wrappers with the stacking argument: multiple asChild triggers compose behavior sets onto one element (tooltip over dialog trigger over link), which as cannot express, and types stay flat on both sides.",
          "ru": "Назначает asChild всем поведенческим обёрткам с аргументом о складывании: несколько asChild-триггеров компонуют наборы поведения на один элемент (тултип поверх триггера диалога поверх линка), чего as не выражает, а типы остаются плоскими с обеих сторон."
        },
        {
          "en": "Writes the asChild child contract explicitly: exactly one element child; the child spreads unknown props onto its DOM node and accepts ref (a regular prop under React 19); handler composition respects defaultPrevented — and plans the remediation path for the teams whose components do not spread props today.",
          "ru": "Явно прописывает контракт ребёнка asChild: ровно один элемент-ребёнок; ребёнок спредит незнакомые пропсы на свой DOM-узел и принимает ref (обычный проп при React 19); композиция обработчиков уважает defaultPrevented — и планирует путь починки для команд, чьи компоненты сегодня не спредят пропсы."
        },
        {
          "en": "Designs guard rails: a lint rule or runtime dev assertion for the single-child rule, a documented DOM-inspection diagnostic (data-state/aria-* present on the child) for silent merge failures, and a CI check or type test pinning the tsc cost of the polymorphic primitives.",
          "ru": "Проектирует ограждения: lint-правило или dev-проверку рантайма для правила одного ребёнка, задокументированную диагностику инспекцией DOM (наличие data-state/aria-* на ребёнке) для тихих провалов слияния и CI-проверку или типовой тест, пиннящий tsc-стоимость полиморфных примитивов."
        }
      ],
      "model": {
        "en": "Policy doc, section 1 — placement: as lives only on Box/Text/Stack/Grid, typed against a curated union (div, span, section, article, label, a, plus the two layout components that need it) instead of ElementType — the conditional type instantiates at every one of tens of thousands of call sites, so union width is the budget lever; behavioral props are forbidden on primitives so no disabled-on-anchor semantics trap exists. Section 2 — asChild on Button and every Trigger: behavior stacking is a product requirement (tooltip over dialog trigger over router link composes three prop sets onto one anchor), as cannot express it, and both sides keep flat types. Section 3 — the child contract, published as one loud paragraph plus examples: exactly one element child (two siblings throw via Children.only at runtime); the child spreads unknown props onto its rendered DOM node; the child accepts ref — under React 19 a regular prop, no forwardRef; primitives compose handlers child-first with a defaultPrevented check so application handlers can veto. Remediation: the non-spreading components get a codemod adding {...rest} and ref pass-through, tracked as a checklist before their teams may adopt asChild wrappers. Section 4 — guard rails: dev-mode assertion message naming the offending component for multiple children; the silent-failure diagnostic documented (inspect DOM for data-state/aria-describedby on the child — absence means the merge was swallowed); a type-level regression test (expect-type) plus a CI timing check on an isolated tsc project containing only the design system, alerting when the polymorphic surface grows the cold check beyond budget.",
        "ru": "Документ политики, раздел 1 — размещение: as живёт только на Box/Text/Stack/Grid, типизирован по курируемому объединению (div, span, section, article, label, a плюс два layout-компонента, которым это нужно) вместо ElementType — условный тип инстанцируется на каждом из десятков тысяч колл-сайтов, поэтому ширина объединения — рычаг бюджета; поведенческие пропсы на примитивах запрещены, так что ловушки семантики disabled-на-анкоре не существует. Раздел 2 — asChild на Button и каждом Trigger: складывание поведений — продуктовое требование (тултип поверх триггера диалога поверх роутерного линка компонует три набора пропсов на один анкор), as этого не выражает, и обе стороны хранят плоские типы. Раздел 3 — контракт ребёнка, опубликованный одним громким абзацем с примерами: ровно один элемент-ребёнок (два сиблинга бросают через Children.only в рантайме); ребёнок спредит незнакомые пропсы на свой отрендеренный DOM-узел; ребёнок принимает ref — при React 19 обычный проп, без forwardRef; примитивы компонуют обработчики ребёнком вперёд с проверкой defaultPrevented, чтобы обработчики приложения могли наложить вето. Починка: неспредящие компоненты получают codemod, добавляющий {...rest} и проброс ref, отслеживаемый чек-листом до того, как их команды смогут принять asChild-обёртки. Раздел 4 — ограждения: dev-сообщение проверки, называющее компонент-нарушитель при нескольких детях; задокументированная диагностика тихого провала (инспекция DOM на data-state/aria-describedby у ребёнка — отсутствие значит, что слияние проглочено); типовой регрессионный тест (expect-type) плюс CI-замер времени на изолированном tsc-проекте только с дизайн-системой, тревожащий, когда полиморфная поверхность выводит холодную проверку за бюджет."
      }
    },
    {
      "id": "incident-aschild-ref-merge-break",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 13,
      "title": {
        "en": "asChild Tooltip stopped positioning after a refactor",
        "ru": "Tooltip с asChild перестал позиционироваться после рефактора"
      },
      "prompt": {
        "en": "A Tooltip uses asChild to merge its trigger props onto a custom Button. After a refactor, tooltips stop appearing in the right place and a warning logs: 'Function components cannot be given refs'. Positioning silently falls back to (0,0). The Button was recently changed from forwardRef to a plain function component. Work it step by step; commit before revealing.",
        "ru": "Tooltip использует asChild, чтобы слить свои триггер-пропы на кастомный Button. После рефактора тултипы перестают появляться в нужном месте, и логируется предупреждение: «Function components cannot be given refs». Позиционирование тихо откатывается к (0,0). Button недавно изменили с forwardRef на обычную функцию-компонент. Веди по шагам; сформулируй ответ до раскрытия."
      },
      "steps": [
        {
          "label": {
            "en": "1 · Why does positioning need a ref?",
            "ru": "1 · Зачем позиционированию ref?"
          },
          "prompt": {
            "en": "The tooltip positions relative to its trigger. How does asChild get the trigger's DOM node, and what broke?",
            "ru": "Тултип позиционируется относительно триггера. Как asChild получает DOM-узел триггера и что сломалось?"
          },
          "reveal": {
            "en": "asChild clones the child and merges props including a ref, so the tooltip can measure the trigger's bounding box. That ref must reach a DOM node. When Button was forwardRef, the ref forwarded to the underlying <button> element. After the refactor to a plain function component, the ref has nowhere to go — React warns and the ref stays null — so getBoundingClientRect runs on null and positioning falls back to origin.",
            "ru": "asChild клонирует ребёнка и сливает пропы, включая ref, чтобы тултип мог измерить bounding box триггера. Этот ref должен дойти до DOM-узла. Когда Button был forwardRef, ref форвардился на нижележащий <button>. После рефактора в обычную функцию-компонент ref некуда деться — React предупреждает, ref остаётся null — поэтому getBoundingClientRect выполняется на null, и позиционирование откатывается к началу координат."
          }
        },
        {
          "label": {
            "en": "2 · The ref-merge requirement",
            "ru": "2 · Требование слияния ref"
          },
          "prompt": {
            "en": "What must any component used as an asChild target guarantee, and how do two refs (library's + consumer's) coexist?",
            "ru": "Что обязан гарантировать любой компонент, используемый как цель asChild, и как уживаются два ref (библиотеки и потребителя)?"
          },
          "reveal": {
            "en": "Any asChild target must forward its ref to a real DOM node (React.forwardRef, or in React 19 a ref prop) — otherwise the parent can't reach the element. When both the library and the consumer attach a ref to the same child, they must be merged (a mergeRefs/composeRefs utility that calls both), because a node can hold only one ref. Slot implementations (Radix) do this merge for you; the contract on the consumer's component is simply: be ref-forwarding.",
            "ru": "Любая цель asChild обязана форвардить ref на реальный DOM-узел (React.forwardRef или в React 19 проп ref) — иначе родитель не дотянется до элемента. Когда и библиотека, и потребитель вешают ref на одного ребёнка, их нужно слить (утилита mergeRefs/composeRefs, вызывающая оба), потому что узел держит только один ref. Реализации Slot (Radix) делают это слияние за вас; контракт на компоненте потребителя прост: будь ref-форвардящим."
          }
        },
        {
          "label": {
            "en": "3 · The guardrail",
            "ru": "3 · Защита"
          },
          "prompt": {
            "en": "How do you prevent this regression class across a design system using polymorphic/asChild widely?",
            "ru": "Как предотвратить этот класс регрессий по дизайн-системе, широко использующей polymorphic/asChild?"
          },
          "reveal": {
            "en": "Make ref-forwarding a hard rule for every interactive primitive (lint rule / type that requires forwardRef), so any component can legally be an asChild target. Type the polymorphic 'as'/asChild API so a non-forwarding component is a type error, not a runtime warning. Add a test that mounts each asChild integration and asserts the merged ref resolves to a non-null DOM node (e.g. the tooltip's measured rect is non-zero). The refactor that dropped forwardRef would then fail CI instead of shipping.",
            "ru": "Сделайте форвардинг ref жёстким правилом для каждого интерактивного примитива (линт-правило / тип, требующий forwardRef), чтобы любой компонент мог легально быть целью asChild. Типизируйте polymorphic 'as'/asChild API так, чтобы не-форвардящий компонент был ошибкой типов, а не рантайм-предупреждением. Добавьте тест, монтирующий каждую asChild-интеграцию и утверждающий, что слитый ref резолвится в не-null DOM-узел (например измеренный rect тултипа ненулевой). Рефактор, убравший forwardRef, тогда упал бы в CI, а не выкатился."
          }
        }
      ]
    },
    {
      "id": "review-aschild-slot-component",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Review an asChild Slot implementation",
        "ru": "Ревью реализации Slot для asChild"
      },
      "prompt": {
        "en": "Here is a Slot that powers asChild. Review like a senior: name the real defects, don't flag non-issues.",
        "ru": "Вот Slot, питающий asChild. Ревьюй как сеньор: назови реальные дефекты, не отмечай не-проблемы."
      },
      "diff": {
        "lang": "tsx",
        "code": "const Slot = forwardRef<HTMLElement, SlotProps>(({ children, ...slotProps }, ref) => {\n  const child = React.Children.only(children) as ReactElement;\n  return React.cloneElement(child, {\n    ...slotProps,\n    ...child.props,\n    ref,\n    onClick: slotProps.onClick,\n  });\n});"
      },
      "findings": [
        {
          "id": "childprops-override-slotprops",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "child.props spread after slotProps wipes injected props",
            "ru": "Спред child.props после slotProps стирает инъецированные пропы"
          },
          "explanation": {
            "en": "Spreading ...child.props after ...slotProps means the child's own props win, so the Slot's injected behavior (aria, handlers) is overwritten by the child. Order should let slotProps apply while merging — not be clobbered.",
            "ru": "Спред ...child.props после ...slotProps означает, что собственные пропы ребёнка побеждают, поэтому инъецированное Slot'ом поведение (aria, обработчики) перезаписывается ребёнком. Порядок должен давать slotProps примениться при слиянии, а не быть затёртым."
          }
        },
        {
          "id": "ref-not-merged",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "ref overwrites the child's own ref instead of merging",
            "ru": "ref перезаписывает собственный ref ребёнка вместо слияния"
          },
          "explanation": {
            "en": "Passing ref directly drops any ref the child already had — the consumer's ref to the same element silently stops working. Merge the Slot's ref with child.props.ref via a composeRefs utility.",
            "ru": "Прямая передача ref выбрасывает любой ref, который уже был у ребёнка — ref потребителя на тот же элемент тихо перестаёт работать. Слейте ref Slot'а с child.props.ref через утилиту composeRefs."
          }
        },
        {
          "id": "onclick-not-composed",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "onClick replaces instead of chaining both handlers",
            "ru": "onClick заменяет вместо вызова обоих обработчиков"
          },
          "explanation": {
            "en": "onClick: slotProps.onClick discards the child's own onClick, so the consumer's click handler never fires. Compose handlers so both the Slot's and the child's onClick run.",
            "ru": "onClick: slotProps.onClick отбрасывает собственный onClick ребёнка, поэтому кликовый обработчик потребителя не срабатывает. Композируйте обработчики, чтобы выполнялись и onClick Slot'а, и ребёнка."
          }
        }
      ],
      "decoys": [
        {
          "id": "children-only-usage",
          "label": {
            "en": "React.Children.only is too strict",
            "ru": "React.Children.only слишком строг"
          },
          "explanation": {
            "en": "Looks wrong, is fine: asChild semantics require exactly one child to clone onto; Children.only enforcing that with a clear error is the intended, correct behavior.",
            "ru": "Выглядит неправильно, но в порядке: семантика asChild требует ровно одного ребёнка для клонирования; Children.only, обеспечивающий это понятной ошибкой, — задуманное корректное поведение."
          }
        }
      ]
    }
  ]
}
