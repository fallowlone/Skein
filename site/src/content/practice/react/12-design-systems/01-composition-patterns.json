{
  "lessonKey": "react/12-design-systems/01-composition-patterns",
  "track": "react",
  "tasks": [
    {
      "id": "composition-predict-controlled-tabs",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict the tab that refuses to switch",
        "ru": "Предскажите вкладку, которая отказывается переключаться"
      },
      "prompt": {
        "en": "Tabs supports the value/defaultValue duality below. A caller renders it with value=\"general\" and no onValueChange. The user clicks the \"billing\" trigger. Predict the initial render, what happens on the click, and the two distinct one-line changes that would each restore switching.",
        "ru": "Tabs поддерживает дуальность value/defaultValue из кода ниже. Вызывающий рендерит его с value=\"general\" и без onValueChange. Пользователь кликает триггер «billing». Предскажите первый рендер, что произойдёт по клику и два разных однострочных изменения, каждое из которых вернёт переключение."
      },
      "scenario": {
        "en": "function Tabs({ value, defaultValue, onValueChange, children }) {\n  const [internal, setInternal] = useState(defaultValue ?? null);\n  const isControlled = value !== undefined;\n  const active = isControlled ? value : internal;\n  const select = (next) => {\n    if (!isControlled) setInternal(next);\n    onValueChange?.(next);\n  };\n  // ... provides { active, select } via context\n}\n\n// Call site:\n<Tabs value=\"general\">\n  <Tabs.Trigger value=\"general\">General</Tabs.Trigger>\n  <Tabs.Trigger value=\"billing\">Billing</Tabs.Trigger>\n</Tabs>",
        "ru": "// Что увидит пользователь после клика по «billing»?\nfunction Tabs({ value, defaultValue, onValueChange, children }) {\n  const [internal, setInternal] = useState(defaultValue ?? null);\n  const isControlled = value !== undefined;\n  const active = isControlled ? value : internal;\n  const select = (next) => {\n    if (!isControlled) setInternal(next);\n    onValueChange?.(next);\n  };\n  // ... отдаёт { active, select } через контекст\n}\n\n// Место вызова:\n<Tabs value=\"general\">\n  <Tabs.Trigger value=\"general\">General</Tabs.Trigger>\n  <Tabs.Trigger value=\"billing\">Billing</Tabs.Trigger>\n</Tabs>"
      },
      "reveal": {
        "en": "Initial render shows \"general\" — value is defined, so the component is controlled and active mirrors it. The click calls select(\"billing\"): isControlled is true, so setInternal is skipped; onValueChange is undefined, so the optional call is a no-op. The parent re-renders nothing, value stays \"general\", and the UI does not move — correctly, by contract. Fix one: pass onValueChange and lift the state into the parent (fully controlled). Fix two: replace value with defaultValue=\"general\" (uncontrolled — the component owns the state again). The invariant: a controlled component must never update its own copy of the state to be helpful; half-controlled components that do both desync from the parent the first time the parent ignores a change.",
        "ru": "Первый рендер показывает «general» — value определён, компонент контролируемый, и active его зеркалит. Клик вызывает select(\"billing\"): isControlled истинен, поэтому setInternal пропущен; onValueChange не передан, и опциональный вызов — no-op. Родитель ничего не перерендеривает, value остаётся «general», UI не двигается — корректно, по контракту. Фикс один: передать onValueChange и поднять состояние в родителя (полностью controlled). Фикс два: заменить value на defaultValue=\"general\" (uncontrolled — компонент снова владеет состоянием). Инвариант: контролируемый компонент никогда не должен «услужливо» обновлять собственную копию состояния; полу-контролируемые компоненты, делающие и то и другое, рассинхронизируются с родителем при первом же проигнорированном изменении."
      }
    },
    {
      "id": "composition-diagnose-boolean-matrix",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the Button nobody can test",
        "ru": "Диагностируйте кнопку, которую никто не может протестировать"
      },
      "prompt": {
        "en": "A production incident: the checkout Button renders a spinner on top of its icon. The component has 14 props, 9 of them boolean. Fill in the blanks naming the scale of the problem, the API smell, and the structural fix.",
        "ru": "Инцидент в проде: кнопка чекаута рисует спиннер поверх своей иконки. У компонента 14 пропсов, 9 из них булевы. Заполните пропуски, назвав масштаб проблемы, запах API и структурное лечение."
      },
      "evidence": {
        "en": "Button.tsx props: variant, size, loading, loadingText, leftIcon, rightIcon,\n  iconOnly, fullWidth, quiet, destructive, uppercase, asLink, href, tooltip\nBug #4821: loading + iconOnly → spinner rendered over the icon\n\n9 boolean props give ___b1___ possible render states; the snapshot suite covers 14.\nHalf of these props describe ___b2___ rather than behavior — that is the smell.\nThe structural fix is to move that variation into ___b3___ at the call site.",
        "ru": "Пропсы Button.tsx: variant, size, loading, loadingText, leftIcon, rightIcon,\n  iconOnly, fullWidth, quiet, destructive, uppercase, asLink, href, tooltip\nБаг #4821: loading + iconOnly → спиннер отрисован поверх иконки\n\n9 булевых пропсов дают ___b1___ возможных состояний рендера; снапшот-сьют покрывает 14.\nПоловина этих пропсов описывает ___b2___, а не поведение — это и есть запах.\nСтруктурное лечение — увести эту вариативность в ___b3___ на месте вызова."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "512",
              "2^9",
              "2 ^ 9",
              "2^9 = 512",
              "five hundred twelve",
              "2 в 9 степени",
              "2 в девятой степени",
              "пятьсот двенадцать"
            ],
            "hint": {
              "en": "Each boolean doubles the state space — two to the power of nine.",
              "ru": "Каждый булев удваивает пространство состояний — два в девятой степени."
            }
          },
          {
            "id": "b2",
            "accept": [
              "contents",
              "content",
              "structure",
              "markup",
              "содержимое",
              "контент",
              "структуру",
              "разметку"
            ],
            "hint": {
              "en": "leftIcon, loadingText, tooltip — what do these props inject that the caller should own?",
              "ru": "leftIcon, loadingText, tooltip — что эти пропсы инжектят, чем должен владеть вызывающий?"
            }
          },
          {
            "id": "b3",
            "accept": [
              "children",
              "composition",
              "slots",
              "children / slots",
              "JSX",
              "children или слоты",
              "композицию",
              "слоты"
            ],
            "hint": {
              "en": "The open end of the configuration–composition spectrum: what JSX channel receives caller-owned content?",
              "ru": "Открытый конец спектра «конфигурация — композиция»: какой JSX-канал принимает контент вызывающего?"
            }
          }
        ]
      }
    },
    {
      "id": "composition-design-menu-family",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design a Menu compound family for twelve teams",
        "ru": "Спроектируйте compound-семейство Menu для двенадцати команд"
      },
      "prompt": {
        "en": "Your design system needs a Menu used by 12 product teams: trigger, list, items with icons and keyboard shortcuts, separators, a danger variant, and submenus later. Design the component API: the part structure, how parts share state, controlled versus uncontrolled open state, what stays a prop versus what becomes composition, and how misuse fails in development.",
        "ru": "Вашей дизайн-системе нужен Menu для 12 продуктовых команд: триггер, список, элементы с иконками и шорткатами, разделители, danger-вариант и позже подменю. Спроектируйте API компонента: структуру частей, как части делят состояние, controlled против uncontrolled для открытости, что остаётся пропсом, а что становится композицией, и как неправильное использование падает в development."
      },
      "constraints": {
        "en": "Teams must not need design-system releases for content-level needs (new icons, badges, custom item layouts). The open state must be controllable for an onboarding tour. No Children.map / cloneElement wiring — one team already ships items wrapped in feature-flag components. Keyboard navigation will come from a headless primitive later; the API must not block that.",
        "ru": "Команды не должны ждать релизов дизайн-системы для контентных нужд (новые иконки, бейджи, кастомные раскладки элементов). Состояние открытости должно быть контролируемым — для онбординг-тура. Никакой связки Children.map / cloneElement — одна команда уже оборачивает элементы в компоненты feature-флагов. Клавиатурная навигация позже приедет из headless-примитива; API не должен этому мешать."
      },
      "rubric": [
        {
          "en": "Defines a compound family (Menu, Menu.Trigger, Menu.List, Menu.Item, Menu.Separator) wired by a context with a null default and a guard hook that throws a named error when a part renders outside Menu — and justifies context over children inspection with the feature-flag-wrapper constraint.",
          "ru": "Определяет compound-семейство (Menu, Menu.Trigger, Menu.List, Menu.Item, Menu.Separator), связанное контекстом с дефолтом null и хуком-стражем, бросающим именованную ошибку при рендере части вне Menu, — и обосновывает контекст против инспекции children ограничением про обёртки feature-флагов."
        },
        {
          "en": "Designs the open-state duality precisely: open + onOpenChange for controlled use (the tour), defaultOpen for uncontrolled, controlled-ness fixed at mount by open !== undefined, and internal state never written while controlled.",
          "ru": "Точно проектирует дуальность открытости: open + onOpenChange для controlled-сценария (тур), defaultOpen для uncontrolled, контролируемость фиксируется на маунте через open !== undefined, и внутреннее состояние никогда не пишется в controlled-режиме."
        },
        {
          "en": "Draws the prop/composition line with criteria: closed enumerable sets stay props (tone: default | danger on Item), caller-owned content goes into children or named slots (icon, shortcut as ReactNode), and rejects boolean content props (hasIcon, showShortcut) with the 2^n argument.",
          "ru": "Проводит границу проп/композиция с критериями: закрытые перечислимые наборы остаются пропсами (tone: default | danger на Item), контент вызывающего уходит в children или именованные слоты (icon, shortcut как ReactNode), и отвергает булевы контентные пропсы (hasIcon, showShortcut) аргументом про 2^n."
        },
        {
          "en": "Keeps the future migration open: the family exposes behavior-free structure now (roles and data attributes welcome), so a headless primitive can later own keyboard and focus logic inside the same part API without breaking the 12 consumers.",
          "ru": "Оставляет будущую миграцию открытой: семейство сейчас даёт структуру без поведения (роли и data-атрибуты приветствуются), чтобы headless-примитив позже взял клавиатуру и фокус внутри того же API частей, не ломая 12 потребителей."
        }
      ],
      "model": {
        "en": "Parts: Menu (provider), Menu.Trigger, Menu.List (portal target later), Menu.Item, Menu.Separator; submenu reserved as Menu.Sub with the same shape. State: MenuCtx = { open, setOpen, activeId } created with createContext(null); useMenuCtx(part) throws `part + \" must be used within <Menu>\"` — misuse fails at the misuse site in dev. Open state: const isControlled = open !== undefined; active = isControlled ? open : internal; setOpen calls onOpenChange always and setInternal only when uncontrolled — the tour passes open + onOpenChange and owns the state; everyone else uses defaultOpen. API line: Item takes tone=\"default\" | \"danger\" (closed set the system owns, themed by tokens), disabled (behavioral boolean, justified — it changes interaction, not content), and children for the label; icon and shortcut are ReactNode slots so badges and custom layouts never need a system release. Explicitly rejected: hasIcon/showShortcut booleans (content described as configuration — the 2^n path) and Children.map wiring (the feature-flag wrappers would make items invisible to type matching; context reaches through them). Forward-compatibility: parts render plain elements with role=\"menu\"/\"menuitem\" and data-state attributes only; when the headless primitive lands, it replaces the internals of the same parts — the 12 teams' JSX is the contract and it does not change.",
        "ru": "Части: Menu (провайдер), Menu.Trigger, Menu.List (позже портал), Menu.Item, Menu.Separator; подменю зарезервировано как Menu.Sub той же формы. Состояние: MenuCtx = { open, setOpen, activeId } через createContext(null); useMenuCtx(part) бросает `part + \" must be used within <Menu>\"` — неправильное использование падает в точке ошибки в dev. Открытость: const isControlled = open !== undefined; active = isControlled ? open : internal; setOpen всегда зовёт onOpenChange и setInternal только в uncontrolled — тур передаёт open + onOpenChange и владеет состоянием; остальные используют defaultOpen. Граница API: Item принимает tone=\"default\" | \"danger\" (закрытый набор, которым владеет система, темизируется токенами), disabled (поведенческий булев, оправдан — меняет взаимодействие, а не контент) и children для подписи; icon и shortcut — слоты ReactNode, чтобы бейджи и кастомные раскладки не требовали релиза системы. Явно отвергнуто: булевы hasIcon/showShortcut (контент, описанный как конфигурация — путь к 2^n) и связка Children.map (обёртки feature-флагов сделали бы элементы невидимыми для сопоставления по типу; контекст достаёт сквозь них). Совместимость вперёд: части рендерят простые элементы только с role=\"menu\"/\"menuitem\" и data-state-атрибутами; когда приедет headless-примитив, он заменит внутренности тех же частей — JSX двенадцати команд и есть контракт, и он не меняется."
      }
    }
  ]
}
