{
  "lessonKey": "browser/08-putting-it-together/02-eight-layers-traced",
  "track": "browser",
  "tasks": [
    {
      "id": "order-eight-layers",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Order the eight layers and pin each delay to its layer", "ru": "Расставь восемь слоёв и привяжи каждую задержку к её слою" },
      "prompt": { "en": "Fill the blanks for the e-commerce product page traced in the lesson. The eight layers run in a fixed order; each symptom below lands in exactly one layer.", "ru": "Заполни пропуски для страницы товара e-commerce из урока. Восемь слоёв идут в фиксированном порядке; каждый симптом ниже приземляется ровно в один слой." },
      "evidence": { "en": "Layer 1 Network → Layer 2 ? → Layer 3 CSS/CSSOM → Layer 4 ? → Layer 5 First paint → Layer 6 ? → Layer 7 Interaction → Layer 8 Second navigation. Symptoms: a ~150 ms main-thread spike before any line runs; a tap that queues behind a long task and produces a 700 ms INP; a hero <img> that started downloading before the parser reached it.", "ru": "Слой 1 Сеть → Слой 2 ? → Слой 3 CSS/CSSOM → Слой 4 ? → Слой 5 Первая отрисовка → Слой 6 ? → Слой 7 Взаимодействие → Слой 8 Вторая навигация. Симптомы: всплеск ~150 мс на главном потоке до выполнения любой строки; тап, встающий в очередь за длинной задачей и дающий INP 700 мс; hero <img>, начавший скачиваться до того, как до него дошёл парсер." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["HTML parse", "HTML parsing", "HTML parse and preload scanner", "html parse", "parse html", "html parsing + preload scanner"], "hint": { "en": "Layer 2 builds the DOM top-to-bottom while the preload scanner runs ahead of it.", "ru": "Слой 2 строит DOM сверху вниз, пока впереди мчится preload-сканер." } },
          { "id": "b2", "accept": ["V8 parse and compile", "V8 parse+compile", "V8", "parse and compile", "v8 parse-and-compile", "v8 parse compile"], "hint": { "en": "The ~150 ms spike before execution is bytes turned into bytecode.", "ru": "Всплеск ~150 мс до выполнения — это байты, превращаемые в байткод." } },
          { "id": "b3", "accept": ["hydration", "hydrate", "hydrateRoot", "гидратация"], "hint": { "en": "The long task that the early tap queues behind is hydrateRoot adopting the server DOM.", "ru": "Длинная задача, за которой встаёт ранний тап, — это hydrateRoot, усыновляющий серверный DOM." } }
        ]
      }
    },
    {
      "id": "predict-second-nav-ttfb",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Predict the second-navigation TTFB and name the layer", "ru": "Предскажи TTFB второй навигации и назови слой" },
      "prompt": { "en": "The first navigation served HTML over the network. The user now taps through to checkout. Predict the second navigation's TTFB and which of the eight layers explains the drop.", "ru": "Первая навигация отдала HTML по сети. Теперь пользователь переходит на checkout. Предскажи TTFB второй навигации и какой из восьми слоёв объясняет падение." },
      "scenario": { "en": "Same product page from the lesson: a service worker was registered on a prior visit. First-navigation TTFB measured ~300 ms (network-served). The app shell is cacheable. The user navigates from the product page to checkout. What TTFB do you expect on this second navigation, and why?", "ru": "Та же страница товара из урока: service worker зарегистрирован с прошлого визита. TTFB первой навигации измерен ~300 мс (отдан сетью). App shell кешируем. Пользователь переходит со страницы товара на checkout. Какой TTFB ты ожидаешь на этой второй навигации и почему?" },
      "reveal": { "en": "≈30 ms — about a 10× drop from ~300 ms. Layer 8: on the second navigation the service worker's fetch handler serves the app shell straight from cache, so there is no DNS, TLS, or HTTP round trip to the origin. TTFB is the floor under every later layer, so this single layer also pulls first paint and interactivity earlier on the repeat load. Note the cause is the service worker cache, not DNS caching (which only saves the lookup, not the round trip) and not hydration (which affects interactivity, never a new navigation's TTFB).", "ru": "≈30 мс — примерно 10× падение с ~300 мс. Слой 8: на второй навигации fetch-обработчик service worker отдаёт app shell прямо из кеша, поэтому нет ни DNS, ни TLS, ни HTTP round trip к origin. TTFB — это пол под каждым последующим слоем, так что этот один слой подтягивает первую отрисовку и интерактивность раньше на повторной загрузке. Причина — кеш service worker, а не кеширование DNS (которое экономит только lookup, не round trip) и не гидратация (которая влияет на интерактивность, но никогда на TTFB новой навигации)." }
    },
    {
      "id": "sandbox-sum-timeline",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Sum the per-layer budget and flag the slowest layer", "ru": "Сложи бюджет по слоям и пометь самый медленный слой" },
      "prompt": { "en": "The per-layer times from the lesson's trace are pre-loaded as `layers`. Write code that prints the total time to interactive (sum of all eight layers) and the name of the single slowest layer, in the exact format `total=<n>ms slowest=<name>`. Pure computation only — no DOM, no timers.", "ru": "Времена по слоям из трассы урока предзагружены как `layers`. Напиши код, печатающий суммарное время до интерактивности (сумму всех восьми слоёв) и имя единственного самого медленного слоя в точном формате `total=<n>ms slowest=<name>`. Только чистое вычисление — без DOM и таймеров." },
      "runtime": "js",
      "setup": "// First-load trace budgets (ms), mid-range phone on 4G, from the lesson.\n// TTFB network-served ~300, HTML parse ~50, CSS/CSSOM ~80, V8 parse+compile ~150,\n// first paint ~120, hydration long task ~600 (300ms-1s+ range, take 600),\n// interaction ~80, second navigation excluded (it is the repeat load).\nconst layers = [\n  { name: 'network', ms: 300 },\n  { name: 'html-parse', ms: 50 },\n  { name: 'css', ms: 80 },\n  { name: 'v8', ms: 150 },\n  { name: 'first-paint', ms: 120 },\n  { name: 'hydration', ms: 600 },\n  { name: 'interaction', ms: 80 }\n];\n",
      "expected": { "kind": "stdout-equals", "value": "total=1380ms slowest=hydration" }
    },
    {
      "id": "fix-uncanny-valley",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Close the hydration uncanny valley on the trace", "ru": "Закрой зловещую долину гидратации на трассе" },
      "prompt": { "en": "A DevTools trace shows good LCP but bad early INP on the product page. Write the changes that attack the real bottleneck layer, then reveal the model answer.", "ru": "DevTools-трасса показывает хороший LCP, но плохой ранний INP на странице товара. Напиши изменения, бьющие по реальному слою-узкому-месту, затем открой образцовый ответ." },
      "starter": "// Trace summary:\n// LCP fires at 1.5 s (good). A 900 ms main-thread long task starts at 1.6 s.\n// User taps 'Add to cart' at 2.0 s -> INP 700 ms. Network track is idle during the long task.\n// Which of the eight layers is the bottleneck, and what change fixes it?",
      "grading": {
        "mode": "self",
        "model": { "en": "The bottleneck is Layer 6, hydration — not the network (its track is idle during the 900 ms task) and not Layer 5 (LCP already fired at 1.5 s, good). The 900 ms main-thread long task is hydrateRoot executing the component tree and the reconciler adopting the server DOM; the 2.0 s tap landed ~400 ms into it and queued behind the remaining work, which is the bulk of the 700 ms INP. Good LCP with bad early INP is the exact signature of the hydration uncanny valley: painted fast, interactive slow. The fix shrinks the hydration long task: code-split so the bundle is smaller (which also trims the ~150 ms V8 parse+compile feeding it), move non-interactive subtrees to React Server Components so they are never hydrated, and hydrate the 'Add to cart' island first / on interaction so the button is live before the rest. Raising the network track does nothing here — the constraint is main-thread CPU.", "ru": "Узкое место — Слой 6, гидратация — не сеть (её дорожка простаивает во время задачи на 900 мс) и не Слой 5 (LCP уже сработал на 1.5 с, хорошо). Длинная задача главного потока на 900 мс — это hydrateRoot, исполняющий дерево компонентов, и реконсилер, усыновляющий серверный DOM; тап на 2.0 с приземлился через ~400 мс в неё и встал в очередь за оставшейся работой, что и составляет основную часть INP 700 мс. Хороший LCP с плохим ранним INP — точная сигнатура зловещей долины гидратации: нарисовано быстро, интерактивно медленно. Фикс сжимает длинную задачу гидратации: код-сплит, чтобы бандл был меньше (это заодно урезает ~150 мс V8 parse+compile, питающий её), вынос неинтерактивных поддеревьев в React Server Components, чтобы они вообще не гидрировались, и гидратация острова 'В корзину' первым / по взаимодействию, чтобы кнопка ожила раньше остального. Поднимать дорожку сети тут бесполезно — ограничение в CPU главного потока." },
        "rubric": [
          { "en": "Names Layer 6 hydration as the bottleneck and rules out network and first paint using the trace evidence.", "ru": "Называет Слой 6 гидратацию узким местом и исключает сеть и первую отрисовку по данным трассы." },
          { "en": "Explains why good LCP and bad early INP coexist (the uncanny valley: painted fast, interactive slow).", "ru": "Объясняет, почему хороший LCP и плохой ранний INP сосуществуют (зловещая долина: нарисовано быстро, интерактивно медленно)." },
          { "en": "Proposes shrinking the hydration task itself — code-split, Server Components, fewer/prioritized hydrated islands — not throwing network bandwidth at it.", "ru": "Предлагает сжать саму задачу гидратации — код-сплит, Server Components, меньше/приоритизированные гидрируемые острова — а не кидать в неё сетевую полосу." }
        ]
      }
    }
  ]
}
