{
  "lessonKey": "cli/08-shell-scripting/02-conditionals-and-exit-codes",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-short-circuit-chain",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Trace the && || chain", "ru": "Проследи цепочку && ||" },
      "prompt": { "en": "Work out which commands run and what is printed, before revealing.", "ru": "Определи, какие команды выполняются и что выводится, прежде чем открыть ответ." },
      "scenario": { "en": "<code>mkdir /tmp/existing_dir 2>/dev/null; mkdir /tmp/existing_dir && echo \"created\" || echo \"failed\"</code>\n<code>/tmp/existing_dir</code> already exists. What is printed?", "ru": "<code>mkdir /tmp/existing_dir 2>/dev/null; mkdir /tmp/existing_dir && echo \"created\" || echo \"failed\"</code>\n<code>/tmp/existing_dir</code> уже существует. Что выводится?" },
      "reveal": { "en": "<code>failed</code>. The second <code>mkdir /tmp/existing_dir</code> fails (directory exists) with exit code 1. Because <code>mkdir</code> returned non-zero, the <code>&&</code> short-circuits and skips <code>echo \"created\"</code>. The <code>||</code> then sees the non-zero from <code>mkdir</code> and runs <code>echo \"failed\"</code>. Output: <code>failed</code>.", "ru": "<code>failed</code>. Вторая команда <code>mkdir /tmp/existing_dir</code> завершается с ошибкой (каталог существует) с кодом выхода 1. Так как <code>mkdir</code> вернул ненулевой код, <code>&&</code> короткозамыкает и пропускает <code>echo \"created\"</code>. Затем <code>||</code> видит ненулевой код от <code>mkdir</code> и выполняет <code>echo \"failed\"</code>. Вывод: <code>failed</code>." }
    },
    {
      "id": "sandbox-exit-code-chain",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Simulate && / || exit-code routing in JS", "ru": "Смоделируй маршрутизацию && / || через коды выхода в JS" },
      "prompt": { "en": "The JS below models a three-command chain: <code>cmdA && cmdB || cmdC</code>. Each function returns an exit code (0=success). Run it and verify the output matches the expected stdout.", "ru": "JS ниже моделирует цепочку из трёх команд: <code>cmdA && cmdB || cmdC</code>. Каждая функция возвращает код выхода (0=успех). Запусти и убедись, что вывод совпадает с ожидаемым stdout." },
      "runtime": "js",
      "setup": "function cmdA() { console.log('A ran'); return 0; }\nfunction cmdB() { console.log('B ran'); return 1; }\nfunction cmdC() { console.log('C ran'); return 0; }",
      "initialCode": "const a = cmdA();\n// TODO: model `cmdA && cmdB || cmdC` with if/else branches.",
      "expected": { "kind": "stdout-equals", "value": "A ran\nB ran\nC ran" }
    },
    {
      "id": "diagnose-bracket-empty-var",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Identify the test construct bug", "ru": "Определи ошибку конструкции test" },
      "prompt": { "en": "Fill in the blank: the script uses <code>[ $name = \"alice\" ]</code> but <code>$name</code> is empty. The resulting expression is (b1), which causes a syntax error. The fix is to use (b2) instead of <code>[ ]</code>.", "ru": "Заполни пропуск: скрипт использует <code>[ $name = \"alice\" ]</code>, но <code>$name</code> пуст. Получившееся выражение — (b1), что вызывает синтаксическую ошибку. Исправление — использовать (b2) вместо <code>[ ]</code>." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["[ = \"alice\" ]", "[ = alice ]", "[ = 'alice' ]", "= alice", "= \"alice\""],
            "hint": { "en": "When $name is empty, it expands to nothing, leaving only the operator and right-hand side inside the brackets.", "ru": "Когда $name пустой, он раскрывается в ничто, оставляя внутри скобок только оператор и правую часть." }
          },
          {
            "id": "b2",
            "accept": ["[[ ]]", "[[ ]]", "double brackets", "двойные скобки", "[[ $name == \"alice\" ]]"],
            "hint": { "en": "The bash-specific test construct that never word-splits its operands.", "ru": "Специфичная для bash конструкция проверки, которая никогда не разбивает операнды по словам." }
          }
        ]
      }
    },
    {
      "id": "review-missing-exit-guards",
      "type": "review",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Spot the missing exit-code guards", "ru": "Найди отсутствующие защиты кода выхода" },
      "prompt": { "en": "This deploy script has planted bugs around exit-code handling. Find them.", "ru": "В этом скрипте деплоя есть намеренно посаженные ошибки в обработке кода выхода. Найди их." },
      "diff": {
        "lang": "bash",
        "code": "#!/usr/bin/env bash\ncd /var/app\nrm -rf dist/\ntar -xzf /tmp/release.tar.gz\necho \"Deploy complete\""
      },
      "findings": [
        {
          "id": "cd-without-guard",
          "label": { "en": "cd without || exit: if cd fails, rm -rf runs in the wrong directory", "ru": "cd без || exit: если cd провалится, rm -rf выполнится в неверном каталоге" },
          "severity": "bug",
          "explanation": { "en": "If <code>/var/app</code> does not exist or is not accessible, <code>cd</code> fails silently and execution continues. The subsequent <code>rm -rf dist/</code> then deletes <code>dist/</code> relative to whatever the current directory is — potentially wiping the wrong tree. Fix: <code>cd /var/app || { echo \"cd failed\" >&2; exit 1; }</code>.", "ru": "Если <code>/var/app</code> не существует или недоступен, <code>cd</code> молча завершается с ошибкой и выполнение продолжается. Последующий <code>rm -rf dist/</code> тогда удаляет <code>dist/</code> относительно того каталога, в котором находится скрипт — возможно удаляя не то дерево. Исправление: <code>cd /var/app || { echo \"cd failed\" >&2; exit 1; }</code>." },
          "planted": true
        },
        {
          "id": "no-set-e-or-guards",
          "label": { "en": "No set -e or per-command checks: tar failure is silently ignored", "ru": "Нет set -e или проверок для каждой команды: ошибка tar молча игнорируется" },
          "severity": "bug",
          "explanation": { "en": "If <code>tar -xzf</code> fails (corrupt archive, missing file), the script prints <code>Deploy complete</code> anyway. Add <code>set -euo pipefail</code> at the top, or guard individually: <code>tar -xzf /tmp/release.tar.gz || { echo \"tar failed\" >&2; exit 1; }</code>.", "ru": "Если <code>tar -xzf</code> завершается с ошибкой (повреждённый архив, отсутствующий файл), скрипт всё равно печатает <code>Deploy complete</code>. Добавь <code>set -euo pipefail</code> в начало или проверяй каждую команду: <code>tar -xzf /tmp/release.tar.gz || { echo \"tar failed\" >&2; exit 1; }</code>." },
          "planted": true
        }
      ]
    }
  ]
}
