{
  "lessonKey": "cli/08-shell-scripting/04-robust-scripts",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-local-trap",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Predict what set -e does with local val=$(cmd)", "ru": "Предскажи, что делает set -e с local val=$(cmd)" },
      "prompt": { "en": "Before revealing: decide whether the script aborts or continues when failing_cmd exits non-zero.", "ru": "Прежде чем открыть ответ: решите, прерывается ли скрипт или продолжается, когда failing_cmd завершается с ненулевым кодом." },
      "scenario": { "en": "<pre>#!/usr/bin/env bash\nset -euo pipefail\nget_val() {\n  local result=$(false)  # false always exits 1\n  echo \"result=${result}\"\n}\nget_val\necho \"after\"</pre>\nDoes the script abort at <code>local result=$(false)</code>, or does it print <code>result=</code> and <code>after</code>?", "ru": "<pre>#!/usr/bin/env bash\nset -euo pipefail\nget_val() {\n  local result=$(false)  # false всегда завершается с кодом 1\n  echo \"result=${result}\"\n}\nget_val\necho \"after\"</pre>\nПрерывается ли скрипт на <code>local result=$(false)</code>, или он выводит <code>result=</code> и <code>after</code>?" },
      "reveal": { "en": "The script prints <code>result=</code> and <code>after</code> — it does <strong>not</strong> abort. <code>local</code> is a builtin command and its exit code is always 0, regardless of what the command substitution inside it returned. So <code>set -e</code> sees exit code 0 from <code>local</code> and continues. The failure of <code>false</code> is swallowed. Fix: declare <code>local result</code> on one line, then assign <code>result=$(false)</code> on the next — now <code>set -e</code> sees the non-zero exit from the assignment and aborts.", "ru": "Скрипт печатает <code>result=</code> и <code>after</code> — он <strong>не</strong> прерывается. <code>local</code> — это встроенная команда, и её код выхода всегда 0, независимо от того, что вернула подстановка команды внутри неё. Поэтому <code>set -e</code> видит код 0 от <code>local</code> и продолжает. Ошибка <code>false</code> проглатывается. Исправление: объявить <code>local result</code> на одной строке, затем присвоить <code>result=$(false)</code> на следующей — теперь <code>set -e</code> видит ненулевой код выхода от присваивания и прерывается." }
    },
    {
      "id": "diagnose-pipefail-semantics",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Name what each set flag catches", "ru": "Назови, что перехватывает каждый флаг set" },
      "prompt": { "en": "Fill in the blanks: <code>set -e</code> exits the script when any command returns (b1). <code>set -u</code> exits when a script references a (b2) variable. <code>set -o pipefail</code> makes the exit code of a pipeline equal to the first (b3) exit code rather than always using the last command.", "ru": "Заполни пропуски: <code>set -e</code> завершает скрипт, когда любая команда возвращает (b1). <code>set -u</code> завершает, когда скрипт обращается к (b2) переменной. <code>set -o pipefail</code> делает код выхода конвейера равным первому (b3) коду выхода, а не всегда использует последнюю команду." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["non-zero", "nonzero", "non-zero exit code", "a non-zero exit code", "ненулевой", "ненулевой код", "ненулевой код выхода"],
            "hint": { "en": "Any exit code that is not 0.", "ru": "Любой код выхода, не равный 0." }
          },
          {
            "id": "b2",
            "accept": ["unset", "undefined", "undeclared", "незаданную", "неопределённую", "необъявленную"],
            "hint": { "en": "A variable that has never been assigned a value.", "ru": "Переменная, которой никогда не присваивалось значение." }
          },
          {
            "id": "b3",
            "accept": ["non-zero", "nonzero", "failing", "failed", "ненулевой", "провального", "упавшего"],
            "hint": { "en": "pipefail surfaces failures from earlier stages, not just the last command.", "ru": "pipefail выявляет ошибки из более ранних стадий, а не только из последней команды." }
          }
        ]
      }
    },
    {
      "id": "review-missing-set-and-trap",
      "type": "review",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Audit a script missing set and trap", "ru": "Проверь скрипт без set и trap" },
      "prompt": { "en": "This script creates a temp dir, does work, and cleans up. Find the planted reliability bugs.", "ru": "Этот скрипт создаёт временный каталог, выполняет работу и убирает за собой. Найди намеренно посаженные ошибки надёжности." },
      "diff": {
        "lang": "bash",
        "code": "#!/usr/bin/env bash\ntmpdir=$(mktemp -d)\ncp \"$1\" \"$tmpdir/input\"\nprocess \"$tmpdir/input\" > \"$tmpdir/output\"\ncp \"$tmpdir/output\" \"$2\"\nrm -rf \"$tmpdir\"\necho \"Done\""
      },
      "findings": [
        {
          "id": "no-set-euo-pipefail",
          "label": { "en": "Missing set -euo pipefail: failures in cp/process are silently ignored", "ru": "Отсутствует set -euo pipefail: ошибки cp/process молча игнорируются" },
          "severity": "bug",
          "explanation": { "en": "Without <code>set -euo pipefail</code>, if <code>process</code> fails the script still runs <code>cp \"$tmpdir/output\" \"$2\"</code> — potentially overwriting the destination with an empty or partial file, then prints <code>Done</code>. Adding <code>set -euo pipefail</code> at the top ensures any failure aborts immediately.", "ru": "Без <code>set -euo pipefail</code>, если <code>process</code> провалится, скрипт всё равно выполняет <code>cp \"$tmpdir/output\" \"$2\"</code> — потенциально перезаписывая назначение пустым или частичным файлом, затем печатает <code>Done</code>. Добавление <code>set -euo pipefail</code> в начало гарантирует немедленное прерывание при любой ошибке." },
          "planted": true
        },
        {
          "id": "no-trap-cleanup",
          "label": { "en": "No trap: tmpdir leaks on any error before rm -rf", "ru": "Нет trap: tmpdir остаётся при любой ошибке до rm -rf" },
          "severity": "bug",
          "explanation": { "en": "If any command between <code>mktemp</code> and <code>rm -rf \"$tmpdir\"</code> fails and exits the script, the temporary directory is never cleaned up. Fix: add <code>trap 'rm -rf \"$tmpdir\"' EXIT</code> immediately after the <code>mktemp</code> line. The <code>EXIT</code> trap fires regardless of how the script terminates.", "ru": "Если любая команда между <code>mktemp</code> и <code>rm -rf \"$tmpdir\"</code> завершается с ошибкой и выходит из скрипта, временный каталог никогда не очищается. Исправление: добавить <code>trap 'rm -rf \"$tmpdir\"' EXIT</code> сразу после строки с <code>mktemp</code>. Ловушка <code>EXIT</code> срабатывает независимо от способа завершения скрипта." },
          "planted": true
        }
      ]
    },
    {
      "id": "review-local-assignment-trap",
      "type": "review",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Spot the local + command-substitution footgun", "ru": "Найди ловушку local + подстановка команды" },
      "prompt": { "en": "This function uses set -e but has a subtle bug that lets a failing command go undetected. Find it.", "ru": "Эта функция использует set -e, но содержит тонкую ошибку, позволяющую провальной команде остаться незамеченной. Найди её." },
      "diff": {
        "lang": "bash",
        "code": "#!/usr/bin/env bash\nset -euo pipefail\n\nget_user_count() {\n  local count=$(getent passwd | wc -l)\n  echo \"$count\"\n}\n\ntotal=$(get_user_count)\necho \"Users: $total\""
      },
      "findings": [
        {
          "id": "local-masks-subshell-failure",
          "label": { "en": "local count=$(cmd) masks cmd's failure: local always returns 0", "ru": "local count=$(cmd) маскирует ошибку cmd: local всегда возвращает 0" },
          "severity": "bug",
          "explanation": { "en": "<code>local count=$(getent passwd | wc -l)</code> combines declaration and assignment on one line. If <code>getent passwd</code> fails (e.g. NSS not configured), <code>local</code> still returns exit code 0, hiding the failure from <code>set -e</code>. Fix: separate declaration and assignment:<br><code>local count</code><br><code>count=$(getent passwd | wc -l)</code><br>Now if the pipeline fails, the assignment line returns non-zero and <code>set -e</code> aborts.", "ru": "<code>local count=$(getent passwd | wc -l)</code> объединяет объявление и присваивание в одной строке. Если <code>getent passwd</code> завершается с ошибкой (например, NSS не настроен), <code>local</code> всё равно возвращает код 0, скрывая ошибку от <code>set -e</code>. Исправление: разделить объявление и присваивание:<br><code>local count</code><br><code>count=$(getent passwd | wc -l)</code><br>Теперь если конвейер провалится, строка присваивания вернёт ненулевой код и <code>set -e</code> прервёт выполнение." },
          "planted": true
        }
      ]
    }
  ]
}
