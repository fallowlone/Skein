{
  "lessonKey": "cli/10-putting-it-together/02-a-maintenance-script",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-trap-order",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Does the cleanup trap fire on error exit?", "ru": "Срабатывает ли trap очистки при выходе с ошибкой?" },
      "prompt": { "en": "Predict what the script prints before revealing.", "ru": "Предскажи, что выводит скрипт, до раскрытия ответа." },
      "scenario": { "en": "A script has <code>set -euo pipefail</code> and registers <code>trap 'echo cleaned' EXIT</code> before running <code>false</code> (which always returns exit code 1). What is the last line printed, and does the trap fire?", "ru": "Скрипт имеет <code>set -euo pipefail</code> и регистрирует <code>trap 'echo cleaned' EXIT</code> перед запуском <code>false</code> (который всегда возвращает код выхода 1). Какая последняя строка напечатана и срабатывает ли trap?" },
      "reveal": { "en": "The trap fires and prints <code>cleaned</code>. <code>set -e</code> causes the script to exit when <code>false</code> returns 1. The EXIT pseudo-signal fires on every exit — including error exits from <code>set -e</code>. This is the core guarantee that makes <code>trap cleanup EXIT</code> reliable: it does not matter how the script terminates.", "ru": "Trap срабатывает и печатает <code>cleaned</code>. <code>set -e</code> вызывает выход скрипта, когда <code>false</code> возвращает 1. Псевдосигнал EXIT срабатывает при любом выходе — включая выходы с ошибкой от <code>set -e</code>. Это основная гарантия, делающая <code>trap cleanup EXIT</code> надёжным: не важно, как завершается скрипт." }
    },
    {
      "id": "diagnose-unset-var-rm",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Name the safeguard that prevents rm -rf on unset variable", "ru": "Назови защитный механизм, предотвращающий rm -rf по неустановленной переменной" },
      "prompt": { "en": "Fill in the blank: in a script with <code>set -euo pipefail</code>, the option that catches <code>rm -rf $WORKDIR/tmp</code> when WORKDIR is unset — before rm executes — is <code>set -(b1)</code>.", "ru": "Заполни пропуск: в скрипте с <code>set -euo pipefail</code> опция, которая перехватывает <code>rm -rf $WORKDIR/tmp</code>, когда WORKDIR не установлен — до выполнения rm — это <code>set -(b1)</code>." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["u", "-u", "u (unset)", "u flag"],
            "hint": { "en": "It is the flag in -euo that treats unset variables as errors.", "ru": "Это флаг в -euo, трактующий неустановленные переменные как ошибки." }
          }
        ]
      }
    },
    {
      "id": "review-backup-script",
      "type": "review",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Review this backup script for safety bugs", "ru": "Проверь этот скрипт резервного копирования на баги безопасности" },
      "prompt": { "en": "Find the safety issues in this backup script.", "ru": "Найди проблемы безопасности в этом скрипте резервного копирования." },
      "diff": {
        "lang": "bash",
        "code": "#!/bin/bash\nSRC=$1\nDEST=$2\nTMPDIR=$(mktemp -d)\ntrap 'rm -rf $TMPDIR' EXIT\ncp -r $SRC $DEST/backup_$(date +%s)"
      },
      "findings": [
        {
          "id": "missing-set-euo-pipefail",
          "label": { "en": "Missing set -euo pipefail — errors are silently ignored", "ru": "Отсутствует set -euo pipefail — ошибки тихо игнорируются" },
          "severity": "bug",
          "explanation": { "en": "Without <code>set -euo pipefail</code>, a failed <code>cp</code> does not stop the script. The script completes with exit 0 even if the backup is incomplete or $SRC does not exist. Add <code>set -euo pipefail</code> as line 2.", "ru": "Без <code>set -euo pipefail</code> неудачный <code>cp</code> не останавливает скрипт. Скрипт завершается с exit 0, даже если резервная копия неполна или $SRC не существует. Добавь <code>set -euo pipefail</code> второй строкой." },
          "planted": true
        },
        {
          "id": "unquoted-variables",
          "label": { "en": "Unquoted $SRC and $DEST — paths with spaces split into multiple arguments", "ru": "Неэкранированные $SRC и $DEST — пути с пробелами разбиваются на несколько аргументов" },
          "severity": "bug",
          "explanation": { "en": "<code>cp -r $SRC $DEST/...</code> without quotes splits on whitespace. A source path like <code>/var/my files/data</code> becomes two arguments: <code>/var/my</code> and <code>files/data</code>. Always quote: <code>cp -r \"$SRC\" \"$DEST/...\"</code>.", "ru": "<code>cp -r $SRC $DEST/...</code> без кавычек разбивается по пробелам. Путь источника вроде <code>/var/my files/data</code> становится двумя аргументами: <code>/var/my</code> и <code>files/data</code>. Всегда экранируй: <code>cp -r \"$SRC\" \"$DEST/...\"</code>." },
          "planted": true
        }
      ]
    },
    {
      "id": "homelab-sandbox-backup-rotation",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Compute which backup archives to delete in a keep-last-N rotation",
        "ru": "Вычисли, какие архивы резервных копий удалить при ротации keep-last-N"
      },
      "prompt": {
        "en": "A NAS at nas01.example stores daily media backups. Given an array of archive filenames already sorted oldest→newest, keep only the 4 newest and print a labelled line for every file: 'delete <name>' or 'keep <name>'. Run the code and confirm the exact output.",
        "ru": "NAS по адресу nas01.example хранит ежедневные резервные копии медиа. Дан массив имён архивов, уже отсортированных от старых к новым. Нужно оставить только 4 новейших и вывести пронумерованную строку для каждого файла: 'delete <имя>' или 'keep <имя>'. Запусти код и подтверди точный вывод."
      },
      "runtime": "js",
      "setup": "const files = [\n  'media-2026-06-10.tar.gz',\n  'media-2026-06-14.tar.gz',\n  'media-2026-06-17.tar.gz',\n  'media-2026-06-19.tar.gz',\n  'media-2026-06-20.tar.gz',\n  'media-2026-06-21.tar.gz'\n];\nconst N = 4;",
      "initialCode": "const deleteCount = files.length - N;\nfor (let i = 0; i < files.length; i++) {\n  // TODO: print `delete <name>` for old archives and `keep <name>` for the N newest.\n}",
      "expected": {
        "kind": "stdout-equals",
        "value": "delete media-2026-06-10.tar.gz\ndelete media-2026-06-14.tar.gz\nkeep media-2026-06-17.tar.gz\nkeep media-2026-06-19.tar.gz\nkeep media-2026-06-20.tar.gz\nkeep media-2026-06-21.tar.gz"
      }
    },
    {
      "id": "homelab-design-age-encryption",
      "type": "design",
      "difficulty": "apply",
      "estMin": 10,
      "title": {
        "en": "Add age encryption to a backup before off-site upload",
        "ru": "Добавь шифрование age к резервной копии перед отправкой за пределы площадки"
      },
      "prompt": {
        "en": "age is a simple modern file-encryption tool: <code>age -r &lt;recipient-pubkey&gt; -o backup.age backup.tar.gz</code> encrypts to a recipient's public key; <code>age -d -i key.txt backup.age</code> decrypts using the private key. Design the encryption step that runs after the keep-last-N rotation and before the archive is uploaded to off-site storage at backup@storage.example or 100.64.0.30. Your private key must NOT be stored on nas01.example. Self-grade against the rubric.",
        "ru": "age — простой современный инструмент шифрования файлов: <code>age -r &lt;recipient-pubkey&gt; -o backup.age backup.tar.gz</code> шифрует на публичный ключ получателя; <code>age -d -i key.txt backup.age</code> расшифровывает с помощью приватного ключа. Спроектируй шаг шифрования, который выполняется после ротации keep-last-N и до отправки архива во внешнее хранилище backup@storage.example или 100.64.0.30. Приватный ключ НЕ должен храниться на nas01.example. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Encrypt each surviving archive before uploading. The private key must be kept off-site (e.g. on the operator's workstation or a hardware key), not on nas01.example. Use a recipient public key embedded in the backup script for encryption. The encrypted file should replace or accompany the plaintext archive — do not upload unencrypted archives off-site.",
        "ru": "Шифруй каждый сохранившийся архив перед отправкой. Приватный ключ должен храниться вне площадки (например, на рабочей станции оператора или аппаратном ключе), не на nas01.example. Используй публичный ключ получателя, встроенный в скрипт резервного копирования, для шифрования. Зашифрованный файл должен заменять или сопровождать открытый архив — не отправляй незашифрованные архивы за пределы площадки."
      },
      "rubric": [
        {
          "en": "Encrypts the archive with age before uploading (age -r <pubkey> -o archive.age archive.tar.gz)",
          "ru": "Шифрует архив с помощью age перед отправкой (age -r <pubkey> -o archive.age archive.tar.gz)"
        },
        {
          "en": "Private key is NOT stored on nas01.example — only the recipient public key is present on the backup server",
          "ru": "Приватный ключ НЕ хранится на nas01.example — на сервере резервного копирования присутствует только публичный ключ получателя"
        },
        {
          "en": "Only the encrypted .age file is uploaded off-site; the unencrypted .tar.gz is not transferred",
          "ru": "За пределы площадки отправляется только зашифрованный файл .age; незашифрованный .tar.gz не передаётся"
        }
      ],
      "model": {
        "en": "<pre># Public key stored in the script or a config file on nas01.example\nRECIPIENT='age1ql3z7hjy54pw3hyww5ayyfg7zqgvc7w3j2elw8zmrj2kg5sfn9aqmcac8p'\nARCHIVE=\"media-$(date +%Y-%m-%d).tar.gz\"\n\n# 1. Create the archive (e.g. from /srv/media)\ntar -czf \"$ARCHIVE\" /srv/media\n\n# 2. Encrypt before any off-site transfer\nage -r \"$RECIPIENT\" -o \"${ARCHIVE}.age\" \"$ARCHIVE\"\n\n# 3. Upload only the encrypted file\nrsync -az \"${ARCHIVE}.age\" backup@storage.example:/backups/\n\n# 4. Remove the local plaintext after successful upload\nrm \"$ARCHIVE\"</pre>The private key (age -d -i key.txt) never touches nas01.example. An operator on their own workstation runs <code>age -d -i ~/.age/key.txt media-2026-06-21.tar.gz.age</code> to restore. This keeps the encryption key out of the blast radius of a compromised NAS.",
        "ru": "<pre># Публичный ключ хранится в скрипте или файле конфигурации на nas01.example\nRECIPIENT='age1ql3z7hjy54pw3hyww5ayyfg7zqgvc7w3j2elw8zmrj2kg5sfn9aqmcac8p'\nARCHIVE=\"media-$(date +%Y-%m-%d).tar.gz\"\n\n# 1. Создать архив (например, из /srv/media)\ntar -czf \"$ARCHIVE\" /srv/media\n\n# 2. Зашифровать перед любой внешней передачей\nage -r \"$RECIPIENT\" -o \"${ARCHIVE}.age\" \"$ARCHIVE\"\n\n# 3. Отправить только зашифрованный файл\nrsync -az \"${ARCHIVE}.age\" backup@storage.example:/backups/\n\n# 4. Удалить локальный открытый текст после успешной отправки\nrm \"$ARCHIVE\"</pre>Приватный ключ (age -d -i key.txt) никогда не касается nas01.example. Оператор на своей рабочей станции запускает <code>age -d -i ~/.age/key.txt media-2026-06-21.tar.gz.age</code> для восстановления. Это удерживает ключ шифрования вне радиуса поражения при компрометации NAS."
      }
    },
    {
      "id": "design-cleanup-with-staging",
      "type": "design",
      "difficulty": "apply",
      "estMin": 10,
      "title": { "en": "Design an atomic backup using a temp staging dir", "ru": "Спроектируй атомарную резервную копию через временный промежуточный каталог" },
      "prompt": { "en": "Design the skeleton of a bash function that: creates a temp dir, copies files there, then atomically renames to the final backup path. The trap must clean up the temp dir if anything fails. Self-grade against the rubric.", "ru": "Спроектируй скелет bash-функции, которая: создаёт временный каталог, копирует файлы туда, затем атомарно переименовывает в итоговый путь резервной копии. Trap должен очистить временный каталог при любом сбое. Оцени себя по рубрике." },
      "constraints": { "en": "Use set -euo pipefail. Register trap before mktemp. The final rename must be mv (atomic on same filesystem). Function must accept src and dest as arguments.", "ru": "Использовать set -euo pipefail. Зарегистрировать trap до mktemp. Итоговое переименование должно быть через mv (атомарно на одной файловой системе). Функция должна принимать src и dest как аргументы." },
      "rubric": [
        { "en": "TMPDIR_WORK initialized to empty string before trap registration", "ru": "TMPDIR_WORK инициализирован пустой строкой до регистрации trap" },
        { "en": "trap cleanup EXIT registered before mktemp call", "ru": "trap cleanup EXIT зарегистрирован до вызова mktemp" },
        { "en": "cp -a used (not cp -r) to preserve permissions and timestamps", "ru": "Используется cp -a (не cp -r) для сохранения прав и временных меток" },
        { "en": "mv renames from temp to final path — not a second cp", "ru": "mv переименовывает из временного в итоговый путь — не второй cp" },
        { "en": "cleanup checks [[ -n $TMPDIR_WORK && -d $TMPDIR_WORK ]] before rm -rf", "ru": "cleanup проверяет [[ -n $TMPDIR_WORK && -d $TMPDIR_WORK ]] перед rm -rf" }
      ],
      "model": { "en": "<pre>TMPDIR_WORK=''\ncleanup() { [[ -n \"$TMPDIR_WORK\" && -d \"$TMPDIR_WORK\" ]] && rm -rf \"$TMPDIR_WORK\"; }\ntrap cleanup EXIT\ndo_backup() {\n  local src=\"$1\" dest=\"$2\" ts\n  ts=$(date +%Y%m%d_%H%M%S)\n  TMPDIR_WORK=$(mktemp -d)\n  cp -a \"${src}/.\" \"$TMPDIR_WORK\"\n  mv \"$TMPDIR_WORK\" \"${dest}/backup_${ts}\"\n  TMPDIR_WORK=''  # mv succeeded; nothing left to clean\n}</pre>Setting TMPDIR_WORK='' after mv prevents the cleanup trap from trying to delete the directory that was just renamed.", "ru": "<pre>TMPDIR_WORK=''\ncleanup() { [[ -n \"$TMPDIR_WORK\" && -d \"$TMPDIR_WORK\" ]] && rm -rf \"$TMPDIR_WORK\"; }\ntrap cleanup EXIT\ndo_backup() {\n  local src=\"$1\" dest=\"$2\" ts\n  ts=$(date +%Y%m%d_%H%M%S)\n  TMPDIR_WORK=$(mktemp -d)\n  cp -a \"${src}/.\" \"$TMPDIR_WORK\"\n  mv \"$TMPDIR_WORK\" \"${dest}/backup_${ts}\"\n  TMPDIR_WORK=''  # mv успешен; нечего очищать\n}</pre>Установка TMPDIR_WORK='' после mv предотвращает попытку cleanup удалить директорию, которая только что была переименована." }
    }
  ]
}
