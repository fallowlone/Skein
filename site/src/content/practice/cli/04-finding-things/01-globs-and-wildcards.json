{
  "lessonKey": "cli/04-finding-things/01-globs-and-wildcards",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-glob-expansion-order",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What does the shell pass to ls?", "ru": "Что оболочка передаёт ls?" },
      "prompt": { "en": "Trace what happens before ls runs and predict what arguments it receives.", "ru": "Проследи, что происходит до запуска ls, и предскажи, какие аргументы он получает." },
      "scenario": { "en": "Directory contains: <code>notes.txt</code>, <code>report.txt</code>, <code>image.png</code>. You run <code>ls *.txt</code>. What arguments does <code>ls</code> actually receive?", "ru": "Каталог содержит: <code>notes.txt</code>, <code>report.txt</code>, <code>image.png</code>. Ты запускаешь <code>ls *.txt</code>. Какие аргументы фактически получает <code>ls</code>?" },
      "reveal": { "en": "<code>ls notes.txt report.txt</code>. The shell expands <code>*.txt</code> to the two matching filenames before calling <code>ls</code>. The <code>ls</code> program never sees the asterisk.", "ru": "<code>ls notes.txt report.txt</code>. Оболочка раскрывает <code>*.txt</code> в два совпадающих имени файла до вызова <code>ls</code>. Программа <code>ls</code> никогда не видит звёздочку." }
    },
    {
      "id": "predict-question-mark-glob",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Which files match file?.log?", "ru": "Какие файлы соответствуют file?.log?" },
      "prompt": { "en": "Identify exactly which filenames match the pattern and which do not.", "ru": "Определи точно, какие имена файлов соответствуют шаблону, а какие нет." },
      "scenario": { "en": "Directory contains: <code>file1.log</code>, <code>file10.log</code>, <code>fileA.log</code>, <code>file.log</code>. Which files match <code>file?.log</code>?", "ru": "Каталог содержит: <code>file1.log</code>, <code>file10.log</code>, <code>fileA.log</code>, <code>file.log</code>. Какие файлы соответствуют <code>file?.log</code>?" },
      "reveal": { "en": "<code>file1.log</code> and <code>fileA.log</code> match. <code>file10.log</code> does not match because <code>?</code> matches exactly one character and <code>10</code> is two. <code>file.log</code> does not match because <code>?</code> requires at least one character between <code>file</code> and <code>.log</code>.", "ru": "<code>file1.log</code> и <code>fileA.log</code> совпадают. <code>file10.log</code> не совпадает, потому что <code>?</code> соответствует ровно одному символу, а <code>10</code> — это два. <code>file.log</code> не совпадает, потому что <code>?</code> требует хотя бы один символ между <code>file</code> и <code>.log</code>." }
    },
    {
      "id": "review-unquoted-glob-find",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the unquoted glob bug", "ru": "Найди ошибку нераскавыченного glob" },
      "prompt": { "en": "A developer wrote this command to find all log files under /var. Identify the bug.", "ru": "Разработчик написал эту команду для поиска всех лог-файлов под /var. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "find /var -name *.log"
      },
      "findings": [
        {
          "id": "unquoted-glob-shell-expands-first",
          "label": { "en": "*.log is unquoted — the shell expands it before find sees it", "ru": "*.log не в кавычках — оболочка раскрывает его до того, как find видит аргумент" },
          "severity": "bug",
          "explanation": { "en": "If the current directory contains <code>.log</code> files, the shell expands <code>*.log</code> into those filenames and <code>find</code> receives a literal filename as its <code>-name</code> argument — not a pattern. If no <code>.log</code> files exist in the current dir, bash passes the literal string <code>*.log</code> which may accidentally work, making the bug intermittent. Fix: <code>find /var -name \"*.log\"</code>.", "ru": "Если в текущем каталоге есть <code>.log</code>-файлы, оболочка раскрывает <code>*.log</code> в эти имена файлов, и <code>find</code> получает буквальное имя файла как аргумент <code>-name</code> — не шаблон. Если в текущем каталоге нет <code>.log</code>-файлов, bash передаёт буквальную строку <code>*.log</code>, что может случайно сработать, делая баг непостоянным. Исправление: <code>find /var -name \"*.log\"</code>." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-cleanup-pipeline",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design a glob-based cleanup command", "ru": "Спроектируй команду очистки на основе glob" },
      "prompt": { "en": "Design a single shell command (using globs and brace expansion) that satisfies all constraints.", "ru": "Спроектируй единственную команду оболочки (используя глобы и раскрытие скобок), удовлетворяющую всем ограничениям." },
      "constraints": { "en": "Directory <code>build/</code> contains: <code>app.js</code>, <code>app.js.map</code>, <code>app.css</code>, <code>app.css.map</code>, <code>vendor.js</code>, <code>vendor.js.map</code>. Delete all <code>.map</code> files in a single <code>rm</code> call using a glob.", "ru": "Каталог <code>build/</code> содержит: <code>app.js</code>, <code>app.js.map</code>, <code>app.css</code>, <code>app.css.map</code>, <code>vendor.js</code>, <code>vendor.js.map</code>. Удали все <code>.map</code>-файлы одним вызовом <code>rm</code>, используя glob." },
      "rubric": [
        { "en": "Uses a glob pattern ending in .map", "ru": "Использует glob-шаблон, заканчивающийся на .map" },
        { "en": "Targets the build/ directory specifically", "ru": "Нацелен конкретно на каталог build/" },
        { "en": "Single rm invocation (no loop)", "ru": "Один вызов rm (без цикла)" }
      ],
      "model": { "en": "<code>rm build/*.map</code>. The glob <code>*.map</code> expands to all three map files in one step; <code>rm</code> receives all three as arguments in a single call. Alternatively <code>rm build/{app,vendor}.{js,css}.map</code> uses brace expansion to enumerate them explicitly — more verbose but documents the exact files.", "ru": "<code>rm build/*.map</code>. Glob <code>*.map</code> раскрывается в все три map-файла за один шаг; <code>rm</code> получает все три как аргументы в одном вызове. Альтернативно <code>rm build/{app,vendor}.{js,css}.map</code> использует раскрытие скобок для явного перечисления — более многословно, но документирует точные файлы." }
    }
  ]
}
