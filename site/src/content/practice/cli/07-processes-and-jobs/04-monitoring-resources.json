{
  "lessonKey": "cli/07-processes-and-jobs/04-monitoring-resources",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-load-average-meaning",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Interpret load average on a 2-core machine", "ru": "Интерпретируй среднюю нагрузку на 2-ядерной машине" },
      "prompt": { "en": "Given uptime output and the CPU count, predict whether the machine is overloaded.", "ru": "По выводу uptime и числу CPU предскажи, перегружена ли машина." },
      "scenario": {
        "en": "<code>uptime</code> outputs: <code>load average: 1.80, 3.20, 4.50</code>. The machine has 2 CPU cores (<code>nproc</code> = 2). Is this machine overloaded now, and is the trend improving or worsening?",
        "ru": "<code>uptime</code> выводит: <code>load average: 1.80, 3.20, 4.50</code>. На машине 2 ядра CPU (<code>nproc</code> = 2). Перегружена ли машина сейчас, и тренд улучшается или ухудшается?"
      },
      "reveal": {
        "en": "Load 1.80 on 2 cores = 90% saturation right now — acceptable but high. However the trend is <strong>improving</strong>: load was 4.50 (225% of capacity) 15 minutes ago, 3.20 five minutes ago, 1.80 now. Something caused a burst that is now subsiding. If load stays below 2.0 for the next reading, no action needed. If it starts rising again, investigate which processes are in the run queue.",
        "ru": "Нагрузка 1.80 на 2 ядра = 90% насыщенности прямо сейчас — приемлемо, но высоко. Однако тренд <strong>улучшается</strong>: нагрузка была 4.50 (225% ёмкости) 15 минут назад, 3.20 пять минут назад, 1.80 сейчас. Что-то вызвало всплеск, который сейчас затухает. Если нагрузка при следующем измерении останется ниже 2.0, действий не требуется. Если начнёт снова расти — расследуй, какие процессы находятся в очереди выполнения."
      }
    },
    {
      "id": "diagnose-free-vs-available",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Diagnose memory: free vs available", "ru": "Диагностика памяти: free против available" },
      "prompt": { "en": "Read free -h output and decide whether the system has a memory problem.", "ru": "Прочти вывод free -h и реши, есть ли у системы проблема с памятью." },
      "scenario": {
        "en": "<pre>             total   used   free  shared  buff/cache  available\nMem:          15G    14G    312M    200M        1.2G       1.5G\nSwap:          4G      0B     4G</pre>A junior engineer opens a ticket: 'Server is out of memory — only 312 MB free'. Is there really a memory problem?",
        "ru": "<pre>             total   used   free  shared  buff/cache  available\nMem:          15G    14G    312M    200M        1.2G       1.5G\nSwap:          4G      0B     4G</pre>Младший инженер открывает тикет: 'Серверу не хватает памяти — только 312 МБ свободно'. Есть ли реальная проблема с памятью?"
      },
      "reveal": {
        "en": "No memory problem. The <strong>available</strong> column shows 1.5 GB — that is what new processes can actually allocate. The 1.2 GB in buff/cache is Linux page cache (file data cached in RAM for performance); it is reclaimable instantly when a process needs memory. Swap usage is 0, confirming there is no pressure. The junior engineer alarmed on <code>free</code> (312 MB), which ignores reclaimable cache. Only worry when <strong>available</strong> drops below a few hundred MB <em>and</em> swap usage starts climbing.",
        "ru": "Проблемы с памятью нет. Столбец <strong>available</strong> показывает 1.5 ГБ — это то, что новые процессы реально могут выделить. 1.2 ГБ в buff/cache — кэш страниц Linux (данные файлов, кэшированные в RAM для производительности); они освобождаются мгновенно при необходимости. Использование swap = 0, подтверждает отсутствие давления. Младший инженер поднял тревогу из-за <code>free</code> (312 МБ), игнорируя освобождаемый кэш. Беспокойся только когда <strong>available</strong> падает ниже нескольких сотен МБ <em>и</em> использование swap начинает расти."
      }
    },
    {
      "id": "review-top-misuse",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Review: misreading top output during an incident", "ru": "Ревью: неверное прочтение вывода top во время инцидента" },
      "prompt": { "en": "An engineer's incident notes contain a faulty conclusion from top output. Identify the error.", "ru": "Заметки инженера об инциденте содержат ошибочный вывод из вывода top. Определи ошибку." },
      "diff": {
        "lang": "bash",
        "code": "# top output during incident:\n# %Cpu(s): 5.2 us, 1.1 sy, 0.0 ni, 14.3 id, 79.1 wa\n# load average: 12.4, 10.8, 8.2  (on an 8-core machine)\n#\n# Engineer's RCA note:\n# 'CPU was not the bottleneck — only 5.2% user CPU used.\n#  Load was high because many processes were created.\n#  Root cause: application spawning too many worker threads.'"
      },
      "findings": [
        {
          "id": "high-wa-means-io-bottleneck",
          "label": { "en": "79.1% wa (I/O wait) indicates an I/O bottleneck, not excessive process spawning", "ru": "79.1% wa (ожидание I/O) указывает на узкое место I/O, а не на избыточное создание процессов" },
          "severity": "bug",
          "explanation": {
            "en": "The <code>wa</code> field (79.1%) means CPU is idle but blocked waiting for I/O to complete — disk reads or writes are the bottleneck. Load average counts processes in both run state and <strong>uninterruptible I/O wait (D state)</strong>. Load 12.4 on 8 cores with low user CPU and high wa is the textbook signature of an I/O-bound system: many processes sitting in D state waiting for storage, driving up load average without consuming CPU. The RCA should have looked at disk I/O metrics (iostat, iotop) to find which device or process was causing the I/O pressure, not at thread counts.",
            "ru": "Поле <code>wa</code> (79.1%) означает, что CPU простаивает, ожидая завершения I/O — узким местом являются операции чтения/записи с диска. Средняя нагрузка считает процессы как в состоянии выполнения, так и в <strong>непрерываемом ожидании I/O (состояние D)</strong>. Нагрузка 12.4 на 8 ядер при низком пользовательском CPU и высоком wa — классический признак системы с узким местом I/O: многие процессы сидят в состоянии D, ожидая хранилища, увеличивая среднюю нагрузку без потребления CPU. Анализ первопричины должен был смотреть на метрики дискового I/O (iostat, iotop), чтобы найти устройство или процесс, вызывающий давление I/O, а не на количество потоков."
          },
          "planted": true
        }
      ]
    },
    {
      "id": "design-triage-checklist",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design a 5-command server triage sequence", "ru": "Спроектируй последовательность триажа сервера из 5 команд" },
      "prompt": { "en": "Design a minimal ordered sequence of 5 commands to triage a slow server, and explain what each command tells you.", "ru": "Спроектируй минимальную упорядоченную последовательность из 5 команд для триажа медленного сервера и объясни, что каждая команда показывает." },
      "constraints": { "en": "SSH access only. No monitoring dashboards. The server may be CPU-bound, I/O-bound, or memory-starved. Your 5 commands must distinguish between these three root causes.", "ru": "Только SSH-доступ. Без дашбордов мониторинга. Сервер может быть ограничен по CPU, I/O или памяти. Твои 5 команд должны различать эти три первопричины." },
      "rubric": [
        { "en": "Checks load average trend (uptime or top header).", "ru": "Проверяет тренд средней нагрузки (uptime или заголовок top)." },
        { "en": "Checks CPU breakdown including wa% to distinguish CPU vs I/O (top or vmstat).", "ru": "Проверяет разбивку CPU включая wa% для различения CPU vs I/O (top или vmstat)." },
        { "en": "Checks memory and swap pressure (free -h or vmstat).", "ru": "Проверяет давление памяти и swap (free -h или vmstat)." },
        { "en": "Identifies the top resource consumers by CPU and memory (ps aux --sort).", "ru": "Определяет главных потребителей ресурсов по CPU и памяти (ps aux --sort)." },
        { "en": "Checks disk usage to rule out full filesystem (df -h).", "ru": "Проверяет использование диска для исключения заполненной файловой системы (df -h)." }
      ],
      "model": {
        "en": "1. <code>uptime</code> — load trend: rising/falling, magnitude vs core count. 2. <code>top -bn1 | head -10</code> — CPU breakdown: high <code>us</code> = CPU-bound app; high <code>wa</code> = I/O bottleneck; high <code>sy</code> = kernel overhead. 3. <code>free -h</code> — memory: if available is low and swap is active (<code>vmstat 1</code> shows si/so > 0) → memory pressure. 4. <code>ps aux --sort=-%cpu | head; ps aux --sort=-%mem | head</code> — pinpoints which process is the culprit. 5. <code>df -h</code> — a full filesystem causes write stalls that manifest as high I/O wait even when the disk itself is fast.",
        "ru": "1. <code>uptime</code> — тренд нагрузки: растёт/падает, величина против числа ядер. 2. <code>top -bn1 | head -10</code> — разбивка CPU: высокий <code>us</code> = CPU-bound приложение; высокий <code>wa</code> = узкое место I/O; высокий <code>sy</code> = накладные расходы ядра. 3. <code>free -h</code> — память: если available низкий и swap активен (<code>vmstat 1</code> показывает si/so > 0) → давление памяти. 4. <code>ps aux --sort=-%cpu | head; ps aux --sort=-%mem | head</code> — определяет виновный процесс. 5. <code>df -h</code> — полная файловая система вызывает зависания записи, проявляющиеся как высокое ожидание I/O даже при быстром диске."
      }
    }
  ]
}
