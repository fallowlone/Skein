{
  "lessonKey": "cli/03-streams-and-pipes/03-pipes-and-grep",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-grep-filter",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What survives the grep filter?", "ru": "Что проходит через фильтр grep?" },
      "prompt": { "en": "Predict exactly which lines appear in the output.", "ru": "Точно предскажи, какие строки появятся в выводе." },
      "scenario": { "en": "Input stream (one line per entry):<br/><code>error: disk full<br/>info: started<br/>error: timeout<br/>warn: retry</code><br/>Command: <code>grep 'error'</code><br/>What is the output?", "ru": "Входной поток (одна запись на строку):<br/><code>error: disk full<br/>info: started<br/>error: timeout<br/>warn: retry</code><br/>Команда: <code>grep 'error'</code><br/>Каков вывод?" },
      "reveal": { "en": "<pre>error: disk full\nerror: timeout</pre>grep passes only lines that contain the literal string 'error'. The 'info' and 'warn' lines are discarded because they do not contain 'error'.", "ru": "<pre>error: disk full\nerror: timeout</pre>grep пропускает только строки, содержащие буквальную строку 'error'. Строки 'info' и 'warn' отбрасываются, потому что они не содержат 'error'." }
    },
    {
      "id": "diagnose-grep-exit-code",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Why does the script abort when grep finds nothing?", "ru": "Почему скрипт прерывается, когда grep ничего не находит?" },
      "prompt": { "en": "Fill in the blanks explaining the failure.", "ru": "Заполни пропуски, объясняя сбой." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["1", "non-zero", "exit code 1", "exit status 1", "ненулевой", "код выхода 1"],
            "hint": { "en": "grep exits with this status code when no lines match.", "ru": "grep завершается с этим кодом статуса, когда ни одна строка не совпала." }
          },
          {
            "id": "b2",
            "accept": ["set -e", "errexit", "-e", "|| true", "or true"],
            "hint": { "en": "The shell option that causes any non-zero exit to terminate the script.", "ru": "Опция оболочки, из-за которой любой ненулевой выход завершает скрипт." }
          }
        ]
      }
    },
    {
      "id": "review-pipe-chain",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the unnecessary grep stage", "ru": "Найди лишнюю стадию grep" },
      "prompt": { "en": "A developer wrote a pipeline to find running nginx processes. Find the inefficiency.", "ru": "Разработчик написал конвейер для поиска запущенных процессов nginx. Найди неэффективность." },
      "diff": {
        "lang": "bash",
        "code": "ps aux | grep nginx | grep nginx | grep -v grep"
      },
      "findings": [
        {
          "id": "duplicate-grep-nginx",
          "label": { "en": "grep nginx is repeated twice — one stage is redundant", "ru": "grep nginx повторяется дважды — одна стадия избыточна" },
          "severity": "bug",
          "explanation": { "en": "The second <code>grep nginx</code> receives lines that already matched 'nginx' from the first stage — running it again produces the same result. It adds a process, a pipe buffer, and scheduling overhead with no benefit. The correct pipeline is: <code>ps aux | grep nginx | grep -v grep</code>.", "ru": "Второй <code>grep nginx</code> получает строки, которые уже совпали с 'nginx' на первой стадии — повторный запуск даёт тот же результат. Это добавляет процесс, буфер канала и накладные расходы на планирование без какой-либо пользы. Правильный конвейер: <code>ps aux | grep nginx | grep -v grep</code>." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-multi-stage-filter",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Build a multi-stage filter pipeline", "ru": "Построй многоступенчатый конвейер фильтрации" },
      "prompt": { "en": "Design a pipeline that answers the question, then compare with the model answer.", "ru": "Спроектируй конвейер, отвечающий на вопрос, затем сравни с образцом." },
      "constraints": { "en": "You have <code>/var/log/auth.log</code>. Find all lines that mention 'Failed password' but exclude lines from the IP address '192.168.1.1' (a known monitoring host that generates benign noise). Print a count of the remaining lines.", "ru": "У тебя есть <code>/var/log/auth.log</code>. Найди все строки, упоминающие 'Failed password', но исключи строки с IP-адреса '192.168.1.1' (известный хост мониторинга, генерирующий безвредный шум). Выведи количество оставшихся строк." },
      "rubric": [
        { "en": "Uses grep 'Failed password' to select relevant lines", "ru": "Использует grep 'Failed password' для выбора нужных строк" },
        { "en": "Uses grep -v '192.168.1.1' to exclude the monitoring host", "ru": "Использует grep -v '192.168.1.1' для исключения хоста мониторинга" },
        { "en": "Pipes into wc -l to count the remaining lines", "ru": "Передаёт в wc -l для подсчёта оставшихся строк" }
      ],
      "model": { "en": "<code>grep 'Failed password' /var/log/auth.log | grep -v '192.168.1.1' | wc -l</code>. Stage 1 selects only failure lines. Stage 2 removes the monitor's noise. Stage 3 counts. Each stage has one job; the pipe composes them.", "ru": "<code>grep 'Failed password' /var/log/auth.log | grep -v '192.168.1.1' | wc -l</code>. Стадия 1 выбирает только строки с ошибками. Стадия 2 убирает шум монитора. Стадия 3 считает. У каждой стадии одна задача; конвейер их компонует." }
    }
  ]
}
