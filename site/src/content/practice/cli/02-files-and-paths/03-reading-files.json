{
  "lessonKey": "cli/02-files-and-paths/03-reading-files",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-cat-vs-less-large-file",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What happens when you cat a 500 MB log?", "ru": "Что происходит при cat 500 МБ лога?" },
      "prompt": { "en": "Predict the practical problem with this command on a large file.", "ru": "Предскажи практическую проблему с этой командой на большом файле." },
      "scenario": { "en": "A log file <code>/var/log/app.log</code> is 500 MB and contains 4 million lines. A junior developer runs: <code>cat /var/log/app.log</code>. What happens and what should they use instead?", "ru": "Файл лога <code>/var/log/app.log</code> имеет размер 500 МБ и содержит 4 миллиона строк. Начинающий разработчик запускает: <code>cat /var/log/app.log</code>. Что происходит и что следует использовать вместо этого?" },
      "reveal": { "en": "The terminal is flooded with 4 million lines scrolling at maximum speed. The terminal buffer likely cannot hold them all, so earlier lines are lost. The shell appears frozen while cat runs. Memory may spike if the terminal emulator buffers the output. The right tool is <code>less /var/log/app.log</code>: it reads only enough to fill the screen, is safe on files of any size, and lets you search with <code>/</code> and navigate with arrow keys.", "ru": "Терминал переполняется 4 миллионами строк, прокручивающихся с максимальной скоростью. Буфер терминала, вероятно, не может вместить их все, поэтому ранние строки теряются. Оболочка кажется замороженной пока cat работает. Память может резко вырасти, если терминальный эмулятор буферизует вывод. Правильный инструмент — <code>less /var/log/app.log</code>: он читает ровно столько, чтобы заполнить экран, безопасен для файлов любого размера и позволяет искать по <code>/</code> и навигировать стрелками." }
    },
    {
      "id": "predict-tail-f-behavior",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What does tail -f do when a new line is written?", "ru": "Что делает tail -f при записи новой строки?" },
      "prompt": { "en": "Predict exactly what appears in the terminal as the log is written to.", "ru": "Предскажи точно, что появится в терминале по мере записи в лог." },
      "scenario": { "en": "You run <code>tail -f /var/log/nginx/access.log</code>. The file currently has 1000 lines. Ten seconds later, nginx handles a request and appends one new line to the file. What happens in your terminal?", "ru": "Ты запускаешь <code>tail -f /var/log/nginx/access.log</code>. Файл сейчас содержит 1000 строк. Через десять секунд nginx обрабатывает запрос и добавляет одну новую строку в файл. Что происходит в твоём терминале?" },
      "reveal": { "en": "When you first run the command, you see the last 10 lines of the file (the default). Then the command blocks — your terminal appears to wait. When nginx writes the new line 10 seconds later, that new line immediately appears on your terminal. <code>tail -f</code> keeps the file open and uses kernel notifications (inotify on Linux) to detect new data without polling. The terminal updates in real time. Press Ctrl-C to exit.", "ru": "При первом запуске команды ты видишь последние 10 строк файла (по умолчанию). Затем команда блокируется — твой терминал как будто ожидает. Когда nginx записывает новую строку через 10 секунд, эта новая строка немедленно появляется в твоём терминале. <code>tail -f</code> держит файл открытым и использует уведомления ядра (inotify на Linux) для обнаружения новых данных без опроса. Терминал обновляется в реальном времени. Нажми Ctrl-C для выхода." }
    },
    {
      "id": "diagnose-head-tail-blanks",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Which tool for which slice?", "ru": "Какой инструмент для какого фрагмента?" },
      "prompt": { "en": "Fill in the command names that match each description.", "ru": "Заполни имена команд, соответствующие каждому описанию." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["head", "head -n"],
            "hint": { "en": "This command prints the first N lines of a file and exits.", "ru": "Эта команда выводит первые N строк файла и завершается." }
          },
          {
            "id": "b2",
            "accept": ["less"],
            "hint": { "en": "This pager lets you scroll through a large file without loading it all into memory. Quit with q.", "ru": "Этот пейджер позволяет прокручивать большой файл без загрузки его целиком в память. Выход по q." }
          },
          {
            "id": "b3",
            "accept": ["tail -f", "tail --follow"],
            "hint": { "en": "This command keeps a file open and streams new lines as they are appended — used for watching live logs.", "ru": "Эта команда держит файл открытым и выводит новые строки по мере их добавления — используется для наблюдения за живыми логами." }
          }
        ]
      }
    },
    {
      "id": "review-cat-binary-file",
      "type": "review",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Spot the wrong tool for a binary file", "ru": "Найди неправильный инструмент для бинарного файла" },
      "prompt": { "en": "A developer wrote a diagnostic script. Find the problem.", "ru": "Разработчик написал диагностический скрипт. Найди проблему." },
      "diff": {
        "lang": "bash",
        "code": "#!/bin/bash\n# check-binary.sh — inspect an uploaded file\nUPLOADED=\"/tmp/user-upload\"\necho \"File contents:\"\ncat \"$UPLOADED\""
      },
      "findings": [
        {
          "id": "cat-on-unknown-binary",
          "label": { "en": "Using cat on an untrusted/unknown file can corrupt the terminal with binary control characters", "ru": "Использование cat на ненадёжном/неизвестном файле может испортить терминал бинарными управляющими символами" },
          "severity": "bug",
          "explanation": { "en": "The script accepts an arbitrary uploaded file and pipes it through <code>cat</code>. If the upload is a binary (executable, image, compressed archive), <code>cat</code> will dump raw bytes to the terminal. Many of those bytes are interpreted as ANSI control sequences, which can scramble the display, clear the screen, change colors, or worse. The fix: check the file type first with <code>file \"$UPLOADED\"</code> and only <code>cat</code> it if it reports a text type. For unknown files, use <code>hexdump -C</code> or <code>xxd</code> to inspect safely.", "ru": "Скрипт принимает произвольный загруженный файл и направляет его через <code>cat</code>. Если загрузка бинарная (исполняемый файл, изображение, сжатый архив), <code>cat</code> выгрузит сырые байты в терминал. Многие из этих байтов интерпретируются как управляющие последовательности ANSI, которые могут исказить отображение, очистить экран, изменить цвета или хуже. Исправление: сначала проверь тип файла с помощью <code>file \"$UPLOADED\"</code> и используй <code>cat</code> только если сообщается текстовый тип. Для неизвестных файлов используй <code>hexdump -C</code> или <code>xxd</code> для безопасного просмотра." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-log-investigation-workflow",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design a log investigation workflow", "ru": "Спроектируй рабочий процесс исследования лога" },
      "prompt": { "en": "Design a step-by-step terminal workflow to investigate an unknown log file efficiently without risking terminal corruption.", "ru": "Спроектируй пошаговый терминальный рабочий процесс для эффективного исследования неизвестного лог-файла без риска повреждения терминала." },
      "constraints": { "en": "The log file path is <code>/var/log/app/service.log</code>. You do not know its size. You want to: (1) safely determine the file size, (2) check the format, (3) browse it without risk, (4) find lines containing ERROR, (5) watch it live.", "ru": "Путь к файлу лога — <code>/var/log/app/service.log</code>. Размер неизвестен. Нужно: (1) безопасно определить размер файла, (2) проверить формат, (3) просмотреть без риска, (4) найти строки с ERROR, (5) наблюдать в реальном времени." },
      "rubric": [
        { "en": "Checks file size (e.g. wc -l or ls -lh) before opening.", "ru": "Проверяет размер файла (например, wc -l или ls -lh) перед открытием." },
        { "en": "Uses less (not cat) to browse the file safely.", "ru": "Использует less (не cat) для безопасного просмотра файла." },
        { "en": "Searches within less using / or uses grep for ERROR.", "ru": "Ищет внутри less с помощью / или использует grep для ERROR." },
        { "en": "Uses tail -f for live following.", "ru": "Использует tail -f для наблюдения в реальном времени." }
      ],
      "model": { "en": "```bash\n# 1. Check size before opening\nwc -l /var/log/app/service.log\nls -lh /var/log/app/service.log\n\n# 2. Browse safely — not cat\nless /var/log/app/service.log\n# Inside less: press / then type ERROR to search\n\n# 3. Extract matching lines\ngrep 'ERROR' /var/log/app/service.log | tail -n 20\n\n# 4. Watch live\ntail -f /var/log/app/service.log\n# Press Ctrl-C to stop\n```\n\nKey principle: never use cat on a file whose size you do not know. Use wc -l or ls -lh first; if it is large, use less.", "ru": "```bash\n# 1. Проверить размер перед открытием\nwc -l /var/log/app/service.log\nls -lh /var/log/app/service.log\n\n# 2. Безопасный просмотр — не cat\nless /var/log/app/service.log\n# Внутри less: нажми / затем введи ERROR для поиска\n\n# 3. Извлечь совпадающие строки\ngrep 'ERROR' /var/log/app/service.log | tail -n 20\n\n# 4. Наблюдать в реальном времени\ntail -f /var/log/app/service.log\n# Нажми Ctrl-C для остановки\n```\n\nКлючевой принцип: никогда не используй cat на файле, размер которого не знаешь. Сначала используй wc -l или ls -lh; если он большой, используй less." }
    }
  ]
}
