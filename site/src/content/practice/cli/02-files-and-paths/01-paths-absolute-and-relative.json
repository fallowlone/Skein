{
  "lessonKey": "cli/02-files-and-paths/01-paths-absolute-and-relative",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-relative-path-resolution",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Where does this relative path resolve?", "ru": "Куда разрешается этот относительный путь?" },
      "prompt": { "en": "Trace the path resolution and predict the absolute path the shell opens.", "ru": "Проследи разрешение пути и предскажи абсолютный путь, который откроет оболочка." },
      "scenario": { "en": "Working directory is <code>/home/bob/projects</code>. You run: <code>cat ../docs/readme.txt</code>. What file does the shell actually open?", "ru": "Рабочий каталог — <code>/home/bob/projects</code>. Ты запускаешь: <code>cat ../docs/readme.txt</code>. Какой файл на самом деле откроет оболочка?" },
      "reveal": { "en": "<code>/home/bob/docs/readme.txt</code>. The relative path <code>../docs/readme.txt</code> is prepended with the working directory: <code>/home/bob/projects</code> + <code>/..</code> = <code>/home/bob</code>, then + <code>/docs/readme.txt</code> = <code>/home/bob/docs/readme.txt</code>.", "ru": "<code>/home/bob/docs/readme.txt</code>. Относительный путь <code>../docs/readme.txt</code> дополняется рабочим каталогом: <code>/home/bob/projects</code> + <code>/..</code> = <code>/home/bob</code>, затем + <code>/docs/readme.txt</code> = <code>/home/bob/docs/readme.txt</code>." }
    },
    {
      "id": "diagnose-absolute-relative-blanks",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Fill in: absolute vs relative", "ru": "Заполни пропуски: абсолютный vs относительный" },
      "prompt": { "en": "Fill in the blanks based on what you learned about path types.", "ru": "Заполни пропуски на основе того, что ты узнал о типах путей." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["absolute", "absolute path", "абсолютный", "абсолютный путь"],
            "hint": { "en": "This type of path always starts with / and works from any working directory.", "ru": "Этот тип пути всегда начинается с / и работает из любого рабочего каталога." }
          },
          {
            "id": "b2",
            "accept": ["~", "tilde", "тильда"],
            "hint": { "en": "This special character expands to your home directory, e.g. /home/alice.", "ru": "Этот специальный символ раскрывается в твой домашний каталог, например /home/alice." }
          },
          {
            "id": "b3",
            "accept": ["..", "two dots", "double dot", "две точки"],
            "hint": { "en": "This notation refers to the parent directory of the current directory.", "ru": "Эта нотация обозначает родительский каталог текущего каталога." }
          }
        ]
      }
    },
    {
      "id": "review-relative-in-script",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the fragile path in a script", "ru": "Найди хрупкий путь в скрипте" },
      "prompt": { "en": "This deploy script is called from a CI system that may run it from any directory. Find the bug.", "ru": "Этот скрипт деплоя вызывается из CI-системы, которая может запустить его из любого каталога. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "#!/bin/bash\n# deploy.sh — called from CI\ncp config/production.env /etc/myapp/.env\necho \"Config deployed\""
      },
      "findings": [
        {
          "id": "relative-path-in-script",
          "label": { "en": "Relative path config/production.env breaks when script is called from a different directory", "ru": "Относительный путь config/production.env ломается при вызове скрипта из другого каталога" },
          "severity": "bug",
          "explanation": { "en": "The path <code>config/production.env</code> is relative to whatever directory the caller's shell is in when the script runs — not to where the script file lives. If CI calls <code>./deploy.sh</code> from <code>/</code> or <code>/tmp</code>, the shell looks for <code>/config/production.env</code> or <code>/tmp/config/production.env</code>, neither of which exists. Fix: use <code>$(dirname \"$0\")/config/production.env</code> to anchor the path to the script's own directory, or use an absolute path.", "ru": "Путь <code>config/production.env</code> относителен к тому каталогу, в котором находится оболочка вызывающей стороны во время запуска скрипта — а не к тому, где живёт файл скрипта. Если CI вызывает <code>./deploy.sh</code> из <code>/</code> или <code>/tmp</code>, оболочка ищет <code>/config/production.env</code> или <code>/tmp/config/production.env</code>, ни одного из которых не существует. Исправление: используй <code>$(dirname \"$0\")/config/production.env</code>, чтобы привязать путь к собственному каталогу скрипта, или используй абсолютный путь." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-portable-path-strategy",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design a portable path strategy for a shell script", "ru": "Спроектируй стратегию переносимых путей для shell-скрипта" },
      "prompt": { "en": "A shell script needs to read a config file that lives in the same directory as the script. Design a solution that works regardless of where the script is invoked from.", "ru": "Shell-скрипту нужно читать файл конфигурации, находящийся в том же каталоге, что и скрипт. Спроектируй решение, которое работает независимо от того, откуда вызывается скрипт." },
      "constraints": { "en": "The script is at <code>/opt/myapp/run.sh</code>. The config is at <code>/opt/myapp/config.env</code>. The script must work when called as <code>./run.sh</code> from <code>/opt/myapp</code>, as <code>/opt/myapp/run.sh</code> from anywhere, and as <code>bash /opt/myapp/run.sh</code> from <code>/tmp</code>.", "ru": "Скрипт находится по адресу <code>/opt/myapp/run.sh</code>. Конфиг — по адресу <code>/opt/myapp/config.env</code>. Скрипт должен работать при вызове как <code>./run.sh</code> из <code>/opt/myapp</code>, как <code>/opt/myapp/run.sh</code> из любого места, и как <code>bash /opt/myapp/run.sh</code> из <code>/tmp</code>." },
      "rubric": [
        { "en": "Does not use a bare relative path like config.env.", "ru": "Не использует голый относительный путь вроде config.env." },
        { "en": "Anchors the config path to the script's own directory, not the caller's CWD.", "ru": "Привязывает путь к конфигу к собственному каталогу скрипта, а не к CWD вызывающей стороны." },
        { "en": "Works when called from any working directory.", "ru": "Работает при вызове из любого рабочего каталога." }
      ],
      "model": { "en": "<code>SCRIPT_DIR=$(dirname \"$(realpath \"$0\")\")\nsource \"$SCRIPT_DIR/config.env\"</code>. <code>dirname \"$0\"</code> gives the directory part of the script's path; <code>realpath</code> resolves any symlinks so the result is always an absolute path. Using <code>$SCRIPT_DIR</code> as the base means the config is found regardless of where the script was invoked. An alternative without <code>realpath</code>: <code>SCRIPT_DIR=$(cd \"$(dirname \"$0\")\" && pwd)</code> — <code>cd</code> into the directory then <code>pwd</code> to get the absolute path.", "ru": "<code>SCRIPT_DIR=$(dirname \"$(realpath \"$0\")\")\nsource \"$SCRIPT_DIR/config.env\"</code>. <code>dirname \"$0\"</code> даёт каталог пути скрипта; <code>realpath</code> разрешает все символические ссылки, поэтому результат всегда абсолютный путь. Использование <code>$SCRIPT_DIR</code> как базы означает, что конфиг найдётся независимо от того, откуда был вызван скрипт. Альтернатива без <code>realpath</code>: <code>SCRIPT_DIR=$(cd \"$(dirname \"$0\")\" && pwd)</code> — <code>cd</code> в каталог, затем <code>pwd</code> для получения абсолютного пути." }
    }
  ]
}
