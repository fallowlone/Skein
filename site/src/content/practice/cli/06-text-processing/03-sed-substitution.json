{
  "lessonKey": "cli/06-text-processing/03-sed-substitution",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-sed-global-flag",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Predict sed with and without g flag", "ru": "Предскажи sed с флагом g и без него" },
      "prompt": { "en": "Predict the output of both commands.", "ru": "Предскажи вывод обеих команд." },
      "scenario": { "en": "Command A: <code>echo 'foo bar foo baz foo' | sed 's/foo/X/'</code><br>Command B: <code>echo 'foo bar foo baz foo' | sed 's/foo/X/g'</code><br><br>What does each print?", "ru": "Команда A: <code>echo 'foo bar foo baz foo' | sed 's/foo/X/'</code><br>Команда B: <code>echo 'foo bar foo baz foo' | sed 's/foo/X/g'</code><br><br>Что выводит каждая?" },
      "reveal": { "en": "Command A: <code>X bar foo baz foo</code>. Without <code>g</code>, sed replaces only the first match per line. The second and third <code>foo</code> are untouched. Command B: <code>X bar X baz X</code>. The <code>g</code> (global) flag replaces every occurrence on the line. Forgetting <code>g</code> is the most common sed mistake.", "ru": "Команда A: <code>X bar foo baz foo</code>. Без <code>g</code> sed заменяет только первое совпадение в строке. Второй и третий <code>foo</code> остаются нетронутыми. Команда B: <code>X bar X baz X</code>. Флаг <code>g</code> (глобальный) заменяет каждое вхождение в строке. Забыть <code>g</code> — самая распространённая ошибка в sed." }
    },
    {
      "id": "review-sed-slash-delimiter",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the bug: sed replacing a URL path", "ru": "Найди ошибку: sed при замене пути URL" },
      "prompt": { "en": "A script updates an API base URL in a config file. Find the bug.", "ru": "Скрипт обновляет базовый URL API в конфигурационном файле. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "# Replace old API endpoint with new one in config.yaml\nsed -i 's/https://api.old.com/v1/https://api.new.com/v2/g' config.yaml"
      },
      "findings": [
        {
          "id": "sed-delimiter-clash-with-url-slashes",
          "label": { "en": "Slashes in the URL pattern clash with the sed / delimiter, breaking the substitution", "ru": "Слэши в шаблоне URL конфликтуют с разделителем / sed, ломая замену" },
          "severity": "bug",
          "explanation": { "en": "sed uses <code>/</code> as the command delimiter by default. When the pattern or replacement contains <code>/</code>, sed misinterprets them as delimiter boundaries and produces a syntax error or wrong substitution. The fix is to choose a delimiter that does not appear in the strings — typically <code>|</code> or <code>#</code>: <code>sed -i 's|https://api.old.com/v1|https://api.new.com/v2|g' config.yaml</code>. Any non-alphanumeric character can serve as the sed delimiter.", "ru": "sed по умолчанию использует <code>/</code> как разделитель команды. Когда шаблон или замена содержат <code>/</code>, sed неправильно интерпретирует их как границы разделителей и выдаёт синтаксическую ошибку или неверную замену. Исправление — выбрать разделитель, которого нет в строках, обычно <code>|</code> или <code>#</code>: <code>sed -i 's|https://api.old.com/v1|https://api.new.com/v2|g' config.yaml</code>. Любой неалфавитно-цифровой символ может служить разделителем sed." },
          "planted": true
        }
      ]
    },
    {
      "id": "review-sed-i-macos",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the portability bug: sed -i on macOS", "ru": "Найди баг переносимости: sed -i на macOS" },
      "prompt": { "en": "A deploy script runs fine on Linux CI but fails on a developer's macOS machine. Find the portability bug.", "ru": "Скрипт деплоя работает нормально в Linux CI, но падает на macOS разработчика. Найди баг переносимости." },
      "diff": {
        "lang": "bash",
        "code": "#!/bin/bash\n# Patch version string in package manifest\nVERSION=\"2.1.0\"\nsed -i \"s/version: .*/version: $VERSION/\" manifest.yaml"
      },
      "findings": [
        {
          "id": "sed-i-bsd-requires-empty-extension",
          "label": { "en": "BSD sed on macOS requires an explicit backup extension argument after -i, even if empty", "ru": "BSD sed на macOS требует явного аргумента расширения резервной копии после -i, даже если он пустой" },
          "severity": "bug",
          "explanation": { "en": "GNU sed (Linux) treats <code>sed -i</code> with no argument as 'edit in place with no backup'. BSD sed (macOS) interprets the next token after <code>-i</code> as the backup extension — so <code>sed -i \"s/...\"</code> treats <code>\"s/...\"</code> as the extension, producing the error <code>sed: 1: \"manifest.yaml\": invalid command code m</code>. The portable fix: <code>sed -i '' \"s/version: .*/version: $VERSION/\" manifest.yaml</code> — the empty string <code>''</code> is the explicit 'no backup' extension that both GNU and BSD sed accept correctly.", "ru": "GNU sed (Linux) воспринимает <code>sed -i</code> без аргумента как «редактировать на месте без резервной копии». BSD sed (macOS) интерпретирует следующий токен после <code>-i</code> как расширение резервной копии — поэтому <code>sed -i \"s/...\"</code> воспринимает <code>\"s/...\"</code> как расширение, выдавая ошибку <code>sed: 1: \"manifest.yaml\": invalid command code m</code>. Переносимое исправление: <code>sed -i '' \"s/version: .*/version: $VERSION/\" manifest.yaml</code> — пустая строка <code>''</code> — явное расширение 'без резервной копии', которое корректно принимают и GNU и BSD sed." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-sed-address-range",
      "type": "design",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Design a sed command for a config section", "ru": "Спроектируй команду sed для секции конфига" },
      "prompt": { "en": "Design a sed command that replaces 'debug' with 'info' only within the [logging] section of an INI file, not in other sections. Compare with the model answer.", "ru": "Спроектируй команду sed, заменяющую 'debug' на 'info' только внутри секции [logging] INI-файла, а не в других секциях. Сравни с образцом." },
      "constraints": { "en": "The file has sections like [logging], [database], [server]. Each section starts with a [name] header on its own line. The replacement must not touch lines in [database] or [server] sections even if they contain 'debug'.", "ru": "Файл содержит секции [logging], [database], [server]. Каждая секция начинается с заголовка [name] на отдельной строке. Замена не должна затрагивать строки в секциях [database] или [server], даже если они содержат 'debug'." },
      "rubric": [
        { "en": "Uses a /pattern/,/pattern/ range address to restrict the substitution to the [logging] section.", "ru": "Использует адрес диапазона /шаблон/,/шаблон/ для ограничения замены секцией [logging]." },
        { "en": "The range ends at the next section header (a line starting with [) so changes stop before [database] or [server].", "ru": "Диапазон заканчивается на следующем заголовке секции (строка начинается с [), чтобы изменения останавливались перед [database] или [server]." },
        { "en": "Uses the g flag so all occurrences of 'debug' on each line in the range are replaced.", "ru": "Использует флаг g, чтобы заменялись все вхождения 'debug' в каждой строке диапазона." }
      ],
      "model": { "en": "<code>sed '/^\\[logging\\]/,/^\\[/{s/debug/info/g}' app.ini</code>. The address <code>/^\\[logging\\]/,/^\\[/</code> selects from the line matching <code>[logging]</code> to the next line starting with <code>[</code> (the next section header). The substitution <code>s/debug/info/g</code> runs only within that range. Note: the range is inclusive of both boundary lines, so the next <code>[</code> header line is also in scope — if that matters, add a <code>!/^\\[/</code> guard inside the block.", "ru": "<code>sed '/^\\[logging\\]/,/^\\[/{s/debug/info/g}' app.ini</code>. Адрес <code>/^\\[logging\\]/,/^\\[/</code> выбирает от строки, совпадающей с <code>[logging]</code>, до следующей строки, начинающейся с <code>[</code> (следующий заголовок секции). Замена <code>s/debug/info/g</code> выполняется только в этом диапазоне. Примечание: диапазон включает обе граничные строки, поэтому строка следующего заголовка <code>[</code> тоже попадает в область — если это важно, добавь защиту <code>!/^\\[/</code> внутри блока." }
    }
  ]
}
