{
  "lessonKey": "cli/06-text-processing/04-awk-essentials",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-awk-nr-nf",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Predict NR and NF values", "ru": "Предскажи значения NR и NF" },
      "prompt": { "en": "Trace the awk program and predict the output.", "ru": "Проследи программу awk и предскажи вывод." },
      "scenario": { "en": "Input file has exactly two lines:<br><code>alice admin 1024</code><br><code>bob dev</code><br><br>You run: <code>awk '{print NR, NF}' file.txt</code>. What is the output?", "ru": "Входной файл содержит ровно две строки:<br><code>alice admin 1024</code><br><code>bob dev</code><br><br>Ты запускаешь: <code>awk '{print NR, NF}' file.txt</code>. Каков вывод?" },
      "reveal": { "en": "<code>1 3</code> then <code>2 2</code>. For the first record <code>alice admin 1024</code>: NR=1 (first line), NF=3 (three whitespace-separated fields). For the second record <code>bob dev</code>: NR=2 (second line), NF=2 (two fields). NF can differ per record — awk recounts fields for each line.", "ru": "<code>1 3</code>, затем <code>2 2</code>. Для первой записи <code>alice admin 1024</code>: NR=1 (первая строка), NF=3 (три поля, разделённые пробелами). Для второй записи <code>bob dev</code>: NR=2 (вторая строка), NF=2 (два поля). NF может различаться для каждой записи — awk пересчитывает поля для каждой строки." }
    },
    {
      "id": "review-awk-unquoted-program",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the bug: unquoted awk program", "ru": "Найди ошибку: программа awk без кавычек" },
      "prompt": { "en": "A developer wrote an awk one-liner to sum the third column. Find the bug.", "ru": "Разработчик написал awk однострочник для суммирования третьей колонки. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "# Sum the bytes column (field 3) in transfer.log\nawk {total += $3} END {print total} transfer.log"
      },
      "findings": [
        {
          "id": "awk-program-must-be-quoted",
          "label": { "en": "The awk program must be wrapped in single quotes to prevent the shell from interpreting $3 and the braces", "ru": "Программа awk должна быть обёрнута в одинарные кавычки, чтобы оболочка не интерпретировала $3 и фигурные скобки" },
          "severity": "bug",
          "explanation": { "en": "Without quotes, the shell processes the awk program before awk sees it. <code>{total += $3}</code> — the shell expands <code>$3</code> to the third positional parameter of the current shell (probably empty), and <code>{</code> and <code>}</code> trigger brace expansion or a syntax error. The correct form wraps the entire program in single quotes: <code>awk '{total += $3} END {print total}' transfer.log</code>. Single quotes prevent all shell interpretation inside, so awk receives the literal <code>$3</code> and processes it as a field reference.", "ru": "Без кавычек оболочка обрабатывает программу awk до того, как awk её увидит. <code>{total += $3}</code> — оболочка раскрывает <code>$3</code> в третий позиционный параметр текущей оболочки (вероятно, пустой), а <code>{</code> и <code>}</code> вызывают раскрытие скобок или синтаксическую ошибку. Правильная форма оборачивает всю программу в одинарные кавычки: <code>awk '{total += $3} END {print total}' transfer.log</code>. Одинарные кавычки предотвращают любую интерпретацию оболочкой внутри, поэтому awk получает литеральное <code>$3</code> и обрабатывает его как ссылку на поле." },
          "planted": true
        }
      ]
    },
    {
      "id": "review-awk-missing-end-block",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the bug: accumulator never printed", "ru": "Найди ошибку: аккумулятор никогда не выводится" },
      "prompt": { "en": "A script computes the total request duration from a log but produces no output. Find the bug.", "ru": "Скрипт вычисляет суммарную длительность запросов из лога, но не производит вывода. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "# Total request duration from field 5 of requests.log\nawk 'NR > 1 { total += $5 }' requests.log\necho \"Done\""
      },
      "findings": [
        {
          "id": "awk-missing-end-block-for-summary",
          "label": { "en": "The accumulator total is computed correctly but never printed — an END block is missing", "ru": "Аккумулятор total вычисляется правильно, но никогда не выводится — отсутствует блок END" },
          "severity": "bug",
          "explanation": { "en": "The awk program accumulates <code>total</code> correctly across all records, but when awk finishes reading input it exits silently — there is no <code>END</code> block to print the result. The variable exists in awk's memory but is discarded when awk exits. The fix: <code>awk 'NR > 1 { total += $5 } END { print \"Total:\", total }' requests.log</code>. The <code>END</code> block runs once after all input is consumed and is the correct place for summary output.", "ru": "Программа awk корректно накапливает <code>total</code> по всем записям, но когда awk заканчивает чтение ввода, она тихо завершается — нет блока <code>END</code> для вывода результата. Переменная существует в памяти awk, но отбрасывается при выходе. Исправление: <code>awk 'NR > 1 { total += $5 } END { print \"Total:\", total }' requests.log</code>. Блок <code>END</code> выполняется один раз после потребления всего ввода и является правильным местом для итогового вывода." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-awk-per-key-summary",
      "type": "design",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Design an awk program to count by HTTP method", "ru": "Спроектируй программу awk для подсчёта по HTTP-методу" },
      "prompt": { "en": "Design an awk one-liner that reads an nginx access log and prints a count for each HTTP method (GET, POST, PUT, DELETE, etc.), sorted by count descending. The HTTP method is field 6 in Combined Log Format (it appears as '\"GET' including the opening quote). Compare with the model answer.", "ru": "Спроектируй awk однострочник, читающий лог доступа nginx и выводящий счётчик для каждого HTTP-метода (GET, POST, PUT, DELETE и т.д.), отсортированный по убыванию. HTTP-метод — поле 6 в Combined Log Format (появляется как '\"GET', включая открывающую кавычку). Сравни с образцом." },
      "constraints": { "en": "Use only awk for the counting and aggregation. You may pipe to sort at the end. No grep, sed, cut, or uniq.", "ru": "Используй только awk для подсчёта и агрегации. Можно добавить пайп в sort в конце. Без grep, sed, cut или uniq." },
      "rubric": [
        { "en": "Uses an associative array keyed by the method field (e.g. method[$6]++).", "ru": "Использует ассоциативный массив с ключом по полю метода (например, method[$6]++)." },
        { "en": "Prints each key and its count in the END block.", "ru": "Выводит каждый ключ и его счётчик в блоке END." },
        { "en": "Pipes to sort -rn to rank by count descending.", "ru": "Пайпит в sort -rn для ранжирования по убыванию счётчика." }
      ],
      "model": { "en": "<code>awk '{method[$6]++} END {for (m in method) print method[m], m}' access.log | sort -rn</code>. Field 6 in Combined Log Format is the HTTP method with the opening double-quote (e.g. <code>\"GET</code>). The associative array <code>method[$6]++</code> counts occurrences per method token. The <code>END</code> block iterates the array and prints count + method. <code>sort -rn</code> ranks by count descending. If you want to strip the leading quote: <code>awk '{m=substr($6,2); method[m]++} END {for (k in method) print method[k], k}' access.log | sort -rn</code>.", "ru": "<code>awk '{method[$6]++} END {for (m in method) print method[m], m}' access.log | sort -rn</code>. Поле 6 в Combined Log Format — HTTP-метод с открывающей двойной кавычкой (например, <code>\"GET</code>). Ассоциативный массив <code>method[$6]++</code> подсчитывает вхождения по токену метода. Блок <code>END</code> итерирует массив и выводит счётчик + метод. <code>sort -rn</code> ранжирует по убыванию счётчика. Если нужно убрать ведущую кавычку: <code>awk '{m=substr($6,2); method[m]++} END {for (k in method) print method[k], k}' access.log | sort -rn</code>." }
    }
  ]
}
