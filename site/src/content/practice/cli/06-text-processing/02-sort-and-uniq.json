{
  "lessonKey": "cli/06-text-processing/02-sort-and-uniq",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-sort-lexical-vs-numeric",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Lexical vs numeric sort", "ru": "Лексическая vs численная сортировка" },
      "prompt": { "en": "Predict the output of each command — they differ.", "ru": "Предскажи вывод каждой команды — они различаются." },
      "scenario": { "en": "You run two commands:<br><br>Command A: <code>printf '10\\n9\\n100\\n2\\n' | sort</code><br>Command B: <code>printf '10\\n9\\n100\\n2\\n' | sort -n</code><br><br>What is the output of each?", "ru": "Ты запускаешь две команды:<br><br>Команда A: <code>printf '10\\n9\\n100\\n2\\n' | sort</code><br>Команда B: <code>printf '10\\n9\\n100\\n2\\n' | sort -n</code><br><br>Каков вывод каждой?" },
      "reveal": { "en": "Command A (lexical): <code>10 / 100 / 2 / 9</code>. Strings are compared character by character: '1' &lt; '2' &lt; '9', so 10 and 100 both start with '1' and sort before 2 and 9. Command B (numeric): <code>2 / 9 / 10 / 100</code>. Numbers are compared as values. This is the most common sort footgun — always use <code>-n</code> for numeric columns.", "ru": "Команда A (лексическая): <code>10 / 100 / 2 / 9</code>. Строки сравниваются посимвольно: '1' &lt; '2' &lt; '9', поэтому 10 и 100, начинаясь с '1', сортируются перед 2 и 9. Команда B (численная): <code>2 / 9 / 10 / 100</code>. Числа сравниваются как значения. Это самая распространённая ловушка sort — всегда используй <code>-n</code> для числовых колонок." }
    },
    {
      "id": "sandbox-top-n-frequency",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Model the sort | uniq -c | sort -rn pipeline in JS", "ru": "Смоделируй конвейер sort | uniq -c | sort -rn на JS" },
      "prompt": { "en": "The array below represents log level keywords extracted from an application log — one entry per log line. Implement the sort | uniq -c | sort -rn idiom in synchronous JS: count each unique word, sort by count descending, and print 'count word' per line. Confirm the exact stdout matches the expected output.", "ru": "Массив ниже представляет ключевые слова уровня лога, извлечённые из лога приложения — по одному на строку. Реализуй идиому sort | uniq -c | sort -rn на синхронном JS: подсчитай каждое уникальное слово, отсортируй по убыванию счётчика и выведи 'счётчик слово' на строку. Убедись, что точный stdout совпадает с ожидаемым выводом." },
      "runtime": "js",
      "setup": "const words = [\"error\",\"warn\",\"error\",\"info\",\"error\",\"warn\",\"info\",\"error\",\"debug\",\"warn\"];",
      "initialCode": "const counts = {};\nfor (const w of words) {\n  // TODO: count each unique word\n}\n\nconst ranked = Object.entries(counts).sort((a, b) => {\n  // TODO: count descending, then word ascending\n  return 0;\n});\n\nfor (const [word, count] of ranked) {\n  console.log(count + \" \" + word);\n}",
      "expected": { "kind": "stdout-equals", "value": "4 error\n3 warn\n2 info\n1 debug" }
    },
    {
      "id": "review-uniq-without-sort",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the bug: uniq -c without sort", "ru": "Найди ошибку: uniq -c без sort" },
      "prompt": { "en": "A developer wrote a pipeline to count HTTP status codes in an access log. Find the bug.", "ru": "Разработчик написал конвейер для подсчёта HTTP-кодов в логе доступа. Найди ошибку." },
      "diff": {
        "lang": "bash",
        "code": "# Count HTTP status codes, most frequent first\nawk '{print $9}' access.log | uniq -c | sort -rn"
      },
      "findings": [
        {
          "id": "uniq-requires-sorted-input",
          "label": { "en": "uniq -c without prior sort gives wrong counts when identical codes are non-adjacent", "ru": "uniq -c без предварительного sort даёт неверные счётчики, когда одинаковые коды не соседствуют" },
          "severity": "bug",
          "explanation": { "en": "<code>uniq -c</code> only collapses consecutive identical lines. In an access log, status codes like <code>200</code> appear throughout the file interspersed with other codes — they are not adjacent. Each non-adjacent run of 200 gets its own count of 1 (or however many appear in a row). The fix is to sort first: <code>awk '{print $9}' access.log | sort | uniq -c | sort -rn</code>. The first <code>sort</code> brings all 200s together so <code>uniq -c</code> counts them as one group.", "ru": "<code>uniq -c</code> сворачивает только соседние одинаковые строки. В логе доступа коды вроде <code>200</code> появляются по всему файлу, перемежаясь другими кодами — они не соседствуют. Каждая несмежная серия из 200 получает свой счётчик 1 (или сколько их идёт подряд). Исправление — сортировать сначала: <code>awk '{print $9}' access.log | sort | uniq -c | sort -rn</code>. Первый <code>sort</code> собирает все 200 вместе, чтобы <code>uniq -c</code> посчитал их как одну группу." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-top-ips",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design the top-10 IPs pipeline", "ru": "Спроектируй конвейер топ-10 IP-адресов" },
      "prompt": { "en": "Design a pipeline that reads an nginx access log and prints the top 10 IP addresses by request count, most frequent first. The IP is the first whitespace-separated field on each line. Compare with the model answer.", "ru": "Спроектируй конвейер, читающий лог доступа nginx и выводящий топ-10 IP-адресов по числу запросов, самые частые первыми. IP — первое поле, разделённое пробелами, на каждой строке. Сравни с образцом." },
      "constraints": { "en": "Use awk or cut for field extraction, then sort, uniq -c, sort -rn, and head. No grep, sed, or external tools.", "ru": "Используй awk или cut для извлечения поля, затем sort, uniq -c, sort -rn и head. Без grep, sed или внешних инструментов." },
      "rubric": [
        { "en": "Extracts field 1 (the IP) from each log line.", "ru": "Извлекает поле 1 (IP) из каждой строки лога." },
        { "en": "Sorts before uniq -c to group identical IPs.", "ru": "Сортирует перед uniq -c для группировки одинаковых IP." },
        { "en": "Uses sort -rn on the uniq -c output to rank by count descending.", "ru": "Использует sort -rn на выводе uniq -c для ранжирования по убыванию счётчика." },
        { "en": "Pipes to head -10 to limit the output.", "ru": "Пайпит в head -10 для ограничения вывода." }
      ],
      "model": { "en": "<code>awk '{print $1}' access.log | sort | uniq -c | sort -rn | head -10</code>. Alternatively <code>cut -d' ' -f1</code> works for simple log formats where the IP is always the first space-separated token. The four-stage pipeline (extract → group → count → rank) is the universal frequency-analysis template.", "ru": "<code>awk '{print $1}' access.log | sort | uniq -c | sort -rn | head -10</code>. Альтернативно <code>cut -d' ' -f1</code> работает для простых форматов лога, где IP всегда первый токен, разделённый пробелами. Четырёхэтапный конвейер (извлечь → сгруппировать → подсчитать → ранжировать) — универсальный шаблон частотного анализа." }
    }
  ]
}
