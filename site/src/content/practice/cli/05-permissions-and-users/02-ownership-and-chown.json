{
  "lessonKey": "cli/05-permissions-and-users/02-ownership-and-chown",
  "track": "cli",
  "tasks": [
    {
      "id": "predict-which-triplet",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Which rwx triplet applies?", "ru": "Какая тройка rwx применяется?" },
      "prompt": { "en": "Trace the kernel's three-step ownership check and predict which triplet is used.", "ru": "Проследи трёхшаговую проверку владения ядра и предскажи, какая тройка применяется." },
      "scenario": { "en": "File: <code>-rw-r----- 1 deploy www-data 1024 app.conf</code>. Process user: <code>bob</code>, groups: <code>staff, www-data</code>. What triplet does the kernel apply and what access does bob have?", "ru": "Файл: <code>-rw-r----- 1 deploy www-data 1024 app.conf</code>. Пользователь процесса: <code>bob</code>, группы: <code>staff, www-data</code>. Какую тройку применяет ядро и какой доступ получает bob?" },
      "reveal": { "en": "The kernel applies the <strong>group triplet</strong> (<code>r--</code>). Step 1: bob's UID ≠ deploy → skip. Step 2: bob is in www-data which matches the file group → use group triplet <code>r--</code>. Bob can read but not write.", "ru": "Ядро применяет тройку <strong>group</strong> (<code>r--</code>). Шаг 1: UID bob ≠ deploy → пропуск. Шаг 2: bob в группе www-data, которая совпадает с группой файла → использовать тройку group <code>r--</code>. Bob может читать, но не писать." }
    },
    {
      "id": "diagnose-403-ownership",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Diagnose a 403 caused by ownership", "ru": "Диагностируй 403, вызванный владением" },
      "prompt": { "en": "Fill in what is wrong and what command fixes it.", "ru": "Укажи, что не так, и какая команда это исправляет." },
      "scenario": { "en": "File: <code>-rw-r----- 1 root root 512 config.yml</code>. The nginx process runs as <code>www-data</code> (not in the root group). nginx returns 403 when serving a page that reads this file. The permissions look fine (640). What is the real problem and what is the one-command fix?", "ru": "Файл: <code>-rw-r----- 1 root root 512 config.yml</code>. Процесс nginx работает как <code>www-data</code> (не в группе root). nginx возвращает 403 при обслуживании страницы, читающей этот файл. Права выглядят нормально (640). В чём реальная проблема и какова однострочная команда для исправления?" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["wrong group", "wrong ownership", "group is root", "group root", "неправильная группа", "неправильное владение", "группа root"],
            "hint": { "en": "www-data is not the owner and not in the root group, so the kernel applies the 'other' triplet (---). The permissions digit is correct but the group is wrong.", "ru": "www-data не является владельцем и не входит в группу root, поэтому ядро применяет тройку 'other' (---). Цифра прав верна, но группа неправильная." }
          },
          {
            "id": "b2",
            "accept": ["sudo chown root:www-data config.yml", "chown root:www-data config.yml", "sudo chown :www-data config.yml", "chown :www-data config.yml"],
            "hint": { "en": "Change the group to www-data so the nginx process matches the group triplet (r--) instead of the other triplet (---).", "ru": "Измени группу на www-data, чтобы процесс nginx совпадал с тройкой group (r--) вместо тройки other (---)." }
          }
        ]
      }
    },
    {
      "id": "predict-owner-triplet-edge-case",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Owner triplet edge case", "ru": "Крайний случай тройки owner" },
      "prompt": { "en": "Predict what access the file owner gets, even though it seems counter-intuitive.", "ru": "Предскажи, какой доступ получает владелец файла, даже если это кажется нелогичным." },
      "scenario": { "en": "File: <code>----rwxrwx 1 alice devs 200 data.txt</code> (mode 077). User alice tries to read the file. What happens?", "ru": "Файл: <code>----rwxrwx 1 alice devs 200 data.txt</code> (режим 077). Пользователь alice пытается прочитать файл. Что произойдёт?" },
      "reveal": { "en": "Alice is <strong>denied</strong>. The kernel matches alice's UID to the owner → applies the user triplet <code>---</code>. The kernel stops at the first match — it does not fall through to the group or other triplet even though those grant rwx. Being the owner is not always an advantage.", "ru": "Alice получает <strong>отказ</strong>. Ядро сопоставляет UID alice с владельцем → применяет тройку user <code>---</code>. Ядро останавливается на первом совпадении — оно не переходит к тройке group или other, даже если те дают rwx. Быть владельцем не всегда преимущество." }
    },
    {
      "id": "design-multi-user-project",
      "type": "design",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Design ownership for a shared project directory", "ru": "Спроектируй владение для общего каталога проекта" },
      "prompt": { "en": "Design the chown and chmod commands that satisfy all constraints, then compare with the model.", "ru": "Спроектируй команды chown и chmod, удовлетворяющие всем ограничениям, затем сравни с образцом." },
      "constraints": { "en": "Directory <code>/srv/project</code>: user <code>alice</code> is the lead dev and needs full control. The <code>devs</code> group needs read+execute (to list and enter). All other users must have no access. Files created inside should follow the same pattern.", "ru": "Каталог <code>/srv/project</code>: пользователь <code>alice</code> — ведущий разработчик и нуждается в полном контроле. Группа <code>devs</code> должна иметь чтение+выполнение (для просмотра и входа). Все остальные пользователи не должны иметь доступа. Файлы, создаваемые внутри, должны следовать той же схеме." },
      "rubric": [
        { "en": "chown alice:devs /srv/project sets correct owner and group.", "ru": "chown alice:devs /srv/project устанавливает правильного владельца и группу." },
        { "en": "chmod 750 /srv/project gives alice rwx, devs r-x, others ---.", "ru": "chmod 750 /srv/project даёт alice rwx, devs r-x, others ---." },
        { "en": "Other users are completely excluded (rightmost digit 0).", "ru": "Все остальные пользователи полностью исключены (крайняя правая цифра 0)." }
      ],
      "model": { "en": "<code>sudo chown alice:devs /srv/project && sudo chmod 750 /srv/project</code>. Mode 750: alice (owner) gets rwx=7; devs (group) get r-x=5; others get ---=0. For new files inside, alice can set a umask of 027 in her shell profile to ensure files default to 640 and directories to 750.", "ru": "<code>sudo chown alice:devs /srv/project && sudo chmod 750 /srv/project</code>. Режим 750: alice (владелец) получает rwx=7; devs (группа) получают r-x=5; остальные получают ---=0. Для новых файлов внутри alice может установить umask 027 в профиле оболочки, чтобы файлы по умолчанию создавались с режимом 640, а каталоги — 750." }
    }
  ]
}
