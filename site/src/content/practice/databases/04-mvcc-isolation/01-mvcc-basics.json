{
  "lessonKey": "databases/04-mvcc-isolation/01-mvcc-basics",
  "track": "databases",
  "tasks": [
    {
      "id": "diagnose-xmin-xmax-visibility",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Name the bookkeeping fields behind tuple visibility", "ru": "Назови служебные поля, на которых держится видимость кортежа" },
      "prompt": { "en": "Every heap tuple in Postgres carries two transaction-id stamps that decide whether a snapshot can see it. Fill in the blanks with the system column names and the visibility rule.", "ru": "Каждый кортеж в куче Postgres несёт две метки transaction-id, которые решают, видит ли его снапшот. Заполни пропуски именами системных колонок и правилом видимости." },
      "evidence": { "en": "SELECT xmin, xmax, balance FROM accounts WHERE id = 7;\n  xmin  | xmax | balance\n--------+------+---------\n   910  |    0 |     100   <- live version\n   880  |  910 |      40   <- superseded version", "ru": "SELECT xmin, xmax, balance FROM accounts WHERE id = 7;\n  xmin  | xmax | balance\n--------+------+---------\n   910  |    0 |     100   <- live version\n   880  |  910 |      40   <- superseded version" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "creator", "accept": ["xmin", "xmin column"], "hint": { "en": "The id of the transaction that INSERTed (created) this row version.", "ru": "Id транзакции, которая INSERT'нула (создала) эту версию строки." } },
          { "id": "deleter", "accept": ["xmax", "xmax column"], "hint": { "en": "The id of the transaction that DELETEd or UPDATEd this version away (0 means still live).", "ru": "Id транзакции, которая DELETE'нула или UPDATE'нула эту версию (0 означает, что она ещё живая)." } },
          { "id": "rule", "accept": ["xmin committed and not visible to no other", "xmin committed before snapshot and xmax not committed", "xmin committed xmax not committed", "xmin committed and xmax not committed or not visible", "xmin visible and xmax not visible"], "hint": { "en": "A tuple is visible when its xmin is committed-and-in-snapshot AND its xmax is either zero or NOT committed-in-snapshot.", "ru": "Кортеж видим, когда его xmin закоммичен-и-в-снапшоте И его xmax либо ноль, либо НЕ закоммичен-в-снапшоте." } }
        ]
      }
    },
    {
      "id": "predict-uncommitted-write-invisible",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Can a reader see a concurrent uncommitted write?", "ru": "Увидит ли читатель параллельную незакоммиченную запись?" },
      "prompt": { "en": "Predict what transaction A reads, then reveal the snapshot-isolation reasoning.", "ru": "Предскажи, что прочитает транзакция A, затем открой рассуждение про snapshot isolation." },
      "scenario": { "en": "Transaction A (READ COMMITTED) runs `SELECT balance FROM accounts WHERE id = 7;` and gets 100. Meanwhile transaction B updates that row to 250 but has NOT committed yet. While B is still open, A runs the exact same SELECT again. What balance does A see on the second read — 100, 250, or does it block waiting for B?", "ru": "Транзакция A (READ COMMITTED) выполняет `SELECT balance FROM accounts WHERE id = 7;` и получает 100. Тем временем транзакция B обновляет эту строку на 250, но ещё НЕ закоммитилась. Пока B открыта, A выполняет тот же SELECT снова. Какой баланс увидит A на втором чтении — 100, 250 или заблокируется в ожидании B?" },
      "reveal": { "en": "A reads 100 and does not block. B's new tuple version has xmin = B's transaction id, and B has not committed, so that version is invisible to A's snapshot — uncommitted data is never visible to other transactions (no dirty reads). A keeps seeing the old committed version. A only sees 250 if B commits AND (under READ COMMITTED) A takes a fresh snapshot on a later statement. Crucially, A never waits: readers don't block on a writer's in-flight tuple, because the old version is still right there to read.", "ru": "A читает 100 и не блокируется. Новая версия кортежа B имеет xmin = id транзакции B, а B не закоммитилась, поэтому эта версия невидима для снапшота A — незакоммиченные данные никогда не видны другим транзакциям (нет грязных чтений). A продолжает видеть старую закоммиченную версию. A увидит 250, только если B закоммитится И (при READ COMMITTED) A возьмёт свежий снапшот на следующем операторе. Главное: A никогда не ждёт — читатели не блокируются на незавершённом кортеже писателя, ведь старая версия всё ещё доступна для чтения." }
    },
    {
      "id": "sandbox-tuple-visibility-checker",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 9,
      "title": { "en": "Implement the tuple visibility rule in JS", "ru": "Реализуй правило видимости кортежа на JS" },
      "prompt": { "en": "A simplified MVCC visibility check. Each tuple has { xmin, xmax }. A snapshot is the set of transaction ids that were COMMITTED at snapshot time (xmax === 0 means the tuple was never deleted). A tuple is VISIBLE when: its xmin is in the committed set AND (xmax === 0 OR xmax is NOT in the committed set). Implement `isVisible(tuple, committed)` and print, in heap order, the balance of each visible tuple — one per line. The starter prints nothing yet; complete it so the output matches exactly.", "ru": "Упрощённая проверка видимости MVCC. У каждого кортежа есть { xmin, xmax }. Снапшот — это множество id транзакций, ЗАКОММИЧЕННЫХ на момент снапшота (xmax === 0 означает, что кортеж не удалялся). Кортеж ВИДИМ, когда: его xmin есть в множестве закоммиченных И (xmax === 0 ИЛИ xmax НЕ в множестве закоммиченных). Реализуй `isVisible(tuple, committed)` и выведи в порядке кучи баланс каждого видимого кортежа — по одному на строку. Стартовый код пока ничего не печатает; доведи его, чтобы вывод совпал точно." },
      "runtime": "js",
      "setup": "// Heap of row versions for account id=7, in physical order.\nconst heap = [\n  { xmin: 700, xmax: 880, balance: 40 },  // created by 700, deleted by 880\n  { xmin: 880, xmax: 910, balance: 70 },  // created by 880, deleted by 910\n  { xmin: 910, xmax: 0,   balance: 100 }, // created by 910, still live\n  { xmin: 999, xmax: 0,   balance: 250 }, // created by 999 (uncommitted)\n];\n// Transactions committed and inside this snapshot:\nconst committed = new Set([700, 880, 910]); // 999 is NOT committed\n\nfunction isVisible(tuple, committed) {\n  // TODO: return true when the tuple is visible to this snapshot\n}\n\nfor (const t of heap) {\n  if (isVisible(t, committed)) console.log(t.balance);\n}",
      "expected": { "kind": "stdout-equals", "value": "100\n" }
    },
    {
      "id": "fix-lost-update",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Close the lost-update gap MVCC leaves open", "ru": "Закрой брешь lost update, которую MVCC оставляет открытой" },
      "prompt": { "en": "This read-modify-write on a balance is correct under a single user but silently loses money under concurrency. Explain why MVCC alone does not save it, then write the fix. Reveal the model answer after.", "ru": "Этот read-modify-write по балансу корректен для одного пользователя, но тихо теряет деньги при конкуренции. Объясни, почему MVCC сам по себе не спасает, затем напиши фикс. Открой образцовый ответ после." },
      "grading": {
        "mode": "self",
        "model": { "en": "MVCC gives each transaction a consistent snapshot, but it does NOT serialize blind writes: two transactions can both read balance=100, both compute 100+X on their own snapshot, and both write — the second commit overwrites the first, and one deposit vanishes. Snapshots don't conflict at the row level, so nothing errors. Fixes: (1) Pessimistic — `SELECT balance FROM accounts WHERE id=7 FOR UPDATE;` inside the txn takes a row lock so the second reader waits until the first commits, then re-reads the fresh value. (2) Atomic write — skip the read entirely: `UPDATE accounts SET balance = balance + 50 WHERE id=7;` computes against the current row, not a stale snapshot. (3) Stricter isolation — `REPEATABLE READ`/`SERIALIZABLE` makes Postgres abort one txn with a serialization failure on the conflicting write, and the app retries. The atomic UPDATE is the simplest correct fix here.", "ru": "MVCC даёт каждой транзакции согласованный снапшот, но НЕ сериализует слепые записи: две транзакции могут обе прочитать balance=100, обе посчитать 100+X на своём снапшоте и обе записать — второй коммит затирает первый, и одно пополнение исчезает. Снапшоты не конфликтуют на уровне строки, поэтому ошибки нет. Фиксы: (1) Пессимистично — `SELECT balance FROM accounts WHERE id=7 FOR UPDATE;` внутри транзакции берёт блокировку строки, так что второй читатель ждёт коммита первого и перечитывает свежее значение. (2) Атомарная запись — вообще без чтения: `UPDATE accounts SET balance = balance + 50 WHERE id=7;` считает по текущей строке, а не по устаревшему снапшоту. (3) Строже изоляция — `REPEATABLE READ`/`SERIALIZABLE` заставит Postgres откатить одну транзакцию с serialization failure на конфликтующей записи, и приложение делает retry. Атомарный UPDATE — простейший корректный фикс здесь." },
        "rubric": [
          { "en": "States that MVCC isolates snapshots but does not serialize concurrent blind writes, so no error is raised.", "ru": "Утверждает, что MVCC изолирует снапшоты, но не сериализует параллельные слепые записи, поэтому ошибка не возникает." },
          { "en": "Offers at least one correct fix: SELECT ... FOR UPDATE, an atomic balance = balance + X UPDATE, or a stricter isolation level with retry.", "ru": "Предлагает минимум один корректный фикс: SELECT ... FOR UPDATE, атомарный UPDATE вида balance = balance + X или строгий уровень изоляции с retry." },
          { "en": "Explains why the chosen fix actually serializes the writes (lock waits, current-row arithmetic, or serialization-failure retry).", "ru": "Объясняет, почему выбранный фикс действительно сериализует записи (ожидание блокировки, арифметика по текущей строке или retry при serialization failure)." }
        ]
      }
    }
  ]
}
