{
  "lessonKey": "js-engine/06-garbage-collection/05-memory-leaks",
  "track": "js-engine",
  "tasks": [
    {
      "id": "incident-rss-growth",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Incident: a Node service RSS grows 40 MB/day", "ru": "Инцидент: RSS Node-сервиса растёт на 40 МБ/день" },
      "prompt": { "en": "A Node API starts at 180 MB RSS and climbs ~40 MB/day, eventually OOM-restarting weekly. No errors. Walk the diagnosis to root cause and fix.", "ru": "Node-API стартует с 180 МБ RSS и набирает ~40 МБ/день, в итоге еженедельно перезапускаясь по OOM. Ошибок нет. Пройди диагностику до корневой причины и лечения." },
      "steps": [
        { "label": { "en": "Is it a leak or fragmentation?", "ru": "Это утечка или фрагментация?" }, "prompt": { "en": "RSS climbs. What two metrics distinguish a true retention leak from fragmentation, and which do you check first?", "ru": "RSS лезет вверх. Какие две метрики отличают настоящую утечку удержания от фрагментации и какую проверить первой?" }, "reveal": { "en": "Compare process.memoryUsage().heapUsed against rss. If heapUsed climbs in lockstep with rss, live JS objects are accumulating — a retention leak. If rss stays high while heapUsed is flat/low, the heap is fragmented (objects collected but pages held) — a different problem (lesson 03 compaction). Here heapUsed climbs too, so it is a retention leak.", "ru": "Сравните process.memoryUsage().heapUsed с rss. Если heapUsed лезет в ногу с rss, живые JS-объекты накапливаются — утечка удержания. Если rss остаётся высоким, а heapUsed плоский/низкий, куча фрагментирована (объекты собраны, но страницы удержаны) — другая проблема (compaction, урок 03). Здесь heapUsed тоже лезет, значит это утечка удержания." } },
        { "label": { "en": "Find what is growing", "ru": "Найди, что растёт" }, "prompt": { "en": "How do you find which object type is accumulating?", "ru": "Как найти, какой тип объектов накапливается?" }, "reveal": { "en": "Connect via node --inspect, open Chrome DevTools Memory panel. Take a heap snapshot, exercise the service, take a second and third snapshot. Use the three-snapshot diff (Objects allocated between snapshots 1 and 2 that are still alive at snapshot 3 are the leak). Sort by retained size / object count. Suppose 'Closure' and an internal 'Map entry' count grows unbounded.", "ru": "Подключитесь через node --inspect, откройте панель Memory в Chrome DevTools. Снимите heap snapshot, погоняйте сервис, снимите второй и третий снимок. Используйте diff трёх снимков (объекты, аллоцированные между снимками 1 и 2 и всё ещё живые в снимке 3, — это утечка). Сортируйте по retained size / числу объектов. Допустим, число 'Closure' и внутренних 'Map entry' растёт неограниченно." } },
        { "label": { "en": "Read the retainer path", "ru": "Прочитай retainer path" }, "prompt": { "en": "The growing objects all hang off one structure. What does the retainer path reveal?", "ru": "Все растущие объекты висят на одной структуре. Что вскрывает retainer path?" }, "reveal": { "en": "The retainer path runs: GC root → module-scoped `const responseCache = new Map()` → entry → request context closure → response buffer. The cache is keyed by request id and never evicted, so every request's context (and its captured buffer) is retained forever. The Map is the unintended root-anchored retainer.", "ru": "Retainer path идёт: корень GC → модульный `const responseCache = new Map()` → запись → замыкание контекста запроса → буфер ответа. Кэш ключуется id запроса и никогда не выселяется, поэтому контекст каждого запроса (и его захваченный буфер) удерживается навсегда. Map — это непреднамеренный удержатель, заякоренный за корень." } },
        { "label": { "en": "Fix and verify", "ru": "Почини и проверь" }, "prompt": { "en": "What is the correct fix, and how do you confirm it?", "ru": "Каково правильное лечение и как его подтвердить?" }, "reveal": { "en": "Give the cache a real eviction policy: an LRU with a max size or a TTL, so old entries drop and their subtrees become unreachable. If the key is an object you control, a WeakMap would auto-evict, but request ids are strings, so LRU/TTL is right. Do NOT rely on WeakRef/FinalizationRegistry to bound it. Verify: redeploy, take snapshots over hours, confirm the Map entry count plateaus and heapUsed/rss stabilize. Add a perf-CI check that fails on unbounded object-count growth after a soak.", "ru": "Дайте кэшу настоящую политику eviction: LRU с max size или TTL, чтобы старые записи отпадали и их поддеревья становились недостижимыми. Если ключ — контролируемый вами объект, WeakMap авто-выселял бы, но id запросов — строки, поэтому правильно LRU/TTL. НЕ опирайтесь на WeakRef/FinalizationRegistry, чтобы его ограничить. Проверка: передеплойте, снимайте снимки в течение часов, подтвердите, что число записей Map выходит на плато, а heapUsed/rss стабилизируются. Добавьте perf-CI проверку, падающую на неограниченном росте числа объектов после прогона." } }
      ]
    },
    {
      "id": "predict-which-leaks",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Predict which snippet leaks", "ru": "Предскажи, какой фрагмент течёт" },
      "prompt": { "en": "Predict which of these leak under repeated calls and why: (A) <code>setInterval(() =&gt; tick(state), 1000)</code> with no clear; (B) <code>node.addEventListener('click', h)</code> with <code>removeEventListener</code> on cleanup; (C) <code>const c = new Map(); c.set(id, ctx)</code> with no eviction; (D) a WeakMap keyed by a node, node removed.", "ru": "Предскажи, что из этого течёт при повторных вызовах и почему: (A) <code>setInterval(() =&gt; tick(state), 1000)</code> без очистки; (B) <code>node.addEventListener('click', h)</code> с <code>removeEventListener</code> при очистке; (C) <code>const c = new Map(); c.set(id, ctx)</code> без eviction; (D) WeakMap, ключуемый узлом, узел удалён." },
      "scenario": { "en": "A leak is unintended retention: a reference from a root keeps a logically-dead object reachable. Weak references are ignored by reachability.", "ru": "Утечка — это непреднамеренное удержание: ссылка от корня держит логически мёртвый объект достижимым. Слабые ссылки игнорируются достижимостью." },
      "reveal": { "en": "<strong>A leaks</strong> — the uncleared interval retains its callback and the captured state forever. <strong>B does not</strong> — the listener (and its closure) is released on removeEventListener. <strong>C leaks</strong> — the module-scoped Map is a root and never evicts, retaining every ctx. <strong>D does not</strong> — a WeakMap holds the key weakly, so once the node is otherwise unreferenced the entry is collectible. A and C share the same shape: a root-anchored container that never lets go.", "ru": "<strong>A течёт</strong> — неочищенный интервал удерживает свой колбэк и захваченный state навсегда. <strong>B нет</strong> — слушатель (и его замыкание) освобождается на removeEventListener. <strong>C течёт</strong> — модульный Map — это корень и никогда не выселяет, удерживая каждый ctx. <strong>D нет</strong> — WeakMap держит ключ слабо, поэтому как только узел больше ничем не ссылается, запись собираема. A и C одной формы: заякоренный за корень контейнер, который никогда не отпускает." }
    },
    {
      "id": "sandbox-lru-evict",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Bound a growing cache with LRU eviction", "ru": "Ограничь растущий кэш через LRU eviction" },
      "prompt": { "en": "Model a bounded LRU cache (max 3 entries) backed by a Map. Insert keys a,b,c,d,e in order, evicting the least-recently-used when over capacity. After all inserts, report the surviving keys (insertion order) and how many were evicted. Print exactly the line below.", "ru": "Смоделируй ограниченный LRU-кэш (макс 3 записи) на Map. Вставь ключи a,b,c,d,e по порядку, выселяя наименее недавно использованный при превышении ёмкости. После всех вставок сообщи выжившие ключи (порядок вставки) и сколько было выселено. Напечатай ровно строку ниже." },
      "runtime": "js",
      "starter": "const MAX = 3;\nconst cache = new Map();\nlet evicted = 0;\nfunction set(key, val) {\n  if (cache.has(key)) cache.delete(key);\n  cache.set(key, val);\n  if (cache.size > MAX) {\n    const oldest = cache.keys().next().value;  // least-recently-used\n    cache.delete(oldest);\n    evicted++;\n  }\n}\nfor (const k of ['a', 'b', 'c', 'd', 'e']) set(k, k.toUpperCase());\nconsole.log('survivors=' + [...cache.keys()].join(',') + ' evicted=' + evicted);\n",
      "expected": { "kind": "stdout-equals", "value": "survivors=c,d,e evicted=2" }
    },
    {
      "id": "diagnose-leak-names",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Name the retainer behind each symptom", "ru": "Назови удержателя за каждым симптомом" },
      "prompt": { "en": "Fill in the specific retainer for each symptom.", "ru": "Заполни конкретного удержателя для каждого симптома." },
      "evidence": { "en": "1. A tiny 10-char substring is keeping a 4 MB string alive in the heap.\n2. Removed DOM elements still show up in snapshots, referenced from a JS array.\n3. RSS is high but heapUsed is flat and low.", "ru": "1. Крошечная подстрока в 10 символов держит 4-МБ строку живой в куче.\n2. Удалённые элементы DOM всё ещё в снимках, ссылаемые из JS-массива.\n3. RSS высок, но heapUsed плоский и низкий." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["sliced string", "slicedstring", "string slice", "нарезанная строка", "slicedstring (нарезанная строка)", "срез строки"], "hint": { "en": "slice() that references the parent instead of copying.", "ru": "slice(), который ссылается на родителя вместо копирования." } },
          { "id": "b2", "accept": ["detached dom", "detached dom nodes", "detached nodes", "отсоединённый dom", "detached dom (отсоединённый dom)", "отсоединённые узлы"], "hint": { "en": "Removed from the document but still referenced from JS.", "ru": "Удалены из документа, но всё ещё ссылаемы из JS." } },
          { "id": "b3", "accept": ["fragmentation", "heap fragmentation", "not a leak", "фрагментация", "фрагментация кучи", "не утечка"], "hint": { "en": "Collected, but pages not returned — this is not a retention leak.", "ru": "Собрано, но страницы не возвращены — это не утечка удержания." } }
        ]
      }
    }
  ]
}
