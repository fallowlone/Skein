{
  "lessonKey": "js-engine/02-values-and-memory/02-smis-and-doubles",
  "track": "js-engine",
  "tasks": [
    {
      "id": "predict-smi-vs-heapnumber",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Smi or HeapNumber: predict the box", "ru": "Smi или HeapNumber: предскажи коробку" },
      "prompt": { "en": "For each value, predict whether V8 keeps it inline as a <strong>Smi</strong> or must box it as a <strong>HeapNumber</strong> holding a double: <code>0</code>, <code>1073741823</code> (about 2³⁰), <code>2147483648</code> (2³¹), <code>0.5</code>, <code>NaN</code>.", "ru": "Для каждого значения предскажи, держит ли V8 его внутри как <strong>Smi</strong> или обязан упаковать как <strong>HeapNumber</strong>, хранящий double: <code>0</code>, <code>1073741823</code> (около 2³⁰), <code>2147483648</code> (2³¹), <code>0.5</code>, <code>NaN</code>." },
      "scenario": { "en": "A Smi is a 31-bit signed integer (about ±2³⁰). Outside that range, fractional, or special (NaN/Infinity/-0), the number is a double and gets boxed as a HeapNumber.", "ru": "Smi — 31-битное знаковое целое (около ±2³⁰). За пределами этого диапазона, дробное или особое (NaN/Infinity/-0) число — это double и упаковывается как HeapNumber." },
      "reveal": { "en": "<strong>Smi:</strong> <code>0</code> and <code>1073741823</code> — both small integers inside ±2³⁰. <strong>HeapNumber:</strong> <code>2147483648</code> (2³¹ overflows the Smi range), <code>0.5</code> (fractional), and <code>NaN</code> (a special double). The boundary sits just above 2³⁰, not 2³¹: the 31st bit is the sign.", "ru": "<strong>Smi:</strong> <code>0</code> и <code>1073741823</code> — оба малые целые внутри ±2³⁰. <strong>HeapNumber:</strong> <code>2147483648</code> (2³¹ переполняет диапазон Smi), <code>0.5</code> (дробное) и <code>NaN</code> (особый double). Граница чуть выше 2³⁰, а не 2³¹: 31-й бит — это знак." }
    },
    {
      "id": "sandbox-smi-range-check",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Model the Smi-range fits check", "ru": "Смоделируй проверку «влезает ли в диапазон Smi»" },
      "prompt": { "en": "Model V8's decision: a number is Smi-eligible only if it is an integer inside the 31-bit signed range (about ±2³⁰). Classify a fixed list and count how many would stay Smis vs become HeapNumbers. Make the program print the exact line below.", "ru": "Смоделируй решение V8: число подходит под Smi, только если это целое внутри 31-битного знакового диапазона (около ±2³⁰). Классифицируй фиксированный список и посчитай, сколько останется Smi, а сколько станет HeapNumber. Заставь программу напечатать ровно строку ниже." },
      "runtime": "js",
      "initialCode": "// 31-bit signed Smi range on 64-bit V8.\nconst SMI_MIN = -(2 ** 30);\nconst SMI_MAX = (2 ** 30) - 1;\n\nfunction isSmi(n) {\n  return Number.isInteger(n) && n >= SMI_MIN && n <= SMI_MAX;\n}\n\nconst values = [0, 42, -1000000000, 2 ** 30, 2 ** 31, 0.5, NaN, Infinity];\n\nlet smi = 0, heap = 0;\nfor (const v of values) {\n  if (isSmi(v)) smi++;\n  else heap++;\n}\nconsole.log('smi=' + smi + ' heapnumber=' + heap);\n",
      "expected": { "kind": "stdout-equals", "value": "smi=3 heapnumber=5" }
    },
    {
      "id": "diagnose-overflow-deopt",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Read the trace: name the deopt cause", "ru": "Прочитай трейс: назови причину деопта" },
      "prompt": { "en": "A hot reducer regressed. The trace below is the evidence. Fill in the blanks with the exact mechanism.", "ru": "Горячий редьюсер просел. Трейс ниже — улика. Заполни пропуски точным механизмом." },
      "evidence": { "en": "$ node --trace-deopt --trace-opt hash.js\n[marking 0x... <JSFunction hash> for optimization]\n[optimizing: hash]\n...\n[deoptimizing (lazy): begin hash @23 (Smi overflow / not a Smi)]\n// accumulator value at deopt: 2243946017  (> 2**30)\n// after: hash runs through Ignition, allocating HeapNumbers per iteration", "ru": "$ node --trace-deopt --trace-opt hash.js\n[marking 0x... <JSFunction hash> for optimization]\n[optimizing: hash]\n...\n[deoptimizing (lazy): begin hash @23 (Smi overflow / not a Smi)]\n// значение аккумулятора при деопте: 2243946017  (> 2**30)\n// после: hash идёт через Ignition, аллоцируя HeapNumber на итерацию" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["smi overflow", "overflow", "smi-range overflow", "exceeded smi range", "переполнение smi", "переполнение", "выход за диапазон smi"], "hint": { "en": "The accumulator crossed ~2³⁰.", "ru": "Аккумулятор перешёл ~2³⁰." } },
          { "id": "b2", "accept": ["deopt", "deoptimization", "deoptimisation", "bailout", "деопт", "деоптимизация", "откат"], "hint": { "en": "What the trace line beginning 'deoptimizing' names.", "ru": "Что называет строка трейса, начинающаяся с 'deoptimizing'." } },
          { "id": "b3", "accept": ["heapnumber", "heap number", "boxed double", "heapnumber allocation", "heapnumber", "упакованный double", "аллокация heapnumber"], "hint": { "en": "What each iteration now allocates instead of using an inline Smi.", "ru": "Что теперь аллоцирует каждая итерация вместо инлайн-Smi." } }
        ]
      }
    },
    {
      "id": "fix-bound-the-accumulator",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 9,
      "title": { "en": "Keep the hash accumulator a Smi", "ru": "Удержи аккумулятор хеша в Smi" },
      "prompt": { "en": "This string-hash function overflows the Smi range within a few characters, deopting the hot loop and allocating HeapNumbers. Rewrite it so the accumulator stays a 32-bit integer (and thus inside Smi range) on every step, without changing the hashing intent. Reveal the model and self-grade.", "ru": "Эта функция хеширования строки переполняет диапазон Smi за несколько символов, деоптя горячий цикл и аллоцируя HeapNumber. Перепиши её так, чтобы аккумулятор оставался 32-битным целым (и тем самым в диапазоне Smi) на каждом шаге, не меняя смысла хеширования. Открой образец и оцени себя." },
      "starter": "function hash(str) {\n  let acc = 0;\n  for (let i = 0; i < str.length; i++) {\n    acc = acc * 31 + str.charCodeAt(i); // grows unbounded -> Smi overflow -> HeapNumber\n  }\n  return acc;\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "Force the accumulator back into a 32-bit integer every step with a bitwise op, so it never escapes the Smi range and TurboFan can keep its integer specialisation:<br><code>function hash(str) {\n  let acc = 0;\n  for (let i = 0; i &lt; str.length; i++) {\n    acc = (acc * 31 + str.charCodeAt(i)) | 0; // | 0 wraps to a 32-bit signed int\n  }\n  return acc | 0;\n}</code><br>The <code>| 0</code> coerces the intermediate (which JS computed as a double internally) back to a 32-bit signed integer each iteration. V8 recognises this idiom and keeps the value a Smi, so the loop stays on the allocation-free integer path with no per-iteration HeapNumber and no overflow deopt. (<code>Math.imul(acc, 31)</code> is the analogous fix for the multiply itself.) Confirm with <code>node --trace-deopt</code>: the bailout disappears.", "ru": "Каждый шаг возвращай аккумулятор в 32-битное целое побитовой операцией, чтобы он никогда не выходил из диапазона Smi и TurboFan мог держать целочисленную специализацию:<br><code>function hash(str) {\n  let acc = 0;\n  for (let i = 0; i &lt; str.length; i++) {\n    acc = (acc * 31 + str.charCodeAt(i)) | 0; // | 0 заворачивает в 32-бит знаковое\n  }\n  return acc | 0;\n}</code><br><code>| 0</code> приводит промежуточный результат (который JS внутри посчитал как double) обратно к 32-битному знаковому целому на каждой итерации. V8 распознаёт этот идиом и держит значение Smi, поэтому цикл остаётся на целочисленном пути без аллокаций, без HeapNumber на итерацию и без деопта по переполнению. (<code>Math.imul(acc, 31)</code> — аналогичный фикс для самого умножения.) Подтверди через <code>node --trace-deopt</code>: откат исчезает." },
        "rubric": [
          { "en": "Applies a 32-bit coercion (| 0, >>> 0, or Math.imul) so the accumulator stays inside Smi range each step.", "ru": "Применяет 32-битное приведение (| 0, >>> 0 или Math.imul), чтобы аккумулятор оставался в диапазоне Smi на каждом шаге." },
          { "en": "Explains that this keeps TurboFan's integer specialisation and avoids per-iteration HeapNumber allocation.", "ru": "Объясняет, что это сохраняет целочисленную специализацию TurboFan и избегает аллокации HeapNumber на итерацию." },
          { "en": "Mentions verifying the deopt is gone (e.g. --trace-deopt).", "ru": "Упоминает проверку, что деопт ушёл (например, --trace-deopt)." }
        ]
      }
    },
    {
      "id": "smi-clamp-boundary",
      "type": "debug",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "clampToSmi uses the wrong boundary constant",
        "ru": "clampToSmi использует неверную константу границы"
      },
      "prompt": {
        "en": "A utility that clamps a number into the V8 Smi range before passing it into a hot numeric pipeline is silently letting out-of-range values through. The bug is a single constant — but picking the wrong one causes values that look fine to become HeapNumbers and silently deopt the hot path. Find and fix it.",
        "ru": "Утилита, зажимающая число в диапазон Smi V8 перед передачей в горячий числовой конвейер, молча пропускает значения за границей диапазона. Баг — в одной константе, но выбор неверной заставляет «нормальные» числа стать HeapNumber и молча деоптировать горячий путь. Найди и исправь."
      },
      "starter": "// Clamps n to the V8 64-bit Smi range so downstream hot loops\n// never receive a value that would force a HeapNumber allocation.\nconst SMI_MAX = 2 ** 31 - 1;\nconst SMI_MIN = -(2 ** 31);\n\nfunction clampToSmi(n) {\n  if (n > SMI_MAX) return SMI_MAX;\n  if (n < SMI_MIN) return SMI_MIN;\n  return n;\n}",
      "verify": "const SMI_REAL_MAX = 2 ** 30 - 1;\nconst SMI_REAL_MIN = -(2 ** 30);\nconst ok =\n  clampToSmi(0) === 0 &&\n  clampToSmi(SMI_REAL_MAX) === SMI_REAL_MAX &&\n  clampToSmi(2 ** 30) === SMI_REAL_MAX &&\n  clampToSmi(SMI_REAL_MIN) === SMI_REAL_MIN &&\n  clampToSmi(-(2 ** 30) - 1) === SMI_REAL_MIN &&\n  clampToSmi(2 ** 31) === SMI_REAL_MAX;\nconsole.log(ok ? 'PASS' : 'FAIL clamp(2**30)=' + clampToSmi(2 ** 30) + ' clamp(-(2**30)-1)=' + clampToSmi(-(2 ** 30) - 1));",
      "check": {
        "kind": "stdout-contains",
        "value": "PASS"
      },
      "evidence": {
        "en": "clampToSmi(2**30) returns 1073741824 instead of 1073741823 — the value is one step outside the real Smi range, so V8 must box it as a HeapNumber. The pipeline degrades silently.",
        "ru": "clampToSmi(2**30) возвращает 1073741824 вместо 1073741823 — значение на шаг за реальным диапазоном Smi, поэтому V8 обязан упаковать его как HeapNumber. Конвейер деградирует молча."
      },
      "hints": [
        {
          "en": "The Smi range on 64-bit V8 is 31-bit signed: how many bits does that leave for the magnitude?",
          "ru": "Диапазон Smi на 64-битном V8 — 31-битное знаковое целое: сколько бит остаётся на величину?"
        },
        {
          "en": "A 31-bit signed integer covers −2^30 to 2^30 − 1. What is 2^30 as a number?",
          "ru": "31-битное знаковое целое покрывает от −2^30 до 2^30 − 1. Чему равно 2^30 как число?"
        },
        {
          "en": "The constants currently look like 32-bit signed integer limits (±2^31). V8's Smi range is one power of two narrower because one bit is the pointer tag.",
          "ru": "Текущие константы похожи на границы 32-битного знакового целого (±2^31). Диапазон Smi V8 на степень двойки уже, потому что один бит занимает тег указателя."
        },
        {
          "en": "Change SMI_MAX to 2**30 - 1 and SMI_MIN to -(2**30). That is the true 31-bit signed range, not the 32-bit one.",
          "ru": "Замени SMI_MAX на 2**30 - 1, а SMI_MIN на -(2**30). Это настоящий 31-битный знаковый диапазон, а не 32-битный."
        }
      ],
      "reveal": {
        "en": "The bug is using 32-bit signed integer limits (2^31 − 1 / −2^31) instead of the true V8 Smi range. On 64-bit V8 a Smi is a 31-bit signed integer stored with a tag bit, giving a range of −2^30 to 2^30 − 1 (about ±1.07 billion). Values in the 32-bit range but above 2^30 − 1 are perfectly representable integers in JS — but they are NOT Smis; V8 must box them as HeapNumbers. A hot path receiving those values has its TurboFan Smi guard silently violated and deopts. Fix: SMI_MAX = 2**30 - 1 and SMI_MIN = -(2**30).",
        "ru": "Баг — использование границ 32-битного знакового целого (2^31 − 1 / −2^31) вместо реального диапазона Smi V8. На 64-битном V8 Smi — это 31-битное знаковое целое с битом тега, что даёт диапазон от −2^30 до 2^30 − 1 (около ±1,07 миллиарда). Значения в 32-битном диапазоне, но выше 2^30 − 1 — вполне допустимые целые в JS, но они НЕ Smi: V8 обязан упаковать их как HeapNumber. Горячий путь, получающий такие значения, нарушает Smi-охрану TurboFan и деоптируется молча. Исправление: SMI_MAX = 2**30 - 1 и SMI_MIN = -(2**30)."
      }
    }
  ]
}
