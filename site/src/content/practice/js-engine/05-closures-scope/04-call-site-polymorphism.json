{
  "lessonKey": "js-engine/05-closures-scope/04-call-site-polymorphism",
  "track": "js-engine",
  "tasks": [
    {
      "id": "predict-ic-state",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Predict the call-site IC state", "ru": "Предскажи состояние IC точки вызова" },
      "prompt": { "en": "Predict the IC state each call site reaches: (a) <code>arr.map(f)</code> with the same <code>f</code> every call; (b) <code>arr.map(x => f(x))</code> with a fresh inline arrow every call; (c) <code>registry[type](ev)</code> over 30 distinct handlers; (d) a call site that, over a run, calls exactly two stable functions.", "ru": "Предскажи состояние IC, в которое приходит каждая точка вызова: (a) <code>arr.map(f)</code> с одной и той же <code>f</code> каждый вызов; (b) <code>arr.map(x => f(x))</code> со свежей инлайн-стрелкой каждый вызов; (c) <code>registry[type](ev)</code> по 30 различным обработчикам; (d) точка вызова, которая за прогон вызывает ровно две стабильные функции." },
      "scenario": { "en": "A call IC tracks distinct callee identities: 1 is monomorphic (inlinable), 2–4 polymorphic, ~5+ megamorphic (generic indirect call). A fresh inline arrow is a new identity each time.", "ru": "Call IC отслеживает различные идентичности callee: 1 — monomorphic (инлайнится), 2–4 — polymorphic, ~5+ — megamorphic (общий косвенный вызов). Свежая инлайн-стрелка — новая идентичность каждый раз." },
      "reveal": { "en": "<strong>(a) Monomorphic</strong> — one stable identity, callback inlinable. <strong>(b) Megamorphic</strong> — each call passes a brand-new arrow identity into map's internal <code>cb(item)</code> site, so it sees many. <strong>(c) Megamorphic</strong> — 30 identities through one site. <strong>(d) Polymorphic</strong> — two identities fit the small dispatch table. Fix (b) by hoisting the arrow or passing <code>f</code> directly; fix (c) with a switch of single-identity sites.", "ru": "<strong>(a) Monomorphic</strong> — одна стабильная идентичность, колбэк инлайнится. <strong>(b) Megamorphic</strong> — каждый вызов передаёт совершенно новую идентичность стрелки во внутреннюю точку <code>cb(item)</code> у map, поэтому она видит много. <strong>(c) Megamorphic</strong> — 30 идентичностей через одну точку. <strong>(d) Polymorphic</strong> — две идентичности влезают в маленькую таблицу диспетчеризации. Чини (b) выносом стрелки или передачей <code>f</code> напрямую; чини (c) switch'ем из точек с одной идентичностью." }
    },
    {
      "id": "sandbox-count-call-targets",
      "type": "sandbox",
      "difficulty": "stretch",
      "estMin": 9,
      "title": { "en": "Count distinct call targets at one site", "ru": "Посчитай различные цели вызова в одной точке" },
      "prompt": { "en": "We model a call site's IC by recording the distinct function identities that flow through it (an instrumented invoke records each callee in a Set). One dispatcher always calls the same function; the other routes 6 different handlers through one site. Run it: the monomorphic site reports 1 target, the megamorphic site reports 6. Print the exact line below.", "ru": "Моделируем IC точки вызова, записывая различные идентичности функций, проходящие через неё (инструментированный invoke кладёт каждую callee в Set). Один диспетчер всегда вызывает ту же функцию; другой маршрутизирует 6 разных обработчиков через одну точку. Запусти: monomorphic точка показывает 1 цель, megamorphic — 6. Напечатай ровно строку ниже." },
      "runtime": "js",
      "initialCode": "// A call site is modeled by the SET of distinct callee identities it has seen.\n// 1 -> monomorphic (inlinable); ~5+ -> megamorphic (generic indirect call).\nfunction makeSite() {\n  const seen = new Set();\n  return {\n    invoke(fn, arg) { seen.add(fn); return fn(arg); },\n    distinct() { return seen.size; },\n  };\n}\n\n// MONOMORPHIC: one identity flows through the site every call.\nconst inc = (x) => x + 1;\nconst monoSite = makeSite();\nfor (let i = 0; i < 6; i++) monoSite.invoke(inc, i);\n\n// MEGAMORPHIC: 6 distinct handler identities flow through one site.\nconst handlers = [ (x)=>x, (x)=>x*2, (x)=>x-1, (x)=>x*x, (x)=>-x, (x)=>x+10 ];\nconst megaSite = makeSite();\nfor (let i = 0; i < handlers.length; i++) megaSite.invoke(handlers[i], i);\n\nconsole.log('mono=' + monoSite.distinct() + ' mega=' + megaSite.distinct());\n",
      "expected": { "kind": "stdout-equals", "value": "mono=1 mega=6" }
    },
    {
      "id": "fix-megamorphic-dispatch",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 9,
      "title": { "en": "De-megamorphise a hot dispatch loop", "ru": "Убери megamorphic из горячего цикла диспетчеризации" },
      "prompt": { "en": "This hot loop dispatches each event through one call site that sees many handler identities, so TurboFan cannot inline it. Rewrite the hot path so the common cases become monomorphic, inlinable call sites. Reveal the model answer and self-grade.", "ru": "Этот горячий цикл диспетчеризует каждое событие через одну точку вызова, видящую много идентичностей обработчиков, поэтому TurboFan не может её заинлайнить. Перепиши горячий путь так, чтобы частые случаи стали monomorphic, инлайнимыми точками вызова. Открой образцовый ответ и оцени себя." },
      "starter": "const registry = { click: onClick, hover: onHover, key: onKey, scroll: onScroll /* ...many */ };\nfunction handleAll(events) {\n  for (const ev of events) {\n    registry[ev.type](ev);   // ONE megamorphic call site for all types\n  }\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "Split the single many-target site into several single-target sites so the hot, common types each hit a monomorphic, inlinable call. A <code>switch</code> on the type does this — each <code>case</code> is its own call site with exactly one callee identity:<br><code>function handleAll(events) {\n  for (const ev of events) {\n    switch (ev.type) {\n      case 'click':  onClick(ev);  break;  // monomorphic site\n      case 'hover':  onHover(ev);  break;  // monomorphic site\n      case 'key':    onKey(ev);    break;\n      case 'scroll': onScroll(ev); break;\n      default:       registry[ev.type](ev); // rare/long tail stays generic\n    }\n  }\n}</code><br>The hot branches now have one callee identity each, so TurboFan can inline them; only the rare long tail keeps the generic indirect call. The variety of identities is no longer concentrated on a single hot line.", "ru": "Раздели единственную многоцелевую точку на несколько одноцелевых, чтобы горячие частые типы попадали каждый в monomorphic, инлайнимый вызов. <code>switch</code> по типу делает это — каждый <code>case</code> это своя точка вызова ровно с одной идентичностью callee:<br><code>function handleAll(events) {\n  for (const ev of events) {\n    switch (ev.type) {\n      case 'click':  onClick(ev);  break;  // monomorphic точка\n      case 'hover':  onHover(ev);  break;  // monomorphic точка\n      case 'key':    onKey(ev);    break;\n      case 'scroll': onScroll(ev); break;\n      default:       registry[ev.type](ev); // редкий длинный хвост остаётся общим\n    }\n  }\n}</code><br>У горячих веток теперь по одной идентичности callee, поэтому TurboFan может их заинлайнить; только редкий длинный хвост сохраняет общий косвенный вызов. Разнообразие идентичностей больше не сконцентрировано на одной горячей строке." },
        "rubric": [
          { "en": "Replaces the single dispatch site with several sites each having one callee identity.", "ru": "Заменяет единственную точку диспетчеризации несколькими точками, каждая с одной идентичностью callee." },
          { "en": "Explains that monomorphic sites are inlinable while the megamorphic one was not.", "ru": "Объясняет, что monomorphic точки инлайнимы, а megamorphic — нет." },
          { "en": "Keeps a generic fallback for the rare long tail rather than enumerating all types.", "ru": "Оставляет общий fallback для редкого длинного хвоста, а не перечисляет все типы." }
        ]
      }
    },
    {
      "id": "diagnose-trace-ic",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Read the --trace-ic transition", "ru": "Прочитай переход из --trace-ic" },
      "prompt": { "en": "A hot helper regressed. A <code>--trace-ic</code> excerpt for its internal call site is below. Fill in the blanks naming the state, the cause, and the lost optimisation.", "ru": "Горячий хелпер просел. Ниже выдержка <code>--trace-ic</code> для его внутренней точки вызова. Заполни пропуски, называя состояние, причину и потерянную оптимизацию." },
      "evidence": { "en": "CallIC ... map@callback  (0->1) MONOMORPHIC fn=arrow#41\nCallIC ... map@callback  (1->P) POLYMORPHIC\nCallIC ... map@callback  (P->N) MEGAMORPHIC  (distinct fns: many)\n// caller passes a fresh `x => f(x)` arrow on each map() call", "ru": "CallIC ... map@callback  (0->1) MONOMORPHIC fn=arrow#41\nCallIC ... map@callback  (1->P) POLYMORPHIC\nCallIC ... map@callback  (P->N) MEGAMORPHIC  (distinct fns: many)\n// вызывающий передаёт свежую `x => f(x)` стрелку на каждый вызов map()" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["megamorphic", "mega", "n", "megamorphic state", "megamorphic (n)", "мегаморфное", "megamorphic-состояние"], "hint": { "en": "The terminal state shown as (P->N).", "ru": "Конечное состояние, показанное как (P->N)." } },
          { "id": "b2", "accept": ["fresh arrow", "new arrow each call", "new function identity", "inline arrow", "fresh inline arrow", "different identity each call", "свежая стрелка", "новая идентичность", "новая стрелка каждый вызов", "инлайн-стрелка"], "hint": { "en": "What the caller passes that creates a new callee identity every time.", "ru": "Что передаёт вызывающий, создавая новую идентичность callee каждый раз." } },
          { "id": "b3", "accept": ["inlining", "inline", "inline the callback", "inlining the callback", "инлайнинг", "инлайн", "заинлайнить колбэк", "инлайнинг колбэка"], "hint": { "en": "The cross-boundary optimisation a megamorphic site cannot get.", "ru": "Оптимизация через границу, которую megamorphic точка получить не может." } }
        ]
      }
    }
  ]
}
