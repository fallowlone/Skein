{
  "lessonKey": "js-engine/03-hidden-classes/05-mono-poly-mega",
  "track": "js-engine",
  "tasks": [
    {
      "id": "predict-ic-state",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Predict the IC state from the shape count", "ru": "Предскажи состояние IC по числу форм" },
      "prompt": { "en": "A single hot load site <code>it.value</code> is fed objects from these many distinct hidden classes, in separate runs: 1, then 3, then 6. Predict the IC state and roughly the per-access cost in each run.", "ru": "Единственная горячая точка load <code>it.value</code> получает объекты из стольких различных hidden class, в отдельных прогонах: 1, затем 3, затем 6. Предскажите состояние IC и примерную стоимость на доступ в каждом прогоне." },
      "scenario": { "en": "The IC ladder is one-way: 1 map = monomorphic, 2-4 maps = polymorphic (scanned list), 5+ maps = megamorphic (global stub-cache probe).", "ru": "Лестница IC односторонняя: 1 map = monomorphic, 2-4 map = polymorphic (сканируемый список), 5+ map = megamorphic (зонд глобального stub cache)." },
      "reveal": { "en": "<strong>1 map → monomorphic</strong>: ~1 cycle (compare map, apply handler). <strong>3 maps → polymorphic</strong>: a scan of a 3-entry {map,handler} list, still fast (a few compares). <strong>6 maps → megamorphic</strong>: exceeds the 4-map polymorphic cap, so V8 drops per-site tracking and probes the global megamorphic stub cache — tens to low hundreds of cycles, and TurboFan can no longer inline or specialize this access. The state is one-way: once at 6, it stays megamorphic for that FeedbackVector even if later accesses use one shape.", "ru": "<strong>1 map → monomorphic</strong>: ~1 цикл (сравни map, примени handler). <strong>3 map → polymorphic</strong>: скан списка из 3 пар {map,handler}, всё ещё быстро (несколько сравнений). <strong>6 map → megamorphic</strong>: превышает порог polymorphic в 4 map, так что V8 сбрасывает пер-точечное отслеживание и зондирует глобальный megamorphic stub cache — десятки-низкие сотни циклов, и TurboFan больше не может заинлайнить или специализировать этот доступ. Состояние одностороннее: дойдя до 6, оно остаётся megamorphic для того FeedbackVector, даже если последующие доступы используют одну форму." }
    },
    {
      "id": "sandbox-classify-ic-state",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 9,
      "title": { "en": "Classify the IC state by distinct maps seen", "ru": "Классифицируй состояние IC по числу различных map" },
      "prompt": { "en": "We model a load site by the set of distinct maps it observes (a map = an object's ordered key signature). Replay a stream of objects through one site, track the distinct maps, and classify the final state: 1 -> monomorphic, 2..4 -> polymorphic, 5+ -> megamorphic. The given stream passes objects of 5 distinct shapes. Make the program print the exact line below.", "ru": "Моделируем точку load набором различных map, которые она наблюдает (map = упорядоченная сигнатура ключей объекта). Прогони поток объектов через одну точку, отследи различные map и классифицируй финальное состояние: 1 -> monomorphic, 2..4 -> polymorphic, 5+ -> megamorphic. Данный поток пропускает объекты 5 различных форм. Заставь программу напечатать ровно строку ниже." },
      "runtime": "js",
      "initialCode": "function mapOf(o) {\n  return Object.keys(o).join(',');\n}\nfunction icState(distinct) {\n  if (distinct <= 1) return 'monomorphic';\n  if (distinct <= 4) return 'polymorphic';\n  return 'megamorphic';\n}\n\n// One load site sees this stream of objects (different shapes mixed in).\nconst stream = [\n  { value: 1 },\n  { value: 2 },\n  { value: 3, tag: 'x' },\n  { value: 4, tag: 'y', extra: 1 },\n  { tag: 'z', value: 5 },         // reversed order -> distinct map\n  { value: 6, note: true },\n];\n\nconst seen = new Set();\nfor (const o of stream) seen.add(mapOf(o));\nconst distinct = seen.size;\nconsole.log('distinctMaps=' + distinct + ' state=' + icState(distinct));\n",
      "expected": { "kind": "stdout-equals", "value": "distinctMaps=5 state=megamorphic" }
    },
    {
      "id": "diagnose-trace-ic-mega",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Read a --trace-ic transition", "ru": "Прочитай переход --trace-ic" },
      "prompt": { "en": "A hot load site's <code>--trace-ic</code> output is below. Fill in the blanks with the mechanism each transition or symptom names.", "ru": "Вывод <code>--trace-ic</code> горячей точки load ниже. Заполни пропуски механизмом, который называет каждый переход или симптом." },
      "evidence": { "en": "LoadIC: MONOMORPHIC -> POLYMORPHIC  (added map #2)\nLoadIC: POLYMORPHIC -> MEGAMORPHIC  (5th map; tracking dropped)\n// thereafter: handler resolved via the global (map,name) hash table\n// TurboFan: cannot inline load at this site (no shape feedback)", "ru": "LoadIC: MONOMORPHIC -> POLYMORPHIC  (added map #2)\nLoadIC: POLYMORPHIC -> MEGAMORPHIC  (5th map; tracking dropped)\n// далее: handler разрешается через глобальную хеш-таблицу (map,name)\n// TurboFan: cannot inline load at this site (no shape feedback)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["polymorphic", "2-4 maps", "polymorphic state", "polymorphic 2-4", "полиморфизм", "2-4 map"], "hint": { "en": "2 to 4 maps puts the site in which state?", "ru": "От 2 до 4 map ставит точку в какое состояние?" } },
          { "id": "b2", "accept": ["megamorphic stub cache", "stub cache", "megamorphic stub-cache", "global stub cache", "megamorphic кэш", "stub cache megamorphic", "глобальный stub cache"], "hint": { "en": "The global (map,name) hash table that megamorphic sites consult.", "ru": "Глобальная хеш-таблица (map,name), к которой обращаются megamorphic-точки." } },
          { "id": "b3", "accept": ["inline", "inlining", "inline the load", "specialize", "specialization", "инлайн", "инлайнинг", "специализация"], "hint": { "en": "What can TurboFan no longer do at a megamorphic site?", "ru": "Что TurboFan больше не может делать в megamorphic-точке?" } }
        ]
      }
    },
    {
      "id": "incident-validity-cell-monkeypatch",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "A polyfill loaded at runtime slowed the whole app", "ru": "Полифил, загруженный во время работы, замедлил всё приложение" },
      "prompt": { "en": "A web app is fast after warm-up. A lazily-loaded analytics SDK runs <code>Array.prototype.includes = Array.prototype.includes || function(){...}</code> and also sets a method on a hot domain class's prototype. Immediately, unrelated hot paths across the app slow down and many functions show as deoptimized. Work the incident.", "ru": "Веб-приложение быстрое после разогрева. Лениво загруженный аналитический SDK выполняет <code>Array.prototype.includes = Array.prototype.includes || function(){...}</code> и также ставит метод на прототип горячего доменного класса. Немедленно несвязанные горячие пути по всему приложению замедляются, и многие функции показываются деоптимизированными. Разбери инцидент." },
      "steps": [
        {
          "label": { "en": "Triage", "ru": "Триаж" },
          "prompt": { "en": "Many functions deoptimized at once, with no change to your own code. Under <code>--trace-deopt</code>, what reason category points at prototype mutation rather than ordinary type churn?", "ru": "Множество функций деоптимизировались разом, без изменений в вашем коде. Под <code>--trace-deopt</code> какая категория причины указывает на мутацию прототипа, а не на обычный type churn?" },
          "reveal": { "en": "Look for deopts attributed to a code/dependency invalidation rather than a failed type guard — V8 reports the optimized code being thrown away because a dependency (a prototype's validity cell) was invalidated, not because an argument had the wrong type. A burst of such deopts across many unrelated functions at the same instant is the signature of one shared invalidation, not many independent type changes.", "ru": "Ищите деопты, приписанные инвалидации кода/зависимости, а не провалившемуся type guard — V8 сообщает, что оптимизированный код выброшен, потому что зависимость (validity cell прототипа) была инвалидирована, а не потому что аргумент имел неверный тип. Всплеск таких деоптов по множеству несвязанных функций в один и тот же миг — это подпись одной общей инвалидации, а не многих независимых смен типов." }
        },
        {
          "label": { "en": "Root cause", "ru": "Корневая причина" },
          "prompt": { "en": "Why does assigning to a prototype after warm-up deoptimize ICs and code that never touched that object directly?", "ru": "Почему присваивание прототипу после разогрева деоптимизирует IC и код, который никогда напрямую не трогал тот объект?" },
          "reveal": { "en": "Prototype-chain ICs and TurboFan-inlined prototype loads are guarded by a validity cell shared by all maps depending on that prototype. Writing to the prototype (even <code>x || function(){}</code> still assigns when the property is absent) mutates it and invalidates the shared validity cell. Every IC and every optimized function that embedded a check on that cell is invalidated at once — including ones that only read methods off that prototype, which is most code that calls a method. Hence a broad, simultaneous slowdown from a single write.", "ru": "Prototype-chain IC и заинлайненные TurboFan'ом prototype-load охраняются validity cell, разделяемой всеми map'ами, зависящими от того прототипа. Запись в прототип (даже <code>x || function(){}</code> всё равно присваивает, когда свойство отсутствует) мутирует его и инвалидирует общую validity cell. Каждый IC и каждая оптимизированная функция, встроившая проверку на ту ячейку, инвалидируются разом — включая те, что лишь читают методы с того прототипа, а это большая часть кода, вызывающего метод. Отсюда широкое одновременное замедление от одной записи." }
        },
        {
          "label": { "en": "Fix", "ru": "Фикс" },
          "prompt": { "en": "What changes make the SDK safe to load late without invalidating everyone's validity cells?", "ru": "Какие изменения делают SDK безопасным для поздней загрузки без инвалидации чужих validity cell?" },
          "reveal": { "en": "Do all prototype setup at startup, before hot paths warm up, so the validity cell stabilizes once and is never invalidated later. Avoid patching shared built-in prototypes (Array.prototype) entirely — use the native method (modern <code>Array.prototype.includes</code> exists) or a standalone helper function instead of extending the prototype. For the domain class, define every method on the prototype at class-definition time, not lazily after instances and call sites already exist.", "ru": "Делай всю настройку прототипов на старте, до разогрева горячих путей, чтобы validity cell стабилизировалась один раз и никогда позже не инвалидировалась. Полностью избегай патча общих встроенных прототипов (Array.prototype) — используй нативный метод (современный <code>Array.prototype.includes</code> существует) или отдельную функцию-хелпер вместо расширения прототипа. Для доменного класса определяй каждый метод на прототипе во время определения класса, а не лениво после того, как экземпляры и точки вызова уже существуют." }
        },
        {
          "label": { "en": "Prevent", "ru": "Профилактика" },
          "prompt": { "en": "What guardrail catches late prototype mutation before it ships?", "ru": "Какой страж ловит позднюю мутацию прототипа до релиза?" },
          "reveal": { "en": "Freeze critical built-in prototypes in development (or assert they are unchanged after startup) so an accidental late patch throws or fails a test instead of silently degrading. Add a lint rule banning assignment to <code>*.prototype</code> outside module top-level / class bodies, and review third-party SDKs for prototype extension. In load tests, run with <code>--trace-deopt</code> and fail the build if a burst of dependency-invalidation deopts appears after the warm-up window.", "ru": "Заморозь критические встроенные прототипы в разработке (или утверждай, что они неизменны после старта), чтобы случайный поздний патч бросал ошибку или валил тест, а не тихо деградировал. Добавь lint-правило, запрещающее присваивание <code>*.prototype</code> вне верхнего уровня модуля / тел классов, и ревьюй сторонние SDK на расширение прототипов. В нагрузочных тестах запускай с <code>--trace-deopt</code> и валите сборку, если всплеск деоптов инвалидации зависимостей появляется после окна разогрева." }
        }
      ]
    }
  ]
}
