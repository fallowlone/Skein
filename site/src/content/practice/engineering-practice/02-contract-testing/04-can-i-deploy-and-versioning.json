{
  "lessonKey": "engineering-practice/02-contract-testing/04-can-i-deploy-and-versioning",
  "track": "engineering-practice",
  "tasks": [
    {
      "id": "diagnose-gate-vocabulary",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": {
        "en": "Name the parts of the deploy gate",
        "ru": "Назови части деплой-гейта"
      },
      "prompt": {
        "en": "Fill in the blanks with the precise Pact terms for each part of the can-i-deploy deployment gate.",
        "ru": "Заполни пропуски точными терминами Pact для каждой части деплой-гейта can-i-deploy."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["can-i-deploy", "can i deploy"],
            "hint": {
              "en": "The CLI command that queries the broker and exits non-zero to block an unsafe deploy.",
              "ru": "Команда CLI, которая опрашивает брокер и выходит с ненулевым кодом, блокируя небезопасный деплой."
            }
          },
          {
            "id": "b2",
            "accept": ["matrix", "verification matrix", "the matrix"],
            "hint": {
              "en": "The table of every tested consumer×provider pair plus what is deployed where.",
              "ru": "Таблица всех протестированных пар потребитель×провайдер плюс что куда задеплоено."
            }
          },
          {
            "id": "b3",
            "accept": ["git sha", "git commit sha", "commit sha", "sha"],
            "hint": {
              "en": "The recommended pacticipant version identifier, because it changes whenever the contract could.",
              "ru": "Рекомендуемый идентификатор версии pacticipant, потому что он меняется при любом изменении контракта."
            }
          },
          {
            "id": "b4",
            "accept": ["record-deployment", "record deployment"],
            "hint": {
              "en": "The command that tells the broker a service version now runs in an environment and undeploys the previous one.",
              "ru": "Команда, которая сообщает брокеру, что версия сервиса теперь работает в окружении, и снимает с деплоя предыдущую."
            }
          }
        ]
      }
    },
    {
      "id": "predict-blocked-deploy",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Will the gate let this consumer ship?",
        "ru": "Пропустит ли гейт этого потребителя?"
      },
      "prompt": {
        "en": "Predict the can-i-deploy outcome (exit code and reason) before revealing the answer.",
        "ru": "Предскажи результат can-i-deploy (код выхода и причину) до того, как откроешь ответ."
      },
      "scenario": {
        "en": "checkout-web v24 (SHA 7c8d9e0) adds a discount_cents expectation. Its pact is verified green — but against pricing v59, the latest CI build that already returns discount_cents. Production currently runs pricing v56, recorded via record-deployment. The pipeline runs: can-i-deploy --pacticipant checkout-web --version 7c8d9e0 --to-environment production. What does the gate do, and why does the green v59 result not save it?",
        "ru": "checkout-web v24 (SHA 7c8d9e0) добавляет ожидание discount_cents. Его pact верифицирован зелёным — но против pricing v59, последней CI-сборки, которая уже возвращает discount_cents. В проде сейчас работает pricing v56, записанный через record-deployment. Пайплайн запускает: can-i-deploy --pacticipant checkout-web --version 7c8d9e0 --to-environment production. Что делает гейт и почему зелёный результат v59 его не спасает?"
      },
      "reveal": {
        "en": "The gate exits non-zero and blocks the deploy. can-i-deploy crosses the version being shipped (v24) against the provider version actually in production (v56, from the deployment record), not the latest build. The matrix cell (checkout-web v24, pricing v56) has no result — that pair was never verified — so the required cell is not green and the command returns exit 1. The green (v24, v59) cell is ignored entirely because v59 is not deployed in production; verifying against latest says nothing about the older provider physically serving traffic. This is exactly the deploy that broke prod in the lesson's Hook, now refused before it ships.",
        "ru": "Гейт выходит с ненулевым кодом и блокирует деплой. can-i-deploy сопоставляет отгружаемую версию (v24) с версией провайдера, реально работающей в проде (v56, из записи о деплое), а не с последней сборкой. Ячейка матрицы (checkout-web v24, pricing v56) не имеет результата — эта пара никогда не верифицировалась — поэтому требуемая ячейка не зелёная, и команда возвращает exit 1. Зелёная ячейка (v24, v59) полностью игнорируется, потому что v59 не задеплоена в проде; верификация против latest ничего не говорит о старом провайдере, который реально обслуживает трафик. Это ровно тот деплой, что уронил прод в Hook урока, теперь отклонённый до отгрузки."
      }
    },
    {
      "id": "sandbox-can-i-deploy",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 9,
      "title": {
        "en": "Implement a tiny can-i-deploy gate",
        "ru": "Реализуй крошечный гейт can-i-deploy"
      },
      "prompt": {
        "en": "Implement canIDeploy(matrix, deploy, deployed): the matrix is an array of {consumer, consumerV, provider, providerV, verified}; deploy is {pacticipant, version} you want to ship; deployed maps each provider name to the version currently in the target environment. For every provider in `deployed` that this pacticipant integrates with (appears as a provider for it in the matrix), require a verified-true cell pairing deploy.version (as consumer) with that provider's deployed version. Print exactly \"safe to deploy\" if every required pair is verified, otherwise print exactly \"blocked: <provider>\" for the first unsatisfied provider (in the order given by `providers`). Make it deterministic.",
        "ru": "Реализуй canIDeploy(matrix, deploy, deployed): matrix — массив {consumer, consumerV, provider, providerV, verified}; deploy — {pacticipant, version}, которую хочешь отгрузить; deployed сопоставляет имя провайдера версии, сейчас работающей в целевом окружении. Для каждого провайдера из `deployed`, с которым этот pacticipant интегрируется (встречается как его provider в matrix), требуй ячейку verified=true, связывающую deploy.version (как consumer) с задеплоенной версией этого провайдера. Печатай ровно \"safe to deploy\", если каждая требуемая пара верифицирована, иначе печатай ровно \"blocked: <provider>\" для первого неудовлетворённого провайдера (в порядке из `providers`). Сделай детерминированным."
      },
      "runtime": "js",
      "initialCode": "const matrix = [\n  { consumer: 'checkout-web', consumerV: 'v23', provider: 'pricing', providerV: 'v56', verified: true },\n  { consumer: 'checkout-web', consumerV: 'v24', provider: 'pricing', providerV: 'v59', verified: true },\n  { consumer: 'checkout-web', consumerV: 'v24', provider: 'inventory', providerV: 'v12', verified: true }\n];\nconst deploy = { pacticipant: 'checkout-web', version: 'v24' };\nconst deployed = { pricing: 'v56', inventory: 'v12' };\nconst providers = ['pricing', 'inventory'];\n\n// Write canIDeploy(matrix, deploy, deployed, providers): for each provider in\n// `providers` that deploy.pacticipant integrates with (appears as its provider\n// in the matrix), require a verified-true cell pairing deploy.version (as\n// consumer) with deployed[provider]. Print 'safe to deploy' if all pass, else\n// 'blocked: <provider>' for the first unsatisfied provider. Then call it.\n",
      "expected": {
        "kind": "stdout-equals",
        "value": "blocked: pricing"
      }
    },
    {
      "id": "design-cd-deploy-gate",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 14,
      "title": {
        "en": "Wire can-i-deploy into the CD pipeline",
        "ru": "Встрой can-i-deploy в CD-пайплайн"
      },
      "prompt": {
        "en": "Design where versioning, the can-i-deploy gate, and deployment recording sit in a continuous-delivery pipeline so that independent deployability holds and the Hook's outage cannot recur. Self-grade against the rubric.",
        "ru": "Спроектируй, где в пайплайне непрерывной доставки находятся версионирование, гейт can-i-deploy и запись деплоя, чтобы сохранялась независимая деплоибельность, а авария из Hook не могла повториться. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "~30 services, each on its own pipeline, deploying many times a day. Environments: staging then production. A Pact Broker is the source of truth. No shared end-to-end staging soak is allowed (it reintroduces the N² lockstep problem). Rollouts are gradual and can fail mid-way.",
        "ru": "~30 сервисов, каждый со своим пайплайном, деплоят много раз в день. Окружения: staging, затем production. Pact Broker — источник истины. Общий end-to-end прогон в staging запрещён (он возвращает N²-проблему lockstep). Раскатки постепенные и могут упасть на середине."
      },
      "rubric": [
        {
          "en": "Versions every pact and deploy by the git SHA (or a semver embedding it) so the version changes whenever the contract could and stale results are never reused.",
          "ru": "Версионирует каждый pact и деплой по git SHA (или semver с ним), чтобы версия менялась при любом изменении контракта, а устаревшие результаты не переиспользовались."
        },
        {
          "en": "Runs can-i-deploy --to-environment as a hard gate immediately before each environment's deploy, failing the pipeline on non-zero exit.",
          "ru": "Запускает can-i-deploy --to-environment как жёсткий гейт прямо перед деплоем в каждое окружение, проваливая пайплайн при ненулевом коде выхода."
        },
        {
          "en": "Calls record-deployment (services) / record-release (store artifacts) at the very end of a successful rollout, never before, so the matrix reflects physical reality for the next team's gate.",
          "ru": "Вызывает record-deployment (сервисы) / record-release (артефакты сторов) в самом конце успешной раскатки, никогда раньше, чтобы матрица отражала физическую реальность для гейта следующей команды."
        },
        {
          "en": "Explains how this yields independent deployability — each service answers its own deploy-safety question from recorded facts without a shared soak.",
          "ru": "Объясняет, как это даёт независимую деплоибельность — каждый сервис отвечает на свой вопрос о безопасности деплоя из записанных фактов без общего прогона."
        }
      ],
      "model": {
        "en": "Pipeline per service: (1) Build & version — tag the pacticipant version with the git SHA; publish the consumer pact (or run provider verification) to the broker keyed to that SHA, so every contract-affecting build gets a distinct version and verification results are reported back as commit statuses. (2) Gate per environment — immediately before deploying to staging run `can-i-deploy --pacticipant svc --version $SHA --to-environment staging`; on exit 1, fail the pipeline. Repeat with `--to-environment production` immediately before the prod deploy. The gate crosses your SHA against whatever provider versions are recorded in that environment, so it refuses the new-consumer-vs-old-provider pair from the Hook. (3) Deploy gradually. (4) Record at the end — only after the rollout fully succeeds and the old version is drained, call `pact-broker record-deployment --pacticipant svc --version $SHA --environment production` (services) or `record-release` for store-published artifacts. Recording at the end (not before) keeps the broker honest if a rollout fails mid-way, so the next team's gate gets a true answer. Because every gate reads recorded per-environment facts, each of the 30 services ships on its own schedule in seconds with no shared end-to-end soak — that is independent deployability, and it closes the Hook's gap without re-introducing the N² problem.",
        "ru": "Пайплайн на каждый сервис: (1) Сборка и версия — пометь версию pacticipant git SHA; опубликуй consumer pact (или прогони provider verification) в брокер с привязкой к этому SHA, чтобы каждая влияющая на контракт сборка получала отдельную версию, а результаты верификации возвращались как commit status. (2) Гейт на каждое окружение — прямо перед деплоем в staging запусти `can-i-deploy --pacticipant svc --version $SHA --to-environment staging`; при exit 1 провали пайплайн. Повтори с `--to-environment production` прямо перед прод-деплоем. Гейт сопоставляет твой SHA с версиями провайдеров, записанными в этом окружении, и отклоняет пару новый-потребитель-против-старого-провайдера из Hook. (3) Раскатывай постепенно. (4) Записывай в конце — только после полного успеха раскатки и слива старой версии вызови `pact-broker record-deployment --pacticipant svc --version $SHA --environment production` (сервисы) или `record-release` для артефактов сторов. Запись в конце (а не раньше) сохраняет честность брокера, если раскатка упала на середине, так что гейт следующей команды получит верный ответ. Поскольку каждый гейт читает записанные пофакторные данные по окружениям, каждый из 30 сервисов отгружается по своему расписанию за секунды без общего end-to-end прогона — это и есть независимая деплоибельность, закрывающая разрыв из Hook без возврата N²-проблемы."
      }
    }
  ]
}
