{
  "lessonKey": "performance/03-cache-vs-bigo/01-memory-hierarchy",
  "track": "performance",
  "tasks": [
    {
      "id": "diagnose-hierarchy-numbers",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Pin the latency of each memory level", "ru": "Закрепи latency каждого уровня памяти" },
      "prompt": { "en": "Fill in the approximate latency the lesson gives for each level of the memory hierarchy, and the size of the unit the hardware moves between RAM and cache.", "ru": "Заполни приблизительную latency, которую урок даёт каждому уровню иерархии памяти, и размер единицы, которую hardware двигает между RAM и cache." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "l1", "accept": ["1", "1 ns", "~1 ns", "1ns", "~1"], "hint": { "en": "Fastest cache, 3–5 cycles.", "ru": "Самый быстрый cache, 3–5 циклов." } },
          { "id": "l3", "accept": ["10", "10 ns", "~10 ns", "10ns", "~10"], "hint": { "en": "Shared cache, 30–50 cycles.", "ru": "Общий cache, 30–50 циклов." } },
          { "id": "ram", "accept": ["100", "70", "70-100", "70–100", "100 ns", "~100 ns", "70-100 ns"], "hint": { "en": "DDR5 main memory, 200–300 cycles.", "ru": "DDR5 main memory, 200–300 циклов." } },
          { "id": "line", "accept": ["64", "64 bytes", "64 байта", "64B", "64 byte"], "hint": { "en": "The cache-line size on x86 and most ARM.", "ru": "Размер cache line на x86 и большинстве ARM." } }
        ]
      }
    },
    {
      "id": "predict-array-vs-list",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict the array-vs-linked-list gap", "ru": "Предскажи разрыв array против linked list" },
      "prompt": { "en": "Read the scenario, commit to a number and a one-line mechanism, then reveal the lesson's answer.", "ru": "Прочитай сценарий, зафиксируй число и однострочный механизм, затем открой ответ урока." },
      "scenario": { "en": "You sum 1 million 64-bit integers two ways: once from a contiguous array, once from a heap-allocated linked list. Both loops are O(N) with the same iteration count. Roughly what wall-clock ratio separates them, and what single hardware fact causes it?", "ru": "Ты суммируешь 1 млн 64-битных int двумя способами: один раз из contiguous array, один раз из linked list на heap. Оба цикла O(N) с одинаковым числом итераций. Какое примерно соотношение по времени их разделяет и какой один факт hardware его вызывает?" },
      "reveal": { "en": "About 17x (array ~1–2 ms, list ~35 ms). The array is one contiguous block, so the prefetcher loads cache lines ahead of the loop and almost every access is a ~1 ns L1 hit. Each list node is a separate heap allocation; following <code>.next</code> jumps to an unpredictable address that the prefetcher cannot guess, so most dereferences pay a cold ~100 ns RAM miss. 100 ns × 1M = 100 ms vs ~1 ms — the 17x is conservative and widens with N.", "ru": "Около 17x (array ~1–2 мс, list ~35 мс). Array — один contiguous блок, поэтому prefetcher грузит cache lines заранее и почти каждый доступ — это ~1 нс L1-хит. Каждый node списка — отдельная аллокация на heap; следование <code>.next</code> прыгает на непредсказуемый адрес, который prefetcher не угадает, поэтому большинство dereference платят холодный ~100 нс RAM-miss. 100 нс × 1М = 100 мс против ~1 мс — 17x консервативен и растёт с N." }
    },
    {
      "id": "sandbox-weighted-latency",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Compute average access latency from hit rates", "ru": "Посчитай среднюю latency доступа по hit rate" },
      "prompt": { "en": "The starter holds the lesson's per-level latencies (L1 1 ns, L2 3 ns, L3 10 ns, RAM 100 ns) and a hit-rate profile. Finish the weighted-average so it prints the effective latency, then sanity-check why a 5% drop in L1 hit rate hurts so much. Print the result with <code>console.log</code>.", "ru": "В starter заданы latency уровней из урока (L1 1 нс, L2 3 нс, L3 10 нс, RAM 100 нс) и профиль hit rate. Доделай взвешенное среднее, чтобы оно печатало эффективную latency, затем прикинь, почему падение L1 hit rate на 5% так больно бьёт. Выведи результат через <code>console.log</code>." },
      "runtime": "js",
      "setup": "// per-level latency in nanoseconds, from the lesson\nconst lat = { l1: 1, l2: 3, l3: 10, ram: 100 };\n// fraction of accesses that resolve at each level (sums to 1.0)\nconst hit = { l1: 0.90, l2: 0.07, l3: 0.02, ram: 0.01 };\n\n// TODO: weighted average = sum(hit[level] * lat[level])\nconst avg = 0; // replace\nconsole.log(avg.toFixed(2));",
      "expected": { "kind": "no-error" }
    },
    {
      "id": "fix-cache-unfriendly-loop",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Make a struct-of-arrays scan cache-friendly", "ru": "Сделай скан structure-of-arrays cache-friendly" },
      "prompt": { "en": "A hot loop sums one field across a million records but the records are an array of pointers to heap objects, so every iteration chases a pointer into cold memory. Describe the layout change that fixes it and why it works against the memory hierarchy. Then reveal the model answer.", "ru": "Горячий цикл суммирует одно поле по миллиону записей, но записи — это массив указателей на heap-объекты, поэтому каждая итерация гонится за pointer в холодную память. Опиши изменение layout, которое это чинит, и почему оно работает против иерархии памяти. Затем открой образцовый ответ." },
      "starter": "// records[i] is a pointer to a heap object; .next-style pointer chasing\nlet total = 0;\nfor (let i = 0; i < records.length; i++) {\n  total += records[i].amount; // cold miss on most iterations\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "Store the hot field in its own flat, contiguous typed array (structure-of-arrays), e.g. <code>const amount = new Float64Array(n)</code>, and iterate that directly: <code>for (let i=0;i&lt;n;i++) total += amount[i];</code>. Now the values sit back-to-back in memory, so one 64-byte cache line holds 8 of them and the hardware prefetcher streams the next lines ahead of the loop — most accesses become ~1 ns L1 hits instead of ~100 ns RAM misses. The big-O is unchanged (still O(N)); only the per-operation constant collapses, which is exactly the lever the memory hierarchy controls. Array-of-pointers/objects defeats this because each dereference lands at an unpredictable heap address the prefetcher cannot follow.", "ru": "Храни горячее поле в собственном плоском contiguous typed array (structure-of-arrays), напр. <code>const amount = new Float64Array(n)</code>, и итерируй прямо его: <code>for (let i=0;i&lt;n;i++) total += amount[i];</code>. Теперь значения лежат впритык в памяти, одна 64-байтная cache line держит 8 штук, и hardware prefetcher стримит следующие lines заранее — большинство доступов становятся ~1 нс L1-хитами вместо ~100 нс RAM-miss. Big-O не меняется (всё ещё O(N)); схлопывается только constant на операцию — это и есть рычаг иерархии памяти. Array указателей/объектов это ломает, потому что каждый dereference попадает на непредсказуемый heap-адрес, за которым prefetcher не пойдёт." },
        "rubric": [
          { "en": "Proposes a contiguous/flat layout (structure-of-arrays or typed array) for the hot field.", "ru": "Предлагает contiguous/плоский layout (structure-of-arrays или typed array) для горячего поля." },
          { "en": "Explains why contiguity helps: 64-byte cache lines + prefetcher turn RAM misses into L1 hits.", "ru": "Объясняет, почему contiguity помогает: 64-байтные cache lines + prefetcher превращают RAM-miss в L1-хиты." },
          { "en": "Notes big-O is unchanged — only the per-operation constant (data layout) improves.", "ru": "Отмечает, что big-O не меняется — улучшается только constant на операцию (data layout)." }
        ]
      }
    }
  ]
}
