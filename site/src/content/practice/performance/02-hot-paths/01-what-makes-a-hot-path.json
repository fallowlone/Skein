{
  "lessonKey": "performance/02-hot-paths/01-what-makes-a-hot-path",
  "track": "performance",
  "tasks": [
    {
      "id": "predict-wide-leaf-meaning",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "What does a wide leaf actually tell you?", "ru": "Что на самом деле говорит широкий leaf?" },
      "prompt": { "en": "Before revealing, write down what a wide flame-graph leaf names and what it does NOT name. Then check yourself.", "ru": "Прежде чем открыть ответ, запиши, что называет широкий leaf во flame graph и что он НЕ называет. Затем проверь себя." },
      "scenario": { "en": "A profile is done. One leaf frame, <code>regex.test</code>, is the widest in the graph. A teammate says: \"That frame is the bottleneck — let's swap the regex engine.\" What is the frame really telling you, and what question must come before any fix?", "ru": "Профиль готов. Один leaf-фрейм, <code>regex.test</code>, самый широкий в графе. Коллега говорит: «Этот фрейм — bottleneck, давай сменим regex-движок». Что фрейм реально вам сообщает и какой вопрос должен предшествовать любой правке?" },
      "reveal": { "en": "The wide leaf names the <strong>symptom</strong> — <em>where</em> time accumulates — not the <strong>cause</strong> (<em>why</em> it is expensive). Before any fix you must ask: is this CPU work, allocation/GC, a cache miss, a lock wait, or a syscall? Each shape demands a different fix. Here the parent chain reveals the regex is compiled on every request from a template string; the fix is compile-once-at-startup, in the caller — not a new engine.", "ru": "Широкий leaf называет <strong>симптом</strong> — <em>где</em> накапливается время — а не <strong>причину</strong> (<em>почему</em> функция дорогая). До любой правки нужно спросить: это CPU-работа, аллокации/GC, cache miss, ожидание лока или syscall? Каждая форма требует своей правки. Здесь parent chain показывает: regex компилируется на каждом запросе из шаблонной строки; правка — компилировать один раз на старте, в caller'е, а не менять движок." }
    },
    {
      "id": "diagnose-five-shapes",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Read the frame, name the shape", "ru": "Прочти фрейм, назови форму" },
      "prompt": { "en": "Three flame-graph observations are below. Fill in each blank with the kind of work the frame really represents and where to look next.", "ru": "Ниже три наблюдения из flame graph. Заполни каждый пропуск видом работы, который реально стоит за фреймом, и тем, куда смотреть дальше." },
      "evidence": { "en": "(1) processOrder at 35% CPU, but most of that 35% is inside runtime.scanobject called from the loop.\n(2) A frame is wide in wall-clock but narrow in CPU.\n(3) An interpreter frame appears where a JIT frame should be.", "ru": "(1) processOrder на 35% CPU, но большая часть этих 35% — внутри runtime.scanobject, вызываемого из цикла.\n(2) Фрейм широкий в wall-clock, но узкий в CPU.\n(3) Фрейм интерпретатора там, где должен быть JIT-фрейм." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["allocation", "allocations", "gc", "garbage collection", "alloc"], "hint": { "en": "scanobject is the GC; the loop allocates a lot. Fix is sync.Pool, not a new algorithm.", "ru": "scanobject — это GC; цикл много аллоцирует. Правка — sync.Pool, не новый алгоритм." } },
          { "id": "b2", "accept": ["waiting", "wait", "lock", "syscall", "blocked", "off-cpu"], "hint": { "en": "Wide in wall-clock, narrow in CPU = the function is waiting, not computing.", "ru": "Широкий в wall-clock, узкий в CPU = функция ждёт, а не считает." } },
          { "id": "b3", "accept": ["jit deopt", "deopt", "deoptimisation", "deoptimization", "jit deoptimised", "fell back to baseline"], "hint": { "en": "The JIT fell back to baseline; stabilise object shapes / types.", "ru": "JIT откатился к baseline; стабилизируй формы объектов / типы." } }
        ]
      }
    },
    {
      "id": "sandbox-weigh-the-hot-path",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Weigh the hot path: frequency × per-call cost", "ru": "Взвесь hot path: частота × цена на вызов" },
      "prompt": { "en": "Hot = where total time lands = call count × self-time per call, not gut feel. Run the code: it computes the total weight of three functions and prints which one dominates. Confirm the cheap-but-frequent function outweighs the expensive-but-rare one.", "ru": "Горячо = куда падает суммарное время = число вызовов × self-time на вызов, а не чутьё. Запусти код: он считает суммарный вес трёх функций и печатает, какая доминирует. Подтверди, что дешёвая, но частая функция перевешивает дорогую, но редкую." },
      "runtime": "js",
      "initialCode": "const fns = [\n  { name: 'validate', calls: 1_000_000, selfUs: 0.8 },\n  { name: 'renderReport', calls: 1, selfUs: 500_000 },\n  { name: 'lookupUser', calls: 200_000, selfUs: 1.5 },\n];\nconst weighed = fns\n  .map(f => ({ name: f.name, totalMs: (f.calls * f.selfUs) / 1000 }))\n  .sort((a, b) => b.totalMs - a.totalMs);\nfor (const f of weighed) console.log(`${f.name}: ${f.totalMs.toFixed(1)} ms`);\nconsole.log('hottest -> ' + weighed[0].name);",
      "expected": { "kind": "stdout-contains", "value": "hottest -> validate" }
    },
    {
      "id": "incident-wrong-fix-right-hotspot",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Right hotspot, wrong fix — work the incident", "ru": "Верный hotspot, неверная правка — разбери инцидент" },
      "prompt": { "en": "A search endpoint's p99 is 3× budget. The profile shows json.Marshal as the widest leaf. The team is mid-flight on swapping the JSON library. Work the diagnosis before the change ships.", "ru": "p99 search-эндпоинта в 3× бюджета. Профиль показывает json.Marshal как самый широкий leaf. Команда уже меняет JSON-библиотеку. Разбери диагноз до того, как изменение уедет." },
      "steps": [
        {
          "label": { "en": "Read the neighbourhood", "ru": "Прочти окрестности" },
          "prompt": { "en": "Before accepting 'wide leaf = bottleneck', what two directions do you read around the leaf, and why?", "ru": "Прежде чем принять «широкий leaf = bottleneck», какие два направления вокруг leaf'а ты читаешь и зачем?" },
          "reveal": { "en": "Read the <strong>parent chain</strong> (who calls Marshal, and how often?) and the <strong>children/self-time split</strong> (is the cost really inside Marshal, or in a callee?). The wide frame is a dashboard light — it opens the investigation, it does not close it.", "ru": "Читай <strong>parent chain</strong> (кто и как часто вызывает Marshal?) и <strong>разбивку self-time / children</strong> (цена реально внутри Marshal или в callee?). Широкий фрейм — лампочка на dashboard'е: он открывает расследование, а не закрывает его." }
        },
        {
          "label": { "en": "Classify the work", "ru": "Классифицируй работу" },
          "prompt": { "en": "The parent chain shows one dominant path: a per-request logger calls Marshal on every request to serialise the request body into a log line. What kind of problem is this, really?", "ru": "Parent chain показывает один доминирующий путь: per-request логгер вызывает Marshal на каждом запросе, чтобы сериализовать тело запроса в строку лога. Что это за проблема на самом деле?" },
          "reveal": { "en": "Not a slow-library problem and not a CPU-algorithm problem — it is a <strong>called-too-often</strong> problem rooted in the caller. The fix lives one level up, not in the leaf. A faster JSON library would shave a fraction; removing the call removes the whole cost.", "ru": "Это не проблема медленной библиотеки и не проблема CPU-алгоритма — это проблема <strong>слишком частого вызова</strong>, корень которой в caller'е. Правка живёт уровнем выше, а не в leaf'е. Более быстрая библиотека срежет долю; убрать вызов убирает всю цену." }
        },
        {
          "label": { "en": "Fix and verify", "ru": "Правка и проверка" },
          "prompt": { "en": "What change do you ship, and how do you prove it worked rather than just looked plausible?", "ru": "Какое изменение ты выкатываешь и как доказываешь, что оно сработало, а не просто выглядело правдоподобно?" },
          "reveal": { "en": "Log the request body at debug level only, or drop it — the leaf shrinks ~50% without touching the library. Verify by capturing a new profile under the <em>same</em> load and diffing against baseline: confirm both the local Marshal frame shrank <em>and</em> the headline p99 metric improved. Local win without headline win means you fixed the wrong thing.", "ru": "Логируй тело запроса только на debug-уровне или убери его — leaf сожмётся примерно на 50% без касания библиотеки. Проверь: сними новый профиль под <em>той же</em> нагрузкой и сравни с baseline — убедись, что и локальный фрейм Marshal сжался, <em>и</em> headline-метрика p99 улучшилась. Локальная победа без улучшения headline означает, что починил не то." }
        }
      ]
    },
    {
      "id": "debug-hot-path-weighting",
      "type": "debug",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "The hot-path ranker picks the rare expensive function", "ru": "Ранкер горячего пути выбирает редкую дорогую функцию" },
      "prompt": { "en": "This helper ranks functions to decide where the time actually lands, so the team optimizes the true hot path. Hotness is total time = call count × per-call self-time, but the ranker compares per-call cost alone and crowns a function that runs once. Fix `hottest` so it ranks by total contributed time, so a cheap-but-frequent function correctly outweighs an expensive-but-rare one.", "ru": "Этот helper ранжирует функции, чтобы решить, куда реально падает время, и команда оптимизировала истинный горячий путь. Горячесть = суммарное время = число вызовов × self-time на вызов, но ранкер сравнивает только цену на вызов и коронует функцию, выполняющуюся один раз. Почини `hottest`, чтобы он ранжировал по суммарно вложенному времени, и дешёвая, но частая функция верно перевешивала дорогую, но редкую." },
      "evidence": { "en": "validate: 1,000,000 calls × 0.8 µs = 800 ms total. renderReport: 1 call × 500,000 µs = 500 ms total. lookupUser: 200,000 × 1.5 µs = 300 ms total. The true hottest is 'validate' (800 ms). The buggy ranker compares per-call selfUs and crowns 'renderReport' (500,000 µs/call).", "ru": "validate: 1 000 000 вызовов × 0.8 мкс = 800 мс суммарно. renderReport: 1 вызов × 500 000 мкс = 500 мс суммарно. lookupUser: 200 000 × 1.5 мкс = 300 мс суммарно. Истинно самый горячий — 'validate' (800 мс). Багнутый ранкер сравнивает selfUs на вызов и коронует 'renderReport' (500 000 мкс/вызов)." },
      "starter": "function hottest(fns) {\n  // fns: [{ name, calls, selfUs }]  selfUs = self-time per call, microseconds\n  let best = null, bestScore = -1;\n  for (const f of fns) {\n    const score = f.selfUs;          // rank by per-call cost\n    if (score > bestScore) { bestScore = score; best = f.name; }\n  }\n  return best;\n}",
      "verify": "const fns = [\n  { name: 'validate', calls: 1_000_000, selfUs: 0.8 },\n  { name: 'renderReport', calls: 1, selfUs: 500_000 },\n  { name: 'lookupUser', calls: 200_000, selfUs: 1.5 },\n];\nconst got = hottest(fns);\nconst ok = got === 'validate';\nconsole.log(ok ? 'PASS' : 'FAIL got=' + got);",
      "check": { "kind": "stdout-contains", "value": "PASS" },
      "hints": [
        { "en": "Per-call cost ignores how often the function runs. A 0.8 µs function called a million times costs more than a 0.5 s function called once.", "ru": "Цена на вызов игнорирует, как часто функция выполняется. Функция 0.8 мкс, вызванная миллион раз, стоит больше, чем функция 0.5 с, вызванная однажды." },
        { "en": "Rank by `f.calls * f.selfUs`, the total contributed time, not `f.selfUs` alone.", "ru": "Ранжируй по `f.calls * f.selfUs`, суммарно вложенному времени, а не по `f.selfUs` отдельно." }
      ],
      "reveal": { "en": "The ranker scores by `f.selfUs` — per-call cost — which ignores frequency. A function that costs 500,000 µs once contributes 500 ms total, but a 0.8 µs function called a million times contributes 800 ms — more total time, and therefore the real hot path. Fix: score by total contributed time, `const score = f.calls * f.selfUs;`. Then 'validate' (800 ms) wins over 'renderReport' (500 ms). This is the core hot-path definition: hot means where total time lands = frequency × per-call self-time, not gut feel about which function 'looks expensive'. Optimizing the rare expensive function caps at its 500 ms slice; the frequent cheap one owns more of the wall clock.", "ru": "Ранкер скорит по `f.selfUs` — цене на вызов — что игнорирует частоту. Функция, стоящая 500 000 мкс однажды, вкладывает 500 мс суммарно, но функция 0.8 мкс, вызванная миллион раз, вкладывает 800 мс — больше суммарного времени, и потому она и есть настоящий горячий путь. Фикс: скорь по суммарно вложенному времени, `const score = f.calls * f.selfUs;`. Тогда 'validate' (800 мс) обходит 'renderReport' (500 мс). Это ядро определения горячего пути: горячо — это куда падает суммарное время = частота × self-time на вызов, а не чутьё о том, какая функция «выглядит дорогой». Оптимизация редкой дорогой функции ограничена её куском в 500 мс; частая дешёвая владеет большей частью wall-clock." }
    }
  ]
}
