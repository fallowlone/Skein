{
  "lessonKey": "performance/07-bundle-budgets/01-what-a-bundle-costs",
  "track": "performance",
  "tasks": [
    {
      "id": "predict-four-phases",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Predict where the time actually goes", "ru": "Предскажи, куда реально уходит время" },
      "prompt": { "en": "Before revealing, name the four sequential phases a JS bundle goes through before a page is interactive, and say which single phase a faster 5G/Wi-Fi connection will NOT speed up.", "ru": "Перед раскрытием назови четыре последовательные фазы, через которые проходит JS-бандл до интерактивности страницы, и скажи, какие фазы более быстрое 5G/Wi-Fi соединение НЕ ускорит." },
      "scenario": { "en": "A 500 KB uncompressed bundle loads in 400 ms on a dev MacBook. The same page sits blank for ~8 s on a mid-range Android. The team's instinct is to buy a faster CDN and switch to 5G.", "ru": "Бандл в 500 КБ без сжатия грузится за 400 мс на MacBook разработчика. Та же страница висит пустой ~8 с на среднем Android. Команда первым делом хочет купить более быстрый CDN и перейти на 5G." },
      "reveal": { "en": "Phases in order: <code>download</code> (network) → <code>parse</code> (V8 walks every byte into an AST) → <code>compile</code> (JIT turns the AST into bytecode then machine code) → <code>execute</code> (top-level code runs on the main thread). A faster connection only helps <code>download</code>. Parse, compile, and execute happen on the user's CPU after the bytes arrive — and a mid-range Android is 4-8x slower there than the MacBook, regardless of network. That is why parse alone for 500 KB is 1.5-2 s on mobile vs a fraction of that on the laptop.", "ru": "Фазы по порядку: <code>download</code> (сеть) → <code>parse</code> (V8 обходит каждый байт в AST) → <code>compile</code> (JIT превращает AST в байткод, затем в машинный код) → <code>execute</code> (код верхнего уровня выполняется в главном потоке). Быстрое соединение помогает только <code>download</code>. Parse, compile и execute идут на CPU пользователя после прихода байтов — и средний Android там в 4-8 раз медленнее MacBook, независимо от сети. Поэтому только parse для 500 КБ занимает 1.5-2 с на мобильном против доли этого на ноутбуке." }
    },
    {
      "id": "sandbox-budget-calc",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Compute the real cost of 500 KB on a phone", "ru": "Посчитай реальную цену 500 КБ на телефоне" },
      "prompt": { "en": "Use the lesson's numbers to compute Time to Interactive for a 500 KB uncompressed bundle on a mid-range Android over 4G. Download the gzip'd transfer over a 1.6 Mbps link, then parse the decompressed bytes at 300 KB/sec, then add the lesson's compile and execute costs. Edit the constants if you want, then run.", "ru": "Используя числа из урока, посчитай Time to Interactive для бандла в 500 КБ без сжатия на среднем Android по 4G. Скачай gzip-передачу по каналу 1.6 Мбит/с, затем распарси распакованные байты на скорости 300 КБ/с, затем добавь стоимость compile и execute из урока. Поменяй константы при желании и запусти." },
      "runtime": "js",
      "setup": "// --- inputs from the lesson ---\nconst uncompressedKB = 500;      // bytes on disk drive parse/compile\nconst gzipKB = 160;              // ~ what actually transfers over 4G\nconst linkMbps = 1.6;            // mobile 4G link\nconst parseRateKBps = 300;       // mid-range Android: 200-400 KB/sec\nconst compileMs = 300;          // lesson figure\nconst executeMs = 400;          // lesson figure\n\n// download: gzip bytes over the link (8 bits per byte)\nconst downloadMs = Math.round((gzipKB * 8) / linkMbps);\n// parse: V8 walks every DECOMPRESSED byte\nconst parseMs = Math.round((uncompressedKB / parseRateKBps) * 1000);\nconst ttiMs = downloadMs + parseMs + compileMs + executeMs;\n\nconsole.log('download ms:', downloadMs);\nconsole.log('parse ms:', parseMs);\nconsole.log('compile ms:', compileMs);\nconsole.log('execute ms:', executeMs);\nconsole.log('TTI ms:', ttiMs);\nconsole.log('Note: switching gzip->brotli shrinks ONLY downloadMs; parse/compile are set by uncompressedKB.');",
      "expected": { "kind": "no-error" }
    },
    {
      "id": "diagnose-brotli-illusion",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Why did brotli barely help?", "ru": "Почему brotli почти не помог?" },
      "prompt": { "en": "A team switched gzip → brotli. Transfer dropped from 180 KB to 150 KB and LCP improved 50 ms, but the phone still blocks for ~3 s before interactive. Fill in the blanks naming what brotli changed and what it did NOT change.", "ru": "Команда переключила gzip → brotli. Передача упала с 180 КБ до 150 КБ, LCP улучшился на 50 мс, но телефон всё ещё блокируется ~3 с до интерактивности. Заполни пропуски: что brotli изменил и что НЕ изменил." },
      "evidence": { "en": "transfer: 180 KB (gzip) -> 150 KB (brotli)\nLCP: -50 ms\nuncompressed bundle on disk: 500 KB (unchanged)\nmid-range Android TTI: ~3 s (unchanged)", "ru": "transfer: 180 KB (gzip) -> 150 KB (brotli)\nLCP: -50 ms\nuncompressed bundle on disk: 500 KB (unchanged)\nmid-range Android TTI: ~3 s (unchanged)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["download", "transfer", "download phase", "network"], "hint": { "en": "Compression only changes the bytes that travel over the wire.", "ru": "Сжатие меняет только байты, которые едут по проводу." } },
          { "id": "b2", "accept": ["uncompressed", "uncompressed size", "decompressed", "decompressed size", "on-disk size"], "hint": { "en": "Parse and compile cost is set by the size after the browser decompresses.", "ru": "Стоимость parse и compile задаётся размером после распаковки браузером." } },
          { "id": "b3", "accept": ["code splitting", "code-splitting", "ship less code", "reduce code", "split", "less code"], "hint": { "en": "To cut parse/compile you must reduce shipped bytes, not compress them — saves 50-90% per route.", "ru": "Чтобы срезать parse/compile, нужно уменьшить отправляемые байты, а не сжать их — экономит 50-90% на маршрут." } }
        ]
      }
    },
    {
      "id": "design-route-budget",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Set and enforce a bundle budget for a route", "ru": "Задай и обеспечь bundle budget для маршрута" },
      "prompt": { "en": "Design a bundle-budget policy for a checkout route that must stay interactive in ~2 s on a mid-range Android over 4G. Decide the byte cap, what it counts, where it is enforced, and how it stops growth-by-accretion. Self-grade against the rubric.", "ru": "Спроектируй политику bundle budget для маршрута оформления заказа, который должен оставаться интерактивным за ~2 с на среднем Android по 4G. Реши: лимит в байтах, что он считает, где он применяется и как останавливает рост по накоплению. Оцени себя по рубрике." },
      "constraints": { "en": "Target: interactive in ~2 s on mid-range Android (4-8x slower CPU than dev laptop, parse ~300 KB/sec) over 4G. Parse alone for 500 KB is 1.5-2 s, so the cap must keep uncompressed JS well under that. Team of 8 engineers, each adding libraries; no single PR is obviously to blame for drift.", "ru": "Цель: интерактивность за ~2 с на среднем Android (CPU в 4-8 раз медленнее ноутбука разработчика, parse ~300 КБ/с) по 4G. Только parse для 500 КБ — это 1.5-2 с, поэтому лимит должен держать несжатый JS заметно ниже этого. Команда из 8 инженеров, каждый добавляет библиотеки; ни один PR явно не виноват в дрейфе." },
      "rubric": [
        { "en": "Sets the cap on UNCOMPRESSED per-route JS (the thing that drives parse/compile), not just gzip transfer, and tracks CSS/images separately.", "ru": "Ставит лимит на НЕСЖАТЫЙ JS на маршрут (то, что определяет parse/compile), а не только на gzip-передачу, и считает CSS/картинки отдельно." },
        { "en": "Enforces the budget at CI so any PR over the cap fails — converting performance from intention to constraint, like an SLO for client bytes.", "ru": "Применяет бюджет в CI, чтобы любой PR сверх лимита падал — превращая производительность из намерения в ограничение, как SLO для клиентских байтов." },
        { "en": "Names a real lever to fit the budget (code splitting / lazy load / drop or swap a heavy dep) rather than relying on better compression.", "ru": "Называет реальный рычаг, чтобы влезть в бюджет (code splitting / ленивая загрузка / удаление или замена тяжёлой зависимости), а не надежду на лучшее сжатие." }
      ],
      "model": { "en": "Cap the checkout route at ~150-170 KB gzip transfer AND assert the uncompressed JS stays roughly under 450-500 KB, since parse/compile scale with the decompressed size, not the wire size. Count JS separately from CSS and images. Enforce in CI with a size-budget check (e.g. bundlesize / size-limit / Lighthouse CI) so any PR crossing the cap fails — the author must code-split, lazy-load below-the-fold widgets, or swap a heavy dependency, or explicitly justify the bump. Verify with Chrome DevTools 4x CPU throttling + 4G emulation, not on the dev laptop, so the ~2 s TTI target is measured against the real-user CPU gap.", "ru": "Ограничь маршрут оформления ~150-170 КБ gzip-передачи И проверяй, что несжатый JS остаётся примерно под 450-500 КБ, так как parse/compile масштабируются с распакованным размером, а не с размером в сети. Считай JS отдельно от CSS и картинок. Применяй в CI проверку size-budget (например bundlesize / size-limit / Lighthouse CI), чтобы любой PR сверх лимита падал — автор обязан сделать code splitting, ленивую загрузку виджетов вне первого экрана или заменить тяжёлую зависимость, либо явно обосновать рост. Проверяй в Chrome DevTools с троттлингом CPU 4x + эмуляцией 4G, а не на ноутбуке, чтобы цель ~2 с TTI мерилась с учётом реального разрыва по CPU." }
    }
  ]
}
