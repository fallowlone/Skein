{
  "lessonKey": "system-design-cases/05-data-money/05-stock-exchange",
  "track": "system-design-cases",
  "tasks": [
    {
      "id": "design-matching-engine",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Design the matching engine", "ru": "Спроектируй движок матчинга" },
      "prompt": { "en": "Design a fair, low-latency matching engine with durability and audit; justify the single-threaded core and how recovery works.", "ru": "Спроектируй честный, низколатентный движок матчинга с долговечностью и аудитом; обоснуй однопоточное ядро и как работает восстановление." },
      "constraints": { "en": "~10,000 instruments, peak ~1M+ orders+cancels/sec at the open. Match latency target under ~10 microseconds. Fairness is paramount: a single canonical order of events, identical on every replica, reproducible for audit years later. No acknowledged order may be lost across a crash.", "ru": "~10 000 инструментов, пик ~1М+ заявок+отмен/с на открытии. Цель задержки матчинга под ~10 микросекунд. Честность первостепенна: единый каноничный порядок событий, идентичный на каждой реплике, воспроизводимый для аудита годы спустя. Ни одна подтверждённая заявка не должна теряться через крэш." },
      "rubric": [
        { "en": "Funnels all orders through a single sequencer that assigns a global sequence number, defining the one canonical total order.", "ru": "Сливает все заявки через единый секвенсор, что назначает глобальный номер последовательности, определяя единый каноничный тотальный порядок." },
        { "en": "Runs a single-threaded matching engine over an in-memory order book by price-time priority — explaining why concurrency would break fairness and that single-thread is also faster (no locks, book in cache).", "ru": "Гоняет однопоточный движок матчинга над in-memory книгой по приоритету цена-время — объясняя, почему конкурентность сломала бы честность и что один поток ещё и быстрее (нет блокировок, книга в кэше)." },
        { "en": "Keeps the core deterministic (no clock reads or randomness inside the match; time/IDs captured as sequencer inputs) so the logged input stream IS the system.", "ru": "Держит ядро детерминированным (никаких чтений часов или случайности внутри матча; время/ID захвачены как входы секвенсора), так что лог входов ЕСТЬ система." },
        { "en": "Uses a durable, replicated event log (fsync + replication before ack) with periodic snapshots for bounded replay, and multicast market-data fan-out off the hot path.", "ru": "Использует durable, реплицируемый лог событий (fsync + репликация до ack) с периодическими снимками для ограниченного реплея и multicast фан-аут рыночных данных прочь с горячего пути." }
      ],
      "model": { "en": "Funnel every order and cancel through a single sequencer that stamps a global sequence number, defining the one canonical total order; append the sequenced stream to a durable, replicated event log (fsync + replication before acknowledging, so nothing acknowledged is lost). A single-threaded matching engine consumes that stream against an in-memory order book by price-time priority — single-threaded because fairness needs one total order (concurrent matching makes 'who filled first' scheduling-dependent and non-reproducible), and it's also faster here (no locks, no cache contention, book in cache). Keep the core strictly deterministic: no clock reads or randomness inside the match — the sequencer stamps timestamps and assigns IDs so they're logged inputs. Because the core is deterministic, the logged inputs ARE the system: replay from a periodic snapshot rebuilds the exact book, hot replicas stand at the same sequence number for failover, and an auditor's question is answered by replay. Scale by partitioning across instruments (independent books on separate engines), never by parallelizing one book. Fan out market data by multicast off the hot path.", "ru": "Сливай каждую заявку и отмену через единый секвенсор, что штампует глобальный номер последовательности, определяя единый каноничный тотальный порядок; добавляй секвенсированный поток в durable, реплицируемый лог событий (fsync + репликация до подтверждения, чтобы ничего подтверждённого не терялось). Однопоточный движок матчинга потребляет этот поток против in-memory книги по приоритету цена-время — однопоточный, потому что честность требует одного тотального порядка (конкурентный матчинг делает «кто первым» зависящим от планирования и невоспроизводимым), и он ещё и быстрее здесь (нет блокировок, нет конкуренции за кэш, книга в кэше). Держи ядро строго детерминированным: никаких чтений часов или случайности внутри матча — секвенсор штампует метки времени и назначает ID, чтобы они были логируемыми входами. Поскольку ядро детерминировано, логируемые входы ЕСТЬ система: реплей с периодического снимка перестраивает точную книгу, горячие реплики стоят на том же номере для failover, а вопрос аудитора отвечается реплеем. Масштабируй партиционированием по инструментам (независимые книги на отдельных движках), никогда не параллеля одну книгу. Фан-аут рыночных данных multicast'ом прочь с горячего пути." }
    },
    {
      "id": "diagnose-nondeterministic-core",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Replicas disagree on trades", "ru": "Реплики расходятся в сделках" },
      "prompt": { "en": "Two replicas fed the identical input stream produced different trades. Fill in the property that was violated and where the offending values must come from instead.", "ru": "Две реплики, накормленные идентичным потоком входов, произвели разные сделки. Впиши нарушенное свойство и откуда проблемные значения должны браться вместо этого." },
      "evidence": { "en": "replica 1 and replica 2 consumed the same sequenced orders\nyet they emitted different trade timestamps and trade IDs\nthe match reads System.now() and uses a random id generator inside the loop", "ru": "реплика 1 и реплика 2 потребили те же секвенсированные заявки\nно выдали разные метки времени сделок и ID сделок\nматч читает System.now() и использует генератор случайных id внутри цикла" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "property", "accept": ["determinism", "deterministic", "the determinism invariant", "determinism of the core"], "hint": { "en": "Same inputs must always produce the same outputs — what does a clock read or random id break?", "ru": "Одни входы должны всегда давать одни выходы — что ломает чтение часов или случайный id?" } },
          { "id": "source", "accept": ["the sequencer", "sequencer inputs", "captured as inputs", "logged inputs from the sequencer", "the sequenced input"], "hint": { "en": "Where should timestamps and IDs be stamped so they're part of the logged, replayable message?", "ru": "Где должны штамповаться метки времени и ID, чтобы они были частью логируемого, воспроизводимого сообщения?" } }
        ]
      }
    },
    {
      "id": "predict-price-time-fill",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Walk the order book", "ru": "Пройди по книге заявок" },
      "prompt": { "en": "Determine which resting orders fill the incoming market order and the resulting trades, then reveal.", "ru": "Определи, какие лежащие заявки исполняют входящую рыночную заявку и получившиеся сделки, затем открой." },
      "scenario": { "en": "The ask side has: 100.75 × 20 (arrived first), 100.75 × 10 (arrived second), 101.00 × 50. An incoming BUY 40 @ market arrives. Using price-time priority, which orders fill and at what prices? What if a second BUY 5 @ market then arrives?", "ru": "Сторона асков: 100,75 × 20 (пришла первой), 100,75 × 10 (пришла второй), 101,00 × 50. Приходит входящая КУПИТЬ 40 @ рынок. По приоритету цена-время, какие заявки исполняются и по каким ценам? А если затем приходит вторая КУПИТЬ 5 @ рынок?" },
      "reveal": { "en": "Price-time priority walks the best (lowest) ask first, FIFO within a price level. BUY 40 fills: 20 @ 100.75 (the earliest order at that level), then 10 @ 100.75 (the second order at that level), then 10 @ 101.00 — three trades, with the remaining 40 @ 101.00 left resting (now 40 of the original 50). The second BUY 5 @ market then fills 5 @ 101.00 against that resting order, leaving 35 @ 101.00. Best price first, and within a price level the earlier-sequenced order fills first — deterministic and reproducible, which is exactly why ties (same price) are resolved by sequence-number arrival order, not chance.", "ru": "Приоритет цена-время идёт по лучшему (низшему) аску первым, FIFO внутри ценового уровня. КУПИТЬ 40 исполняет: 20 @ 100,75 (самая ранняя заявка на этом уровне), затем 10 @ 100,75 (вторая заявка на этом уровне), затем 10 @ 101,00 — три сделки, с оставшимися 40 @ 101,00 лежащими (теперь 40 из исходных 50). Вторая КУПИТЬ 5 @ рынок затем исполняет 5 @ 101,00 против той лежащей заявки, оставляя 35 @ 101,00. Лучшая цена первой, а внутри ценового уровня более ранне-секвенсированная заявка исполняется первой — детерминированно и воспроизводимо, ровно поэтому ничьи (та же цена) решаются порядком прибытия по номеру последовательности, не случаем." }
    },
    {
      "id": "fix-disk-in-hot-path",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Fix the latency-killing log write", "ru": "Почини убивающую задержку запись лога" },
      "prompt": { "en": "This match loop blocks on a synchronous fsync per order, blowing the microsecond budget. Keep durability but remove the stall; describe the fix, then check against the model.", "ru": "Этот цикл матчинга блокируется на синхронном fsync на заявку, взрывая микросекундный бюджет. Сохрани долговечность, но убери затык; опиши починку, затем сверься с образцом." },
      "starter": "for order in sequenced_stream:\n    log.append(order)\n    log.fsync()          # blocks ~ms per order, on the hot path\n    trades = book.match(order)   # microseconds\n    publish(trades)",
      "grading": {
        "mode": "self",
        "model": { "en": "The per-order synchronous fsync (~milliseconds) sits in series with a microsecond match, so durability is serializing the hot path and destroying latency. The fix keeps durability but takes the stall off the critical path: batch log writes (append many sequenced messages, fsync the batch) and overlap replication so the durable write proceeds concurrently rather than blocking each match; write to fast storage. Crucially, because the engine is deterministic and replicas consume the same sequenced log, you can acknowledge once the message is durably sequenced/replicated without each match waiting on its own fsync. Recovery stays exact via the log + periodic snapshots. The principle: never put a synchronous disk sync in series with a microsecond match — batch it, overlap it, and lean on determinism + replication for durability.", "ru": "Синхронный fsync на заявку (~миллисекунды) стоит последовательно с микросекундным матчем, так что долговечность сериализует горячий путь и разрушает задержку. Починка сохраняет долговечность, но убирает затык с критического пути: батчь записи лога (добавь много секвенсированных сообщений, fsync батча) и перекрывай репликацию, чтобы долговечная запись шла конкурентно, а не блокировала каждый матч; пиши в быстрое хранилище. Критически: поскольку движок детерминирован и реплики потребляют тот же секвенсированный лог, можно подтверждать, как только сообщение долговечно секвенсировано/реплицировано, без ожидания каждым матчем своего fsync. Восстановление остаётся точным через лог + периодические снимки. Принцип: никогда не ставь синхронный sync диска последовательно с микросекундным матчем — батчь его, перекрывай и опирайся на детерминизм + репликацию для долговечности." },
        "rubric": [
          { "en": "Identifies the per-order synchronous fsync as a hot-path stall serializing the microsecond match.", "ru": "Называет синхронный fsync на заявку затыком горячего пути, что сериализует микросекундный матч." },
          { "en": "Keeps durability by batching log writes and overlapping replication off the critical path (not by dropping durability).", "ru": "Сохраняет долговечность батчингом записей лога и перекрытием репликации прочь с критического пути (не дропом долговечности)." }
        ]
      }
    }
  ]
}
