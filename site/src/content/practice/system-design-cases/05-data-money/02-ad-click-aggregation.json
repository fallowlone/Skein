{
  "lessonKey": "system-design-cases/05-data-money/02-ad-click-aggregation",
  "track": "system-design-cases",
  "tasks": [
    {
      "id": "design-click-pipeline",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Design the billing-grade click pipeline", "ru": "Спроектируй конвейер кликов биллинг-уровня" },
      "prompt": { "en": "Design a pipeline that counts ad clicks for both a real-time dashboard and an authoritative billing total; justify each correctness mechanism.", "ru": "Спроектируй конвейер, что считает клики и для real-time дашборда, и для авторитетного итога биллинга; обоснуй каждый механизм корректности." },
      "constraints": { "en": "Peak ~1M clicks/sec, ~100 B each. Clients retry on timeout (duplicate events with the same click_id). Some clicks arrive minutes-to-hours late from flaky mobile. Dashboard must be seconds-fresh; billing must be exactly correct but may lag by hours. A single double-counted click is a billing dispute.", "ru": "Пик ~1М кликов/с, ~100 Б каждый. Клиенты ретраят при таймауте (дубликаты с тем же click_id). Часть кликов приходит с опозданием на минуты-часы с дрянной мобильной связи. Дашборд должен быть свежим в секунды; биллинг должен быть в точности корректен, но может отставать на часы. Один дважды посчитанный клик — спор по биллингу." },
      "rubric": [
        { "en": "Ingests into a durable, partitioned, replayable log (Kafka) as the source of truth, keeping raw events for reconciliation and disputes.", "ru": "Принимает в durable, партиционированный, воспроизводимый лог (Kafka) как источник истины, храня сырые события для сверки и споров." },
        { "en": "Aggregates by EVENT time with windows closed on a watermark sized for the common case (a minute or two), accepting that rare late events miss the stream window.", "ru": "Агрегирует по EVENT time с окнами, закрываемыми по watermark под обычный случай (минута-две), принимая, что редкие поздние события минуют потоковое окно." },
        { "en": "Achieves exactly-once as at-least-once + idempotent/transactional application: dedup retries by click_id, checkpoints + a transactional sink so a crash-replay doesn't double-count.", "ru": "Достигает exactly-once как at-least-once + идемпотентное/транзакционное применение: дедуп ретраев по click_id, checkpoint'ы + транзакционный sink, чтобы крэш-реплей не посчитал дважды." },
        { "en": "Adds a batch reconciliation layer (lambda) that re-reads all raw events with no time pressure for the authoritative billing total — fast path approximate, slow path correct.", "ru": "Добавляет пакетный слой сверки (lambda), что перечитывает все сырые события без давления времени для авторитетного итога биллинга — быстрый путь приближён, медленный корректен." }
      ],
      "model": { "en": "Ingest clicks into a partitioned, replayable Kafka log (the source of truth; keep raw events in cold storage for reconciliation and disputes). A Flink job aggregates by EVENT time, closing per-minute windows on a watermark lagged ~1–2 minutes for the common case. Achieve exactly-once as at-least-once + idempotent/transactional application: dedup retries by the edge-generated click_id within the open window, and bind the output commit to the input offset via periodic checkpoints + a transactional/idempotent sink so a crash-replay doesn't double-count. The fast path feeds the seconds-fresh dashboard. A daily batch reconciliation job (lambda) re-reads ALL raw events — including the hours-late ones — with no watermark pressure, computes the authoritative billing total, and corrects the fast path. Hot ad keys get a salt to spread across partitions. Fast path approximate; slow path is the money number.", "ru": "Принимай клики в партиционированный, воспроизводимый лог Kafka (источник истины; храни сырые события в холодном хранилище для сверки и споров). Задача Flink агрегирует по EVENT time, закрывая поминутные окна по watermark с отставанием ~1–2 минуты под обычный случай. Достигай exactly-once как at-least-once + идемпотентное/транзакционное применение: дедуп ретраев по сгенерированному на краю click_id в открытом окне и связка коммита выхода с входным оффсетом через периодические checkpoint'ы + транзакционный/идемпотентный sink, чтобы крэш-реплей не посчитал дважды. Быстрый путь питает дашборд свежестью в секунды. Дневная пакетная задача сверки (lambda) перечитывает ВСЕ сырые события — включая опоздавшие на часы — без давления watermark, считает авторитетный итог биллинга и поправляет быстрый путь. Горячие ключи рекламы получают соль для размазывания по партициям. Быстрый путь приближён; медленный — число денег." }
    },
    {
      "id": "diagnose-double-count-on-replay",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "The double-count after a crash", "ru": "Двойной счёт после крэша" },
      "prompt": { "en": "After a stream-job restart, some clicks are counted twice. Fill in the mechanism that should have prevented it and where the side effect must move.", "ru": "После рестарта потоковой задачи часть кликов посчитана дважды. Впиши механизм, что должен был предотвратить это, и куда должен переехать сайд-эффект." },
      "evidence": { "en": "job crashed mid-window, recovered from last checkpoint, replayed\nchargeAdvertiser() runs inside the aggregation operator\nresult: some advertisers charged twice for the same window", "ru": "задача упала посреди окна, восстановилась с последнего checkpoint, переиграла\nchargeAdvertiser() выполняется внутри оператора агрегации\nрезультат: часть рекламодателей списана дважды за то же окно" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "mechanism", "accept": ["exactly-once", "exactly once", "idempotent application", "idempotency", "transactional sink"], "hint": { "en": "The property that binds output commit to input offset so a replay doesn't re-apply — but only inside the engine's boundary.", "ru": "Свойство, что связывает коммит выхода с входным оффсетом, чтобы реплей не применил снова — но лишь внутри границы движка." } },
          { "id": "location", "accept": ["outside the boundary", "external side effect", "past the sink", "behind a transactional sink", "outside the checkpoint boundary", "to an idempotent sink"], "hint": { "en": "Where does the HTTP charge live relative to the engine's checkpoint/transaction boundary, and where must it move?", "ru": "Где живёт HTTP-списание относительно границы checkpoint/транзакции движка, и куда оно должно переехать?" } }
        ]
      }
    },
    {
      "id": "predict-watermark-lag",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Pick the watermark lag", "ru": "Выбери отставание watermark" },
      "prompt": { "en": "Reason about the latency vs completeness tradeoff for the lateness profile below, then reveal.", "ru": "Порассуждай о компромиссе задержка против полноты для профиля опоздания ниже, затем открой." },
      "scenario": { "en": "99.5% of clicks arrive within 8 seconds, 0.4% within 5 minutes, and 0.1% can take up to 40 minutes (subway/airplane mode). The dashboard SLO is 'fresh within ~1 minute.' What watermark lag do you set, and what happens to the 0.1%?", "ru": "99,5% кликов приходят за 8 секунд, 0,4% за 5 минут, а 0,1% могут брать до 40 минут (метро/режим полёта). SLO дашборда — «свежесть в пределах ~1 минуты». Какое отставание watermark ставишь, и что происходит с 0,1%?" },
      "reveal": { "en": "Size the watermark for the common case, not the worst case — roughly 1–2 minutes covers 99.9% of clicks and keeps the dashboard within its ~1-minute freshness SLO. Setting it to 40 minutes to rescue the 0.1% would tax EVERY dashboard number with 40 minutes of staleness — a terrible trade. The 0.1% truly-late clicks miss their stream window and are dropped from the fast path; they are recovered by the batch reconciliation layer, which re-reads all raw events with no time pressure for the authoritative billing total. You keep the fast path fast and let the slow path be correct.", "ru": "Размерь watermark под обычный случай, не худший — примерно 1–2 минуты покрывают 99,9% кликов и держат дашборд в его ~1-минутном SLO свежести. Выставление в 40 минут ради спасения 0,1% обложило бы КАЖДОЕ число дашборда 40 минутами устаревания — ужасный обмен. Эти 0,1% по-настоящему поздних кликов минуют своё потоковое окно и отбрасываются с быстрого пути; они восстанавливаются пакетным слоем сверки, что перечитывает все сырые события без давления времени для авторитетного итога биллинга. Держишь быстрый путь быстрым и даёшь медленному быть корректным." }
    },
    {
      "id": "fix-processing-time-windows",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Fix the wrong-minute counts", "ru": "Почини счёт в неверной минуте" },
      "prompt": { "en": "Per-minute billing counts are wrong: late clicks land in the minute they arrived, not the minute they happened. Identify the flaw and the fix, then check against the model.", "ru": "Поминутные счётчики биллинга неверны: поздние клики падают в минуту прибытия, не в минуту, когда случились. Назови дефект и починку, затем сверься с образцом." },
      "starter": "// windowing by when the event was RECEIVED\nstream\n  .keyBy(click -> click.adId)\n  .window(TumblingProcessingTimeWindows.of(Time.minutes(1)))\n  .aggregate(new CountClicks());",
      "grading": {
        "mode": "self",
        "model": { "en": "The window is keyed on PROCESSING time (when the event was received), so a click that happened at 14:05 but arrived at 14:09 (flaky connection) is counted in the 14:09 minute — corrupting per-minute billing. Fix by windowing on EVENT time (the timestamp stamped on the device at click time) with a watermark to close windows: switch to TumblingEventTimeWindows and assign a timestamp/watermark strategy (e.g. bounded-out-of-orderness of ~1–2 minutes). Now each click counts in the minute it actually happened; truly-late stragglers that miss the closed window are recovered by the batch reconciliation layer. Processing-time windows are fine for ops dashboards but wrong for billing, where the event's own time is what you bill.", "ru": "Окно ключуется на PROCESSING time (когда событие получено), так что клик, что случился в 14:05, но пришёл в 14:09 (дрянная связь), считается в минуте 14:09 — портя поминутный биллинг. Чинится оконностью по EVENT time (метка времени, проставленная на устройстве в момент клика) с watermark для закрытия окон: переключись на TumblingEventTimeWindows и назначь стратегию timestamp/watermark (напр. bounded-out-of-orderness ~1–2 минуты). Теперь каждый клик считается в минуте, когда реально случился; по-настоящему поздние опоздавшие, что минуют закрытое окно, восстанавливаются пакетным слоем сверки. Окна по processing time норм для ops-дашбордов, но неверны для биллинга, где собственное время события — то, что биллингуешь." },
        "rubric": [
          { "en": "Identifies that windowing by processing time places late clicks in the wrong minute, corrupting per-minute billing.", "ru": "Называет, что оконность по processing time кладёт поздние клики в неверную минуту, портя поминутный биллинг." },
          { "en": "Switches to event-time windows with a watermark, and notes the batch layer recovers true stragglers.", "ru": "Переключается на окна по event time с watermark и отмечает, что пакетный слой восстанавливает настоящих опоздавших." }
        ]
      }
    }
  ]
}
