{
  "lessonKey": "system-design-cases/03-media-storage/04-distributed-email",
  "track": "system-design-cases",
  "tasks": [
    {
      "id": "design-mailbox-partitioning",
      "type": "design",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Design partitioning and search", "ru": "Спроектируй партиционирование и поиск" },
      "prompt": { "en": "Design how Gmail-scale email partitions its data and serves sub-100 ms full-text search over a 200,000-message mailbox.", "ru": "Спроектируй, как почта масштаба Gmail партиционирует данные и обслуживает полнотекстовый поиск за sub-100 мс по ящику на 200 000 сообщений." },
      "constraints": { "en": "There are ~1 billion mailboxes; a single mailbox can hold hundreds of thousands of messages. Search must return in tens of milliseconds and you never search across users. Reads dominate writes.", "ru": "Есть ~1 миллиард ящиков; один ящик может держать сотни тысяч сообщений. Поиск должен возвращать за десятки миллисекунд, и ты никогда не ищешь по пользователям. Чтения доминируют над записями." },
      "rubric": [
        { "en": "Partition everything by user (message store, search index, change log) because mailboxes are independent with no cross-user joins on the hot path.", "ru": "Партиционируй всё по пользователю (хранилище сообщений, индекс поиска, журнал изменений), ведь ящики независимы без кросс-юзер-джойнов на горячем пути." },
        { "en": "Search uses a per-user inverted index (term → list of message IDs), so a query intersects short posting lists in tens of ms instead of scanning the mailbox.", "ru": "Поиск использует инвертированный индекс на пользователя (терм → список ID сообщений), так что запрос пересекает короткие posting-списки за десятки мс вместо скана ящика." },
        { "en": "The index is updated asynchronously by an indexer as mail arrives, and stays local to one user's shard (never global).", "ru": "Индекс обновляется асинхронно индексатором по мере прихода почты и остаётся локальным для шарда одного пользователя (никогда не глобальным)." }
      ],
      "model": { "en": "The natural partition key is the user, because mailboxes are independent — your inbox never needs to join against mine on the hot path. Shard the message store, the search index, and the change log all by user ID; this scales horizontally with no hot cross-shard queries. Search cannot be a scan: a 200,000-message mailbox queried for 'invoice acme march' must answer in tens of milliseconds, so build a per-user inverted index — a map from each term to the list of message IDs containing it. A query then intersects a few short posting lists rather than reading every message. The index is updated asynchronously by an indexer as mail is delivered, and because it's per-user it stays small and the query is local to one shard — you never search across users, so the index never needs to be global. This per-user partitioning is the cleanest sharding in the unit precisely because email data has no cross-user coupling on the common path.", "ru": "Естественный ключ партиции — пользователь, ведь ящики независимы — твоему inbox никогда не нужно джойниться с моим на горячем пути. Шардируй хранилище сообщений, индекс поиска и журнал изменений все по ID пользователя; это масштабируется горизонтально без горячих кросс-шард запросов. Поиск не может быть сканом: ящик на 200 000 сообщений, запрошенный по «счёт acme март», должен ответить за десятки миллисекунд, так что строй инвертированный индекс на пользователя — отображение каждого терма в список ID сообщений, его содержащих. Запрос тогда пересекает несколько коротких posting-списков, а не читает каждое сообщение. Индекс обновляется асинхронно индексатором по мере доставки почты, и поскольку он на пользователя, он остаётся малым, а запрос локален для одного шарда — ты никогда не ищешь по пользователям, так что индексу не нужно быть глобальным. Это партиционирование на пользователя — чистейший шардинг в юните именно потому, что данные почты не имеют кросс-юзер связности на общем пути." }
    },
    {
      "id": "design-attachment-fanout",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Design attachment storage for a 50M-recipient send", "ru": "Спроектируй хранение вложения для отправки 50M получателям" },
      "prompt": { "en": "A 2 MB PDF is emailed to 50 million subscribers. Design attachment storage and delivery so this is affordable.", "ru": "PDF на 2 МБ отправлен по почте 50 миллионам подписчиков. Спроектируй хранение и доставку вложения, чтобы это было по карману." },
      "constraints": { "en": "Each recipient genuinely receives the message in their mailbox, but you must not store 50 million copies of the identical 2 MB PDF (that would be 100 TB for one send). Recipients must keep their copy logically even if the sender deletes theirs.", "ru": "Каждый получатель действительно получает сообщение в своём ящике, но ты не должен хранить 50 миллионов копий идентичного PDF на 2 МБ (это было бы 100 ТБ на одну отправку). Получатели должны логически сохранять свою копию, даже если отправитель удалит свою." },
      "rubric": [
        { "en": "Split the attachment from the message: store the bytes content-addressed and deduplicated in object storage (one physical blob), keep a reference count.", "ru": "Отдели вложение от сообщения: храни байты адресуемо по содержимому и дедуплицированно в объектном хранилище (один физический блоб), держи счётчик ссылок." },
        { "en": "Each delivered message holds only a pointer (content hash), not the bytes; fan-out writes 50M tiny message rows, but the 2 MB is stored once.", "ru": "Каждое доставленное сообщение держит лишь указатель (контент-хеш), не байты; fan-out пишет 50M крошечных строк сообщений, но 2 МБ хранятся однажды." },
        { "en": "Distinguishes the unavoidable delivery fan-out (small rows) from the avoidable duplicate-byte storage (eliminated by dedup + ref-counting).", "ru": "Различает неизбежный fan-out доставки (малые строки) от избегаемого хранения дублирующих байт (устранено дедупом + ref-counting)." }
      ],
      "model": { "en": "A message is a small structured part (headers, body, label set) glued to potentially large attachments — store them separately. Put the attachment bytes in object storage, content-addressed and deduplicated: hash the 2 MB PDF and key the blob by its hash, so it is stored exactly once with a reference count. Each of the 50 million delivered messages then holds only a pointer (the content hash), not the bytes. The send still fans out 50 million genuine message rows — each recipient really does receive the message — but those rows are tiny without the embedded attachment, so the heavy cost (100 TB of duplicate bytes) collapses to a single 2 MB blob plus 50M small pointers. The reference count means the blob is deleted only when the last referencing message is gone, so a recipient keeps their copy logically even if the sender deletes theirs. The key insight is to separate 'deliver to 50M inboxes' (unavoidable small writes) from 'store 50M copies of the attachment' (avoidable, via dedup).", "ru": "Сообщение — малая структурированная часть (заголовки, тело, набор меток), склеенная с потенциально крупными вложениями — храни их раздельно. Клади байты вложения в объектное хранилище, адресуемо по содержимому и дедуплицированно: хешируй PDF на 2 МБ и ключуй блоб его хешем, так что он хранится ровно однажды со счётчиком ссылок. Каждое из 50 миллионов доставленных сообщений тогда держит лишь указатель (контент-хеш), не байты. Отправка всё же разворачивается в 50 миллионов настоящих строк сообщений — каждый получатель действительно получает сообщение — но эти строки крошечны без встроенного вложения, так что тяжёлая стоимость (100 ТБ дублирующих байт) схлопывается в один блоб 2 МБ плюс 50M малых указателей. Счётчик ссылок значит, что блоб удаляется лишь когда уходит последнее ссылающееся сообщение, так что получатель логически сохраняет свою копию, даже если отправитель удалит свою. Ключевой инсайт — отделить «доставить в 50M inbox-ов» (неизбежные малые записи) от «хранить 50M копий вложения» (избегаемо, через дедуп)." }
    },
    {
      "id": "diagnose-folders-vs-labels",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Folders fighting the domain", "ru": "Папки воюют против домена почты" },
      "prompt": { "en": "Diagnose the data-model symptom below and name the wrong model and the right one.", "ru": "Диагностируй симптом модели данных ниже и назови неверную модель и верную." },
      "evidence": { "en": "messages have a single parent_folder_id\nusers complain: a receipt that is also work-related can only live in ONE place\n'archiving' physically moves the message row out of Inbox\nshowing 'all Work mail' requires walking a folder tree\nrequested feature: a message tagged Work AND Receipts AND Important at once — impossible", "ru": "у сообщений один parent_folder_id\nпользователи жалуются: чек, что также рабочий, может жить лишь в ОДНОМ месте\n«архивация» физически перемещает строку сообщения из Inbox\nпоказ «всей рабочей почты» требует обхода дерева папок\nзапрошенная фича: сообщение с тегами Work И Receipts И Important сразу — невозможно" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "wrong_model", "accept": ["folders", "folder", "single-location folders", "parent folder", "folder model", "single location"], "hint": { "en": "A single parent_folder_id forces a message into exactly one location.", "ru": "Один parent_folder_id форсирует сообщение в ровно одну локацию." } },
          { "id": "right_model", "accept": ["labels", "label", "many-to-many labels", "tags", "tagging", "label overlay", "many-to-many overlay"], "hint": { "en": "Model organization as a set of tags a message can have many of, not a location.", "ru": "Моделируй организацию как набор тегов, которых у сообщения может быть много, не локацию." } }
        ]
      }
    },
    {
      "id": "fix-store-then-filter-spam",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Fix the store-then-filter spam pipeline", "ru": "Почини спам-пайплайн храни-затем-фильтруй" },
      "prompt": { "en": "This pipeline stores every inbound message, then filters spam afterward. Explain why it's wasteful at scale and the fix, then check against the model.", "ru": "Этот пайплайн хранит каждое входящее сообщение, затем фильтрует спам после. Объясни, почему это расточительно на масштабе, и починку, затем сверься с образцом." },
      "starter": "def handle_inbound(msg):\n    store_durably(msg)          # write to mailbox shard, replicate\n    index_for_search(msg)       # add to inverted index\n    if classify(msg) == 'spam': # ~90% of inbound is spam\n        mark_as_spam(msg)       # already stored, indexed, replicated",
      "grading": {
        "mode": "self",
        "model": { "en": "The flaw is ordering: it durably stores, indexes, and replicates every inbound message and only then classifies spam — but ~90% of inbound is spam, so you're paying to store, index, and replicate roughly 10× the mail you actually keep, plus the search index is polluted with junk. The fix is to filter at SMTP ingest, before storage. Apply cheap rejects first at the SMTP stage — sender IP reputation / DNSBL, SPF/DKIM/DMARC authentication failures, rate limits — to drop obvious spam at the door, then run content classification (ML over headers/body) and either reject outright or route to a Spam label without indexing it as normal mail. The principle is reject early and cheap: never spend durable storage, replication, or indexing on mail you're going to throw away. Reordered: classify/screen at ingest → only accepted mail is stored, replicated, and indexed.", "ru": "Изъян — порядок: он долговечно хранит, индексирует и реплицирует каждое входящее сообщение и лишь затем классифицирует спам — но ~90% входящего спам, так что ты платишь за хранение, индексацию и репликацию примерно 10× почты, что реально держишь, плюс индекс поиска засорён мусором. Починка — фильтровать на SMTP-приёме, до хранения. Применяй дешёвые отказы первыми на стадии SMTP — репутация IP отправителя / DNSBL, провалы аутентификации SPF/DKIM/DMARC, рейт-лимиты — чтобы отбросить очевидный спам на пороге, затем прогони классификацию контента (ML по заголовкам/телу) и либо отвергни напрочь, либо направь в метку Spam без индексации как нормальной почты. Принцип — отвергай рано и дёшево: никогда не трать долговечное хранилище, репликацию или индексацию на почту, что выбросишь. Переупорядочено: классифицируй/проверяй на приёме → лишь принятая почта хранится, реплицируется и индексируется." },
        "rubric": [
          { "en": "Names the waste: storing/indexing/replicating ~10× the kept mail because ~90% is spam filtered only after storage.", "ru": "Называет расточительство: хранение/индексация/репликация ~10× хранимой почты, ведь ~90% — спам, фильтруемый лишь после хранения." },
          { "en": "Fix moves filtering to SMTP ingest before storage (cheap IP/auth checks then content classification), so only accepted mail is stored/indexed.", "ru": "Починка переносит фильтрацию на SMTP-приём до хранения (дешёвые IP/auth-проверки, затем классификация контента), так что хранится/индексируется лишь принятая почта." }
        ]
      }
    }
  ]
}
