{
  "lessonKey": "system-design-cases/03-media-storage/03-object-storage",
  "track": "system-design-cases",
  "tasks": [
    {
      "id": "design-durability-scheme",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Design the durability scheme", "ru": "Спроектируй схему долговечности" },
      "prompt": { "en": "Design how an S3-like store reaches ~11 nines of durability for a mix of tiny hot objects and huge cold archives, at minimum cost.", "ru": "Спроектируй, как хранилище как S3 достигает ~11 девяток долговечности для смеси крошечных горячих объектов и огромных холодных архивов при минимальной стоимости." },
      "constraints": { "en": "Commodity disks fail at ~1–4%/year and thousands fail per day at fleet scale, so durability must come from redundancy across independent failure domains. Storage cost is the binding constraint at exabyte scale; the tiny hot objects are read constantly while the cold archives are read rarely.", "ru": "Commodity-диски отказывают на ~1–4%/год, и тысячи отказывают в день на масштабе флота, так что долговечность должна идти от избыточности по независимым доменам отказа. Стоимость хранения — связывающее ограничение на масштабе эксабайт; крошечные горячие объекты читаются постоянно, а холодные архивы редко." },
      "rubric": [
        { "en": "Spreads redundancy across independent failure domains (different racks, zones, datacenters), not disks in one machine.", "ru": "Размазывает избыточность по независимым доменам отказа (разные стойки, зоны, ЦОД), не диски в одной машине." },
        { "en": "Erasure-codes large cold data (e.g. 10+4: reconstruct from any 10 of 14 shards, tolerate 4 losses at ~1.4× overhead) instead of 3× replication.", "ru": "Erasure-кодирует крупные холодные данные (напр. 10+4: реконструкция из любых 10 из 14 шардов, терпит 4 потери при ~1.4× накладных) вместо 3× репликации." },
        { "en": "Replicates small hot objects (cheap single-copy reads) because erasure-code read fan-out/reconstruction would hurt their latency; notes durability is a race between failure and background repair.", "ru": "Реплицирует малые горячие объекты (дешёвые чтения одной копии), ведь fan-out/реконструкция erasure-code били бы их задержку; отмечает, что долговечность — гонка между отказом и фоновым ремонтом." }
      ],
      "model": { "en": "Durability comes from redundancy across independent failure domains — shards/copies on different racks, zones, and datacenters, so no single failure (disk, machine, rack, zone) can lose data. Tier the scheme by object profile. For large cold archives, use erasure coding: split each object into k data + m parity shards (e.g. 10+4) computed with Reed-Solomon, store all 14 on independent nodes, and reconstruct the original from any 10 — tolerating 4 simultaneous losses at only ~1.4× storage overhead versus 3× (200%) for triple replication, with better fault tolerance. That storage saving is decisive at exabyte scale and the higher per-read reconstruction cost is fine because cold data is rarely read. For tiny hot objects, replicate instead (e.g. 3 copies): a single-copy read is cheap, while erasure coding would force gathering k shards plus a reconstruction computation per read, hurting tail latency for the most-read objects. Finally, eleven nines is a statement about the race between failure and repair: background re-replication / re-encoding must rebuild lost shards faster than new failures accumulate, so repair throughput is a first-class requirement.", "ru": "Долговечность идёт от избыточности по независимым доменам отказа — шарды/копии на разных стойках, зонах и ЦОД, так что ни один отказ (диск, машина, стойка, зона) не теряет данные. Тируй схему по профилю объекта. Для крупных холодных архивов используй erasure coding: разбей каждый объект на k data + m parity шардов (напр. 10+4), посчитанных Рид-Соломоном, храни все 14 на независимых узлах и реконструируй оригинал из любых 10 — терпя 4 одновременные потери при лишь ~1.4× накладных против 3× (200%) для тройной репликации, при лучшей отказоустойчивости. Эта экономия хранилища решает на масштабе эксабайт, а высокая стоимость реконструкции на чтение нормальна, ведь холодные данные редко читаются. Для крошечных горячих объектов реплицируй (напр. 3 копии): чтение одной копии дёшево, тогда как erasure coding форсировал бы сбор k шардов плюс реконструкцию на чтение, ударяя по хвостовой задержке самых читаемых объектов. Наконец, одиннадцать девяток — утверждение о гонке между отказом и ремонтом: фоновая ре-репликация / ре-кодировка должна перестраивать потерянные шарды быстрее, чем накапливаются новые отказы, так что пропускная ремонта — требование первого класса." }
    },
    {
      "id": "predict-erasure-overhead",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Overhead and fault tolerance: EC vs 3×", "ru": "Накладные и отказоустойчивость: EC vs 3×" },
      "prompt": { "en": "Compute and compare, then reveal.", "ru": "Посчитай и сравни, затем открой." },
      "scenario": { "en": "You must store 100 PB of cold data. Option A is 3× replication; Option B is 10+4 erasure coding. For each, what raw storage is consumed and how many simultaneous shard/copy losses can a given object survive? Which would you pick and why?", "ru": "Тебе нужно хранить 100 ПБ холодных данных. Вариант A — 3× репликация; вариант B — 10+4 erasure coding. Для каждого: сколько сырого хранилища потребляется и сколько одновременных потерь шардов/копий переживёт данный объект? Что выберешь и почему?" },
      "reveal": { "en": "Option A (3×): raw storage = 100 PB × 3 = 300 PB (200% overhead); an object survives 2 lost copies (3 copies, need 1). Option B (10+4): raw storage = 100 PB × 14/10 = 140 PB (~40% overhead); an object survives 4 lost shards (14 shards, need any 10). So erasure coding uses 160 PB LESS raw storage AND tolerates more simultaneous losses (4 vs 2). For cold data — read rarely, so the per-read reconstruction cost doesn't matter — Option B is the clear pick; the only reason to prefer replication is for small, hot objects where single-copy read latency matters. The lesson: at exabyte scale, 1.4× vs 3× is the difference between viable and bankrupt.", "ru": "Вариант A (3×): сырое хранилище = 100 ПБ × 3 = 300 ПБ (+200% накладных); объект переживает 2 потерянные копии (3 копии, нужна 1). Вариант B (10+4): сырое хранилище = 100 ПБ × 14/10 = 140 ПБ (~40% накладных); объект переживает 4 потерянных шарда (14 шардов, нужны любые 10). Так erasure coding использует на 160 ПБ МЕНЬШЕ сырого хранилища И терпит больше одновременных потерь (4 vs 2). Для холодных данных — читаются редко, так что стоимость реконструкции на чтение не важна — вариант B явный выбор; единственная причина предпочесть репликацию — малые горячие объекты, где важна задержка чтения одной копии. Урок: на масштабе эксабайт 1.4× vs 3× — разница между жизнеспособным и банкротством." }
    },
    {
      "id": "design-namespace-and-keys",
      "type": "design",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Design the flat namespace and key scheme", "ru": "Спроектируй плоское пространство имён и схему ключей" },
      "prompt": { "en": "Design the namespace and a key-naming scheme for a billion objects, and explain why filesystem assumptions (cheap LIST, O(1) folder rename) don't hold.", "ru": "Спроектируй пространство имён и схему именования ключей для миллиарда объектов и объясни, почему допущения файловой системы (дешёвый LIST, O(1) переименование папки) не держатся." },
      "constraints": { "en": "A team's spec assumes LIST prefix/ is a cheap directory listing, that 'renaming folder logs/ to archive/' is an atomic O(1) flip, and writes everything under one prefix uploads/2024/ for a billion objects. The store must scale to trillions of keys with high write throughput.", "ru": "Спека команды предполагает, что LIST prefix/ — дешёвый листинг директории, что «переименование папки logs/ в archive/» — атомарный O(1) флип, и пишет всё под один префикс uploads/2024/ для миллиарда объектов. Хранилище должно масштабироваться до триллионов ключей с высокой пропускной записи." },
      "rubric": [
        { "en": "Explains the flat namespace: a key is one opaque string, slashes are convention, there is no real directory tree — which is why it scales to trillions of keys (just a sorted key→location index).", "ru": "Объясняет плоское пространство имён: ключ — одна непрозрачная строка, слеши — соглашение, реального дерева директорий нет — потому оно масштабируется до триллионов ключей (лишь сортированный индекс ключ→локация)." },
        { "en": "Notes LIST prefix is a range scan over the key index (fine for moderate prefixes) and 'renaming a folder' is O(number of objects) — copy every object to new keys and delete the old, not an O(1) flip.", "ru": "Отмечает, что LIST по префиксу — range-скан по индексу ключей (нормально для умеренных префиксов), а «переименование папки» — O(число объектов): копировать каждый объект под новые ключи и удалить старые, не O(1) флип." },
        { "en": "Designs keys to spread load across the metadata index (avoid a single hot prefix / monotonic prefixes that all hit one partition), e.g. by hashing or distributing the leading key component.", "ru": "Проектирует ключи так, чтобы размазать нагрузку по индексу метаданных (избегая одного горячего префикса / монотонных префиксов, что все бьют в одну партицию), например хешируя или распределяя ведущий компонент ключа." }
      ],
      "model": { "en": "The store exposes a flat key→bytes namespace: a key like uploads/2024/cat.jpg is a single opaque string, not a path through real directories — the slashes are just convention for prefix LIST queries. Behind it is a giant sorted key→location index partitioned (by key hash or range) across the metadata store, which is exactly why it scales to trillions of keys: there is no directory tree to walk or rebalance. Three filesystem assumptions break. LIST prefix/ is a range scan over the key index, fine for moderate prefixes but pathological if you cram a billion keys under one prefix. 'Renaming folder logs/ to archive/' is not an O(1) metadata flip — there are no folders — it means copying every object to new keys and deleting the old ones, O(number of objects). And listing can be eventually consistent in some systems even where GET is strong. Design the keys to spread load across the index: avoid sequential/monotonic prefixes (like uploads/2024/...) that all land on one metadata partition and create a write hotspot; instead distribute the leading component (e.g. hash-prefix the key, or interleave a high-cardinality field first) so writes and LISTs fan out across partitions. Never assume filesystem semantics under the flat-namespace hood.", "ru": "Хранилище даёт плоское пространство ключ→байты: ключ вроде uploads/2024/cat.jpg — одна непрозрачная строка, не путь сквозь реальные директории — слеши лишь соглашение для префиксных LIST-запросов. За ним гигантский сортированный индекс ключ→локация, разбитый (по хешу или диапазону ключа) по хранилищу метаданных, именно поэтому оно масштабируется до триллионов ключей: нет дерева директорий для обхода или ребаланса. Три допущения файловой системы ломаются. LIST prefix/ — range-скан по индексу ключей, нормально для умеренных префиксов, но патологично, если набить миллиард ключей под один префикс. «Переименование папки logs/ в archive/» — не O(1) флип метаданных — папок нет — это значит скопировать каждый объект под новые ключи и удалить старые, O(число объектов). А листинг в некоторых системах eventually consistent даже там, где GET сильный. Проектируй ключи так, чтобы размазать нагрузку по индексу: избегай последовательных/монотонных префиксов (вроде uploads/2024/...), что все садятся на одну партицию метаданных и создают горячую точку записи; вместо этого распредели ведущий компонент (например, хеш-префикс ключа или поставь высококардинальное поле первым), чтобы записи и LIST-ы расходились по партициям. Никогда не предполагай семантику файловой системы под капотом плоского пространства имён." }
    },
    {
      "id": "fix-monolithic-upload",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Fix the all-or-nothing big upload", "ru": "Почини большую загрузку всё-или-ничего" },
      "prompt": { "en": "This uploads a 5 TB object as one request over a flaky link and keeps failing near the end. Explain the flaw and the fix, then check against the model.", "ru": "Это грузит объект на 5 ТБ одним запросом по нестабильному каналу и всё время падает под конец. Объясни изъян и починку, затем сверься с образцом." },
      "starter": "def upload_object(bucket, key, fobj):\n    data = fobj.read()              # read all 5 TB into memory\n    http.put(f'{bucket}/{key}', body=data, timeout=3600)\n    # on any network blip, the whole 5 TB restarts from zero",
      "grading": {
        "mode": "self",
        "model": { "en": "The flaw is a monolithic single PUT of a 5 TB object: any network drop forces restarting the entire transfer from zero, and reading all 5 TB into memory is itself untenable. A longer timeout doesn't help a connection that intermittently drops. The fix is multipart upload: initiate an upload, split the object into parts (e.g. hundreds of MB each), upload each part independently and in parallel — each part is separately retryable, so a failed part 37 is retried alone rather than the whole object — and finally issue a 'complete' call listing the parts, which the service stitches into one object. The object becomes visible only after completion (atomic all-or-nothing), and you get reliability (per-part retry), throughput (parallel parts), and bounded memory (stream parts, don't buffer 5 TB). This is exactly the mechanism object stores expose for large objects.", "ru": "Изъян — монолитный одиночный PUT объекта на 5 ТБ: любой обрыв сети форсирует перезапуск всей передачи с нуля, а чтение всех 5 ТБ в память само по себе несостоятельно. Длиннее таймаут не помогает соединению, что временами рвётся. Починка — multipart upload: инициируй загрузку, разбей объект на части (напр. по сотни МБ), грузи каждую часть независимо и параллельно — каждая часть ретраится отдельно, так что отвалившаяся часть 37 ретраится одна, а не весь объект — и наконец шли «complete»-вызов со списком частей, что сервис сшивает в один объект. Объект становится виден лишь после завершения (атомарно всё-или-ничего), и ты получаешь надёжность (ретрай по части), пропускную (параллельные части) и ограниченную память (стримь части, не буферизуй 5 ТБ). Это ровно механизм, что объектные хранилища дают для крупных объектов." },
        "rubric": [
          { "en": "Names the flaw: a single monolithic PUT restarts the whole 5 TB on any failure (and buffers it all in memory).", "ru": "Называет изъян: один монолитный PUT перезапускает все 5 ТБ на любом отказе (и буферизует всё в памяти)." },
          { "en": "Fix is multipart upload: independent, parallel, individually-retryable parts plus an atomic 'complete' that assembles the object.", "ru": "Починка — multipart upload: независимые, параллельные, индивидуально ретраемые части плюс атомарный «complete», что собирает объект." }
        ]
      }
    }
  ]
}
