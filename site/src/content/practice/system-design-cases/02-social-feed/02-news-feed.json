{
  "lessonKey": "system-design-cases/02-social-feed/02-news-feed",
  "track": "system-design-cases",
  "tasks": [
    {
      "id": "design-feed-fanout-strategy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 9,
      "title": { "en": "Choose the fan-out strategy", "ru": "Выбери стратегию fan-out" },
      "prompt": { "en": "Decide the feed's fan-out approach and the celebrity threshold, and justify both with the numbers.", "ru": "Реши подход fan-out ленты и порог знаменитости, обоснуй оба числами." },
      "constraints": { "en": "300M DAU, each opening their feed ~10x/day; users post ~2x/day; average followers ~few hundred, but the tail has accounts with 30M+ followers. Feed load must feel instant (tens of ms first page). A post appearing seconds late is acceptable.", "ru": "300M активных в день, каждый открывает ленту ~10 раз/день; постят ~2 раза/день; в среднем несколько сотен подписчиков, но в хвосте аккаунты с 30M+ подписчиков. Загрузка ленты должна ощущаться мгновенной (десятки мс на первую страницу). Пост, всплывший секундами позже, приемлем." },
      "rubric": [
        { "en": "Derives the ~100:1 read:write ratio (≈35K feed-reads/sec vs ≈7K posts/sec) and uses it to justify fan-out-on-write as the default — do the work once on write, amortize over the many reads.", "ru": "Выводит ~100:1 отношение чтение:запись (≈35K чтений ленты/сек vs ≈7K постов/сек) и обосновывает им fan-out-on-write дефолтом — сделать работу раз на записи, амортизировать на многих чтениях." },
        { "en": "Identifies that pure push breaks at the power-law tail (a 30M-follower post is 30M writes) and routes celebrities to fan-out-on-read, merged at query time — the hybrid.", "ru": "Выявляет, что чистый push ломается на степенном хвосте (пост на 30M подписчиков — 30M записей) и маршрутизирует знаменитостей в fan-out-on-read, слитый на запросе — гибрид." },
        { "en": "Sets a follower-count threshold to classify push vs pull, and notes the merge is cheap because celebrities-followed-per-user is small.", "ru": "Задаёт порог по числу подписчиков для классификации push vs pull и отмечает, что слияние дёшево, ведь знаменитостей-в-подписках-на-юзера мало." }
      ],
      "model": { "en": "Reads dwarf writes ~100:1 (≈35K feed-reads/sec vs ≈7K posts/sec), so doing the work once on write and amortizing over ~100 reads beats redoing a scatter-gather every read — default to fan-out-on-write (push), precomputing each follower's feed as a list of post ids. But pure push breaks at the tail: a 30M-follower post is 30M writes, and a burst of celebrity posts is a write storm. So run a hybrid — keep push for normal authors, and for accounts above a follower-count threshold (say ~100K–1M) switch to fan-out-on-read: store their posts once and merge their recent posts into the (precomputed) feed at read time. It works because the power law cuts both ways — celebrities make push expensive yet are rare per reader, so pulling them costs each reader only a few extra lookups. The read path: read the precomputed feed, pull followed celebrities, merge, rank, slice.", "ru": "Чтения превосходят записи ~100:1 (≈35K чтений ленты/сек vs ≈7K постов/сек), так что сделать работу раз на записи и амортизировать на ~100 чтениях бьёт повтор scatter-gather на каждом чтении — дефолт fan-out-on-write (push), предвычисляя ленту каждого подписчика как список id постов. Но чистый push ломается на хвосте: пост на 30M подписчиков — 30M записей, а всплеск постов знаменитостей — шторм записей. Так что запусти гибрид — сохрани push для обычных авторов, а для аккаунтов выше порога по числу подписчиков (скажем, ~100K–1M) переключись на fan-out-on-read: храни их посты раз и сливай их недавние посты в (предвычисленную) ленту на чтении. Работает, потому что степенной закон режет в обе стороны — знаменитости делают push дорогим, но редки на читателя, так что тянуть их стоит каждому читателю лишь нескольких лишних лукапов. Путь чтения: читать предвычисленную ленту, тянуть знаменитостей из подписок, слить, ранжировать, нарезать." }
    },
    {
      "id": "predict-celebrity-write-storm",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Predict the write storm", "ru": "Предскажи шторм записей" },
      "prompt": { "en": "Estimate the fan-out cost, then reveal the answer.", "ru": "Оцени стоимость fan-out, затем открой ответ." },
      "scenario": { "en": "Your feed uses pure fan-out-on-write. In one minute, three celebrities with 30M, 25M, and 45M followers each post once, while normal users post too. The feed store sustains about 2M writes/sec before it saturates. What happens, and what should those three authors have used instead?", "ru": "Твоя лента использует чистый fan-out-on-write. За одну минуту три знаменитости с 30M, 25M и 45M подписчиков постят по разу, пока обычные пользователи тоже постят. Хранилище лент держит около 2M записей/сек до насыщения. Что произойдёт и что эти трое авторов должны были использовать?" },
      "reveal": { "en": "Those three posts alone require 30M+25M+45M = 100M feed writes. Spread over a minute that is ~1.67M writes/sec from just three posts — before any normal-user fan-out — so combined with the rest of the traffic the feed store saturates, fan-out falls behind, and posts stop appearing in feeds (the hook's outage). Pushing celebrity posts is the wrong strategy: those three authors should be fan-out-on-read (pull). Store each celebrity post once, and merge it into a follower's feed at read time, so one celebrity post costs ~one write plus a per-reader lookup instead of tens of millions of writes. The hybrid keeps push for the many normal authors and pull for the few celebrities.", "ru": "Эти три поста сами требуют 30M+25M+45M = 100M записей в ленты. Растянутые на минуту — это ~1.67M записей/сек лишь от трёх постов — до любого fan-out обычных пользователей — так что вместе с остальным трафиком хранилище лент насыщается, fan-out отстаёт, и посты перестают появляться в лентах (сбой из hook). Толкать посты знаменитостей — неверная стратегия: эти трое авторов должны быть fan-out-on-read (pull). Храни каждый пост знаменитости раз и сливай в ленту подписчика на чтении, так один пост знаменитости стоит ~одной записи плюс лукап на читателя вместо десятков миллионов записей. Гибрид держит push для многих обычных авторов и pull для немногих знаменитостей." }
    },
    {
      "id": "fix-offset-pagination",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Fix the paginated feed", "ru": "Почини постраничную ленту" },
      "prompt": { "en": "This feed query slows on deep scroll and skips or duplicates posts as new ones arrive. Identify the flaw and rewrite it, then check against the model.", "ru": "Этот запрос ленты тормозит при глубокой прокрутке и пропускает или дублирует посты при приходе новых. Назови дефект и перепиши, затем сверься с образцом." },
      "starter": "-- page n of the feed (newest first)\nSELECT post_id, created_at FROM feed\nWHERE user_id = :uid\nORDER BY created_at DESC\nLIMIT 20 OFFSET :n;   -- n = page_index * 20",
      "grading": {
        "mode": "self",
        "model": { "en": "The flaw is offset pagination. OFFSET n makes the database scan and discard n rows, so it gets slower the deeper the user scrolls (linear in scroll depth), and because new posts are inserted at the top between requests, the window shifts — items get skipped or shown twice. Fix with cursor (keyset) pagination: instead of an offset, the client sends the (created_at, post_id) of the last item it saw, and the query returns items strictly after that cursor: WHERE user_id = :uid AND (created_at, post_id) < (:last_created_at, :last_post_id) ORDER BY created_at DESC, post_id DESC LIMIT 20. This is O(1) regardless of depth (it seeks to the cursor via the index rather than scanning n rows) and stable under inserts, because each page is anchored to a concrete item, not a shifting position. Cursor pagination is the right primitive for infinite scroll over a constantly-growing list.", "ru": "Дефект — offset-пагинация. OFFSET n заставляет базу сканировать и отбрасывать n строк, так что медленнее, чем глубже прокрутка (линейно по глубине), а раз новые посты вставляются сверху между запросами, окно сдвигается — элементы пропускаются или показываются дважды. Починка — курсорная (keyset) пагинация: вместо offset клиент шлёт (created_at, post_id) последнего виденного элемента, и запрос возвращает элементы строго после этого курсора: WHERE user_id = :uid AND (created_at, post_id) < (:last_created_at, :last_post_id) ORDER BY created_at DESC, post_id DESC LIMIT 20. Это O(1) независимо от глубины (он сикает к курсору через индекс, а не сканирует n строк) и стабильно при вставках, ведь каждая страница привязана к конкретному элементу, а не к сдвигающейся позиции. Курсорная пагинация — верный примитив для бесконечной прокрутки над постоянно растущим списком." },
        "rubric": [
          { "en": "Names OFFSET as the cause: scans n rows (slow at depth) and shifts under inserts (skips/dupes).", "ru": "Называет OFFSET причиной: сканирует n строк (медленно на глубине) и сдвигается при вставках (пропуски/дубли)." },
          { "en": "Rewrites to cursor/keyset pagination anchored on the last seen item, noting O(1) depth and insert-stability.", "ru": "Переписывает на курсорную/keyset пагинацию, привязанную к последнему виденному элементу, отмечая O(1) глубину и стабильность при вставках." }
        ]
      }
    },
    {
      "id": "design-feed-cache",
      "type": "design",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Design the feed cache", "ru": "Спроектируй feed cache" },
      "prompt": { "en": "Design what the precomputed feed stores and how deletes/privacy changes are handled without re-fanning-out.", "ru": "Спроектируй, что хранит предвычисленная лента и как обрабатываются удаления/смены приватности без пере-fan-out." },
      "constraints": { "en": "Hundreds of millions of users, each with a feed. A viral post can be hydrated by millions of feeds at once. Posts get edited, deleted, made private, and authors get unfollowed — and re-fanning-out on every such mutation would be ruinous.", "ru": "Сотни миллионов пользователей, у каждого лента. Вирусный пост может гидрироваться миллионами лент разом. Посты редактируют, удаляют, делают приватными, авторов отписывают — и пере-fan-out на каждой такой мутации был бы разорителен." },
      "rubric": [
        { "en": "Stores post ids (not bodies) in a capped per-user structure (e.g. a Redis sorted set scored by time/rank), so the cache stays small and one post body is hydrated once and shared across millions of feeds.", "ru": "Хранит id постов (не тела) в ограниченной структуре на юзера (например, Redis sorted set со скором по времени/ранку), так что кеш мал, а одно тело поста гидрируется раз и шарится по миллионам лент." },
        { "en": "Treats the feed as an approximate, eventually-consistent index resolved at hydration — visibility (deleted/blocked/now-private) is checked when hydrating an id, not by scrubbing millions of precomputed lists.", "ru": "Трактует ленту как приблизительный, eventually-consistent индекс, решаемый на гидрации — видимость (удалён/заблокирован/теперь-приватен) проверяется при гидрации id, а не вычисткой миллионов предвычисленных списков." }
      ],
      "model": { "en": "Store post ids, not bodies, in a capped per-user structure — a Redis sorted set of the top few hundred ids scored by time or rank, with old entries falling off. That keeps each feed tiny and lets the same post object be hydrated once and shared across millions of feeds; a viral post's body is itself cached (behind a CDN/edge cache) so hydration does not stampede the post store. Crucially, treat the feed as an approximate, eventually-consistent index, not the source of truth (the posts and graph are). Do NOT re-fan-out on edits, deletes, unfollows, or privacy changes — that would trigger another N-write storm per mutation. Instead resolve correctness at hydration: when you hydrate a post id, check its current visibility (deleted? blocked? now private? still followed?) and drop it if it no longer qualifies. On a cache miss or for inactive users, lazily rebuild the feed from the authors' timelines. The feed is allowed to be slightly stale; fighting that turns every mutation into a fan-out storm.", "ru": "Храни id постов, не тела, в ограниченной структуре на юзера — Redis sorted set из топа нескольких сотен id со скором по времени или ранку, со старыми записями, отпадающими. Это держит каждую ленту крошечной и позволяет один объект поста гидрировать раз и шарить по миллионам лент; тело вирусного поста само кешируется (за CDN/edge-кешем), так что гидрация не штурмует хранилище постов. Ключевое: трактуй ленту как приблизительный, eventually-consistent индекс, а не источник истины (им являются посты и граф). НЕ делай пере-fan-out на редактировании, удалении, отписке или смене приватности — это триггерило бы ещё один шторм на N записей на мутацию. Вместо этого решай корректность на гидрации: гидрируя id поста, проверь его текущую видимость (удалён? заблокирован? теперь приватен? всё ещё в подписках?) и отбрось, если больше не годится. На промахе кеша или для неактивных пользователей лениво пересобери ленту из таймлайнов авторов. Ленте позволено быть слегка устаревшей; борьба с этим превращает каждую мутацию в шторм fan-out." }
    }
  ]
}
