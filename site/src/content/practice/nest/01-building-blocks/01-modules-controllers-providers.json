{
  "lessonKey": "nest/01-building-blocks/01-modules-controllers-providers",
  "track": "nest",
  "tasks": [
    {
      "id": "predict-cross-module-injection",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Predict whether the injection resolves", "ru": "Предскажи, разрешится ли инъекция" },
      "prompt": { "en": "OrdersService asks for UsersService in its constructor. Read the two modules below and predict whether the app boots or throws 'Nest can't resolve dependencies' — then reveal the answer.", "ru": "OrdersService просит UsersService в конструкторе. Прочитай два module ниже и предскажи, стартует ли приложение или бросит 'Nest can't resolve dependencies', затем открой ответ." },
      "scenario": { "en": "@Module({\n  controllers: [UsersController],\n  providers: [UsersService],\n})\nexport class UsersModule {}\n\n@Module({\n  imports: [UsersModule],\n  controllers: [OrdersController],\n  providers: [OrdersService], // OrdersService constructor: (private users: UsersService)\n})\nexport class OrdersModule {}\n\nDoes UsersService inject into OrdersService?", "ru": "@Module({\n  controllers: [UsersController],\n  providers: [UsersService],\n})\nexport class UsersModule {}\n\n@Module({\n  imports: [UsersModule],\n  controllers: [OrdersController],\n  providers: [OrdersService], // конструктор OrdersService: (private users: UsersService)\n})\nexport class OrdersModule {}\n\nИнжектится ли UsersService в OrdersService?" },
      "reveal": { "en": "No — it throws 'Nest can't resolve dependencies of OrdersService'. OrdersModule correctly imports UsersModule, but UsersModule never lists UsersService in an exports array, so the provider stays private to UsersModule. Imports only grant access to what the imported module exports. Fix: add exports: [UsersService] to UsersModule. Both halves must be present — export from the owner AND import the owner.", "ru": "Нет — бросит 'Nest can't resolve dependencies of OrdersService'. OrdersModule правильно импортирует UsersModule, но UsersModule не перечисляет UsersService в массиве exports, поэтому provider остаётся приватным для UsersModule. Imports дают доступ только к тому, что импортируемый module экспортирует. Фикс: добавить exports: [UsersService] в UsersModule. Нужны обе половины — export у владельца И import владельца." }
    },
    {
      "id": "diagnose-block-roles",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Name the block and the decorator at fault", "ru": "Назови блок и декоратор, который виноват" },
      "prompt": { "en": "Fill in the precise term for each blank about where logic belongs and what gates visibility.", "ru": "Заполни точный термин в каждом пропуске о том, где живёт логика и что управляет видимостью." },
      "evidence": { "en": "Case A: A class doing a DB query and validation was placed directly in the route method; reviewers say the request-handling block should be thin and delegate. The block that should hold this logic is a ____.\nCase B: A provider compiles fine and is in its module's providers, but another module can't inject it. The @Module() array it must be added to is ____.", "ru": "Случай A: Класс с запросом к БД и валидацией положили прямо в метод route; ревьюеры говорят, что блок обработки запроса должен быть тонким и делегировать. Блок, который должен держать эту логику, — это ____.\nСлучай B: Provider компилируется и есть в providers своего module, но другой module не может его инжектить. Массив @Module(), куда его надо добавить, — это ____." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["provider", "service", "provider (service)", "a provider", "injectable provider"], "hint": { "en": "The @Injectable() block that holds business logic.", "ru": "Блок @Injectable(), который держит бизнес-логику." } },
          { "id": "b2", "accept": ["exports", "exports array", "the exports array", "export"], "hint": { "en": "The @Module() field that makes a provider visible to importers.", "ru": "Поле @Module(), которое делает provider видимым для импортирующих." } }
        ]
      }
    },
    {
      "id": "fix-rebind-breaks-singleton",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Fix the duplicate-instance shortcut", "ru": "Почини костыль с дублирующим инстансом" },
      "prompt": { "en": "A teammate silenced 'Nest can't resolve dependencies' by adding the shared CacheService to the consuming module's own providers array. Now the cache mysteriously misses entries another module wrote. Explain why, and give the correct wiring. Then reveal the model answer.", "ru": "Коллега заглушил 'Nest can't resolve dependencies', добавив общий CacheService в собственный массив providers потребляющего module. Теперь кэш загадочно не находит записи, которые писал другой module. Объясни почему и дай правильную обвязку. Затем открой образцовый ответ." },
      "starter": "// CacheModule owns CacheService.\n// FeatureModule needs it but got 'Nest can't resolve dependencies',\n// so it was 'fixed' like this:\n//\n// @Module({\n//   controllers: [FeatureController],\n//   providers: [FeatureService, CacheService], // <-- the shortcut\n// })\n// export class FeatureModule {}\n//\n// Why does the shared cache now behave inconsistently, and what is the correct wiring?",
      "grading": {
        "mode": "self",
        "model": { "en": "Re-listing CacheService in FeatureModule's providers makes Nest instantiate a SECOND, independent CacheService scoped to FeatureModule. Providers are singletons per scope, not globally — so FeatureModule's cache and CacheModule's cache are two different objects with separate in-memory state. Writes through one are invisible to the other, which is exactly the 'mysterious miss.' The error was real (the provider was private), but the shortcut traded a startup error for a silent correctness bug. Correct wiring: keep CacheService declared once, in CacheModule, and export it; then import CacheModule wherever it's needed.\n\n@Module({ providers: [CacheService], exports: [CacheService] })\nexport class CacheModule {}\n\n@Module({\n  imports: [CacheModule],\n  controllers: [FeatureController],\n  providers: [FeatureService], // NOT CacheService\n})\nexport class FeatureModule {}\n\nNow every importer shares the one CacheService singleton.", "ru": "Пере-перечисление CacheService в providers у FeatureModule заставляет Nest создать ВТОРОЙ, независимый CacheService в области видимости FeatureModule. Provider-ы — синглтоны на область видимости, а не глобально, — поэтому кэш FeatureModule и кэш CacheModule это два разных объекта с раздельным in-memory состоянием. Записи через один невидимы другому — это и есть «загадочный промах». Ошибка была настоящей (provider был приватным), но костыль обменял ошибку старта на тихий баг корректности. Правильная обвязка: объяви CacheService один раз, в CacheModule, и экспортируй его; затем импортируй CacheModule везде, где он нужен.\n\n@Module({ providers: [CacheService], exports: [CacheService] })\nexport class CacheModule {}\n\n@Module({\n  imports: [CacheModule],\n  controllers: [FeatureController],\n  providers: [FeatureService], // НЕ CacheService\n})\nexport class FeatureModule {}\n\nТеперь каждый импортирующий разделяет один синглтон CacheService." },
        "rubric": [
          { "en": "Explains that re-listing CacheService in FeatureModule's providers creates a second, independent instance (providers are singletons per scope, not global).", "ru": "Объясняет, что повторное перечисление CacheService в providers у FeatureModule создаёт второй, независимый инстанс (provider-ы — синглтоны на область видимости, а не глобально)." },
          { "en": "Identifies that the two instances have separate state, so writes through one are invisible to the other — the silent correctness bug behind the misses.", "ru": "Определяет, что у двух инстансов раздельное состояние, поэтому записи через один невидимы другому — тихий баг корректности за промахами." },
          { "en": "Gives the correct wiring: declare CacheService once in CacheModule, export it, and import CacheModule into FeatureModule (do not re-list the provider).", "ru": "Даёт правильную обвязку: объяви CacheService один раз в CacheModule, экспортируй его и импортируй CacheModule в FeatureModule (не перечисляй provider повторно)." }
        ]
      }
    },
    {
      "id": "design-feature-module",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 10,
      "title": { "en": "Design a feature module's three blocks", "ru": "Спроектируй три блока feature-модуля" },
      "prompt": { "en": "Design the modules-controllers-providers wiring for the billing feature below. Decide what each block contains, where logic lives, and exactly what crosses the module boundary. Self-grade against the rubric.", "ru": "Спроектируй обвязку modules-controllers-providers для фичи биллинга ниже. Реши, что содержит каждый блок, где живёт логика и что именно пересекает границу module. Оцени себя по рубрике." },
      "constraints": { "en": "A billing feature exposes REST endpoints (create invoice, get invoice by id, list invoices for a query filter). It needs an InvoiceService (business logic + persistence) and a PdfService (renders an invoice PDF). The NotificationsModule, a separate feature, must be able to call InvoiceService to look up an invoice when sending a reminder — but must NOT touch PdfService. PdfService is internal to billing.", "ru": "Фича биллинга выставляет REST-эндпоинты (создать счёт, получить счёт по id, список счетов по фильтру query). Ей нужны InvoiceService (бизнес-логика + персистентность) и PdfService (рендерит PDF счёта). NotificationsModule, отдельная фича, должен мочь вызвать InvoiceService, чтобы найти счёт при отправке напоминания, — но НЕ должен трогать PdfService. PdfService внутренний для биллинга." },
      "rubric": [
        { "en": "Controller (@Controller('invoices')) is thin: @Post create, @Get(':id') with @Param, @Get() list with @Query — each delegates to InvoiceService and returns; no business logic in the controller.", "ru": "Controller (@Controller('invoices')) тонкий: @Post create, @Get(':id') с @Param, @Get() list с @Query — каждый делегирует InvoiceService и возвращает; никакой бизнес-логики в controller." },
        { "en": "Both InvoiceService and PdfService are @Injectable() providers listed in BillingModule's providers; logic lives in the services, not the controller.", "ru": "И InvoiceService, и PdfService — @Injectable() provider-ы, перечисленные в providers у BillingModule; логика живёт в сервисах, а не в controller." },
        { "en": "BillingModule exports ONLY InvoiceService (not PdfService), and NotificationsModule imports BillingModule — so the boundary exposes exactly what's needed and keeps PdfService private.", "ru": "BillingModule экспортирует ТОЛЬКО InvoiceService (не PdfService), а NotificationsModule импортирует BillingModule — так граница открывает ровно нужное и держит PdfService приватным." }
      ],
      "model": { "en": "@Controller('invoices')\nexport class InvoicesController {\n  constructor(private readonly invoices: InvoiceService) {}\n  @Post() create(@Body() dto: CreateInvoiceDto) { return this.invoices.create(dto); }\n  @Get(':id') findOne(@Param('id') id: string) { return this.invoices.findOne(id); }\n  @Get() list(@Query() q: ListInvoicesDto) { return this.invoices.list(q); }\n}\n\n@Injectable() export class InvoiceService { /* business logic + persistence; may use PdfService */ }\n@Injectable() export class PdfService { /* renders PDF; internal to billing */ }\n\n@Module({\n  controllers: [InvoicesController],\n  providers: [InvoiceService, PdfService],\n  exports: [InvoiceService], // PdfService stays private\n})\nexport class BillingModule {}\n\n@Module({\n  imports: [BillingModule], // grants access to the exported InvoiceService only\n  providers: [NotificationsService],\n})\nexport class NotificationsModule {}\n\nThe controller is a thin HTTP adapter; both services hold the logic; the boundary exports just InvoiceService so NotificationsModule can inject it while PdfService remains encapsulated inside billing.", "ru": "@Controller('invoices')\nexport class InvoicesController {\n  constructor(private readonly invoices: InvoiceService) {}\n  @Post() create(@Body() dto: CreateInvoiceDto) { return this.invoices.create(dto); }\n  @Get(':id') findOne(@Param('id') id: string) { return this.invoices.findOne(id); }\n  @Get() list(@Query() q: ListInvoicesDto) { return this.invoices.list(q); }\n}\n\n@Injectable() export class InvoiceService { /* бизнес-логика + персистентность; может использовать PdfService */ }\n@Injectable() export class PdfService { /* рендерит PDF; внутренний для биллинга */ }\n\n@Module({\n  controllers: [InvoicesController],\n  providers: [InvoiceService, PdfService],\n  exports: [InvoiceService], // PdfService остаётся приватным\n})\nexport class BillingModule {}\n\n@Module({\n  imports: [BillingModule], // даёт доступ только к экспортированному InvoiceService\n  providers: [NotificationsService],\n})\nexport class NotificationsModule {}\n\nController — тонкий HTTP-адаптер; оба сервиса держат логику; граница экспортирует только InvoiceService, чтобы NotificationsModule мог его инжектить, а PdfService остаётся инкапсулированным внутри биллинга." }
    }
  ]
}
