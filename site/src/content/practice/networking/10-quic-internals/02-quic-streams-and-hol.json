{
  "lessonKey": "networking/10-quic-internals/02-quic-streams-and-hol",
  "track": "networking",
  "tasks": [
    {
      "id": "predict-one-packet-stalls",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "One lost packet — how many streams stall?", "ru": "Один потерянный пакет — сколько потоков зависнет?" },
      "prompt": { "en": "Before revealing: write down how many of the three in-flight responses stall in each transport, and the one-line reason.", "ru": "Прежде чем открыть ответ: запиши, сколько из трёх ответов в полёте зависнет в каждом транспорте, и одну строку с причиной." },
      "scenario": { "en": "Three responses are multiplexed over one connection. Each carries its bytes in separate logical messages. A single packet — the one carrying response B's bytes — is dropped by a congested router. Responses A and C already have all their bytes sitting in the receiver's buffer.\n\nCase 1: <code>HTTP/2 over TCP</code>.\nCase 2: <code>HTTP/3 over QUIC</code> (each response on its own stream).", "ru": "Три ответа мультиплексированы по одному соединению. Каждый несёт свои байты в отдельных логических сообщениях. Один пакет — тот, что несёт байты ответа B — теряется на перегруженном роутере. У ответов A и C все байты уже лежат в буфере получателя.\n\nСлучай 1: <code>HTTP/2 поверх TCP</code>.\nСлучай 2: <code>HTTP/3 поверх QUIC</code> (каждый ответ на своём потоке)." },
      "reveal": { "en": "<strong>HTTP/2 over TCP: all three stall.</strong> TCP delivers a single ordered byte stream, so until the lost packet is retransmitted the receiver cannot hand <em>any</em> later bytes to the application — A and C are stuck behind B even though their bytes already arrived. This is connection-level head-of-line blocking, and HTTP/2's own multiplexing cannot escape it because TCP is underneath.\n\n<strong>HTTP/3 over QUIC: only one stalls (B).</strong> Each response rides its own stream with independent ordering and loss detection. Only the stream carrying the lost STREAM frame waits for the retransmit; A and C keep flowing. At 0.5% loss and 100 ms RTT this is the difference between ~230 ms of blocking and ~0.4 ms — a 575× improvement.", "ru": "<strong>HTTP/2 поверх TCP: зависают все три.</strong> TCP доставляет единый упорядоченный поток байт, поэтому, пока потерянный пакет не переотправлен, получатель не может отдать приложению <em>никакие</em> последующие байты — A и C застряли за B, хотя их байты уже пришли. Это head-of-line blocking на уровне соединения, и собственное мультиплексирование HTTP/2 не спасает, потому что снизу TCP.\n\n<strong>HTTP/3 поверх QUIC: зависает только один (B).</strong> Каждый ответ едет на своём потоке с независимым порядком и обнаружением потерь. Только поток с потерянным STREAM-фреймом ждёт переотправки; A и C продолжают идти. При 0,5% потерях и RTT 100 ms это разница между ~230 ms блокировки и ~0,4 ms — улучшение в 575 раз." }
    },
    {
      "id": "diagnose-stream-id-and-blocker",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Read the stream IDs, name the real blocker", "ru": "Прочитай Stream ID, назови реального виновника" },
      "prompt": { "en": "An HTTP/3 trace shows a stall. Fill in the blanks: classify the stream ID, and name the layer where blocking would (or would not) occur.", "ru": "Трейс HTTP/3 показывает зависание. Заполни пропуски: классифицируй Stream ID и назови уровень, где блокировка возникла бы (или не возникла)." },
      "evidence": { "en": "STREAM frame lost: stream_id=7, offset=4096\nOther active streams: 0, 4, 8 (HTTP/3 requests), 3 (control)\nObservation: streams 0, 4, 8 keep receiving new bytes; only stream 7's reader is paused.", "ru": "Потерян STREAM-фрейм: stream_id=7, offset=4096\nДругие активные потоки: 0, 4, 8 (запросы HTTP/3), 3 (управление)\nНаблюдение: потоки 0, 4, 8 продолжают получать новые байты; на паузе только читатель потока 7." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["server-initiated unidirectional", "server unidirectional", "server-initiated uni", "server uni"], "hint": { "en": "Last two bits of 7 are 11. Bit 0 = initiator, bit 1 = directionality.", "ru": "Младшие два бита 7 — это 11. Бит 0 = инициатор, бит 1 = направленность." } },
          { "id": "b2", "accept": ["stream", "stream-level", "per-stream", "stream 7 only", "only stream 7", "application stream"], "hint": { "en": "Streams 0, 4, 8 keep flowing — the stall is scoped to one of them, not the connection.", "ru": "Потоки 0, 4, 8 продолжают идти — зависание ограничено одним из них, не соединением." } },
          { "id": "b3", "accept": ["no", "none", "would not", "no transport-level blocking", "no connection-level blocking", "not blocked"], "hint": { "en": "Would the other streams suffer transport-level (connection-wide) head-of-line blocking here?", "ru": "Пострадали бы остальные потоки от транспортной (на всё соединение) HoL-блокировки здесь?" } }
        ]
      }
    },
    {
      "id": "fix-explain-http2-still-blocks",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Fix the engineer's mental model of HTTP/2", "ru": "Почини ментальную модель инженера про HTTP/2" },
      "prompt": { "en": "A teammate claims: “HTTP/2 multiplexes requests, so a dropped packet only stalls one request — same as QUIC.” Rewrite their statement correctly, explaining exactly why HTTP/2 over TCP still suffers HoL blocking and what QUIC changes. Then reveal the model answer.", "ru": "Коллега утверждает: «HTTP/2 мультиплексирует запросы, поэтому потерянный пакет зависает только один запрос — как в QUIC». Перепиши утверждение корректно, объяснив, почему HTTP/2 поверх TCP всё же страдает от HoL-блокировки и что меняет QUIC. Затем открой образцовый ответ." },
      "starter": "HTTP/2 multiplexes requests, so a dropped packet only stalls one request — same as QUIC.",
      "grading": {
        "mode": "self",
        "model": { "en": "HTTP/2 multiplexes at the <em>HTTP layer</em>, but all of its logical streams ride a single <code>TCP</code> byte stream underneath. TCP guarantees in-order delivery of that one stream, so a dropped packet forces <em>every</em> HTTP/2 stream to wait for the retransmit — even streams whose bytes already arrived. The blocking lives at the transport layer, which HTTP/2 cannot reach. QUIC moves multiplexing <em>into the transport</em>: each stream has its own ordering, its own flow-control window (<code>max-stream-data</code>), and independent loss detection, so a lost STREAM frame stalls only its own stream. That is why QUIC eliminates transport-level HoL blocking and HTTP/2-over-TCP cannot.", "ru": "HTTP/2 мультиплексирует на <em>уровне HTTP</em>, но все его логические потоки едут поверх единого байтового потока <code>TCP</code>. TCP гарантирует упорядоченную доставку этого одного потока, поэтому потерянный пакет заставляет ждать переотправки <em>каждый</em> HTTP/2-поток — даже те, чьи байты уже пришли. Блокировка живёт на транспортном уровне, до которого HTTP/2 не достаёт. QUIC переносит мультиплексирование <em>в транспорт</em>: у каждого потока свой порядок, своё окно управления потоком (<code>max-stream-data</code>) и независимое обнаружение потерь, поэтому потерянный STREAM-фрейм зависает только свой поток. Поэтому QUIC устраняет транспортную HoL-блокировку, а HTTP/2-поверх-TCP — нет." },
        "rubric": [
          { "en": "States that HTTP/2 multiplexes at the HTTP layer but runs over a single ordered TCP byte stream.", "ru": "Указывает, что HTTP/2 мультиплексирует на уровне HTTP, но работает поверх единого упорядоченного TCP-потока." },
          { "en": "Locates the blocking at the transport (TCP) layer, which HTTP/2 cannot fix.", "ru": "Помещает блокировку на транспортный (TCP) уровень, который HTTP/2 не может исправить." },
          { "en": "Explains QUIC's fix: per-stream ordering, flow control, and independent loss detection in the transport.", "ru": "Объясняет фикс QUIC: попотоковый порядок, управление потоком и независимое обнаружение потерь в транспорте." }
        ]
      }
    },
    {
      "id": "design-stream-mapping-live-app",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Map an app's data onto QUIC streams", "ru": "Разложи данные приложения по потокам QUIC" },
      "prompt": { "en": "Design how a real-time app maps its data onto QUIC streams so that loss in one data class never stalls another. Decide stream count, direction (bidi vs uni), and which data shares a stream. Self-grade against the rubric.", "ru": "Спроектируй, как приложение реального времени раскладывает данные по потокам QUIC, чтобы потеря в одном классе данных никогда не зависала другой. Выбери число потоков, направление (bidi vs uni) и что делит поток. Оцени себя по рубрике." },
      "constraints": { "en": "An HTTP/3 client app has three concurrent data classes: a live video feed (server→client, large, latency-sensitive), a chat channel (bidirectional, small messages), and an analytics beacon (client→server, fire-and-forget). The path is lossy mobile (~0.5% loss, 100 ms RTT). A stall in one class must not freeze another.", "ru": "Клиентское приложение HTTP/3 имеет три параллельных класса данных: живой видеопоток (сервер→клиент, большой, чувствительный к задержке), чат-канал (двунаправленный, мелкие сообщения) и аналитический маяк (клиент→сервер, fire-and-forget). Путь — нестабильный мобильный (~0,5% потерь, RTT 100 ms). Зависание одного класса не должно замораживать другой." },
      "rubric": [
        { "en": "Puts each independent data class on its own stream so a lost packet stalls only that class (no shared-stream coupling).", "ru": "Кладёт каждый независимый класс данных на свой поток, чтобы потерянный пакет зависал только этот класс (без связывания через общий поток)." },
        { "en": "Chooses correct stream direction per class and gives the right ID family (e.g. server-initiated uni for video = 3,7,11…; client-initiated bidi for chat = 0,4,8…; client-initiated uni for analytics = 2,6,10…).", "ru": "Выбирает верное направление потока для каждого класса и правильную семью ID (например, сервер-инициированный uni для видео = 3,7,11…; клиент-инициированный bidi для чата = 0,4,8…; клиент-инициированный uni для аналитики = 2,6,10…)." },
        { "en": "Justifies the choice using per-stream flow control and independent loss detection, and ties it to the 575× / ~0.4 ms benefit on the lossy path.", "ru": "Обосновывает выбор попотоковым управлением и независимым обнаружением потерь и связывает с выигрышем 575× / ~0,4 ms на нестабильном пути." }
      ],
      "model": { "en": "Use three streams, one per class. Video: a <strong>server-initiated unidirectional</strong> stream (ID family 3, 7, 11…) — data only flows server→client. Chat: a <strong>client-initiated bidirectional</strong> stream (ID family 0, 4, 8…) since messages flow both ways. Analytics: a <strong>client-initiated unidirectional</strong> stream (ID family 2, 6, 10…) — fire-and-forget upstream. Because each stream has its own ordering, its own <code>max-stream-data</code> flow-control window, and independent loss detection, a dropped packet in the video stream only pauses video; chat and analytics keep flowing. Never multiplex two classes onto one stream — that re-introduces head-of-line coupling between them. On the lossy mobile path this turns ~230 ms of cross-class blocking into ~0.4 ms per-stream stalls (575×).", "ru": "Используй три потока, по одному на класс. Видео: <strong>сервер-инициированный однонаправленный</strong> поток (семья ID 3, 7, 11…) — данные текут только сервер→клиент. Чат: <strong>клиент-инициированный двунаправленный</strong> поток (семья ID 0, 4, 8…), так как сообщения идут в обе стороны. Аналитика: <strong>клиент-инициированный однонаправленный</strong> поток (семья ID 2, 6, 10…) — fire-and-forget вверх. Поскольку у каждого потока свой порядок, своё окно управления <code>max-stream-data</code> и независимое обнаружение потерь, потерянный пакет в видеопотоке ставит на паузу только видео; чат и аналитика продолжают идти. Никогда не мультиплексируй два класса на один поток — это возвращает head-of-line-связывание между ними. На нестабильном мобильном пути это превращает ~230 ms межклассовой блокировки в ~0,4 ms попотоковых зависаний (575×)." }
    }
  ]
}
