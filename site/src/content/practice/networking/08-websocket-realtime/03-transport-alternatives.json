{
  "lessonKey": "networking/08-websocket-realtime/03-transport-alternatives",
  "track": "networking",
  "tasks": [
    {
      "id": "recall-transport-properties",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Pin each transport to its defining property", "ru": "Сопоставь каждому транспорту его определяющее свойство" },
      "prompt": { "en": "Fill the blanks so each statement names the right transport. Use only the four transports from the lesson: WebSocket, SSE, long-polling, WebTransport.", "ru": "Заполни пропуски так, чтобы каждое утверждение называло правильный транспорт. Используй только четыре транспорта из урока: WebSocket, SSE, long-polling, WebTransport." },
      "evidence": { "en": "<code>__b1__</code> is the only HTTP-native transport whose browser API reconnects automatically via <code>Last-Event-ID</code>.\n<code>__b2__</code> is full-duplex over a single TCP connection with 2–6 bytes of per-frame overhead.\n<code>__b3__</code> carries ~500+ bytes of HTTP headers on every request and is the universal fallback.\n<code>__b4__</code> runs over HTTP/3 / QUIC and can send <code>unreliable</code> datagrams with no head-of-line blocking.", "ru": "<code>__b1__</code> — единственный HTTP-нативный транспорт, чей браузерный API переподключается автоматически через <code>Last-Event-ID</code>.\n<code>__b2__</code> — full-duplex поверх одного TCP-соединения с накладными расходами 2–6 байт на фрейм.\n<code>__b3__</code> несёт ~500+ байт HTTP-заголовков на каждом запросе и является универсальным запасным вариантом.\n<code>__b4__</code> работает поверх HTTP/3 / QUIC и может слать <code>unreliable</code>-датаграммы без head-of-line-блокировки." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["SSE", "Server-Sent Events", "server-sent events", "EventSource"], "hint": { "en": "text/event-stream, server→client only.", "ru": "text/event-stream, только сервер→клиент." } },
          { "id": "b2", "accept": ["WebSocket", "websocket", "WS"], "hint": { "en": "Binary frames after the upgrade handshake.", "ru": "Бинарные фреймы после upgrade-handshake." } },
          { "id": "b3", "accept": ["long-polling", "long polling", "longpolling", "long-poll"], "hint": { "en": "Client re-opens an HTTP request the server holds open.", "ru": "Клиент переоткрывает HTTP-запрос, который сервер держит открытым." } },
          { "id": "b4", "accept": ["WebTransport", "web transport", "webtransport"], "hint": { "en": "QUIC streams + datagrams, the newest option.", "ru": "QUIC-стримы + датаграммы, самый новый вариант." } }
        ]
      }
    },
    {
      "id": "predict-sse-client-send",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict the cost of forcing client→server over SSE", "ru": "Предскажи цену отправки клиент→сервер поверх SSE" },
      "prompt": { "en": "A collaborative editor already pushes cursor updates to clients over SSE. Now it must also stream each user's mouse position back to the server every 50 ms. The team plans to keep SSE for the push direction and add HTTP POSTs for the upstream. Predict the failure, then reveal.", "ru": "Совместный редактор уже пушит обновления курсора клиентам через SSE. Теперь ему нужно стримить позицию мыши каждого пользователя обратно на сервер каждые 50 мс. Команда планирует оставить SSE для пуша и добавить HTTP POST для восходящего потока. Предскажи проблему, затем открой ответ." },
      "scenario": { "en": "SSE is server→client only: the browser cannot write back on the <code>EventSource</code> stream. So upstream mouse positions must travel as separate HTTP POSTs. Estimate the request rate and header overhead per user, and say what transport removes the problem.", "ru": "SSE — только сервер→клиент: браузер не может писать обратно в поток <code>EventSource</code>. Значит восходящие позиции мыши пойдут отдельными HTTP POST. Оцени частоту запросов и накладные расходы заголовков на пользователя и скажи, какой транспорт убирает проблему." },
      "reveal": { "en": "Every 50 ms = <strong>20 POST requests/second per user</strong>, each carrying ~500 bytes of HTTP headers — so ~10 KB/s of pure header overhead per user before any payload, plus connection churn and no ordering guarantee with the inbound SSE events. WebSocket is the correct fix: after the upgrade handshake the browser sends frames in either direction with only 2–6 bytes of overhead over the one persistent connection, keeping latency under 5 ms. (WebTransport over HTTP/3 is the modern alternative when you also want unreliable, unordered datagrams and no head-of-line blocking.)", "ru": "Каждые 50 мс = <strong>20 POST-запросов/секунду на пользователя</strong>, каждый несёт ~500 байт HTTP-заголовков — то есть ~10 КБ/с чистых накладных расходов заголовков на пользователя ещё до payload, плюс churn соединений и отсутствие гарантии порядка с входящими SSE-событиями. WebSocket — правильный фикс: после upgrade-handshake браузер шлёт фреймы в обе стороны с накладными расходами всего 2–6 байт по одному постоянному соединению, удерживая задержку ниже 5 мс. (WebTransport поверх HTTP/3 — современная альтернатива, когда нужны ещё и unreliable, неупорядоченные датаграммы без head-of-line-блокировки.)" }
    },
    {
      "id": "fix-permessage-deflate-oom",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Fix the WebSocket fleet that OOMs at 100k connections", "ru": "Почини WebSocket-флот, который ловит OOM на 100k соединений" },
      "prompt": { "en": "A WebSocket gateway holds 100k mostly-idle connections and keeps getting OOM-killed. Heap dumps show ~25 GB tied up in compression state. Propose the change(s) that stop the OOM without throwing away compression where it actually helps. Then reveal the model answer.", "ru": "WebSocket-шлюз держит 100k в основном idle-соединений и постоянно ловит OOM-kill. Дампы кучи показывают ~25 ГБ в состоянии сжатия. Предложи изменение(я), которые останавливают OOM, не выбрасывая сжатие там, где оно реально помогает. Затем открой образцовый ответ." },
      "starter": "// gateway negotiates permessage-deflate at default window_bits=15\n// per connection: ~256 KB compressor + ~44 KB decompressor\n// 100_000 idle connections => ~25 GB just for compression state",
      "grading": {
        "mode": "self",
        "model": { "en": "The root cause is <code>permessage-deflate</code> (RFC 7692) at the default <code>window_bits=15</code>: ~256 KB per compressor context + ~44 KB per decompressor, ×100k ≈ 25 GB. Primary fix: <strong>disable permessage-deflate by default</strong> — these connections are idle and most production deployments turn it off. If the payloads are large JSON/text where 50–90% compression matters, instead <strong>negotiate a smaller <code>window_bits</code> (e.g. 12)</strong>, dropping per-connection memory to ~50 KB. Also skip compression on small (&lt;64 byte) messages, where DEFLATE can expand the payload, and on already-compressed data (images/video), where it does nothing.", "ru": "Корень — <code>permessage-deflate</code> (RFC 7692) при дефолтном <code>window_bits=15</code>: ~256 КБ на контекст компрессора + ~44 КБ на декомпрессор, ×100k ≈ 25 ГБ. Основной фикс: <strong>отключить permessage-deflate по умолчанию</strong> — соединения idle, и большинство production-инсталляций его выключают. Если payload — крупный JSON/текст, где сжатие 50–90% важно, вместо этого <strong>согласовать меньший <code>window_bits</code> (например, 12)</strong>, снизив память до ~50 КБ на соединение. Также пропускать сжатие на маленьких (&lt;64 байт) сообщениях, где DEFLATE может раздуть payload, и на уже сжатых данных (изображения/видео), где эффекта нет." },
        "rubric": [
          { "en": "Identifies permessage-deflate at window_bits=15 (~256 KB/conn) as the root cause, not generic 'too many connections'.", "ru": "Определяет корнем permessage-deflate при window_bits=15 (~256 КБ/соед.), а не общее «слишком много соединений»." },
          { "en": "Disables compression by default for idle connections OR lowers window_bits (~50 KB) when compression is genuinely needed.", "ru": "Отключает сжатие по умолчанию для idle-соединений ИЛИ снижает window_bits (~50 КБ), когда сжатие действительно нужно." },
          { "en": "Notes compression is useless/harmful on tiny (<64 B) or already-compressed payloads.", "ru": "Отмечает, что сжатие бесполезно/вредно на крошечных (<64 Б) или уже сжатых payload." }
        ]
      }
    },
    {
      "id": "design-pick-realtime-transport",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Pick the realtime transport for three features", "ru": "Выбери realtime-транспорт для трёх фич" },
      "prompt": { "en": "Choose one transport per feature and justify the data-flow direction, latency, proxy, and reconnect trade-offs. Self-grade against the rubric.", "ru": "Выбери по одному транспорту на фичу и обоснуй направление потока данных, задержку, прокси и компромиссы реконнекта. Оцени себя по рубрике." },
      "constraints": { "en": "Feature A: a stock dashboard pushing ~3 price ticks/sec to 200k users behind strict corporate HTTP proxies; users never send data. Feature B: a multiplayer game sending player input ~30×/sec with a <10 ms latency budget; you control the infra. Feature C: a live telemetry firehose where the newest sample matters and stale samples are worthless, and a few dropped samples are acceptable. Decide bidirectional vs unidirectional, who handles reconnect, and whether head-of-line blocking is a concern for each.", "ru": "Фича A: биржевой дашборд, пушащий ~3 тика цены/сек на 200k пользователей за строгими корпоративными HTTP-прокси; пользователи не отправляют данные. Фича B: мультиплеерная игра, шлющая ввод игрока ~30×/сек с бюджетом задержки <10 мс; инфраструктуру контролируешь ты. Фича C: поток живой телеметрии, где важен новейший сэмпл, а устаревшие бесполезны, и потеря нескольких сэмплов допустима. Реши для каждой: двунаправленный или однонаправленный, кто обрабатывает реконнект и важна ли head-of-line-блокировка." },
      "rubric": [
        { "en": "Feature A → SSE: server-push-only, browser EventSource auto-reconnects via Last-Event-ID, plain HTTP/1.1 passes the corporate proxies; latency difference vs WebSocket is irrelevant at 3 ticks/sec.", "ru": "Фича A → SSE: только server-push, браузерный EventSource автореконнектится через Last-Event-ID, обычный HTTP/1.1 проходит корпоративные прокси; разница в задержке с WebSocket несущественна при 3 тиках/сек." },
        { "en": "Feature B → WebSocket: high-frequency bidirectional input needs full-duplex with 2–6 byte frames and <10 ms latency; the app must implement reconnect/backoff on close code 1006.", "ru": "Фича B → WebSocket: высокочастотный двунаправленный ввод требует full-duplex с фреймами 2–6 байт и задержкой <10 мс; приложение само реализует реконнект/backoff на close-коде 1006." },
        { "en": "Feature C → WebTransport (HTTP/3/QUIC) unreliable datagrams: dropping stale samples is fine, newest-wins, and QUIC avoids the TCP head-of-line blocking that would stall WebSocket/SSE when a packet is lost.", "ru": "Фича C → WebTransport (HTTP/3/QUIC) unreliable-датаграммы: терять устаревшие сэмплы нормально, побеждает новейший, а QUIC избегает TCP head-of-line-блокировки, которая застопорила бы WebSocket/SSE при потере пакета." }
      ],
      "model": { "en": "A → <strong>SSE</strong>: data flows server→client only, so bidirectionality is wasted; EventSource gives free reconnection with Last-Event-ID, and text/event-stream over HTTP/1.1 survives the corporate proxies that would block a WebSocket Upgrade. B → <strong>WebSocket</strong>: ~30 client→server sends/sec demands full-duplex; frames cost 2–6 bytes over one persistent connection and keep latency under 5 ms — accept that you must write reconnect-with-backoff yourself. C → <strong>WebTransport over HTTP/3/QUIC, unreliable datagrams</strong>: because only the freshest sample matters and loss is tolerable, datagrams avoid retransmission and ordering overhead, and QUIC's independent streams sidestep the TCP head-of-line blocking that makes a single lost packet stall everything on WebSocket or SSE. Use WebTransport's reliable streams only for the control channel.", "ru": "A → <strong>SSE</strong>: данные идут только сервер→клиент, поэтому двунаправленность лишняя; EventSource даёт бесплатный реконнект с Last-Event-ID, а text/event-stream поверх HTTP/1.1 проходит корпоративные прокси, которые заблокировали бы WebSocket-Upgrade. B → <strong>WebSocket</strong>: ~30 отправок клиент→сервер/сек требуют full-duplex; фреймы стоят 2–6 байт по одному постоянному соединению и держат задержку ниже 5 мс — прими, что реконнект-с-backoff придётся писать самому. C → <strong>WebTransport поверх HTTP/3/QUIC, unreliable-датаграммы</strong>: раз важен только свежайший сэмпл и потери терпимы, датаграммы избегают ретрансмиссии и накладных расходов на упорядочивание, а независимые стримы QUIC обходят TCP head-of-line-блокировку, из-за которой один потерянный пакет застопорил бы всё на WebSocket или SSE. Надёжные стримы WebTransport используй только для управляющего канала." }
    }
  ]
}
