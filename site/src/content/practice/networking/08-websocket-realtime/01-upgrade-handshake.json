{
  "lessonKey": "networking/08-websocket-realtime/01-upgrade-handshake",
  "track": "networking",
  "tasks": [
    {
      "id": "diagnose-handshake-headers",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Read the upgrade request line by line", "ru": "Разбери upgrade-запрос строка за строкой" },
      "prompt": { "en": "A client opens a WebSocket. Fill in the blanks naming what each part of the handshake does.", "ru": "Клиент открывает WebSocket. Заполни пропуски, назвав, что делает каждая часть рукопожатия." },
      "evidence": { "en": "GET /chat HTTP/1.1\nHost: example.com\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\nSec-WebSocket-Version: 13", "ru": "GET /chat HTTP/1.1\nHost: example.com\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\nSec-WebSocket-Version: 13" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["101", "101 Switching Protocols", "101 switching protocols"], "hint": { "en": "The HTTP status code the server returns to accept the upgrade.", "ru": "HTTP-код, который сервер возвращает, принимая апгрейд." } },
          { "id": "b2", "accept": ["Sec-WebSocket-Accept", "sec-websocket-accept", "Accept"], "hint": { "en": "The response header derived from the client's key.", "ru": "Заголовок ответа, выведенный из ключа клиента." } },
          { "id": "b3", "accept": ["16", "16 bytes", "16 random bytes"], "hint": { "en": "How many random bytes the Sec-WebSocket-Key is before base64.", "ru": "Сколько случайных байт в Sec-WebSocket-Key до base64." } }
        ]
      }
    },
    {
      "id": "predict-accept-value",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict what the server hashes", "ru": "Предскажи, что сервер хеширует" },
      "prompt": { "en": "The server received Sec-WebSocket-Key from the client. Predict exactly what it concatenates and hashes to produce Sec-WebSocket-Accept, and why a plain HTTP cache cannot fake it.", "ru": "Сервер получил Sec-WebSocket-Key от клиента. Предскажи, что именно он конкатенирует и хеширует, чтобы получить Sec-WebSocket-Accept, и почему обычный HTTP-кэш не может это подделать." },
      "scenario": { "en": "Client sends <code>Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==</code>. The server must answer with a <code>Sec-WebSocket-Accept</code> header so the client trusts the upgrade. What goes into the computation?", "ru": "Клиент шлёт <code>Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==</code>. Сервер должен ответить заголовком <code>Sec-WebSocket-Accept</code>, чтобы клиент доверял апгрейду. Что входит в вычисление?" },
      "reveal": { "en": "The server computes <code>base64(SHA-1(key + \"258EAFA5-E914-47DA-95CA-C5AB0DC85B11\"))</code> — the client's key string concatenated with the fixed magic GUID from RFC 6455, SHA-1 hashed, then base64-encoded. For the sample key that yields <code>s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</code>. The GUID is the trick: a generic proxy or cache that merely echoes the request never performed this computation and does not know the constant, so it cannot forge a correct Accept value. This proves the response came from a real WebSocket-aware server, not a poisoned cache.", "ru": "Сервер вычисляет <code>base64(SHA-1(key + \"258EAFA5-E914-47DA-95CA-C5AB0DC85B11\"))</code> — строку ключа клиента, конкатенированную с фиксированным magic GUID из RFC 6455, хешированную SHA-1 и закодированную в base64. Для примера ключа это даёт <code>s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</code>. GUID — это и есть хитрость: обычный прокси или кэш, который просто эхирует запрос, никогда не делал это вычисление и не знает константу, поэтому не может подделать правильное значение Accept. Это доказывает, что ответ пришёл от настоящего WebSocket-сервера, а не от отравленного кэша." }
    },
    {
      "id": "incident-426-behind-proxy",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "WebSocket fails behind the load balancer", "ru": "WebSocket падает за балансировщиком" },
      "prompt": { "en": "Locally the chat connects fine. In staging, behind nginx and a corporate proxy, the browser console shows the connection never opens. Work the incident.", "ru": "Локально чат подключается нормально. В staging, за nginx и корпоративным прокси, консоль браузера показывает, что соединение не открывается. Разбери инцидент." },
      "steps": [
        {
          "label": { "en": "Observe", "ru": "Наблюдение" },
          "prompt": { "en": "What does the failing response look like in the Network tab, and what status code rules out a routing typo?", "ru": "Как выглядит сбойный ответ во вкладке Network и какой код исключает опечатку в маршруте?" },
          "reveal": { "en": "The request returns <code>200 OK</code> (or sometimes <code>426 Upgrade Required</code>) instead of <code>101 Switching Protocols</code>, and the <code>Upgrade: websocket</code> / <code>Connection: Upgrade</code> headers are missing from the response. A 200 means an intermediary swallowed the upgrade and served a normal HTTP reply; a 426 means a server demanded the upgrade headers it never received.", "ru": "Запрос возвращает <code>200 OK</code> (иногда <code>426 Upgrade Required</code>) вместо <code>101 Switching Protocols</code>, а заголовки <code>Upgrade: websocket</code> / <code>Connection: Upgrade</code> отсутствуют в ответе. 200 означает, что посредник проглотил апгрейд и отдал обычный HTTP-ответ; 426 — что сервер потребовал заголовки апгрейда, которых не получил." }
        },
        {
          "label": { "en": "Root cause", "ru": "Корневая причина" },
          "prompt": { "en": "Why would an HTTP/1.1 proxy strip the headers that make the upgrade work?", "ru": "Почему HTTP/1.1-прокси вырезает заголовки, нужные для апгрейда?" },
          "reveal": { "en": "<code>Connection: Upgrade</code> is a hop-by-hop header. An HTTP/1.1 intermediary is allowed to drop hop-by-hop headers (and the <code>Upgrade</code> it names) unless explicitly told to forward them. nginx by default does not pass <code>Upgrade</code>/<code>Connection</code> upstream, so the backend never sees a switch request and answers as plain HTTP. A corporate proxy that buffers full responses does the same.", "ru": "<code>Connection: Upgrade</code> — это hop-by-hop заголовок. HTTP/1.1-посредник вправе отбрасывать hop-by-hop заголовки (и названный в нём <code>Upgrade</code>), если ему явно не велено их пробрасывать. nginx по умолчанию не передаёт <code>Upgrade</code>/<code>Connection</code> наверх, поэтому бэкенд не видит запрос на переключение и отвечает как обычный HTTP. Корпоративный прокси, буферизующий полные ответы, делает то же самое." }
        },
        {
          "label": { "en": "Fix", "ru": "Фикс" },
          "prompt": { "en": "What config makes nginx forward the upgrade correctly?", "ru": "Какая конфигурация заставит nginx правильно пробрасывать апгрейд?" },
          "reveal": { "en": "In the proxy location set <code>proxy_http_version 1.1;</code>, <code>proxy_set_header Upgrade $http_upgrade;</code> and <code>proxy_set_header Connection \"upgrade\";</code> so the hop-by-hop headers are re-added on the upstream hop. Confirm the backend now returns <code>101</code> with a valid <code>Sec-WebSocket-Accept</code>. For the corporate proxy, allow CONNECT / disable response buffering for the WS path.", "ru": "В location прокси задай <code>proxy_http_version 1.1;</code>, <code>proxy_set_header Upgrade $http_upgrade;</code> и <code>proxy_set_header Connection \"upgrade\";</code>, чтобы hop-by-hop заголовки заново добавлялись на upstream-хопе. Проверь, что бэкенд теперь возвращает <code>101</code> с валидным <code>Sec-WebSocket-Accept</code>. Для корпоративного прокси разреши CONNECT / отключи буферизацию ответа для WS-пути." }
        },
        {
          "label": { "en": "Prevent", "ru": "Профилактика" },
          "prompt": { "en": "What check keeps an Upgrade-stripping hop from shipping again?", "ru": "Какая проверка не даст снова выкатить хоп, режущий Upgrade?" },
          "reveal": { "en": "Add a smoke test that opens a real WebSocket through the full proxy chain in CI and asserts the response status is exactly <code>101</code> and the <code>Sec-WebSocket-Accept</code> header matches <code>base64(SHA-1(key+GUID))</code>. A test against the backend alone hides proxy stripping — the assertion must traverse every hop that runs in production.", "ru": "Добавь в CI smoke-тест, который открывает настоящий WebSocket через полную цепочку прокси и проверяет, что статус ответа ровно <code>101</code>, а заголовок <code>Sec-WebSocket-Accept</code> совпадает с <code>base64(SHA-1(key+GUID))</code>. Тест только против бэкенда скрывает срезание прокси — проверка должна проходить через каждый хоп, работающий в проде." }
        }
      ]
    },
    {
      "id": "design-realtime-channel",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Decide: WebSocket upgrade or repeated HTTP?", "ru": "Реши: WebSocket-апгрейд или повторяющийся HTTP?" },
      "prompt": { "en": "Given the constraints, justify whether to upgrade to WebSocket and how the handshake survives your infra. Self-grade against the rubric.", "ru": "С учётом ограничений обоснуй, стоит ли апгрейдиться до WebSocket и как рукопожатие переживёт твою инфраструктуру. Оцени себя по рубрике." },
      "constraints": { "en": "A trading dashboard pushes price ticks to ~20k browsers, server-initiated, ~10 updates/sec each. Traffic passes a CDN, then an L7 load balancer, then app servers. TLS terminates at the LB. Target push latency under 50 ms.", "ru": "Дашборд трейдинга пушит ценовые тики на ~20k браузеров, инициируется сервером, ~10 обновлений/сек на каждого. Трафик идёт через CDN, затем L7-балансировщик, затем app-серверы. TLS терминируется на балансировщике. Цель — задержка пуша меньше 50 мс." },
      "rubric": [
        { "en": "Chooses WebSocket because the server pushes without a client request, and shows that repeated HTTP polling would add a full RTT per update.", "ru": "Выбирает WebSocket, потому что сервер пушит без запроса клиента, и показывает, что повторяющийся HTTP-поллинг добавил бы полный RTT на каждое обновление." },
        { "en": "Confirms every hop (CDN, LB) forwards Upgrade/Connection and passes the 101 through, not buffering it as a normal response.", "ru": "Подтверждает, что каждый хоп (CDN, балансировщик) пробрасывает Upgrade/Connection и пропускает 101 насквозь, не буферизуя его как обычный ответ." },
        { "en": "Accounts for the upgrade costing zero extra TCP/TLS round-trips since it reuses the existing connection.", "ru": "Учитывает, что апгрейд не стоит дополнительных TCP/TLS round-trip, так как переиспользует существующее соединение." }
      ],
      "model": { "en": "Upgrade to WebSocket: with ~10 server-initiated updates/sec each, HTTP polling would pay a full request round-trip per tick and never hit the 50 ms budget, while one persistent socket pushes frames with 0–5 ms added latency and a 2-byte frame header. The handshake reuses the already-open TLS connection, so it costs zero extra TCP or TLS round-trips. The risk is the path: ensure the CDN supports WebSocket pass-through and the L7 LB forwards <code>Upgrade: websocket</code> / <code>Connection: Upgrade</code> and relays the raw <code>101 Switching Protocols</code> instead of buffering it. Since TLS terminates at the LB, the upgrade is validated there; sticky routing keeps each socket pinned to one app server. 20k long-lived sockets is a connection-count concern, not a per-message one — size file descriptors and memory accordingly.", "ru": "Апгрейдься до WebSocket: при ~10 server-initiated обновлениях/сек на клиента HTTP-поллинг платил бы полный round-trip запроса на каждый тик и никогда не уложился бы в 50 мс, тогда как один постоянный сокет пушит фреймы с добавленной задержкой 0–5 мс и заголовком фрейма в 2 байта. Рукопожатие переиспользует уже открытое TLS-соединение, поэтому не стоит дополнительных TCP- или TLS-round-trip. Риск — путь: убедись, что CDN поддерживает pass-through WebSocket, а L7-балансировщик пробрасывает <code>Upgrade: websocket</code> / <code>Connection: Upgrade</code> и ретранслирует сырой <code>101 Switching Protocols</code>, а не буферизует его. Так как TLS терминируется на балансировщике, апгрейд валидируется там; sticky-маршрутизация держит каждый сокет привязанным к одному app-серверу. 20k долгоживущих сокетов — это вопрос числа соединений, а не сообщений: рассчитай файловые дескрипторы и память соответственно." }
    }
  ]
}
