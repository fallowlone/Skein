{
  "lessonKey": "linux/05-users-groups-pam/03-sudo-and-privilege",
  "track": "linux",
  "tasks": [
    {
      "id": "diagnose-sudoers-syntax",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Fill in the sudoers rule fields", "ru": "Заполни поля правила sudoers" },
      "prompt": { "en": "Fill in the correct value for each blank in the sudoers rule that grants the 'deploy' user passwordless restart of myapp.service only.", "ru": "Заполни правильное значение для каждого пропуска в правиле sudoers, предоставляющем пользователю 'deploy' право перезапуска myapp.service без пароля и только это." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["deploy"],
            "hint": { "en": "The subject of the rule — who is granted this permission?", "ru": "Субъект правила — кому предоставляется это разрешение?" }
          },
          {
            "id": "b2",
            "accept": ["NOPASSWD:", "NOPASSWD"],
            "hint": { "en": "What keyword before the command path removes the password prompt?", "ru": "Какое ключевое слово перед путём команды убирает запрос пароля?" }
          },
          {
            "id": "b3",
            "accept": ["/usr/bin/systemctl restart myapp.service", "systemctl restart myapp.service"],
            "hint": { "en": "The exact command allowed — should restart only myapp.service, not any systemctl subcommand", "ru": "Точная разрешённая команда — должна перезапускать только myapp.service, не любую подкоманду systemctl" }
          }
        ]
      }
    },
    {
      "id": "review-sudoers-rules",
      "type": "review",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Review a sudoers drop-in file for security issues", "ru": "Проверь drop-in файл sudoers на проблемы безопасности" },
      "prompt": { "en": "Review this /etc/sudoers.d/developers file for rules that grant more access than intended.", "ru": "Проверь этот файл /etc/sudoers.d/developers на правила, предоставляющие больше доступа, чем предполагалось." },
      "diff": {
        "lang": "text",
        "code": "# Developer sudo rules\nalice   ALL=(root)   /usr/bin/vim /etc/nginx/nginx.conf\nbob     ALL=(root)   /usr/bin/python3\ncharlie ALL=(root)   /usr/bin/systemctl * nginx\ndeploy  ALL=(root)   NOPASSWD: ALL"
      },
      "findings": [
        {
          "id": "vim-shell-escape",
          "planted": true,
          "label": { "en": "sudo vim allows shell escape via :!bash — grants full root shell despite appearing file-specific", "ru": "sudo vim разрешает побег через оболочку (:!bash) — даёт полную root-оболочку, несмотря на видимую привязку к файлу" },
          "severity": "bug",
          "explanation": { "en": "Any text editor granted via sudo can be used to escape to a root shell with :!bash (vim/nano) or similar. The file argument does not restrict what vim can execute. Replace with: alice ALL=(root) sudoedit /etc/nginx/nginx.conf — sudoedit edits a copy as alice's own process with no shell escape possible.", "ru": "Любой текстовый редактор, предоставленный через sudo, можно использовать для получения root-оболочки через :!bash (vim/nano) или аналогично. Аргумент файла не ограничивает то, что vim может выполнить. Замени на: alice ALL=(root) sudoedit /etc/nginx/nginx.conf — sudoedit редактирует копию от имени процесса alice без возможности побега через оболочку." }
        },
        {
          "id": "python3-full-root",
          "planted": true,
          "label": { "en": "sudo python3 with no argument restriction gives full root — python3 -c 'import os; os.execv(\"/bin/bash\", [\"-p\"])' works", "ru": "sudo python3 без ограничения аргументов даёт полный root — python3 -c 'import os; os.execv(\"/bin/bash\", [\"-p\"])' работает" },
          "severity": "bug",
          "explanation": { "en": "Granting sudo to an interpreter (python3, perl, ruby, awk) with no argument restriction is equivalent to granting root shell access. The user can execute arbitrary code: sudo python3 -c 'import os; os.execv(\"/bin/bash\", [\"-p\"])'. If bob needs to run a specific script, grant: bob ALL=(root) /usr/bin/python3 /opt/scripts/specific_task.py — but be aware bob cannot modify that script path.", "ru": "Предоставление sudo интерпретатору (python3, perl, ruby, awk) без ограничения аргументов эквивалентно предоставлению доступа к root-оболочке. Пользователь может выполнять произвольный код: sudo python3 -c 'import os; os.execv(\"/bin/bash\", [\"-p\"])'. Если бобу нужно запускать конкретный скрипт, используй: bob ALL=(root) /usr/bin/python3 /opt/scripts/specific_task.py — но учти, что bob не сможет изменить этот путь к скрипту." }
        },
        {
          "id": "systemctl-wildcard-bypass",
          "planted": true,
          "label": { "en": "systemctl * nginx wildcard matches spaces — allows subcommands beyond the intended restart/reload", "ru": "Wildcard systemctl * nginx совпадает с пробелами — разрешает подкоманды за пределами задуманных restart/reload" },
          "severity": "bug",
          "explanation": { "en": "The sudoers wildcard * matches any string including spaces. 'systemctl * nginx' matches 'systemctl restart nginx' (intended) but also 'systemctl set-environment LD_PRELOAD=/tmp/evil.so nginx' (not intended). Always list specific subcommands: charlie ALL=(root) /usr/bin/systemctl restart nginx, /usr/bin/systemctl reload nginx", "ru": "Wildcard * в sudoers совпадает с любой строкой, включая пробелы. 'systemctl * nginx' совпадает с 'systemctl restart nginx' (задумано), но также с 'systemctl set-environment LD_PRELOAD=/tmp/evil.so nginx' (не задумано). Всегда перечисляй конкретные подкоманды: charlie ALL=(root) /usr/bin/systemctl restart nginx, /usr/bin/systemctl reload nginx" }
        },
        {
          "id": "deploy-nopasswd-all",
          "planted": true,
          "label": { "en": "deploy user has NOPASSWD: ALL — full passwordless root, not scoped to any specific command", "ru": "Пользователь deploy имеет NOPASSWD: ALL — полный passwordless root, не ограниченный конкретной командой" },
          "severity": "bug",
          "explanation": { "en": "NOPASSWD: ALL grants the deploy user complete root access to everything on the system without any password. Even for a CI service account, this is far beyond what is needed. Scope it to the specific deployment commands: deploy ALL=(root) NOPASSWD: /usr/bin/systemctl restart myapp.service", "ru": "NOPASSWD: ALL предоставляет пользователю deploy полный root-доступ ко всему на системе без пароля. Даже для сервисной учётки CI это намного больше необходимого. Ограничь конкретными командами деплоя: deploy ALL=(root) NOPASSWD: /usr/bin/systemctl restart myapp.service" }
        }
      ]
    },
    {
      "id": "incident-locked-out-of-sudo",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Recover from a sudo lockout caused by a bad sudoers edit", "ru": "Восстановление после блокировки sudo из-за ошибки в sudoers" },
      "prompt": { "en": "Walk through the recovery procedure after a syntax error in /etc/sudoers has disabled sudo and you have no other root access path.", "ru": "Пройди по шагам процедуру восстановления после синтаксической ошибки в /etc/sudoers, отключившей sudo, при отсутствии других путей доступа к root." },
      "steps": [
        {
          "label": { "en": "Confirm the lockout and identify what broke", "ru": "Подтверждаем блокировку и определяем, что сломалось" },
          "prompt": { "en": "sudo fails with 'sudo: parse error in /etc/sudoers near line 12'. You have an active SSH session as alice. What do you check first to understand the severity?", "ru": "sudo завершается с ошибкой 'sudo: parse error in /etc/sudoers near line 12'. У тебя есть активная SSH-сессия как alice. Что ты проверяешь первым, чтобы понять серьёзность ситуации?" },
          "reveal": { "en": "Check if you have any other root access: ssh root@localhost (does the server allow root SSH?), check /etc/ssh/sshd_config for PermitRootLogin. Check if pkexec is available (polkit-based privilege escalation as a sudo fallback): which pkexec. Check if you are in any group that might grant direct access. If none of these work, you need console/physical access or a recovery mechanism.", "ru": "Проверь наличие других путей доступа к root: ssh root@localhost (разрешён ли root SSH на сервере?), проверь /etc/ssh/sshd_config на наличие PermitRootLogin. Проверь доступность pkexec (повышение привилегий через polkit как запасной вариант sudo): which pkexec. Проверь, состоишь ли ты в группе с прямым доступом. Если ничего не работает, нужен консольный/физический доступ или механизм восстановления." }
        },
        {
          "label": { "en": "Access the system for repair", "ru": "Получаем доступ к системе для ремонта" },
          "prompt": { "en": "You have console access (cloud provider rescue mode, or physical keyboard). The system boots normally but sudo is broken. What is the sequence to get a root shell for repair?", "ru": "У тебя есть консольный доступ (режим спасения облачного провайдера или физическая клавиатура). Система загружается нормально, но sudo сломан. Какова последовательность для получения root-оболочки для ремонта?" },
          "reveal": { "en": "Option A (GRUB single user): Reboot → hold Shift at boot → edit kernel cmdline → append 'single' or 'init=/bin/bash' → boot → filesystem may be read-only, remount: mount -o remount,rw /\nOption B (cloud rescue): Attach the disk to a rescue VM, mount it at /mnt, chroot /mnt.\nOption C (if pkexec available without sudo): pkexec visudo — polkit may still work if its own config is intact.", "ru": "Вариант A (GRUB single user): перезагрузка → держать Shift при загрузке → редактировать cmdline ядра → добавить 'single' или 'init=/bin/bash' → загрузиться → файловая система может быть read-only, перемонтировать: mount -o remount,rw /\nВариант B (облачное спасение): подключить диск к rescue VM, смонтировать по /mnt, chroot /mnt.\nВариант C (если pkexec доступен без sudo): pkexec visudo — polkit может работать при целой собственной конфигурации." }
        },
        {
          "label": { "en": "Fix the sudoers file and verify", "ru": "Исправляем файл sudoers и проверяем" },
          "prompt": { "en": "You now have a root shell. How do you safely fix the sudoers file, and how do you verify it before rebooting?", "ru": "У тебя теперь есть root-оболочка. Как безопасно исправить файл sudoers, и как проверить его до перезагрузки?" },
          "reveal": { "en": "Use visudo to edit (even as root in single-user mode, visudo is safer because it syntax-checks): visudo\nFix the offending line (the parse error message told you line 12).\nBefore saving, visudo will run the syntax check automatically.\nAfter saving, verify: visudo -c (check all of /etc/sudoers and /etc/sudoers.d/)\nTest WITHOUT rebooting from a second terminal: su - alice -c 'sudo -l'\nOnly reboot once sudo -l works correctly. Never close your repair session until you have confirmed sudo works from a separate login.", "ru": "Используй visudo для редактирования (даже от root в single-user mode, visudo безопаснее — он проверяет синтаксис): visudo\nИсправь проблемную строку (сообщение об ошибке указало строку 12).\nПеред сохранением visudo автоматически запустит проверку синтаксиса.\nПосле сохранения проверь: visudo -c (проверяет весь /etc/sudoers и /etc/sudoers.d/)\nПроверь БЕЗ перезагрузки из второго терминала: su - alice -c 'sudo -l'\nПерезагружайся только после подтверждения работы sudo -l. Никогда не закрывай сессию ремонта, пока не подтвердишь работу sudo из отдельного входа." }
        }
      ]
    }
  ]
}
