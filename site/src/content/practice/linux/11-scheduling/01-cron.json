{
  "lessonKey": "linux/11-scheduling/01-cron",
  "track": "linux",
  "tasks": [
    {
      "id": "predict-cron-fields",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What time does this cron job fire?", "ru": "В какое время срабатывает эта задача cron?" },
      "prompt": { "en": "Trace the five time fields and predict when the job runs.", "ru": "Разбери пять временны́х полей и предскажи, когда запускается задача." },
      "scenario": { "en": "This crontab entry: <code>30 6 * * 1-5 /usr/bin/report.sh</code>. When does it run?", "ru": "Запись crontab: <code>30 6 * * 1-5 /usr/bin/report.sh</code>. Когда она запускается?" },
      "reveal": { "en": "At 06:30 on weekdays (Monday through Friday). Field breakdown: minute=30, hour=6, day-of-month=* (every day), month=* (every month), day-of-week=1-5 (Mon–Fri). The day-of-week range 1-5 restricts the job to weekdays only.", "ru": "В 06:30 по будням (с понедельника по пятницу). Разбор полей: минута=30, час=6, день_месяца=* (каждый), месяц=* (каждый), день_недели=1-5 (Пн–Пт). Диапазон дней недели 1-5 ограничивает задачу только буднями." }
    },
    {
      "id": "diagnose-cron-path",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Why does the script fail in cron but not in the shell?", "ru": "Почему скрипт завершается ошибкой в cron, но не в оболочке?" },
      "prompt": { "en": "Fill in the blanks to complete the explanation of why this cron job fails silently.", "ru": "Заполни пропуски, чтобы объяснить, почему эта задача cron молча завершается с ошибкой." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["PATH", "path", "PATH variable", "переменная PATH"],
            "hint": { "en": "cron sets this environment variable to just /usr/bin:/bin", "ru": "cron устанавливает эту переменную окружения только в /usr/bin:/bin" }
          },
          {
            "id": "b2",
            "accept": ["absolute path", "full path", "абсолютный путь", "полный путь", "/usr/local/bin/node"],
            "hint": { "en": "To fix the PATH issue, use the ___ to the binary instead of just its name", "ru": "Чтобы исправить проблему с PATH, используй ___ до бинарника вместо его имени" }
          }
        ]
      }
    },
    {
      "id": "predict-percent-trap",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "What does the % do in this cron command?", "ru": "Что делает % в этой команде cron?" },
      "prompt": { "en": "Predict what cron does with the unescaped % character before revealing.", "ru": "Предскажи, что cron сделает с неэкранированным символом %, прежде чем открыть ответ." },
      "scenario": { "en": "A crontab entry contains: <code>0 2 * * * /usr/bin/backup.sh &gt; /tmp/log-$(date +%Y%m%d).txt</code>. The backup script runs but the log filename is wrong — it appears as just <code>/tmp/log-</code>. Why?", "ru": "Запись crontab содержит: <code>0 2 * * * /usr/bin/backup.sh &gt; /tmp/log-$(date +%Y%m%d).txt</code>. Скрипт бэкапа запускается, но имя лог-файла неверно — оно выглядит просто как <code>/tmp/log-</code>. Почему?" },
      "reveal": { "en": "cron treats an unescaped % as a newline. The command is truncated at the first % — so cron runs <code>/usr/bin/backup.sh &gt; /tmp/log-$(date +</code> and the rest (<code>Y%m%d).txt</code>) becomes stdin for the command. The fix is to escape every %: <code>date +\\%Y\\%m\\%d</code>.", "ru": "cron воспринимает неэкранированный % как символ новой строки. Команда усекается на первом % — cron выполняет <code>/usr/bin/backup.sh &gt; /tmp/log-$(date +</code>, а остаток (<code>Y%m%d).txt</code>) становится stdin для команды. Исправление: экранировать каждый %: <code>date +\\%Y\\%m\\%d</code>." }
    },
    {
      "id": "review-crontab-entry",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Find the bugs in this crontab", "ru": "Найди ошибки в этом crontab" },
      "prompt": { "en": "A colleague added this crontab entry. Find all problems.", "ru": "Коллега добавил эту запись crontab. Найди все проблемы." },
      "diff": {
        "lang": "bash",
        "code": "# Goal: run /opt/app/deploy.py every day at 03:00 and log output\n0 3 * * * python3 /opt/app/deploy.py > /var/log/deploy-$(date +%Y%m%d).log"
      },
      "findings": [
        {
          "id": "no-absolute-path",
          "label": { "en": "python3 is called without an absolute path — will fail if /usr/local/bin is not in cron's minimal PATH", "ru": "python3 вызван без абсолютного пути — упадёт, если /usr/local/bin не в минимальном PATH cron" },
          "severity": "bug",
          "explanation": { "en": "cron's PATH is /usr/bin:/bin. If python3 is installed in /usr/local/bin (common with pyenv, Homebrew, or custom installs), the command silently fails. Fix: use /usr/bin/python3 or /usr/local/bin/python3 explicitly.", "ru": "PATH cron — /usr/bin:/bin. Если python3 установлен в /usr/local/bin (типично при pyenv, Homebrew или пользовательских установках), команда молча завершится с ошибкой. Исправление: явно указать /usr/bin/python3 или /usr/local/bin/python3." },
          "planted": true
        },
        {
          "id": "unescaped-percent",
          "label": { "en": "date +%Y%m%d contains unescaped % — cron will truncate the command at the first %", "ru": "date +%Y%m%d содержит неэкранированные % — cron усечёт команду на первом %" },
          "severity": "bug",
          "explanation": { "en": "Every % in a crontab command is translated to a newline by cron. The log filename will be wrong and output will be misdirected. Fix: write date +\\%Y\\%m\\%d.", "ru": "Каждый % в команде crontab переводится cron в символ новой строки. Имя лог-файла будет неверным, а вывод — перенаправлен не туда. Исправление: написать date +\\%Y\\%m\\%d." },
          "planted": true
        },
        {
          "id": "no-stderr-redirect",
          "label": { "en": "stderr is not redirected — errors go to the mail spool and are silently lost on most systems", "ru": "stderr не перенаправлен — ошибки уходят в спул почты и молча теряются на большинстве систем" },
          "severity": "tradeoff",
          "explanation": { "en": "Without 2>&1, any error output from the Python script is mailed to the cron user — which nobody reads. Append 2>&1 to capture both stdout and stderr: > /var/log/... 2>&1 (or use >> to append rather than overwrite).", "ru": "Без 2>&1 любой вывод ошибок Python-скрипта отправляется на почту cron-пользователю — которую никто не читает. Добавь 2>&1, чтобы захватить stdout и stderr: > /var/log/... 2>&1 (или используй >> для дозаписи вместо перезаписи)." },
          "planted": true
        }
      ]
    },
    {
      "id": "design-cron-schedule",
      "type": "design",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Design a safe cron entry for a database backup", "ru": "Спроектируй безопасную запись cron для резервного копирования БД" },
      "prompt": { "en": "Design a complete, production-safe crontab entry that satisfies all the constraints, then compare with the model.", "ru": "Спроектируй полную, безопасную для продакшна запись crontab, удовлетворяющую всем ограничениям, и сравни с образцом." },
      "constraints": { "en": "Requirements: (1) run /usr/local/bin/pg_dump as user postgres at 02:30 every night; (2) write output to /var/backups/db/dump-YYYYMMDD.sql.gz (date in filename); (3) capture both stdout and stderr to a separate log at /var/log/pgdump.log; (4) use /etc/cron.d/ so the job survives user-account changes.", "ru": "Требования: (1) запускать /usr/local/bin/pg_dump от имени пользователя postgres в 02:30 каждую ночь; (2) сохранять вывод в /var/backups/db/dump-YYYYMMDD.sql.gz (дата в имени файла); (3) перехватывать stdout и stderr в отдельный лог /var/log/pgdump.log; (4) использовать /etc/cron.d/, чтобы задача пережила изменения учётных записей." },
      "rubric": [
        { "en": "Time fields are 30 2 * * *", "ru": "Временны́е поля: 30 2 * * *" },
        { "en": "User field is postgres (required in /etc/cron.d/)", "ru": "Поле пользователя: postgres (обязательно в /etc/cron.d/)" },
        { "en": "% in date format is escaped as \\%", "ru": "% в формате даты экранирован как \\%" },
        { "en": "stderr is redirected with 2>&1 or 2>>/var/log/pgdump.log", "ru": "stderr перенаправлен через 2>&1 или 2>>/var/log/pgdump.log" }
      ],
      "model": { "en": "In <code>/etc/cron.d/pg-backup</code>:<br><code>30 2 * * * postgres /usr/local/bin/pg_dump mydb | gzip &gt; /var/backups/db/dump-$(date +\\%Y\\%m\\%d).sql.gz 2&gt;&gt;/var/log/pgdump.log</code><br>Key decisions: /etc/cron.d/ with the postgres user field survives account management; \\% escapes the date format; 2>> appends errors to a persistent log without overwriting it each night.", "ru": "В файле <code>/etc/cron.d/pg-backup</code>:<br><code>30 2 * * * postgres /usr/local/bin/pg_dump mydb | gzip &gt; /var/backups/db/dump-$(date +\\%Y\\%m\\%d).sql.gz 2&gt;&gt;/var/log/pgdump.log</code><br>Ключевые решения: /etc/cron.d/ с полем пользователя postgres переживает управление учётными записями; \\% экранирует формат даты; 2>> дозаписывает ошибки в постоянный лог без перезаписи каждую ночь." }
    }
  ]
}
