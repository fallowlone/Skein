{
  "lessonKey": "linux/02-boot-and-init/02-grub",
  "track": "linux",
  "tasks": [
    {
      "id": "predict-grub-edit-effect",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "What happens after this GRUB edit?", "ru": "Что произойдёт после этой правки GRUB?" },
      "prompt": { "en": "Predict the outcome of the described action.", "ru": "Предскажи результат описанного действия." },
      "scenario": { "en": "At the GRUB menu you press <code>e</code>, find the <code>linux</code> line, and append <code>init=/bin/bash</code>. You press Ctrl+X. Describe: (1) what boots, (2) whether the change persists after reboot, (3) whether the filesystem is initially writable.", "ru": "В меню GRUB ты нажимаешь <code>e</code>, находишь строку <code>linux</code> и добавляешь в конец <code>init=/bin/bash</code>. Нажимаешь Ctrl+X. Опиши: (1) что загрузится, (2) сохранится ли изменение после перезагрузки, (3) будет ли файловая система изначально доступна для записи." },
      "reveal": { "en": "(1) The kernel boots and executes /bin/bash as PID 1 — you get a root shell immediately, no login, no systemd. (2) The change does NOT persist — it only applies to this one boot. The next boot uses the normal grub.cfg. (3) The filesystem is mounted read-only initially. You must run <code>mount -o remount,rw /</code> before making any changes.", "ru": "(1) Ядро загружается и выполняет /bin/bash как PID 1 — ты получаешь root-оболочку немедленно, без входа, без systemd. (2) Изменение НЕ сохраняется — применяется только к этой одной загрузке. Следующая загрузка использует нормальный grub.cfg. (3) Файловая система монтируется только для чтения изначально. Нужно выполнить <code>mount -o remount,rw /</code> перед любыми изменениями." }
    },
    {
      "id": "diagnose-grub-config",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "GRUB config layer terms", "ru": "Термины слоёв конфигурации GRUB" },
      "prompt": { "en": "Fill in the blanks about GRUB's configuration architecture.", "ru": "Заполни пропуски об архитектуре конфигурации GRUB." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["/etc/default/grub", "etc/default/grub"],
            "hint": { "en": "The human-editable source file for GRUB options like GRUB_TIMEOUT and GRUB_CMDLINE_LINUX_DEFAULT.", "ru": "Редактируемый человеком источник опций GRUB, таких как GRUB_TIMEOUT и GRUB_CMDLINE_LINUX_DEFAULT." }
          },
          {
            "id": "b2",
            "accept": ["update-grub", "grub-mkconfig", "grub2-mkconfig"],
            "hint": { "en": "The command that reads /etc/default/grub and /etc/grub.d/* to generate /boot/grub/grub.cfg on Debian/Ubuntu.", "ru": "Команда, которая читает /etc/default/grub и /etc/grub.d/* для генерации /boot/grub/grub.cfg на Debian/Ubuntu." }
          },
          {
            "id": "b3",
            "accept": ["/boot/grub/grub.cfg", "boot/grub/grub.cfg", "grub.cfg"],
            "hint": { "en": "The generated file GRUB actually reads at boot time — never edit this directly.", "ru": "Сгенерированный файл, который GRUB реально читает при загрузке — никогда не редактируй его напрямую." }
          }
        ]
      }
    },
    {
      "id": "review-grub-runbook",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the errors in this GRUB runbook", "ru": "Найди ошибки в этом GRUB runbook" },
      "prompt": { "en": "A colleague wrote this runbook for customizing GRUB. Find all planted errors.", "ru": "Коллега написал этот runbook для настройки GRUB. Найди все намеренно внесённые ошибки." },
      "diff": {
        "lang": "bash",
        "code": "# Runbook: Add kernel parameter net.ifnames=0\n\n# Step 1: Edit the generated config directly\nsudo nano /boot/grub/grub.cfg\n# Find the linux line and add net.ifnames=0\n\n# Step 2: No need to run update-grub —\n# changes to grub.cfg take effect immediately\n\n# Step 3: Reboot to apply\nsudo reboot"
      },
      "findings": [
        {
          "id": "editing-grub-cfg-directly",
          "label": { "en": "Never edit /boot/grub/grub.cfg directly — it is generated and will be overwritten by update-grub on next kernel install", "ru": "Никогда не редактируй /boot/grub/grub.cfg напрямую — он генерируется и будет перезаписан update-grub при следующей установке ядра" },
          "severity": "bug",
          "explanation": { "en": "The correct workflow is: edit GRUB_CMDLINE_LINUX_DEFAULT in /etc/default/grub, then run sudo update-grub. Direct edits to grub.cfg survive until the next kernel update, at which point they are silently lost.", "ru": "Правильный процесс: отредактировать GRUB_CMDLINE_LINUX_DEFAULT в /etc/default/grub, затем запустить sudo update-grub. Прямые правки grub.cfg выживают до следующего обновления ядра, после чего молча теряются." },
          "planted": true
        },
        {
          "id": "no-update-grub-needed-false",
          "label": { "en": "update-grub IS required after editing /etc/default/grub — the change does not take effect until grub.cfg is regenerated", "ru": "update-grub ОБЯЗАТЕЛЕН после редактирования /etc/default/grub — изменение не вступает в силу, пока grub.cfg не перегенерирован" },
          "severity": "bug",
          "explanation": { "en": "The runbook says 'no need to run update-grub'. This is wrong in both scenarios. If you correctly edited /etc/default/grub, you must run update-grub to write the change into grub.cfg before rebooting.", "ru": "Runbook говорит 'не нужно запускать update-grub'. Это неверно в обоих сценариях. Если ты правильно отредактировал /etc/default/grub, нужно запустить update-grub чтобы записать изменение в grub.cfg перед перезагрузкой." },
          "planted": true
        }
      ]
    }
  ]
}
