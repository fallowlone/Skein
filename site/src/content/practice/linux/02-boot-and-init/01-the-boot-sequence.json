{
  "lessonKey": "linux/02-boot-and-init/01-the-boot-sequence",
  "track": "linux",
  "tasks": [
    {
      "id": "predict-boot-stage",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Which stage printed this message?", "ru": "Какой этап вывел это сообщение?" },
      "prompt": { "en": "Identify the boot stage responsible for each message.", "ru": "Определи этап загрузки, ответственный за каждое сообщение." },
      "scenario": { "en": "You see these messages during a console boot. For each one, name the stage (firmware / bootloader / kernel / initramfs / PID 1):<br>A) <code>error: no such device: UUID=abc123</code><br>B) <code>[ 0.123456] Freeing unused kernel image memory</code><br>C) <code>Waiting for root device /dev/sda2...</code><br>D) <code>Started OpenBSD Secure Shell server.</code>", "ru": "Ты видишь эти сообщения при загрузке через консоль. Для каждого назови этап (прошивка / загрузчик / ядро / initramfs / PID 1):<br>A) <code>error: no such device: UUID=abc123</code><br>B) <code>[ 0.123456] Freeing unused kernel image memory</code><br>C) <code>Waiting for root device /dev/sda2...</code><br>D) <code>Started OpenBSD Secure Shell server.</code>" },
      "reveal": { "en": "A) Bootloader (GRUB) — UUID not found in its device scan. B) Kernel — the bracketed timestamp format is the kernel ring buffer. C) initramfs — the 'Waiting for root device' message is printed by initramfs scripts. D) PID 1 (systemd) — 'Started ...' lines are systemd unit activation messages.", "ru": "A) Загрузчик (GRUB) — UUID не найден при сканировании устройств. B) Ядро — формат с временной меткой в скобках — это кольцевой буфер ядра. C) initramfs — сообщение 'Waiting for root device' выводят скрипты initramfs. D) PID 1 (systemd) — строки 'Started ...' — это сообщения активации юнитов systemd." }
    },
    {
      "id": "diagnose-boot-hang",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Fill in the boot recovery blanks", "ru": "Заполни пропуски в восстановлении загрузки" },
      "prompt": { "en": "Fill in the blanks with the correct terms.", "ru": "Заполни пропуски правильными терминами." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["initramfs", "initial ramdisk", "initrd"],
            "hint": { "en": "The temporary in-memory filesystem the kernel mounts before the real root disk is accessible.", "ru": "Временная файловая система в памяти, которую ядро монтирует до того, как реальный корневой диск доступен." }
          },
          {
            "id": "b2",
            "accept": ["switch_root", "pivot_root"],
            "hint": { "en": "The operation that transitions from the temporary RAM filesystem to the real disk root filesystem.", "ru": "Операция, переключающая с временной RAM-файловой системы на реальную дисковую корневую файловую систему." }
          },
          {
            "id": "b3",
            "accept": ["blkid", "lsblk"],
            "hint": { "en": "The command that shows the UUID of a block device — used when fixing a UUID mismatch after disk replacement.", "ru": "Команда, показывающая UUID блочного устройства — используется при исправлении несоответствия UUID после замены диска." }
          }
        ]
      }
    },
    {
      "id": "review-boot-explanation",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the error in this boot explanation", "ru": "Найди ошибку в этом объяснении загрузки" },
      "prompt": { "en": "A junior engineer wrote this runbook section. Find the factual error.", "ru": "Джуниор написал этот раздел runbook. Найди фактическую ошибку." },
      "diff": {
        "lang": "text",
        "code": "Linux Boot Recovery Runbook\n\nIf the system shows 'Waiting for root device':\n1. The GRUB bootloader failed to load the kernel.\n2. Re-install GRUB using: grub-install /dev/sda\n3. Run update-grub to regenerate config.\n\nThis message means GRUB cannot find the kernel image\non the boot partition."
      },
      "findings": [
        {
          "id": "wrong-stage-attribution",
          "label": { "en": "'Waiting for root device' is an initramfs message, not a GRUB failure — GRUB already succeeded if you see it", "ru": "'Waiting for root device' — это сообщение initramfs, а не сбой GRUB — GRUB уже успешно выполнился, если ты это видишь" },
          "severity": "bug",
          "explanation": { "en": "'Waiting for root device' is printed by scripts inside the initramfs after the kernel has already decompressed and the initramfs has already been mounted. GRUB's job was completed successfully — it loaded the kernel and initramfs. The actual problem is that initramfs cannot find or mount the real root disk (wrong UUID, missing LVM, encrypted volume not unlocked). Re-installing GRUB will not fix this.", "ru": "'Waiting for root device' выводится скриптами внутри initramfs после того, как ядро уже распаковалось и initramfs уже смонтирован. Работа GRUB успешно завершена — он загрузил ядро и initramfs. Реальная проблема: initramfs не может найти или смонтировать реальный корневой диск (неверный UUID, отсутствующий LVM, незашифрованный том). Переустановка GRUB это не исправит." },
          "planted": true
        }
      ]
    },
    {
      "id": "incident-uuid-mismatch",
      "type": "incident",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "VM will not boot after disk migration", "ru": "VM не загружается после миграции диска" },
      "prompt": { "en": "Work through the recovery steps for a VM that stopped booting after its block device was replaced.", "ru": "Пройди шаги восстановления VM, которая перестала загружаться после замены блочного устройства." },
      "steps": [
        {
          "label": { "en": "Identify the stage", "ru": "Определи этап" },
          "prompt": { "en": "The VM console shows 'Waiting for root device /dev/disk/by-uuid/OLD-UUID-HERE...' and hangs. Which boot stage failed and why?", "ru": "Консоль VM показывает 'Waiting for root device /dev/disk/by-uuid/OLD-UUID-HERE...' и зависает. Какой этап загрузки завершился неудачей и почему?" },
          "reveal": { "en": "Stage 4 — initramfs. The initramfs scripts are looking for a block device with a specific UUID that no longer exists (it was the old disk). The new disk has a different UUID, so the lookup fails and the system hangs waiting.", "ru": "Этап 4 — initramfs. Скрипты initramfs ищут блочное устройство с определённым UUID, которого больше не существует (это был старый диск). У нового диска другой UUID, поэтому поиск не находит ничего и система зависает в ожидании." }
        },
        {
          "label": { "en": "Boot to recovery environment", "ru": "Загрузись в среду восстановления" },
          "prompt": { "en": "You cannot fix the running system because it never finishes booting. What do you boot from, and what is the first command you run on the new disk?", "ru": "Ты не можешь исправить работающую систему, потому что она так и не завершает загрузку. С чего загружаться, и какую первую команду запустить для нового диска?" },
          "reveal": { "en": "Boot from a live USB/ISO (Ubuntu live, rescue disk). First command: <code>blkid /dev/sda2</code> (or whichever partition is the root) to find the real UUID of the new disk.", "ru": "Загрузиться с live USB/ISO (Ubuntu live, rescue disk). Первая команда: <code>blkid /dev/sda2</code> (или какой раздел является корневым) чтобы найти реальный UUID нового диска." }
        },
        {
          "label": { "en": "Fix fstab and regenerate bootloader config", "ru": "Исправь fstab и перегенерируй конфиг загрузчика" },
          "prompt": { "en": "You have the new UUID. List the files you must update and the command to regenerate the bootloader config.", "ru": "У тебя есть новый UUID. Перечисли файлы, которые нужно обновить, и команду для перегенерации конфига загрузчика." },
          "reveal": { "en": "Mount root: <code>mount /dev/sda2 /mnt</code>. Edit <code>/mnt/etc/fstab</code> — replace old UUID with new one. Bind-mount /dev /proc /sys, then chroot: <code>chroot /mnt update-grub</code>. update-grub writes the new UUID into grub.cfg and also regenerates the initramfs reference.", "ru": "Монтируем корень: <code>mount /dev/sda2 /mnt</code>. Редактируем <code>/mnt/etc/fstab</code> — заменяем старый UUID новым. Bind-монтируем /dev /proc /sys, затем chroot: <code>chroot /mnt update-grub</code>. update-grub записывает новый UUID в grub.cfg и также обновляет ссылку initramfs." }
        }
      ]
    }
  ]
}
