{
  "lessonKey": "linux/07-storage-and-lvm/04-disk-failure-and-recovery",
  "track": "linux",
  "tasks": [
    {
      "id": "diagnose-mdstat-reading",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": {
        "en": "Read /proc/mdstat output",
        "ru": "Читай вывод /proc/mdstat"
      },
      "prompt": {
        "en": "Fill in the correct answer for each /proc/mdstat interpretation question.",
        "ru": "Заполни правильный ответ для каждого вопроса по интерпретации /proc/mdstat."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["3", "three"],
            "hint": {
              "en": "md0 : active raid5 sde[3] sdc[1] sdb[0]  [4/3] [UUU_] — how many disks are currently active?",
              "ru": "md0 : active raid5 sde[3] sdc[1] sdb[0]  [4/3] [UUU_] — сколько дисков сейчас активны?"
            }
          },
          {
            "id": "b2",
            "accept": ["sdd", "/dev/sdd", "position 2", "slot 2"],
            "hint": {
              "en": "md0 : active raid5 sde[3] sdc[1] sdb[0]  [4/3] [UUU_] — which disk is missing? (It was sdb=0, sdc=1, sdd=2, sde=3)",
              "ru": "md0 : active raid5 sde[3] sdc[1] sdb[0]  [4/3] [UUU_] — какой диск отсутствует? (sdb=0, sdc=1, sdd=2, sde=3)"
            }
          },
          {
            "id": "b3",
            "accept": ["mdadm --fail", "mdadm /dev/md0 --fail"],
            "hint": {
              "en": "The mdadm command to explicitly mark a disk as faulty before removing it, when the kernel has not auto-failed it.",
              "ru": "Команда mdadm для явной пометки диска как неисправного перед удалением, когда ядро не сделало это автоматически."
            }
          },
          {
            "id": "b4",
            "accept": ["update-initramfs -u", "update-initramfs", "sudo update-initramfs -u"],
            "hint": {
              "en": "The command to run after updating /etc/mdadm/mdadm.conf so the new config is included in the boot initramfs on Debian/Ubuntu.",
              "ru": "Команда для запуска после обновления /etc/mdadm/mdadm.conf, чтобы новая конфигурация была включена в initramfs при загрузке на Debian/Ubuntu."
            }
          }
        ]
      }
    },
    {
      "id": "predict-wrong-disk-removal",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": {
        "en": "Predict the outcome of removing the wrong disk from a degraded RAID 5",
        "ru": "Предскажи исход удаления не того диска из деградированного RAID 5"
      },
      "prompt": {
        "en": "Predict exactly what happens and whether the data is recoverable.",
        "ru": "Предскажи точно что произойдёт и можно ли восстановить данные."
      },
      "scenario": {
        "en": "RAID 5 with 4 disks. /proc/mdstat shows [4/3] [UUU_] — position 3 (sde) is the failed disk.\nAn operator under pressure runs:\nsudo mdadm /dev/md0 --fail /dev/sdc\nsudo mdadm /dev/md0 --remove /dev/sdc\n\nsdc was an ACTIVE disk (position 1). What happens?",
        "ru": "RAID 5 из 4 дисков. /proc/mdstat показывает [4/3] [UUU_] — позиция 3 (sde) — упавший диск.\nОператор под давлением запускает:\nsudo mdadm /dev/md0 --fail /dev/sdc\nsudo mdadm /dev/md0 --remove /dev/sdc\n\nsdc был АКТИВНЫМ диском (позиция 1). Что происходит?"
      },
      "reveal": {
        "en": "Immediately after --fail /dev/sdc:\n- The array now has TWO failed disks: sde (original failure) and sdc (just failed)\n- /proc/mdstat: md0 : active raid5 sdb[0]  [4/2] [U__U] or goes inactive\n- RAID 5 minimum disks = N-1 = 3. With only 2 active (sdb, sde was already gone), array goes INACTIVE.\n- All data is immediately inaccessible.\n\nIs data recoverable?\n- POSSIBLY, if you act fast. The data on sdb, sdc (before you --removed it), and sde (original failure) is still physically present on the disks.\n- Recovery path: do NOT remove sdc yet. Stop the array: mdadm --stop /dev/md0. Attempt reassembly with: mdadm --assemble --force /dev/md0 /dev/sdb /dev/sdc /dev/sdd -- this may bring the array up in degraded read-only mode to salvage data.\n- If --remove already ran: professional data recovery may still read raw disk content but is expensive.\n\nPrevention: Always cross-reference mdadm --detail (shows 'active sync' vs 'faulty'), journalctl for the disk failure event, and smartctl -a before running --fail on any disk.",
        "ru": "Сразу после --fail /dev/sdc:\n- Массив теперь имеет ДВА упавших диска: sde (исходный отказ) и sdc (только что упал)\n- /proc/mdstat: md0 : active raid5 sdb[0]  [4/2] [U__U] или становится неактивным\n- Минимум дисков RAID 5 = N-1 = 3. С только 2 активными (sdb, sde уже пропал), массив становится НЕАКТИВНЫМ.\n- Все данные немедленно недоступны.\n\nМожно ли восстановить данные?\n- ВОЗМОЖНО, если действовать быстро. Данные на sdb, sdc (до --remove) и sde (исходный отказ) физически присутствуют на дисках.\n- Путь восстановления: НЕ удалять sdc. Остановить массив: mdadm --stop /dev/md0. Попытаться пересобрать: mdadm --assemble --force /dev/md0 /dev/sdb /dev/sdc /dev/sdd — может поднять массив в деградированном режиме только для чтения для сохранения данных.\n- Если --remove уже выполнен: профессиональное восстановление данных может читать сырое содержимое дисков, но это дорого.\n\nПрофилактика: всегда сверяй mdadm --detail (показывает 'active sync' vs 'faulty'), journalctl на событие отказа диска и smartctl -a перед запуском --fail на любом диске."
      }
    },
    {
      "id": "review-rebuild-monitoring",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": {
        "en": "Review a RAID recovery runbook for gaps",
        "ru": "Проверь runbook восстановления RAID на пробелы"
      },
      "prompt": {
        "en": "Review this RAID 5 disk replacement runbook. Identify what is missing or wrong.",
        "ru": "Проверь этот runbook замены диска RAID 5. Найди что отсутствует или неверно."
      },
      "diff": {
        "lang": "bash",
        "code": "# RAID 5 disk replacement runbook\n# Step 1: identify failed disk\ncat /proc/mdstat\n# Step 2: remove failed disk\nsudo mdadm /dev/md0 --remove /dev/sdd\n# Step 3: physically replace disk, hot-swap in new /dev/sdd\n# Step 4: add new disk\nsudo mdadm /dev/md0 --add /dev/sdd\n# Step 5: done when /proc/mdstat shows [UUUU]"
      },
      "findings": [
        {
          "id": "missing-fail-before-remove",
          "planted": true,
          "label": {
            "en": "Step 2 runs --remove without --fail first — mdadm will error if disk is still in 'faulty' state, not 'removed'",
            "ru": "Шаг 2 запускает --remove без предварительного --fail — mdadm выдаст ошибку если диск всё ещё в состоянии 'faulty', а не 'removed'"
          },
          "severity": "bug",
          "explanation": {
            "en": "mdadm requires a disk to be in 'faulty' state before --remove will work. If the kernel auto-failed the disk it may already show as 'removed', but if it shows as 'faulty' you must run '--fail' first. The runbook skips this step. Add: sudo mdadm /dev/md0 --fail /dev/sdd (if it shows as faulty, not removed). Also: the runbook should verify the disk IS actually the failed one via mdadm --detail before running --remove.",
            "ru": "mdadm требует чтобы диск был в состоянии 'faulty' прежде чем --remove сработает. Если ядро авто-упало диск, он может уже показываться как 'removed', но если как 'faulty' нужно сначала запустить '--fail'. Runbook пропускает этот шаг. Добавить: sudo mdadm /dev/md0 --fail /dev/sdd (если показывается как faulty, не removed). Также: runbook должен проверить что диск действительно упавший через mdadm --detail перед --remove."
          }
        },
        {
          "id": "missing-config-update",
          "planted": true,
          "label": {
            "en": "No mdadm.conf update or update-initramfs after rebuild — next reboot may not reassemble the array",
            "ru": "Нет обновления mdadm.conf или update-initramfs после перестройки — следующая перезагрузка может не собрать массив"
          },
          "severity": "bug",
          "explanation": {
            "en": "After a disk replacement, /etc/mdadm/mdadm.conf needs to be updated to reflect the new disk, and update-initramfs -u must be run so the initramfs includes the new configuration. Without this, a reboot may fail to auto-assemble the array. Add after rebuild completes: sudo mdadm --detail --scan | sudo tee /etc/mdadm/mdadm.conf && sudo update-initramfs -u",
            "ru": "После замены диска /etc/mdadm/mdadm.conf нужно обновить для отражения нового диска, и нужно запустить update-initramfs -u чтобы initramfs включал новую конфигурацию. Без этого перезагрузка может не собрать массив автоматически. Добавить после завершения перестройки: sudo mdadm --detail --scan | sudo tee /etc/mdadm/mdadm.conf && sudo update-initramfs -u"
          }
        },
        {
          "id": "missing-parity-check",
          "planted": true,
          "label": {
            "en": "No post-rebuild parity check — mismatch_cnt is never verified after rebuild completes",
            "ru": "Нет проверки чётности после перестройки — mismatch_cnt никогда не проверяется после завершения перестройки"
          },
          "severity": "bug",
          "explanation": {
            "en": "After rebuild completes, best practice is to run a parity check to verify the rebuilt data is consistent: echo check | sudo tee /sys/block/md0/md/sync_action. When complete, check: cat /sys/block/md0/md/mismatch_cnt — should be 0. A non-zero value indicates parity inconsistencies that may signal data integrity issues from the rebuild. The runbook declares done at [UUUU] but that only confirms the rebuild finished, not that the data is consistent.",
            "ru": "После завершения перестройки лучшая практика — запустить проверку чётности для верификации согласованности перестроенных данных: echo check | sudo tee /sys/block/md0/md/sync_action. После завершения проверить: cat /sys/block/md0/md/mismatch_cnt — должно быть 0. Ненулевое значение указывает на несоответствия чётности, которые могут сигнализировать о проблемах целостности данных после перестройки."
          }
        }
      ]
    },
    {
      "id": "incident-raid1-disk-replace",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 15,
      "title": {
        "en": "Incident: replace a failed disk in a degraded RAID 1 array",
        "ru": "Инцидент: замена упавшего диска в деградированном массиве RAID 1"
      },
      "prompt": {
        "en": "Walk through the complete disk replacement procedure for a degraded RAID 1 array on a production server.",
        "ru": "Пройди через полную процедуру замены диска для деградированного массива RAID 1 на production-сервере."
      },
      "steps": [
        {
          "label": {
            "en": "Confirm the failure and identify the failed disk",
            "ru": "Подтверди отказ и определи упавший диск"
          },
          "prompt": {
            "en": "Monitoring fired: md1 degraded. What commands do you run to confirm the failure and identify exactly which disk failed?",
            "ru": "Мониторинг сработал: md1 деградирован. Какие команды запускаешь для подтверждения отказа и определения точно какой диск упал?"
          },
          "reveal": {
            "en": "# Primary: /proc/mdstat for quick overview\ncat /proc/mdstat\n# Look for: [2/1] [_U] or [U_] — the _ position is the failed disk\n# Example: md1 : active raid1 sdc[1]  [2/1] [_U]\n# sdb (position 0) is missing\n\n# Detail view: confirms state explicitly\nsudo mdadm --detail /dev/md1\n# State : clean, degraded\n# Number   Major   Minor   RaidDevice State\n#    0       0       0        0      removed  <-- this is the failed disk slot\n#    1       8      32        1      active sync /dev/sdc\n\n# Cross-reference with kernel logs\nsudo journalctl -b | grep -i 'sdb\\|md1\\|I/O error\\|disk failure' | tail -20\n# Should show the exact error event that triggered the failure\n\n# Confirm with SMART data (if disk is still visible)\nsudo smartctl -a /dev/sdb 2>/dev/null | grep -E 'Reallocated|Reported|Current_Pending|Offline_Uncorrectable'\n# High values here confirm the disk is genuinely failed",
            "ru": "# Основное: /proc/mdstat для быстрого обзора\ncat /proc/mdstat\n# Ищи: [2/1] [_U] или [U_] — позиция _ это упавший диск\n# Пример: md1 : active raid1 sdc[1]  [2/1] [_U]\n# sdb (позиция 0) отсутствует\n\n# Детальный вид: явно подтверждает состояние\nsudo mdadm --detail /dev/md1\n# State : clean, degraded\n# Number   Major   Minor   RaidDevice State\n#    0       0       0        0      removed  <-- это слот упавшего диска\n#    1       8      32        1      active sync /dev/sdc\n\n# Перекрёстная проверка с логами ядра\nsudo journalctl -b | grep -i 'sdb\\|md1\\|I/O error\\|disk failure' | tail -20\n# Должно показать точное событие ошибки вызвавшее отказ\n\n# Подтверждение данными SMART (если диск ещё виден)\nsudo smartctl -a /dev/sdb 2>/dev/null | grep -E 'Reallocated|Reported|Current_Pending|Offline_Uncorrectable'\n# Высокие значения здесь подтверждают что диск действительно неисправен"
          }
        },
        {
          "label": {
            "en": "Remove the failed disk from the array",
            "ru": "Удали упавший диск из массива"
          },
          "prompt": {
            "en": "mdadm --detail shows sdb as 'removed'. The physical disk is being hot-swapped. What commands clean up the failed slot?",
            "ru": "mdadm --detail показывает sdb как 'removed'. Физический диск заменяется hot-swap. Какие команды очищают упавший слот?"
          },
          "reveal": {
            "en": "# If the disk shows as 'removed' (kernel already failed it): nothing to do for --fail\n# If it still shows as 'faulty': explicitly fail it first\nsudo mdadm /dev/md1 --fail /dev/sdb    # only if state is 'faulty', not 'removed'\n\n# Remove from array (cleans the slot)\nsudo mdadm /dev/md1 --remove /dev/sdb\n# mdadm: hot removed /dev/sdb from /dev/md1\n\n# Verify the slot is clean\nsudo mdadm --detail /dev/md1 | grep -E 'State|Devices'\n# State : clean, degraded\n# Active Devices : 1\n# Failed Devices : 0   <-- cleaned up\n# Removed Devices : 1\n\n# Now the hot-swap can proceed safely (or if physical already removed, proceed to add)",
            "ru": "# Если диск показывается как 'removed' (ядро уже упало его): ничего делать для --fail не нужно\n# Если всё ещё показывается как 'faulty': явно упасть его сначала\nsudo mdadm /dev/md1 --fail /dev/sdb    # только если состояние 'faulty', не 'removed'\n\n# Удалить из массива (очищает слот)\nsudo mdadm /dev/md1 --remove /dev/sdb\n# mdadm: hot removed /dev/sdb from /dev/md1\n\n# Проверить что слот чист\nsudo mdadm --detail /dev/md1 | grep -E 'State|Devices'\n# State : clean, degraded\n# Active Devices : 1\n# Failed Devices : 0   <-- очищено\n# Removed Devices : 1\n\n# Теперь hot-swap можно выполнять безопасно (или если физически уже удалён, переходим к add)"
          }
        },
        {
          "label": {
            "en": "Add the replacement disk and start rebuild",
            "ru": "Добавь диск замены и запусти перестройку"
          },
          "prompt": {
            "en": "The new disk /dev/sdd is inserted and visible to the OS. What do you run, and how do you confirm the rebuild started?",
            "ru": "Новый диск /dev/sdd вставлен и виден ОС. Что запускаешь и как убеждаешься что перестройка началась?"
          },
          "reveal": {
            "en": "# Confirm OS sees the new disk and it is the right size\nlsblk /dev/sdd\n# sdd  8:48  0  500G  0 disk   (must be >= size of failed disk)\n\n# Wipe any old superblock to prevent mdadm confusion\nsudo mdadm --zero-superblock /dev/sdd\n\n# Add to array — rebuild starts automatically\nsudo mdadm /dev/md1 --add /dev/sdd\n# mdadm: added /dev/sdd\n\n# Confirm rebuild started\ncat /proc/mdstat\n# md1 : active raid1 sdd[2] sdc[1]\n#       488386584 blocks super 1.2 [2/1] [_U]\n#       [======>...............]  recovery = 31.4% (...)\n#       finish=28.3min speed=112000K/sec\n# The recovery line confirms rebuild is running\n\n# Monitor during rebuild — watch for errors on sdc (the surviving disk)\nwatch -n30 'cat /proc/mdstat && echo --- && sudo journalctl -n5 | grep -i error'",
            "ru": "# Убедиться что ОС видит новый диск и он правильного размера\nlsblk /dev/sdd\n# sdd  8:48  0  500G  0 disk   (должен быть >= размера упавшего диска)\n\n# Обнулить старый суперблок во избежание путаницы mdadm\nsudo mdadm --zero-superblock /dev/sdd\n\n# Добавить в массив — перестройка начинается автоматически\nsudo mdadm /dev/md1 --add /dev/sdd\n# mdadm: added /dev/sdd\n\n# Подтвердить что перестройка началась\ncat /proc/mdstat\n# md1 : active raid1 sdd[2] sdc[1]\n#       488386584 blocks super 1.2 [2/1] [_U]\n#       [======>...............]  recovery = 31.4% (...)\n#       finish=28.3min speed=112000K/sec\n# Строка recovery подтверждает что перестройка идёт\n\n# Мониторим во время перестройки — следим за ошибками на sdc (выживший диск)\nwatch -n30 'cat /proc/mdstat && echo --- && sudo journalctl -n5 | grep -i error'"
          }
        },
        {
          "label": {
            "en": "Verify rebuild complete and update configuration",
            "ru": "Убедись в завершении перестройки и обнови конфигурацию"
          },
          "prompt": {
            "en": "/proc/mdstat now shows [2/2] [UU]. What are the remaining steps to close the incident?",
            "ru": "/proc/mdstat теперь показывает [2/2] [UU]. Каковы оставшиеся шаги для закрытия инцидента?"
          },
          "reveal": {
            "en": "# 1. Verify full health\nsudo mdadm --detail /dev/md1\n# State : clean              <-- no longer 'degraded'\n# Active Devices : 2\n# Failed Devices : 0\n# Number   Major   Minor   RaidDevice State\n#    2       8      48        0      active sync /dev/sdd  <-- new disk\n#    1       8      32        1      active sync /dev/sdc\n\n# 2. Update mdadm.conf to include new disk\nsudo mdadm --detail --scan | sudo tee /etc/mdadm/mdadm.conf\n\n# 3. Regenerate initramfs (CRITICAL on Debian/Ubuntu)\nsudo update-initramfs -u\n\n# 4. Run a parity check to verify data integrity\necho check | sudo tee /sys/block/md1/md/sync_action\n# Wait for it to complete (watch /proc/mdstat)\n# Then check:\ncat /sys/block/md1/md/mismatch_cnt\n# Should be 0\n\n# 5. Incident closed. Document:\n# - Which disk failed (serial number from smartctl)\n# - When it was replaced\n# - Rebuild duration\n# - mismatch_cnt result",
            "ru": "# 1. Проверить полное здоровье\nsudo mdadm --detail /dev/md1\n# State : clean              <-- больше нет 'degraded'\n# Active Devices : 2\n# Failed Devices : 0\n# Number   Major   Minor   RaidDevice State\n#    2       8      48        0      active sync /dev/sdd  <-- новый диск\n#    1       8      32        1      active sync /dev/sdc\n\n# 2. Обновить mdadm.conf для включения нового диска\nsudo mdadm --detail --scan | sudo tee /etc/mdadm/mdadm.conf\n\n# 3. Перегенерировать initramfs (КРИТИЧНО на Debian/Ubuntu)\nsudo update-initramfs -u\n\n# 4. Запустить проверку чётности для верификации целостности данных\necho check | sudo tee /sys/block/md1/md/sync_action\n# Ждём завершения (наблюдаем /proc/mdstat)\n# Затем проверяем:\ncat /sys/block/md1/md/mismatch_cnt\n# Должно быть 0\n\n# 5. Инцидент закрыт. Документируем:\n# - Какой диск упал (серийный номер из smartctl)\n# - Когда был заменён\n# - Длительность перестройки\n# - Результат mismatch_cnt"
          }
        }
      ]
    }
  ]
}
