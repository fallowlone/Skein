{
  "lessonKey": "python/08-performance/04-when-python-is-wrong",
  "track": "python",
  "tasks": [
    {
      "id": "workload-triage-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Predict: three services, three verdicts",
        "ru": "Предскажите: три сервиса, три вердикта"
      },
      "prompt": {
        "en": "For each marked service, predict the verdict from the decision tree: is Python the problem, and what is the prescribed move? One verdict must include the arithmetic.",
        "ru": "Для каждого помеченного сервиса предскажите вердикт по дереву решений: проблема ли Python и какой ход предписан? Один вердикт обязан включать арифметику."
      },
      "scenario": {
        "en": "# service A — API gateway, 6 pods\n#   py-spy: 94% of samples in socket.recv / await; CPU ~12%\n#   p95 = 80 ms, SLO 150 ms, team unhappy that 'Python is slow'\n\n# service B — nightly ETL, 1 fat node\n#   py-spy --native: 91% inside pandas.groupby / pyarrow parquet writes\n#   runtime 49 min, window 60 min\n\n# service C — fraud-scoring endpoint\n#   py-spy: 88% in pure-Python loop over dict-of-lists features\n#   320 sustained cores across the fleet; $11k/month compute\n#   vCPU-year ~$400; a Go port is estimated at 3 engineer-months",
        "ru": "# Триаж трёх сервисов по дереву решений урока\n# сервис A — API-гейтвей, 6 подов\n#   py-spy: 94% сэмплов в socket.recv / await; CPU ~12%\n#   p95 = 80 мс, SLO 150 мс, команда недовольна, что «Python медленный»\n\n# сервис B — ночной ETL, 1 толстый узел\n#   py-spy --native: 91% внутри pandas.groupby / записи parquet через pyarrow\n#   время 49 мин, окно 60 мин\n\n# сервис C — эндпоинт фрод-скоринга\n#   py-spy: 88% в чисто-питоновском цикле по фичам dict-of-lists\n#   320 постоянных ядер по флоту; $11k/месяц вычислений\n#   vCPU-год ~$400; Go-порт оценён в 3 инженеро-месяца"
      },
      "reveal": {
        "en": "Service A: I/O-bound — 94% socket waits with idle CPU means the cores wait on the network in any language; verdict: stay Python, and since p95 already beats the SLO, the move is nothing (maybe pool tuning). A Go rewrite would buy syntax. Service B: C-library-bound — the time is inside pandas/pyarrow compiled kernels; Python is a thin orchestrator. Verdict: stay Python; if the 60-minute window tightens, the levers are batching, dtype hygiene, and partitioning — rewriting the glue gains ~nothing per core. Service C: interpreter-bound at scale — the honest case. Arithmetic: ~10x waste on 320 cores is ~290 wasted cores x $400 = ~$116k/yr; a 3 engineer-month strangler port (~$40-75k loaded) pays back well under a year and keeps paying. Verdict: climb lesson 3's ladder first (vectorize the feature loop); if it stays the bill, extract that endpoint behind a contract — and only that endpoint.",
        "ru": "Сервис A: I/O-bound — 94% ожиданий сокетов при простаивающем CPU значит, что ядра ждут сеть на любом языке; вердикт: остаться на Python, и поскольку p95 уже лучше SLO, ход — ничего (разве что настройка пулов). Go-переписывание купило бы синтаксис. Сервис B: C-library-bound — время внутри скомпилированных ядер pandas/pyarrow; Python — тонкий оркестратор. Вердикт: остаться на Python; если 60-минутное окно сожмётся, рычаги — батчинг, гигиена dtype, партиционирование: переписывание клея даёт ~ничего на ядро. Сервис C: interpreter-bound в масштабе — честный случай. Арифметика: ~10x растраты на 320 ядрах — это ~290 потраченных ядер x $400 = ~$116k/год; стренглер-порт за 3 инженеро-месяца (~$40-75k полной стоимости) окупается сильно меньше чем за год и платит дальше. Вердикт: сначала лестница урока 3 (векторизовать цикл фич); если путь остаётся счётом — вынести этот эндпоинт за контракт, и только его."
      }
    },
    {
      "id": "rewrite-rfc-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: the fleet-wide Go migration RFC",
        "ru": "Диагностируйте: RFC о миграции всего флота на Go"
      },
      "prompt": {
        "en": "An RFC proposes migrating all 31 services to Go, citing 'up to 30x efficiency'. Read the fleet evidence and find the three reasons the RFC's scope is wrong — and what the correct scope is.",
        "ru": "RFC предлагает мигрировать все 31 сервис на Go, ссылаясь на «до 30x эффективности». Прочитайте улики по флоту и найдите три причины, по которым масштаб RFC ошибочен, — и каков правильный масштаб."
      },
      "scenario": {
        "en": "# RFC excerpt:\n# 'Python is 10-50x slower than Go per core. Migrating our 31 services\n#  will cut compute spend by up to 30x. Estimated effort: 6 quarters.'\n#\n# fleet today: 280 vCPUs total across 31 Python services\n# proposal treats every service as equally migration-worthy",
        "ru": "# Выдержка из RFC и текущая картина флота\n# «Python в 10-50 раз медленнее Go на ядро. Миграция наших 31 сервиса\n#  сократит расходы на вычисления до 30 раз. Оценка: 6 кварталов.»\n#\n# флот сегодня: 280 vCPU суммарно на 31 Python-сервис\n# предложение считает все сервисы одинаково достойными миграции"
      },
      "evidence": {
        "en": "# core usage breakdown (monitoring, 30-day average):\n#   scorer-svc:        238 vCPU   py-spy: 86% pure-Python frames\n#   31 other services:  42 vCPU combined\n#     - 24 services:    py-spy >85% socket/DB waits (CPU mostly idle)\n#     - 6 services:     py-spy --native: time inside numpy/pyarrow\n#     - 1 service:      mixed, 3 vCPU total\n# vCPU-year ~$400 -> total possible saving outside scorer-svc:\n#   42 cores x $400 = $16.8k/yr even if Go made them FREE\n# 6 quarters of a 5-person team, loaded: ~$1.5-2.5M",
        "ru": "# раскладка ядер (мониторинг, среднее за 30 дней):\n#   scorer-svc:        238 vCPU   py-spy: 86% чисто-питоновских фреймов\n#   остальные 31 - 1:   42 vCPU суммарно\n#     - 24 сервиса:     py-spy >85% ожиданий сокетов/БД (CPU простаивает)\n#     - 6 сервисов:     py-spy --native: время внутри numpy/pyarrow\n#     - 1 сервис:       смешанный, всего 3 vCPU\n# vCPU-год ~$400 -> вся возможная экономия вне scorer-svc:\n#   42 ядра x $400 = $16.8k/год, даже если бы Go сделал их БЕСПЛАТНЫМИ\n# 6 кварталов команды из 5, полная стоимость: ~$1.5-2.5M"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "bill-concentration",
            "accept": [
              "the bill is concentrated: scorer-svc alone is 238 of 280 cores, so fleet-wide scope attacks 42 cores with 6 quarters of work",
              "one service is 85% of the compute; migrating the other thirty addresses almost nothing",
              "the RFC scope ignores that the cost lives in a single interpreter-bound service",
              "счёт сконцентрирован: один scorer-svc — 238 из 280 ядер, и масштаб «весь флот» атакует 42 ядра шестью кварталами работы",
              "один сервис — 85% вычислений; миграция остальных тридцати не решает почти ничего",
              "масштаб RFC игнорирует, что стоимость живёт в одном интерпретаторном сервисе"
            ],
            "hint": {
              "en": "Compare scorer-svc cores against everything else combined. Where does the money actually sit?",
              "ru": "Сравните ядра scorer-svc со всем остальным вместе. Где на самом деле сидят деньги?"
            }
          },
          {
            "id": "io-and-clib-gain-nothing",
            "accept": [
              "24 services are I/O-bound (idle cores wait the same in Go) and 6 are inside numpy/pyarrow C kernels — per-core 'up to 30x' does not apply to either bucket",
              "idle waiting and compiled-kernel time do not speed up from a language change",
              "the 30x figure only applies to interpreter-bound code, which 30 of 31 services are not",
              "24 сервиса I/O-bound (простаивающие ядра ждут так же и на Go) и 6 сидят в C-ядрах numpy/pyarrow — «до 30x» на ядро не применимо ни к одной из этих корзин",
              "простой на ожидании и время в скомпилированных ядрах не ускоряются сменой языка",
              "цифра 30x относится только к интерпретаторному коду, каким 30 из 31 сервисов не являются"
            ],
            "hint": {
              "en": "Apply the decision tree to the 24 + 6 services: which bucket does each fall into, and what does the per-core multiplier do there?",
              "ru": "Примените дерево решений к 24 + 6 сервисам: в какую корзину падает каждый и что там делает межъядерный множитель?"
            }
          },
          {
            "id": "strangler-correct-scope",
            "accept": [
              "correct scope: strangler-extract scorer-svc alone behind a contract — ~238 cores x 10-30x reduction repays one service-sized effort; the $16.8k/yr ceiling elsewhere never repays $1.5-2.5M",
              "extract the one hot service, keep the other thirty in Python",
              "one-service strangler port; the fleet migration costs millions to chase under $17k a year",
              "правильный масштаб: вынести стренглером один scorer-svc за контракт — ~238 ядер x сокращение 10-30x окупают усилие размером в один сервис; потолок $16.8k/год в остальном никогда не окупит $1.5-2.5M",
              "вынести один горячий сервис, остальные тридцать оставить на Python",
              "стренглер-порт одного сервиса; миграция флота тратит миллионы в погоне за неполными $17k в год"
            ],
            "hint": {
              "en": "Run salary-versus-servers on both scopes: the whole fleet versus just scorer-svc. Which clears the bar?",
              "ru": "Прогоните зарплаты-против-серверов на обоих масштабах: весь флот против одного scorer-svc. Какой проходит планку?"
            }
          }
        ]
      }
    },
    {
      "id": "strangler-extraction-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: extracting the scorer to Go without a big bang",
        "ru": "Спроектируйте: вынос скорера на Go без большого взрыва"
      },
      "prompt": {
        "en": "The 238-core scorer-svc cleared the arithmetic. Design the strangler extraction: contract, traffic migration, rollback, what stays in Python, and the success metrics that decide when the old path dies.",
        "ru": "238-ядерный scorer-svc прошёл арифметику. Спроектируйте стренглер-вынос: контракт, миграцию трафика, откат, что остаётся на Python и метрики успеха, решающие, когда умирает старый путь."
      },
      "constraints": {
        "en": "Zero downtime; the scoring API has 14 internal consumers. Results must match the Python implementation within defined tolerance (floats). The Python orchestration (feature fetching, batching, fallbacks) is NOT being rewritten. Both implementations run in parallel for some period; budget for that double-run.",
        "ru": "Ноль даунтайма; у скорингового API 14 внутренних потребителей. Результаты должны совпадать с Python-реализацией в заданном допуске (float). Python-оркестрация (загрузка фич, батчинг, фолбэки) НЕ переписывается. Обе реализации работают параллельно некоторый период; заложите цену двойного прогона."
      },
      "rubric": [
        {
          "en": "Freezes the interface as an explicit contract first (protobuf/OpenAPI with versioned schema), so 14 consumers keep one stable API regardless of which implementation answers",
          "ru": "Сначала замораживает интерфейс явным контрактом (protobuf/OpenAPI с версионированной схемой), чтобы 14 потребителей сохранили один стабильный API независимо от того, какая реализация отвечает"
        },
        {
          "en": "Stages traffic: shadow mode (Go scores everything, results compared, Python answers) before any percentage shift, then 1/5/25/50/100 with automated rollback triggers",
          "ru": "Стадирует трафик: теневой режим (Go скорит всё, результаты сравниваются, отвечает Python) до любого сдвига процентов, затем 1/5/25/50/100 с автоматическими триггерами отката"
        },
        {
          "en": "Defines numeric tolerance for float comparison (e.g. relative epsilon, agreed mismatch budget) instead of demanding bit-equality across runtimes",
          "ru": "Определяет числовой допуск сравнения float (например, относительный эпсилон и согласованный бюджет расхождений) вместо требования бит-в-бит между рантаймами"
        },
        {
          "en": "Scopes the rewrite to the scoring kernel only — feature fetching, batching, and fallback orchestration stay Python, which is exactly why the effort is service-sized, not fleet-sized",
          "ru": "Ограничивает переписывание самим скоринговым ядром — загрузка фич, батчинг и фолбэк-оркестрация остаются на Python, поэтому усилие имеет размер сервиса, а не флота"
        },
        {
          "en": "Names success and exit criteria: p99 and correctness during shadow, fleet/core reduction after cutover (≈12 pods to ≈2-class numbers), old path deleted only after a full peak cycle on 100%",
          "ru": "Называет критерии успеха и выхода: p99 и корректность в тени, сокращение флота/ядер после переключения (числа класса ≈12 подов к ≈2), старый путь удаляется только после полного пикового цикла на 100%"
        }
      ],
      "model": {
        "en": "Phase 0 — contract: extract the scoring call into a versioned protobuf service definition (score(FeatureVector) -> Scores), generate clients for the 14 consumers, and route them through it while Python still serves — the contract ships before any Go exists, so the migration becomes an implementation detail behind a stable interface. Phase 1 — shadow: deploy the Go scorer behind the same contract; the router sends every request to both, answers from Python, and logs pairwise diffs. Float tolerance is defined up front: relative epsilon (e.g. 1e-9) with an agreed mismatch budget; investigate systematic divergence (summation order, libm differences), not individual ULPs. Run shadow through at least one full peak. Cost note: the double-run temporarily increases compute — budgeted, time-boxed. Phase 2 — shift: 1% / 5% / 25% / 50% / 100%, gated on p99 and mismatch-rate dashboards, with automated rollback to Python on breach (the router flips a flag; both stay deployed). Phase 3 — decommission: only after 100% has survived a full peak cycle, delete the Python scoring kernel; the Python orchestration — feature fetching, batching, fallbacks — never moved, which is what kept this at 3 engineer-months instead of 6 quarters. Success metrics: scoring p99 (expect ~30x kernel speedup to show as fleet shrink, the 12-to-2-pods class of result), core count (238 to ~10-20), mismatch rate at zero-tolerance-breach, and — the strategic one — no other service qualifying for extraction afterward, proving the scope was right.",
        "ru": "# Стренглер: контракт -> тень -> проценты -> вывод из эксплуатации; оркестрация остаётся Python\nФаза 0 — контракт: вынести скоринговый вызов в версионированное protobuf-описание сервиса (score(FeatureVector) -> Scores), сгенерировать клиентов для 14 потребителей и завести их через него, пока отвечает ещё Python, — контракт отгружается до того, как существует какой-либо Go, и миграция становится деталью реализации за стабильным интерфейсом. Фаза 1 — тень: развернуть Go-скорер за тем же контрактом; роутер шлёт каждый запрос обоим, отвечает Python, попарные диффы логируются. Допуск по float определён заранее: относительный эпсилон (например, 1e-9) и согласованный бюджет расхождений; расследовать систематическую дивергенцию (порядок суммирования, различия libm), а не отдельные ULP. Гнать тень минимум через один полный пик. Заметка о цене: двойной прогон временно увеличивает вычисления — заложено, ограничено по времени. Фаза 2 — сдвиг: 1% / 5% / 25% / 50% / 100% с воротами по дашбордам p99 и доли расхождений и автоматическим откатом на Python при нарушении (роутер щёлкает флагом; развёрнуты оба). Фаза 3 — вывод: только после того, как 100% пережили полный пиковый цикл, удалить Python-ядро скоринга; Python-оркестрация — загрузка фич, батчинг, фолбэки — не переезжала никогда, что и удержало работу в 3 инженеро-месяцах вместо 6 кварталов. Метрики успеха: p99 скоринга (ожидаем, что ~30x ускорение ядра проявится сжатием флота — результат класса «12 подов к 2»), число ядер (238 к ~10-20), доля расхождений без пробития допуска и — стратегическая — отсутствие других сервисов, квалифицирующихся на вынос после, как доказательство правильного масштаба."
      }
    }
  ]
}
