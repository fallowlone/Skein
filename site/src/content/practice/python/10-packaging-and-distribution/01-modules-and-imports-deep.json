{
  "lessonKey": "python/10-packaging-and-distribution/01-modules-and-imports-deep",
  "track": "python",
  "tasks": [
    {
      "id": "circular-import-three-fates-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Predict: one cycle, three import styles, three fates",
        "ru": "Предскажите: один цикл, три стиля импорта, три судьбы"
      },
      "prompt": {
        "en": "Modules a and b import each other. For each of the three marked points, predict the exact outcome when you run python -m a: works, raises (which exception, at import time or call time), and why.",
        "ru": "Модули a и b импортируют друг друга. Для каждой из трёх помеченных точек предскажите точный исход при запуске python -m a: работает, падает (какое исключение, при импорте или при вызове) и почему."
      },
      "scenario": {
        "en": "# a.py\nREGION = \"eu\"\nimport b                      # cycle starts here\ndef helper():\n    return \"from a\"\n\nprint(b.use())                # point 3 — runs after both modules finish\n\n# b.py\nfrom a import REGION          # point 1\nfrom a import helper          # point 2\nimport a\n\ndef use():\n    return a.helper() + \":\" + REGION",
        "ru": "# Цикл a ↔ b: REGION связан до цикла, helper — после\n# a.py\nREGION = \"eu\"\nimport b                      # цикл начинается здесь\ndef helper():\n    return \"from a\"\n\nprint(b.use())                # точка 3 — выполняется после обоих модулей\n\n# b.py\nfrom a import REGION          # точка 1\nfrom a import helper          # точка 2\nimport a\n\ndef use():\n    return a.helper() + \":\" + REGION"
      },
      "reveal": {
        "en": "Point 1 works: when b starts executing, the half-initialized a is already in sys.modules, and REGION was bound BEFORE the import b line — the attribute exists on the partial module. Point 2 raises ImportError: cannot import name 'helper' from partially initialized module — helper is defined below import b, so at this moment it is not bound yet; from-import demands the attribute right now. Point 3 (after deleting point 2 or moving it) works: import a merely binds the module object, and by the time use() runs, a has finished executing, so a.helper() resolves at call time. The whole exercise is one fact: modules register in sys.modules before their body finishes, so cycle survival depends on WHEN a name is fetched, not on the cycle itself.",
        "ru": "Точка 1 работает: когда b начинает выполняться, полусобранный a уже лежит в sys.modules, а REGION связан ДО строки import b — атрибут на частичном модуле существует. Точка 2 поднимает ImportError: cannot import name 'helper' from partially initialized module — helper определён ниже import b и в этот момент ещё не связан; from-import требует атрибут немедленно. Точка 3 (после удаления или переноса точки 2) работает: import a лишь связывает объект модуля, и к моменту запуска use() модуль a довыполнился, так что a.helper() разрешается при вызове. Всё упражнение — один факт: модули регистрируются в sys.modules до завершения тела, поэтому выживание цикла зависит от того, КОГДА читают имя, а не от самого цикла."
      }
    },
    {
      "id": "slow-startup-importtime-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 9,
      "title": {
        "en": "Diagnose: 14-second startup, nobody wrote slow code",
        "ru": "Диагностируйте: старт 14 секунд, медленный код никто не писал"
      },
      "prompt": {
        "en": "The API service takes 14 s to reach main() and readiness probes kill it. Read the code and the importtime evidence; name the mechanism, the reason the cost lands on every worker, and the fix that removes it without touching the batch job.",
        "ru": "API-сервис идёт до main() 14 секунд, и readiness-пробы его убивают. Прочитайте код и улики importtime; назовите механизм, причину, по которой цена ложится на каждый воркер, и фикс, убирающий её, не трогая батч-джоб."
      },
      "scenario": {
        "en": "# app/__init__.py\nfrom .handlers import router          # convenience re-export\n\n# app/handlers.py\nfrom app.analytics import score       # used by ONE endpoint\n\n# app/analytics.py\nimport pandas\nimport torch                          # model inference, batch job only\n\ndef score(frame: \"pandas.DataFrame\"):\n    return _model(torch.tensor(frame.values))",
        "ru": "# Цепочка: __init__ реэкспортирует handlers → analytics → pandas+torch на уровне модуля\n# app/__init__.py\nfrom .handlers import router          # удобный реэкспорт\n\n# app/handlers.py\nfrom app.analytics import score       # нужен ОДНОМУ эндпоинту\n\n# app/analytics.py\nimport pandas\nimport torch                          # инференс модели, только батч-джоб\n\ndef score(frame: \"pandas.DataFrame\"):\n    return _model(torch.tensor(frame.values))"
      },
      "evidence": {
        "en": "$ python -X importtime -c \"import app\" 2>&1 | tail -4\nimport time: self [us] | cumulative | imported package\nimport time:      211 |    7421337 | pandas\nimport time:      188 |    5208441 | torch\nimport time:      102 |   13981245 | app\n# every gunicorn worker re-imports app on fork-exec; probes allow 10 s\n# the only caller of score() is jobs/nightly.py",
        "ru": "$ python -X importtime -c \"import app\" 2>&1 | tail -4\nimport time: self [us] | cumulative | imported package\nimport time:      211 |    7421337 | pandas\nimport time:      188 |    5208441 | torch\nimport time:      102 |   13981245 | app\n# каждый воркер gunicorn заново импортирует app при старте; пробам хватает терпения на 10 с\n# единственный вызыватель score() — jobs/nightly.py"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "module-level-import",
            "accept": [
              "pandas and torch are imported at module level so they execute at import time",
              "module-level imports run unconditionally during import",
              "the heavy libraries load when the module is imported, not when score is called",
              "pandas и torch импортируются на уровне модуля и выполняются во время импорта",
              "импорты уровня модуля выполняются безусловно при импорте",
              "тяжёлые библиотеки грузятся при импорте модуля, а не при вызове score"
            ],
            "hint": {
              "en": "When does import pandas at the top of analytics.py actually run — and is calling score() required for it?",
              "ru": "Когда на самом деле выполняется import pandas наверху analytics.py — и нужен ли для этого вызов score()?"
            }
          },
          {
            "id": "init-chain-tax",
            "accept": [
              "app/__init__.py re-exports handlers so importing app pulls the whole chain",
              "the package init executes on every import of any submodule",
              "every worker pays because import app transitively executes analytics",
              "app/__init__.py реэкспортирует handlers, поэтому import app тянет всю цепочку",
              "инициализатор пакета выполняется при каждом импорте любого сабмодуля",
              "каждый воркер платит, потому что import app транзитивно выполняет analytics"
            ],
            "hint": {
              "en": "Why does a worker that never calls the analytics endpoint still pay? Trace what import app executes first.",
              "ru": "Почему платит даже воркер, который не зовёт эндпоинт аналитики? Проследите, что import app выполняет первым."
            }
          },
          {
            "id": "lazy-fix",
            "accept": [
              "move torch and pandas imports inside score or under TYPE_CHECKING",
              "lazy import in the function plus if TYPE_CHECKING for annotations",
              "defer the heavy imports to call time",
              "перенести импорты torch и pandas внутрь score или под TYPE_CHECKING",
              "ленивый импорт в функции плюс if TYPE_CHECKING для аннотаций",
              "отложить тяжёлые импорты до момента вызова"
            ],
            "hint": {
              "en": "The annotation is already a string. Where can the imports move so only the nightly job ever executes them?",
              "ru": "Аннотация уже строковая. Куда перенести импорты, чтобы их выполнял только ночной джоб?"
            }
          }
        ]
      }
    },
    {
      "id": "import-discipline-policy-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: an import-discipline policy for a 40-module service",
        "ru": "Спроектируйте: политику импортов для сервиса на 40 модулей"
      },
      "prompt": {
        "en": "Your team inherits a service with circular imports, a 9-second cold start, two past stdlib-shadowing incidents, and scripts run as python app/jobs/foo.py. Design the import policy: package layout rules, side-effect rules, entrypoint convention, and the checks that enforce it in CI.",
        "ru": "Команда наследует сервис с циклическими импортами, холодным стартом 9 секунд, двумя прошлыми инцидентами затенения stdlib и скриптами, запускаемыми как python app/jobs/foo.py. Спроектируйте политику импортов: правила раскладки пакетов, правила сайд-эффектов, конвенцию точек входа и проверки, закрепляющие её в CI."
      },
      "constraints": {
        "en": "The batch jobs genuinely need pandas; web workers must start in under 2 s. Circular imports exist between handlers and services today and cannot all be restructured this quarter. No new runtime dependencies; checks must run in CI with stdlib tooling or existing linters.",
        "ru": "Батч-джобам pandas действительно нужен; веб-воркеры обязаны стартовать быстрее 2 секунд. Циклы между handlers и services есть уже сегодня, и реструктурировать все в этом квартале нельзя. Без новых рантайм-зависимостей; проверки должны идти в CI на stdlib-инструментах или существующих линтерах."
      },
      "rubric": [
        {
          "en": "Side-effect rule stated as the spine: module level is definitions only — no I/O, no connections; startup work moves to explicit lifespan hooks",
          "ru": "Правило сайд-эффектов — как хребет: уровень модуля только для определений, без I/O и подключений; стартовая работа уезжает в явные lifespan-хуки"
        },
        {
          "en": "Heavy deps gated: pandas/torch imported lazily inside batch-only functions, type-only imports under if TYPE_CHECKING — with the run-once sys.modules mechanism named as the reason it works",
          "ru": "Тяжёлые зависимости под шлюзом: pandas/torch импортируются лениво внутри батч-функций, импорты только-для-типов — под if TYPE_CHECKING, с названным механизмом однократности sys.modules как причиной работоспособности"
        },
        {
          "en": "Cycle triage ranked: restructure shared pieces into a third module where feasible; surviving cycles converted to import-module-not-name with attribute access at call time, never from-import",
          "ru": "Триаж циклов по ранжиру: где можно — вынести общее в третий модуль; выжившие циклы перевести на импорт-модуля-вместо-имени с доступом к атрибутам при вызове, никогда from-import"
        },
        {
          "en": "Entrypoint convention: all jobs run as python -m app.jobs.foo (package context, cwd at sys.path[0]); direct-file execution banned, with the relative-import and shadowing failure modes cited",
          "ru": "Конвенция точек входа: все джобы запускаются как python -m app.jobs.foo (пакетный контекст, cwd в sys.path[0]); прямой запуск файла запрещён, с указанием отказов — относительные импорты и затенение"
        },
        {
          "en": "CI enforcement is concrete: an import-time budget test (python -X importtime or a timed import of the web package), a no-stdlib-name lint for module filenames, and a test asserting import app performs no network/DB calls",
          "ru": "Принуждение в CI конкретно: тест бюджета времени импорта (python -X importtime или замер импорта веб-пакета), линт на совпадение имён модулей со stdlib и тест, что import app не делает сетевых/БД-вызовов"
        }
      ],
      "model": {
        "en": "Policy. 1) Module level is for definitions: any connect/open/read at import time is a review blocker; startup work lives in the app lifespan hook. 2) Heavy imports are call-site-local: pandas/torch move inside the batch functions that use them, annotations switch to TYPE_CHECKING imports — sys.modules caching makes the lazy import a one-time cost for the nightly job and a zero cost for web workers. 3) Cycles: extract shared types/config into app/core (both sides import it, cycle dies) for the worst three; the rest become import services + services.fn() at call time — from-import across the cycle is banned by lint regex until restructured. 4) Entrypoints: every job is python -m app.jobs.foo; a Makefile target wraps it; running files directly is documented as broken (no package context, script dir at sys.path[0] — both past shadowing incidents started there). 5) CI gates: (a) timed import — assert time-to-import app under 1.5 s on the CI runner, keeping the 2 s pod budget honest; (b) filename lint — no module may share a name with the stdlib list (sys.stdlib_module_names); (c) a socket-guard test that imports app with network syscalls stubbed to raise, proving no import-time I/O. The policy attacks mechanism, not symptoms: run-once import cost, registration-before-exec cycle behavior, and sys.path[0] are each named in the doc so the rules survive the next refactor.",
        "ru": "# Политика: определения на уровне модуля, ленивые тяжёлые импорты, -m, три CI-шлюза\nПолитика. 1) Уровень модуля — для определений: любой connect/open/read при импорте — блокер на ревью; стартовая работа живёт в lifespan-хуке приложения. 2) Тяжёлые импорты локальны месту вызова: pandas/torch переезжают внутрь батч-функций, аннотации — на импорты под TYPE_CHECKING; кеширование sys.modules делает ленивый импорт одноразовой ценой для ночного джоба и нулевой — для веб-воркеров. 3) Циклы: для трёх худших — вынести общие типы/конфиг в app/core (обе стороны импортируют его, цикл умирает); остальные переводятся на import services + services.fn() при вызове — from-import через цикл запрещён линт-регекспом до реструктуризации. 4) Точки входа: каждый джоб — python -m app.jobs.foo; цель в Makefile его оборачивает; прямой запуск файлов задокументирован как сломанный (нет пакетного контекста, каталог скрипта в sys.path[0] — оба прошлых инцидента затенения начались там). 5) CI-шлюзы: (а) замер импорта — время import app меньше 1,5 с на CI-раннере, чтобы бюджет пода в 2 с оставался честным; (б) линт имён файлов — ни один модуль не совпадает со stdlib-списком (sys.stdlib_module_names); (в) тест с socket-заглушкой, импортирующий app при сетевых вызовах, заглушенных на исключение, — доказательство отсутствия I/O при импорте. Политика бьёт по механизму, а не по симптомам: одноразовая цена импорта, регистрация-до-exec и sys.path[0] названы в документе поимённо, чтобы правила пережили следующий рефакторинг."
      }
    }
  ]
}
