{
  "lessonKey": "python/05-concurrency/03-asyncio-fundamentals",
  "track": "python",
  "tasks": [
    {
      "id": "predict-await-order-and-timing",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict the print order and total time of three variants",
        "ru": "Предскажи порядок печати и общее время трёх вариантов"
      },
      "prompt": {
        "en": "Each variant runs two 1-second operations. Predict total runtime and what happens to other tasks on the loop in each case, then reveal.",
        "ru": "Каждый вариант выполняет две односекундные операции. Предскажи общее время и что происходит с другими задачами цикла в каждом случае, затем открой ответ."
      },
      "scenario": {
        "en": "import asyncio, time\n\nasync def op(name):\n    await asyncio.sleep(1)\n    return name\n\nasync def variant_a():            # sequential awaits\n    ra = await op(\"a\")\n    rb = await op(\"b\")\n\nasync def variant_b():            # tasks first, await later\n    ta = asyncio.create_task(op(\"a\"))\n    tb = asyncio.create_task(op(\"b\"))\n    ra, rb = await ta, await tb\n\nasync def variant_c():            # blocking sleep by mistake\n    time.sleep(1)\n    await op(\"a\")\n\n# Total time of A? Of B? And what does C do to every other task on the loop?",
        "ru": "# Три варианта: последовательные await, задачи заранее, блокирующий sleep\nimport asyncio, time\n\nasync def op(name):\n    await asyncio.sleep(1)\n    return name\n\nasync def variant_a():            # sequential awaits\n    ra = await op(\"a\")\n    rb = await op(\"b\")\n\nasync def variant_b():            # tasks first, await later\n    ta = asyncio.create_task(op(\"a\"))\n    tb = asyncio.create_task(op(\"b\"))\n    ra, rb = await ta, await tb\n\nasync def variant_c():            # blocking sleep by mistake\n    time.sleep(1)\n    await op(\"a\")\n\n# Общее время A? B? И что C делает с каждой другой задачей цикла?"
      },
      "reveal": {
        "en": "A: ~2 s. await op(\"a\") drives the coroutine sequentially to completion before op(\"b\") even starts — calling an async def creates a frozen frame, and execution begins at the await.\n\nB: ~1 s. create_task schedules both coroutines on the next loop iteration; both asyncio.sleep timers run concurrently, and the awaits just collect results. Other tasks on the loop keep running fine in both A and B — asyncio.sleep suspends, returning control to the loop.\n\nC: ~2 s for itself, but the real damage is external: time.sleep(1) is a synchronous call that never yields, so for that full second the loop runs NO other task, fires no timer, accepts no connection. A and B are slow-vs-fast; C is an outage in miniature. The fix is await asyncio.sleep(1) — or, for real blocking calls, await asyncio.to_thread(...).",
        "ru": "A: ~2 с. await op(\"a\") последовательно доводит корутину до конца прежде, чем op(\"b\") вообще начнётся — вызов async def создаёт замороженный фрейм, исполнение начинается на await.\n\nB: ~1 с. create_task планирует обе корутины на следующую итерацию цикла; оба таймера asyncio.sleep идут конкурентно, а await лишь собирают результаты. Другие задачи цикла в A и B продолжают работать — asyncio.sleep приостанавливается, возвращая управление циклу.\n\nC: ~2 с для самой себя, но настоящий ущерб снаружи: time.sleep(1) — синхронный вызов, который не уступает, и целую секунду цикл не исполняет НИ одной другой задачи, не запускает таймеры, не принимает соединения. A против B — медленно против быстро; C — инцидент в миниатюре. Лечение — await asyncio.sleep(1), а для настоящих блокирующих вызовов — await asyncio.to_thread(...)."
      }
    },
    {
      "id": "diagnose-slow-callback-freeze",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the loop-wide p99 spike from one handler",
        "ru": "Диагностируй общий пик p99 из-за одного хендлера"
      },
      "prompt": {
        "en": "An async service shows correlated latency spikes on all endpoints. Read the debug output and fill in the mechanism precisely.",
        "ru": "Async-сервис показывает коррелированные пики латентности на всех эндпойнтах. Прочитай дебаг-вывод и заполни механизм точно."
      },
      "evidence": {
        "en": "Symptoms: p99 jumps from 40 ms to ~9 s in spikes, on every endpoint simultaneously, including /health which awaits nothing slow.\n\nWith PYTHONASYNCIODEBUG=1:\nWARNING:asyncio:Executing <Task pending name='Task-8211' coro=<rate_handler() running at handlers/fx.py:52>> took 8.74 seconds\n\nhandlers/fx.py:52 reads:\n    resp = legacy_client.get_rate(currency)   # urllib3 under the hood, timeout=10\n\nCPU during spikes: ~3% of one core. Connections pile up in the accept queue.",
        "ru": "# Симптомы и вывод PYTHONASYNCIODEBUG\nСимптомы: p99 прыгает с 40 мс до ~9 с пиками, на всех эндпойнтах одновременно, включая /health, который не await-ит ничего медленного.\n\nС PYTHONASYNCIODEBUG=1:\nWARNING:asyncio:Executing <Task pending name='Task-8211' coro=<rate_handler() running at handlers/fx.py:52>> took 8.74 seconds\n\nhandlers/fx.py:52 содержит:\n    resp = legacy_client.get_rate(currency)   # urllib3 под капотом, timeout=10\n\nCPU во время пиков: ~3% одного ядра. Соединения копятся в accept-очереди."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "blocking call",
              "synchronous call",
              "blocking i/o",
              "synchronous http call",
              "блокирующий вызов",
              "синхронный вызов",
              "синхронный http-вызов"
            ],
            "hint": {
              "en": "Line 52 performs a ___ inside a coroutine — it never reaches an await, so control never returns to the loop.",
              "ru": "Строка 52 делает ___ внутри корутины — он не доходит до await, и управление не возвращается циклу."
            }
          },
          {
            "id": "b2",
            "accept": [
              "event loop",
              "the event loop thread",
              "loop callback step",
              "цикл событий",
              "поток цикла событий",
              "шаг колбэков цикла"
            ],
            "hint": {
              "en": "Low CPU plus everything stalled at once means the ___ is held hostage — it cannot poll the selector or run any ready task while the call blocks.",
              "ru": "Низкий CPU плюс одновременный стоп всего означает, что в заложниках ___ — он не может опросить селектор и исполнить ни одну готовую задачу, пока вызов блокирует."
            }
          },
          {
            "id": "b3",
            "accept": [
              "asyncio.to_thread",
              "to_thread",
              "run_in_executor",
              "loop.run_in_executor",
              "перенести в to_thread",
              "делегировать в run_in_executor"
            ],
            "hint": {
              "en": "Until the client is replaced with an async one, the minimal fix is wrapping the call: resp = await ___(legacy_client.get_rate, currency).",
              "ru": "Пока клиент не заменён на асинхронный, минимальное лечение — обернуть вызов: resp = await ___(legacy_client.get_rate, currency)."
            }
          }
        ]
      }
    },
    {
      "id": "design-async-scraper-with-cpu-step",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design an async fetch-and-parse pipeline that never starves the loop",
        "ru": "Спроектируй async-пайплайн fetch-and-parse, не морящий цикл голодом"
      },
      "prompt": {
        "en": "Lay out the coroutine/task structure, the concurrency limit, and where each kind of work executes. Justify every placement with the single-switch-point rule. Self-grade against the rubric.",
        "ru": "Разложи структуру корутин/задач, лимит конкурентности и где исполняется каждый вид работы. Обоснуй каждое размещение правилом единственной точки переключения. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Job: fetch 2,000 product pages from one API (rate limit: at most 50 in flight), parse each response with a pure-Python HTML parser (~150 ms CPU per page), and bulk-insert results to the DB with an async driver. The service shares its process with other latency-sensitive endpoints; their p99 must stay under 100 ms while the job runs. CPython 3.12, single event loop.",
        "ru": "Задача: забрать 2 000 страниц товаров из одного API (rate limit: не больше 50 в полёте), распарсить каждый ответ чистопайтоновским HTML-парсером (~150 мс CPU на страницу) и bulk-insert результатов в БД async-драйвером. Сервис делит процесс с другими латентно-чувствительными эндпойнтами; их p99 должен оставаться ниже 100 мс, пока идёт задача. CPython 3.12, один цикл событий."
      },
      "rubric": [
        {
          "en": "Fetches run as tasks bounded by asyncio.Semaphore(50) (or a worker-pool of 50 tasks over a queue) — not 2,000 simultaneous tasks and not sequential awaits.",
          "ru": "Загрузки идут задачами, ограниченными asyncio.Semaphore(50) (или пулом из 50 задач над очередью) — не 2 000 одновременных задач и не последовательные await."
        },
        {
          "en": "The 150 ms pure-Python parse is identified as the loop-killer: at 100 ms+ per callback it alone blows the 100 ms p99 budget, so it must NOT run inline in the coroutine.",
          "ru": "150-мс чистопайтоновский парсинг опознан как убийца цикла: при 100+ мс на колбэк он один срывает бюджет p99 в 100 мс, поэтому ему НЕЛЬЗЯ исполняться инлайн в корутине."
        },
        {
          "en": "Parsing is delegated via run_in_executor to a ProcessPoolExecutor — with the GIL named as the reason a thread pool would not protect latency for CPU-bound bytecode (threads still contend with the loop for the GIL).",
          "ru": "Парсинг делегирован через run_in_executor в ProcessPoolExecutor — с GIL, названным причиной, по которой пул потоков не защитил бы латентность для CPU-bound байткода (потоки всё равно конкурируют с циклом за GIL)."
        },
        {
          "en": "DB writes are batched (e.g. accumulate N parsed results, one bulk insert) through the async driver with awaits, keeping each loop callback short.",
          "ru": "Записи в БД батчуются (например, копим N распарсенных результатов — один bulk insert) через async-драйвер с await, чтобы каждый колбэк цикла оставался коротким."
        },
        {
          "en": "Mentions verification: run with loop.set_debug(True)/PYTHONASYNCIODEBUG during rollout and assert no slow-callback warnings above 100 ms while the job runs.",
          "ru": "Упомянута проверка: на выкатке гонять с loop.set_debug(True)/PYTHONASYNCIODEBUG и убеждаться, что нет предупреждений о медленных колбэках выше 100 мс во время задачи."
        }
      ],
      "model": {
        "en": "Structure: a queue of 2,000 URLs; 50 fetch-worker tasks (or a Semaphore(50) around fetch) await the HTTP client — 50 in flight satisfies the rate limit and keeps each callback tiny, since awaiting the socket suspends. Each fetched body goes to parsing via await loop.run_in_executor(cpu_pool, parse_html, body) where cpu_pool is a ProcessPoolExecutor(max_workers≈cores-1): the parse is 150 ms of pure bytecode, so inline it would freeze every endpoint for 150 ms per page (3,000 pages = minutes of cumulative stall), and a thread executor would still contend for the GIL with the loop's own bytecode — processes isolate it completely at the cost of pickling ~100 KB of HTML per page, which is microseconds against 150 ms. Parsed results accumulate in batches of ~100 and one coroutine awaits the async driver's bulk insert. Every long operation in the pipeline is therefore behind an await: sockets (native), CPU (executor future), DB (native). Verify the budget empirically: PYTHONASYNCIODEBUG=1 in staging, alert on any 'Executing took' warning over 0.1 s while the job runs.",
        "ru": "Структура: очередь из 2 000 URL; 50 задач-фетчеров (или Semaphore(50) вокруг fetch) await-ят HTTP-клиент — 50 в полёте удовлетворяет rate limit и держит каждый колбэк крошечным, потому что await сокета приостанавливает. Каждое скачанное тело уходит в парсинг через await loop.run_in_executor(cpu_pool, parse_html, body), где cpu_pool — ProcessPoolExecutor(max_workers≈ядра-1): парсинг — 150 мс чистого байткода, инлайн он морозил бы каждый эндпойнт на 150 мс на страницу (3 000 страниц = минуты суммарного простоя), а поточный экзекьютор всё равно конкурировал бы за GIL с байткодом самого цикла — процессы изолируют его полностью ценой pickle ~100 КБ HTML на страницу, что микросекунды против 150 мс. Распарсенные результаты копятся пачками по ~100, и одна корутина await-ит bulk insert async-драйвера. Каждая долгая операция пайплайна, таким образом, спрятана за await: сокеты (нативно), CPU (future экзекьютора), БД (нативно). Бюджет проверяется эмпирически: PYTHONASYNCIODEBUG=1 в стейджинге и алерт на любое предупреждение 'Executing took' свыше 0,1 с во время задачи."
      }
    }
  ]
}
