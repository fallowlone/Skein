{
  "lessonKey": "python/05-concurrency/01-gil-honestly",
  "track": "python",
  "tasks": [
    {
      "id": "predict-threads-cpu-vs-c-ext",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict which workload speeds up with 4 threads",
        "ru": "Предскажи, какая нагрузка ускорится на 4 потоках"
      },
      "prompt": {
        "en": "Both benchmarks run the same work serially, then split across 4 threads on a 4-core CPython 3.12. Predict roughly how each 4-thread time compares to its serial time, then reveal.",
        "ru": "Оба бенчмарка выполняют одну и ту же работу последовательно, затем делят её на 4 потока на 4-ядерном CPython 3.12. Предскажи примерно, как каждое 4-поточное время соотносится с последовательным, затем открой ответ."
      },
      "scenario": {
        "en": "import threading, time, hashlib\n\ndef bench_a():                      # pure-Python loop, total 40M iterations\n    s = 0\n    for i in range(10_000_000):\n        s += i\n\ndef bench_b():                      # sha256 over 200 MB per thread\n    hashlib.sha256(b\"x\" * 200_000_000).hexdigest()\n\n# Serial: run the function 4 times in a row.\n# Threaded: run 4 threads, each calling the function once.\n# Question: for A and for B, is threaded ~4x faster, ~equal, or slower?",
        "ru": "# Бенчмарк: чистопайтоновский цикл против sha256 — что выиграет от 4 потоков?\nimport threading, time, hashlib\n\ndef bench_a():                      # pure-Python loop, total 40M iterations\n    s = 0\n    for i in range(10_000_000):\n        s += i\n\ndef bench_b():                      # sha256 over 200 MB per thread\n    hashlib.sha256(b\"x\" * 200_000_000).hexdigest()\n\n# Serial: run the function 4 times in a row.\n# Threaded: run 4 threads, each calling the function once.\n# Question: for A and for B, is threaded ~4x faster, ~equal, or slower?"
      },
      "reveal": {
        "en": "A (pure-Python loop): threaded is roughly EQUAL to serial — often a few percent SLOWER. The loop is bytecode-bound, so the GIL serializes all 4 threads onto one core, and the ~5 ms switch dance plus convoy effects add overhead on top.\n\nB (sha256 over a large buffer): threaded is close to 4x FASTER. hashlib releases the GIL while hashing buffers over 2 KB, so the four hashes run truly in parallel in C; only the tiny Python wrapper code competes for the lock.\n\nThe rule: the GIL gates bytecode execution. Work that spends its time in GIL-releasing C code (or blocked in the kernel) scales across threads; work that spends its time in the eval loop does not.",
        "ru": "A (чистопайтоновский цикл): с потоками время примерно РАВНО последовательному — часто на несколько процентов ХУЖЕ. Цикл упирается в байткод, GIL сериализует все 4 потока на одно ядро, а пляска с переключением каждые ~5 мс и эффект конвоя добавляют накладных сверху.\n\nB (sha256 по большому буферу): с потоками — почти в 4 раза БЫСТРЕЕ. hashlib отпускает GIL при хешировании буферов больше 2 КБ, поэтому четыре хеша считаются по-настоящему параллельно в C; за замок конкурирует лишь крошечная питоновская обёртка.\n\nПравило: GIL стережёт исполнение байткода. Работа, проводящая время в C-коде, отпускающем GIL (или заблокированной в ядре), масштабируется по потокам; работа в eval-цикле — нет."
      }
    },
    {
      "id": "diagnose-313t-no-scaling",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose why free-threaded 3.13t still scales like one core",
        "ru": "Диагностируй, почему free-threaded 3.13t всё равно масштабируется как одно ядро"
      },
      "prompt": {
        "en": "A team moved a CPU-bound service to the free-threaded build expecting linear scaling. Read the evidence and fill in the blanks with the precise mechanism.",
        "ru": "Команда перевела CPU-bound сервис на free-threaded-сборку, ожидая линейного масштабирования. Прочитай улики и заполни пропуски точным механизмом."
      },
      "evidence": {
        "en": "$ python3.13t app.py\n<frozen importlib._bootstrap>: RuntimeWarning: The global interpreter lock (GIL) has been enabled to load module 'fastproto', which has not declared that it can run safely without the GIL.\n\n$ python3.13t -c \"import fastproto, sys; print(sys._is_gil_enabled())\"\nTrue\n\nhtop during load: 16 threads, combined CPU ~105% of one core.\nBenchmark: 16 threads, throughput 1.02x of single-threaded.",
        "ru": "# Улики: предупреждение при старте и проверка состояния GIL\n$ python3.13t app.py\n<frozen importlib._bootstrap>: RuntimeWarning: The global interpreter lock (GIL) has been enabled to load module 'fastproto', which has not declared that it can run safely without the GIL.\n\n$ python3.13t -c \"import fastproto, sys; print(sys._is_gil_enabled())\"\nTrue\n\nhtop под нагрузкой: 16 потоков, суммарный CPU ~105% одного ядра.\nБенчмарк: 16 потоков, throughput 1.02x от однопоточного."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "re-enabled the gil",
              "gil was re-enabled",
              "enabled the gil at runtime",
              "gil re-enabled at runtime",
              "включил gil обратно",
              "gil включён обратно",
              "включение gil в рантайме"
            ],
            "hint": {
              "en": "Importing fastproto did not crash — instead the interpreter ___ as a compatibility fallback.",
              "ru": "Импорт fastproto не упал — вместо этого интерпретатор ___ как совместимостный откат."
            }
          },
          {
            "id": "b2",
            "accept": [
              "py_mod_gil",
              "py_mod_gil slot",
              "free-threading support declaration",
              "has not declared free-threading support",
              "слот py_mod_gil",
              "декларация поддержки free-threading",
              "не задекларировало поддержку free-threading"
            ],
            "hint": {
              "en": "The extension lacks the ___ that tells CPython it is safe without the GIL.",
              "ru": "У расширения нет ___, сообщающего CPython, что оно безопасно без GIL."
            }
          },
          {
            "id": "b3",
            "accept": [
              "python_gil=0",
              "python_gil",
              "pythongil=0",
              "запуск с python_gil=0",
              "переменная python_gil=0"
            ],
            "hint": {
              "en": "To make incompatible extensions fail loudly instead of silently restoring the lock, run with the environment variable ___.",
              "ru": "Чтобы несовместимые расширения падали громко, а не тихо возвращали замок, запускай с переменной окружения ___."
            }
          }
        ]
      }
    },
    {
      "id": "design-mixed-pipeline-3-12-vs-3-13t",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design the concurrency plan for a mixed pipeline, 3.12 vs 3.13t",
        "ru": "Спроектируй план конкурентности для смешанного пайплайна: 3.12 против 3.13t"
      },
      "prompt": {
        "en": "Decide, stage by stage, what runs in threads, what cannot benefit from them, and how the answer changes on the free-threaded build. Justify every choice with the GIL mechanism. Self-grade against the rubric.",
        "ru": "Реши по стадиям: что идёт в потоки, чему они не помогут и как ответ меняется на free-threaded-сборке. Обоснуй каждый выбор механизмом GIL. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Pipeline per batch: (1) download 500 JSON files over HTTPS; (2) parse and validate them in pure Python (~30 s of bytecode on one core); (3) compute embeddings via a NumPy-heavy C extension that releases the GIL; (4) write results to Postgres. Deployment A is stock CPython 3.12; deployment B is python3.13t where every extension supports free-threading. You may use threads and the stdlib only — no process pools in this exercise (that is the next lesson's tool).",
        "ru": "Пайплайн на батч: (1) скачать 500 JSON-файлов по HTTPS; (2) распарсить и провалидировать их на чистом Python (~30 с байткода на одном ядре); (3) посчитать эмбеддинги через NumPy-тяжёлое C-расширение, отпускающее GIL; (4) записать результаты в Postgres. Деплой A — обычный CPython 3.12; деплой B — python3.13t, где все расширения поддерживают free-threading. Можно использовать только потоки и stdlib — без процессных пулов в этом упражнении (это инструмент следующего урока)."
      },
      "rubric": [
        {
          "en": "Stage 1 and 4 (downloads, DB writes) go to a thread pool on BOTH builds, justified by blocking I/O releasing the GIL — threads overlap kernel waits regardless of free-threading.",
          "ru": "Стадии 1 и 4 (загрузки, запись в БД) идут в пул потоков на ОБЕИХ сборках с обоснованием: блокирующий I/O отпускает GIL — потоки перекрывают ожидания в ядре независимо от free-threading."
        },
        {
          "en": "Stage 3 (GIL-releasing NumPy extension) is identified as thread-scalable even on 3.12, because the C code drops the GIL during compute.",
          "ru": "Стадия 3 (NumPy-расширение, отпускающее GIL) опознана как масштабируемая потоками даже на 3.12, потому что C-код сбрасывает GIL на время счёта."
        },
        {
          "en": "Stage 2 (pure-Python parsing) is named as the stage threads cannot help on 3.12 — bytecode serializes under the GIL — but CAN parallelize on 3.13t; the answer states this is the only stage where the builds differ.",
          "ru": "Стадия 2 (чистопайтоновский парсинг) названа стадией, где потоки не помогут на 3.12 — байткод сериализуется под GIL, — но МОГУТ распараллелить на 3.13t; в ответе сказано, что это единственная стадия, где сборки различаются."
        },
        {
          "en": "Mentions the latency interaction: on 3.12, running stage 2 on threads next to the I/O stages invites the convoy effect (CPU-bound threads starving I/O threads of the GIL), so stage 2 should be isolated or kept serial.",
          "ru": "Упомянуто латентностное взаимодействие: на 3.12 запуск стадии 2 в потоках рядом с I/O-стадиями провоцирует эффект конвоя (CPU-потоки морят I/O-потоки голодом по GIL), поэтому стадию 2 стоит изолировать или оставить последовательной."
        },
        {
          "en": "Names the 3.13t costs honestly: single-thread overhead (~30-40% on 3.13, shrinking in 3.14) must be paid on every stage, and one non-declared extension would silently re-enable the GIL — worth a startup assert on sys._is_gil_enabled().",
          "ru": "Честно названы цены 3.13t: однопоточные накладные (~30–40% на 3.13, меньше в 3.14) платятся на каждой стадии, а одно незадекларированное расширение тихо вернёт GIL — стоит добавить стартовый assert на sys._is_gil_enabled()."
        }
      ],
      "model": {
        "en": "Both builds: stage 1 runs in ThreadPoolExecutor(max_workers≈32) — sockets release the GIL, so 500 downloads overlap; stage 4 likewise (DB driver blocks in the kernel). Stage 3 runs in a small thread pool (≈cores) on both builds because the extension releases the GIL during compute — real parallelism already on 3.12. The fork in the road is stage 2: on 3.12 those 30 s of bytecode serialize no matter how many threads you add, and running them on threads beside the I/O pools would convoy the GIL — keep parsing serial (or accept it as the critical path); on 3.13t, split parsing across ≈cores threads for near-linear speedup, paying the build's single-thread tax everywhere. Add a startup check: assert not sys._is_gil_enabled() on deployment B, so a stray non-free-threaded wheel fails the deploy instead of silently flattening stage 2 back to one core.",
        "ru": "Обе сборки: стадия 1 — в ThreadPoolExecutor(max_workers≈32): сокеты отпускают GIL, и 500 загрузок перекрываются; стадия 4 аналогично (драйвер БД блокируется в ядре). Стадия 3 — в маленьком пуле потоков (≈числу ядер) на обеих сборках, потому что расширение отпускает GIL на время счёта — настоящий параллелизм уже на 3.12. Развилка — стадия 2: на 3.12 эти 30 с байткода сериализуются при любом числе потоков, а запуск их в потоках рядом с I/O-пулами устроит конвой за GIL — парсинг оставить последовательным (или принять его как критический путь); на 3.13t — разделить парсинг на ≈число ядер потоков с почти линейным ускорением, платя однопоточный налог сборки везде. Добавить стартовую проверку: assert not sys._is_gil_enabled() на деплое B, чтобы случайный не-free-threaded wheel ронял деплой, а не тихо сплющивал стадию 2 обратно в одно ядро."
      }
    }
  ]
}
