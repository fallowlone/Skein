{
  "lessonKey": "python/05-concurrency/04-structured-concurrency",
  "track": "python",
  "tasks": [
    {
      "id": "predict-taskgroup-failure-path",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict what a failing TaskGroup prints and raises",
        "ru": "Предскажи, что напечатает и кинет падающий TaskGroup"
      },
      "prompt": {
        "en": "Three children run in a TaskGroup; one fails fast. Predict the printed lines, their approximate timing, and the exception type the caller sees. Then reveal.",
        "ru": "Три ребёнка работают в TaskGroup; один быстро падает. Предскажи напечатанные строки, их примерный тайминг и тип исключения у вызывающего. Затем открой ответ."
      },
      "scenario": {
        "en": "import asyncio\n\nasync def slow(name, secs):\n    try:\n        await asyncio.sleep(secs)\n        print(f\"{name} done\")\n    finally:\n        print(f\"{name} cleanup\")\n\nasync def boom():\n    await asyncio.sleep(0.1)\n    raise ValueError(\"boom\")\n\nasync def main():\n    try:\n        async with asyncio.TaskGroup() as tg:\n            tg.create_task(slow(\"A\", 5))\n            tg.create_task(slow(\"B\", 5))\n            tg.create_task(boom())\n    except* ValueError as eg:\n        print(\"caught:\", eg.exceptions)\n\nasyncio.run(main())\n# What prints, when, and does 'A done' ever appear?",
        "ru": "# TaskGroup: один ребёнок кидает через 0.1 с — что с двумя медленными?\nimport asyncio\n\nasync def slow(name, secs):\n    try:\n        await asyncio.sleep(secs)\n        print(f\"{name} done\")\n    finally:\n        print(f\"{name} cleanup\")\n\nasync def boom():\n    await asyncio.sleep(0.1)\n    raise ValueError(\"boom\")\n\nasync def main():\n    try:\n        async with asyncio.TaskGroup() as tg:\n            tg.create_task(slow(\"A\", 5))\n            tg.create_task(slow(\"B\", 5))\n            tg.create_task(boom())\n    except* ValueError as eg:\n        print(\"caught:\", eg.exceptions)\n\nasyncio.run(main())\n# Что печатается, когда, и появится ли 'A done'?"
      },
      "reveal": {
        "en": "At ~0.1 s (not 5 s): boom raises ValueError; the group cancels A and B. CancelledError is delivered at their await asyncio.sleep, so neither ever prints 'A done'/'B done' — but both finally blocks run, printing 'A cleanup' and 'B cleanup'. After the group has awaited both cancelled children, it raises an ExceptionGroup containing the single ValueError; the except* ValueError clause matches and prints caught: (ValueError('boom'),).\n\nKeys: the whole thing resolves in ~0.1 s because failure cancels siblings promptly; cleanup is guaranteed because the group awaits the children's unwinding before re-raising; and even one error arrives wrapped in an ExceptionGroup — a bare 'except ValueError' would NOT have caught it.",
        "ru": "На ~0,1 с (не на 5 с): boom кидает ValueError; группа отменяет A и B. CancelledError доставляется в их await asyncio.sleep, поэтому ни 'A done', ни 'B done' не печатаются никогда — но оба finally исполняются, печатая 'A cleanup' и 'B cleanup'. Дождавшись обоих отменённых детей, группа кидает ExceptionGroup с единственным ValueError; клауза except* ValueError срабатывает и печатает caught: (ValueError('boom'),).\n\nКлючевое: всё разрешается за ~0,1 с, потому что отказ отменяет соседей сразу; очистка гарантирована, потому что группа await-ит раскрутку детей перед перебросом; и даже одиночная ошибка приходит завёрнутой в ExceptionGroup — голый 'except ValueError' её бы НЕ поймал."
      }
    },
    {
      "id": "diagnose-destroyed-pending-task",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose the audit writes that vanish without errors",
        "ru": "Диагностируй аудит-записи, исчезающие без ошибок"
      },
      "prompt": {
        "en": "A payment service intermittently loses audit rows. Read the evidence and fill in the precise mechanism behind each symptom.",
        "ru": "Платёжный сервис перемежающе теряет аудит-строки. Прочитай улики и заполни точный механизм за каждым симптомом."
      },
      "evidence": {
        "en": "Code (handler):\n    asyncio.create_task(audit.write(event))   # 'async for latency'\n    return response\n\nLog excerpts, hours apart, no request context attached:\n    ERROR asyncio Task exception was never retrieved\n      future: <Task finished coro=<write() done> exception=ConnectionResetError()>\n    ERROR asyncio Task was destroyed but it is pending!\n      task: <Task pending coro=<write() running at audit.py:18>>\n\nThe second message clusters around deploys and, rarely, mid-traffic. Missing rows ≈ 0.3% of payments.",
        "ru": "# Код хендлера и фрагменты логов\nКод (хендлер):\n    asyncio.create_task(audit.write(event))   # «асинхронно ради латентности»\n    return response\n\nФрагменты логов, с разницей в часы, без контекста запроса:\n    ERROR asyncio Task exception was never retrieved\n      future: <Task finished coro=<write() done> exception=ConnectionResetError()>\n    ERROR asyncio Task was destroyed but it is pending!\n      task: <Task pending coro=<write() running at audit.py:18>>\n\nВторое сообщение кучкуется вокруг деплоев и, редко, посреди трафика. Потерянные строки ≈ 0,3% платежей."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "weak reference",
              "only a weak reference",
              "weakly referenced",
              "слабая ссылка",
              "только слабая ссылка",
              "слабую ссылку"
            ],
            "hint": {
              "en": "Mid-traffic 'destroyed but pending' happens because the loop holds only a ___ to the task and nothing else references it, so the GC can collect it mid-write.",
              "ru": "'destroyed but pending' посреди трафика случается потому, что цикл держит на задачу лишь ___, а больше на неё не ссылается никто, — и GC может собрать её посреди записи."
            }
          },
          {
            "id": "b2",
            "accept": [
              "never awaited",
              "no one awaits the task",
              "unretrieved exception",
              "exception is observed only at gc",
              "никто не await-ит задачу",
              "исключение никто не забрал",
              "исключение всплывает только при gc"
            ],
            "hint": {
              "en": "'Exception was never retrieved' fires hours late because the error sits on the Task object until GC — a fire-and-forget task is ___.",
              "ru": "'Exception was never retrieved' приходит на часы позже, потому что ошибка лежит на объекте Task до GC — fire-and-forget задачу ___."
            }
          },
          {
            "id": "b3",
            "accept": [
              "taskgroup",
              "task group",
              "structured scope",
              "queue drained by a lifespan worker",
              "taskgroup или очередь с воркером",
              "очередь с воркером времени жизни приложения"
            ],
            "hint": {
              "en": "The structural fix: supervise the write inside the request's ___ (or hand events to a queue owned by an app-lifetime scope) so every task has an owner and shutdown drains in-flight writes.",
              "ru": "Структурное лечение: вести запись под присмотром ___ запроса (или отдавать события в очередь, принадлежащую скоупу времени жизни приложения), чтобы у каждой задачи был владелец, а shutdown дожидался записей в полёте."
            }
          }
        ]
      }
    },
    {
      "id": "design-fanout-notifications-with-deadline",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design a fan-out with a deadline, partial tolerance, and an untearable commit",
        "ru": "Спроектируй fan-out с дедлайном, частичной терпимостью и неразрываемым коммитом"
      },
      "prompt": {
        "en": "Structure the coroutine using TaskGroup, asyncio.timeout, except*, and (where justified) shield. State exactly which failures are tolerated, which cancel what, and why the commit cannot be torn. Self-grade against the rubric.",
        "ru": "Построй корутину на TaskGroup, asyncio.timeout, except* и (где оправдано) shield. Скажи точно, какие отказы терпимы, что кого отменяет и почему коммит нельзя порвать. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Operation 'close deal': (1) commit the deal row to the DB — must complete even if the request is cancelled mid-flight; (2) then notify 3 downstream services over HTTP in parallel — each may fail independently; a notification failure must be logged but must NOT fail the request or cancel the other notifications... unless it is an AuthError, which means our token is dead and ALL remaining calls are pointless. (3) The whole operation has a 10 s deadline; on timeout the caller must get TimeoutError. Python 3.11+.",
        "ru": "Операция «закрыть сделку»: (1) закоммитить строку сделки в БД — обязано завершиться, даже если запрос отменили в полёте; (2) затем уведомить 3 даунстрим-сервиса по HTTP параллельно — каждый может падать независимо; отказ уведомления логируется, но НЕ должен ронять запрос и отменять остальные уведомления... кроме AuthError: он означает, что наш токен мёртв и ВСЕ оставшиеся вызовы бессмысленны. (3) На всю операцию дедлайн 10 с; по таймауту вызывающий должен получить TimeoutError. Python 3.11+."
      },
      "rubric": [
        {
          "en": "The whole operation is wrapped in async with asyncio.timeout(10), relying on boundary conversion of CancelledError to TimeoutError for the caller.",
          "ru": "Вся операция обёрнута в async with asyncio.timeout(10) с опорой на граничную конверсию CancelledError в TimeoutError для вызывающего."
        },
        {
          "en": "The DB commit — and ONLY the commit — is wrapped in asyncio.shield, with the tradeoff named: the shielded task survives outer cancellation but becomes detached work whose result/exception must still be observed (await it or log via callback).",
          "ru": "Коммит БД — и ТОЛЬКО он — обёрнут в asyncio.shield, с названным компромиссом: экранированная задача переживает внешнюю отмену, но становится отцепленной работой, чей результат/исключение всё равно нужно наблюдать (await или логирование колбэком)."
        },
        {
          "en": "Per-notification tolerance is implemented by catching expected errors INSIDE each notify coroutine (logging and returning a status), so an ordinary failure never reaches the TaskGroup and cannot trigger sibling cancellation.",
          "ru": "Терпимость к отказу отдельного уведомления реализована перехватом ожидаемых ошибок ВНУТРИ каждой notify-корутины (лог + возврат статуса), чтобы обычный отказ не доходил до TaskGroup и не вызывал отмену соседей."
        },
        {
          "en": "AuthError is deliberately NOT caught inside the children — it propagates to the TaskGroup, which cancels the remaining notifications; the caller handles it with except* AuthError.",
          "ru": "AuthError сознательно НЕ ловится внутри детей — он пропагирует в TaskGroup, который отменяет оставшиеся уведомления; вызывающий обрабатывает его через except* AuthError."
        },
        {
          "en": "Cancellation discipline is stated: notify coroutines never swallow CancelledError (their except clauses target specific HTTP errors, not BaseException), so the 10 s timeout and group cancellation stay functional.",
          "ru": "Сформулирована дисциплина отмены: notify-корутины никогда не глотают CancelledError (их except целятся в конкретные HTTP-ошибки, не в BaseException), поэтому 10-секундный таймаут и отмена группы остаются рабочими."
        }
      ],
      "model": {
        "en": "async def close_deal(deal): async with asyncio.timeout(10): row = await asyncio.shield(commit_deal(deal)) — shield because a cancellation landing mid-INSERT must not tear the commit; the shield is the smallest possible scope, and we still await it so its exception has an address (if the outer scope is cancelled, log the shielded task's outcome via add_done_callback). Then: async with TaskGroup() as tg: for svc in services: tg.create_task(notify(svc, row)). Each notify catches its expected failures internally — except (HTTPError, ConnectionError) as e: log.warning(...); return NotifyResult(svc, failed=True) — so routine failures return values instead of exceptions and never abort siblings. AuthError is left to escape: the group cancels the other notifies (correct: the token is dead) and raises ExceptionGroup; the caller does except* AuthError to refresh the token / alert. CancelledError is never caught anywhere in the children, so the timeout's cancellation sweeps through cleanly and converts to TimeoutError at the asyncio.timeout boundary. Failure matrix: notify failure → logged, others continue; AuthError → all notifies cancelled, request fails loudly; timeout at 10 s → caller gets TimeoutError, commit either completed (shield) or never started.",
        "ru": "async def close_deal(deal): async with asyncio.timeout(10): row = await asyncio.shield(commit_deal(deal)) — shield, потому что отмена, прилетевшая посреди INSERT, не должна порвать коммит; shield — минимально возможный скоуп, и мы всё равно await-им его, чтобы у исключения был адрес (если внешний скоуп отменили — логируем исход экранированной задачи через add_done_callback). Затем: async with TaskGroup() as tg: for svc in services: tg.create_task(notify(svc, row)). Каждый notify ловит свои ожидаемые отказы внутри — except (HTTPError, ConnectionError) as e: log.warning(...); return NotifyResult(svc, failed=True) — так рутинные отказы возвращают значения вместо исключений и не прерывают соседей. AuthError оставлен вырываться: группа отменяет остальные notify (корректно: токен мёртв) и кидает ExceptionGroup; вызывающий делает except* AuthError для обновления токена/алерта. CancelledError нигде в детях не ловится, поэтому отмена таймаута проходит насквозь чисто и превращается в TimeoutError на границе asyncio.timeout. Матрица отказов: упало уведомление → залогировано, остальные продолжают; AuthError → все notify отменены, запрос падает громко; таймаут на 10 с → вызывающий получает TimeoutError, коммит либо завершён (shield), либо не начинался."
      }
    }
  ]
}
