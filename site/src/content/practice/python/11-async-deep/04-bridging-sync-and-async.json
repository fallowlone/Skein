{
  "lessonKey": "python/11-async-deep/04-bridging-sync-and-async",
  "track": "python",
  "tasks": [
    {
      "id": "bridge-callsites-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Predict: three bridge call sites, three outcomes",
        "ru": "Предскажите: три точки вызова моста, три исхода"
      },
      "prompt": {
        "en": "One long-lived loop runs in a daemon thread. For each marked point, predict the outcome: works (what is returned), hangs (until what), or raises (which exception and why).",
        "ru": "Один долгоживущий цикл крутится в daemon-потоке. Для каждой помеченной точки предскажите исход: работает (что вернётся), виснет (до чего) или падает (какое исключение и почему)."
      },
      "scenario": {
        "en": "import asyncio, threading\n\nloop = asyncio.new_event_loop()\nthreading.Thread(target=loop.run_forever, daemon=True).start()\n\nasync def ping():\n    return \"pong\"\n\ndef sync_caller():                                    # runs on the MAIN thread\n    fut = asyncio.run_coroutine_threadsafe(ping(), loop)\n    return fut.result(timeout=1)                      # point 1\n\nasync def async_caller():                             # runs ON the loop thread\n    fut = asyncio.run_coroutine_threadsafe(ping(), loop)\n    return fut.result(timeout=1)                      # point 2\n\nasync def nested():\n    return asyncio.run(ping())                        # point 3",
        "ru": "# Один долгоживущий цикл в daemon-потоке; три способа до него дотянуться\nimport asyncio, threading\n\nloop = asyncio.new_event_loop()\nthreading.Thread(target=loop.run_forever, daemon=True).start()\n\nasync def ping():\n    return \"pong\"\n\ndef sync_caller():                                    # выполняется на ГЛАВНОМ потоке\n    fut = asyncio.run_coroutine_threadsafe(ping(), loop)\n    return fut.result(timeout=1)                      # точка 1\n\nasync def async_caller():                             # выполняется НА потоке цикла\n    fut = asyncio.run_coroutine_threadsafe(ping(), loop)\n    return fut.result(timeout=1)                      # точка 2\n\nasync def nested():\n    return asyncio.run(ping())                        # точка 3"
      },
      "reveal": {
        "en": "Point 1 works: the coroutine is handed to the loop thread (with the selector wake-up byte), the main thread blocks at most 1 s on the concurrent Future and gets 'pong'. Point 2 self-deadlocks until the timeout: .result() parks the loop thread itself, and the parked loop is the only executor that could ever run ping() — after 1 s a TimeoutError, without the timeout an eternal hang; from the loop thread the correct form is await ping(). Point 3 raises RuntimeError immediately: asyncio.run refuses to start inside a running loop — the loop is not re-entrant, and a nested run would have to pause the current iteration mid-drain. Three call sites, one rule: which thread you stand on decides the legal bridge.",
        "ru": "Точка 1 работает: корутина передана потоку цикла (с байтом побудки селектора), главный поток блокируется максимум 1 с на concurrent-футуре и получает 'pong'. Точка 2 — само-дедлок до таймаута: .result() паркует сам поток цикла, а запаркованный цикл — единственный исполнитель, который мог бы выполнить ping(); через 1 с TimeoutError, без таймаута — вечное зависание; с потока цикла правильная форма — await ping(). Точка 3 немедленно поднимает RuntimeError: asyncio.run отказывается стартовать внутри работающего цикла — цикл нереентерабелен, и вложенный запуск должен был бы поставить текущую итерацию на паузу посреди разгрузки. Три точки вызова, одно правило: поток, на котором вы стоите, решает, какой мост законен."
      }
    },
    {
      "id": "pool-rebuild-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: a TLS handshake on every single request",
        "ru": "Диагностируйте: TLS-хендшейк на каждом запросе"
      },
      "prompt": {
        "en": "After switching to an async-only SDK behind a sync view, search latency tripled. Read the bridge and the evidence; name what dies per request, why the pool cannot survive, and the correct long-lived-loop fix.",
        "ru": "После перехода на async-only SDK за синхронной вью латентность поиска утроилась. Прочитайте мост и улики; назовите, что умирает на каждом запросе, почему пул не может выжить, и корректный фикс с долгоживущим циклом."
      },
      "scenario": {
        "en": "# legacy WSGI view, called ~2000 times/minute\nclient = SearchClient(pool_size=20)        # module level: created once... right?\n\ndef search(request):\n    result = asyncio.run(client.search(request.GET[\"q\"]))\n    return JsonResponse(result)",
        "ru": "# легаси WSGI-вью, вызывается ~2000 раз в минуту\nclient = SearchClient(pool_size=20)        # уровень модуля: создан один раз... ведь так?\n\ndef search(request):\n    result = asyncio.run(client.search(request.GET[\"q\"]))\n    return JsonResponse(result)"
      },
      "evidence": {
        "en": "# APM waterfall, every request: [dns 2ms][tcp 8ms][TLS HANDSHAKE 38ms][http 22ms]\n# upstream connection graph: saw-tooth, builds to 20 and drops to 0 each request\n# SDK debug log, per request: 'transport pool initialized (20 slots)'\n# worker CPU +40% vs the old sync client; latency p50 70ms -> 210ms\n# the client OBJECT is created once; its connections are not",
        "ru": "# APM waterfall, каждый запрос: [dns 2мс][tcp 8мс][TLS-ХЕНДШЕЙК 38мс][http 22мс]\n# график соединений к апстриму: пила — растёт до 20 и падает в 0 на каждом запросе\n# debug-лог SDK, на каждый запрос: 'transport pool initialized (20 slots)'\n# CPU воркера +40% против старого синхронного клиента; p50 латентности 70мс -> 210мс\n# ОБЪЕКТ клиента создан один раз; его соединения — нет"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "fresh-loop",
            "accept": [
              "asyncio.run creates a fresh event loop per request and closes it after the coroutine",
              "every request builds and tears down its own loop",
              "the loop is per-call, not per-process",
              "asyncio.run создаёт свежий event loop на каждый запрос и закрывает его после корутины",
              "каждый запрос строит и сносит собственный цикл",
              "цикл живёт на вызов, а не на процесс"
            ],
            "hint": {
              "en": "The client object survives between requests. What does asyncio.run build and destroy around every call?",
              "ru": "Объект клиента живёт между запросами. Что asyncio.run строит и уничтожает вокруг каждого вызова?"
            }
          },
          {
            "id": "loop-bound-pool",
            "accept": [
              "the connection pool, keep-alive sockets and TLS sessions are bound to the loop and die with it, so every request handshakes from scratch",
              "transports and connections are loop-bound resources; a dead loop means a dead pool regardless of the surviving client object",
              "pool reuse is zero because the pooled connections cannot outlive their loop",
              "пул соединений, keep-alive-сокеты и TLS-сессии привязаны к циклу и умирают с ним, поэтому каждый запрос хендшейкается с нуля",
              "транспорты и соединения — ресурсы цикла; мёртвый цикл означает мёртвый пул, как бы ни выжил объект клиента",
              "переиспользование пула нулевое, потому что соединения пула не могут пережить свой цикл"
            ],
            "hint": {
              "en": "The evidence says the client object is created once but 'transport pool initialized' logs per request. What are connections attached to?",
              "ru": "Улики говорят: объект клиента создан один раз, но 'transport pool initialized' логируется на каждый запрос. К чему привязаны соединения?"
            }
          },
          {
            "id": "shared-loop-fix",
            "accept": [
              "start one loop in a dedicated daemon thread at boot and submit coroutines with run_coroutine_threadsafe, blocking on result with a timeout",
              "a long-lived loop thread plus run_coroutine_threadsafe restores pool reuse without rewriting the framework",
              "create the client inside the shared loop and bridge every view call through run_coroutine_threadsafe",
              "поднять один цикл в выделенном daemon-потоке при старте и отправлять корутины через run_coroutine_threadsafe, блокируясь на result с таймаутом",
              "долгоживущий поток цикла плюс run_coroutine_threadsafe возвращают переиспользование пула без переписывания фреймворка",
              "создать клиента внутри общего цикла и мостить каждый вызов вью через run_coroutine_threadsafe"
            ],
            "hint": {
              "en": "The pool must live as long as the process. Which bridge submits a coroutine into an already-running loop from a sync thread?",
              "ru": "Пул должен жить столько же, сколько процесс. Какой мост отправляет корутину в уже работающий цикл из синхронного потока?"
            }
          }
        ]
      }
    },
    {
      "id": "wsgi-bridge-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: one async bridge for a sync monolith",
        "ru": "Спроектируйте: один async-мост для синхронного монолита"
      },
      "prompt": {
        "en": "A legacy WSGI monolith (gunicorn, sync workers, 8 threads each) must adopt an async-only vendor SDK this quarter without a framework migration. Design the bridge module: loop lifecycle, the crossing discipline, timeouts, backpressure, and the contagion policy that keeps the boundary singular.",
        "ru": "Легаси WSGI-монолит (gunicorn, синхронные воркеры, по 8 потоков) обязан в этом квартале взять async-only SDK вендора без миграции фреймворка. Спроектируйте модуль моста: жизненный цикл цикла, дисциплину пересечения, таймауты, backpressure и политику заразности, сохраняющую границу единственной."
      },
      "constraints": {
        "en": "p99 budget for the bridged calls: 80 ms. The SDK client must be constructed inside its loop. Worker processes recycle on deploys, so startup and shutdown must be clean. 8 WSGI threads can stampede the bridge concurrently. Python 3.11; no framework rewrite, no second bridge allowed elsewhere in the codebase.",
        "ru": "Бюджет p99 для мостовых вызовов: 80 мс. Клиент SDK обязан создаваться внутри своего цикла. Процессы-воркеры пересоздаются на деплоях, так что старт и остановка должны быть чистыми. 8 WSGI-потоков могут одновременно навалиться на мост. Python 3.11; без переписывания фреймворка, второй мост где-либо ещё в кодовой базе запрещён."
      },
      "rubric": [
        {
          "en": "One loop per process in a daemon thread, started lazily or at boot; the SDK client is constructed INSIDE the loop (via a bootstrap coroutine submitted through the bridge), never on a WSGI thread",
          "ru": "Один цикл на процесс в daemon-потоке, стартующий лениво или при загрузке; клиент SDK создаётся ВНУТРИ цикла (бутстрап-корутиной, отправленной через мост), никогда на WSGI-потоке"
        },
        {
          "en": "Every crossing is run_coroutine_threadsafe + .result(timeout) with the timeout derived from the 80 ms p99 budget; no naked .result() and no asyncio.run anywhere in request paths",
          "ru": "Каждое пересечение — run_coroutine_threadsafe + .result(timeout) с таймаутом, выведенным из бюджета p99 в 80 мс; никаких голых .result() и никакого asyncio.run на путях запросов"
        },
        {
          "en": "Thread-safety rule stated explicitly: foreign threads touch the loop only via run_coroutine_threadsafe / call_soon_threadsafe (the selector wake-up), and .result() is never called on the loop thread",
          "ru": "Правило потокобезопасности проговорено явно: чужие потоки трогают цикл только через run_coroutine_threadsafe / call_soon_threadsafe (побудка селектора), а .result() никогда не вызывается на потоке цикла"
        },
        {
          "en": "Backpressure is designed, not hoped for: a semaphore inside the loop (or a bounded submission gate) caps concurrent bridged calls so 8 stampeding WSGI threads cannot pile unbounded work onto one loop",
          "ru": "Backpressure спроектирован, а не выпрошен: семафор внутри цикла (или ограниченный шлюз отправки) лимитирует одновременные мостовые вызовы, чтобы 8 навалившихся WSGI-потоков не свалили неограниченную работу на один цикл"
        },
        {
          "en": "Contagion policy: the bridge is ONE module with a sync facade; shutdown drains via call_soon_threadsafe(loop.stop) after cancelling in-flight work; per-call asyncio.run is named as the forbidden anti-pattern with the pool-rebuild reason",
          "ru": "Политика заразности: мост — ОДИН модуль с синхронным фасадом; остановка дренируется через call_soon_threadsafe(loop.stop) после отмены работы в полёте; по-вызовный asyncio.run назван запрещённым антипаттерном с причиной пересоздания пулов"
        }
      ],
      "model": {
        "en": "import asyncio, threading, atexit\n\nclass Bridge:\n    def __init__(self):\n        self.loop = asyncio.new_event_loop()\n        self._thread = threading.Thread(\n            target=self.loop.run_forever, daemon=True, name=\"sdk-bridge\")\n        self._thread.start()\n        self.client = self.call(self._bootstrap(), timeout=10)   # built IN the loop\n        atexit.register(self.close)\n\n    async def _bootstrap(self):\n        self._gate = asyncio.Semaphore(16)        # backpressure lives loop-side\n        return await SearchClient.create(pool_size=20)\n\n    async def _guarded(self, coro):\n        async with self._gate:                    # 8 threads cannot pile past 16\n            return await coro\n\n    def call(self, coro, timeout=0.075):          # 75 ms of the 80 ms budget\n        fut = asyncio.run_coroutine_threadsafe(self._guarded(coro), self.loop)\n        return fut.result(timeout)                # blocks the WSGI thread only\n\n    def close(self):\n        self.loop.call_soon_threadsafe(self.loop.stop)   # the legal wake-up\n        self._thread.join(timeout=5)\n\nbridge = Bridge()                                  # exactly one, module singleton\n\ndef search_view(request):                          # sync facade, framework untouched\n    return JsonResponse(bridge.call(bridge.client.search(request.GET[\"q\"])))\n\nWhy it holds: the client and its pool live exactly as long as the loop — one TLS handshake per connection per process, not per request. The semaphore sits inside the loop because that is the only thread-safe home for shared async state; the WSGI threads feel backpressure as slightly longer .result() waits instead of the loop melting. The timeout is budget-derived and applies at every crossing, so a dead vendor degrades to fast errors, not stuck workers. Shutdown crosses the boundary the only legal way — call_soon_threadsafe — and the atexit hook keeps worker recycles clean. The policy line for review: any new asyncio.run in a request path is a rejected diff, with the waterfall from the incident attached as the reason.",
        "ru": "# Один цикл на процесс, клиент строится в цикле, семафор-шлюз, таймаут из бюджета, чистый shutdown\nimport asyncio, threading, atexit\n\nclass Bridge:\n    def __init__(self):\n        self.loop = asyncio.new_event_loop()\n        self._thread = threading.Thread(\n            target=self.loop.run_forever, daemon=True, name=\"sdk-bridge\")\n        self._thread.start()\n        self.client = self.call(self._bootstrap(), timeout=10)   # построен В цикле\n        atexit.register(self.close)\n\n    async def _bootstrap(self):\n        self._gate = asyncio.Semaphore(16)        # backpressure живёт на стороне цикла\n        return await SearchClient.create(pool_size=20)\n\n    async def _guarded(self, coro):\n        async with self._gate:                    # 8 потоков не навалят больше 16\n            return await coro\n\n    def call(self, coro, timeout=0.075):          # 75 мс из бюджета в 80 мс\n        fut = asyncio.run_coroutine_threadsafe(self._guarded(coro), self.loop)\n        return fut.result(timeout)                # блокирует только WSGI-поток\n\n    def close(self):\n        self.loop.call_soon_threadsafe(self.loop.stop)   # законная побудка\n        self._thread.join(timeout=5)\n\nbridge = Bridge()                                  # ровно один, синглтон модуля\n\ndef search_view(request):                          # синхронный фасад, фреймворк не тронут\n    return JsonResponse(bridge.call(bridge.client.search(request.GET[\"q\"])))\n\nПочему держится: клиент и его пул живут ровно столько, сколько цикл, — один TLS-хендшейк на соединение на процесс, а не на запрос. Семафор сидит внутри цикла, потому что это единственный потокобезопасный дом для общего асинхронного состояния; WSGI-потоки чувствуют backpressure как чуть более долгие ожидания .result() вместо плавящегося цикла. Таймаут выведен из бюджета и стоит на каждом пересечении, так что мёртвый вендор деградирует в быстрые ошибки, а не в застрявших воркеров. Остановка пересекает границу единственным законным способом — call_soon_threadsafe, — а atexit-хук держит пересоздание воркеров чистым. Строка политики для ревью: любой новый asyncio.run на пути запроса — отклонённый дифф, с приложенным waterfall из инцидента в качестве причины."
      }
    }
  ]
}
