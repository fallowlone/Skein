{
  "lessonKey": "python/11-async-deep/02-cancellation-semantics",
  "track": "python",
  "tasks": [
    {
      "id": "cancel-catch-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: three except clauses, three fates under cancel()",
        "ru": "Предскажите: три except, три судьбы под cancel()"
      },
      "prompt": {
        "en": "All three workers are sleeping when task.cancel() is called on each. For each one, predict: does the task end, in what state (cancelled / finished normally), and what does await task return or raise?",
        "ru": "Все три воркера спят, когда на каждом вызывают task.cancel(). Для каждого предскажите: завершится ли задача, в каком состоянии (cancelled / нормально), и что вернёт или бросит await task."
      },
      "scenario": {
        "en": "import asyncio\n\nasync def w1():\n    try:\n        await asyncio.sleep(3600)\n    except Exception:\n        return \"w1 caught\"        # point 1\n\nasync def w2():\n    try:\n        await asyncio.sleep(3600)\n    except:\n        return \"w2 caught\"        # point 2\n\nasync def w3():\n    try:\n        await asyncio.sleep(3600)\n    except asyncio.CancelledError:\n        await release_lease()\n        raise                     # point 3",
        "ru": "# Три воркера: except Exception, голый except, поимка с re-raise\nimport asyncio\n\nasync def w1():\n    try:\n        await asyncio.sleep(3600)\n    except Exception:\n        return \"w1 caught\"        # точка 1\n\nasync def w2():\n    try:\n        await asyncio.sleep(3600)\n    except:\n        return \"w2 caught\"        # точка 2\n\nasync def w3():\n    try:\n        await asyncio.sleep(3600)\n    except asyncio.CancelledError:\n        await release_lease()\n        raise                     # точка 3"
      },
      "reveal": {
        "en": "Point 1: except Exception does NOT match CancelledError (BaseException since 3.8) — the exception unwinds, the task ends cancelled, await task raises CancelledError, task.cancelled() is True. Point 2: the bare except catches it; the function returns 'w2 caught', so the task ends NORMALLY despite cancel() having returned True — await task returns 'w2 caught', task.cancelled() is False, and every scope above has been lied to. This is the immortality bug in miniature (here the function happens to return; a while-True around it would never die). Point 3: the handler cleans up and re-raises — the task ends cancelled and truthfully reports it; the only risk left is the unbounded await release_lease() in the cancellation path, where a second cancel could land.",
        "ru": "Точка 1: except Exception НЕ матчит CancelledError (BaseException с 3.8) — исключение разматывается, задача заканчивается cancelled, await task бросает CancelledError, task.cancelled() — True. Точка 2: голый except его ловит; функция возвращает 'w2 caught', и задача заканчивается НОРМАЛЬНО, хотя cancel() вернул True — await task возвращает 'w2 caught', task.cancelled() — False, и всем скоупам выше солгали. Это баг бессмертия в миниатюре (здесь функция хотя бы возвращается; while-True вокруг — и она не умерла бы никогда). Точка 3: обработчик убирается и пере-бросает — задача заканчивается cancelled и честно это сообщает; единственный оставшийся риск — неограниченный await release_lease() на пути отмены, куда может приземлиться вторая отмена."
      }
    },
    {
      "id": "deploy-hang-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: shutdown logs, then hangs ten minutes to SIGKILL",
        "ru": "Диагностируйте: shutdown логирует — и виснет десять минут до SIGKILL"
      },
      "prompt": {
        "en": "Shutdown cancels all workers and awaits them; the process hangs until SIGKILL and the queue double-processes after restart. Read the code and the evidence; find the swallowing bug, the smoking-gun log line, and the correct handler shape.",
        "ru": "Shutdown отменяет всех воркеров и ждёт их; процесс висит до SIGKILL, а очередь после рестарта обрабатывает джобы дважды. Прочитайте код и улики; найдите баг проглатывания, улику в логах и корректную форму обработчика."
      },
      "scenario": {
        "en": "async def worker(q):\n    while True:\n        try:\n            job = await q.get()\n            await handle(job)\n            await q.ack(job)\n        except:                       # \"workers must survive anything\"\n            log.warning(\"worker recovered: %r\", sys.exc_info()[1])\n\nasync def shutdown(tasks):\n    log.info(\"shutting down, cancelling %d workers\", len(tasks))\n    for t in tasks:\n        t.cancel()\n    await asyncio.gather(*tasks)      # hangs here",
        "ru": "# Воркер с голым except «на всякий случай» и shutdown, который его отменяет\nasync def worker(q):\n    while True:\n        try:\n            job = await q.get()\n            await handle(job)\n            await q.ack(job)\n        except:                       # «воркеры должны переживать всё»\n            log.warning(\"worker recovered: %r\", sys.exc_info()[1])\n\nasync def shutdown(tasks):\n    log.info(\"shutting down, cancelling %d workers\", len(tasks))\n    for t in tasks:\n        t.cancel()\n    await asyncio.gather(*tasks)      # висит здесь"
      },
      "evidence": {
        "en": "# logs after SIGTERM:\n#   shutting down, cancelling 32 workers\n#   worker recovered: CancelledError()        x32, then repeating\n# py-spy dump 5 minutes in: all 32 tasks alive, parked in q.get()\n# t.cancel() returned True for every task; t.cancelled() is False for all\n# after SIGKILL + restart: broker redelivered 32 unacked jobs -> duplicates",
        "ru": "# логи после SIGTERM:\n#   shutting down, cancelling 32 workers\n#   worker recovered: CancelledError()        x32, затем по кругу\n# py-spy dump через 5 минут: все 32 задачи живы, стоят в q.get()\n# t.cancel() вернул True для каждой; t.cancelled() — False у всех\n# после SIGKILL + рестарта: брокер передоставил 32 неподтверждённых джоба -> дубли"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "bare-except-swallows",
            "accept": [
              "the bare except catches CancelledError and the while True resumes, so the task never ends",
              "bare except catches BaseException including cancellation",
              "cancellation is swallowed by the bare except and the loop continues",
              "голый except ловит CancelledError, и while True продолжается — задача не заканчивается никогда",
              "голый except ловит BaseException, включая отмену",
              "отмена проглочена голым except, и цикл продолжается"
            ],
            "hint": {
              "en": "cancel() returned True, the exception WAS delivered. Which handler matched it, and what did the loop do next?",
              "ru": "cancel() вернул True, исключение БЫЛО доставлено. Какой обработчик его поймал и что цикл сделал дальше?"
            }
          },
          {
            "id": "smoking-gun",
            "accept": [
              "the log line worker recovered: CancelledError() is the smoking gun — the worker logged its own death sentence as a recoverable error",
              "CancelledError appearing as a recovered error in logs",
              "gather never returning plus tasks parked in q.get() proves they survived cancellation",
              "строка worker recovered: CancelledError() — улика: воркер залогировал собственный смертный приговор как восстановимую ошибку",
              "CancelledError в логах в роли восстановленной ошибки",
              "gather не возвращается, а задачи стоят в q.get() — значит, они пережили отмену"
            ],
            "hint": {
              "en": "One log line literally prints the exception class that should have killed the task. Which one?",
              "ru": "Одна строка лога буквально печатает класс исключения, которое должно было убить задачу. Какая?"
            }
          },
          {
            "id": "correct-shape",
            "accept": [
              "add except asyncio.CancelledError with bounded cleanup and raise before the broad handler, and make the broad handler except Exception",
              "catch CancelledError separately, nack within a timeout, re-raise; use except Exception for job errors",
              "except CancelledError: bounded nack then raise; never bare except",
              "добавить except asyncio.CancelledError с ограниченной уборкой и raise до широкого обработчика, а широкий сделать except Exception",
              "ловить CancelledError отдельно, nack под таймаутом, re-raise; для ошибок джобов — except Exception",
              "except CancelledError: ограниченный nack и raise; голого except не бывает"
            ],
            "hint": {
              "en": "Two changes: what replaces the bare except for job errors, and what must the cancellation branch do with the exception after cleanup?",
              "ru": "Два изменения: что заменяет голый except для ошибок джобов и что ветка отмены обязана сделать с исключением после уборки?"
            }
          }
        ]
      }
    },
    {
      "id": "graceful-shutdown-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design: a cancellation-correct shutdown for a consumer pool",
        "ru": "Спроектируйте: корректную по отмене остановку пула консьюмеров"
      },
      "prompt": {
        "en": "Design the shutdown protocol for a pool of 32 queue consumers: how workers handle CancelledError, how in-flight jobs are released, how cleanup is bounded against the double-cancel, and how the at-least-once broker shapes the handlers.",
        "ru": "Спроектируйте протокол остановки пула из 32 консьюмеров очереди: как воркеры обрабатывают CancelledError, как освобождаются джобы в полёте, как уборка ограничена против двойной отмены и как at-least-once брокер формует обработчики."
      },
      "constraints": {
        "en": "Total shutdown budget 30 s; a job takes up to 20 s; the broker redelivers unacked jobs (at-least-once). Python 3.12, TaskGroup available. An in-flight job should be nacked fast on cancel, not finished. The broker connection may already be dead during shutdown.",
        "ru": "Общий бюджет остановки 30 с; джоб занимает до 20 с; брокер передоставляет неподтверждённое (at-least-once). Python 3.12, TaskGroup доступен. Джоб в полёте при отмене надо быстро nack-нуть, а не доделывать. Соединение с брокером во время остановки может быть уже мёртвым."
      },
      "rubric": [
        {
          "en": "Cancellation enters through structure: workers live in a TaskGroup (or explicit cancel + gather with return_exceptions=True); no loop-kill, no flag-polling protocol invented from scratch",
          "ru": "Отмена входит через структуру: воркеры живут в TaskGroup (или явный cancel + gather с return_exceptions=True); никакого убийства цикла и самодельного протокола опроса флагов"
        },
        {
          "en": "Handler shape is exact: except asyncio.CancelledError does bounded cleanup then raise; job errors go through except Exception and keep the loop; no bare except anywhere",
          "ru": "Форма обработчика точная: except asyncio.CancelledError делает ограниченную уборку и raise; ошибки джобов идут через except Exception и сохраняют цикл; голого except нет нигде"
        },
        {
          "en": "Cleanup is double-cancel-proof: the nack is wrapped in asyncio.timeout (~2 s) with TimeoutError suppressed, because the broker may be dead and a second cancel may land inside finally",
          "ru": "Уборка устойчива к двойной отмене: nack обёрнут в asyncio.timeout (~2 с) с подавлением TimeoutError, потому что брокер может быть мёртв, а вторая отмена — приземлиться внутри finally"
        },
        {
          "en": "The re-raise is non-negotiable and justified: without it gather/TaskGroup wait forever and task.cancelled() lies to the shutdown path",
          "ru": "Re-raise безусловен и обоснован: без него gather/TaskGroup ждут вечно, а task.cancelled() лжёт пути остановки"
        },
        {
          "en": "At-least-once is embraced, not fought: handlers are idempotent (dedup key or natural idempotency), because nack-on-cancel plus broker redelivery guarantees occasional double delivery",
          "ru": "At-least-once принят, а не оспорен: обработчики идемпотентны (ключ дедупликации или естественная идемпотентность), потому что nack-при-отмене плюс передоставка брокера гарантируют редкие двойные доставки"
        }
      ],
      "model": {
        "en": "import asyncio, contextlib\n\nasync def worker(q):\n    while True:\n        job = await q.get()                      # cancellation point: nothing in flight\n        try:\n            await handle(job)                    # idempotent: dedup key = job.id\n            await q.ack(job)\n        except asyncio.CancelledError:\n            with contextlib.suppress(TimeoutError, ConnectionError):\n                async with asyncio.timeout(2):   # broker may be dead: 2 s, not forever\n                    await q.nack(job)            # release fast; redelivery will retry\n            raise                                # the canceller is owed the unwind\n        except Exception:\n            log.exception(\"job failed\")\n            await q.nack(job)\n\nasync def main():\n    async with asyncio.TaskGroup() as tg:        # structure owns the lifetimes\n        for _ in range(32):\n            tg.create_task(worker(q))\n        await sigterm_event.wait()\n        raise ShutdownRequested                  # one failure cancels all siblings\n\nWhy it holds the budget: cancel lands either in q.get() (nothing in flight, unwind is instant) or inside handle() at its next await — the nack branch then spends at most 2 s. Worst case is bounded by the longest uncancellable stretch inside handle(), so handle() must keep real awaits in its long phases. Idempotency is the partner decision: cancel-then-nack plus at-least-once means some job WILL run twice; the dedup key turns that from incident into noise. The bare-except clause never appears: job errors and cancellation have different contracts, and the code spells both.",
        "ru": "# Протокол: TaskGroup владеет жизнями, ветка отмены ограничена, re-raise обязателен, обработчики идемпотентны\nimport asyncio, contextlib\n\nasync def worker(q):\n    while True:\n        job = await q.get()                      # точка отмены: в полёте ничего\n        try:\n            await handle(job)                    # идемпотентно: ключ дедупа = job.id\n            await q.ack(job)\n        except asyncio.CancelledError:\n            with contextlib.suppress(TimeoutError, ConnectionError):\n                async with asyncio.timeout(2):   # брокер может быть мёртв: 2 с, не вечность\n                    await q.nack(job)            # освободить быстро; передоставка дотянет\n            raise                                # отменяющему причитается размотка\n        except Exception:\n            log.exception(\"джоб упал\")\n            await q.nack(job)\n\nasync def main():\n    async with asyncio.TaskGroup() as tg:        # структура владеет временами жизни\n        for _ in range(32):\n            tg.create_task(worker(q))\n        await sigterm_event.wait()\n        raise ShutdownRequested                  # один сбой отменяет всех соседей\n\nПочему бюджет держится: отмена приземляется либо в q.get() (в полёте ничего, размотка мгновенна), либо внутри handle() на его ближайшем await — ветка nack тратит максимум 2 с. Худший случай ограничен самым длинным неотменяемым отрезком внутри handle(), поэтому handle() обязан держать настоящие await в длинных фазах. Идемпотентность — парное решение: отмена-затем-nack плюс at-least-once означает, что какой-то джоб ТОЧНО выполнится дважды; ключ дедупа превращает это из инцидента в шум. Голый except не появляется нигде: у ошибок джобов и у отмены разные контракты, и код проговаривает оба."
      }
    }
  ]
}
