{
  "lessonKey": "python/07-testing/01-pytest-idioms",
  "track": "python",
  "tasks": [
    {
      "id": "pytest-behaviors-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: three pytest setups, three fates",
        "ru": "Предскажите: три конфигурации pytest, три судьбы"
      },
      "prompt": {
        "en": "For each of the three marked points, predict the exact outcome: what the failure output looks like, whether the suite goes red or green, and what exit code CI sees.",
        "ru": "Для каждой из трёх помеченных точек предскажите точный исход: как выглядит вывод при падении, краснеет сьют или зеленеет и какой код выхода видит CI."
      },
      "scenario": {
        "en": "# tests/helpers.py  (imported by tests, never collected itself)\ndef check_total(order):\n    assert order.total == sum(i.price for i in order.items)   # point 1 — its failure output\n\n# tests/test_orders.py\nimport pytest\nfrom .helpers import check_total\n\n@pytest.mark.xfail(reason=\"bug #88\", strict=True)\ndef test_rounding():                                          # point 2 — bug #88 was FIXED yesterday\n    assert round_total(199, 3) == 66\n\n# CI step, after a module rename last sprint:\n#   pytest -k \"test_chekout\"                                  # point 3 — note the spelling",
        "ru": "# Три точки: assert в хелпере, строгий xfail на починенном баге, фильтр с опечаткой\n# tests/helpers.py  (импортируется тестами, сам не собирается)\ndef check_total(order):\n    assert order.total == sum(i.price for i in order.items)   # точка 1 — вывод его падения\n\n# tests/test_orders.py\nimport pytest\nfrom .helpers import check_total\n\n@pytest.mark.xfail(reason=\"баг #88\", strict=True)\ndef test_rounding():                                          # точка 2 — баг #88 ВЧЕРА починили\n    assert round_total(199, 3) == 66\n\n# Шаг CI, после переименования модуля в прошлом спринте:\n#   pytest -k \"test_chekout\"                                  # точка 3 — обратите внимание на написание"
      },
      "reveal": {
        "en": "Point 1: the assert still raises on failure, but as a bare AssertionError with no subexpression values — helpers.py is imported by tests, not collected, so pytest never AST-rewrote it. Fix: pytest.register_assert_rewrite('tests.helpers') in a root conftest before the first import. Point 2: the bug is fixed, so the test now passes — XPASS — and strict=True reports it as FAILED: the suite goes red on purpose, forcing someone to delete the stale marker. Without strict it would quietly show xpassed and rot. Point 3: -k matches nothing (the real test is test_checkout), pytest collects 0 tests and exits with code 5 — the CI job goes red even though no test failed. Handle exit 5 deliberately; do not mask it with || true, which also swallows exit 1.",
        "ru": "Точка 1: assert по-прежнему поднимается при провале, но как голый AssertionError без значений подвыражений — helpers.py импортируется тестами, а не собирается, поэтому pytest его не переписывал. Фикс: pytest.register_assert_rewrite('tests.helpers') в корневом conftest до первого импорта. Точка 2: баг починен, тест теперь проходит — XPASS — и strict=True репортит его как FAILED: сьют краснеет намеренно, заставляя удалить протухший маркер. Без strict он бы тихо показывал xpassed и гнил. Точка 3: -k не находит ничего (настоящий тест — test_checkout), pytest собирает 0 тестов и выходит с кодом 5 — джоба CI красная, хотя ни один тест не упал. Обрабатывайте код 5 явно; не маскируйте его через || true, который заодно глотает код 1."
      }
    },
    {
      "id": "collection-import-crash-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: a docs-only PR turned 1,847 tests red",
        "ru": "Диагностируйте: PR только с документацией покрасил 1 847 тестов"
      },
      "prompt": {
        "en": "A PR that changes only README files makes the entire suite error in 11 seconds — zero tests executed. Read the module and the CI log, name the root cause, explain why the blast radius is the whole suite, and state the fix.",
        "ru": "PR, меняющий только README, обваливает весь сьют ошибками за 11 секунд — выполнено ноль тестов. Прочитайте модуль и лог CI, назовите первопричину, объясните, почему радиус поражения — весь сьют, и сформулируйте фикс."
      },
      "scenario": {
        "en": "# tests/payments/test_refunds.py\nfrom app.db import engine          # top of the test module\n\ndef test_refund_full(db_session):\n    ...\n\n# app/db.py\nfrom sqlalchemy import create_engine\nfrom app.settings import settings\n\nengine = create_engine(settings.DSN, pool_pre_ping=True)   # module level\nengine.connect().close()                                    # eager health ping",
        "ru": "# Тест-модуль импортирует синглтон приложения на верхнем уровне\n# tests/payments/test_refunds.py\nfrom app.db import engine          # верх тест-модуля\n\ndef test_refund_full(db_session):\n    ...\n\n# app/db.py\nfrom sqlalchemy import create_engine\nfrom app.settings import settings\n\nengine = create_engine(settings.DSN, pool_pre_ping=True)   # уровень модуля\nengine.connect().close()                                    # нетерпеливый health-пинг"
      },
      "evidence": {
        "en": "==================== ERRORS ====================\n____ ERROR collecting tests/payments/test_refunds.py ____\ntests/payments/test_refunds.py:1: in <module>\n    from app.db import engine\napp/db.py:9: in <module>\n    engine.connect().close()\nE   OperationalError: connection refused\n!!!! Interrupted: 1 error during collection !!!!\n=========== 1847 errors in 11.02s ===========\n# the PR diff touches docs/*.md only; CI has no database service on this stage",
        "ru": "==================== ERRORS ====================\n____ ERROR collecting tests/payments/test_refunds.py ____\ntests/payments/test_refunds.py:1: in <module>\n    from app.db import engine\napp/db.py:9: in <module>\n    engine.connect().close()\nE   OperationalError: connection refused\n!!!! Interrupted: 1 error during collection !!!!\n=========== 1847 errors in 11.02s ===========\n# дифф PR трогает только docs/*.md; на этой стадии CI нет сервиса базы данных"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "root-cause",
            "accept": [
              "app/db.py builds the engine and pings the database at module import time",
              "a module-level singleton runs side effects on import",
              "import-time side effect in app.db",
              "app/db.py строит engine и пингует базу в момент импорта модуля",
              "синглтон уровня модуля выполняет побочные эффекты при импорте",
              "побочный эффект времени импорта в app.db"
            ],
            "hint": {
              "en": "Look at what app/db.py executes the moment anyone imports it — before any test or fixture runs.",
              "ru": "Посмотрите, что app/db.py выполняет в момент, когда его кто-либо импортирует, — до любого теста или фикстуры."
            }
          },
          {
            "id": "why-suite-wide",
            "accept": [
              "collection imports every test module before anything runs, and a collection error interrupts the whole run",
              "pytest must import test files to collect them, so the import error fires during collection and nothing executes",
              "the error happens at collection time, which precedes all test execution",
              "сбор импортирует каждый тест-модуль до запуска чего-либо, и ошибка сбора прерывает весь прогон",
              "pytest обязан импортировать тест-файлы для сбора, поэтому ошибка импорта стреляет на этапе сбора и ничего не выполняется",
              "ошибка происходит на этапе сбора, который предшествует всему выполнению тестов"
            ],
            "hint": {
              "en": "Zero tests ran, yet 1,847 errors. At which phase of the pytest pipeline does an import happen?",
              "ru": "Выполнено ноль тестов, но 1 847 ошибок. На какой фазе конвейера pytest происходит импорт?"
            }
          },
          {
            "id": "fix",
            "accept": [
              "make the engine lazy and inject it via a fixture",
              "move the import or engine creation behind a fixture or function",
              "create the engine in a cached factory function instead of at module level",
              "сделать engine ленивым и внедрять его через фикстуру",
              "перенести импорт или создание engine внутрь фикстуры или функции",
              "создавать engine в кэшируемой функции-фабрике, а не на уровне модуля"
            ],
            "hint": {
              "en": "The connection itself is legitimate — tests need it. What scopes the failure to only the tests that asked for it?",
              "ru": "Само соединение легитимно — тестам оно нужно. Что ограничит провал только тестами, которые его запросили?"
            }
          }
        ]
      }
    },
    {
      "id": "ci-contract-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design: the pytest CI contract for a 2,000-test monorepo",
        "ru": "Спроектируйте: CI-контракт pytest для монорепо на 2 000 тестов"
      },
      "prompt": {
        "en": "Fourteen teams share one test suite. Design the pytest configuration and CI policy: marker taxonomy and enforcement, xfail policy, PR versus nightly selection, exit-code handling for the optional perf suite, and the assert-rewrite policy for the shared tests/helpers package.",
        "ru": "Четырнадцать команд делят один тестовый сьют. Спроектируйте конфигурацию pytest и политику CI: таксономию и контроль маркеров, политику xfail, выборку для PR и для ночного прогона, обработку кодов выхода для необязательного perf-сьюта и политику переписывания assert для общего пакета tests/helpers."
      },
      "constraints": {
        "en": "PR feedback must land under 5 minutes; the nightly run executes everything. No || true anywhere. The shared helpers package contains assert-based checkers used by all teams. The perf suite is legitimately empty on some branches. pytest and stdlib only.",
        "ru": "Обратная связь на PR — меньше 5 минут; ночной прогон выполняет всё. Никаких || true. Общий пакет хелперов содержит проверки на assert, используемые всеми командами. Perf-сьют на некоторых ветках законно пуст. Только pytest и stdlib."
      },
      "rubric": [
        {
          "en": "Registers every marker (smoke, slow, perf, ...) in pyproject and turns on --strict-markers so a typoed marker is a collection error, not a silently deselected test",
          "ru": "Регистрирует каждый маркер (smoke, slow, perf, ...) в pyproject и включает --strict-markers, чтобы опечатка в маркере была ошибкой сбора, а не молча отключённым тестом"
        },
        {
          "en": "Sets xfail_strict = true (or strict=True per marker) as suite policy so fixed bugs surface as failures instead of rotting as xpassed",
          "ru": "Устанавливает xfail_strict = true (или strict=True на каждом маркере) политикой сьюта, чтобы починенные баги всплывали провалами, а не гнили как xpassed"
        },
        {
          "en": "Splits selection: PR runs -m 'smoke or not slow' (or path-scoped via -k/paths) to stay under 5 minutes; nightly runs the full suite with no filter",
          "ru": "Разделяет выборку: PR гоняет -m 'smoke or not slow' (или ограничение путями/-k), чтобы уложиться в 5 минут; ночной прогон — весь сьют без фильтра"
        },
        {
          "en": "Handles exit code 5 for the perf stage explicitly (rc=$?; exit 0 only when rc equals 5) so empty selection passes while exit 1 still fails — and names why || true is forbidden",
          "ru": "Обрабатывает код 5 для perf-стадии явно (rc=$?; exit 0 только когда rc равен 5), чтобы пустая выборка проходила, а код 1 по-прежнему валил, — и объясняет, почему || true запрещён"
        },
        {
          "en": "Calls pytest.register_assert_rewrite('tests.helpers') in the root conftest before any helper import, so shared checkers keep full assertion introspection",
          "ru": "Вызывает pytest.register_assert_rewrite('tests.helpers') в корневом conftest до первого импорта хелперов, чтобы общие проверки сохраняли полную интроспекцию assert"
        }
      ],
      "model": {
        "en": "# pyproject.toml\n[tool.pytest.ini_options]\naddopts = \"--strict-markers -ra\"\nxfail_strict = true\nmarkers = [\n  \"smoke: fast cross-team gate, runs on every PR\",\n  \"slow: excluded from PR runs\",\n  \"perf: optional perf suite, nightly hardware only\",\n]\n\n# conftest.py (repo root) — BEFORE anything imports the helpers\nimport pytest\npytest.register_assert_rewrite(\"tests.helpers\")\n\n# CI: PR stage (under 5 min)\n#   pytest -m \"smoke or not slow\"        -> exit 1 fails the job, as it should\n# CI: nightly\n#   pytest                                -> everything, no filters\n# CI: perf stage (legitimately empty on some branches)\n#   pytest -m perf; rc=$?\n#   if [ $rc -eq 5 ]; then exit 0; else exit $rc; fi\n\nWhy this shape: --strict-markers makes the marker taxonomy enforceable — fourteen teams will typo markers, and a typo must be a collection error, not a test that never runs again. xfail_strict=true is the anti-rot policy: an xfail that starts passing fails the suite and forces marker deletion the same day. The PR/nightly split spends the 5-minute budget on smoke plus fast tests and leaves completeness to the nightly, where exit 1 pages the owning team. The perf stage distinguishes exit codes because || true would flatten 1 (real failures) and 5 (empty selection) into the same green; the explicit rc check passes only the empty case. register_assert_rewrite goes in the root conftest because rewriting happens at first import — registering after the helpers are imported does nothing, and without it every shared checker reports bare AssertionError, which at 2,000 tests means the most-used diagnostics in the repo are the worst ones.",
        "ru": "# Контракт: строгие маркеры, строгий xfail, раздельные выборки, явный код 5, переписывание хелперов\n# pyproject.toml\n[tool.pytest.ini_options]\naddopts = \"--strict-markers -ra\"\nxfail_strict = true\nmarkers = [\n  \"smoke: быстрый межкомандный гейт, на каждом PR\",\n  \"slow: исключён из PR-прогонов\",\n  \"perf: необязательный perf-сьют, только ночное железо\",\n]\n\n# conftest.py (корень репо) — ДО того, как кто-либо импортирует хелперы\nimport pytest\npytest.register_assert_rewrite(\"tests.helpers\")\n\n# CI: стадия PR (меньше 5 минут)\n#   pytest -m \"smoke or not slow\"        -> код 1 валит джобу, как и должен\n# CI: ночной прогон\n#   pytest                                -> всё, без фильтров\n# CI: perf-стадия (на части веток законно пуста)\n#   pytest -m perf; rc=$?\n#   if [ $rc -eq 5 ]; then exit 0; else exit $rc; fi\n\nПочему такая форма: --strict-markers делает таксономию маркеров принуждаемой — четырнадцать команд будут опечатываться, и опечатка обязана быть ошибкой сбора, а не тестом, который больше никогда не запускается. xfail_strict=true — политика против гниения: xfail, начавший проходить, валит сьют и заставляет удалить маркер в тот же день. Раздвоение PR/ночь тратит пятиминутный бюджет на smoke плюс быстрые тесты, оставляя полноту ночному прогону, где код 1 будит владеющую команду. Perf-стадия различает коды выхода, потому что || true сплющил бы 1 (настоящие провалы) и 5 (пустую выборку) в одинаковый зелёный; явная проверка rc пропускает только пустой случай. register_assert_rewrite стоит в корневом conftest, потому что переписывание происходит при первом импорте — регистрация после импорта хелперов не делает ничего, а без неё каждая общая проверка репортит голый AssertionError: на 2 000 тестах самые используемые диагностики в репозитории оказались бы худшими."
      }
    }
  ]
}
