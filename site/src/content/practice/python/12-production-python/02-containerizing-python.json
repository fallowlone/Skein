{
  "lessonKey": "python/12-production-python/02-containerizing-python",
  "track": "python",
  "tasks": [
    {
      "id": "dockerfile-three-traps-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Predict: one Dockerfile, three production behaviors",
        "ru": "Предскажите: один Dockerfile, три продакшен-поведения"
      },
      "prompt": {
        "en": "This Dockerfile builds and the service runs. Predict three concrete behaviors: what a one-line code change costs in CI, what kubectl logs shows right after boot, and what happens at the next rolling deploy.",
        "ru": "Этот Dockerfile собирается, сервис работает. Предскажите три конкретных поведения: во что обходится однострочная правка кода в CI, что показывает kubectl logs сразу после старта и что произойдёт на следующей раскатке."
      },
      "scenario": {
        "en": "FROM python:3.12-slim\nWORKDIR /app\nCOPY . .                          # everything, no .dockerignore\nRUN pip install -r requirements.txt\nCMD gunicorn -c gunicorn.conf.py app.main:app\n# (no ENV lines anywhere)",
        "ru": "# Однострочная COPY всего контекста, установка после, shell-форма CMD, без ENV\nFROM python:3.12-slim\nWORKDIR /app\nCOPY . .                          # всё подряд, без .dockerignore\nRUN pip install -r requirements.txt\nCMD gunicorn -c gunicorn.conf.py app.main:app\n# (нигде нет строк ENV)"
      },
      "reveal": {
        "en": "CI: every commit reinstalls every dependency — COPY . . is the first layer that sees code, its checksum changes on any edit, and every later layer (the pip install) rebuilds; with no .dockerignore the context also drags in .git and the host .venv, so the COPY itself is slow and the image bloated. Logs: kubectl logs shows nothing for minutes — stdout is not a TTY, so CPython block-buffers it without PYTHONUNBUFFERED=1; if the pod crashes early, the buffered tail is lost with it. Deploy: SIGTERM goes to /bin/sh -c (shell-form CMD), which forwards nothing; gunicorn keeps serving until the grace period expires and SIGKILL drops every in-flight request — the burst of rollout 502s. Three behaviors, three one-line fixes: lockfile-first COPY ordering, ENV PYTHONUNBUFFERED=1, exec-form CMD.",
        "ru": "CI: каждый коммит переустанавливает все зависимости — COPY . . это первый слой, видящий код, его чексумма меняется от любой правки, и все последующие слои (pip install) пересобираются; без .dockerignore контекст вдобавок тащит .git и хостовый .venv, так что сам COPY медленный, а образ раздут. Логи: kubectl logs минутами не показывает ничего — stdout не TTY, и CPython буферизует его блоками без PYTHONUNBUFFERED=1; если под падает рано, буферизованный хвост теряется вместе с ним. Деплой: SIGTERM уходит в /bin/sh -c (shell-форма CMD), который ничего не пересылает; gunicorn обслуживает дальше, пока период ожидания не истечёт, и SIGKILL роняет все запросы в полёте — всплеск 502-х на раскатке. Три поведения — три однострочных фикса: порядок COPY с lock-файлом первым, ENV PYTHONUNBUFFERED=1, exec-форма CMD."
      }
    },
    {
      "id": "image-bloat-sigkill-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 9,
      "title": {
        "en": "Diagnose: a 1.4GB image and deploys that drop requests",
        "ru": "Диагностируйте: образ на 1,4 ГБ и деплои, роняющие запросы"
      },
      "prompt": {
        "en": "Platform flagged your service: the image is 1.4GB and every rollout produces a spike of 502s. Read the build log and the in-container evidence; identify what bloats the image and why requests die at deploy.",
        "ru": "Платформенная команда пометила ваш сервис: образ 1,4 ГБ, и каждая раскатка даёт всплеск 502-х. Прочитайте лог сборки и улики изнутри контейнера; определите, что раздувает образ и почему запросы умирают на деплое."
      },
      "scenario": {
        "en": "FROM python:3.12                  # full image 'because builds failed on slim'\nWORKDIR /app\nCOPY . .\nRUN pip install -r requirements.txt\nCMD gunicorn app.main:app",
        "ru": "# Полная база «потому что на slim сборка падала», COPY всего, shell-форма CMD\nFROM python:3.12                  # полный образ «потому что на slim сборка падала»\nWORKDIR /app\nCOPY . .\nRUN pip install -r requirements.txt\nCMD gunicorn app.main:app"
      },
      "evidence": {
        "en": "$ docker history app:latest | head -4\nCOPY . .                          412MB     # context includes .git (290MB) + .venv (110MB)\nFROM python:3.12                  1.01GB\n\n$ kubectl exec pod -- ps -o pid,comm\n  PID COMMAND\n    1 sh                          # PID 1 is a shell\n    7 gunicorn\n\n# rollout events: SIGTERM at t+0, pod killed at t+30s (grace period), 502 burst at t+30s",
        "ru": "$ docker history app:latest | head -4\nCOPY . .                          412MB     # контекст включает .git (290 МБ) + .venv (110 МБ)\nFROM python:3.12                  1.01GB\n\n$ kubectl exec pod -- ps -o pid,comm\n  PID COMMAND\n    1 sh                          # PID 1 — шелл\n    7 gunicorn\n\n# события раскатки: SIGTERM в t+0, под убит в t+30с (период ожидания), всплеск 502 в t+30с"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "context-bloat",
            "accept": [
              "no .dockerignore so COPY . . drags .git and the host .venv into the image",
              "the build context includes .git and .venv",
              "412MB COPY layer comes from repo junk that needs a .dockerignore",
              "нет .dockerignore, и COPY . . тащит .git и хостовый .venv в образ",
              "контекст сборки включает .git и .venv",
              "слой COPY на 412 МБ — это мусор репозитория, нужен .dockerignore"
            ],
            "hint": {
              "en": "docker history attributes 412MB to one COPY. What two directories does the annotation name, and which file would have excluded them?",
              "ru": "docker history приписывает 412 МБ одному COPY. Какие две директории названы в аннотации и какой файл бы их исключил?"
            }
          },
          {
            "id": "full-base",
            "accept": [
              "the full python:3.12 base ships a gigabyte of compilers; slim plus a multi-stage builder fixes both size and the slim build failure",
              "use python:3.12-slim with a builder stage instead of the full image",
              "multi-stage would let slim work because compilers stay in the builder",
              "полная база python:3.12 везёт гигабайт компиляторов; slim плюс multi-stage билдер чинит и размер, и падение сборки на slim",
              "использовать python:3.12-slim со стадией билдера вместо полного образа",
              "multi-stage позволил бы slim работать, потому что компиляторы остаются в билдере"
            ],
            "hint": {
              "en": "The full base was chosen because slim could not compile a wheel. Which build layout keeps compilers available at build time but out of the shipped image?",
              "ru": "Полную базу выбрали, потому что slim не мог собрать колесо. Какая раскладка сборки оставляет компиляторы на этапе сборки, но не в итоговом образе?"
            }
          },
          {
            "id": "pid1-shell",
            "accept": [
              "PID 1 is sh because of shell-form CMD so SIGTERM is never forwarded and SIGKILL lands mid-request",
              "shell-form CMD means gunicorn never receives SIGTERM",
              "exec-form CMD would put gunicorn at PID 1 and let it drain",
              "PID 1 — это sh из-за shell-формы CMD, SIGTERM не пересылается, и SIGKILL прилетает посреди запросов",
              "shell-форма CMD означает, что gunicorn никогда не получает SIGTERM",
              "exec-форма CMD поставила бы gunicorn в PID 1 и дала бы додренировать"
            ],
            "hint": {
              "en": "ps shows PID 1 as sh and the 502 burst lands exactly at the grace-period boundary. Who received the SIGTERM, and who never did?",
              "ru": "ps показывает PID 1 как sh, а всплеск 502 приходится ровно на границу периода ожидания. Кто получил SIGTERM и кто его так и не увидел?"
            }
          }
        ]
      }
    },
    {
      "id": "uv-multistage-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Design: a production Dockerfile for a FastAPI service with compiled deps",
        "ru": "Спроектируйте: продакшен-Dockerfile для FastAPI-сервиса с компилируемыми зависимостями"
      },
      "prompt": {
        "en": "Design the production image for a FastAPI service whose dependency tree includes one package that compiles a C extension. Write the Dockerfile shape (stages, ordering, flags, user, CMD) and justify each choice against the constraints.",
        "ru": "Спроектируйте продакшен-образ для FastAPI-сервиса, в чьём дереве зависимостей есть один пакет, компилирующий C-расширение. Напишите форму Dockerfile (стадии, порядок, флаги, пользователь, CMD) и обоснуйте каждый выбор относительно ограничений."
      },
      "constraints": {
        "en": "Final image under 200MB; no compilers or package-manager caches in it. CI rebuilds on every commit: a code-only change must not reinstall dependencies. The cluster enforces nonroot images and sends SIGTERM with a 30s grace period. Logs must stream immediately. Dependencies are locked with uv (uv.lock committed). Readiness must reflect real dependency health, not process existence.",
        "ru": "Финальный образ меньше 200 МБ; в нём нет компиляторов и кешей пакетного менеджера. CI пересобирает на каждый коммит: правка только кода не должна переустанавливать зависимости. Кластер требует nonroot-образы и шлёт SIGTERM с периодом ожидания 30 с. Логи должны течь немедленно. Зависимости зафиксированы uv (uv.lock в репозитории). Readiness обязан отражать реальное здоровье зависимостей, а не существование процесса."
      },
      "rubric": [
        {
          "en": "Two stages: builder installs uv and any compilers, runs uv sync --frozen into a venv; final stage starts from python:3.12-slim and copies only the venv and source",
          "ru": "Две стадии: билдер ставит uv и нужные компиляторы, выполняет uv sync --frozen в venv; финальная стадия стартует с python:3.12-slim и копирует только venv и исходники"
        },
        {
          "en": "Cache-ordered layers: COPY pyproject.toml uv.lock then uv sync BEFORE COPY src/ — with the stated mechanism that everything after the first changed layer rebuilds",
          "ru": "Слои под кеш: COPY pyproject.toml uv.lock, затем uv sync ДО COPY src/ — с проговорённым механизмом: всё после первого изменившегося слоя пересобирается"
        },
        {
          "en": "Runtime env set: PYTHONUNBUFFERED=1 for immediate log streaming, PATH pointing into the venv; .dockerignore excludes .git, .venv, __pycache__",
          "ru": "Выставлено рантайм-окружение: PYTHONUNBUFFERED=1 для немедленных логов, PATH указывает в venv; .dockerignore исключает .git, .venv, __pycache__"
        },
        {
          "en": "Nonroot USER and exec-form CMD, with the signal path explained: gunicorn at PID 1 receives SIGTERM and drains within the 30s grace period",
          "ru": "Nonroot USER и exec-форма CMD с объяснением пути сигнала: gunicorn в PID 1 получает SIGTERM и дренируется в 30-секундный период ожидания"
        },
        {
          "en": "Readiness wired to an endpoint that checks the DB pool and downstream clients — noting k8s ignores Dockerfile HEALTHCHECK, so the probe lives in the pod spec",
          "ru": "Readiness подключён к эндпоинту, проверяющему пул БД и downstream-клиентов, — с пометкой, что k8s игнорирует Dockerfile HEALTHCHECK и проба живёт в спеке пода"
        }
      ],
      "model": {
        "en": "# .dockerignore: .git, .venv, __pycache__, tests/, dist/\n\nFROM python:3.12-slim AS builder\nRUN apt-get update && apt-get install -y --no-install-recommends gcc libpq-dev\nCOPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv\nWORKDIR /app\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev --no-install-project    # slow layer, cached by lockfile\nCOPY src/ src/\nRUN uv sync --frozen --no-dev\n\nFROM python:3.12-slim\nENV PYTHONUNBUFFERED=1 PATH=\"/app/.venv/bin:$PATH\"\nRUN useradd --create-home appuser\nWORKDIR /app\nCOPY --from=builder /app/.venv .venv/\nCOPY src/ src/\nUSER appuser\nCMD [\"gunicorn\", \"-c\", \"gunicorn.conf.py\", \"app.main:app\"]\n\nWhy it satisfies each constraint: the C extension compiles in the builder (gcc lives and dies there); the final stage copies a finished venv onto clean slim — no toolchain, no apt or uv caches, ~120-150MB. Lockfile-first ordering means a code commit re-runs only the final COPY: the uv sync layer is keyed on pyproject+uv.lock checksums and survives. PYTHONUNBUFFERED=1 makes stdout write-through so logs stream and crash tails survive. appuser satisfies the nonroot admission policy. Exec-form CMD makes gunicorn PID 1: SIGTERM arrives directly, the master stops accepting and drains workers inside the 30s grace period — no SIGKILL corpses. Readiness: the pod spec points readinessProbe at /ready, the lifespan-backed endpoint that pings the DB pool; Dockerfile HEALTHCHECK is omitted because Kubernetes ignores it.",
        "ru": "# .dockerignore: .git, .venv, __pycache__, tests/, dist/\n\nFROM python:3.12-slim AS builder\nRUN apt-get update && apt-get install -y --no-install-recommends gcc libpq-dev\nCOPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv\nWORKDIR /app\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev --no-install-project    # медленный слой, кешируется lock-файлом\nCOPY src/ src/\nRUN uv sync --frozen --no-dev\n\nFROM python:3.12-slim\nENV PYTHONUNBUFFERED=1 PATH=\"/app/.venv/bin:$PATH\"\nRUN useradd --create-home appuser\nWORKDIR /app\nCOPY --from=builder /app/.venv .venv/\nCOPY src/ src/\nUSER appuser\nCMD [\"gunicorn\", \"-c\", \"gunicorn.conf.py\", \"app.main:app\"]\n\nПочему это закрывает каждое ограничение: C-расширение компилируется в билдере (gcc живёт и умирает там); финальная стадия копирует готовый venv на чистый slim — без тулчейна, без кешей apt и uv, ~120–150 МБ. Порядок «lock-файл первым» означает, что коммит с кодом перезапускает только финальный COPY: слой uv sync ключуется чексуммами pyproject+uv.lock и выживает. PYTHONUNBUFFERED=1 делает stdout сквозным — логи текут сразу, и хвост при падении не теряется. appuser удовлетворяет nonroot-политику admission. Exec-форма CMD ставит gunicorn в PID 1: SIGTERM приходит напрямую, мастер прекращает приём и дренирует воркеров внутри 30-секундного периода — без трупов от SIGKILL. Readiness: спека пода направляет readinessProbe на /ready — эндпоинт на lifespan, пингующий пул БД; HEALTHCHECK в Dockerfile опущен, потому что Kubernetes его игнорирует."
      }
    }
  ]
}
