{
  "lessonKey": "python/12-production-python/03-workers-and-lifecycle",
  "track": "python",
  "tasks": [
    {
      "id": "preload-fork-predict",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Predict: what fork duplicates, and what eight workers now share",
        "ru": "Предскажите: что дублирует fork и что теперь делят восемь воркеров"
      },
      "prompt": {
        "en": "gunicorn starts with this config and module. For each marked point, predict the runtime behavior across 8 workers: what is shared, what is copied, and what the user-visible symptom is.",
        "ru": "gunicorn стартует с этим конфигом и модулем. Для каждой помеченной точки предскажите поведение в рантайме на 8 воркерах: что общее, что скопировано и каков видимый пользователю симптом."
      },
      "scenario": {
        "en": "# gunicorn.conf.py\nworkers = 8\npreload_app = True\nmax_requests = 5_000          # point 3 — no jitter set\n\n# app/state.py (imported at startup)\nimport random, redis\n\nrandom.seed()                  # point 2 — seeded at import\ncache = redis.Redis()          # point 1 — connects at import\n\ndef session_for(user_id):\n    return cache.get(f\"session:{user_id}\")",
        "ru": "# Конфиг с preload и модуль, создающий клиента и сид на этапе импорта\n# gunicorn.conf.py\nworkers = 8\npreload_app = True\nmax_requests = 5_000          # точка 3 — jitter не задан\n\n# app/state.py (импортируется на старте)\nimport random, redis\n\nrandom.seed()                  # точка 2 — сид на импорте\ncache = redis.Redis()          # точка 1 — подключается на импорте\n\ndef session_for(user_id):\n    return cache.get(f\"session:{user_id}\")"
      },
      "reveal": {
        "en": "Point 1: the Redis connection opens in the master, before fork; the socket fd is duplicated into all 8 workers — one shared TCP stream. Under concurrency, commands from different workers interleave and each worker reads whichever reply is next: users intermittently get each other's sessions, or the driver throws protocol errors. Point 2: random's state is seeded once pre-fork and copied — all 8 workers produce the IDENTICAL 'random' sequence until something reseeds, which corrupts anything relying on uniqueness (tokens, sampling, jitter). Point 3: with max_requests but no jitter, all workers that booted together hit 5,000 requests around the same time and recycle in the same window — a synchronized capacity dip every few hours. Fixes: init clients in post_fork (or lazily), reseed per worker, set max_requests_jitter.",
        "ru": "Точка 1: соединение с Redis открывается в мастере, до форка; fd сокета дублируется во все 8 воркеров — один общий TCP-поток. Под конкурентностью команды разных воркеров перемешиваются, и каждый читает тот ответ, что оказался следующим: пользователи изредка получают чужие сессии, либо драйвер кидает протокольные ошибки. Точка 2: состояние random засеяно один раз до форка и скопировано — все 8 воркеров выдают ИДЕНТИЧНУЮ «случайную» последовательность, пока что-то не пересеет, что ломает всё, полагающееся на уникальность (токены, сэмплинг, джиттер). Точка 3: max_requests без джиттера — стартовавшие вместе воркеры достигают 5 000 запросов примерно одновременно и рециклятся в одном окне: синхронная просадка ёмкости каждые несколько часов. Фиксы: клиенты в post_fork (или лениво), пересев на воркер, задать max_requests_jitter."
      }
    },
    {
      "id": "deploy-drain-mismatch-diagnose",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: 502s at the 10-second mark of every rollout",
        "ru": "Диагностируйте: 502-е на 10-й секунде каждой раскатки"
      },
      "prompt": {
        "en": "Every deploy produces a burst of 502s exactly 10 seconds after the rollout starts. The CMD is exec-form, signals arrive fine. Read the configs and the timeline; find the three timing/routing mistakes in the shutdown contract.",
        "ru": "Каждый деплой даёт всплеск 502-х ровно через 10 секунд после начала раскатки. CMD в exec-форме, сигналы доходят нормально. Прочитайте конфиги и таймлайн; найдите три ошибки тайминга/маршрутизации в контракте выключения."
      },
      "scenario": {
        "en": "# gunicorn.conf.py\nworkers = 4\nworker_class = \"uvicorn_worker.UvicornWorker\"\ngraceful_timeout = 30          # long requests: report generation up to 25s\n\n# deployment.yaml (trimmed)\nspec:\n  terminationGracePeriodSeconds: 10\n  containers:\n    - name: app\n      # no preStop hook\n      readinessProbe:\n        httpGet: {path: /health, port: 8000}   # returns 200 unconditionally",
        "ru": "# Конфиг gunicorn и спека деплоймента с коротким периодом ожидания\n# gunicorn.conf.py\nworkers = 4\nworker_class = \"uvicorn_worker.UvicornWorker\"\ngraceful_timeout = 30          # длинные запросы: генерация отчёта до 25 с\n\n# deployment.yaml (сокращено)\nspec:\n  terminationGracePeriodSeconds: 10\n  containers:\n    - name: app\n      # нет preStop-хука\n      readinessProbe:\n        httpGet: {path: /health, port: 8000}   # безусловно отвечает 200"
      },
      "evidence": {
        "en": "# rollout timeline, observed\nt+0.0s  pod marked Terminating; SIGTERM delivered to gunicorn (exec form OK)\nt+0.2s  gunicorn: 'Handling signal: term', workers stop accepting\nt+1.5s  502s begin: ingress still routes NEW requests to the pod for ~2s\nt+10s   SIGKILL; 3 report-generation requests (15-25s long) die mid-flight\n# grafana: drain completed on idle pods in 0.4s; pods with reports never finish",
        "ru": "# наблюдаемый таймлайн раскатки\nt+0.0с  под помечен Terminating; SIGTERM доставлен gunicorn (exec-форма ок)\nt+0.2с  gunicorn: 'Handling signal: term', воркеры прекращают приём\nt+1.5с  начинаются 502-е: ingress ещё ~2 с маршрутизирует НОВЫЕ запросы в под\nt+10с   SIGKILL; 3 запроса генерации отчётов (длиной 15–25 с) умирают в полёте\n# grafana: на простаивающих подах дренаж завершается за 0,4 с; поды с отчётами не успевают никогда"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "grace-mismatch",
            "accept": [
              "graceful_timeout 30 exceeds terminationGracePeriodSeconds 10 so k8s SIGKILLs mid-drain",
              "the platform deadline is shorter than the drain budget",
              "terminationGracePeriodSeconds must exceed preStop plus graceful_timeout",
              "graceful_timeout 30 больше terminationGracePeriodSeconds 10, и k8s шлёт SIGKILL посреди дренажа",
              "дедлайн платформы короче бюджета дренажа",
              "terminationGracePeriodSeconds должен превышать preStop плюс graceful_timeout"
            ],
            "hint": {
              "en": "Compare the two timeout numbers and the t+10s event. Which clock fired first, and which system owns it?",
              "ru": "Сравните два таймаута и событие t+10с. Какие часы сработали первыми и какой системе они принадлежат?"
            }
          },
          {
            "id": "routing-window",
            "accept": [
              "no preStop sleep so the ingress keeps routing new requests after workers stopped accepting",
              "endpoint removal propagates slower than SIGTERM, causing the t+1.5s 502s",
              "a preStop sleep covers the readiness propagation gap",
              "нет preStop-sleep, и ingress продолжает слать новые запросы после прекращения приёма",
              "изъятие эндпоинта распространяется медленнее SIGTERM, отсюда 502-е на t+1,5с",
              "preStop-sleep покрывает лаг распространения readiness"
            ],
            "hint": {
              "en": "502s start at t+1.5s, before any kill. Who was still sending traffic to a pod that had stopped accepting, and why?",
              "ru": "502-е начинаются на t+1,5с, до всякого убийства. Кто всё ещё слал трафик в под, прекративший приём, и почему?"
            }
          },
          {
            "id": "fake-readiness",
            "accept": [
              "the readiness probe returns 200 unconditionally so it cannot reflect shutdown or dependency state",
              "readiness is wired to a stub instead of a real health endpoint",
              "an unconditional /health defeats readiness-off-first",
              "readiness-проба безусловно отвечает 200 и не отражает ни выключение, ни состояние зависимостей",
              "readiness подключён к заглушке вместо настоящего эндпоинта здоровья",
              "безусловный /health обесценивает readiness-off-first"
            ],
            "hint": {
              "en": "Look at what /health actually checks. Can the pod ever signal 'stop routing to me' through this probe?",
              "ru": "Посмотрите, что реально проверяет /health. Может ли под через эту пробу хоть когда-нибудь сказать «не шлите мне трафик»?"
            }
          }
        ]
      }
    },
    {
      "id": "worker-topology-design",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: worker topology for a mixed CPU and I/O service",
        "ru": "Спроектируйте: топологию воркеров для смешанного CPU+I/O сервиса"
      },
      "prompt": {
        "en": "A FastAPI service runs on 4-core pods: 90 percent of requests are async I/O (DB, HTTP fan-out, p50 40ms), 10 percent are CPU-heavy PDF rendering (2-4s). Memory grows ~30MB/h per worker (unfound leak). Startup imports 800MB of shared reference data. Design the full gunicorn config and process topology; justify every number.",
        "ru": "FastAPI-сервис на подах с 4 ядрами: 90 процентов запросов — async I/O (БД, HTTP-фанаут, p50 40 мс), 10 процентов — тяжёлый по CPU рендеринг PDF (2–4 с). Память растёт ~30 МБ/ч на воркер (ненайденная утечка). Старт импортирует 800 МБ общих справочных данных. Спроектируйте полный конфиг gunicorn и процессную топологию; обоснуйте каждое число."
      },
      "constraints": {
        "en": "Pod: 4 cores, 6GB RAM. The 800MB reference data is read-only after load. PDF rendering must not freeze I/O traffic. k8s grace period is 30s; longest I/O request 5s. The leak will not be fixed this quarter. All clients (DB pool, Redis) currently constructed at module import.",
        "ru": "Под: 4 ядра, 6 ГБ RAM. 800 МБ справочных данных read-only после загрузки. Рендеринг PDF не должен замораживать I/O-трафик. Период ожидания k8s — 30 с; самый длинный I/O-запрос — 5 с. Утечку в этом квартале не починят. Все клиенты (пул БД, Redis) сейчас конструируются на импорте модуля."
      },
      "rubric": [
        {
          "en": "Uses preload_app=True to share the 800MB read-only data copy-on-write across workers, with the explicit fork-safety consequence: move client construction to post_fork or lazy init",
          "ru": "Использует preload_app=True, чтобы делить 800 МБ read-only данных copy-on-write между воркерами, с явным следствием fork-безопасности: перенести конструирование клиентов в post_fork или в ленивую инициализацию"
        },
        {
          "en": "Separates the CPU work from the loop: PDF rendering goes to a process pool or a dedicated sync-worker deployment, because a 2-4s CPU burn on a uvicorn worker freezes every coroutine on it",
          "ru": "Отделяет CPU-работу от цикла: рендеринг PDF уходит в пул процессов или отдельный деплоймент с sync-воркерами, потому что 2–4 с CPU на uvicorn-воркере замораживают все его корутины"
        },
        {
          "en": "Worker count argued from the GIL: ~4-8 uvicorn workers for the I/O tier on 4 cores, with the reasoning that loops multiplex I/O and extra workers add cores and isolation, not concurrency",
          "ru": "Число воркеров обосновано через GIL: ~4–8 uvicorn-воркеров для I/O-яруса на 4 ядрах, с аргументом, что циклы мультиплексируют I/O, а лишние воркеры добавляют ядра и изоляцию, не конкурентность"
        },
        {
          "en": "max_requests with jitter sized against the 30MB/h leak and 6GB budget, framed explicitly as a mitigation while tracemalloc work is scheduled",
          "ru": "max_requests с джиттером, подобранный под утечку 30 МБ/ч и бюджет 6 ГБ, явно поданный как смягчение, пока запланирована работа с tracemalloc"
        },
        {
          "en": "Shutdown budget closes: graceful_timeout above the 5s longest request but, with preStop, safely under the 30s grace period",
          "ru": "Бюджет выключения сходится: graceful_timeout больше самого длинного запроса в 5 с, но вместе с preStop с запасом меньше 30-секундного периода ожидания"
        }
      ],
      "model": {
        "en": "# gunicorn.conf.py — I/O tier\nworkers = 6                          # 4 cores, async I/O: 1.5x cores; loops multiplex, workers add isolation\nworker_class = \"uvicorn_worker.UvicornWorker\"\npreload_app = True                   # 800MB reference data loads once, shared CoW: ~800MB total, not 4.8GB\nmax_requests = 8_000                 # at ~30MB/h leak per worker, recycle well before RSS matters on a 6GB pod\nmax_requests_jitter = 800            # never recycle 6 workers in the same window\ngraceful_timeout = 15                # > 5s longest request, << 30s grace period\ntimeout = 30\n\ndef post_fork(server, worker):\n    from app import clients\n    clients.init()                   # DB pool + Redis born per worker — module-level init removed\n\n# CPU work: handlers submit PDF jobs to a ProcessPoolExecutor(max_workers=2)\n# created post-fork (or a separate sync-worker deployment if volume grows):\n# a 2-4s render on the loop would freeze hundreds of in-flight coroutines.\n# k8s: preStop sleep 5s; terminationGracePeriodSeconds stays 30 (5+15 fits with margin).\n\nWhy it holds: preload turns the 800MB into one shared copy — the single biggest RAM lever — but it forces the fork-safety migration: every client moves from import time to post_fork, or eight processes share sockets. Six uvicorn workers use all four cores with headroom for the executor processes; more workers would buy context switches. The process pool isolates CPU burns from the loop so PDF jobs cannot freeze I/O p99. max_requests=8000 with jitter is named a mitigation in the config comment and a tracemalloc task is filed — the recycle hides the leak from users, not from the team. Shutdown: 5s preStop covers endpoint propagation, 15s drain covers the 5s worst case, total 20s under the 30s deadline.",
        "ru": "# gunicorn.conf.py — I/O-ярус\nworkers = 6                          # 4 ядра, async I/O: 1.5 на ядро; циклы мультиплексируют, воркеры дают изоляцию\nworker_class = \"uvicorn_worker.UvicornWorker\"\npreload_app = True                   # 800 МБ справочника грузятся один раз, общие CoW: ~800 МБ всего, а не 4,8 ГБ\nmax_requests = 8_000                 # при утечке ~30 МБ/ч на воркер рециклим задолго до того, как RSS станет проблемой на 6 ГБ\nmax_requests_jitter = 800            # никогда не рециклить 6 воркеров в одном окне\ngraceful_timeout = 15                # > 5 с самого длинного запроса, << 30 с периода ожидания\ntimeout = 30\n\ndef post_fork(server, worker):\n    from app import clients\n    clients.init()                   # пул БД + Redis рождаются на воркер — модульная инициализация удалена\n\n# CPU-работа: обработчики отправляют PDF-задачи в ProcessPoolExecutor(max_workers=2),\n# созданный после форка (или в отдельный деплоймент с sync-воркерами при росте объёма):\n# рендер 2–4 с на цикле заморозил бы сотни корутин в полёте.\n# k8s: preStop sleep 5 с; terminationGracePeriodSeconds остаётся 30 (5+15 умещается с запасом).\n\nПочему это сходится: preload превращает 800 МБ в одну общую копию — самый крупный рычаг по памяти, — но вынуждает миграцию fork-безопасности: каждый клиент переезжает с импорта в post_fork, иначе восемь процессов делят сокеты. Шесть uvicorn-воркеров загружают все четыре ядра с запасом под процессы экзекутора; больше воркеров купило бы переключения контекста. Пул процессов изолирует CPU-нагрузку от цикла: PDF-задачи не могут заморозить p99 I/O-трафика. max_requests=8000 с джиттером назван смягчением прямо в комментарии конфига, и задача на tracemalloc заведена — рециклинг прячет утечку от пользователей, но не от команды. Выключение: preStop 5 с покрывает распространение эндпоинта, дренаж 15 с покрывает худшие 5 с, итого 20 с при дедлайне 30 с."
      }
    }
  ]
}
