{
  "lessonKey": "typescript/08-real-world/02-zod-and-validation",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-infer-output",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": {
        "en": "Infer the type through a transform",
        "ru": "Выведите тип сквозь трансформ"
      },
      "prompt": {
        "en": "Read the schema in the evidence. Fill in the blanks: the inferred type of `z.infer<typeof S>` and the inferred type of `z.input<typeof S>`.",
        "ru": "Прочитайте схему в evidence. Заполните пропуски: выведенный тип `z.infer<typeof S>` и выведенный тип `z.input<typeof S>`."
      },
      "evidence": {
        "en": "const S = z.object({\n  createdAt: z.string().transform((s) => new Date(s)),\n});\ntype Out = z.infer<typeof S>;   // { createdAt: ___ }  (b1)\ntype In  = z.input<typeof S>;   // { createdAt: ___ }  (b2)",
        "ru": "const S = z.object({\n  createdAt: z.string().transform((s) => new Date(s)),\n});\ntype Out = z.infer<typeof S>;   // { createdAt: ___ }  (b1)\ntype In  = z.input<typeof S>;   // { createdAt: ___ }  (b2)"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["date"],
            "hint": {
              "en": "z.infer equals the OUTPUT type — the value after the transform runs.",
              "ru": "z.infer равен ВЫХОДНОМУ типу — значению после выполнения трансформа."
            }
          },
          {
            "id": "b2",
            "accept": ["string"],
            "hint": {
              "en": "z.input is the shape before the transform — what .parse accepts.",
              "ru": "z.input — форма до трансформа — то, что принимает .parse."
            }
          }
        ]
      }
    },
    {
      "id": "predict-safeparse-shape",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": {
        "en": "What does safeParse hand back?",
        "ru": "Что возвращает safeParse?"
      },
      "prompt": {
        "en": "Predict the exact shape of the value returned by safeParse and how you reach the validated data, then reveal.",
        "ru": "Предскажите точную форму значения, которое возвращает safeParse, и как добраться до провалидированных данных, затем откройте."
      },
      "scenario": {
        "en": "const User = z.object({ id: z.number() }); const r = User.safeParse(json). You want to log r.error on failure and use r.data on success. What is the type of r, and what guard must you write first?",
        "ru": "const User = z.object({ id: z.number() }); const r = User.safeParse(json). Вы хотите логировать r.error при отказе и использовать r.data при успехе. Каков тип r и какой страж нужно написать сначала?"
      },
      "reveal": {
        "en": "r is the discriminated union { success: true; data: { id: number } } | { success: false; error: ZodError }. You must narrow on r.success first: inside if (r.success) only r.data is available (typed { id: number }); inside the else only r.error (ZodError) is available. Accessing r.data without the guard is a type error — the success flag is the discriminant that earns the validated value, exactly mirroring lesson-1 discriminated unions but now backed by a real runtime check.",
        "ru": "r — это размеченное объединение { success: true; data: { id: number } } | { success: false; error: ZodError }. Сначала надо сузить по r.success: внутри if (r.success) доступен только r.data (тип { id: number }); внутри else — только r.error (ZodError). Доступ к r.data без стража — ошибка типа: флаг success и есть дискриминант, зарабатывающий провалидированное значение, ровно как размеченные объединения из урока 1, но теперь подкреплённый реальной рантайм-проверкой."
      }
    },
    {
      "id": "fix-double-source",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 8,
      "title": {
        "en": "Kill the drifting double definition",
        "ru": "Убейте расходящееся двойное определение"
      },
      "prompt": {
        "en": "The code hand-writes both an interface and a zod schema. A teammate added `phone` to the interface but not the schema — now the type claims a field the runtime never validates. Refactor so there is a single source of truth, and explain why drift becomes impossible. Then reveal the model answer.",
        "ru": "Код пишет руками и интерфейс, и zod-схему. Коллега добавил `phone` в интерфейс, но не в схему — теперь тип заявляет поле, которое рантайм не валидирует. Отрефакторите так, чтобы был единый источник истины, и объясните, почему расхождение становится невозможным. Затем откройте образцовый ответ."
      },
      "starter": "interface User { id: number; name: string; phone: string }\nconst UserSchema = z.object({ id: z.number(), name: z.string() });\nfunction load(json: unknown): User {\n  return UserSchema.parse(json) as User; // schema lacks phone; cast hides it\n}",
      "grading": {
        "mode": "self",
        "model": {
          "en": "Delete the hand-written interface and derive the type from the schema: add the field once to the schema, then <code>type User = z.infer&lt;typeof UserSchema&gt;</code>. Drop the <code>as User</code> cast entirely — <code>UserSchema.parse(json)</code> already returns <code>User</code>. So: <code>const UserSchema = z.object({ id: z.number(), name: z.string(), phone: z.string() }); type User = z.infer&lt;typeof UserSchema&gt;; function load(json: unknown) { return UserSchema.parse(json); }</code>. Drift becomes impossible because the type and the runtime check now have ONE origin: you cannot add a field to the type without adding it to the check, since the type IS the check's inferred output. The <code>as User</code> was masking the mismatch — removing it would have surfaced the original bug as a compile error.",
          "ru": "Удалите рукописный интерфейс и выведите тип из схемы: добавьте поле один раз в схему, затем <code>type User = z.infer&lt;typeof UserSchema&gt;</code>. Полностью уберите каст <code>as User</code> — <code>UserSchema.parse(json)</code> уже возвращает <code>User</code>. То есть: <code>const UserSchema = z.object({ id: z.number(), name: z.string(), phone: z.string() }); type User = z.infer&lt;typeof UserSchema&gt;; function load(json: unknown) { return UserSchema.parse(json); }</code>. Расхождение становится невозможным, потому что у типа и рантайм-проверки теперь ОДИН исток: нельзя добавить поле в тип, не добавив его в проверку, ведь тип — это и есть выведенный выход проверки. Каст <code>as User</code> маскировал несовпадение — его удаление вскрыло бы исходный баг как ошибку компиляции."
        },
        "rubric": [
          {
            "en": "Derives the type via z.infer<typeof Schema> and deletes the hand-written interface.",
            "ru": "Выводит тип через z.infer<typeof Schema> и удаляет рукописный интерфейс."
          },
          {
            "en": "Removes the `as User` cast since parse already returns the inferred type.",
            "ru": "Убирает каст `as User`, так как parse уже возвращает выведенный тип."
          },
          {
            "en": "Explains that a single origin makes type/runtime drift structurally impossible.",
            "ru": "Объясняет, что единый исток делает расхождение тип/рантайм структурно невозможным."
          }
        ]
      }
    }
  ]
}
