{
  "lessonKey": "typescript/08-real-world/05-common-pitfalls",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-object-keys",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": {
        "en": "What does Object.keys give you?",
        "ru": "Что вам даёт Object.keys?"
      },
      "prompt": {
        "en": "Read the loop in the evidence. Fill in the blanks: the inferred type of `k`, and the one-word reason the indexing line is an error.",
        "ru": "Прочитайте цикл в evidence. Заполните пропуски: выведенный тип `k` и одно слово — причину, почему строка индексации — ошибка."
      },
      "evidence": {
        "en": "const o = { a: 1, b: 2 };\nfor (const k of Object.keys(o)) {\n  // k has type: ___ (b1)\n  const v = o[k]; // Error: implicit any — k is not a ___ (b2) of o\n}",
        "ru": "const o = { a: 1, b: 2 };\nfor (const k of Object.keys(o)) {\n  // k has type: ___ (b1)\n  const v = o[k]; // Error: implicit any — k is not a ___ (b2) of o\n}"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": ["string"],
            "hint": {
              "en": "Object.keys is typed broadly because extra runtime keys could exist.",
              "ru": "Object.keys типизирован широко, потому что в рантайме могут быть лишние ключи."
            }
          },
          {
            "id": "b2",
            "accept": ["keyof", "key"],
            "hint": {
              "en": "Indexing o needs a type that is provably one of o's keys.",
              "ru": "Индексация o требует типа, доказуемо являющегося одним из ключей o."
            }
          }
        ]
      }
    },
    {
      "id": "predict-covariance-crash",
      "type": "predict",
      "difficulty": "stretch",
      "estMin": 6,
      "title": {
        "en": "Which line crashes, and which compiled?",
        "ru": "Какая строка падает и какая скомпилировалась?"
      },
      "prompt": {
        "en": "Predict which lines compile and which one crashes at runtime, then reveal.",
        "ru": "Предскажите, какие строки компилируются и какая падает в рантайме, затем откройте."
      },
      "scenario": {
        "en": "class Animal {} class Dog extends Animal { bark() {} } class Cat extends Animal {}. const dogs: Dog[] = [new Dog()]; const animals: Animal[] = dogs; animals.push(new Cat()); dogs[1].bark(). Which of these four statements compile, and which throws at runtime, and why?",
        "ru": "class Animal {} class Dog extends Animal { bark() {} } class Cat extends Animal {}. const dogs: Dog[] = [new Dog()]; const animals: Animal[] = dogs; animals.push(new Cat()); dogs[1].bark(). Какие из этих четырёх операторов компилируются, какой бросает в рантайме и почему?"
      },
      "reveal": {
        "en": "All four compile. `const animals: Animal[] = dogs` is allowed by array covariance (Dog[] is assignable to Animal[]). `animals.push(new Cat())` compiles because animals is typed Animal[] and Cat is an Animal — but animals and dogs are the SAME array, so it now holds a Cat at index 1. `dogs[1].bark()` is typed Dog so .bark() is allowed, but the runtime value is a Cat with no bark method, so this line THROWS. The unsoundness is intentional (covariance trades soundness for ergonomics) and is only triggered by mutating through the widened reference; accepting readonly Animal[] removes push and prevents it.",
        "ru": "Все четыре компилируются. `const animals: Animal[] = dogs` разрешён ковариантностью массивов (Dog[] присваивается Animal[]). `animals.push(new Cat())` компилируется, потому что animals имеет тип Animal[], а Cat — это Animal — но animals и dogs это ОДИН массив, так что он теперь держит Cat по индексу 1. `dogs[1].bark()` типизирован как Dog, так что .bark() разрешён, но рантайм-значение — Cat без метода bark, поэтому эта строка БРОСАЕТ. Несостоятельность намеренна (ковариантность разменивает состоятельность на эргономику) и срабатывает только при мутации через расширенную ссылку; приём readonly Animal[] убирает push и предотвращает это."
      }
    },
    {
      "id": "fix-structural-id-mixup",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 8,
      "title": {
        "en": "Stop the ID mix-up with branding",
        "ru": "Остановите путаницу ID брендингом"
      },
      "prompt": {
        "en": "The code aliases both IDs to string, so passing an OrderId where a UserId is wanted compiles — a real bug. Brand the two types so the mistake becomes a compile error, keep them plain strings at runtime, and explain why aliasing alone never caught it. Then reveal the model answer.",
        "ru": "Код задаёт псевдонимы обоих ID как string, так что передача OrderId туда, где ждут UserId, компилируется — реальный баг. Забрендируйте два типа так, чтобы ошибка стала ошибкой компиляции, оставьте их обычными строками в рантайме и объясните, почему один псевдоним никогда это не ловил. Затем откройте образцовый ответ."
      },
      "starter": "type UserId = string;\ntype OrderId = string;\nfunction getUser(id: UserId) { /* ... */ }\nconst orderId: OrderId = \"ord_1\";\ngetUser(orderId); // compiles — both are just string, the bug slips through",
      "grading": {
        "mode": "self",
        "model": {
          "en": "Brand each type with a phantom property that only exists in the type system: <code>type UserId = string & { readonly __brand: 'UserId' }</code> and <code>type OrderId = string & { readonly __brand: 'OrderId' }</code>. Now <code>getUser(orderId)</code> is a compile error — OrderId is not assignable to UserId because their __brand literals differ. At runtime nothing changes: the value is still a string; the __brand is never created. You mint a branded value with a deliberate cast at the trusted boundary where you know what the string is, e.g. <code>const id = row.user_id as UserId</code> right after a validated DB read. Plain aliasing never caught the bug because TypeScript is structural: two aliases of string ARE the same type, fully interchangeable. Branding deliberately breaks that structural identity to discriminate IDs that are physically the same primitive.",
          "ru": "Забрендируйте каждый тип фантомным свойством, существующим только в системе типов: <code>type UserId = string & { readonly __brand: 'UserId' }</code> и <code>type OrderId = string & { readonly __brand: 'OrderId' }</code>. Теперь <code>getUser(orderId)</code> — ошибка компиляции: OrderId не присваивается UserId, потому что их литералы __brand различаются. В рантайме ничего не меняется: значение всё ещё строка; __brand никогда не создаётся. Вы чеканите брендированное значение намеренным кастом на доверенной границе, где знаете, что это за строка, например <code>const id = row.user_id as UserId</code> сразу после провалидированного чтения из БД. Один псевдоним никогда не ловил баг, потому что TypeScript структурен: два псевдонима string — это ОДИН тип, полностью взаимозаменяемый. Брендинг намеренно ломает эту структурную идентичность, чтобы различать ID, физически являющиеся одним примитивом."
        },
        "rubric": [
          {
            "en": "Intersects each type with a distinct phantom brand property.",
            "ru": "Пересекает каждый тип с отдельным фантомным свойством-брендом."
          },
          {
            "en": "Notes the value stays a plain string at runtime and is minted via a deliberate cast at a trusted boundary.",
            "ru": "Отмечает, что значение остаётся обычной строкой в рантайме и чеканится намеренным кастом на доверенной границе."
          },
          {
            "en": "Explains structural typing made the two string aliases identical, so aliasing alone could not catch it.",
            "ru": "Объясняет, что структурная типизация сделала два псевдонима string идентичными, так что один псевдоним не мог это поймать."
          }
        ]
      }
    }
  ]
}
