{
  "lessonKey": "typescript/07-config-modules-build/01-tsconfig-strictness",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-which-flag",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Name the flag behind the error", "ru": "Назови флаг, стоящий за ошибкой" },
      "prompt": { "en": "The snippet compiled fine yesterday; today it errors. Fill in the blanks with the flag that started enforcing the check and the meta-flag that turned it on.", "ru": "Сниппет компилировался вчера; сегодня — ошибка. Заполни пропуски флагом, который начал применять проверку, и мета-флагом, который его включил." },
      "evidence": { "en": "// tsconfig changed: added \"strict\": true\nconst el = document.querySelector(\".btn\");\nel.addEventListener(\"click\", onClick);\n//   ^^ error TS18047: 'el' is possibly 'null'.", "ru": "// tsconfig changed: added \"strict\": true\nconst el = document.querySelector(\".btn\");\nel.addEventListener(\"click\", onClick);\n//   ^^ error TS18047: 'el' is possibly 'null'." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["strictnullchecks", "strict null checks", "strictNullChecks"], "hint": { "en": "querySelector returns Element | null — which flag separates null from the type?", "ru": "querySelector возвращает Element | null — какой флаг отделяет null от типа?" } },
          { "id": "b2", "accept": ["strict", "strict: true", "strict true"], "hint": { "en": "The single meta-flag that was added to tsconfig and switched on the family.", "ru": "Единственный мета-флаг, добавленный в tsconfig, который включил всё семейство." } }
        ]
      }
    },
    {
      "id": "predict-unchecked-index",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Predict the element type under the new flag", "ru": "Предскажи тип элемента при новом флаге" },
      "prompt": { "en": "A team just enabled noUncheckedIndexedAccess. Predict the type of the bound value and whether the call below still type-checks.", "ru": "Команда только что включила noUncheckedIndexedAccess. Предскажи тип связанного значения и пройдёт ли вызов ниже проверку типов." },
      "scenario": { "en": "const users: string[] = [\"ana\", \"bo\"]; const first = users[0]; first.toUpperCase(); — what is the type of first, and does the .toUpperCase() call compile?", "ru": "const users: string[] = [\"ana\", \"bo\"]; const first = users[0]; first.toUpperCase(); — каков тип first и компилируется ли вызов .toUpperCase()?" },
      "reveal": { "en": "<code>first</code> is <code>string | undefined</code>, not <code>string</code> — <code>noUncheckedIndexedAccess</code> widens every array/record index access with <code>| undefined</code> because the index could be out of bounds. So <code>first.toUpperCase()</code> is an error ('first' is possibly 'undefined'). You must narrow first: <code>if (first) first.toUpperCase()</code>, or <code>users.at(0)?.toUpperCase()</code>. The flag does not look at the literal length of the array.", "ru": "<code>first</code> — это <code>string | undefined</code>, а не <code>string</code>: <code>noUncheckedIndexedAccess</code> расширяет каждый доступ по индексу к массиву/record через <code>| undefined</code>, потому что индекс может выйти за границы. Поэтому <code>first.toUpperCase()</code> — ошибка ('first' is possibly 'undefined'). Сначала надо сузить: <code>if (first) first.toUpperCase()</code> или <code>users.at(0)?.toUpperCase()</code>. Флаг не смотрит на литеральную длину массива." }
    },
    {
      "id": "predict-exact-optional",
      "type": "predict",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Predict which assignment fails", "ru": "Предскажи, какое присваивание упадёт" },
      "prompt": { "en": "With exactOptionalPropertyTypes: true, predict which of the three assignments to an optional property errors and why.", "ru": "При exactOptionalPropertyTypes: true предскажи, какое из трёх присваиваний опциональному свойству даст ошибку и почему." },
      "scenario": { "en": "interface Opts { timeout?: number } — then: const a: Opts = {}; const b: Opts = { timeout: undefined }; const c: Opts = { timeout: 200 }; Which line fails to compile?", "ru": "interface Opts { timeout?: number } — затем: const a: Opts = {}; const b: Opts = { timeout: undefined }; const c: Opts = { timeout: 200 }; Какая строка не скомпилируется?" },
      "reveal": { "en": "Line <code>b</code> fails. Under <code>exactOptionalPropertyTypes</code>, <code>timeout?: number</code> means the property may be <em>absent</em>, not 'present and undefined'. So <code>{}</code> (a) and <code>{ timeout: 200 }</code> (c) are fine, but <code>{ timeout: undefined }</code> (b) explicitly writes <code>undefined</code> into the slot, which the exact-optional type forbids. This matters because <code>\"timeout\" in opts</code> / <code>Object.keys(opts)</code> would otherwise see a key that the type claimed could be missing.", "ru": "Падает строка <code>b</code>. При <code>exactOptionalPropertyTypes</code> <code>timeout?: number</code> означает, что свойство может <em>отсутствовать</em>, а не «есть и undefined». Поэтому <code>{}</code> (a) и <code>{ timeout: 200 }</code> (c) в порядке, но <code>{ timeout: undefined }</code> (b) явно пишет <code>undefined</code> в слот, что exact-optional тип запрещает. Это важно, потому что иначе <code>\"timeout\" in opts</code> / <code>Object.keys(opts)</code> увидели бы ключ, который тип объявлял возможно отсутствующим." }
    },
    {
      "id": "fix-migration-config",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Fix the all-at-once strictness migration", "ru": "Почини миграцию строгости «всё разом»" },
      "prompt": { "en": "This config tried to enable everything at once and the build has 1,400 errors, blocking all merges. Rewrite it into a staged migration, then reveal the model answer.", "ru": "Этот конфиг попытался включить всё разом, и в билде 1400 ошибок, блокирующих все мерджи. Перепиши его в поэтапную миграцию, затем открой образцовый ответ." },
      "starter": "{\n  \"compilerOptions\": {\n    \"strict\": true,\n    \"noUncheckedIndexedAccess\": true,\n    \"exactOptionalPropertyTypes\": true\n  }\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "Stage it so the build stays green at every step. Step 1: <code>\"strict\": true</code> plus <code>\"strictNullChecks\": false</code> — you adopt the cheap siblings (noImplicitAny, strictBindCallApply, etc.) with small local error counts while deferring the null wall. Step 2: drop the override, flip <code>strictNullChecks: true</code>, and fix the null/undefined errors directory-by-directory (or with `// @ts-expect-error` debt markers). Step 3 (separate PR each): add <code>noUncheckedIndexedAccess</code>, then <code>exactOptionalPropertyTypes</code> last — these surface the deepest latent bugs and need the most call-site work. Never land all three nullability-related flags in one commit.", "ru": "Раздели на этапы, чтобы билд оставался зелёным на каждом шаге. Шаг 1: <code>\"strict\": true</code> плюс <code>\"strictNullChecks\": false</code> — принимаешь дешёвых братьев (noImplicitAny, strictBindCallApply и т.д.) с малым числом локальных ошибок, откладывая стену null. Шаг 2: убери переопределение, включи <code>strictNullChecks: true</code> и чини ошибки null/undefined по каталогам (или маркерами долга `// @ts-expect-error`). Шаг 3 (каждый отдельным PR): добавь <code>noUncheckedIndexedAccess</code>, затем <code>exactOptionalPropertyTypes</code> последним — они обнажают самые глубокие скрытые баги и требуют больше всего работы по местам вызова. Никогда не вкатывай все три nullability-флага одним коммитом." },
        "rubric": [
          { "en": "Keeps the build green at each step (uses strictNullChecks: false override first).", "ru": "Держит билд зелёным на каждом шаге (сначала переопределение strictNullChecks: false)." },
          { "en": "Sequences the cheap siblings before the strictNullChecks wall.", "ru": "Ставит дешёвых братьев перед стеной strictNullChecks." },
          { "en": "Adds noUncheckedIndexedAccess and exactOptionalPropertyTypes last and separately.", "ru": "Добавляет noUncheckedIndexedAccess и exactOptionalPropertyTypes последними и по отдельности." }
        ]
      }
    }
  ]
}
