{
  "lessonKey": "typescript/07-config-modules-build/02-module-resolution",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-missing-extension",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Locate where resolution looks and what's missing", "ru": "Найди, куда смотрит разрешение и чего не хватает" },
      "prompt": { "en": "An ESM file fails at runtime though the editor was green. Fill in the blanks with the resolution mode to switch to and what the import must end with.", "ru": "ESM-файл падает в рантайме, хотя редактор был зелёным. Заполни пропуски режимом разрешения, на который надо перейти, и тем, чем должен оканчиваться импорт." },
      "evidence": { "en": "// package.json: { \"type\": \"module\" }\n// tsconfig: { \"moduleResolution\": \"node\" }\nimport { parse } from \"./parser\";\n// runtime: ERR_MODULE_NOT_FOUND Cannot find module './parser'", "ru": "// package.json: { \"type\": \"module\" }\n// tsconfig: { \"moduleResolution\": \"node\" }\nimport { parse } from \"./parser\";\n// runtime: ERR_MODULE_NOT_FOUND Cannot find module './parser'" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["nodenext", "node16", "node next", "node 16"], "hint": { "en": "The mode that enforces real Node ESM rules and reads exports.", "ru": "Режим, применяющий настоящие правила Node ESM и читающий exports." } },
          { "id": "b2", "accept": [".js", "js", "the .js extension", ".js extension", "./parser.js"], "hint": { "en": "Node ESM requires this on relative imports — write the output extension.", "ru": "Node ESM требует это на относительных импортах — пиши расширение результата." } }
        ]
      }
    },
    {
      "id": "diagnose-exports-encapsulation",
      "type": "diagnose",
      "difficulty": "stretch",
      "estMin": 5,
      "title": { "en": "Why does this subpath import fail?", "ru": "Почему этот subpath-импорт падает?" },
      "prompt": { "en": "Importing a file that physically exists fails to resolve under nodenext. Fill in the blanks with the package.json field that gates it and the one-word reason.", "ru": "Импорт физически существующего файла не разрешается при nodenext. Заполни пропуски полем package.json, которое его гейтит, и причиной одним словом." },
      "evidence": { "en": "// @acme/sdk/package.json exports: { \".\": \"./dist/index.js\" }\nimport { helper } from \"@acme/sdk/internal/util\";\n// error TS2307: Cannot find module '@acme/sdk/internal/util'\n// (the file dist/internal/util.js DOES exist on disk)", "ru": "// @acme/sdk/package.json exports: { \".\": \"./dist/index.js\" }\nimport { helper } from \"@acme/sdk/internal/util\";\n// error TS2307: Cannot find module '@acme/sdk/internal/util'\n// (the file dist/internal/util.js DOES exist on disk)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["exports", "the exports field", "exports field"], "hint": { "en": "The field that maps allowed entry points and hides everything else.", "ru": "Поле, задающее разрешённые точки входа и скрывающее всё остальное." } },
          { "id": "b2", "accept": ["encapsulation", "encapsulated", "boundary", "private"], "hint": { "en": "exports is a ___ boundary: unlisted paths can't be imported even if they exist.", "ru": "exports — граница ___: непрописанные пути нельзя импортировать, даже если они есть." } }
        ]
      }
    },
    {
      "id": "predict-paths-runtime",
      "type": "predict",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Predict whether the paths alias survives to runtime", "ru": "Предскажи, доживёт ли алиас paths до рантайма" },
      "prompt": { "en": "A project uses a @/ alias. It works in the editor and the Vite dev server. Predict what `node dist/index.js` does, and why.", "ru": "Проект использует алиас @/. Он работает в редакторе и dev-сервере Vite. Предскажи, что сделает `node dist/index.js` и почему." },
      "scenario": { "en": "tsconfig has paths: { \"@/*\": [\"src/*\"] }. The source imports `import { db } from \"@/db\"`. tsc compiled to dist/. What is in dist/index.js, and does plain Node run it?", "ru": "В tsconfig paths: { \"@/*\": [\"src/*\"] }. Исходник импортирует `import { db } from \"@/db\"`. tsc скомпилировал в dist/. Что в dist/index.js и запустит ли его голый Node?" },
      "reveal": { "en": "Plain Node throws <code>Cannot find module '@/db'</code>. tsc resolves <code>paths</code> for type-checking but <strong>does not rewrite them in emit</strong> — the literal <code>\"@/db\"</code> is still in dist/index.js, and Node has no idea what <code>@/</code> means. It worked in the editor (tsc-only resolution) and in Vite (the dev server rewrites the alias via resolve.alias). To make plain Node work you must rewrite the alias yourself: a bundler step, a loader like <code>tsconfig-paths</code>, or replace <code>paths</code> with a package.json <code>imports</code> subpath that Node understands.", "ru": "Голый Node бросает <code>Cannot find module '@/db'</code>. tsc разрешает <code>paths</code> для проверки типов, но <strong>не переписывает их в emit</strong> — литеральная <code>\"@/db\"</code> по-прежнему в dist/index.js, и Node не знает, что значит <code>@/</code>. Работало в редакторе (разрешение только tsc) и в Vite (dev-сервер переписывает алиас через resolve.alias). Чтобы заработал голый Node, алиас надо переписать самому: шаг бандлера, загрузчик вроде <code>tsconfig-paths</code> или замена <code>paths</code> на subpath из package.json <code>imports</code>, который Node понимает." }
    },
    {
      "id": "fix-interop-default-import",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Fix the CJS default-import error", "ru": "Почини ошибку default-импорта CJS" },
      "prompt": { "en": "This import of a CommonJS package errors. Fix the config (preferred) or the import, then reveal the model answer.", "ru": "Этот импорт CommonJS-пакета даёт ошибку. Почини конфиг (предпочтительно) или импорт, затем открой образцовый ответ." },
      "starter": "// tsconfig: { \"esModuleInterop\": false }\nimport express from \"express\"; // CJS package: module.exports = createApplication\n// error TS1259: Module can only be default-imported using the 'esModuleInterop' flag",
      "grading": {
        "mode": "self",
        "model": { "en": "Root cause: CommonJS has no real default export — <code>module.exports = createApplication</code> is a whole-namespace assignment, so a bare <code>import express from \"express\"</code> has nothing to bind to. Preferred fix: set <code>\"esModuleInterop\": true</code> (and it implies <code>allowSyntheticDefaultImports</code>), which synthesises a default and emits <code>__importDefault</code> so the call works at runtime. Without changing config, the alternatives are <code>import express = require(\"express\");</code> (TS import-equals form) or <code>import * as express from \"express\"</code> and call the namespace — but the namespace form is wrong when the export is callable, so esModuleInterop is the correct, idiomatic fix for the whole project.", "ru": "Корень: у CommonJS нет настоящего default-экспорта — <code>module.exports = createApplication</code> присваивает всё пространство имён, поэтому голому <code>import express from \"express\"</code> не к чему привязаться. Предпочтительный фикс: поставить <code>\"esModuleInterop\": true</code> (он подразумевает <code>allowSyntheticDefaultImports</code>), который синтезирует default и генерирует <code>__importDefault</code>, чтобы вызов работал в рантайме. Без правки конфига альтернативы — <code>import express = require(\"express\");</code> (форма import-equals TS) или <code>import * as express from \"express\"</code> с вызовом пространства имён — но форма с пространством имён неверна, когда экспорт вызываемый, так что esModuleInterop — корректный идиоматичный фикс для всего проекта." },
        "rubric": [
          { "en": "Names that CJS has no real default export as the root cause.", "ru": "Называет отсутствие настоящего default-экспорта у CJS корнем." },
          { "en": "Prefers esModuleInterop: true over per-import workarounds.", "ru": "Предпочитает esModuleInterop: true обходным путям на каждый импорт." },
          { "en": "Knows import * as express is wrong for a callable export.", "ru": "Знает, что import * as express неверен для вызываемого экспорта." }
        ]
      }
    }
  ]
}
