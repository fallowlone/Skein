{
  "lessonKey": "typescript/07-config-modules-build/04-project-references",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-ts6306",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Place composite correctly", "ru": "Поставь composite в нужное место" },
      "prompt": { "en": "A build fails with TS6306. Fill in the blanks with which project needs the setting and the setting it forces on.", "ru": "Сборка падает с TS6306. Заполни пропуски тем, какому проекту нужна настройка, и тем, что она форсирует." },
      "evidence": { "en": "// packages/web/tsconfig.json\n{ \"references\": [{ \"path\": \"../core\" }] }\n// packages/core/tsconfig.json\n{ \"compilerOptions\": { \"outDir\": \"dist\" } }\n// tsc -b -> error TS6306: Referenced project '../core' must have\n// setting \"composite\": true.", "ru": "// packages/web/tsconfig.json\n{ \"references\": [{ \"path\": \"../core\" }] }\n// packages/core/tsconfig.json\n{ \"compilerOptions\": { \"outDir\": \"dist\" } }\n// tsc -b -> error TS6306: Referenced project '../core' must have\n// setting \"composite\": true." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["core", "packages/core", "../core", "the referenced project", "referenced"], "hint": { "en": "The error names the project that is referenced, not the one that references it.", "ru": "Ошибка называет проект, на который ссылаются, а не тот, что ссылается." } },
          { "id": "b2", "accept": ["declaration", "declaration: true", "declaration true", ".d.ts", "d.ts emit"], "hint": { "en": "composite forces this emit so dependents read types instead of source.", "ru": "composite форсирует этот emit, чтобы зависящие читали типы вместо исходника." } }
        ]
      }
    },
    {
      "id": "predict-incremental-skip",
      "type": "predict",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Predict what rebuilds after an internal edit", "ru": "Предскажи, что пересоберётся после внутренней правки" },
      "prompt": { "en": "In a web → ui → core graph, you edit a private helper inside core that does NOT change any exported type. Predict which projects `tsc -b` rebuilds and which it skips.", "ru": "В графе web → ui → core ты правишь приватный хелпер внутри core, который НЕ меняет ни одного экспортируемого типа. Предскажи, какие проекты `tsc -b` пересоберёт, а какие пропустит." },
      "scenario": { "en": "All three projects are composite and were built once (each has a .tsbuildinfo). The edit changes implementation only; core's emitted .d.ts is byte-identical. What does the next `tsc -b packages/web` do?", "ru": "Все три проекта composite и были собраны однажды (у каждого .tsbuildinfo). Правка меняет только реализацию; сгенерированный .d.ts у core байт-в-байт идентичен. Что сделает следующий `tsc -b packages/web`?" },
      "reveal": { "en": "Only <code>core</code> rebuilds; <code>ui</code> and <code>web</code> are skipped. <code>core</code>'s inputs changed, so it recompiles and re-emits — but because its <strong>public .d.ts is unchanged</strong> (the edit was internal), the build's signature check sees that dependents have nothing new to type-check against. So <code>tsc -b</code> reuses the cached state of <code>ui</code> and <code>web</code>. This signature optimisation is exactly why a deep monorepo stays fast: implementation-only churn doesn't cascade. Had the edit changed an exported type's signature, the cascade would propagate up to every dependent.", "ru": "Пересоберётся только <code>core</code>; <code>ui</code> и <code>web</code> пропускаются. Входы <code>core</code> изменились, поэтому он перекомпилируется и переэмитит — но поскольку его <strong>публичный .d.ts не изменился</strong> (правка внутренняя), проверка сигнатуры видит, что зависящим не на что новое проверяться. Поэтому <code>tsc -b</code> переиспользует кэш <code>ui</code> и <code>web</code>. Именно эта оптимизация по сигнатуре держит глубокое монорепо быстрым: правки только реализации не каскадируют. Изменила бы правка сигнатуру экспортируемого типа — каскад распространился бы вверх на каждого зависящего." }
    },
    {
      "id": "diagnose-circular",
      "type": "diagnose",
      "difficulty": "stretch",
      "estMin": 5,
      "title": { "en": "Name the cycle error and the structural fix", "ru": "Назови ошибку цикла и структурный фикс" },
      "prompt": { "en": "Two packages reference each other and the build refuses. Fill in the blanks with the error code and the structural change that resolves it.", "ru": "Два пакета ссылаются друг на друга, и сборка отказывает. Заполни пропуски кодом ошибки и структурным изменением, которое это решает." },
      "evidence": { "en": "// packages/auth/tsconfig.json references ../users\n// packages/users/tsconfig.json references ../auth\n// tsc -b -> error TS6202: Project references may not form\n// a circular graph.", "ru": "// packages/auth/tsconfig.json references ../users\n// packages/users/tsconfig.json references ../auth\n// tsc -b -> error TS6202: Project references may not form\n// a circular graph." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["ts6202", "6202", "ts 6202"], "hint": { "en": "The error code printed for a circular reference graph.", "ru": "Код ошибки для циклического графа ссылок." } },
          { "id": "b2", "accept": ["shared", "shared package", "third package", "extract shared types", "leaf", "common package", "shared leaf"], "hint": { "en": "Extract what both need into a third ___ project both depend on, breaking the cycle.", "ru": "Вынеси нужное обоим в третий ___ проект, от которого зависят оба, разорвав цикл." } }
        ]
      }
    },
    {
      "id": "fix-stale-buildinfo",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Fix the stale .tsbuildinfo build", "ru": "Почини сборку с устаревшим .tsbuildinfo" },
      "prompt": { "en": "CI passes but a freshly cloned machine shows type errors that `tsc -b` claims are 'up to date'. Write the recovery commands and the prevention, then reveal the model answer.", "ru": "CI зелёный, но на свежесклонированной машине видны ошибки типов, которые `tsc -b` считает «актуальными». Напиши команды восстановления и профилактику, затем открой образцовый ответ." },
      "starter": "# committed dist/ and .tsbuildinfo into git by mistake;\n# now tsc -b skips projects whose source changed\ntsc -b packages/web   # reports everything up-to-date, but types are wrong",
      "grading": {
        "mode": "self",
        "model": { "en": "Recovery: run <code>tsc -b --clean</code> to delete all outputs and every .tsbuildinfo, then <code>tsc -b</code> to rebuild from scratch (or a single <code>tsc -b --force</code> to ignore the cache and rebuild). The root cause is that <code>dist/</code> and <code>.tsbuildinfo</code> were committed, so the cache reflects a stale machine's state, not the current source. Prevention: gitignore <code>dist/</code> and <code>*.tsbuildinfo</code> (they are build artifacts, never source of truth), and let each machine/CI build its own cache. If CI must be fast, cache the .tsbuildinfo as a CI cache keyed on a lockfile/source hash — not committed to the repo.", "ru": "Восстановление: запусти <code>tsc -b --clean</code>, чтобы удалить все выводы и каждый .tsbuildinfo, затем <code>tsc -b</code> для пересборки с нуля (или единый <code>tsc -b --force</code>, чтобы игнорировать кэш и пересобрать). Корень в том, что <code>dist/</code> и <code>.tsbuildinfo</code> были закоммичены, поэтому кэш отражает устаревшее состояние чужой машины, а не текущий исходник. Профилактика: добавь в gitignore <code>dist/</code> и <code>*.tsbuildinfo</code> (это артефакты сборки, никогда не источник истины) и дай каждой машине/CI собирать свой кэш. Если CI должен быть быстрым, кэшируй .tsbuildinfo как CI-кэш с ключом по хешу lockfile/исходника — а не коммить в репо." },
        "rubric": [
          { "en": "Uses tsc -b --clean or --force to recover.", "ru": "Использует tsc -b --clean или --force для восстановления." },
          { "en": "Identifies committed build artifacts as the root cause.", "ru": "Определяет закоммиченные артефакты сборки как корень." },
          { "en": "Gitignores dist and *.tsbuildinfo as prevention.", "ru": "Добавляет dist и *.tsbuildinfo в gitignore как профилактику." }
        ]
      }
    }
  ]
}
