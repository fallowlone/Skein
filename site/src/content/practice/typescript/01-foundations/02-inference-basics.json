{
  "lessonKey": "typescript/01-foundations/02-inference-basics",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-infer-the-types",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 6,
      "title": { "en": "Infer the type the checker picks", "ru": "Выведите тип, который выберет проверщик" },
      "prompt": { "en": "Mentally run inference on each declaration and fill in the type TypeScript reports on hover.", "ru": "Прогоните вывод в уме для каждого объявления и впишите тип, который TypeScript показывает при наведении." },
      "evidence": { "en": "1) let x = 'hi';            // x:\n2) const y = 'hi';          // y:\n3) const o = { k: 'hi' };    // o.k:\n4) const arr = [1, 'a'];     // element type:", "ru": "1) let x = 'hi';            // x:\n2) const y = 'hi';          // y:\n3) const o = { k: 'hi' };    // o.k:\n4) const arr = [1, 'a'];     // element type:" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["string"], "hint": { "en": "let is mutable, so the literal widens.", "ru": "let изменяем, поэтому литерал расширяется." } },
          { "id": "b2", "accept": ["\"hi\"", "'hi'", "hi"], "hint": { "en": "const on a primitive keeps the literal.", "ru": "const на примитиве хранит литерал." } },
          { "id": "b3", "accept": ["string"], "hint": { "en": "Object properties widen even under const.", "ru": "Свойства объекта расширяются даже под const." } },
          { "id": "b4", "accept": ["string | number", "number | string", "(string | number)", "(string | number)[]"], "hint": { "en": "Best-common-type of mixed elements is their union.", "ru": "Наилучший общий тип смешанных элементов — их объединение." } }
        ]
      }
    },
    {
      "id": "predict-contextual-loss",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict where the callback loses its type", "ru": "Предскажите, где колбэк теряет тип" },
      "prompt": { "en": "Think about contextual typing before peeking. One version compiles cleanly; the other errors under noImplicitAny. Which is which, and why?", "ru": "Подумайте о контекстной типизации, прежде чем подсматривать. Один вариант компилируется чисто; другой — ошибка при noImplicitAny. Какой есть какой и почему?" },
      "scenario": { "en": "Version A (inline): `['a','b'].map(s => s.toUpperCase())`. Version B (extracted): `const f = s => s.toUpperCase(); ['a','b'].map(f);`. The project has `noImplicitAny: true`. Predict the result of each.", "ru": "Вариант A (встроенный): `['a','b'].map(s => s.toUpperCase())`. Вариант B (вынесенный): `const f = s => s.toUpperCase(); ['a','b'].map(f);`. В проекте `noImplicitAny: true`. Предскажите результат каждого." },
      "reveal": { "en": "Version A COMPILES: the arrow sits directly in the argument position of `Array<string>.map`, so contextual typing flows the expected callback type inward and `s` is inferred as `string` — no annotation needed, `s.toUpperCase()` is valid. Version B ERRORS at the declaration of `f`: 'Parameter \"s\" implicitly has an \"any\" type.' Pulling the function out of the call removes the context — at the point `const f = s => ...` is written there is no expected type to drive `s`, so it falls back to implicit `any`, which `noImplicitAny` rejects. Fixes for B: annotate the parameter (`const f = (s: string) => ...`) or type the binding (`const f: (s: string) => string = s => ...`). The lesson: contextual typing is positional — it only works while the function literal is in a slot whose expected type is visible. Extracting a callback trades that free typing for an explicit annotation.", "ru": "Вариант A КОМПИЛИРУЕТСЯ: стрелка стоит прямо в позиции аргумента `Array<string>.map`, поэтому контекстная типизация течёт ожидаемым типом колбэка внутрь, и `s` выводится как `string` — аннотация не нужна, `s.toUpperCase()` валиден. Вариант B — ОШИБКА при объявлении `f`: 'Parameter \"s\" implicitly has an \"any\" type.' Вынос функции из вызова убирает контекст — в точке написания `const f = s => ...` нет ожидаемого типа, чтобы задать `s`, поэтому он падает в неявный `any`, который `noImplicitAny` отвергает. Лекарства для B: аннотировать параметр (`const f = (s: string) => ...`) или типизировать связку (`const f: (s: string) => string = s => ...`). Урок: контекстная типизация позиционна — она работает, лишь пока функциональный литерал в слоте, чей ожидаемый тип виден. Вынос колбэка меняет эту бесплатную типизацию на явную аннотацию." }
    },
    {
      "id": "predict-widen-vs-asconst",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict the widened config rejection", "ru": "Предскажите отказ из-за расширенного конфига" },
      "prompt": { "en": "Reason about property widening before revealing. Does the call type-check, and what is the minimal fix?", "ru": "Порассуждайте о расширении свойств, прежде чем открыть. Проходит ли вызов проверку типов и каково минимальное лекарство?" },
      "scenario": { "en": "`type Dir = 'in' | 'out'; function flow(d: Dir) {}`. Code: `const cfg = { dir: 'in' }; flow(cfg.dir);`. Predict whether `flow(cfg.dir)` compiles, the exact reason, and the smallest change that fixes it.", "ru": "`type Dir = 'in' | 'out'; function flow(d: Dir) {}`. Код: `const cfg = { dir: 'in' }; flow(cfg.dir);`. Предскажите, компилируется ли `flow(cfg.dir)`, точную причину и наименьшее исправляющее изменение." },
      "reveal": { "en": "It does NOT compile: 'Argument of type \"string\" is not assignable to parameter of type \"Dir\".' Even though the binding is `const`, `const` only freezes the reference `cfg`, not its members — `cfg.dir` is a mutable property, so TS widens it to `string`, and `string` is not assignable to the literal union `Dir`. The minimal fixes: (1) `const cfg = { dir: 'in' as const }` to keep just that property as `'in'`; (2) `const cfg = { dir: 'in' } as const` to make the whole object deeply readonly with literal properties; or (3) annotate `const cfg: { dir: Dir } = { dir: 'in' }`. All three stop `dir` from widening to `string` so it satisfies `Dir`. The recurring trap: `const` preserving a literal is true for *primitive* bindings (`const x = 'in'` is `'in'`), but the moment a value lands in a mutable object property it widens again unless you intervene.", "ru": "Это НЕ компилируется: 'Argument of type \"string\" is not assignable to parameter of type \"Dir\".' Хотя связка — `const`, `const` замораживает лишь ссылку `cfg`, а не члены — `cfg.dir` это изменяемое свойство, поэтому TS расширяет его до `string`, а `string` не совместим с объединением литералов `Dir`. Минимальные лекарства: (1) `const cfg = { dir: 'in' as const }`, чтобы сохранить лишь это свойство как `'in'`; (2) `const cfg = { dir: 'in' } as const`, чтобы сделать весь объект глубоко readonly с литеральными свойствами; или (3) аннотировать `const cfg: { dir: Dir } = { dir: 'in' }`. Все три останавливают расширение `dir` до `string`, и он удовлетворяет `Dir`. Повторяющаяся ловушка: сохранение литерала через `const` верно для *примитивных* связок (`const x = 'in'` это `'in'`), но как только значение попадает в изменяемое свойство объекта, оно снова расширяется, если не вмешаться." }
    },
    {
      "id": "fix-annotation-policy",
      "type": "fix",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Apply the annotate-boundaries policy", "ru": "Примените политику аннотирования границ" },
      "prompt": { "en": "The module below is both over- and under-annotated: it annotates locals that inference would handle, and leaves the exported API under-specified. Rewrite it to follow 'annotate boundaries, infer interiors': drop redundant local annotations and add the ones that lock the public contract. Write your version, then reveal the model and self-grade.", "ru": "Модуль ниже одновременно пере- и недоаннотирован: он аннотирует локальные переменные, которые вывел бы сам, и оставляет экспортируемый API недоопределённым. Перепишите его по принципу «аннотируйте границы, выводите внутренности»: уберите избыточные локальные аннотации и добавьте те, что фиксируют публичный контракт. Напишите свой вариант, затем откройте образец и оцените себя." },
      "starter": "// over-annotated locals, under-annotated boundary:\nexport function parsePort(raw) {            // param has no annotation -> implicit any\n  const n: number = Number(raw);            // redundant: Number() already returns number\n  const ok: boolean = Number.isInteger(n);  // redundant\n  return ok ? n : null;                      // return type left to inference on a PUBLIC api\n}\n",
      "grading": {
        "mode": "self",
        "model": { "en": "Annotate the boundaries (the parameter and the exported return type) and let inference handle the locals:\n\n```ts\nexport function parsePort(raw: string): number | null {\n  const n = Number(raw);                 // inferred number — no annotation needed\n  const ok = Number.isInteger(n);        // inferred boolean — no annotation needed\n  return ok ? n : null;\n}\n```\n\nWhy: `raw` is an input boundary — inference can't see the callers, and under `noImplicitAny` an unannotated parameter is an error, so it must be annotated (`string`). The explicit return type `number | null` is the highest-value annotation here: it locks the public contract so a future edit to the body (say, returning `undefined` instead of `null`) is caught at the function rather than silently widening the API for every caller. The two local annotations were pure noise — `Number(raw)` already yields `number` and `Number.isInteger(...)` already yields `boolean`; annotating them only creates a second place that can drift out of sync with the value. Rule of thumb: annotate parameters and exported return types; infer locals and inline callbacks.", "ru": "Аннотируйте границы (параметр и тип возврата экспортируемой функции) и доверьте вывод локальным переменным:\n\n```ts\nexport function parsePort(raw: string): number | null {\n  const n = Number(raw);                 // выведено number — аннотация не нужна\n  const ok = Number.isInteger(n);        // выведено boolean — аннотация не нужна\n  return ok ? n : null;\n}\n```\n\nПочему: `raw` — входная граница, вывод не видит вызывающих, и при `noImplicitAny` неаннотированный параметр — ошибка, поэтому его надо аннотировать (`string`). Явный тип возврата `number | null` здесь — аннотация наивысшей ценности: она фиксирует публичный контракт, так что будущая правка тела (скажем, возврат `undefined` вместо `null`) ловится на функции, а не тихо расширяет API для всех вызывающих. Две локальные аннотации были чистым шумом — `Number(raw)` уже даёт `number`, а `Number.isInteger(...)` уже даёт `boolean`; их аннотирование лишь создаёт второе место, способное рассинхронизироваться со значением. Правило: аннотируйте параметры и типы возврата экспортируемых функций; локальные и встроенные колбэки выводите." },
        "rubric": [
          { "en": "Annotates the parameter `raw: string` (an input boundary; required under noImplicitAny) and adds an explicit return type on the exported function.", "ru": "Аннотирует параметр `raw: string` (входная граница; обязательна при noImplicitAny) и добавляет явный тип возврата у экспортируемой функции." },
          { "en": "Removes the redundant local annotations (n, ok) and explains that inference already produces number/boolean accurately.", "ru": "Убирает избыточные локальные аннотации (n, ok) и объясняет, что вывод уже точно даёт number/boolean." },
          { "en": "Justifies the explicit return type as locking the public contract / preventing silent API widening on future edits.", "ru": "Обосновывает явный тип возврата как фиксацию публичного контракта / предотвращение тихого расширения API при будущих правках." }
        ]
      }
    }
  ]
}
