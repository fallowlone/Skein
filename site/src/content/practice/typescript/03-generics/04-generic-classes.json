{
  "lessonKey": "typescript/03-generics/04-generic-classes",
  "track": "typescript",
  "tasks": [
    {
      "id": "diagnose-method-own-param",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Instance T vs method U", "ru": "T экземпляра против U метода" },
      "prompt": { "en": "Fill in the instance type and the result type after the map call.", "ru": "Впишите тип экземпляра и тип результата после вызова map." },
      "evidence": { "en": "class Box<T> { constructor(public value: T) {} map<U>(fn: (v: T) => U): Box<U> { return new Box(fn(this.value)); } }\nconst b = new Box(42);          // b: ____\nconst s = b.map(n => n.toFixed(2)); // s: ____", "ru": "class Box<T> { constructor(public value: T) {} map<U>(fn: (v: T) => U): Box<U> { return new Box(fn(this.value)); } }\nconst b = new Box(42);          // b: ____\nconst s = b.map(n => n.toFixed(2)); // s: ____" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b", "accept": ["Box<number>", "Box< number >"], "hint": { "en": "T is inferred from the constructor argument 42.", "ru": "T выводится из аргумента конструктора 42." } },
          { "id": "s", "accept": ["Box<string>", "Box< string >"], "hint": { "en": "map has its own U, inferred from the callback's return n.toFixed(2).", "ru": "У map свой U, выведенный из возврата колбэка n.toFixed(2)." } }
        ]
      }
    },
    {
      "id": "diagnose-static-side",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Why the static won't compile", "ru": "Почему статика не компилируется" },
      "prompt": { "en": "Explain in 2-3 sentences why the static member errors and give the standard fix.", "ru": "Объясните в 2-3 предложениях, почему статический член ошибается, и приведите стандартный фикс." },
      "evidence": { "en": "class Box<T> {\n  constructor(public value: T) {}\n  static empty(): Box<T> { return new Box(undefined as any); }\n}\n// Error: Static members cannot reference class type parameters.", "ru": "class Box<T> {\n  constructor(public value: T) {}\n  static empty(): Box<T> { return new Box(undefined as any); }\n}\n// Error: Static members cannot reference class type parameters." },
      "grading": {
        "mode": "self",
        "model": { "en": "A class type parameter <code>T</code> is bound per INSTANCE, solved from each constructor call, so there is no single instance-bound T for a static member — which lives on the one class object shared across every instance — to reference. The fix is to make the static its OWN generic with a fresh parameter resolved at the static call site: <code>static empty&lt;U&gt;(): Box&lt;U&gt; { return new Box&lt;U&gt;(undefined as U); }</code>, or better a value-taking factory <code>static of&lt;U&gt;(value: U): Box&lt;U&gt; { return new Box(value); }</code> so U is inferred. This is why factory methods are standalone generics, unrelated to any instance's T.", "ru": "Параметр типа класса <code>T</code> привязан по-ЭКЗЕМПЛЯРНО, решается из каждого вызова конструктора, поэтому нет единого привязанного к экземпляру T, на который мог бы сослаться статический член — живущий на единственном объекте класса, общем для всех экземпляров. Фикс — сделать статику ЕЁ СОБСТВЕННЫМ дженериком со свежим параметром, решаемым в точке статического вызова: <code>static empty&lt;U&gt;(): Box&lt;U&gt; { return new Box&lt;U&gt;(undefined as U); }</code>, или лучше фабрику со значением <code>static of&lt;U&gt;(value: U): Box&lt;U&gt; { return new Box(value); }</code>, чтобы U выводился. Поэтому фабричные методы — самостоятельные дженерики, не связанные с T какого-либо экземпляра." },
        "rubric": [
          { "en": "States the static side is shared across instances and has no single instance-bound T to reference.", "ru": "Указывает, что статическая сторона общая для экземпляров и не имеет единого привязанного к экземпляру T для ссылки." },
          { "en": "Gives the fix: make the static its own generic with its own type parameter resolved at the static call site.", "ru": "Даёт фикс: сделать статику собственным дженериком со своим параметром типа, решаемым в точке статического вызова." }
        ]
      }
    },
    {
      "id": "predict-result-narrowing",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "What can you read after if (r.ok)?", "ru": "Что можно читать после if (r.ok)?" },
      "prompt": { "en": "Predict which payload field is accessible in each branch and why the unused slot is never.", "ru": "Предскажите, какое поле нагрузки доступно в каждой ветви и почему неиспользуемый слот это never." },
      "scenario": { "en": "Given <code>type Result&lt;T, E&gt; = { ok: true; value: T } | { ok: false; error: E }</code> and <code>ok&lt;T&gt;(v: T): Result&lt;T, never&gt;</code>, a value <code>r: Result&lt;number, string&gt;</code> is checked with <code>if (r.ok) { /* A */ } else { /* B */ }</code>. What is readable in A and B, and why does <code>ok</code> use <code>never</code> for its error slot?", "ru": "Дано <code>type Result&lt;T, E&gt; = { ok: true; value: T } | { ok: false; error: E }</code> и <code>ok&lt;T&gt;(v: T): Result&lt;T, never&gt;</code>, значение <code>r: Result&lt;number, string&gt;</code> проверяется через <code>if (r.ok) { /* A */ } else { /* B */ }</code>. Что доступно в A и B и почему <code>ok</code> использует <code>never</code> для слота ошибки?" },
      "reveal": { "en": "The discriminant <code>r.ok</code> narrows the union per branch. In A (<code>r.ok === true</code>) the type is <code>{ ok: true; value: number }</code>, so <code>r.value: number</code> is readable and <code>r.error</code> does not exist. In B it is <code>{ ok: false; error: string }</code>, so only <code>r.error: string</code> is readable. <code>ok</code> returns <code>Result&lt;T, never&gt;</code> because there is no error in the success case, and <code>never</code> (the bottom type) is assignable to ANY <code>E</code> — so <code>ok(8080)</code> still fits where a <code>Result&lt;number, string&gt;</code> is expected. The compiler thus forces handling both outcomes while exposing exactly one branch's payload.", "ru": "Дискриминант <code>r.ok</code> сужает объединение по ветвям. В A (<code>r.ok === true</code>) тип — <code>{ ok: true; value: number }</code>, поэтому <code>r.value: number</code> доступен, а <code>r.error</code> не существует. В B это <code>{ ok: false; error: string }</code>, поэтому доступен лишь <code>r.error: string</code>. <code>ok</code> возвращает <code>Result&lt;T, never&gt;</code>, потому что в случае успеха ошибки нет, а <code>never</code> (нижний тип) присваиваем ЛЮБОМУ <code>E</code> — поэтому <code>ok(8080)</code> всё ещё подходит туда, где ждут <code>Result&lt;number, string&gt;</code>. Компилятор тем самым заставляет обработать оба исхода, открывая нагрузку ровно одной ветви." }
    },
    {
      "id": "fix-static-instance-leak",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 7,
      "title": { "en": "Make the factory typed", "ru": "Сделайте фабрику типизированной" },
      "prompt": { "en": "This factory either errors or returns Stack<any>. Rewrite it so the element type flows from the argument to the returned stack.", "ru": "Эта фабрика либо ошибается, либо возвращает Stack<any>. Перепишите её так, чтобы тип элемента тёк из аргумента в возвращаемый стек." },
      "starter": "class Stack<T> {\n  private items: T[] = [];\n  push(x: T) { this.items.push(x); }\n  pop(): T | undefined { return this.items.pop(); }\n  static from(initial: T[]): Stack<T> { // Error: static references T\n    const s = new Stack<T>();\n    initial.forEach(x => s.push(x));\n    return s;\n  }\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "The static <code>from</code> references the class type parameter <code>T</code>, which is illegal because statics are not bound to any instance's T. Give the static its OWN generic so the element type is inferred from the argument: <code>static from&lt;U&gt;(initial: U[]): Stack&lt;U&gt; { const s = new Stack&lt;U&gt;(); initial.forEach(x =&gt; s.push(x)); return s; }</code>. Now <code>Stack.from([1, 2, 3])</code> infers <code>U = number</code> and returns <code>Stack&lt;number&gt;</code> — no <code>any</code>, no reference to a nonexistent instance T.", "ru": "Статический <code>from</code> ссылается на параметр типа класса <code>T</code>, что недопустимо, потому что статика не привязана к T какого-либо экземпляра. Дайте статике ЕЁ СОБСТВЕННЫЙ дженерик, чтобы тип элемента выводился из аргумента: <code>static from&lt;U&gt;(initial: U[]): Stack&lt;U&gt; { const s = new Stack&lt;U&gt;(); initial.forEach(x =&gt; s.push(x)); return s; }</code>. Теперь <code>Stack.from([1, 2, 3])</code> выводит <code>U = number</code> и возвращает <code>Stack&lt;number&gt;</code> — без <code>any</code>, без ссылки на несуществующий T экземпляра." },
        "rubric": [
          { "en": "Identifies that the static method illegally references the class type parameter T.", "ru": "Определяет, что статический метод недопустимо ссылается на параметр типа класса T." },
          { "en": "Introduces a method-local type parameter (e.g. U) inferred from the argument and returns Stack<U>.", "ru": "Вводит локальный для метода параметр типа (например U), выводимый из аргумента, и возвращает Stack<U>." }
        ]
      }
    }
  ]
}
