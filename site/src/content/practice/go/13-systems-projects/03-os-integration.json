{
  "lessonKey": "go/13-systems-projects/03-os-integration",
  "track": "go",
  "tasks": [
    {
      "id": "predict-exec-context-grandchildren",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "The timeout fires — who actually dies?", "ru": "Таймаут сработал — кто на самом деле умрёт?" },
      "prompt": { "en": "A service runs a backup helper with <code>exec.CommandContext(ctx, \"backup.sh\")</code> under a 10-second timeout, capturing combined output into a buffer. The script internally launches <code>tar</code> and a long <code>gpg</code> encryption as child processes. The timeout fires. Predict: (1) which processes receive the kill, (2) what happens to tar and gpg, and (3) why the Go call may STILL not return promptly even after the kill.", "ru": "Сервис запускает бэкап-хелпер через <code>exec.CommandContext(ctx, \"backup.sh\")</code> под 10-секундным таймаутом, собирая объединённый вывод в буфер. Скрипт внутри запускает <code>tar</code> и долгое шифрование <code>gpg</code> как дочерние процессы. Таймаут срабатывает. Предскажи: (1) какие процессы получат kill, (2) что станет с tar и gpg и (3) почему вызов в Go может ВСЁ РАВНО не вернуться быстро даже после kill." },
      "scenario": { "en": "CommandContext's cancellation signals one specific PID. Think in process-tree terms: backup.sh is the child; tar and gpg are grandchildren holding inherited copies of the output pipe. What does Wait block on?", "ru": "Отмена CommandContext сигналит одному конкретному PID. Думай в терминах дерева процессов: backup.sh — ребёнок; tar и gpg — внуки, держащие унаследованные копии пайпа вывода. На чём блокируется Wait?" },
      "reveal": { "en": "(1) Only backup.sh — the direct child — receives the kill (SIGKILL by default; Cancel/WaitDelay can soften it to TERM-then-KILL). (2) tar and gpg are orphaned, get reparented, and keep running to completion — the backup half-finishes in the background with nobody watching. (3) The grandchildren inherited the write end of the stdout/stderr pipe, so the pipe never reaches EOF while they live; cmd.Wait (and thus Run) blocks reading output until gpg finally exits, minutes after your 10-second timeout supposedly fired. The structural fix: give the child its own process group via SysProcAttr Setpgid, and on cancellation signal the negative PID to kill the whole group — or set WaitDelay so Go abandons the pipe after a grace window. The timeout you wrote bounds one process; the work you started is a tree.", "ru": "(1) Kill получит только backup.sh — прямой ребёнок (по умолчанию SIGKILL; Cancel/WaitDelay смягчают до TERM-затем-KILL). (2) tar и gpg осиротеют, переусыновятся и продолжат работать до конца — бэкап наполовину доделывается в фоне, и никто за ним не смотрит. (3) Внуки унаследовали пишущий конец пайпа stdout/stderr, поэтому пайп не достигает EOF, пока они живы; cmd.Wait (а значит и Run) блокируется на чтении вывода, пока gpg наконец не выйдет — через минуты после якобы сработавшего 10-секундного таймаута. Структурный фикс: дать ребёнку собственную группу процессов через SysProcAttr Setpgid и при отмене сигналить отрицательному PID, убивая всю группу, — или задать WaitDelay, чтобы Go бросил пайп после grace-окна. Написанный тобой таймаут ограничивает один процесс; запущенная тобой работа — дерево." }
    },
    {
      "id": "diagnose-zero-byte-config-after-outage",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "The zero-byte config after the power blink", "ru": "Конфиг в ноль байт после мигания питания" },
      "prompt": { "en": "After a datacenter power event, dozens of agents crash-loop on startup — see the evidence. Fill in the blanks: os.Create opens with a flag that ____ (b1) the file before any new bytes land; the safe pattern writes a temp file in the same directory and then ____ (b2) it over the target; before that swap, the writer must call ____ (b3) so the new bytes are durable on disk.", "ru": "После энергоаварии в датацентре десятки агентов крутятся в crash-loop на старте — смотри улики. Заполни пропуски: os.Create открывает файл с флагом, который его ____ (b1) до того, как лягут новые байты; безопасный паттерн пишет temp-файл в том же каталоге и затем делает ему ____ (b2) поверх цели; перед этой подменой писатель обязан вызвать ____ (b3), чтобы новые байты стали долговечными на диске." },
      "evidence": { "en": "$ ls -l /etc/agent/config.json\n-rw-r--r-- 1 agent agent 0 Jun 10 21:14 /etc/agent/config.json\n\n$ journalctl -u agent | tail -2\nagent[312]: parse /etc/agent/config.json: unexpected end of JSON input\nsystemd[1]: agent.service: Failed with result 'exit-code'.\n\n// writer code, simplified\nf, _ := os.Create(cfgPath)   // opens with O_TRUNC\nf.Write(buf)\nf.Close()", "ru": "$ ls -l /etc/agent/config.json\n-rw-r--r-- 1 agent agent 0 Jun 10 21:14 /etc/agent/config.json\n\n$ journalctl -u agent | tail -2\nagent[312]: parse /etc/agent/config.json: unexpected end of JSON input\nsystemd[1]: agent.service: Failed with result 'exit-code'.\n\n// код писателя, упрощённо (комментарий: открывает с O_TRUNC)\nf, _ := os.Create(cfgPath)\nf.Write(buf)\nf.Close()" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["truncates", "truncate", "zeroes", "empties", "O_TRUNC", "усекает", "обнуляет", "очищает", "стирает"], "hint": { "en": "The zero-byte listing is this flag's signature: the old content was destroyed the instant the file opened.", "ru": "Листинг в ноль байт — подпись этого флага: старое содержимое уничтожено в момент открытия файла." } },
          { "id": "b2", "accept": ["renames", "rename", "rename(2)", "os.Rename", "переименовывает", "rename-ит", "делает rename", "переименование"], "hint": { "en": "The one POSIX operation that swaps a directory entry atomically within a filesystem.", "ru": "Единственная операция POSIX, атомарно подменяющая запись каталога в пределах файловой системы." } },
          { "id": "b3", "accept": ["fsync", "sync", "f.Sync", "Sync", "файловый sync", "fsync файла"], "hint": { "en": "Without it the kernel may persist the name swap before the data, leaving a well-named file of garbage.", "ru": "Без него ядро может сделать подмену имени долговечной раньше данных, оставив правильно названный файл с мусором." } }
        ]
      }
    },
    {
      "id": "design-host-agent-state-subsystem",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Design the file and process layer of a host agent", "ru": "Спроектируй файловый и процессный слой хост-агента" },
      "prompt": { "en": "Design the agent's OS-integration layer: single-instance guard, crash-safe state persistence, config watching that survives editor saves, plugin execution, and the cross-platform split. Self-grade against the rubric.", "ru": "Спроектируй слой интеграции агента с ОС: защиту единственного экземпляра, сбоеустойчивое сохранение состояния, наблюдение за конфигом, переживающее сохранения из редактора, запуск плагинов и кроссплатформенное разделение. Оцени себя по рубрике." },
      "constraints": { "en": "A host agent runs on Linux and macOS fleets: it must never run twice on one host, persists a state file every 30 s that must survive power loss, hot-reloads a config that admins edit with vim and ansible (both save atomically), and executes operator-supplied diagnostic plugins as subprocesses with a hard 15 s budget. Embedded defaults must ship inside the binary. A predecessor left stale lock files after crashes, requiring manual cleanup.", "ru": "Хост-агент работает на флотах Linux и macOS: он никогда не должен запускаться дважды на одном хосте, каждые 30 с сохраняет файл состояния, обязанный пережить потерю питания, горячо перечитывает конфиг, который админы правят vim-ом и ansible-ом (оба сохраняют атомарно), и исполняет операторские диагностические плагины как сабпроцессы с жёстким бюджетом 15 с. Дефолты должны ехать внутри бинарника. Предшественник оставлял протухшие lock-файлы после крашей, требуя ручной уборки." },
      "rubric": [
        { "en": "Chooses flock over an O_EXCL lock file for the single-instance guard and justifies it with the constraint: an advisory flock dies with the process descriptor, so a crash leaves no stale lock — while stating the advisory caveat (only cooperating processes are bound).", "ru": "Выбирает flock вместо O_EXCL-файла для защиты единственного экземпляра и обосновывает это ограничением задачи: advisory flock умирает с дескриптором процесса, краш не оставляет протухший lock — и проговаривает оговорку advisory (обязателен только для сотрудничающих процессов)." },
        { "en": "Persists state via temp-write in the state directory, fsync, rename — naming why same-filesystem placement matters (EXDEV, tmpfs /tmp) and acknowledging the millisecond fsync cost as the price of the every-30-s durability guarantee.", "ru": "Сохраняет состояние через temp-запись в каталоге состояния, fsync, rename — называя, почему важно размещение на той же ФС (EXDEV, tmpfs в /tmp), и признавая миллисекундную цену fsync платой за гарантию долговечности каждые 30 с." },
        { "en": "Watches the config DIRECTORY, not the file, filters by name, debounces the burst, and explains the inode mechanics: vim and ansible rename a new inode over the path, so a file watch dies after the first save; reads happen once per burst, never on a raw WRITE event.", "ru": "Наблюдает за КАТАЛОГОМ конфига, а не файлом, фильтрует по имени, дебаунсит вспышку и объясняет механику инодов: vim и ansible rename-ят новый инод поверх пути, поэтому watch на файле умирает после первого сохранения; чтение — один раз на вспышку и никогда по сырому событию WRITE." },
        { "en": "Runs plugins with CommandContext plus a process group (Setpgid, group kill) so the 15 s budget bounds the whole tree, with argv slices and an explicit minimal cmd.Env; splits flock behind build tags (lock_unix.go) and embeds defaults with go:embed, using path semantics for the embedded FS.", "ru": "Запускает плагины через CommandContext плюс группу процессов (Setpgid, kill группе), чтобы бюджет 15 с ограничивал всё дерево, с argv-срезами и явным минимальным cmd.Env; разносит flock по build-тегам (lock_unix.go) и встраивает дефолты через go:embed, используя семантику path для встроенной ФС." }
      ],
      "model": { "en": "Single instance: open a lock file under the state directory and take syscall.Flock LOCK_EX|LOCK_NB at startup; failure means another instance holds it — exit 1 with a clear stderr message. flock wins over O_EXCL precisely because of the predecessor's pain: the kernel releases the lock when the descriptor dies, crash included, so stale locks are structurally gone; advisory is acceptable since only the agent itself competes. State: atomicWrite(stateDir/state.json) — CreateTemp in the same directory (same filesystem; a /tmp temp would make the final rename fail with EXDEV or, worse, tempt a non-atomic copy fallback), write, f.Sync, close, rename; the fsync costs milliseconds every 30 s, which is noise against the power-loss guarantee the constraint demands. Config watching: fsnotify on the config directory, filter events for config.yaml, 150 ms debounce timer reset per event, then a single load-validate-swap into atomic.Pointer; this survives vim and ansible because their atomic saves are renames of fresh inodes — a watch on the file itself would fire REMOVE once and go silent. Plugins: exec.CommandContext with a 15 s context, argv slice only (operator input never reaches a shell), explicit minimal cmd.Env, cmd.Dir pinned to a scratch dir, SysProcAttr Setpgid true, and on cancellation a kill to the negative PID so plugin-spawned children die with the plugin; stdout capped and captured separately from stderr; ExitError distinguishes plugin failed from could not start. Portability: flock lives in lock_unix.go behind go:build tags with a Windows stub returning unsupported (fleet is Unix-only today); go:embed carries default config and dashboards, addressed with the path package — filepath only ever touches the real disk.", "ru": "Единственный экземпляр: открыть lock-файл в каталоге состояния и взять syscall.Flock LOCK_EX|LOCK_NB на старте; неудача означает, что его держит другой экземпляр, — выход с кодом 1 и ясным сообщением в stderr. flock побеждает O_EXCL ровно из-за боли предшественника: ядро отпускает блокировку со смертью дескриптора, включая краш, поэтому протухшие локи исчезают структурно; advisory приемлем, раз конкурирует только сам агент. Состояние: atomicWrite(stateDir/state.json) — CreateTemp в том же каталоге (та же ФС; temp в /tmp уронил бы финальный rename с EXDEV или, хуже, соблазнил бы неатомарный fallback с копированием), записать, f.Sync, закрыть, rename; fsync стоит миллисекунды раз в 30 с — шум на фоне гарантии при потере питания, которой требует условие. Наблюдение за конфигом: fsnotify на каталоге конфига, фильтр событий по config.yaml, дебаунс-таймер 150 мс со сбросом на каждое событие, затем один цикл загрузить-провалидировать-подменить в atomic.Pointer; это переживает vim и ansible, потому что их атомарные сохранения — это rename свежих инодов: watch на самом файле выстрелил бы REMOVE один раз и замолчал. Плагины: exec.CommandContext с 15-секундным контекстом, только argv-срез (операторский ввод никогда не достигает шелла), явный минимальный cmd.Env, cmd.Dir прибит к scratch-каталогу, SysProcAttr Setpgid true, и при отмене kill отрицательному PID, чтобы дети плагина умирали вместе с ним; stdout ограничен по размеру и захвачен отдельно от stderr; ExitError отличает «плагин упал» от «не смог стартовать». Портируемость: flock живёт в lock_unix.go за go:build тегами с Windows-заглушкой, возвращающей unsupported (флот сегодня только Unix); go:embed везёт дефолтный конфиг и дашборды, адресуемые пакетом path — filepath трогает только настоящий диск." }
    }
  ]
}
