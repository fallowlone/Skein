{
  "lessonKey": "go/13-systems-projects/01-cli-tools",
  "track": "go",
  "tasks": [
    {
      "id": "predict-stdout-narration-breaks-jq",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "What does the pipeline see?", "ru": "Что увидит пайплайн?" },
      "prompt": { "en": "A release tool prints <code>fmt.Println(\"scanning 12 modules...\")</code>, then writes its JSON report with <code>json.NewEncoder(os.Stdout)</code>, then on a registry warning calls <code>log.Printf(\"registry slow, retrying\")</code>. A CI step runs <code>tool report --json | jq .modules</code> under <code>set -euo pipefail</code>. Predict: (1) which of the three prints reaches jq, (2) whether jq succeeds, and (3) what the CI step's final status is.", "ru": "Релизный инструмент печатает <code>fmt.Println(\"scanning 12 modules...\")</code>, затем пишет JSON-отчёт через <code>json.NewEncoder(os.Stdout)</code>, затем при предупреждении от реестра вызывает <code>log.Printf(\"registry slow, retrying\")</code>. Шаг CI выполняет <code>tool report --json | jq .modules</code> под <code>set -euo pipefail</code>. Предскажи: (1) какие из трёх печатей дойдут до jq, (2) отработает ли jq успешно и (3) каким будет итоговый статус шага CI." },
      "scenario": { "en": "Recall the default destinations: fmt.Println and the json encoder both target os.Stdout; the log package targets stderr. A pipe forwards only stdout bytes, in order, with no framing. pipefail makes the pipeline status the rightmost failing command.", "ru": "Вспомни дефолтные направления: fmt.Println и json-энкодер целятся в os.Stdout; пакет log — в stderr. Пайп передаёт только байты stdout, по порядку и без фрейминга. pipefail делает статусом пайплайна самый правый упавший элемент." },
      "reveal": { "en": "(1) jq receives the fmt.Println narration line AND the JSON — both went to stdout; the log.Printf line goes to stderr, bypasses the pipe, and appears in the CI log directly. (2) jq fails: its input begins with the text line scanning 12 modules..., which is not a JSON term, so it dies with a parse error before reaching the report. (3) Under pipefail the pipeline takes jq's nonzero status, and set -e aborts the CI step. The one-line fix: move narration to stderr — fmt.Fprintln(os.Stderr, ...) — leaving stdout to carry exactly one thing, the data. The log call was already correct, which is the quiet lesson: Go's log package is pipeline-safe by default; fmt is not.", "ru": "(1) jq получит строку повествования от fmt.Println И сам JSON — обе ушли в stdout; строка log.Printf уходит в stderr, минует пайп и появляется прямо в логе CI. (2) jq падает: его вход начинается с текстовой строки scanning 12 modules..., которая не является JSON-термом, и он умирает с parse error, не дойдя до отчёта. (3) Под pipefail пайплайн берёт ненулевой статус jq, и set -e обрывает шаг CI. Однострочный фикс: перенести повествование в stderr — fmt.Fprintln(os.Stderr, ...), — оставив stdout ровно одну вещь: данные. Вызов log уже был корректен, и в этом тихий урок: пакет log в Go безопасен для пайплайнов по умолчанию, а fmt — нет." }
    },
    {
      "id": "diagnose-ansi-noise-in-ci-capture",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Escape codes in the artifact, clean output on laptops", "ru": "Escape-коды в артефакте, чистый вывод на ноутбуках" },
      "prompt": { "en": "A teammate's CLI looks perfect in a terminal, but the CI artifact is full of garbage and the JSON consumer chokes — see the evidence. Fill in the blanks: the tool should decide decoration by checking whether stdout is a ____ (b1); when that check fails, color and spinners must be disabled because the consumer is a ____ (b2); the per-record progress lines belong on ____ (b3).", "ru": "CLI коллеги отлично выглядит в терминале, но CI-артефакт полон мусора, и потребитель JSON давится — смотри улики. Заполни пропуски: инструмент должен решать вопрос декора, проверяя, является ли stdout ____ (b1); когда проверка не проходит, цвет и спиннеры надо выключить, потому что потребитель — ____ (b2); строки прогресса по каждой записи должны идти на ____ (b3)." },
      "evidence": { "en": "$ tool export --json > /artifacts/report.json   # in CI\n$ head -c 120 /artifacts/report.json\n\\x1b[32m✓\\x1b[0m record 1 ok\n\\x1b[32m✓\\x1b[0m record 2 ok\n{\"records\":[{\"id\":1,\n\n$ jq . /artifacts/report.json\njq: error (at /artifacts/report.json:1): Invalid literal\n\n# on the developer laptop the same command renders green checkmarks and valid output", "ru": "// Улики из CI (вывод не изменён)\n$ tool export --json > /artifacts/report.json   # в CI\n$ head -c 120 /artifacts/report.json\n\\x1b[32m✓\\x1b[0m record 1 ok\n\\x1b[32m✓\\x1b[0m record 2 ok\n{\"records\":[{\"id\":1,\n\n$ jq . /artifacts/report.json\njq: error (at /artifacts/report.json:1): Invalid literal\n\n# на ноутбуке разработчика та же команда рисует зелёные галочки и валидный вывод" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["tty", "TTY", "terminal", "a terminal", "терминал", "терминалом", "tty-устройством", "интерактивный терминал"], "hint": { "en": "term.IsTerminal(int(os.Stdout.Fd())) answers exactly this question about the output descriptor.", "ru": "term.IsTerminal(int(os.Stdout.Fd())) отвечает ровно на этот вопрос о дескрипторе вывода." } },
          { "id": "b2", "accept": ["machine", "a machine", "program", "a program", "another process", "script", "машина", "программа", "другой процесс", "скрипт", "парсер"], "hint": { "en": "Nothing with eyes reads a redirected stream — checkmarks and ANSI color serve nobody there.", "ru": "Перенаправленный поток не читает никто с глазами — галочки и ANSI-цвет там никому не служат." } },
          { "id": "b3", "accept": ["stderr", "standard error", "fd 2", "стандартный поток ошибок", "поток ошибок"], "hint": { "en": "The stream that bypasses both the pipe and the redirect, and reaches the operator.", "ru": "Поток, который минует и пайп, и редирект — и достигает оператора." } }
        ]
      }
    },
    {
      "id": "design-internal-cli-contract",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Design the contract for a fleet-query CLI", "ru": "Спроектируй контракт CLI для опроса флота" },
      "prompt": { "en": "Design the tool's public contract: streams, exit-code table, output modes, stdin behavior, interrupt handling, flag library choice, and distribution. Self-grade against the rubric.", "ru": "Спроектируй публичный контракт инструмента: потоки, таблицу кодов выхода, режимы вывода, поведение stdin, обработку прерывания, выбор библиотеки флагов и дистрибуцию. Оцени себя по рубрике." },
      "constraints": { "en": "An internal tool fleetctl queries hundreds of edge hosts: humans run it interactively to eyeball status; CI pipes it into jq and branches on the exit code; some teams feed host lists via stdin. It writes a per-run temp cache, takes 30-90 s on big fleets, and today has one verb (status) with a second (drain) planned. Target: Linux and macOS engineers, no Go toolchain guaranteed on operator laptops.", "ru": "Внутренний инструмент fleetctl опрашивает сотни edge-хостов: люди запускают его интерактивно, чтобы глянуть статус; CI пайпит его в jq и ветвится на коде выхода; некоторые команды подают списки хостов через stdin. Он пишет временный кэш на запуск, работает 30–90 с на больших флотах, и сегодня у него один глагол (status), второй (drain) в планах. Аудитория: инженеры на Linux и macOS, Go-тулчейн на ноутбуках операторов не гарантирован." },
      "rubric": [
        { "en": "Locks the stream contract: stdout carries only records (table or JSON), all progress and per-host warnings go to stderr, and the exit-code table is documented and frozen — for example 0 all healthy, 1 some hosts failing, 2 usage error, 3 query infrastructure failure — because CI branches on it.", "ru": "Фиксирует контракт потоков: stdout несёт только записи (таблицу или JSON), весь прогресс и похостовые предупреждения идут в stderr, а таблица кодов выхода документирована и заморожена — например 0 все здоровы, 1 часть хостов сбоит, 2 ошибка использования, 3 отказ инфраструктуры опроса — потому что CI на ней ветвится." },
        { "en": "Separates format from decoration: --json is an explicit flag, never inferred; TTY detection plus NO_COLOR controls only color and the progress display, so the same invocation is stable between a laptop demo and the script copied from it.", "ru": "Разделяет формат и декор: --json — явный флаг, никогда не выводится автоматически; детекция TTY плюс NO_COLOR управляют только цветом и отображением прогресса, чтобы один и тот же вызов был стабилен между демо на ноутбуке и скриптом, скопированным с него." },
        { "en": "Designs the filter and interrupt paths: reads host lists from stdin when no args are given; wires signal.NotifyContext so ctrl-C during a 90-second fleet sweep cancels in-flight queries, removes the temp cache via defers, and exits 130 — naming the os.Exit-skips-defers trap explicitly.", "ru": "Проектирует пути фильтра и прерывания: читает списки хостов из stdin при отсутствии аргументов; проводит signal.NotifyContext, чтобы ctrl-C посреди 90-секундного обхода флота отменял текущие запросы, удалял временный кэш через defer и выходил с кодом 130 — явно называя ловушку os.Exit, пропускающего defer." },
        { "en": "Makes honest tooling calls: stays on stdlib flag while there is one verb and hand-rolls the second with flag.NewFlagSet, naming the cobra threshold (completions, nested verbs) as a future migration; ships checksummed per-platform release archives because operators lack a Go toolchain, with go install as the developer-only extra.", "ru": "Делает честные инструментальные выборы: остаётся на stdlib flag, пока глагол один, и собирает второй вручную через flag.NewFlagSet, называя порог cobra (комплишены, вложенные глаголы) будущей миграцией; поставляет релизные архивы с чексуммами по платформам, потому что у операторов нет Go-тулчейна, а go install — бонус только для разработчиков." }
      ],
      "model": { "en": "Contract first, code second. Streams: stdout emits exactly one thing per run — a human table or, under --json, one JSON document; every byte of narration (per-host progress, retry warnings, the spinner) goes to stderr, so the jq pipeline and the redirect never see it. Exit codes are the CI API and get a frozen, documented table: 0 fleet healthy, 1 partial failures (CI can branch to a soft alert), 2 usage, 3 the query layer itself broke; signal death surfaces as 130 by convention. Format versus decoration: --json is explicit because silent format switching turns a tested-by-eye command into a differently-behaving script; term.IsTerminal on stdout plus NO_COLOR gate only color and the progress meter. With no positional args, fleetctl reads newline-separated hosts from stdin, joining the grep-sort family. Interrupts: signal.NotifyContext at the top of run() returning an int into a single os.Exit(run()) in main; the temp cache is created once, removed by defer, and the 30-90 s sweep passes ctx into every host query so ctrl-C cancels cleanly — no direct os.Exit anywhere else, because it would skip the defers. Tooling: stdlib flag now (one verb, six flags); the second verb gets a NewFlagSet switch; cobra only when completions and nested verbs are actually requested. Distribution: goreleaser-style tarballs with checksums for both platforms, go install as a convenience for Go developers.", "ru": "Сначала контракт, потом код. Потоки: stdout выдаёт ровно одну вещь за запуск — человеческую таблицу или, под --json, один JSON-документ; каждый байт повествования (похостовый прогресс, предупреждения о ретраях, спиннер) идёт в stderr, так что jq-пайплайн и редирект их никогда не видят. Коды выхода — API для CI и получают замороженную документированную таблицу: 0 флот здоров, 1 частичные сбои (CI может ветвиться в мягкий алерт), 2 usage, 3 сломался сам слой опроса; смерть от сигнала проявляется как 130 по конвенции. Формат против декора: --json явный, потому что молчаливое переключение формата превращает проверенную глазами команду в иначе ведущий себя скрипт; term.IsTerminal на stdout плюс NO_COLOR управляют только цветом и индикатором прогресса. Без позиционных аргументов fleetctl читает хосты построчно из stdin, вступая в семью grep-sort. Прерывания: signal.NotifyContext наверху run(), возвращающей int в единственный os.Exit(run()) в main; временный кэш создаётся один раз, удаляется defer-ом, а 30–90-секундный обход передаёт ctx в каждый запрос к хосту, чтобы ctrl-C отменял чисто — больше нигде нет прямого os.Exit, потому что он пропустил бы defer-ы. Инструменты: stdlib flag сейчас (один глагол, шесть флагов); второй глагол получает switch с NewFlagSet; cobra — только когда комплишены и вложенные глаголы реально запрошены. Дистрибуция: tar-архивы в стиле goreleaser с чексуммами под обе платформы, go install — как удобство для Go-разработчиков." }
    }
  ]
}
