{
  "lessonKey": "go/13-systems-projects/02-daemons-and-signals",
  "track": "go",
  "tasks": [
    {
      "id": "predict-swallowed-sigterm-deploy",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Timeline of a deploy against a deaf daemon", "ru": "Хронология деплоя против глухого демона" },
      "prompt": { "en": "A daemon runs under Kubernetes with terminationGracePeriodSeconds: 30. Its code calls <code>signal.Notify(ch, syscall.SIGTERM)</code>, and the only goroutine reading <code>ch</code> logs a line and returns to its loop without stopping anything. A deploy rolls the pod. Predict the full timeline from the kubelet's SIGTERM to the container's end, including what the process is doing in its final moment.", "ru": "Демон работает под Kubernetes с terminationGracePeriodSeconds: 30. Его код вызывает <code>signal.Notify(ch, syscall.SIGTERM)</code>, и единственная горутина, читающая <code>ch</code>, логирует строку и возвращается в свой цикл, ничего не останавливая. Деплой перекатывает под. Предскажи полную хронологию от SIGTERM kubelet-а до конца контейнера, включая то, чем процесс занят в свой последний момент." },
      "scenario": { "en": "Two facts decide everything: signal.Notify replaces the default disposition for registered signals, and SIGKILL is uncatchable. Walk the clock: t=0 SIGTERM arrives — then what does the runtime do, what does the daemon do, and who acts at t=30?", "ru": "Всё решают два факта: signal.Notify заменяет дефолтную диспозицию зарегистрированных сигналов, а SIGKILL неперехватываем. Пройди по часам: t=0 приходит SIGTERM — что дальше делает рантайм, что делает демон и кто действует на t=30?" },
      "reveal": { "en": "t=0: the kubelet sends SIGTERM. Because Notify claimed the signal, the default die-on-TERM is disabled; the runtime delivers a value into ch. The goroutine logs its line and keeps looping — the daemon continues accepting and processing work as if nothing happened. t=0 to t=30: full normal operation; the only trace is one log line. t=30: the grace period expires and the kubelet sends SIGKILL, which no handler can catch — the process dies instantly, mid-whatever-it-was-doing: possibly mid-write, mid-transaction, mid-flush. Every deploy now ends in a hard kill at the worst possible instant, and any state file or queue being written at t=30 is torn. The fix is making SIGTERM mean something: cancel a context, stop intake, drain, exit 0 well inside the 30 s — or simply never register a signal you do not fully handle.", "ru": "t=0: kubelet шлёт SIGTERM. Поскольку Notify забрал сигнал, дефолтное «умереть по TERM» отключено; рантайм доставляет значение в ch. Горутина логирует строку и продолжает цикл — демон принимает и обрабатывает работу, как будто ничего не случилось. t=0 — t=30: полностью обычная работа; единственный след — одна строка лога. t=30: grace-период истекает, и kubelet шлёт SIGKILL, который не ловит никакой обработчик, — процесс умирает мгновенно, посреди того, чем был занят: возможно, посреди записи, транзакции, флаша. Каждый деплой теперь заканчивается жёстким убийством в худший из возможных моментов, и любой файл состояния или очередь, записываемые на t=30, рвутся. Фикс — придать SIGTERM смысл: отменить контекст, остановить приём, дренировать, выйти с 0 задолго до конца 30 с — или просто никогда не регистрировать сигнал, который не обрабатываешь полностью." }
    },
    {
      "id": "diagnose-systemd-timeout-kill",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Why does every restart end in SIGKILL?", "ru": "Почему каждый рестарт заканчивается SIGKILL?" },
      "prompt": { "en": "Ops reports that systemctl restart of a Go service always takes a minute and a half and the journal looks violent — see the evidence. Fill in the blanks: the service blew past ____ (b1), so systemd escalated to ____ (b2); the graceful path the service should implement on SIGTERM is to stop accepting new work and ____ (b3) what is already in flight before exiting.", "ru": "Эксплуатация сообщает: systemctl restart Go-сервиса всегда занимает полторы минуты, и журнал выглядит жестоко — смотри улики. Заполни пропуски: сервис вышел за ____ (b1), поэтому systemd эскалировал до ____ (b2); graceful-путь, который сервис должен реализовать по SIGTERM, — перестать принимать новую работу и ____ (b3) уже начатую перед выходом." },
      "evidence": { "en": "$ journalctl -u feeder --since '-5m'\n12:01:03 systemd[1]: Stopping feeder.service...\n12:02:33 systemd[1]: feeder.service: State 'stop-sigterm' timed out. Killing.\n12:02:33 systemd[1]: feeder.service: Killing process 4127 (feeder) with signal SIGKILL.\n12:02:33 systemd[1]: feeder.service: Failed with result 'timeout'.\n\n$ systemctl show feeder -p TimeoutStopUSec\nTimeoutStopUSec=1min 30s", "ru": "// Улики из журнала (вывод не изменён)\n$ journalctl -u feeder --since '-5m'\n12:01:03 systemd[1]: Stopping feeder.service...\n12:02:33 systemd[1]: feeder.service: State 'stop-sigterm' timed out. Killing.\n12:02:33 systemd[1]: feeder.service: Killing process 4127 (feeder) with signal SIGKILL.\n12:02:33 systemd[1]: feeder.service: Failed with result 'timeout'.\n\n$ systemctl show feeder -p TimeoutStopUSec\nTimeoutStopUSec=1min 30s" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["timeoutstopsec", "TimeoutStopSec", "the stop timeout", "stop timeout", "таймаут остановки", "дедлайн остановки", "TimeoutStopUSec"], "hint": { "en": "The systemd setting shown at the bottom of the evidence — 90 seconds here, the default.", "ru": "Настройка systemd внизу улик — здесь 90 секунд, значение по умолчанию." } },
          { "id": "b2", "accept": ["sigkill", "SIGKILL", "kill -9", "сигкилл", "сигнал SIGKILL"], "hint": { "en": "The uncatchable signal named explicitly in the 12:02:33 journal line.", "ru": "Неперехватываемый сигнал, названный явно в строке журнала за 12:02:33." } },
          { "id": "b3", "accept": ["drain", "finish", "complete", "drain in-flight", "дренировать", "дренаж", "завершить", "доделать", "довести до конца"], "hint": { "en": "The middle clause of the SIGTERM contract: in-flight work is not abandoned, it is...", "ru": "Средний пункт контракта SIGTERM: начатая работа не бросается, она..." } }
        ]
      }
    },
    {
      "id": "design-queue-daemon-lifecycle",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Design the signal lifecycle for a queue daemon", "ru": "Спроектируй сигнальный жизненный цикл демона очереди" },
      "prompt": { "en": "Design the daemon's complete lifecycle: signal wiring, drain sequence and budget, SIGHUP reload, the double-signal escape hatch, and the systemd unit including an honest watchdog decision. Self-grade against the rubric.", "ru": "Спроектируй полный жизненный цикл демона: проводку сигналов, последовательность и бюджет дренажа, перезагрузку по SIGHUP, аварийный люк двойного сигнала и systemd-юнит, включая честное решение по watchdog. Оцени себя по рубрике." },
      "constraints": { "en": "A Go daemon consumes a local disk queue and posts batches to an upstream API; a batch takes up to 8 s. It runs under systemd (TimeoutStopSec=30) on hundreds of hosts. Operators need config reload without restart (upstream URL, batch size) and a way to kill a hung drain by hand. A previous version corrupted the queue file when killed mid-write — that must be impossible by design, not by luck.", "ru": "Go-демон разбирает локальную дисковую очередь и отправляет батчи в upstream API; батч занимает до 8 с. Работает под systemd (TimeoutStopSec=30) на сотнях хостов. Эксплуатации нужны перезагрузка конфига без рестарта (URL upstream-а, размер батча) и способ вручную убить зависший дренаж. Прошлая версия портила файл очереди при убийстве посреди записи — это должно быть невозможно по конструкции, а не по везению." },
      "rubric": [
        { "en": "Builds shutdown on signal.NotifyContext(SIGTERM, SIGINT) with one context tree: intake stops first, the current batch finishes (8 s worst case), state is flushed, and the drain runs under a timeout meaningfully below TimeoutStopSec=30 — naming the budget arithmetic explicitly.", "ru": "Строит shutdown на signal.NotifyContext(SIGTERM, SIGINT) с единым деревом контекстов: сначала останавливается приём, текущий батч доделывается (худший случай 8 с), состояние сбрасывается, а дренаж идёт под таймаутом ощутимо ниже TimeoutStopSec=30 — с явной арифметикой бюджета." },
        { "en": "Implements the double-signal discipline: stop() is called right after the first signal so a second SIGTERM hits the restored default disposition and kills instantly — the operator's documented way to end a hung drain.", "ru": "Реализует дисциплину двойного сигнала: stop() вызывается сразу после первого сигнала, чтобы второй SIGTERM попал в восстановленную дефолтную диспозицию и убил мгновенно — документированный способ оператора оборвать зависший дренаж." },
        { "en": "Routes SIGHUP to a separate channel with transactional reload: load, validate, atomic.Pointer swap; a rejected config keeps the old one running and logs the rejection — the daemon never dies on a bad reload.", "ru": "Ведёт SIGHUP в отдельный канал с транзакционной перезагрузкой: загрузить, провалидировать, подменить через atomic.Pointer; отвергнутый конфиг оставляет старый в работе и логирует отказ — демон никогда не умирает на плохой перезагрузке." },
        { "en": "Makes queue corruption structurally impossible and the unit honest: queue writes go through temp-fsync-rename so SIGKILL mid-write tears nothing; the unit uses Type=simple, Restart=on-failure, logs to stderr for journald, and the watchdog ping (if WatchdogSec is adopted) is sent from the consume loop itself, not a side goroutine.", "ru": "Делает порчу очереди структурно невозможной, а юнит честным: записи очереди идут через temp-fsync-rename, и SIGKILL посреди записи ничего не рвёт; юнит использует Type=simple, Restart=on-failure, логи в stderr для journald, а пинг watchdog (если берётся WatchdogSec) шлётся из самого цикла потребления, не из боковой горутины." }
      ],
      "model": { "en": "One context tree, four signals, zero forks. main is os.Exit(run()); run opens with signal.NotifyContext(ctx, SIGTERM, SIGINT). The consume loop checks ctx before claiming each batch, so the first signal stops intake at a batch boundary. Immediately after ctx.Done(), stop() restores default dispositions: the documented operator action for a hung drain is simply a second kill, which now terminates instantly. Drain budget arithmetic: worst batch 8 s plus state flush plus margin — a 20 s WithTimeout under the 30 s TimeoutStopSec, leaving 10 s of headroom for scheduling and journald flush. SIGHUP rides its own Notify channel: loadAndValidate builds a complete new Config; failure logs and continues on the old one; success stores into atomic.Pointer[Config], which the consume loop Loads per batch — no half-applied state, no death by typo. The corruption fix is structural: every queue-file update writes a temp file in the queue directory, fsyncs, and renames over the old segment, so even SIGKILL at t=30 leaves the previous complete segment in place; recovery on boot is read-and-continue, not fsck. The unit: Type=simple, ExecStart the foreground binary, Restart=on-failure, TimeoutStopSec=30, stderr logging for journald. WatchdogSec=30 is adopted only with the ping inside the consume loop — sd_notify WATCHDOG=1 sent after each batch — because a side-goroutine ping would keep a deadlocked consumer alive forever, which is precisely the failure the watchdog exists to catch.", "ru": "Одно дерево контекстов, четыре сигнала, ноль форков. main — это os.Exit(run()); run начинается с signal.NotifyContext(ctx, SIGTERM, SIGINT). Цикл потребления проверяет ctx перед взятием каждого батча, так что первый сигнал останавливает приём на границе батча. Сразу после ctx.Done() stop() возвращает дефолтные диспозиции: документированное действие оператора при зависшем дренаже — просто второй kill, который теперь завершает мгновенно. Арифметика бюджета дренажа: худший батч 8 с плюс сброс состояния плюс запас — WithTimeout на 20 с под TimeoutStopSec=30, оставляя 10 с на планировщик и флаш journald. SIGHUP едет по своему каналу Notify: loadAndValidate собирает полный новый Config; провал логируется, работа продолжается на старом; успех кладётся в atomic.Pointer[Config], который цикл потребления делает Load на каждый батч — ни полуприменённого состояния, ни смерти от опечатки. Фикс порчи структурный: каждое обновление файла очереди пишет temp-файл в каталоге очереди, делает fsync и rename поверх старого сегмента, так что даже SIGKILL на t=30 оставляет предыдущий целый сегмент на месте; восстановление на старте — прочитать и продолжить, а не fsck. Юнит: Type=simple, ExecStart форграунд-бинарника, Restart=on-failure, TimeoutStopSec=30, логи в stderr для journald. WatchdogSec=30 берётся только с пингом внутри цикла потребления — sd_notify WATCHDOG=1 после каждого батча, — потому что пинг из боковой горутины держал бы дедлокнутого консьюмера живым вечно, а это ровно тот отказ, ради которого watchdog существует." }
    }
  ]
}
