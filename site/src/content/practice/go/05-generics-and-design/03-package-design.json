{
  "lessonKey": "go/05-generics-and-design/03-package-design",
  "track": "go",
  "tasks": [
    {
      "id": "predict-internal-fence-imports",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Who survives the move into internal/?", "ru": "Кто переживёт переезд в internal/?" },
      "prompt": { "en": "A repo has one module with: <code>payments/internal/ledger</code>, importers <code>payments/api</code>, <code>payments/worker/jobs</code>, <code>billing/reports</code> (same module, different top-level dir), and an external module <code>github.com/acme/tools</code>. The ledger package just moved under <code>payments/internal/</code>. Predict which of the four importers still compile, and what error the others get.", "ru": "В репозитории один модуль: <code>payments/internal/ledger</code>, импортёры <code>payments/api</code>, <code>payments/worker/jobs</code>, <code>billing/reports</code> (тот же модуль, другая корневая директория) и внешний модуль <code>github.com/acme/tools</code>. Пакет ledger только что переехал под <code>payments/internal/</code>. Предскажи, какие из четырёх импортёров продолжат компилироваться и какую ошибку получат остальные." },
      "scenario": { "en": "The rule is geometric, not module-scoped: find the parent directory of internal/ and draw the subtree. Everything inside the line keeps the import; everything outside loses it — including code in the same repository.", "ru": "Правило геометрическое, а не модульное: найди родительскую директорию internal/ и очерти поддерево. Всё внутри линии сохраняет импорт; всё снаружи теряет — включая код в том же репозитории." },
      "reveal": { "en": "The parent of internal/ is payments/, so the allowed tree is everything under payments/. payments/api and payments/worker/jobs compile — depth within the tree does not matter. billing/reports breaks even though it is the same module: the compiler rejects it with use of internal package payments/internal/ledger not allowed. The external acme/tools breaks identically. That asymmetry is the point of the design: internal/ is a structure tool inside your own repo, not just a publishing fence — billing was importing another service's bookkeeping, and the move converts that hidden coupling into a compile error that forces either a real public API or a copy of the needed logic into billing's own tree.", "ru": "Родитель internal/ — payments/, значит, разрешённое дерево — всё под payments/. payments/api и payments/worker/jobs компилируются — глубина внутри дерева не важна. billing/reports ломается, хотя это тот же модуль: компилятор отвергает его с use of internal package payments/internal/ledger not allowed. Внешний acme/tools ломается точно так же. Эта асимметрия и есть смысл дизайна: internal/ — инструмент структуры внутри собственного репозитория, а не только забор для публикации; billing импортировал бухгалтерию чужого сервиса, и переезд превращает эту скрытую связанность в ошибку компиляции, заставляющую завести настоящий публичный API или скопировать нужную логику в собственное дерево billing." }
    },
    {
      "id": "diagnose-import-cycle-chain",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Trace the cycle the hotfix tripped", "ru": "Проследи цикл, на который наступил хотфикс" },
      "prompt": { "en": "A hotfix fails to build with the output below. Fill in the blanks: the compiler refused an import ____ (b1); the structural fix is a small interface declared in the ____ package (b2); the package that may import both sides to wire them together is the ____ layer (b3).", "ru": "Хотфикс не собирается, вывод ниже. Заполни пропуски: компилятор отверг ____ импортов (b1); структурный фикс — маленький интерфейс, объявленный в пакете-____ (b2); пакет, которому можно импортировать обе стороны для их соединения, — слой ____ (b3)." },
      "evidence": { "en": "$ go build ./...\npackage acme/billing\n\timports acme/customer\n\timports acme/billing: import cycle not allowed\n\n# the new edge came from this diff:\n--- a/customer/enrich.go\n+++ b/customer/enrich.go\n+import \"acme/billing\"\n+func (s *Service) Enrich(c *Customer) { c.Tier = billing.TierFor(c.Spend) }", "ru": "// вывод go build: компилятор отказывается собирать пакет из-за цикла\n$ go build ./...\npackage acme/billing\n\timports acme/customer\n\timports acme/billing: import cycle not allowed\n\n# новое ребро пришло из этого диффа:\n--- a/customer/enrich.go\n+++ b/customer/enrich.go\n+import \"acme/billing\"\n+func (s *Service) Enrich(c *Customer) { c.Tier = billing.TierFor(c.Spend) }" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["cycle", "import cycle", "цикл", "цикл импортов", "циклический импорт"], "hint": { "en": "The error text names it: billing imports customer imports billing.", "ru": "Текст ошибки его называет: billing импортирует customer, который импортирует billing." } },
          { "id": "b2", "accept": ["consumer", "consuming", "потребитель", "потребителе", "потребляющем", "потребителя"], "hint": { "en": "Implicit satisfaction lets the side that needs the behaviour own the contract — which side needs TierFor?", "ru": "Неявное удовлетворение позволяет стороне, которой нужно поведение, владеть контрактом — какой стороне нужен TierFor?" } },
          { "id": "b3", "accept": ["wiring", "main", "cmd", "composition", "сборки", "сборка", "композиции", "main/cmd"], "hint": { "en": "The top of the graph — cmd/serve — imports everything and injects adapters; nothing imports it back.", "ru": "Вершина графа — cmd/serve — импортирует всё и внедряет адаптеры; её саму не импортирует никто." } }
        ]
      }
    },
    {
      "id": "design-untangle-models-util-service",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Plan the untangling of a models+util codebase", "ru": "Спланируй распутывание кодовой базы с models+util" },
      "prompt": { "en": "Design a behaviour-preserving refactoring plan: target package layout, the order of moves, how the latent cycle is defused, and the metric that proves the refactor worked. Self-grade against the rubric.", "ru": "Спроектируй рефакторинг без изменения поведения: целевую раскладку пакетов, порядок ходов, как обезвреживается латентный цикл и метрику, доказывающую, что рефакторинг сработал. Оцени себя по рубрике." },
      "constraints": { "en": "Repo: orders, inventory, shipping services in one module; a models package (31 shared structs, imported by all three), util (74 exported functions, drags in the AWS SDK), and a latent orders -> inventory -> orders cycle that fires whenever both are touched in one build. Constraints: no behaviour change, PRs of reviewable size (each green on CI), and shipping cannot be frozen — it deploys daily.", "ru": "Репозиторий: сервисы orders, inventory, shipping в одном модуле; пакет models (31 общая структура, импортируется всеми тремя), util (74 экспортированные функции, тянет AWS SDK) и латентный цикл orders -> inventory -> orders, срабатывающий, когда оба пакета попадают в одну сборку. Ограничения: без изменения поведения, PR обозримого размера (каждый зелёный на CI), и shipping нельзя замораживать — он деплоится ежедневно." },
      "rubric": [
        { "en": "Dissolves models by ownership: each struct moves to the package owning its lifecycle, and cross-package consumers get narrow view-structs (or parameters) instead of the shared 31-struct hub.", "ru": "Растворяет models по владению: каждая структура переезжает в пакет, владеющий её жизненным циклом, а межпакетные потребители получают узкие view-структуры (или параметры) вместо общего хаба из 31 структуры." },
        { "en": "Defuses the cycle with a consumer-side interface plus an adapter in the wiring layer — and rejects the shared contracts-package answer explicitly.", "ru": "Обезвреживает цикл интерфейсом на стороне потребителя плюс адаптером в слое сборки — и явно отвергает вариант с общим пакетом contracts." },
        { "en": "Scatters util to call sites (functions with one caller move next to it), promotes genuinely shared survivors into capability-named packages, and calls out the dependency-payload win (AWS SDK no longer rides along).", "ru": "Рассеивает util по точкам вызова (функции с одним вызывающим переезжают к нему), оформляет по-настоящему общие выжившие в пакеты с именами-способностями и отмечает выигрыш по зависимостям (AWS SDK больше не едет прицепом)." },
        { "en": "Sequences for safety: type aliases or forwarding shims keep every intermediate PR compiling, internal/ fences land last, and shipping's daily deploys never block.", "ru": "Выстраивает безопасную последовательность: type alias-ы или форвардинг-прослойки держат каждый промежуточный PR компилируемым, заборы internal/ ставятся последними, и ежедневные деплои shipping не блокируются." },
        { "en": "Defines a success metric beyond aesthetics: change amplification (median packages touched per PR) or build-graph fan-in of the former hubs, measured before and after.", "ru": "Определяет метрику успеха помимо эстетики: амплификация изменений (медиана затронутых пакетов на PR) или fan-in бывших хабов в графе сборки, измеренные до и после." }
      ],
      "model": { "en": "Phase 1 — defuse the live risk first: break the orders -> inventory edge by declaring a one-method StockSource interface inside orders, adapting inventory behind it in cmd/ wiring; no types move yet, cycle pressure gone. Phase 2 — dissolve models with alias bridges: move each struct to its owner (Order to orders, SKU to inventory…), leave type Order = orders.Order aliases in models so all three services compile untouched, then migrate importers service by service (shipping last, in small PRs between its deploys) and delete the empty husk. Cross-service consumers that used two fields of a 30-field struct get their own view-structs. Phase 3 — scatter util: a script counts callers per function; the ~60 single-caller functions move next to their caller as unexported helpers, the rest become retry, money, awsx — and orders' build no longer downloads the AWS SDK it never used. Phase 4 — fence: everything not consumed cross-service moves under each service's internal/, so the next convenient import fails at compile time instead of at the next incident. Metric: median packages touched per PR over the trailing month (expect roughly 3+ to under 2) plus fan-in of models/util going to zero; both are mechanical to pull from git log and the build graph.", "ru": "Фаза 1 — сначала обезвредить живой риск: разорвать ребро orders -> inventory, объявив одно-методный интерфейс StockSource внутри orders и адаптировав inventory за ним в сборке cmd/; типы пока не двигаются, давление цикла снято. Фаза 2 — растворить models через алиасы-мостики: перенести каждую структуру к владельцу (Order в orders, SKU в inventory…), оставить в models алиасы type Order = orders.Order, чтобы все три сервиса компилировались нетронутыми, затем мигрировать импортёров сервис за сервисом (shipping последним, маленькими PR между его деплоями) и удалить пустую оболочку. Межсервисные потребители, использовавшие два поля 30-полевой структуры, получают собственные view-структуры. Фаза 3 — рассеять util: скрипт считает вызывающих на функцию; ~60 функций с единственным вызывающим переезжают к нему неэкспортированными хелперами, остальные становятся retry, money, awsx — и сборка orders больше не качает AWS SDK, которым никогда не пользовалась. Фаза 4 — забор: всё, что не потребляется между сервисами, уезжает под internal/ каждого сервиса, и следующий удобный импорт падает на компиляции, а не на следующем инциденте. Метрика: медиана затронутых пакетов на PR за скользящий месяц (ожидаемо с 3+ до менее 2) плюс fan-in models/util, уходящий в ноль; обе механически достаются из git log и графа сборки." }
    }
  ]
}
