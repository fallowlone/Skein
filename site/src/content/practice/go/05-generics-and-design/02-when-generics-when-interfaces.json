{
  "lessonKey": "go/05-generics-and-design/02-when-generics-when-interfaces",
  "track": "go",
  "tasks": [
    {
      "id": "predict-stringer-constraint-conversion",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "What does converting to [T fmt.Stringer] actually change?", "ru": "Что реально меняет конверсия в [T fmt.Stringer]?" },
      "prompt": { "en": "A PR rewrites <code>func Audit(v fmt.Stringer)</code> as <code>func Audit[T fmt.Stringer](v T)</code>, claiming it removes interface overhead. Call sites pass two things: <code>UserEvent</code> (a 3-field struct, by value) and <code>*Order</code> (a pointer). Predict, per call site, what changes in allocations, dispatch, and generated code — and whether the API contract changed for anyone storing Audit in a variable.", "ru": "PR переписывает <code>func Audit(v fmt.Stringer)</code> в <code>func Audit[T fmt.Stringer](v T)</code>, заявляя, что это убирает интерфейсные накладные расходы. Точки вызова передают две вещи: <code>UserEvent</code> (структура из 3 полей, по значению) и <code>*Order</code> (указатель). Предскажи по каждой точке вызова, что изменится в аллокациях, диспетчеризации и сгенерированном коде — и поменялся ли контракт API для тех, кто хранил Audit в переменной." },
      "scenario": { "en": "Before: every call converts the argument into an interface value (boxing the struct), then dispatches String() through the itab. After: instantiation per GC shape. Think shape by shape.", "ru": "До: каждый вызов конвертирует аргумент в интерфейсное значение (боксит структуру), затем диспетчеризует String() через itab. После: инстанцирование по GC-шейпу. Думай шейп за шейпом." },
      "reveal": { "en": "UserEvent (value shape): a dedicated instantiation is stenciled — no interface boxing allocation, String() can devirtualize and inline. This is the one honest win, and it only matters if Audit is hot. *Order (pointer shape): joins the shared pointer instantiation; String() dispatches indirectly through the gcshape dictionary — roughly the same cost as the itab call it replaced, so no measurable change. Generated code grows: one extra instantiation per value shape that appears, plus dictionaries. And yes, the contract changed: Audit is now a generic function, so code that stored it — <code>var f func(fmt.Stringer) = Audit</code> — breaks; callers must instantiate to get a function value. The review verdict the lesson teaches: behaviour-only signature, so keep the interface unless a profiler says the boxing allocation matters.", "ru": "UserEvent (шейп-значение): стенсилируется выделенное инстанцирование — нет аллокации интерфейсного боксинга, String() может девиртуализоваться и инлайниться. Это единственный честный выигрыш, и он важен, только если Audit горячий. *Order (указательный шейп): попадает в общее указательное инстанцирование; String() диспетчеризуется косвенно через gcshape-словарь — примерно та же цена, что у замещённого itab-вызова, то есть измеримых изменений нет. Сгенерированный код растёт: по лишнему инстанцированию на каждый встретившийся шейп-значение, плюс словари. И да, контракт изменился: Audit теперь дженерик-функция, и код, который её хранил — <code>var f func(fmt.Stringer) = Audit</code>, — ломается; вызывающим придётся инстанцировать, чтобы получить функциональное значение. Вердикт ревью из урока: сигнатура только про поведение — оставить интерфейс, пока профайлер не скажет, что аллокация боксинга важна." }
    },
    {
      "id": "diagnose-modernization-regression",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Read the post-sprint build report", "ru": "Прочитай сборочный отчёт после спринта" },
      "prompt": { "en": "After a generics-modernization sprint the pipeline flags regressions while benchmarks stay flat. Fill in the blanks: the binary grew because each distinct value shape gets its own ____ (b1); runtime stayed flat because pointer callers still dispatch through a ____ (b2); the conversions worth keeping are the ones abstracting ____, not behaviour (b3).", "ru": "После спринта модернизации на дженериках пайплайн помечает регрессии, а бенчмарки ровные. Заполни пропуски: бинарник вырос, потому что каждый отдельный шейп-значение получает своё ____ (b1); рантайм остался ровным, потому что вызовы с указателями всё ещё диспетчеризуются через ____ (b2); конверсии, которые стоит оставить, — те, что абстрагируют ____, а не поведение (b3)." },
      "evidence": { "en": "$ ci-report --compare main..sprint\n\nbinary size:   38.1 MB -> 42.7 MB   (+12.1%)\nfull build:    142 s   -> 191 s     (+34%)\nbenchmarks:    61 suites, 0 significant deltas (all callers pass *T)\n\nchanged: 40 funcs interface->type-param, e.g.\n-  func process(r io.Reader) error\n+  func process[R io.Reader](r R) error\nkept: Set[T comparable], LRU[K comparable, V any]  (new containers)", "ru": "// ci-report: сравнение ветки спринта с main по размеру, сборке и бенчмаркам\n$ ci-report --compare main..sprint\n\nbinary size:   38.1 MB -> 42.7 MB   (+12.1%)\nfull build:    142 s   -> 191 s     (+34%)\nbenchmarks:    61 suites, 0 significant deltas (все вызывающие передают *T)\n\nchanged: 40 funcs interface->type-param, например\n-  func process(r io.Reader) error\n+  func process[R io.Reader](r R) error\nkept: Set[T comparable], LRU[K comparable, V any]  (новые контейнеры)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["instantiation", "stenciled code", "machine code", "code copy", "инстанцирование", "копия кода", "машинный код", "стенсилированный код"], "hint": { "en": "What stenciling emits per value shape — the bytes behind the +12%.", "ru": "Что стенсилинг генерирует на каждый шейп-значение — байты за теми +12%." } },
          { "id": "b2", "accept": ["dictionary", "gcshape dictionary", "dict", "словарь", "gcshape-словарь", "словари"], "hint": { "en": "The hidden argument that pointer-shape instantiations load method addresses from.", "ru": "Скрытый аргумент, из которого указательные инстанцирования грузят адреса методов." } },
          { "id": "b3", "accept": ["data", "data structures", "element types", "containers", "данные", "структуры данных", "типы элементов", "контейнеры"], "hint": { "en": "What Set[T] and LRU[K,V] have in common — the type flows through; nothing is merely called.", "ru": "Что общего у Set[T] и LRU[K,V] — тип течёт насквозь; ничего не просто вызывается." } }
        ]
      }
    },
    {
      "id": "design-api-audit-six-functions",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Audit six signatures: interface, generic, or func argument", "ru": "Аудит шести сигнатур: интерфейс, дженерик или аргумент-функция" },
      "prompt": { "en": "You inherit a shared library and must rule on six proposed signatures: (1) Drain(r io.Reader), (2) Unique[T comparable]([]T) []T, (3) Render[T fmt.Stringer](T) string, (4) Sort customization for a []Server slice, (5) Cache[K, V] container, (6) Validate[T Validator](T) error where Validator has one method. For each: interface, type parameter, or behaviour-as-func — with the rule that justifies it. Self-grade against the rubric.", "ru": "Вам достаётся общая библиотека, и надо вынести вердикт по шести предложенным сигнатурам: (1) Drain(r io.Reader), (2) Unique[T comparable]([]T) []T, (3) Render[T fmt.Stringer](T) string, (4) кастомизация сортировки для слайса []Server, (5) контейнер Cache[K, V], (6) Validate[T Validator](T) error, где у Validator один метод. По каждой: интерфейс, параметр типа или поведение-как-func — с правилом, которое это обосновывает. Оцени себя по рубрике." },
      "constraints": { "en": "The library is imported by ~30 services; signature churn is expensive. Most argument types in practice are pointers. The team has no profiling data showing any of these on a hot path. slices and cmp from the stdlib are available (Go 1.21+).", "ru": "Библиотеку импортируют ~30 сервисов; перетряска сигнатур дорогая. Большинство типов аргументов на практике — указатели. У команды нет профилей, показывающих хоть одну из этих функций на горячем пути. Доступны slices и cmp из стандартной библиотеки (Go 1.21+)." },
      "rubric": [
        { "en": "Applies the rule explicitly: bodies that only call methods (Drain, Render, Validate) keep interface parameters; element-type-flows-through cases (Unique, Cache) get type parameters.", "ru": "Явно применяет правило: тела, которые только вызывают методы (Drain, Render, Validate), оставляют интерфейсные параметры; случаи, где тип элемента течёт насквозь (Unique, Cache), получают параметры типов." },
        { "en": "Routes sort customization through a func value — slices.SortFunc with a comparison function — rather than a method-bearing constraint or a new interface on Server.", "ru": "Проводит кастомизацию сортировки через func-значение — slices.SortFunc с функцией сравнения, — а не через constraint с методами или новый интерфейс на Server." },
        { "en": "Rejects Render's and Validate's generic spelling with the honest caveat: the value-type devirtualization niche exists but requires profiling evidence, which the constraints say is absent — and pointer-heavy callers make it moot anyway.", "ru": "Отклоняет дженерик-запись Render и Validate с честной оговоркой: ниша девиртуализации для типов-значений существует, но требует профилей, которых по условию нет, — а вызывающие с указателями всё равно делают её бессмысленной." },
        { "en": "Accounts for stability: generic signatures freeze type-parameter lists and break function-value assignments across 30 services, so each bracket must pay rent.", "ru": "Учитывает стабильность: дженерик-сигнатуры замораживают списки параметров типов и ломают присваивания в функциональные переменные по 30 сервисам, так что каждая скобка должна окупаться." },
        { "en": "Reuses stdlib vocabulary (comparable, cmp.Ordered, slices) instead of inventing house constraints.", "ru": "Переиспользует словарь стандартной библиотеки (comparable, cmp.Ordered, slices) вместо изобретения доморощенных constraints." }
      ],
      "model": { "en": "(1) Drain: interface — the body only calls Read; a type parameter would re-spell io.Reader with brackets and pointer callers keep dictionary dispatch. (2) Unique: generic, [T comparable] — T flows input to output and keys a map; the interface{} version cannot return a typed slice. (3) Render: interface (fmt.Stringer) — behaviour only; the generic niche (saving boxing for value types) needs a profile the team does not have. (4) Sorting Servers: no new abstraction — slices.SortFunc(servers, func(a, b *Server) int {...}); behaviour enters as a func value, the slices package already owns the algorithm. (5) Cache: generic container, [K comparable, V any] — the canonical data-structure case, like the kept Set/LRU from the lesson. (6) Validate: interface — one-method constraint used once in argument position is an interface wearing brackets; func Validate(v Validator) error is shorter, stabler, and assignable to function variables. Net: two brackets out of six, both on containers/algorithms — matching the stdlib canon where slices/maps/cmp are generic and io/fmt stay interfaces.", "ru": "(1) Drain: интерфейс — тело только вызывает Read; параметр типа переписал бы io.Reader скобками, а вызовы с указателями сохраняют словарную диспетчеризацию. (2) Unique: дженерик, [T comparable] — T течёт от входа к выходу и служит ключом мапы; версия на interface{} не может вернуть типизированный слайс. (3) Render: интерфейс (fmt.Stringer) — только поведение; дженерик-ниша (экономия боксинга для типов-значений) требует профиля, которого у команды нет. (4) Сортировка Servers: без новой абстракции — slices.SortFunc(servers, func(a, b *Server) int {...}); поведение входит func-значением, алгоритмом уже владеет пакет slices. (5) Cache: дженерик-контейнер, [K comparable, V any] — канонический случай структуры данных, как оставленные Set/LRU из урока. (6) Validate: интерфейс — одно-методный constraint, использованный один раз в позиции аргумента, — это интерфейс в скобках; func Validate(v Validator) error короче, стабильнее и присваивается в функциональные переменные. Итог: две скобки из шести, обе на контейнерах/алгоритмах — в точности канон стандартной библиотеки, где slices/maps/cmp дженерики, а io/fmt остаются интерфейсами." }
    }
  ]
}
