{
  "lessonKey": "go/02-concurrency/03-sync-and-races",
  "track": "go",
  "tasks": [
    {
      "id": "predict-hoisted-flag-loop",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Will the worker ever stop?", "ru": "Остановится ли когда-нибудь воркер?" },
      "prompt": { "en": "Goroutine A loops <code>for !done { work() }</code> over a plain <code>var done bool</code>; goroutine B sets <code>done = true</code> after 5 seconds. <code>work()</code> is a leaf function the compiler inlines. Predict the possible behaviors of a normal build, what <code>go test -race</code> says, and whether replacing the bool with <code>atomic.Bool</code> changes the guarantee or just the odds.", "ru": "Горутина A крутит <code>for !done { work() }</code> по обычному <code>var done bool</code>; горутина B ставит <code>done = true</code> через 5 секунд. <code>work()</code> — листовая функция, которую компилятор инлайнит. Предскажи возможные поведения обычной сборки, что скажет <code>go test -race</code> и меняет ли замена bool на <code>atomic.Bool</code> гарантию или только шансы." },
      "scenario": { "en": "There is no synchronization between the write and the reads — no channel, no mutex, no atomic. What is the compiler allowed to assume about a variable the loop never writes?", "ru": "Между записью и чтениями нет синхронизации — ни канала, ни мьютекса, ни атомика. Что компилятор вправе предположить о переменной, которую цикл не пишет?" },
      "reveal": { "en": "A normal build may stop after ~5 s, stop late, or never stop: with no happens-before edge the compiler may hoist the load of <code>done</code> out of the loop (legal, since the loop body provably does not write it once work() is inlined) and spin on a register forever. All three outcomes are correct compilations of an undefined program. <code>go test -race</code> reports a data race deterministically whenever both goroutines actually run — racing read at the loop, racing write in B, both stacks. <code>atomic.Bool</code> changes the <em>guarantee</em>, not the odds: Store/Load are synchronizing operations that create the happens-before edge, obligating the compiler to re-read and the CPU to publish. The loop then terminates promptly — and the race report disappears because there is no race, not because it was hidden.", "ru": "Обычная сборка может остановиться через ~5 с, остановиться поздно или не остановиться никогда: без ребра happens-before компилятор вправе вынести чтение <code>done</code> за цикл (легально: тело цикла доказуемо его не пишет после инлайна work()) и крутиться на регистре вечно. Все три исхода — корректные компиляции неопределённой программы. <code>go test -race</code> детерминированно сообщает о гонке всякий раз, когда обе горутины реально исполняются: гоночное чтение в цикле, гоночная запись в B, оба стека. <code>atomic.Bool</code> меняет <em>гарантию</em>, а не шансы: Store/Load — синхронизирующие операции, создающие ребро happens-before и обязывающие компилятор перечитывать, а CPU — публиковать. Цикл тогда завершается быстро, и отчёт о гонке исчезает потому, что гонки нет, а не потому, что её спрятали." }
    },
    {
      "id": "diagnose-race-report-cache",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Read the -race report and name the fix", "ru": "Прочитай отчёт -race и назови фикс" },
      "prompt": { "en": "CI prints the report below for a request-scoped cache. Fill in the blanks: the two stacks show an unsynchronized write and read with no ____ edge between them (b1); because both fields must change together, the right fix is a ____ (b2), not a pair of atomics; the report cannot be a false positive because the detector flags only races that actually ____ (b3).", "ru": "CI печатает отчёт ниже для кэша уровня запроса. Заполни пропуски: два стека показывают несинхронизированные запись и чтение без ребра ____ между ними (b1); поскольку оба поля должны меняться вместе, правильный фикс — ____ (b2), а не пара атомиков; отчёт не может быть ложным срабатыванием, потому что детектор помечает только гонки, которые реально ____ (b3)." },
      "evidence": { "en": "WARNING: DATA RACE\nWrite at 0x00c0003a2e30 by goroutine 211:\n  acme/api/cache.(*entryCache).put()\n      cache.go:48 +0x6b   // c.entries[k] = v; c.size++\n\nPrevious read at 0x00c0003a2e30 by goroutine 187:\n  acme/api/cache.(*entryCache).stats()\n      cache.go:91 +0x3f   // return c.size, len(c.entries)\n\nGoroutine 211 (running) created at:\n  net/http.(*Server).Serve()", "ru": "WARNING: DATA RACE\nWrite at 0x00c0003a2e30 by goroutine 211:\n  acme/api/cache.(*entryCache).put()\n      cache.go:48 +0x6b   // c.entries[k] = v; c.size++\n\nPrevious read at 0x00c0003a2e30 by goroutine 187:\n  acme/api/cache.(*entryCache).stats()\n      cache.go:91 +0x3f   // return c.size, len(c.entries)\n\nGoroutine 211 (running) created at:\n  net/http.(*Server).Serve()" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["happens-before", "happens before", "hb", "хэппенс-бифор", "happens-before ребро"], "hint": { "en": "The only ordering relation the Go memory model recognizes between goroutines.", "ru": "Единственное отношение порядка, которое модель памяти Go признаёт между горутинами." } },
          { "id": "b2", "accept": ["mutex", "sync.mutex", "мьютекс", "rwmutex", "sync.rwmutex", "лок", "lock"], "hint": { "en": "entries and size form one invariant — they must be guarded as a unit.", "ru": "entries и size образуют один инвариант — их нужно охранять как целое." } },
          { "id": "b3", "accept": ["executed", "ran", "happened", "occurred", "исполнились", "выполнились", "случились", "произошли"], "hint": { "en": "The detector has no false positives and only sees interleavings that…", "ru": "У детектора нет ложных срабатываний, и он видит только те переплетения, которые…" } }
        ]
      }
    },
    {
      "id": "design-read-mostly-config-sync",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Choose the synchronization for a hot read-mostly config", "ru": "Выбери синхронизацию для горячего read-mostly конфига" },
      "prompt": { "en": "Pick and justify the synchronization strategy — Mutex, RWMutex, atomic.Pointer snapshot, or a channel-owning goroutine — for each access pattern, and define how you prove race-freedom in CI. Self-grade against the rubric.", "ru": "Выбери и обоснуй стратегию синхронизации — Mutex, RWMutex, снапшот через atomic.Pointer или горутина-владелец с каналами — для каждого паттерна доступа и определи, как доказывать свободу от гонок в CI. Оцени себя по рубрике." },
      "constraints": { "en": "An API gateway reads routing config on every request: ~50,000 reads/s across goroutines, each read touches 3–4 fields. Config reloads atomically from disk every ~30 s (single writer). Separately, a per-route hit counter increments on every request, and a 'drain mode' bool flips during deploys. p99 budget for the config read path: 1 µs.", "ru": "API-шлюз читает конфиг маршрутизации на каждом запросе: ~50 000 чтений/с из разных горутин, каждое чтение трогает 3–4 поля. Конфиг атомарно перезагружается с диска каждые ~30 с (один писатель). Отдельно: счётчик хитов на маршрут инкрементится на каждом запросе, и bool «drain mode» переключается при деплоях. Бюджет p99 на чтение конфига: 1 мкс." },
      "rubric": [
        { "en": "Config: chooses atomic.Pointer[Config] with immutable snapshots — readers do one pointer load (no lock, fits 1 µs), the writer builds a fresh Config and Stores it; explains why RWMutex at 50k reads/s with nanosecond sections is slower and contended.", "ru": "Конфиг: выбирает atomic.Pointer[Config] с неизменяемыми снапшотами — читатели делают одну загрузку указателя (без лока, укладывается в 1 мкс), писатель строит свежий Config и делает Store; объясняет, почему RWMutex при 50k чтений/с с наносекундными секциями медленнее и конкурентен." },
        { "en": "Counters: atomic.Int64 per route (or sharded counters if contended) — single-word, no multi-field invariant, so no mutex needed.", "ru": "Счётчики: atomic.Int64 на маршрут (или шардированные счётчики при конкуренции) — одно слово, нет многополевого инварианта, мьютекс не нужен." },
        { "en": "Drain flag: atomic.Bool (or a closed channel for one-shot drain), never a plain bool — cites the hoisted-load failure mode.", "ru": "Drain-флаг: atomic.Bool (или закрываемый канал для одноразового drain), никогда не голый bool — ссылается на отказ с вынесенным чтением." },
        { "en": "Rejects the channel-owning-goroutine design for reads: every config read would become a round-trip through a goroutine — wrong tool when nothing transfers ownership.", "ru": "Отвергает горутину-владельца с каналами для чтений: каждое чтение конфига стало бы round-trip через горутину — не тот инструмент, когда владение ничего не передаёт." },
        { "en": "CI proof: go test -race ./... plus a dedicated concurrent test (parallel readers during reload, drain flips) so the racy interleavings actually execute; notes -race finds only executed races.", "ru": "Доказательство в CI: go test -race ./... плюс отдельный конкурентный тест (параллельные читатели во время перезагрузки, переключения drain), чтобы гоночные переплетения реально исполнялись; отмечает, что -race находит только исполнившиеся гонки." }
      ],
      "model": { "en": "Config: var cfg atomic.Pointer[Config]; readers cfg.Load() once per request and read fields off the immutable snapshot — one atomic load ≈ a few ns, no contention, deep inside the 1 µs budget; the reloader parses, validates, then cfg.Store(newCfg) — old snapshots die when their readers finish. Mutating a loaded *Config is forbidden (treat as frozen). Counters: atomic.Int64 increments. Drain: atomic.Bool. RWMutex loses here twice: its read-path bookkeeping costs more than the entire pointer-load read, and 50k RLocks/s contend on the reader count cacheline. A mutex remains correct but serializes the hottest path for no invariant gain — there is no multi-field write to protect once snapshots are immutable. Verification: a test spawning N reader goroutines hammering Load while reloads and drain flips run, executed under -race in CI; the detector's no-false-positive property makes any report actionable, and the test's job is to make racy interleavings execute at all.", "ru": "Конфиг: var cfg atomic.Pointer[Config]; читатели делают cfg.Load() раз на запрос и читают поля неизменяемого снапшота — одна атомарная загрузка ≈ единицы нс, без конкуренции, с большим запасом в бюджете 1 мкс; перезагрузчик парсит, валидирует, затем cfg.Store(newCfg) — старые снапшоты умирают, когда их читатели заканчивают. Мутировать загруженный *Config запрещено (считать замороженным). Счётчики: инкременты atomic.Int64. Drain: atomic.Bool. RWMutex проигрывает дважды: учёт его пути чтения стоит дороже всего чтения через указатель, а 50k RLock/с дерутся за кэш-линию счётчика читателей. Mutex корректен, но сериализует самый горячий путь без выигрыша по инвариантам — при неизменяемых снапшотах защищать многополевую запись нечего. Проверка: тест, порождающий N горутин-читателей, молотящих Load, пока идут перезагрузки и переключения drain, выполняется под -race в CI; отсутствие ложных срабатываний делает любой отчёт поводом к действию, а задача теста — чтобы гоночные переплетения вообще исполнились." }
    }
  ]
}
