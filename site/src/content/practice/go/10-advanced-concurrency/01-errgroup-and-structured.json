{
  "lessonKey": "go/10-advanced-concurrency/01-errgroup-and-structured",
  "track": "go",
  "tasks": [
    {
      "id": "predict-deaf-sibling-latency",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "First error at 100 ms, Wait returns when?", "ru": "Первая ошибка на 100 мс — когда вернётся Wait?" },
      "prompt": { "en": "An errgroup.WithContext group runs three children: A finishes cleanly in 50 ms, B returns an error at 100 ms, C calls a downstream with a hand-rolled 5 s timeout and never looks at the group ctx. Predict: (1) what error Wait returns, (2) approximately when Wait returns, (3) what C does between 100 ms and that moment.", "ru": "Группа errgroup.WithContext запускает трёх потомков: A чисто завершается за 50 мс, B возвращает ошибку на 100 мс, C зовёт downstream с самодельным таймаутом 5 с и ни разу не смотрит на групповой ctx. Предскажи: (1) какую ошибку вернёт Wait, (2) примерно когда Wait вернётся, (3) что C делает между 100 мс и этим моментом." },
      "scenario": { "en": "Walk the timeline. At 100 ms the failing callback returns and the group cancels its ctx. Cancellation is a signal: which of the three children is subscribed to it? Wait is a join point — what is the last child it has to wait for?", "ru": "Пройди по таймлайну. На 100 мс неудачный колбэк возвращается, и группа отменяет свой ctx. Отмена — это сигнал: кто из трёх потомков на него подписан? Wait — точка соединения: какого последнего потомка ему придётся ждать?" },
      "reveal": { "en": "(1) Wait returns the error from B — the first non-nil return; anything C later returns is discarded. (2) Wait returns at roughly the 5-second mark, because it joins ALL children and C ignores the cancelled ctx, running out its private timeout. (3) Between 100 ms and ~5 s, C keeps executing normally: the cancelled ctx changes nothing for code that never selects on ctx.Done or passes ctx into its blocking calls. The group did its job at 100 ms; the remaining 4.9 s is C being a deaf sibling. The fix is plumbing the group ctx into the downstream call (NewRequestWithContext or equivalent), turning Wait latency into ~100 ms.", "ru": "(1) Wait вернёт ошибку B — первый ненулевой возврат; всё, что позже вернёт C, отбрасывается. (2) Wait вернётся примерно на отметке 5 секунд, потому что соединяет ВСЕХ потомков, а C игнорирует отменённый ctx и досиживает свой приватный таймаут. (3) Между 100 мс и ~5 с C продолжает выполняться как ни в чём не бывало: отменённый ctx ничего не меняет для кода, который не делает select по ctx.Done и не передаёт ctx в блокирующие вызовы. Группа сделала свою работу на 100 мс; оставшиеся 4.9 с — это C в роли глухого соседа. Фикс — провести групповой ctx в downstream-вызов (NewRequestWithContext или аналог), что превращает латентность Wait в ~100 мс." }
    },
    {
      "id": "diagnose-discarded-errors-fanout",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Forty-nine results gone and one unattributable error", "ru": "Сорок девять результатов пропали и одна неатрибутируемая ошибка" },
      "prompt": { "en": "An enrichment fan-out over 50 records started failing entire batches — see the evidence. Fill in the blanks: the group stores only the ____ error and discards the rest (b1); because the code uses WithContext, one failure also ____ the sibling calls (b2); for a keep-what-succeeded policy the callbacks should record errors per slot and combine them after Wait with errors.____ (b3).", "ru": "Обогащающее разветвление по 50 записям начало валить целые батчи — смотри улики. Заполни пропуски: группа хранит только ____ ошибку и отбрасывает остальные (b1); из-за WithContext один отказ ещё и ____ вызовы соседей (b2); для политики «сохрани удавшееся» колбэки должны записывать ошибки по слотам и объединять их после Wait через errors.____ (b3)." },
      "evidence": { "en": "// fan-out: g, ctx := errgroup.WithContext(ctx); g.Go per record; return enrich(ctx, rec)\n$ grep 'enrich batch' service.log | tail -3\n14:31:02 ERROR enrich batch failed err=\"upstream 429\"            # which record? unknown\n14:31:02 WARN  batch dropped records=50 enriched=0\n14:31:05 ERROR enrich batch failed err=\"context canceled\"        # retry hit the same wall\n\n# dashboards: per-record success rate 96% — yet whole batches return empty", "ru": "// Разветвление: g, ctx := errgroup.WithContext(ctx); g.Go на запись; return enrich(ctx, rec)\n$ grep 'enrich batch' service.log | tail -3\n14:31:02 ERROR enrich batch failed err=\"upstream 429\"            # какая запись? неизвестно\n14:31:02 WARN  batch dropped records=50 enriched=0\n14:31:05 ERROR enrich batch failed err=\"context canceled\"        # повтор упёрся туда же\n\n# дашборды: успех по записям 96% — но целые батчи возвращаются пустыми" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["first", "first non-nil", "первую", "первая", "первую ненулевую"], "hint": { "en": "A sync.Once guards the slot — whoever returns non-nil earliest wins it.", "ru": "Слот охраняет sync.Once — побеждает тот, кто раньше вернул ненулевую." } },
          { "id": "b2", "accept": ["cancels", "cancel", "отменяет", "отмена", "cancels via ctx", "отменяет через ctx"], "hint": { "en": "The second log line is the tell: 49 healthy calls died of context canceled, not of their own errors.", "ru": "Подсказка во второй строке лога: 49 здоровых вызовов умерли от context canceled, а не от собственных ошибок." } },
          { "id": "b3", "accept": ["join", "Join", "errors.join", "errors.Join", "джойн"], "hint": { "en": "The stdlib function that folds a slice of errors into one, dropping the nils.", "ru": "Функция stdlib, сворачивающая слайс ошибок в одну и отбрасывающая nil." } }
        ]
      }
    },
    {
      "id": "design-fanout-failure-policy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Design the checkout fan-out with a two-tier failure policy", "ru": "Спроектируй разветвление чекаута с двухуровневой политикой отказа" },
      "prompt": { "en": "Design the fan-out: which calls are all-or-nothing, which are best-effort, how the two tiers share one request budget, how every goroutine is guaranteed to exit, and what the returned error tells the caller. Self-grade against the rubric.", "ru": "Спроектируй разветвление: какие вызовы всё-или-ничего, какие best-effort, как два уровня делят один бюджет запроса, как гарантируется завершение каждой горутины и что возвращаемая ошибка говорит вызывающему. Оцени себя по рубрике." },
      "constraints": { "en": "A checkout request fans out to 6 downstreams: inventory, pricing and fraud are mandatory; loyalty, recommendations and analytics are optional enrichment. Request budget: 800 ms end to end. The optional calls must never fail a checkout, and a mandatory failure must not wait for slow optional calls. One incident requirement: no goroutine may outlive the request.", "ru": "Запрос чекаута разветвляется в 6 downstream-вызовов: склад, прайсинг и антифрод обязательны; лояльность, рекомендации и аналитика — опциональное обогащение. Бюджет запроса: 800 мс от края до края. Опциональные вызовы никогда не должны валить чекаут, а отказ обязательного не должен ждать медленных опциональных. Требование из инцидента: ни одна горутина не переживает запрос." },
      "rubric": [
        { "en": "Splits the tiers structurally: one errgroup.WithContext for the three mandatory calls (first error cancels siblings and fails the checkout) and a separate best-effort group whose callbacks record errors per slot and always return nil.", "ru": "Разделяет уровни структурно: один errgroup.WithContext на три обязательных вызова (первая ошибка отменяет соседей и валит чекаут) и отдельная best-effort-группа, чьи колбэки записывают ошибки по слотам и всегда возвращают nil." },
        { "en": "Derives both groups from one parent ctx with the 800 ms deadline, so the request budget bounds every child; optional calls additionally get a shorter own timeout so they cannot consume the whole budget.", "ru": "Выводит обе группы из одного родительского ctx с дедлайном 800 мс, чтобы бюджет запроса ограничивал каждого потомка; опциональные вызовы дополнительно получают свой более короткий таймаут, чтобы не съесть весь бюджет." },
        { "en": "Guarantees exit: every callback passes ctx into its blocking calls (NewRequestWithContext / QueryContext), and channel sends, if any, sit inside select with ctx.Done — names this as the leak rule, not an optimization.", "ru": "Гарантирует завершение: каждый колбэк передаёт ctx в блокирующие вызовы (NewRequestWithContext / QueryContext), а отправки в каналы, если есть, сидят в select с ctx.Done — называет это правилом против утечек, а не оптимизацией." },
        { "en": "Specifies the error contract: mandatory-tier errors are wrapped with the downstream name (fmt.Errorf with %w) so the single surviving error is attributable; optional-tier errors are joined and logged with a metric, never returned.", "ru": "Задаёт контракт ошибок: ошибки обязательного уровня обёрнуты именем downstream (fmt.Errorf с %w), чтобы единственная выжившая ошибка была атрибутируема; ошибки опционального уровня объединяются и логируются с метрикой, но не возвращаются." }
      ],
      "model": { "en": "Two groups, one parent ctx. Parent: ctx, cancel := context.WithTimeout(req.Context(), 800*time.Millisecond), defer cancel. Mandatory tier: g, gctx := errgroup.WithContext(ctx); three g.Go callbacks (inventory, pricing, fraud), each wrapping its error as fmt.Errorf(\"pricing: %w\", err) and writing its result into its own variable — Wait is the happens-before edge, no mutex. First failure cancels gctx, the other mandatory calls return on ctx.Done because every HTTP/DB call takes gctx. Optional tier: a second group started from octx, _ := context.WithTimeout(ctx, 400*time.Millisecond) so enrichment can never eat the full budget; callbacks record errs[i] = call(octx) and return nil — nothing cancels a sibling, nothing fails the checkout. The handler waits mandatory first; on error it returns immediately and the optional ctx, being a child of the request ctx, dies with the request — no goroutine outlives it. On success it waits the optional group, joins its recorded errors into a metric/log line (errors.Join), and degrades the response (no recommendations block) instead of failing. Caller contract: a checkout error always names the mandatory downstream that broke; enrichment failures are observable but invisible to the client.", "ru": "Две группы, один родительский ctx. Родитель: ctx, cancel := context.WithTimeout(req.Context(), 800*time.Millisecond), defer cancel. Обязательный уровень: g, gctx := errgroup.WithContext(ctx); три колбэка g.Go (склад, прайсинг, антифрод), каждый оборачивает ошибку как fmt.Errorf(\"pricing: %w\", err) и пишет результат в свою переменную — Wait даёт happens-before, мьютекс не нужен. Первый отказ отменяет gctx, остальные обязательные возвращаются по ctx.Done, потому что каждый HTTP/DB-вызов принимает gctx. Опциональный уровень: вторая группа от octx, _ := context.WithTimeout(ctx, 400*time.Millisecond), чтобы обогащение не съело весь бюджет; колбэки записывают errs[i] = call(octx) и возвращают nil — никто не отменяет соседа и не валит чекаут. Хендлер сначала ждёт обязательную группу; при ошибке возвращается немедленно, а опциональный ctx, будучи потомком ctx запроса, умирает вместе с запросом — ни одна горутина его не переживает. При успехе ждёт опциональную группу, сворачивает записанные ошибки в метрику/лог (errors.Join) и деградирует ответ (без блока рекомендаций) вместо отказа. Контракт для вызывающего: ошибка чекаута всегда называет сломавшийся обязательный downstream; отказы обогащения наблюдаемы, но невидимы клиенту." }
    },
    {
      "id": "incident-cancelled-batch-kept-running",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "The cancelled batch that ran for another hour", "ru": "Отменённый батч, который работал ещё час" },
      "prompt": { "en": "A nightly product-feed import fans 50,000 items into a partner API under errgroup.WithContext with SetLimit(8). At 00:02 the partner started returning 401 — an expired credential — so the group had its first error within seconds. Yet the job kept hammering the API until 01:04, burned through the partner's rate-limit budget, and the partner opened an abuse ticket. Wait returned exactly one error, an hour late. Work it step by step; commit to an answer before each reveal.", "ru": "Ночной импорт товарного фида разветвляет 50 000 позиций в партнёрский API под errgroup.WithContext с SetLimit(8). В 00:02 партнёр начал отвечать 401 — протух креденшал — так что первая ошибка в группе случилась за секунды. Но джоба продолжала долбить API до 01:04, сожгла rate-limit партнёра, и тот завёл abuse-тикет. Wait вернул ровно одну ошибку — на час позже. Веди разбор по шагам; формулируй ответ до каждого раскрытия." },
      "steps": [
        {
          "label": { "en": "1 · An error at 00:02, traffic until 01:04", "ru": "1 · Ошибка в 00:02, трафик до 01:04" },
          "prompt": { "en": "The group context was cancelled seconds into the run. What exactly did that cancellation do — and what did it NOT do — to the spawning loop and to the callbacks already written?", "ru": "Групповой контекст был отменён через секунды после старта. Что именно эта отмена сделала — и чего НЕ сделала — со спавнящим циклом и с уже написанными колбэками?" },
          "reveal": { "en": "Cancellation is a signal, not a kill. The group cancelled its ctx right after the first failing callback returned — and that closed a channel, nothing more. g.Go does not stop accepting work after a group error: SetLimit blocks the spawner on capacity, never on failure, so the loop happily submitted the remaining ~49,700 items. And each callback ran its full HTTP call because neither the loop nor the callback bodies ever consulted ctx — no ctx.Err() check, no NewRequestWithContext. The group did its job in seconds; the rest of the hour was deaf submissions to a downstream that was already saying no.", "ru": "Отмена — это сигнал, а не убийство. Группа отменила свой ctx сразу после возврата первого неудачного колбэка — и это закрыло канал, не более. g.Go не перестаёт принимать работу после ошибки группы: SetLimit блокирует спавнер по ёмкости, но никогда по отказу, так что цикл радостно отправил оставшиеся ~49 700 позиций. И каждый колбэк выполнял свой HTTP-вызов целиком, потому что ни цикл, ни тела колбэков ни разу не заглянули в ctx — ни проверки ctx.Err(), ни NewRequestWithContext. Группа сделала свою работу за секунды; остаток часа — глухие отправки в downstream, который уже сказал «нет»." }
        },
        {
          "label": { "en": "2 · Why one error, an hour late", "ru": "2 · Почему одна ошибка — и на час позже" },
          "prompt": { "en": "Thousands of calls failed with 401, yet Wait returned a single error at 01:04. Explain both halves with the group's internals.", "ru": "Тысячи вызовов упали с 401, но Wait вернул одну ошибку в 01:04. Объясни обе половины через внутренности группы." },
          "reveal": { "en": "One error: the group keeps a single error slot guarded by sync.Once — the first non-nil return is stored, every later 401 is discarded, so the postmortem sees one unattributed failure instead of a pattern. An hour late: Wait is a join point — it returns only after every g.Go callback has returned, and the spawning loop kept feeding new callbacks for the whole hour. Wait was not slow; it was honestly reporting that the region never emptied. The latency of Wait is always the latency of the least cooperative child plus everything you keep submitting.", "ru": "Одна ошибка: группа хранит единственный слот ошибки под sync.Once — сохраняется первый ненулевой возврат, каждый последующий 401 отбрасывается, и постмортем видит один неатрибутированный отказ вместо паттерна. На час позже: Wait — точка соединения, он возвращается только после возврата каждого колбэка g.Go, а спавнящий цикл целый час подкармливал новые. Wait не тормозил; он честно сообщал, что регион так и не опустел. Латентность Wait — это всегда латентность наименее кооперативного потомка плюс всё, что ты продолжаешь отправлять." }
        },
        {
          "label": { "en": "3 · Fix and prevention", "ru": "3 · Фикс и профилактика" },
          "prompt": { "en": "Name the minimal code changes that make a failing batch stop within seconds, and the test that keeps it that way.", "ru": "Назови минимальные изменения кода, чтобы падающий батч останавливался за секунды, и тест, который это закрепит." },
          "reveal": { "en": "Three lines of discipline. In the spawn loop: break (or select) on ctx.Err() so submission stops at the first group error. First line of every callback: if err := ctx.Err(); err != nil { return err } so queued work exits instantly. And pass the group ctx into the actual call — http.NewRequestWithContext(gctx, ...) — so in-flight requests abort too. Wrap each error with item identity (fmt.Errorf(\"item %s: %w\", ...)) so the one surviving error is attributable. Prevention: a review rule that every g.Go body has a return path through ctx, and a test that feeds a 1k-item fake batch with a poisoned early item and asserts the job ends within a bounded time and the downstream call count stays near the failure point — not near the batch size.", "ru": "Три строки дисциплины. В цикле спавна: break (или select) по ctx.Err(), чтобы отправка прекращалась на первой ошибке группы. Первая строка каждого колбэка: if err := ctx.Err(); err != nil { return err } — чтобы очередь выходила мгновенно. И передавай групповой ctx в сам вызов — http.NewRequestWithContext(gctx, ...) — чтобы и in-flight запросы обрывались. Оборачивай каждую ошибку идентичностью позиции (fmt.Errorf(\"item %s: %w\", ...)), чтобы единственная выжившая ошибка была атрибутируема. Профилактика: правило ревью «у каждого тела g.Go есть путь выхода через ctx» и тест, который скармливает фейковый батч из 1k позиций с отравленной ранней позицией и проверяет, что джоба завершается за ограниченное время, а число вызовов downstream остаётся около точки отказа — а не около размера батча." }
        }
      ]
    },
    {
      "id": "review-fanout-collector",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Review a fan-out collector", "ru": "Отревьюй сборщик разветвления" },
      "prompt": { "en": "This function fans out item fetches under an errgroup and collects the results. Review it like a senior: name the real defects, don't flag non-issues.", "ru": "Эта функция разветвляет загрузку позиций под errgroup и собирает результаты. Отревьюй как senior: назови реальные дефекты и не цепляйся к не-проблемам." },
      "diff": {
        "lang": "go",
        "code": "func fetchAll(ctx context.Context, ids []string) ([]Item, error) {\n\tg, gctx := errgroup.WithContext(ctx)\n\tg.SetLimit(8)\n\n\titems := make([]Item, 0, len(ids))\n\tfor _, id := range ids {\n\t\tg.Go(func() error {\n\t\t\tit, err := fetchItem(ctx, id) // per-item fetch\n\t\t\tif err != nil {\n\t\t\t\treturn fmt.Errorf(\"fetch %s: %w\", id, err)\n\t\t\t}\n\t\t\titems = append(items, it)\n\t\t\treturn nil\n\t\t})\n\t}\n\tif err := g.Wait(); err != nil {\n\t\treturn nil, err\n\t}\n\treturn items, nil\n}"
      },
      "findings": [
        { "id": "outer-ctx-ignores-group-cancel", "severity": "bug", "planted": true, "label": { "en": "Callbacks use the outer ctx, so the first error cancels nobody", "ru": "Колбэки используют внешний ctx, и первая ошибка никого не отменяет" }, "explanation": { "en": "WithContext returns gctx precisely so the first error cancels the siblings — but fetchItem receives the outer ctx, which the group never cancels. On a failure every other in-flight fetch runs to its own timeout and Wait stretches to the slowest deaf sibling. Pass gctx into fetchItem (and into the HTTP call inside it).", "ru": "WithContext возвращает gctx именно затем, чтобы первая ошибка отменяла соседей — но fetchItem получает внешний ctx, который группа никогда не отменяет. При отказе каждый остальной in-flight запрос досиживает свой собственный таймаут, и Wait растягивается до самого глухого соседа. Передавай gctx в fetchItem (и в HTTP-вызов внутри него)." } },
        { "id": "racy-append-shared-slice", "severity": "bug", "planted": true, "label": { "en": "Concurrent append to a shared slice is a data race", "ru": "Конкурентный append в общий слайс — гонка данных" }, "explanation": { "en": "items = append(items, it) runs from up to 8 goroutines at once: append reads and writes the same len/cap header, so results get lost and the backing array can be corrupted. Either preallocate items := make([]Item, len(ids)) and write items[i] per goroutine — disjoint slots need no mutex, and Wait provides the happens-before edge for the final read — or guard the append with a mutex.", "ru": "items = append(items, it) выполняется из до 8 горутин разом: append читает и пишет один и тот же заголовок len/cap, результаты теряются, а backing-массив может быть повреждён. Либо предвыдели items := make([]Item, len(ids)) и пиши items[i] в каждой горутине — непересекающиеся слоты не требуют мьютекса, а Wait даёт happens-before для финального чтения — либо защити append мьютексом." } },
        { "id": "no-race-cancellation-test", "severity": "missing-test", "planted": true, "label": { "en": "No -race run and no first-error-cancels test", "ru": "Нет прогона с -race и теста «первая ошибка отменяет»" }, "explanation": { "en": "Both planted defects are invisible to a sequential happy-path test: the race needs go test -race with concurrent items, and the dead cancellation needs a test where one early item fails and the assertion is that sibling fetches abort promptly (bounded duration, bounded downstream call count). Without those, this merges green.", "ru": "Оба заложенных дефекта невидимы последовательному happy-path тесту: гонке нужен go test -race с конкурентными позициями, а мёртвой отмене — тест, где ранняя позиция падает, и проверяется, что соседние загрузки быстро обрываются (ограниченная длительность, ограниченное число вызовов downstream). Без этого код мёржится зелёным." } }
      ],
      "decoys": [
        { "id": "loop-var-capture", "label": { "en": "The closure captures the loop variable id", "ru": "Замыкание захватывает переменную цикла id" }, "explanation": { "en": "Since Go 1.22 each iteration gets its own id, so capturing it in the g.Go closure is correct — the classic pre-1.22 footgun no longer applies here.", "ru": "С Go 1.22 каждая итерация получает собственную id, так что захват её в замыкании g.Go корректен — классический футган до 1.22 здесь больше не действует." } },
        { "id": "set-limit-eight", "label": { "en": "SetLimit(8) looks arbitrary and too low", "ru": "SetLimit(8) выглядит произвольным и слишком маленьким" }, "explanation": { "en": "A bounded fan-out is the point, and 8 is a tuning choice, not a defect; the call is also correctly placed before any g.Go (changing it with active children panics). Size it by the downstream's budget, but don't flag the existence of the bound.", "ru": "Ограниченное разветвление и есть цель, а 8 — вопрос настройки, не дефект; вызов к тому же корректно стоит до любого g.Go (менять лимит при активных потомках — паника). Подбирай по бюджету downstream, но не флагай само наличие ограничения." } }
      ]
    }
  ]
}
