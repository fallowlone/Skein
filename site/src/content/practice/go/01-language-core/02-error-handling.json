{
  "lessonKey": "go/01-language-core/02-error-handling",
  "track": "go",
  "tasks": [
    {
      "id": "02-errors-recall-verb-and-walkers",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": {
        "en": "Recall the verb, the two walkers, and the discard",
        "ru": "Вспомни глагол, двух обходчиков и отбрасывание"
      },
      "prompt": {
        "en": "Four blanks on the core vocabulary of Go error handling. Fill each with the exact identifier or verb the lesson uses — no scenario, just the mechanics you should be able to recall.",
        "ru": "Четыре пропуска по базовому словарю обработки ошибок в Go. Заполни каждый точным идентификатором или глаголом из урока — без сценария, только механика, которую нужно помнить."
      },
      "evidence": {
        "en": "Blank 1: the fmt.Errorf verb that STORES the wrapped error and gives the result an Unwrap() method (its sibling %v only formats text).\n\nBlank 2: the errors-package function that walks the Unwrap chain matching a sentinel VALUE — the wrap-safe replacement for ==.\n\nBlank 3: the errors-package function that walks the chain matching a TYPE and copies the match out so you can read its fields.\n\nBlank 4: the one-character blank identifier that discards a returned error — the construct that, used without justification, lets a zero value flow downstream and detonate far from the cause.",
        "ru": "Пропуск 1: глагол fmt.Errorf, который СОХРАНЯЕТ обёрнутую ошибку и даёт результату метод Unwrap() (его собрат %v лишь форматирует текст).\n\nПропуск 2: функция пакета errors, которая идёт по цепочке Unwrap, матча ЗНАЧЕНИЕ-сентинел — безопасная к обёрткам замена ==.\n\nПропуск 3: функция пакета errors, которая идёт по цепочке, матча ТИП, и копирует совпадение наружу, чтобы можно было прочитать его поля.\n\nПропуск 4: однобуквенный пустой идентификатор, отбрасывающий возвращённую ошибку — конструкция, которая без обоснования пускает нулевое значение вниз по потоку, и оно взрывается далеко от причины."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "%w",
              "w",
              "w verb",
              "the %w verb",
              "глагол %w",
              "процент w"
            ],
            "hint": {
              "en": "Two characters in the format string — the verb that makes the new error wrap, not just describe, the cause.",
              "ru": "Два символа в форматной строке — глагол, который заставляет новую ошибку обёртывать причину, а не только её описывать."
            }
          },
          {
            "id": "b2",
            "accept": [
              "errors.Is",
              "Is",
              "errors.Is()",
              "эррорс ис"
            ],
            "hint": {
              "en": "Answers 'is any link in the chain this exact value?' — use it instead of == on wrapped sentinels.",
              "ru": "Отвечает на «является ли какое-то звено цепочки именно этим значением?» — используй вместо == на обёрнутых сентинелах."
            }
          },
          {
            "id": "b3",
            "accept": [
              "errors.As",
              "As",
              "errors.As()",
              "эррорс ас"
            ],
            "hint": {
              "en": "Answers 'is any link in the chain this TYPE?' and extracts it into a pointer you pass — reach a *os.PathError's path through any wrap depth.",
              "ru": "Отвечает на «есть ли в цепочке звено этого ТИПА?» и извлекает его в переданный указатель — достань путь *os.PathError сквозь любую глубину обёрток."
            }
          },
          {
            "id": "b4",
            "accept": [
              "_",
              "underscore",
              "blank identifier",
              "the blank identifier",
              "подчёркивание",
              "пустой идентификатор",
              "знак подчёркивания"
            ],
            "hint": {
              "en": "One character that tells the compiler 'throw this value away' — fine with a comment, a bug-factory without one.",
              "ru": "Один символ, говорящий компилятору «выброси это значение» — нормально с комментарием, фабрика багов без него."
            }
          }
        ]
      }
    },
    {
      "id": "02-errors-predict-wrap-chain",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict what == , errors.Is, and %v do to a wrapped sentinel",
        "ru": "Предскажи, что ==, errors.Is и %v сделают с обёрнутым сентинелом"
      },
      "prompt": {
        "en": "Trace the error chain each call builds, then reveal: which comparisons match, which fail, and where the chain gets severed.",
        "ru": "Протрассируй цепочку ошибок, которую строит каждый вызов, затем открой ответ: какие сравнения совпадут, какие провалятся и где цепочка обрывается."
      },
      "scenario": {
        "en": "var ErrNotFound = errors.New(\"not found\")\n\nfunc repo() error  { return fmt.Errorf(\"repo: %w\", ErrNotFound) }\nfunc svc() error   { return fmt.Errorf(\"svc: %w\", repo()) }\nfunc sealed() error{ return fmt.Errorf(\"api: %v\", repo()) } // note: %v\n\nerr := svc()\n\nQ1: err == ErrNotFound — true or false?\nQ2: errors.Is(err, ErrNotFound) — true or false, and through how many links?\nQ3: errors.Is(sealed(), ErrNotFound) — true or false, and why?",
        "ru": "var ErrNotFound = errors.New(\"not found\")\n\nfunc repo() error  { return fmt.Errorf(\"repo: %w\", ErrNotFound) }\nfunc svc() error   { return fmt.Errorf(\"svc: %w\", repo()) }\nfunc sealed() error{ return fmt.Errorf(\"api: %v\", repo()) } // обрати внимание: %v\n\nerr := svc()\n\nВопрос 1: err == ErrNotFound — true или false?\nВопрос 2: errors.Is(err, ErrNotFound) — true или false, и через сколько звеньев?\nВопрос 3: errors.Is(sealed(), ErrNotFound) — true или false, и почему?"
      },
      "reveal": {
        "en": "Q1: false. err is the value created by the outermost fmt.Errorf — a brand-new error wrapping another. == compares the identity of that outer value against the sentinel; they are different values, so false. This is exactly the comparison that silently broke when a repository layer added wrapping.\n\nQ2: true, through two links. errors.Is unwraps svc's error to repo's error, then repo's error to ErrNotFound, and matches the sentinel by identity at the second link. Wrap depth doesn't matter — Is walks until it matches or the chain ends.\n\nQ3: false. %v formats repo()'s error into the message STRING and stores nothing — the returned error has no Unwrap method, so the chain is severed at that layer. errors.Is has nothing to walk and returns false. That's a bug if callers were supposed to branch on ErrNotFound, and a legitimate technique if the API layer deliberately seals internals so callers can't couple to them. The difference between %w and %v is one character in the verb and the entire difference between a traversable causal chain and a flat string.",
        "ru": "Вопрос 1: false. err — значение, созданное внешним fmt.Errorf: совершенно новая ошибка, оборачивающая другую. == сравнивает идентичность этого внешнего значения с сентинелом; это разные значения, поэтому false. Ровно это сравнение молча сломалось, когда слой репозитория добавил обёртывание.\n\nВопрос 2: true, через два звена. errors.Is разворачивает ошибку svc до ошибки repo, затем ошибку repo до ErrNotFound и матчит сентинел по идентичности на втором звене. Глубина обёртывания не важна — Is идёт, пока не совпадёт или цепочка не закончится.\n\nВопрос 3: false. %v форматирует ошибку repo() в СТРОКУ сообщения и ничего не сохраняет — у возвращённой ошибки нет метода Unwrap, цепочка обрывается на этом слое. errors.Is нечего обходить, и он возвращает false. Это баг, если вызывающие должны были ветвиться по ErrNotFound, и легитимный приём, если слой API намеренно запечатывает внутренности, чтобы вызывающие к ним не привязывались. Разница между %w и %v — один символ в глаголе и вся разница между обходимой причинной цепочкой и плоской строкой."
      }
    },
    {
      "id": "02-errors-diagnose-swallowed-zero-value",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Trace the nil-deref back to the swallowed error",
        "ru": "Проследи nil-deref до проглоченной ошибки"
      },
      "prompt": {
        "en": "A checkout service panics with a nil pointer dereference in renderInvoice, but the database was the thing that failed. Read the evidence and fill in the blanks: the discard that caused it, the verb that keeps a chain inspectable, and the function that extracts a typed error.",
        "ru": "Сервис чекаута паникует с разыменованием nil-указателя в renderInvoice, хотя отказала база данных. Прочитай улики и заполни пропуски: отбрасывание, которое это вызвало, глагол, сохраняющий цепочку проверяемой, и функцию, извлекающую типизированную ошибку."
      },
      "evidence": {
        "en": "// checkout.go\ncust, _ := loadCustomer(ctx, id)     // line A: db timeout happened HERE\ninv := buildInvoice(cust)            // cust is nil — the zero value of *Customer\n\n// render.go (panics two files away)\nfunc renderInvoice(inv *Invoice) {\n    fmt.Println(inv.Customer.Name)   // nil pointer dereference\n}\n\n// repo.go — the team also wants to branch on the driver's typed error:\n// *pq.Error carries a Code field, but it arrives wrapped in two layers of context.\n\nBlank 1: the bug's root cause is the identifier used at line A to discard the error — name it.\nBlank 2: for the repo's wrapping to keep the chain inspectable, fmt.Errorf must use which verb?\nBlank 3: to reach the *pq.Error and its Code through the wrap layers, which errors-package function do you call?",
        "ru": "// checkout.go\ncust, _ := loadCustomer(ctx, id)     // строка A: таймаут БД случился ЗДЕСЬ\ninv := buildInvoice(cust)            // cust равен nil — нулевое значение *Customer\n\n// render.go (паникует двумя файлами дальше)\nfunc renderInvoice(inv *Invoice) {\n    fmt.Println(inv.Customer.Name)   // разыменование nil-указателя\n}\n\n// repo.go — команда также хочет ветвиться по типизированной ошибке драйвера:\n// *pq.Error несёт поле Code, но приходит обёрнутым в два слоя контекста.\n\nПропуск 1: первопричина бага — идентификатор, которым на строке A отбросили ошибку, — назови его.\nПропуск 2: чтобы обёртывание в repo сохраняло цепочку проверяемой, какой глагол должен использовать fmt.Errorf?\nПропуск 3: чтобы добраться до *pq.Error и его Code сквозь слои обёрток, какую функцию пакета errors вызвать?"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "_",
              "underscore",
              "blank identifier",
              "подчёркивание",
              "пустой идентификатор",
              "знак подчёркивания"
            ],
            "hint": {
              "en": "One character. It tells the compiler — and your teammates — that the error is deliberately ignored, with no comment saying why.",
              "ru": "Один символ. Он говорит компилятору — и коллегам, — что ошибка проигнорирована намеренно, без комментария почему."
            }
          },
          {
            "id": "b2",
            "accept": [
              "%w",
              "w verb",
              "глагол %w",
              "процент w"
            ],
            "hint": {
              "en": "The fmt verb that stores the wrapped error and gives the result an Unwrap method — its sibling %v only formats text.",
              "ru": "Глагол fmt, который сохраняет обёрнутую ошибку и даёт результату метод Unwrap — его собрат %v лишь форматирует текст."
            }
          },
          {
            "id": "b3",
            "accept": [
              "errors.As",
              "As",
              "errors.As()",
              "эррорс ас"
            ],
            "hint": {
              "en": "Walks the Unwrap chain looking for a link assignable to your target's TYPE and copies it out — its sibling matches sentinel values.",
              "ru": "Идёт по цепочке Unwrap в поисках звена, присваиваемого ТИПУ вашей цели, и копирует его наружу — его собрат матчит значения-сентинелы."
            }
          }
        ]
      }
    },
    {
      "id": "02-errors-design-payment-error-strategy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design the error strategy for a payments service",
        "ru": "Спроектируй стратегию ошибок для платёжного сервиса"
      },
      "prompt": {
        "en": "Design the error architecture for the payments service below: which failures get sentinels, which get types, which stay opaque; the wrapping policy per layer; and the panic/recover policy. Self-grade against the rubric.",
        "ru": "Спроектируй архитектуру ошибок для платёжного сервиса ниже: какие отказы получают сентинелы, какие — типы, какие остаются непрозрачными; политику обёртывания по слоям; и политику panic/recover. Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Three layers: HTTP handlers -> payment domain service -> two adapters (Postgres repo, third-party card gateway). Callers must distinguish: insufficient funds (show the user a message), card declined with a gateway reason code (shown to the user AND logged for disputes), transient gateway timeouts (retried up to 2 times), and everything else (500 + page on-call). Handlers spawn a goroutine per webhook to notify merchants. The gateway SDK panics on malformed responses (vendor bug, ticket open). Error messages reaching users must never leak internal details like SQL or gateway URLs.",
        "ru": "Три слоя: HTTP-хендлеры -> доменный сервис платежей -> два адаптера (Postgres-репозиторий, сторонний карточный шлюз). Вызывающие обязаны различать: недостаточно средств (показать пользователю сообщение), отказ по карте с кодом причины от шлюза (показывается пользователю И логируется для диспутов), временные таймауты шлюза (ретрай до 2 раз) и всё остальное (500 + пейдж дежурному). Хендлеры запускают горутину на каждый вебхук для уведомления мерчантов. SDK шлюза паникует на искажённых ответах (баг вендора, тикет открыт). Сообщения об ошибках, доходящие до пользователей, не должны утекать внутренними деталями вроде SQL или URL шлюза."
      },
      "rubric": [
        {
          "en": "Matches idiom to need: a sentinel (var ErrInsufficientFunds) for the data-free branch; a typed error (struct with gateway Code field, matched via errors.As) for card declines that carry data; opaque wrapped errors for everything else — and justifies each as a coupling decision, exporting nothing callers don't branch on.",
          "ru": "Сопоставляет идиому с потребностью: сентинел (var ErrInsufficientFunds) для ветвления без данных; типизированная ошибка (структура с полем Code шлюза, матчится через errors.As) для отказов по карте, несущих данные; непрозрачные обёрнутые ошибки для всего остального — и обосновывает каждое как решение о связности, не экспортируя ничего, по чему вызывающие не ветвятся."
        },
        {
          "en": "Defines a wrapping policy: adapters and domain wrap with %w plus operation context ('charge card %s: %w'), while the HTTP layer seals with %v or maps to safe user-facing messages, so internals (SQL, gateway URLs) never reach the response body.",
          "ru": "Определяет политику обёртывания: адаптеры и домен оборачивают через %w с контекстом операции ('charge card %s: %w'), а HTTP-слой запечатывает через %v или маппит в безопасные пользовательские сообщения, чтобы внутренности (SQL, URL шлюза) никогда не попадали в тело ответа."
        },
        {
          "en": "Routes retries through errors.Is on a transient sentinel (or a Temporary-style typed check) so the retry loop in the domain layer matches timeouts through any wrap depth, capped at 2 attempts.",
          "ru": "Маршрутизирует ретраи через errors.Is по транзиентному сентинелу (или типизированную проверку в стиле Temporary), чтобы retry-цикл в доменном слое матчил таймауты сквозь любую глубину обёрток, с лимитом в 2 попытки."
        },
        {
          "en": "Contains the vendor SDK panic: a deferred recover wrapping ONLY the gateway adapter call converts the panic into an opaque error (preserving the panic value in the message), instead of a process-wide crash or a blanket recover hiding real bugs.",
          "ru": "Локализует панику вендорского SDK: отложенный recover, оборачивающий ТОЛЬКО вызов адаптера шлюза, превращает панику в непрозрачную ошибку (сохраняя значение паники в сообщении), вместо падения всего процесса или огульного recover, прячущего настоящие баги."
        },
        {
          "en": "Protects the webhook goroutines: each go func carries its own deferred recover (panics don't cross goroutine boundaries — handler middleware can't catch them), and their errors are reported via a channel or logger, never discarded with _.",
          "ru": "Защищает горутины вебхуков: каждая go func несёт собственный отложенный recover (паники не пересекают границы горутин — middleware хендлера их не поймает), а их ошибки сообщаются через канал или логгер и никогда не отбрасываются через _."
        }
      ],
      "model": {
        "en": "Domain package exports exactly two error symbols. ErrInsufficientFunds is a sentinel — the caller needs identity only, no data, so a package-level errors.New value matched with errors.Is is the cheapest stable contract. DeclineError is a typed error: type DeclineError struct { Code, Reason string } with Error() string — handlers need the gateway code for the user message and disputes log, so errors.As extracts it through any wrapping. Transient gateway failures get an unexported sentinel errTransient wrapped into returns; the domain retry loop does for attempt := 0; attempt < 3; attempt++ { ... if !errors.Is(err, errTransient) { break } } — unexported because retryability is a domain decision, not caller API. Everything else stays opaque: wrapped with %w and operation context at each layer (\"persist payment %s: %w\"), so the final log line is a causal chain, but nothing is exported to couple to. The HTTP layer is the sealing boundary: it switches on errors.Is/As for the three known cases and maps them to 402/402/503 with curated messages; the default branch logs the full chain, returns a generic 500 body, and never interpolates err.Error() into the response — that's how SQL fragments leak. The gateway adapter wraps the SDK call in func() (err error) { defer func() { if r := recover(); r != nil { err = fmt.Errorf(\"gateway sdk panic: %v\", r) } }(); ... } — recover scoped to the one call known to panic, converting a vendor bug into a retriable-or-500 error instead of a process death, while leaving genuinely unexpected panics elsewhere fatal and loud. Webhook notifications run as go func() { defer recoverAndLog(); if err := notify(m); err != nil { log... } }() — each goroutine owns its recover because the handler's middleware cannot catch a child goroutine's panic, and notify errors are logged with the merchant ID, never discarded.",
        "ru": "Доменный пакет экспортирует ровно два символа ошибок. ErrInsufficientFunds — сентинел: вызывающему нужна только идентичность, без данных, поэтому значение errors.New уровня пакета, матчуемое errors.Is, — самый дешёвый стабильный контракт. DeclineError — типизированная ошибка: type DeclineError struct { Code, Reason string } с Error() string — хендлерам нужен код шлюза для сообщения пользователю и лога диспутов, и errors.As извлекает его сквозь любые обёртки. Временные отказы шлюза получают неэкспортируемый сентинел errTransient, оборачиваемый в возвраты; retry-цикл домена делает for attempt := 0; attempt < 3; attempt++ { ... if !errors.Is(err, errTransient) { break } } — неэкспортируемый, потому что ретраябельность — решение домена, а не API вызывающих. Всё остальное остаётся непрозрачным: обёрнуто %w с контекстом операции на каждом слое (\"persist payment %s: %w\"), так что итоговая строка лога — причинная цепочка, но наружу не экспортировано ничего, к чему можно привязаться. HTTP-слой — граница запечатывания: он ветвится errors.Is/As по трём известным случаям и маппит их в 402/402/503 с выверенными сообщениями; ветка default логирует полную цепочку, возвращает generic-тело 500 и никогда не интерполирует err.Error() в ответ — именно так утекают фрагменты SQL. Адаптер шлюза оборачивает вызов SDK в func() (err error) { defer func() { if r := recover(); r != nil { err = fmt.Errorf(\"gateway sdk panic: %v\", r) } }(); ... } — recover ограничен единственным вызовом, про который известно, что он паникует, превращая баг вендора в ошибку (ретраябельную или 500) вместо смерти процесса, и оставляя по-настоящему неожиданные паники в других местах фатальными и громкими. Уведомления вебхуков выполняются как go func() { defer recoverAndLog(); if err := notify(m); err != nil { log... } }() — каждая горутина владеет своим recover, потому что middleware хендлера не может поймать панику дочерней горутины, а ошибки notify логируются с ID мерчанта и никогда не отбрасываются."
      }
    },
    {
      "id": "incident-sealed-chain-decline-storm",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": {
        "en": "A log cleanup turns card declines into a pager storm",
        "ru": "«Чистка логов» превращает отказы по картам в шторм алертов"
      },
      "prompt": {
        "en": "A payments service classifies processor failures in its handler: errors.Is(err, ErrInsufficientFunds) maps to 402, no retry. A PR titled 'tidy error messages' lands in the adapter layer — 'the messages were doubling up'. Within an hour of deploy: 5xx rate jumps to 8%, the retry queue is hammering the processor with repeat attempts on declined cards (duplicate-authorization risk), and on-call is paged. Declines are ~7% of traffic and a normal business outcome. Work it step by step; commit before revealing.",
        "ru": "Платёжный сервис классифицирует отказы процессинга в хендлере: errors.Is(err, ErrInsufficientFunds) даёт 402 и никаких ретраев. В слой адаптера заезжает PR с названием «причесать сообщения об ошибках» — «тексты дублировались». Через час после деплоя: доля 5xx подскакивает до 8%, очередь ретраев долбит процессинг повторными попытками по отклонённым картам (риск дублирующих авторизаций), дежурного будит пейджер. Отказы — ~7% трафика и нормальный бизнес-исход. Веди по шагам; сформулируй ответ до раскрытия."
      },
      "steps": [
        {
          "label": {
            "en": "1 · A cosmetic diff with teeth",
            "ru": "1 · Косметический диф с зубами"
          },
          "prompt": {
            "en": "The PR only touched fmt.Errorf format strings, and the log lines still contain 'insufficient funds'. What exactly stopped working in the handler, and why did the diff look harmless in review?",
            "ru": "PR трогал только форматные строки fmt.Errorf, и в логах по-прежнему есть «insufficient funds». Что именно сломалось в хендлере и почему диф выглядел безобидным на ревью?"
          },
          "reveal": {
            "en": "The handler's classification went blind: errors.Is(err, ErrInsufficientFunds) now returns false for every decline, so declines fall into the default branch — unknown failure, 500, retry. The diff looked harmless because the error TEXT is unchanged — 'insufficient funds' still appears in logs. What changed is the error's structure: the PR swapped %w for %v, so the sentinel is no longer in the chain, only its prose. Identity checks read values, never strings.",
            "ru": "Классификация в хендлере ослепла: errors.Is(err, ErrInsufficientFunds) теперь возвращает false для каждого отказа, и отказы падают в ветку по умолчанию — неизвестный сбой, 500, ретрай. Диф выглядел безобидным, потому что ТЕКСТ ошибки не изменился — «insufficient funds» по-прежнему в логах. Изменилась структура ошибки: PR заменил %w на %v, так что сентинела больше нет в цепочке — осталась только его проза. Проверки идентичности читают значения, а не строки."
          }
        },
        {
          "label": {
            "en": "2 · %w vs %v at the machine level",
            "ru": "2 · %w против %v на уровне механики"
          },
          "prompt": {
            "en": "Why does %v sever what %w preserves — what does errors.Is actually walk, and what does that make the choice of verb?",
            "ru": "Почему %v разрывает то, что %w сохраняет, — по чему на самом деле идёт errors.Is и чем тогда оказывается выбор глагола формата?"
          },
          "reveal": {
            "en": "fmt.Errorf with %w stores the wrapped error inside the new value and gives it an Unwrap() method; stacked wraps form a linked chain, and errors.Is walks that chain comparing each link's identity against the target. %v formats the cause into the output string and discards the value — no Unwrap, chain severed; the sentinel is unreachable. That makes the verb an API decision, not formatting: every %w publishes the wrapped error as part of your contract, and a %v at any layer seals everything beneath it — legitimate at a deliberate boundary, a silent regression anywhere callers still branch on the cause.",
            "ru": "fmt.Errorf с %w сохраняет оборачиваемую ошибку внутри нового значения и даёт ему метод Unwrap(); наслоенные обёртки образуют связный список, и errors.Is идёт по нему, сравнивая идентичность каждого звена с целью. %v форматирует причину в строку и выбрасывает значение — нет Unwrap, цепочка разорвана; сентинел недостижим. Поэтому выбор глагола — решение об API, а не о форматировании: каждый %w публикует обёрнутую ошибку как часть контракта, а %v на любом слое запечатывает всё, что ниже, — легитимно на осознанной границе и тихая регрессия везде, где вызывающие ещё ветвятся по причине."
          }
        },
        {
          "label": {
            "en": "3 · Fix and the safety net",
            "ru": "3 · Фикс и страховочная сетка"
          },
          "prompt": {
            "en": "Beyond reverting the verb, what makes this class of regression impossible to ship again — and what should the retry policy have done on an unknown error?",
            "ru": "Помимо отката глагола: что делает этот класс регрессий невыпускаемым впредь — и как политика ретраев должна была повести себя на неизвестной ошибке?"
          },
          "reveal": {
            "en": "Restore %w on every path where callers branch; keep %v only at documented sealing boundaries. Add a contract test at the TOP of the stack: drive a decline through the real layers and assert errors.Is(err, ErrInsufficientFunds) at the handler — adapter-level tests pass while the composition is broken. And make retries default-safe for payments: only errors explicitly classified retryable get retried; unknown means stop, because a duplicate authorization costs more than a failed request. The hour of damage: ~7% of traffic × 3 retry attempts ≈ +20% load aimed at the processor for nothing.",
            "ru": "Верни %w на каждом пути, где вызывающие ветвятся; %v оставь только на задокументированных запечатывающих границах. Добавь контрактный тест НА ВЕРХУ стека: прогони отказ через реальные слои и проверь errors.Is(err, ErrInsufficientFunds) в хендлере — тесты уровня адаптера проходят, пока композиция сломана. И сделай ретраи безопасными по умолчанию для платежей: ретраится только то, что явно классифицировано как retryable; неизвестное — стоп, потому что дублирующая авторизация стоит дороже неудавшегося запроса. Цена часа: ~7% трафика × 3 попытки ≈ +20% нагрузки на процессинг впустую."
          }
        }
      ]
    },
    {
      "id": "review-user-lookup-error-paths",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Review a user-lookup handler's error paths",
        "ru": "Отревьюй пути ошибок в хендлере поиска пользователя"
      },
      "prompt": {
        "en": "A repository wraps its errors with context; the handler classifies them. Review it like a senior: name the real defects in how errors travel and are handled, and don't flag the non-issues.",
        "ru": "Репозиторий оборачивает ошибки с контекстом; хендлер их классифицирует. Отревьюй как senior: назови реальные дефекты в том, как ошибки движутся и обрабатываются, и не цепляйся к не-проблемам."
      },
      "diff": {
        "lang": "go",
        "code": "func (s *Store) Get(ctx context.Context, id int64) (*User, error) {\n\trow := s.db.QueryRowContext(ctx,\n\t\t\"SELECT id, email FROM users WHERE id = $1\", id)\n\tvar u User\n\tif err := row.Scan(&u.ID, &u.Email); err != nil {\n\t\treturn nil, fmt.Errorf(\"get user %d: %w\", id, err)\n\t}\n\treturn &u, nil\n}\n\nfunc getUserHandler(w http.ResponseWriter, r *http.Request) {\n\tid, _ := strconv.ParseInt(r.PathValue(\"id\"), 10, 64)\n\n\tu, err := store.Get(r.Context(), id)\n\tif err == sql.ErrNoRows {\n\t\thttp.Error(w, \"not found\", http.StatusNotFound)\n\t\treturn\n\t}\n\tif err != nil {\n\t\tlog.Printf(\"get user failed: %v\", err)\n\t}\n\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\tjson.NewEncoder(w).Encode(u)\n}"
      },
      "findings": [
        {
          "id": "sentinel-eq-under-wrap",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "err == sql.ErrNoRows can never match a wrapped error",
            "ru": "err == sql.ErrNoRows никогда не совпадёт с обёрнутой ошибкой"
          },
          "explanation": {
            "en": "Get wraps with %w, so the handler receives a new error value whose chain contains the sentinel — == compares the outermost identity and is always false. Every missing user skips the 404 branch and falls through as a generic failure. Use errors.Is(err, sql.ErrNoRows), which walks the Unwrap chain.",
            "ru": "Get оборачивает через %w, так что хендлер получает новое значение ошибки, в чьей цепочке лежит сентинел, — == сравнивает идентичность внешнего значения и всегда даёт false. Каждый отсутствующий пользователь минует ветку 404 и проваливается как общий сбой. Используй errors.Is(err, sql.ErrNoRows) — он идёт по цепочке Unwrap."
          }
        },
        {
          "id": "swallowed-parse-error",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "ParseInt error discarded — garbage input becomes id 0",
            "ru": "Ошибка ParseInt отброшена — мусорный ввод становится id 0"
          },
          "explanation": {
            "en": "id, _ := strconv.ParseInt(...) swallows the error, so /users/abc silently queries id 0 — the zero value flows downstream and the client gets a confusing 'not found' (or worse, user 0's data) instead of 400. Handle the error and return http.StatusBadRequest; discards need a written justification, and this one has none.",
            "ru": "id, _ := strconv.ParseInt(...) глотает ошибку, так что /users/abc молча запрашивает id 0 — нулевое значение утекает вниз, и клиент получает сбивающий с толку «not found» (или хуже — данные пользователя 0) вместо 400. Обработай ошибку и верни http.StatusBadRequest; отбрасывание требует письменного обоснования, а здесь его нет."
          }
        },
        {
          "id": "log-and-continue-encodes-nil",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "Log-and-continue: failed lookups return 200 with a null body",
            "ru": "Залогировал и поехал дальше: неудачные запросы возвращают 200 с null в теле"
          },
          "explanation": {
            "en": "After logging the error, execution falls through to Encode(u) with u == nil: the client receives 200 and 'null' for what was actually a database failure — the error is invisible to callers and to alerting. After logging, return a 500 (or map the error); a handled error must terminate the success path.",
            "ru": "Залогировав ошибку, код проваливается в Encode(u) с u == nil: клиент получает 200 и «null» там, где на самом деле упала база, — ошибка невидима и для вызывающих, и для алертинга. После логирования возвращай 500 (или классифицируй ошибку); обработанная ошибка обязана прерывать успешный путь."
          }
        }
      ],
      "decoys": [
        {
          "id": "wrap-adds-context",
          "label": {
            "en": "Get wraps the Scan error instead of returning it raw",
            "ru": "Get оборачивает ошибку Scan вместо того, чтобы вернуть её как есть"
          },
          "explanation": {
            "en": "That is the discipline working as intended: each layer annotates with what it was doing ('get user 42: ...'), and %w keeps the chain walkable for errors.Is/As. Not noise — the causal chain is the trace.",
            "ru": "Это дисциплина, работающая как задумано: каждый слой аннотирует, что именно он делал («get user 42: ...»), а %w сохраняет цепочку проходимой для errors.Is/As. Не шум — причинная цепочка и есть трейс."
          }
        },
        {
          "id": "encode-error-unchecked",
          "label": {
            "en": "json.NewEncoder(w).Encode's error is not checked",
            "ru": "Ошибка json.NewEncoder(w).Encode не проверяется"
          },
          "explanation": {
            "en": "By the time Encode fails the status line has usually been sent, so there is nothing useful to return to the client — the typical cause is the client hanging up. Logging it is optional polish; ignoring it here is a widely accepted, deliberate discard.",
            "ru": "К моменту сбоя Encode статусная строка обычно уже отправлена, и вернуть клиенту нечего — типичная причина в том, что клиент сам отвалился. Логировать это — опциональный лоск; игнорирование здесь — широко принятое осознанное отбрасывание."
          }
        }
      ]
    }
  ]
}
