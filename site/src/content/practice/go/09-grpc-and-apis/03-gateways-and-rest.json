{
  "lessonKey": "go/09-grpc-and-apis/03-gateways-and-rest",
  "track": "go",
  "tasks": [
    {
      "id": "predict-streaming-over-rest",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "A streaming RPC behind a GET — what does curl actually receive?", "ru": "Стриминговый RPC за GET — что на самом деле получит curl?" },
      "prompt": { "en": "A team annotates <code>rpc WatchOrders(WatchRequest) returns (stream OrderEvent)</code> with <code>get: \"/v1/orders:watch\"</code> and ships it through grpc-gateway. A frontend developer calls it with fetch and <code>response.json()</code>, expecting an array of events. Predict: (1) what bytes actually arrive over the wire, (2) why response.json() fails, and (3) what happens if the team tries the same trick with a bidi-streaming RPC.", "ru": "Команда аннотирует <code>rpc WatchOrders(WatchRequest) returns (stream OrderEvent)</code> как <code>get: \"/v1/orders:watch\"</code> и выкатывает через grpc-gateway. Фронтенд-разработчик зовёт его fetch-ем с <code>response.json()</code>, ожидая массив событий. Предскажи: (1) какие байты реально придут по проводу, (2) почему response.json() упадёт и (3) что будет, если команда попробует тот же трюк с bidi-стриминговым RPC." },
      "scenario": { "en": "Recall how the gateway emits server-streaming responses: one message at a time, each wrapped in an envelope, separated how? Then check which of the four call types the transcoder supports at all.", "ru": "Вспомни, как gateway отдаёт ответы серверного стриминга: по одному сообщению, каждое в конверте, разделённые чем? Затем проверь, какие из четырёх типов вызовов транскодер вообще поддерживает." },
      "reveal": { "en": "(1) The connection stays open and the gateway writes newline-delimited JSON chunks, one per streamed message, each wrapped in an envelope: a line like a result object containing the OrderEvent, then a newline, then the next — and a stream error arrives mid-body as an error-wrapped line with HTTP status already sent as 200. (2) response.json() expects exactly one JSON document; an NDJSON sequence of envelopes is not valid single-document JSON, and with an infinite watch the body never ends, so the promise never resolves — the client must read the body stream incrementally and parse line by line. (3) Bidi (and client) streaming does not transcode: there is no REST request shape for a channel that sends many and receives many interleaved; the endpoint needs a different transport — websockets, gRPC-Web, or Connect — or a redesigned unary/server-streaming contract.", "ru": "(1) Соединение остаётся открытым, и gateway пишет JSON-чанки через перевод строки, по одному на стримовое сообщение, каждое в конверте: строка с объектом result, содержащим OrderEvent, перевод строки, следующая — а ошибка стрима приходит посреди тела строкой с конвертом error, когда HTTP-статус 200 уже отправлен. (2) response.json() ждёт ровно один JSON-документ; NDJSON-последовательность конвертов — не валидный одиночный JSON, а у бесконечного вотча тело вообще не кончается, и промис никогда не зарезолвится — клиент должен читать body-стрим инкрементально и парсить построчно. (3) Bidi- (и клиентский) стриминг не транскодируется: у канала, который шлёт много и получает много вперемешку, нет REST-формы запроса; эндпоинту нужен другой транспорт — вебсокеты, gRPC-Web или Connect — либо перепроектированный unary/server-streaming контракт." }
    },
    {
      "id": "diagnose-gateway-goroutine-pileup",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "41,000 goroutines and one locked table", "ru": "41 000 горутин и одна заблокированная таблица" },
      "prompt": { "en": "The public gateway OOM-crashed while the backend stayed up — see the evidence. Fill in the blanks: each REST request parks a ____ waiting on the backend stream (b1); they wait the full 90 seconds because no call carries a ____ (b2); public REST clients never send the gRPC header that would bound the call, named ____ (b3).", "ru": "Публичный gateway упал по OOM, а бэкенд остался жив — смотри улики. Заполни пропуски: каждый REST-запрос паркует ____, ждущую на стриме бэкенда (b1); они ждут все 90 секунд, потому что ни один вызов не несёт ____ (b2); публичные REST-клиенты никогда не шлют gRPC-заголовок, который ограничил бы вызов, — он называется ____ (b3)." },
      "evidence": { "en": "$ curl -s gateway:6060/debug/pprof/goroutine?debug=1 | head -3\ngoroutine profile: total 41208\n38911 @ google.golang.org/grpc.(*ClientStream).RecvMsg ...\n\n$ kubectl describe pod gateway-7d4f | grep -A1 'Last State'\nLast State: Terminated\nReason:     OOMKilled\n\n# backend p99: 90s (table lock); gateway dialed with context.Background()\n# inbound requests: 30 rps; gateway RSS grew for 11 min, then died", "ru": "// Улики с прода (вывод инструментов без изменений)\n$ curl -s gateway:6060/debug/pprof/goroutine?debug=1 | head -3\ngoroutine profile: total 41208\n38911 @ google.golang.org/grpc.(*ClientStream).RecvMsg ...\n\n$ kubectl describe pod gateway-7d4f | grep -A1 'Last State'\nLast State: Terminated\nReason:     OOMKilled\n\n# p99 бэкенда: 90 c (блокировка таблицы); gateway звонил с context.Background()\n# входящие: 30 rps; RSS gateway рос 11 минут, затем смерть" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["goroutine", "горутину", "горутина", "goroutine per request", "горутину на запрос"], "hint": { "en": "Count what the pprof profile counts: 41,208 of them, mostly parked in RecvMsg.", "ru": "Посчитай то, что считает pprof-профиль: их 41 208, и почти все запаркованы в RecvMsg." } },
          { "id": "b2", "accept": ["deadline", "timeout", "дедлайн", "таймаут", "context deadline", "дедлайн контекста", "бюджет"], "hint": { "en": "The thing context.Background() conspicuously lacks — an upper bound on how long the call may take.", "ru": "То, чего демонстративно нет у context.Background(), — верхняя граница времени вызова." } },
          { "id": "b3", "accept": ["grpc-timeout", "grpc timeout", "заголовок grpc-timeout"], "hint": { "en": "The same header that carries the shrinking budget between internal gRPC hops — browsers and curl never set it.", "ru": "Тот же заголовок, что несёт сжимающийся бюджет между внутренними gRPC-хопами, — браузеры и curl его не ставят никогда." } }
        ]
      }
    },
    {
      "id": "design-public-edge-choice",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Design the public edge for a gRPC backend", "ru": "Спроектируй публичный край для gRPC-бэкенда" },
      "prompt": { "en": "Choose and justify the edge architecture: grpc-gateway, ConnectRPC, or a handwritten REST BFF. Define the error contract, the deadline budget, the streaming strategy, and how API docs stay truthful. Self-grade against the rubric.", "ru": "Выбери и обоснуй архитектуру края: grpc-gateway, ConnectRPC или рукописный REST-BFF. Определи контракт ошибок, бюджет дедлайнов, стратегию стриминга и способ держать документацию API правдивой. Оцени себя по рубрике." },
      "constraints": { "en": "A Go backend of 7 gRPC services must open to two audiences: third-party integrators (expect classic REST + OpenAPI, will use curl and generated SDKs) and your own browser SPA (needs one server-streaming notifications feed). Team of 4, no service mesh today, and the postmortem from the last outage mandates explicit deadline budgets at every boundary.", "ru": "Go-бэкенд из 7 gRPC-сервисов должен открыться двум аудиториям: сторонним интеграторам (ждут классический REST + OpenAPI, будут жить на curl и сгенерированных SDK) и собственному браузерному SPA (нужен один server-streaming фид уведомлений). Команда из 4 человек, сервис-меша нет, а постмортем последнего аутеджа требует явных бюджетов дедлайнов на каждой границе." },
      "rubric": [
        { "en": "Picks per audience instead of one-size-fits-all: gateway (or Connect) compiled from annotations for integrators — handwritten BFF rejected because 7 services of endpoints guarantee doc drift and triple maintenance; and names what the SPA uses for the streaming feed.", "ru": "Выбирает под аудиторию, а не одно решение для всех: скомпилированный из аннотаций gateway (или Connect) для интеграторов — рукописный BFF отвергнут, потому что эндпоинты 7 сервисов гарантируют дрейф документации и тройную поддержку; и называет, чем SPA получает стриминговый фид." },
        { "en": "Owns the error contract in one place: a single WithErrorHandler (or Connect error codes) defining the public error envelope and the status mapping table, so InvalidArgument/NotFound/Unavailable surface as 400/404/503 consistently across all 7 services.", "ru": "Владеет контрактом ошибок в одном месте: единый WithErrorHandler (или коды ошибок Connect), определяющий публичный конверт ошибок и таблицу маппинга статусов, чтобы InvalidArgument/NotFound/Unavailable одинаково выходили как 400/404/503 во всех 7 сервисах." },
        { "en": "Implements the postmortem mandate concretely: the edge originates a default deadline (DefaultContextTimeout or middleware) because public clients send none, internal hops reuse the inbound ctx, and the streaming feed gets its own idle/lifetime bounds instead of the unary budget.", "ru": "Конкретно исполняет требование постмортема: край порождает дефолтный дедлайн (DefaultContextTimeout или middleware), потому что публичные клиенты его не шлют; внутренние хопы переиспользуют входящий ctx; а стриминговому фиду — собственные границы простоя/времени жизни вместо unary-бюджета." },
        { "en": "Keeps docs compiled, not written: OpenAPI generated from the same annotations (or Connect schema), published per release, with the proto repo and buf breaking as the single review point for any public API change.", "ru": "Держит документацию компилируемой, а не написанной: OpenAPI генерируется из тех же аннотаций (или схемы Connect), публикуется на каждый релиз, а proto-репозиторий с buf breaking — единственная точка ревью любого изменения публичного API." }
      ],
      "model": { "en": "For the integrator audience, compile the edge: grpc-gateway from google.api.http annotations, because third parties expect plain REST with OpenAPI and generated SDKs, and a 4-person team cannot hand-maintain a BFF over 7 services without drift. ConnectRPC is the credible alternative — one port, curl-able JSON, no extra proxy — and wins if SDK expectations are flexible; pick gateway here for OpenAPI maturity, and note the cost: own the proxy hop and its config. Error contract: one WithErrorHandler installs the public envelope (machine-readable code, human message, request id) and the standard mapping — 400/403/404/503/504 — so no endpoint invents its own. Deadlines per the postmortem: the gateway sets a 5s default budget on every inbound request via middleware (public clients send no grpc-timeout), backends receive the shrinking budget through ctx propagation, and dashboards alert on DeadlineExceeded rates per route. The SPA notifications feed does not go through REST transcoding: NDJSON envelopes and a never-ending body are hostile to fetch-json clients — serve it as gRPC-Web (Envoy filter) or, cheaper without a mesh, Connect server streaming from the same .proto; give the stream its own bounds (idle timeout, max stream age) instead of the 5s unary budget. Docs: protoc-gen-openapiv2 output published per release from CI, and every public-contract change reviewed in the proto repo behind buf breaking — the API surface has exactly one source of truth.", "ru": "Для аудитории интеграторов край компилируется: grpc-gateway из аннотаций google.api.http — сторонние ждут обычный REST с OpenAPI и сгенерированными SDK, а команда из 4 человек не удержит рукописный BFF над 7 сервисами без дрейфа. ConnectRPC — достойная альтернатива: один порт, curl-доступный JSON, без лишнего прокси — и выигрывает, если ожидания по SDK гибкие; здесь берём gateway за зрелость OpenAPI и честно называем цену: владеть хопом прокси и его конфигом. Контракт ошибок: один WithErrorHandler ставит публичный конверт (машиночитаемый код, человеческое сообщение, request id) и стандартный маппинг — 400/403/404/503/504, — чтобы ни один эндпоинт не изобретал свой. Дедлайны по постмортему: gateway ставит дефолтный бюджет 5 секунд на каждый входящий запрос через middleware (публичные клиенты grpc-timeout не шлют), бэкенды получают сжимающийся бюджет через распространение ctx, а дашборды алертят на долю DeadlineExceeded по маршрутам. Фид уведомлений SPA не идёт через REST-транскодинг: NDJSON-конверты и никогда не кончающееся тело враждебны fetch-json-клиентам — отдаём его как gRPC-Web (фильтр Envoy) или, дешевле без меша, как Connect server streaming из того же .proto; стриму — собственные границы (idle-таймаут, максимальный возраст стрима) вместо unary-бюджета в 5 секунд. Документация: вывод protoc-gen-openapiv2 публикуется на каждый релиз из CI, а каждое изменение публичного контракта ревьюится в proto-репозитории за buf breaking — у поверхности API ровно один источник истины." }
    }
  ]
}
