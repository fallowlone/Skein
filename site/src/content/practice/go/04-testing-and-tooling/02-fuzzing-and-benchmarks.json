{
  "lessonKey": "go/04-testing-and-tooling/02-fuzzing-and-benchmarks",
  "track": "go",
  "tasks": [
    {
      "id": "predict-dead-code-benchmark",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Why does the optimized parser report 0.28 ns/op?", "ru": "Почему оптимизированный парсер рапортует 0.28 ns/op?" },
      "prompt": { "en": "A teammate rewrites a benchmark to the old style: <code>for i := 0; i &lt; b.N; i++ { v := ParseTag(line) ; _ = v }</code> — where ParseTag is a pure function and v is otherwise unused. The run reports 0.28 ns/op and 0 allocs/op, a 1400x speedup over last week. Predict what the benchmark measured and what the honest numbers would show.", "ru": "Коллега переписывает бенчмарк в старом стиле: <code>for i := 0; i &lt; b.N; i++ { v := ParseTag(line) ; _ = v }</code> — где ParseTag чистая функция, а v больше нигде не используется. Прогон рапортует 0.28 ns/op и 0 allocs/op — ускорение в 1400 раз за неделю. Предскажи, что бенчмарк измерил и что показали бы честные числа." },
      "scenario": { "en": "0.28 ns is roughly one cycle of a 3.5 GHz core. ParseTag splits a string and allocates a small struct. Ask: can a string split fit in one cycle, and what is the compiler allowed to do with a pure call whose result provably goes nowhere?", "ru": "0.28 нс — примерно один такт ядра на 3.5 ГГц. ParseTag разбивает строку и аллоцирует маленькую структуру. Спроси: помещается ли разбор строки в один такт и что компилятору разрешено сделать с чистым вызовом, результат которого доказуемо никуда не идёт?" },
      "reveal": { "en": "It measured an empty loop. The blank assignment _ = v does not constitute a use; the compiler proves ParseTag's result dead, eliminates the call, and the loop spins doing nothing — hence one-cycle iterations and zero allocations. The 1400x speedup is fictional. Honest versions: Go 1.24's for b.Loop() keeps the body's arguments and results alive by design; pre-1.24, assign to a package-level sink variable after the loop or use runtime.KeepAlive. Expect realistic numbers in the hundreds of ns/op with a few allocs/op — and verify with allocs/op first, because allocation counts come from exact runtime counters and cannot be faked by timer noise.", "ru": "Он измерил пустой цикл. Пустое присваивание _ = v использованием не является; компилятор доказывает, что результат ParseTag мёртв, удаляет вызов, и цикл крутится вхолостую — отсюда итерации в один такт и ноль аллокаций. Ускорение в 1400 раз — фикция. Честные варианты: for b.Loop() из Go 1.24 по дизайну держит аргументы и результаты тела живыми; до 1.24 — присваивание в пакетную sink-переменную после цикла или runtime.KeepAlive. Реалистичные числа — сотни ns/op с несколькими allocs/op; и сверяться сначала с allocs/op, потому что счётчики аллокаций берутся из точных счётчиков рантайма и шумом таймера не подделываются." }
    },
    {
      "id": "diagnose-benchstat-noise-claim",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Is this 4% win real?", "ru": "Реален ли этот выигрыш в 4%?" },
      "prompt": { "en": "A PR claims a 4% latency win based on the benchstat output below. Fill in the blanks: the claim fails because the p-value of ____ (b1) means the difference is statistically ____ (b2); the honest protocol is to rerun each side with the flag ____ (b3) before comparing.", "ru": "PR заявляет выигрыш 4% по latency на основании вывода benchstat ниже. Заполни пропуски: заявление несостоятельно, потому что p-value, равное ____ (b1), означает, что разница статистически ____ (b2); честный протокол — перегнать каждую сторону с флагом ____ (b3) перед сравнением." },
      "evidence": { "en": "$ benchstat old.txt new.txt\ngoos: linux, goarch: amd64\n            │  old.txt   │            new.txt            │\nDecode/json   1.052µ ± 6%   1.011µ ± 7%  ~ (p=0.483 n=3)\n\n# old.txt and new.txt were each produced by a single CI run\n# with -count=3 on a shared runner; PR description says:\n# \"Decode is now 4% faster, see benchstat\"", "ru": "// Вывод benchstat, на который ссылается PR\n$ benchstat old.txt new.txt\ngoos: linux, goarch: amd64\n            │  old.txt   │            new.txt            │\nDecode/json   1.052µ ± 6%   1.011µ ± 7%  ~ (p=0.483 n=3)\n\n# old.txt и new.txt получены по одному CI-прогону\n# с -count=3 на разделяемом раннере; в описании PR сказано:\n# «Decode теперь на 4% быстрее, см. benchstat»" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["0.483", "p=0.483", "0,483", "p = 0.483"], "hint": { "en": "benchstat prints it in parentheses next to the tilde.", "ru": "benchstat печатает его в скобках рядом с тильдой." } },
          { "id": "b2", "accept": ["insignificant", "not significant", "indistinguishable", "незначима", "незначимая", "неразличима", "статистически незначима"], "hint": { "en": "The tilde character is benchstat's verdict — it refuses to print a percentage delta at all.", "ru": "Символ тильды — вердикт benchstat: он вообще отказывается печатать процентную дельту." } },
          { "id": "b3", "accept": ["-count=10", "count=10", "-count", "-count=10 (или больше)", "count 10"], "hint": { "en": "Three samples per side is far too few for a Mann-Whitney test to see through ±6-7% spread; ten is the conventional floor.", "ru": "Три замера на сторону — слишком мало, чтобы критерий Манна — Уитни пробился сквозь разброс ±6–7%; десять — конвенциональный минимум." } }
        ]
      }
    },
    {
      "id": "design-fuzz-strategy-template-parser",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Design the fuzzing strategy for a template parser", "ru": "Спроектируй стратегию фаззинга для парсера шаблонов" },
      "prompt": { "en": "Design the fuzz targets and the CI integration: which properties each target asserts, seed selection, corpus and crasher policy, and how fuzzing time is budgeted between the PR gate and nightly runs. Self-grade against the rubric.", "ru": "Спроектируй фазз-таргеты и интеграцию с CI: какие свойства утверждает каждый таргет, выбор сидов, политика корпуса и крашеров, и как бюджет времени фаззинга делится между гейтом PR и ночными прогонами. Оцени себя по рубрике." },
      "constraints": { "en": "An internal templating engine parses user-supplied templates ({{var}}, filters, nested blocks) in a multi-tenant SaaS — a panic in the parser kills a shared worker. There is already a 60-row table suite. PR CI has a 10-minute total budget; a nightly job can run for 2 hours. The team has never committed a fuzz corpus before.", "ru": "Внутренний шаблонизатор парсит пользовательские шаблоны ({{var}}, фильтры, вложенные блоки) в multi-tenant SaaS — паника в парсере убивает общий воркер. Уже есть табличный сьют на 60 строк. Бюджет CI на PR — 10 минут суммарно; ночная джоба может крутиться 2 часа. Команда никогда раньше не коммитила фазз-корпус." },
      "rubric": [
        { "en": "Defines at least two property-based targets beyond no-panic: parse-render round-trip stability and parser/renderer agreement (every parsed template renders without error or fails parse), seeded with real templates from the table suite via f.Add.", "ru": "Определяет минимум два таргета-свойства помимо отсутствия паник: стабильность round-trip парсинг-рендер и согласованность парсера/рендерера (каждый распарсенный шаблон рендерится без ошибки либо не парсится), с сидами из реальных шаблонов табличного сьюта через f.Add." },
        { "en": "Splits the budget correctly: plain go test (seeds + committed corpus, no -fuzz) in the PR gate; open-ended -fuzz with -fuzztime in the nightly job only.", "ru": "Правильно делит бюджет: обычный go test (сиды + закоммиченный корпус, без -fuzz) в гейте PR; открытый -fuzz с -fuzztime — только в ночной джобе." },
        { "en": "Commits minimized crashers in testdata/fuzz as permanent regressions and defines triage: a crasher is a bug to fix, never a corpus file to delete.", "ru": "Коммитит минимизированные крашеры в testdata/fuzz как постоянные регрессии и задаёт триаж: крашер — это баг для починки, а не файл корпуса для удаления." },
        { "en": "Connects findings to production hardening: the multi-tenant worker still wraps template execution in a recover boundary, because fuzzing reduces but never proves the absence of panics.", "ru": "Связывает находки с продакшен-укреплением: multi-tenant воркер всё равно оборачивает выполнение шаблона границей recover, потому что фаззинг снижает, но никогда не доказывает отсутствие паник." }
      ],
      "model": { "en": "Targets: FuzzParse(s string) asserts no panic and that a successful Parse yields an AST that Render can execute against an empty context without panicking; FuzzRoundTrip asserts Format(Parse(s)) reparses to an equal AST — the property that catches silent precedence and escaping bugs, not just crashes. Seeds: f.Add every input from the 60-row table plus a handful of pathological shapes (unclosed blocks, nested 100 deep, lone braces). PR gate: plain go test runs seeds plus committed corpus deterministically in seconds — fits the 10-minute budget with room. Nightly: go test -fuzz=FuzzParse -fuzztime=50m per target, two targets, leaving margin in the 2 h window; any crasher is auto-minimized into testdata/fuzz/FuzzParse/, committed by the on-call as a failing regression plus an issue. Policy: corpus files are append-only; deleting one to make CI green is treated like deleting a test. Production: worker keeps recover around template execution and logs the input hash — fuzzing shrinks the panic surface; the recover boundary is the guarantee.", "ru": "Таргеты: FuzzParse(s string) утверждает отсутствие паники и что успешный Parse даёт AST, который Render исполняет на пустом контексте без паники; FuzzRoundTrip утверждает, что Format(Parse(s)) репарсится в равный AST — свойство, ловящее тихие баги приоритетов и экранирования, а не только краши. Сиды: f.Add каждого входа из 60-строчной таблицы плюс горсть патологических форм (незакрытые блоки, вложенность 100, одинокие фигурные скобки). Гейт PR: обычный go test детерминированно гоняет сиды плюс закоммиченный корпус за секунды — укладывается в 10-минутный бюджет с запасом. Ночью: go test -fuzz=FuzzParse -fuzztime=50m на таргет, два таргета, с запасом в окне 2 ч; любой крашер автоминимизируется в testdata/fuzz/FuzzParse/, коммитится дежурным как падающая регрессия плюс тикет. Политика: файлы корпуса append-only; удалить один ради зелёного CI — то же, что удалить тест. Продакшен: воркер сохраняет recover вокруг выполнения шаблона и логирует хеш входа — фаззинг сжимает поверхность паник; гарантией остаётся граница recover." }
    }
  ]
}
