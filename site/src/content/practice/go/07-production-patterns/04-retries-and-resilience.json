{
  "lessonKey": "go/07-production-patterns/04-retries-and-resilience",
  "track": "go",
  "tasks": [
    {
      "id": "predict-layered-amplification",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "How much traffic does the dead downstream receive?",
        "ru": "Сколько трафика получит мёртвая зависимость?"
      },
      "prompt": {
        "en": "Baseline: 1k rps of user actions. The mobile client retries up to 2 extra attempts, the API gateway up to 2 extra attempts per incoming request, the checkout service does not retry. The payment downstream goes to 100% failure for ten minutes, and every failed attempt burns its full 500ms timeout. Predict: (1) the offered load on the downstream during the outage, (2) what the gateway's goroutine count does, (3) which two policy changes shrink the number most.",
        "ru": "База: 1k rps пользовательских действий. Мобильный клиент ретраит до 2 дополнительных попыток, API-гейтвей — до 2 дополнительных попыток на каждый входящий запрос, сервис чекаута не ретраит. Платёжная зависимость уходит в 100% отказов на десять минут, и каждая неудачная попытка сжигает полный таймаут 500мс. Предскажи: (1) предложенную нагрузку на зависимость во время аварии, (2) что произойдёт с числом горутин гейтвея, (3) какие два изменения политики сильнее всего уменьшат число."
      },
      "scenario": {
        "en": "Layered retries compose multiplicatively: every gateway attempt is itself an arrival that the client layer may have multiplied. Count attempts per user action layer by layer (1 original + 2 retries = 3 at each retrying layer). Then think about what each in-flight 500ms attempt occupies at the gateway while it waits.",
        "ru": "Слоёные ретраи композируются умножением: каждая попытка гейтвея — сама по себе прилёт, который слой клиента уже мог умножить. Посчитай попытки на действие пользователя слой за слоем (1 исходная + 2 ретрая = 3 на каждом ретраящем слое). Затем подумай, что занимает каждая летящая 500мс-попытка на гейтвее, пока ждёт."
      },
      "reveal": {
        "en": "(1) Up to ~9k rps: the client turns one action into 3 requests, the gateway turns each into 3 attempts — 3x3=9 arrivals per action, all of them doomed. (2) Goroutines balloon: every attempt parks a goroutine (plus a connection-pool slot) for its full 500ms timeout, so the gateway holds roughly 9k x 0.5s = 4500 concurrent doomed calls — memory and pool exhaustion follow, and the gateway itself starts failing callers that never touched payments. (3) First: retry at exactly one layer (the one owning the user-visible deadline) — that alone cuts 9x to 3x. Second: a ~10% retry budget at that layer caps the outage load near 1.1x; a circuit breaker does even better against a 100%-dead peer by dropping to near zero and failing in microseconds instead of 500ms.",
        "ru": "(1) До ~9k rps: клиент превращает одно действие в 3 запроса, гейтвей каждое — в 3 попытки: 3x3=9 прилётов на действие, все обречённые. (2) Горутины раздуваются: каждая попытка паркует горутину (плюс слот пула соединений) на полные 500мс таймаута, и гейтвей держит порядка 9k x 0,5с = 4500 одновременных обречённых вызовов — следом истощение памяти и пула, и гейтвей сам начинает отказывать вызывающим, которые платежей и не касались. (3) Первое: ретраить ровно на одном слое (владеющем видимым пользователю дедлайном) — одно это режет 9x до 3x. Второе: бюджет ретраев ~10% на этом слое ограничивает аварийную нагрузку около 1,1x; circuit breaker против мёртвого на 100% соседа ещё лучше — опускает почти до нуля и отказывает за микросекунды вместо 500мс."
      }
    },
    {
      "id": "diagnose-synchronized-retry-waves",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "The spikes that return every power of two",
        "ru": "Пики, возвращающиеся каждую степень двойки"
      },
      "prompt": {
        "en": "After a cache-tier restart, the downstream sees the traffic pattern in the evidence. Fill in the blanks: the restart failed all clients at the same instant, so their backoff schedules are ____ (b1); pure exponential backoff spaces the waves but each wave is still a thundering ____ (b2); the fix is sleeping a uniformly random duration between zero and the exponential ceiling, the strategy named full ____ (b3).",
        "ru": "После рестарта кэш-тира зависимость видит паттерн трафика из улик. Заполни пропуски: рестарт уронил всех клиентов в один момент, поэтому их расписания бэкоффа ____ (b1); чистый экспоненциальный бэкофф раздвигает волны, но каждая волна — всё ещё thundering ____ (b2); фикс — спать равномерно случайное время между нулём и экспоненциальным потолком, стратегия по имени full ____ (b3)."
      },
      "evidence": {
        "en": "# downstream RPS, 100ms buckets, after cache restart at t=0:\n t=0.0s   18,400  (all clients fail simultaneously)\n t=0.1s   17,900  ← retry wave 1 (backoff 100ms)\n t=0.3s   17,650  ← retry wave 2 (backoff 200ms)\n t=0.7s   17,300  ← retry wave 3 (backoff 400ms)\n t=1.5s   17,100  ← retry wave 4 (backoff 800ms)\n between waves: ~600 rps (near silence)\n\n// client code: sleep = base * 2^attempt  // no randomness anywhere",
        "ru": "// Улики: RPS зависимости, бакеты 100мс, после рестарта кэша в t=0:\n t=0.0s   18 400  (все клиенты падают одновременно)\n t=0.1s   17 900  ← волна ретраев 1 (бэкофф 100мс)\n t=0.3s   17 650  ← волна ретраев 2 (бэкофф 200мс)\n t=0.7s   17 300  ← волна ретраев 3 (бэкофф 400мс)\n t=1.5s   17 100  ← волна ретраев 4 (бэкофф 800мс)\n между волнами: ~600 rps (почти тишина)\n\n// клиентский код: sleep = base * 2^attempt  // нигде никакой случайности"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "synchronized",
              "in sync",
              "aligned",
              "identical",
              "синхронизированы",
              "синхронны",
              "одинаковы",
              "выровнены"
            ],
            "hint": {
              "en": "A shared failure moment plus a deterministic formula gives every client the same schedule.",
              "ru": "Общий момент отказа плюс детерминированная формула дают каждому клиенту одно расписание."
            }
          },
          {
            "id": "b2",
            "accept": [
              "herd",
              "стадо",
              "херд",
              "thundering herd"
            ],
            "hint": {
              "en": "The classic name for everyone arriving at once — the graph shows it at every power of two.",
              "ru": "Классическое имя для «все приходят разом» — график показывает его на каждой степени двойки."
            }
          },
          {
            "id": "b3",
            "accept": [
              "jitter",
              "джиттер",
              "full jitter",
              "полный джиттер"
            ],
            "hint": {
              "en": "The AWS Architecture Blog compared the variants and this one spreads clients evenly across the window.",
              "ru": "Блог архитектуры AWS сравнил варианты, и этот размазывает клиентов равномерно по окну."
            }
          }
        ]
      }
    },
    {
      "id": "design-checkout-resilience-policy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design the resilience policy for a checkout path",
        "ru": "Спроектируй политику устойчивости для пути чекаута"
      },
      "prompt": {
        "en": "Design the full call policy: idempotency classification per operation, backoff parameters, budget, the timeout geometry under the SLO, breaker placement, and the build-vs-import line. Self-grade against the rubric.",
        "ru": "Спроектируй полную политику вызовов: классификация идемпотентности по операциям, параметры бэкоффа, бюджет, геометрия таймаутов под SLO, размещение брейкера и граница «писать самим против импортировать». Оцени себя по рубрике."
      },
      "constraints": {
        "en": "Checkout calls: GET /catalog/items (read), PUT /cart/items (replace), POST /payments/charge (money moves). User-visible SLO: 2s p99. The payment provider has monthly brownouts; catalog had the famous retry-amplification outage. The team is split between importing a resilience framework and writing everything by hand.",
        "ru": "Вызовы чекаута: GET /catalog/items (чтение), PUT /cart/items (замена), POST /payments/charge (движение денег). Видимый пользователю SLO: 2с p99. У платёжного провайдера ежемесячные brownout-ы; каталог пережил знаменитую аварию усиления ретраями. Команда расколота между импортом resilience-фреймворка и написанием всего руками."
      },
      "rubric": [
        {
          "en": "Classifies before looping: GET retries freely, PUT retries because replace is idempotent, POST /charge retries only with a client-generated idempotency key the provider dedupes on — and says out loud that a timeout does not prove failure.",
          "ru": "Классифицирует до цикла: GET ретраится свободно, PUT ретраится, потому что замена идемпотентна, POST /charge — только с клиентским ключом идемпотентности, по которому провайдер дедуплицирует, — и проговаривает вслух, что таймаут не доказывает отказ."
        },
        {
          "en": "Specifies the loop: exponential base with a cap, full jitter via a uniform draw, at most 3 attempts per request, a ~10% per-client retry budget, and retries owned by exactly one layer with the catalog outage as the cited reason.",
          "ru": "Конкретизирует цикл: экспоненциальная база с потолком, полный джиттер равномерной выборкой, максимум 3 попытки на запрос, бюджет ретраев ~10% на клиента и владение ретраями ровно одним слоем — со ссылкой на аварию каталога как причину."
        },
        {
          "en": "Builds the timeout geometry under the 2s SLO: parent context deadline at the edge, per-attempt children (e.g. 500ms), select on ctx.Done during backoff so sleep never outlives the deadline, and arithmetic showing 3 attempts plus backoff fit.",
          "ru": "Строит геометрию таймаутов под 2с SLO: родительский дедлайн контекста на краю, дети на попытку (например, 500мс), select на ctx.Done во время бэкоффа, чтобы сон не пережил дедлайн, и арифметика, что 3 попытки плюс бэкофф помещаются."
        },
        {
          "en": "Places a circuit breaker only on the brownout-prone payment provider (binary-dead case, microsecond fail-fast, half-open probes) and draws the build-vs-import line: copy the 30-line retry loop, import a small battle-tested breaker, no framework.",
          "ru": "Ставит circuit breaker только на склонного к brownout платёжного провайдера (бинарно-мёртвый случай, отказ за микросекунды, half-open зонды) и проводит границу «писать против импортировать»: скопировать 30-строчный ретрай-цикл, импортировать маленький обкатанный брейкер, без фреймворка."
        }
      ],
      "model": {
        "en": "Classification first. GET /catalog/items is idempotent by contract: retry freely within budget. PUT /cart/items replaces state: idempotent, retryable. POST /payments/charge moves money: retryable only because we attach an Idempotency-Key per checkout attempt and the provider replays the stored result on duplicates — without that header, zero retries, surface the error. The loop is the lesson's 30 lines, copied into the codebase and unit-tested: base 100ms doubling to a 2s cap, full jitter via rand.N(ceiling), 3 attempts max. The checkout service owns all retrying — the gateway and mobile client retry nothing — because the catalog postmortem showed layered retries multiplying 3x3 into a 9x DDoS. A 10% retry budget per dependency keeps a total payment outage at 1.1x offered load. Geometry under the 2s SLO: the edge sets the parent deadline at 2s; attempts run under 500ms children; worst case 3x500ms attempts + jittered sleeps (bounded by the remaining deadline via select on ctx.Done) lands inside 2s with reserve for response assembly. The breaker wraps only the payment provider: monthly brownouts mean a dependency that goes binary-dead, where the budget would still feed baseline traffic into 500ms timeouts; open-state failures return in microseconds with a clear error, half-open probes detect recovery. Catalog gets budget + jitter but no breaker — its failure mode is degradation, not death. Build-vs-import: the retry loop is policy, readable and owned; the breaker is a concurrency-sensitive mechanism, so a small library (gobreaker-style) wins; a full resilience framework is rejected as dependency surface without leverage.",
        "ru": "Сначала классификация. GET /catalog/items идемпотентен по контракту: ретраится свободно в рамках бюджета. PUT /cart/items заменяет состояние: идемпотентен, ретраибелен. POST /payments/charge двигает деньги: ретраибелен только потому, что мы прикладываем Idempotency-Key на попытку чекаута, и провайдер переигрывает сохранённый результат на дубликатах — без этого заголовка ноль ретраев, ошибку наверх. Цикл — те самые 30 строк урока, скопированные в кодбазу и покрытые юнит-тестами: база 100мс с удвоением до потолка 2с, полный джиттер через rand.N(ceiling), максимум 3 попытки. Всеми ретраями владеет сервис чекаута — гейтвей и мобильный клиент не ретраят ничего, — потому что постмортем каталога показал, как слоёные ретраи перемножают 3x3 в 9x DDoS. Бюджет ретраев 10% на зависимость держит тотальную аварию платежей на 1,1x предложенной нагрузки. Геометрия под 2с SLO: край ставит родительский дедлайн 2с; попытки идут под 500мс-детьми; худший случай 3x500мс попыток + джиттерные сны (ограниченные остатком дедлайна через select на ctx.Done) укладывается в 2с с резервом на сборку ответа. Брейкер оборачивает только платёжного провайдера: ежемесячные brownout-ы означают зависимость, умирающую бинарно, где бюджет всё равно кормил бы базовый трафик 500мс-таймаутами; отказы открытого состояния возвращаются за микросекунды с ясной ошибкой, half-open зонды ловят выздоровление. Каталог получает бюджет + джиттер, но не брейкер — его режим отказа деградация, а не смерть. Писать против импортировать: ретрай-цикл — это политика, читаемая и своя; брейкер — чувствительный к конкурентности механизм, и маленькая библиотека (в духе gobreaker) выигрывает; полный resilience-фреймворк отклонён как поверхность зависимостей без рычага."
      }
    }
  ]
}