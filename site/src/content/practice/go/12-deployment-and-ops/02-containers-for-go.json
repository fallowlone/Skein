{
  "lessonKey": "go/12-deployment-and-ops/02-containers-for-go",
  "track": "go",
  "tasks": [
    {
      "id": "predict-layer-cache-busts",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Which layers rebuild, and what does each change cost?", "ru": "Какие слои пересоберутся и что стоит каждое изменение?" },
      "prompt": { "en": "A Dockerfile goes: FROM golang AS build → COPY go.mod go.sum → RUN go mod download → COPY . . → RUN go build → final distroless stage with COPY --from=build. Predict which layers rebuild for each of three commits: (1) edits one .go file, (2) bumps a dependency version in go.mod, (3) only changes README.md. Then predict what happens to all three answers if someone moves COPY . . above go mod download.", "ru": "Dockerfile такой: FROM golang AS build → COPY go.mod go.sum → RUN go mod download → COPY . . → RUN go build → финальная distroless-стадия с COPY --from=build. Предскажи, какие слои пересоберутся для трёх коммитов: (1) правит один .go-файл, (2) поднимает версию зависимости в go.mod, (3) меняет только README.md. Затем предскажи, что станет со всеми тремя ответами, если кто-то поднимет COPY . . выше go mod download." },
      "scenario": { "en": "The cache rule is mechanical: a layer is reused if its instruction and its input files are unchanged AND every layer before it was reused; the first busted layer invalidates everything after it. Apply that rule layer by layer for each commit — and remember COPY . . hashes every file in the context, including README.md.", "ru": "Правило кэша механическое: слой переиспользуется, если его инструкция и входные файлы не изменились И каждый слой до него переиспользован; первый сброшенный слой инвалидирует всё после себя. Примени правило послойно для каждого коммита — и помни, что COPY . . хэширует каждый файл контекста, включая README.md." },
      "reveal": { "en": "Commit 1 (.go edit): manifests unchanged, so COPY go.mod/go.sum and go mod download replay from cache; COPY . . busts (file hash changed), go build and the final COPY rerun. Cost: one compile. Commit 2 (go.mod bump): COPY go.mod busts immediately, so download, source copy, and build all rerun — the expensive full path, correctly paid only when deps change. Commit 3 (README only): same as commit 1 — COPY . . hashes README.md too, so you pay a compile for a docs change (the fix is a .dockerignore or narrower COPY). With COPY . . moved above the download: every commit of any kind busts the source layer first, so go mod download reruns every single build — 30-90 s of module fetching added to commits 1 and 3 that previously cached it.", "ru": "Коммит 1 (правка .go): манифесты не тронуты, COPY go.mod/go.sum и go mod download берутся из кэша; COPY . . сбрасывается (хэш файла изменился), go build и финальный COPY перевыполняются. Цена: одна компиляция. Коммит 2 (бамп go.mod): COPY go.mod сбрасывается сразу, поэтому скачивание, копирование исходников и сборка перевыполняются — дорогой полный путь, справедливо оплачиваемый только при смене зависимостей. Коммит 3 (только README): то же, что коммит 1, — COPY . . хэширует и README.md, так что за правку документации платишь компиляцией (лечится .dockerignore или более узким COPY). С COPY . . выше скачивания: любой коммит сначала сбрасывает слой исходников, и go mod download перевыполняется каждую сборку — плюс 30-90 с скачивания модулей к коммитам 1 и 3, которые раньше шли из кэша." }
    },
    {
      "id": "diagnose-cfs-throttle-fleet",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "At the limit on the graphs, parked in reality", "ru": "На графиках — в лимите, в реальности — на паркинге" },
      "prompt": { "en": "A Go 1.24 service moved to 64-core nodes with limits.cpu: 2 and p99 jumped 8x — see the evidence. Fill the blanks: GOMAXPROCS defaulted to the ____ CPU count (b1); the 64 threads exhaust the 200 ms-per-period CFS ____ within milliseconds, freezing the cgroup (b2); the one-variable fix is pinning ____ to the limit (b3).", "ru": "Go 1.24-сервис переехал на 64-ядерные ноды с limits.cpu: 2, и p99 прыгнул в 8 раз — смотри улики. Заполни пропуски: GOMAXPROCS по умолчанию взял число CPU ____ (b1); 64 потока исчерпывают 200-мс CFS-____ периода за миллисекунды, замораживая cgroup (b2); лечение одной переменной — прибить ____ к лимиту (b3)." },
      "evidence": { "en": "# node: 64 cores; pod: limits.cpu \"2\", limits.memory \"1Gi\"\n$ kubectl exec api-7d4f9 -- cat /sys/fs/cgroup/cpu.max\n200000 100000\n$ kubectl exec api-7d4f9 -- cat /sys/fs/cgroup/cpu.stat\nnr_periods 84211\nnr_throttled 80926\nthrottled_usec 7311248819\n\n# runtime metric scraped from the pod:\ngo_sched_gomaxprocs_threads 64\n\n# p99 before: 40ms — after migration: 320ms; avg CPU graph: ~2.0 cores", "ru": "// Улики с прода (вывод без изменений)\n# нода: 64 ядра; под: limits.cpu \"2\", limits.memory \"1Gi\"\n$ kubectl exec api-7d4f9 -- cat /sys/fs/cgroup/cpu.max\n200000 100000\n$ kubectl exec api-7d4f9 -- cat /sys/fs/cgroup/cpu.stat\nnr_periods 84211\nnr_throttled 80926\nthrottled_usec 7311248819\n\n# метрика рантайма, снятая с пода:\ngo_sched_gomaxprocs_threads 64\n\n# p99 до: 40 мс — после переезда: 320 мс; график среднего CPU: ~2.0 ядра" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["host", "host cpus", "node", "хоста", "ноды", "хост", "64"], "hint": { "en": "The metric shows 64 — whose core count is that, the pod or the machine under it?", "ru": "Метрика показывает 64 — чьё это число ядер: пода или машины под ним?" } },
          { "id": "b2", "accept": ["quota", "cpu quota", "bandwidth", "квоту", "квота", "полосу", "полоса"], "hint": { "en": "cpu.max names it: 200000 us of CPU time allowed per 100000 us period.", "ru": "Её называет cpu.max: 200000 мкс процессорного времени на период в 100000 мкс." } },
          { "id": "b3", "accept": ["gomaxprocs", "GOMAXPROCS", "гомаксспрокс", "go maxprocs"], "hint": { "en": "The runtime knob whose default came from the wrong machine — set it to 2, or let automaxprocs / Go 1.25 derive it.", "ru": "Ручка рантайма, чей дефолт пришёл не с той машины, — поставь 2 или дай automaxprocs / Go 1.25 вывести её." } }
        ]
      }
    },
    {
      "id": "design-container-contract",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Design the container contract for a Go API", "ru": "Спроектируй контейнерный контракт для Go API" },
      "prompt": { "en": "Design the full container contract: the multi-stage Dockerfile (stages, layer order, base image choice with reasons), the runtime environment block (CPU and memory knobs with values and justification), and the two dashboards that prove the contract holds in production. Self-grade against the rubric.", "ru": "Спроектируй полный контейнерный контракт: multi-stage Dockerfile (стадии, порядок слоёв, выбор базового образа с обоснованием), блок рантайм-окружения (ручки CPU и памяти со значениями и обоснованием) и два дашборда, доказывающих, что контракт держится в проде. Оцени себя по рубрике." },
      "constraints": { "en": "A payments API in Go 1.24 (upgrade to 1.25 is allowed but must be argued, not assumed). Pod resources: limits.cpu 2, limits.memory 1Gi. Live heap measured at ~500 MiB at peak. It calls external HTTPS APIs. CI rebuilds images on every commit; module graph download takes ~60 s cold. Security wants non-root and no shell in production images.", "ru": "Платёжный API на Go 1.24 (апгрейд на 1.25 разрешён, но его нужно аргументировать, а не подразумевать). Ресурсы пода: limits.cpu 2, limits.memory 1Gi. Живая куча на пике — около 500 МиБ. Сервис ходит во внешние HTTPS API. CI пересобирает образы на каждый коммит; холодное скачивание графа модулей — около 60 с. Безопасность требует non-root и отсутствие шелла в прод-образах." },
      "rubric": [
        { "en": "Dockerfile: builder stage copies go.mod/go.sum and runs go mod download as its own layer before COPY . . (names the 60 s saved per commit), builds with CGO_ENABLED=0, and the runtime stage is distroless/static:nonroot — justified by the HTTPS requirement (CA roots) and the non-root/no-shell mandate, over raw scratch.", "ru": "Dockerfile: builder-стадия копирует go.mod/go.sum и выполняет go mod download отдельным слоем до COPY . . (называет экономию 60 с на коммит), собирает с CGO_ENABLED=0, рантайм-стадия — distroless/static:nonroot, обоснованная требованием HTTPS (корневые CA) и мандатом non-root/без шелла, а не голый scratch." },
        { "en": "CPU: on Go 1.24 sets GOMAXPROCS=2 explicitly (env var next to the limit) or imports automaxprocs, and notes that Go 1.25 would make this the default — limits only, requests do not produce a quota.", "ru": "CPU: на Go 1.24 явно ставит GOMAXPROCS=2 (переменная рядом с лимитом) или импортирует automaxprocs, и отмечает, что Go 1.25 сделал бы это дефолтом — только лимиты, реквесты квоту не порождают." },
        { "en": "Memory: sets GOMEMLIMIT around 900MiB (~90% of 1Gi), explains the mechanism (pacer converts approaching-limit into GC frequency instead of OOM-kill), checks feasibility against the 500 MiB live heap, and names why not 100% (cgo/mmap/kernel memory invisible to the runtime).", "ru": "Память: ставит GOMEMLIMIT около 900MiB (~90% от 1Gi), объясняет механизм (пейсер превращает приближение к лимиту в частоту GC вместо OOM-kill), проверяет реалистичность против живой кучи 500 МиБ и называет, почему не 100% (cgo/mmap/память ядра невидимы рантайму)." },
        { "en": "Verification: dashboard one is cgroup truth — nr_throttled/nr_periods ratio and throttled_usec near zero after the change; dashboard two is GC/memory — GC CPU fraction and heap vs GOMEMLIMIT headroom, with OOM-kill events as the alert, not the average memory graph.", "ru": "Проверка: первый дашборд — правда cgroup: отношение nr_throttled/nr_periods и throttled_usec около нуля после изменения; второй — GC/память: доля CPU на GC и куча против запаса GOMEMLIMIT, с алертом на события OOM-kill, а не на средний график памяти." }
      ],
      "model": { "en": "Dockerfile: stage one FROM golang:1.24, WORKDIR /src, COPY go.mod go.sum ./, RUN go mod download (own layer — ordinary commits replay it from cache, saving the 60 s cold download), COPY . ., RUN CGO_ENABLED=0 go build -trimpath -ldflags \"-s -w\" -o /out/api ./cmd/api. Stage two FROM gcr.io/distroless/static-debian12:nonroot — chosen over scratch because the service makes HTTPS calls (needs CA roots) and security mandates non-root with no shell; distroless ships CA bundle, tzdata, and a nonroot user for ~2 MiB, and debugging happens via ephemeral debug containers. Runtime env block in the deployment, adjacent to resources: GOMAXPROCS=2 (Go 1.24 cannot derive it; automaxprocs is the code-side equivalent; the 1.25 upgrade would make the default cgroup-aware and is worth scheduling but not assuming) and GOMEMLIMIT=900MiB — the pacer turns the last stretch before the limit into more frequent GC instead of a SIGKILL; with ~500 MiB live there is real headroom, so GC pressure stays episodic; the missing 10% covers memory the runtime cannot see. Verification dashboards: (1) cpu.stat scrape — nr_throttled/nr_periods must fall from ~96% to ~0 and stay there; alert on ratio above 5%; (2) runtime metrics — GC CPU fraction and live-heap-vs-GOMEMLIMIT gap, with container OOM-kill events alerting at any occurrence. The average CPU and memory graphs are explicitly demoted: both hide the failure modes this contract exists to prevent.", "ru": "Dockerfile: первая стадия FROM golang:1.24, WORKDIR /src, COPY go.mod go.sum ./, RUN go mod download (отдельный слой — обычные коммиты берут его из кэша, экономя 60 с холодного скачивания), COPY . ., RUN CGO_ENABLED=0 go build -trimpath -ldflags \"-s -w\" -o /out/api ./cmd/api. Вторая стадия FROM gcr.io/distroless/static-debian12:nonroot — выбрана вместо scratch, потому что сервис ходит по HTTPS (нужны корневые CA), а безопасность требует non-root без шелла; distroless привозит CA-бандл, tzdata и пользователя nonroot за ~2 МиБ, отладка — через эфемерные debug-контейнеры. Блок env в деплойменте, рядом с resources: GOMAXPROCS=2 (Go 1.24 сам вывести не умеет; automaxprocs — эквивалент на стороне кода; апгрейд на 1.25 сделает дефолт cgroup-aware — его стоит запланировать, но не подразумевать) и GOMEMLIMIT=900MiB — пейсер превращает последний отрезок перед лимитом в более частый GC вместо SIGKILL; при ~500 МиБ живых данных запас реален, давление GC останется эпизодическим; недостающие 10% покрывают память, которую рантайм не видит. Дашборды проверки: (1) съём cpu.stat — nr_throttled/nr_periods должен упасть с ~96% к ~0 и там остаться; алерт при доле выше 5%; (2) метрики рантайма — доля CPU на GC и зазор живой кучи до GOMEMLIMIT, с алертом на любое событие OOM-kill контейнера. Средние графики CPU и памяти явно понижены в статусе: оба прячут именно те режимы отказа, ради которых контракт существует." }
    },
    {
      "id": "incident-shell-form-entrypoint-eats-sigterm",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 13,
      "title": { "en": "Every deploy waits the full 30 s grace, then SIGKILL", "ru": "Каждый деплой ждёт все 30 с грейса, затем SIGKILL" },
      "prompt": { "en": "A Go API has correct graceful-shutdown code: signal.NotifyContext on SIGTERM, srv.Shutdown, in-flight requests drained. Yet every rollout shows the pod taking the full terminationGracePeriodSeconds (30 s) to die and the logs never print the 'shutting down' line; ingress shows a burst of dropped connections each deploy. The Dockerfile ends with ENTRYPOINT /app/server --config /etc/cfg.yaml. Work it step by step; commit before revealing.", "ru": "У Go API корректный код graceful-shutdown: signal.NotifyContext на SIGTERM, srv.Shutdown, дренаж текущих запросов. Но каждый раскат показывает, что под умирает все terminationGracePeriodSeconds (30 с), а в логах никогда не печатается строка 'shutting down'; ингресс показывает всплеск сброшенных соединений на каждый деплой. Dockerfile кончается на ENTRYPOINT /app/server --config /etc/cfg.yaml. Веди по шагам; сформулируй ответ до раскрытия." },
      "steps": [
        { "label": { "en": "1 · shell form vs exec form", "ru": "1 · shell-форма против exec-формы" }, "prompt": { "en": "ENTRYPOINT is written without brackets. What does Docker do with that string, and what is PID 1 inside the container as a result?", "ru": "ENTRYPOINT записан без скобок. Что Docker делает с этой строкой и кто в итоге PID 1 внутри контейнера?" }, "reveal": { "en": "The bracketless ENTRYPOINT /app/server ... is the SHELL form: Docker runs it as /bin/sh -c \"/app/server --config ...\". So PID 1 is /bin/sh, and your Go server is a child (PID 2+). The kubelet sends SIGTERM to PID 1 — the shell — not to your process. /bin/sh does not forward signals to its children, so the Go server never receives SIGTERM, never runs its shutdown code, and the shell just sits until the grace period expires and SIGKILL nukes the whole tree. That is why the 'shutting down' line never prints and every connection drops hard.", "ru": "Бесскобочный ENTRYPOINT /app/server ... — это SHELL-форма: Docker запускает её как /bin/sh -c \"/app/server --config ...\". Поэтому PID 1 — это /bin/sh, а Go-сервер — ребёнок (PID 2+). Kubelet шлёт SIGTERM в PID 1 — в шелл, не в твой процесс. /bin/sh не форвардит сигналы детям, поэтому Go-сервер никогда не получает SIGTERM, не выполняет код остановки, а шелл просто сидит до конца грейса, и SIGKILL сносит всё дерево. Вот почему строка 'shutting down' не печатается и каждое соединение рвётся жёстко." } },
        { "label": { "en": "2 · the one-line fix", "ru": "2 · фикс в одну строку" }, "prompt": { "en": "Give the corrected ENTRYPOINT and explain why it makes the existing shutdown code start working with no code change.", "ru": "Дай исправленный ENTRYPOINT и объясни, почему он заставит существующий код остановки заработать без изменений кода." }, "reveal": { "en": "Use the exec (JSON-array) form: ENTRYPOINT [\"/app/server\", \"--config\", \"/etc/cfg.yaml\"]. Now Docker execs the binary directly, so the Go server is PID 1 and receives SIGTERM straight from the kubelet — signal.NotifyContext fires, Shutdown runs, connections drain. No application code changes; the bug was entirely in how the process was launched. (Note: if the entrypoint genuinely needs shell features, use exec in the script: ENTRYPOINT [\"/bin/sh\", \"-c\", \"exec /app/server ...\"] so the binary replaces the shell as PID 1.)", "ru": "Использовать exec-форму (JSON-массив): ENTRYPOINT [\"/app/server\", \"--config\", \"/etc/cfg.yaml\"]. Теперь Docker запускает бинарь напрямую, Go-сервер становится PID 1 и получает SIGTERM прямо от kubelet — signal.NotifyContext срабатывает, Shutdown идёт, соединения дренируются. Код приложения не меняется; баг был целиком в том, как запускался процесс. (Если энтрипойнту реально нужны фичи шелла — использовать exec в скрипте: ENTRYPOINT [\"/bin/sh\", \"-c\", \"exec /app/server ...\"], чтобы бинарь заменил шелл как PID 1.)" } },
        { "label": { "en": "3 · the PID-1 reaping caveat", "ru": "3 · оговорка о reaping PID 1" }, "prompt": { "en": "Once the Go binary is PID 1, what extra responsibility does it inherit, and when does it actually matter for a Go service?", "ru": "Как только Go-бинарь стал PID 1, какую дополнительную обязанность он наследует и когда это реально важно для Go-сервиса?" }, "reveal": { "en": "PID 1 is the init process: it must reap orphaned children (zombies) by calling wait, and the kernel suppresses default signal dispositions for PID 1. A typical Go server that never forks subprocesses is fine — it has no children to reap. But if the service shells out (exec.Command for image processing, git, ffmpeg, etc.), those grandchildren can re-parent to PID 1 and, if not waited on, accumulate as zombies. Go's os/exec reaps the children it directly starts via Wait, so normal usage is safe; the risk is double-forking daemons or processes that outlive their parent. When that is real, add a tiny init (tini, or ENTRYPOINT [\"/usr/bin/tini\", \"--\", ...], or Kubernetes shareProcessNamespace with a pause). For most Go HTTP services, exec-form ENTRYPOINT alone is the whole fix.", "ru": "PID 1 — это init-процесс: он обязан reaping осиротевших детей (зомби), вызывая wait, и ядро подавляет дефолтные диспозиции сигналов для PID 1. Типичный Go-сервер, который не форкает сабпроцессы, в порядке — реаппить некого. Но если сервис шеллит наружу (exec.Command для обработки изображений, git, ffmpeg и т.п.), эти внуки могут переусыновиться к PID 1 и, если их не дождаться, копиться зомби. os/exec в Go реаппит детей, которых сам стартовал, через Wait, поэтому обычное использование безопасно; риск — двойной форк демонов или процессы, переживающие родителя. Когда это реально — добавить крошечный init (tini, или ENTRYPOINT [\"/usr/bin/tini\", \"--\", ...], или shareProcessNamespace в Kubernetes с pause). Для большинства Go HTTP-сервисов exec-форма ENTRYPOINT — это весь фикс." } }
      ]
    },
    {
      "id": "review-go-dockerfile-layering",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Review a multi-stage Go Dockerfile", "ru": "Ревью multi-stage Dockerfile для Go" },
      "prompt": { "en": "Review this multi-stage Dockerfile for a Go HTTP service that runs under a 2-CPU limit and makes outbound HTTPS calls. Review like a senior: name the real defects, don't flag non-issues.", "ru": "Отревьюй этот multi-stage Dockerfile для Go HTTP-сервиса, работающего под лимитом 2 CPU и делающего исходящие HTTPS-вызовы. Ревью как сеньор: назови реальные дефекты, не помечай не-проблемы." },
      "diff": {
        "lang": "go",
        "code": "# --- Dockerfile ---\nFROM golang:1.24 AS build\nWORKDIR /src\nCOPY . .\nRUN go mod download\nRUN CGO_ENABLED=0 go build -o /out/api ./cmd/api\n\nFROM gcr.io/distroless/static-debian12:nonroot\nCOPY --from=build /out/api /api\nENTRYPOINT [\"/api\"]\n\n# deployment.yaml (excerpt)\n#   resources:\n#     limits: { cpu: \"2\", memory: \"1Gi\" }\n#   # no GOMAXPROCS, no GOMEMLIMIT set" },
      "findings": [
        { "id": "copy-before-mod-download", "severity": "bug", "planted": true, "label": { "en": "COPY . . before go mod download busts the dep-cache layer on every source edit", "ru": "COPY . . до go mod download сбрасывает слой кэша зависимостей на каждой правке исходника" }, "explanation": { "en": "Because the whole tree is copied first, any .go change invalidates the layer before 'go mod download', forcing a full module re-download (~60 s cold) every commit. Copy go.mod/go.sum and run go mod download as its own layer before COPY . .", "ru": "Поскольку всё дерево копируется первым, любая правка .go инвалидирует слой до 'go mod download', форсируя полное перескачивание модулей (~60 с вхолодную) на каждый коммит. Скопировать go.mod/go.sum и выполнить go mod download отдельным слоем до COPY . ." } },
        { "id": "no-gomaxprocs-under-limit", "severity": "bug", "planted": true, "label": { "en": "On Go 1.24 with a CPU limit and no GOMAXPROCS, the runtime sees host cores and gets CFS-throttled", "ru": "На Go 1.24 с CPU-лимитом и без GOMAXPROCS рантайм видит ядра хоста и попадает под CFS-троттлинг" }, "explanation": { "en": "Go 1.24 defaults GOMAXPROCS to the host's CPU count, not the cgroup limit, so on a many-core node the runtime spawns far more Ps than the 2-CPU quota allows, exhausting the CFS quota and throttling p99. Set GOMAXPROCS=2 (or import automaxprocs).", "ru": "Go 1.24 по умолчанию ставит GOMAXPROCS в число ядер хоста, а не в лимит cgroup, поэтому на многоядерной ноде рантайм плодит куда больше P, чем позволяет квота 2 CPU, исчерпывая CFS-квоту и троттля p99. Поставить GOMAXPROCS=2 (или импортировать automaxprocs)." } },
        { "id": "no-gomemlimit", "severity": "tradeoff", "planted": true, "label": { "en": "No GOMEMLIMIT against a 1Gi cap risks OOM-kill instead of GC backpressure", "ru": "Без GOMEMLIMIT при лимите 1Gi риск OOM-kill вместо обратного давления GC" }, "explanation": { "en": "With no soft memory limit, a heap spike runs past 1Gi and the kernel OOM-kills the container rather than the pacer running GC harder. Set GOMEMLIMIT to ~90% (900MiB) so the runtime trades CPU for staying alive.", "ru": "Без мягкого лимита памяти всплеск кучи проскакивает за 1Gi, и ядро делает OOM-kill контейнера вместо того, чтобы пейсер чаще гонял GC. Поставить GOMEMLIMIT ~90% (900MiB), чтобы рантайм менял CPU на выживание." } }
      ],
      "decoys": [
        { "id": "distroless-not-scratch", "label": { "en": "Using distroless instead of scratch wastes ~2 MiB of image size", "ru": "Использование distroless вместо scratch тратит ~2 МиБ размера образа" }, "explanation": { "en": "looks wrong, is fine: distroless/static:nonroot is the correct choice here, not waste — the service makes HTTPS calls so it needs the CA bundle distroless ships, plus the nonroot user. Dropping to scratch would reintroduce the missing-CA-certs bug. The 2 MiB buys correctness.", "ru": "выглядит ошибкой, но в порядке: distroless/static:nonroot здесь правильный выбор, а не трата — сервис ходит по HTTPS, ему нужен CA-бандл, который везёт distroless, плюс nonroot-пользователь. Спуск на scratch вернул бы баг отсутствующих CA-сертификатов. Эти 2 МиБ покупают корректность." } }
      ]
    }
  ]
}
