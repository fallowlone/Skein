{
  "lessonKey": "go/12-deployment-and-ops/01-static-binaries-and-cross-compilation",
  "track": "go",
  "tasks": [
    {
      "id": "predict-scratch-two-failures",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Two scratch images, two different deaths", "ru": "Два scratch-образа — две разные смерти" },
      "prompt": { "en": "An HTTPS-calling Go service is packaged two ways. Image A: built on the golang:1.25 image with plain <code>go build</code> (no env overrides), binary copied into scratch. Image B: built with <code>CGO_ENABLED=0 go build</code>, binary copied into scratch, nothing else added. Predict exactly how each container fails — at what moment, with what error class, and why.", "ru": "Go-сервис, ходящий по HTTPS, упакован двумя способами. Образ A: собран на образе golang:1.25 обычным <code>go build</code> (без переопределения env), бинарник скопирован в scratch. Образ B: собран с <code>CGO_ENABLED=0 go build</code>, бинарник скопирован в scratch, больше ничего не добавлено. Предскажи, как именно упадёт каждый контейнер — в какой момент, с каким классом ошибки и почему." },
      "scenario": { "en": "Walk each image through its lifecycle: first execve at container start, then the first outbound TLS call. For A, think about what plain go build links when a C toolchain is present and what scratch lacks. For B, the binary starts — so what does crypto/x509 need from the filesystem to verify a server certificate?", "ru": "Проведи каждый образ по жизненному циклу: сначала execve на старте контейнера, потом первый исходящий TLS-вызов. Для A подумай, что линкует обычный go build при наличии C-тулчейна и чего нет в scratch. Для B бинарник стартует — а что нужно crypto/x509 из файловой системы, чтобы проверить сертификат сервера?" },
      "reveal": { "en": "Image A dies at execve, before a single line of your code runs: the golang image has a C toolchain, so CGO_ENABLED defaults to 1, net links libc, and the ELF header names /lib64/ld-linux-x86-64.so.2 as interpreter — absent in scratch, so the kernel returns ENOENT and the shell prints no such file or directory about a binary that exists. Image B boots fine (truly static, no interpreter) but the first HTTPS call fails with x509: certificate signed by unknown authority — scratch has no /etc/ssl/certs, so the cert pool is empty. The fixes differ: A needs CGO_ENABLED=0 (or a glibc base); B needs CA roots copied from the builder or a distroless/static base that ships them.", "ru": "Образ A умирает на execve, до первой строки вашего кода: в образе golang есть C-тулчейн, поэтому CGO_ENABLED по умолчанию 1, net линкует libc, и ELF-заголовок называет интерпретатором /lib64/ld-linux-x86-64.so.2 — его в scratch нет, ядро возвращает ENOENT, а шелл печатает no such file or directory про существующий бинарник. Образ B стартует нормально (по-настоящему статический, без интерпретатора), но первый HTTPS-вызов падает с x509: certificate signed by unknown authority — в scratch нет /etc/ssl/certs, пул сертификатов пуст. Лекарства разные: A нужен CGO_ENABLED=0 (или glibc-база); B нужны корневые CA, скопированные из билдера, или distroless/static-база, где они уже есть." }
    },
    {
      "id": "diagnose-dns-changed-in-scratch",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "The hostname that resolves everywhere except the new image", "ru": "Имя хоста, которое резолвится везде, кроме нового образа" },
      "prompt": { "en": "After moving from a debian-slim image to scratch (and to CGO_ENABLED=0), one internal hostname stopped resolving — see the evidence. Fill the blanks: the old image resolved it through the ____ resolver, which honors nsswitch plugins (b1); the pure-Go resolver only reads resolv.conf and the file ____ (b2); the GODEBUG knob that forces and logs the resolver decision is ____ (b3).", "ru": "После переезда с образа debian-slim на scratch (и на CGO_ENABLED=0) одно внутреннее имя хоста перестало резолвиться — смотри улики. Заполни пропуски: старый образ резолвил его через ____ резолвер, который уважает плагины nsswitch (b1); чистый Go-резолвер читает только resolv.conf и файл ____ (b2); ручка GODEBUG, которая форсирует и логирует выбор резолвера, — это ____ (b3)." },
      "evidence": { "en": "# old image (debian-slim, CGO_ENABLED=1)\n$ grep hosts /etc/nsswitch.conf\nhosts: files ldap dns\n$ ./server   # lookups for db.corp.internal succeed\n\n# new image (scratch, CGO_ENABLED=0)\napp log: dial tcp: lookup db.corp.internal on 10.96.0.10:53: no such host\n\n# locally, forcing the decision to be visible:\n$ GODEBUG=netdns=go+1 ./server\ngo package net: GODEBUG setting forcing use of Go's resolver", "ru": "// Улики (вывод команд без изменений)\n# старый образ (debian-slim, CGO_ENABLED=1)\n$ grep hosts /etc/nsswitch.conf\nhosts: files ldap dns\n$ ./server   # лукапы db.corp.internal успешны\n\n# новый образ (scratch, CGO_ENABLED=0)\napp log: dial tcp: lookup db.corp.internal on 10.96.0.10:53: no such host\n\n# локально, делаем решение видимым:\n$ GODEBUG=netdns=go+1 ./server\ngo package net: GODEBUG setting forcing use of Go's resolver" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["cgo", "libc", "cgo resolver", "libc resolver", "getaddrinfo", "cgo-резолвер", "libc-резолвер", "сишный"], "hint": { "en": "The one that calls getaddrinfo and therefore consults nsswitch.conf — including its ldap module.", "ru": "Тот, что зовёт getaddrinfo и потому ходит в nsswitch.conf — включая его модуль ldap." } },
          { "id": "b2", "accept": ["/etc/hosts", "etc/hosts", "hosts", "хостс", "файл hosts"], "hint": { "en": "The static name table every Unix has had since before DNS existed.", "ru": "Статическая таблица имён, которая была в Unix ещё до появления DNS." } },
          { "id": "b3", "accept": ["netdns", "GODEBUG=netdns", "netdns=go+1", "godebug netdns"], "hint": { "en": "Visible in the last line of the evidence — the setting name before the = sign.", "ru": "Видна в последней строке улик — имя настройки до знака =." } }
        ]
      }
    },
    {
      "id": "design-release-build-pipeline",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Design the release build for a multi-arch fleet", "ru": "Спроектируй релизную сборку для мультиархитектурного флота" },
      "prompt": { "en": "Design the release build pipeline: cgo policy per artifact, the exact flag set, the symbol strategy, the cross-compile matrix, and how an on-call engineer audits what a given container is running. Self-grade against the rubric.", "ru": "Спроектируй пайплайн релизной сборки: политику cgo по артефактам, точный набор флагов, стратегию символов, матрицу кросс-компиляции и то, как дежурный инженер проверяет, что именно крутится в контейнере. Оцени себя по рубрике." },
      "constraints": { "en": "One repo builds: an API server (deployed to scratch on linux/amd64 and linux/arm64), and an internal CLI tool that embeds sqlite via mattn/go-sqlite3. CI also runs the test suite with -race. Requirements: binaries traceable to a commit, reproducible builds, production panics must be debuggable, image size matters.", "ru": "Один репозиторий собирает: API-сервер (деплой в scratch на linux/amd64 и linux/arm64) и внутренний CLI-инструмент со встроенным sqlite через mattn/go-sqlite3. CI также гоняет тесты с -race. Требования: бинарники прослеживаются до коммита, сборки воспроизводимы, паники в проде должны быть отлаживаемы, размер образа важен." },
      "rubric": [
        { "en": "Splits cgo policy per artifact: server ships CGO_ENABLED=0 (scratch-safe, pure resolver), the sqlite CLI keeps cgo (or consciously migrates to modernc.org/sqlite, naming the speed tradeoff), and -race stays a CI-only build where cgo is fine.", "ru": "Разделяет политику cgo по артефактам: сервер уходит с CGO_ENABLED=0 (безопасен для scratch, чистый резолвер), sqlite-CLI оставляет cgo (или осознанно мигрирует на modernc.org/sqlite, называя цену по скорости), а -race остаётся сборкой только для CI, где cgo допустим." },
        { "en": "Names the flag discipline: -trimpath on every release build for reproducibility, -ldflags -X for the human version, and notes that vcs.revision is embedded automatically since Go 1.18.", "ru": "Называет дисциплину флагов: -trimpath на каждой релизной сборке ради воспроизводимости, -ldflags -X для человеческой версии, и отмечает, что vcs.revision встраивается автоматически с Go 1.18." },
        { "en": "Resolves the strip-vs-debug tension explicitly: either ship unstripped, or ship -s -w AND archive the unstripped twin / upload symbols — and states that panic traces survive stripping via pclntab while delve needs DWARF.", "ru": "Явно разрешает напряжение «стрип против отладки»: либо выпускает без стрипа, либо -s -w ПЛЮС архив нестрипнутого близнеца / выгрузка символов — и отмечает, что трейсы паник переживают стрип через pclntab, а delve нужен DWARF." },
        { "en": "Cross-compiles the pure-Go server with GOOS/GOARCH alone (no C toolchain), flags that the cgo CLI would need a per-target C cross-compiler if it ever went multi-arch, and gives the audit answer: go version -m on the binary from the container.", "ru": "Кросс-компилирует чистый Go-сервер одними GOOS/GOARCH (без C-тулчейна), помечает, что cgo-CLI понадобился бы C-кросс-компилятор на каждую цель при мультиархе, и даёт ответ про аудит: go version -m на бинарнике из контейнера." }
      ],
      "model": { "en": "Three build profiles. (1) Server, release: CGO_ENABLED=0 GOOS=linux GOARCH=amd64|arm64 go build -trimpath -ldflags \"-s -w -X main.version=$TAG\" — two artifacts from one runner, no cross toolchain, scratch-compatible, pure-Go resolver accepted and documented. The unstripped twin of each binary is built in the same job (same flags minus -s -w) and archived next to the release; panic traces symbolize either way via pclntab, but delve and external symbolizers get the twin. (2) CLI with mattn/go-sqlite3: CGO_ENABLED=1, native build per platform it actually targets (today: linux/amd64 only); the plan notes that going multi-arch means either CC=aarch64-linux-gnu-gcc / zig cc or migrating to modernc.org/sqlite and paying the measured 1.5-2x query overhead. (3) CI tests: go test -race with cgo enabled on the runner — never shipped, so its cgo requirement is irrelevant to deploys. Provenance: -trimpath makes the builds reproducible; the release job rebuilds from a clean checkout and diffs the hash as a check. On-call audit: kubectl cp the binary out (or docker create + cp) and run go version -m — module, dep hashes, CGO_ENABLED, vcs.revision, vcs.modified=false expected; vcs.modified=true in prod is an immediate red flag.", "ru": "Три профиля сборки. (1) Сервер, релиз: CGO_ENABLED=0 GOOS=linux GOARCH=amd64|arm64 go build -trimpath -ldflags \"-s -w -X main.version=$TAG\" — два артефакта с одного раннера, без кросс-тулчейна, совместимо со scratch, чистый Go-резолвер принят и задокументирован. Нестрипнутый близнец каждого бинарника собирается в той же джобе (те же флаги минус -s -w) и архивируется рядом с релизом; трейсы паник символизируются в любом случае через pclntab, а delve и внешним символизаторам достаётся близнец. (2) CLI с mattn/go-sqlite3: CGO_ENABLED=1, нативная сборка под фактические платформы (сегодня — только linux/amd64); план отмечает: мультиарх означает либо CC=aarch64-linux-gnu-gcc / zig cc, либо миграцию на modernc.org/sqlite с измеренной ценой 1,5-2x на запросах. (3) CI-тесты: go test -race с включённым cgo на раннере — никогда не выпускается, так что его требование cgo деплоев не касается. Происхождение: -trimpath делает сборки воспроизводимыми; релизная джоба пересобирает из чистого чекаута и сверяет хэш как проверку. Аудит на дежурстве: kubectl cp бинарник наружу (или docker create + cp) и go version -m — модуль, хэши зависимостей, CGO_ENABLED, vcs.revision; ожидается vcs.modified=false, а true в проде — немедленный красный флаг." }
    },
    {
      "id": "incident-cgo-arm-binary-exec-format-error",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "exec format error on the new arm64 nodes", "ru": "exec format error на новых arm64-нодах" },
      "prompt": { "en": "A team cross-compiled their service on an amd64 CI runner with CGO_ENABLED=1 GOOS=linux GOARCH=arm64 go build (they need a cgo image library). The build succeeds and the image pushes, but on the arm64 worker nodes the container crashes instantly with 'exec /app: exec format error', while the same code on amd64 nodes runs fine. The arm64 rollout is blocked and 40% of the fleet's new capacity sits idle. Work it step by step; commit before revealing.", "ru": "Команда кросс-компилировала сервис на amd64 CI-раннере командой CGO_ENABLED=1 GOOS=linux GOARCH=arm64 go build (нужна cgo-библиотека для изображений). Сборка проходит, образ пушится, но на arm64-воркерах контейнер мгновенно падает с 'exec /app: exec format error', тогда как тот же код на amd64-нодах работает. Раскат на arm64 заблокирован, 40% новой ёмкости флота простаивает. Веди по шагам; сформулируй ответ до раскрытия." },
      "steps": [
        { "label": { "en": "1 · read the ELF", "ru": "1 · прочитать ELF" }, "prompt": { "en": "'exec format error' is the kernel rejecting the binary. What does file ./app or readelf -h say, and what does CGO_ENABLED=1 do to a cross-compile that GOARCH alone does not?", "ru": "'exec format error' — ядро отвергает бинарь. Что скажет file ./app или readelf -h и что CGO_ENABLED=1 делает с кросс-компиляцией, чего не делает один GOARCH?" }, "reveal": { "en": "readelf -h shows Machine: Advanced Micro Devices X86-64 — the binary is amd64 despite GOARCH=arm64. With CGO_ENABLED=1, go build must invoke a C compiler; on the amd64 runner the default cc is the host's x86 gcc, which cannot emit arm64 objects. The Go toolchain can cross-compile pure Go to any GOARCH alone, but the moment cgo is on, the C half is built by whatever CC points at — here the wrong-arch host compiler — and the link silently produces (or the toolchain falls back toward) a host-arch result. The 'exec format error' is the arm64 kernel refusing an x86 ELF.", "ru": "readelf -h показывает Machine: Advanced Micro Devices X86-64 — бинарь amd64, несмотря на GOARCH=arm64. При CGO_ENABLED=1 go build обязан звать C-компилятор; на amd64-раннере дефолтный cc — это хостовый x86 gcc, который не умеет выдавать arm64-объекты. Тулчейн Go кросс-компилирует чистый Go в любой GOARCH одной переменной, но как только включён cgo, C-половину собирает то, на что указывает CC — здесь хостовый компилятор не той архитектуры. 'exec format error' — это arm64-ядро, отвергающее x86 ELF." } },
        { "label": { "en": "2 · pick the build path", "ru": "2 · выбрать путь сборки" }, "prompt": { "en": "Name three concrete ways to actually produce an arm64 cgo binary, and the tradeoff of each.", "ru": "Назови три конкретных способа реально получить arm64 cgo-бинарь и трейдофф каждого." }, "reveal": { "en": "(a) Native arm64 runner: build on an arm64 CI machine with the native toolchain — simplest, correct by construction, costs a second runner pool. (b) Cross C compiler: set CC=aarch64-linux-gnu-gcc (install gcc-aarch64-linux-gnu) so the C half targets arm64 — one runner, but you maintain a cross-toolchain per target. (c) zig cc: CC=\"zig cc -target aarch64-linux-gnu\" — zig ships a self-contained cross-compiler, popular for exactly this, one binary covers many targets. (d) docker buildx with QEMU emulation — works but slow for compile-heavy builds. The robust escape hatch, if the cgo dep allows: drop cgo entirely (a pure-Go image library or modernc.org/sqlite) and GOARCH alone cross-compiles cleanly.", "ru": "(a) Нативный arm64-раннер: собрать на arm64 CI-машине нативным тулчейном — проще всего, корректно по построению, цена — второй пул раннеров. (b) Кросс C-компилятор: CC=aarch64-linux-gnu-gcc (поставить gcc-aarch64-linux-gnu), чтобы C-половина целилась в arm64 — один раннер, но держишь кросс-тулчейн на каждую цель. (c) zig cc: CC=\"zig cc -target aarch64-linux-gnu\" — zig везёт самодостаточный кросс-компилятор, популярен ровно для этого, один бинарь покрывает много целей. (d) docker buildx с эмуляцией QEMU — работает, но медленно для компиляции. Надёжный аварийный выход, если cgo-зависимость позволяет: убрать cgo целиком (чисто-Go библиотека изображений или modernc.org/sqlite), и один GOARCH кросс-компилирует чисто." } },
        { "label": { "en": "3 · why the build was green", "ru": "3 · почему сборка была зелёной" }, "prompt": { "en": "The build never errored. What CI gate would have caught an arch mismatch before the bad image shipped?", "ru": "Сборка ни разу не упала с ошибкой. Какой CI-гейт поймал бы несоответствие архитектуры до отгрузки плохого образа?" }, "reveal": { "en": "Cross-builds can succeed while producing a host-arch artifact because the failure is in the C link path, not Go's front end. The cheap, deterministic gate: after each build, run file $out or go tool readelf and assert the Machine field matches the intended GOARCH — fail the job on mismatch. Better, a smoke test that actually execs the binary under the target arch (native runner or QEMU) before push. The general principle: a cross-compile that emits no error is not proof of the right target; verify the artifact's architecture, do not trust the absence of a build error.", "ru": "Кросс-сборки могут проходить, выдавая артефакт хостовой архитектуры, потому что сбой в C-линковке, а не во фронтенде Go. Дешёвый детерминированный гейт: после каждой сборки запустить file $out или go tool readelf и проверить, что поле Machine совпадает с задуманным GOARCH — валить джоб при несовпадении. Лучше — smoke-тест, реально запускающий бинарь под целевой архитектурой (нативный раннер или QEMU) до пуша. Общий принцип: кросс-компиляция без ошибки — не доказательство правильной цели; проверяй архитектуру артефакта, не доверяй отсутствию ошибки сборки." } }
      ]
    },
    {
      "id": "review-cross-compile-makefile",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": { "en": "Review a release build script and its version wiring", "ru": "Ревью релизного скрипта сборки и проводки версии" },
      "prompt": { "en": "Review this Go release build snippet (Makefile target plus the version code it feeds). Review like a senior: name the real defects, don't flag non-issues.", "ru": "Отревьюй этот фрагмент релизной сборки Go (цель Makefile плюс код версии, который она питает). Ревью как сеньор: назови реальные дефекты, не помечай не-проблемы." },
      "diff": {
        "lang": "go",
        "code": "# --- Makefile ---\n# release builds for the scratch-deployed server\nrelease:\n\tCGO_ENABLED=1 GOOS=linux GOARCH=amd64 \\\n\t  go build -ldflags \"-X main.version=$(shell git rev-parse --short HEAD)\" \\\n\t  -o dist/server ./cmd/server\n\n// --- cmd/server/build.go ---\npackage main\n\n// version is set via -ldflags at build time.\nconst version = \"dev\"\n\nfunc Version() string { return version }" },
      "findings": [
        { "id": "cgo-on-for-scratch-server", "severity": "bug", "planted": true, "label": { "en": "CGO_ENABLED=1 on a scratch-deployed server produces a dynamically-linked binary that won't run", "ru": "CGO_ENABLED=1 для scratch-сервера даёт динамически слинкованный бинарь, который не запустится" }, "explanation": { "en": "The comment says the server deploys to scratch, but CGO_ENABLED=1 links libc and a dynamic interpreter that scratch lacks — the container dies at execve. A pure-Go server must build CGO_ENABLED=0 to be truly static.", "ru": "Комментарий говорит, что сервер деплоится в scratch, но CGO_ENABLED=1 линкует libc и динамический интерпретатор, которых нет в scratch — контейнер умирает на execve. Чисто-Go серверу нужно CGO_ENABLED=0 ради настоящей статичности." } },
        { "id": "ldflags-x-on-const", "severity": "bug", "planted": true, "label": { "en": "-X cannot set a const; main.version must be a var or the injection is silently ignored", "ru": "-X не может задать const; main.version должна быть var, иначе инъекция тихо игнорируется" }, "explanation": { "en": "The linker's -X flag only overwrites a string var, not a const. With 'const version', the ldflag is silently dropped and every release reports 'dev'. Change to 'var version = \"dev\"'.", "ru": "Флаг линкера -X переписывает только string var, не const. С 'const version' ldflag тихо отбрасывается, и каждый релиз сообщает 'dev'. Заменить на 'var version = \"dev\"'." } },
        { "id": "no-trimpath-not-reproducible", "severity": "tradeoff", "planted": true, "label": { "en": "No -trimpath: build paths leak into the binary and defeat reproducible builds", "ru": "Нет -trimpath: пути сборки протекают в бинарь и ломают воспроизводимость" }, "explanation": { "en": "Without -trimpath the absolute source paths of the build machine are embedded, so the same commit produces different bytes on different runners — provenance verification by hash diff becomes impossible. Add -trimpath to every release build.", "ru": "Без -trimpath абсолютные пути исходников билд-машины встраиваются, и один коммит даёт разные байты на разных раннерах — проверка происхождения по diff хэшей становится невозможной. Добавить -trimpath в каждую релизную сборку." } }
      ],
      "decoys": [
        { "id": "no-s-w-strip-flags", "label": { "en": "Missing -s -w means the binary ships with the symbol table and is larger", "ru": "Отсутствие -s -w означает, что бинарь едет с таблицей символов и больше по размеру" }, "explanation": { "en": "looks wrong, is fine: omitting -s -w is a legitimate, often deliberate choice — unstripped binaries keep DWARF for delve and richer symbolization, at a modest size cost. It is a tradeoff, not a defect, and many teams ship unstripped on purpose. Not a bug to flag.", "ru": "выглядит ошибкой, но в порядке: опускать -s -w — законный, часто намеренный выбор — нестрипнутые бинари держат DWARF для delve и богаче символизируются ценой умеренного размера. Это трейдофф, а не дефект, и многие команды специально едут нестрипнутыми. Не баг для пометки." } }
      ]
    }
  ]
}
