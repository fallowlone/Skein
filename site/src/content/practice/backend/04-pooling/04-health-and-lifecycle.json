{
  "lessonKey": "backend/04-pooling/04-health-and-lifecycle",
  "track": "backend",
  "tasks": [
    {
      "id": "diagnose-three-controls",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Name the control and its binding constraint", "ru": "Назови контроль и его жёсткое ограничение" },
      "prompt": { "en": "For each staleness defense, fill in the one setting that implements it and the constraint that makes it actually work. Use HikariCP-style names.", "ru": "Для каждой защиты от устаревания впиши настройку, которая её реализует, и ограничение, без которого она бесполезна. Используй имена в стиле HikariCP." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["maxLifetime", "max-lifetime", "max lifetime"], "hint": { "en": "Proactively retires every connection after a fixed age (~30 min default), healthy or not.", "ru": "Проактивно выводит каждое соединение после фиксированного возраста (~30 мин по умолчанию), здоровое оно или нет." } },
          { "id": "b2", "accept": ["wait_timeout", "wait-timeout", "db wait_timeout", "database wait_timeout", "server timeout"], "hint": { "en": "maxLifetime must be shorter than this server-side limit so the pool closes first.", "ru": "maxLifetime должен быть короче этого серверного лимита, чтобы пул закрыл соединение первым." } },
          { "id": "b3", "accept": ["minimumIdle", "minimum-idle", "minimum idle", "minIdle"], "hint": { "en": "idleTimeout only shrinks the pool when this is set below the maximum.", "ru": "idleTimeout сжимает пул только если это установлено ниже максимума." } },
          { "id": "b4", "accept": ["aliveBypassWindow", "alive-bypass-window", "bypass window", "alive bypass window"], "hint": { "en": "A connection re-borrowed within 500 ms is trusted without a validation probe.", "ru": "Соединение, взятое заново в пределах 500 мс, считается живым без проверочного пинга." } }
        ]
      }
    },
    {
      "id": "predict-restart-storm",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Predict the failure after a database restart", "ru": "Предскажи отказ после рестарта базы" },
      "prompt": { "en": "Reason about what the pool holds after the DB bounces, and which requests pay for it.", "ru": "Рассуди, что держит пул после перезапуска базы и какие запросы за это заплатят." },
      "scenario": { "en": "A pool holds 20 connections to Postgres. At 02:10 the DBA restarts the database for a minor-version patch; it is back up in 8 seconds. The app does no validation on borrow, maxLifetime is unset (connections live forever), and traffic is light overnight so most pooled connections sit idle. At 02:10:08 the next request arrives and is handed a pooled connection. What happens to it, and how many connections in the pool are affected?", "ru": "Пул держит 20 соединений к Postgres. В 02:10 DBA перезапускает базу ради патча минорной версии; она поднимается за 8 секунд. Приложение не валидирует соединения при выдаче, maxLifetime не задан (соединения живут вечно), а ночью трафик низкий, поэтому большинство соединений в пуле простаивают. В 02:10:08 приходит следующий запрос и получает соединение из пула. Что с ним произойдёт и сколько соединений в пуле затронуто?" },
      "reveal": { "en": "When Postgres restarted it dropped all 20 server-side backends, but the client sockets were never notified — the pool still sees them as ESTABLISHED. With no validation on borrow, the first request gets a dead socket: it writes its query into a closed pipe and fails immediately with 'connection reset by peer' (or a hang until OS TCP timeout). Crucially this is not one bad connection — all 20 pooled connections are stale, so every request keeps drawing dead sockets until the pool eventually discovers each failure, discards it, and reconnects one at a time. A single test-on-borrow ping (or a `maxLifetime` shorter than the restart-to-traffic gap) would have caught the dead socket and silently replaced it, so the request never sees the reset. This is exactly why validation defends the idle-death case and the first-request-after-a-quiet-period is the classic victim.", "ru": "При перезапуске Postgres сбросил все 20 серверных backend'ов, но клиентские сокеты об этом не уведомили — пул по-прежнему видит их как ESTABLISHED. Без валидации при выдаче первый запрос получает мёртвый сокет: он пишет запрос в закрытую трубу и сразу падает с 'connection reset by peer' (или висит до TCP-таймаута ОС). Важно: это не одно плохое соединение — все 20 соединений пула устарели, поэтому каждый запрос будет вытаскивать мёртвые сокеты, пока пул по очереди не обнаружит каждый сбой, не выбросит соединение и не переподключится. Один пинг test-on-borrow (или `maxLifetime` короче паузы между рестартом и трафиком) поймал бы мёртвый сокет и тихо заменил его, и запрос не увидел бы reset. Именно поэтому валидация закрывает случай смерти при простое, а первый запрос после затишья — классическая жертва." }
    },
    {
      "id": "sandbox-lifetime-budget",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Compute a safe maxLifetime", "ru": "Посчитай безопасный maxLifetime" },
      "prompt": { "en": "Implement chooseMaxLifetime(limits) returning the maxLifetime in seconds: it must be strictly below every external idle/lifetime limit (DB wait_timeout, NAT idle, LB idle), with a safety margin. The rule from the lesson: stay below the smallest external limit by a margin. Use the smallest limit, subtract a 60s safety margin, and never return more than the 30-minute default (1800s). Run the provided cases — the harness prints each result.", "ru": "Реализуй chooseMaxLifetime(limits), возвращающую maxLifetime в секундах: она должна быть строго меньше любого внешнего лимита простоя/жизни (DB wait_timeout, NAT idle, LB idle) с запасом. Правило из урока: держись ниже наименьшего внешнего лимита с запасом. Возьми наименьший лимит, вычти 60с запаса и никогда не возвращай больше дефолтных 30 минут (1800с). Запусти приведённые случаи — обвязка печатает каждый результат." },
      "runtime": "js",
      "initialCode": "function chooseMaxLifetime(limits) {\n  // TODO: take the smallest external limit, subtract a 60s safety margin,\n  // and never return more than the 30-minute default (1800s).\n}\n\n// Case A: cloud NAT idle 350s is the tightest limit\nconsole.log(chooseMaxLifetime([28800, 350, 600]));\n// Case B: only generous limits -> capped at the 1800s default\nconsole.log(chooseMaxLifetime([28800, 3600, 7200]));\n// Case C: MySQL wait_timeout 8h is large, LB idle 900s wins\nconsole.log(chooseMaxLifetime([28800, 900]));",
      "expected": { "kind": "stdout-equals", "value": "290\n1800\n840" }
    },
    {
      "id": "design-lifetime-vs-idle",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Set the lifecycle budget against the network", "ru": "Выставь бюджет жизненного цикла против сети" },
      "prompt": { "en": "Design the full lifecycle configuration for this pool. Decide maxLifetime, keepalive interval, idleTimeout, minimumIdle, and validation policy, and justify each number against a specific external limit. Self-grade against the rubric.", "ru": "Спроектируй полную конфигурацию жизненного цикла для этого пула. Выбери maxLifetime, интервал keepalive, idleTimeout, minimumIdle и политику валидации, обосновав каждое число конкретным внешним лимитом. Оцени себя по рубрике." },
      "constraints": { "en": "App on Kubernetes behind a cloud NAT gateway with a 350s idle timeout. Postgres via a PgBouncer (server_idle_timeout 600s) in front of it; Postgres idle_in_transaction_session_timeout is 60s. Pool max=20, traffic is bursty: heavy 09:00–18:00, near-idle overnight. p99 query latency budget 50ms; you cannot afford a validation round trip on every checkout.", "ru": "Приложение в Kubernetes за облачным NAT-шлюзом с таймаутом простоя 350с. Postgres через PgBouncer (server_idle_timeout 600с) перед ним; idle_in_transaction_session_timeout Postgres равен 60с. Пул max=20, трафик всплесковый: пик 09:00–18:00, ночью почти простой. Бюджет p99 запроса 50мс; round trip валидации на каждой выдаче ты позволить не можешь." },
      "rubric": [
        { "en": "Sets maxLifetime strictly below the tightest external idle limit (NAT 350s), e.g. ~270–290s, not the 30-min default — and explains the NAT 350s is the binding constraint.", "ru": "Ставит maxLifetime строго ниже самого жёсткого внешнего лимита простоя (NAT 350с), например ~270–290с, а не дефолтные 30 мин — и объясняет, что NAT 350с — связывающее ограничение." },
        { "en": "Adds a keepalive probe interval shorter than 350s so an idle connection is kept warm through the NAT idle window rather than dropped.", "ru": "Добавляет интервал keepalive короче 350с, чтобы простаивающее соединение поддерживалось живым сквозь окно простоя NAT, а не сбрасывалось." },
        { "en": "Uses idleTimeout with minimumIdle below max (e.g. minimumIdle 2–4) so the pool shrinks overnight, noting idleTimeout is inert if minimumIdle equals max.", "ru": "Использует idleTimeout с minimumIdle ниже max (например minimumIdle 2–4), чтобы пул сжимался ночью, отмечая, что idleTimeout бесполезен, если minimumIdle равен max." },
        { "en": "Keeps validation as test-on-borrow with an aliveBypassWindow (~500ms) instead of validating every checkout, protecting the 50ms p99 budget on hot connections.", "ru": "Оставляет валидацию как test-on-borrow с aliveBypassWindow (~500мс) вместо проверки на каждой выдаче, защищая бюджет p99 50мс на горячих соединениях." }
      ],
      "model": { "en": "Bind everything to the tightest external limit, the NAT 350s idle. Set maxLifetime ~285s (350s minus a margin) so every connection is proactively retired before NAT, PgBouncer (600s), or the server can drop it unannounced — this is the lever that prevents in-use death. Add a keepalive probe every ~150s so a connection idle but not yet retired stays warm through the NAT window and never goes silent. Set minimumIdle to 2–4 with idleTimeout ~120s so the pool collapses to a small warm core overnight (idleTimeout does nothing if minimumIdle equals max). Validation: test-on-borrow with the 500ms aliveBypassWindow, so a connection re-borrowed within half a second skips the probe — hot daytime traffic pays no extra round trip and stays under 50ms, while the first request after a lull still gets validated. Note the 60s idle_in_transaction_session_timeout only bites if you leave a transaction open between checkouts, which a correct app does not; the 350s NAT limit is what actually governs maxLifetime here.", "ru": "Привяжи всё к самому жёсткому внешнему лимиту — NAT 350с простоя. Поставь maxLifetime ~285с (350с минус запас), чтобы каждое соединение проактивно выводилось раньше, чем его без предупреждения сбросит NAT, PgBouncer (600с) или сервер — это рычаг, предотвращающий смерть во время использования. Добавь keepalive каждые ~150с, чтобы простаивающее, но ещё не выведенное соединение оставалось тёплым сквозь окно NAT и не замолкало. Поставь minimumIdle 2–4 с idleTimeout ~120с, чтобы ночью пул сжимался до небольшого тёплого ядра (idleTimeout бесполезен, если minimumIdle равен max). Валидация: test-on-borrow с aliveBypassWindow 500мс, чтобы соединение, взятое заново в пределах полусекунды, пропускало пинг — горячий дневной трафик не платит лишний round trip и остаётся под 50мс, а первый запрос после затишья всё равно проходит проверку. Учти, что idle_in_transaction_session_timeout 60с кусается, только если оставить транзакцию открытой между выдачами, чего корректное приложение не делает; именно лимит NAT 350с реально определяет maxLifetime здесь." }
    }
  ]
}
