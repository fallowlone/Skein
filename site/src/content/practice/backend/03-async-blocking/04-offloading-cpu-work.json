{
  "lessonKey": "backend/03-async-blocking/04-offloading-cpu-work",
  "track": "backend",
  "tasks": [
    {
      "id": "diagnose-wrong-pool",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": {
        "en": "Which pool runs which work",
        "ru": "Какой пул выполняет какую работу"
      },
      "prompt": {
        "en": "A team raised <code>UV_THREADPOOL_SIZE</code> from 4 to 32 to fix a CPU-heavy JS image resize, and nothing improved. Fill in the blanks to explain why the libuv pool is the wrong lever and which mechanism actually runs the resize.",
        "ru": "Команда подняла <code>UV_THREADPOOL_SIZE</code> с 4 до 32, чтобы починить CPU-тяжёлый JS-ресайз картинок, и ничего не улучшилось. Заполни пропуски, объяснив, почему пул libuv — не тот рычаг и какой механизм реально выполняет ресайз."
      },
      "evidence": {
        "en": "// pure-JS resize on the request handler\napp.post('/resize', (req, res) => { res.send(resizeSync(req.body.buffer)); });\n// env: UV_THREADPOOL_SIZE=32",
        "ru": "// чистый JS-ресайз в обработчике запроса\napp.post('/resize', (req, res) => { res.send(resizeSync(req.body.buffer)); });\n// env: UV_THREADPOOL_SIZE=32"
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "native io",
              "native i/o",
              "native operations",
              "fs/dns/crypto/zlib",
              "fs, dns, crypto, zlib",
              "native",
              "io"
            ],
            "hint": {
              "en": "What the libuv pool actually runs — not JavaScript.",
              "ru": "Что пул libuv реально выполняет — не JavaScript."
            }
          },
          {
            "id": "b2",
            "accept": [
              "worker thread",
              "worker threads",
              "worker_threads",
              "worker",
              "a worker thread"
            ],
            "hint": {
              "en": "Separate V8 isolate with true multicore parallelism for CPU-bound JS.",
              "ru": "Отдельный изолят V8 с истинным многоядерным параллелизмом для CPU-bound JS."
            }
          }
        ]
      }
    },
    {
      "id": "predict-clone-cost",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": {
        "en": "Predict the worker round-trip cost",
        "ru": "Предскажи цену кругового рейса воркера"
      },
      "prompt": {
        "en": "You move a multi-MB ArrayBuffer to a worker via postMessage with no transferList. Predict where the time goes and how the number changes if you move ownership instead.",
        "ru": "Ты перемещаешь многомегабайтный ArrayBuffer в воркер через postMessage без transferList. Предскажи, куда уходит время и как изменится число, если вместо этого передать владение."
      },
      "scenario": {
        "en": "Same buffer, two paths: (a) default postMessage(buf); (b) postMessage(buf, [buf]) so the ArrayBuffer rides in the transferList. The worker does almost no compute — you are measuring delivery, not the job.",
        "ru": "Один и тот же буфер, два пути: (a) обычный postMessage(buf); (b) postMessage(buf, [buf]), чтобы ArrayBuffer ехал в transferList. Воркер почти ничего не считает — ты меряешь доставку, а не саму работу."
      },
      "reveal": {
        "en": "Path (a) deep-copies the buffer with the structured-clone algorithm — measured around 268 ms for a multi-MB ArrayBuffer; that copy is the cost, not the worker's compute. Path (b) transfers ownership: the bytes are not copied, only the handle moves (the sender can no longer touch the buffer), so the same delivery drops to about 29 ms — roughly a 9× win. Lesson: offloading only pays when the CPU work dwarfs this message-passing cost; a 2 ms computation can be slower in a worker than inline once you pay clone plus round-trip.",
        "ru": "Путь (a) глубоко копирует буфер алгоритмом structured-clone — замерялось около 268 мс для многомегабайтного ArrayBuffer; эта копия и есть цена, а не вычисление воркера. Путь (b) передаёт владение: байты не копируются, перемещается только хэндл (отправитель больше не может трогать буфер), поэтому та же доставка падает примерно до 29 мс — выигрыш около 9×. Вывод: вынос окупается, только когда CPU-работа затмевает эту цену передачи сообщений; вычисление на 2 мс может оказаться в воркере медленнее, чем инлайн, как только заплатишь за клон плюс круговой рейс."
      }
    },
    {
      "id": "sandbox-chunk-loop",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Chunk a long loop across simulated ticks",
        "ru": "Нарежь длинный цикл на симулированные тики"
      },
      "prompt": {
        "en": "A 100000-iteration sum monopolizes the loop. Partition it: process a fixed chunk, then yield via the provided setImmediate shim so queued I/O can interleave, then resume — until done. Log the final sum. Keep total work identical; you are only changing WHEN it runs, not WHAT it computes.",
        "ru": "Сумма на 100000 итераций монополизирует цикл. Разбей её: обработай фиксированный кусок, затем уступи через предоставленный шим setImmediate, чтобы вклинился очередной I/O, затем продолжи — до завершения. Залогируй итоговую сумму. Сохрани общий объём работы тем же; ты меняешь только КОГДА это бежит, а не ЧТО считает."
      },
      "runtime": "js",
      "setup": "// Deterministic setImmediate shim: drains a FIFO macrotask queue, no real timers.\nglobalThis.__q = [];\nglobalThis.setImmediate = (fn) => { globalThis.__q.push(fn); };\nglobalThis.__drain = () => { while (globalThis.__q.length) { const fn = globalThis.__q.shift(); fn(); } };",
      "expected": {
        "kind": "stdout-contains",
        "value": "4999950000"
      }
    },
    {
      "id": "design-offload-strategy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 14,
      "title": {
        "en": "Pick the offload strategy per CPU task",
        "ru": "Выбери стратегию выноса под каждую CPU-задачу"
      },
      "prompt": {
        "en": "Four CPU profiles hit the same Node service. For each, choose exactly one of: leave inline, chunk + setImmediate, worker pool, or cluster / horizontal scale — and justify it by task size and frequency. Then self-grade against the rubric.",
        "ru": "Четыре CPU-профиля приходят в один Node-сервис. Для каждого выбери ровно одно из: оставить инлайн, нарезка + setImmediate, пул воркеров или cluster / горизонтальное масштабирование — и обоснуй размером и частотой задачи. Затем оцени себя по рубрике."
      },
      "constraints": {
        "en": "8-core box, one Node process = one loop, work_mem of workers is irrelevant. Tasks: (T1) a 2 ms hot-path validation on every request; (T2) a single monolithic 400 ms image resize, ~5 req/s; (T3) summing/scanning a 2M-element in-memory array on a few admin reports, must stay responsive; (T4) the whole service is CPU-saturated at peak across all endpoints.",
        "ru": "8-ядерная машина, один процесс Node = один цикл, work_mem воркеров нерелевантен. Задачи: (T1) валидация на горячем пути 2 мс на каждый запрос; (T2) одиночный монолитный ресайз картинки 400 мс, ~5 rps; (T3) суммирование/скан in-memory массива на 2M элементов в нескольких админ-отчётах, должно оставаться отзывчивым; (T4) весь сервис на пике упирается в CPU по всем эндпоинтам."
      },
      "rubric": [
        {
          "en": "T1 stays inline: 2 ms is below the offload threshold — clone plus round-trip would cost more than the work itself.",
          "ru": "T1 остаётся инлайн: 2 мс ниже порога выноса — клон плюс круговой рейс обойдутся дороже самой работы."
        },
        {
          "en": "T2 goes to a reused worker pool, NOT a fresh worker per request: monolithic + heavy is the worker case, and pooling (~one per core, e.g. Piscina) avoids paying isolate startup each call.",
          "ru": "T2 идёт в переиспользуемый пул воркеров, а НЕ свежий воркер на запрос: монолитная + тяжёлая — это случай воркера, а пулинг (~один на ядро, напр. Piscina) избегает оплаты старта изолята на каждом вызове."
        },
        {
          "en": "T3 chunks + setImmediate: long-but-interruptible array work, partition and yield to keep the loop responsive, trading throughput, no transfer cost.",
          "ru": "T3 нарезка + setImmediate: длинная-но-прерываемая работа с массивом, разбей и уступай, чтобы держать цикл отзывчивым, ценой пропускной способности, без цены передачи."
        },
        {
          "en": "T4 is a core-count problem → cluster / more machines, because one process maps to one loop and no single-task trick lifts whole-service CPU throughput.",
          "ru": "T4 — проблема числа ядер → cluster / больше машин, потому что один процесс маппится на один цикл и ни один трюк с одной задачей не поднимает CPU-пропускную способность всего сервиса."
        }
      ],
      "model": {
        "en": "T1 → leave inline: at 2 ms the message-passing and round-trip dominate the actual compute, so a worker would be a net loss; only offload when the CPU cost dwarfs the transfer. T2 → worker pool: a 400 ms monolithic resize is pure CPU-bound JS that the libuv pool will never run, so it needs a worker thread; create a fixed pool (~one per core on the 8-core box) once and reuse it via a task queue rather than spawning a worker per request, which would turn a CPU problem into thread-churn (tens of ms startup and real memory each). At ~5 req/s the pool is comfortably bounded. T3 → chunk + setImmediate: scanning 2M elements is long but interruptible, so process a fixed chunk, setImmediate to yield the loop so I/O callbacks interleave, then resume — keeping admin reports responsive at the cost of total throughput and with no structured-clone transfer cost. T4 → cluster / horizontal scale: when CPU is the bottleneck across the whole service, one Node process is one loop, so the only lever is more loops — cluster across the 8 cores or add machines; chunking and a single pool cannot raise whole-service CPU throughput.",
        "ru": "T1 → оставить инлайн: на 2 мс передача сообщений и круговой рейс доминируют над самим вычислением, поэтому воркер будет чистым проигрышем; выноси, только когда CPU-цена затмевает передачу. T2 → пул воркеров: монолитный ресайз 400 мс — это чистый CPU-bound JS, который пул libuv никогда не выполнит, поэтому нужен worker thread; создай фиксированный пул (~один на ядро на 8-ядерной машине) один раз и переиспользуй через очередь задач, а не порождай воркер на запрос, что превратит CPU-проблему в текучку потоков (десятки мс старта и реальная память на каждого). При ~5 rps пул комфортно ограничен. T3 → нарезка + setImmediate: скан 2M элементов длинный, но прерываемый, поэтому обработай фиксированный кусок, setImmediate, чтобы уступить цикл и дать вклиниться I/O-колбэкам, затем продолжи — держа админ-отчёты отзывчивыми ценой общей пропускной способности и без цены передачи structured-clone. T4 → cluster / горизонтальное масштабирование: когда CPU — узкое место всего сервиса, один процесс Node — это один цикл, поэтому единственный рычаг — больше циклов: cluster по 8 ядрам или добавить машины; нарезка и единственный пул не поднимут CPU-пропускную способность всего сервиса."
      }
    }
  ]
}
