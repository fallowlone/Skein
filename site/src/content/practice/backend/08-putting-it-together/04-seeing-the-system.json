{
  "lessonKey": "backend/08-putting-it-together/04-seeing-the-system",
  "track": "backend",
  "tasks": [
    {
      "id": "diagnose-golden-signals",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Name the signals behind RED and the four golden signals", "ru": "Назови сигналы за RED и четырьмя золотыми сигналами" },
      "prompt": { "en": "A new service ships with a dashboard that shows only a single 'average latency' tile. Fill in the request-edge signals it is missing.", "ru": "Новый сервис выкатывается с дашбордом, на котором есть только одна плитка «средняя задержка». Заполни сигналы на границе запроса, которых не хватает." },
      "evidence": { "en": "Dashboard tiles present: [ avg_latency: 180ms ]\nDashboard tiles missing: ___, ___, ___ (RED) plus ___ (the 4th golden signal that RED omits)", "ru": "Dashboard tiles present: [ avg_latency: 180ms ]\nDashboard tiles missing: ___, ___, ___ (RED) plus ___ (the 4th golden signal that RED omits)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["rate", "traffic", "requests per second", "rps", "request rate"], "hint": { "en": "The R in RED: the load the service carries.", "ru": "R в RED: нагрузка, которую несёт сервис." } },
          { "id": "b2", "accept": ["errors", "error rate", "errors rate", "failed fraction", "error fraction"], "hint": { "en": "The E in RED: includes breaker rejections, not just 5xx.", "ru": "E в RED: включает отказы breaker'а, а не только 5xx." } },
          { "id": "b3", "accept": ["duration", "latency distribution", "duration distribution", "latency", "percentile latency"], "hint": { "en": "The D in RED: the distribution of response times, not the mean.", "ru": "D в RED: распределение времён ответа, а не среднее." } },
          { "id": "b4", "accept": ["saturation", "saturation signal", "resource saturation"], "hint": { "en": "The golden signal RED leaves out — how full a resource is (pool, loop, queue).", "ru": "Золотой сигнал, который RED опускает — насколько заполнен ресурс (пул, loop, очередь)." } }
        ]
      }
    },
    {
      "id": "sandbox-p99-vs-mean",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 8,
      "title": { "en": "Compute p99 from a latency sample and watch the mean lie", "ru": "Посчитай p99 по выборке задержек и увидь, как лжёт среднее" },
      "prompt": { "en": "Run the snippet. It builds a realistic sample — 99 fast requests at 20ms and one slow request at 4000ms — then prints the mean and the p99 (nearest-rank: sort ascending, index = ceil(p/100 * n) - 1). Notice the mean reads ~60ms (a value nobody experiences) while p99 surfaces the 4s tail the cascade hides in. The output is deterministic; confirm it matches exactly.", "ru": "Запусти сниппет. Он строит реалистичную выборку — 99 быстрых запросов по 20ms и один медленный по 4000ms — затем печатает среднее и p99 (nearest-rank: сортировка по возрастанию, индекс = ceil(p/100 * n) - 1). Заметь: среднее показывает ~60ms (значение, которое никто не испытывает), а p99 вытаскивает хвост в 4s, в котором прячется каскад. Вывод детерминирован; подтверди точное совпадение." },
      "runtime": "js",
      "setup": "const samples = [];\nfor (let i = 0; i < 99; i++) samples.push(20);\nsamples.push(4000);\n\nfunction percentile(arr, p) {\n  const sorted = [...arr].sort((a, b) => a - b);\n  const rank = Math.ceil((p / 100) * sorted.length);\n  return sorted[rank - 1];\n}\n\nconst mean = samples.reduce((a, b) => a + b, 0) / samples.length;\nconst p50 = percentile(samples, 50);\nconst p99 = percentile(samples, 99);\nconsole.log('mean=' + mean.toFixed(1) + 'ms p50=' + p50 + 'ms p99=' + p99 + 'ms');\nconsole.log(mean < p99 ? 'mean HIDES the tail' : 'mean ok');",
      "expected": { "kind": "stdout-equals", "value": "mean=59.8ms p50=20ms p99=20ms\nmean HIDES the tail" }
    },
    {
      "id": "predict-breaker-flap-invisible",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict what the on-call sees during a breaker flap", "ru": "Предскажи, что видит дежурный во время флапа breaker'а" },
      "prompt": { "en": "Predict what the 3am on-call engineer can and cannot see, and why — then reveal.", "ru": "Предскажи, что дежурный инженер в 3 ночи видит и не видит, и почему — затем открой ответ." },
      "scenario": { "en": "A downstream payment API starts failing intermittently. The circuit breaker transitions closed → open → half-open → open four times in one minute. The dashboard emits RED (rate, errors, duration) and pool in-use, but breaker state is only written to application logs, never to a metric. The error rate tile shows 0.4% because breaker-rejected requests are served a cached fallback and counted as success. What does the on-call see, and what is the single highest-value signal they are blind to?", "ru": "Нижестоящий платёжный API начинает периодически сбоить. Circuit breaker переходит closed → open → half-open → open четыре раза за минуту. Дашборд эмитит RED (rate, errors, duration) и pool in-use, но состояние breaker'а пишется только в логи приложения, а не в метрику. Плитка error rate показывает 0.4%, потому что отклонённые breaker'ом запросы обслуживаются кэшированным фоллбэком и считаются успешными. Что видит дежурный, и какого единственного самого ценного сигнала он не видит?" },
      "reveal": { "en": "The on-call sees a deceptively calm board: RED looks nearly healthy (errors 0.4%, served from fallback), pool in-use is normal because the breaker is shedding load to the downstream rather than holding connections, and duration may even look better since rejected calls return fast from cache. The single highest-value signal they are blind to is the breaker state and its transition count: four open/close flaps in a minute is the clearest possible statement that a dependency is unhealthy, yet it lives in logs nobody is tailing. Fix: emit breaker state (closed/open/half-open) and a transition counter as first-class metrics, and alert on flap rate. A flapping breaker is a leading indicator — it fires before the fallback cache goes stale and real errors finally surface in RED.", "ru": "Дежурный видит обманчиво спокойный борд: RED почти здоров (ошибки 0.4%, отдаются из фоллбэка), pool in-use в норме, потому что breaker сбрасывает нагрузку на нижестоящий, а не держит соединения, а duration может даже выглядеть лучше, поскольку отклонённые вызовы быстро возвращаются из кэша. Единственный самый ценный сигнал, которого он не видит, — состояние breaker'а и счётчик его переходов: четыре флапа open/close за минуту — яснейшее заявление, что зависимость нездорова, но оно лежит в логах, которые никто не читает. Фикс: эмить состояние breaker'а (closed/open/half-open) и счётчик переходов как полноценные метрики и алертить по частоте флапов. Флапающий breaker — опережающий индикатор: он срабатывает раньше, чем протухнет кэш фоллбэка и реальные ошибки наконец проявятся в RED." }
    },
    {
      "id": "design-cascade-dashboard",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Design a dashboard that catches the cascade early", "ru": "Спроектируй дашборд, который ловит каскад заранее" },
      "prompt": { "en": "Design the telemetry and dashboard for a Node service that fronts a downstream API, using a connection pool, a circuit breaker, and retries. The goal: surface the cascade from the previous lesson before it collapses the service, and wire an SLO/error-budget decision on top. Specify exactly which metrics each mechanism emits, which percentiles you watch, and what gates shipping. Self-grade against the rubric.", "ru": "Спроектируй телеметрию и дашборд для Node-сервиса, который стоит перед нижестоящим API, использует пул соединений, circuit breaker и ретраи. Цель: вытащить каскад из предыдущего урока до того, как он обрушит сервис, и навесить сверху решение по SLO/error-budget. Укажи точно, какие метрики эмитит каждый механизм, какие перцентили ты смотришь и что гейтит выкатку. Оцени себя по рубрике." },
      "constraints": { "en": "Pool limit 50. Single-threaded event loop. Breaker wraps the downstream. Retries with backoff. SLO: 99.9% of requests succeed under 300ms over 30 days (error budget ≈ 43m/month). 8 services, one shared Grafana board. Alerting must give ≥5 minutes of warning before user-visible collapse.", "ru": "Лимит пула 50. Однопоточный event loop. Breaker оборачивает нижестоящий. Ретраи с backoff. SLO: 99.9% запросов успешны под 300ms за 30 дней (error budget ≈ 43 мин/месяц). 8 сервисов, один общий борд Grafana. Алертинг должен давать ≥5 минут предупреждения до видимого пользователю обрушения." },
      "rubric": [
        { "en": "Emits RED at the request edge with duration as p50/p99/p999 (never the mean), and includes breaker rejections in the error count.", "ru": "Эмитит RED на границе запроса с duration как p50/p99/p999 (никогда не среднее) и включает отказы breaker'а в счётчик ошибок." },
        { "en": "Instruments each mechanism's leading-indicator state: pool in-use/limit + acquire-wait, event-loop lag, breaker state + transition count, retry rate separate from request rate.", "ru": "Инструментирует опережающее состояние каждого механизма: pool in-use/limit + acquire-wait, лаг event loop'а, состояние breaker'а + счётчик переходов, retry rate отдельно от request rate." },
        { "en": "Defines the alert ladder on leading indicators (pool saturation >0.8, p99 trend, retry-rate ratio, breaker flap) so it fires ≥5 min before RED errors spike.", "ru": "Определяет лестницу алертов по опережающим индикаторам (насыщение пула >0.8, тренд p99, доля retry-rate, флап breaker'а), чтобы сработать за ≥5 мин до всплеска ошибок в RED." },
        { "en": "Connects the SLO/error budget to a ship-or-freeze decision: spend budget on features when healthy, freeze and do reliability work when nearly exhausted.", "ru": "Связывает SLO/error budget с решением ship-or-freeze: тратишь бюджет на фичи, когда здоров, замораживаешь и делаешь надёжность, когда почти исчерпан." }
      ],
      "model": { "en": "Request edge: emit rate (rps), errors (a fraction that counts breaker rejections and retry-exhausted failures, not just 5xx), and duration as a histogram → derive p50/p99/p999; the board's primary latency tile is p99, with p999 as a secondary disaster line and the mean explicitly excluded. Mechanism panels: pool publishes in_use/limit (alert at saturation >0.8 = 40/50) and acquire_wait_ms p99; event loop publishes loop_lag_ms (alert as it rises, since it starves every concurrent request); breaker publishes state (closed/open/half-open as a gauge) and a transition counter (alert on flap rate, not a log line); retries publish retry_rate as a separate series and a retries/requests ratio (alert as it climbs toward 1.0 — a storm forming). Alert ladder is built on leading indicators so it warns ≥5 min ahead: pool saturation and acquire-wait rise first, then p99 lifts while the mean is still flat, then the breaker flaps — all before RED error rate spikes. SLO layer: 99.9%/300ms over 30 days gives ~43 min of monthly error budget; a burn-rate panel (e.g. 14.4× fast-burn over 1h, 6× over 6h) drives the governor — when budget is healthy the team ships features and spends risk, when burn-rate alerts fire or the budget is nearly exhausted the team freezes features and shifts engineering to reliability. The shared 8-service board templates these panels per service so the same ladder applies everywhere.", "ru": "Граница запроса: эмить rate (rps), errors (доля, считающая отказы breaker'а и провалы исчерпанных ретраев, а не только 5xx) и duration как гистограмму → выведи p50/p99/p999; главная плитка задержки на борде — p99, p999 как вторичная «катастрофическая» линия, а среднее явно исключено. Панели механизмов: пул публикует in_use/limit (алерт при насыщении >0.8 = 40/50) и acquire_wait_ms p99; event loop публикует loop_lag_ms (алерт при росте, ведь он морит голодом каждый конкурентный запрос); breaker публикует state (closed/open/half-open как gauge) и счётчик переходов (алерт по частоте флапов, а не строка лога); ретраи публикуют retry_rate отдельной серией и отношение retries/requests (алерт при росте к 1.0 — формируется шторм). Лестница алертов построена на опережающих индикаторах, чтобы предупредить за ≥5 мин: первыми растут насыщение пула и acquire-wait, затем поднимается p99, пока среднее ещё плоское, затем флапает breaker — всё это до всплеска error rate в RED. Слой SLO: 99.9%/300ms за 30 дней даёт ~43 мин месячного error budget; панель burn-rate (например 14.4× быстрое сгорание за 1ч, 6× за 6ч) управляет governor'ом — когда бюджет здоров, команда выкатывает фичи и тратит риск, когда срабатывают алерты burn-rate или бюджет почти исчерпан, команда замораживает фичи и переводит инженерию на надёжность. Общий борд на 8 сервисов шаблонизирует эти панели по сервису, чтобы одна и та же лестница работала везде." }
    }
  ]
}
