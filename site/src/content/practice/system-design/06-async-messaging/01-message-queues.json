{
  "lessonKey": "system-design/06-async-messaging/01-message-queues",
  "track": "system-design",
  "tasks": [
    {
      "id": "predict-visibility-timeout-duplicate",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Predict the duplicate", "ru": "Предскажи дубль" },
      "prompt": { "en": "Reason about the visibility timeout, then reveal what the consumer fleet actually does.", "ru": "Порассуждай о visibility timeout, затем открой, что реально делает флот consumer'ов." },
      "scenario": { "en": "An at-least-once queue has its visibility timeout set to 30 seconds. A worker pulls a message and starts a job that, because a dependency is slow today, takes 50 seconds to finish. The handler is NOT idempotent — it sends a webhook to the customer's URL. What does the customer observe, and why?", "ru": "У очереди at-least-once visibility timeout выставлен в 30 секунд. Воркер забирает сообщение и стартует задачу, которая из-за сегодня медленной зависимости занимает 50 секунд. Обработчик НЕ идемпотентен — он шлёт вебхук на URL клиента. Что наблюдает клиент и почему?" },
      "reveal": { "en": "The customer receives the webhook TWICE. At 30s the timeout expires while worker A is still running (it never deleted the message), so the queue assumes a crash and redelivers to worker B, which sends a second webhook around the 30–35s mark; worker A then finishes near 50s and sends its own. The bug isn't the queue — it's a timeout sized below real processing time plus a non-idempotent handler. Fixes: set the timeout above p99 processing time, heartbeat to extend it while the job runs, and make the handler idempotent (dedup on a message key) so even a wrong timeout can't double-fire.", "ru": "Клиент получает вебхук ДВАЖДЫ. На 30с таймаут истекает, пока воркер A ещё работает (он так и не удалил сообщение), поэтому очередь считает это падением и переотправляет воркеру B, который шлёт второй вебхук около отметки 30–35с; затем воркер A заканчивает к ~50с и шлёт свой. Баг не в очереди — он в таймауте, размеренном ниже реального времени обработки, плюс неидемпотентный обработчик. Починки: выставь таймаут выше p99 времени обработки, heartbeat'ь, продлевая его, пока задача идёт, и сделай обработчик идемпотентным (дедуп по ключу сообщения), чтобы даже неверный таймаут не выстрелил дважды." }
    },
    {
      "id": "diagnose-lost-message-ack-order",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "The vanishing messages", "ru": "Исчезающие сообщения" },
      "prompt": { "en": "A consumer occasionally loses messages on crash. Fill in the delivery semantic it's running and the single line that fixes it.", "ru": "Consumer изредка теряет сообщения при падении. Впиши семантику доставки, на которой он работает, и единственную строку, что чинит это." },
      "evidence": { "en": "handler(msg):\n    queue.ack(msg)          # line A\n    result = process(msg)   # line B — sometimes the worker is OOM-killed here\n    save(result)\nsymptom: a few messages are never processed after a worker restart", "ru": "handler(msg):\n    queue.ack(msg)          # строка A\n    result = process(msg)   # строка B — иногда воркер тут убит по OOM\n    save(result)\nсимптом: несколько сообщений так и не обработаны после рестарта воркера" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "semantic", "accept": ["at-most-once", "at most once", "atmostonce", "at-most once"], "hint": { "en": "Acking BEFORE processing gives you which guarantee — no duplicates, possible loss?", "ru": "Ack ДО обработки даёт какую гарантию — без дублей, возможна потеря?" } },
          { "id": "fix", "accept": ["ack after process", "ack last", "move ack after processing", "ack after processing", "ack after save", "acknowledge after processing"], "hint": { "en": "Where must the ack move so a crash redelivers instead of losing the message?", "ru": "Куда должен переехать ack, чтобы падение переотправляло, а не теряло сообщение?" } }
        ]
      }
    },
    {
      "id": "fix-poison-message-loop",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Break the poison loop", "ru": "Разорви ядовитый цикл" },
      "prompt": { "en": "One malformed message is being redelivered forever, throttling the whole queue. Identify why and design the fix, then check against the model.", "ru": "Одно кривое сообщение переотправляется вечно, тротля всю очередь. Назови причину и спроектируй починку, затем сверься с образцом." },
      "starter": "def handler(msg):\n    data = parse(msg.body)   # throws on one malformed message\n    apply(data)\n    queue.ack(msg)\n# this message has been redelivered ~40,000 times today",
      "grading": {
        "mode": "self",
        "model": { "en": "This is a poison message: parse() always throws, so the handler never acks, the visibility timeout expires, and the message is redelivered endlessly — burning consumer capacity and starving healthy messages. The fix is a dead-letter queue (DLQ): configure a maxReceiveCount (e.g. 5) so that after N failed deliveries the broker moves the message to a separate DLQ instead of re-driving it. Alarm on DLQ depth (a non-empty DLQ means a bug, not a blip), and consume the DLQ manually to inspect/fix/replay. Optionally wrap parse() to catch and route obviously-invalid input to the DLQ immediately rather than burning N retries on input that can never succeed.", "ru": "Это ядовитое сообщение: parse() всегда падает, поэтому обработчик никогда не делает ack, visibility timeout истекает, и сообщение переотправляется бесконечно — выжигая ёмкость consumer'а и заморяя здоровые сообщения. Починка — dead-letter queue (DLQ): настрой maxReceiveCount (например, 5), чтобы после N неудачных доставок брокер переносил сообщение в отдельную DLQ вместо переотправки. Алармь на глубину DLQ (непустой DLQ означает баг, а не икоту) и разбирай DLQ вручную для осмотра/починки/реплея. Опционально оберни parse(), чтобы ловить и сразу отправлять явно невалидный вход в DLQ, а не жечь N ретраев на входе, что никогда не пройдёт." },
        "rubric": [
          { "en": "Names the cause: a poison message that never acks loops via visibility-timeout redelivery and starves the queue.", "ru": "Называет причину: ядовитое сообщение, что никогда не делает ack, циклится через переотправку по visibility timeout и заморяет очередь." },
          { "en": "Fix routes to a DLQ after a maxReceiveCount, with an alarm on DLQ depth and manual inspection/replay.", "ru": "Починка отправляет в DLQ после maxReceiveCount, с алярмом на глубину DLQ и ручным осмотром/реплеем." }
        ]
      }
    },
    {
      "id": "design-decouple-checkout-email",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Decouple a fragile dependency", "ru": "Развяжи хрупкую зависимость" },
      "prompt": { "en": "Redesign a synchronous call into a queued one and state the new failure mode and guarantees.", "ru": "Перепроектируй синхронный вызов в очередной и назови новый режим отказа и гарантии." },
      "constraints": { "en": "Checkout currently calls a third-party receipt-email API synchronously inside the payment transaction. When that API is slow, payment threads block and checkout falls over. Email may be delivered minutes late but must never be silently dropped, and a single payment must never produce two charges (the charge stays synchronous and is out of scope here — only the email is being moved).", "ru": "Checkout сейчас синхронно дёргает стороннее API чеков-email внутри платёжной транзакции. Когда это API медленное, платёжные потоки блокируются, и checkout падает. Email может быть доставлен на минуты позже, но никогда не должен быть тихо потерян, и один платёж никогда не должен породить два списания (списание остаётся синхронным и вне рамок здесь — переносится только email)." },
      "rubric": [
        { "en": "Moves email off the synchronous path: checkout enqueues a 'send receipt' message and returns immediately, so a slow email API no longer blocks payment threads (synchronous availability dependency → asynchronous latency dependency).", "ru": "Убирает email с синхронного пути: checkout кладёт сообщение 'отправить чек' и сразу возвращается, поэтому медленное email API больше не блокирует платёжные потоки (синхронная зависимость по доступности → асинхронная по задержке)." },
          { "en": "Chooses at-least-once delivery (no loss) and makes the email worker idempotent via a stable key (order/receipt id) so a redelivery doesn't send two emails.", "ru": "Выбирает доставку at-least-once (без потерь) и делает email-воркера идемпотентным через стабильный ключ (id заказа/чека), чтобы переотправка не послала два письма." },
        { "en": "Names the new failure handling: visibility timeout sized above the email API's p99, a DLQ for repeatedly-failing sends with an alarm, and explicitly accepts 'late' as the tradeoff for 'never lost'.", "ru": "Называет новую обработку сбоев: visibility timeout выше p99 email API, DLQ для повторно падающих отправок с алярмом, и явно принимает 'поздно' как компромисс за 'никогда не потеряно'." }
      ],
      "model": { "en": "Put a durable queue between checkout and email. On a successful payment, checkout enqueues { receipt_id, order_id, to } and returns to the user immediately — payment threads no longer wait on the email API, so its slowness can't drain the pool. A separate email worker consumes the queue at-least-once: it dedups on receipt_id (skip + re-ack if already sent) so a redelivery never sends twice, sizes the visibility timeout above the email API's p99 and heartbeats long sends, and after N failures routes the message to a DLQ that's alarmed for human follow-up. The guarantee shifts from 'email is synchronous and can take checkout down' to 'email is never lost but may arrive minutes late' — an explicit, acceptable tradeoff. Note this only protects email; the payment charge remains synchronous and its own idempotency is handled separately.", "ru": "Поставь долговечную очередь между checkout и email. При успешном платеже checkout кладёт { receipt_id, order_id, to } и сразу возвращает ответ пользователю — платёжные потоки больше не ждут email API, поэтому его медлительность не вычерпает пул. Отдельный email-воркер потребляет очередь at-least-once: дедуплицирует по receipt_id (пропустить + переподтвердить, если уже отправлено), чтобы переотправка не послала дважды, размеряет visibility timeout выше p99 email API и heartbeat'ит долгие отправки, а после N сбоев отправляет сообщение в DLQ, что заалармлен для разбора человеком. Гарантия сдвигается с 'email синхронен и может уронить checkout' на 'email никогда не потерян, но может прийти на минуты позже' — явный, приемлемый компромисс. Заметь: это защищает лишь email; платёжное списание остаётся синхронным, и его идемпотентность обрабатывается отдельно." }
    }
  ]
}
