{
  "lessonKey": "system-design/08-building-blocks/05-leader-election",
  "track": "system-design",
  "tasks": [
    {
      "id": "predict-longer-lease",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Will a longer lease fix it?", "ru": "Починит ли более длинный lease?" },
      "prompt": { "en": "Reason about timeouts vs split-brain, then reveal.", "ru": "Порассуждай о таймаутах против split-brain, затем открой ответ." },
      "scenario": { "en": "After a double-charge incident caused by a leader's GC pause, an engineer proposes raising the lock lease from 10 s to 5 minutes 'so a GC pause can't outlast it.' Predict whether this eliminates split-brain, and what new problem it introduces.", "ru": "После инцидента с двойным списанием из-за паузы GC у лидера инженер предлагает поднять lease лока с 10 с до 5 минут, «чтобы пауза GC не пережила его». Предскажи, устранит ли это split-brain и какую новую проблему вводит." },
      "reveal": { "en": "It does NOT eliminate split-brain. A longer lease only makes the GC-pause case rarer; a stall longer than 5 minutes (a long stop-the-world GC, a VM migration, a swapped-out process, a network partition with no pause at all) still produces a leader acting on an expired lease. No timeout can distinguish 'dead' from 'frozen, about to wake.' Worse, the longer lease introduces a new problem: failover is now slow — when the leader genuinely dies, the cluster must wait up to 5 minutes before a standby can take over, so the singular job stalls for minutes. You've traded a rare correctness bug for a guaranteed availability hit, and you still haven't fixed split-brain. The actual fix is a fencing token enforced at the resource, which makes a stale leader's writes harmless regardless of lease length, letting you keep the lease short for fast failover.", "ru": "Это НЕ устраняет split-brain. Более длинный lease лишь делает случай паузы GC реже; затык дольше 5 минут (долгий stop-the-world GC, миграция VM, выгруженный процесс, сетевое разделение вообще без паузы) всё равно даёт лидера, действующего на истёкшем lease. Никакой таймаут не отличит «мёртв» от «заморожен, вот-вот проснётся». Хуже, длинный lease вводит новую проблему: failover теперь медленный — когда лидер по-настоящему умирает, кластер должен ждать до 5 минут, прежде чем резерв перехватит, так что единичная задача встаёт на минуты. Ты сменил редкий баг корректности на гарантированный удар по доступности и всё ещё не починил split-brain. Настоящая починка — fencing token на ресурсе, делающий записи устаревшего лидера безвредными независимо от длины lease, позволяя держать lease коротким ради быстрого failover." }
    },
    {
      "id": "diagnose-two-leaders",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Two leaders, one job", "ru": "Два лидера, одна задача" },
      "prompt": { "en": "Diagnose how two nodes both acted as leader and name the resource-side mechanism that would have prevented harm.", "ru": "Диагностируй, как два узла оба действовали как лидер, и назови механизм на стороне ресурса, что предотвратил бы вред." },
      "evidence": { "en": "setup: 3 scheduler nodes, single distributed lock, 10 s lease\nlog node-1: 02:00:01 acquired lock; 02:00:02 GC pause begins\nlog node-2: 02:00:13 lease expired, acquired lock, ran job\nlog node-1: 02:00:31 GC pause ends; 02:00:31 ran job (still thinks it holds lock)\nresult: every subscription charged twice; the resource accepted both writers", "ru": "схема: 3 узла-планировщика, один распределённый лок, lease 10 с\nлог node-1: 02:00:01 захватил лок; 02:00:02 начало паузы GC\nлог node-2: 02:00:13 lease истёк, захватил лок, запустил задачу\nлог node-1: 02:00:31 пауза GC кончилась; 02:00:31 запустил задачу (всё ещё думает, что держит лок)\nрезультат: каждая подписка списана дважды; ресурс принял обоих писателей" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "failure", "accept": ["split-brain", "split brain", "splitbrain", "two leaders", "stale leader", "zombie leader"], "hint": { "en": "Two nodes both believed they were leader at once after the pause. What is that situation called?", "ru": "Два узла оба верили, что они лидер, сразу после паузы. Как зовётся эта ситуация?" } },
          { "id": "mechanism", "accept": ["fencing token", "fencing", "fencing-token", "token", "monotonic token"], "hint": { "en": "A monotonically increasing number per grant that the resource checks, rejecting any lower one. What's it called?", "ru": "Монотонно растущее число на выдачу, что ресурс проверяет, отвергая любое меньшее. Как зовётся?" } }
        ]
      }
    },
    {
      "id": "fix-redis-lock-correctness",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Make the lock safe for correctness", "ru": "Сделай лок безопасным для корректности" },
      "prompt": { "en": "This worker uses a Redis lock as a correctness guarantee but has no protection against a stale holder. Add fencing, then check against the model.", "ru": "Этот воркер использует лок Redis как гарантию корректности, но не защищён от устаревшего держателя. Добавь fencing, затем сверься с образцом." },
      "starter": "async function runJob() {\n  const ok = await redis.set('job-lock', id, 'NX', 'PX', 10000);\n  if (!ok) return;                 // someone else holds it\n  await chargeAllSubscriptions();  // writes directly to the billing DB, no token\n  await redis.del('job-lock');\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "The flaw: a SET NX PX lock guards against two workers STARTING the job, but it cannot stop a worker whose lease expired mid-run (GC pause, stall) from completing chargeAllSubscriptions() after a new holder already ran it — the billing DB accepts writes from both. A plain lock is fine for efficiency (a duplicate is wasteful) but unsafe for correctness (a duplicate is a double-charge). Fix: get a monotonically increasing fencing token with the lock grant (e.g. from etcd/ZooKeeper, or Redis INCR of a generation counter under the lock), pass that token into every write, and have the billing resource reject any write whose token is lower than the highest it has seen. Shape: const {acquired, token} = await acquireWithToken('job-lock'); if (!acquired) return; await chargeAllSubscriptions(token); — and inside, each charge does `WHERE token >= last_seen_token` / stores max(token), so a woken-up worker with an old token is fenced. Also prefer a lock service whose tokens are linearizable (etcd/ZooKeeper) over a single-node Redis lock when correctness is at stake. The lease can stay short for fast failover; correctness now comes from the resource rejecting stale tokens, not from the timeout.", "ru": "Дефект: лок SET NX PX защищает от того, чтобы два воркера НАЧАЛИ задачу, но не может остановить воркера, чей lease истёк посреди прогона (пауза GC, затык), от завершения chargeAllSubscriptions() после того, как новый держатель уже её прогнал — биллинговая БД принимает записи от обоих. Голый лок в порядке для эффективности (дубликат расточителен), но небезопасен для корректности (дубликат — двойное списание). Починка: получи монотонно растущий fencing token с выдачей лока (например, из etcd/ZooKeeper или Redis INCR счётчика поколения под локом), передавай этот token в каждую запись, и пусть биллинговый ресурс отвергает любую запись, чей token ниже наибольшего виденного. Вид: const {acquired, token} = await acquireWithToken('job-lock'); if (!acquired) return; await chargeAllSubscriptions(token); — и внутри каждое списание делает `WHERE token >= last_seen_token` / хранит max(token), так что проснувшийся воркер со старым token отгорожен. Также предпочти сервис локов, чьи token'ы линеаризуемы (etcd/ZooKeeper), одноузловому локу Redis, когда на кону корректность. Lease может остаться коротким ради быстрого failover; корректность теперь идёт из отвержения ресурсом устаревших token'ов, а не из таймаута." },
        "rubric": [
          { "en": "Recognizes a plain Redis lock guards efficiency, not correctness, and can't stop a stale holder mid-run.", "ru": "Понимает, что голый лок Redis защищает эффективность, не корректность, и не остановит устаревшего держателя посреди прогона." },
          { "en": "Adds a monotonically increasing fencing token passed into every write, with the billing resource rejecting any lower token.", "ru": "Добавляет монотонно растущий fencing token, передаваемый в каждую запись, при отвержении биллинговым ресурсом любого меньшего token." }
        ]
      }
    },
    {
      "id": "design-singleton-scheduler",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Design a single-leader scheduler", "ru": "Спроектируй планировщик с одним лидером" },
      "prompt": { "en": "Design leader election for the scheduler below and justify the election mechanism, failover behaviour, and how you guarantee exactly-one-execution.", "ru": "Спроектируй выбор лидера для планировщика ниже и обоснуй механизм выборов, поведение failover и как гарантируешь ровно-одно-исполнение." },
      "constraints": { "en": "A fleet of 5 scheduler instances must ensure each cron job fires exactly once per schedule, even across instance crashes, GC pauses, and brief network partitions. Jobs have real side effects (sending emails, charging cards) where a duplicate is harmful. Failover should be fast (seconds, not minutes). You can use etcd, ZooKeeper, or Redis.", "ru": "Флот из 5 инстансов-планировщиков должен гарантировать, что каждая cron-задача запускается ровно раз за расписание, даже при крахах инстансов, паузах GC и кратких сетевых разделениях. У задач реальные сайд-эффекты (отправка писем, списание с карт), где дубликат вреден. Failover должен быть быстрым (секунды, не минуты). Можешь использовать etcd, ZooKeeper или Redis." },
      "rubric": [
        { "en": "Elects a single leader via a coordination service that runs consensus (etcd/ZooKeeper) using a renewable, time-bounded lease, with standbys ready to acquire on expiry.", "ru": "Избирает одного лидера через сервис координации, гоняющий консенсус (etcd/ZooKeeper), используя продлеваемый, ограниченный по времени lease, с резервами в готовности захватить при истечении." },
        { "en": "Keeps the lease short for fast failover and explicitly does NOT rely on timeout length for correctness, acknowledging split-brain can't be timed away.", "ru": "Держит lease коротким ради быстрого failover и явно НЕ полагается на длину таймаута для корректности, признавая, что split-brain нельзя устранить таймаутом." },
        { "en": "Guarantees exactly-once via fencing tokens (or an idempotency key per job run) enforced at the side-effecting resource, so a stale leader's duplicate execution is rejected.", "ru": "Гарантирует ровно-один через fencing token'ы (или ключ идемпотентности на прогон задачи), применяемые на ресурсе с сайд-эффектом, так что дублирующее исполнение устаревшего лидера отвергается." }
      ],
      "model": { "en": "Election: use a coordination service that runs consensus — etcd or ZooKeeper — and have the 5 instances contend for a single renewable lease (the leader holds the lease and renews via heartbeat; standbys watch and acquire the moment it expires). Avoid a single-node Redis lock as the sole authority here because correctness is at stake and its lease can expire under a pause without any fencing. Keep the lease short (e.g. a few seconds) so failover is fast — a crashed leader's lease expires quickly and a standby takes over in seconds. Critically, do NOT rely on the lease length for correctness: a GC pause or partition can always make a stale leader act, and no timeout fixes that. Guarantee exactly-once at the side-effecting resource: each leadership grant carries a monotonically increasing fencing token, every job execution (email send, card charge) is tagged with that token plus a per-(job, schedule-tick) idempotency key, and the resource rejects a token lower than the highest seen and dedupes on the idempotency key. So even if a paused old leader wakes and re-runs a job, its stale token is fenced and/or the idempotency key makes the duplicate a no-op. Net: consensus picks one leader, a short lease gives fast failover, and fencing tokens + idempotency keys make a stale leader's duplicate execution harmless — correctness lives at the resource, not in the timeout.", "ru": "Выборы: используй сервис координации, гоняющий консенсус — etcd или ZooKeeper — и пусть 5 инстансов борются за один продлеваемый lease (лидер держит lease и продлевает heartbeat'ом; резервы наблюдают и захватывают в момент истечения). Избегай одноузлового лока Redis как единственного авторитета здесь, потому что на кону корректность, а его lease может истечь под паузой без всякого fencing. Держи lease коротким (например, несколько секунд), чтобы failover был быстрым — lease упавшего лидера истекает быстро, и резерв перехватывает за секунды. Критично, НЕ полагайся на длину lease для корректности: пауза GC или разделение всегда могут заставить устаревшего лидера действовать, и никакой таймаут это не чинит. Гарантируй ровно-один на ресурсе с сайд-эффектом: каждая выдача лидерства несёт монотонно растущий fencing token, каждое исполнение задачи (отправка письма, списание с карты) помечается этим token плюс ключом идемпотентности на (задача, тик расписания), и ресурс отвергает token ниже наибольшего виденного и дедупит по ключу идемпотентности. Так что даже если приостановленный старый лидер просыпается и перезапускает задачу, его устаревший token отгорожен и/или ключ идемпотентности делает дубликат no-op. Итог: консенсус выбирает одного лидера, короткий lease даёт быстрый failover, а fencing token'ы + ключи идемпотентности делают дублирующее исполнение устаревшего лидера безвредным — корректность живёт на ресурсе, а не в таймауте." }
    }
  ]
}
