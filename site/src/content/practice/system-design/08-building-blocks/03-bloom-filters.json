{
  "lessonKey": "system-design/08-building-blocks/03-bloom-filters",
  "track": "system-design",
  "tasks": [
    {
      "id": "predict-saturation",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Insert past the budget", "ru": "Вставка сверх бюджета" },
      "prompt": { "en": "Predict what happens as insertions exceed the sized capacity, then reveal.", "ru": "Предскажи, что произойдёт, когда вставки превысят рассчитанную ёмкость, затем открой ответ." },
      "scenario": { "en": "A bloom filter is sized with m bits for n = 1,000,000 elements at a target ~1% false-positive rate. The service grows and quietly keeps adding elements until ~5,000,000 have been inserted into the same array, with no resize. What happens to the false-positive rate, the false-negative rate, and the filter's usefulness?", "ru": "Фильтр Блума размерен с m битами под n = 1 000 000 элементов при целевой доле ложноположительных ~1%. Сервис растёт и тихо продолжает добавлять элементы, пока в тот же массив не вставлено ~5 000 000, без увеличения. Что станет с долей ложноположительных, долей ложноотрицательных и полезностью фильтра?" },
      "reveal": { "en": "False positives climb steeply toward 100% as the array saturates — with 5× the planned elements, far more bits are set to 1, so an unrelated element's k positions are very likely all already 1, and the filter starts answering 'maybe' to almost everything. At that point it has no value: every query falls through to the expensive store anyway. The false-NEGATIVE rate stays exactly 0 the whole time — that guarantee is structural (adding only sets bits) and is never affected by saturation. The fix is not to keep adding: a plain bloom filter can't be resized, so you rebuild a larger one sized for the true n (or use a scalable bloom filter that chains progressively larger filters).", "ru": "Ложноположительные круто лезут к 100% по мере насыщения массива — при 5× плановых элементов установлено куда больше бит в 1, поэтому k позиций несвязанного элемента очень вероятно уже все в 1, и фильтр начинает отвечать «возможно» почти на всё. В этой точке у него нет ценности: каждый запрос всё равно проваливается к дорогому хранилищу. Доля ложноОТРИЦАТЕЛЬНЫХ всё время остаётся ровно 0 — эта гарантия структурна (добавление лишь устанавливает биты) и никогда не затрагивается насыщением. Починка — не продолжать добавлять: простой фильтр Блума нельзя увеличить, поэтому перестрой больший, размеренный под истинное n (или используй scalable bloom filter, сцепляющий всё бо́льшие фильтры)." }
    },
    {
      "id": "diagnose-deletion-corruption",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Deletions break lookups", "ru": "Удаления ломают поиски" },
      "prompt": { "en": "Diagnose the corruption and name the structure that supports deletion.", "ru": "Диагностируй порчу и назови структуру, поддерживающую удаление." },
      "evidence": { "en": "use: a bloom filter tracks 'active session IDs'\nremove(sessionId) clears that ID's k bits in the array\nbug report: after some sessions are removed, contains(stillActiveId) sometimes returns FALSE for sessions that are definitely still active\nimpact: live users get logged out", "ru": "использование: фильтр Блума отслеживает «активные ID сессий»\nremove(sessionId) сбрасывает k бит этого ID в массиве\nбаг-репорт: после удаления некоторых сессий contains(stillActiveId) иногда возвращает FALSE для сессий, что точно ещё активны\nэффект: живых пользователей разлогинивает" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "cause", "accept": ["shared bits", "shared bit", "bits are shared", "clearing shared bits", "bit sharing", "false negative", "false negatives", "shared positions"], "hint": { "en": "When you clear a removed ID's bits, what happens to a bit that another still-active ID also relies on?", "ru": "Когда сбрасываешь биты удалённого ID, что станет с битом, на который опирается и другой ещё активный ID?" } },
          { "id": "structure", "accept": ["counting bloom filter", "counting bloom", "counting filter", "cuckoo filter", "cuckoo", "counting"], "hint": { "en": "Replace each bit with a small counter (increment on add, decrement on remove) — what's that variant called?", "ru": "Замени каждый бит маленьким счётчиком (инкремент при добавлении, декремент при удалении) — как зовётся этот вариант?" } }
        ]
      }
    },
    {
      "id": "fix-bloom-as-authority",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Bloom as the sole authority", "ru": "Bloom как единственный авторитет" },
      "prompt": { "en": "This coupon check uses a bloom filter as the only source of truth and wrongly rejects some first-time redemptions. Explain the flaw and fix it, then check against the model.", "ru": "Эта проверка купона использует фильтр Блума как единственный источник истины и ошибочно отвергает некоторые первые погашения. Объясни дефект и почини, затем сверься с образцом." },
      "starter": "// reject if the coupon was already used\nfunction redeem(couponId) {\n  if (usedFilter.contains(couponId)) {\n    return reject('already used');   // trusts the filter alone\n  }\n  usedFilter.add(couponId);\n  return grantDiscount();\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "The flaw: the code treats the bloom filter as ground truth for 'already used,' but contains() can return a FALSE POSITIVE — a coupon never redeemed whose k bits were all set by other coupons. That falsely takes the reject path, so a legitimate first-time redemption is wrongly denied. A bloom filter must only be used where a false positive is cheap (triggers a fallback), never as the sole authority for a harmful 'yes.' Fix: on a positive, confirm against ground truth (the database) before rejecting; on a negative, you can trust 'definitely absent' and fast-path. Shape: if (usedFilter.contains(id)) { if (await db.wasRedeemed(id)) return reject('already used'); /* else false positive: continue */ } then atomically record redemption in the DB (the DB's unique constraint is the real guard against double-spend) and add to the filter. The filter becomes a fast pre-check that skips the DB read for the common never-seen case, while the database remains the authority.", "ru": "Дефект: код считает фильтр Блума истиной для «уже использован», но contains() может вернуть ЛОЖНОПОЛОЖИТЕЛЬНЫЙ — купон, никогда не погашенный, чьи k бит установлены другими купонами. Это ложно уводит на путь reject, и легитимное первое погашение ошибочно отклоняется. Фильтр Блума можно использовать лишь там, где ложноположительный дёшев (запускает fallback), никогда как единственный авторитет для вредного «да». Починка: на положительном сверься с истиной (базой) перед отказом; на отрицательном можешь доверять «точно отсутствует» и идти быстрым путём. Вид: if (usedFilter.contains(id)) { if (await db.wasRedeemed(id)) return reject('уже использован'); /* иначе ложноположительный: продолжить */ } затем атомарно запиши погашение в БД (уникальное ограничение БД — настоящий страж от двойной траты) и добавь в фильтр. Фильтр становится быстрой предпроверкой, пропускающей чтение БД в частом случае «не видели», а база остаётся авторитетом." },
        "rubric": [
          { "en": "Identifies the false positive: the filter wrongly reports an unredeemed coupon as 'used,' so trusting it alone denies a valid redemption.", "ru": "Опознаёт ложноположительный: фильтр ошибочно рапортует непогашенный купон как «использованный», поэтому доверие лишь ему отклоняет валидное погашение." },
          { "en": "Fix confirms every positive against ground truth (the DB) before rejecting, keeping the filter as a fast pre-check, not the authority.", "ru": "Починка сверяет каждый положительный с истиной (БД) перед отказом, оставляя фильтр быстрой предпроверкой, не авторитетом." }
        ]
      }
    },
    {
      "id": "design-cache-penetration-guard",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 7,
      "title": { "en": "Guard against cache penetration", "ru": "Защита от пробивания кэша" },
      "prompt": { "en": "Design a bloom-filter guard for the read path below and justify the structure, sizing, and what stays authoritative.", "ru": "Спроектируй страж на фильтре Блума для пути чтения ниже и обоснуй структуру, размер и то, что остаётся авторитетным." },
      "constraints": { "en": "A product API reads by product ID: check a Redis cache, and on a miss read the primary database. Attackers are flooding requests for random non-existent product IDs — every one misses the cache AND misses the DB, so the DB takes the full flood (cache penetration). There are ~50M real product IDs; the set changes slowly (new products added in batches).", "ru": "API товаров читает по ID товара: проверь кэш Redis, при промахе читай первичную базу. Атакующие заваливают запросами случайных несуществующих ID товаров — каждый промахивается мимо кэша И мимо базы, так что база ловит весь потоп (пробивание кэша). Реальных ID товаров ~50M; множество меняется медленно (новые товары добавляются батчами)." },
      "rubric": [
        { "en": "Places a bloom filter of all valid product IDs before the cache/DB, so a request for an ID 'definitely absent' from the filter is rejected immediately without touching the DB.", "ru": "Ставит фильтр Блума со всеми валидными ID товаров перед кэшем/БД, так что запрос ID, «точно отсутствующего» в фильтре, отвергается немедленно, не трогая БД." },
        { "en": "Sizes the filter for ~50M elements at a chosen false-positive rate (e.g. ~10 bits/element ≈ 60 MB for ~1%), and explains a false positive just falls through to the normal cache/DB path (harmless).", "ru": "Размеряет фильтр под ~50M элементов при выбранной доле ложноположительных (например, ~10 бит/элемент ≈ 60 МБ для ~1%) и объясняет, что ложноположительный лишь проваливается на обычный путь кэш/БД (безвредно)." },
        { "en": "Keeps the DB authoritative (the filter never confirms existence, only rules out), and describes refreshing/rebuilding the filter as products are added in batches.", "ru": "Оставляет БД авторитетом (фильтр никогда не подтверждает существование, только исключает) и описывает обновление/перестройку фильтра по мере добавления товаров батчами." }
      ],
      "model": { "en": "Put a bloom filter of all ~50M valid product IDs in front of the cache and DB. On a read: query the filter first. 'Definitely absent' → return 404 immediately, never touching cache or DB — this kills the attack, since random non-existent IDs are almost all ruled out for free. 'Possibly present' → proceed to the normal cache→DB path. A false positive (a non-existent ID the filter says 'maybe' to) just does one normal cache/DB read that returns empty — harmless, and its rate is whatever you sized for (e.g. ~1% at ~10 bits/element ≈ ~60 MB for 50M IDs; raise bits/element to lower it). The database stays authoritative: the filter only ever RULES OUT, it never confirms a product exists. Because the ID set changes slowly in batches, rebuild or incrementally update the filter when products are added (and remember a plain bloom filter can't delete — for removals, rebuild on a schedule or use a counting filter). Net: the flood of certain-misses is absorbed by a ~60 MB structure instead of the DB.", "ru": "Поставь фильтр Блума со всеми ~50M валидными ID товаров перед кэшем и БД. На чтении: сперва запрос к фильтру. «Точно отсутствует» → верни 404 немедленно, не трогая кэш или БД — это убивает атаку, ведь случайные несуществующие ID почти все исключаются бесплатно. «Возможно есть» → иди обычным путём кэш→БД. Ложноположительный (несуществующий ID, которому фильтр говорит «возможно») лишь делает одно обычное чтение кэш/БД, возвращающее пусто — безвредно, и его доля — та, под которую размерил (например, ~1% при ~10 бит/элемент ≈ ~60 МБ для 50M ID; подними бит/элемент, чтобы снизить). База остаётся авторитетом: фильтр только ИСКЛЮЧАЕТ, он никогда не подтверждает существование товара. Поскольку множество ID меняется медленно батчами, перестраивай или инкрементально обновляй фильтр при добавлении товаров (и помни, простой фильтр Блума не удаляет — для удалений перестраивай по расписанию или используй counting filter). Итог: потоп точных-промахов поглощается структурой ~60 МБ вместо БД." }
    }
  ]
}
