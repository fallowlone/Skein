{
  "lessonKey": "system-design/01-scalability/01-latency-vs-throughput",
  "track": "system-design",
  "tasks": [
    {
      "id": "predict-littles-law-ceiling",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Find the throughput ceiling", "ru": "Найди потолок пропускной способности" },
      "prompt": { "en": "Apply Little's Law, then reveal the answer.", "ru": "Примени закон Литтла, затем открой ответ." },
      "scenario": { "en": "A payment service has a worker pool that allows at most 80 in-flight requests. Each request takes on average 120 ms end to end. Marketing forecasts a peak of 1,000 RPS. Will the service hold the peak on its current pool, and what is its actual ceiling?", "ru": "У платёжного сервиса пул воркеров, допускающий максимум 80 запросов в полёте. Каждый запрос в среднем занимает 120 мс от начала до конца. Маркетинг прогнозирует пик 1 000 RPS. Удержит ли сервис пик на текущем пуле, и каков его реальный потолок?" },
      "reveal": { "en": "Ceiling λ = L / W = 80 / 0.120 s ≈ 667 RPS. The forecast peak of 1,000 RPS is 1.5× over capacity — the pool saturates, requests queue, and latency climbs the 1/(μ−λ) curve until timeouts cascade. To hold 1,000 RPS you need either L ≥ 120 (more workers/connections) at the same 120 ms, or W cut to ≤ 80 ms at the same pool. Compute the ceiling before the launch, not during the incident.", "ru": "Потолок λ = L / W = 80 / 0,120 с ≈ 667 RPS. Прогнозный пик 1 000 RPS — это 1,5× сверх ёмкости: пул насыщается, запросы встают в очередь, и задержка лезет по кривой 1/(μ−λ), пока таймауты не пойдут каскадом. Чтобы держать 1 000 RPS, нужно либо L ≥ 120 (больше воркеров/соединений) при тех же 120 мс, либо W урезать до ≤ 80 мс при том же пуле. Считай потолок до запуска, а не во время инцидента." }
    },
    {
      "id": "diagnose-tail-under-load",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "The flat mean, the rising tail", "ru": "Ровное среднее, растущий хвост" },
      "prompt": { "en": "A dashboard shows the symptom below. Fill in the mechanism and the right metric to watch.", "ru": "Дашборд показывает симптом ниже. Впиши механизм и правильную метрику для наблюдения." },
      "evidence": { "en": "mean latency: 45 ms → 47 ms (flat)\np99 latency: 200 ms → 1,900 ms\nCPU utilization: 58% → 91%\nthroughput: still climbing, no errors yet", "ru": "среднее: 45 мс → 47 мс (ровно)\np99: 200 мс → 1 900 мс\nутилизация CPU: 58% → 91%\nпропускная способность: ещё растёт, ошибок пока нет" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "mechanism", "accept": ["queueing", "queuing", "queueing delay", "queue", "queueing theory", "m/m/1"], "hint": { "en": "What grows as 1/(μ−λ) as utilization approaches the service rate?", "ru": "Что растёт как 1/(μ−λ) при приближении утилизации к темпу обслуживания?" } },
          { "id": "metric", "accept": ["p99", "percentiles", "tail latency", "p999", "tail", "percentile"], "hint": { "en": "Not the mean — the statistic that exposes the slow tail users actually feel.", "ru": "Не среднее — статистика, что вскрывает медленный хвост, который чувствуют пользователи." } }
        ]
      }
    },
    {
      "id": "design-capacity-headroom",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Size a pool with headroom", "ru": "Рассчитай пул с запасом" },
      "prompt": { "en": "Decide the connection-pool size and target utilization for the service below, and justify with Little's Law and the queueing curve.", "ru": "Определи размер пула соединений и целевую утилизацию для сервиса ниже, обоснуй законом Литтла и кривой очередей." },
      "constraints": { "en": "Service must sustain 2,000 RPS at p99 ≤ 250 ms. Measured unloaded latency is 60 ms. You control pool size and number of instances; each instance handles part of the load behind a load balancer.", "ru": "Сервис должен держать 2 000 RPS при p99 ≤ 250 мс. Измеренная задержка без нагрузки — 60 мс. Ты управляешь размером пула и числом инстансов; каждый инстанс обрабатывает часть нагрузки за балансировщиком." },
      "rubric": [
        { "en": "Computes minimum concurrency from Little's Law: L = λW = 2000 × 0.06 = 120 in-flight at the unloaded latency.", "ru": "Считает минимальную конкурентность по закону Литтла: L = λW = 2000 × 0,06 = 120 в полёте при задержке без нагрузки." },
        { "en": "Adds headroom rather than provisioning for exactly 120 — targets ~60–70% utilization so the queueing curve stays off its knee and p99 ≤ 250 ms holds under bursts.", "ru": "Добавляет запас, а не провижинит ровно 120 — целится в ~60–70% утилизации, чтобы кривая очередей не вышла на колено и p99 ≤ 250 мс держался при всплесках." },
        { "en": "Spreads the concurrency across multiple instances (and notes that one slow instance's p99 becomes the system tail under fan-out / retries).", "ru": "Распределяет конкурентность по нескольким инстансам (и отмечает, что p99 одного медленного инстанса становится хвостом системы при fan-out / ретраях)." }
      ],
      "model": { "en": "Little's Law gives L = 2000 × 0.06 = 120 concurrent requests at unloaded latency. Provisioning exactly 120 runs at ~100% utilization, where W = 1/(μ−λ) explodes and p99 blows past 250 ms. Target 60–70%: provision for ~180–200 concurrency (e.g., 4 instances × pool 50), keeping each instance near 65% so queueing delay stays small. Add per-call timeouts below 250 ms and bounded retries so a single slow instance can't drag the tail. Re-measure under open-loop load to avoid coordinated omission masking the real p99.", "ru": "Закон Литтла даёт L = 2000 × 0,06 = 120 одновременных запросов при задержке без нагрузки. Провижининг ровно 120 даёт ~100% утилизации, где W = 1/(μ−λ) взрывается и p99 пробивает 250 мс. Целься в 60–70%: провижинь ~180–200 конкурентности (например, 4 инстанса × пул 50), держа каждый около 65%, чтобы задержка очереди оставалась малой. Добавь таймауты вызова ниже 250 мс и ограниченные ретраи, чтобы один медленный инстанс не утянул хвост. Перемерь под open-loop нагрузкой, чтобы coordinated omission не маскировал реальный p99." }
    },
    {
      "id": "fix-coordinated-omission",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Fix the lying benchmark", "ru": "Почини лгущий бенчмарк" },
      "prompt": { "en": "A load test reports p99 = 35 ms, but production p99 is 900 ms at the same throughput. Identify the measurement flaw and how to fix the harness, then check against the model answer.", "ru": "Нагрузочный тест показывает p99 = 35 мс, но в проде p99 = 900 мс при той же пропускной способности. Назови дефект измерения и как починить харнесс, затем сверься с образцом." },
      "starter": "// closed-loop load generator\nwhile (running) {\n  const t0 = now();\n  await sendRequest();      // blocks until response\n  record(now() - t0);\n  // next request only starts AFTER this one returns\n}",
      "grading": {
        "mode": "self",
        "model": { "en": "This is coordinated omission: the generator only sends the next request after the previous returns, so during a server stall it simply stops sending — the requests that *would* have arrived and waited are never recorded, hiding the tail. Fix by going open-loop: send requests on a fixed schedule (constant arrival rate) regardless of when responses come back, and measure each request's latency from its intended send time, not from when the generator got around to it. Tools like wrk2 and Gatling's constant-rate injection do this. After the fix, the benchmark p99 should track production.", "ru": "Это coordinated omission: генератор шлёт следующий запрос только после возврата предыдущего, поэтому при затыке сервера он просто перестаёт слать — запросы, которые *должны* были прийти и ждать, не записываются, и хвост скрыт. Чинится переходом в open-loop: слать запросы по фиксированному расписанию (постоянный темп прибытия) независимо от того, когда приходят ответы, и мерить задержку каждого запроса от запланированного времени отправки, а не от момента, когда генератор до него дошёл. Так делают wrk2 и constant-rate инъекция в Gatling. После починки p99 бенчмарка должен совпасть с продом." },
        "rubric": [
          { "en": "Names the flaw as coordinated omission (closed-loop generator stops sending during stalls).", "ru": "Называет дефект как coordinated omission (closed-loop генератор перестаёт слать во время затыков)." },
          { "en": "Fix moves to open-loop constant-rate sending and measures latency from intended send time.", "ru": "Починка переходит на open-loop отправку с постоянным темпом и мерит задержку от запланированного времени отправки." }
        ]
      }
    }
  ]
}
