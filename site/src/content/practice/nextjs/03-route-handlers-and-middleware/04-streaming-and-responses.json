{
  "lessonKey": "nextjs/03-route-handlers-and-middleware/04-streaming-and-responses",
  "track": "nextjs",
  "tasks": [
    {
      "id": "04-streaming-predict-buffering-hops",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: where the stream survives and where it batches",
        "ru": "Предскажи: где стрим выживает, а где превращается в батч"
      },
      "prompt": {
        "en": "For each of the three test points, predict whether tokens arrive progressively or all at once, and name the responsible mechanism before reading the reveal.",
        "ru": "Для каждой из трёх точек теста предскажите, приходят ли токены постепенно или разом, и назовите ответственный механизм до просмотра разбора."
      },
      "scenario": {
        "en": "An SSE chat handler returns a ReadableStream with content-type: text/event-stream but NO other headers. Infrastructure: app pod → nginx with default config (proxy_buffering on, gzip on for text/*) → users. Test 1: curl -N http://pod:3000/api/chat directly against the pod. Test 2: curl -N through nginx as deployed. Test 3: through nginx after the handler adds X-Accel-Buffering: no but gzip still compresses text/event-stream. Predict: progressive or batched at each point?",
        "ru": "SSE-чат-хендлер возвращает ReadableStream с content-type: text/event-stream, но БЕЗ других заголовков. Инфраструктура: под приложения → nginx с дефолтным конфигом (proxy_buffering on, gzip on для text/*) → пользователи. Тест 1: curl -N http://pod:3000/api/chat прямо в под. Тест 2: curl -N через nginx как задеплоено. Тест 3: через nginx после того, как хендлер добавил X-Accel-Buffering: no, но gzip всё ещё сжимает text/event-stream. Предскажите: постепенно или батчем в каждой точке?"
      },
      "reveal": {
        "en": "Test 1: progressive — the handler and the Next server flush chunks as enqueued; direct curl -N proves the application code is correct, which is exactly why this is the first diagnostic step. Test 2: batched — nginx proxy_buffering collects the upstream response before forwarding; the user sees a long pause then the full answer, with zero errors anywhere. Test 3: still batched (or arriving in coarse lumps) — X-Accel-Buffering: no disables proxy buffering, but the gzip module buffers input to build compression frames, so the stream stalls inside the compressor; SSE must be exempted from compression (gzip off for text/event-stream, or gzip_proxied/no-transform discipline), after which tokens flow. The general method: walk outward hop by hop with curl -N; the first hop where progressive becomes batched is the culprit, and each hop needs its own remedy — headers can switch off some buffers (X-Accel-Buffering) but configuration must fix the others (compression exemptions).",
        "ru": "Тест 1: постепенно — хендлер и сервер Next сбрасывают чанки по мере enqueue; прямой curl -N доказывает корректность кода приложения — именно поэтому это первый шаг диагностики. Тест 2: батчем — nginx proxy_buffering собирает upstream-ответ перед пересылкой; пользователь видит долгую паузу и затем весь ответ, при нуле ошибок где-либо. Тест 3: всё ещё батчем (или грубыми комками) — X-Accel-Buffering: no выключает буферизацию прокси, но gzip-модуль буферизует вход для сборки фреймов сжатия, и стрим застревает внутри компрессора; SSE нужно исключить из сжатия (gzip off для text/event-stream или дисциплина gzip_proxied/no-transform), после чего токены текут. Общий метод: идти наружу хоп за хопом с curl -N; первый хоп, где постепенное становится батчем, — виновник, и каждому хопу нужно своё лекарство — заголовки выключают часть буферов (X-Accel-Buffering), но остальные чинятся конфигурацией (исключения сжатия)."
      }
    },
    {
      "id": "04-streaming-diagnose-orphan-generations",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose: the LLM bill for answers nobody read",
        "ru": "Диагностика: счёт за LLM-ответы, которых никто не читал"
      },
      "prompt": {
        "en": "Trace the cost leak through the stream lifecycle and name the missing pieces. Fill the blanks.",
        "ru": "Проследите утечку денег через жизненный цикл стрима и назовите недостающие части. Заполните пропуски."
      },
      "evidence": {
        "en": "Metrics: average chat session shows the user disconnecting after ~20% of tokens are delivered, but the LLM provider bills full-length generations for ~95% of requests. The handler builds a ReadableStream with only pull() defined and passes no abort signal to the provider SDK. Memory per active stream also grows when clients are on slow connections. Blank 1: when the client disconnects, the runtime cancels the response stream and invokes the source's ___ callback — which this handler never defined. Blank 2: inside it, the handler must propagate termination upstream, e.g. via an ___ passed to the provider SDK (or calling the iterator's return). Blank 3: the slow-client memory growth means chunks are being enqueued faster than the consumer drains — the stream's ___ signal (desiredSize / pull pacing) is being ignored somewhere.",
        "ru": "Метрики: средняя чат-сессия показывает отключение пользователя после доставки ~20% токенов, но LLM-провайдер биллит генерации полной длины для ~95% запросов. Хендлер строит ReadableStream только с pull() и не передаёт abort-сигнал в SDK провайдера. Память на активный стрим также растёт, когда клиенты на медленных соединениях. Пропуск 1: при отключении клиента рантайм отменяет поток ответа и вызывает колбэк источника ___, который этот хендлер не определил. Пропуск 2: внутри него хендлер обязан пропагировать завершение вверх по потоку, например через ___, переданный в SDK провайдера (или вызвав return итератора). Пропуск 3: рост памяти на медленных клиентах означает, что чанки enqueue-ятся быстрее, чем потребитель вычитывает, — где-то игнорируется сигнал ___ стрима (desiredSize / темп pull)."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "cancel",
              "cancel()",
              "отмены",
              "cancel-колбэк",
              "колбэк cancel"
            ],
            "hint": {
              "en": "The ReadableStream source callback that fires when the consumer goes away mid-stream.",
              "ru": "Колбэк источника ReadableStream, срабатывающий, когда потребитель исчезает посреди стрима."
            }
          },
          {
            "id": "b2",
            "accept": [
              "abortsignal",
              "abort signal",
              "AbortController",
              "abort controller",
              "signal",
              "абортсигнал",
              "сигнал прерывания",
              "AbortSignal"
            ],
            "hint": {
              "en": "The standard web primitive for propagating cancellation into fetch and SDK calls.",
              "ru": "Стандартный веб-примитив для пропагации отмены в fetch и вызовы SDK."
            }
          },
          {
            "id": "b3",
            "accept": [
              "backpressure",
              "back pressure",
              "back-pressure",
              "бэкпрешер",
              "противодавления",
              "обратного давления"
            ],
            "hint": {
              "en": "The pull-based mechanism that makes the producer match the consumer's pace instead of queueing ahead.",
              "ru": "Pull-механизм, заставляющий продюсера совпадать с темпом потребителя вместо набегания очереди."
            }
          }
        ]
      }
    },
    {
      "id": "04-streaming-design-llm-gateway-route",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: a production SSE gateway route for an AI assistant",
        "ru": "Спроектируй: продакшен SSE-гейтвей-маршрут для AI-ассистента"
      },
      "prompt": {
        "en": "Design the /api/assistant route handler end to end: framing, headers, cancellation, backpressure, runtime choice, and the infrastructure checklist that keeps the stream streaming in production.",
        "ru": "Спроектируйте route handler /api/assistant целиком: фрейминг, заголовки, отмена, backpressure, выбор runtime и инфраструктурный чек-лист, сохраняющий стрим стримом в продакшене."
      },
      "constraints": {
        "en": "Requirements: token streaming to browsers with time-to-first-token under 500ms; payloads may contain newlines and markdown; users frequently abandon mid-answer (must not pay for abandoned generations); deployment is self-hosted Next.js behind nginx with gzip enabled site-wide; the route only calls the LLM provider over HTTPS (no database); mobile clients on slow connections must not inflate server memory.",
        "ru": "Требования: стриминг токенов в браузеры с временем до первого токена меньше 500 мс; payload-ы могут содержать переводы строк и markdown; пользователи часто бросают ответ на середине (нельзя платить за брошенные генерации); деплой — self-hosted Next.js за nginx со включённым на весь сайт gzip; маршрут зовёт только LLM-провайдера по HTTPS (без базы данных); мобильные клиенты на медленных соединениях не должны раздувать память сервера."
      },
      "rubric": [
        {
          "en": "Frames messages as SSE with JSON-encoded payloads (data: {json}\\n\\n) so embedded newlines and markdown cannot corrupt the parser, and sets the full header armor: content-type text/event-stream, cache-control no-cache + no-transform, X-Accel-Buffering: no.",
          "ru": "Оформляет сообщения как SSE с JSON-кодированными payload-ами (data: {json}\\n\\n), чтобы вложенные переводы строк и markdown не портили парсер, и ставит полную заголовочную броню: content-type text/event-stream, cache-control no-cache + no-transform, X-Accel-Buffering: no."
        },
        {
          "en": "Wires cancellation end to end: an AbortController whose signal goes into the provider SDK call, triggered from the stream's cancel() callback on client disconnect — abandoned generations stop within a token or two.",
          "ru": "Прошивает отмену насквозь: AbortController, чей signal уходит в вызов SDK провайдера, срабатывает из колбэка cancel() стрима при отключении клиента — брошенные генерации останавливаются в пределах одного-двух токенов."
        },
        {
          "en": "Uses pull-based production (one upstream token per pull, paced by desiredSize) or pipeThrough composition so slow mobile consumers throttle the producer instead of growing the server-side queue.",
          "ru": "Использует производство по pull (один upstream-токен на pull, темп от desiredSize) или композицию через pipeThrough, чтобы медленные мобильные потребители тормозили продюсера, а не растили серверную очередь."
        },
        {
          "en": "Chooses the runtime with the data-gravity logic: no DB, streaming-native, long wall-clock waits — an edge candidate; on self-hosted Node it notes the equivalent requirement (flushing server, no response buffering in the Node layer).",
          "ru": "Выбирает runtime логикой гравитации данных: без БД, нативный стриминг, долгие ожидания по настенным часам — кандидат на edge; на self-hosted Node отмечает эквивалентное требование (flush-ащий сервер, без буферизации ответа в Node-слое)."
        },
        {
          "en": "Includes the infrastructure checklist: exempt text/event-stream from nginx gzip, verify proxy_buffering is defeated (header or config), and the hop-by-hop curl -N verification as a deploy gate.",
          "ru": "Включает инфраструктурный чек-лист: исключить text/event-stream из gzip в nginx, убедиться, что proxy_buffering побеждён (заголовком или конфигом), и похоповую проверку curl -N как деплой-гейт."
        }
      ],
      "model": {
        "en": "Handler shape: POST /api/assistant reads the prompt, creates an AbortController, and calls the provider SDK with { signal: controller.signal }. The response is a ReadableStream whose pull() awaits exactly one upstream token and enqueues one SSE frame — data: JSON.stringify({ delta }) + blank line — so newlines and markdown inside tokens are inert; on upstream done it emits data: [DONE] and closes. cancel() calls controller.abort(): when a user closes the tab at 20% of the answer, the provider stops generating within a token, which is the difference between billing 20% and 100% of every abandoned answer. Backpressure comes free from the pull model: the runtime only pulls when the consumer drains (desiredSize), so a phone on 3G throttles the upstream read loop instead of inflating a server-side queue — flat memory per stream regardless of client speed. Headers: text/event-stream; cache-control: no-cache, no-transform; X-Accel-Buffering: no. Runtime: the route is a pure streaming proxy — no database, ~tens of ms CPU, up to 30s wall-clock waiting — making it an edge-runtime candidate; self-hosted on Node it works identically provided the serving layer flushes (no compression of this content type, no response collection). Infrastructure checklist as a deploy gate: (1) nginx: gzip off (or types exemption) for text/event-stream — compression buffers defeat streams even with proxy buffering disabled; (2) X-Accel-Buffering honored or proxy_buffering off for the location; (3) verification: curl -N against the pod, then through nginx, then from the public edge — the first hop where tokens stop flowing progressively is the regression, and this check runs in CI against staging because buffering regressions arrive silently with infra changes, not code changes.",
        "ru": "Форма хендлера: POST /api/assistant читает prompt, создаёт AbortController и вызывает SDK провайдера с { signal: controller.signal }. Ответ — ReadableStream, чей pull() ждёт ровно один upstream-токен и enqueue-ит один SSE-фрейм — data: JSON.stringify({ delta }) + пустая строка, — поэтому переводы строк и markdown внутри токенов безвредны; на done от upstream выдаёт data: [DONE] и закрывается. cancel() вызывает controller.abort(): когда пользователь закрывает вкладку на 20% ответа, провайдер прекращает генерацию в пределах токена — разница между оплатой 20% и 100% каждого брошенного ответа. Backpressure достаётся бесплатно из pull-модели: рантайм тянет только когда потребитель вычитал (desiredSize), и телефон на 3G тормозит цикл чтения upstream, а не раздувает серверную очередь — ровная память на стрим при любой скорости клиента. Заголовки: text/event-stream; cache-control: no-cache, no-transform; X-Accel-Buffering: no. Runtime: маршрут — чистый стриминговый прокси: без базы, ~десятки мс CPU, до 30 с ожидания по настенным часам — кандидат на edge-runtime; на self-hosted Node работает идентично при условии, что отдающий слой flush-ит (без сжатия этого content type, без сборки ответа целиком). Инфраструктурный чек-лист как деплой-гейт: (1) nginx: gzip off (или исключение по types) для text/event-stream — буферы сжатия убивают стримы даже при выключенной буферизации прокси; (2) X-Accel-Buffering уважается или proxy_buffering off для location; (3) проверка: curl -N в под, затем через nginx, затем с публичного края — первый хоп, где токены перестают течь постепенно, и есть регрессия, и эта проверка живёт в CI против staging, потому что регрессии буферизации приходят молча с изменениями инфраструктуры, а не кода."
      }
    }
  ]
}
