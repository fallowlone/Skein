{
  "lessonKey": "nextjs/02-app-router-and-data/01-app-router-architecture",
  "track": "nextjs",
  "tasks": [
    {
      "id": "01-app-router-predict-layout-state",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: sidebar state across navigation",
        "ru": "Предскажи: состояние сайдбара при навигации"
      },
      "prompt": {
        "en": "Before reading the reveal, write down exactly what the user sees at each step: the sidebar's collapsed state, whether the layout's server fetch re-runs, and what the analytics panel does.",
        "ru": "Прежде чем смотреть разбор, выпишите, что именно видит пользователь на каждом шаге: состояние свёрнутости сайдбара, выполняется ли заново серверный fetch layout и что делает панель аналитики."
      },
      "scenario": {
        "en": "app/dashboard/layout.tsx is a server component that fetches the org name and renders a client <Sidebar> with useState for collapsed/expanded. app/dashboard/loading.tsx shows a skeleton. The user: (1) lands on /dashboard, collapses the sidebar; (2) clicks to /dashboard/reports, whose page fetches for 2 seconds; (3) clicks back to /dashboard. Predict for steps 2 and 3: is the sidebar still collapsed? Does the org-name fetch run again? Where does the skeleton appear?",
        "ru": "app/dashboard/layout.tsx — серверный компонент: запрашивает название организации и рендерит клиентский <Sidebar> с useState для свёрнут/развёрнут. app/dashboard/loading.tsx показывает скелетон. Пользователь: (1) открывает /dashboard, сворачивает сайдбар; (2) кликает на /dashboard/reports, чья страница грузит данные 2 секунды; (3) кликает обратно на /dashboard. Предскажите для шагов 2 и 3: сайдбар всё ещё свёрнут? Выполняется ли заново fetch названия организации? Где появляется скелетон?"
      },
      "reveal": {
        "en": "Step 2: the dashboard layout is shared between /dashboard and /dashboard/reports, so it persists — the same Sidebar instance stays mounted and remains collapsed; the org-name fetch does NOT re-run (partial rendering skips persisted layouts). The skeleton from loading.tsx appears only in the page slot — the area where children render — while the sidebar stays interactive around it for the 2 seconds the reports page streams. Step 3: same again — sidebar still collapsed, no layout fetch. If /dashboard's page is in the client Router Cache, back-navigation can render it without a server round trip at all. The only things that re-render across these navigations are the page segments below the shared layout.",
        "ru": "Шаг 2: layout дашборда общий для /dashboard и /dashboard/reports, поэтому он сохраняется — смонтированным остаётся тот же экземпляр Sidebar, и он по-прежнему свёрнут; fetch названия организации НЕ выполняется заново (частичный рендеринг пропускает сохранённые layout). Скелетон из loading.tsx появляется только в слоте страницы — там, где рендерится children, — а сайдбар вокруг него остаётся интерактивным те 2 секунды, что стримится страница отчётов. Шаг 3: то же самое — сайдбар свёрнут, fetch layout не выполняется. Если страница /dashboard лежит в клиентском Router Cache, навигация назад может отрендериться вообще без похода на сервер. Единственное, что перерендеривается при этих переходах, — сегменты страниц ниже общего layout."
      }
    },
    {
      "id": "01-app-router-diagnose-stale-badge",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose: the plan badge that would not update",
        "ru": "Диагностика: бейдж тарифа, который не обновлялся"
      },
      "prompt": {
        "en": "Name the mechanism that keeps the badge stale and the two legitimate fixes. Fill the blanks.",
        "ru": "Назовите механизм, из-за которого бейдж протухает, и два легитимных исправления. Заполните пропуски."
      },
      "evidence": {
        "en": "Bug report: 'After upgrading my plan on /settings/billing, the sidebar badge still says Free until I hit F5.' Facts: the badge is fetched and rendered in app/(app)/layout.tsx; the upgrade is a server action that writes to the DB and returns { ok: true }; a hard refresh always shows the correct plan; adding cache: 'no-store' to the layout's fetch changed nothing. Blank 1: the badge is stale because the shared layout ___ across client-side navigation (name the behavior). Blank 2: the one-line fix inside the upgrade action is calling ___. Blank 3: the client-side alternative after the mutation resolves is calling ___.",
        "ru": "Баг-репорт: «После апгрейда тарифа на /settings/billing бейдж в сайдбаре показывает Free, пока я не нажму F5». Факты: бейдж запрашивается и рендерится в app/(app)/layout.tsx; апгрейд — серверный экшен, который пишет в БД и возвращает { ok: true }; жёсткая перезагрузка всегда показывает правильный тариф; добавление cache: 'no-store' в fetch layout ничего не изменило. Пропуск 1: бейдж протухает, потому что общий layout ___ при клиентской навигации (назовите поведение). Пропуск 2: однострочный фикс внутри экшена апгрейда — вызвать ___. Пропуск 3: клиентская альтернатива после завершения мутации — вызвать ___."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "persists",
              "is preserved",
              "does not re-render",
              "doesn't re-render",
              "never re-renders",
              "stays mounted",
              "сохраняется",
              "не перерендеривается",
              "не рендерится заново",
              "остаётся смонтированным",
              "живёт между навигациями"
            ],
            "hint": {
              "en": "Partial rendering keeps shared layout instances mounted — their server code never re-runs on navigation.",
              "ru": "Частичный рендеринг держит экземпляры общих layout смонтированными — их серверный код при навигации не выполняется заново."
            }
          },
          {
            "id": "b2",
            "accept": [
              "revalidatePath",
              "revalidatePath('/')",
              "revalidatepath",
              "revalidateTag",
              "revalidatetag"
            ],
            "hint": {
              "en": "The server-side invalidation call that tells the router the routes depending on this data are now stale.",
              "ru": "Серверный вызов инвалидации, сообщающий роутеру, что маршруты, зависящие от этих данных, протухли."
            }
          },
          {
            "id": "b3",
            "accept": [
              "router.refresh",
              "router.refresh()",
              "refresh",
              "router refresh",
              "роутер.refresh"
            ],
            "hint": {
              "en": "Re-fetches the current route's server components without losing client state — from useRouter.",
              "ru": "Заново запрашивает серверные компоненты текущего маршрута, не теряя клиентское состояние, — из useRouter."
            }
          }
        ]
      }
    },
    {
      "id": "01-app-router-design-photo-modal-routes",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: route tree for a feed with URL-addressable photo modals",
        "ru": "Спроектируй: дерево маршрутов для ленты с модалками фото по URL"
      },
      "prompt": {
        "en": "Design the app/ directory structure (folders and special files) for this product. Name every convention you use and what it buys you, then list the two failure cases you must test before shipping.",
        "ru": "Спроектируйте структуру каталога app/ (папки и спецфайлы) для этого продукта. Назовите каждую использованную конвенцию и что она даёт, затем перечислите два сценария отказа, которые обязательно протестировать перед релизом."
      },
      "constraints": {
        "en": "Product: a photo-sharing app. Requirements: (1) /feed shows the photo feed; clicking a photo opens /photo/[id] as a modal over the feed, and the URL must be shareable — a pasted link shows a standalone photo page; (2) the marketing pages (/, /pricing) use a completely different layout (no app chrome) at the same URL depth as the app pages; (3) the feed page has a notifications panel that loads slowly from a separate service and must not block or crash the feed; (4) browser refresh while the modal is open must show something sensible.",
        "ru": "Продукт: приложение для обмена фото. Требования: (1) /feed показывает ленту; клик по фото открывает /photo/[id] модалкой поверх ленты, и URL должен шариться — вставленная ссылка показывает отдельную страницу фото; (2) маркетинговые страницы (/, /pricing) используют совершенно другой layout (без app-обвязки) на той же глубине URL, что и страницы приложения; (3) на странице ленты есть панель уведомлений, которая медленно грузится из отдельного сервиса и не должна блокировать или ронять ленту; (4) перезагрузка браузера при открытой модалке должна показать что-то осмысленное."
      },
      "rubric": [
        {
          "en": "Uses route groups — e.g. (marketing) with its own layout for / and /pricing, and (app) with the app chrome layout — and notes that the groups must not claim the same URL path and that navigating between groups is a hard navigation.",
          "ru": "Использует группы маршрутов — например, (marketing) со своим layout для / и /pricing и (app) с layout app-обвязки — и отмечает, что группы не должны претендовать на один URL и что переход между группами — жёсткая навигация."
        },
        {
          "en": "Implements the modal with an intercepting route — (.)photo/[id] (or the correct level prefix) inside the feed segment rendering into a parallel @modal slot — while keeping a real photo/[id]/page.tsx as the standalone page for direct loads and refreshes.",
          "ru": "Реализует модалку перехватывающим маршрутом — (.)photo/[id] (или с корректным префиксом уровня) внутри сегмента ленты с рендером в параллельный слот @modal, — сохраняя настоящий photo/[id]/page.tsx как отдельную страницу для прямых заходов и перезагрузок."
        },
        {
          "en": "Isolates the notifications panel as a parallel route slot (@notifications) with its own loading.tsx and error.tsx so it streams independently and its failure doesn't take down the feed; includes default.tsx to avoid hard-refresh 404s.",
          "ru": "Изолирует панель уведомлений как слот параллельного маршрута (@notifications) со своими loading.tsx и error.tsx, чтобы она стримилась независимо и её падение не роняло ленту; добавляет default.tsx, чтобы жёсткая перезагрузка не дала 404."
        },
        {
          "en": "Names the two ship-blocking test cases: hard-loading /photo/[id] (must render the standalone page, not the modal — interception applies only to soft navigation) and hard-refreshing a URL while a slot has no match (must hit default.tsx, not 404).",
          "ru": "Называет два обязательных предрелизных теста: жёсткая загрузка /photo/[id] (должна отрендериться отдельная страница, а не модалка — перехват действует только на мягкую навигацию) и жёсткая перезагрузка URL, когда слоту нечего сматчить (должен сработать default.tsx, а не 404)."
        }
      ],
      "model": {
        "en": "app/ splits into two route groups under one root layout. (marketing)/layout.tsx carries the public chrome with page.tsx for / and pricing/page.tsx; (app)/layout.tsx carries the app shell. Inside (app), feed/ is the heart: feed/layout.tsx declares three slots — children, @modal, @notifications. The feed page lives at feed/page.tsx. The notifications panel is a parallel route: feed/@notifications/page.tsx with its own loading.tsx (skeleton streams independently), error.tsx (panel-local failure UI), and default.tsx (so a hard refresh of any feed URL doesn't 404). The modal is an intercepting route rendered into the slot: feed/@modal/(.)photo/[id]/page.tsx — on soft navigation from the feed, clicking a photo renders the photo inside the modal slot over the still-visible feed, URL becomes /photo/[id]. The standalone route (app)/photo/[id]/page.tsx remains the real page: pasted links, new tabs, and refreshes render it because interception never applies to hard loads — refresh-while-modal-open lands on the full photo page, which is the sensible behavior required. Tests before shipping: direct-load /photo/[id] shows the standalone page; hard refresh on /feed renders @notifications' default/own page rather than 404; killing the notifications service shows the slot's error UI while the feed keeps working. Honest tradeoff worth stating: this encodes three conventions ((group), @slot, (.)intercept) that are opaque to newcomers — the README should map the tree, because debugging a missing default.tsx from the file system alone is miserable.",
        "ru": "app/ делится на две группы маршрутов под одним корневым layout. (marketing)/layout.tsx несёт публичную обвязку с page.tsx для / и pricing/page.tsx; (app)/layout.tsx — оболочку приложения. Внутри (app) сердце — feed/: feed/layout.tsx объявляет три слота — children, @modal, @notifications. Страница ленты — feed/page.tsx. Панель уведомлений — параллельный маршрут: feed/@notifications/page.tsx со своими loading.tsx (скелетон стримится независимо), error.tsx (локальный UI отказа панели) и default.tsx (чтобы жёсткая перезагрузка любого URL ленты не дала 404). Модалка — перехватывающий маршрут, рендерящийся в слот: feed/@modal/(.)photo/[id]/page.tsx — при мягкой навигации из ленты клик по фото рендерит его в слоте модалки поверх видимой ленты, URL становится /photo/[id]. Отдельный маршрут (app)/photo/[id]/page.tsx остаётся настоящей страницей: вставленные ссылки, новые вкладки и перезагрузки рендерят его, потому что перехват никогда не действует на жёсткие загрузки — перезагрузка при открытой модалке попадает на полную страницу фото, то самое осмысленное поведение из требований. Тесты перед релизом: прямой заход на /photo/[id] показывает отдельную страницу; жёсткая перезагрузка /feed рендерит default/собственную страницу @notifications, а не 404; убитый сервис уведомлений показывает error-UI слота, пока лента продолжает работать. Честный компромисс, который стоит проговорить: здесь три конвенции ((группа), @слот, (.)перехват), непрозрачные для новичков, — дерево стоит описать в README, потому что отлаживать отсутствующий default.tsx по одной файловой системе мучительно."
      }
    }
  ]
}
