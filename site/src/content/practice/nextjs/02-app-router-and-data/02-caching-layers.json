{
  "lessonKey": "nextjs/02-app-router-and-data/02-caching-layers",
  "track": "nextjs",
  "tasks": [
    {
      "id": "02-caching-predict-deploy-stale-price",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: what a redeploy does (and doesn't) clear",
        "ru": "Предскажи: что очищает редеплой (а что нет)"
      },
      "prompt": {
        "en": "Before the reveal, predict exactly what production serves after each event, naming the cache layer responsible each time.",
        "ru": "До разбора предскажите, что именно отдаёт прод после каждого события, каждый раз называя ответственный слой кеша."
      },
      "scenario": {
        "en": "Next 14 app. A statically rendered page /pricing fetches plans from a CMS with a bare fetch(url) — no options. Timeline: (1) site is built and deployed Monday; (2) Wednesday, the CMS price changes from 49 to 59; (3) Thursday, the team redeploys with an unrelated CSS fix; (4) Friday, an engineer changes the fetch to fetch(url, { next: { revalidate: 60 } }) and deploys. For each point after (2), (3), and (4): which price do visitors see, and which cache layer decides it? Bonus: what is special about the FIRST request more than 60s after the (4) deploy?",
        "ru": "Приложение на Next 14. Статически рендеримая страница /pricing запрашивает тарифы из CMS голым fetch(url) — без опций. Хронология: (1) сайт собран и задеплоен в понедельник; (2) в среду цена в CMS меняется с 49 на 59; (3) в четверг команда редеплоит с непричастным CSS-фиксом; (4) в пятницу инженер меняет fetch на fetch(url, { next: { revalidate: 60 } }) и деплоит. Для каждого момента после (2), (3) и (4): какую цену видят посетители и какой слой кеша это решает? Бонус: что особенного в ПЕРВОМ запросе спустя более 60 секунд после деплоя (4)?"
      },
      "reveal": {
        "en": "After (2): visitors see 49. The Full Route Cache serves the build-time HTML; nothing has invalidated it. After (3): still 49 — and this is the trap. The redeploy DID clear the Full Route Cache, but the rebuild re-ran the page, whose bare fetch (Next 14 default: force-cache) hit the Data Cache — which persists across deployments — and reused Monday's CMS response. Stale data got baked into a fresh build. After (4): the deploy rebuilds again; whether the build itself picks up 59 depends on the same Data Cache entry, but now the entry carries a 60-second freshness window, so within a minute of traffic the route shows 59. Bonus: the first request after the 60s window still receives the STALE entry — revalidation is stale-while-revalidate: that request triggers a background refetch and the NEXT request sees fresh data. The durable lesson: deploys clear the Full Route Cache but never the Data Cache; an unbounded default cache entry survives until something explicitly revalidates it.",
        "ru": "После (2): посетители видят 49. Full Route Cache отдаёт HTML времён сборки; его ничто не инвалидировало. После (3): всё ещё 49 — и вот ловушка. Редеплой ДЕЙСТВИТЕЛЬНО очистил Full Route Cache, но пересборка заново выполнила страницу, чей голый fetch (умолчание Next 14: force-cache) попал в Data Cache — который переживает деплои — и переиспользовал понедельничный ответ CMS. Протухшие данные запеклись в свежую сборку. После (4): деплой снова пересобирает; увидит ли сама сборка 59, зависит от той же записи Data Cache, но теперь у записи окно свежести 60 секунд, так что в течение минуты трафика маршрут покажет 59. Бонус: первый запрос после истечения 60-секундного окна всё ещё получает ПРОТУХШУЮ запись — ревалидация работает как stale-while-revalidate: этот запрос запускает фоновый перезапрос, и свежие данные видит СЛЕДУЮЩИЙ. Устойчивый вывод: деплои чистят Full Route Cache, но никогда — Data Cache; бессрочная кеш-запись по умолчанию живёт, пока её явно не ревалидируют."
      }
    },
    {
      "id": "02-caching-diagnose-stale-layers",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 8,
      "title": {
        "en": "Diagnose: three stale symptoms, three different layers",
        "ru": "Диагностика: три симптома протухания — три разных слоя"
      },
      "prompt": {
        "en": "Each symptom isolates one cache layer. Name the layer (or the missing call) for each blank.",
        "ru": "Каждый симптом изолирует один слой кеша. Назовите слой (или недостающий вызов) для каждого пропуска."
      },
      "evidence": {
        "en": "One Next.js app, three tickets. Ticket A: 'CMS content updated two weeks ago; we've deployed four times since; the page still shows the old text. curl of the page shows the old text too.' The layer that survives deployments and serves the old response is the ___ (blank 1). Ticket B: 'A server action updates the record — the DB row is correct, curl shows fresh data was possible to render, but the page everyone loads still shows the old value until we manually purge. The action body has zero cache calls.' The missing line in the action is ___ (blank 2). Ticket C: 'After the fix, the editing user sees fresh data immediately, but a colleague with the tab already open still sees the old value when navigating back and forth; F5 fixes it.' The colleague's staleness lives in the ___ (blank 3).",
        "ru": "Одно приложение Next.js, три тикета. Тикет A: «Контент CMS обновили две недели назад; с тех пор деплоились четыре раза; страница всё ещё показывает старый текст. curl страницы тоже отдаёт старый текст». Слой, который переживает деплои и отдаёт старый ответ, — это ___ (пропуск 1). Тикет B: «Серверный экшен обновляет запись — строка в БД корректна, по curl видно, что свежие данные могли бы отрендериться, но страница у всех показывает старое значение, пока мы не почистим вручную. В теле экшена ноль кеш-вызовов». Недостающая строка в экшене — ___ (пропуск 2). Тикет C: «После фикса редактировавший пользователь сразу видит свежие данные, но коллега с уже открытой вкладкой при навигации туда-обратно всё ещё видит старое значение; F5 лечит». Протухание у коллеги живёт в ___ (пропуск 3)."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "data cache",
              "the data cache",
              "Data Cache",
              "дата-кеш",
              "дата кеш",
              "data-кеш",
              "кеш данных"
            ],
            "hint": {
              "en": "The server-side fetch-response store whose lifetime spans deployments — its signature symptom.",
              "ru": "Серверное хранилище ответов fetch, чей срок жизни охватывает деплои, — его фирменный симптом."
            }
          },
          {
            "id": "b2",
            "accept": [
              "revalidatePath",
              "revalidateTag",
              "revalidatepath",
              "revalidatetag",
              "revalidatePath or revalidateTag",
              "revalidatePath/revalidateTag",
              "вызов revalidatePath",
              "вызов revalidateTag"
            ],
            "hint": {
              "en": "The write succeeded; the caches were never told. The most common omission in server actions.",
              "ru": "Запись прошла; кешам не сказали. Самое частое упущение в серверных экшенах."
            }
          },
          {
            "id": "b3",
            "accept": [
              "router cache",
              "the router cache",
              "Router Cache",
              "client router cache",
              "client-side router cache",
              "роутер-кеш",
              "роутер кеш",
              "клиентский router cache",
              "клиентский кеш роутера",
              "кеш роутера"
            ],
            "hint": {
              "en": "The only per-browser layer — fresh on the server, stale in one tab, cleared by hard reload.",
              "ru": "Единственный пер-браузерный слой — свежо на сервере, протухло в одной вкладке, лечится жёсткой перезагрузкой."
            }
          }
        ]
      }
    },
    {
      "id": "02-caching-design-product-page-strategy",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: a caching contract for an e-commerce product page",
        "ru": "Спроектируй: кеш-контракт для страницы товара в e-commerce"
      },
      "prompt": {
        "en": "Design the caching strategy per data source: which layer holds what, the freshness contract for each fetch, the invalidation path for every mutation, and how the answer changes between Next 14 and Next 15.",
        "ru": "Спроектируйте кеш-стратегию по источникам данных: какой слой что хранит, контракт свежести для каждого fetch, путь инвалидации для каждой мутации и как ответ меняется между Next 14 и Next 15."
      },
      "constraints": {
        "en": "Page: /product/[id], 2M views/day, p95 budget 300ms. Data: (a) product description and images from a CMS — changes a few times a month, editors expect updates live within a minute of publishing; (b) price and stock from an inventory service — must never be more than 30s stale, stock can flip to zero mid-traffic-spike; (c) the viewer's cart count in the header — strictly per-user. The CMS can send webhooks. The inventory service handles at most 200 rps. You may split components/fetches however you like, but the route's TTFB must stay fast.",
        "ru": "Страница: /product/[id], 2 млн просмотров/день, бюджет p95 — 300 мс. Данные: (a) описание и изображения товара из CMS — меняются несколько раз в месяц, редакторы ждут обновления на сайте в течение минуты после публикации; (b) цена и остатки из inventory-сервиса — протухание не более 30 с, остаток может упасть в ноль посреди пика трафика; (c) счётчик корзины пользователя в шапке — строго пер-пользовательский. CMS умеет слать вебхуки. Inventory-сервис держит максимум 200 rps. Компоненты/запросы можно делить как угодно, но TTFB маршрута должен оставаться быстрым."
      },
      "rubric": [
        {
          "en": "CMS content: cached in the Data Cache with tags (e.g. product-[id]) and invalidated on demand by a CMS webhook hitting a route handler that calls revalidateTag — meeting the within-a-minute editor expectation without per-request CMS load; explicitly notes a route-handler webhook cannot flush browsers' Router Caches and why that's acceptable.",
          "ru": "Контент CMS: кешируется в Data Cache с тегами (например, product-[id]) и инвалидируется по требованию вебхуком CMS в route handler с revalidateTag — закрывая ожидание редакторов «в течение минуты» без пер-запросной нагрузки на CMS; явно отмечает, что вебхук в route handler не может сбросить Router Cache браузеров, и почему это приемлемо."
        },
        {
          "en": "Price/stock: time-based revalidate around 15-30s on the inventory fetch (stale-while-revalidate keeps origin load near 1 request per window per product, far under 200 rps) rather than no-store, with the stale-one-more-hit semantics acknowledged; mentions the spike scenario — zero-stock flips propagate within the window, and the checkout action is the authoritative stock check anyway.",
          "ru": "Цена/остатки: revalidate по времени около 15–30 с на fetch к inventory (stale-while-revalidate держит нагрузку на источник около 1 запроса на окно на товар — сильно ниже 200 rps), а не no-store, с проговорённой семантикой «ещё один протухший хит»; упоминает сценарий пика — обнуление остатка распространяется в пределах окна, а авторитетная проверка остатка всё равно в экшене чекаута."
        },
        {
          "en": "Cart count: never in shared caches — per-user data read via cookies()/session, isolated so it doesn't drag the whole route dynamic (e.g. a separate component/slot or fetched client-side / via dynamic rendering boundaries), keeping the product content statically servable for TTFB.",
          "ru": "Счётчик корзины: никогда в общих кешах — пер-пользовательские данные через cookies()/сессию, изолированные так, чтобы не утащить весь маршрут в динамику (отдельный компонент/слот, клиентский fetch или границы динамического рендеринга), сохраняя статическую отдачу контента товара ради TTFB."
        },
        {
          "en": "States the route-level decision: route cached/static (or PPR-style split) with the Full Route Cache serving the shell, and acknowledges that route caching and data caching are independent axes — a no-store cart fetch in the same tree is what would silently force the route dynamic if not isolated.",
          "ru": "Формулирует решение уровня маршрута: маршрут кеширован/статичен (или PPR-подобное разделение), Full Route Cache отдаёт каркас, и признаёт, что кеширование маршрута и данных — независимые оси: неизолированный no-store fetch корзины в том же дереве молча сделал бы маршрут динамическим."
        },
        {
          "en": "Versions the design: on Next 14 the bare CMS/inventory fetches would default to force-cache (the tags/revalidate must be explicit anyway), on Next 15 fetch defaults to uncached so every cached fetch needs an explicit opt-in — and the inventory service's 200 rps limit is exactly what breaks if the team upgrades and loses default caching unknowingly.",
          "ru": "Версионирует дизайн: на Next 14 голые fetch к CMS/inventory по умолчанию force-cache (теги/revalidate всё равно нужно задать явно), на Next 15 fetch по умолчанию не кешируется, так что каждый кешируемый fetch требует явного включения — и лимит inventory-сервиса в 200 rps — ровно то, что сломается, если команда апгрейдится и незаметно потеряет кеширование по умолчанию."
        }
      ],
      "model": {
        "en": "Split by freshness contract, not by page. (a) CMS content: fetch with next: { tags: ['product-' + id] } and a generous revalidate (e.g. 86400) as a safety net; a route handler at /api/revalidate receives the CMS webhook, verifies a shared secret, and calls revalidateTag('product-' + id) — Data Cache purged, Full Route Cache regenerated on next request, editors see changes within seconds. The webhook can't reach open browsers' Router Caches; acceptable, because content edits don't need to interrupt active sessions and tabs catch up on next hard navigation. (b) Price/stock: fetch with next: { revalidate: 30 } (or 15). At 2M views/day the Data Cache collapses inventory load to roughly one origin request per product per window — orders of magnitude under 200 rps; no-store would instead forward thousands of rps and breach both the limit and the p95 budget. Accept stale-while-revalidate semantics: one request per window sees the stale price; the checkout server action revalidates stock authoritatively at purchase time, so a mid-spike zero-stock flip costs at most a 30s window of optimistic UI, not oversold orders. (c) Cart count: per-user, so it must never enter shared caches — read the session via cookies() in an isolated boundary (own component streamed via Suspense, or a client-side fetch to a route handler) so the product shell stays in the Full Route Cache and TTFB holds; remember the axes are independent — an unisolated cookies() call or no-store fetch in the page would force the whole route dynamic. Version note: this design is explicit about every cache decision precisely so it survives the 14→15 boundary — on 15, fetch and GET handlers default to uncached, so the tags and revalidate options above are the opt-ins; a team relying on 14's silent force-cache would melt the inventory service on upgrade day.",
        "ru": "Делите по контракту свежести, а не по странице. (a) Контент CMS: fetch с next: { tags: ['product-' + id] } и щедрым revalidate (например, 86400) как страховкой; route handler на /api/revalidate принимает вебхук CMS, проверяет общий секрет и вызывает revalidateTag('product-' + id) — Data Cache вычищен, Full Route Cache перегенерируется на следующем запросе, редакторы видят изменения за секунды. До Router Cache открытых браузеров вебхук не дотянется; приемлемо: правки контента не должны прерывать активные сессии, вкладки догонят при следующей жёсткой навигации. (b) Цена/остатки: fetch с next: { revalidate: 30 } (или 15). При 2 млн просмотров/день Data Cache схлопывает нагрузку на inventory примерно до одного запроса на товар на окно — на порядки ниже 200 rps; no-store вместо этого пробросил бы тысячи rps и пробил бы и лимит, и бюджет p95. Семантику stale-while-revalidate принимаем: один запрос на окно видит протухшую цену; авторитетную проверку остатка делает серверный экшен чекаута в момент покупки, так что обнуление остатка посреди пика стоит максимум 30-секундного окна оптимистичного UI, а не перепроданных заказов. (c) Счётчик корзины: пер-пользовательский, в общие кеши не попадает никогда — сессия через cookies() в изолированной границе (свой компонент, стримящийся через Suspense, или клиентский fetch к route handler), чтобы каркас товара остался в Full Route Cache и TTFB держался; помните, оси независимы — неизолированный вызов cookies() или no-store fetch в странице сделал бы весь маршрут динамическим. Замечание о версиях: дизайн явен в каждом кеш-решении именно затем, чтобы пережить границу 14→15 — на 15 fetch и GET-handlers по умолчанию некешируемы, так что теги и revalidate выше и есть явные включения; команда, полагавшаяся на молчаливый force-cache в 14, расплавила бы inventory-сервис в день апгрейда."
      }
    }
  ]
}
