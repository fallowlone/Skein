{
  "lessonKey": "nextjs/02-app-router-and-data/03-server-actions",
  "track": "nextjs",
  "tasks": [
    {
      "id": "03-server-actions-predict-hidden-admin-action",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": {
        "en": "Predict: the admin-only action and the crafted POST",
        "ru": "Предскажи: админский экшен и поддельный POST"
      },
      "prompt": {
        "en": "Before the reveal, predict the outcome of each of the three requests, and name which framework check (if any) stops each one.",
        "ru": "До разбора предскажите исход каждого из трёх запросов и назовите, какая проверка фреймворка (если есть) останавливает каждый."
      },
      "scenario": {
        "en": "An archiveProject server action is exported from app/actions.ts. Its body: const session = await getSession(); if (!session) throw new Error('Unauthenticated'); await db.project.update({ where: { id: projectId }, data: { archived: true } }). The archive button renders only for project owners. Three requests arrive: (1) a logged-in NON-owner replays the action's POST from their own session with someone else's projectId; (2) an anonymous visitor sends the same POST with no session cookie; (3) a malicious third-party site evil.com auto-submits a form to your origin from a victim's browser. For each: does the archive happen, and what stopped it (or failed to)?",
        "ru": "Серверный экшен archiveProject экспортируется из app/actions.ts. Его тело: const session = await getSession(); if (!session) throw new Error('Unauthenticated'); await db.project.update({ where: { id: projectId }, data: { archived: true } }). Кнопка архивации рендерится только владельцам проекта. Приходят три запроса: (1) залогиненный НЕ-владелец воспроизводит POST экшена из своей сессии с чужим projectId; (2) анонимный посетитель шлёт тот же POST без сессионной cookie; (3) злонамеренный сторонний сайт evil.com автоматически отправляет форму на ваш origin из браузера жертвы. Для каждого: произойдёт ли архивация и что её остановило (или не смогло)?"
      },
      "reveal": {
        "en": "(1) The archive HAPPENS — this is the bug. The action checks authentication ('is there a session?') but not authorization ('does this session own this project?'). The UI-level owner gating never executes on the request path; the action is a public endpoint addressed by id, and a valid session plus an arbitrary projectId sails through. This is the IDOR pattern: entity-level auth without an instance-level ownership check. The missing line: load the project and compare ownerId to session.userId. (2) Blocked — but by YOUR code, not the framework: getSession() returns null and the action throws. Nothing in Next.js itself requires authentication. (3) Blocked by the framework: Next compares the Origin header to the Host header on action POSTs and rejects the mismatch — same-site CSRF protection out of the box. Note what each layer buys: the framework stops cross-site forgery only; everything about who may do what to which row is exclusively your code inside the action.",
        "ru": "(1) Архивация ПРОИСХОДИТ — это и есть баг. Экшен проверяет аутентификацию («есть ли сессия?»), но не авторизацию («владеет ли эта сессия этим проектом?»). UI-гейтинг по владельцу на пути запроса не выполняется; экшен — публичный эндпоинт, адресуемый по id, и валидная сессия плюс произвольный projectId проходят насквозь. Это паттерн IDOR: авторизация на уровне типа сущности без по-экземплярной проверки владения. Недостающая строка: загрузить проект и сравнить ownerId с session.userId. (2) Заблокировано — но ВАШИМ кодом, не фреймворком: getSession() возвращает null, и экшен бросает исключение. Сам Next.js аутентификации не требует. (3) Заблокировано фреймворком: Next сравнивает заголовок Origin с Host на POST-ах экшенов и отклоняет расхождение — same-site CSRF-защита из коробки. Заметьте, что покупает каждый слой: фреймворк останавливает только межсайтовую подделку; всё про то, кто что может делать с какой строкой, — исключительно ваш код внутри экшена."
      }
    },
    {
      "id": "03-server-actions-diagnose-saved-but-stale",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 7,
      "title": {
        "en": "Diagnose: saved to the DB, broken on the screen",
        "ru": "Диагностика: в БД сохранилось, на экране сломалось"
      },
      "prompt": {
        "en": "Two independent bugs hide in this action. Name the missing call, the serialization rule being violated, and the security property the team wrongly assumed.",
        "ru": "В этом экшене прячутся два независимых бага. Назовите недостающий вызов, нарушаемое правило сериализации и свойство безопасности, которое команда ошибочно предположила."
      },
      "evidence": {
        "en": "Action: export async function createInvoice(data) { 'use server'; const session = await requireSession(); const invoice = await db.invoice.create({ data: { ...data, userId: session.userId } }); return invoice; } Symptoms: (1) QA: 'Creating an invoice works — the row appears in the DB — but the invoices list on the page doesn't show it until F5.' The action mutates but never calls ___ (blank 1). (2) Sentry, intermittent: 'Error: Only plain objects can be passed to Client Components from Server Components. Decimal objects are not supported.' The return value violates the rule that action results must be ___ (blank 2) — return a plain DTO instead of the ORM model. (3) Code review note: 'data goes into db.invoice.create via spread, and the client TypeScript type is our only guard.' At runtime the request body is attacker-controlled, so the action needs ___ (blank 3) inside it.",
        "ru": "Экшен: export async function createInvoice(data) { 'use server'; const session = await requireSession(); const invoice = await db.invoice.create({ data: { ...data, userId: session.userId } }); return invoice; } Симптомы: (1) QA: «Создание инвойса работает — строка появляется в БД, — но список инвойсов на странице не показывает его до F5». Экшен мутирует, но не вызывает ___ (пропуск 1). (2) Sentry, периодически: «Error: Only plain objects can be passed to Client Components from Server Components. Decimal objects are not supported». Возвращаемое значение нарушает правило: результаты экшена должны быть ___ (пропуск 2) — верните плоский DTO вместо ORM-модели. (3) Заметка с код-ревью: «data уходит в db.invoice.create через spread, и клиентский тип TypeScript — наша единственная защита». В рантайме тело запроса контролирует атакующий, поэтому экшену нужна ___ (пропуск 3) внутри."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "revalidatePath",
              "revalidateTag",
              "revalidatepath",
              "revalidatetag",
              "revalidatePath or revalidateTag",
              "revalidatePath/revalidateTag",
              "вызов revalidatePath",
              "вызов revalidateTag",
              "ревалидацию"
            ],
            "hint": {
              "en": "The mutation succeeded; the caches were never told the list route is stale.",
              "ru": "Мутация прошла; кешам не сказали, что маршрут списка протух."
            }
          },
          {
            "id": "b2",
            "accept": [
              "serializable",
              "react-serializable",
              "react serializable",
              "plain objects",
              "plain serializable objects",
              "сериализуемыми",
              "react-сериализуемыми",
              "плоскими объектами",
              "сериализуемы"
            ],
            "hint": {
              "en": "React's wire protocol handles Date, Map, Set, FormData — but not class instances like Prisma's Decimal.",
              "ru": "Проводной протокол React понимает Date, Map, Set, FormData — но не экземпляры классов вроде Decimal из Prisma."
            }
          },
          {
            "id": "b3",
            "accept": [
              "validation",
              "schema validation",
              "input validation",
              "zod validation",
              "zod parse",
              "validate the input",
              "валидация",
              "схемная валидация",
              "валидация входа",
              "валидация схемой",
              "zod-валидация"
            ],
            "hint": {
              "en": "TypeScript signatures are compile-time fiction; parse the untyped wire bytes with a schema before they touch the DB.",
              "ru": "Сигнатуры TypeScript — фикция этапа компиляции; парсите нетипизированные байты провода схемой до того, как они коснутся БД."
            }
          }
        ]
      }
    },
    {
      "id": "03-server-actions-design-mutation-layer",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 12,
      "title": {
        "en": "Design: the mutation layer for a multi-tenant SaaS",
        "ru": "Спроектируй: слой мутаций для мультитенантного SaaS"
      },
      "prompt": {
        "en": "Design the write path: which mutations are server actions and which are route handlers, the mandatory per-action pipeline, the progressive-enhancement stance, and the cache-revalidation contract.",
        "ru": "Спроектируйте путь записи: какие мутации — серверные экшены, а какие — route handlers, обязательный конвейер каждого экшена, позицию по прогрессивному улучшению и контракт ревалидации кешей."
      },
      "constraints": {
        "en": "Product: a multi-tenant project-management SaaS (Next.js App Router). Consumers of writes: (1) the web app's own forms and buttons; (2) a native mobile app; (3) Stripe webhooks for subscription events; (4) a public partner API with an OpenAPI spec. Security: tenants must never touch each other's rows; a pentest is scheduled. The login and 'create task' forms must work on flaky 3G before JS loads. One mutation — bulk CSV import — takes up to 60s and users fire other actions while it runs.",
        "ru": "Продукт: мультитенантный SaaS для управления проектами (Next.js App Router). Потребители записи: (1) собственные формы и кнопки веб-приложения; (2) нативное мобильное приложение; (3) вебхуки Stripe для событий подписки; (4) публичный партнёрский API со спекой OpenAPI. Безопасность: тенанты никогда не должны касаться чужих строк; запланирован пентест. Формы логина и «создать задачу» должны работать на нестабильном 3G до загрузки JS. Одна мутация — массовый импорт CSV — занимает до 60 с, и пользователи запускают другие экшены, пока она идёт."
      },
      "rubric": [
        {
          "en": "Splits callers correctly: server actions for the web app's own form/button mutations; route handlers for the mobile app, Stripe webhooks, and the partner API — because action ids are opaque, non-deterministic per build, and speak React's wire format, not a stable documented contract.",
          "ru": "Правильно делит вызывающих: серверные экшены — для мутаций собственных форм/кнопок веб-приложения; route handlers — для мобильного приложения, вебхуков Stripe и партнёрского API, потому что id экшенов непрозрачны, недетерминированы между сборками и говорят на проводном формате React, а не на стабильном задокументированном контракте."
        },
        {
          "en": "Defines a mandatory in-action pipeline (and ideally a shared wrapper): authenticate, authorize per instance (tenant/ownership check on the actual row — names IDOR as the pentest finding this prevents), schema-validate the untyped input, mutate, then revalidate.",
          "ru": "Определяет обязательный конвейер внутри экшена (в идеале общий враппер): аутентифицировать, авторизовать по-экземплярно (проверка тенанта/владения на конкретной строке — называет IDOR как находку пентеста, которую это предотвращает), провалидировать нетипизированный вход схемой, мутировать, затем ревалидировать."
        },
        {
          "en": "Progressive enhancement: login and create-task are <form action={...}> submissions (work pre-hydration, FormData parsing with coercion server-side), explicitly noting that onClick-wired actions get no no-JS guarantee.",
          "ru": "Прогрессивное улучшение: логин и создание задачи — отправки <form action={...}> (работают до гидратации, парсинг FormData с приведением типов на сервере), с явной оговоркой, что экшены на onClick гарантии без-JS не получают."
        },
        {
          "en": "Handles the 60s CSV import outside the sequential action queue — one client's actions run one at a time, so a long-running action blocks every other mutation; the import goes to a route handler plus background job/polling (or equivalent), not a server action.",
          "ru": "Выносит 60-секундный CSV-импорт из последовательной очереди экшенов — экшены одного клиента выполняются по одному, и долгий экшен блокирует все остальные мутации; импорт идёт в route handler плюс фоновая задача/поллинг (или эквивалент), а не в серверный экшен."
        },
        {
          "en": "Cache contract: every state-changing action ends with revalidatePath/revalidateTag (which also refreshes the mutating user's Router Cache); route-handler writes (webhooks, partner API) revalidate server caches via the same calls but are acknowledged as unable to reach open browser tabs.",
          "ru": "Кеш-контракт: каждый меняющий состояние экшен завершается revalidatePath/revalidateTag (что обновляет и Router Cache мутировавшего пользователя); записи через route handlers (вебхуки, партнёрский API) ревалидируют серверные кеши теми же вызовами, но с признанием, что до открытых вкладок браузеров они не дотягиваются."
        }
      ],
      "model": {
        "en": "Two write surfaces, one rule: actions face our own React tree, route handlers face everyone else. Web forms and buttons use server actions; the mobile app and partner API get versioned route handlers under /api (stable URLs, OpenAPI row, explicit status codes), and Stripe hits a dedicated webhook handler that verifies the signature and calls revalidateTag for affected subscription data. Every action runs through a shared withAuthz wrapper that encodes the pipeline: getSession or throw; load the target row and assert tenantId matches the session's tenant (the per-instance check — the IDOR class the pentest would otherwise flag); Input.parse(raw) with zod because the wire is untyped bytes regardless of the TS signature; mutate; revalidateTag for the entity. The wrapper makes the safe path the lazy path — reviewers only need to spot a mutation NOT using it. Login and create-task are plain <form action={...}> with useActionState for errors: the browser can submit them as native POSTs before hydration, satisfying the 3G requirement; we document that anything wired to onClick (drag-reorder, inline toggles) requires JS and that's accepted. The CSV import must not be a server action: one client executes actions sequentially, so a 60s import would queue every other mutation behind it. It becomes POST /api/imports returning 202 with a job id; a worker processes it; the client polls (or receives a push) and calls router.refresh() on completion — when the import finishes, the job's final step revalidates the affected tags so the next render is fresh. Cache contract, stated as a team rule: a mutation without a revalidate call is a bug by definition ('saved but nothing changed' is indistinguishable from failure to users); actions get instant UI consistency for the mutating user because in-action revalidation also invalidates their Router Cache, while webhook/API writes fix the server caches only — open tabs converge on next hard navigation, which is acceptable for subscription state and partner-driven changes.",
        "ru": "Две поверхности записи, одно правило: экшены смотрят на наше собственное React-дерево, route handlers — на всех остальных. Веб-формы и кнопки используют серверные экшены; мобильное приложение и партнёрский API получают версионированные route handlers под /api (стабильные URL, строка в OpenAPI, явные статус-коды), а Stripe бьёт в выделенный вебхук-handler, который проверяет подпись и вызывает revalidateTag для затронутых данных подписки. Каждый экшен проходит через общий враппер withAuthz, кодирующий конвейер: getSession или исключение; загрузить целевую строку и убедиться, что tenantId совпадает с тенантом сессии (по-экземплярная проверка — класс IDOR, который иначе пометил бы пентест); Input.parse(raw) через zod, потому что провод — нетипизированные байты независимо от TS-сигнатуры; мутация; revalidateTag для сущности. Враппер делает безопасный путь ленивым путём — ревьюерам остаётся замечать мутации, которые им НЕ пользуются. Логин и создание задачи — обычные <form action={...}> с useActionState для ошибок: браузер может отправить их нативным POST-ом до гидратации, закрывая требование 3G; документируем, что всё повешенное на onClick (drag-сортировка, инлайн-переключатели) требует JS, и это принято. CSV-импорт не должен быть серверным экшеном: один клиент выполняет экшены последовательно, и 60-секундный импорт поставил бы все прочие мутации в очередь за собой. Он становится POST /api/imports с ответом 202 и id задачи; воркер обрабатывает; клиент поллит (или получает push) и по завершении вызывает router.refresh() — финальный шаг задачи ревалидирует затронутые теги, чтобы следующий рендер был свежим. Кеш-контракт как командное правило: мутация без вызова ревалидации — баг по определению («сохранил, ничего не изменилось» для пользователя неотличимо от сбоя); экшены дают мгновенную консистентность UI мутировавшему пользователю, потому что ревалидация изнутри экшена инвалидирует и его Router Cache, тогда как записи через вебхуки/API чинят только серверные кеши — открытые вкладки сойдутся при следующей жёсткой навигации, что для состояния подписки и партнёрских изменений приемлемо."
      }
    },
    {
      "id": "incident-action-queue-freeze",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 13,
      "title": {
        "en": "One export button freezes every save",
        "ru": "Одна кнопка экспорта замораживает все сохранения"
      },
      "prompt": {
        "en": "The new 'Export workspace' button ships — a server action that assembles a ZIP and takes ~25 seconds for large workspaces. Within a day, support reports pile up: 'after I click Export, nothing else saves'. Notes autosave (a server action on blur), kanban drag-and-drop (an action per card move), and the rename dialog all hang for half a minute — then every pending change applies at once in a burst. Server logs show zero errors: every single action completed successfully. The export itself also works. Work it step by step; commit to an answer before revealing.",
        "ru": "Выходит новая кнопка «Экспорт воркспейса» — серверный экшен, собирающий ZIP, для больших воркспейсов это ~25 секунд. За день копятся обращения: «после клика по экспорту больше ничего не сохраняется». Автосохранение заметок (экшен по blur), drag-and-drop канбана (экшен на каждое перемещение карточки) и диалог переименования зависают на полминуты — а потом все накопленные изменения применяются разом, пачкой. В серверных логах ноль ошибок: каждый экшен завершился успешно. Сам экспорт тоже работает. Разбирайте по шагам; сформулируйте ответ до раскрытия."
      },
      "steps": [
        {
          "label": {
            "en": "1 · The burst is the clue",
            "ru": "1 · Пачка — это улика"
          },
          "prompt": {
            "en": "Mutations hang for ~25 seconds, then all apply at once, with zero errors server-side. What does the burst pattern rule out, and what does it point to?",
            "ru": "Мутации висят ~25 секунд, потом применяются все разом, при нуле ошибок на сервере. Что паттерн «пачки» исключает и на что указывает?"
          },
          "reveal": {
            "en": "Zero errors plus eventual success rules out failures, timeouts, and dropped requests — nothing died, everything completed. The burst — all pending mutations landing immediately after the export finishes — is the signature of serialized execution: the requests were accepted but waited in line behind one long-running job. If this were server overload you'd see degradation across users; here each affected user's own actions wait for that user's own export, which localizes the queue to the client session, not the server pool.",
            "ru": "Ноль ошибок плюс итоговый успех исключают сбои, таймауты и потерянные запросы — ничего не умерло, всё завершилось. Пачка — все накопленные мутации приземляются сразу после окончания экспорта — это сигнатура сериализованного выполнения: запросы приняты, но стояли в очереди за одной длинной задачей. Будь это перегрузка сервера, деградация была бы размазана по пользователям; здесь же экшены каждого затронутого пользователя ждут его собственного экспорта, что локализует очередь в клиентской сессии, а не в серверном пуле."
          }
        },
        {
          "label": {
            "en": "2 · Sequential by design",
            "ru": "2 · Последовательность по дизайну"
          },
          "prompt": {
            "en": "Name the framework mechanism that produces this exact behavior, and why it exists.",
            "ru": "Назовите механизм фреймворка, дающий ровно такое поведение, и зачем он существует."
          },
          "reveal": {
            "en": "A given client executes server actions sequentially: invocations queue, and a slow action blocks every one behind it. That's a wire rule of the action protocol — actions are mutation primitives whose results can carry cache revalidations and an updated RSC payload back to the client, so ordering matters; running a client's mutations concurrently would let later state overtake earlier state. The team put a 25-second batch job at the head of that queue: every autosave, card move, and rename behind it waits the full duration. Nothing is misconfigured — the tool is being used outside its design: actions are not a general async-job mechanism, and anything long-running in one starves the user's entire mutation channel.",
            "ru": "Один клиент выполняет серверные экшены последовательно: вызовы становятся в очередь, и медленный экшен блокирует все за собой. Это правило провода протокола экшенов — экшены суть примитивы мутаций, чьи результаты могут нести ревалидации кешей и обновлённый RSC-пейлоад обратно клиенту, поэтому порядок важен; конкурентное выполнение мутаций одного клиента позволило бы позднему состоянию обогнать раннее. Команда поставила 25-секундную батч-задачу в голову этой очереди: каждый автосейв, перенос карточки и переименование за ней ждут весь срок. Ничего не сломано в конфигурации — инструмент используется вне своего дизайна: экшены не универсальный механизм асинхронных задач, и всё долгоиграющее в них морит голодом весь канал мутаций пользователя."
          }
        },
        {
          "label": {
            "en": "3 · Unblocking the queue",
            "ru": "3 · Разблокировать очередь"
          },
          "prompt": {
            "en": "Redesign the export so saves never hang, and set the guardrail that keeps the action queue healthy.",
            "ru": "Перепроектируйте экспорт так, чтобы сохранения никогда не висели, и задайте ограничитель, сохраняющий очередь экшенов здоровой."
          },
          "reveal": {
            "en": "Get the long work out of the action path. Two sound shapes: (1) the action becomes an enqueue — it records an export job and returns in milliseconds; a worker builds the ZIP; the client polls a route handler (or receives a notification) and downloads the artifact from a URL. (2) Skip actions entirely: a route handler owns the export — handler requests run in parallel, can stream the ZIP as it's built, and return proper download headers, which React-serialized action returns can't do anyway. Either way the user's mutation queue only ever carries sub-second operations. Guardrail: a latency budget for actions (e.g. p95 under one second) with per-action duration metrics and an alert; a review rule that anything which can take seconds — exports, imports, bulk operations, third-party syncs — must be a job or a handler, never an action body. The queue is per-client and invisible in dev (one developer rarely races their own export), so only the budget catches it before users do.",
            "ru": "Уберите долгую работу из пути экшена. Две здоровые формы: (1) экшен становится постановкой в очередь — фиксирует задачу экспорта и возвращается за миллисекунды; воркер собирает ZIP; клиент опрашивает route handler (или получает уведомление) и скачивает артефакт по URL. (2) Вовсе без экшена: экспортом владеет route handler — запросы к хендлерам идут параллельно, могут стримить ZIP по мере сборки и возвращать правильные заголовки скачивания, чего React-сериализованный ответ экшена всё равно не умеет. В обоих случаях очередь мутаций пользователя несёт только субсекундные операции. Ограничитель: бюджет латентности экшенов (например, p95 меньше секунды) с метриками длительности по каждому экшену и алертом; правило ревью — всё, что может занять секунды (экспорты, импорты, массовые операции, синки с третьими сторонами), обязано быть задачей или хендлером, но не телом экшена. Очередь per-client и невидима в dev (разработчик редко гоняется со своим же экспортом), так что до пользователей её ловит только бюджет."
          }
        }
      ]
    },
    {
      "id": "review-archive-project-action",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 11,
      "title": {
        "en": "Review an archive-project action",
        "ru": "Ревью экшена архивации проекта"
      },
      "prompt": {
        "en": "This server action archives a project from the workspace UI. Review it like a senior, remembering what a 'use server' function actually is on the wire: name the real defects, don't flag non-issues.",
        "ru": "Этот серверный экшен архивирует проект из UI воркспейса. Отревьюйте как senior, помня, чем функция с 'use server' является на проводе: назовите реальные дефекты и не цепляйтесь к не-проблемам."
      },
      "diff": {
        "lang": "ts",
        "code": "// app/actions/archive-project.ts\n\"use server\";\nimport { db } from \"~/lib/db\";\nimport { revalidatePath } from \"next/cache\";\nimport { getSession } from \"~/lib/auth\";\n\ntype Input = { projectId: string; cascade: boolean };\n\nexport async function archiveProject(input: Input) {\n  const session = await getSession();\n  if (!session) throw new Error(\"Unauthenticated\");\n\n  await db.project.update({\n    where: { id: input.projectId },\n    data: { archived: true },\n  });\n\n  if (input.cascade) {\n    await db.task.updateMany({\n      where: { projectId: input.projectId },\n      data: { archived: true },\n    });\n  }\n\n  revalidatePath(\"/projects\");\n  return { ok: true };\n}"
      },
      "findings": [
        {
          "id": "idor-no-ownership-check",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "Authenticated-but-not-authorized: any user can archive any projectId",
            "ru": "Аутентифицирован, но не авторизован: любой пользователь архивирует любой projectId"
          },
          "explanation": {
            "en": "The action checks 'is logged in' and then updates whatever projectId arrives — classic insecure direct object reference. The action is a public POST endpoint addressable by id; that the UI only renders the button for members is not a gate. Load the project and verify ownership/membership against session.userId before mutating.",
            "ru": "Экшен проверяет «залогинен ли» и затем обновляет любой пришедший projectId — классический insecure direct object reference. Экшен — публичный POST-эндпоинт, адресуемый по id; то, что UI рисует кнопку только участникам, — не защита. Загрузите проект и проверьте владение/членство по session.userId до мутации."
          }
        },
        {
          "id": "typescript-signature-not-validation",
          "severity": "bug",
          "planted": true,
          "label": {
            "en": "The Input type is trusted as if it were runtime validation",
            "ru": "Типу Input доверяют, как будто это валидация времени выполнения"
          },
          "explanation": {
            "en": "At runtime the POST body is attacker-controlled bytes; the TypeScript signature is a compile-time fiction. projectId could be an empty string, an object crafted for the ORM, or cascade could be anything truthy. Parse with a schema (e.g. zod: uuid for projectId, strict boolean for cascade) before the data touches the database.",
            "ru": "Во время выполнения тело POST — это байты под контролем атакующего; сигнатура TypeScript — фикция этапа компиляции. projectId может быть пустой строкой или объектом, скроенным под ORM, а cascade — чем угодно truthy. Парсите схемой (например, zod: uuid для projectId, строгий boolean для cascade) до того, как данные коснутся базы."
          }
        },
        {
          "id": "missing-nonowner-test",
          "severity": "missing-test",
          "planted": true,
          "label": {
            "en": "No test invokes the action as an authenticated non-member",
            "ru": "Нет теста, вызывающего экшен от аутентифицированного не-участника"
          },
          "explanation": {
            "en": "The exact case a pentest will file — a logged-in user replaying the POST with someone else's projectId — is the test that must exist: expect Forbidden, assert the row is untouched. UI-level tests can't cover it because the request path never goes through the UI.",
            "ru": "Ровно тот случай, который заведёт пентест, — залогиненный пользователь, переигрывающий POST с чужим projectId, — и есть обязательный тест: ожидаем Forbidden, проверяем, что строка не тронута. Тесты уровня UI это не покроют, потому что путь запроса через UI не проходит."
          }
        }
      ],
      "decoys": [
        {
          "id": "revalidatepath-after-write",
          "label": {
            "en": "revalidatePath('/projects') after the mutation",
            "ru": "revalidatePath('/projects') после мутации"
          },
          "explanation": {
            "en": "This is the required step, done right: the mutation declares what it invalidated, and inside an action it also refreshes the mutating user's Router Cache. Tags could make it more granular, but path-level invalidation here is correct, not a defect.",
            "ru": "Это обязательный шаг, сделанный правильно: мутация объявляет, что инвалидировала, а внутри экшена это заодно обновляет Router Cache мутирующего пользователя. Теги дали бы больше гранулярности, но инвалидация по пути здесь корректна, это не дефект."
          }
        },
        {
          "id": "throw-on-unauthenticated",
          "label": {
            "en": "Throwing an Error for the unauthenticated case",
            "ru": "Бросание Error для неаутентифицированного случая"
          },
          "explanation": {
            "en": "Failing loudly on a missing session is a fine pattern — the nearest error boundary handles it, and no data path continues. Returning a typed error object instead is a style choice, not a security or correctness difference.",
            "ru": "Громко падать при отсутствии сессии — нормальный паттерн: ближайшая граница ошибок это обработает, и ни один путь к данным не продолжится. Возврат типизированного объекта ошибки вместо этого — вопрос стиля, а не безопасности или корректности."
          }
        }
      ]
    }
  ]
}
