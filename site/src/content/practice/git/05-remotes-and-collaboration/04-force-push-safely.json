{
  "lessonKey": "git/05-remotes-and-collaboration/04-force-push-safely",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-force-options",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Name the safe force flag", "ru": "Назови безопасный флаг force" },
      "prompt": { "en": "Fill in why a normal push is rejected and the safe flag to override it.", "ru": "Заполни, почему обычный push отклонён, и безопасный флаг, чтобы это переопределить." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["non-fast-forward", "non fast forward", "не fast-forward", "не-fast-forward"], "hint": { "en": "The rejection reason when your rewritten branch no longer extends the remote.", "ru": "Причина отклонения, когда твоя переписанная ветка больше не продолжает удалёнку." } },
          { "id": "b2", "accept": ["--force-with-lease", "force-with-lease", "with-lease"], "hint": { "en": "The force flag that aborts if the remote moved since your last fetch.", "ru": "Флаг force, который прерывается, если удалёнка сдвинулась с твоего последнего fetch." } }
        ]
      },
      "scenario": { "en": "After a rebase, a plain push is rejected as (b1). The safe way to publish the rewrite is <code>git push (b2)</code>.", "ru": "После rebase обычный push отклоняется как (b1). Безопасный способ опубликовать перезапись — <code>git push (b2)</code>." }
    },
    {
      "id": "predict-amend-then-push",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Push after amend", "ru": "Push после amend" },
      "prompt": { "en": "Predict what happens when you push after amending an already-pushed commit.", "ru": "Предскажи, что произойдёт при push после amend уже запушенного коммита." },
      "scenario": { "en": "You pushed commit <code>O</code> to <code>origin/feature</code>. Then you run <code>git commit --amend</code> to fix the message and run a plain <code>git push</code>. What does git do?", "ru": "Ты запушил коммит <code>O</code> в <code>origin/feature</code>. Затем запускаешь <code>git commit --amend</code>, чтобы поправить сообщение, и обычный <code>git push</code>. Что сделает git?" },
      "reveal": { "en": "The push is rejected as non-fast-forward. <code>--amend</code> replaced <code>O</code> with a new commit <code>O'</code> that has a different hash; the remote still points at <code>O</code>, which your branch no longer contains, so it cannot fast-forward. To publish the amended commit you must force — and on a branch others might use, with <code>--force-with-lease</code> so the push aborts if someone pushed since your last fetch.", "ru": "Push отклонён как non-fast-forward. <code>--amend</code> заменил <code>O</code> новым коммитом <code>O'</code> с другим хешем; удалёнка всё ещё указывает на <code>O</code>, которого твоя ветка больше не содержит, поэтому fast-forward невозможен. Чтобы опубликовать исправленный коммит, нужен force — и на ветке, которой могут пользоваться другие, через <code>--force-with-lease</code>, чтобы push прервался, если кто-то запушил с твоего последнего fetch." }
    },
    {
      "id": "predict-lease-aborts",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Lease versus a teammate's commit", "ru": "Аренда против коммита коллеги" },
      "prompt": { "en": "Predict the outcome of each force command when a teammate has pushed.", "ru": "Предскажи исход каждой команды force, когда коллега запушил." },
      "scenario": { "en": "Your rebased <code>feature</code> ends at <code>R</code>. Your <code>origin/feature</code> still shows the old tip <code>O</code>. Unknown to you, a teammate pushed <code>T</code> on top of <code>O</code>, so the server is at <code>T</code>. Compare <code>git push --force</code> with <code>git push --force-with-lease</code>.", "ru": "Твоя ребейзнутая <code>feature</code> заканчивается на <code>R</code>. Твоя <code>origin/feature</code> всё ещё показывает старую вершину <code>O</code>. Без твоего ведома коллега запушил <code>T</code> поверх <code>O</code>, так что сервер на <code>T</code>. Сравни <code>git push --force</code> с <code>git push --force-with-lease</code>." },
      "reveal": { "en": "<code>--force</code> ignores the server and sets <code>feature</code> to <code>R</code>; the teammate's <code>T</code> becomes unreachable and is lost with no warning. <code>--force-with-lease</code> compares the server (<code>T</code>) against your remembered <code>origin/feature</code> (<code>O</code>); they differ, so the lease is stale and git aborts with 'stale info'. You then fetch, see <code>T</code>, rebase onto it, and push — keeping both your cleanup and their fix.", "ru": "<code>--force</code> игнорирует сервер и ставит <code>feature</code> на <code>R</code>; <code>T</code> коллеги становится недостижимым и теряется без предупреждения. <code>--force-with-lease</code> сравнивает сервер (<code>T</code>) с твоей запомненной <code>origin/feature</code> (<code>O</code>); они различаются, поэтому аренда устарела, и git прерывается с 'stale info'. Ты затем делаешь fetch, видишь <code>T</code>, ребейзишь поверх него и пушишь — сохраняя и свою чистку, и их правку." }
    },
    {
      "id": "review-force-guide",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Review a force-push guide", "ru": "Оцени руководство по force-push" },
      "prompt": { "en": "A teammate wrote a force-push guide. Find the wrong or dangerous claims.", "ru": "Коллега написал руководство по force-push. Найди неверные или опасные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git push --force and --force-with-lease are identical; both\ncheck the remote before overwriting. It is fine to force-push\nmain whenever your local copy looks right. If --force-with-lease\nis rejected, just run git fetch and force again to fix it."
      },
      "findings": [
        {
          "id": "force-has-no-check",
          "label": { "en": "--force does not check the remote", "ru": "--force не проверяет удалёнку" },
          "severity": "bug",
          "explanation": { "en": "--force overwrites unconditionally. Only --force-with-lease compares the remote against your remote-tracking ref and aborts on mismatch.", "ru": "--force перезаписывает безусловно. Только --force-with-lease сравнивает удалёнку с твоей удалённо-отслеживающей ссылкой и прерывается при несовпадении." },
          "planted": true
        },
        {
          "id": "never-force-main",
          "label": { "en": "Never force-push shared branches like main", "ru": "Никогда не форси общие ветки вроде main" },
          "severity": "bug",
          "explanation": { "en": "main is shared; forcing it rewrites history under everyone who pulled from it, breaking their clones. Force only your own feature/PR branches, and protect main server-side.", "ru": "main общая; её force переписывает историю под всеми, кто пуллил из неё, ломая их клоны. Форси только свои feature/PR-ветки, а main защити на стороне сервера." },
          "planted": true
        },
        {
          "id": "dont-defeat-lease",
          "label": { "en": "Fetch-then-force defeats the lease's protection", "ru": "Fetch-затем-force сводит на нет защиту аренды" },
          "severity": "bug",
          "explanation": { "en": "A rejected lease means someone pushed. Running git fetch refreshes origin/<branch> to their commit, so the next force-with-lease matches and overwrites exactly the commit it should have protected. Inspect the rejection instead of bypassing it.", "ru": "Отклонённая аренда означает, что кто-то запушил. git fetch обновляет origin/<branch> до их коммита, поэтому следующий force-with-lease совпадёт и перезапишет ровно тот коммит, который должен был защитить. Осмотри отклонение, а не обходи его." },
          "planted": true
        }
      ]
    }
  ]
}
