{
  "lessonKey": "git/05-remotes-and-collaboration/03-fetch-and-integrate",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-integrate-flags",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Name the integrate options", "ru": "Назови варианты интеграции" },
      "prompt": { "en": "Fill in the pull flag for a linear history and the flag that aborts when diverged.", "ru": "Заполни флаг pull для линейной истории и флаг, который прерывается при расхождении." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["--rebase", "rebase", "-r"], "hint": { "en": "Replays your commits on top of the remote for a straight line.", "ru": "Переигрывает твои коммиты поверх удалёнки ради прямой линии." } },
          { "id": "b2", "accept": ["--ff-only", "ff-only", "--ff only"], "hint": { "en": "Fast-forwards or fails; never creates a merge commit.", "ru": "Делает fast-forward или падает; никогда не создаёт merge-коммит." } }
        ]
      },
      "scenario": { "en": "For a linear history you run <code>git pull (b1)</code>. To refuse a merge and abort when the branch has diverged you run <code>git pull (b2)</code>.", "ru": "Ради линейной истории ты запускаешь <code>git pull (b1)</code>. Чтобы отказаться от merge и прерваться, когда ветка разошлась, ты запускаешь <code>git pull (b2)</code>." }
    },
    {
      "id": "predict-fetch-safety",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Can fetch lose your work?", "ru": "Может ли fetch потерять твою работу?" },
      "prompt": { "en": "Predict whether git fetch can ever conflict with or overwrite local changes.", "ru": "Предскажи, может ли git fetch конфликтовать с локальными изменениями или перезаписать их." },
      "scenario": { "en": "You have uncommitted edits in your working tree and two local commits not yet pushed. You run <code>git fetch origin</code>, and the server has five new commits on main. Can this fetch overwrite your edits or cause a merge conflict?", "ru": "У тебя есть незакоммиченные правки в рабочем дереве и два локальных коммита, ещё не запушенных. Ты запускаешь <code>git fetch origin</code>, а на сервере пять новых коммитов в main. Может ли этот fetch перезаписать твои правки или вызвать конфликт слияния?" },
      "reveal": { "en": "No. fetch only writes to remote-tracking branches like <code>origin/main</code>. It never touches your working tree, index, or local branches, so it cannot conflict or overwrite anything. After it, <code>origin/main</code> shows the five new commits and you can inspect them with <code>git log main..origin/main</code> before choosing how to integrate. Conflicts only become possible at the later merge or rebase step.", "ru": "Нет. fetch пишет только в удалённо-отслеживающие ветки вроде <code>origin/main</code>. Он никогда не трогает твоё рабочее дерево, индекс или локальные ветки, поэтому не может конфликтовать или что-то перезаписать. После него <code>origin/main</code> показывает пять новых коммитов, и их можно осмотреть через <code>git log main..origin/main</code>, прежде чем выбрать способ интеграции. Конфликты возможны лишь на более позднем шаге merge или rebase." }
    },
    {
      "id": "predict-rebase-vs-merge-shape",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Which history shape results?", "ru": "Какая форма истории получится?" },
      "prompt": { "en": "Predict the resulting history for merge versus rebase.", "ru": "Предскажи итоговую историю для merge против rebase." },
      "scenario": { "en": "You branched at <code>A</code> and committed <code>X</code>, <code>Y</code>. The team pushed <code>B</code>, <code>C</code> to origin/main. You fetch, then integrate. Describe the history after <code>git merge origin/main</code> versus after <code>git rebase origin/main</code>.", "ru": "Ты ответвился на <code>A</code> и закоммитил <code>X</code>, <code>Y</code>. Команда запушила <code>B</code>, <code>C</code> в origin/main. Ты делаешь fetch, затем интегрируешь. Опиши историю после <code>git merge origin/main</code> против после <code>git rebase origin/main</code>." },
      "reveal": { "en": "Merge: a merge commit <code>M</code> is created with two parents (<code>Y</code> and <code>C</code>); both lines stay visible and <code>X</code>, <code>Y</code> keep their original hashes — a bubble in <code>git log --graph</code>. Rebase: your branch moves to <code>C</code> and <code>X</code>, <code>Y</code> are re-applied as new commits <code>X'</code>, <code>Y'</code>, giving the straight line <code>A-B-C-X'-Y'</code> with no merge commit but rewritten hashes. Merge preserves true parallelism; rebase preserves linearity.", "ru": "Merge: создаётся merge-коммит <code>M</code> с двумя родителями (<code>Y</code> и <code>C</code>); обе линии остаются видимыми, а <code>X</code>, <code>Y</code> сохраняют исходные хеши — пузырь в <code>git log --graph</code>. Rebase: твоя ветка переходит на <code>C</code>, а <code>X</code>, <code>Y</code> заново накладываются как новые коммиты <code>X'</code>, <code>Y'</code>, давая прямую линию <code>A-B-C-X'-Y'</code> без merge-коммита, но с переписанными хешами. Merge сохраняет настоящую параллельность; rebase — линейность." }
    },
    {
      "id": "review-integration-advice",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Review an integration cheat-sheet", "ru": "Оцени шпаргалку по интеграции" },
      "prompt": { "en": "A teammate wrote an integration cheat-sheet. Find the incorrect claims.", "ru": "Коллега написал шпаргалку по интеграции. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git fetch integrates the remote commits into your current\nbranch automatically. Rebasing keeps your original commit\nhashes unchanged. It is always safe to rebase shared commits\nthat teammates have already pulled."
      },
      "findings": [
        {
          "id": "fetch-does-not-integrate",
          "label": { "en": "fetch does not integrate into your branch", "ru": "fetch не интегрирует в твою ветку" },
          "severity": "bug",
          "explanation": { "en": "fetch only updates remote-tracking branches (origin/*). Integration into your current branch happens at a separate merge or rebase step (which pull bundles in).", "ru": "fetch лишь обновляет удалённо-отслеживающие ветки (origin/*). Интеграция в твою текущую ветку происходит на отдельном шаге merge или rebase (который pull объединяет в себе)." },
          "planted": true
        },
        {
          "id": "rebase-rewrites-hashes",
          "label": { "en": "Rebase rewrites commits into new hashes", "ru": "Rebase переписывает коммиты в новые хеши" },
          "severity": "bug",
          "explanation": { "en": "Replaying commits onto a new base produces new commit objects with new hashes. The originals are replaced, which is exactly why rebase is unsafe for shared history.", "ru": "Переигрывание коммитов на новую базу создаёт новые объекты-коммиты с новыми хешами. Оригиналы заменяются — именно поэтому rebase опасен для расшаренной истории." },
          "planted": true
        },
        {
          "id": "no-rebase-shared",
          "label": { "en": "Do not rebase commits others already pulled", "ru": "Не ребейзь коммиты, которые другие уже забрали" },
          "severity": "bug",
          "explanation": { "en": "Rewriting shared commits diverges your history from teammates' clones, causing rejected or force-needed pushes that can orphan their work. Rebase only local, unshared commits.", "ru": "Переписывание расшаренных коммитов разводит твою историю с клонами коллег, вызывая отклонённые или требующие force push-и, способные осиротить их работу. Ребейзь только локальные, нерасшаренные коммиты." },
          "planted": true
        }
      ]
    }
  ]
}
