{
  "lessonKey": "git/05-remotes-and-collaboration/02-push-and-pull",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-upstream-and-pull",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Upstream and what pull is", "ru": "Upstream и что такое pull" },
      "prompt": { "en": "Fill in the flag that records an upstream and the command pull runs first.", "ru": "Заполни флаг, записывающий upstream, и команду, которую pull запускает первой." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["-u", "--set-upstream", "-u/--set-upstream"], "hint": { "en": "The short push flag that remembers the tracking branch.", "ru": "Короткий флаг push, запоминающий tracking-ветку." } },
          { "id": "b2", "accept": ["git fetch", "fetch"], "hint": { "en": "The command pull runs before it integrates.", "ru": "Команда, которую pull запускает перед интеграцией." } }
        ]
      },
      "scenario": { "en": "The first push of a branch can record an upstream with <code>git push (b1) origin feature</code>. A <code>git pull</code> is just (b2) followed by a merge.", "ru": "Первый push ветки может записать upstream через <code>git push (b1) origin feature</code>. <code>git pull</code> — это просто (b2), за которой следует merge." }
    },
    {
      "id": "predict-no-upstream",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Bare push on a fresh branch", "ru": "Голый push на свежей ветке" },
      "prompt": { "en": "Predict what a bare git push does on a branch that has never been pushed.", "ru": "Предскажи, что делает голый git push на ветке, которую ни разу не пушили." },
      "scenario": { "en": "You create <code>git switch -c feature</code>, make a commit, and run a bare <code>git push</code> with no arguments. The branch has never been pushed before. What happens?", "ru": "Ты создаёшь <code>git switch -c feature</code>, делаешь коммит и запускаешь голый <code>git push</code> без аргументов. Ветку раньше никогда не пушили. Что произойдёт?" },
      "reveal": { "en": "It fails with <code>fatal: The current branch feature has no upstream branch</code>. With no tracking relationship recorded, bare push does not know which remote/branch to target. Git even suggests the fix: <code>git push --set-upstream origin feature</code> (or <code>-u</code>), which pushes and records the upstream so future bare pushes work.", "ru": "Он падает с <code>fatal: The current branch feature has no upstream branch</code>. Без записанного отношения отслеживания голый push не знает, в какой remote/ветку целиться. Git даже подсказывает фикс: <code>git push --set-upstream origin feature</code> (или <code>-u</code>), который пушит и записывает upstream, чтобы будущие голые push-и работали." }
    },
    {
      "id": "predict-divergence",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Why this push is rejected", "ru": "Почему этот push отклонён" },
      "prompt": { "en": "Predict the outcome of the push and the right fix.", "ru": "Предскажи исход push и правильный фикс." },
      "scenario": { "en": "You and a teammate both branched from <code>origin/main</code> at <code>c10</code>. You committed <code>c11</code> locally. The teammate committed <code>c12</code> and already pushed it, so the server's main is at <code>c12</code>. You run <code>git push</code>. What happens, and what should you do?", "ru": "Ты и коллега оба ответвились от <code>origin/main</code> на <code>c10</code>. Ты закоммитил <code>c11</code> локально. Коллега закоммитил <code>c12</code> и уже запушил, так что main сервера на <code>c12</code>. Ты запускаешь <code>git push</code>. Что произойдёт и что делать?" },
      "reveal": { "en": "The push is rejected as <code>(non-fast-forward)</code>: moving the server's main from <code>c12</code> to your <code>c11</code> would drop the teammate's <code>c12</code>, which git will not do. Run <code>git pull</code> — it fetches <code>c12</code> and merges it with <code>c11</code> into a merge commit <code>c13</code> that contains both. Now your branch builds on <code>c12</code>, so <code>git push</code> fast-forwards and succeeds, preserving both commits.", "ru": "Push отклонён как <code>(non-fast-forward)</code>: сдвиг main сервера с <code>c12</code> на твой <code>c11</code> выкинул бы <code>c12</code> коллеги, чего git не сделает. Запусти <code>git pull</code> — он получает <code>c12</code> и сливает его с <code>c11</code> в merge-коммит <code>c13</code>, содержащий оба. Теперь твоя ветка строится поверх <code>c12</code>, поэтому <code>git push</code> fast-forward-ится и проходит, сохраняя оба коммита." }
    },
    {
      "id": "review-push-reflex",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Review a teammate's push habits", "ru": "Оцени привычки коллеги при push" },
      "prompt": { "en": "A teammate documented their push workflow. Find the wrong or dangerous claims.", "ru": "Коллега задокументировал свой push-воркфлоу. Найди неверные или опасные утверждения." },
      "diff": {
        "lang": "text",
        "code": "If git push is rejected with non-fast-forward, just run\ngit push --force to make it go through. git pull only\ndownloads commits and never changes your local branch.\ngit push uploads your edited files even before you commit them."
      },
      "findings": [
        {
          "id": "force-is-data-loss",
          "label": { "en": "Forcing past a normal rejection deletes others' commits", "ru": "Force после обычного отклонения удаляет чужие коммиты" },
          "severity": "bug",
          "explanation": { "en": "A non-fast-forward rejection means the remote has commits you lack. push --force overwrites the remote branch, erasing those commits. The correct response is to pull (or fetch + rebase) and push again.", "ru": "Отклонение non-fast-forward означает, что на удалёнке есть коммиты, которых нет у тебя. push --force перезаписывает ветку удалёнки, стирая эти коммиты. Правильный ответ — pull (или fetch + rebase) и push снова." },
          "planted": true
        },
        {
          "id": "pull-integrates",
          "label": { "en": "pull also integrates into your local branch", "ru": "pull ещё и интегрирует в твою локальную ветку" },
          "severity": "bug",
          "explanation": { "en": "git pull is fetch PLUS an integrate step (merge by default). It does move your current local branch by merging the remote's commits into it, not just download.", "ru": "git pull — это fetch ПЛЮС шаг интеграции (по умолчанию merge). Он двигает твою текущую локальную ветку, вливая в неё коммиты удалёнки, а не просто скачивает." },
          "planted": true
        },
        {
          "id": "push-needs-commit",
          "label": { "en": "push only sends committed work", "ru": "push отправляет только закоммиченное" },
          "severity": "bug",
          "explanation": { "en": "push uploads commits, not your uncommitted working-tree edits. Unstaged or uncommitted changes never leave your machine via push.", "ru": "push загружает коммиты, а не твои незакоммиченные правки в рабочем дереве. Неиндексированные или незакоммиченные изменения никогда не покидают машину через push." },
          "planted": true
        }
      ]
    }
  ]
}
