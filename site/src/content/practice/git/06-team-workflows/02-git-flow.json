{
  "lessonKey": "git/06-team-workflows/02-git-flow",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-branch-roles",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "The two long-lived branches", "ru": "Две долгоживущие ветки" },
      "prompt": { "en": "Fill in the blanks naming git-flow's two long-lived branches by their role.", "ru": "Заполни пропуски, называя две долгоживущие ветки git-flow по их роли." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["main", "master", "main ветка", "main-ветка"], "hint": { "en": "Holds only released, production code; commits are tagged versions.", "ru": "Держит только выпущенный продакшен-код; коммиты — помеченные тегами версии." } },
          { "id": "b2", "accept": ["develop", "develop ветка", "develop-ветка", "девелоп"], "hint": { "en": "The integration line where finished features accumulate for the next release.", "ru": "Линия интеграции, где готовые фичи копятся для следующего релиза." } }
        ]
      },
      "scenario": { "en": "Git-flow keeps two long-lived branches: (b1) holds released production code, and (b2) integrates finished features destined for the next release.", "ru": "Git-flow держит две долгоживущие ветки: (b1) держит выпущенный продакшен-код, а (b2) интегрирует готовые фичи, нацеленные на следующий релиз." }
    },
    {
      "id": "predict-skip-develop-merge",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "The forgotten second merge", "ru": "Забытое второе слияние" },
      "prompt": { "en": "Predict the consequence before revealing the answer.", "ru": "Предскажи последствие, прежде чем открыть ответ." },
      "scenario": { "en": "On <code>release/1.5.0</code> a developer fixes a release-only bug, then merges the release into <code>main</code> and tags <code>v1.5.0</code>. They delete the release branch but forget to merge it back into <code>develop</code>. What happens to that bug fix in the next release?", "ru": "На <code>release/1.5.0</code> разработчик чинит релизный баг, затем сливает релиз в <code>main</code> и тегирует <code>v1.5.0</code>. Он удаляет релизную ветку, но забывает слить её обратно в <code>develop</code>. Что станет с этой правкой бага в следующем релизе?" },
      "reveal": { "en": "The fix regresses. It now exists on <code>main</code> but never made it into <code>develop</code>, so the next release cut from <code>develop</code> will not contain it — the bug reappears in 1.6.0. This is why git-flow requires release and hotfix branches to merge into <em>both</em> <code>main</code> and <code>develop</code>: the double merge is what keeps production fixes from being silently dropped in the next development cycle.", "ru": "Правка регрессирует. Теперь она есть на <code>main</code>, но так и не попала в <code>develop</code>, поэтому следующий релиз, нарезанный из <code>develop</code>, её не будет содержать — баг вернётся в 1.6.0. Поэтому git-flow требует, чтобы релизные и hotfix-ветки сливались в <em>обе</em> — <code>main</code> и <code>develop</code>: двойное слияние не даёт продакшен-правкам молча потеряться в следующем цикле разработки." }
    },
    {
      "id": "predict-gitflow-for-webapp",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Git-flow on a CD web app", "ru": "Git-flow на CD-веб-приложении" },
      "prompt": { "en": "Decide whether git-flow is a good fit before revealing the answer.", "ru": "Реши, хорошо ли подходит git-flow, прежде чем открыть ответ." },
      "scenario": { "en": "A SaaS web team deploys to production on every merge, many times a day. There is no \"version 1.5.0\" — there is only what is live right now. Is git-flow a good fit for them?", "ru": "SaaS-команда деплоит в продакшен на каждом слиянии, много раз в день. Нет никакой «версии 1.5.0» — есть только то, что сейчас живое. Хорошо ли им подходит git-flow?" },
      "reveal": { "en": "No. Git-flow's <code>develop</code> → <code>release</code> → <code>main</code> ceremony and its two long-lived branches add latency and constant cross-branch merging that buy nothing when there are no versioned releases. Driessen himself noted git-flow targets versioned software; a continuously-deployed web app wants a single deployable branch (GitHub flow or trunk-based) instead.", "ru": "Нет. Церемония git-flow <code>develop</code> → <code>release</code> → <code>main</code> и его две долгоживущие ветки добавляют задержку и постоянное межветочное слияние, которые ничего не дают, когда версионных релизов нет. Сам Дриссен отметил, что git-flow нацелен на версионный софт; непрерывно деплоящемуся веб-приложению нужна одна деплоибельная ветка (GitHub flow или trunk-based)." }
    },
    {
      "id": "review-gitflow-commands",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Review a git-flow runbook", "ru": "Ревью инструкции git-flow" },
      "prompt": { "en": "A teammate wrote this git-flow runbook. Find the incorrect claims.", "ru": "Коллега написал эту инструкцию по git-flow. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "To start a feature, branch off main and merge it straight back\ninto main when done. Hotfixes should branch off develop. A release\nbranch only ever needs to merge into main, never back into develop.\nThe git flow helper commands are mandatory; raw git cannot do this."
      },
      "findings": [
        {
          "id": "feature-off-develop",
          "label": { "en": "Features branch off develop, not main", "ru": "Фичи ответвляются от develop, а не от main" },
          "severity": "bug",
          "explanation": { "en": "In git-flow features start from develop and merge back into develop. Production code reaches main only through a release branch — features never touch main directly.", "ru": "В git-flow фичи стартуют с develop и сливаются обратно в develop. Продакшен-код достигает main только через релизную ветку — фичи никогда не трогают main напрямую." },
          "planted": true
        },
        {
          "id": "hotfix-off-main",
          "label": { "en": "Hotfixes branch off main, not develop", "ru": "Хотфиксы ответвляются от main, а не от develop" },
          "severity": "bug",
          "explanation": { "en": "A hotfix must patch the exact code in production, which lives on main. Branching off develop would pull in unreleased, half-finished features.", "ru": "Хотфикс должен патчить точный код в продакшене, который живёт на main. Ответвление от develop затащило бы невыпущенные, наполовину готовые фичи." },
          "planted": true
        },
        {
          "id": "release-merges-both",
          "label": { "en": "A release branch merges into BOTH main and develop", "ru": "Релизная ветка сливается в ОБЕ — main и develop" },
          "severity": "bug",
          "explanation": { "en": "Merging the release only into main drops release-only fixes from the next cycle. The release must also merge back into develop so those fixes survive.", "ru": "Слияние релиза только в main теряет релизные правки из следующего цикла. Релиз должен также слиться обратно в develop, чтобы эти правки выжили." },
          "planted": true
        },
        {
          "id": "helper-not-mandatory",
          "label": { "en": "The git flow helper is optional, not mandatory", "ru": "Хелпер git flow необязателен, а не обязателен" },
          "severity": "simplification",
          "explanation": { "en": "git-flow is a convention; every step is plain git checkout/merge/tag. The helper just automates the sequences — raw git does all of it.", "ru": "git-flow — это соглашение; каждый шаг — обычные git checkout/merge/tag. Хелпер лишь автоматизирует последовательности — сырой git делает всё это." },
          "planted": true
        }
      ]
    }
  ]
}
