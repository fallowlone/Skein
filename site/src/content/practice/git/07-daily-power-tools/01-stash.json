{
  "lessonKey": "git/07-daily-power-tools/01-stash",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-stash-commands",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "The right stash verb", "ru": "Правильный глагол stash" },
      "prompt": { "en": "Fill in the blanks with the correct stash subcommands.", "ru": "Заполни пропуски правильными подкомандами stash." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["pop", "поп"], "hint": { "en": "Reapplies the top stash AND removes it from the stack.", "ru": "Возвращает верхнюю заначку И удаляет её из стека." } },
          { "id": "b2", "accept": ["apply", "эплай", "применить"], "hint": { "en": "Reapplies the stash but leaves the entry in place for reuse.", "ru": "Возвращает заначку, но оставляет запись на месте для повторного использования." } }
        ]
      },
      "scenario": { "en": "To resume your only stashed change and clear the stack, run <code>git stash (b1)</code>. To reapply the same change on two branches in turn, run <code>git stash (b2)</code> so the entry survives the first reapply.", "ru": "Чтобы продолжить единственное спрятанное изменение и очистить стек, выполни <code>git stash (b1)</code>. Чтобы применить то же изменение по очереди на двух ветках, выполни <code>git stash (b2)</code>, чтобы запись пережила первый возврат." }
    },
    {
      "id": "predict-untracked-left-behind",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Did the new file get stashed?", "ru": "Спрятался ли новый файл?" },
      "prompt": { "en": "Predict the state of the working tree after the stash.", "ru": "Предскажи состояние рабочего дерева после stash." },
      "scenario": { "en": "You edited the tracked file <code>app.js</code> and also created a brand-new, never-added file <code>notes.tmp</code>. You run a plain <code>git stash</code> and then <code>git status</code>. Is the tree fully clean?", "ru": "Ты изменил отслеживаемый файл <code>app.js</code> и ещё создал совершенно новый, никогда не добавлявшийся файл <code>notes.tmp</code>. Ты выполняешь обычный <code>git stash</code>, затем <code>git status</code>. Дерево полностью чистое?" },
      "reveal": { "en": "No. Plain <code>git stash</code> only shelves tracked changes, so <code>app.js</code> is parked but the untracked <code>notes.tmp</code> stays right where it is — <code>git status</code> still lists it under untracked files. To sweep it in too you needed <code>git stash -u</code> (<code>--include-untracked</code>). This is exactly why a stash sometimes still blocks a branch switch.", "ru": "Нет. Обычный <code>git stash</code> прячет только отслеживаемые изменения, поэтому <code>app.js</code> припаркован, а неотслеживаемый <code>notes.tmp</code> остаётся на месте — <code>git status</code> по-прежнему показывает его среди неотслеживаемых файлов. Чтобы захватить и его, нужен был <code>git stash -u</code> (<code>--include-untracked</code>). Именно поэтому заначка иногда всё равно мешает переключению ветки." }
    },
    {
      "id": "predict-conflicting-pop",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "A pop that conflicts", "ru": "Конфликтующий pop" },
      "prompt": { "en": "Predict what happens to the stash entry when pop hits a conflict.", "ru": "Предскажи, что станет с записью заначки, если pop наткнётся на конфликт." },
      "scenario": { "en": "You stashed a change to <code>config.js</code>, then a teammate's commit you pulled also edited <code>config.js</code>. You run <code>git stash pop</code> and git reports a merge conflict. After you finish, is the stash entry gone like a normal pop?", "ru": "Ты спрятал изменение в <code>config.js</code>, затем коммит коллеги, который ты подтянул, тоже правил <code>config.js</code>. Ты выполняешь <code>git stash pop</code>, и git сообщает о конфликте слияния. Когда закончишь, пропадёт ли запись заначки, как при обычном pop?" },
      "reveal": { "en": "No — a conflicting <code>pop</code> intentionally KEEPS the stash entry so you cannot lose it mid-conflict. You resolve the conflict, <code>git add</code> the files, then run <code>git stash drop</code> yourself to clear the leftover entry. A clean pop drops automatically; a conflicting one does not.", "ru": "Нет — конфликтный <code>pop</code> намеренно ОСТАВЛЯЕТ запись заначки, чтобы ты не потерял её посреди конфликта. Ты разрешаешь конфликт, делаешь <code>git add</code> файлов, затем сам выполняешь <code>git stash drop</code>, чтобы убрать оставшуюся запись. Чистый pop удаляет автоматически; конфликтный — нет." }
    },
    {
      "id": "review-stash-as-storage",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Spot the stash misconceptions", "ru": "Найди заблуждения о stash" },
      "prompt": { "en": "A colleague wrote this note about git stash. Find the incorrect claims.", "ru": "Коллега написал эту заметку о git stash. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git stash is a safe place for long-term work: it gets pushed to\nthe remote with your next push, so teammates can see it too.\nA plain git stash also captures every untracked file automatically,\nso a new file is always swept in."
      },
      "findings": [
        {
          "id": "stash-is-local",
          "label": { "en": "Stashes are local and never pushed", "ru": "Заначки локальны и никогда не пушатся" },
          "severity": "bug",
          "explanation": { "en": "A stash is not part of any commit and is never sent to a remote. Teammates cannot see it, and it survives only on your machine — use a branch commit for anything that must be shared or kept long.", "ru": "Заначка не является частью какого-либо коммита и никогда не отправляется на удалённый сервер. Коллеги её не видят, и живёт она только на твоей машине — для всего, что нужно делить или хранить долго, используй коммит на ветке." },
          "planted": true
        },
        {
          "id": "untracked-not-default",
          "label": { "en": "Untracked files are not stashed by default", "ru": "Неотслеживаемые файлы по умолчанию не прячутся" },
          "severity": "bug",
          "explanation": { "en": "Plain git stash skips untracked and ignored files. You must pass -u (--include-untracked) to capture new files, or -a (--all) to also include ignored ones.", "ru": "Обычный git stash пропускает неотслеживаемые и игнорируемые файлы. Нужно передать -u (--include-untracked), чтобы захватить новые файлы, или -a (--all), чтобы включить и игнорируемые." },
          "planted": true
        }
      ]
    }
  ]
}
