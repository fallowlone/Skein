{
  "lessonKey": "git/07-daily-power-tools/04-worktrees",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-worktree-commands",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Add and clean up a worktree", "ru": "Добавить и убрать рабочее дерево" },
      "prompt": { "en": "Fill in the blanks for creating and removing a second working tree.", "ru": "Заполни пропуски для создания и удаления второго рабочего дерева." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["add", "добавить"], "hint": { "en": "The subcommand that creates a new working tree.", "ru": "Подкоманда, создающая новое рабочее дерево." } },
          { "id": "b2", "accept": ["remove", "удалить"], "hint": { "en": "The subcommand that deletes a worktree and its admin entry.", "ru": "Подкоманда, удаляющая рабочее дерево и его админ-запись." } }
        ]
      },
      "scenario": { "en": "To review a branch in a separate folder you run <code>git worktree (b1) ../review feature/pr</code>. When finished, you tidy up with <code>git worktree (b2) ../review</code> rather than deleting the folder by hand.", "ru": "Чтобы отревьюить ветку в отдельной папке, ты выполняешь <code>git worktree (b1) ../review feature/pr</code>. Закончив, ты убираешь за собой через <code>git worktree (b2) ../review</code>, а не удаляя папку руками." }
    },
    {
      "id": "predict-branch-already-checked-out",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Same branch, second tree", "ru": "Та же ветка, второе дерево" },
      "prompt": { "en": "Predict what git does when you add a worktree on an already-checked-out branch.", "ru": "Предскажи, что сделает git, когда добавишь рабочее дерево на уже извлечённой ветке." },
      "scenario": { "en": "Your main folder is on <code>main</code>. You want a separate worktree to build <code>main</code> in parallel, so you run <code>git worktree add ../build main</code>. What happens?", "ru": "Твоя основная папка на <code>main</code>. Ты хочешь отдельное рабочее дерево, чтобы собирать <code>main</code> параллельно, поэтому выполняешь <code>git worktree add ../build main</code>. Что происходит?" },
      "reveal": { "en": "Git refuses with <code>fatal: 'main' is already checked out at ...</code>. A branch can live in only one worktree at a time, because all worktrees share the same branch pointers. To build off main in a new tree, create a branch from it: <code>git worktree add -b build-main ../build main</code>, which makes a fresh branch starting at main and checks it out there.", "ru": "Git отказывает с <code>fatal: 'main' is already checked out at ...</code>. Ветка может жить только в одном рабочем дереве за раз, потому что все рабочие деревья разделяют одни указатели веток. Чтобы собирать от main в новом дереве, создай из него ветку: <code>git worktree add -b build-main ../build main</code>, что создаёт свежую ветку, начинающуюся на main, и извлекает её там." }
    },
    {
      "id": "predict-commit-visible-across",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "A commit in one tree", "ru": "Коммит в одном дереве" },
      "prompt": { "en": "Predict whether a commit made in one worktree is visible in another.", "ru": "Предскажи, виден ли коммит, сделанный в одном рабочем дереве, в другом." },
      "scenario": { "en": "You have two worktrees of the same repo. In <code>../review</code> you commit on <code>feature/x</code>. Back in your main folder you run <code>git log feature/x</code>. Does the new commit show up immediately, without any push or fetch?", "ru": "У тебя два рабочих дерева одного репозитория. В <code>../review</code> ты коммитишь на <code>feature/x</code>. Вернувшись в основную папку, ты выполняешь <code>git log feature/x</code>. Появляется ли новый коммит сразу, без всякого push или fetch?" },
      "reveal": { "en": "Yes, immediately. All worktrees share one .git object store and the same branch pointers, so a commit in one is part of the single repository every tree reads from — no push or fetch is needed (those are for moving work between separate clones or remotes). This shared state is a key advantage worktrees have over a second clone.", "ru": "Да, сразу. Все рабочие деревья разделяют одно хранилище объектов .git и одни указатели веток, поэтому коммит в одном — часть единого репозитория, из которого читает каждое дерево — никаких push или fetch не нужно (они для переноса работы между отдельными клонами или серверами). Это общее состояние — ключевое преимущество рабочих деревьев перед вторым клоном." }
    },
    {
      "id": "review-worktree-note",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Audit the worktree note", "ru": "Проверь заметку о worktree" },
      "prompt": { "en": "A teammate described git worktree like this. Find the incorrect claims.", "ru": "Коллега так описал git worktree. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "Each worktree makes a full independent copy of the repository's\nhistory, just like a second clone, so they cost the same disk.\nTwo worktrees can sit on the exact same branch at once.\nTo delete one, just rm -rf its folder; git tracks nothing extra."
      },
      "findings": [
        {
          "id": "shared-not-copied",
          "label": { "en": "Worktrees share one object store, not separate copies", "ru": "Рабочие деревья разделяют одно хранилище, не копии" },
          "severity": "bug",
          "explanation": { "en": "Worktrees share a single .git object store; only the checked-out files are separate. That is why they are far cheaper in disk and time than a second clone.", "ru": "Рабочие деревья разделяют единое хранилище объектов .git; отдельны только извлечённые файлы. Поэтому они куда дешевле по диску и времени, чем второй клон." },
          "planted": true
        },
        {
          "id": "one-branch-one-tree",
          "label": { "en": "A branch can be checked out in only one worktree", "ru": "Ветку можно извлечь только в одном рабочем дереве" },
          "severity": "bug",
          "explanation": { "en": "Git forbids checking out the same branch in two worktrees at once, since they share branch pointers. Use -b to branch from it instead.", "ru": "Git запрещает извлекать одну ветку в двух рабочих деревьях сразу, так как они разделяют указатели веток. Используй -b, чтобы ответвиться от неё." },
          "planted": true
        },
        {
          "id": "use-remove-not-rmrf",
          "label": { "en": "Removing by rm -rf leaves stale bookkeeping", "ru": "Удаление через rm -rf оставляет устаревший учёт" },
          "severity": "bug",
          "explanation": { "en": "Git keeps an admin record per worktree. Deleting the folder by hand leaves a stale entry; use git worktree remove, or git worktree prune to clean up.", "ru": "Git ведёт админ-запись на каждое рабочее дерево. Удаление папки руками оставляет устаревшую запись; используй git worktree remove или git worktree prune для очистки." },
          "planted": true
        }
      ]
    }
  ]
}
