{
  "lessonKey": "git/10-putting-it-together/02-incident-a-bad-merge",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-revert-merge",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": {
        "en": "Undo a shared merge",
        "ru": "Отменить общий merge"
      },
      "prompt": {
        "en": "Fill in the blanks for undoing a merge that is already on origin/main.",
        "ru": "Заполни пропуски для отмены merge, который уже на origin/main."
      },
      "grading": {
        "mode": "blanks",
        "blanks": [
          {
            "id": "b1",
            "accept": [
              "revert",
              "git revert"
            ],
            "hint": {
              "en": "The verb that appends an undo commit instead of rewriting history.",
              "ru": "Глагол, дописывающий коммит-отмену вместо переписывания истории."
            }
          },
          {
            "id": "b2",
            "accept": [
              "-m 1",
              "m 1",
              "-m1",
              "1"
            ],
            "hint": {
              "en": "The flag and value naming the mainline (first) parent to keep.",
              "ru": "Флаг и значение, называющие магистрального (первого) родителя, которого оставить."
            }
          }
        ]
      },
      "scenario": {
        "en": "The merge is on shared origin/main, so you must append, not rewrite. You run <code>git (b1) (b2) &lt;merge-sha&gt;</code>, which keeps main as the baseline and undoes the merged branch's changes.",
        "ru": "Merge на общем origin/main, поэтому нужно дописать, а не переписать. Ты запускаешь <code>git (b1) (b2) &lt;merge-sha&gt;</code>, что оставляет main базой и отменяет изменения смёрженной ветки."
      }
    },
    {
      "id": "predict-reset-on-shared",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": {
        "en": "Reset + force on a shared branch",
        "ru": "Reset + force на общей ветке"
      },
      "prompt": {
        "en": "Predict the fallout of resetting and force-pushing main during an incident.",
        "ru": "Предскажи последствия reset и force-push в main во время инцидента."
      },
      "scenario": {
        "en": "To kill a bad merge on <code>origin/main</code> that three teammates already fetched, an engineer runs <code>git reset --hard HEAD~1</code> then <code>git push --force origin main</code>. What happens to the team?",
        "ru": "Чтобы убить плохой merge на <code>origin/main</code>, который трое коллег уже стянули, инженер запускает <code>git reset --hard HEAD~1</code>, затем <code>git push --force origin main</code>. Что происходит с командой?"
      },
      "reveal": {
        "en": "Every clone that fetched the merge now disagrees with the rewritten <code>origin/main</code>. Teammates' next pull either re-introduces the bad merge through a phantom merge commit or reports diverged branches, and anyone who branched off the erased range is stranded. Force-rewriting shared history multiplies the incident. The correct move was <code>git revert -m 1</code>: an ordinary commit that undoes the merge without touching anyone else's history.",
        "ru": "Каждый клон, стянувший merge, теперь расходится с переписанным <code>origin/main</code>. Следующий pull коллег либо снова вносит плохой merge через фантомный merge-коммит, либо сообщает о разошедшихся ветках, а любой, кто ответвился от стёртого диапазона, оказывается брошен. Force-переписывание общей истории умножает инцидент. Правильный ход был <code>git revert -m 1</code>: обычный коммит, отменяющий merge без касания чужой истории."
      }
    },
    {
      "id": "predict-reflog-recovery",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": {
        "en": "Recovering force-pushed commits",
        "ru": "Восстановление force-push коммитов"
      },
      "prompt": {
        "en": "Predict whether commits erased by a force-push are truly lost.",
        "ru": "Предскажи, действительно ли коммиты, стёртые force-push, потеряны."
      },
      "scenario": {
        "en": "A force-push wiped three good commits off <code>origin/main</code>. Nobody saved their SHAs. A colleague's machine still has the old tip in its clone. Are those commits recoverable, and how?",
        "ru": "Force-push стёр три хороших коммита с <code>origin/main</code>. Никто не сохранил их SHA. На машине коллеги в клоне всё ещё есть старая верхушка. Восстановимы ли эти коммиты, и как?"
      },
      "reveal": {
        "en": "Yes. The commits are unreferenced, not deleted, and the colleague's <code>git reflog</code> (and <code>git reflog show origin/main</code>) still names the pre-force-push SHA. On their machine: find the last good SHA, <code>git branch rescue &lt;sha&gt;</code>, then push that branch. The lost work is restored. Reflog is the per-clone safety net that makes force-push mistakes recoverable rather than fatal.",
        "ru": "Да. Коммиты без ссылок, а не удалены, и <code>git reflog</code> коллеги (и <code>git reflog show origin/main</code>) всё ещё называет SHA до force-push. На его машине: найди последний хороший SHA, <code>git branch rescue &lt;sha&gt;</code>, затем запушь эту ветку. Потерянная работа восстановлена. Reflog — это страховка на каждый клон, делающая ошибки force-push восстановимыми, а не фатальными."
      }
    },
    {
      "id": "review-incident-runbook",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": {
        "en": "Audit the incident runbook",
        "ru": "Проверь runbook инцидента"
      },
      "prompt": {
        "en": "A junior wrote a bad-merge runbook. Find the incorrect steps.",
        "ru": "Джун написал runbook для плохого merge. Найди неверные шаги."
      },
      "diff": {
        "lang": "text",
        "code": "When a bad merge hits main and CI is red:\n1. Confirm the merge with git show; it has two parents.\n2. Since main is shared, git reset --hard before the merge and\n   force-push to clean it up.\n3. To re-merge the fixed branch later, just merge it again —\n   git will pick up the new changes normally."
      },
      "findings": [
        {
          "id": "step2-should-be-revert",
          "label": {
            "en": "Step 2 must be revert, not reset+force",
            "ru": "Шаг 2 должен быть revert, не reset+force"
          },
          "severity": "bug",
          "explanation": {
            "en": "main is shared, so the rule is append-don't-rewrite: use git revert -m 1 <merge-sha>. reset --hard + force-push rewrites history every teammate already fetched, desyncing all clones and stranding anyone who branched off it.",
            "ru": "main общий, поэтому правило — дописывать, не переписывать: используй git revert -m 1 <merge-sha>. reset --hard + force-push переписывает историю, которую каждый коллега уже стянул, рассинхронизируя все клоны и бросая всех, кто от неё ответвился."
          },
          "planted": true
        },
        {
          "id": "step3-revert-of-revert",
          "label": {
            "en": "Step 3 ignores the revert-of-a-revert trap",
            "ru": "Шаг 3 игнорирует ловушку revert-of-a-revert"
          },
          "severity": "bug",
          "explanation": {
            "en": "Once a merge is reverted, git considers that branch already integrated, so re-merging it is a no-op and brings in nothing. You must first revert the revert (or rebase the branch onto the new main) to re-land the fixed feature.",
            "ru": "Раз merge откачен, git считает ветку уже интегрированной, поэтому повторный merge — no-op и ничего не приносит. Сначала нужно откатить revert (или сделать rebase ветки на новый main), чтобы заново влить починенную фичу."
          },
          "planted": true
        }
      ]
    }
  ]
}
