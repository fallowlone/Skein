{
  "lessonKey": "git/08-under-the-hood/04-hooks",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-hook-roles",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Which hook for which job", "ru": "Какой хук для какой задачи" },
      "prompt": { "en": "Fill in the blanks with the right hook names.", "ru": "Заполни пропуски правильными именами хуков." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["pre-commit", "precommit"], "hint": { "en": "Runs before the commit is created; ideal for fast linting.", "ru": "Запускается перед созданием коммита; идеален для быстрого линтинга." } },
          { "id": "b2", "accept": ["commit-msg", "commitmsg", "commit msg"], "hint": { "en": "Receives the message file path; used to enforce a message format.", "ru": "Получает путь к файлу сообщения; используется для навязывания формата сообщения." } }
        ]
      },
      "scenario": { "en": "To lint staged files before a commit exists, use the (b1) hook. To reject a commit whose message lacks a Conventional Commits prefix, use the (b2) hook.", "ru": "Чтобы линтить staged-файлы до того, как коммит существует, используй хук (b1). Чтобы отклонить коммит, в чьём сообщении нет префикса Conventional Commits, используй хук (b2)." }
    },
    {
      "id": "predict-exit-code",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "What a non-zero exit does", "ru": "Что делает ненулевой выход" },
      "prompt": { "en": "Decide what happens to the commit before revealing the answer.", "ru": "Реши, что станет с коммитом, прежде чем открыть ответ." },
      "scenario": { "en": "Your <code>pre-commit</code> hook runs the linter, which finds an error, so the hook script ends with <code>exit 1</code>. You run <code>git commit -m \"add feature\"</code>. What happens?", "ru": "Твой хук <code>pre-commit</code> запускает линтер, который находит ошибку, поэтому скрипт хука завершается через <code>exit 1</code>. Ты выполняешь <code>git commit -m \"add feature\"</code>. Что происходит?" },
      "reveal": { "en": "The commit is aborted — no commit object is created. Git runs the pre-commit hook first and checks its exit status; any non-zero code means 'do not proceed', so git stops before writing the commit. Your staged changes stay staged, nothing is recorded, and you fix the lint error and try again. (You could force past it with <code>git commit --no-verify</code>, which skips client-side hooks entirely.)", "ru": "Коммит прерывается — объект-коммит не создаётся. Git сначала запускает хук pre-commit и проверяет его код выхода; любой ненулевой код означает «не продолжать», поэтому git останавливается до записи коммита. Твои staged-изменения остаются в staging, ничего не записывается, и ты исправляешь ошибку линтинга и пробуешь снова. (Можно продавить через <code>git commit --no-verify</code>, который вообще пропускает клиентские хуки.)" }
    },
    {
      "id": "predict-shared-hook",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Will the team get your hook?", "ru": "Получит ли команда твой хук?" },
      "prompt": { "en": "Decide whether committing a hook into .git/hooks shares it with the team.", "ru": "Реши, делится ли хук с командой, если положить его в .git/hooks." },
      "scenario": { "en": "You create <code>.git/hooks/pre-push</code> that runs the test suite, then do a normal <code>git add . && git commit && git push</code>. A colleague clones the repo fresh. Do they get your pre-push hook?", "ru": "Ты создаёшь <code>.git/hooks/pre-push</code>, запускающий набор тестов, затем делаешь обычные <code>git add . && git commit && git push</code>. Коллега клонирует репозиторий заново. Получит ли он твой хук pre-push?" },
      "reveal": { "en": "No. The <code>.git</code> directory is local repository metadata — it is never part of the tracked tree, so <code>git add .</code> cannot stage anything inside it, and clone does not copy it. Your colleague gets none of the hooks. To share them, move the scripts to a tracked folder (e.g. <code>.githooks/</code>) and set <code>core.hooksPath</code> to it — which is exactly what husky and lefthook automate so a fresh <code>npm install</code> wires the hooks up.", "ru": "Нет. Каталог <code>.git</code> — это локальные метаданные репозитория; он никогда не входит в отслеживаемое дерево, поэтому <code>git add .</code> не может добавить ничего внутри него, а клон его не копирует. Коллега не получает ни одного хука. Чтобы поделиться ими, перенеси скрипты в отслеживаемую папку (напр. <code>.githooks/</code>) и укажи на неё <code>core.hooksPath</code> — ровно это автоматизируют husky и lefthook, чтобы свежий <code>npm install</code> подключал хуки." }
    },
    {
      "id": "review-hooks-claims",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the wrong claims", "ru": "Найди неверные утверждения" },
      "prompt": { "en": "A teammate wrote this note about git hooks. Find the incorrect claims.", "ru": "Коллега написал эту заметку о хуках git. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "Git hooks are committed with the repo, so every clone runs them\nautomatically. A pre-commit hook that exits with code 1 still lets\nthe commit through, just with a warning. Client-side hooks cannot\nbe bypassed, so they are a reliable security boundary."
      },
      "findings": [
        {
          "id": "hooks-not-committed",
          "label": { "en": "Hooks in .git/hooks are not committed or cloned", "ru": "Хуки в .git/hooks не коммитятся и не клонируются" },
          "severity": "bug",
          "explanation": { "en": "Hooks live in local .git metadata, which clone does not copy. Sharing them requires a tracked directory plus core.hooksPath, which is why husky/lefthook exist.", "ru": "Хуки живут в локальных метаданных .git, которые клон не копирует. Чтобы поделиться ими, нужен отслеживаемый каталог плюс core.hooksPath, — поэтому и существуют husky/lefthook." },
          "planted": true
        },
        {
          "id": "nonzero-aborts",
          "label": { "en": "A non-zero exit aborts the commit, it doesn't warn", "ru": "Ненулевой выход прерывает коммит, а не предупреждает" },
          "severity": "bug",
          "explanation": { "en": "Exit 1 from pre-commit stops the operation outright — no commit object is created. Exit 0 is the only status that lets the commit proceed.", "ru": "Выход 1 из pre-commit полностью останавливает операцию — объект-коммит не создаётся. Выход 0 — единственный статус, который пропускает коммит." },
          "planted": true
        },
        {
          "id": "hooks-bypassable",
          "label": { "en": "Client hooks are bypassable, not a security boundary", "ru": "Клиентские хуки обходимы, это не граница безопасности" },
          "severity": "bug",
          "explanation": { "en": "git commit/push --no-verify skips client hooks, and a contributor without them installed never runs them. Mandatory rules belong in server-side hooks or CI.", "ru": "git commit/push --no-verify пропускает клиентские хуки, а участник без них вообще их не запускает. Обязательные правила — это серверные хуки или CI." },
          "planted": true
        }
      ]
    }
  ]
}
