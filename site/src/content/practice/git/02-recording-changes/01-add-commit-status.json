{
  "lessonKey": "git/02-recording-changes/01-add-commit-status",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-three-places",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Name the two commands", "ru": "Назови две команды" },
      "prompt": { "en": "Fill in the commands that move a change through the git loop.", "ru": "Заполни команды, которые продвигают изменение по циклу git." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["git add", "add", "git add ."], "hint": { "en": "Copies a change from the working tree into the staging area.", "ru": "Копирует изменение из рабочего дерева в область индекса." } },
          { "id": "b2", "accept": ["git commit", "commit", "git commit -m"], "hint": { "en": "Records whatever is staged as a new commit.", "ru": "Записывает то, что проиндексировано, как новый коммит." } }
        ]
      },
      "scenario": { "en": "A change starts in the working tree. You run (b1) to put it in the staging area, then (b2) to record it permanently in the repository.", "ru": "Изменение начинается в рабочем дереве. Ты запускаешь (b1), чтобы положить его в область индекса, затем (b2), чтобы записать его навсегда в репозиторий." }
    },
    {
      "id": "predict-commit-am-newfile",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "What does -am skip?", "ru": "Что пропускает -am?" },
      "prompt": { "en": "Predict which files land in the commit before revealing the answer.", "ru": "Предскажи, какие файлы попадут в коммит, прежде чем открыть ответ." },
      "scenario": { "en": "Your repo already tracks <code>app.js</code>. You edit <code>app.js</code> and also create a new file <code>config.js</code>. Without running <code>git add</code>, you run <code>git commit -am 'Wire up config'</code>. Which of the two files is recorded in the commit?", "ru": "Твой репозиторий уже отслеживает <code>app.js</code>. Ты правишь <code>app.js</code> и ещё создаёшь новый файл <code>config.js</code>. Не запуская <code>git add</code>, ты выполняешь <code>git commit -am 'Wire up config'</code>. Какой из двух файлов попадёт в коммит?" },
      "reveal": { "en": "Only <code>app.js</code>. The <code>-a</code> flag auto-stages modifications to files git already tracks, so the edit to <code>app.js</code> is included. But <code>config.js</code> is brand-new and untracked, so <code>-a</code> ignores it entirely — the commit silently leaves it out. To include it you must <code>git add config.js</code> first. Always run <code>git status</code> after a commit to catch this.", "ru": "Только <code>app.js</code>. Флаг <code>-a</code> авто-индексирует изменения файлов, которые git уже отслеживает, поэтому правка <code>app.js</code> войдёт. Но <code>config.js</code> совсем новый и неотслеживаемый, так что <code>-a</code> его полностью игнорирует — коммит молча оставляет его вне. Чтобы включить, сначала сделай <code>git add config.js</code>. Всегда запускай <code>git status</code> после коммита, чтобы поймать это." }
    },
    {
      "id": "predict-stage-subset",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Staging a subset", "ru": "Индексируем подмножество" },
      "prompt": { "en": "Predict what ends up in the commit when only some files are staged.", "ru": "Предскажи, что окажется в коммите, когда проиндексированы только некоторые файлы." },
      "scenario": { "en": "You edited <code>a.js</code>, <code>b.js</code>, and <code>c.js</code>. You run <code>git add a.js b.js</code> and then <code>git commit -m 'Fix a and b'</code>. Afterward, what is the state of <code>c.js</code>?", "ru": "Ты правил <code>a.js</code>, <code>b.js</code> и <code>c.js</code>. Ты запускаешь <code>git add a.js b.js</code>, затем <code>git commit -m 'Fix a and b'</code>. Каким будет состояние <code>c.js</code> после этого?" },
      "reveal": { "en": "<code>c.js</code> is unchanged in your working tree and was NOT recorded in the commit. Commit captures only what is staged, so <code>a.js</code> and <code>b.js</code> went into history while your edit to <code>c.js</code> still sits in the working tree as a 'change not staged for commit', waiting for a separate commit. This is exactly how you slice one messy editing session into clean, single-purpose commits.", "ru": "<code>c.js</code> не изменён в рабочем дереве и НЕ был записан в коммит. Коммит фиксирует только проиндексированное, поэтому <code>a.js</code> и <code>b.js</code> ушли в историю, а твоя правка <code>c.js</code> всё ещё лежит в рабочем дереве как «изменение не проиндексировано», ожидая отдельного коммита. Именно так один грязный сеанс правок нарезается на чистые односмысловые коммиты." }
    },
    {
      "id": "review-commit-workflow",
      "type": "review",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Review a workflow note", "ru": "Разбери заметку о рабочем процессе" },
      "prompt": { "en": "A teammate documented the git basics. Find the incorrect claims.", "ru": "Коллега задокументировал основы git. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git status only shows tracked files, never new ones.\ngit add . is always the safest choice because it stages exactly\nthe files you intend. A plain git commit records every changed\nfile on disk whether or not you staged it."
      },
      "findings": [
        {
          "id": "status-shows-untracked",
          "label": { "en": "git status also lists untracked files", "ru": "git status показывает и неотслеживаемые файлы" },
          "severity": "bug",
          "explanation": { "en": "git status has a dedicated 'Untracked files' section. Surfacing brand-new files is one of its main jobs, so you do not forget to add them.", "ru": "У git status есть отдельный раздел «Untracked files». Показ совсем новых файлов — одна из его главных задач, чтобы ты не забыл их добавить." },
          "planted": true
        },
        {
          "id": "add-dot-is-blunt",
          "label": { "en": "git add . is blunt, not precise", "ru": "git add . грубый, а не точный" },
          "severity": "bug",
          "explanation": { "en": "git add . stages every change under the current path, including scratch files and half-done edits. For an intentional commit, naming explicit paths is what stages exactly what you mean.", "ru": "git add . индексирует все изменения под текущим путём, включая черновики и недоделанные правки. Для намеренного коммита явные пути индексируют ровно то, что ты имеешь в виду." },
          "planted": true
        },
        {
          "id": "commit-records-only-staged",
          "label": { "en": "A plain commit records only staged changes", "ru": "Обычный commit записывает только проиндексированное" },
          "severity": "bug",
          "explanation": { "en": "git commit records the staging area, not the whole working tree. Unstaged edits are left out. Only -a auto-stages tracked files, and even then untracked files are skipped.", "ru": "git commit записывает область индекса, а не всё рабочее дерево. Непроиндексированные правки остаются вне. Лишь -a авто-индексирует отслеживаемые файлы, и даже тогда неотслеживаемые пропускаются." },
          "planted": true
        }
      ]
    }
  ]
}
