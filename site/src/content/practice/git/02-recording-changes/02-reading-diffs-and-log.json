{
  "lessonKey": "git/02-recording-changes/02-reading-diffs-and-log",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-diff-variants",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Match the diff to its pair", "ru": "Сопоставь diff с его парой" },
      "prompt": { "en": "Fill in the diff command for each comparison.", "ru": "Заполни команду diff для каждого сравнения." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["git diff --staged", "git diff --cached", "--staged", "--cached", "diff --staged"], "hint": { "en": "Compares the index against HEAD — what the next commit records.", "ru": "Сравнивает индекс с HEAD — что запишет следующий коммит." } },
          { "id": "b2", "accept": ["git diff", "diff", "git diff (no args)"], "hint": { "en": "With no arguments, compares the working tree against the index.", "ru": "Без аргументов сравнивает рабочее дерево с индексом." } }
        ]
      },
      "scenario": { "en": "To see exactly what your next commit will contain, run (b1). To see only the changes you have edited but not yet staged, run (b2).", "ru": "Чтобы увидеть ровно то, что будет содержать следующий коммит, запусти (b1). Чтобы увидеть только изменения, которые ты правил, но ещё не проиндексировал, запусти (b2)." }
    },
    {
      "id": "predict-read-hunk-header",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Decode the hunk header", "ru": "Расшифруй заголовок куска" },
      "prompt": { "en": "Predict what the @@ header tells you before revealing the answer.", "ru": "Предскажи, что говорит заголовок @@, прежде чем открыть ответ." },
      "scenario": { "en": "A diff hunk begins with the header <code>@@ -40,3 +40,5 @@</code>. What does this tell you about the old and new versions of the file?", "ru": "Кусок diff начинается с заголовка <code>@@ -40,3 +40,5 @@</code>. Что это говорит о старой и новой версиях файла?" },
      "reveal": { "en": "<code>-40,3</code> means the old version of the file at this hunk starts at line 40 and spans 3 lines; <code>+40,5</code> means the new version starts at line 40 and spans 5 lines. So this hunk grew by 2 lines (5 minus 3) — the change added net two lines here. The numbers let git and tools locate and apply the hunk even if surrounding code shifted, using the context lines as anchors.", "ru": "<code>-40,3</code> значит, что старая версия файла в этом куске начинается со строки 40 и тянется на 3 строки; <code>+40,5</code> значит, что новая версия начинается со строки 40 и тянется на 5 строк. Значит, кусок вырос на 2 строки (5 минус 3) — изменение добавило здесь чистых две строки. Числа позволяют git и инструментам находить и применять кусок, даже если окружающий код сдвинулся, используя строки контекста как якоря." }
    },
    {
      "id": "predict-empty-diff",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Why is the diff empty?", "ru": "Почему diff пустой?" },
      "prompt": { "en": "Predict the output before revealing the answer.", "ru": "Предскажи вывод, прежде чем открыть ответ." },
      "scenario": { "en": "You edit <code>server.js</code>, run <code>git add server.js</code>, then run a plain <code>git diff</code> with no arguments. The output is empty. Did your change disappear?", "ru": "Ты правишь <code>server.js</code>, запускаешь <code>git add server.js</code>, затем обычный <code>git diff</code> без аргументов. Вывод пустой. Твоё изменение пропало?" },
      "reveal": { "en": "No — the change is safely staged. Plain <code>git diff</code> compares the working tree against the index, and once you ran <code>git add</code> the working tree and index match, so there is nothing to show. The edit now lives on the staged side. To see it, run <code>git diff --staged</code> (index vs HEAD) or <code>git diff HEAD</code> (everything since the last commit).", "ru": "Нет — изменение благополучно проиндексировано. Обычный <code>git diff</code> сравнивает рабочее дерево с индексом, а после <code>git add</code> рабочее дерево и индекс совпадают, так что показывать нечего. Правка теперь на проиндексированной стороне. Чтобы её увидеть, запусти <code>git diff --staged</code> (индекс против HEAD) или <code>git diff HEAD</code> (всё с последнего коммита)." }
    },
    {
      "id": "review-log-claims",
      "type": "review",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Review notes on log and diff", "ru": "Разбери заметки о log и diff" },
      "prompt": { "en": "A teammate wrote these notes. Find the incorrect claims.", "ru": "Коллега написал эти заметки. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "In a unified diff, a line starting with a space was added.\ngit log --oneline shows the full diff of every commit.\ngit show <commit> prints that commit's message and full diff."
      },
      "findings": [
        {
          "id": "space-is-context",
          "label": { "en": "A leading space means an unchanged context line", "ru": "Ведущий пробел означает неизменную строку контекста" },
          "severity": "bug",
          "explanation": { "en": "In a unified diff, a leading space is unchanged context, '+' is an added line, and '-' is a removed line. The note has it backwards.", "ru": "В унифицированном diff ведущий пробел — это неизменный контекст, «+» — добавленная строка, «-» — удалённая. В заметке всё наоборот." },
          "planted": true
        },
        {
          "id": "oneline-no-diff",
          "label": { "en": "git log --oneline shows one line per commit, not diffs", "ru": "git log --oneline показывает одну строку на коммит, не diff" },
          "severity": "bug",
          "explanation": { "en": "--oneline collapses each commit to a short hash plus subject. To see the diff of every commit you need git log -p instead.", "ru": "--oneline сворачивает каждый коммит до короткого хеша и темы. Чтобы видеть diff каждого коммита, нужен git log -p." },
          "planted": true
        },
        {
          "id": "show-is-correct",
          "label": { "en": "git show <commit> printing message + diff is correct", "ru": "git show <commit>, печатающий сообщение + diff, верно" },
          "severity": "simplification",
          "explanation": { "en": "This claim is actually correct: git show prints the commit's metadata (author, date, message) and its full diff. It is the right tool to inspect a single commit.", "ru": "Это утверждение на самом деле верно: git show печатает метаданные коммита (автор, дата, сообщение) и его полный diff. Это правильный инструмент для разбора одного коммита." },
          "planted": true
        }
      ]
    }
  ]
}
