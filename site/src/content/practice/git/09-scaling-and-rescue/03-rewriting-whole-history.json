{
  "lessonKey": "git/09-scaling-and-rescue/03-rewriting-whole-history",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-filter-repo-flags",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "filter-repo flags", "ru": "Флаги filter-repo" },
      "prompt": { "en": "Fill in the flags that strip a file from all history.", "ru": "Заполни флаги, вычищающие файл из всей истории." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["--path", "--path config/prod.env"], "hint": { "en": "Selects which file the rewrite operates on.", "ru": "Выбирает, над каким файлом работает переписывание." } },
          { "id": "b2", "accept": ["--invert-paths", "invert-paths"], "hint": { "en": "Inverts the selection to mean keep everything except it.", "ru": "Инвертирует выбор в «сохранить всё, кроме этого»." } }
        ]
      },
      "scenario": { "en": "To remove <code>config/prod.env</code> from every commit it ever appeared in, run <code>git filter-repo (b1) config/prod.env (b2)</code>.", "ru": "Чтобы убрать <code>config/prod.env</code> из каждого коммита, где он когда-либо был, запусти <code>git filter-repo (b1) config/prod.env (b2)</code>." }
    },
    {
      "id": "predict-revert-not-enough",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Does git rm fix the leak?", "ru": "Чинит ли git rm утечку?" },
      "prompt": { "en": "Predict whether the secret is removed from history.", "ru": "Предскажи, убран ли секрет из истории." },
      "scenario": { "en": "A live API key was committed in <code>config/prod.env</code> 300 commits ago. A developer runs <code>git rm config/prod.env</code> and commits the deletion, then pushes. Is the key now gone from the repository's history?", "ru": "Живой API-ключ был закоммичен в <code>config/prod.env</code> 300 коммитов назад. Разработчик запускает <code>git rm config/prod.env</code> и коммитит удаление, затем пушит. Исчез ли теперь ключ из истории репозитория?" },
      "reveal": { "en": "No. <code>git rm</code> + commit only removes the file from the tip going forward; the key still sits in the 300-commit-old commit and every descendant tree, in the remote and every clone. Anyone can <code>git show</code> that old commit and read it. Only a whole-history rewrite (<code>git filter-repo --path config/prod.env --invert-paths</code>) actually erases it — and the key must still be rotated, since it was already exposed.", "ru": "Нет. <code>git rm</code> + commit убирает файл только с верхушки вперёд; ключ по-прежнему сидит в коммите 300-коммитной давности и в каждом дочернем дереве, на удалённом сервере и в каждом клоне. Любой может сделать <code>git show</code> того старого коммита и прочитать его. Лишь переписывание всей истории (<code>git filter-repo --path config/prod.env --invert-paths</code>) реально стирает его — и ключ всё равно надо ротировать, ведь он уже был раскрыт." }
    },
    {
      "id": "predict-pull-after-rewrite",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "git pull after a history rewrite", "ru": "git pull после переписывания истории" },
      "prompt": { "en": "Predict what happens when a teammate pulls onto an old clone.", "ru": "Предскажи, что произойдёт, когда коллега сделает pull на старом клоне." },
      "scenario": { "en": "You rewrote history with filter-repo and force-pushed; every commit now has a new SHA. A teammate who still has the old clone runs a normal <code>git pull</code> instead of re-cloning. What is the likely result?", "ru": "Ты переписал историю через filter-repo и сделал force-push; у каждого коммита теперь новый SHA. Коллега, у которого всё ещё старый клон, делает обычный <code>git pull</code> вместо re-clone. Каков вероятный результат?" },
      "reveal": { "en": "The pull tries to merge the old history back in. Because the local clone still holds the old commits (with the secret), Git sees two diverged histories and attempts a merge, which can reintroduce the removed file and create a tangled merge commit — re-leaking the very secret you purged. That is why a rewrite requires everyone to discard their clone and <code>git clone</code> fresh, not pull.", "ru": "Pull пытается влить старую историю обратно. Поскольку локальный клон всё ещё держит старые коммиты (с секретом), Git видит две разошедшиеся истории и пытается слить их, что может вернуть удалённый файл и создать запутанный merge-коммит — пере-утекая ровно тот секрет, что ты вычистил. Поэтому переписывание требует, чтобы все выбросили свой клон и сделали свежий <code>git clone</code>, а не pull." }
    },
    {
      "id": "review-incident-runbook",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Review the leaked-secret runbook", "ru": "Проверь ранбук по утёкшему секрету" },
      "prompt": { "en": "Someone drafted this runbook for a leaked-secret incident. Find the incorrect claims.", "ru": "Кто-то набросал этот ранбук для инцидента с утёкшим секретом. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "Step 1: run git filter-branch to remove the file (it is the\nrecommended tool). Step 2: once history is rewritten and\nforce-pushed, the leaked key is safe and does not need rotating.\nStep 3: a normal git push (not --force) is enough to publish the\nrewritten history."
      },
      "findings": [
        {
          "id": "filter-branch-deprecated",
          "label": { "en": "filter-branch is deprecated; use filter-repo", "ru": "filter-branch устарел; используй filter-repo" },
          "severity": "bug",
          "explanation": { "en": "Git's docs warn against filter-branch (slow, foot-guns) and recommend git filter-repo (or BFG) for whole-history rewrites.", "ru": "Документация Git предупреждает против filter-branch (медленный, ловушки) и рекомендует git filter-repo (или BFG) для переписывания всей истории." },
          "planted": true
        },
        {
          "id": "must-rotate",
          "label": { "en": "The leaked key must still be rotated", "ru": "Утёкший ключ всё равно надо ротировать" },
          "severity": "bug",
          "explanation": { "en": "Rewriting history cannot un-leak an already-public secret — it may be cached, forked, in CI logs, or scraped. Rotation is mandatory and should come first.", "ru": "Переписывание истории не может «раз-утечь» уже публичный секрет — он может быть закеширован, форкнут, в логах CI или собран. Ротация обязательна и должна идти первой." },
          "planted": true
        },
        {
          "id": "needs-force-push",
          "label": { "en": "Publishing a rewrite needs a force-push", "ru": "Публикация переписывания требует force-push" },
          "severity": "bug",
          "explanation": { "en": "A rewrite diverges from the remote (new SHAs), so a fast-forward is impossible and a normal push is rejected. You must git push --force-with-lease.", "ru": "Переписывание расходится с удалённым (новые SHA), поэтому fast-forward невозможен и обычный push отклоняется. Нужно git push --force-with-lease." },
          "planted": true
        }
      ]
    }
  ]
}
