{
  "lessonKey": "git/09-scaling-and-rescue/04-signing-and-trust",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-signing-config",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Configure automatic signing", "ru": "Настроить автоматическую подпись" },
      "prompt": { "en": "Fill in the config that makes every commit signed by default.", "ru": "Заполни конфиг, делающий каждый коммит подписанным по умолчанию." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["user.signingkey", "signingkey"], "hint": { "en": "Tells Git which key to sign with.", "ru": "Говорит Git, каким ключом подписывать." } },
          { "id": "b2", "accept": ["commit.gpgsign", "commit.gpgsign true", "gpgsign"], "hint": { "en": "Set to true to sign every commit without -S.", "ru": "Установи в true, чтобы подписывать каждый коммит без -S." } }
        ]
      },
      "scenario": { "en": "After <code>git config gpg.format ssh</code>, you point Git at your key with <code>git config (b1) ~/.ssh/id_ed25519.pub</code>, then sign all commits automatically with <code>git config (b2) true</code>.", "ru": "После <code>git config gpg.format ssh</code> ты указываешь Git на свой ключ через <code>git config (b1) ~/.ssh/id_ed25519.pub</code>, затем автоматически подписываешь все коммиты через <code>git config (b2) true</code>." }
    },
    {
      "id": "predict-forged-author",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Can the author field be forged?", "ru": "Можно ли подделать поле автора?" },
      "prompt": { "en": "Predict whether the commit will appear authored by the maintainer.", "ru": "Предскажи, будет ли коммит выглядеть написанным мейнтейнером." },
      "scenario": { "en": "A contributor runs <code>git config user.name \"Jane Maintainer\"</code> and sets <code>user.email</code> to match the real maintainer, then commits and pushes to their fork. Will <code>git log</code> show the commit as authored by Jane Maintainer? Does that prove she wrote it?", "ru": "Контрибьютор запускает <code>git config user.name \"Jane Maintainer\"</code> и ставит <code>user.email</code>, совпадающий с настоящим мейнтейнером, затем коммитит и пушит в свой форк. Покажет ли <code>git log</code> коммит как написанный Jane Maintainer? Доказывает ли это, что написала его она?" },
      "reveal": { "en": "Yes, the log will display \"Jane Maintainer\" — and no, it proves nothing. The author field is plain text copied from local config with zero verification, so anyone can claim any identity. Only a cryptographic signature would prove authorship: the contributor cannot sign as Jane because they lack her private key. On a host, the commit shows as Unverified, exposing the impersonation.", "ru": "Да, лог покажет «Jane Maintainer» — и нет, это ничего не доказывает. Поле автора — это обычный текст, скопированный из локальной конфигурации без какой-либо проверки, так что любой может присвоить любую личность. Лишь криптографическая подпись доказала бы авторство: контрибьютор не может подписаться как Jane, ведь у него нет её приватного ключа. На хостинге коммит показывается как Unverified, раскрывая имперсонацию." }
    },
    {
      "id": "predict-unverified-badge",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Signed but Unverified", "ru": "Подписан, но Unverified" },
      "prompt": { "en": "Predict why genuinely signed commits show as Unverified.", "ru": "Предскажи, почему по-настоящему подписанные коммиты показываются как Unverified." },
      "scenario": { "en": "You set <code>commit.gpgsign true</code> and <code>user.signingkey</code> correctly; <code>git log --show-signature</code> locally confirms a good signature. Yet on GitHub every commit shows the gray \"Unverified\" label. What is the most likely cause?", "ru": "Ты корректно поставил <code>commit.gpgsign true</code> и <code>user.signingkey</code>; <code>git log --show-signature</code> локально подтверждает хорошую подпись. Но на GitHub каждый коммит показывает серый ярлык «Unverified». Какова наиболее вероятная причина?" },
      "reveal": { "en": "You never registered the public signing key with GitHub. Signing locally and uploading the key to your account are two separate steps; without the key on file, the host cannot match the signature to your identity, so it labels the commit Unverified even though the signature is genuine. Add the SSH/GPG signing key in your account settings and the badge turns green.", "ru": "Ты так и не зарегистрировал публичный ключ подписи на GitHub. Подписать локально и загрузить ключ в аккаунт — два отдельных шага; без ключа на учёте хостинг не может сопоставить подпись с твоей личностью, поэтому метит коммит как Unverified, хотя подпись настоящая. Добавь SSH/GPG-ключ подписи в настройках аккаунта — и значок станет зелёным." }
    },
    {
      "id": "review-signing-claims",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Review the signing wiki page", "ru": "Проверь вики-страницу о подписи" },
      "prompt": { "en": "A teammate wrote this wiki page about commit signing. Find the incorrect claims.", "ru": "Коллега написал эту вики-страницу о подписи коммитов. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "Git verifies the author email on every commit, so the author\nfield already proves who wrote it. You can only sign commits with\nGPG; SSH keys cannot be used for signing. A green Verified badge\nguarantees the code in the commit is high quality and safe."
      },
      "findings": [
        {
          "id": "author-not-verified",
          "label": { "en": "Git does not verify the author field", "ru": "Git не проверяет поле автора" },
          "severity": "bug",
          "explanation": { "en": "The author name and email come from local config with no verification; anyone can set them. That is exactly why signing is needed to prove authorship.", "ru": "Имя и email автора берутся из локальной конфигурации без проверки; любой может их задать. Именно поэтому для доказательства авторства нужна подпись." },
          "planted": true
        },
        {
          "id": "ssh-signing-works",
          "label": { "en": "SSH keys can sign commits", "ru": "SSH-ключи могут подписывать коммиты" },
          "severity": "bug",
          "explanation": { "en": "Git supports SSH signing via gpg.format ssh, often simpler than GPG since most developers already have an SSH key.", "ru": "Git поддерживает подпись через SSH с gpg.format ssh, часто проще GPG, ведь у большинства разработчиков уже есть SSH-ключ." },
          "planted": true
        },
        {
          "id": "verified-not-quality",
          "label": { "en": "Verified means authentic origin, not good code", "ru": "Verified означает подлинное происхождение, а не хороший код" },
          "severity": "bug",
          "explanation": { "en": "The badge only proves the commit was signed by a key on file for an account. It says nothing about code quality, correctness, or safety.", "ru": "Значок лишь доказывает, что коммит подписан ключом, который на учёте у аккаунта. Он ничего не говорит о качестве, корректности или безопасности кода." },
          "planted": true
        }
      ]
    }
  ]
}
