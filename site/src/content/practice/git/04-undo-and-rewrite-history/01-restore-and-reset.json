{
  "lessonKey": "git/04-undo-and-rewrite-history/01-restore-and-reset",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-reset-modes",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Name the reset modes", "ru": "Назови режимы reset" },
      "prompt": { "en": "Fill in the blanks with the reset mode that matches each behavior.", "ru": "Заполни пропуски режимом reset, соответствующим каждому поведению." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["--soft", "soft"], "hint": { "en": "Moves HEAD only; everything stays staged.", "ru": "Двигает только HEAD; всё остаётся застейдженным." } },
          { "id": "b2", "accept": ["--hard", "hard"], "hint": { "en": "Also overwrites the working tree, destroying uncommitted edits.", "ru": "Ещё и перезаписывает рабочее дерево, уничтожая незакоммиченные правки." } }
        ]
      },
      "scenario": { "en": "To uncommit but keep all changes staged, use <code>git reset (b1) HEAD~1</code>. To wipe HEAD, index, and the working tree back to a commit — losing uncommitted work — use <code>git reset (b2) HEAD~1</code>.", "ru": "Чтобы раскоммитить, но оставить все изменения застейдженными, используй <code>git reset (b1) HEAD~1</code>. Чтобы сбросить HEAD, индекс и рабочее дерево до коммита — потеряв незакоммиченную работу — используй <code>git reset (b2) HEAD~1</code>." }
    },
    {
      "id": "predict-restore-staged",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Unstage without losing edits", "ru": "Снять со стейджа, не теряя правок" },
      "prompt": { "en": "Predict what happens to your edits before revealing the answer.", "ru": "Предскажи, что станет с твоими правками, прежде чем открыть ответ." },
      "scenario": { "en": "You edited <code>config.yml</code> and ran <code>git add config.yml</code>, then realised it should not go in this commit. You run <code>git restore --staged config.yml</code>. Are your edits to the file lost?", "ru": "Ты отредактировал <code>config.yml</code> и выполнил <code>git add config.yml</code>, потом понял, что в этот коммит он попадать не должен. Ты запускаешь <code>git restore --staged config.yml</code>. Потеряются ли твои правки в файле?" },
      "reveal": { "en": "No. <code>git restore --staged</code> only touches the index — it removes the file from the staging area but leaves the working-tree copy exactly as you edited it. After the command, <code>git status</code> shows <code>config.yml</code> as modified-but-unstaged. To also throw the edits away you would need <code>git restore config.yml</code> (no <code>--staged</code>), which overwrites the file from HEAD.", "ru": "Нет. <code>git restore --staged</code> трогает только индекс — он убирает файл из области подготовки, но оставляет копию в рабочем дереве ровно такой, какой ты её отредактировал. После команды <code>git status</code> покажет <code>config.yml</code> как изменённый-но-не-застейдженный. Чтобы ещё и выбросить правки, понадобился бы <code>git restore config.yml</code> (без <code>--staged</code>), который перезапишет файл из HEAD." }
    },
    {
      "id": "predict-hard-reset-loss",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Can you get it back?", "ru": "Можно ли это вернуть?" },
      "prompt": { "en": "Decide whether the uncommitted work survives the command.", "ru": "Реши, переживёт ли незакоммиченная работа эту команду." },
      "scenario": { "en": "You have two hours of uncommitted edits in the working tree (never staged, never committed). Annoyed at a merge, you run <code>git reset --hard HEAD</code>. Can you recover those two hours afterward?", "ru": "У тебя два часа незакоммиченных правок в рабочем дереве (никогда не застейдженных, никогда не закоммиченных). Раздражённый слиянием, ты запускаешь <code>git reset --hard HEAD</code>. Сможешь ли ты восстановить эти два часа после?" },
      "reveal": { "en": "No. <code>--hard</code> overwrites the working tree from the target commit, and because the edits were never committed there is no object in git's database holding them — <code>git reflog</code> can recover lost commits but not work that was never committed. The edits are gone. The lesson: run <code>git stash</code> (which does create a commit-like object) before any <code>--hard</code> when uncommitted work matters.", "ru": "Нет. <code>--hard</code> перезаписывает рабочее дерево из целевого коммита, и поскольку правки никогда не коммитились, в базе git нет объекта, который бы их хранил — <code>git reflog</code> восстанавливает потерянные коммиты, но не работу, которая никогда не коммитилась. Правки потеряны. Урок: запускай <code>git stash</code> (он создаёт объект вроде коммита) перед любым <code>--hard</code>, когда незакоммиченная работа важна." }
    },
    {
      "id": "review-undo-advice",
      "type": "review",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Spot the wrong undo advice", "ru": "Найди неверный совет по отмене" },
      "prompt": { "en": "A teammate left these notes in a wiki. Find the incorrect claims.", "ru": "Коллега оставил эти заметки в вики. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git reset --soft HEAD~1 deletes your working-tree changes.\ngit restore --staged file.txt throws away your edits to file.txt.\ngit reset --hard is always safe because git keeps a backup of\neverything on disk."
      },
      "findings": [
        {
          "id": "soft-not-destructive",
          "label": { "en": "--soft does not delete working-tree changes", "ru": "--soft не удаляет изменения рабочего дерева" },
          "severity": "bug",
          "explanation": { "en": "--soft moves HEAD only; the index and working tree are untouched, so all changes stay staged and on disk.", "ru": "--soft двигает только HEAD; индекс и рабочее дерево не тронуты, поэтому все изменения остаются застейдженными и на диске." },
          "planted": true
        },
        {
          "id": "restore-staged-keeps-edits",
          "label": { "en": "--staged only unstages; it keeps your edits", "ru": "--staged только снимает со стейджа; правки сохраняются" },
          "severity": "bug",
          "explanation": { "en": "git restore --staged file.txt removes the file from the index but leaves your working-tree edits intact. To discard edits you drop the --staged flag.", "ru": "git restore --staged file.txt убирает файл из индекса, но оставляет правки рабочего дерева нетронутыми. Чтобы отменить правки, убирают флаг --staged." },
          "planted": true
        },
        {
          "id": "hard-not-always-safe",
          "label": { "en": "--hard is destructive for uncommitted work", "ru": "--hard разрушителен для незакоммиченной работы" },
          "severity": "bug",
          "explanation": { "en": "git does not back up uncommitted edits. --hard overwrites the working tree, and anything never committed (or stashed) is unrecoverable.", "ru": "git не делает резервную копию незакоммиченных правок. --hard перезаписывает рабочее дерево, и всё, что никогда не коммитилось (или не было в стэше), не восстановить." },
          "planted": true
        }
      ]
    }
  ]
}
