{
  "lessonKey": "git/04-undo-and-rewrite-history/02-revert-safe-undo",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-revert-merge",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Revert command shape", "ru": "Форма команды revert" },
      "prompt": { "en": "Fill in the blanks for safely undoing commits on shared history.", "ru": "Заполни пропуски для безопасной отмены коммитов в общей истории." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["revert", "git revert"], "hint": { "en": "The subcommand that adds an inverse commit instead of rewriting history.", "ru": "Подкоманда, добавляющая инвертирующий коммит вместо переписывания истории." } },
          { "id": "b2", "accept": ["-m 1", "-m1", "--mainline 1", "1"], "hint": { "en": "Selects the mainline parent (usually main) when the target is a merge commit.", "ru": "Выбирает магистрального родителя (обычно main), когда цель — мерж-коммит." } }
        ]
      },
      "scenario": { "en": "To undo an ordinary pushed commit safely, run <code>git (b1) abc123</code>. If that commit is a merge with two parents, you must add <code>(b2)</code> so git knows which parent is the mainline.", "ru": "Чтобы безопасно отменить обычный запушенный коммит, запусти <code>git (b1) abc123</code>. Если этот коммит — мерж с двумя родителями, нужно добавить <code>(b2)</code>, чтобы git знал, какой родитель магистральный." }
    },
    {
      "id": "predict-revert-vs-reset",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Which undo on a shared branch?", "ru": "Какая отмена на общей ветке?" },
      "prompt": { "en": "Predict the consequence before revealing the answer.", "ru": "Предскажи последствие, прежде чем открыть ответ." },
      "scenario": { "en": "A bug shipped in commit <code>d4e5f6</code> on <code>main</code>, which six teammates have already pulled. A junior runs <code>git reset --hard d4e5f6^</code> and then <code>git push --force</code>. What goes wrong for the team?", "ru": "Баг уехал в коммите <code>d4e5f6</code> на <code>main</code>, который уже подтянули шесть коллег. Джуниор запускает <code>git reset --hard d4e5f6^</code>, а затем <code>git push --force</code>. Что пойдёт не так для команды?" },
      "reveal": { "en": "The force-push rewrites shared history. Each teammate's local <code>main</code> still has <code>d4e5f6</code> and the commits after it; their branches now diverge from the rewritten remote. The next person who pulls gets confusing merge conflicts, and an accidental push of their old branch can re-introduce the bug. The safe move was <code>git revert d4e5f6</code>, which adds an inverse commit everyone fast-forwards onto.", "ru": "Форс-пуш переписывает общую историю. У локального <code>main</code> каждого коллеги всё ещё есть <code>d4e5f6</code> и коммиты после него; их ветки теперь расходятся с переписанным remote. Следующий, кто сделает pull, получит запутанные конфликты слияния, а случайный push их старой ветки может заново внести баг. Безопасным ходом был <code>git revert d4e5f6</code>, добавляющий инвертирующий коммит, на который все перематываются вперёд." }
    },
    {
      "id": "predict-revert-keeps-history",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Does the bad commit disappear?", "ru": "Исчезает ли плохой коммит?" },
      "prompt": { "en": "Predict what git log shows after a revert.", "ru": "Предскажи, что покажет git log после revert." },
      "scenario": { "en": "You run <code>git revert 9f1c2ab</code> to undo a bad change and push. A teammate later runs <code>git log --oneline</code>. Will they still see the original commit <code>9f1c2ab</code> in the history?", "ru": "Ты запускаешь <code>git revert 9f1c2ab</code>, чтобы отменить плохое изменение, и пушишь. Коллега позже запускает <code>git log --oneline</code>. Увидит ли он по-прежнему исходный коммит <code>9f1c2ab</code> в истории?" },
      "reveal": { "en": "Yes. Revert does not delete anything — it adds a new inverse commit on top. The history shows both <code>9f1c2ab</code> (the original) and the new <code>Revert \"...\"</code> commit. That is by design: the record of what was done and that it was undone is preserved, which is exactly why revert is safe to share. If you wanted the commit gone from history entirely you would need a rewrite (reset/rebase) — unsafe on shared branches.", "ru": "Да. Revert ничего не удаляет — он добавляет новый инвертирующий коммит сверху. История показывает и <code>9f1c2ab</code> (исходный), и новый коммит <code>Revert \"...\"</code>. Это задумано так: запись о том, что было сделано и что это отменили, сохраняется — именно поэтому revert безопасно расшаривать. Если бы ты хотел убрать коммит из истории целиком, понадобилось бы переписывание (reset/rebase) — небезопасное на общих ветках." }
    },
    {
      "id": "review-revert-notes",
      "type": "review",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Spot the wrong revert claims", "ru": "Найди неверные утверждения о revert" },
      "prompt": { "en": "Review these onboarding notes and find the incorrect claims.", "ru": "Просмотри эти заметки для новичков и найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git revert deletes the target commit from history.\nReverting a merge commit works the same as a normal commit;\nno extra flag is needed. git revert is only safe on local\nbranches and must never be pushed."
      },
      "findings": [
        {
          "id": "revert-does-not-delete",
          "label": { "en": "Revert adds an inverse commit; it does not delete", "ru": "Revert добавляет инвертирующий коммит; он не удаляет" },
          "severity": "bug",
          "explanation": { "en": "The original commit stays in history. Revert appends a new commit whose changes cancel it out, leaving both visible in git log.", "ru": "Исходный коммит остаётся в истории. Revert дописывает новый коммит, чьи изменения его гасят, оставляя оба видимыми в git log." },
          "planted": true
        },
        {
          "id": "merge-needs-mainline",
          "label": { "en": "Reverting a merge requires -m to pick the parent", "ru": "Откат мержа требует -m для выбора родителя" },
          "severity": "bug",
          "explanation": { "en": "A merge has two parents, so git cannot infer the mainline. You must pass -m 1 (or -m 2); otherwise git refuses with an error.", "ru": "У мержа два родителя, поэтому git не может вывести магистраль. Нужно передать -m 1 (или -m 2); иначе git откажется с ошибкой." },
          "planted": true
        },
        {
          "id": "revert-is-push-safe",
          "label": { "en": "Revert is the push-safe undo for shared history", "ru": "Revert — это безопасная для push отмена общей истории" },
          "severity": "bug",
          "explanation": { "en": "Revert is specifically the undo you push to shared branches, because it preserves history. It is reset (history rewrite) that must stay local.", "ru": "Revert — это именно та отмена, которую пушат на общие ветки, потому что она сохраняет историю. Локальным должен оставаться reset (переписывание истории)." },
          "planted": true
        }
      ]
    }
  ]
}
