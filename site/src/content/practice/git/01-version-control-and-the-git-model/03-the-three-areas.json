{
  "lessonKey": "git/01-version-control-and-the-git-model/03-the-three-areas",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-area-per-command",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Which command, which hop", "ru": "Какая команда, какой шаг" },
      "prompt": { "en": "Fill in the blanks naming the command for each move between areas.", "ru": "Заполни пропуски, называя команду для каждого перехода между областями." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["git add", "add"], "hint": { "en": "Moves a change from the working tree into the index.", "ru": "Переносит изменение из рабочего дерева в индекс." } },
          { "id": "b2", "accept": ["git commit", "commit"], "hint": { "en": "Records the staged snapshot and moves HEAD.", "ru": "Записывает подготовленный снимок и двигает HEAD." } }
        ]
      },
      "scenario": { "en": "You edit a file in the working tree. Running (b1) stages it into the index; running (b2) writes the index as a new commit in the repository.", "ru": "Ты правишь файл в рабочем дереве. Запуск (b1) подготавливает его в индекс; запуск (b2) пишет индекс как новый коммит в репозиторий." }
    },
    {
      "id": "predict-stale-stage",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Edit after add", "ru": "Правка после add" },
      "prompt": { "en": "Predict what the commit contains before revealing the answer.", "ru": "Предскажи, что содержит коммит, прежде чем открыть ответ." },
      "scenario": { "en": "You run <code>git add config.json</code>. Then you realize a value is wrong and edit <code>config.json</code> again, saving the file. Without running <code>git add</code> a second time, you run <code>git commit -m \"update config\"</code>. Does the commit contain your corrected value?", "ru": "Ты запускаешь <code>git add config.json</code>. Потом понимаешь, что значение неверное, и снова правишь <code>config.json</code>, сохраняя файл. Не запуская <code>git add</code> второй раз, ты делаешь <code>git commit -m \"update config\"</code>. Содержит ли коммит исправленное значение?" },
      "reveal": { "en": "No. The commit contains the version captured by the first <code>git add</code> — the wrong value. Your correction lives only in the working tree because it was never staged. <code>git commit</code> records the index, not the file on disk. <code>git status</code> would have shown <code>config.json</code> in BOTH 'Changes to be committed' (the staged old version) and 'Changes not staged for commit' (your newer edit). Re-running <code>git add</code> before committing fixes it.", "ru": "Нет. Коммит содержит версию, захваченную первым <code>git add</code> — неверное значение. Твоё исправление живёт только в рабочем дереве, потому что оно никогда не подготавливалось. <code>git commit</code> записывает индекс, а не файл на диске. <code>git status</code> показал бы <code>config.json</code> И в 'Changes to be committed' (подготовленная старая версия), И в 'Changes not staged for commit' (твоя новая правка). Повторный <code>git add</code> перед коммитом это чинит." }
    },
    {
      "id": "predict-status-buckets",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Reading git status", "ru": "Чтение git status" },
      "prompt": { "en": "Decide which status bucket a brand-new file appears in.", "ru": "Реши, в какой корзине статуса появляется совершенно новый файл." },
      "scenario": { "en": "You create a new file <code>notes.txt</code> in a repo and never run <code>git add</code>. Under which heading does <code>git status</code> list it: 'Changes to be committed', 'Changes not staged for commit', or 'Untracked files'?", "ru": "Ты создаёшь новый файл <code>notes.txt</code> в репозитории и никогда не запускаешь <code>git add</code>. Под каким заголовком <code>git status</code> его перечислит: 'Changes to be committed', 'Changes not staged for commit' или 'Untracked files'?" },
      "reveal": { "en": "Under 'Untracked files'. Git has never seen this file, so it is neither staged nor a modification of a tracked file — it is untracked. 'Changes not staged for commit' is for edits to files git already tracks; 'Changes to be committed' is for anything you have staged with <code>git add</code>. A first <code>git add notes.txt</code> would move it into 'Changes to be committed'.", "ru": "Под 'Untracked files'. Git никогда не видел этот файл, так что он ни подготовлен, ни является изменением отслеживаемого файла — он неотслеживаемый. 'Changes not staged for commit' — для правок файлов, которые git уже отслеживает; 'Changes to be committed' — для всего, что ты подготовил через <code>git add</code>. Первый <code>git add notes.txt</code> перенёс бы его в 'Changes to be committed'." }
    },
    {
      "id": "review-areas-claims",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the wrong claims", "ru": "Найди неверные утверждения" },
      "prompt": { "en": "A teammate wrote this note about git's areas. Find the incorrect claims.", "ru": "Коллега написал эту заметку об областях git. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "git commit saves whatever is currently in the working tree on disk.\nThe staging area is pointless busywork that does nothing useful.\nHEAD is a backup of your files; it has nothing to do with which\ncommit you are currently on."
      },
      "findings": [
        {
          "id": "commit-records-index",
          "label": { "en": "Commit records the index, not the working tree", "ru": "Коммит записывает индекс, а не рабочее дерево" },
          "severity": "bug",
          "explanation": { "en": "git commit writes the staged snapshot from the index. Unstaged working-tree edits are excluded — which is exactly why an edit made after git add is left out of the commit.", "ru": "git commit пишет подготовленный снимок из индекса. Неподготовленные правки рабочего дерева исключаются — поэтому правка, сделанная после git add, и не попадает в коммит." },
          "planted": true
        },
        {
          "id": "staging-has-a-purpose",
          "label": { "en": "The staging area lets you craft focused commits", "ru": "Область подготовки позволяет собирать сфокусированные коммиты" },
          "severity": "bug",
          "explanation": { "en": "The index is the whole point of the two-step flow: it lets you commit a subset of your changes — even individual hunks via git add -p — producing a clean, reviewable history instead of one lump.", "ru": "Индекс — весь смысл двухшагового потока: он позволяет закоммитить подмножество изменений — даже отдельные куски через git add -p — давая чистую, пригодную к ревью историю вместо одного кома." },
          "planted": true
        },
        {
          "id": "head-is-current-commit",
          "label": { "en": "HEAD points at the commit you are currently on", "ru": "HEAD указывает на коммит, на котором ты сейчас" },
          "severity": "bug",
          "explanation": { "en": "HEAD is a pointer to your current commit — the one your next commit will build on. It is not a file backup; after a commit, HEAD advances to point at the new commit.", "ru": "HEAD — это указатель на твой текущий коммит, тот, поверх которого построится следующий. Это не резервная копия файлов; после коммита HEAD продвигается, указывая на новый коммит." },
          "planted": true
        }
      ]
    }
  ]
}
