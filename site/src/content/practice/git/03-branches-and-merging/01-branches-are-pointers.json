{
  "lessonKey": "git/03-branches-and-merging/01-branches-are-pointers",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-head-chain",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "The HEAD chain", "ru": "Цепочка HEAD" },
      "prompt": { "en": "Fill in the blanks naming each link in the pointer chain.", "ru": "Заполни пропуски, называя каждое звено в цепочке указателей." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["branch", "branch name", "ветка", "имя ветки", "ветку"], "hint": { "en": "Normally HEAD points at this, not at a commit directly.", "ru": "Обычно HEAD указывает на это, а не на коммит напрямую." } },
          { "id": "b2", "accept": ["commit", "коммит"], "hint": { "en": "The thing a branch ultimately points at.", "ru": "То, на что в итоге указывает ветка." } }
        ]
      },
      "scenario": { "en": "Read <code>.git/HEAD</code> and it says <code>ref: refs/heads/main</code>. So HEAD points at a (b1), and that in turn points at a (b2).", "ru": "Читаешь <code>.git/HEAD</code>, и там написано <code>ref: refs/heads/main</code>. Значит, HEAD указывает на (b1), а она, в свою очередь, указывает на (b2)." }
    },
    {
      "id": "predict-branch-then-commit",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Which pointer moves?", "ru": "Какой указатель двигается?" },
      "prompt": { "en": "Predict which branch files change before revealing the answer.", "ru": "Предскажи, какие файлы веток изменятся, прежде чем открыть ответ." },
      "scenario": { "en": "You are on <code>main</code> (commit A). You run <code>git branch feature</code>, then <code>git switch feature</code>, then <code>git commit -m \"work\"</code> (this creates commit B). Afterwards, what commit does the file <code>.git/refs/heads/main</code> contain?", "ru": "Ты на <code>main</code> (коммит A). Запускаешь <code>git branch feature</code>, затем <code>git switch feature</code>, затем <code>git commit -m \"work\"</code> (создаётся коммит B). После этого какой коммит содержит файл <code>.git/refs/heads/main</code>?" },
      "reveal": { "en": "Still A. The commit advanced only the <em>current</em> branch — <code>feature</code> — so <code>.git/refs/heads/feature</code> now holds B while <code>main</code> is untouched at A. HEAD had been repointed to <code>feature</code> by the switch, and a commit moves whatever branch HEAD names. The two branches have diverged: main → A, feature → B.", "ru": "По-прежнему A. Коммит продвинул только <em>текущую</em> ветку — <code>feature</code> — поэтому <code>.git/refs/heads/feature</code> теперь держит B, а <code>main</code> не тронут и остался на A. HEAD был перенаправлен на <code>feature</code> при переключении, а коммит двигает ту ветку, которую называет HEAD. Две ветки разошлись: main → A, feature → B." }
    },
    {
      "id": "predict-delete-merged",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Does deleting a branch lose commits?", "ru": "Теряет ли удаление ветки коммиты?" },
      "prompt": { "en": "Decide whether the commits survive deleting the branch.", "ru": "Реши, переживут ли коммиты удаление ветки." },
      "scenario": { "en": "You merged <code>feature</code> into <code>main</code>, so <code>main</code> can now reach every commit that was on <code>feature</code>. You then delete <code>feature</code>. Are the commits that were on <code>feature</code> gone?", "ru": "Ты слил <code>feature</code> в <code>main</code>, так что <code>main</code> теперь может дотянуться до каждого коммита, что был на <code>feature</code>. Затем ты удаляешь <code>feature</code>. Исчезли ли коммиты, что были на <code>feature</code>?" },
      "reveal": { "en": "No. Deleting a branch only removes the <em>pointer</em> file under <code>.git/refs/heads/</code>. The commit objects stay in the object store as long as something can still reach them — and here <code>main</code> can, because the merge made those commits part of main's history. This is why <code>git branch -d</code> (safe delete) refuses an <em>unmerged</em> branch but happily deletes a merged one.", "ru": "Нет. Удаление ветки убирает лишь файл-<em>указатель</em> под <code>.git/refs/heads/</code>. Объекты-коммиты остаются в хранилище объектов, пока до них может что-то дотянуться — а здесь <code>main</code> может, потому что слияние сделало эти коммиты частью истории main. Поэтому <code>git branch -d</code> (безопасное удаление) отказывается удалять <em>не слитую</em> ветку, но спокойно удаляет слитую." }
    },
    {
      "id": "review-branch-claims",
      "type": "review",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Spot the wrong claims", "ru": "Найди неверные утверждения" },
      "prompt": { "en": "A teammate wrote this note about branches. Find the incorrect claims.", "ru": "Коллега написал эту заметку о ветках. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "Creating a branch copies the whole working tree, so branches\nare expensive on big repos. HEAD always points directly at a\ncommit ID. The 'main' branch is hard-coded into git and cannot\nbe renamed or removed."
      },
      "findings": [
        {
          "id": "branch-is-not-a-copy",
          "label": { "en": "A branch is a pointer, not a copy", "ru": "Ветка — это указатель, а не копия" },
          "severity": "bug",
          "explanation": { "en": "Creating a branch writes one ~41-byte file holding a commit ID. It copies nothing, so it is O(1) regardless of repo size.", "ru": "Создание ветки пишет один файл в ~41 байт с идентификатором коммита. Оно ничего не копирует, поэтому это O(1) независимо от размера репозитория." },
          "planted": true
        },
        {
          "id": "head-points-at-branch",
          "label": { "en": "HEAD normally points at a branch, not a commit", "ru": "HEAD обычно указывает на ветку, а не на коммит" },
          "severity": "bug",
          "explanation": { "en": "Normally HEAD holds 'ref: refs/heads/<name>'. Only in detached-HEAD state does it hold a commit ID directly.", "ru": "Обычно HEAD держит 'ref: refs/heads/<имя>'. Лишь в состоянии detached HEAD он держит идентификатор коммита напрямую." },
          "planted": true
        },
        {
          "id": "main-is-not-special",
          "label": { "en": "'main' is just a default name and can be renamed", "ru": "'main' — это лишь имя по умолчанию, его можно переименовать" },
          "severity": "bug",
          "explanation": { "en": "'main' is a conventional default branch name, not a built-in. You can rename it (git branch -m) or use any name; git treats it like any other ref.", "ru": "'main' — это общепринятое имя ветки по умолчанию, а не встроенное. Его можно переименовать (git branch -m) или использовать любое имя; git относится к нему как к любому другому ref." },
          "planted": true
        }
      ]
    }
  ]
}
