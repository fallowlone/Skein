{
  "lessonKey": "git/03-branches-and-merging/04-merge-conflicts",
  "track": "git",
  "tasks": [
    {
      "id": "diagnose-conflict-markers",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 3,
      "title": { "en": "Which side is which?", "ru": "Какая сторона где?" },
      "prompt": { "en": "Fill in the blanks identifying the two sides of a conflict block.", "ru": "Заполни пропуски, определяя две стороны конфликтного блока." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["current", "HEAD", "your", "current branch", "текущая", "твоя", "текущей ветки"], "hint": { "en": "The branch you ran the merge from, labelled HEAD.", "ru": "Ветка, с которой ты запустил слияние, помеченная HEAD." } },
          { "id": "b2", "accept": ["incoming", "merged", "other", "feature", "incoming branch", "входящая", "сливаемая", "другая"], "hint": { "en": "The branch named after the >>>>>>> marker.", "ru": "Ветка, названная после маркера >>>>>>>." } }
        ]
      },
      "scenario": { "en": "In a conflict block, the lines between <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</code> and <code>=======</code> come from the (b1) branch, and the lines between <code>=======</code> and <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature</code> come from the (b2) branch.", "ru": "В конфликтном блоке строки между <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</code> и <code>=======</code> приходят из (b1) ветки, а строки между <code>=======</code> и <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature</code> приходят из (b2) ветки." }
    },
    {
      "id": "predict-add-resolves",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Why is it still Unmerged?", "ru": "Почему он всё ещё Unmerged?" },
      "prompt": { "en": "Predict what git status shows after editing but not staging.", "ru": "Предскажи, что покажет git status после правки, но без индексации." },
      "scenario": { "en": "You edit the conflicted <code>app.js</code>, remove all the markers, and save it in your editor. You have NOT run <code>git add</code>. You run <code>git status</code>. Does it consider <code>app.js</code> resolved?", "ru": "Ты правишь конфликтный <code>app.js</code>, убираешь все маркеры и сохраняешь в редакторе. <code>git add</code> ты НЕ запускал. Ты запускаешь <code>git status</code>. Считает ли он <code>app.js</code> разрешённым?" },
      "reveal": { "en": "No — it still lists <code>app.js</code> under 'Unmerged paths'. Saving in your editor is invisible to git as a resolution signal; git only treats a conflicted file as resolved once you <code>git add</code> it. Stage it, confirm <code>git status</code> shows nothing unmerged, then <code>git commit</code> to record the merge. (Git does not even check that you removed the markers — that responsibility is yours.)", "ru": "Нет — он по-прежнему числит <code>app.js</code> под «Unmerged paths». Сохранение в редакторе невидимо для git как сигнал разрешения; git считает конфликтный файл разрешённым лишь после того, как ты сделаешь <code>git add</code>. Проиндексируй его, убедись, что <code>git status</code> не показывает несведённого, затем <code>git commit</code>, чтобы записать слияние. (Git даже не проверяет, что ты убрал маркеры — это твоя ответственность.)" }
    },
    {
      "id": "predict-abort",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 4,
      "title": { "en": "Backing out of a merge", "ru": "Откат из слияния" },
      "prompt": { "en": "Predict what state the repo returns to after --abort.", "ru": "Предскажи, в какое состояние вернётся репозиторий после --abort." },
      "scenario": { "en": "You started <code>git merge feature</code>, hit conflicts in three files, and realised you merged into the wrong branch. Before committing anything, you run <code>git merge --abort</code>. What is the state of your branch and working tree?", "ru": "Ты начал <code>git merge feature</code>, поймал конфликты в трёх файлах и понял, что слил не в ту ветку. Ничего не закоммитив, ты запускаешь <code>git merge --abort</code>. Каково состояние твоей ветки и рабочего дерева?" },
      "reveal": { "en": "Exactly as they were the instant before you ran <code>git merge</code>. <code>--abort</code> discards the in-progress merge entirely: the branch pointer, the index, and the working tree (including the conflict markers git had inserted) all revert. No merge commit is made, <code>feature</code> is untouched, and you are free to switch to the correct branch and merge there. It is the clean escape hatch from any conflicted merge you have not yet committed.", "ru": "Ровно как были в момент перед запуском <code>git merge</code>. <code>--abort</code> полностью отбрасывает идущее слияние: указатель ветки, индекс и рабочее дерево (включая маркеры конфликта, что вставил git) — всё откатывается. Merge-коммит не делается, <code>feature</code> не тронут, и ты свободен переключиться на правильную ветку и слить там. Это чистый аварийный выход из любого конфликтного слияния, которое ты ещё не закоммитил." }
    },
    {
      "id": "review-conflict-resolution",
      "type": "review",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Review the resolution steps", "ru": "Проверь шаги разрешения" },
      "prompt": { "en": "A teammate documented conflict resolution. Find the incorrect claims.", "ru": "Коллега задокументировал разрешение конфликта. Найди неверные утверждения." },
      "diff": {
        "lang": "text",
        "code": "A conflict means the merge failed and git already rolled it\nback. You must pick either HEAD or the incoming side exactly as\nwritten; you cannot type a combined result. It is fine to leave\nthe ======= marker in the file as long as the code compiles."
      },
      "findings": [
        {
          "id": "paused-not-rolled-back",
          "label": { "en": "A conflict pauses the merge, it does not roll it back", "ru": "Конфликт ставит слияние на паузу, а не откатывает его" },
          "severity": "bug",
          "explanation": { "en": "On conflict the merge is paused mid-flight: cleanly merged files are staged and conflicted files await resolution. To actually roll back you run git merge --abort.", "ru": "При конфликте слияние ставится на паузу на середине: чисто слитые файлы проиндексированы, а конфликтные ждут разрешения. Чтобы действительно откатить, запускают git merge --abort." },
          "planted": true
        },
        {
          "id": "you-can-write-anything",
          "label": { "en": "You can write any final content, not just one side", "ru": "Ты можешь написать любое финальное содержимое, не только одну сторону" },
          "severity": "bug",
          "explanation": { "en": "Resolving means editing the block to whatever is correct — yours, theirs, a blend, or new code. You are not limited to picking one untouched side.", "ru": "Разрешить — значит привести блок к тому, что верно: твоё, их, смесь или новый код. Ты не ограничен выбором одной нетронутой стороны." },
          "planted": true
        },
        {
          "id": "markers-must-go",
          "label": { "en": "All conflict markers must be removed", "ru": "Все маркеры конфликта должны быть удалены" },
          "severity": "bug",
          "explanation": { "en": "Leaving a ======= (or <<<<<<< / >>>>>>>) marker ships broken syntax. Even if some files still compile, the leftover marker is a defect; grep for markers before committing.", "ru": "Оставленный маркер ======= (или <<<<<<< / >>>>>>>) отправляет сломанный синтаксис. Даже если какие-то файлы ещё компилируются, забытый маркер — это дефект; грепай маркеры перед коммитом." },
          "planted": true
        }
      ]
    }
  ]
}
