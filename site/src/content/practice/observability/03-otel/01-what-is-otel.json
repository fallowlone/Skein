{
  "lessonKey": "observability/03-otel/01-what-is-otel",
  "track": "observability",
  "tasks": [
    {
      "id": "predict-noop-api",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "What runs when only the API is present?", "ru": "Что выполняется, когда есть только API?" },
      "prompt": { "en": "Predict the runtime behaviour before revealing the answer. The OTel API is intentionally lightweight — its default implementation is a no-op.", "ru": "Предскажи runtime-поведение, прежде чем открыть ответ. OTel API намеренно лёгкий — его дефолтная реализация это no-op." },
      "scenario": { "en": "A third-party library calls <code>tracer.start_span()</code> from the OTel API. The application that pulled in this library has installed the OTel API package but has <em>not</em> installed any OTel SDK. The service starts and the library code runs. What telemetry comes out?", "ru": "Сторонняя библиотека вызывает <code>tracer.start_span()</code> из OTel API. Приложение, подтянувшее эту библиотеку, поставило пакет OTel API, но <em>не</em> поставило никакой OTel SDK. Сервис стартует, код библиотеки выполняется. Какая телеметрия выходит наружу?" },
      "reveal": { "en": "None. With the API present but no SDK installed, the API resolves to its no-op default implementation: <code>start_span()</code> returns a do-nothing span, no records are built, nothing is exported. This is the whole point of the API/SDK split — a library can depend only on the small, stable OTel API and emit spans without forcing any SDK (or vendor) on its users. The application owner installs the SDK at deploy time to turn the no-ops into real telemetry.", "ru": "Никакая. При наличии API, но без установленного SDK, API резолвится в свою no-op-реализацию по умолчанию: <code>start_span()</code> возвращает ничего-не-делающий span, записи не строятся, ничего не экспортится. В этом весь смысл разделения API/SDK — библиотека зависит только от маленького стабильного OTel API и эмитит span'ы, не навязывая пользователям ни SDK, ни вендора. Application owner ставит SDK на деплое, чтобы превратить no-op'ы в реальную телеметрию." }
    },
    {
      "id": "diagnose-name-the-piece",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Name the piece for each job", "ru": "Назови часть под каждую задачу" },
      "prompt": { "en": "Each row below describes a job in the OTel pipeline. Fill in the blanks with the one piece — API, SDK, Collector, or OTLP — that owns it.", "ru": "Каждая строка ниже описывает задачу в пайплайне OTel. Заполни пропуски той единственной частью — API, SDK, Collector или OTLP — которая ею владеет." },
      "evidence": { "en": "Job A: a tail-sampling + redaction policy lives in YAML, runs outside the app, and routes telemetry to two backends at once.\nJob B: it owns batching and the per-trace keep/drop sampling decision, and serializes records before export.\nJob C: protobuf-encoded messages over gRPC/HTTP that any pair of OTel-aware components speak.\nJob D: the public interface application code imports; no-op by default.", "ru": "Job A: tail-sampling + redaction-политика живёт в YAML, выполняется вне приложения и маршрутизирует телеметрию сразу в два backend'а.\nJob B: владеет батчингом и решением keep/drop сэмплинга для каждого трейса, сериализует записи перед экспортом.\nJob C: protobuf-кодированные сообщения по gRPC/HTTP, на которых говорит любая пара OTel-aware компонентов.\nJob D: публичный интерфейс, который импортирует код приложения; no-op по умолчанию." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "a", "accept": ["collector", "otel collector", "the collector"], "hint": { "en": "A standalone process configured in YAML, outside the application — the policy layer.", "ru": "Standalone-процесс, конфигурируемый в YAML, вне приложения — policy-слой." } },
          { "id": "b", "accept": ["sdk", "otel sdk", "the sdk"], "hint": { "en": "The runtime piece installed by the application owner that turns API calls into records.", "ru": "Runtime-часть, устанавливаемая application owner, превращающая вызовы API в записи." } },
          { "id": "c", "accept": ["otlp", "otlp wire format", "the otlp protocol"], "hint": { "en": "Not a process — a wire format, stable across versions.", "ru": "Не процесс — wire-формат, стабильный через версии." } },
          { "id": "d", "accept": ["api", "otel api", "the api"], "hint": { "en": "What library and application code call; lightweight on purpose.", "ru": "То, к чему обращается код библиотек и приложения; намеренно лёгкий." } }
        ]
      }
    },
    {
      "id": "incident-swap-backend",
      "type": "incident",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Migrate Datadog to Honeycomb without touching app code", "ru": "Перевези с Datadog на Honeycomb, не трогая код приложения" },
      "prompt": { "en": "Finance says cut the observability bill: move 50 OTel-instrumented services off Datadog onto Honeycomb. Bea fears a four-engineer-week rewrite. Work the migration the OTel way.", "ru": "Финансы требуют срезать счёт за observability: перевести 50 сервисов на OTel с Datadog на Honeycomb. Bea боится переписывания на четыре инженер-недели. Проведи миграцию по-OTel'ному." },
      "steps": [
        {
          "label": { "en": "Locate the change", "ru": "Найди место правки" },
          "prompt": { "en": "Bea asks: 'Do we rewrite every service?' Where does the backend choice actually live, and how many code changes are required?", "ru": "Bea спрашивает: «Нам переписывать каждый сервис?» Где на самом деле живёт выбор backend'а и сколько правок кода нужно?" },
          "reveal": { "en": "Zero code changes. Application code calls only the OTel API; the SDK emits OTLP to the Collector; the backend is selected entirely by the Collector's exporter config. The change lives in one place — the Collector YAML — not in any of the 50 services.", "ru": "Ноль правок кода. Код приложения вызывает только OTel API; SDK эмитит OTLP в Collector; backend выбирается целиком конфигом exporter'а Collector'а. Правка в одном месте — в Collector YAML — а не в одном из 50 сервисов." }
        },
        {
          "label": { "en": "Make the edit", "ru": "Внеси правку" },
          "prompt": { "en": "Concretely, what do you change in the Collector to point telemetry at Honeycomb instead of Datadog?", "ru": "Конкретно: что меняешь в Collector'е, чтобы направить телеметрию в Honeycomb вместо Datadog?" },
          "reveal": { "en": "Swap the exporter block: change the exporter type/endpoint to Honeycomb's OTLP endpoint and set the new API key, then wire it into the pipeline's exporters list. Three fields — exporter, endpoint, API key. Optionally run both exporters in parallel for a dual-write validation window before dropping Datadog.", "ru": "Замени exporter-блок: смени тип/endpoint exporter'а на OTLP-endpoint Honeycomb и пропиши новый API-ключ, затем подключи его в список exporters пайплайна. Три поля — exporter, endpoint, API-ключ. Опционально — гоняй оба exporter'а параллельно как dual-write для окна валидации, прежде чем выключить Datadog." }
        },
        {
          "label": { "en": "Roll out", "ru": "Выкати" },
          "prompt": { "en": "What do you redeploy, and what stays untouched?", "ru": "Что редеплоишь, а что остаётся нетронутым?" },
          "reveal": { "en": "Redeploy only the Collector (the standalone process / sidecar / gateway). The 50 application services keep running unchanged — they keep emitting the same OTLP at the edge. No app rebuild, no re-instrumentation, no four-week rewrite.", "ru": "Редеплоишь только Collector (standalone-процесс / sidecar / gateway). 50 сервисов приложения работают без изменений — продолжают эмитить тот же OTLP на edge. Никакого ребилда приложения, ре-инструментации и переписывания на четыре недели." }
        },
        {
          "label": { "en": "Find the catch", "ru": "Найди подвох" },
          "prompt": { "en": "The Crux warns 'instrument once, route anywhere — but only if your edge speaks OTLP.' What single condition makes this whole one-day migration possible, and when does it break down?", "ru": "Crux предупреждает: «инструментируй один раз, маршрутизируй куда угодно — но только если edge говорит OTLP». Какое единственное условие делает эту однодневную миграцию возможной и когда оно ломается?" },
          "reveal": { "en": "The condition: the application edge must already emit OTLP. If services were wired to a proprietary vendor agent (dd-trace, the New Relic agent) instead of the OTel API+SDK, the backend is hard-coded into the instrumentation and you are back to the four-engineer-week rewrite. Vendor neutrality is bought at instrumentation time, not migration time — OTLP at the edge is the precondition, not an afterthought.", "ru": "Условие: edge приложения уже должен эмитить OTLP. Если сервисы были завязаны на проприетарный vendor-агент (dd-trace, агент New Relic) вместо OTel API+SDK, backend зашит в инструментацию, и ты снова в переписывании на четыре инженер-недели. Vendor-нейтральность покупается во время инструментации, а не во время миграции — OTLP на edge это предусловие, а не запоздалая мысль." }
        }
      ]
    },
    {
      "id": "design-escape-lock-in",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Design instrumentation that escapes vendor lock-in", "ru": "Спроектируй инструментацию, уходящую от vendor lock-in" },
      "prompt": { "en": "A greenfield platform must never face a re-instrument-everything migration when it changes observability vendor. Lay out the layering and the portability contract, then self-grade against the rubric.", "ru": "Гринфилд-платформа не должна никогда столкнуться с миграцией «переинструментируй всё» при смене observability-вендора. Разложи слои и контракт портабельности, затем оцени себя по рубрике." },
      "constraints": { "en": "20+ services across Java, Python, and Go. Third-party libraries must be able to emit spans without forcing a vendor on consumers. Today's backend is Datadog; the business expects to re-evaluate vendors yearly. Switching cost target: one config change + one redeploy, zero application code edits. Note what OTel is NOT responsible for (storage, query UI, dashboards).", "ru": "20+ сервисов на Java, Python и Go. Сторонние библиотеки должны эмитить span'ы, не навязывая вендора потребителям. Сегодняшний backend — Datadog; бизнес планирует пересматривать вендоров ежегодно. Целевая стоимость переключения: одна правка конфига + один редеплой, ноль правок кода приложения. Отметь, за что OTel НЕ отвечает (хранилище, UI запросов, дашборды)." },
      "rubric": [
        { "en": "Application and library code depend only on the OTel API (no-op by default), never on a vendor library — so libraries can emit spans without locking consumers.", "ru": "Код приложения и библиотек зависит только от OTel API (no-op по умолчанию), никогда от vendor-библиотеки — чтобы библиотеки эмитили span'ы, не залочивая потребителей." },
        { "en": "The SDK (installed by the application owner) emits OTLP; the backend choice lives solely in the Collector's exporter config, making a vendor switch a YAML+redeploy.", "ru": "SDK (ставит application owner) эмитит OTLP; выбор backend'а живёт только в exporter-конфиге Collector'а, превращая смену вендора в YAML+редеплой." },
        { "en": "States the portability contract — emit OTLP at the edge — and that OTel is NOT a backend/storage/query UI; the vendor still provides storage and dashboards.", "ru": "Формулирует контракт портабельности — эмитируй OTLP на edge — и что OTel это НЕ backend/хранилище/UI запросов; вендор всё ещё даёт хранилище и дашборды." }
      ],
      "model": { "en": "Layer it as the four OTel pieces. (1) <strong>API</strong>: every service and shared library imports only the language OTel API (<code>io.opentelemetry.api</code>, <code>opentelemetry.trace</code>, <code>go.opentelemetry.io/otel</code>) — no-op until an SDK is present, so a library can emit spans without forcing a vendor on its users. (2) <strong>SDK</strong>: the application owner installs the OTel SDK per service; it owns sampling, batching, and OTLP serialization, and is swappable without touching app code. (3) <strong>Collector</strong>: a gateway/sidecar receives OTLP and holds the only backend-specific config — the exporter block. Yearly vendor re-evaluation = edit one exporter (type, endpoint, API key) and redeploy the Collector; the 20+ services never change. (4) <strong>OTLP</strong> is the stable protobuf wire format binding the layers. Crucially, OTel is NOT a backend: it does not store, index, query, or render telemetry — Datadog (or its successor) still provides storage, the query engine, and dashboards. Portability is bought up front by emitting OTLP at the edge; the architecture, not a marketing claim, is what caps switching cost at one config change.", "ru": "Разложи на четыре части OTel. (1) <strong>API</strong>: каждый сервис и общая библиотека импортируют только language OTel API (<code>io.opentelemetry.api</code>, <code>opentelemetry.trace</code>, <code>go.opentelemetry.io/otel</code>) — no-op, пока нет SDK, так что библиотека эмитит span'ы, не навязывая вендора пользователям. (2) <strong>SDK</strong>: application owner ставит OTel SDK на каждый сервис; он владеет сэмплингом, батчингом и OTLP-сериализацией и заменяем без правки кода. (3) <strong>Collector</strong>: gateway/sidecar принимает OTLP и держит единственный backend-специфичный конфиг — exporter-блок. Ежегодный пересмотр вендора = правка одного exporter'а (тип, endpoint, API-ключ) и редеплой Collector'а; 20+ сервисов не меняются. (4) <strong>OTLP</strong> — стабильный protobuf wire-формат, связывающий слои. Важно: OTel это НЕ backend — он не хранит, не индексирует, не запрашивает и не рендерит телеметрию; Datadog (или его преемник) всё ещё даёт хранилище, движок запросов и дашборды. Портабельность покупается заранее эмитом OTLP на edge; стоимость переключения в одну правку конфига держит архитектура, а не маркетинговое заявление." }
    },
    {
      "id": "review-otel-layering",
      "type": "review",
      "difficulty": "stretch",
      "estMin": 12,
      "title": { "en": "Review how a shared library wires up OTel", "ru": "Отревьюй, как общая библиотека подключает OTel" },
      "prompt": { "en": "This is the telemetry setup inside a reusable HTTP-client library that dozens of services depend on. Review it like a senior who cares about vendor neutrality: name the real layering defects that lock consumers in; don't flag the non-issues.", "ru": "Это настройка телеметрии внутри переиспользуемой HTTP-клиент-библиотеки, от которой зависят десятки сервисов. Отревьюй как senior, заботящийся о vendor-нейтральности: назови реальные дефекты слоёв, залочивающие потребителей; не цепляйся к не-проблемам." },
      "diff": {
        "lang": "js",
        "code": "// inside @acme/http-client, imported by ~40 services\nimport { NodeSDK } from '@opentelemetry/sdk-node';          // (1)\nimport { DatadogExporter } from '@datadog/otel-exporter';   // (2)\n\nconst sdk = new NodeSDK({\n  traceExporter: new DatadogExporter({ apiKey: process.env.DD_KEY }),  // (3)\n});\nsdk.start();                                                 // (4)\n\nexport function get(url) {\n  const span = sdk.tracer('http').startSpan('GET ' + url);\n  // ...\n}"
      },
      "findings": [
        { "id": "library-imports-sdk", "severity": "bug", "planted": true, "label": { "en": "A library installs the SDK instead of depending on the API", "ru": "Библиотека ставит SDK вместо зависимости от API" }, "explanation": { "en": "A reusable library must depend only on the OTel API (no-op by default) and let the application owner install the SDK. By importing and starting `NodeSDK` itself, the library forces an SDK — and a specific configuration — onto all ~40 consumers, the exact coupling the API/SDK split exists to prevent.", "ru": "Переиспользуемая библиотека должна зависеть только от OTel API (no-op по умолчанию) и давать application owner ставить SDK. Импортируя и стартуя `NodeSDK` сама, библиотека навязывает SDK — и конкретную конфигурацию — всем ~40 потребителям, ровно ту связанность, ради предотвращения которой и существует разделение API/SDK." } },
        { "id": "vendor-exporter-in-code", "severity": "bug", "planted": true, "label": { "en": "The Datadog backend is hard-coded into the library", "ru": "Datadog-backend зашит в библиотеку" }, "explanation": { "en": "Lines (2)-(3) bake the Datadog exporter into shared code. The backend choice belongs solely in the Collector's exporter config; here a vendor switch means re-releasing the library and bumping it in 40 services — back to a multi-week migration. Emit OTLP at the edge and route in the Collector instead.", "ru": "Строки (2)-(3) впекают Datadog-exporter в общий код. Выбор backend'а принадлежит только exporter-конфигу Collector'а; здесь смена вендора означает релиз библиотеки и её апгрейд в 40 сервисах — снова многонедельная миграция. Эмить OTLP на edge и маршрутизировать в Collector'е." } },
        { "id": "sdk-start-side-effect", "severity": "bug", "planted": true, "label": { "en": "Library calls sdk.start() as an import side effect", "ru": "Библиотека вызывает sdk.start() как побочный эффект импорта" }, "explanation": { "en": "`sdk.start()` runs at module load (4), so merely importing the client boots a global SDK and seizes provider registration from the application owner. Two such libraries fight over the global tracer provider. Lifecycle (start/shutdown) is the application's job, not a library's import side effect.", "ru": "`sdk.start()` выполняется при загрузке модуля (4), поэтому сам импорт клиента поднимает глобальный SDK и отбирает регистрацию провайдера у application owner. Две такие библиотеки дерутся за глобальный tracer provider. Жизненный цикл (start/shutdown) — дело приложения, а не побочный эффект импорта библиотеки." } },
        { "id": "no-neutrality-test", "severity": "missing-test", "planted": true, "label": { "en": "No test that the library emits spans with no SDK present", "ru": "Нет теста, что библиотека эмитит span'ы без установленного SDK" }, "explanation": { "en": "There is no test asserting the library works (no-ops cleanly) when only the OTel API is installed. That test is the contract of a neutral library and would have failed loudly against this SDK-importing code.", "ru": "Нет теста, утверждающего, что библиотека работает (чисто no-op'ит), когда установлен только OTel API. Этот тест — контракт нейтральной библиотеки, и он громко упал бы против этого кода, импортирующего SDK." } }
      ],
      "decoys": [
        { "id": "span-name-concat", "label": { "en": "Span name built by string concatenation", "ru": "Имя span'а через конкатенацию строк" }, "explanation": { "en": "Stylistic. `'GET ' + url` as a span name is fine (though high-cardinality URLs are better as attributes); not the lock-in defect under review.", "ru": "Стилистика. `'GET ' + url` как имя span'а нормально (хотя URL высокой кардинальности лучше в атрибуты); не тот lock-in-дефект, что ревьюим." } },
        { "id": "env-var-key", "label": { "en": "Reading the API key from an env var", "ru": "Чтение API-ключа из env-переменной" }, "explanation": { "en": "Reading a secret from the environment is correct practice — not a defect. The defect is that the exporter exists in library code at all, not where its key comes from.", "ru": "Чтение секрета из окружения — правильная практика, не дефект. Дефект в том, что exporter вообще есть в коде библиотеки, а не в источнике его ключа." } }
      ]
    }
  ]
}
