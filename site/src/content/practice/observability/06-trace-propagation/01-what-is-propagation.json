{
  "lessonKey": "observability/06-trace-propagation/01-what-is-propagation",
  "track": "observability",
  "tasks": [
    {
      "id": "recall-header-and-fields",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Name the header and the three steps each hop owes", "ru": "Назови заголовок и три шага, которые обязан сделать каждый hop" },
      "prompt": { "en": "A junior asks how a request stays in one trace across services. Fill in the blanks: name the W3C header and the rule that keeps every span in the same trace.", "ru": "Джуниор спрашивает, как запрос остаётся в одном трейсе между сервисами. Заполни пропуски: назови W3C-заголовок и правило, удерживающее каждый span в одном трейсе." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "b1", "accept": ["traceparent"], "hint": { "en": "The 55-byte W3C Trace Context header. Not x-request-id.", "ru": "55-байтный заголовок W3C Trace Context. Не x-request-id." } },
          { "id": "b2", "accept": ["trace-id", "trace id", "traceid"], "hint": { "en": "This stays constant on every hop; only the parent-id changes.", "ru": "Это остаётся постоянным на каждом hop; меняется только parent-id." } },
          { "id": "b3", "accept": ["span-id", "span id", "spanid", "parent span-id", "parent-id"], "hint": { "en": "Each service generates a new one and sets it as the parent for downstream calls.", "ru": "Каждый сервис генерирует новый и ставит его как parent для исходящих вызовов." } }
        ]
      }
    },
    {
      "id": "predict-missing-hop",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Predict the dashboard when one hop drops the header", "ru": "Предскажи дашборд, когда один hop теряет заголовок" },
      "prompt": { "en": "Read the scenario, predict exactly what the tracing dashboard shows and why the slow service stays invisible, then reveal.", "ru": "Прочитай сценарий, предскажи, что именно покажет дашборд трейсинга и почему медленный сервис останется невидимым, затем открой ответ." },
      "scenario": { "en": "Request path: <code>API gateway → auth → inventory → payment</code>. Gateway, auth, and payment forward <code>traceparent</code> correctly. Inventory was deployed without HTTP-client auto-instrumentation, so it reads the incoming header but does NOT write a fresh <code>traceparent</code> on its outbound call to payment. Inventory itself spends 28 seconds on a DB query. A customer reports a 30-second checkout.", "ru": "Путь запроса: <code>API gateway → auth → inventory → payment</code>. Gateway, auth и payment корректно пробрасывают <code>traceparent</code>. Inventory выкатили без HTTP-client auto-instrumentation, поэтому он читает входящий заголовок, но НЕ пишет свежий <code>traceparent</code> на исходящем вызове в payment. Сам inventory тратит 28 секунд на DB-запрос. Клиент сообщает о 30-секундном checkout." },
      "reveal": { "en": "You get two disconnected traces, not one. Trace 1: gateway → auth → inventory, sharing one trace-id — and inventory's 28-second span IS visible here, so this fragment looks correct. Trace 2: payment alone, with a brand-new trace-id it minted because no valid parent context arrived — an orphan single-span trace. The end-to-end tree <code>API → auth → inventory → payment</code> never forms. The danger: an engineer who opens the payment orphan sees a fast 200ms trace and a healthy dashboard, never linking it to the slow checkout. The break is at the inject step on inventory's outbound call — the in-process span was fine; the cross-process boundary lost the context.", "ru": "Получаешь два несвязанных трейса, а не один. Трейс 1: gateway → auth → inventory с одним trace-id — и 28-секундный span inventory ВИДЕН здесь, так что этот фрагмент выглядит верным. Трейс 2: один payment с новым trace-id, который он сгенерировал, потому что валидный parent-context не пришёл — orphan single-span трейс. Сквозное дерево <code>API → auth → inventory → payment</code> так и не складывается. Опасность: инженер, открывший orphan payment, видит быстрый 200ms трейс и здоровый дашборд, никогда не связывая его с медленным checkout. Разрыв — на шаге inject на исходящем вызове inventory: in-process span был в порядке; границу между процессами context не пережил." }
    },
    {
      "id": "incident-trace-splits-at-boundary",
      "type": "incident",
      "difficulty": "apply",
      "estMin": 12,
      "title": { "en": "Traces split at a service boundary", "ru": "Трейсы расходятся на границе сервиса" },
      "prompt": { "en": "Checkout p99 is awful but every trace in the dashboard is exactly one span deep. Work the incident hop by hop.", "ru": "p99 checkout ужасен, но каждый трейс в дашборде глубиной ровно в один span. Разбери инцидент hop за hop." },
      "steps": [
        {
          "label": { "en": "Confirm the symptom", "ru": "Подтверди симптом" },
          "prompt": { "en": "The dashboard is full of traces, yet you can't follow checkout end-to-end. What is the precise observable that tells you propagation, not data collection, is broken?", "ru": "Дашборд полон трейсов, но checkout не пройти end-to-end. Какой именно наблюдаемый признак говорит, что сломана propagation, а не сбор данных?" },
          "reveal": { "en": "Every trace is one span deep and none share a trace-id with another service's span. Spans exist (collection works), but no parent-child links cross a service boundary — the classic broken-propagation signature, which is worse than no tracing because the dashboard still claims end-to-end visibility.", "ru": "Каждый трейс глубиной в один span, и ни один не делит trace-id со span'ом другого сервиса. Span'ы есть (сбор работает), но parent-child связей через границу сервиса нет — классическая подпись сломанной propagation, которая хуже отсутствия трейсов, потому что дашборд всё ещё заявляет о сквозной видимости." }
        },
        {
          "label": { "en": "Find the breaking hop", "ru": "Найди ломающий hop" },
          "prompt": { "en": "How do you locate which service stops forwarding the header, using only the request/response headers on each hop?", "ru": "Как найти, какой сервис перестаёт пробрасывать заголовок, используя только заголовки запроса/ответа на каждом hop?" },
          "reveal": { "en": "Replay one checkout and inspect inbound headers at each service. Where <code>traceparent</code> is present on a service's inbound request but absent on that same service's outbound request, that service is the broken hop — it received context but failed the inject step before its downstream call.", "ru": "Повтори один checkout и проверь входящие заголовки на каждом сервисе. Там, где <code>traceparent</code> есть на входящем запросе сервиса, но отсутствует на его же исходящем — этот сервис и есть сломанный hop: он получил context, но провалил шаг inject перед вызовом downstream." }
        },
        {
          "label": { "en": "Root cause", "ru": "Корневая причина" },
          "prompt": { "en": "The broken service has tracing enabled and emits its own spans. Why does it still drop the chain?", "ru": "Сломанный сервис имеет включённый tracing и эмитирует свои span'ы. Почему он всё равно рвёт цепочку?" },
          "reveal": { "en": "In-process tracing was set up, but the outbound HTTP client was never instrumented to inject context. The service extracts the incoming context fine, yet starts every outbound call without writing a fresh <code>traceparent</code>, so the next service sees no parent and mints a brand-new trace-id — splitting the trace at exactly this boundary.", "ru": "In-process tracing настроен, но исходящий HTTP-клиент так и не научили inject'ить context. Сервис нормально извлекает входящий context, но стартует каждый исходящий вызов, не записав свежий <code>traceparent</code>, поэтому следующий сервис не видит parent и генерирует новый trace-id — расщепляя трейс ровно на этой границе." }
        },
        {
          "label": { "en": "Fix and verify", "ru": "Фикс и проверка" },
          "prompt": { "en": "What restores one connected trace, and how do you prove it worked?", "ru": "Что восстанавливает один связный трейс и как доказать, что сработало?" },
          "reveal": { "en": "Enable HTTP-client auto-instrumentation (or manually inject the propagator) on the broken service's outbound calls so it writes a fresh <code>traceparent</code> carrying the same trace-id and its own span-id as parent-id. Verify by replaying checkout and confirming a single trace with the full tree <code>gateway → auth → inventory → payment</code> sharing one trace-id, top-to-bottom in seconds.", "ru": "Включи HTTP-client auto-instrumentation (или вручную добавь propagator) на исходящих вызовах сломанного сервиса, чтобы он писал свежий <code>traceparent</code> с тем же trace-id и своим span-id как parent-id. Проверь, повторив checkout и убедившись, что есть один трейс с полным деревом <code>gateway → auth → inventory → payment</code> с общим trace-id, проходимый сверху вниз за секунды." }
        }
      ]
    },
    {
      "id": "design-propagation-rollout",
      "type": "design",
      "difficulty": "stretch",
      "estMin": 14,
      "title": { "en": "Design a propagation contract for a 12-service mesh", "ru": "Спроектируй контракт propagation для меша из 12 сервисов" },
      "prompt": { "en": "Design the rollout and guardrails so trace context survives every hop across a heterogeneous fleet. Self-grade against the rubric.", "ru": "Спроектируй раскатку и гардрейлы, чтобы trace-context выживал на каждом hop в разнородном флоте. Оцени себя по рубрике." },
      "constraints": { "en": "12 services: 7 use OTel auto-instrumentation, 3 are legacy apps with hand-rolled HTTP clients, 2 sit behind a queue (async). Some inbound traffic arrives with no <code>traceparent</code> (third-party callers). You must avoid the 'broken propagation is worse than none' trap and detect a missing hop before customers do.", "ru": "12 сервисов: 7 на OTel auto-instrumentation, 3 — legacy с самописными HTTP-клиентами, 2 за очередью (async). Часть входящего трафика приходит без <code>traceparent</code> (сторонние вызывающие). Нужно избежать ловушки «сломанная propagation хуже отсутствия» и обнаружить пропавший hop раньше клиентов." },
      "rubric": [
        { "en": "Mandates the W3C traceparent contract on every hop: extract on inbound, inject on every outbound (HTTP and queue), trace-id constant, new span-id per service.", "ru": "Обязывает контракт W3C traceparent на каждом hop: extract на входе, inject на каждом исходящем (HTTP и очередь), trace-id постоянен, новый span-id на сервис." },
        { "en": "Handles the legacy and async edges explicitly: manual propagator inject in hand-rolled clients; carry traceparent in message metadata across the queue so async spans rejoin the trace.", "ru": "Явно закрывает legacy и async края: ручной inject propagator'а в самописных клиентах; перенос traceparent в метаданных сообщения через очередь, чтобы async-span'ы воссоединились с трейсом." },
        { "en": "Adds a detection guardrail (e.g. alert on orphan/single-span trace rate or on hops where inbound traceparent has no matching outbound) so a broken hop is caught before customers report it.", "ru": "Добавляет гардрейл детекции (например, алерт на долю orphan/single-span трейсов или на hop'ы, где у входящего traceparent нет соответствующего исходящего), чтобы сломанный hop ловился до жалоб клиентов." }
      ],
      "model": { "en": "Make the propagation contract a platform standard, not per-team luck: every service must extract <code>traceparent</code> on inbound and inject a fresh one (same trace-id, own span-id as parent-id) on every outbound HTTP call and every queue publish. For the 7 OTel services, enable the W3C propagator and HTTP-client auto-instrumentation. For the 3 legacy services, wire the propagator's inject/extract manually around the hand-rolled clients. For the 2 async services, stamp <code>traceparent</code> into message metadata on publish and extract it on consume so spans across the queue share one trace-id. For third-party traffic with no incoming context, start a fresh root trace at the edge gateway rather than dropping it. Finally, guard the whole thing: alert when the single-span/orphan trace ratio rises, or when any hop shows inbound traceparent without a matching outbound — that catches a regression at the boundary before a customer files a 30-second-checkout ticket.", "ru": "Сделай контракт propagation платформенным стандартом, а не везением каждой команды: каждый сервис обязан extract'ить <code>traceparent</code> на входе и inject'ить свежий (тот же trace-id, свой span-id как parent-id) на каждом исходящем HTTP-вызове и каждой публикации в очередь. Для 7 OTel-сервисов включи W3C-propagator и HTTP-client auto-instrumentation. Для 3 legacy вручную обвяжи inject/extract propagator'а вокруг самописных клиентов. Для 2 async-сервисов штампуй <code>traceparent</code> в метаданные сообщения при публикации и извлекай при consume, чтобы span'ы через очередь делили один trace-id. Для стороннего трафика без входящего context'а стартуй свежий root-трейс на edge-gateway, а не выбрасывай его. Наконец, защити всё гардрейлом: алерт, когда растёт доля single-span/orphan трейсов или когда у какого-то hop есть входящий traceparent без соответствующего исходящего — это ловит регресс на границе раньше, чем клиент заведёт тикет о 30-секундном checkout." }
    }
  ]
}
