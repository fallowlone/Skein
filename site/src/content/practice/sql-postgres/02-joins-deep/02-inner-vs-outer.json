{
  "lessonKey": "sql-postgres/02-joins-deep/02-inner-vs-outer",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-demoted-join",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Will the non-orderers survive?", "ru": "Выживут ли незаказывавшие?" },
      "prompt": { "en": "Decide what the query returns for users who never ordered, then reveal.", "ru": "Реши, что запрос вернёт для пользователей, которые никогда не заказывали, затем открой." },
      "scenario": { "en": "There are 3 users; only u1 has an order, and its status is 'shipped'. The query is:<br><code>SELECT u.id FROM users u LEFT JOIN orders o ON o.user_id = u.id WHERE o.status = 'paid';</code><br>How many rows come back, and which users?", "ru": "Есть 3 пользователя; заказ есть только у u1, его статус 'shipped'. Запрос:<br><code>SELECT u.id FROM users u LEFT JOIN orders o ON o.user_id = u.id WHERE o.status = 'paid';</code><br>Сколько строк вернётся и какие пользователи?" },
      "reveal": { "en": "<b>Zero rows.</b> The LEFT JOIN NULL-extends u2 and u3 (no orders) and keeps u1 paired with its 'shipped' order. Then WHERE o.status = 'paid' runs: u2/u3 have NULL status (NULL = 'paid' is UNKNOWN → dropped), and u1's status is 'shipped' not 'paid' (also dropped). The WHERE on the outer table demoted the LEFT JOIN to an INNER JOIN and then filtered out the only match. Moving the condition into ON (<code>... AND o.status = 'paid'</code>) would instead return all 3 users, NULL-extended.", "ru": "<b>Ноль строк.</b> LEFT JOIN NULL-расширяет u2 и u3 (без заказов) и оставляет u1 спаренным со своим 'shipped'-заказом. Затем выполняется WHERE o.status = 'paid': у u2/u3 статус NULL (NULL = 'paid' это UNKNOWN → выброшены), а у u1 статус 'shipped', а не 'paid' (тоже выброшен). WHERE на внешней таблице низвёл LEFT JOIN до INNER JOIN и затем отфильтровал единственное совпадение. Перенос условия в ON (<code>... AND o.status = 'paid'</code>) вместо этого вернул бы всех 3 пользователей, NULL-расширенными." }
    },
    {
      "id": "sandbox-users-with-no-orders",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Find users who never ordered", "ru": "Найди пользователей, которые никогда не заказывали" },
      "prompt": { "en": "The schema is seeded. Write a LEFT JOIN that returns only the users with zero orders, ordered by id. (Hint: keep the outer rows, then keep only the NULL-extended ones.)", "ru": "Схема засеяна. Напиши LEFT JOIN, возвращающий только пользователей с нулём заказов, упорядоченных по id. (Подсказка: оставь строки outer, затем оставь лишь NULL-расширенные.)" },
      "runtime": "sql",
      "setup": "CREATE TABLE users(id int primary key, name text); CREATE TABLE orders(id int primary key, user_id int, status text); INSERT INTO users VALUES (1,'a'),(2,'b'),(3,'c'),(4,'d'); INSERT INTO orders VALUES (10,1,'shipped'),(11,3,'paid');",
      "expected": { "kind": "rows-equal", "value": "SELECT u.id FROM users u LEFT JOIN orders o ON o.user_id = u.id WHERE o.id IS NULL ORDER BY u.id;" }
    },
    {
      "id": "diagnose-count-star",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Why does every user show 1 order?", "ru": "Почему у каждого пользователя показан 1 заказ?" },
      "prompt": { "en": "A 'orders per user' report shows a minimum of 1 even for brand-new users who clearly ordered nothing. Name the aggregate bug and the fix.", "ru": "Отчёт «заказов на пользователя» показывает минимум 1 даже для новичков, которые явно ничего не заказывали. Назови баг агрегата и фикс." },
      "evidence": { "en": "SELECT u.id, COUNT(*) AS orders\nFROM users u\nLEFT JOIN orders o ON o.user_id = u.id\nGROUP BY u.id;\n-- a user with no orders shows orders = 1", "ru": "SELECT u.id, COUNT(*) AS orders\nFROM users u\nLEFT JOIN orders o ON o.user_id = u.id\nGROUP BY u.id;\n-- пользователь без заказов показывает orders = 1" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "bug", "accept": ["count(*)", "count star", "counting the null-extended row", "count(*) counts the null row", "null-extended row"], "hint": { "en": "The LEFT JOIN emits one NULL-extended row per orderless user; what does COUNT(*) do with that row?", "ru": "LEFT JOIN выдаёт по одной NULL-расширенной строке на пользователя без заказа; что делает с ней COUNT(*)?" } },
          { "id": "fix", "accept": ["count(o.id)", "count(o.*)", "count the order id", "count a non-null order column"], "hint": { "en": "Count a column from the order side that is NULL when there is no order.", "ru": "Считай колонку со стороны заказа, которая NULL при отсутствии заказа." } }
        ]
      }
    },
    {
      "id": "fix-move-predicate-to-on",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 7,
      "title": { "en": "Keep the outer semantics with a status filter", "ru": "Сохрани семантику outer с фильтром по статусу" },
      "prompt": { "en": "Rewrite the query so it returns EVERY user along with the count of their 'shipped' orders — zero for users with none — without losing any user. Explain why your placement of the status predicate is correct.", "ru": "Перепиши запрос так, чтобы он возвращал КАЖДОГО пользователя с числом его 'shipped'-заказов — ноль у тех, у кого их нет — не теряя ни одного пользователя. Объясни, почему твоё размещение предиката по статусу верно." },
      "starter": "-- broken: WHERE demotes the LEFT JOIN and drops users with no shipped order\n-- SELECT u.id, COUNT(o.id) FROM users u\n--   LEFT JOIN orders o ON o.user_id = u.id\n--   WHERE o.status = 'shipped' GROUP BY u.id;",
      "grading": {
        "mode": "self",
        "model": { "en": "Move the status condition into ON so it constrains which orders count as a match, before NULL-extension:<br><code>SELECT u.id, COUNT(o.id) AS shipped_orders FROM users u LEFT JOIN orders o ON o.user_id = u.id AND o.status = 'shipped' GROUP BY u.id;</code><br>Now a user whose orders are all non-'shipped' (or who has no orders) is still NULL-extended and kept, so COUNT(o.id) correctly reports 0 for them. The status predicate must live in ON because in WHERE it runs after NULL-extension and rejects the NULL rows (NULL = 'shipped' is UNKNOWN), silently demoting the LEFT JOIN to INNER and dropping exactly the users you needed to keep.", "ru": "Перенеси условие по статусу в ON, чтобы оно ограничивало, какие заказы считаются совпадением, до NULL-расширения:<br><code>SELECT u.id, COUNT(o.id) AS shipped_orders FROM users u LEFT JOIN orders o ON o.user_id = u.id AND o.status = 'shipped' GROUP BY u.id;</code><br>Теперь пользователь, чьи заказы все не-'shipped' (или у кого заказов нет), всё равно NULL-расширяется и сохраняется, так что COUNT(o.id) корректно показывает для него 0. Предикат по статусу должен жить в ON, потому что в WHERE он выполняется после NULL-расширения и отвергает NULL-строки (NULL = 'shipped' это UNKNOWN), молча низводя LEFT JOIN до INNER и выбрасывая ровно тех пользователей, которых нужно было сохранить." },
        "rubric": [
          { "en": "Places the status predicate in ON, not WHERE.", "ru": "Помещает предикат по статусу в ON, а не в WHERE." },
          { "en": "Uses COUNT(o.id) so non-matching users get 0, not 1.", "ru": "Использует COUNT(o.id), чтобы несовпавшие пользователи получали 0, а не 1." },
          { "en": "Explains that WHERE runs after NULL-extension and demotes the LEFT JOIN to INNER.", "ru": "Объясняет, что WHERE выполняется после NULL-расширения и низводит LEFT JOIN до INNER." }
        ]
      }
    }
  ]
}
