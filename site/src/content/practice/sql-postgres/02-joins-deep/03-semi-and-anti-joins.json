{
  "lessonKey": "sql-postgres/02-joins-deep/03-semi-and-anti-joins",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-not-in-null",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Will NOT IN find the non-orderers?", "ru": "Найдёт ли NOT IN незаказывавших?" },
      "prompt": { "en": "Commit to the result before revealing.", "ru": "Зафиксируй результат, прежде чем открыть." },
      "scenario": { "en": "users has ids 1,2,3. orders.user_id holds the values {1, NULL} (one real order for user 1, one row with a NULL user_id). You run:<br><code>SELECT id FROM users WHERE id NOT IN (SELECT user_id FROM orders);</code><br>Which user ids come back?", "ru": "В users id 1,2,3. orders.user_id содержит значения {1, NULL} (один реальный заказ пользователя 1, одна строка с NULL user_id). Ты запускаешь:<br><code>SELECT id FROM users WHERE id NOT IN (SELECT user_id FROM orders);</code><br>Какие id пользователей вернутся?" },
      "reveal": { "en": "<b>None — zero rows.</b> You expected {2, 3}. But NOT IN expands to <code>id &lt;&gt; 1 AND id &lt;&gt; NULL</code>. The term <code>id &lt;&gt; NULL</code> is UNKNOWN for every id, and UNKNOWN AND anything can never be true, so WHERE rejects all rows. A single NULL in the subquery collapses the whole anti-join. Rewriting with <code>NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = users.id)</code> correctly returns {2, 3}, because the NULL order simply fails to correlate to any user.", "ru": "<b>Никакие — ноль строк.</b> Ты ожидал {2, 3}. Но NOT IN разворачивается в <code>id &lt;&gt; 1 AND id &lt;&gt; NULL</code>. Член <code>id &lt;&gt; NULL</code> это UNKNOWN для каждого id, а UNKNOWN AND что-угодно никогда не истинно, поэтому WHERE отвергает все строки. Единственный NULL в подзапросе схлопывает весь anti-join. Переписав через <code>NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = users.id)</code>, корректно вернёшь {2, 3}, потому что NULL-заказ просто не коррелирует ни с одним пользователем." }
    },
    {
      "id": "sandbox-semi-join",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "List buyers once with a semi-join", "ru": "Перечисли покупателей по разу через semi-join" },
      "prompt": { "en": "The schema is seeded; user 1 has many orders. Write a query that returns each user who has at least one order, exactly once, ordered by id — without DISTINCT and without fanning out.", "ru": "Схема засеяна; у пользователя 1 много заказов. Напиши запрос, возвращающий каждого пользователя с хотя бы одним заказом, ровно по разу, упорядоченных по id — без DISTINCT и без fan-out." },
      "runtime": "sql",
      "setup": "CREATE TABLE users(id int primary key); CREATE TABLE orders(id int primary key, user_id int); INSERT INTO users VALUES (1),(2),(3),(4); INSERT INTO orders VALUES (10,1),(11,1),(12,1),(13,3);",
      "expected": { "kind": "rows-equal", "value": "SELECT u.id FROM users u WHERE EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id) ORDER BY u.id;" }
    },
    {
      "id": "sandbox-anti-join",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Find non-buyers safely", "ru": "Безопасно найди не-покупателей" },
      "prompt": { "en": "Using the same seeded data (note: one order has a NULL user_id), write a NULL-safe anti-join that returns the users who have never ordered, ordered by id. Avoid NOT IN.", "ru": "На тех же засеянных данных (внимание: у одного заказа NULL user_id) напиши NULL-безопасный anti-join, возвращающий пользователей, которые никогда не заказывали, упорядоченных по id. Избегай NOT IN." },
      "runtime": "sql",
      "setup": "CREATE TABLE users(id int primary key); CREATE TABLE orders(id int primary key, user_id int); INSERT INTO users VALUES (1),(2),(3),(4); INSERT INTO orders VALUES (10,1),(11,3),(12,NULL);",
      "expected": { "kind": "rows-equal", "value": "SELECT u.id FROM users u WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id) ORDER BY u.id;" }
    },
    {
      "id": "fix-not-in-to-not-exists",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Make the anti-join NULL-safe", "ru": "Сделай anti-join NULL-безопасным" },
      "prompt": { "en": "A 'customers with no failed payments' query uses NOT IN and intermittently returns nobody. Rewrite it to be correct regardless of NULLs, and explain the failure precisely.", "ru": "Запрос «клиенты без неудачных платежей» использует NOT IN и периодически не возвращает никого. Перепиши его так, чтобы он был верен независимо от NULL, и объясни сбой точно." },
      "starter": "-- broken:\n-- SELECT c.id FROM customers c\n-- WHERE c.id NOT IN (SELECT customer_id FROM payments WHERE status = 'failed');",
      "grading": {
        "mode": "self",
        "model": { "en": "Rewrite as an anti-join with NOT EXISTS:<br><code>SELECT c.id FROM customers c WHERE NOT EXISTS (SELECT 1 FROM payments p WHERE p.customer_id = c.id AND p.status = 'failed');</code><br>The original fails because if any failed-payment row has a NULL customer_id, the subquery list contains a NULL; NOT IN becomes <code>c.id &lt;&gt; ... AND c.id &lt;&gt; NULL</code>, and that NULL comparison is UNKNOWN, making the conjunction never-true for every customer — zero rows, no error. NOT EXISTS instead correlates per customer and a NULL customer_id simply doesn't match, giving the intended set. (Equivalent alternative: LEFT JOIN payments ... AND status='failed' WHERE p.id IS NULL.)", "ru": "Перепиши как anti-join через NOT EXISTS:<br><code>SELECT c.id FROM customers c WHERE NOT EXISTS (SELECT 1 FROM payments p WHERE p.customer_id = c.id AND p.status = 'failed');</code><br>Оригинал ломается, потому что если у любой строки неудачного платежа NULL в customer_id, список подзапроса содержит NULL; NOT IN становится <code>c.id &lt;&gt; ... AND c.id &lt;&gt; NULL</code>, и это сравнение с NULL даёт UNKNOWN, делая конъюнкцию никогда-истинной для каждого клиента — ноль строк, без ошибки. NOT EXISTS вместо этого коррелирует на каждого клиента, и NULL customer_id просто не совпадает, давая нужное множество. (Эквивалентная альтернатива: LEFT JOIN payments ... AND status='failed' WHERE p.id IS NULL.)" },
        "rubric": [
          { "en": "Replaces NOT IN with NOT EXISTS (or LEFT JOIN ... IS NULL).", "ru": "Заменяет NOT IN на NOT EXISTS (или LEFT JOIN ... IS NULL)." },
          { "en": "Keeps the status = 'failed' filter inside the correlated subquery / join condition.", "ru": "Сохраняет фильтр status = 'failed' внутри коррелированного подзапроса / условия join." },
          { "en": "Explains that one NULL in the NOT IN list makes the predicate UNKNOWN for all rows.", "ru": "Объясняет, что один NULL в списке NOT IN делает предикат UNKNOWN для всех строк." }
        ]
      }
    }
  ]
}
