{
  "lessonKey": "sql-postgres/01-sql-foundations/05-sorting-and-limiting",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-offset-cost",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "How many rows does OFFSET touch?", "ru": "Сколько строк трогает OFFSET?" },
      "prompt": { "en": "Predict how many rows the engine must produce to serve this page, then reveal.", "ru": "Предскажи, сколько строк движок должен произвести, чтобы отдать эту страницу, затем открой ответ." },
      "scenario": { "en": "<code>SELECT * FROM events ORDER BY created_at DESC LIMIT 20 OFFSET 100000;</code> on a 5M-row table with an index on created_at. How many ordered rows must Postgres generate before it can return the 20-row page?", "ru": "<code>SELECT * FROM events ORDER BY created_at DESC LIMIT 20 OFFSET 100000;</code> на таблице в 5M строк с индексом на created_at. Сколько упорядоченных строк Postgres должен сгенерировать, прежде чем вернуть страницу из 20 строк?" },
      "reveal": { "en": "<b>100,020 rows.</b> OFFSET has no shortcut to row 100,000 — it must walk the ordered source (even via the index) producing the first 100,000 rows, discard them, then emit the next 20. Returning 20 rows but doing the work of 100,020 is the OFFSET cliff: cost is proportional to the offset, so page 5,000 is ~5,000× the work of page 1. Keyset pagination fixes this by seeking straight to the cursor and reading only 20.", "ru": "<b>100 020 строк.</b> У OFFSET нет короткого пути к строке 100 000 — он должен обойти упорядоченный источник (даже через индекс), произведя первые 100 000 строк, выбросить их, затем выдать следующие 20. Вернуть 20 строк, но сделать работу на 100 020 — это обрыв OFFSET: цена пропорциональна offset, так что страница 5000 — это ~5000× работы страницы 1. Keyset-пагинация это чинит, ища прямо к курсору и читая лишь 20." }
    },
    {
      "id": "sandbox-keyset-next-page",
      "type": "sandbox",
      "difficulty": "stretch",
      "estMin": 8,
      "title": { "en": "Fetch the next keyset page", "ru": "Возьми следующую keyset-страницу" },
      "prompt": { "en": "events is seeded with 12 rows. Assume the previous page ended at (created_at, id) = ('2026-01-09', 9). Write the keyset query for the NEXT page of 3 rows, newest first, using a row-comparison on (created_at, id) and the id tiebreaker. Verify it matches the canonical answer (rows 8, 7, 6).", "ru": "events засеяна 12 строками. Предположим, прошлая страница кончилась на (created_at, id) = ('2026-01-09', 9). Напиши keyset-запрос для СЛЕДУЮЩЕЙ страницы из 3 строк, свежайшие первыми, используя row-comparison по (created_at, id) и тайбрейкер id. Проверь совпадение с каноническим ответом (строки 8, 7, 6)." },
      "runtime": "sql",
      "setup": "CREATE TABLE events(id int primary key, title text, created_at date); INSERT INTO events SELECT g, 'e' || g, date '2026-01-01' + (g - 1) FROM generate_series(1, 12) g;",
      "expected": { "kind": "rows-equal", "value": "SELECT id, title, created_at FROM events WHERE (created_at, id) < (date '2026-01-09', 9) ORDER BY created_at DESC, id DESC LIMIT 3" }
    },
    {
      "id": "diagnose-nulls-on-top",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Why are NULL dates at the top?", "ru": "Почему NULL-даты наверху?" },
      "prompt": { "en": "A 'newest first' list unexpectedly shows rows with NULL created_at before any real date. Fill in the blanks: the default NULL placement for DESC, and the clause that pins NULLs to the bottom.", "ru": "Список «свежайшие первыми» неожиданно показывает строки с NULL created_at перед любой реальной датой. Заполни пропуски: размещение NULL по умолчанию для DESC и клаузу, прибивающую NULL вниз." },
      "evidence": { "en": "SELECT id, created_at FROM events ORDER BY created_at DESC LIMIT 5;\n-- rows with created_at = NULL appear first, before 2026-03-xx rows", "ru": "SELECT id, created_at FROM events ORDER BY created_at DESC LIMIT 5;\n-- строки с created_at = NULL идут первыми, перед строками 2026-03-xx" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "default", "accept": ["NULLS FIRST", "nulls first", "first", "FIRST"], "hint": { "en": "For DESC, Postgres places NULLs where by default?", "ru": "Для DESC Postgres размещает NULL куда по умолчанию?" } },
          { "id": "fix", "accept": ["NULLS LAST", "nulls last", "last", "LAST"], "hint": { "en": "Append this to ORDER BY to send NULLs to the bottom.", "ru": "Добавь это к ORDER BY, чтобы отправить NULL вниз." } }
        ]
      }
    },
    {
      "id": "fix-offset-to-keyset",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 7,
      "title": { "en": "Convert OFFSET pagination to keyset", "ru": "Переведи OFFSET-пагинацию в keyset" },
      "prompt": { "en": "Rewrite this infinite-scroll endpoint to keyset pagination and name the index it needs. Explain why the tiebreaker is mandatory, then reveal the model answer.", "ru": "Перепиши этот эндпоинт бесконечного скролла на keyset-пагинацию и назови нужный индекс. Объясни, почему тайбрейкер обязателен, затем открой образцовый ответ." },
      "starter": "-- current: gets slower every page\n-- SELECT id, title, created_at FROM events ORDER BY created_at DESC LIMIT 20 OFFSET :page*20;",
      "grading": {
        "mode": "self",
        "model": { "en": "Keyset version — the client sends the last row's (created_at, id) as the cursor: <code>SELECT id, title, created_at FROM events WHERE (created_at, id) < (:last_created_at, :last_id) ORDER BY created_at DESC, id DESC LIMIT 20;</code>. <b>Index:</b> a composite index on <code>(created_at DESC, id DESC)</code> so the row-comparison is a single seek to the cursor followed by 20 leaf reads — O(20) at any depth, versus OFFSET's O(offset). <b>Why the tiebreaker is mandatory:</b> if you ordered by created_at alone and two rows shared a timestamp, the cursor boundary would be ambiguous — a row could be returned twice or skipped as the cursor crosses the tie. Appending the unique id makes the ordering total, and the (created_at, id) row-comparison advances past the exact last row deterministically. Keep both columns the same direction so the single < comparison stays correct.", "ru": "Keyset-версия — клиент шлёт (created_at, id) последней строки как курсор: <code>SELECT id, title, created_at FROM events WHERE (created_at, id) < (:last_created_at, :last_id) ORDER BY created_at DESC, id DESC LIMIT 20;</code>. <b>Индекс:</b> композитный индекс на <code>(created_at DESC, id DESC)</code>, чтобы row-comparison был одним seek к курсору с последующими 20 чтениями листьев — O(20) на любой глубине против O(offset) у OFFSET. <b>Почему тайбрейкер обязателен:</b> если сортировать по одному created_at и две строки делят timestamp, граница курсора была бы неоднозначна — строка могла бы вернуться дважды или пропуститься, когда курсор пересекает равенство. Добавление уникального id делает порядок полным, а row-comparison (created_at, id) детерминированно продвигается за точную последнюю строку. Держи обе колонки в одном направлении, чтобы одиночное сравнение < оставалось корректным." },
        "rubric": [
          { "en": "Replaces OFFSET with WHERE (created_at, id) < (cursor) row-comparison.", "ru": "Заменяет OFFSET на row-comparison WHERE (created_at, id) < (курсор)." },
          { "en": "Names a composite index on (created_at DESC, id DESC) to make it a single seek.", "ru": "Называет композитный индекс на (created_at DESC, id DESC) для одного seek." },
          { "en": "Explains the unique tiebreaker prevents skipped/duplicated rows at timestamp ties.", "ru": "Объясняет, что уникальный тайбрейкер предотвращает пропуск/дублирование строк при равных timestamp." }
        ]
      }
    }
  ]
}
