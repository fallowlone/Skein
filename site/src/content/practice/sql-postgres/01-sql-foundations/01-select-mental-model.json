{
  "lessonKey": "sql-postgres/01-sql-foundations/01-select-mental-model",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-alias-in-where",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Will the alias resolve?", "ru": "Разрешится ли алиас?" },
      "prompt": { "en": "Decide whether the query below runs or errors, and why, then reveal.", "ru": "Реши, выполнится запрос или упадёт с ошибкой, и почему, затем открой ответ." },
      "scenario": { "en": "Table <code>orders(id, price, qty, status)</code>. The query is:<br><code>SELECT price * qty AS revenue FROM orders WHERE revenue > 100;</code><br>Does it return rows, or raise an error?", "ru": "Таблица <code>orders(id, price, qty, status)</code>. Запрос:<br><code>SELECT price * qty AS revenue FROM orders WHERE revenue > 100;</code><br>Вернёт строки или поднимет ошибку?" },
      "reveal": { "en": "<b>It errors:</b> <code>column \"revenue\" does not exist</code>. WHERE is evaluated at step 2 of logical processing, but the alias <code>revenue</code> is only created at step 5 (SELECT/projection). At WHERE time the name doesn't exist yet. Fixes: repeat the expression — <code>WHERE price * qty > 100</code> — or wrap it in a subquery/CTE so the alias materializes in an inner step. Note the same <code>revenue</code> would work in <code>ORDER BY</code>, which runs after SELECT.", "ru": "<b>Падает с ошибкой:</b> <code>column \"revenue\" does not exist</code>. WHERE вычисляется на шаге 2 логической обработки, но алиас <code>revenue</code> создаётся лишь на шаге 5 (SELECT/проекция). На момент WHERE имени ещё нет. Фиксы: повторить выражение — <code>WHERE price * qty > 100</code> — или обернуть в подзапрос/CTE, чтобы алиас материализовался во внутреннем шаге. Заметь: тот же <code>revenue</code> работал бы в <code>ORDER BY</code>, который бежит после SELECT." }
    },
    {
      "id": "sandbox-projection-vs-selection",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "Filter in WHERE, name in SELECT, sort by the alias", "ru": "Фильтруй в WHERE, именуй в SELECT, сортируй по алиасу" },
      "prompt": { "en": "The orders table is seeded. Write a query that returns user_id and price*qty AS revenue for paid orders only, sorted by revenue descending. Filtering must be in WHERE (on the raw expression or status), the alias must be defined in SELECT, and ORDER BY must use the alias. Run it and confirm no error.", "ru": "Таблица orders засеяна. Напиши запрос, возвращающий user_id и price*qty AS revenue только для оплаченных заказов, отсортированный по revenue убыванию. Фильтрация должна быть в WHERE (по сырому выражению или status), алиас определён в SELECT, а ORDER BY использует алиас. Запусти и убедись в отсутствии ошибки." },
      "runtime": "sql",
      "setup": "CREATE TABLE orders(id int primary key, user_id int, price numeric, qty int, status text); INSERT INTO orders SELECT g, (g % 9) + 1, (g % 50) + 1, (g % 5) + 1, CASE WHEN g % 3 = 0 THEN 'paid' ELSE 'pending' END FROM generate_series(1, 120) g;",
      "expected": { "kind": "no-error" }
    },
    {
      "id": "diagnose-order-of-clauses",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Which step did the name break at?", "ru": "На каком шаге сломалось имя?" },
      "prompt": { "en": "A query errors at name resolution. Fill in the blanks: name the logical step that created the alias, and the step that referenced it too early.", "ru": "Запрос падает на разрешении имени. Заполни пропуски: назови логический шаг, создавший алиас, и шаг, сославшийся на него слишком рано." },
      "evidence": { "en": "SELECT lower(email) AS e FROM users WHERE e LIKE 'a%';\n-- ERROR: column \"e\" does not exist", "ru": "SELECT lower(email) AS e FROM users WHERE e LIKE 'a%';\n-- ERROR: column \"e\" does not exist" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "creates", "accept": ["select", "SELECT", "projection", "select step", "select clause"], "hint": { "en": "Which clause computes output columns and assigns aliases?", "ru": "Какая клауза вычисляет выходные колонки и присваивает алиасы?" } },
          { "id": "toolearly", "accept": ["where", "WHERE", "where clause", "where step"], "hint": { "en": "Which clause runs at step 2, before projection?", "ru": "Какая клауза бежит на шаге 2, до проекции?" } }
        ]
      }
    },
    {
      "id": "fix-filter-on-computed",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Make the computed filter legal — two ways", "ru": "Сделай фильтр по вычисленному легальным — двумя способами" },
      "prompt": { "en": "Rewrite SELECT price*qty AS revenue FROM orders WHERE revenue > 100 so it runs. Give both idiomatic fixes and explain the tradeoff, then reveal the model answer.", "ru": "Перепиши SELECT price*qty AS revenue FROM orders WHERE revenue > 100 так, чтобы он выполнился. Дай оба идиоматичных фикса и объясни компромисс, затем открой образцовый ответ." },
      "starter": "-- broken: WHERE references the SELECT alias 'revenue'\n-- SELECT price*qty AS revenue FROM orders WHERE revenue > 100;\n-- write two working versions",
      "grading": {
        "mode": "self",
        "model": { "en": "<b>Fix A (repeat the expression):</b> <code>SELECT price*qty AS revenue FROM orders WHERE price*qty > 100;</code> — the WHERE refers to base columns, which always exist at step 2. Under a normal plan the engine evaluates <code>price*qty</code> once per row, so there's no real double cost. <b>Fix B (subquery/CTE):</b> <code>WITH r AS (SELECT price*qty AS revenue FROM orders) SELECT * FROM r WHERE revenue > 100;</code> — the alias is created in the inner step, so the outer WHERE can see it. Tradeoff: Fix A is shorter and lets the planner push the predicate down freely; Fix B is cleaner when the expression is long or reused, but a non-inlined CTE can become an optimization fence (see the CTE-materialization lesson). Prefer A for simple filters; reach for B when readability or reuse wins.", "ru": "<b>Фикс A (повторить выражение):</b> <code>SELECT price*qty AS revenue FROM orders WHERE price*qty > 100;</code> — WHERE ссылается на базовые колонки, которые существуют на шаге 2. Под нормальным планом движок вычисляет <code>price*qty</code> раз на строку, так что реального двойного расхода нет. <b>Фикс B (подзапрос/CTE):</b> <code>WITH r AS (SELECT price*qty AS revenue FROM orders) SELECT * FROM r WHERE revenue > 100;</code> — алиас создаётся во внутреннем шаге, и внешний WHERE его видит. Компромисс: фикс A короче и даёт планировщику свободно протолкнуть предикат вниз; фикс B чище, когда выражение длинное или переиспользуется, но не-инлайненный CTE может стать оптимизационным барьером (см. урок про материализацию CTE). Предпочитай A для простых фильтров; бери B, когда побеждают читаемость или переиспользование." },
        "rubric": [
          { "en": "Fix A repeats the raw expression price*qty in WHERE (refers to base columns).", "ru": "Фикс A повторяет сырое выражение price*qty в WHERE (ссылается на базовые колонки)." },
          { "en": "Fix B introduces a subquery or CTE so the alias is visible to the outer WHERE.", "ru": "Фикс B вводит подзапрос или CTE, чтобы алиас был виден внешнему WHERE." },
          { "en": "States a tradeoff (e.g. CTE readability/reuse vs possible optimization fence).", "ru": "Называет компромисс (например, читаемость/переиспользование CTE против возможного оптимизационного барьера)." }
        ]
      }
    }
  ]
}
