{
  "lessonKey": "sql-postgres/01-sql-foundations/03-null-and-three-valued-logic",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-not-in-null",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "How many rows does NOT IN return?", "ru": "Сколько строк вернёт NOT IN?" },
      "prompt": { "en": "Commit to a row count for the query, then reveal the reasoning.", "ru": "Назови число строк для запроса, затем открой рассуждение." },
      "scenario": { "en": "Values to test: ids 1..5. The query is <code>SELECT id FROM (VALUES (1),(2),(3),(4),(5)) v(id) WHERE id NOT IN (2, NULL);</code>. How many rows come back: 4, 3, or 0?", "ru": "Тестируем id 1..5. Запрос: <code>SELECT id FROM (VALUES (1),(2),(3),(4),(5)) v(id) WHERE id NOT IN (2, NULL);</code>. Сколько строк вернётся: 4, 3 или 0?" },
      "reveal": { "en": "<b>0 rows.</b> <code>id NOT IN (2, NULL)</code> expands to <code>id <> 2 AND id <> NULL</code>. The second term is always UNKNOWN, so even for id = 1 the predicate is <code>TRUE AND UNKNOWN = UNKNOWN</code>, which WHERE drops. With any NULL in a NOT IN list, no row can ever be TRUE. Fix: <code>NOT IN (2)</code> after filtering NULLs, or use NOT EXISTS against a subquery.", "ru": "<b>0 строк.</b> <code>id NOT IN (2, NULL)</code> разворачивается в <code>id <> 2 AND id <> NULL</code>. Второй член всегда UNKNOWN, так что даже для id = 1 предикат это <code>TRUE AND UNKNOWN = UNKNOWN</code>, который WHERE отбрасывает. С любым NULL в списке NOT IN ни одна строка не станет TRUE. Фикс: <code>NOT IN (2)</code> после фильтрации NULL, или NOT EXISTS против подзапроса." }
    },
    {
      "id": "sandbox-not-exists-vs-not-in",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Make the anti-join NULL-safe", "ru": "Сделай anti-join NULL-безопасным" },
      "prompt": { "en": "blocked_users contains a NULL user_id. Write the NULL-safe query that lists every users.id NOT present in blocked_users, using NOT EXISTS. Verify it returns the expected ids (the NOT IN version would return nothing).", "ru": "blocked_users содержит NULL user_id. Напиши NULL-безопасный запрос, перечисляющий каждый users.id, отсутствующий в blocked_users, используя NOT EXISTS. Проверь, что он возвращает ожидаемые id (версия с NOT IN вернула бы ничего)." },
      "runtime": "sql",
      "setup": "CREATE TABLE users(id int primary key); INSERT INTO users SELECT generate_series(1,5); CREATE TABLE blocked_users(user_id int); INSERT INTO blocked_users VALUES (2), (4), (NULL);",
      "expected": { "kind": "rows-equal", "value": "SELECT u.id FROM users u WHERE NOT EXISTS (SELECT 1 FROM blocked_users b WHERE b.user_id = u.id) ORDER BY u.id" }
    },
    {
      "id": "diagnose-count-discrepancy",
      "type": "diagnose",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Why do the two counts differ?", "ru": "Почему два счёта различаются?" },
      "prompt": { "en": "Two COUNTs on the same table return different numbers. Fill in the blanks: which one ignores NULLs, and the IS-based operator that correctly tests for NULL.", "ru": "Два COUNT на одной таблице возвращают разные числа. Заполни пропуски: какой из них игнорирует NULL, и IS-оператор, корректно проверяющий NULL." },
      "evidence": { "en": "SELECT count(*) AS all_rows, count(phone) AS with_phone FROM users;\n-- all_rows = 1000, with_phone = 740   (260 users have phone = NULL)", "ru": "SELECT count(*) AS all_rows, count(phone) AS with_phone FROM users;\n-- all_rows = 1000, with_phone = 740   (у 260 пользователей phone = NULL)" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "skips", "accept": ["count(phone)", "count(col)", "count(column)", "with_phone", "count phone"], "hint": { "en": "Aggregates skip NULLs, so counting a specific column counts only non-NULL values.", "ru": "Агрегаты пропускают NULL, так что счёт конкретной колонки считает только не-NULL значения." } },
          { "id": "test", "accept": ["IS NULL", "is null", "IS NOT NULL", "is not null"], "hint": { "en": "The dedicated operator that returns a real boolean for nullness.", "ru": "Специальный оператор, возвращающий настоящий булев для nullness." } }
        ]
      }
    },
    {
      "id": "fix-divide-by-zero",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Guard a division and a default with NULL helpers", "ru": "Защити деление и значение по умолчанию NULL-помощниками" },
      "prompt": { "en": "A conversion-rate query crashes on rows where visits = 0, and shows blank names where nickname is NULL. Rewrite both using NULLIF and COALESCE, then reveal the model answer.", "ru": "Запрос конверсии падает на строках, где visits = 0, и показывает пустые имена, где nickname равен NULL. Перепиши оба, используя NULLIF и COALESCE, затем открой образцовый ответ." },
      "starter": "-- crashes with division by zero on visits = 0:\n-- SELECT name, signups / visits AS rate FROM funnels;\n-- also: show COALESCE-style default name from nickname/email",
      "grading": {
        "mode": "self",
        "model": { "en": "<code>SELECT COALESCE(nickname, email, 'anon') AS display, signups::numeric / NULLIF(visits, 0) AS rate FROM funnels;</code>. <b>NULLIF(visits, 0)</b> turns a 0 divisor into NULL, and division by NULL yields NULL instead of raising <code>division by zero</code> — so the row survives with a NULL rate rather than crashing the whole query. <b>COALESCE(nickname, email, 'anon')</b> returns the first non-NULL of the list, giving a guaranteed display name. The senior point: NULL propagation is a feature here — NULLIF converts a fatal error into a benign NULL you can then COALESCE or filter. Cast to numeric to avoid integer division truncation.", "ru": "<code>SELECT COALESCE(nickname, email, 'anon') AS display, signups::numeric / NULLIF(visits, 0) AS rate FROM funnels;</code>. <b>NULLIF(visits, 0)</b> превращает делитель 0 в NULL, а деление на NULL даёт NULL вместо поднятия <code>division by zero</code> — так строка выживает с NULL-rate, а не роняет весь запрос. <b>COALESCE(nickname, email, 'anon')</b> возвращает первый не-NULL из списка, давая гарантированное отображаемое имя. Сеньорская мысль: распространение NULL здесь — фича — NULLIF превращает фатальную ошибку в безобидный NULL, который потом можно COALESCE или отфильтровать. Кастуй к numeric, чтобы избежать усечения целочисленного деления." },
        "rubric": [
          { "en": "Uses NULLIF(visits, 0) so a zero divisor becomes NULL (no division-by-zero error).", "ru": "Использует NULLIF(visits, 0), чтобы нулевой делитель стал NULL (без ошибки деления на ноль)." },
          { "en": "Uses COALESCE to supply a default display name from the first non-NULL.", "ru": "Использует COALESCE, чтобы подставить имя по умолчанию из первого не-NULL." },
          { "en": "Notes NULL propagation / casting to numeric to avoid integer truncation.", "ru": "Отмечает распространение NULL / каст к numeric во избежание целочисленного усечения." }
        ]
      }
    }
  ]
}
