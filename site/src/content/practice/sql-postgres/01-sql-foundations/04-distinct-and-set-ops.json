{
  "lessonKey": "sql-postgres/01-sql-foundations/04-distinct-and-set-ops",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-union-vs-all",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "How many rows: UNION vs UNION ALL?", "ru": "Сколько строк: UNION против UNION ALL?" },
      "prompt": { "en": "Predict the row counts for both combiners, then reveal.", "ru": "Предскажи число строк для обоих комбинаторов, затем открой ответ." },
      "scenario": { "en": "Left side returns the values {1, 2, 2, 3}; right side returns {2, 3, 4}. How many rows does <code>left UNION right</code> produce, and how many does <code>left UNION ALL right</code> produce?", "ru": "Левая сторона возвращает значения {1, 2, 2, 3}; правая — {2, 3, 4}. Сколько строк даёт <code>left UNION right</code> и сколько <code>left UNION ALL right</code>?" },
      "reveal": { "en": "<b>UNION → 4 rows</b> {1,2,3,4}: it concatenates to {1,2,2,3,2,3,4} then sorts/hashes and removes all duplicates, leaving the distinct set. <b>UNION ALL → 7 rows</b> {1,2,2,3,2,3,4}: no dedup, just concatenation, so both the within-left duplicate (the extra 2) and the across-side overlaps (2 and 3) are kept. UNION pays an O(n log n) dedup pass for those 3 removed rows; if the sides were disjoint it would remove nothing and the pass would be wasted.", "ru": "<b>UNION → 4 строки</b> {1,2,3,4}: конкатенирует в {1,2,2,3,2,3,4}, затем сортирует/хеширует и убирает все дубликаты, оставляя различное множество. <b>UNION ALL → 7 строк</b> {1,2,2,3,2,3,4}: без дедупа, просто конкатенация, так что сохраняются и дубликат внутри левой (лишняя 2), и пересечения между сторонами (2 и 3). UNION платит проход дедупа O(n log n) за эти 3 убранные строки; будь стороны непересекающимися, он не убрал бы ничего, и проход был бы напрасным." }
    },
    {
      "id": "sandbox-distinct-on-latest",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Latest order per user with DISTINCT ON", "ru": "Последний заказ на пользователя через DISTINCT ON" },
      "prompt": { "en": "orders is seeded with several orders per user. Write a DISTINCT ON query returning exactly one row per user_id — the most recent by created_at — with columns user_id, id, total, created_at. Remember ORDER BY must lead with user_id, then created_at DESC. Verify the result matches the canonical answer.", "ru": "orders засеяна несколькими заказами на пользователя. Напиши запрос DISTINCT ON, возвращающий ровно одну строку на user_id — самую свежую по created_at — с колонками user_id, id, total, created_at. Помни: ORDER BY должен начинаться с user_id, затем created_at DESC. Проверь совпадение с каноническим ответом." },
      "runtime": "sql",
      "setup": "CREATE TABLE orders(id int primary key, user_id int, total numeric, created_at timestamptz); INSERT INTO orders VALUES (1, 10, 50, '2026-01-01'), (2, 10, 80, '2026-02-01'), (3, 10, 30, '2026-01-15'), (4, 20, 25, '2026-03-01'), (5, 20, 90, '2026-03-05'), (6, 30, 60, '2026-02-20');",
      "expected": { "kind": "rows-equal", "value": "SELECT DISTINCT ON (user_id) user_id, id, total, created_at FROM orders ORDER BY user_id, created_at DESC" }
    },
    {
      "id": "diagnose-needless-union",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "The 11-second report", "ru": "11-секундный отчёт" },
      "prompt": { "en": "A report UNIONs paid orders and refunded orders — two statuses that can never overlap — yet it sorts millions of rows. Fill in the blanks: the keyword that removes the wasted work, and the operation UNION performs that UNION ALL skips.", "ru": "Отчёт делает UNION оплаченных и возвращённых заказов — двух статусов, которые никогда не пересекаются — но сортирует миллионы строк. Заполни пропуски: ключевое слово, убирающее напрасную работу, и операцию, которую UNION выполняет, а UNION ALL пропускает." },
      "evidence": { "en": "SELECT ... FROM orders WHERE status = 'paid'\nUNION\nSELECT ... FROM orders WHERE status = 'refunded';\n-- plan shows a HashAggregate / Sort over ~9M combined rows; statuses are mutually exclusive", "ru": "SELECT ... FROM orders WHERE status = 'paid'\nUNION\nSELECT ... FROM orders WHERE status = 'refunded';\n-- план показывает HashAggregate / Sort по ~9M объединённым строкам; статусы взаимоисключающие" },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "keyword", "accept": ["UNION ALL", "union all", "all", "ALL"], "hint": { "en": "The combiner that concatenates without removing duplicates.", "ru": "Комбинатор, конкатенирующий без удаления дубликатов." } },
          { "id": "op", "accept": ["dedup", "deduplication", "deduplicate", "duplicate removal", "sort/hash", "sort and dedup", "дедуп", "дедупликация", "удаление дубликатов"], "hint": { "en": "The sort-or-hash step that finds and removes duplicate rows.", "ru": "Шаг sort-or-hash, находящий и убирающий дублирующиеся строки." } }
        ]
      }
    },
    {
      "id": "fix-distinct-masking-join",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "DISTINCT is hiding a join bug", "ru": "DISTINCT прячет баг join" },
      "prompt": { "en": "A teammate added SELECT DISTINCT to stop a query returning duplicate users. Explain what the DISTINCT is really masking and the better fix, then reveal the model answer.", "ru": "Коллега добавил SELECT DISTINCT, чтобы запрос перестал возвращать дублирующихся пользователей. Объясни, что на самом деле маскирует DISTINCT, и лучший фикс, затем открой образцовый ответ." },
      "starter": "-- users join orders; one user with 3 orders showed up 3 times\n-- SELECT DISTINCT u.id, u.email FROM users u JOIN orders o ON o.user_id = u.id;",
      "grading": {
        "mode": "self",
        "model": { "en": "The duplicates aren't a bug in the data — they're the join correctly producing one output row per matching order (a user with 3 orders → 3 rows). Slapping DISTINCT on top forces a full sort-or-hash dedup over the whole result just to collapse them back, which is O(n log n) waste and hides the real intent. The right fix depends on the question: if you only want users who have at least one order, use a semi-join — <code>SELECT u.id, u.email FROM users u WHERE EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id);</code> — which returns each user once without any dedup pass. If you want per-user aggregates, GROUP BY u.id and aggregate the orders. Reserve DISTINCT for genuinely duplicate rows, not for undoing a join you didn't need to fan out.", "ru": "Дубликаты — не баг данных, это join корректно даёт одну выходную строку на совпадающий заказ (пользователь с 3 заказами → 3 строки). Нашлёпка DISTINCT сверху форсит полный дедуп sort-or-hash по всему результату только чтобы схлопнуть их обратно — это трата O(n log n) и прячет реальное намерение. Правильный фикс зависит от вопроса: если нужны только пользователи хотя бы с одним заказом, используй semi-join — <code>SELECT u.id, u.email FROM users u WHERE EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id);</code> — он возвращает каждого пользователя один раз без прохода дедупа. Если нужны агрегаты на пользователя, GROUP BY u.id и агрегируй заказы. Береги DISTINCT для действительно дублирующихся строк, а не для отмены join, который не нужно было размножать." },
        "rubric": [
          { "en": "Identifies the duplicates as the join fanning out (1 row per matching order), not a data bug.", "ru": "Определяет дубликаты как размножение join (1 строка на совпадающий заказ), а не баг данных." },
          { "en": "Proposes a semi-join (EXISTS) or GROUP BY instead of DISTINCT, per the actual intent.", "ru": "Предлагает semi-join (EXISTS) или GROUP BY вместо DISTINCT, по реальному намерению." },
          { "en": "Notes DISTINCT adds an O(n log n) dedup pass and masks intent.", "ru": "Отмечает, что DISTINCT добавляет проход дедупа O(n log n) и маскирует намерение." }
        ]
      }
    }
  ]
}
