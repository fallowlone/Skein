{
  "lessonKey": "sql-postgres/05-cte-and-recursion/01-cte-basics",
  "track": "sql-postgres",
  "tasks": [
    {
      "id": "predict-forward-reference",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Will this WITH clause compile?", "ru": "Скомпилируется ли эта клауза WITH?" },
      "prompt": { "en": "Decide whether the query below is legal, then reveal the answer.", "ru": "Реши, легален ли запрос ниже, затем открой ответ." },
      "scenario": { "en": "<code>WITH a AS (SELECT id FROM b), b AS (SELECT id FROM employees) SELECT * FROM a;</code> — CTE <code>a</code> reads from CTE <code>b</code>, but <code>b</code> is declared after <code>a</code>. Does this run?", "ru": "<code>WITH a AS (SELECT id FROM b), b AS (SELECT id FROM employees) SELECT * FROM a;</code> — CTE <code>a</code> читает из CTE <code>b</code>, но <code>b</code> объявлен после <code>a</code>. Это выполнится?" },
      "reveal": { "en": "<b>No.</b> A non-recursive CTE can only reference CTEs declared <i>before</i> it. Here <code>a</code> forward-references <code>b</code>, so Postgres raises <code>relation \"b\" does not exist</code> (it interprets <code>b</code> as a missing table, since the CTE <code>b</code> isn't visible yet). Swap the order — declare <code>b</code> first — and it runs. Reference flows strictly forward in declaration order.", "ru": "<b>Нет.</b> Нерекурсивный CTE может ссылаться только на CTE, объявленные <i>раньше</i> него. Здесь <code>a</code> ссылается вперёд на <code>b</code>, поэтому Postgres поднимет <code>relation \"b\" does not exist</code> (он трактует <code>b</code> как отсутствующую таблицу, ведь CTE <code>b</code> ещё не виден). Поменяй порядок — объяви <code>b</code> первым — и запрос выполнится. Ссылка течёт строго вперёд в порядке объявления." }
    },
    {
      "id": "sandbox-chain-ctes",
      "type": "sandbox",
      "difficulty": "apply",
      "estMin": 7,
      "title": { "en": "Build a two-step CTE pipeline", "ru": "Построй конвейер из двух CTE" },
      "prompt": { "en": "The schema is seeded. Write a WITH chain: CTE 1 selects each employee's direct-report count from the seeded data; CTE 2 reads from CTE 1 and keeps only managers with 2+ reports. Return their names and report counts ordered by name. Match the canonical result.", "ru": "Схема засеяна. Напиши цепочку WITH: CTE 1 считает число прямых подчинённых каждого сотрудника по засеянным данным; CTE 2 читает из CTE 1 и оставляет только менеджеров с 2+ подчинёнными. Верни их имена и число подчинённых, упорядоченные по имени. Совпади с эталонным результатом." },
      "runtime": "sql",
      "setup": "CREATE TABLE employees(id int primary key, manager_id int, name text); INSERT INTO employees VALUES (1,NULL,'Ada'),(2,1,'Bo'),(3,1,'Cy'),(4,2,'Di'),(5,2,'El'),(6,2,'Fi'),(7,3,'Gu');",
      "expected": { "kind": "rows-equal", "value": "WITH counts AS (SELECT manager_id, count(*) AS reports FROM employees WHERE manager_id IS NOT NULL GROUP BY manager_id), big AS (SELECT manager_id, reports FROM counts WHERE reports >= 2) SELECT e.name, b.reports FROM big b JOIN employees e ON e.id = b.manager_id ORDER BY e.name" }
    },
    {
      "id": "diagnose-duplicated-subquery",
      "type": "diagnose",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "The copy-pasted subquery", "ru": "Скопированный подзапрос" },
      "prompt": { "en": "A query computes 'orders in the last 30 days' in two separate places by pasting the same subquery twice. Name the readability/correctness risk this creates and the single CTE refactor that removes it.", "ru": "Запрос вычисляет «заказы за последние 30 дней» в двух разных местах, вставив один и тот же подзапрос дважды. Назови риск читаемости/корректности, который это создаёт, и единственный рефактор через CTE, который его убирает." },
      "evidence": { "en": "-- branch 1\n... FROM (SELECT * FROM orders WHERE created_at >= now() - interval '30 days') a ...\n-- branch 2 (pasted, then someone edited only this one to 14 days)\n... FROM (SELECT * FROM orders WHERE created_at >= now() - interval '14 days') b ...", "ru": "-- ветка 1\n... FROM (SELECT * FROM orders WHERE created_at >= now() - interval '30 days') a ...\n-- ветка 2 (вставлено, потом кто-то поправил только её на 14 дней)\n... FROM (SELECT * FROM orders WHERE created_at >= now() - interval '14 days') b ..." },
      "grading": {
        "mode": "blanks",
        "blanks": [
          { "id": "risk", "accept": ["drift", "they drift", "drift apart", "divergence", "the two copies diverge", "inconsistency", "they diverge"], "hint": { "en": "Two copies of the same logic that someone can edit independently…", "ru": "Две копии одной логики, которые можно править независимо…" } },
          { "id": "fix", "accept": ["cte", "a cte", "with", "with clause", "common table expression", "single cte", "one cte"], "hint": { "en": "Declare the window once and reference it by name from both branches.", "ru": "Объяви окно один раз и ссылайся на него по имени из обеих веток." } }
        ]
      }
    },
    {
      "id": "fix-flatten-nesting",
      "type": "fix",
      "difficulty": "stretch",
      "estMin": 7,
      "title": { "en": "Flatten a nested derived table", "ru": "Расплющи вложенную производную таблицу" },
      "prompt": { "en": "Rewrite the nested query into a single-CTE pipeline with identical semantics, then reveal the model answer and check your work against the rubric.", "ru": "Перепиши вложенный запрос в конвейер с одним CTE с идентичной семантикой, затем открой образцовый ответ и сверься с критериями." },
      "starter": "-- Flatten this into WITH ... AS (...) SELECT ...\nSELECT name FROM employees e\nJOIN (SELECT manager_id FROM employees GROUP BY manager_id HAVING count(*) > 3) m\n  ON m.manager_id = e.id;",
      "grading": {
        "mode": "self",
        "model": { "en": "<code>WITH busy_managers AS (SELECT manager_id FROM employees GROUP BY manager_id HAVING count(*) > 3) SELECT e.name FROM busy_managers m JOIN employees e ON e.id = m.manager_id;</code> The derived table moves into a named CTE; the final SELECT reads from it by name. Same rows, but now the 'managers with more than 3 reports' step is named and independently runnable.", "ru": "<code>WITH busy_managers AS (SELECT manager_id FROM employees GROUP BY manager_id HAVING count(*) > 3) SELECT e.name FROM busy_managers m JOIN employees e ON e.id = m.manager_id;</code> Производная таблица переезжает в именованный CTE; финальный SELECT читает из него по имени. Те же строки, но шаг «менеджеры с более чем 3 подчинёнными» теперь именован и запускается отдельно." },
        "rubric": [
          { "en": "The inner aggregate is moved verbatim into a named CTE (GROUP BY / HAVING unchanged).", "ru": "Внутренний агрегат перенесён дословно в именованный CTE (GROUP BY / HAVING без изменений)." },
          { "en": "The final SELECT joins employees to the CTE by name instead of nesting a derived table.", "ru": "Финальный SELECT соединяет employees с CTE по имени, а не вкладывает производную таблицу." }
        ]
      }
    }
  ]
}
