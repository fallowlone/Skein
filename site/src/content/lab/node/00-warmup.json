{
  "track": "node",
  "tier": "warmup",
  "order": 0,
  "title": { "en": "Warmup", "ru": "Разминка" },
  "intro": { "en": "Reaffirm the runtime model before you build.", "ru": "Закрепи модель рантайма перед стройкой." },
  "challenges": [
    {
      "id": "warmup-event-loop-order",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 5,
      "title": { "en": "Order of output", "ru": "Порядок вывода" },
      "prompt": { "en": "Given sync logs, a setTimeout(0), a Promise.then, and process.nextTick — predict the print order.", "ru": "Дано: синхронные логи, setTimeout(0), Promise.then и process.nextTick — предскажи порядок печати." },
      "scenario": { "en": "// predict the print order\nconsole.log('a'); setTimeout(()=>console.log('b'),0); Promise.resolve().then(()=>console.log('c')); process.nextTick(()=>console.log('d')); console.log('e');", "ru": "// предскажи порядок печати\nconsole.log('a'); setTimeout(()=>console.log('b'),0); Promise.resolve().then(()=>console.log('c')); process.nextTick(()=>console.log('d')); console.log('e');" },
      "reveal": { "en": "a, e, d, c, b — sync first, then nextTick, then the promise microtask, then the timer macrotask.", "ru": "a, e, d, c, b — сначала синхронно, затем nextTick, потом микротаска промиса, затем макротаска таймера." }
    },
    {
      "id": "warmup-blocking-spot",
      "type": "predict",
      "difficulty": "recall",
      "estMin": 4,
      "title": { "en": "Which call blocks", "ru": "Какой вызов блокирует" },
      "prompt": { "en": "Pick the call that freezes the event loop for every client, and say why.", "ru": "Выбери вызов, который замораживает event loop для всех клиентов, и объясни почему." },
      "scenario": { "en": "// which one freezes the loop?\na) await readFile(p)   b) crypto.pbkdf2Sync(pwd, salt, 1e6, 64, 'sha512')   c) await fetch(u)", "ru": "// какой замораживает loop?\na) await readFile(p)   b) crypto.pbkdf2Sync(pwd, salt, 1e6, 64, 'sha512')   c) await fetch(u)" },
      "reveal": { "en": "b — the *Sync* CPU call runs on the one JS thread with no I/O to offload, so it blocks every other client. a and c are async I/O handed to libuv.", "ru": "b — *Sync* CPU-вызов исполняется на единственном JS-потоке без I/O для офлоада, поэтому блокирует всех остальных. a и c — async I/O, отданные libuv." }
    },
    {
      "id": "warmup-stream-vs-buffer",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 5,
      "title": { "en": "Stream or buffer", "ru": "Стрим или буфер" },
      "prompt": { "en": "A 2GB file must be sent to a client on a 512MB container. Stream or readFile? Why?", "ru": "Файл 2ГБ нужно отдать клиенту на контейнере с 512МБ. Стрим или readFile? Почему?" },
      "scenario": { "en": "res sends a 2GB file; container memory limit is 512MB.", "ru": "res отдаёт файл 2ГБ; лимит памяти контейнера 512МБ." },
      "reveal": { "en": "Stream (createReadStream + pipe): readFile buffers all 2GB into memory and OOMs; streaming holds only a small chunk at a time and respects backpressure.", "ru": "Стрим (createReadStream + pipe): readFile буферизует все 2ГБ в память и упадёт по OOM; стриминг держит лишь маленький чанк за раз и уважает backpressure." }
    },
    {
      "id": "warmup-esm-cjs-resolution",
      "type": "predict",
      "difficulty": "apply",
      "estMin": 6,
      "title": { "en": "What does the import resolve to", "ru": "Во что резолвится импорт" },
      "prompt": { "en": "A package has both an `exports` map and a legacy `main`. An ESM consumer does `import x from 'pkg'`. Which file wins, and what happens to a deep import?", "ru": "У пакета есть и карта `exports`, и легаси `main`. ESM-потребитель делает `import x from 'pkg'`. Какой файл победит и что будет с глубоким импортом?" },
      "scenario": { "en": "// resolution puzzle\n// package.json: { \"main\": \"./legacy.js\", \"exports\": { \".\": { \"import\": \"./esm.mjs\", \"require\": \"./cjs.cjs\" } } }\nimport x from 'pkg';        // ?\nimport y from 'pkg/internal/util.js'; // ?", "ru": "// загадка резолюции\n// package.json: { \"main\": \"./legacy.js\", \"exports\": { \".\": { \"import\": \"./esm.mjs\", \"require\": \"./cjs.cjs\" } } }\nimport x from 'pkg';        // ?\nimport y from 'pkg/internal/util.js'; // ?" },
      "reveal": { "en": "`exports` takes priority over `main` entirely, so `x` resolves to ./esm.mjs (the `import` condition). The deep import throws ERR_PACKAGE_PATH_NOT_EXPORTED: once `exports` exists it encapsulates the package — only listed subpaths are reachable, legacy `main` is ignored.", "ru": "`exports` полностью приоритетнее `main`, поэтому `x` резолвится в ./esm.mjs (условие `import`). Глубокий импорт бросает ERR_PACKAGE_PATH_NOT_EXPORTED: как только есть `exports`, он инкапсулирует пакет — доступны лишь перечисленные подпути, легаси `main` игнорируется." }
    },
    {
      "id": "warmup-nexttick-starvation",
      "type": "predict",
      "difficulty": "stretch",
      "estMin": 6,
      "title": { "en": "Does the timer ever fire", "ru": "Сработает ли таймер" },
      "prompt": { "en": "A function reschedules itself via process.nextTick on every call. A setTimeout(0) is also queued. Predict whether the timer ever runs.", "ru": "Функция переназначает себя через process.nextTick на каждом вызове. Также поставлен setTimeout(0). Предскажи, сработает ли таймер." },
      "scenario": { "en": "// will 'timer' ever print?\nsetTimeout(() => console.log('timer'), 0);\nfunction loop(){ process.nextTick(loop); }\nloop();", "ru": "// напечатается ли 'timer'?\nsetTimeout(() => console.log('timer'), 0);\nfunction loop(){ process.nextTick(loop); }\nloop();" },
      "reveal": { "en": "Never. The nextTick queue is drained completely between each event-loop phase, before timers/I/O. A recursively-scheduled nextTick refills the queue forever, so the loop never advances to the timers phase — the process starves. (A recursive setImmediate would NOT starve it, since immediates yield to one loop turn each.)", "ru": "Никогда. Очередь nextTick полностью опустошается между фазами event loop, до таймеров/I/O. Рекурсивно назначаемый nextTick бесконечно наполняет очередь, поэтому loop не доходит до фазы таймеров — процесс голодает. (Рекурсивный setImmediate НЕ заморит, т.к. immediate уступает по одному обороту loop.)" }
    }
  ]
}
