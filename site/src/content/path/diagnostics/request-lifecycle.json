{
  "concept": "request-lifecycle",
  "items": [
    {
      "id": "request-lifecycle-q1",
      "type": "mcq",
      "prompt": {
        "en": "A Node.js/Express service registers middleware in this order: json body-parser → rate-limiter → auth JWT verifier → business handler. During a load test an attacker floods the endpoint with 50 MB JSON payloads — the auth never even rejects them, but RAM spikes and the process crashes. Which ordering fix addresses the root cause?",
        "ru": "Node.js/Express-сервис регистрирует middleware в порядке: json body-parser → rate-limiter → JWT-верификатор → бизнес-обработчик. При нагрузочном тесте атакующий заваливает эндпоинт 50 МБ JSON-телами — auth их вообще не отклоняет, но RAM взлетает и процесс падает. Какое исправление порядка устраняет первопричину?"
      },
      "choices": [
        {
          "en": "Move rate-limiter before body-parser so that flood requests are dropped before the payload is parsed into memory.",
          "ru": "Переместить rate-limiter перед body-parser, чтобы запросы-наводнения отбрасывались до разбора тела в память."
        },
        {
          "en": "Move auth JWT verifier before body-parser so that unauthenticated requests never touch the parser.",
          "ru": "Переместить JWT-верификатор перед body-parser, чтобы неаутентифицированные запросы не касались парсера."
        },
        {
          "en": "Move body-parser after the business handler so that it runs last.",
          "ru": "Переместить body-parser после бизнес-обработчика, чтобы он выполнялся последним."
        },
        {
          "en": "Move auth JWT verifier before rate-limiter so authenticated users bypass throttling.",
          "ru": "Переместить JWT-верификатор перед rate-limiter, чтобы аутентифицированные пользователи обходили ограничение запросов."
        }
      ],
      "answer": 0,
      "irt": { "b": -0.6, "a": 1.6, "c": 0.22 }
    },
    {
      "id": "request-lifecycle-q2",
      "type": "mcq",
      "prompt": {
        "en": "A senior engineer reviews this Express middleware chain for a JSON API:\n\n  app.use(auth)\n  app.use(express.json())\n  app.use(validate)\n  app.use(handler)\n  app.use(responseTimeLogger)\n\nShe flags two bugs. Which pair correctly identifies both?",
        "ru": "Старший инженер проверяет эту цепочку middleware Express для JSON API:\n\n  app.use(auth)\n  app.use(express.json())\n  app.use(validate)\n  app.use(handler)\n  app.use(responseTimeLogger)\n\nОна находит два бага. Какая пара верно их идентифицирует?"
      },
      "choices": [
        {
          "en": "auth runs before body is parsed so req.body is undefined inside auth; responseTimeLogger is registered after handler so it never executes (response already sent).",
          "ru": "auth выполняется до разбора тела, поэтому req.body внутри auth равен undefined; responseTimeLogger зарегистрирован после handler, поэтому никогда не выполняется (ответ уже отправлен)."
        },
        {
          "en": "validate runs before handler, creating unnecessary CPU overhead; responseTimeLogger should be the first middleware to capture full round-trip time.",
          "ru": "validate выполняется перед handler, создавая лишнюю нагрузку на CPU; responseTimeLogger должен быть первым middleware для захвата полного времени round-trip."
        },
        {
          "en": "auth and validate are in the wrong order; validate should reject malformed input before auth to avoid wasted JWT verification.",
          "ru": "auth и validate расставлены неправильно; validate должен отклонять некорректный ввод до auth, чтобы не тратить ресурсы на JWT-верификацию."
        },
        {
          "en": "express.json() should come first before auth to ensure all middleware can read the body; responseTimeLogger placement is correct because it captures only handler duration.",
          "ru": "express.json() должен стоять первым перед auth, чтобы все middleware могли читать тело; размещение responseTimeLogger верно, так как он захватывает только время выполнения handler."
        }
      ],
      "answer": 0,
      "irt": { "b": 0.9, "a": 1.8, "c": 0.22 }
    },
    {
      "id": "request-lifecycle-q3",
      "type": "blanks",
      "prompt": {
        "en": "In a server-side HTTP request lifecycle, the phase that inspects req.headers, populates req.user, and may call next() or respond with 401 before the route handler runs is called ____.",
        "ru": "В серверном жизненном цикле HTTP-запроса фаза, которая инспектирует req.headers, заполняет req.user и может вызвать next() или ответить 401 до выполнения обработчика маршрута, называется ____."
      },
      "answer": ["middleware", "мидлвэр", "мидлвер", "middleware-цепочка"],
      "irt": { "b": -1.0, "a": 1.4, "c": 0.05 }
    }
  ]
}
