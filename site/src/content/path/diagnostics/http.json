{
  "concept": "http",
  "items": [
    {
      "id": "http-q1",
      "type": "mcq",
      "prompt": {
        "en": "HTTP/2 multiplexes many request streams over one TCP connection. Why does it still suffer head-of-line blocking that HTTP/3 fixes?",
        "ru": "HTTP/2 мультиплексирует множество потоков запросов поверх одного TCP-соединения. Почему он всё же страдает от блокировки начала очереди (HOL), которую исправляет HTTP/3?"
      },
      "choices": [
        {
          "en": "HPACK header compression serialises the streams, forcing them to wait on each other",
          "ru": "Сжатие заголовков HPACK сериализует потоки, заставляя их ждать друг друга"
        },
        {
          "en": "TCP delivers bytes strictly in order, so one lost segment stalls every multiplexed HTTP/2 stream until retransmission; HTTP/3 over QUIC has per-stream loss recovery",
          "ru": "TCP доставляет байты строго по порядку, поэтому один потерянный сегмент тормозит каждый мультиплексированный поток HTTP/2 до переотправки; HTTP/3 поверх QUIC имеет восстановление потерь на уровне отдельного потока"
        },
        {
          "en": "HTTP/2 still opens 6 parallel connections, and one of them always lags",
          "ru": "HTTP/2 всё ещё открывает 6 параллельных соединений, и одно из них всегда отстаёт"
        },
        {
          "en": "Server Push frames block the stream they were promised on until the browser accepts them",
          "ru": "Кадры Server Push блокируют поток, на котором они были обещаны, пока браузер их не примет"
        }
      ],
      "answer": 1
    },
    {
      "id": "http-q2",
      "type": "mcq",
      "prompt": {
        "en": "When does a client and server agree on whether to speak HTTP/1.1, HTTP/2, or HTTP/3, and via what mechanism?",
        "ru": "Когда клиент и сервер договариваются, на чём говорить — HTTP/1.1, HTTP/2 или HTTP/3 — и через какой механизм?"
      },
      "choices": [
        {
          "en": "After the first HTTP request, via an Upgrade header exchanged in plain text",
          "ru": "После первого HTTP-запроса, через заголовок Upgrade, передаваемый открытым текстом"
        },
        {
          "en": "During the TLS handshake via ALPN: the client lists supported versions (e.g. [h2, http/1.1]) and the server picks one, with no extra round-trip",
          "ru": "Во время TLS-рукопожатия через ALPN: клиент перечисляет поддерживаемые версии (например, [h2, http/1.1]), а сервер выбирает одну, без дополнительного кругового обхода"
        },
        {
          "en": "During DNS resolution, where the version is encoded in a special TXT record",
          "ru": "Во время разрешения DNS, где версия закодирована в специальной TXT-записи"
        },
        {
          "en": "It is fixed per TCP port: 80 is always HTTP/1.1, 443 is always HTTP/2",
          "ru": "Она фиксирована по TCP-порту: 80 — всегда HTTP/1.1, 443 — всегда HTTP/2"
        }
      ],
      "answer": 1
    },
    {
      "id": "http-q3",
      "type": "blanks",
      "prompt": {
        "en": "HTTP/1.1's workaround for its one-request-per-connection limit is to open about ____ parallel TCP connections per origin, each paying its own handshake and starting its congestion window from scratch.",
        "ru": "Обходной путь HTTP/1.1 для ограничения «один запрос на соединение» — открывать около ____ параллельных TCP-соединений на источник, каждое из которых оплачивает своё рукопожатие и начинает окно перегрузки с нуля."
      },
      "answer": ["6", "six", "6-8", "шесть"]
    }
  ]
}
