{
  "concept": "argument-validation",
  "items": [
    {
      "id": "argument-validation-q1",
      "type": "mcq",
      "prompt": {
        "en": "The model returns a tool_use for cancel_order with id 'ord_9f3c', an id that never appeared anywhere in the conversation. What is the senior move before executing?",
        "ru": "Модель возвращает tool_use для cancel_order с id 'ord_9f3c' — id, который нигде в диалоге не появлялся. Как поступит сеньор перед выполнением?"
      },
      "choices": [
        {
          "en": "Schema-validate the shape, then confirm the order exists and belongs to this caller before mutating; on failure return a tool_result error",
          "ru": "Провалидировать форму по схеме, затем подтвердить, что заказ существует и принадлежит этому вызывающему, до мутации; при отказе вернуть ошибку как tool_result"
        },
        {
          "en": "Execute it — the model would not emit an id it did not actually have",
          "ru": "Выполнить — модель не выдала бы id, которого у неё на самом деле не было"
        },
        {
          "en": "Throw an exception out of the loop so the request fails loudly",
          "ru": "Бросить исключение из цикла, чтобы запрос упал громко"
        },
        {
          "en": "Retry the same tool call automatically a few times in case it was transient",
          "ru": "Автоматически повторить тот же вызов несколько раз на случай, если это временная ошибка"
        }
      ],
      "answer": 0
    },
    {
      "id": "argument-validation-q2",
      "type": "mcq",
      "prompt": {
        "en": "When schema validation or an authorization check rejects a model's tool arguments, why return the failure as a tool_result error rather than throwing an exception?",
        "ru": "Когда валидация по схеме или проверка авторизации отклоняет аргументы инструмента от модели, почему вернуть отказ как ошибку tool_result, а не бросить исключение?"
      },
      "choices": [
        {
          "en": "The model reads the tool_result error and can self-correct on the next turn; throwing ends the turn and kills that feedback path",
          "ru": "Модель читает ошибку tool_result и может самокорректироваться на следующем ходу; бросок завершает ход и убивает этот путь обратной связи"
        },
        {
          "en": "A tool_result error is cheaper in tokens than an exception stack trace",
          "ru": "Ошибка tool_result дешевле по токенам, чем стек-трейс исключения"
        },
        {
          "en": "Exceptions are forbidden by the tool-use API and would be rejected by the provider",
          "ru": "Исключения запрещены API вызова инструментов и были бы отклонены провайдером"
        },
        {
          "en": "Returning a tool_result error retries the tool automatically with corrected arguments",
          "ru": "Возврат ошибки tool_result автоматически повторяет инструмент с исправленными аргументами"
        }
      ],
      "answer": 0
    },
    {
      "id": "argument-validation-q3",
      "type": "blanks",
      "prompt": {
        "en": "The model emits plausible JSON, not guaranteed-correct JSON, so treat tool arguments exactly like any other untrusted input crossing a ____ boundary.",
        "ru": "Модель выдаёт правдоподобный JSON, а не гарантированно корректный, поэтому относись к аргументам инструмента точно как к любому недоверенному вводу, пересекающему границу ____."
      },
      "answer": ["trust", "доверия", "trust boundary"]
    }
  ]
}
