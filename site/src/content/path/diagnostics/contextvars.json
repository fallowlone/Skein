{
  "concept": "contextvars",
  "items": [
    {
      "id": "contextvars-q1",
      "type": "mcq",
      "prompt": {
        "en": "In Python, contextvars (PEP 567) carry the OTel trace context automatically across asyncio awaits. When does the trace context get silently lost, and what is the fix?",
        "ru": "В Python contextvars (PEP 567) автоматически переносят OTel-контекст трейса через await в asyncio. Когда контекст трейса незаметно теряется и каков фикс?"
      },
      "choices": [
        {
          "en": "When you spawn OS threads manually — the child thread does not inherit the parent's contextvars; copy the context and attach it on the child thread's entry",
          "ru": "Когда вручную порождаешь потоки ОС — дочерний поток не наследует contextvars родителя; скопируй контекст и привяжи его при входе в дочерний поток"
        },
        {
          "en": "Whenever an await point is hit, since contextvars reset on every await",
          "ru": "При каждой точке await, ведь contextvars сбрасываются на каждом await"
        },
        {
          "en": "Only when the trace-id exceeds 64 bits and overflows the contextvar slot",
          "ru": "Только когда trace-id превышает 64 бита и переполняет слот contextvar"
        },
        {
          "en": "When the asyncio event loop is single-threaded, because contextvars need at least two threads",
          "ru": "Когда цикл событий asyncio однопоточный, потому что contextvars требуют минимум двух потоков"
        }
      ],
      "answer": 0
    },
    {
      "id": "contextvars-q2",
      "type": "mcq",
      "prompt": {
        "en": "Each runtime hooks trace context into a different substrate. Which pairing of substrate and its characteristic loss point is correct?",
        "ru": "Каждый рантайм цепляет контекст трейса к своей подложке. Какая пара «подложка и характерная точка потери» верна?"
      },
      "choices": [
        {
          "en": "Node uses AsyncLocalStorage and loses context across setTimeout/setImmediate/queueMicrotask unless the callback is wrapped with context.bind",
          "ru": "Node использует AsyncLocalStorage и теряет контекст через setTimeout/setImmediate/queueMicrotask, если колбэк не обёрнут context.bind"
        },
        {
          "en": "Go uses an implicit goroutine-local store, so context.Context is never needed",
          "ru": "Go использует неявное goroutine-local хранилище, поэтому context.Context не нужен"
        },
        {
          "en": "Python uses thread-local storage that propagates across processes automatically",
          "ru": "Python использует thread-local хранилище, автоматически передающееся между процессами"
        },
        {
          "en": "Java's ThreadLocal carries context across a message queue into the consumer process",
          "ru": "ThreadLocal в Java переносит контекст через очередь сообщений в процесс-потребитель"
        }
      ],
      "answer": 0
    },
    {
      "id": "contextvars-q3",
      "type": "blanks",
      "prompt": {
        "en": "A context substrate (Node AsyncLocalStorage, Python contextvars, Java ThreadLocal, Go context.Context) only carries trace context inside one ____; crossing a message queue requires inject on the producer and extract on the consumer instead.",
        "ru": "Подложка контекста (Node AsyncLocalStorage, Python contextvars, Java ThreadLocal, Go context.Context) переносит контекст трейса только внутри одного ____; пересечение очереди сообщений требует inject у продюсера и extract у потребителя."
      },
      "answer": [
        "process",
        "процесса",
        "процесс"
      ]
    }
  ]
}
