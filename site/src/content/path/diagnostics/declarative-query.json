{
  "concept": "declarative-query",
  "items": [
    {
      "id": "declarative-query-q1",
      "type": "mcq",
      "prompt": {
        "en": "Why can one unchanged SELECT keep getting faster years later — e.g. 600x after you add an index — without editing a single character of the SQL?",
        "ru": "Почему один и тот же неизменный SELECT может становиться быстрее спустя годы — например, в 600 раз после добавления индекса — без правки ни одного символа SQL?"
      },
      "choices": [
        {
          "en": "Postgres rewrites your SQL text on disk to a more efficient form the first time it runs",
          "ru": "Postgres переписывает текст вашего SQL на диске в более эффективную форму при первом выполнении"
        },
        {
          "en": "The result set is memoized after the first run, so later runs skip execution",
          "ru": "Результат запоминается после первого запуска, поэтому последующие запуски пропускают выполнение"
        },
        {
          "en": "The query states only the logical result; the planner is free to pick a new physical plan (e.g. an index scan instead of a seq scan) for the same text",
          "ru": "Запрос описывает только логический результат; планировщик волен выбрать новый физический план (например, index scan вместо seq scan) для того же текста"
        },
        {
          "en": "Adding an index changes the SQL standard's evaluation order to favor that column",
          "ru": "Добавление индекса меняет порядок вычисления по стандарту SQL в пользу этого столбца"
        }
      ],
      "answer": 2,
      "irt": {
        "b": 0,
        "a": 1.6,
        "c": 0.2
      }
    },
    {
      "id": "declarative-query-q2",
      "type": "mcq",
      "prompt": {
        "en": "The N+1 query pattern is the most common performance bug born from thinking row-at-a-time instead of in sets. What exactly makes it slow?",
        "ru": "Паттерн N+1 — самая частая ошибка производительности, рождённая мышлением «строка за строкой» вместо множеств. Что именно делает его медленным?"
      },
      "choices": [
        {
          "en": "Each sub-query forces a full table rewrite, multiplying disk I/O",
          "ru": "Каждый под-запрос вызывает полную перезапись таблицы, умножая дисковый I/O"
        },
        {
          "en": "The engine cannot cache plans for queries fired inside a loop",
          "ru": "Движок не может кэшировать планы для запросов, запускаемых внутри цикла"
        },
        {
          "en": "It always triggers a sequential scan because the loop disables indexes",
          "ru": "Он всегда вызывает sequential scan, потому что цикл отключает индексы"
        },
        {
          "en": "It issues one round trip per row (N+1 total) instead of pushing one set-based query down into the engine",
          "ru": "Он делает по одному сетевому обращению на строку (всего N+1) вместо того, чтобы опустить один запрос над множеством в движок"
        }
      ],
      "answer": 3,
      "irt": {
        "b": -0.2,
        "a": 1.6,
        "c": 0.2
      }
    },
    {
      "id": "declarative-query-q3",
      "type": "blanks",
      "prompt": {
        "en": "SQL is ____: you describe the result set you want, and the engine decides how to compute it — the wall between the logical query and the physical plan.",
        "ru": "SQL ____: вы описываете желаемый результат, а движок решает, как его вычислить — стена между логическим запросом и физическим планом."
      },
      "answer": [
        "declarative",
        "декларативный",
        "декларативен"
      ],
      "irt": {
        "b": -0.4,
        "a": 1.3,
        "c": 0.06
      }
    }
  ]
}
