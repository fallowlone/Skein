{
  "concept": "ADD-COLUMN-instant",
  "items": [
    {
      "id": "ADD-COLUMN-instant-q1",
      "type": "mcq",
      "prompt": {
        "en": "On Postgres 11+, a team runs `ALTER TABLE orders ADD COLUMN archived BOOLEAN DEFAULT false NOT NULL` on a 500-million-row table. It completes in milliseconds. A junior asks why it was instant. Which explanation is correct?",
        "ru": "На Postgres 11+ команда выполняет `ALTER TABLE orders ADD COLUMN archived BOOLEAN DEFAULT false NOT NULL` на таблице из 500 миллионов строк. Операция завершается за миллисекунды. Джуниор спрашивает, почему это мгновенно. Какое объяснение верно?"
      },
      "choices": [
        {
          "en": "Postgres uses a deferred write: it adds the column definition but schedules the actual row backfill for autovacuum to apply lazily",
          "ru": "Postgres использует отложенную запись: добавляет определение столбца, а фактический бэкфилл строк откладывает на autovacuum"
        },
        {
          "en": "When the DEFAULT is a constant expression, Postgres 11+ stores it in the catalog (`pg_attribute.attmissingval`) and returns it for existing rows without touching heap pages; the table file is not rewritten",
          "ru": "Если DEFAULT — константное выражение, Postgres 11+ хранит его в каталоге (`pg_attribute.attmissingval`) и возвращает для существующих строк без касания страниц heap; файл таблицы не перезаписывается"
        },
        {
          "en": "Postgres parallelises the row update across all CPUs, completing the rewrite faster than a single-threaded scan would",
          "ru": "Postgres распараллеливает обновление строк по всем CPU, завершая перезапись быстрее однопоточного сканирования"
        },
        {
          "en": "The NOT NULL constraint is validated lazily at query time, so the column addition itself is always instant regardless of the DEFAULT",
          "ru": "Ограничение NOT NULL проверяется лениво во время запроса, поэтому добавление столбца всегда мгновенно независимо от DEFAULT"
        }
      ],
      "answer": 1,
      "irt": { "b": -0.4, "a": 1.7, "c": 0.22 }
    },
    {
      "id": "ADD-COLUMN-instant-q2",
      "type": "mcq",
      "prompt": {
        "en": "A DBA tries the same trick on a Postgres 11+ instance: `ALTER TABLE events ADD COLUMN payload JSONB DEFAULT gen_random_uuid()::text`. The table has 200 million rows and the operation runs for 40 minutes, causing heavy I/O. What went wrong?",
        "ru": "DBA пробует тот же приём на Postgres 11+: `ALTER TABLE events ADD COLUMN payload JSONB DEFAULT gen_random_uuid()::text`. В таблице 200 миллионов строк, операция выполняется 40 минут при высокой нагрузке I/O. Что пошло не так?"
      },
      "choices": [
        {
          "en": "JSONB columns always require a heap rewrite because of their variable-length storage",
          "ru": "Столбцы JSONB всегда требуют перезаписи heap из-за хранения переменной длины"
        },
        {
          "en": "`gen_random_uuid()` is a VOLATILE function, so Postgres cannot store a single missing value in the catalog — each existing row needs a unique value, forcing a full table rewrite",
          "ru": "`gen_random_uuid()` — VOLATILE функция, поэтому Postgres не может сохранить одно общее значение в каталоге — каждая существующая строка требует уникального значения, что вынуждает полную перезапись таблицы"
        },
        {
          "en": "The `::text` cast disables the optimisation because it crosses a type boundary that Postgres cannot represent in `attmissingval`",
          "ru": "Приведение `::text` отключает оптимизацию, так как пересекает границу типов, которую Postgres не может представить в `attmissingval`"
        },
        {
          "en": "The optimisation only applies to columns with a NOT NULL constraint; nullable columns with a DEFAULT always trigger a rewrite",
          "ru": "Оптимизация применяется только к столбцам с ограничением NOT NULL; nullable-столбцы с DEFAULT всегда вызывают перезапись"
        }
      ],
      "answer": 1,
      "irt": { "b": 0.7, "a": 1.8, "c": 0.22 }
    },
    {
      "id": "ADD-COLUMN-instant-q3",
      "type": "blanks",
      "prompt": {
        "en": "Postgres 11+ stores the constant DEFAULT value for existing rows in the system catalog column `pg_attribute.____` so that existing heap pages need not be rewritten.",
        "ru": "Postgres 11+ хранит константное значение DEFAULT для существующих строк в столбце системного каталога `pg_attribute.____`, чтобы не перезаписывать существующие страницы heap."
      },
      "answer": ["attmissingval", "аттмиссингвал"],
      "irt": { "b": 0.6, "a": 1.6, "c": 0.04 }
    }
  ]
}
