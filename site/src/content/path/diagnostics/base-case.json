{
  "concept": "base-case",
  "items": [
    {
      "id": "base-case-q1",
      "type": "mcq",
      "prompt": {
        "en": "A recursive function omits its base case. What actually happens, and how fast?",
        "ru": "Рекурсивная функция опускает свой базовый случай. Что на самом деле происходит и как быстро?"
      },
      "choices": [
        { "en": "Each call pushes another stack frame with no stopping condition; the stack region (1–8 MB) fills within milliseconds and the OS terminates the program with a stack overflow", "ru": "Каждый вызов помещает ещё один кадр стека без условия остановки; область стека (1–8 МБ) заполняется за миллисекунды, и ОС роняет программу переполнением стека" },
        { "en": "The language runtime detects the missing base case and limits recursion to a safe depth automatically", "ru": "Среда выполнения обнаруживает отсутствующий базовый случай и автоматически ограничивает рекурсию безопасной глубиной" },
        { "en": "The function returns undefined immediately because there is no terminating branch", "ru": "Функция сразу возвращает undefined, потому что нет завершающей ветви" },
        { "en": "It loops forever but uses no extra memory, since each call reuses the same frame", "ru": "Она зацикливается навсегда, но не использует доп. память, так как каждый вызов переиспользует один кадр" }
      ],
      "answer": 0
    },
    {
      "id": "base-case-q2",
      "type": "mcq",
      "prompt": {
        "en": "A recursive TypeScript conditional type errors with 'Type instantiation is excessively deep and possibly infinite.' Given a correct base case exists, what is the second distinct cause to check?",
        "ru": "Рекурсивный условный тип в TypeScript падает с ошибкой «Type instantiation is excessively deep and possibly infinite.» При наличии корректного базового случая какова вторая отдельная причина для проверки?"
      },
      "choices": [
        { "en": "The recursive call is wrapped (e.g. [T, ...Self<...>]) rather than in tail position, so TS 4.5 tail-recursion elimination cannot apply and it walls out around ~50 frames", "ru": "Рекурсивный вызов обёрнут (например, [T, ...Self<...>]), а не в хвостовой позиции, поэтому устранение хвостовой рекурсии из TS 4.5 не применяется и упирается в стену около ~50 кадров" },
        { "en": "The base case must use the empty object {} instead of the empty tuple [] to terminate", "ru": "Базовый случай должен использовать пустой объект {} вместо пустого кортежа [], чтобы завершиться" },
        { "en": "infer is not allowed inside recursive types, so the recursion never resolves", "ru": "infer не допускается внутри рекурсивных типов, поэтому рекурсия никогда не разрешается" },
        { "en": "Conditional types cannot return tuples, so any tuple result triggers the error", "ru": "Условные типы не могут возвращать кортежи, поэтому любой кортежный результат вызывает ошибку" }
      ],
      "answer": 0
    },
    {
      "id": "base-case-q3",
      "type": "blanks",
      "prompt": {
        "en": "The base case is the branch that returns a value directly without recursing; the recursive case must move toward it by calling itself on a ____ input — meaning closer to the base case, not necessarily numerically smaller.",
        "ru": "Базовый случай — это ветвь, что возвращает значение напрямую без рекурсии; рекурсивный случай должен двигаться к нему, вызывая себя на ____ входе — то есть более близком к базовому случаю, не обязательно численно меньшем."
      },
      "answer": ["smaller", "меньшем", "smaller input"]
    }
  ]
}
