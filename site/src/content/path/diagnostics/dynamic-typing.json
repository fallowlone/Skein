{
  "concept": "dynamic-typing",
  "items": [
    {
      "id": "dynamic-typing-q1",
      "type": "mcq",
      "prompt": {
        "en": "A dynamically typed runtime (plain JavaScript, Python) and a statically typed one (TypeScript) both guard against the same underlying problem. What is that problem, and how do the two strategies differ?",
        "ru": "Динамически типизированная среда выполнения (обычный JavaScript, Python) и статически типизированная (TypeScript) защищают от одной и той же базовой проблемы. В чём проблема и чем различаются эти две стратегии?"
      },
      "choices": [
        { "en": "Both prevent applying the wrong interpretation rule to a value's bits; static typing catches the mismatch at compile time, dynamic typing only at the moment the bad operation runs", "ru": "Обе предотвращают применение неверного правила интерпретации к битам значения; статическая типизация ловит несоответствие при компиляции, динамическая — лишь в момент выполнения неверной операции" },
        { "en": "Both manage how much memory each value occupies; static typing allocates less than dynamic typing", "ru": "Обе управляют тем, сколько памяти занимает каждое значение; статическая типизация выделяет меньше, чем динамическая" },
        { "en": "Both eliminate every category of bug, differing only in error-message wording", "ru": "Обе устраняют все категории багов, различаясь только формулировкой сообщений об ошибках" },
        { "en": "Dynamic typing catches type errors earlier than static typing because the runtime sees real values", "ru": "Динамическая типизация ловит ошибки типов раньше статической, потому что среда выполнения видит реальные значения" }
      ],
      "answer": 0
    },
    {
      "id": "dynamic-typing-q2",
      "type": "mcq",
      "prompt": {
        "en": "In dynamically typed JavaScript, the expression \"9.99\" * 1.08 returns a number and \"9.99\" + \" USD\" concatenates — neither throws. Why is this silent coercion a hazard a senior engineer watches for?",
        "ru": "В динамически типизированном JavaScript выражение \"9.99\" * 1.08 возвращает число, а \"9.99\" + \" USD\" конкатенирует — ни одно не бросает исключение. Почему это тихое приведение типов — опасность, за которой следит опытный инженер?"
      },
      "choices": [
        { "en": "The runtime coerces the string instead of failing, so a wrong-typed value produces a plausible wrong result that propagates with no error to flag it", "ru": "Среда выполнения приводит строку вместо сбоя, поэтому значение неверного типа даёт правдоподобный неверный результат, который распространяется без ошибки, что его бы пометила" },
        { "en": "Coercion is slower than a type error, so it degrades performance on the hot path", "ru": "Приведение медленнее ошибки типа, поэтому ухудшает производительность на горячем пути" },
        { "en": "The multiplication throws a TypeError that crashes the process before the concatenation runs", "ru": "Умножение бросает TypeError, который роняет процесс до выполнения конкатенации" },
        { "en": "Coercion permanently mutates the original string variable into a number", "ru": "Приведение навсегда превращает исходную строковую переменную в число" }
      ],
      "answer": 0
    },
    {
      "id": "dynamic-typing-q3",
      "type": "blanks",
      "prompt": {
        "en": "Python, like JavaScript, is dynamically typed: variables are names bound to values, the type lives on the ____ , and type errors surface at runtime unless you add tooling like mypy or pyright.",
        "ru": "Python, как и JavaScript, динамически типизирован: переменные — это имена, привязанные к значениям, тип живёт на ____ , а ошибки типов всплывают во время выполнения, если не добавить инструменты вроде mypy или pyright."
      },
      "answer": ["value", "values", "значении", "значениях"]
    }
  ]
}
