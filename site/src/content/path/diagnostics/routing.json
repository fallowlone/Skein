{
  "concept": "routing",
  "items": [
    {
      "id": "routing-q1",
      "type": "mcq",
      "prompt": {
        "en": "An admin endpoint is reachable without authentication, even though auth middleware is implemented and tested. What is the most likely cause?",
        "ru": "Админ-эндпоинт доступен без аутентификации, хотя middleware аутентификации реализован и протестирован. Какова наиболее вероятная причина?"
      },
      "choices": [
        {
          "en": "The route was registered before the auth middleware, so the handler runs first and auth never gets to short-circuit",
          "ru": "Маршрут зарегистрирован до middleware аутентификации, поэтому обработчик выполняется первым, и аутентификация не успевает прервать цепочку"
        },
        {
          "en": "The auth token format changed",
          "ru": "Формат токена аутентификации изменился"
        },
        {
          "en": "The radix trie matched the wrong path",
          "ru": "Radix trie сопоставил неправильный путь"
        },
        {
          "en": "The database rejected the auth query",
          "ru": "База данных отклонила запрос аутентификации"
        }
      ],
      "answer": 0
    },
    {
      "id": "routing-q2",
      "type": "mcq",
      "prompt": {
        "en": "A team replaces a linear-scan router with a radix-trie router (httprouter/find-my-way) and expects a large latency win. Why is that expectation usually wrong?",
        "ru": "Команда заменяет роутер с линейным перебором на radix-trie (httprouter/find-my-way), ожидая большой выигрыш по задержке. Почему это ожидание обычно неверно?"
      },
      "choices": [
        {
          "en": "Routing is microseconds either way and almost never dominates latency; the trie only makes match time independent of route count, which matters for correctness/scale, not request latency",
          "ru": "Маршрутизация в любом случае занимает микросекунды и почти никогда не доминирует в задержке; trie лишь делает время сопоставления независимым от числа маршрутов, что важно для корректности/масштаба, а не для задержки запроса"
        },
        {
          "en": "A radix trie is actually O(n) in route count, so it is slower than a linear scan",
          "ru": "Radix trie на самом деле O(n) по числу маршрутов, поэтому медленнее линейного перебора"
        },
        {
          "en": "Tries cache responses, so the win comes from skipping the handler entirely",
          "ru": "Trie кэширует ответы, поэтому выигрыш — в полном пропуске обработчика"
        },
        {
          "en": "The trie removes the need for middleware, which is where the latency was",
          "ru": "Trie устраняет необходимость в middleware, где и была задержка"
        }
      ],
      "answer": 0
    },
    {
      "id": "routing-q3",
      "type": "blanks",
      "prompt": {
        "en": "A radix-trie router matches in O(path length), independent of route count, so 10 routes and 10,000 routes match in the same time; the routing bugs that actually bite are correctness ones like overlapping patterns and ____ confusion (a GET route shadowing a POST on the same path).",
        "ru": "Роутер на radix-trie сопоставляет за O(длины пути), независимо от числа маршрутов, поэтому 10 и 10 000 маршрутов сопоставляются за одно время; реально кусающие баги маршрутизации — это ошибки корректности, такие как пересекающиеся паттерны и путаница ____ (маршрут GET, затеняющий POST на том же пути)."
      },
      "answer": ["method", "метод", "методов", "HTTP method"]
    }
  ]
}
