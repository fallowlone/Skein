{
  "concept": "value-space",
  "items": [
    {
      "id": "value-space-q1",
      "type": "mcq",
      "prompt": {
        "en": "A junior writes `type Status = string` and checks `if (status === 'active')`. Six months later a teammate passes `'ACTIVE'` and the check silently fails. Which TypeScript feature would have caught this at compile time by shrinking the value space?",
        "ru": "Джун пишет `type Status = string` и проверяет `if (status === 'active')`. Через полгода коллега передаёт `'ACTIVE'`, и проверка молча падает. Какой инструмент TypeScript поймал бы это во время компиляции, сужая допустимое множество значений?"
      },
      "choices": [
        {
          "en": "A string literal union: `type Status = 'active' | 'inactive' | 'pending'` — the type's value space becomes exactly three strings, so any other value is rejected by the type checker",
          "ru": "Строковый литеральный юнион: `type Status = 'active' | 'inactive' | 'pending'` — допустимое множество значений типа ограничивается ровно тремя строками, и любое другое значение отвергается проверкой типов"
        },
        {
          "en": "An interface with a `status` field typed as `string` — interfaces enforce shape, which constrains the value space of objects",
          "ru": "Интерфейс с полем `status` типа `string` — интерфейсы задают форму объекта, тем самым ограничивая допустимые значения"
        },
        {
          "en": "A type assertion `status as 'active'` — telling the compiler the string is already the right literal",
          "ru": "Приведение типа `status as 'active'` — говорим компилятору, что строка уже является нужным литералом"
        },
        {
          "en": "Enabling `strict: true` in tsconfig — strict mode tightens all string value spaces automatically",
          "ru": "Включение `strict: true` в tsconfig — строгий режим автоматически сужает допустимые значения всех строк"
        }
      ],
      "answer": 0,
      "irt": {
        "b": -0.5,
        "a": 1.6,
        "c": 0.22
      }
    },
    {
      "id": "value-space-q2",
      "type": "mcq",
      "prompt": {
        "en": "You have `type Shape = { kind: 'circle'; radius: number } | { kind: 'square'; side: number }`. After a narrowing `if (s.kind === 'circle')` block, TypeScript infers `s` has type `{ kind: 'square'; side: number }` in the else branch. What principle explains this?",
        "ru": "Дан тип `type Shape = { kind: 'circle'; radius: number } | { kind: 'square'; side: number }`. После сужающей проверки `if (s.kind === 'circle')` TypeScript выводит в ветке else тип `{ kind: 'square'; side: number }`. Какой принцип это объясняет?"
      },
      "choices": [
        {
          "en": "Each branch of the union covers a disjoint slice of the value space; after a check eliminates one slice, the remaining value space is the complement — only the members not ruled out survive",
          "ru": "Каждая ветка юниона покрывает непересекающийся срез допустимого множества значений; после того как проверка исключает один срез, оставшееся множество — это дополнение: выживают только те члены, которые не были отсеяны"
        },
        {
          "en": "TypeScript replaces the union with the else-branch member when it sees a discriminant property used in an equality check",
          "ru": "TypeScript заменяет юнион на член ветки else, когда видит дискриминирующее свойство в проверке равенства"
        },
        {
          "en": "The `kind` field is a readonly literal, so the compiler treats it as a constant and resolves the union at parse time",
          "ru": "Поле `kind` является литералом только для чтения, поэтому компилятор трактует его как константу и разрешает юнион на этапе парсинга"
        },
        {
          "en": "Discriminated unions require explicit `as const` assertions before narrowing works in else branches",
          "ru": "Дискриминируемые юнионы требуют явных `as const`-утверждений, прежде чем сужение заработает в ветках else"
        }
      ],
      "answer": 0,
      "irt": {
        "b": 0.8,
        "a": 1.7,
        "c": 0.22
      }
    },
    {
      "id": "value-space-q3",
      "type": "blanks",
      "prompt": {
        "en": "In TypeScript's type system, the type that represents the ____ set — admitting no values at all — is called `never`.",
        "ru": "В системе типов TypeScript тип, представляющий ____ множество — не допускающий ни одного значения — называется `never`."
      },
      "answer": [
        "empty",
        "пустое",
        "пустой"
      ],
      "irt": {
        "b": -0.8,
        "a": 1.4,
        "c": 0.05
      }
    }
  ]
}
