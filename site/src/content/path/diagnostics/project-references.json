{
  "concept": "project-references",
  "items": [
    {
      "id": "project-references-q1",
      "type": "mcq",
      "prompt": {
        "en": "A monorepo has packages `core`, `api` (imports `core`), and `web` (imports `core` and `api`). A junior adds `composite: true` and `references` to each `tsconfig.json` then runs `tsc -b`. The build succeeds in under a second on second runs even when only `web` changes. What is the primary mechanism enabling this?",
        "ru": "Монорепозиторий содержит пакеты `core`, `api` (импортирует `core`) и `web` (импортирует `core` и `api`). Джуниор добавляет `composite: true` и `references` в каждый `tsconfig.json` и запускает `tsc -b`. Второй запуск сборки занимает меньше секунды даже при изменении только `web`. Какой основной механизм это обеспечивает?"
      },
      "choices": [
        {
          "en": "TypeScript caches ASTs in memory so repeated parses of the same file are skipped",
          "ru": "TypeScript кэширует AST в памяти, пропуская повторный парсинг тех же файлов"
        },
        {
          "en": "`tsc -b` reads pre-built `.d.ts` declaration files and per-project `.tsbuildinfo` fingerprints to skip recompiling unchanged projects",
          "ru": "`tsc -b` читает заранее собранные файлы `.d.ts` и отпечатки `.tsbuildinfo` каждого проекта, пропуская перекомпиляцию неизменённых проектов"
        },
        {
          "en": "Parallel compilation threads handle each package simultaneously, reducing wall-clock time",
          "ru": "Параллельные потоки компиляции обрабатывают каждый пакет одновременно, сокращая реальное время"
        },
        {
          "en": "Tree-shaking removes unused exports before type-checking, reducing the input surface",
          "ru": "Tree-shaking удаляет неиспользуемые экспорты до проверки типов, уменьшая входной объём"
        }
      ],
      "answer": 1,
      "irt": { "b": -0.4, "a": 1.7, "c": 0.22 }
    },
    {
      "id": "project-references-q2",
      "type": "mcq",
      "prompt": {
        "en": "After wiring project references, a CI pipeline breaks: `api` imports a symbol from `core` that exists in `core/src/index.ts` but tsc reports it cannot find the module. The `core` package has `composite: true` and `declarationDir: 'dist'`. What is the most likely root cause?",
        "ru": "После настройки project references сборка CI ломается: `api` импортирует символ из `core`, который есть в `core/src/index.ts`, но tsc сообщает, что не может найти модуль. В пакете `core` установлены `composite: true` и `declarationDir: 'dist'`. Какова наиболее вероятная причина?"
      },
      "choices": [
        {
          "en": "`core` is missing `paths` mappings in its `tsconfig.json` for its own exports",
          "ru": "В `tsconfig.json` пакета `core` отсутствует маппинг `paths` для его собственных экспортов"
        },
        {
          "en": "`api`'s `tsconfig.json` references `core` source files directly instead of using a `references` entry pointing at `core`'s `tsconfig.json`",
          "ru": "В `tsconfig.json` пакета `api` происходит прямое обращение к исходникам `core` вместо использования записи `references`, указывающей на `tsconfig.json` пакета `core`"
        },
        {
          "en": "`core` has not been compiled yet so its `dist/` declaration files do not exist; `tsc -b` requires pre-built outputs from referenced projects",
          "ru": "Пакет `core` ещё не был скомпилирован, поэтому файлы деклараций в `dist/` отсутствуют; `tsc -b` требует заранее собранных выходных файлов зависимых проектов"
        },
        {
          "en": "`composite` mode prevents cross-package type sharing to enforce stricter isolation",
          "ru": "Режим `composite` запрещает обмен типами между пакетами для более строгой изоляции"
        }
      ],
      "answer": 2,
      "irt": { "b": 0.9, "a": 1.8, "c": 0.22 }
    },
    {
      "id": "project-references-q3",
      "type": "blanks",
      "prompt": {
        "en": "For a TypeScript project to be usable as a project reference dependency, its `tsconfig.json` must set `____: true`, which forces `declaration` on and writes a `.tsbuildinfo` file that enables incremental rebuild tracking.",
        "ru": "Чтобы TypeScript-проект можно было использовать как зависимость через project references, его `tsconfig.json` должен содержать `____: true`, что принудительно включает `declaration` и записывает файл `.tsbuildinfo` для отслеживания инкрементальных пересборок."
      },
      "answer": ["composite", "composite"],
      "irt": { "b": -0.8, "a": 1.4, "c": 0.05 }
    }
  ]
}
