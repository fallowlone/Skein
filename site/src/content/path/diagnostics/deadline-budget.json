{
  "concept": "deadline-budget",
  "items": [
    {
      "id": "deadline-budget-q1",
      "type": "mcq",
      "prompt": {
        "en": "A team adds a correct 45-second SIGTERM drain handler, but requests are still cut off on every deploy. The container runs the app via `sh -c 'node server.js'`. What is wrong?",
        "ru": "Команда добавляет корректный обработчик SIGTERM с дренажом на 45 секунд, но запросы по-прежнему обрываются на каждом деплое. Контейнер запускает приложение через `sh -c 'node server.js'`. В чём проблема?"
      },
      "choices": [
        {
          "en": "The shell is PID 1 and receives SIGTERM but does not forward it to the node child, so the handler never runs and the app is SIGKILLed at the deadline",
          "ru": "Shell является PID 1 и получает SIGTERM, но не пересылает его дочернему node, поэтому обработчик не запускается и приложение получает SIGKILL по истечении дедлайна"
        },
        {
          "en": "45 seconds exceeds the default grace period, so the handler is cut off mid-drain",
          "ru": "45 секунд превышает grace period по умолчанию, поэтому обработчик обрывается на середине дренажа"
        },
        {
          "en": "SIGTERM handlers are not supported inside containers",
          "ru": "Обработчики SIGTERM не поддерживаются внутри контейнеров"
        },
        {
          "en": "The node process registers the handler too slowly at startup",
          "ru": "Процесс node слишком медленно регистрирует обработчик при старте"
        }
      ],
      "answer": 0
    },
    {
      "id": "deadline-budget-q2",
      "type": "mcq",
      "prompt": {
        "en": "Why wrap the drain in a guardian timeout set below the grace period and call `process.exit()` yourself if it fires, instead of just awaiting the drain?",
        "ru": "Зачем оборачивать дренаж в guardian-таймаут, заданный меньше grace period, и самим вызывать `process.exit()` при его срабатывании, вместо того чтобы просто ждать завершения дренажа?"
      },
      "choices": [
        {
          "en": "A drain can hang on a stuck request or a keep-alive client that never closes; a self-inflicted logged exit a second before SIGKILL lets you control the exit code and flush logs/metrics first, instead of the kernel pulling the plug mid-cleanup",
          "ru": "Дренаж может зависнуть на застрявшем запросе или keep-alive клиенте, который никогда не закрывается; собственный логируемый выход за секунду до SIGKILL позволяет управлять кодом выхода и сначала сбросить логи/метрики, вместо того чтобы ядро обрубило процесс посреди очистки"
        },
        {
          "en": "The guardian timeout makes the grace period longer, giving the drain more time",
          "ru": "Guardian-таймаут удлиняет grace period, давая дренажу больше времени"
        },
        {
          "en": "It resends SIGTERM so the handler gets a second chance to run",
          "ru": "Он повторно отправляет SIGTERM, чтобы обработчик получил второй шанс"
        },
        {
          "en": "Calling process.exit() cancels the orchestrator's SIGKILL entirely",
          "ru": "Вызов process.exit() полностью отменяет SIGKILL от оркестратора"
        }
      ],
      "answer": 0
    },
    {
      "id": "deadline-budget-q3",
      "type": "blanks",
      "prompt": {
        "en": "The grace period (Kubernetes `terminationGracePeriodSeconds`, default 30 s) is a hard deadline because when it expires the orchestrator sends ____, which cannot be caught or handled — the process is destroyed immediately and any in-flight work is lost.",
        "ru": "Grace period (Kubernetes `terminationGracePeriodSeconds`, по умолчанию 30 с) — это жёсткий дедлайн, потому что по его истечении оркестратор отправляет ____ , который нельзя перехватить или обработать — процесс уничтожается немедленно, и вся незавершённая работа теряется."
      },
      "answer": ["SIGKILL", "sigkill", "signal 9", "SIGKILL (9)"]
    }
  ]
}
