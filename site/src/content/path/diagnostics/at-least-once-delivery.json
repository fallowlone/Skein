{
  "concept": "at-least-once-delivery",
  "items": [
    {
      "id": "at-least-once-delivery-q1",
      "type": "mcq",
      "prompt": {
        "en": "An outbox relay publishes an event to the broker, then crashes before it can UPDATE the row to 'sent'. On restart it re-reads the still-unsent row and publishes again. Why is this the correct design rather than a bug to fix?",
        "ru": "Релей outbox публикует событие в брокер, затем падает, не успев пометить строку как «отправлено» (UPDATE). После перезапуска он снова читает всё ещё неотправленную строку и публикует повторно. Почему это корректный дизайн, а не баг, который нужно чинить?"
      },
      "choices": [
        { "en": "The two non-atomic steps (publish, then mark sent) can only fail as a duplicate or as a loss; publish-then-mark gives at-least-once, the strictly safer choice, so duplicates are pushed onto idempotent consumers", "ru": "Два неатомарных шага (публикация, затем пометка «отправлено») могут дать сбой только как дубликат или как потеря; «сначала публикация, потом пометка» даёт at-least-once — строго более безопасный выбор, поэтому дубликаты перекладываются на идемпотентных потребителей" },
        { "en": "The broker deduplicates the second publish automatically, so the re-publish is a harmless no-op", "ru": "Брокер автоматически дедуплицирует вторую публикацию, поэтому повторная публикация — безвредный no-op" },
        { "en": "Marking the row sent first would be safer, but the relay simply has a bug in its ordering", "ru": "Сначала помечать строку отправленной было бы безопаснее, но у релея просто баг в порядке действий" },
        { "en": "It is not correct — the relay must use two-phase commit between the DB and the broker", "ru": "Это некорректно — релей обязан использовать двухфазный коммит между БД и брокером" }
      ],
      "answer": 0
    },
    {
      "id": "at-least-once-delivery-q2",
      "type": "mcq",
      "prompt": {
        "en": "Given at-least-once delivery, what is the load-bearing requirement that makes the rest of an event pipeline safe to lean on?",
        "ru": "При доставке at-least-once какое требование является несущим и делает остальной конвейер событий безопасным для опоры на него?"
      },
      "choices": [
        { "en": "Consumers must be idempotent, deduping on a stable event id (e.g. an inbox table or a unique constraint) so a redelivered message produces the same effect once", "ru": "Потребители должны быть идемпотентными, дедуплицируя по стабильному id события (например, через inbox-таблицу или уникальное ограничение), чтобы повторно доставленное сообщение давало тот же эффект один раз" },
        { "en": "Producers must send each message exactly once and never retry on timeout", "ru": "Производители должны отправлять каждое сообщение ровно один раз и никогда не повторять при таймауте" },
        { "en": "The broker must be configured for synchronous replication so no message is ever delivered twice", "ru": "Брокер должен быть настроен на синхронную репликацию, чтобы ни одно сообщение не доставлялось дважды" },
        { "en": "Consumers must process messages strictly in the order the broker delivered them", "ru": "Потребители должны обрабатывать сообщения строго в порядке доставки брокером" }
      ],
      "answer": 0
    },
    {
      "id": "at-least-once-delivery-q3",
      "type": "blanks",
      "prompt": {
        "en": "At-least-once delivery plus an idempotent consumer gives you exactly-once ____ (effects), even though exactly-once delivery over the wire is impossible.",
        "ru": "Доставка at-least-once плюс идемпотентный потребитель дают вам exactly-once ____ (эффекты/обработку), хотя exactly-once доставка по сети невозможна."
      },
      "answer": ["processing", "effects", "обработку", "эффекты"]
    }
  ]
}
