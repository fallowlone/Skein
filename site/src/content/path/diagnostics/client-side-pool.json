{
  "concept": "client-side-pool",
  "items": [
    {
      "id": "client-side-pool-q1",
      "type": "mcq",
      "prompt": {
        "en": "A Node.js API server opens a new pg connection per request. At 50 rps latency is fine. At 200 rps p99 latency spikes and the database logs show 200 idle connections. A junior adds connection pooling and caps the pool at 10. Latency drops but some requests now queue. What does the pool actually fix and what does the cap introduce?",
        "ru": "Node.js API-сервер открывает новое pg-соединение на каждый запрос. При 50 rps задержка в норме. При 200 rps p99 растёт, а в логах базы видно 200 idle-соединений. Джуниор добавляет пул соединений и ограничивает его 10. Задержка падает, но некоторые запросы теперь встают в очередь. Что именно исправляет пул и что вносит ограничение?"
      },
      "choices": [
        {
          "en": "Pooling reduces query parse time; the cap causes lock contention on the pool mutex",
          "ru": "Пул сокращает время разбора запроса; ограничение вызывает конкуренцию за mutex пула"
        },
        {
          "en": "Pooling eliminates per-request TCP+TLS+auth handshake cost (amortised across reuse); the cap means requests beyond pool size queue for a free connection instead of failing immediately",
          "ru": "Пул устраняет стоимость TCP+TLS+auth-хендшейка на каждый запрос (амортизируется при повторном использовании); ограничение означает, что запросы сверх размера пула ждут свободного соединения вместо немедленного отказа"
        },
        {
          "en": "Pooling batches queries into fewer round trips; the cap limits parallelism to one query at a time",
          "ru": "Пул объединяет запросы в меньшее число round trip; ограничение снижает параллелизм до одного запроса за раз"
        },
        {
          "en": "Pooling caches query results in the connection object; the cap forces cache eviction",
          "ru": "Пул кэширует результаты запросов в объекте соединения; ограничение вызывает вытеснение кэша"
        }
      ],
      "answer": 1,
      "irt": { "b": -0.6, "a": 1.6, "c": 0.22 }
    },
    {
      "id": "client-side-pool-q2",
      "type": "mcq",
      "prompt": {
        "en": "A team runs 8 Node.js API pods, each with a client-side pg pool of 50 connections. The Postgres instance has max_connections = 200. Under load the database starts refusing connections. The team proposes raising max_connections to 400. An architect recommends deploying PgBouncer in transaction mode instead. Why is the architect correct?",
        "ru": "Команда запускает 8 Node.js API-подов, каждый с клиентским pg-пулом на 50 соединений. Экземпляр Postgres имеет max_connections = 200. Под нагрузкой база начинает отклонять соединения. Команда предлагает поднять max_connections до 400. Архитектор рекомендует вместо этого развернуть PgBouncer в transaction mode. Почему архитектор прав?"
      },
      "choices": [
        {
          "en": "PgBouncer compresses SQL so fewer bytes travel over the wire",
          "ru": "PgBouncer сжимает SQL, поэтому по сети передаётся меньше байт"
        },
        {
          "en": "Raising max_connections increases Postgres memory (shared_buffers per backend) and scheduler overhead; PgBouncer multiplexes N app connections onto far fewer real Postgres backends, keeping backend count manageable",
          "ru": "Увеличение max_connections повышает потребление памяти Postgres (shared_buffers на каждый backend) и накладные расходы планировщика; PgBouncer мультиплексирует N соединений приложения на значительно меньшее число реальных Postgres-бэкендов, сохраняя их количество управляемым"
        },
        {
          "en": "PgBouncer enables query-level read replicas which reduces primary load",
          "ru": "PgBouncer включает репликацию чтения на уровне запросов, что снижает нагрузку на primary"
        },
        {
          "en": "Client-side pools cannot handle prepared statements; PgBouncer adds that support",
          "ru": "Клиентские пулы не могут обрабатывать prepared statements; PgBouncer добавляет эту поддержку"
        }
      ],
      "answer": 1,
      "irt": { "b": 0.7, "a": 1.8, "c": 0.22 }
    },
    {
      "id": "client-side-pool-q3",
      "type": "blanks",
      "prompt": {
        "en": "When a client-side pool has no idle connections and the wait queue is full, the pool is said to be ____ and new requests are rejected immediately.",
        "ru": "Когда в клиентском пуле нет свободных соединений и очередь ожидания заполнена, говорят, что пул ____ и новые запросы немедленно отклоняются."
      },
      "answer": [
        "exhausted",
        "исчерпан",
        "exhaustion"
      ],
      "irt": { "b": -0.9, "a": 1.4, "c": 0.05 }
    }
  ]
}
