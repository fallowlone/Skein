{
  "concept": "group-by",
  "items": [
    {
      "id": "group-by-q1",
      "type": "mcq",
      "prompt": {
        "en": "A junior writes: `SELECT department, name, COUNT(*) FROM employees GROUP BY department`. The database rejects it. Which rule does this violate?",
        "ru": "Джуниор пишет: `SELECT department, name, COUNT(*) FROM employees GROUP BY department`. База данных отклоняет запрос. Какое правило это нарушает?"
      },
      "choices": [
        {
          "en": "`name` is neither in GROUP BY nor wrapped in an aggregate function, so its value per group is undefined",
          "ru": "`name` не входит в GROUP BY и не обёрнут в агрегатную функцию, поэтому его значение для каждой группы не определено"
        },
        {
          "en": "COUNT(*) cannot be combined with GROUP BY in a single query",
          "ru": "COUNT(*) нельзя комбинировать с GROUP BY в одном запросе"
        },
        {
          "en": "GROUP BY must list all columns in the table, not just one",
          "ru": "GROUP BY должен перечислять все столбцы таблицы, а не только один"
        },
        {
          "en": "SELECT * must be used instead of named columns when using GROUP BY",
          "ru": "При использовании GROUP BY вместо именованных столбцов нужно использовать SELECT *"
        }
      ],
      "answer": 0,
      "irt": { "b": -0.6, "a": 1.6, "c": 0.22 }
    },
    {
      "id": "group-by-q2",
      "type": "mcq",
      "prompt": {
        "en": "A query correctly groups orders by customer and uses HAVING COUNT(*) > 5. A teammate suggests replacing HAVING with WHERE for performance. What is the correct analysis?",
        "ru": "Запрос корректно группирует заказы по клиенту и использует HAVING COUNT(*) > 5. Коллега предлагает заменить HAVING на WHERE для производительности. Какой правильный анализ?"
      },
      "choices": [
        {
          "en": "WHERE filters rows before grouping and cannot reference aggregate results; HAVING filters groups after aggregation — the replacement is semantically invalid",
          "ru": "WHERE фильтрует строки до группировки и не может ссылаться на результаты агрегации; HAVING фильтрует группы после агрегации — замена семантически неверна"
        },
        {
          "en": "WHERE and HAVING are interchangeable for aggregate conditions; WHERE is simply evaluated earlier in the plan",
          "ru": "WHERE и HAVING взаимозаменяемы для агрегатных условий; WHERE просто вычисляется раньше в плане"
        },
        {
          "en": "WHERE COUNT(*) > 5 is valid SQL and will produce identical results with better index use",
          "ru": "WHERE COUNT(*) > 5 является допустимым SQL и даёт идентичные результаты при лучшем использовании индекса"
        },
        {
          "en": "HAVING must always follow ORDER BY; if ORDER BY is absent, WHERE can substitute",
          "ru": "HAVING всегда должен следовать за ORDER BY; если ORDER BY отсутствует, WHERE может заменить его"
        }
      ],
      "answer": 0,
      "irt": { "b": 0.7, "a": 1.8, "c": 0.22 }
    },
    {
      "id": "group-by-q3",
      "type": "blanks",
      "prompt": {
        "en": "After GROUP BY collapses rows into groups, the clause used to filter those groups based on aggregate conditions is called ____.",
        "ru": "После того как GROUP BY сворачивает строки в группы, для фильтрации этих групп по агрегатным условиям используется клауза ____."
      },
      "answer": ["having", "having"],
      "irt": { "b": -1.0, "a": 1.4, "c": 0.05 }
    }
  ]
}
