{
  "concept": "go-http-server",
  "items": [
    {
      "id": "go-http-server-q1",
      "type": "mcq",
      "prompt": {
        "en": "Using the Go 1.22 ServeMux, a service registers `GET /users/{id}` first and `GET /users/new` second. A request arrives for `GET /users/new`. Which handler runs, and why?",
        "ru": "С ServeMux из Go 1.22 сервис регистрирует сначала `GET /users/{id}`, затем `GET /users/new`. Приходит запрос `GET /users/new`. Какой обработчик выполнится и почему?"
      },
      "choices": [
        {
          "en": "The /users/new handler — ServeMux picks the most specific matching pattern regardless of registration order",
          "ru": "Обработчик /users/new — ServeMux выбирает наиболее специфичный совпавший паттерн независимо от порядка регистрации"
        },
        {
          "en": "The /users/{id} handler, because it was registered first",
          "ru": "Обработчик /users/{id}, потому что он зарегистрирован первым"
        },
        {
          "en": "ServeMux panics at startup because the two patterns overlap",
          "ru": "ServeMux паникует при старте, потому что два паттерна пересекаются"
        },
        {
          "en": "The request gets a 405 because two methods conflict",
          "ru": "Запрос получает 405, потому что два метода конфликтуют"
        }
      ],
      "answer": 0
    },
    {
      "id": "go-http-server-q2",
      "type": "mcq",
      "prompt": {
        "en": "A service binds with `http.ListenAndServe(addr, mux)` and its handlers share a plain `map` without a mutex. Under concurrent traffic this is a bug. Why?",
        "ru": "Сервис запускается через `http.ListenAndServe(addr, mux)`, и его обработчики разделяют обычный `map` без мьютекса. При конкурентном трафике это баг. Почему?"
      },
      "choices": [
        {
          "en": "The server spawns one goroutine per connection, so handlers run concurrently and a shared map without synchronization is a data race on the second simultaneous request",
          "ru": "Сервер запускает по одной горутине на соединение, поэтому обработчики выполняются конкурентно, и общий map без синхронизации — это data race на втором одновременном запросе"
        },
        {
          "en": "ServeMux serializes all requests through one goroutine, so the map is fine until you add a second mux",
          "ru": "ServeMux сериализует все запросы через одну горутину, поэтому map в порядке, пока вы не добавите второй mux"
        },
        {
          "en": "Maps in Go are safe for concurrent reads and writes; the real bug is the missing Content-Type header",
          "ru": "Map в Go безопасны для конкурентного чтения и записи; настоящий баг — отсутствующий заголовок Content-Type"
        },
        {
          "en": "The handler must call w.WriteHeader before touching the map, or the map panics",
          "ru": "Обработчик должен вызвать w.WriteHeader перед обращением к map, иначе map паникует"
        }
      ],
      "answer": 0
    },
    {
      "id": "go-http-server-q3",
      "type": "blanks",
      "prompt": {
        "en": "If two 1.22 ServeMux patterns overlap and neither is more specific (e.g. `GET /a/{x}/c` vs `GET /a/b/{y}`), `Handle` ____ at registration rather than silently picking one — failing loud and early.",
        "ru": "Если два паттерна ServeMux из 1.22 пересекаются и ни один не более специфичен (например, `GET /a/{x}/c` против `GET /a/b/{y}`), `Handle` ____ при регистрации, а не выбирает один молча — отказ громкий и ранний."
      },
      "answer": ["panics", "panic", "паникует", "panics at startup"]
    }
  ]
}
