{
  "concept": "constraint",
  "items": [
    {
      "id": "constraint-q1",
      "type": "mcq",
      "prompt": {
        "en": "A competitive problem states n ≤ 1,000,000 with a 1-second limit. Before writing any code, what does that constraint let you eliminate?",
        "ru": "В задаче указано n ≤ 1 000 000 при лимите 1 секунда. Прежде чем писать код, что это ограничение позволяет исключить?"
      },
      "choices": [
        {
          "en": "An O(n²) approach — ~10¹² operations vastly exceeds the ~10⁸-ops/sec budget, so you need O(n) or O(n log n)",
          "ru": "Подход O(n²) — ~10¹² операций намного превышает бюджет ~10⁸ оп/сек, поэтому нужен O(n) или O(n log n)"
        },
        {
          "en": "An O(n log n) approach — at this n it would time out, leaving only strictly O(n)",
          "ru": "Подход O(n log n) — при таком n он не уложится, оставляя строго O(n)"
        },
        {
          "en": "Nothing — the constraint bounds memory, not time, so any time complexity is fine",
          "ru": "Ничего — ограничение задаёт память, а не время, поэтому любая временная сложность подходит"
        },
        {
          "en": "An O(n) approach — a single pass is too slow for a million elements within one second",
          "ru": "Подход O(n) — один проход слишком медленный для миллиона элементов за секунду"
        }
      ],
      "answer": 0,
      "irt": {
        "b": -0.4,
        "a": 1.6,
        "c": 0.22
      }
    },
    {
      "id": "constraint-q2",
      "type": "mcq",
      "prompt": {
        "en": "Why is a Postgres CHECK or FOREIGN KEY constraint considered the schema's first line of defence rather than equivalent application-layer validation?",
        "ru": "Почему ограничение CHECK или FOREIGN KEY в Postgres считают первой линией защиты схемы, а не эквивалентом валидации на уровне приложения?"
      },
      "choices": [
        {
          "en": "The database enforces it on every write regardless of which client or code path inserts the row, so a bug or a second service cannot bypass the invariant",
          "ru": "База применяет его при каждой записи независимо от того, какой клиент или путь кода вставляет строку, поэтому баг или второй сервис не могут обойти инвариант"
        },
        {
          "en": "Constraints make queries faster by letting the planner skip validation at read time",
          "ru": "Ограничения ускоряют запросы, позволяя планировщику пропускать валидацию при чтении"
        },
        {
          "en": "Application validation runs after commit, so it can only detect violations, never prevent them",
          "ru": "Валидация приложения выполняется после commit, поэтому может только обнаруживать нарушения, но не предотвращать их"
        },
        {
          "en": "A CHECK constraint replaces the need for indexes on the constrained column",
          "ru": "Ограничение CHECK устраняет необходимость в индексах на ограниченном столбце"
        }
      ],
      "answer": 0,
      "irt": {
        "b": 0,
        "a": 1.6,
        "c": 0.2
      }
    },
    {
      "id": "constraint-q3",
      "type": "blanks",
      "prompt": {
        "en": "The competitive-programming rule of thumb is that a machine does roughly 10⁸ simple operations per second, so the input ____ tells you the fastest complexity class you must reach.",
        "ru": "Эмпирическое правило спортивного программирования: машина выполняет порядка 10⁸ простых операций в секунду, поэтому ____ входных данных подсказывает, какого самого быстрого класса сложности нужно достичь."
      },
      "answer": [
        "constraint",
        "constraints",
        "size",
        "n"
      ],
      "irt": {
        "b": -0.2,
        "a": 0.9,
        "c": 0.06
      }
    }
  ]
}
