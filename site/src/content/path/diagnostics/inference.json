{
  "concept": "inference",
  "items": [
    {
      "id": "inference-q1",
      "type": "mcq",
      "prompt": {
        "en": "For `function box<T = string>(value: T): { value: T }`, what is the type of `box(42)`, and what does it reveal about how the checker resolves T?",
        "ru": "Для `function box<T = string>(value: T): { value: T }` каков тип `box(42)` и что это говорит о том, как checker разрешает T?"
      },
      "choices": [
        {
          "en": "{ value: number } — inference from the argument wins; the default only applies when nothing can be inferred",
          "ru": "{ value: number } — вывод из аргумента побеждает; дефолт применяется только тогда, когда вывести ничего нельзя"
        },
        {
          "en": "{ value: string } — the `= string` default overrides the inferred argument type",
          "ru": "{ value: string } — дефолт `= string` перекрывает выведенный тип аргумента"
        },
        {
          "en": "{ value: string | number } — the default and the inferred type are unioned together",
          "ru": "{ value: string | number } — дефолт и выведенный тип объединяются в union"
        },
        {
          "en": "An error, because supplying a default and an argument that disagree is a conflict",
          "ru": "Ошибка, потому что дефолт и аргумент, которые расходятся, — это конфликт"
        }
      ],
      "answer": 0,
      "irt": {
        "b": 1.2,
        "a": 1.8,
        "c": 0.22
      }
    },
    {
      "id": "inference-q2",
      "type": "mcq",
      "prompt": {
        "en": "In LLM integration, 'inference' is the compute step that produces output. Why can that step return a confident but factually wrong answer?",
        "ru": "В интеграции LLM «инференс» — это вычислительный шаг, порождающий вывод. Почему этот шаг может вернуть уверенный, но фактически неверный ответ?"
      },
      "choices": [
        {
          "en": "It predicts the next token by sampling from a probability distribution rather than retrieving stored facts, so plausible-sounding but baseless sequences can be generated",
          "ru": "Он предсказывает следующий токен, сэмплируя из распределения вероятностей, а не извлекая хранимые факты, поэтому могут порождаться правдоподобные, но беспочвенные последовательности"
        },
        {
          "en": "It runs too fast on the GPU to verify its own output before returning it",
          "ru": "Он выполняется на GPU слишком быстро, чтобы проверить собственный вывод перед возвратом"
        },
        {
          "en": "The context window is too small to hold every fact, so missing facts get fabricated",
          "ru": "Контекстное окно слишком мало, чтобы вместить все факты, поэтому недостающие факты выдумываются"
        },
        {
          "en": "Temperature was left above zero, and any non-zero temperature makes answers factually unreliable",
          "ru": "Температура осталась выше нуля, а любая ненулевая температура делает ответы фактически ненадёжными"
        }
      ],
      "answer": 0,
      "irt": {
        "b": 0.4,
        "a": 1.4,
        "c": 0.25
      }
    },
    {
      "id": "inference-q3",
      "type": "blanks",
      "prompt": {
        "en": "TypeScript resolves a type parameter in strict order: an explicit type argument first, then ____ from the call arguments, and only then the default.",
        "ru": "TypeScript разрешает типовой параметр в строгом порядке: сначала явный типовой аргумент, затем ____ из аргументов вызова, и только потом дефолт."
      },
      "answer": [
        "inference",
        "infer",
        "вывод",
        "инференс"
      ],
      "irt": {
        "b": -0.2,
        "a": 0.9,
        "c": 0.06
      }
    }
  ]
}
