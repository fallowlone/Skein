{
  "concept": "cartesian-product",
  "items": [
    {
      "id": "cartesian-product-q1",
      "type": "mcq",
      "prompt": {
        "en": "A reporting query returned 4.2 billion rows and pinned a CPU because of one missing word in the ON clause. Holding the 'product then filter' model, why does the output row count of an INNER JOIN equal neither |A| nor |B|?",
        "ru": "Отчётный запрос вернул 4,2 млрд строк и загрузил CPU из-за одного пропущенного слова в ON. Держа модель «продукт, затем фильтр», почему число строк на выходе INNER JOIN не равно ни |A|, ни |B|?"
      },
      "choices": [
        {
          "en": "It is always max(|A|, |B|) because the engine keeps the larger table whole",
          "ru": "Это всегда max(|A|, |B|), потому что движок сохраняет большую таблицу целиком"
        },
        {
          "en": "It is |A| + |B| because matching rows are concatenated",
          "ru": "Это |A| + |B|, потому что совпавшие строки конкатенируются"
        },
        {
          "en": "It is the count of pairs from the |A| x |B| product that survive the ON predicate, which depends entirely on the relationship",
          "ru": "Это число пар из продукта |A| x |B|, переживших предикат ON, и оно полностью зависит от связи"
        },
        {
          "en": "It is fixed by the smaller table because the join collapses to it",
          "ru": "Оно задаётся меньшей таблицей, потому что join схлопывается к ней"
        }
      ],
      "answer": 2
    },
    {
      "id": "cartesian-product-q2",
      "type": "mcq",
      "prompt": {
        "en": "Which statement is logically identical to: FROM users u JOIN orders o ON o.user_id = u.id?",
        "ru": "Какой оператор логически идентичен: FROM users u JOIN orders o ON o.user_id = u.id?"
      },
      "choices": [
        {
          "en": "FROM users u, orders o with no WHERE — the comma already restricts to matches",
          "ru": "FROM users u, orders o без WHERE — запятая уже ограничивает совпадениями"
        },
        {
          "en": "Two separate SELECTs whose result rows are concatenated vertically",
          "ru": "Два отдельных SELECT, результаты которых конкатенируются вертикально"
        },
        {
          "en": "FROM users u LEFT JOIN orders o ON true — every left row kept regardless of match",
          "ru": "FROM users u LEFT JOIN orders o ON true — каждая левая строка сохраняется независимо от совпадения"
        },
        {
          "en": "FROM users u CROSS JOIN orders o WHERE o.user_id = u.id — the product, then filtered by the predicate",
          "ru": "FROM users u CROSS JOIN orders o WHERE o.user_id = u.id — продукт, затем отфильтрованный предикатом"
        }
      ],
      "answer": 3
    },
    {
      "id": "cartesian-product-q3",
      "type": "blanks",
      "prompt": {
        "en": "Every join begins as the Cartesian ____ of two tables — |A| x |B| candidate pairs — and the ON clause is just a filter applied to it.",
        "ru": "Каждый join начинается как декартово ____ двух таблиц — |A| x |B| кандидатных пар — и предложение ON это лишь применённый к нему фильтр."
      },
      "answer": [
        "product",
        "произведение"
      ]
    }
  ]
}
