{
  "concept": "accumulator",
  "items": [
    {
      "id": "accumulator-q1",
      "type": "mcq",
      "prompt": {
        "en": "In the accumulate traversal pattern (sum, product, count), what is the defining structural property that makes it correct?",
        "ru": "В паттерне обхода с накоплением (сумма, произведение, счёт) какое определяющее структурное свойство делает его корректным?"
      },
      "choices": [
        { "en": "An accumulator is initialized before the loop, updated once per element, and read after the loop — its initial value is the identity for the combining operation (0 for sum, 1 for product)", "ru": "Аккумулятор инициализируется перед циклом, обновляется один раз на элемент и читается после цикла — его начальное значение является нейтральным для операции объединения (0 для суммы, 1 для произведения)" },
        { "en": "The accumulator must be reset to zero inside the loop body on every iteration", "ru": "Аккумулятор должен сбрасываться в ноль внутри тела цикла на каждой итерации" },
        { "en": "The array must be sorted so the accumulator combines values in order", "ru": "Массив должен быть отсортирован, чтобы аккумулятор объединял значения по порядку" },
        { "en": "An accumulator only works with early exit, returning as soon as a match is found", "ru": "Аккумулятор работает только с ранним выходом, возвращая значение, как только найдено совпадение" }
      ],
      "answer": 0
    },
    {
      "id": "accumulator-q2",
      "type": "mcq",
      "prompt": {
        "en": "In V8's Ignition interpreter, what role does the accumulator register play in the bytecode dispatch loop?",
        "ru": "В интерпретаторе Ignition движка V8 какую роль играет регистр-аккумулятор в цикле диспетчеризации байткода?"
      },
      "choices": [
        { "en": "It is a single shared register that threads intermediate values between handlers — a value one bytecode produces sits right where the next bytecode expects it, avoiding explicit operand passing", "ru": "Это единственный общий регистр, протягивающий промежуточные значения между обработчиками — значение, произведённое одним байткодом, лежит ровно там, где его ждёт следующий, избегая явной передачи операндов" },
        { "en": "It counts how many times each function is called so the JIT knows when to compile", "ru": "Он считает, сколько раз вызвана каждая функция, чтобы JIT знал, когда компилировать" },
        { "en": "It stores the return address for the currently executing bytecode handler", "ru": "Он хранит адрес возврата для текущего выполняемого обработчика байткода" },
        { "en": "It holds the entire feedback vector used for type-feedback collection", "ru": "Он содержит весь вектор обратной связи, используемый для сбора обратной связи по типам" }
      ],
      "answer": 0
    },
    {
      "id": "accumulator-q3",
      "type": "blanks",
      "prompt": {
        "en": "To sum an array with the accumulate pattern, you initialize the accumulator to ____ before the loop, add each element to it inside the loop, and return it afterward.",
        "ru": "Чтобы просуммировать массив паттерном накопления, вы инициализируете аккумулятор значением ____ перед циклом, прибавляете к нему каждый элемент внутри цикла и возвращаете его после."
      },
      "answer": ["0", "zero", "ноль"]
    }
  ]
}
