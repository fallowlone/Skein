{
  "concept": "native-histograms",
  "items": [
    {
      "id": "native-histograms-q1",
      "type": "mcq",
      "prompt": {
        "en": "A team's service latency distribution shifted so a slow path now dominates the 1-5 s range, and their classic histogram's fixed buckets can no longer resolve p99 there without a redeploy. How do native (exponential) histograms remove this problem?",
        "ru": "Распределение задержек сервиса сместилось так, что медленный путь теперь доминирует в диапазоне 1-5 с, и фиксированные бакеты классической гистограммы больше не разрешают там p99 без передеплоя. Как нативные (экспоненциальные) гистограммы убирают эту проблему?"
      },
      "choices": [
        {
          "en": "They place every observation in a dynamically sized bucket at ~1% relative precision across the whole range, so no static boundary list is needed and p99 accuracy is uniform everywhere",
          "ru": "Они помещают каждое наблюдение в динамически масштабируемый бакет с ~1% относительной точностью по всему диапазону, поэтому статический список границ не нужен, а точность p99 однородна везде"
        },
        {
          "en": "They store one pre-computed p99 per replica, which is always exact after aggregation",
          "ru": "Они хранят один предвычисленный p99 на реплику, который всегда точен после агрегации"
        },
        {
          "en": "They sample 1% of requests and keep full latency for those, dropping the rest",
          "ru": "Они сэмплируют 1% запросов и хранят полную задержку для них, отбрасывая остальные"
        },
        {
          "en": "They disable histogram_quantile and compute the mean instead, which never needs buckets",
          "ru": "Они отключают histogram_quantile и считают среднее, которому бакеты не нужны"
        }
      ],
      "answer": 0
    },
    {
      "id": "native-histograms-q2",
      "type": "mcq",
      "prompt": {
        "en": "Replica A serves 100 requests all at 100 ms; replica B serves 100 requests all at 1000 ms. A summary-based dashboard averages their per-replica p99s and reports 550 ms. Why is this wrong, and what does the histogram approach do instead?",
        "ru": "Реплика A обслуживает 100 запросов по 100 мс; реплика B — 100 запросов по 1000 мс. Дашборд на основе summary усредняет их пореплицированные p99 и сообщает 550 мс. Почему это неверно и что делает подход на гистограммах?"
      },
      "choices": [
        {
          "en": "Percentiles are not additive — the true p99 of the merged 200 requests is 1000 ms; histograms emit per-replica bucket counts that are summed by (le) and fed to histogram_quantile to get the real fleet-wide percentile",
          "ru": "Перцентили не аддитивны — истинный p99 объединённых 200 запросов равен 1000 мс; гистограммы отдают пореплицированные счётчики бакетов, которые суммируются by (le) и подаются в histogram_quantile для реального перцентиля по всему флоту"
        },
        {
          "en": "550 ms is correct because the replicas had equal traffic; the histogram returns the same number",
          "ru": "550 мс верно, потому что у реплик был равный трафик; гистограмма вернёт то же число"
        },
        {
          "en": "The summary is just less precise; a histogram improves per-replica accuracy but the average stays valid",
          "ru": "Summary просто менее точна; гистограмма повышает пореплицированную точность, но среднее остаётся валидным"
        },
        {
          "en": "Summaries cannot be queried in PromQL at all, so any number they produce is meaningless",
          "ru": "Summary вообще нельзя запрашивать в PromQL, поэтому любое их число бессмысленно"
        }
      ],
      "answer": 0
    },
    {
      "id": "native-histograms-q3",
      "type": "blanks",
      "prompt": {
        "en": "A native histogram needs no manual bucket tuning because it uses a ____ bucket scale at fixed relative precision instead of a static list of boundaries.",
        "ru": "Нативной гистограмме не нужна ручная настройка бакетов, потому что она использует ____ шкалу бакетов с фиксированной относительной точностью вместо статического списка границ."
      },
      "answer": [
        "logarithmic",
        "exponential",
        "логарифмическую",
        "экспоненциальную"
      ]
    }
  ]
}
