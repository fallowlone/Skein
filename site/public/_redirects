# Cloudflare Pages edge redirects (evaluated before static assets).
# Real 302 at the edge for the site root — avoids the Astro static
# meta-refresh "Redirecting from / to /en/" flash page.
/  /en/  302
