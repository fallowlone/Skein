{
  "name": "awesome-everything",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "cd site && bun run build",
    "dev:functions": "wrangler pages dev site/dist --d1 DB --kv SESSIONS --compatibility-date 2024-11-01",
    "deploy:prod": "bun run build && wrangler pages deploy site/dist --project-name=awesome-everything --branch=main"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
